# CodeBook — Платформа для изучения программирования

## Обзор

Веб-платформа для изучения языков программирования на Vue 3. Начинаем с Java — полный курс от основ до фреймворков (50+ модулей). Контент разжёвывается максимально понятно с аналогиями, множеством примеров и практическими заданиями.

## Целевая аудитория

Смешанная: от полных новичков до продвинутых. Контент от "что такое переменная" до фреймворков и многопоточности.

## Стек технологий

- **Vue 3** (Composition API) + **Vite**
- **Vue Router** — клиентский роутинг
- **Pinia** — стейт-менеджмент
- **highlight.js** — подсветка синтаксиса
- **localStorage** — хранение прогресса (MVP)

## Архитектура

Чистый SPA без бэкенда. Весь контент хранится в JS-файлах внутри проекта. Прогресс пользователя в localStorage с возможностью добавить серверную синхронизацию позже.

## Структура проекта

```
src/
├── assets/              # стили, шрифты, изображения
├── components/
│   ├── layout/          # AppHeader, AppSidebar, AppFooter
│   ├── course/          # CourseCard, ModuleCard, LessonContent, CodeBlock, NoteBlock
│   ├── practice/        # TaskCard, SolutionReveal, HintBlock
│   └── ui/              # ProgressBar, DifficultyBadge, Breadcrumbs, LessonNav
├── views/
│   ├── HomePage.vue     # список курсов (карточки)
│   ├── CoursePage.vue   # список модулей курса
│   ├── ModulePage.vue   # уроки модуля (с сайдбаром)
│   ├── LessonPage.vue   # содержимое урока / практика
│   └── NotFoundPage.vue # страница 404
├── stores/
│   ├── progress.js      # прогресс пользователя
│   └── course.js        # данные курсов
├── data/
│   └── java/
│       ├── index.js     # метаданные курса + список модулей
│       └── modules/     # module-1.js, module-2.js, ...
├── router/
│   └── index.js
└── App.vue
```

## Роутинг

| Путь | Страница | Описание |
|------|----------|----------|
| `/` | HomePage | Список языков (карточки курсов) |
| `/course/:lang` | CoursePage | Модули курса (карточки модулей) |
| `/course/:lang/module/:moduleId` | ModulePage | Уроки модуля (сайдбар + список) |
| `/course/:lang/module/:moduleId/lesson/:lessonId` | LessonPage | Контент урока или практика |
| `/:pathMatch(.*)*` | NotFoundPage | Страница 404 |

Ленивая загрузка: каждый модуль данных — отдельный чанк. Если маршрут ведёт к несуществующему курсу/модулю/уроку — редирект на NotFoundPage.

## Структура данных

### Курс (data/java/index.js)

```js
export default {
  id: 'java',
  title: 'Java',
  icon: '☕',
  description: 'Полный курс Java от основ до продвинутого уровня',
  color: '#f89820',
  modules: [
    { id: 1, title: 'Введение в Java', icon: '🚀', totalLessons: 5 },
    { id: 2, title: 'Переменные и типы данных', icon: '📦', totalLessons: 8 },
    // ... totalLessons needed for progress calculation on homepage without loading every module
  ]
}
```

### Модуль (data/java/modules/module-1.js)

```js
export default {
  id: 1,
  title: 'Введение в Java',
  description: 'Что такое Java, зачем нужна, как установить',
  lessons: [
    {
      id: 1,
      title: 'Что такое Java?',
      type: 'theory',
      content: [
        { type: 'text', value: '...' },
        { type: 'heading', value: '...' },
        { type: 'code', language: 'java', value: '...' },
        { type: 'note', value: '...' },
        { type: 'warning', value: '...' },
        { type: 'tip', value: '...' },
        { type: 'image', src: '...', alt: '...' },
        { type: 'list', items: ['...'] }
      ]
    },
    {
      id: 2,
      title: 'Практика: Первая программа',
      type: 'practice',
      difficulty: 'easy',        // easy | medium | hard
      description: 'Описание задачи',
      requirements: ['Требование 1', 'Требование 2'],
      expectedOutput: 'Ожидаемый вывод программы',
      hint: 'Текст подсказки',
      solution: 'Код решения',
      explanation: 'Разбор решения'
    }
  ]
}
```

### Типы контент-блоков

| Тип | Описание | Визуал |
|-----|----------|--------|
| `text` | Обычный текст, аналогии | Стандартный параграф |
| `heading` | Подзаголовок внутри урока | h3 |
| `code` | Блок кода с подсветкой | Тёмный фон, кнопка копирования |
| `note` | Важная заметка | Жёлтый блок с иконкой 📝 |
| `warning` | Частая ошибка | Красный блок с иконкой ⚠️ |
| `tip` | Полезный совет | Зелёный блок с иконкой 💡 |
| `image` | Картинка/схема | Картинка с alt-текстом |
| `list` | Маркированный список | ul > li |

## Система прогресса

### Хранилище (Pinia + localStorage)

Единая карта завершённых уроков. Ключ: `{courseId}:{moduleId}:{lessonId}` — один формат для всех типов уроков (theory и practice).

```js
{
  completed: {
    'java:1:1': true,   // теория — нажал "Прочитано"
    'java:1:2': true,   // практика — нажал "Решил!"
    'java:2:1': true
  }
}
```

### Правила подсчёта

- **Прогресс модуля:** количество завершённых уроков / общее количество уроков в модуле (включая и theory, и practice)
- **Модуль считается завершённым:** когда 100% уроков пройдено
- **Прогресс курса:** количество завершённых модулей / общее количество модулей (в процентах)

### Что отслеживается

- Прочитанные уроки — кнопка "Прочитано" внизу урока (type: theory)
- Решённые задания — кнопка "Решил!" (type: practice)
- Прогресс по модулю — вычисляется: X из Y уроков
- Общий прогресс курса — вычисляется: процент завершённых модулей
- Кнопка "Сбросить прогресс" на главной странице (в футере или в выпадающем меню профиля)

### Отображение прогресса

- Главная: процент по курсу на карточке
- CoursePage: прогресс-бар на каждом модуле
- Сайдбар: галочки ✅ напротив пройденных уроков
- Активный урок выделен цветом

## Визуальный стиль

Светлая минималистичная тема:

- **Фон:** #f8f9fb (страницы), #fff (карточки, сайдбар)
- **Текст:** #1a1a2e (заголовки), #444 (основной), #999 (вторичный)
- **Акцент:** #667eea (ссылки, активный урок)
- **Прогресс:** #4caf50 (зелёный)
- **Практика:** #f5a623 (оранжевый)
- **Код:** тёмная тема (#1e1e2e фон) с подсветкой Dracula-стиль
- **Шрифт кода:** JetBrains Mono или системный monospace
- **Скругления:** 8-12px на карточках, 4px на кнопках
- **Тени:** мягкие (0 2px 8px rgba(0,0,0,0.06))

## Компоненты

| Компонент | Назначение |
|-----------|-----------|
| `AppHeader` | Логотип, breadcrumbs (кнопка "Войти" скрыта в MVP) |
| `AppSidebar` | Дерево уроков модуля с прогрессом |
| `CourseCard` | Карточка языка на главной |
| `ModuleCard` | Карточка модуля на странице курса |
| `LessonContent` | Рендерит контент-блоки по типу |
| `CodeBlock` | Код с highlight.js + копирование |
| `NoteBlock` | Заметки/советы/предупреждения |
| `TaskCard` | Практическое задание с требованиями |
| `SolutionReveal` | Раскрывающееся решение с разбором |
| `HintBlock` | Раскрывающаяся подсказка |
| `ProgressBar` | Прогресс-бар |
| `LessonNav` | Навигация "Назад / Прочитано — Далее" |
| `DifficultyBadge` | Бейдж сложности (Лёгкая/Средняя/Сложная) |

## Навигация

- **Главная:** карточки курсов (языков) с прогрессом
- **Страница курса:** карточки модулей с прогрессом
- **Внутри модуля:** сайдбар слева с деревом уроков, контент справа
- **Breadcrumbs:** CodeBook › Java › Модуль › Урок
- **Навигация внизу урока:** "← Предыдущий" / "Прочитано — Далее →"
- **Мобильная версия:** сайдбар в бургер-меню

## Практические задания

Формат самопроверки:

1. Пользователь видит задачу, требования, ожидаемый вывод
2. Решает у себя локально в IDE
3. Может раскрыть подсказку
4. Нажимает "Показать решение" — видит эталонный код + разбор
5. Нажимает "Решил!" — засчитывается в прогресс
6. Или "Не получилось — пройти теорию заново" — возврат к теории

Уровни сложности: Лёгкая (зелёный), Средняя (оранжевый), Сложная (красный).

## Авторизация (будущее, не MVP)

- На старте: всё в localStorage, кнопка "Войти" скрыта
- Позже: опциональная регистрация
- При логине: localStorage мержится с серверными данными
- Бэкенд: REST API (Node/Express или Firebase)

## Деплой

Локальная разработка на старте. Для будущего деплоя — любой статический хостинг (Vercel, Netlify, GitHub Pages). Vue Router в режиме `history` — потребуется fallback на `index.html` на хостинге.

## SEO

Не приоритет для MVP (SPA без SSR). Если понадобится — миграция на Nuxt для SSG/SSR.

## MVP — первый релиз

- Главная страница с карточкой Java (другие языки: "Скоро")
- Курс Java: первые 3-5 модулей с полным контентом
- Система прогресса в localStorage
- Адаптивная вёрстка (desktop + mobile)
- Без авторизации (кнопка "Войти" скрыта)
- Страница 404 для несуществующих маршрутов
- Кнопка "Сбросить прогресс" в футере

## Язык интерфейса

Русский.
