export default {
  id: 4,
  title: 'Кеширование',
  description: 'CDN, Redis, Memcached. Стратегии кеширования: write-through, write-back, cache-aside, read-through. Инвалидация кеша и распространённые проблемы.',
  lessons: [
    {
      id: 1,
      title: 'Зачем нужен кеш: базовые концепции',
      type: 'theory',
      description: 'Иерархия хранилищ по скорости (L1 Cache → HDD → сеть), принцип Pareto для кеша (20% данных = 80% запросов) и что стоит кешировать.',
      solution: 'Иерархия: in-memory ~50нс, Redis ~1мс, SSD ~100мкс, HDD ~10мс, сеть ~0.5-150мс. Кешировать: дорогие SQL JOIN, внешние API, сессии, конфигурации, агрегаты. Не кешировать: часто меняющиеся данные, уникальные per-user данные с требованием актуальности.',
      content: [
        { type: 'text', value: 'Кеш — это быстрое хранилище промежуточных результатов. Вместо повторного выполнения дорогой операции (запрос к БД, HTTP-вызов) — берём из кеша.' },
        { type: 'heading', value: 'Иерархия хранилищ по скорости' },
        { type: 'text', value: 'L1 Cache (CPU): ~0.5 нс\nL2 Cache (CPU): ~7 нс\nRAM: ~100 нс\nSSD: ~100 мкс\nHDD: ~10 мс\nСеть (Local DC): ~0.5 мс\nСеть (Internet): ~150 мс\n\nRedis (in-memory) = ~1 мс (сеть к Redis серверу)\nПрямо в памяти приложения = ~50 нс\n\nКеш в памяти приложения в 1000+ раз быстрее запроса к БД!' },
        { type: 'heading', value: 'Принцип Pareto для кеша' },
        { type: 'text', value: '20% данных используются в 80% запросов. Кешируя только эти 20%, мы снимаем 80% нагрузки с БД.' },
        { type: 'heading', value: 'Что кешировать' },
        { type: 'list', value: [
          'Результаты дорогих SQL запросов (JOIN нескольких таблиц)',
          'Внешние API ответы (курс валют, погода)',
          'Сессии пользователей',
          'Конфигурации и Feature Flags',
          'Агрегированные данные (статистика, счётчики)',
          'HTML-страницы или их фрагменты'
        ]},
        { type: 'note', value: 'Кешировать нужно не всё. Часто меняющиеся данные, уникальные для каждого пользователя данные, данные с высокими требованиями к актуальности — плохие кандидаты для кеша.' }
      ]
    },
    {
      id: 2,
      title: 'Redis: возможности и структуры данных',
      type: 'theory',
      description: 'Шесть структур данных Redis: String, Hash, List, Set, Sorted Set, Bitmap — с паттернами использования, TTL и политиками вытеснения (LRU/LFU).',
      solution: 'String → кеш/счётчики; Hash → профили объектов; List → очереди (LPUSH/RPOP); Set → уникальные теги/лайки; Sorted Set → рейтинги/приоритеты; Bitmap → компактные флаги активности. TTL: SET key val EX 3600. Eviction: LRU (давно не читали) или LFU (редко читали). Redis: ~100K RPS однопоточно.',
      content: [
        { type: 'text', value: 'Redis (Remote Dictionary Server) — in-memory хранилище данных с богатым набором структур данных. Самый популярный кеш в индустрии.' },
        { type: 'heading', value: 'Структуры данных Redis' },
        { type: 'text', value: 'String: SET user:1:name "Alice" / GET user:1:name\n  → Простые значения, счётчики (INCR), кеш HTML\n\nHash: HSET user:1 name "Alice" age 30\n  → Объекты, профили пользователей\n\nList: LPUSH queue "task1" / RPOP queue\n  → Очереди задач, последние N элементов\n\nSet: SADD tags "python" "redis"\n  → Уникальные значения, теги, "кто лайкнул"\n\nSorted Set: ZADD leaderboard 1500 "Alice"\n  → Рейтинги, очереди с приоритетом\n\nBitmap: SETBIT active:2024-01-01 userId 1\n  → Компактное хранение булевых флагов (активность пользователей)' },
        { type: 'heading', value: 'TTL и инвалидация' },
        { type: 'text', value: 'SET session:abc123 "{...data...}" EX 3600  → Ключ удалится через 3600 секунд (1 час)\n\nExpiry политики Redis при нехватке памяти:\n- LRU (Least Recently Used): удалять давно не использованные ключи\n- LFU (Least Frequently Used): удалять редко используемые\n- allkeys-random: удалять случайные ключи' },
        { type: 'tip', value: 'Redis — однопоточный, но невероятно быстрый (~100,000 RPS на одном ядре). Для большей пропускной способности: Redis Cluster (шардирование) или несколько экземпляров Redis.' }
      ]
    },
    {
      id: 3,
      title: 'Стратегия Cache-Aside (Lazy Loading)',
      type: 'theory',
      description: 'Cache-Aside: приложение само управляет кешем — при промахе загружает из БД, при записи инвалидирует кеш. Алгоритм чтения и записи с race condition.',
      solution: 'Read: redis.get(key) → hit → return; miss → db.query() → redis.setex(key, 3600, val) → return. Write: db.update() → redis.del(key) (инвалидировать, не обновлять!). Плюсы: только нужное кешируется, устойчив к падению кеша. Минус: cold start, race condition.',
      content: [
        { type: 'text', value: 'Cache-Aside — самая распространённая стратегия. Приложение само управляет кешем: читает из кеша, при промахе идёт в БД и обновляет кеш.' },
        { type: 'heading', value: 'Алгоритм Cache-Aside (чтение)' },
        { type: 'text', value: 'Псевдокод:\n\nfunc getUserById(id):\n  // 1. Проверить кеш\n  cached = redis.get("user:" + id)\n  if cached != null:\n    return cached  // Cache Hit\n\n  // 2. Cache Miss: идём в БД\n  user = db.query("SELECT * FROM users WHERE id = ?", id)\n\n  // 3. Положить в кеш на 1 час\n  redis.setex("user:" + id, 3600, serialize(user))\n\n  return user' },
        { type: 'heading', value: 'Алгоритм Cache-Aside (запись)' },
        { type: 'text', value: 'Псевдокод:\n\nfunc updateUser(id, data):\n  // 1. Обновить в БД\n  db.execute("UPDATE users SET ... WHERE id = ?", id, data)\n\n  // 2. Инвалидировать кеш (НЕ обновлять!)\n  redis.del("user:" + id)\n  // При следующем чтении данные будут загружены из БД' },
        { type: 'heading', value: 'Плюсы и минусы' },
        { type: 'list', value: [
          'Плюс: кешируется только то, что реально запрашивается (lazy)',
          'Плюс: устойчивость к отказу кеша (просто идём в БД)',
          'Минус: первый запрос всегда промах (cold start)',
          'Минус: возможна race condition при параллельной записи'
        ]},
        { type: 'note', value: 'Cache-Aside — стандарт для большинства CRUD-приложений. Начинайте с него. Race condition решается: удалять кеш после записи в БД (не до), или использовать distributed lock.' }
      ]
    },
    {
      id: 4,
      title: 'Write-Through и Write-Back стратегии',
      type: 'theory',
      description: 'Write-Through: запись в кеш и БД одновременно (всегда актуально, но медленнее). Write-Back: только в кеш + асинхронный flush в БД (быстро, но риск потери данных).',
      solution: 'Write-Through: db.write() + cache.write() → кеш актуален, задержка = БД+кеш, лишние write. Write-Back: cache.write() + queue → асинхронный db.write() → быстро, батчинг. Применение: Write-Through → финансовые данные; Write-Back → счётчики просмотров, метрики (допустима потеря).',
      content: [
        { type: 'text', value: 'Два подхода к синхронизации кеша и базы данных при записи.' },
        { type: 'heading', value: 'Write-Through: запись в кеш и БД одновременно' },
        { type: 'text', value: 'Алгоритм:\nfunc write(key, value):\n  db.write(key, value)    // 1. Записать в БД\n  cache.write(key, value) // 2. Записать в кеш\n  return success\n\nПлюсы:\n- Кеш всегда актуален (нет stale data)\n- Нет cache miss при чтении после записи\n\nМинусы:\n- Каждая запись → 2 операции (медленнее)\n- Записываем в кеш данные, которые могут никогда не прочитаться\n- Задержка записи = задержка БД + задержка кеша' },
        { type: 'heading', value: 'Write-Back (Write-Behind): запись только в кеш, асинхронно в БД' },
        { type: 'text', value: 'Алгоритм:\nfunc write(key, value):\n  cache.write(key, value)  // 1. Быстро записать в кеш\n  queue.add(key, value)    // 2. В очередь для последующей записи в БД\n  return success           // Отдаём ответ немедленно!\n\n// В фоне (асинхронно):\nfunc flushToDB():\n  while queue not empty:\n    db.write(queue.dequeue())\n\nПлюсы:\n- Очень быстрая запись (нет ожидания БД)\n- Батчинг: несколько обновлений одного ключа → одна запись в БД\n\nМинусы:\n- Риск потери данных: если кеш упал до flush → данные потеряны\n- Сложность реализации' },
        { type: 'tip', value: 'Write-Through: для финансовых данных, где потеря недопустима. Write-Back: для метрик, счётчиков просмотров, где небольшая потеря терпима, но нужна высокая скорость записи. Пример: счётчик просмотров YouTube — не страшно потерять 100 просмотров, важна скорость.' }
      ]
    },
    {
      id: 5,
      title: 'CDN: Content Delivery Network',
      type: 'theory',
      description: 'CDN (POP Edge-серверы по всему миру): Pull CDN (lazy, при первом запросе) vs Push CDN (заранее загружаем). Снижение latency с 200мс до 10мс и нагрузки на origin на 60–90%.',
      solution: 'Pull CDN (CloudFront, Cloudflare): кеш при первом запросе → origin. Подходит: много файлов, нечасто запрашиваемых. Push CDN: загружаем заранее, редко меняемый контент. Кешировать: JS/CSS/шрифты/изображения/видео. Бонус: DDoS-защита (CDN поглощает трафик).',
      content: [
        { type: 'text', value: 'CDN — сеть серверов, распределённых по всему миру, которые хранят копии статического контента рядом с пользователями.' },
        { type: 'heading', value: 'Как работает CDN' },
        { type: 'text', value: 'Без CDN:\n[Пользователь в Сингапуре] → [Сервер в США] → ответ через 200 мс\n\nС CDN:\n[Пользователь в Сингапуре] → [CDN Edge в Сингапуре] → ответ через 10 мс\n\nCDN Edge (POP — Point of Presence) хранит кеш файлов.\nЕсли файла нет → идёт к Origin серверу → получает → кеширует → отдаёт.\nСледующий запрос → отдаёт из локального кеша.' },
        { type: 'heading', value: 'Pull CDN vs Push CDN' },
        { type: 'text', value: 'Pull CDN (ленивый):\n- Контент загружается на CDN при первом запросе\n- Если файла нет в кеше → CDN "тянет" его с origin\n- Подходит для: большое количество файлов, нечасто запрашиваемых\n- Примеры: CloudFront, Cloudflare\n\nPush CDN (активный):\n- Вы сами загружаете файлы на CDN заранее\n- CDN хранит их, пока вы не удалите\n- Подходит для: небольшое число часто запрашиваемых файлов\n- Например: статичный сайт, который редко меняется' },
        { type: 'heading', value: 'Что кешировать в CDN' },
        { type: 'list', value: [
          'Статические файлы: JS, CSS, шрифты, иконки',
          'Изображения и видео',
          'Статичные HTML страницы',
          'API ответы с Cache-Control заголовком (для публичных, редко меняющихся данных)'
        ]},
        { type: 'note', value: 'CDN — первый шаг к оптимизации для глобальной аудитории. Помимо скорости даёт защиту от DDoS (CDN поглощает трафик) и снижает нагрузку на origin-серверы на 60–90%.' }
      ]
    },
    {
      id: 6,
      title: 'Проблемы кеширования: thundering herd, cache penetration, hotspots',
      type: 'theory',
      description: 'Четыре классические проблемы: Thundering Herd (все в БД при истечении кеша), Cache Penetration (несуществующие ключи), Cache Avalanche (массовое истечение TTL), Hot Key.',
      solution: 'Thundering Herd: distributed lock или TTL jitter (+random(0,300)с). Cache Penetration: Bloom Filter (нет → сразу 404) или кешировать NULL на 60с. Cache Avalanche: TTL = base + random(0, base*0.1). Hot Key: реплицировать ключ на несколько нод или local in-memory cache для топ-N ключей.',
      content: [
        { type: 'text', value: 'Кеш — мощный инструмент, но с рядом классических проблем, которые нужно знать.' },
        { type: 'heading', value: 'Thundering Herd (Стадо буйволов)' },
        { type: 'text', value: 'Проблема: кеш для популярного ключа истёк. Тысячи запросов одновременно идут в БД за одними и теми же данными. БД перегружена.\n\nРешения:\n1. Cache Lock: первый запрос берёт distributed lock, загружает данные, остальные ждут\n2. Probabilistic Early Expiration: обновлять кеш чуть раньше TTL, случайным образом\n3. Jitter: добавлять случайный разброс к TTL, чтобы не все ключи истекали одновременно\n   TTL = 3600 + random(0, 300) секунд' },
        { type: 'heading', value: 'Cache Penetration (Проникновение)' },
        { type: 'text', value: 'Проблема: запросы за данными, которых нет ни в кеше, ни в БД (например, несуществующий ID). Каждый такой запрос идёт в БД.\n\nАтака: злоумышленник посылает миллионы запросов с несуществующими ID.\n\nРешения:\n1. Bloom Filter: вероятностная структура данных, которая проверяет "существует ли ключ". Если Bloom Filter говорит нет — сразу отвечаем без похода в БД.\n2. Cache Null Values: кешировать факт отсутствия данных: redis.set("user:99999", "NULL", EX=60)' },
        { type: 'heading', value: 'Cache Avalanche (Лавина)' },
        { type: 'text', value: 'Проблема: сразу много ключей истекает одновременно (например, все TTL = 1 час и кеш заполнен в одно время). Все запросы идут в БД.\n\nРешение: Random TTL для каждого ключа:\nTTL = base_ttl + random(0, base_ttl * 0.1)' },
        { type: 'heading', value: 'Hot Key (Горячий ключ)' },
        { type: 'text', value: 'Проблема: один ключ получает огромную нагрузку (например, профиль знаменитости). Один Redis-сервер перегружен.\n\nРешения:\n1. Реплицировать горячие ключи на несколько нод и читать с разных реплик\n2. Local cache в памяти приложения для топ-N горячих ключей' }
      ]
    },
    {
      id: 7,
      title: 'Инвалидация кеша: сложнейшая задача',
      type: 'theory',
      description: 'Три стратегии инвалидации: TTL, Event-driven (удалить при обновлении), Version-based (добавить версию в ключ). Race condition при параллельной записи и её решение.',
      solution: 'TTL: auto-expire, просто но stale data. Event-driven: при update → DEL ключ, актуально но нужно не забыть инвалидировать везде. Version-based: "user:123:v5" → старые ключи умрут по TTL. Race condition: всегда DEL после write (не до). Допустимая "свежесть" = 1–24ч для большинства данных.',
      content: [
        { type: 'text', value: 'Известная цитата Фила Карлтона: "В computer science есть только две сложные задачи: инвалидация кеша и именование вещей."' },
        { type: 'heading', value: 'Стратегии инвалидации' },
        { type: 'text', value: 'TTL (Time-To-Live): ключ автоматически удаляется через N секунд.\n  Плюс: просто. Минус: данные могут устареть до истечения TTL.\n\nEvent-driven: при обновлении данных → явно удаляем/обновляем кеш.\n  Плюс: данные актуальны. Минус: нужно помнить инвалидировать везде.\n\nVersion-based: к ключу добавляем версию: "user:123:v5".\n  При обновлении версии — старые ключи автоматически не используются (отомрут по TTL).' },
        { type: 'heading', value: 'Race Condition при инвалидации' },
        { type: 'text', value: 'Сценарий:\n1. Thread A читает данные из БД\n2. Thread B обновляет данные в БД, удаляет кеш\n3. Thread A записывает старые данные в кеш\n4. Кеш устарел!\n\nРешение: Write invalidation after DB write (не до), или Read-Your-Writes consistency через Primary сервер БД.' },
        { type: 'tip', value: 'Практическое правило: для большинства данных подходит TTL = 1–24 часа + явная инвалидация при обновлении. Не стремитесь к 100% актуальности кеша — это слишком дорого. Определите допустимую "свежесть" данных для каждого типа.' }
      ]
    },
    {
      id: 8,
      title: 'Практика: спроектируй систему кеширования',
      type: 'practice',
      description: 'Практика проектирования многоуровневой системы кеширования для e-commerce каталога (10M товаров, 50M запросов/день) с инвалидацией через Kafka.',
      requirements: [
        'Описать многоуровневую стратегию кеширования',
        'Выбрать стратегию записи (cache-aside, write-through, write-back)',
        'Определить TTL и политику вытеснения',
        'Спроектировать механизм инвалидации кеша',
        'Оценить ожидаемый cache hit rate и снижение нагрузки на БД'
      ],
      hint: 'Начни с анализа паттерна чтения: 80% запросов — к 20% данных (принцип Pareto). Определи горячие данные → кешируй их в первую очередь. Продумай инвалидацию: TTL или event-driven при обновлении.',
      expectedOutput: 'Спроектирован многоуровневый кеш (in-memory → Redis → БД). Выбраны стратегии записи и вытеснения с обоснованием. Описана инвалидация при обновлении данных. Посчитан ожидаемый cache hit rate. Нагрузка на БД снижена в несколько раз.',
      solution: 'Система кеширования для e-commerce каталога (10M товаров, 50M запросов/день):\n\nУровень 1 — In-memory (в приложении):\n- Топ-1000 товаров, TTL 5 минут, ~100 МБ RAM\n- Скорость: ~50 нс (без сети)\n\nУровень 2 — Redis Cluster:\n- Топ-100,000 товаров, TTL 30 мин + jitter, ~10 ГБ\n- Стратегия: Cache-Aside, LFU eviction\n- Скорость: ~1 мс\n\nУровень 3 — PostgreSQL:\n- Все 10M товаров, индекс на product_id\n- Получает только ~5% запросов (промахи кеша)\n\nСтратегия инвалидации:\n1. Обновить товар в БД\n2. Событие "product_updated" → Kafka\n3. Cache Service → Redis DEL "product:{id}"\n4. In-memory устареет по TTL через 5 мин (eventual consistency)',
      explanation: 'Многоуровневый кеш снижает нагрузку на БД в 20 раз (95% hit rate). Ключевые решения: LFU для вытеснения (самые редкие удаляются), jitter на TTL против Cache Avalanche, event-driven инвалидация для актуальности данных. Eventual consistency (5 мин) приемлемо для каталога e-commerce.',
      content: [
        { type: 'text', value: 'Применим знания о кешировании к реальному сценарию.' },
        { type: 'heading', value: 'Задача: кеширование для e-commerce каталога' },
        { type: 'text', value: 'Система:\n- 10 млн товаров в каталоге\n- 50 млн запросов в день на чтение товаров\n- Данные обновляются раз в час (цены, наличие)\n- 80% запросов — топ-100,000 популярных товаров' },
        { type: 'heading', value: 'Решение: многоуровневый кеш' },
        { type: 'text', value: 'Уровень 1 — In-memory кеш в приложении:\n- Топ-1000 самых популярных товаров\n- TTL: 5 минут\n- Размер: ~100 МБ RAM на инстанс\n- Скорость: ~50 нс (без сети)\n\nУровень 2 — Redis Cluster:\n- Топ-100,000 популярных товаров\n- TTL: 30 минут + случайный jitter\n- Размер: ~10 ГБ\n- Скорость: ~1 мс\n\nУровень 3 — БД с индексами:\n- Все 10 млн товаров\n- Попадает только ~5% запросов (кеш-промахи)\n- Скорость: ~10–50 мс' },
        { type: 'heading', value: 'Стратегия обновления' },
        { type: 'text', value: 'При обновлении цены/наличия товара:\n1. Обновить в БД\n2. Опубликовать событие "product_updated" в очередь\n3. Cache Service получает событие → инвалидирует Redis ключ\n4. In-memory кеш устареет по TTL через 5 минут\n\nЭто eventual consistency — допустимо для e-commerce.' },
        { type: 'note', value: 'Результат: БД получает 2.5 млн запросов в день вместо 50 млн — снижение в 20 раз. Средняя латентность: 2 мс (было 30 мс). Cache hit rate: 95%.' }
      ]
    }
  ]
}
