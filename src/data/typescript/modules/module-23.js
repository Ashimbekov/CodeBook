export default {
  id: 23,
  title: 'Практикум: Проекты',
  description: 'Полноценные мини-проекты на TypeScript: REST API клиент, система управления задачами, mini ORM и другие',
  lessons: [
    {
      id: 1,
      title: 'Проект: Типизированный HTTP клиент',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создайте полностью типизированный HTTP клиент с поддержкой interceptors, generic методами и обработкой ошибок.',
      requirements: [
        'Класс HttpClient с методами get<T>, post<T>, put<T>, delete<T>',
        'Интерфейс HttpConfig: baseURL, headers, timeout',
        'Типы RequestInterceptor и ResponseInterceptor',
        'Методы addRequestInterceptor и addResponseInterceptor',
        'Класс HttpError extends Error с полем statusCode и response',
        'Все методы возвращают Promise<T>'
      ],
      hint: 'Методы get/post принимают url и config (опционально). Interceptors — массивы функций, применяемых последовательно до/после запроса.',
      solution: 'interface HttpConfig {\n  baseURL?: string;\n  headers?: Record<string, string>;\n  timeout?: number;\n}\n\ninterface RequestConfig extends HttpConfig {\n  method: "GET" | "POST" | "PUT" | "DELETE" | "PATCH";\n  url: string;\n  body?: unknown;\n}\n\ntype RequestInterceptor  = (config: RequestConfig) => RequestConfig | Promise<RequestConfig>;\ntype ResponseInterceptor = (response: Response) => Response | Promise<Response>;\n\nclass HttpError extends Error {\n  constructor(\n    message: string,\n    public statusCode: number,\n    public response: Response\n  ) {\n    super(message);\n    this.name = "HttpError";\n  }\n}\n\nclass HttpClient {\n  private config: HttpConfig;\n  private requestInterceptors:  RequestInterceptor[]  = [];\n  private responseInterceptors: ResponseInterceptor[] = [];\n\n  constructor(config: HttpConfig = {}) {\n    this.config = config;\n  }\n\n  addRequestInterceptor(fn: RequestInterceptor): void {\n    this.requestInterceptors.push(fn);\n  }\n\n  addResponseInterceptor(fn: ResponseInterceptor): void {\n    this.responseInterceptors.push(fn);\n  }\n\n  private async request<T>(config: RequestConfig): Promise<T> {\n    let reqConfig = { ...this.config, ...config,\n      url: (this.config.baseURL || "") + config.url,\n      headers: { ...this.config.headers, ...config.headers }\n    };\n\n    for (const interceptor of this.requestInterceptors) {\n      reqConfig = await interceptor(reqConfig);\n    }\n\n    const fetchOptions: RequestInit = {\n      method: reqConfig.method,\n      headers: reqConfig.headers,\n      body: reqConfig.body ? JSON.stringify(reqConfig.body) : undefined,\n    };\n\n    let response = await fetch(reqConfig.url, fetchOptions);\n\n    for (const interceptor of this.responseInterceptors) {\n      response = await interceptor(response);\n    }\n\n    if (!response.ok) {\n      throw new HttpError(`HTTP ${response.status}`, response.status, response);\n    }\n\n    return response.json() as Promise<T>;\n  }\n\n  get<T>(url: string, config?: HttpConfig): Promise<T> {\n    return this.request<T>({ method: "GET", url, ...config });\n  }\n\n  post<T>(url: string, body?: unknown, config?: HttpConfig): Promise<T> {\n    return this.request<T>({ method: "POST", url, body, ...config });\n  }\n\n  put<T>(url: string, body?: unknown, config?: HttpConfig): Promise<T> {\n    return this.request<T>({ method: "PUT", url, body, ...config });\n  }\n\n  delete<T>(url: string, config?: HttpConfig): Promise<T> {\n    return this.request<T>({ method: "DELETE", url, ...config });\n  }\n}\n\n// Использование:\ninterface User { id: number; name: string; email: string; }\n\nconst client = new HttpClient({ baseURL: "https://jsonplaceholder.typicode.com" });\n\nclient.addRequestInterceptor(config => {\n  console.log(`Запрос: ${config.method} ${config.url}`);\n  return config;\n});\n\nconst users = await client.get<User[]>("/users");\nconsole.log(users[0].name); // string — TypeScript знает тип!',
      explanation: 'Generic методы get<T> позволяют вызывающему коду указать тип ответа. Interceptors — цепочки трансформаций для запросов и ответов. HttpError с statusCode позволяет обработчикам различать типы ошибок. Все методы delegate в request<T> — единая точка обработки запросов.'
    },
    {
      id: 2,
      title: 'Проект: Менеджер задач с хранилищем',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создайте полноценный менеджер задач с категориями, приоритетами, фильтрацией и персистентностью через localStorage.',
      requirements: [
        'Типы: Task (id, title, category, priority, completed, tags, dueDate?), Category, Priority',
        'Класс TaskManager с методами CRUD + filter/sort',
        'Метод filterTasks(filters: TaskFilters): Task[]',
        'Метод sortTasks(field: keyof Task, direction: "asc" | "desc"): Task[]',
        'Сохранение в localStorage через TypedStorage',
        'Статистика: getStats() возвращает объект с метриками'
      ],
      hint: 'filterTasks использует Partial<Record<keyof Task, unknown>> для фильтров. sortTasks сортирует по любому полю Task. getStats считает completed, по категориям, по приоритетам.',
      solution: 'type Category = "work" | "personal" | "shopping" | "health";\ntype Priority  = "low" | "medium" | "high";\n\ninterface Task {\n  id: string;\n  title: string;\n  category: Category;\n  priority: Priority;\n  completed: boolean;\n  tags: string[];\n  createdAt: Date;\n  dueDate?: Date;\n}\n\ninterface TaskFilters {\n  category?:  Category;\n  priority?:  Priority;\n  completed?: boolean;\n  tag?:       string;\n}\n\ninterface TaskStats {\n  total: number;\n  completed: number;\n  pending: number;\n  byCategory: Record<Category, number>;\n  byPriority:  Record<Priority, number>;\n}\n\nclass TaskManager {\n  private tasks: Task[] = [];\n  private storageKey = "taskmanager:tasks";\n\n  constructor() { this.load(); }\n\n  private load(): void {\n    const raw = localStorage.getItem(this.storageKey);\n    if (raw) this.tasks = JSON.parse(raw);\n  }\n\n  private save(): void {\n    localStorage.setItem(this.storageKey, JSON.stringify(this.tasks));\n  }\n\n  add(data: Omit<Task, "id" | "createdAt" | "completed">): Task {\n    const task: Task = { ...data, id: crypto.randomUUID(), createdAt: new Date(), completed: false };\n    this.tasks.push(task);\n    this.save();\n    return task;\n  }\n\n  update(id: string, updates: Partial<Omit<Task, "id" | "createdAt">>): Task {\n    const idx = this.tasks.findIndex(t => t.id === id);\n    if (idx === -1) throw new Error(`Задача ${id} не найдена`);\n    this.tasks[idx] = { ...this.tasks[idx], ...updates };\n    this.save();\n    return this.tasks[idx];\n  }\n\n  delete(id: string): boolean {\n    const before = this.tasks.length;\n    this.tasks = this.tasks.filter(t => t.id !== id);\n    this.save();\n    return this.tasks.length < before;\n  }\n\n  filterTasks(filters: TaskFilters): Task[] {\n    return this.tasks.filter(task => {\n      if (filters.category  && task.category  !== filters.category)  return false;\n      if (filters.priority  && task.priority   !== filters.priority)  return false;\n      if (filters.completed !== undefined && task.completed !== filters.completed) return false;\n      if (filters.tag       && !task.tags.includes(filters.tag))     return false;\n      return true;\n    });\n  }\n\n  sortTasks(field: keyof Task, dir: "asc" | "desc" = "asc"): Task[] {\n    return [...this.tasks].sort((a, b) => {\n      const av = a[field], bv = b[field];\n      const cmp = av < bv ? -1 : av > bv ? 1 : 0;\n      return dir === "asc" ? cmp : -cmp;\n    });\n  }\n\n  getStats(): TaskStats {\n    const byCategory = { work: 0, personal: 0, shopping: 0, health: 0 };\n    const byPriority  = { low: 0, medium: 0, high: 0 };\n    this.tasks.forEach(t => { byCategory[t.category]++; byPriority[t.priority]++; });\n    const completed = this.tasks.filter(t => t.completed).length;\n    return { total: this.tasks.length, completed, pending: this.tasks.length - completed, byCategory, byPriority };\n  }\n}\n\nconst manager = new TaskManager();\nmanager.add({ title: "Написать отчёт", category: "work", priority: "high", tags: ["отчёт"] });\nmanager.add({ title: "Купить продукты", category: "shopping", priority: "medium", tags: [] });\nconsole.log(manager.filterTasks({ priority: "high" }));\nconsole.log(manager.getStats());',
      explanation: 'Omit<Task, "id" | "createdAt" | "completed"> в add — не нужно передавать поля, которые генерируются автоматически. filterTasks с TaskFilters — только нужные фильтры передаются. sortTasks с keyof Task — TypeScript не позволит передать несуществующее поле.'
    },
    {
      id: 3,
      title: 'Проект: Типизированный State Manager',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте упрощённый Redux-подобный state manager с TypeScript. Полная типобезопасность для actions, reducers и selectors.',
      requirements: [
        'Класс Store<TState, TAction extends { type: string }>',
        'Методы: dispatch(action: TAction), getState(): TState, subscribe(listener: (state: TState) => void)',
        'Функция combineReducers<T>(reducers: Reducers<T>): Reducer<T>',
        'Typed selectors через функции createSelector',
        'Протестировать с counter и todos слайсами'
      ],
      hint: 'Store хранит state и список listeners. dispatch вызывает reducer и уведомляет подписчиков. combineReducers создаёт один reducer из объекта частичных reducers.',
      solution: 'type Reducer<TState, TAction> = (state: TState, action: TAction) => TState;\ntype Reducers<T> = { [K in keyof T]: Reducer<T[K], any> };\n\nclass Store<TState, TAction extends { type: string }> {\n  private state: TState;\n  private listeners: ((state: TState) => void)[] = [];\n\n  constructor(private reducer: Reducer<TState, TAction>, initialState: TState) {\n    this.state = initialState;\n  }\n\n  getState(): TState { return this.state; }\n\n  dispatch(action: TAction): void {\n    this.state = this.reducer(this.state, action);\n    this.listeners.forEach(l => l(this.state));\n  }\n\n  subscribe(listener: (state: TState) => void): () => void {\n    this.listeners.push(listener);\n    return () => { this.listeners = this.listeners.filter(l => l !== listener); };\n  }\n}\n\nfunction combineReducers<T extends object>(reducers: Reducers<T>): Reducer<T, any> {\n  return (state: Partial<T> = {}, action: any): T => {\n    const nextState = {} as T;\n    for (const key in reducers) {\n      nextState[key] = reducers[key](state[key] as T[typeof key], action);\n    }\n    return nextState;\n  };\n}\n\n// Слайс счётчика\ntype CounterAction = { type: "INCREMENT" } | { type: "DECREMENT" } | { type: "RESET" };\nconst counterReducer: Reducer<number, CounterAction> = (state = 0, action) => {\n  switch (action.type) {\n    case "INCREMENT": return state + 1;\n    case "DECREMENT": return state - 1;\n    case "RESET":     return 0;\n    default:          return state;\n  }\n};\n\n// Слайс todos\ninterface Todo { id: number; text: string; done: boolean; }\ntype TodoAction = { type: "ADD_TODO"; text: string } | { type: "TOGGLE_TODO"; id: number };\nconst todosReducer: Reducer<Todo[], TodoAction> = (state = [], action) => {\n  switch (action.type) {\n    case "ADD_TODO":    return [...state, { id: Date.now(), text: action.text, done: false }];\n    case "TOGGLE_TODO": return state.map(t => t.id === action.id ? { ...t, done: !t.done } : t);\n    default:            return state;\n  }\n};\n\ninterface AppState { counter: number; todos: Todo[]; }\nconst rootReducer = combineReducers<AppState>({ counter: counterReducer, todos: todosReducer });\nconst store = new Store(rootReducer, { counter: 0, todos: [] });\n\nconst unsub = store.subscribe(state => console.log("State:", state.counter, state.todos.length));\nstore.dispatch({ type: "INCREMENT" });\nstore.dispatch({ type: "ADD_TODO", text: "Изучить TypeScript" });\nstore.dispatch({ type: "INCREMENT" });\nunsub(); // Отписаться\nconsole.log(store.getState());',
      explanation: 'Store<TState, TAction> — полностью типизирован. dispatch принимает только TAction — нельзя dispatach неизвестные события. subscribe возвращает функцию отписки. combineReducers создаёт корневой reducer из частичных, TypeScript проверяет соответствие ключей AppState.'
    },
    {
      id: 4,
      title: 'Проект: Schema Validator',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создайте библиотеку валидации схем в стиле Zod. Схема описывает форму объекта, validate проверяет данные и возвращает типизированный результат.',
      requirements: [
        'class StringSchema, NumberSchema, BooleanSchema с методами min, max, required',
        'class ObjectSchema<T> принимает схему полей',
        'Метод validate(data: unknown): { success: true; data: T } | { success: false; errors: string[] }',
        'Функция z.string(), z.number(), z.boolean(), z.object()',
        'Тип Infer<Schema> — выводит TypeScript тип из схемы'
      ],
      hint: 'Каждая схема — класс с методом validate(value: unknown). ObjectSchema перебирает поля и собирает ошибки. Infer использует conditional types для извлечения типа схемы.',
      solution: 'abstract class Schema<T> {\n  abstract validate(value: unknown): { success: true; data: T } | { success: false; errors: string[] };\n}\n\nclass StringSchema extends Schema<string> {\n  private _min?: number;\n  private _max?: number;\n  private _required = true;\n\n  min(n: number): this { this._min = n; return this; }\n  max(n: number): this { this._max = n; return this; }\n  optional(): this { this._required = false; return this; }\n\n  validate(value: unknown): { success: true; data: string } | { success: false; errors: string[] } {\n    const errors: string[] = [];\n    if (value === undefined || value === null) {\n      if (this._required) errors.push("Поле обязательно");\n      else return { success: true, data: "" };\n    } else if (typeof value !== "string") {\n      errors.push("Ожидается строка");\n    } else {\n      if (this._min !== undefined && value.length < this._min) errors.push(`Минимум ${this._min} символов`);\n      if (this._max !== undefined && value.length > this._max) errors.push(`Максимум ${this._max} символов`);\n    }\n    return errors.length === 0 ? { success: true, data: value as string } : { success: false, errors };\n  }\n}\n\nclass NumberSchema extends Schema<number> {\n  private _min?: number;\n  private _max?: number;\n\n  min(n: number): this { this._min = n; return this; }\n  max(n: number): this { this._max = n; return this; }\n\n  validate(value: unknown): { success: true; data: number } | { success: false; errors: string[] } {\n    const errors: string[] = [];\n    if (typeof value !== "number") { errors.push("Ожидается число"); }\n    else {\n      if (this._min !== undefined && value < this._min) errors.push(`Минимум ${this._min}`);\n      if (this._max !== undefined && value > this._max) errors.push(`Максимум ${this._max}`);\n    }\n    return errors.length === 0 ? { success: true, data: value as number } : { success: false, errors };\n  }\n}\n\ntype SchemaShape<T> = { [K in keyof T]: Schema<T[K]> };\n\nclass ObjectSchema<T extends object> extends Schema<T> {\n  constructor(private shape: SchemaShape<T>) { super(); }\n\n  validate(value: unknown): { success: true; data: T } | { success: false; errors: string[] } {\n    if (typeof value !== "object" || value === null) {\n      return { success: false, errors: ["Ожидается объект"] };\n    }\n    const data = {} as T;\n    const errors: string[] = [];\n    for (const key in this.shape) {\n      const result = this.shape[key].validate((value as any)[key]);\n      if (result.success) { data[key] = result.data; }\n      else { errors.push(...result.errors.map(e => `${key}: ${e}`)); }\n    }\n    return errors.length === 0 ? { success: true, data } : { success: false, errors };\n  }\n}\n\nconst z = {\n  string: () => new StringSchema(),\n  number: () => new NumberSchema(),\n  object: <T extends object>(shape: SchemaShape<T>) => new ObjectSchema(shape),\n};\n\nconst UserSchema = z.object({\n  name:  z.string().min(2).max(50),\n  email: z.string().min(5),\n  age:   z.number().min(0).max(150),\n});\n\nconsole.log(UserSchema.validate({ name: "Алиса", email: "alice@test.com", age: 30 }));\n// { success: true, data: { name: "Алиса", email: ..., age: 30 } }\nconsole.log(UserSchema.validate({ name: "A", email: "x", age: -1 }));\n// { success: false, errors: ["name: Минимум 2 символов", "email: Минимум 5 символов", "age: Минимум 0"] }',
      explanation: 'abstract Schema<T> — базовый класс с абстрактным validate. Method chaining (min().max()) возможен благодаря return this. ObjectSchema<T> перебирает все поля схемы и собирает ошибки с префиксом ключа. Тип T автоматически выводится из shape в ObjectSchema.'
    },
    {
      id: 5,
      title: 'Проект: Обёртка над WebSocket',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создайте типизированную обёртку над WebSocket с поддержкой typed messages, автоматическим переподключением и promise-based send.',
      requirements: [
        'Класс TypedWebSocket<TIncoming, TOutgoing>',
        'send(message: TOutgoing): void',
        'on<K extends keyof TIncoming>(type: K, handler: (data: TIncoming[K]) => void): () => void',
        'Автоматическое переподключение при разрыве (maxRetries, retryDelay)',
        'Состояния: connecting, connected, disconnected, reconnecting',
        'События жизненного цикла: onConnect, onDisconnect, onError'
      ],
      hint: 'TIncoming — карта типов входящих сообщений { chat: ChatMsg; notification: NotifMsg }. TOutgoing — аналогично. send сериализует в JSON с полем type. on фильтрует по type и вызывает handler.',
      solution: 'type WSState = "connecting" | "connected" | "disconnected" | "reconnecting";\n\ntype IncomingMessage<T extends Record<string, any>> = {\n  [K in keyof T]: { type: K } & T[K];\n}[keyof T];\n\nclass TypedWebSocket<TIn extends Record<string, any>, TOut extends Record<string, any>> {\n  private ws: WebSocket | null = null;\n  private _state: WSState = "disconnected";\n  private handlers: Partial<{ [K in keyof TIn]: ((data: TIn[K]) => void)[] }> = {};\n  private retryCount = 0;\n  private maxRetries: number;\n  private retryDelay: number;\n\n  onConnect?:    () => void;\n  onDisconnect?: () => void;\n  onError?:      (error: Event) => void;\n\n  constructor(private url: string, options: { maxRetries?: number; retryDelay?: number } = {}) {\n    this.maxRetries = options.maxRetries ?? 3;\n    this.retryDelay = options.retryDelay ?? 2000;\n    this.connect();\n  }\n\n  get state(): WSState { return this._state; }\n\n  private connect(): void {\n    this._state = "connecting";\n    this.ws = new WebSocket(this.url);\n\n    this.ws.onopen = () => {\n      this._state = "connected";\n      this.retryCount = 0;\n      this.onConnect?.();\n    };\n\n    this.ws.onclose = () => {\n      this._state = "disconnected";\n      this.onDisconnect?.();\n      this.tryReconnect();\n    };\n\n    this.ws.onerror = (e) => { this.onError?.(e); };\n\n    this.ws.onmessage = (event: MessageEvent) => {\n      try {\n        const msg = JSON.parse(event.data) as IncomingMessage<TIn>;\n        const type = msg.type as keyof TIn;\n        const list = this.handlers[type];\n        list?.forEach(h => h(msg as unknown as TIn[typeof type]));\n      } catch { /* Игнорируем невалидные сообщения */ }\n    };\n  }\n\n  private tryReconnect(): void {\n    if (this.retryCount >= this.maxRetries) return;\n    this.retryCount++;\n    this._state = "reconnecting";\n    setTimeout(() => this.connect(), this.retryDelay);\n  }\n\n  send<K extends keyof TOut>(type: K, data: TOut[K]): void {\n    if (this._state !== "connected" || !this.ws) {\n      throw new Error("WebSocket не подключён");\n    }\n    this.ws.send(JSON.stringify({ type, ...data }));\n  }\n\n  on<K extends keyof TIn>(type: K, handler: (data: TIn[K]) => void): () => void {\n    const list = (this.handlers[type] as ((data: TIn[K]) => void)[] | undefined) || [];\n    this.handlers[type] = [...list, handler] as any;\n    return () => {\n      const cur = this.handlers[type] as ((data: TIn[K]) => void)[] | undefined;\n      this.handlers[type] = cur?.filter(h => h !== handler) as any;\n    };\n  }\n\n  disconnect(): void {\n    this.maxRetries = 0;\n    this.ws?.close();\n  }\n}\n\n// Использование:\ninterface ServerMessages {\n  chat:         { userId: number; text: string; timestamp: string };\n  notification: { title: string; body: string };\n  error:        { code: number; message: string };\n}\n\ninterface ClientMessages {\n  sendChat:  { text: string; roomId: string };\n  joinRoom:  { roomId: string };\n  leaveRoom: { roomId: string };\n}\n\nconst ws = new TypedWebSocket<ServerMessages, ClientMessages>("wss://example.com/ws", { maxRetries: 5 });\nws.onConnect = () => console.log("Подключено!");\n\nconst unsub = ws.on("chat", ({ userId, text }) => {\n  // userId: number, text: string — TypeScript знает!\n  console.log(`Пользователь ${userId}: ${text}`);\n});\n\nws.send("sendChat", { text: "Привет!", roomId: "general" });\nunsub(); // Отписаться от chat',
      explanation: 'Двойной generic TIn/TOut: TypeScript знает типы всех входящих и исходящих сообщений. on("chat", handler) — handler автоматически типизирован как (data: ServerMessages["chat"]) => void. send("sendChat", data) проверяет, что data соответствует ClientMessages["sendChat"]. Автоматическое переподключение с отслеживанием retryCount.'
    },
    {
      id: 6,
      title: 'Проект: CSV парсер и генератор',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создайте типизированный CSV парсер и генератор. parseCSV должна возвращать массив объектов с типом, выведенным из заголовков.',
      requirements: [
        'Функция parseCSV<T>(csv: string, schema: CSVSchema<T>): T[]',
        'CSVSchema<T>: объект с конвертерами для каждого поля',
        'Функция generateCSV<T>(data: T[], fields: (keyof T)[]): string',
        'Поддержка кавычек для полей с запятыми',
        'Тест на данных пользователей: id(number), name(string), active(boolean)'
      ],
      hint: 'CSVSchema<T> = { [K in keyof T]: (raw: string) => T[K] }. parseCSV читает первую строку как заголовки, остальные — данные. generateCSV объединяет поля через запятую, оборачивая в кавычки если содержат запятую.',
      solution: 'type CSVSchema<T> = { [K in keyof T]: (raw: string) => T[K] };\n\nfunction parseCSV<T>(csv: string, schema: CSVSchema<T>): T[] {\n  const lines = csv.trim().split("\\n");\n  if (lines.length < 2) return [];\n\n  const headers = lines[0].split(",").map(h => h.trim());\n  const results: T[] = [];\n\n  for (let i = 1; i < lines.length; i++) {\n    const values = lines[i].split(",").map(v => v.trim().replace(/^"|"$/g, ""));\n    const obj = {} as T;\n    headers.forEach((header, idx) => {\n      const key = header as keyof T;\n      if (key in schema) {\n        obj[key] = schema[key](values[idx] || "");\n      }\n    });\n    results.push(obj);\n  }\n\n  return results;\n}\n\nfunction generateCSV<T extends object>(data: T[], fields: (keyof T)[]): string {\n  const header = fields.map(String).join(",");\n  const rows = data.map(item =>\n    fields.map(field => {\n      const val = String(item[field]);\n      return val.includes(",") ? `"${val}"` : val;\n    }).join(",")\n  );\n  return [header, ...rows].join("\\n");\n}\n\n// Тест:\ninterface UserCSV { id: number; name: string; email: string; active: boolean; }\n\nconst csvData = `id,name,email,active\n1,Алиса,alice@test.com,true\n2,Боб,bob@test.com,false\n3,Петя,petya@test.com,true`;\n\nconst schema: CSVSchema<UserCSV> = {\n  id:     (s) => parseInt(s),\n  name:   (s) => s,\n  email:  (s) => s,\n  active: (s) => s === "true",\n};\n\nconst users = parseCSV<UserCSV>(csvData, schema);\nconsole.log(users[0].id);     // number: 1\nconsole.log(users[0].active); // boolean: true\n\nconst generated = generateCSV(users, ["id", "name", "email"]);\nconsole.log(generated);',
      explanation: 'CSVSchema<T> — объект где каждое значение — конвертер из строки в T[K]. parseCSV использует schema для преобразования строк в правильные типы. Результат полностью типизирован — users[0].id — number, не string. generateCSV<T> принимает только реальные ключи T через keyof T.'
    },
    {
      id: 7,
      title: 'Проект: Middleware Pipeline',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте типизированный middleware pipeline (как в Koa.js) с поддержкой generic контекста и цепочки вызовов.',
      requirements: [
        'Тип Middleware<TCtx>: функция (ctx: TCtx, next: () => Promise<void>) => Promise<void>',
        'Класс Pipeline<TCtx> с методами use(middleware: Middleware<TCtx>) и run(ctx: TCtx)',
        'Middleware выполняются последовательно, каждый вызывает next() для передачи',
        'Поддержка before/after паттерна (код до и после next())',
        'Протестировать с ctx: { path: string; method: string; body: unknown; response?: string }'
      ],
      hint: 'Pipeline.run создаёт цепочку через reduce или рекурсию. Каждый middleware оборачивается так: вызов следующего через compose. Порядок: первый добавленный выполняется первым.',
      solution: 'type Middleware<TCtx> = (ctx: TCtx, next: () => Promise<void>) => Promise<void>;\n\nclass Pipeline<TCtx> {\n  private middlewares: Middleware<TCtx>[] = [];\n\n  use(middleware: Middleware<TCtx>): this {\n    this.middlewares.push(middleware);\n    return this;\n  }\n\n  async run(ctx: TCtx): Promise<void> {\n    const compose = (index: number): (() => Promise<void>) => {\n      if (index >= this.middlewares.length) return async () => {};\n      const middleware = this.middlewares[index];\n      return () => middleware(ctx, compose(index + 1));\n    };\n    await compose(0)();\n  }\n}\n\n// Контекст запроса\ninterface RequestCtx {\n  path:      string;\n  method:    string;\n  body:      unknown;\n  response?: string;\n  userId?:   number;\n  startTime: number;\n}\n\n// Middleware логирования\nconst logMiddleware: Middleware<RequestCtx> = async (ctx, next) => {\n  console.log(`--> ${ctx.method} ${ctx.path}`);\n  await next();\n  console.log(`<-- ${ctx.method} ${ctx.path}: ${ctx.response || "нет ответа"}`);\n};\n\n// Middleware замера времени\nconst timingMiddleware: Middleware<RequestCtx> = async (ctx, next) => {\n  const start = Date.now();\n  await next();\n  console.log(`Время: ${Date.now() - start}мс`);\n};\n\n// Middleware аутентификации\nconst authMiddleware: Middleware<RequestCtx> = async (ctx, next) => {\n  if (ctx.path.startsWith("/public")) { await next(); return; }\n  ctx.userId = 1; // В реальности — проверка токена\n  await next();\n};\n\n// Обработчик\nconst handler: Middleware<RequestCtx> = async (ctx) => {\n  ctx.response = `Обработан: ${ctx.path} для пользователя ${ctx.userId ?? "гость"}`;\n};\n\nconst pipeline = new Pipeline<RequestCtx>()\n  .use(logMiddleware)\n  .use(timingMiddleware)\n  .use(authMiddleware)\n  .use(handler);\n\nconst ctx: RequestCtx = { path: "/api/users", method: "GET", body: null, startTime: Date.now() };\nawait pipeline.run(ctx);\nconsole.log(ctx.response);',
      explanation: 'compose функция рекурсивно создаёт цепочку вызовов. Каждый middleware получает ctx (общий для всей цепочки) и next (следующий middleware). Before/after паттерн: код до await next() выполняется на входе, после — на выходе. Method chaining через return this позволяет .use().use().use().'
    },
    {
      id: 8,
      title: 'Проект: Итоговый — Mini задачник с API',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создайте полноценное mini приложение: типизированный API клиент для JSONPlaceholder + локальный кэш + вывод данных. Объедините все знания модуля.',
      requirements: [
        'Интерфейсы: Todo (userId, id, title, completed), User (id, name, email)',
        'Класс ApiService с методами getTodos, getTodoById, getUserTodos, createTodo',
        'Класс Cache<K, V> с TTL для кэширования ответов',
        'Функция displayTodos(todos: Todo[]): void',
        'Обработка ошибок через try/catch с типом unknown',
        'Типизация всех async функций с правильными return types'
      ],
      hint: 'ApiService использует fetch и cache.get/set. Ключи кэша — строки URL. Если в кэше есть — возвращаем, нет — запрашиваем и кладём в кэш. displayTodos форматирует список для вывода.',
      solution: 'interface Todo { userId: number; id: number; title: string; completed: boolean; }\ninterface User { id: number; name: string; email: string; }\n\ninterface CacheEntry<V> { value: V; expiresAt: number; }\n\nclass Cache<K extends string, V> {\n  private store = new Map<K, CacheEntry<V>>();\n  constructor(private ttl: number) {}\n\n  get(key: K): V | null {\n    const entry = this.store.get(key);\n    if (!entry) return null;\n    if (Date.now() > entry.expiresAt) { this.store.delete(key); return null; }\n    return entry.value;\n  }\n\n  set(key: K, value: V): void {\n    this.store.set(key, { value, expiresAt: Date.now() + this.ttl });\n  }\n}\n\nclass ApiService {\n  private base = "https://jsonplaceholder.typicode.com";\n  private cache = new Cache<string, any>(60_000); // 1 минута\n\n  private async fetch<T>(path: string): Promise<T> {\n    const cached = this.cache.get(path);\n    if (cached) { console.log(`[Кэш] ${path}`); return cached as T; }\n\n    const res = await fetch(`${this.base}${path}`);\n    if (!res.ok) throw new Error(`HTTP ${res.status}: ${path}`);\n    const data: T = await res.json();\n    this.cache.set(path, data);\n    return data;\n  }\n\n  getTodos(): Promise<Todo[]> { return this.fetch<Todo[]>("/todos"); }\n  getTodoById(id: number): Promise<Todo> { return this.fetch<Todo>(`/todos/${id}`); }\n  getUserTodos(userId: number): Promise<Todo[]> {\n    return this.fetch<Todo[]>(`/todos?userId=${userId}`);\n  }\n\n  async createTodo(data: Omit<Todo, "id">): Promise<Todo> {\n    const res = await fetch(`${this.base}/todos`, {\n      method: "POST",\n      headers: { "Content-Type": "application/json" },\n      body: JSON.stringify(data),\n    });\n    return res.json() as Promise<Todo>;\n  }\n}\n\nfunction displayTodos(todos: Todo[]): void {\n  const done    = todos.filter(t => t.completed).length;\n  const pending = todos.length - done;\n  console.log(`\\nЗадачи (${todos.length}): выполнено ${done}, осталось ${pending}`);\n  todos.slice(0, 5).forEach(t => {\n    const status = t.completed ? "[x]" : "[ ]";\n    console.log(`  ${status} #${t.id}: ${t.title}`);\n  });\n  if (todos.length > 5) console.log(`  ... и ещё ${todos.length - 5} задач`);\n}\n\nasync function main(): Promise<void> {\n  const api = new ApiService();\n  try {\n    const allTodos = await api.getTodos();\n    displayTodos(allTodos);\n\n    const userTodos = await api.getUserTodos(1);\n    console.log(`\\nЗадачи пользователя 1: ${userTodos.length}`);\n\n    // Повторный запрос — из кэша\n    const cached = await api.getTodoById(1);\n    console.log(`\\nЗадача #1: ${cached.title}`);\n\n    const newTodo = await api.createTodo({ userId: 1, title: "Новая задача", completed: false });\n    console.log(`Создана: #${newTodo.id} ${newTodo.title}`);\n\n  } catch (error: unknown) {\n    if (error instanceof Error) {\n      console.error("Ошибка:", error.message);\n    } else {\n      console.error("Неизвестная ошибка");\n    }\n  }\n}\n\nmain();',
      explanation: 'ApiService инкапсулирует HTTP и кэш. Cache<string, any> — универсальный для разных типов данных. fetch<T> — generic метод, выводит тип из вызывающего кода. error: unknown в catch — правильно: TypeScript 4+ требует explicit type assertion. Omit<Todo, "id"> в createTodo — сервер сам назначает id.'
    }
  ]
}
