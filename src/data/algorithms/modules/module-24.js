export default {
  id: 24,
  title: 'Два указателя и скользящее окно',
  description: 'Техники двух указателей и скользящего окна: поиск пар в отсортированном массиве, обнаружение цикла, фиксированное и переменное окно, максимальная сумма подмассива и наибольшая подстрока без повторов',
  lessons: [
    {
      id: 1,
      title: 'Техника двух указателей: сумма пары',
      type: 'theory',
      content: [
        { type: 'text', value: 'Техника двух указателей (Two Pointers) — это подход, при котором два индекса перемещаются по массиву навстречу друг другу или в одном направлении. Работает эффективно на отсортированных массивах.' },
        { type: 'tip', value: 'Представь, что ты ищешь двух людей в очереди, у которых в сумме ровно 100 монет. Очередь отсортирована: первый — беднейший, последний — богатейший. Поставь одного пальца на начало, другой на конец. Сумма велика — двигай правый влево. Мала — двигай левый вправо. Так за O(n) найдёшь пару!' },
        { type: 'heading', value: 'Two Sum для отсортированного массива' },
        { type: 'code', language: 'java', value: 'public class TwoPointers {\n    // Two Sum: найти два числа с суммой target в отсортированном массиве\n    public static int[] twoSum(int[] nums, int target) {\n        int left = 0, right = nums.length - 1;\n\n        while (left < right) {\n            int sum = nums[left] + nums[right];\n            System.out.printf("left=%d(%d), right=%d(%d), sum=%d%n",\n                left, nums[left], right, nums[right], sum);\n\n            if (sum == target) {\n                return new int[]{left, right};\n            } else if (sum < target) {\n                left++;  // Нужна большая сумма — двигаем левый\n            } else {\n                right--; // Нужна меньшая сумма — двигаем правый\n            }\n        }\n        return new int[]{-1, -1}; // Не найдено\n    }\n\n    public static void main(String[] args) {\n        int[] nums = {2, 7, 11, 15};\n        int[] result = twoSum(nums, 9);\n        System.out.println("Индексы: [" + result[0] + ", " + result[1] + "]"); // [0, 1]\n    }\n}' },
        { type: 'heading', value: 'Трассировка' },
        { type: 'code', language: 'java', value: '// nums = [2, 7, 11, 15], target = 9\n// left=0, right=3\n//\n// Шаг 1: left=0(2), right=3(15), sum=17 > 9 -> right--\n// Шаг 2: left=0(2), right=2(11), sum=13 > 9 -> right--\n// Шаг 3: left=0(2), right=1(7),  sum=9 == 9 -> НАШЛИ!\n// Ответ: индексы [0, 1]' },
        { type: 'heading', value: 'Three Sum: найти три числа с суммой 0' },
        { type: 'code', language: 'java', value: 'import java.util.*;\n\npublic static List<List<Integer>> threeSum(int[] nums) {\n    Arrays.sort(nums);\n    List<List<Integer>> result = new ArrayList<>();\n\n    for (int i = 0; i < nums.length - 2; i++) {\n        if (i > 0 && nums[i] == nums[i-1]) continue; // Пропускаем дубликаты\n        int left = i + 1, right = nums.length - 1;\n        while (left < right) {\n            int sum = nums[i] + nums[left] + nums[right];\n            if (sum == 0) {\n                result.add(Arrays.asList(nums[i], nums[left], nums[right]));\n                while (left < right && nums[left] == nums[left+1]) left++;\n                while (left < right && nums[right] == nums[right-1]) right--;\n                left++; right--;\n            } else if (sum < 0) {\n                left++;\n            } else {\n                right--;\n            }\n        }\n    }\n    return result;\n}\n// [-1,0,1,2,-1,-4] -> [[-1,-1,2],[-1,0,1]]' },
        { type: 'note', value: 'Two Pointers работает за O(n) — оба указателя вместе проходят не более n шагов. Это значительно лучше, чем перебор всех пар O(n²). Ключевое условие: массив должен быть отсортирован.' }
      ]
    },
    {
      id: 2,
      title: 'Медленный и быстрый указатели (Floyd\'s Cycle Detection)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Алгоритм обнаружения цикла Флойда (черепаха и заяц) использует два указателя: медленный (slow) продвигается на 1 шаг, быстрый (fast) — на 2. Если есть цикл, они обязательно встретятся.' },
        { type: 'tip', value: 'Представь трек для бега. Медленная черепаха идёт по 1 шагу, быстрый заяц — по 2. Если трек круговой (есть цикл), заяц в итоге догонит черепаху — обгонит на круг. Если трека конец есть, заяц упадёт в пропасть (null) раньше встречи.' },
        { type: 'code', language: 'java', value: 'public class LinkedListCycle {\n    static class ListNode {\n        int val;\n        ListNode next;\n        ListNode(int val) { this.val = val; }\n    }\n\n    // Определить: есть ли цикл в связном списке\n    public static boolean hasCycle(ListNode head) {\n        if (head == null) return false;\n        ListNode slow = head;\n        ListNode fast = head;\n\n        while (fast != null && fast.next != null) {\n            slow = slow.next;       // Черепаха: +1 шаг\n            fast = fast.next.next;  // Заяц: +2 шага\n            if (slow == fast) return true; // Встретились -> цикл!\n        }\n        return false; // fast дошёл до конца -> нет цикла\n    }\n\n    // Найти начало цикла\n    public static ListNode detectCycleStart(ListNode head) {\n        ListNode slow = head, fast = head;\n        boolean hasCycle = false;\n\n        while (fast != null && fast.next != null) {\n            slow = slow.next;\n            fast = fast.next.next;\n            if (slow == fast) { hasCycle = true; break; }\n        }\n        if (!hasCycle) return null;\n\n        // Переставить slow в начало, идти по 1 шагу\n        slow = head;\n        while (slow != fast) {\n            slow = slow.next;\n            fast = fast.next;\n        }\n        return slow; // Начало цикла\n    }\n}' },
        { type: 'heading', value: 'Нахождение середины списка' },
        { type: 'code', language: 'java', value: '// Задача: найти середину связного списка за один проход\npublic static ListNode middleNode(ListNode head) {\n    ListNode slow = head, fast = head;\n    // Когда fast дойдёт до конца, slow будет на середине!\n    while (fast != null && fast.next != null) {\n        slow = slow.next;\n        fast = fast.next.next;\n    }\n    return slow;\n}\n\n// Трассировка для [1->2->3->4->5]:\n// Шаг 1: slow=2, fast=3\n// Шаг 2: slow=3, fast=5\n// fast.next=null -> стоп\n// slow=3 — это середина!\n\n// Трассировка для [1->2->3->4->5->6]:\n// Шаг 1: slow=2, fast=3\n// Шаг 2: slow=3, fast=5\n// Шаг 3: slow=4, fast=null -> стоп (вторая из двух средних)\n// slow=4' },
        { type: 'heading', value: 'Счастливое число (Happy Number)' },
        { type: 'code', language: 'java', value: '// Счастливое число: заменяй число суммой квадратов цифр.\n// Если попадёт в 1 — счастливое. Если зациклится — нет.\npublic static boolean isHappy(int n) {\n    int slow = n;\n    int fast = getNext(n);\n    while (fast != 1 && fast != slow) {\n        slow = getNext(slow);\n        fast = getNext(getNext(fast));\n    }\n    return fast == 1;\n}\n\npublic static int getNext(int n) {\n    int sum = 0;\n    while (n > 0) {\n        int d = n % 10;\n        sum += d * d;\n        n /= 10;\n    }\n    return sum;\n}\n// isHappy(19): 19->82->68->100->1 — счастливое!\n// isHappy(2):  2->4->16->37->58->89->145->42->20->4 (цикл!) — нет' },
        { type: 'note', value: 'Алгоритм Флойда: время O(n), память O(1). Альтернатива — HashSet для хранения посещённых узлов, но это O(n) памяти.' }
      ]
    },
    {
      id: 3,
      title: 'Скользящее окно: концепция',
      type: 'theory',
      content: [
        { type: 'text', value: 'Скользящее окно (Sliding Window) — техника для работы с подмассивами или подстроками. Вместо того чтобы пересчитывать результат с нуля для каждого окна, мы "скользим" окно вправо, добавляя новый элемент и удаляя старый.' },
        { type: 'tip', value: 'Представь окно поезда. Когда поезд едет, в окне всегда видно несколько деревьев. Новое дерево появляется справа, старое исчезает слева. Ты не пересматриваешь весь пейзаж — только добавляешь новое и убираешь старое. Это и есть скользящее окно!' },
        { type: 'heading', value: 'Максимальная сумма подмассива размера k (фиксированное окно)' },
        { type: 'code', language: 'java', value: 'public class SlidingWindow {\n    public static int maxSumFixed(int[] nums, int k) {\n        int n = nums.length;\n        if (n < k) return -1;\n\n        // Сумма первого окна\n        int windowSum = 0;\n        for (int i = 0; i < k; i++) windowSum += nums[i];\n        int maxSum = windowSum;\n        System.out.println("Окно [0.." + (k-1) + "]: сумма=" + windowSum);\n\n        // Скользим окно вправо\n        for (int i = k; i < n; i++) {\n            // Добавляем новый элемент справа, убираем старый слева\n            windowSum = windowSum + nums[i] - nums[i - k];\n            maxSum = Math.max(maxSum, windowSum);\n            System.out.println("Окно [" + (i-k+1) + ".." + i + "]: сумма=" + windowSum);\n        }\n        return maxSum;\n    }\n\n    public static void main(String[] args) {\n        int[] nums = {2, 1, 5, 1, 3, 2};\n        System.out.println("Макс сумма (k=3): " + maxSumFixed(nums, 3)); // 9\n    }\n}' },
        { type: 'heading', value: 'Трассировка для [2,1,5,1,3,2], k=3' },
        { type: 'code', language: 'java', value: '// nums = [2, 1, 5, 1, 3, 2], k=3\n//\n// Первое окно: [2,1,5] -> sum=8\n// Сдвиг: убираем 2, добавляем 1: [1,5,1] -> sum=8-2+1=7\n// Сдвиг: убираем 1, добавляем 3: [5,1,3] -> sum=7-1+3=9  <- макс!\n// Сдвиг: убираем 5, добавляем 2: [1,3,2] -> sum=9-5+2=6\n//\n// Максимальная сумма: 9 (подмассив [5,1,3])\n//\n// БЕЗ скользящего окна (наивно O(n*k)):\n// Каждый раз суммируем k элементов заново\n// С окном O(n): одно сложение и одно вычитание на шаг!' },
        { type: 'note', value: 'Фиксированное окно: O(n) вместо O(n*k). Ключевая идея: windowSum = windowSum + nums[i] - nums[i-k]. Мы "переиспользуем" предыдущую сумму.' }
      ]
    },
    {
      id: 4,
      title: 'Переменное скользящее окно',
      type: 'theory',
      content: [
        { type: 'text', value: 'Переменное скользящее окно меняет размер динамически: расширяется вправо при добавлении нового элемента и сужается слева, когда условие нарушается. Используется для поиска минимального/максимального окна с заданным условием.' },
        { type: 'tip', value: 'Представь резиновое окно. Ты растягиваешь его вправо, пока всё хорошо. Когда стало "плохо" (например, сумма превысила лимит), сжимаешь слева. Продолжаешь растягивать. Это позволяет найти минимальное окно, которое удовлетворяет условию.' },
        { type: 'heading', value: 'Минимальный подмассив с суммой >= target' },
        { type: 'code', language: 'java', value: 'public static int minSubarrayLen(int target, int[] nums) {\n    int left = 0;\n    int sum = 0;\n    int minLen = Integer.MAX_VALUE;\n\n    for (int right = 0; right < nums.length; right++) {\n        sum += nums[right]; // Расширяем окно вправо\n\n        while (sum >= target) { // Условие выполнено — пробуем сжать\n            minLen = Math.min(minLen, right - left + 1);\n            sum -= nums[left];\n            left++; // Сжимаем слева\n        }\n    }\n    return minLen == Integer.MAX_VALUE ? 0 : minLen;\n}\n\n// nums=[2,3,1,2,4,3], target=7\n// Окно расширяется: [2], [2,3], [2,3,1], [2,3,1,2] sum=8 >= 7!\n// Сжимаем: [3,1,2] sum=6 < 7, стоп. minLen=4\n// Расширяем: [3,1,2,4] sum=10 >= 7!\n// Сжимаем: [1,2,4] sum=7 >= 7! minLen=3\n// Сжимаем: [2,4] sum=6 < 7, стоп\n// Расширяем: [2,4,3] sum=9 >= 7!\n// Сжимаем: [4,3] sum=7 >= 7! minLen=2\n// Сжимаем: [3] sum=3 < 7, стоп\n// Ответ: 2 (подмассив [4,3])' },
        { type: 'heading', value: 'Шаблон переменного окна' },
        { type: 'code', language: 'java', value: '// Общий шаблон переменного скользящего окна:\n//\n// int left = 0;\n// for (int right = 0; right < n; right++) {\n//     // 1. Добавить nums[right] в состояние окна\n//     addToWindow(nums[right]);\n//\n//     // 2. Пока условие нарушено — сжимаем слева\n//     while (conditionViolated()) {\n//         removeFromWindow(nums[left]);\n//         left++;\n//     }\n//\n//     // 3. Обновляем ответ (окно [left..right] корректно)\n//     updateAnswer(right - left + 1);\n// }\n\nSystem.out.println("Шаблон переменного окна — запомни эти три шага!");\nSystem.out.println("1. Расширить окно (right++)");\nSystem.out.println("2. Сжать окно пока нарушено условие (left++)");\nSystem.out.println("3. Обновить ответ");' },
        { type: 'note', value: 'Переменное окно: O(n) — каждый элемент добавляется и удаляется не более одного раза. left и right вместе делают не более 2n шагов.' }
      ]
    },
    {
      id: 5,
      title: 'Наибольшая подстрока без повторяющихся символов',
      type: 'theory',
      content: [
        { type: 'text', value: 'Задача: найти длину наибольшей подстроки без повторяющихся символов. Классическая задача на переменное скользящее окно с HashSet для отслеживания уникальных символов.' },
        { type: 'tip', value: 'Представь, что ты пишешь пароль и хочешь, чтобы все символы были разные. Скользи по строке окном. Как только появляется дубликат — сжимай окно слева, пока дубликат не уйдёт. Запоминай максимальный размер окна.' },
        { type: 'code', language: 'java', value: 'import java.util.HashSet;\nimport java.util.Set;\n\npublic class LongestSubstringUnique {\n    public static int lengthOfLongestSubstring(String s) {\n        Set<Character> window = new HashSet<>();\n        int left = 0;\n        int maxLen = 0;\n\n        for (int right = 0; right < s.length(); right++) {\n            char c = s.charAt(right);\n\n            // Пока c уже в окне — удаляем слева\n            while (window.contains(c)) {\n                window.remove(s.charAt(left));\n                left++;\n            }\n\n            window.add(c);\n            maxLen = Math.max(maxLen, right - left + 1);\n        }\n        return maxLen;\n    }\n\n    public static void main(String[] args) {\n        System.out.println(lengthOfLongestSubstring("abcabcbb")); // 3 ("abc")\n        System.out.println(lengthOfLongestSubstring("bbbbb"));    // 1 ("b")\n        System.out.println(lengthOfLongestSubstring("pwwkew"));   // 3 ("wke")\n    }\n}' },
        { type: 'heading', value: 'Трассировка для "abcabcbb"' },
        { type: 'code', language: 'java', value: '// s = "abcabcbb"\n//       01234567\n//\n// right=0 \'a\': window={a}, maxLen=1\n// right=1 \'b\': window={a,b}, maxLen=2\n// right=2 \'c\': window={a,b,c}, maxLen=3\n// right=3 \'a\': \'a\' в window!\n//   Удаляем left=0 (\'a\'), left=1. window={b,c}\n//   Добавляем \'a\': window={b,c,a}, maxLen=3\n// right=4 \'b\': \'b\' в window!\n//   Удаляем left=1 (\'b\'), left=2. window={c,a}\n//   Добавляем \'b\': window={c,a,b}, maxLen=3\n// right=5 \'c\': \'c\' в window!\n//   Удаляем left=2 (\'c\'), left=3. window={a,b}\n//   Добавляем \'c\': window={a,b,c}, maxLen=3\n// right=6 \'b\': \'b\' в window!\n//   Удаляем left=3 (\'a\'), left=4. window={b,c}\n//   Удаляем left=4 (\'b\'), left=5. window={c}\n//   Добавляем \'b\': window={c,b}, maxLen=3\n// right=7 \'b\': \'b\' в window!\n//   Удаляем left=5 (\'c\'), left=6. window={b}\n//   Удаляем left=6 (\'b\'), left=7. window={}\n//   Добавляем \'b\': window={b}, maxLen=3\n//\n// Ответ: 3 ("abc")' },
        { type: 'heading', value: 'Оптимизация с HashMap (прыжок через дубликат)' },
        { type: 'code', language: 'java', value: 'import java.util.HashMap;\n\npublic static int lengthOfLongestSubstringFast(String s) {\n    HashMap<Character, Integer> lastIndex = new HashMap<>();\n    int left = 0, maxLen = 0;\n\n    for (int right = 0; right < s.length(); right++) {\n        char c = s.charAt(right);\n        // Если c уже встречался и его индекс >= left — прыгаем!\n        if (lastIndex.containsKey(c) && lastIndex.get(c) >= left) {\n            left = lastIndex.get(c) + 1; // Прыгаем за дубликат\n        }\n        lastIndex.put(c, right);\n        maxLen = Math.max(maxLen, right - left + 1);\n    }\n    return maxLen;\n}\n// "abcabcbb": left прыгает сразу при дубликате,\n// не удаляет по одному элементу — ещё быстрее на практике' },
        { type: 'note', value: 'Обе версии работают за O(n), но HashMap-версия на практике быстрее: left прыгает сразу к нужной позиции, не итерируя.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Два указателя — поиск пар',
      type: 'practice',
      difficulty: 'easy',
      description: 'Реализуй задачи с двумя указателями: Two Sum, Three Sum и разворот массива.',
      requirements: [
        'Метод twoSum(int[] nums, int target): вернуть индексы, nums отсортирован',
        'Метод twoSumCount(int[] nums, int target): сколько пар с суммой target',
        'Метод reverseArray(int[] nums): развернуть массив двумя указателями',
        'Метод isPalindrome(String s): проверка палиндрома двумя указателями',
        'Вывести трассировку для twoSum: состояние указателей на каждом шаге'
      ],
      expectedOutput: 'twoSum([2,7,11,15], 9): [0,1]\nШаги: left=0(2),right=3(15) sum=17>9 right--; left=0(2),right=2(11) sum=13>9 right--; left=0(2),right=1(7) sum=9 НАШЛИ!\ntwoSumCount([1,2,3,4,5,6,7], 8): 3 пары\nreverseArray([1,2,3,4,5]): [5,4,3,2,1]\nisPalindrome("racecar"): true\nisPalindrome("hello"): false',
      hint: 'В twoSum: пока left < right, проверяй сумму. twoSumCount: считай пары не возвращая сразу. reverseArray: swap(nums[left], nums[right]), left++, right--. isPalindrome: сравнивай s.charAt(left) и s.charAt(right).',
      solution: 'import java.util.Arrays;\n\npublic class TwoPointersProblems {\n    public static int[] twoSum(int[] nums, int target) {\n        int left = 0, right = nums.length - 1;\n        System.out.print("Шаги: ");\n        while (left < right) {\n            int sum = nums[left] + nums[right];\n            System.out.print("left=" + left + "(" + nums[left] + "),right=" + right + "(" + nums[right] + ") sum=" + sum);\n            if (sum == target) { System.out.println(" НАШЛИ!"); return new int[]{left, right}; }\n            else if (sum < target) { System.out.print(">target left++; "); left++; }\n            else { System.out.print(">"+target+" right--; "); right--; }\n        }\n        return new int[]{-1, -1};\n    }\n\n    public static int twoSumCount(int[] nums, int target) {\n        int left = 0, right = nums.length - 1, count = 0;\n        while (left < right) {\n            int sum = nums[left] + nums[right];\n            if (sum == target) { count++; left++; right--; }\n            else if (sum < target) left++;\n            else right--;\n        }\n        return count;\n    }\n\n    public static void reverseArray(int[] nums) {\n        int left = 0, right = nums.length - 1;\n        while (left < right) {\n            int tmp = nums[left]; nums[left] = nums[right]; nums[right] = tmp;\n            left++; right--;\n        }\n    }\n\n    public static boolean isPalindrome(String s) {\n        int left = 0, right = s.length() - 1;\n        while (left < right) {\n            if (s.charAt(left) != s.charAt(right)) return false;\n            left++; right--;\n        }\n        return true;\n    }\n\n    public static void main(String[] args) {\n        int[] a = {2, 7, 11, 15};\n        int[] r = twoSum(a, 9);\n        System.out.println("twoSum([2,7,11,15], 9): [" + r[0] + "," + r[1] + "]");\n\n        int[] b = {1, 2, 3, 4, 5, 6, 7};\n        System.out.println("twoSumCount([1,2,3,4,5,6,7], 8): " + twoSumCount(b, 8) + " пары");\n\n        int[] c = {1, 2, 3, 4, 5};\n        reverseArray(c);\n        System.out.println("reverseArray([1,2,3,4,5]): " + Arrays.toString(c));\n\n        System.out.println("isPalindrome(\\"racecar\\"): " + isPalindrome("racecar"));\n        System.out.println("isPalindrome(\\"hello\\"): " + isPalindrome("hello"));\n    }\n}',
      explanation: 'Два указателя — мощная техника для задач на отсортированных массивах и симметричных структурах. Ключевое условие для корректности: отсортированный массив гарантирует, что движение левого указателя вправо увеличивает сумму, а правого влево — уменьшает.'
    },
    {
      id: 7,
      title: 'Практика: Скользящее окно — максимальная сумма и уникальная подстрока',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй задачи на скользящее окно: максимальная сумма подмассива и наибольшая подстрока без повторов.',
      requirements: [
        'Метод maxSumSubarray(int[] nums, int k): макс сумма подмассива размера k с трассировкой',
        'Метод minWindowWithSum(int[] nums, int target): минимальный подмассив с суммой >= target',
        'Метод longestUniqueSubstring(String s): длина наибольшей подстроки без повторов',
        'Вывести окно на каждом шаге для longestUniqueSubstring',
        'Тесты: nums=[2,1,5,1,3,2] k=3; target=7; s="abcabcbb"'
      ],
      expectedOutput: 'maxSumSubarray k=3:\n  [2,1,5] = 8\n  [1,5,1] = 7\n  [5,1,3] = 9 <-- макс\n  [1,3,2] = 6\nРезультат: 9\nminWindowWithSum target=7: 2 (подмассив [4,3])\nlongestUniqueSubstring "abcabcbb": 3 ("abc")',
      hint: 'maxSumSubarray: сначала посчитай сумму первых k элементов, затем скользи: sum = sum + nums[right] - nums[right-k]. minWindow: используй left/right с while(sum>=target) { сжимай }. Для строки используй HashSet.',
      solution: 'import java.util.*;\n\npublic class SlidingWindowProblems {\n    public static int maxSumSubarray(int[] nums, int k) {\n        int sum = 0;\n        for (int i = 0; i < k; i++) sum += nums[i];\n        int maxSum = sum;\n        System.out.print("  [");\n        for (int i = 0; i < k; i++) System.out.print(nums[i] + (i<k-1?",":""));\n        System.out.println("] = " + sum);\n        for (int i = k; i < nums.length; i++) {\n            sum = sum + nums[i] - nums[i-k];\n            System.out.print("  [");\n            for (int j = i-k+1; j <= i; j++) System.out.print(nums[j] + (j<i?",":""));\n            System.out.println("] = " + sum + (sum > maxSum ? " <-- макс" : ""));\n            maxSum = Math.max(maxSum, sum);\n        }\n        return maxSum;\n    }\n\n    public static int minWindowWithSum(int[] nums, int target) {\n        int left = 0, sum = 0, minLen = Integer.MAX_VALUE;\n        for (int right = 0; right < nums.length; right++) {\n            sum += nums[right];\n            while (sum >= target) {\n                minLen = Math.min(minLen, right - left + 1);\n                sum -= nums[left++];\n            }\n        }\n        return minLen == Integer.MAX_VALUE ? 0 : minLen;\n    }\n\n    public static int longestUniqueSubstring(String s) {\n        Set<Character> win = new HashSet<>();\n        int left = 0, maxLen = 0;\n        for (int right = 0; right < s.length(); right++) {\n            char c = s.charAt(right);\n            while (win.contains(c)) { win.remove(s.charAt(left++)); }\n            win.add(c);\n            maxLen = Math.max(maxLen, right - left + 1);\n        }\n        return maxLen;\n    }\n\n    public static void main(String[] args) {\n        int[] nums = {2, 1, 5, 1, 3, 2};\n        System.out.println("maxSumSubarray k=3:");\n        System.out.println("Результат: " + maxSumSubarray(nums, 3));\n        System.out.println("minWindowWithSum target=7: " + minWindowWithSum(nums, 7) + " (подмассив [4,3])");\n        System.out.println("longestUniqueSubstring \\"abcabcbb\\": " + longestUniqueSubstring("abcabcbb") + " (\\"abc\\")");\n    }\n}',
      explanation: 'Скользящее окно — это паттерн "добавь справа, убери слева". Фиксированное окно: жёстко убираем элемент i-k. Переменное окно: убираем пока нарушено условие. Оба варианта работают за O(n) — линейное время, что намного лучше O(n²) наивного перебора.'
    }
  ]
}
