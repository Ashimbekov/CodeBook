export default {
  id: 30,
  title: 'Топологическая сортировка',
  description: 'Топологическая сортировка ориентированного ациклического графа (DAG): алгоритм Кана (BFS), DFS-подход, обнаружение циклов и классические задачи — порядок курсов и Alien Dictionary.',
  lessons: [
    {
      id: 1,
      title: 'Что такое топологическая сортировка',
      type: 'theory',
      content: [
        { type: 'text', value: 'Топологическая сортировка — это линейный порядок вершин ориентированного ациклического графа (DAG), при котором для каждого ребра (u, v) вершина u стоит перед v. Это порядок, в котором можно "выполнить" все задачи с учётом зависимостей.' },
        { type: 'tip', value: 'Представьте учебный план: чтобы пройти курс B, нужно сначала пройти A. Чтобы пройти C, нужно пройти B. Топологическая сортировка — это порядок, в котором можно пройти все курсы без нарушения зависимостей: A -> B -> C.' },
        { type: 'heading', value: 'Ключевые свойства' },
        { type: 'list', value: [
          'Существует ТОЛЬКО для DAG (ориентированный ациклический граф)',
          'Если есть цикл — топологическая сортировка невозможна',
          'Может быть несколько допустимых порядков',
          'Первая вершина в порядке — вершина без входящих рёбер (in-degree = 0)'
        ]},
        { type: 'heading', value: 'Примеры' },
        { type: 'code', language: 'java', value: '// Пример 1: Порядок одевания\n//   Трусы -> Брюки -> Ремень\n//   Трусы -> Брюки -> Ботинки\n//   Рубашка -> Ремень\n//   Рубашка -> Галстук\n//\n//   Один из порядков: Трусы, Рубашка, Брюки, Галстук, Ремень, Ботинки\n//   Другой: Рубашка, Трусы, Брюки, Ремень, Галстук, Ботинки\n//   Оба корректны!\n\n// Пример 2: Зависимости курсов\n//   0 -> 1 (для курса 1 нужен курс 0)\n//   0 -> 2\n//   1 -> 3\n//   2 -> 3\n//\n//   Порядок: 0, 1, 2, 3 или 0, 2, 1, 3\n//\n//   Граф:\n//   0 --> 1\n//   |     |\n//   v     v\n//   2 --> 3' },
        { type: 'heading', value: 'In-degree (входящая степень)' },
        { type: 'code', language: 'java', value: '// In-degree вершины = количество входящих рёбер\n// Вершина с in-degree = 0 не имеет зависимостей -> можно начать с неё\n//\n//   0 --> 1\n//   |     |\n//   v     v\n//   2 --> 3\n//\n// in-degree: вершина 0 = 0 (нет входящих)\n//            вершина 1 = 1 (из 0)\n//            вершина 2 = 1 (из 0)\n//            вершина 3 = 2 (из 1 и 2)\n//\n// Начинаем с вершины 0 (in-degree = 0)\n// После обработки 0: уменьшаем in-degree 1 и 2\n// in-degree 1 = 0, in-degree 2 = 0 -> обрабатываем обе\n// После обработки 1 и 2: in-degree 3 = 0 -> обрабатываем\n// Результат: [0, 1, 2, 3] или [0, 2, 1, 3]' },
        { type: 'warning', value: 'Если после обработки всех вершин в результате меньше n элементов — в графе есть цикл! Топологическая сортировка невозможна.' }
      ]
    },
    {
      id: 2,
      title: 'Алгоритм Кана (BFS)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Алгоритм Кана — BFS-подход к топологической сортировке. Начинаем с вершин, у которых in-degree = 0, и последовательно "удаляем" их из графа, уменьшая in-degree соседей.' },
        { type: 'heading', value: 'Реализация алгоритма Кана' },
        { type: 'code', language: 'java', value: 'import java.util.*;\n\npublic class KahnAlgorithm {\n    public static List<Integer> topologicalSort(int numNodes, int[][] edges) {\n        // 1. Строим граф и считаем in-degree\n        List<List<Integer>> graph = new ArrayList<>();\n        int[] inDegree = new int[numNodes];\n        for (int i = 0; i < numNodes; i++) graph.add(new ArrayList<>());\n\n        for (int[] edge : edges) {\n            graph.get(edge[0]).add(edge[1]); // edge[0] -> edge[1]\n            inDegree[edge[1]]++;\n        }\n\n        // 2. Добавляем все вершины с in-degree = 0 в очередь\n        Queue<Integer> queue = new LinkedList<>();\n        for (int i = 0; i < numNodes; i++) {\n            if (inDegree[i] == 0) {\n                queue.offer(i);\n            }\n        }\n\n        // 3. BFS: извлекаем вершину, уменьшаем in-degree соседей\n        List<Integer> result = new ArrayList<>();\n        while (!queue.isEmpty()) {\n            int node = queue.poll();\n            result.add(node);\n\n            for (int neighbor : graph.get(node)) {\n                inDegree[neighbor]--;\n                if (inDegree[neighbor] == 0) {\n                    queue.offer(neighbor); // Все зависимости выполнены!\n                }\n            }\n        }\n\n        // 4. Проверка на цикл\n        if (result.size() != numNodes) {\n            System.out.println("Цикл обнаружен! Топологическая сортировка невозможна.");\n            return Collections.emptyList();\n        }\n\n        return result;\n    }\n\n    public static void main(String[] args) {\n        // Граф: 0->1, 0->2, 1->3, 2->3\n        int[][] edges = {{0,1}, {0,2}, {1,3}, {2,3}};\n        List<Integer> order = topologicalSort(4, edges);\n        System.out.println("Топологический порядок: " + order);\n        // [0, 1, 2, 3] или [0, 2, 1, 3]\n\n        // Граф с циклом: 0->1, 1->2, 2->0\n        int[][] cyclic = {{0,1}, {1,2}, {2,0}};\n        List<Integer> result = topologicalSort(3, cyclic);\n        // \"Цикл обнаружен!\"\n    }\n}' },
        { type: 'heading', value: 'Пошаговая трассировка' },
        { type: 'code', language: 'java', value: '// Граф: 0->1, 0->2, 1->3, 2->3\n// in-degree: [0, 1, 1, 2]\n//\n// Начало: queue = [0] (in-degree[0] = 0)\n//\n// Итерация 1: node = 0, result = [0]\n//   Соседи 0: [1, 2]\n//   in-degree[1]-- = 0 -> queue.add(1)\n//   in-degree[2]-- = 0 -> queue.add(2)\n//   queue = [1, 2]\n//\n// Итерация 2: node = 1, result = [0, 1]\n//   Соседи 1: [3]\n//   in-degree[3]-- = 1 (было 2)\n//   queue = [2]\n//\n// Итерация 3: node = 2, result = [0, 1, 2]\n//   Соседи 2: [3]\n//   in-degree[3]-- = 0 -> queue.add(3)\n//   queue = [3]\n//\n// Итерация 4: node = 3, result = [0, 1, 2, 3]\n//   Соседи 3: []\n//   queue = []\n//\n// result.size() == 4 == numNodes -> Цикла нет!\n// Результат: [0, 1, 2, 3]' },
        { type: 'tip', value: 'Алгоритм Кана интуитивен: сначала выполняем задачи без зависимостей, потом те, чьи зависимости уже выполнены. Это как в реальной жизни — нельзя надеть ботинки, не надев сначала брюки.' }
      ]
    },
    {
      id: 3,
      title: 'DFS подход',
      type: 'theory',
      content: [
        { type: 'text', value: 'Альтернативный подход к топологической сортировке — через DFS с постпорядковым обходом. Вершина добавляется в результат ПОСЛЕ обработки всех её потомков. Затем результат реверсируется.' },
        { type: 'heading', value: 'Реализация через DFS' },
        { type: 'code', language: 'java', value: 'import java.util.*;\n\npublic class TopologicalDFS {\n    private static List<List<Integer>> graph;\n    private static boolean[] visited;\n    private static Stack<Integer> stack; // Для хранения результата\n\n    public static List<Integer> topologicalSort(int numNodes, int[][] edges) {\n        // Строим граф\n        graph = new ArrayList<>();\n        for (int i = 0; i < numNodes; i++) graph.add(new ArrayList<>());\n        for (int[] edge : edges) {\n            graph.get(edge[0]).add(edge[1]);\n        }\n\n        visited = new boolean[numNodes];\n        stack = new Stack<>();\n\n        // Запускаем DFS от каждой непосещённой вершины\n        for (int i = 0; i < numNodes; i++) {\n            if (!visited[i]) {\n                dfs(i);\n            }\n        }\n\n        // Стек содержит результат в обратном порядке\n        List<Integer> result = new ArrayList<>();\n        while (!stack.isEmpty()) {\n            result.add(stack.pop());\n        }\n        return result;\n    }\n\n    private static void dfs(int node) {\n        visited[node] = true;\n\n        // Сначала обходим всех соседей\n        for (int neighbor : graph.get(node)) {\n            if (!visited[neighbor]) {\n                dfs(neighbor);\n            }\n        }\n\n        // ПОСЛЕ обработки всех потомков — добавляем в стек\n        // (постпорядок = post-order)\n        stack.push(node);\n    }\n\n    public static void main(String[] args) {\n        // 5 -> 2, 5 -> 0, 4 -> 0, 4 -> 1, 2 -> 3, 3 -> 1\n        int[][] edges = {{5,2}, {5,0}, {4,0}, {4,1}, {2,3}, {3,1}};\n        List<Integer> order = topologicalSort(6, edges);\n        System.out.println("Топологический порядок (DFS): " + order);\n        // Один из: [5, 4, 2, 3, 1, 0] или [4, 5, 2, 3, 1, 0]\n    }\n}' },
        { type: 'heading', value: 'Почему DFS работает?' },
        { type: 'code', language: 'java', value: '// Граф: 5->2, 5->0, 4->0, 4->1, 2->3, 3->1\n//\n//   5 --> 2 --> 3\n//   |          |\n//   v          v\n//   0          1\n//   ^          ^\n//   |          |\n//   4 --------/\n//\n// DFS от 5:\n//   visit(5)\n//     visit(2)\n//       visit(3)\n//         visit(1) -> нет соседей -> stack.push(1). Stack: [1]\n//       stack.push(3). Stack: [1, 3]\n//     stack.push(2). Stack: [1, 3, 2]\n//     visit(0) -> нет невизитированных соседей -> stack.push(0). Stack: [1, 3, 2, 0]\n//   stack.push(5). Stack: [1, 3, 2, 0, 5]\n//\n// DFS от 4:\n//   visit(4)\n//     0 уже visited, 1 уже visited\n//   stack.push(4). Stack: [1, 3, 2, 0, 5, 4]\n//\n// Pop: [4, 5, 0, 2, 3, 1]\n// Вершина добавляется ПОСЛЕ всех зависимых -> в стеке она ВЫШЕ' },
        { type: 'heading', value: 'BFS (Кан) vs DFS' },
        { type: 'code', language: 'java', value: '// Алгоритм Кана (BFS):\n// + Легко определить цикл (result.size() < n)\n// + Интуитивный: \"убираем вершины без зависимостей\"\n// + Лексикографически наименьший порядок (если использовать PriorityQueue)\n// - Нужен массив in-degree\n//\n// DFS подход:\n// + Не нужен in-degree\n// + Естественный для рекурсии\n// + Легко модифицируется для обнаружения циклов (три цвета)\n// - Результат в обратном порядке (нужен стек)\n//\n// Оба: O(V + E) по времени и пространству' },
        { type: 'tip', value: 'Для задач, где нужен лексикографически наименьший порядок, используйте алгоритм Кана с PriorityQueue вместо обычной Queue. DFS не гарантирует лексикографический порядок.' }
      ]
    },
    {
      id: 4,
      title: 'Обнаружение цикла в DAG',
      type: 'theory',
      content: [
        { type: 'text', value: 'Обнаружение цикла в ориентированном графе — важная подзадача. В алгоритме Кана цикл обнаруживается, если не все вершины попали в результат. В DFS используется система трёх цветов.' },
        { type: 'heading', value: 'Обнаружение цикла через DFS (три цвета)' },
        { type: 'code', language: 'java', value: 'import java.util.*;\n\npublic class CycleDetection {\n    // Три состояния вершины:\n    static final int WHITE = 0; // Не посещена\n    static final int GRAY = 1;  // В процессе обработки (на стеке вызовов)\n    static final int BLACK = 2; // Полностью обработана\n\n    private static List<List<Integer>> graph;\n    private static int[] color;\n\n    public static boolean hasCycle(int numNodes, int[][] edges) {\n        graph = new ArrayList<>();\n        for (int i = 0; i < numNodes; i++) graph.add(new ArrayList<>());\n        for (int[] edge : edges) graph.get(edge[0]).add(edge[1]);\n\n        color = new int[numNodes]; // Все WHITE по умолчанию\n\n        for (int i = 0; i < numNodes; i++) {\n            if (color[i] == WHITE) {\n                if (dfsHasCycle(i)) return true;\n            }\n        }\n        return false;\n    }\n\n    private static boolean dfsHasCycle(int node) {\n        color[node] = GRAY; // Начали обработку\n\n        for (int neighbor : graph.get(node)) {\n            if (color[neighbor] == GRAY) {\n                // Нашли обратное ребро -> ЦИКЛ!\n                System.out.println("Цикл: ребро " + node + " -> " + neighbor +\n                    " (вершина " + neighbor + " ещё в обработке)");\n                return true;\n            }\n            if (color[neighbor] == WHITE) {\n                if (dfsHasCycle(neighbor)) return true;\n            }\n            // BLACK — уже обработана, пропускаем\n        }\n\n        color[node] = BLACK; // Завершили обработку\n        return false;\n    }\n\n    public static void main(String[] args) {\n        // Без цикла: 0->1, 0->2, 1->3, 2->3\n        int[][] dag = {{0,1}, {0,2}, {1,3}, {2,3}};\n        System.out.println("DAG: " + hasCycle(4, dag)); // false\n\n        // С циклом: 0->1, 1->2, 2->0\n        int[][] cyclic = {{0,1}, {1,2}, {2,0}};\n        System.out.println("Цикл: " + hasCycle(3, cyclic)); // true\n\n        // Сложный: 0->1, 1->2, 2->3, 3->1\n        int[][] complex = {{0,1}, {1,2}, {2,3}, {3,1}};\n        System.out.println("Цикл 1->2->3->1: " + hasCycle(4, complex)); // true\n    }\n}' },
        { type: 'heading', value: 'Почему три цвета, а не два?' },
        { type: 'code', language: 'java', value: '// Два цвета (visited/not visited) работают для НЕОРИЕНТИРОВАННЫХ графов.\n// Для ОРИЕНТИРОВАННЫХ — нужны три цвета!\n//\n// Контрпример с двумя цветами:\n//   0 -> 1\n//   0 -> 2\n//   1 -> 3\n//   2 -> 3\n//\n// DFS от 0: visit(0) -> visit(1) -> visit(3) -> done\n//           вернулись в 0 -> visit(2) -> 3 уже visited!\n//           С двумя цветами: \"3 уже visited\" = цикл? НЕТ! Цикла нет.\n//\n// С тремя цветами:\n//   visit(3) -> color[3] = BLACK (полностью обработан)\n//   visit(2) -> 3 is BLACK (не GRAY!) -> не цикл ✓\n//\n// GRAY = \"я ещё на стеке вызовов, ещё обрабатываюсь\"\n// BLACK = \"я уже полностью обработан\"\n// Цикл = ребро к GRAY вершине (обратное ребро)' },
        { type: 'tip', value: 'Запомните: WHITE -> GRAY -> BLACK. GRAY означает "в процессе DFS". Если встретили GRAY соседа — мы нашли обратное ребро, т.е. цикл. BLACK-сосед — это нормально, просто перекрёстное или прямое ребро.' }
      ]
    },
    {
      id: 5,
      title: 'Порядок выполнения курсов',
      type: 'theory',
      content: [
        { type: 'text', value: 'Классическая задача (LeetCode 207/210): даны n курсов и список зависимостей. Нужно определить, можно ли пройти все курсы (нет ли цикла), и если да — в каком порядке.' },
        { type: 'heading', value: 'Course Schedule (LeetCode 207)' },
        { type: 'code', language: 'java', value: 'import java.util.*;\n\npublic class CourseSchedule {\n    // Можно ли пройти все курсы?\n    public static boolean canFinish(int numCourses, int[][] prerequisites) {\n        List<List<Integer>> graph = new ArrayList<>();\n        int[] inDegree = new int[numCourses];\n        for (int i = 0; i < numCourses; i++) graph.add(new ArrayList<>());\n\n        for (int[] prereq : prerequisites) {\n            // prereq = [course, prerequisite]\n            // prerequisite -> course\n            graph.get(prereq[1]).add(prereq[0]);\n            inDegree[prereq[0]]++;\n        }\n\n        Queue<Integer> queue = new LinkedList<>();\n        for (int i = 0; i < numCourses; i++) {\n            if (inDegree[i] == 0) queue.offer(i);\n        }\n\n        int count = 0;\n        while (!queue.isEmpty()) {\n            int course = queue.poll();\n            count++;\n            for (int next : graph.get(course)) {\n                if (--inDegree[next] == 0) {\n                    queue.offer(next);\n                }\n            }\n        }\n\n        return count == numCourses; // Все курсы обработаны?\n    }\n\n    public static void main(String[] args) {\n        // Можно: 0->1 (для курса 1 нужен курс 0)\n        System.out.println(canFinish(2, new int[][]{{1,0}})); // true\n\n        // Нельзя: 0->1, 1->0 (цикл)\n        System.out.println(canFinish(2, new int[][]{{1,0},{0,1}})); // false\n\n        // Можно: 0->1, 0->2, 1->3, 2->3\n        System.out.println(canFinish(4,\n            new int[][]{{1,0},{2,0},{3,1},{3,2}})); // true\n    }\n}' },
        { type: 'heading', value: 'Course Schedule II — найти порядок (LeetCode 210)' },
        { type: 'code', language: 'java', value: 'import java.util.*;\n\npublic class CourseScheduleII {\n    public static int[] findOrder(int numCourses, int[][] prerequisites) {\n        List<List<Integer>> graph = new ArrayList<>();\n        int[] inDegree = new int[numCourses];\n        for (int i = 0; i < numCourses; i++) graph.add(new ArrayList<>());\n\n        for (int[] prereq : prerequisites) {\n            graph.get(prereq[1]).add(prereq[0]);\n            inDegree[prereq[0]]++;\n        }\n\n        Queue<Integer> queue = new LinkedList<>();\n        for (int i = 0; i < numCourses; i++) {\n            if (inDegree[i] == 0) queue.offer(i);\n        }\n\n        int[] order = new int[numCourses];\n        int idx = 0;\n        while (!queue.isEmpty()) {\n            int course = queue.poll();\n            order[idx++] = course;\n            for (int next : graph.get(course)) {\n                if (--inDegree[next] == 0) {\n                    queue.offer(next);\n                }\n            }\n        }\n\n        if (idx != numCourses) return new int[0]; // Цикл!\n        return order;\n    }\n\n    public static void main(String[] args) {\n        // Зависимости: для 1 нужен 0, для 2 нужен 0,\n        //              для 3 нужны 1 и 2\n        int[][] prereqs = {{1,0}, {2,0}, {3,1}, {3,2}};\n        int[] order = findOrder(4, prereqs);\n        System.out.println("Порядок курсов: " + Arrays.toString(order));\n        // [0, 1, 2, 3] или [0, 2, 1, 3]\n\n        // С циклом\n        int[][] cyclic = {{1,0}, {0,1}};\n        int[] result = findOrder(2, cyclic);\n        System.out.println("С циклом: " + Arrays.toString(result)); // []\n    }\n}' },
        { type: 'tip', value: 'Course Schedule — одна из самых частых задач на собеседованиях. Она проверяет знание топологической сортировки и обнаружения циклов. Запомните паттерн: граф + in-degree + BFS.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Course Schedule',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте решение задачи Course Schedule: даны n курсов (0..n-1) и список пар зависимостей. Определите, можно ли пройти все курсы, и если да — выведите один из допустимых порядков.',
      requirements: [
        'Класс CourseScheduleSolver с алгоритмом Кана',
        'Метод canFinish(int numCourses, int[][] prerequisites): boolean',
        'Метод findOrder(int numCourses, int[][] prerequisites): int[]',
        'Обнаружение цикла: вернуть пустой массив, если цикл есть',
        'Тест 1: 4 курса, зависимости [[1,0],[2,0],[3,1],[3,2]] -> порядок',
        'Тест 2: 2 курса, зависимости [[1,0],[0,1]] -> цикл'
      ],
      expectedOutput: 'Тест 1: numCourses=4, prereqs=[[1,0],[2,0],[3,1],[3,2]]\nМожно пройти: true\nПорядок: [0, 1, 2, 3]\n\nТест 2: numCourses=2, prereqs=[[1,0],[0,1]]\nМожно пройти: false\nПорядок: []\n\nТест 3: numCourses=5, prereqs=[[1,0],[2,1],[3,2],[4,3]]\nМожно пройти: true\nПорядок: [0, 1, 2, 3, 4]',
      hint: 'Используйте алгоритм Кана: (1) посчитайте in-degree для каждой вершины, (2) добавьте в очередь вершины с in-degree=0, (3) BFS — для каждого извлечённого узла уменьшайте in-degree соседей, (4) если result.size() < numCourses — цикл.',
      solution: 'import java.util.*;\n\npublic class CourseScheduleSolver {\n    public static boolean canFinish(int numCourses, int[][] prerequisites) {\n        return findOrder(numCourses, prerequisites).length > 0;\n    }\n\n    public static int[] findOrder(int numCourses, int[][] prerequisites) {\n        List<List<Integer>> graph = new ArrayList<>();\n        int[] inDegree = new int[numCourses];\n        for (int i = 0; i < numCourses; i++) graph.add(new ArrayList<>());\n\n        for (int[] pre : prerequisites) {\n            graph.get(pre[1]).add(pre[0]);\n            inDegree[pre[0]]++;\n        }\n\n        Queue<Integer> queue = new LinkedList<>();\n        for (int i = 0; i < numCourses; i++) {\n            if (inDegree[i] == 0) queue.offer(i);\n        }\n\n        int[] order = new int[numCourses];\n        int idx = 0;\n        while (!queue.isEmpty()) {\n            int course = queue.poll();\n            order[idx++] = course;\n            for (int next : graph.get(course)) {\n                if (--inDegree[next] == 0) {\n                    queue.offer(next);\n                }\n            }\n        }\n\n        return idx == numCourses ? order : new int[0];\n    }\n\n    public static void main(String[] args) {\n        int[][] p1 = {{1,0},{2,0},{3,1},{3,2}};\n        System.out.println("Тест 1: numCourses=4, prereqs=" + Arrays.deepToString(p1));\n        System.out.println("Можно пройти: " + canFinish(4, p1));\n        System.out.println("Порядок: " + Arrays.toString(findOrder(4, p1)));\n        System.out.println();\n\n        int[][] p2 = {{1,0},{0,1}};\n        System.out.println("Тест 2: numCourses=2, prereqs=" + Arrays.deepToString(p2));\n        System.out.println("Можно пройти: " + canFinish(2, p2));\n        System.out.println("Порядок: " + Arrays.toString(findOrder(2, p2)));\n        System.out.println();\n\n        int[][] p3 = {{1,0},{2,1},{3,2},{4,3}};\n        System.out.println("Тест 3: numCourses=5, prereqs=" + Arrays.deepToString(p3));\n        System.out.println("Можно пройти: " + canFinish(5, p3));\n        System.out.println("Порядок: " + Arrays.toString(findOrder(5, p3)));\n    }\n}',
      explanation: 'Алгоритм Кана: строим граф зависимостей и считаем in-degree. Начинаем с курсов без зависимостей (in-degree=0). Извлекая курс из очереди, уменьшаем in-degree его "зависимых" курсов. Если in-degree стал 0 — курс можно пройти, добавляем в очередь. Если обработали все n курсов — цикла нет. Иначе — цикл, невозможно пройти все курсы. Сложность: O(V + E).'
    },
    {
      id: 7,
      title: 'Практика: Alien Dictionary',
      type: 'practice',
      difficulty: 'hard',
      description: 'Дан отсортированный словарь инопланетного языка (массив строк). Определите порядок букв в этом языке. Если порядок невозможен (цикл), верните пустую строку.',
      requirements: [
        'Метод alienOrder(String[] words): String — порядок символов',
        'Построить граф зависимостей из пар соседних слов',
        'Сравнить каждую пару соседних слов: первое различие даёт ребро char1 -> char2',
        'Топологическая сортировка графа символов (алгоритм Кана)',
        'Обнаружение цикла: если цикл — вернуть ""',
        'Обнаружение ошибки: если "abc" стоит перед "ab" — это невалидный словарь',
        'Тест: ["wrt","wrf","er","ett","rftt"] -> "wertf"'
      ],
      expectedOutput: 'words = [wrt, wrf, er, ett, rftt]\nПорядок: wertf\n\nwords = [z, x]\nПорядок: zx\n\nwords = [z, x, z]\nПорядок:  (пустая строка — цикл)',
      hint: 'Шаг 1: собрать все уникальные символы. Шаг 2: для каждой пары соседних слов найти первое различие — это даёт ребро (char_word1 -> char_word2). Шаг 3: топосортировка Каном. Особый случай: если word1 длиннее word2 и word2 является префиксом word1 — это невалидный порядок.',
      solution: 'import java.util.*;\n\npublic class AlienDictionary {\n    public static String alienOrder(String[] words) {\n        // 1. Собираем все символы\n        Map<Character, Set<Character>> graph = new HashMap<>();\n        Map<Character, Integer> inDegree = new HashMap<>();\n        for (String word : words) {\n            for (char c : word.toCharArray()) {\n                graph.putIfAbsent(c, new HashSet<>());\n                inDegree.putIfAbsent(c, 0);\n            }\n        }\n\n        // 2. Строим рёбра из пар соседних слов\n        for (int i = 0; i < words.length - 1; i++) {\n            String w1 = words[i], w2 = words[i + 1];\n            // Проверка: \"abc\" перед \"ab\" — невалидно\n            if (w1.length() > w2.length() && w1.startsWith(w2)) {\n                return "";\n            }\n            // Находим первое различие\n            int minLen = Math.min(w1.length(), w2.length());\n            for (int j = 0; j < minLen; j++) {\n                char c1 = w1.charAt(j), c2 = w2.charAt(j);\n                if (c1 != c2) {\n                    if (!graph.get(c1).contains(c2)) {\n                        graph.get(c1).add(c2);\n                        inDegree.merge(c2, 1, Integer::sum);\n                    }\n                    break; // Только первое различие информативно!\n                }\n            }\n        }\n\n        // 3. Алгоритм Кана\n        Queue<Character> queue = new LinkedList<>();\n        for (Map.Entry<Character, Integer> e : inDegree.entrySet()) {\n            if (e.getValue() == 0) queue.offer(e.getKey());\n        }\n\n        StringBuilder result = new StringBuilder();\n        while (!queue.isEmpty()) {\n            char c = queue.poll();\n            result.append(c);\n            for (char neighbor : graph.get(c)) {\n                inDegree.merge(neighbor, -1, Integer::sum);\n                if (inDegree.get(neighbor) == 0) {\n                    queue.offer(neighbor);\n                }\n            }\n        }\n\n        // 4. Проверка на цикл\n        if (result.length() != inDegree.size()) return "";\n        return result.toString();\n    }\n\n    public static void main(String[] args) {\n        String[] w1 = {"wrt", "wrf", "er", "ett", "rftt"};\n        System.out.println("words = " + Arrays.toString(w1));\n        System.out.println("Порядок: " + alienOrder(w1));\n        System.out.println();\n\n        String[] w2 = {"z", "x"};\n        System.out.println("words = " + Arrays.toString(w2));\n        System.out.println("Порядок: " + alienOrder(w2));\n        System.out.println();\n\n        String[] w3 = {"z", "x", "z"};\n        System.out.println("words = " + Arrays.toString(w3));\n        System.out.println("Порядок: " + alienOrder(w3));\n    }\n}',
      explanation: 'Alien Dictionary — топологическая сортировка на символах. Из каждой пары соседних слов извлекаем одно ребро: первое различие символов означает, что символ из первого слова идёт раньше. Например, "wrt" < "wrf" даёт ребро t->f. Собрав все рёбра, запускаем алгоритм Кана. Особый случай: если word1 длиннее word2 и начинается с word2 — это противоречие (в любом языке "ab" < "abc"). Сложность: O(C), где C — суммарная длина всех слов.'
    }
  ]
}
