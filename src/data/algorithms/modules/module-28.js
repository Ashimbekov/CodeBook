export default {
  id: 28,
  title: 'Segment Tree',
  description: 'Дерево отрезков (Segment Tree) для эффективных запросов и обновлений на отрезках массива. Построение, range query, point update и lazy propagation.',
  lessons: [
    {
      id: 1,
      title: 'Зачем нужен Segment Tree',
      type: 'theory',
      content: [
        { type: 'text', value: 'Segment Tree (дерево отрезков) — структура данных для эффективного выполнения запросов на отрезках массива: сумма, минимум, максимум и обновление элементов. Построение за O(n), запрос и обновление за O(log n).' },
        { type: 'tip', value: 'Представьте, что у вас массив из миллиона чисел и тысяча вопросов: "Какова сумма с позиции L до R?". Наивный подход — каждый раз считать сумму за O(n). Prefix sum — O(1) запрос, но обновление элемента — O(n). Segment Tree — O(log n) на оба!' },
        { type: 'heading', value: 'Сравнение подходов' },
        { type: 'code', language: 'java', value: '// Задача: массив int[], запросы sum(L, R), обновление update(i, val)\n//\n// Наивный подход:\n//   sum(L, R):   O(n) — суммируем от L до R\n//   update(i):   O(1) — просто меняем элемент\n//\n// Prefix Sum:\n//   sum(L, R):   O(1) — prefix[R+1] - prefix[L]\n//   update(i):   O(n) — пересчитываем все prefix от i\n//\n// Segment Tree:\n//   build:       O(n) — один раз\n//   sum(L, R):   O(log n)\n//   update(i):   O(log n)\n//\n// Segment Tree побеждает, когда есть MIX запросов и обновлений!' },
        { type: 'heading', value: 'Структура дерева отрезков' },
        { type: 'code', language: 'java', value: '// Массив: [2, 1, 5, 3, 4]\n//\n// Дерево отрезков (суммы):\n//\n//                  [15]           <- сумма [0..4] = 15\n//                /      \\\n//            [8]          [7]     <- [0..2]=8, [3..4]=7\n//           /   \\        /   \\\n//        [3]    [5]    [3]    [4] <- [0..1]=3, [2..2]=5, [3..3]=3, [4..4]=4\n//       /   \\\n//     [2]   [1]                   <- [0..0]=2, [1..1]=1\n//\n// Каждый узел хранит агрегат (сумму) своего отрезка\n// Листья = элементы массива\n// Внутренние узлы = сумма дочерних узлов\n//\n// Хранение: массив tree[] размера 4*n\n// Для узла i:\n//   Левый ребёнок: 2*i + 1\n//   Правый ребёнок: 2*i + 2' },
        { type: 'warning', value: 'Размер массива tree должен быть 4*n, а не 2*n. Это связано с тем, что дерево не всегда является полным бинарным деревом, и нужен запас.' }
      ]
    },
    {
      id: 2,
      title: 'Построение дерева отрезков',
      type: 'theory',
      content: [
        { type: 'text', value: 'Дерево отрезков строится рекурсивно снизу вверх. Листья соответствуют элементам массива. Каждый внутренний узел — сумма (или мин/макс) его дочерних узлов.' },
        { type: 'heading', value: 'Построение Segment Tree для суммы' },
        { type: 'code', language: 'java', value: 'public class SegmentTree {\n    private int[] tree; // Массив дерева\n    private int n;      // Размер исходного массива\n\n    public SegmentTree(int[] arr) {\n        n = arr.length;\n        tree = new int[4 * n]; // Запас для хранения\n        build(arr, 0, 0, n - 1);\n    }\n\n    // Рекурсивное построение\n    // node — индекс текущего узла в tree[]\n    // start, end — границы отрезка, за который отвечает узел\n    private void build(int[] arr, int node, int start, int end) {\n        if (start == end) {\n            // Лист: храним элемент массива\n            tree[node] = arr[start];\n        } else {\n            int mid = (start + end) / 2;\n            // Строим левое поддерево (отрезок [start, mid])\n            build(arr, 2 * node + 1, start, mid);\n            // Строим правое поддерево (отрезок [mid+1, end])\n            build(arr, 2 * node + 2, mid + 1, end);\n            // Текущий узел = сумма дочерних\n            tree[node] = tree[2 * node + 1] + tree[2 * node + 2];\n        }\n    }\n\n    public static void main(String[] args) {\n        int[] arr = {2, 1, 5, 3, 4};\n        SegmentTree st = new SegmentTree(arr);\n        System.out.println("Дерево построено для массива [2, 1, 5, 3, 4]");\n        System.out.println("Корень (сумма всех): " + st.tree[0]); // 15\n    }\n}' },
        { type: 'heading', value: 'Трассировка построения' },
        { type: 'code', language: 'java', value: '// arr = [2, 1, 5, 3, 4]\n//\n// build(0, 0, 4):  отрезок [0..4]\n//   build(1, 0, 2):  отрезок [0..2]\n//     build(3, 0, 1):  отрезок [0..1]\n//       build(7, 0, 0):  лист -> tree[7] = arr[0] = 2\n//       build(8, 1, 1):  лист -> tree[8] = arr[1] = 1\n//     tree[3] = tree[7] + tree[8] = 2 + 1 = 3\n//     build(4, 2, 2):  лист -> tree[4] = arr[2] = 5\n//   tree[1] = tree[3] + tree[4] = 3 + 5 = 8\n//   build(2, 3, 4):  отрезок [3..4]\n//     build(5, 3, 3):  лист -> tree[5] = arr[3] = 3\n//     build(6, 4, 4):  лист -> tree[6] = arr[4] = 4\n//   tree[2] = tree[5] + tree[6] = 3 + 4 = 7\n// tree[0] = tree[1] + tree[2] = 8 + 7 = 15\n//\n// Итог: tree = [15, 8, 7, 3, 5, 3, 4, 2, 1, ...]' },
        { type: 'tip', value: 'Построение занимает O(n) времени — каждый элемент массива обрабатывается ровно один раз. Каждый внутренний узел вычисляется за O(1) из двух дочерних.' }
      ]
    },
    {
      id: 3,
      title: 'Запрос на отрезке (range query)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Range query — запрос на отрезке [L, R]. Мы спускаемся по дереву и комбинируем ответы только тех узлов, чьи отрезки полностью попадают в [L, R].' },
        { type: 'heading', value: 'Range Sum Query' },
        { type: 'code', language: 'java', value: 'public class SegmentTree {\n    private int[] tree;\n    private int n;\n\n    public SegmentTree(int[] arr) {\n        n = arr.length;\n        tree = new int[4 * n];\n        build(arr, 0, 0, n - 1);\n    }\n\n    private void build(int[] arr, int node, int start, int end) {\n        if (start == end) {\n            tree[node] = arr[start];\n        } else {\n            int mid = (start + end) / 2;\n            build(arr, 2 * node + 1, start, mid);\n            build(arr, 2 * node + 2, mid + 1, end);\n            tree[node] = tree[2 * node + 1] + tree[2 * node + 2];\n        }\n    }\n\n    // Запрос суммы на отрезке [l, r]\n    public int query(int l, int r) {\n        return query(0, 0, n - 1, l, r);\n    }\n\n    private int query(int node, int start, int end, int l, int r) {\n        // Случай 1: отрезок узла полностью вне запроса\n        if (r < start || end < l) {\n            return 0; // Нейтральный элемент для суммы\n        }\n        // Случай 2: отрезок узла полностью внутри запроса\n        if (l <= start && end <= r) {\n            return tree[node]; // Берём весь узел целиком\n        }\n        // Случай 3: частичное пересечение — идём в оба поддерева\n        int mid = (start + end) / 2;\n        int leftSum = query(2 * node + 1, start, mid, l, r);\n        int rightSum = query(2 * node + 2, mid + 1, end, l, r);\n        return leftSum + rightSum;\n    }\n\n    public static void main(String[] args) {\n        int[] arr = {2, 1, 5, 3, 4};\n        SegmentTree st = new SegmentTree(arr);\n\n        System.out.println("sum(0, 2) = " + st.query(0, 2)); // 2+1+5 = 8\n        System.out.println("sum(1, 3) = " + st.query(1, 3)); // 1+5+3 = 9\n        System.out.println("sum(2, 4) = " + st.query(2, 4)); // 5+3+4 = 12\n        System.out.println("sum(0, 4) = " + st.query(0, 4)); // 2+1+5+3+4 = 15\n        System.out.println("sum(3, 3) = " + st.query(3, 3)); // 3\n    }\n}' },
        { type: 'heading', value: 'Трассировка query(1, 3)' },
        { type: 'code', language: 'java', value: '// arr = [2, 1, 5, 3, 4], запрос: сумма [1..3]\n//\n// query(node=0, [0..4], l=1, r=3):\n//   Частичное пересечение -> идём в оба поддерева\n//\n//   query(node=1, [0..2], l=1, r=3):\n//     Частичное пересечение (0 < 1)\n//\n//     query(node=3, [0..1], l=1, r=3):\n//       Частичное пересечение (0 < 1)\n//\n//       query(node=7, [0..0], l=1, r=3):\n//         [0..0] вне [1..3] -> return 0\n//       query(node=8, [1..1], l=1, r=3):\n//         [1..1] внутри [1..3] -> return tree[8] = 1\n//     return 0 + 1 = 1\n//\n//     query(node=4, [2..2], l=1, r=3):\n//       [2..2] внутри [1..3] -> return tree[4] = 5\n//   return 1 + 5 = 6\n//\n//   query(node=2, [3..4], l=1, r=3):\n//     Частичное пересечение (4 > 3)\n//\n//     query(node=5, [3..3], l=1, r=3):\n//       [3..3] внутри [1..3] -> return tree[5] = 3\n//     query(node=6, [4..4], l=1, r=3):\n//       [4..4] вне [1..3] -> return 0\n//   return 3 + 0 = 3\n//\n// return 6 + 3 = 9  ✓  (1+5+3 = 9)' },
        { type: 'tip', value: 'На каждом уровне дерева мы посещаем максимум 4 узла (на практике обычно 2). Уровней log n — итого O(log n) на запрос.' }
      ]
    },
    {
      id: 4,
      title: 'Обновление элемента (point update)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Point update — обновление одного элемента массива. Меняем значение в листе и пересчитываем все узлы-предки снизу вверх. Сложность O(log n).' },
        { type: 'heading', value: 'Реализация point update' },
        { type: 'code', language: 'java', value: 'public class SegmentTree {\n    private int[] tree;\n    private int n;\n\n    public SegmentTree(int[] arr) {\n        n = arr.length;\n        tree = new int[4 * n];\n        build(arr, 0, 0, n - 1);\n    }\n\n    private void build(int[] arr, int node, int start, int end) {\n        if (start == end) {\n            tree[node] = arr[start];\n        } else {\n            int mid = (start + end) / 2;\n            build(arr, 2 * node + 1, start, mid);\n            build(arr, 2 * node + 2, mid + 1, end);\n            tree[node] = tree[2 * node + 1] + tree[2 * node + 2];\n        }\n    }\n\n    // Обновить arr[idx] = val\n    public void update(int idx, int val) {\n        update(0, 0, n - 1, idx, val);\n    }\n\n    private void update(int node, int start, int end, int idx, int val) {\n        if (start == end) {\n            // Нашли лист — обновляем\n            tree[node] = val;\n        } else {\n            int mid = (start + end) / 2;\n            if (idx <= mid) {\n                // Индекс в левом поддереве\n                update(2 * node + 1, start, mid, idx, val);\n            } else {\n                // Индекс в правом поддереве\n                update(2 * node + 2, mid + 1, end, idx, val);\n            }\n            // Пересчитываем текущий узел\n            tree[node] = tree[2 * node + 1] + tree[2 * node + 2];\n        }\n    }\n\n    public int query(int l, int r) {\n        return query(0, 0, n - 1, l, r);\n    }\n\n    private int query(int node, int start, int end, int l, int r) {\n        if (r < start || end < l) return 0;\n        if (l <= start && end <= r) return tree[node];\n        int mid = (start + end) / 2;\n        return query(2 * node + 1, start, mid, l, r)\n             + query(2 * node + 2, mid + 1, end, l, r);\n    }\n\n    public static void main(String[] args) {\n        int[] arr = {2, 1, 5, 3, 4};\n        SegmentTree st = new SegmentTree(arr);\n\n        System.out.println("sum(0, 4) = " + st.query(0, 4)); // 15\n        System.out.println("sum(1, 3) = " + st.query(1, 3)); // 9\n\n        // Обновляем: arr[2] = 10 (было 5)\n        st.update(2, 10);\n        System.out.println("\\nПосле update(2, 10):");\n        System.out.println("sum(0, 4) = " + st.query(0, 4)); // 20 (было 15, +5)\n        System.out.println("sum(1, 3) = " + st.query(1, 3)); // 14 (было 9, +5)\n        System.out.println("sum(2, 2) = " + st.query(2, 2)); // 10\n    }\n}' },
        { type: 'heading', value: 'Трассировка update(2, 10)' },
        { type: 'code', language: 'java', value: '// arr = [2, 1, 5, 3, 4], update(idx=2, val=10)\n//\n// update(node=0, [0..4], idx=2, val=10):\n//   mid=2, idx=2 <= 2 -> идём ВЛЕВО\n//\n//   update(node=1, [0..2], idx=2, val=10):\n//     mid=1, idx=2 > 1 -> идём ВПРАВО\n//\n//     update(node=4, [2..2], idx=2, val=10):\n//       start==end -> tree[4] = 10 (было 5)\n//\n//   tree[1] = tree[3] + tree[4] = 3 + 10 = 13 (было 8)\n//\n// tree[0] = tree[1] + tree[2] = 13 + 7 = 20 (было 15)\n//\n// Обновили всего 3 узла (глубина дерева) -> O(log n)' },
        { type: 'tip', value: 'При point update мы проходим ровно один путь от корня до листа. На каждом уровне — один узел. Всего уровней log n. Каждый узел обновляется за O(1). Итого O(log n).' }
      ]
    },
    {
      id: 5,
      title: 'Lazy propagation',
      type: 'theory',
      content: [
        { type: 'text', value: 'Lazy propagation — оптимизация для массовых обновлений на отрезке (range update). Вместо обновления каждого элемента, мы "откладываем" обновление и применяем его только при необходимости.' },
        { type: 'tip', value: 'Без lazy propagation: "прибавить 5 ко всем элементам от L до R" требует O(n) обновлений. С lazy propagation — O(log n). Мы просто помечаем узел: "здесь отложено +5", и реально обновляем только когда спускаемся ниже.' },
        { type: 'heading', value: 'Segment Tree с Lazy Propagation' },
        { type: 'code', language: 'java', value: 'public class LazySegmentTree {\n    private int[] tree;\n    private int[] lazy; // Массив отложенных обновлений\n    private int n;\n\n    public LazySegmentTree(int[] arr) {\n        n = arr.length;\n        tree = new int[4 * n];\n        lazy = new int[4 * n]; // Изначально все 0\n        build(arr, 0, 0, n - 1);\n    }\n\n    private void build(int[] arr, int node, int start, int end) {\n        if (start == end) {\n            tree[node] = arr[start];\n        } else {\n            int mid = (start + end) / 2;\n            build(arr, 2 * node + 1, start, mid);\n            build(arr, 2 * node + 2, mid + 1, end);\n            tree[node] = tree[2 * node + 1] + tree[2 * node + 2];\n        }\n    }\n\n    // "Проталкиваем" отложенное обновление вниз\n    private void pushDown(int node, int start, int end) {\n        if (lazy[node] != 0) {\n            int mid = (start + end) / 2;\n            // Обновляем левого ребёнка\n            tree[2 * node + 1] += lazy[node] * (mid - start + 1);\n            lazy[2 * node + 1] += lazy[node];\n            // Обновляем правого ребёнка\n            tree[2 * node + 2] += lazy[node] * (end - mid);\n            lazy[2 * node + 2] += lazy[node];\n            // Сбрасываем lazy текущего узла\n            lazy[node] = 0;\n        }\n    }\n\n    // Range update: прибавить val ко всем элементам [l, r]\n    public void rangeUpdate(int l, int r, int val) {\n        rangeUpdate(0, 0, n - 1, l, r, val);\n    }\n\n    private void rangeUpdate(int node, int start, int end, int l, int r, int val) {\n        if (r < start || end < l) return; // Вне диапазона\n        if (l <= start && end <= r) {\n            // Отрезок полностью внутри — откладываем\n            tree[node] += val * (end - start + 1);\n            lazy[node] += val;\n            return;\n        }\n        pushDown(node, start, end); // Проталкиваем перед спуском\n        int mid = (start + end) / 2;\n        rangeUpdate(2 * node + 1, start, mid, l, r, val);\n        rangeUpdate(2 * node + 2, mid + 1, end, l, r, val);\n        tree[node] = tree[2 * node + 1] + tree[2 * node + 2];\n    }\n\n    // Range query с учётом lazy\n    public int query(int l, int r) {\n        return query(0, 0, n - 1, l, r);\n    }\n\n    private int query(int node, int start, int end, int l, int r) {\n        if (r < start || end < l) return 0;\n        if (l <= start && end <= r) return tree[node];\n        pushDown(node, start, end); // Проталкиваем перед спуском\n        int mid = (start + end) / 2;\n        return query(2 * node + 1, start, mid, l, r)\n             + query(2 * node + 2, mid + 1, end, l, r);\n    }\n\n    public static void main(String[] args) {\n        int[] arr = {1, 3, 5, 7, 9};\n        LazySegmentTree lst = new LazySegmentTree(arr);\n\n        System.out.println("sum(0, 4) = " + lst.query(0, 4)); // 25\n        System.out.println("sum(1, 3) = " + lst.query(1, 3)); // 15\n\n        // Прибавить 10 ко всем элементам [1, 3]\n        lst.rangeUpdate(1, 3, 10);\n        System.out.println("\\nПосле rangeUpdate(1, 3, +10):");\n        System.out.println("sum(0, 4) = " + lst.query(0, 4)); // 55 (было 25, +30)\n        System.out.println("sum(1, 3) = " + lst.query(1, 3)); // 45 (было 15, +30)\n        System.out.println("sum(0, 0) = " + lst.query(0, 0)); // 1 (не изменился)\n    }\n}' },
        { type: 'heading', value: 'Сложность операций' },
        { type: 'code', language: 'java', value: '// Сложность Segment Tree:\n//\n// Без lazy propagation:\n//   build:         O(n)\n//   point update:  O(log n)\n//   range query:   O(log n)\n//   range update:  O(n) — обновляем каждый лист\n//\n// С lazy propagation:\n//   build:         O(n)\n//   range update:  O(log n) — откладываем обновления\n//   range query:   O(log n) — проталкиваем lazy вниз\n//\n// Пространство: O(n) — массивы tree и lazy по 4*n' },
        { type: 'warning', value: 'Lazy propagation корректна только для коммутативных операций (сложение, min, max, xor). Для некоммутативных операций (присваивание + сложение одновременно) нужна более сложная схема с приоритетами lazy-операций.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Range Sum Query',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте структуру данных NumArray с Segment Tree для ответа на запросы суммы на отрезке и обновления элементов.',
      requirements: [
        'Класс NumArray с конструктором NumArray(int[] nums)',
        'Метод update(int index, int val): обновить arr[index] = val',
        'Метод sumRange(int left, int right): сумма на отрезке [left, right]',
        'Внутренняя реализация через Segment Tree',
        'Тест: nums = [1, 3, 5, 7, 9, 11], серия запросов и обновлений',
        'Проверить корректность после нескольких обновлений'
      ],
      expectedOutput: 'sumRange(0, 5) = 36\nsumRange(1, 3) = 15\nsumRange(2, 4) = 21\nПосле update(2, 10):\nsumRange(0, 5) = 41\nsumRange(1, 3) = 20\nПосле update(4, 1):\nsumRange(2, 4) = 18\nsumRange(0, 5) = 33',
      hint: 'Реализуйте стандартный Segment Tree: build за O(n), query(l,r) и update(idx,val) за O(log n). Используйте массив tree[] размера 4*n. Для узла i дочерние — 2*i+1 и 2*i+2.',
      solution: 'public class NumArray {\n    private int[] tree;\n    private int n;\n\n    public NumArray(int[] nums) {\n        n = nums.length;\n        tree = new int[4 * n];\n        build(nums, 0, 0, n - 1);\n    }\n\n    private void build(int[] nums, int node, int start, int end) {\n        if (start == end) {\n            tree[node] = nums[start];\n        } else {\n            int mid = (start + end) / 2;\n            build(nums, 2 * node + 1, start, mid);\n            build(nums, 2 * node + 2, mid + 1, end);\n            tree[node] = tree[2 * node + 1] + tree[2 * node + 2];\n        }\n    }\n\n    public void update(int index, int val) {\n        update(0, 0, n - 1, index, val);\n    }\n\n    private void update(int node, int start, int end, int idx, int val) {\n        if (start == end) {\n            tree[node] = val;\n        } else {\n            int mid = (start + end) / 2;\n            if (idx <= mid) update(2 * node + 1, start, mid, idx, val);\n            else update(2 * node + 2, mid + 1, end, idx, val);\n            tree[node] = tree[2 * node + 1] + tree[2 * node + 2];\n        }\n    }\n\n    public int sumRange(int left, int right) {\n        return query(0, 0, n - 1, left, right);\n    }\n\n    private int query(int node, int start, int end, int l, int r) {\n        if (r < start || end < l) return 0;\n        if (l <= start && end <= r) return tree[node];\n        int mid = (start + end) / 2;\n        return query(2 * node + 1, start, mid, l, r)\n             + query(2 * node + 2, mid + 1, end, l, r);\n    }\n\n    public static void main(String[] args) {\n        int[] nums = {1, 3, 5, 7, 9, 11};\n        NumArray na = new NumArray(nums);\n\n        System.out.println("sumRange(0, 5) = " + na.sumRange(0, 5));\n        System.out.println("sumRange(1, 3) = " + na.sumRange(1, 3));\n        System.out.println("sumRange(2, 4) = " + na.sumRange(2, 4));\n\n        na.update(2, 10);\n        System.out.println("После update(2, 10):");\n        System.out.println("sumRange(0, 5) = " + na.sumRange(0, 5));\n        System.out.println("sumRange(1, 3) = " + na.sumRange(1, 3));\n\n        na.update(4, 1);\n        System.out.println("После update(4, 1):");\n        System.out.println("sumRange(2, 4) = " + na.sumRange(2, 4));\n        System.out.println("sumRange(0, 5) = " + na.sumRange(0, 5));\n    }\n}',
      explanation: 'Segment Tree хранит суммы отрезков в массиве tree[4*n]. Построение O(n): каждый лист = элемент, внутренний узел = сумма дочерних. Query O(log n): если отрезок узла полностью внутри запроса — берём целиком, иначе делим на два подзапроса. Update O(log n): обновляем лист и пересчитываем все предки снизу вверх.'
    },
    {
      id: 7,
      title: 'Практика: Range Minimum Query',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте Segment Tree для нахождения минимума на отрезке (Range Minimum Query) с поддержкой обновления элементов.',
      requirements: [
        'Класс RangeMinQuery с конструктором RangeMinQuery(int[] nums)',
        'Метод update(int index, int val): обновить элемент',
        'Метод queryMin(int left, int right): минимум на отрезке [left, right]',
        'Нейтральный элемент для минимума: Integer.MAX_VALUE',
        'Тест: nums = [5, 2, 8, 1, 9, 3, 7, 4], серия запросов',
        'Проверить после обновлений'
      ],
      expectedOutput: 'min(0, 7) = 1\nmin(0, 3) = 1\nmin(4, 7) = 3\nmin(2, 5) = 1\nПосле update(3, 10):\nmin(0, 3) = 2\nmin(0, 7) = 2\nПосле update(1, 15):\nmin(0, 3) = 5\nmin(0, 7) = 3',
      hint: 'Замените сложение на Math.min. Нейтральный элемент (при выходе за границы) — Integer.MAX_VALUE, так как min(x, MAX_VALUE) = x. Всё остальное аналогично Range Sum.',
      solution: 'public class RangeMinQuery {\n    private int[] tree;\n    private int n;\n\n    public RangeMinQuery(int[] nums) {\n        n = nums.length;\n        tree = new int[4 * n];\n        build(nums, 0, 0, n - 1);\n    }\n\n    private void build(int[] nums, int node, int start, int end) {\n        if (start == end) {\n            tree[node] = nums[start];\n        } else {\n            int mid = (start + end) / 2;\n            build(nums, 2 * node + 1, start, mid);\n            build(nums, 2 * node + 2, mid + 1, end);\n            tree[node] = Math.min(tree[2 * node + 1], tree[2 * node + 2]);\n        }\n    }\n\n    public void update(int index, int val) {\n        update(0, 0, n - 1, index, val);\n    }\n\n    private void update(int node, int start, int end, int idx, int val) {\n        if (start == end) {\n            tree[node] = val;\n        } else {\n            int mid = (start + end) / 2;\n            if (idx <= mid) update(2 * node + 1, start, mid, idx, val);\n            else update(2 * node + 2, mid + 1, end, idx, val);\n            tree[node] = Math.min(tree[2 * node + 1], tree[2 * node + 2]);\n        }\n    }\n\n    public int queryMin(int left, int right) {\n        return query(0, 0, n - 1, left, right);\n    }\n\n    private int query(int node, int start, int end, int l, int r) {\n        if (r < start || end < l) return Integer.MAX_VALUE;\n        if (l <= start && end <= r) return tree[node];\n        int mid = (start + end) / 2;\n        return Math.min(\n            query(2 * node + 1, start, mid, l, r),\n            query(2 * node + 2, mid + 1, end, l, r)\n        );\n    }\n\n    public static void main(String[] args) {\n        int[] nums = {5, 2, 8, 1, 9, 3, 7, 4};\n        RangeMinQuery rmq = new RangeMinQuery(nums);\n\n        System.out.println("min(0, 7) = " + rmq.queryMin(0, 7));\n        System.out.println("min(0, 3) = " + rmq.queryMin(0, 3));\n        System.out.println("min(4, 7) = " + rmq.queryMin(4, 7));\n        System.out.println("min(2, 5) = " + rmq.queryMin(2, 5));\n\n        rmq.update(3, 10);\n        System.out.println("После update(3, 10):");\n        System.out.println("min(0, 3) = " + rmq.queryMin(0, 3));\n        System.out.println("min(0, 7) = " + rmq.queryMin(0, 7));\n\n        rmq.update(1, 15);\n        System.out.println("После update(1, 15):");\n        System.out.println("min(0, 3) = " + rmq.queryMin(0, 3));\n        System.out.println("min(0, 7) = " + rmq.queryMin(0, 7));\n    }\n}',
      explanation: 'Range Minimum Query — Segment Tree, где вместо суммы в каждом узле хранится минимум отрезка. При построении: node = min(left, right). При запросе: нейтральный элемент Integer.MAX_VALUE (min(x, MAX_VALUE) = x). При обновлении: меняем лист и пересчитываем min для всех предков. Сложность та же: O(n) построение, O(log n) запрос и обновление.'
    }
  ]
}
