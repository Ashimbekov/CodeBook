export default {
  id: 1,
  title: 'Введение в алгоритмы и Big O',
  description: 'Узнайте, что такое алгоритмы, почему важна эффективность и как измерять её с помощью нотации Big O.',
  lessons: [
    {
      id: 1,
      type: 'theory',
      title: 'Что такое алгоритм?',
      content: [
        {
          type: 'heading',
          text: 'Алгоритм — это рецепт для компьютера'
        },
        {
          type: 'text',
          text: 'Представьте, что вы хотите приготовить яичницу. Что вы делаете? Берёте яйцо, разбиваете его, кладёте на сковородку, ждёте пару минут — и готово! Это и есть алгоритм в обычной жизни: пошаговая инструкция для решения задачи.'
        },
        {
          type: 'text',
          text: 'Алгоритм в программировании — это точно такая же пошаговая инструкция, только для компьютера. Он говорит компьютеру: «Сначала сделай это, потом то, а потом вот это» — и в результате задача решена!'
        },
        {
          type: 'note',
          text: 'Три главных свойства алгоритма: 1) Конечность — он должен завершиться. 2) Определённость — каждый шаг должен быть чётким. 3) Результативность — он должен давать результат.'
        },
        {
          type: 'heading',
          text: 'Примеры алгоритмов из жизни'
        },
        {
          type: 'list',
          items: [
            'Рецепт приготовления борща — алгоритм приготовления еды',
            'Инструкция по сборке мебели из IKEA — алгоритм сборки',
            'Правила перехода улицы — алгоритм безопасного перехода',
            'Таблица умножения — алгоритм умножения чисел',
            'GPS-навигатор строит маршрут — алгоритм поиска пути'
          ]
        },
        {
          type: 'heading',
          text: 'Первый алгоритм на Java'
        },
        {
          type: 'text',
          text: 'Давайте напишем простой алгоритм: найдём максимальное число из двух. Это как спросить: "Кто выше — Вася или Петя?"'
        },
        {
          type: 'code',
          language: 'java',
          code: 'public class MaxNumber {\n    // Алгоритм нахождения максимума из двух чисел\n    public static int findMax(int a, int b) {\n        // Шаг 1: Сравниваем два числа\n        if (a > b) {\n            // Шаг 2а: Если a больше — возвращаем a\n            return a;\n        } else {\n            // Шаг 2б: Иначе возвращаем b\n            return b;\n        }\n    }\n\n    public static void main(String[] args) {\n        int result = findMax(5, 3);\n        System.out.println("Максимум: " + result); // Максимум: 5\n\n        result = findMax(2, 8);\n        System.out.println("Максимум: " + result); // Максимум: 8\n    }\n}'
        },
        {
          type: 'text',
          text: 'Видите? Алгоритм чёткий: сравниваем два числа и возвращаем большее. Компьютер точно знает, что делать на каждом шаге.'
        },
        {
          type: 'tip',
          text: 'Хороший алгоритм похож на хороший рецепт: он должен быть понятным, точным и всегда давать правильный результат.'
        }
      ]
    },
    {
      id: 2,
      type: 'theory',
      title: 'Почему важна эффективность?',
      content: [
        {
          type: 'heading',
          text: 'История о двух поварах'
        },
        {
          type: 'text',
          text: 'Представьте двух поваров. Первый повар, чтобы найти нужный ингредиент, каждый раз перебирает все продукты в кладовке по одному. Второй повар держит продукты в алфавитном порядке и сразу знает, где что лежит. Оба найдут нужное — но второй сделает это намного быстрее!'
        },
        {
          type: 'text',
          text: 'То же самое в программировании. Есть алгоритмы быстрые и медленные. Когда данных мало — разница незаметна. Но когда данных миллионы, разница может быть между "работает мгновенно" и "работает несколько часов".'
        },
        {
          type: 'heading',
          text: 'Наглядный пример: поиск числа'
        },
        {
          type: 'code',
          language: 'java',
          code: 'public class EfficiencyDemo {\n    // МЕДЛЕННЫЙ способ: проверяем каждый элемент\n    public static boolean slowSearch(int[] arr, int target) {\n        for (int i = 0; i < arr.length; i++) {\n            if (arr[i] == target) return true;\n        }\n        return false;\n    }\n\n    // БЫСТРЫЙ способ (если массив отсортирован): делим пополам\n    public static boolean fastSearch(int[] arr, int target) {\n        int left = 0, right = arr.length - 1;\n        while (left <= right) {\n            int mid = (left + right) / 2;\n            if (arr[mid] == target) return true;\n            if (arr[mid] < target) left = mid + 1;\n            else right = mid - 1;\n        }\n        return false;\n    }\n\n    public static void main(String[] args) {\n        int[] billion = new int[1_000_000_000]; // миллиард чисел\n        // slowSearch проверит до 1 000 000 000 элементов!\n        // fastSearch проверит максимум 30 элементов!\n        System.out.println("Разница огромная!");\n    }\n}'
        },
        {
          type: 'note',
          text: 'Для массива из миллиарда элементов: медленный поиск делает до 1 000 000 000 шагов, быстрый — всего около 30 шагов! Это как разница между поиском иголки в стоге сена руками и с металлодетектором.'
        },
        {
          type: 'heading',
          text: 'Два ресурса, которые мы экономим'
        },
        {
          type: 'list',
          items: [
            'Время (Time Complexity) — сколько операций выполняет алгоритм. Чем меньше операций — тем быстрее программа.',
            'Память (Space Complexity) — сколько памяти использует алгоритм. Чем меньше памяти — тем лучше для телефонов и компьютеров с ограниченной памятью.',
            'Обычно нужно искать баланс — иногда можно потратить больше памяти, чтобы работать быстрее (и наоборот).'
          ]
        },
        {
          type: 'warning',
          text: 'Не всегда нужен самый быстрый алгоритм! Если вы обрабатываете 10 элементов — разница между O(n) и O(n²) незначительна. Оптимизируйте только там, где это действительно важно.'
        }
      ]
    },
    {
      id: 3,
      type: 'theory',
      title: 'Нотация Big O — что это такое?',
      content: [
        {
          type: 'heading',
          text: 'Big O — это способ описать скорость роста'
        },
        {
          type: 'text',
          text: 'Представьте, что вы едете на машине. Вам не важно знать точную скорость — достаточно знать: "еду быстро", "еду медленно", "стою в пробке". Нотация Big O — это похожий способ описать алгоритм: не точное количество операций, а как быстро это количество растёт при увеличении данных.'
        },
        {
          type: 'text',
          text: 'Big O описывает наихудший случай. Это как планировать поездку: лучше думать "если будут пробки" — и обрадоваться, если их нет, чем думать "пробок не будет" — и опоздать.'
        },
        {
          type: 'heading',
          text: 'Как читать нотацию Big O'
        },
        {
          type: 'text',
          text: 'Буква O означает "Order of" (порядок). Внутри скобок — функция от n, где n — это размер входных данных (количество элементов). Например, O(n) читается как "О от н" или "линейная сложность".'
        },
        {
          type: 'list',
          items: [
            'O(1) — постоянная сложность: сколько бы данных ни было, алгоритм работает одинаково быстро',
            'O(n) — линейная сложность: вдвое больше данных → вдвое больше времени',
            'O(n²) — квадратичная сложность: вдвое больше данных → вчетверо больше времени',
            'O(log n) — логарифмическая сложность: очень быстрый рост даже при огромных данных'
          ]
        },
        {
          type: 'heading',
          text: 'Правила упрощения Big O'
        },
        {
          type: 'code',
          language: 'java',
          code: '// Правило 1: Константы игнорируются\n// Реально: 3n + 5 операций\n// Big O: O(n)  — константы не важны\n\n// Правило 2: Берём только главный член\n// Реально: n² + n + 100 операций\n// Big O: O(n²) — n² растёт быстрее всего\n\n// Правило 3: Последовательные блоки — складываем\nvoid example1(int n) {\n    // Блок 1: O(n)\n    for (int i = 0; i < n; i++) {\n        System.out.println(i); // n операций\n    }\n    // Блок 2: O(n)\n    for (int i = 0; i < n; i++) {\n        System.out.println(i * 2); // ещё n операций\n    }\n    // Итого: O(n) + O(n) = O(2n) = O(n)\n}\n\n// Правило 4: Вложенные циклы — перемножаем\nvoid example2(int n) {\n    for (int i = 0; i < n; i++) {     // n раз\n        for (int j = 0; j < n; j++) { // ещё n раз каждый\n            System.out.println(i + j); // n*n операций\n        }\n    }\n    // Итого: O(n²)\n}'
        },
        {
          type: 'tip',
          text: 'Запомните: Big O — это не точный ответ, а категория. Как размеры одежды: S, M, L, XL. O(n) — это "M", O(n²) — "XL". Точный размер может отличаться, но категория важнее.'
        }
      ]
    },
    {
      id: 4,
      type: 'theory',
      title: 'O(1) — постоянная сложность',
      content: [
        {
          type: 'heading',
          text: 'O(1) — мгновенный ответ, как по волшебству'
        },
        {
          type: 'text',
          text: 'Представьте библиотеку, где каждой книге присвоен номер. Вы хотите взять книгу №42. Библиотекарь идёт прямо к полке 42 и берёт её — неважно, сколько книг в библиотеке: 100 или 100 миллионов. Это и есть O(1): количество операций не зависит от размера данных!'
        },
        {
          type: 'heading',
          text: 'Примеры O(1) в Java'
        },
        {
          type: 'code',
          language: 'java',
          code: 'public class ConstantTime {\n\n    // Пример 1: Получение элемента массива по индексу\n    // Неважно, массив из 10 или 10 000 000 элементов\n    // — операция мгновенная!\n    public static int getElement(int[] arr, int index) {\n        return arr[index]; // O(1)\n    }\n\n    // Пример 2: Проверка чётности числа\n    public static boolean isEven(int n) {\n        return n % 2 == 0; // O(1) — одна операция\n    }\n\n    // Пример 3: Обмен двух переменных\n    public static void swap(int[] arr, int i, int j) {\n        int temp = arr[i]; // O(1)\n        arr[i] = arr[j];   // O(1)\n        arr[j] = temp;     // O(1)\n        // Три операции, но это константа — O(1)!\n    }\n\n    // Пример 4: Несколько O(1) вместе — всё равно O(1)\n    public static int sumOfFirstAndLast(int[] arr) {\n        int first = arr[0];              // O(1)\n        int last = arr[arr.length - 1];  // O(1)\n        return first + last;             // O(1)\n        // Итого: O(1) + O(1) + O(1) = O(3) = O(1)\n    }\n\n    public static void main(String[] args) {\n        int[] arr = {10, 20, 30, 40, 50};\n\n        System.out.println(getElement(arr, 2)); // 30 — мгновенно!\n        System.out.println(isEven(42));          // true\n        System.out.println(sumOfFirstAndLast(arr)); // 60\n    }\n}'
        },
        {
          type: 'note',
          text: 'O(1) — самая лучшая сложность, о которой может мечтать программист! Если алгоритм работает за O(1) — он одинаково быстр для 1 элемента и для миллиарда элементов.'
        },
        {
          type: 'heading',
          text: 'Пошаговая работа O(1): arr[2]'
        },
        {
          type: 'list',
          items: [
            'Шаг 1: Компьютер знает адрес начала массива в памяти (например, адрес 1000)',
            'Шаг 2: Каждый int занимает 4 байта, значит arr[2] находится по адресу 1000 + 2*4 = 1008',
            'Шаг 3: Компьютер мгновенно читает значение по адресу 1008',
            'Готово! Всего 1 вычисление + 1 чтение памяти — это и есть O(1)'
          ]
        },
        {
          type: 'warning',
          text: 'Осторожно: O(1) означает "константное количество операций", но не обязательно "одна операция". Если функция всегда выполняет ровно 100 операций — это всё равно O(1), потому что количество не растёт с n.'
        }
      ]
    },
    {
      id: 5,
      type: 'theory',
      title: 'O(n) и O(n²) — линейная и квадратичная сложность',
      content: [
        {
          type: 'heading',
          text: 'O(n) — один проход, как проверка журнала посещаемости'
        },
        {
          type: 'text',
          text: 'Учитель проверяет журнал: смотрит на первого ученика, второго, третьего... Чем больше учеников, тем дольше проверка. 30 учеников → 30 взглядов. 60 учеников → 60 взглядов. Это O(n): время растёт пропорционально количеству элементов.'
        },
        {
          type: 'code',
          language: 'java',
          code: 'public class LinearTime {\n\n    // O(n): один цикл по всем элементам\n    public static int findMax(int[] arr) {\n        int max = arr[0];\n        // Шаг 1: max = arr[0] = 3\n        // Шаг 2: 7 > 3? Да! max = 7\n        // Шаг 3: 1 > 7? Нет. max = 7\n        // Шаг 4: 9 > 7? Да! max = 9\n        // Шаг 5: 2 > 9? Нет. max = 9\n        // Результат: 9  (5 шагов для 5 элементов)\n        for (int i = 1; i < arr.length; i++) {\n            if (arr[i] > max) {\n                max = arr[i];\n            }\n        }\n        return max;\n    }\n\n    // O(n): подсчёт суммы\n    public static long sum(int[] arr) {\n        long total = 0;\n        for (int num : arr) { // n итераций\n            total += num;\n        }\n        return total;\n    }\n\n    public static void main(String[] args) {\n        int[] arr = {3, 7, 1, 9, 2};\n        System.out.println("Максимум: " + findMax(arr)); // 9\n        System.out.println("Сумма: " + sum(arr));        // 22\n    }\n}'
        },
        {
          type: 'heading',
          text: 'O(n²) — вложенные циклы, как турнир "каждый с каждым"'
        },
        {
          type: 'text',
          text: 'Представьте шахматный турнир, где каждый участник должен сыграть с каждым другим. 4 игрока → 6 партий. 8 игроков → 28 партий. 100 игроков → 4950 партий! Количество партий растёт как квадрат числа игроков. Это O(n²).'
        },
        {
          type: 'code',
          language: 'java',
          code: 'public class QuadraticTime {\n\n    // O(n²): вложенные циклы\n    public static void printAllPairs(int[] arr) {\n        // Для arr = {1, 2, 3}:\n        // (1,1), (1,2), (1,3)\n        // (2,1), (2,2), (2,3)\n        // (3,1), (3,2), (3,3)\n        // Итого 9 пар для 3 элементов (3² = 9)\n        for (int i = 0; i < arr.length; i++) {     // n раз\n            for (int j = 0; j < arr.length; j++) { // n раз каждый\n                System.out.println("(" + arr[i] + ", " + arr[j] + ")");\n            }\n        }\n    }\n\n    // O(n²): пузырьковая сортировка (упрощённо)\n    public static void bubbleSort(int[] arr) {\n        int n = arr.length;\n        for (int i = 0; i < n; i++) {       // внешний цикл: n раз\n            for (int j = 0; j < n-1; j++) { // внутренний цикл: n раз\n                if (arr[j] > arr[j+1]) {\n                    // Обмен соседних элементов\n                    int temp = arr[j];\n                    arr[j] = arr[j+1];\n                    arr[j+1] = temp;\n                }\n            }\n        }\n    }\n\n    public static void main(String[] args) {\n        int[] small = {1, 2, 3};    // 9 операций\n        int[] medium = new int[10]; // 100 операций\n        int[] large = new int[100]; // 10000 операций!\n        // n=1000 -> 1 000 000 операций\n        // n=10000 -> 100 000 000 операций — уже медленно!\n        System.out.println("n=10: " + (10*10) + " операций");\n        System.out.println("n=100: " + (100*100) + " операций");\n        System.out.println("n=1000: " + (1000*1000) + " операций");\n    }\n}'
        },
        {
          type: 'tip',
          text: 'Золотое правило: если вы видите цикл внутри цикла, где оба проходят по n элементам — скорее всего, это O(n²).'
        }
      ]
    },
    {
      id: 6,
      type: 'theory',
      title: 'O(log n) — логарифмическая сложность',
      content: [
        {
          type: 'heading',
          text: 'O(log n) — игра в "угадай число"'
        },
        {
          type: 'text',
          text: 'Представьте игру: я загадал число от 1 до 100. Вы спрашиваете: "Это больше 50?" — Да. "Больше 75?" — Нет. "Больше 62?" — Да. И так далее. Каждый вопрос вы отбрасываете ПОЛОВИНУ оставшихся вариантов! Для 100 чисел нужно всего ~7 вопросов. Для 1000 чисел — ~10 вопросов. Для миллиарда — ~30 вопросов! Это и есть O(log n).'
        },
        {
          type: 'text',
          text: 'Логарифм log₂(n) означает: "сколько раз нужно разделить n на 2, чтобы получить 1?". log₂(8) = 3, потому что 8 → 4 → 2 → 1 (три деления).'
        },
        {
          type: 'code',
          language: 'java',
          code: 'public class LogarithmicTime {\n\n    // O(log n): бинарный поиск\n    // Работает только на ОТСОРТИРОВАННОМ массиве!\n    public static int binarySearch(int[] arr, int target) {\n        int left = 0;\n        int right = arr.length - 1;\n\n        // Для arr = {1, 3, 5, 7, 9, 11, 13} ищем 7:\n        // Итерация 1: left=0, right=6, mid=3, arr[3]=7 == 7. Нашли!\n\n        // Для arr = {1, 3, 5, 7, 9, 11, 13} ищем 11:\n        // Итерация 1: left=0, right=6, mid=3, arr[3]=7 < 11 -> left=4\n        // Итерация 2: left=4, right=6, mid=5, arr[5]=11 == 11. Нашли!\n\n        while (left <= right) {\n            int mid = left + (right - left) / 2; // избегаем переполнения\n\n            if (arr[mid] == target) {\n                return mid; // нашли!\n            } else if (arr[mid] < target) {\n                left = mid + 1; // ищем в правой половине\n            } else {\n                right = mid - 1; // ищем в левой половине\n            }\n        }\n        return -1; // не нашли\n    }\n\n    public static void main(String[] args) {\n        int[] arr = {1, 3, 5, 7, 9, 11, 13};\n        System.out.println(binarySearch(arr, 7));  //  3 (индекс)\n        System.out.println(binarySearch(arr, 11)); //  5 (индекс)\n        System.out.println(binarySearch(arr, 4));  // -1 (не найдено)\n\n        // Сравнение количества шагов:\n        // n=8:          log₂(8)  = 3 шага\n        // n=1024:       log₂(1024) = 10 шагов\n        // n=1048576:    log₂(1048576) = 20 шагов\n        // n=1073741824: log₂(1073741824) = 30 шагов (!)\n        System.out.println("Для миллиарда элементов - всего 30 шагов!");\n    }\n}'
        },
        {
          type: 'note',
          text: 'Сравнение для n = 1 000 000 000 (миллиард): O(n) = 1 000 000 000 шагов, O(log n) = примерно 30 шагов. O(log n) быстрее в 33 миллиона раз!'
        },
        {
          type: 'tip',
          text: 'Ключевой признак O(log n): на каждой итерации задача уменьшается вдвое (или в любое постоянное число раз). Если вы видите "делим пополам" — это O(log n).'
        }
      ]
    },
    {
      id: 7,
      type: 'practice',
      title: 'Практика: Определите сложность кода',
      difficulty: 'medium',
      description: 'Перед вами несколько фрагментов кода на Java. Ваша задача — написать метод, который вычисляет сумму всех чётных чисел в массиве от 1 до n, и правильно определить его сложность.',
      requirements: [
        'Напишите метод `sumOfEvens(int n)`, который возвращает сумму всех чётных чисел от 1 до n включительно',
        'Например, sumOfEvens(10) = 2 + 4 + 6 + 8 + 10 = 30',
        'Сначала напишите версию с циклом O(n)',
        'Затем напишите версию с формулой O(1): сумма чётных от 1 до n = k*(k+1), где k = n/2'
      ],
      expectedOutput: 'sumOfEvens(10) = 30\nsumOfEvens(100) = 2550\nsumOfEvens(1) = 0',
      hint: 'Чётные числа от 1 до n — это 2, 4, 6, ..., n (если n чётное) или n-1 (если n нечётное). Количество чётных чисел k = n/2 (целочисленное деление).',
      solution: 'public class EvenSum {\n    // Версия O(n): цикл\n    public static long sumOfEvensLinear(int n) {\n        long sum = 0;\n        for (int i = 2; i <= n; i += 2) { // каждый шаг +2\n            sum += i;\n        }\n        return sum;\n    }\n\n    // Версия O(1): математическая формула\n    // Чётные числа: 2, 4, 6, ..., 2k где k = n/2\n    // Сумма = 2*(1+2+3+...+k) = 2 * k*(k+1)/2 = k*(k+1)\n    public static long sumOfEvensConstant(int n) {\n        long k = n / 2; // количество чётных чисел\n        return k * (k + 1);\n    }\n\n    public static void main(String[] args) {\n        System.out.println("sumOfEvens(10) = " + sumOfEvensLinear(10));    // 30\n        System.out.println("sumOfEvens(10) = " + sumOfEvensConstant(10));  // 30\n        System.out.println("sumOfEvens(100) = " + sumOfEvensConstant(100)); // 2550\n        System.out.println("sumOfEvens(1) = " + sumOfEvensConstant(1));    // 0\n    }\n}',
      explanation: 'Версия с циклом — O(n), потому что мы проходим по n/2 числам (что является O(n)). Версия с формулой — O(1), потому что мы выполняем константное количество арифметических операций независимо от n. Вот почему математика так важна в алгоритмах — иногда формула заменяет весь цикл!'
    }
  ]
};
