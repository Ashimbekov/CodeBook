export default {
  id: 26,
  title: 'Trie (префиксное дерево)',
  description: 'Структура данных Trie для хранения строк и быстрого поиска по префиксу. Реализация insert, search, startsWith, удаление, автодополнение и подсчёт слов.',
  lessons: [
    {
      id: 1,
      title: 'Что такое Trie и зачем он нужен',
      type: 'theory',
      content: [
        { type: 'text', value: 'Trie (от retrieval — «извлечение», произносится «трай») — это древовидная структура данных для эффективного хранения и поиска строк. Каждый узел представляет один символ, а путь от корня до помеченного узла образует слово.' },
        { type: 'tip', value: 'Представьте телефонную книгу. Чтобы найти "Иванов", вы открываете раздел "И", потом ищете "Ив", потом "Ива"... Trie работает точно так же — спускается по символам, мгновенно отсекая ненужные слова.' },
        { type: 'heading', value: 'Зачем нужен Trie?' },
        { type: 'list', value: [
          'Быстрый поиск по префиксу — O(m), где m — длина слова',
          'Автодополнение в поисковых строках (Google, IDE)',
          'Проверка орфографии',
          'IP-маршрутизация (longest prefix match)',
          'T9-набор текста на телефоне'
        ]},
        { type: 'heading', value: 'Структура узла Trie' },
        { type: 'code', language: 'java', value: '// Узел Trie — хранит массив дочерних узлов (по одному на каждый символ)\nclass TrieNode {\n    TrieNode[] children; // 26 слотов для букв a-z\n    boolean isEndOfWord; // Флаг: здесь заканчивается слово\n\n    public TrieNode() {\n        children = new TrieNode[26];\n        isEndOfWord = false;\n    }\n}\n\n// Визуализация Trie для слов: "app", "apple", "apt", "bat"\n//\n//         (root)\n//        /      \\\n//       a        b\n//       |        |\n//       p        a\n//      / \\       |\n//     p*  t*     t*\n//     |\n//     l\n//     |\n//     e*\n//\n// * — isEndOfWord = true' },
        { type: 'heading', value: 'Сравнение с другими структурами' },
        { type: 'code', language: 'java', value: '// Поиск строки длиной m среди n строк:\n//\n// HashSet:    O(m) — но нет поиска по префиксу!\n// TreeSet:    O(m log n) — есть поиск по префиксу через subSet\n// Trie:       O(m) — быстрый поиск + префиксный поиск\n//\n// Trie побеждает когда нужны:\n// - Все слова с данным префиксом\n// - Автодополнение\n// - Поиск самого длинного совпадающего префикса\n// - Проверка, есть ли хотя бы одно слово с данным префиксом' },
        { type: 'warning', value: 'Trie потребляет больше памяти, чем HashSet. Каждый узел хранит массив из 26 указателей. Для 100 000 слов Trie может занимать в 5-10 раз больше памяти, чем HashSet. Используйте Trie только когда нужен именно префиксный поиск.' }
      ]
    },
    {
      id: 2,
      title: 'Реализация Trie: insert, search',
      type: 'theory',
      content: [
        { type: 'text', value: 'Вставка (insert) и поиск (search) — базовые операции Trie. Вставка создаёт узлы для каждого символа слова. Поиск проходит по дереву, проверяя наличие каждого символа.' },
        { type: 'heading', value: 'Полная реализация Trie' },
        { type: 'code', language: 'java', value: 'public class Trie {\n    private TrieNode root;\n\n    // Узел Trie\n    static class TrieNode {\n        TrieNode[] children = new TrieNode[26];\n        boolean isEndOfWord = false;\n    }\n\n    public Trie() {\n        root = new TrieNode();\n    }\n\n    // Вставка слова — O(m), m = длина слова\n    public void insert(String word) {\n        TrieNode current = root;\n        for (char ch : word.toCharArray()) {\n            int index = ch - \'a\'; // Индекс: \'a\'->0, \'b\'->1, ...\n            // Если нет дочернего узла — создаём\n            if (current.children[index] == null) {\n                current.children[index] = new TrieNode();\n            }\n            current = current.children[index];\n        }\n        current.isEndOfWord = true; // Помечаем конец слова\n    }\n\n    // Поиск слова — O(m)\n    public boolean search(String word) {\n        TrieNode node = findNode(word);\n        // Слово найдено только если дошли до конца И флаг isEndOfWord\n        return node != null && node.isEndOfWord;\n    }\n\n    // Вспомогательный метод: найти узел по строке\n    private TrieNode findNode(String prefix) {\n        TrieNode current = root;\n        for (char ch : prefix.toCharArray()) {\n            int index = ch - \'a\';\n            if (current.children[index] == null) {\n                return null; // Такого пути нет\n            }\n            current = current.children[index];\n        }\n        return current;\n    }\n\n    public static void main(String[] args) {\n        Trie trie = new Trie();\n        trie.insert("apple");\n        trie.insert("app");\n        trie.insert("bat");\n\n        System.out.println(trie.search("apple")); // true\n        System.out.println(trie.search("app"));   // true\n        System.out.println(trie.search("ap"));    // false (\"ap\" не было вставлено)\n        System.out.println(trie.search("bat"));   // true\n        System.out.println(trie.search("bad"));   // false\n    }\n}' },
        { type: 'heading', value: 'Трассировка insert("apple")' },
        { type: 'code', language: 'java', value: '// insert("apple"):\n//\n// root -> \'a\' (создаём) -> \'p\' (создаём) -> \'p\' (создаём)\n//      -> \'l\' (создаём) -> \'e\' (создаём, isEndOfWord=true)\n//\n// insert("app"):\n// root -> \'a\' (уже есть) -> \'p\' (уже есть) -> \'p\' (уже есть, isEndOfWord=true)\n//\n// search("app"):\n// root -> \'a\' (есть) -> \'p\' (есть) -> \'p\' (есть, isEndOfWord=true) -> true\n//\n// search("ap"):\n// root -> \'a\' (есть) -> \'p\' (есть, isEndOfWord=false) -> false\n// Путь есть, но это НЕ конец слова!' },
        { type: 'tip', value: 'Ключевое отличие search от startsWith: search требует, чтобы isEndOfWord был true. Без этого флага "ap" считалось бы словом, хотя мы его не вставляли — оно просто часть пути к "app" и "apple".' }
      ]
    },
    {
      id: 3,
      title: 'Префиксный поиск (startsWith)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Метод startsWith проверяет, есть ли в Trie хотя бы одно слово с данным префиксом. В отличие от search, нам НЕ нужно проверять isEndOfWord — достаточно, что путь существует.' },
        { type: 'heading', value: 'Реализация startsWith' },
        { type: 'code', language: 'java', value: 'public class Trie {\n    private TrieNode root;\n\n    static class TrieNode {\n        TrieNode[] children = new TrieNode[26];\n        boolean isEndOfWord = false;\n    }\n\n    public Trie() {\n        root = new TrieNode();\n    }\n\n    public void insert(String word) {\n        TrieNode current = root;\n        for (char ch : word.toCharArray()) {\n            int idx = ch - \'a\';\n            if (current.children[idx] == null)\n                current.children[idx] = new TrieNode();\n            current = current.children[idx];\n        }\n        current.isEndOfWord = true;\n    }\n\n    public boolean search(String word) {\n        TrieNode node = findNode(word);\n        return node != null && node.isEndOfWord;\n    }\n\n    // Есть ли слово с таким префиксом? — O(m)\n    public boolean startsWith(String prefix) {\n        return findNode(prefix) != null;\n        // Не проверяем isEndOfWord!\n    }\n\n    private TrieNode findNode(String s) {\n        TrieNode current = root;\n        for (char ch : s.toCharArray()) {\n            int idx = ch - \'a\';\n            if (current.children[idx] == null) return null;\n            current = current.children[idx];\n        }\n        return current;\n    }\n\n    public static void main(String[] args) {\n        Trie trie = new Trie();\n        trie.insert("apple");\n        trie.insert("application");\n        trie.insert("banana");\n\n        // search vs startsWith\n        System.out.println(trie.search("app"));      // false — \"app\" не вставляли\n        System.out.println(trie.startsWith("app"));   // true — есть \"apple\", \"application\"\n        System.out.println(trie.startsWith("appl"));  // true — есть \"apple\", \"application\"\n        System.out.println(trie.startsWith("appz"));  // false — нет слов на \"appz\"\n        System.out.println(trie.startsWith("ban"));   // true — есть \"banana\"\n        System.out.println(trie.startsWith("cat"));   // false — нет слов на \"cat\"\n    }\n}' },
        { type: 'heading', value: 'Сбор всех слов с данным префиксом' },
        { type: 'code', language: 'java', value: 'import java.util.*;\n\n// Собрать все слова в Trie, начинающиеся с prefix\npublic List<String> wordsWithPrefix(String prefix) {\n    List<String> result = new ArrayList<>();\n    TrieNode node = findNode(prefix);\n    if (node == null) return result; // Нет слов с таким префиксом\n\n    // DFS: обходим все пути от node\n    collectWords(node, new StringBuilder(prefix), result);\n    return result;\n}\n\nprivate void collectWords(TrieNode node, StringBuilder path, List<String> result) {\n    if (node.isEndOfWord) {\n        result.add(path.toString()); // Нашли слово!\n    }\n    for (int i = 0; i < 26; i++) {\n        if (node.children[i] != null) {\n            path.append((char) (\'a\' + i));\n            collectWords(node.children[i], path, result);\n            path.deleteCharAt(path.length() - 1); // Backtrack\n        }\n    }\n}\n\n// Пример:\n// trie содержит: apple, app, application, banana\n// wordsWithPrefix("app") -> [app, apple, application]\n// wordsWithPrefix("ban") -> [banana]\n// wordsWithPrefix("cat") -> []' },
        { type: 'tip', value: 'wordsWithPrefix — это основа автодополнения! Пользователь вводит "app", и мы моментально находим все слова: app, apple, application. Собираем их обходом DFS от узла, соответствующего префиксу.' }
      ]
    },
    {
      id: 4,
      title: 'Удаление из Trie',
      type: 'theory',
      content: [
        { type: 'text', value: 'Удаление слова из Trie сложнее, чем вставка. Нельзя просто снять флаг isEndOfWord — нужно также удалить лишние узлы, которые не являются частью других слов.' },
        { type: 'tip', value: 'Представьте, что в дереве есть "apple" и "app". Удаляем "apple": снимаем флаг с "e", а узлы "l" и "e" можно удалить (нет других слов через них). Но "a", "p", "p" удалять нельзя — они часть слова "app"!' },
        { type: 'heading', value: 'Рекурсивное удаление' },
        { type: 'code', language: 'java', value: 'public class TrieWithDelete {\n    private TrieNode root = new TrieNode();\n\n    static class TrieNode {\n        TrieNode[] children = new TrieNode[26];\n        boolean isEndOfWord = false;\n\n        // Проверка: есть ли у узла хотя бы один дочерний узел\n        boolean hasChildren() {\n            for (TrieNode child : children) {\n                if (child != null) return true;\n            }\n            return false;\n        }\n    }\n\n    public void insert(String word) {\n        TrieNode curr = root;\n        for (char ch : word.toCharArray()) {\n            int idx = ch - \'a\';\n            if (curr.children[idx] == null)\n                curr.children[idx] = new TrieNode();\n            curr = curr.children[idx];\n        }\n        curr.isEndOfWord = true;\n    }\n\n    // Удаление слова — рекурсивно\n    // Возвращает true, если текущий узел можно удалить\n    public boolean delete(String word) {\n        return deleteHelper(root, word, 0);\n    }\n\n    private boolean deleteHelper(TrieNode node, String word, int depth) {\n        if (node == null) return false;\n\n        // Базовый случай: дошли до конца слова\n        if (depth == word.length()) {\n            if (!node.isEndOfWord) return false; // Слова нет в Trie\n            node.isEndOfWord = false;\n            // Можно ли удалить этот узел? Только если нет детей\n            return !node.hasChildren();\n        }\n\n        // Рекурсивный случай: идём глубже\n        int idx = word.charAt(depth) - \'a\';\n        boolean shouldDeleteChild = deleteHelper(node.children[idx], word, depth + 1);\n\n        if (shouldDeleteChild) {\n            node.children[idx] = null; // Удаляем дочерний узел\n            // Можно ли удалить текущий узел?\n            return !node.isEndOfWord && !node.hasChildren();\n        }\n        return false;\n    }\n\n    public boolean search(String word) {\n        TrieNode curr = root;\n        for (char ch : word.toCharArray()) {\n            int idx = ch - \'a\';\n            if (curr.children[idx] == null) return false;\n            curr = curr.children[idx];\n        }\n        return curr.isEndOfWord;\n    }\n\n    public static void main(String[] args) {\n        TrieWithDelete trie = new TrieWithDelete();\n        trie.insert("apple");\n        trie.insert("app");\n\n        System.out.println(trie.search("apple")); // true\n        System.out.println(trie.search("app"));   // true\n\n        trie.delete("apple");\n        System.out.println(trie.search("apple")); // false\n        System.out.println(trie.search("app"));   // true (не затронуто!)\n\n        trie.delete("app");\n        System.out.println(trie.search("app"));   // false\n    }\n}' },
        { type: 'heading', value: 'Трассировка удаления' },
        { type: 'code', language: 'java', value: '// Trie: "apple", "app"\n// Удаляем "apple":\n//\n// deleteHelper(root, "apple", 0) -> идём к \'a\'\n//   deleteHelper(a, "apple", 1) -> идём к \'p\'\n//     deleteHelper(p, "apple", 2) -> идём к \'p\'\n//       deleteHelper(p, "apple", 3) -> идём к \'l\'\n//         deleteHelper(l, "apple", 4) -> идём к \'e\'\n//           deleteHelper(e, "apple", 5) -> depth == length\n//             e.isEndOfWord = false\n//             e не имеет детей -> return true (можно удалить)\n//         l.children[\'e\'] = null, l не имеет детей -> return true\n//       p.children[\'l\'] = null, НО p.isEndOfWord=true -> return false\n//     Стоп! Узел \'p\' — конец слова "app", не удаляем\n//\n// Результат: "apple" удалено, "app" на месте!' },
        { type: 'warning', value: 'При удалении обязательно проверяйте два условия перед удалением узла: (1) узел НЕ является концом другого слова, (2) узел НЕ имеет дочерних узлов. Если хотя бы одно условие не выполнено — узел нельзя удалять.' }
      ]
    },
    {
      id: 5,
      title: 'Автодополнение (autocomplete)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Автодополнение — одно из главных применений Trie. Пользователь вводит префикс, и система мгновенно предлагает варианты. Добавим поддержку частоты и ранжирования результатов.' },
        { type: 'heading', value: 'Trie с частотой для автодополнения' },
        { type: 'code', language: 'java', value: 'import java.util.*;\n\npublic class AutocompleteTrie {\n    static class TrieNode {\n        TrieNode[] children = new TrieNode[26];\n        boolean isEndOfWord = false;\n        int frequency = 0; // Частота слова (для ранжирования)\n    }\n\n    private TrieNode root = new TrieNode();\n\n    // Вставка с частотой\n    public void insert(String word, int freq) {\n        TrieNode curr = root;\n        for (char ch : word.toCharArray()) {\n            int idx = ch - \'a\';\n            if (curr.children[idx] == null)\n                curr.children[idx] = new TrieNode();\n            curr = curr.children[idx];\n        }\n        curr.isEndOfWord = true;\n        curr.frequency = freq;\n    }\n\n    // Автодополнение: top-k слов по частоте\n    public List<String> autocomplete(String prefix, int k) {\n        TrieNode node = findNode(prefix);\n        if (node == null) return Collections.emptyList();\n\n        // Max-heap по частоте\n        PriorityQueue<int[]> pq = new PriorityQueue<>((a, b) -> b[0] - a[0]);\n        List<String> words = new ArrayList<>();\n        List<Integer> freqs = new ArrayList<>();\n\n        // Собираем все слова с данным префиксом\n        collectAll(node, new StringBuilder(prefix), words, freqs);\n\n        // Кладём в приоритетную очередь\n        for (int i = 0; i < words.size(); i++) {\n            pq.offer(new int[]{freqs.get(i), i});\n        }\n\n        // Достаём top-k\n        List<String> result = new ArrayList<>();\n        while (!pq.isEmpty() && result.size() < k) {\n            int idx = pq.poll()[1];\n            result.add(words.get(idx));\n        }\n        return result;\n    }\n\n    private void collectAll(TrieNode node, StringBuilder path,\n                            List<String> words, List<Integer> freqs) {\n        if (node.isEndOfWord) {\n            words.add(path.toString());\n            freqs.add(node.frequency);\n        }\n        for (int i = 0; i < 26; i++) {\n            if (node.children[i] != null) {\n                path.append((char) (\'a\' + i));\n                collectAll(node.children[i], path, words, freqs);\n                path.deleteCharAt(path.length() - 1);\n            }\n        }\n    }\n\n    private TrieNode findNode(String prefix) {\n        TrieNode curr = root;\n        for (char ch : prefix.toCharArray()) {\n            int idx = ch - \'a\';\n            if (curr.children[idx] == null) return null;\n            curr = curr.children[idx];\n        }\n        return curr;\n    }\n\n    public static void main(String[] args) {\n        AutocompleteTrie ac = new AutocompleteTrie();\n        ac.insert("apple", 100);\n        ac.insert("application", 80);\n        ac.insert("app", 150);\n        ac.insert("apply", 90);\n        ac.insert("apt", 30);\n        ac.insert("banana", 70);\n\n        // Пользователь вводит "app" — показываем top-3\n        System.out.println("Автодополнение для \\\"app\\\": " +\n            ac.autocomplete("app", 3));\n        // [app (150), apple (100), apply (90)]\n\n        System.out.println("Автодополнение для \\\"ap\\\": " +\n            ac.autocomplete("ap", 2));\n        // [app (150), apple (100)]\n    }\n}' },
        { type: 'tip', value: 'В реальных поисковых системах частота обновляется динамически: каждый клик по подсказке увеличивает счётчик слова. Google Suggest использует похожий подход, но с триллионами запросов и распределённой системой.' },
        { type: 'heading', value: 'Оптимизации для реальных систем' },
        { type: 'list', value: [
          'Ограничение глубины обхода — не собирать слова длиннее N символов',
          'Кеширование top-k результатов для популярных префиксов',
          'Сжатый Trie (Patricia Trie) — объединение узлов с одним ребёнком',
          'Ternary Search Tree — компромисс между Trie и BST по памяти'
        ]}
      ]
    },
    {
      id: 6,
      title: 'Подсчёт слов с префиксом',
      type: 'theory',
      content: [
        { type: 'text', value: 'Часто нужно не только найти слова с префиксом, но и быстро посчитать их количество. Для этого в каждом узле храним счётчик — сколько слов проходит через этот узел.' },
        { type: 'heading', value: 'Trie со счётчиком префиксов' },
        { type: 'code', language: 'java', value: 'public class CountingTrie {\n    static class TrieNode {\n        TrieNode[] children = new TrieNode[26];\n        int wordCount = 0;     // Сколько слов заканчиваются здесь\n        int prefixCount = 0;   // Сколько слов проходят через этот узел\n    }\n\n    private TrieNode root = new TrieNode();\n\n    // Вставка с обновлением счётчиков\n    public void insert(String word) {\n        TrieNode curr = root;\n        for (char ch : word.toCharArray()) {\n            int idx = ch - \'a\';\n            if (curr.children[idx] == null)\n                curr.children[idx] = new TrieNode();\n            curr = curr.children[idx];\n            curr.prefixCount++; // Каждый узел на пути — +1\n        }\n        curr.wordCount++; // Конец слова — +1\n    }\n\n    // Сколько раз слово было вставлено\n    public int countWord(String word) {\n        TrieNode node = findNode(word);\n        return node == null ? 0 : node.wordCount;\n    }\n\n    // Сколько слов начинается с данного префикса — O(m)!\n    public int countPrefix(String prefix) {\n        TrieNode node = findNode(prefix);\n        return node == null ? 0 : node.prefixCount;\n    }\n\n    // Удаление с обновлением счётчиков\n    public void delete(String word) {\n        if (countWord(word) == 0) return; // Слова нет\n        TrieNode curr = root;\n        for (char ch : word.toCharArray()) {\n            int idx = ch - \'a\';\n            curr = curr.children[idx];\n            curr.prefixCount--; // Уменьшаем счётчик\n        }\n        curr.wordCount--;\n    }\n\n    private TrieNode findNode(String s) {\n        TrieNode curr = root;\n        for (char ch : s.toCharArray()) {\n            int idx = ch - \'a\';\n            if (curr.children[idx] == null) return null;\n            curr = curr.children[idx];\n        }\n        return curr;\n    }\n\n    public static void main(String[] args) {\n        CountingTrie trie = new CountingTrie();\n        trie.insert("apple");\n        trie.insert("apple");     // Вставляем повторно\n        trie.insert("app");\n        trie.insert("application");\n        trie.insert("banana");\n\n        System.out.println("Слов \\\"apple\\\": " + trie.countWord("apple"));       // 2\n        System.out.println("Слов с \\\"app\\\": " + trie.countPrefix("app"));        // 4\n        System.out.println("Слов с \\\"appl\\\": " + trie.countPrefix("appl"));      // 3\n        System.out.println("Слов с \\\"b\\\": " + trie.countPrefix("b"));            // 1\n        System.out.println("Слов с \\\"c\\\": " + trie.countPrefix("c"));            // 0\n\n        trie.delete("apple"); // Удаляем одно вхождение\n        System.out.println("После удаления одного \\\"apple\\\":");\n        System.out.println("Слов \\\"apple\\\": " + trie.countWord("apple"));       // 1\n        System.out.println("Слов с \\\"app\\\": " + trie.countPrefix("app"));        // 3\n    }\n}' },
        { type: 'heading', value: 'Трассировка подсчёта' },
        { type: 'code', language: 'java', value: '// После вставок: "apple"(x2), "app", "application"\n//\n// Путь:  root -> a -> p -> p -> l -> e\n//                              |         \\-> i -> c -> a -> t -> i -> o -> n\n//                              |\n// prefixCount:   4    4    4    3    2\n//                                         1    1    1    1    1    1    1\n// wordCount:                   1(app)  2(apple)                      1(application)\n//\n// countPrefix("app") = prefixCount у третьего \'p\' = 4\n//   (apple x2, app x1, application x1)\n// countPrefix("appl") = prefixCount у \'l\' = 3\n//   (apple x2, application x1)\n// countWord("apple") = wordCount у \'e\' = 2' },
        { type: 'tip', value: 'Счётчик prefixCount даёт ответ за O(m), не нужно обходить всё поддерево. Это критично для систем реального времени: Google обрабатывает миллионы запросов автодополнения в секунду.' }
      ]
    },
    {
      id: 7,
      title: 'Практика: Реализация словаря',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте полноценный словарь на основе Trie с операциями вставки, поиска, удаления и подсчёта слов с префиксом.',
      requirements: [
        'Класс Dictionary с внутренним классом TrieNode (children, isEndOfWord, prefixCount)',
        'Метод addWord(String word): вставка слова с обновлением prefixCount',
        'Метод containsWord(String word): точный поиск слова',
        'Метод startsWith(String prefix): есть ли слова с данным префиксом',
        'Метод countWordsWithPrefix(String prefix): количество слов с данным префиксом',
        'Метод removeWord(String word): удаление слова с обновлением счётчиков',
        'Тест: добавить "apple", "app", "application", "bat", "ball", затем проверить все операции'
      ],
      expectedOutput: 'containsWord("apple"): true\ncontainsWord("ap"): false\nstartsWith("app"): true\nstartsWith("xyz"): false\ncountWordsWithPrefix("app"): 3\ncountWordsWithPrefix("ba"): 2\ncountWordsWithPrefix("c"): 0\nПосле удаления "app":\ncontainsWord("app"): false\ncontainsWord("apple"): true\ncountWordsWithPrefix("app"): 2',
      hint: 'Используйте prefixCount в каждом узле: при insert увеличивайте на 1, при delete уменьшайте. Для removeWord проверьте, что слово существует, прежде чем уменьшать счётчики.',
      solution: 'public class Dictionary {\n    static class TrieNode {\n        TrieNode[] children = new TrieNode[26];\n        boolean isEndOfWord = false;\n        int prefixCount = 0;\n    }\n\n    private TrieNode root = new TrieNode();\n\n    public void addWord(String word) {\n        TrieNode curr = root;\n        for (char ch : word.toCharArray()) {\n            int idx = ch - \'a\';\n            if (curr.children[idx] == null)\n                curr.children[idx] = new TrieNode();\n            curr = curr.children[idx];\n            curr.prefixCount++;\n        }\n        curr.isEndOfWord = true;\n    }\n\n    public boolean containsWord(String word) {\n        TrieNode node = findNode(word);\n        return node != null && node.isEndOfWord;\n    }\n\n    public boolean startsWith(String prefix) {\n        return findNode(prefix) != null;\n    }\n\n    public int countWordsWithPrefix(String prefix) {\n        TrieNode node = findNode(prefix);\n        return node == null ? 0 : node.prefixCount;\n    }\n\n    public void removeWord(String word) {\n        if (!containsWord(word)) return;\n        TrieNode curr = root;\n        for (char ch : word.toCharArray()) {\n            int idx = ch - \'a\';\n            curr = curr.children[idx];\n            curr.prefixCount--;\n        }\n        curr.isEndOfWord = false;\n    }\n\n    private TrieNode findNode(String s) {\n        TrieNode curr = root;\n        for (char ch : s.toCharArray()) {\n            int idx = ch - \'a\';\n            if (curr.children[idx] == null) return null;\n            curr = curr.children[idx];\n        }\n        return curr;\n    }\n\n    public static void main(String[] args) {\n        Dictionary dict = new Dictionary();\n        dict.addWord("apple");\n        dict.addWord("app");\n        dict.addWord("application");\n        dict.addWord("bat");\n        dict.addWord("ball");\n\n        System.out.println("containsWord(\\\"apple\\\"): " + dict.containsWord("apple"));\n        System.out.println("containsWord(\\\"ap\\\"): " + dict.containsWord("ap"));\n        System.out.println("startsWith(\\\"app\\\"): " + dict.startsWith("app"));\n        System.out.println("startsWith(\\\"xyz\\\"): " + dict.startsWith("xyz"));\n        System.out.println("countWordsWithPrefix(\\\"app\\\"): " + dict.countWordsWithPrefix("app"));\n        System.out.println("countWordsWithPrefix(\\\"ba\\\"): " + dict.countWordsWithPrefix("ba"));\n        System.out.println("countWordsWithPrefix(\\\"c\\\"): " + dict.countWordsWithPrefix("c"));\n\n        dict.removeWord("app");\n        System.out.println("После удаления \\\"app\\\":");\n        System.out.println("containsWord(\\\"app\\\"): " + dict.containsWord("app"));\n        System.out.println("containsWord(\\\"apple\\\"): " + dict.containsWord("apple"));\n        System.out.println("countWordsWithPrefix(\\\"app\\\"): " + dict.countWordsWithPrefix("app"));\n    }\n}',
      explanation: 'Ключевая идея — хранить prefixCount в каждом узле. При вставке инкрементируем счётчик на каждом узле пути. При удалении — декрементируем. Это позволяет отвечать на запрос countWordsWithPrefix за O(m), не обходя поддерево. containsWord проверяет не только наличие пути, но и флаг isEndOfWord.'
    },
    {
      id: 8,
      title: 'Практика: Автодополнение поиска',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте систему автодополнения с поддержкой частоты запросов и возвратом top-K результатов, отсортированных по популярности.',
      requirements: [
        'Класс SearchAutocomplete с TrieNode (children, suggestions — отсортированный список top-3 слов)',
        'Метод addSentence(String sentence, int times): добавить запрос с частотой',
        'Метод search(String prefix): вернуть top-3 слова по частоте для данного префикса',
        'При равной частоте — лексикографический порядок',
        'Метод input(char c): посимвольный ввод, возврат текущих подсказок',
        'Тест: добавить запросы с разными частотами, проверить top-3 при вводе символов'
      ],
      expectedOutput: 'Подсказки для "a": [apple(5), application(3), app(2)]\nПодсказки для "ap": [apple(5), application(3), app(2)]\nПодсказки для "app": [apple(5), application(3), app(2)]\nПодсказки для "b": [banana(4), ball(1)]\nПодсказки для "c": []\nПосимвольный ввод "ap": [apple(5), application(3), app(2)]',
      hint: 'Для top-K используйте PriorityQueue с компаратором: сначала по убыванию частоты, при равенстве — по алфавиту. Храните Map<String, Integer> для частот. В каждом узле можно кешировать top-3 подсказки для быстрого отклика.',
      solution: 'import java.util.*;\n\npublic class SearchAutocomplete {\n    static class TrieNode {\n        TrieNode[] children = new TrieNode[26];\n        boolean isEnd = false;\n    }\n\n    private TrieNode root = new TrieNode();\n    private Map<String, Integer> freqMap = new HashMap<>(); // слово -> частота\n    private StringBuilder currentInput = new StringBuilder();\n\n    public void addSentence(String sentence, int times) {\n        freqMap.merge(sentence, times, Integer::sum);\n        TrieNode curr = root;\n        for (char ch : sentence.toCharArray()) {\n            int idx = ch - \'a\';\n            if (curr.children[idx] == null)\n                curr.children[idx] = new TrieNode();\n            curr = curr.children[idx];\n        }\n        curr.isEnd = true;\n    }\n\n    // Поиск top-3 по префиксу\n    public List<String> search(String prefix) {\n        TrieNode node = findNode(prefix);\n        if (node == null) return Collections.emptyList();\n\n        // Собираем все слова с данным префиксом\n        List<String> candidates = new ArrayList<>();\n        collectWords(node, new StringBuilder(prefix), candidates);\n\n        // Сортируем: по убыванию частоты, при равенстве — лексикографически\n        candidates.sort((a, b) -> {\n            int freqDiff = freqMap.getOrDefault(b, 0) - freqMap.getOrDefault(a, 0);\n            return freqDiff != 0 ? freqDiff : a.compareTo(b);\n        });\n\n        // Возвращаем top-3\n        return candidates.subList(0, Math.min(3, candidates.size()));\n    }\n\n    // Посимвольный ввод\n    public List<String> input(char c) {\n        currentInput.append(c);\n        return search(currentInput.toString());\n    }\n\n    public void resetInput() {\n        currentInput.setLength(0);\n    }\n\n    private void collectWords(TrieNode node, StringBuilder path, List<String> result) {\n        if (node.isEnd) result.add(path.toString());\n        for (int i = 0; i < 26; i++) {\n            if (node.children[i] != null) {\n                path.append((char) (\'a\' + i));\n                collectWords(node.children[i], path, result);\n                path.deleteCharAt(path.length() - 1);\n            }\n        }\n    }\n\n    private TrieNode findNode(String prefix) {\n        TrieNode curr = root;\n        for (char ch : prefix.toCharArray()) {\n            int idx = ch - \'a\';\n            if (curr.children[idx] == null) return null;\n            curr = curr.children[idx];\n        }\n        return curr;\n    }\n\n    public static void main(String[] args) {\n        SearchAutocomplete ac = new SearchAutocomplete();\n        ac.addSentence("apple", 5);\n        ac.addSentence("application", 3);\n        ac.addSentence("app", 2);\n        ac.addSentence("banana", 4);\n        ac.addSentence("ball", 1);\n\n        System.out.println("Подсказки для \\\"a\\\": " + formatResults(ac.search("a"), ac.freqMap));\n        System.out.println("Подсказки для \\\"ap\\\": " + formatResults(ac.search("ap"), ac.freqMap));\n        System.out.println("Подсказки для \\\"app\\\": " + formatResults(ac.search("app"), ac.freqMap));\n        System.out.println("Подсказки для \\\"b\\\": " + formatResults(ac.search("b"), ac.freqMap));\n        System.out.println("Подсказки для \\\"c\\\": " + formatResults(ac.search("c"), ac.freqMap));\n\n        // Посимвольный ввод\n        ac.resetInput();\n        ac.input(\'a\');\n        List<String> result = ac.input(\'p\');\n        System.out.println("Посимвольный ввод \\\"ap\\\": " + formatResults(result, ac.freqMap));\n    }\n\n    private static String formatResults(List<String> words, Map<String, Integer> freqs) {\n        StringBuilder sb = new StringBuilder("[");\n        for (int i = 0; i < words.size(); i++) {\n            if (i > 0) sb.append(", ");\n            sb.append(words.get(i)).append("(").append(freqs.getOrDefault(words.get(i), 0)).append(")");\n        }\n        sb.append("]");\n        return sb.toString();\n    }\n}',
      explanation: 'Система автодополнения объединяет Trie для хранения строк и HashMap для хранения частот. Для каждого запроса: (1) находим узел по префиксу за O(m), (2) DFS-обходом собираем все слова, (3) сортируем по частоте и возвращаем top-3. В реальных системах top-k кешируется в каждом узле, чтобы избежать повторного обхода.'
    }
  ]
}
