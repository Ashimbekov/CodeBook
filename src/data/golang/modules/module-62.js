export default {
  id: 62,
  title: 'Микросервисы на Go',
  description: 'Архитектура микросервисов на Go: коммуникация сервисов, обнаружение сервисов, API Gateway, Circuit Breaker, health checks, конфигурация и docker-compose для оркестрации.',
  lessons: [
    {
      id: 1,
      title: 'Архитектура микросервисов',
      type: 'theory',
      content: [
        { type: 'text', value: 'Микросервисная архитектура — это подход, при котором большое приложение разбивается на маленькие независимые сервисы. Представьте завод: вместо одного рабочего, делающего всё, каждый рабочий специализируется на одной задаче.' },
        { type: 'heading', value: 'Монолит vs Микросервисы' },
        { type: 'code', language: 'go', value: '// МОНОЛИТ: всё в одном бинарнике\n// main.go\npackage main\n\n// Весь код в одном месте:\n// - Авторизация\n// - Управление пользователями\n// - Каталог продуктов\n// - Заказы\n// - Уведомления\n// - Отчёты\n\n// Проблемы монолита:\n// - При изменении одной части нужно переразворачивать всё\n// - Один баг может обрушить всё приложение\n// - Сложно масштабировать только горячую часть\n// - Разные команды мешают друг другу\n\n// МИКРОСЕРВИСЫ: каждый сервис — отдельный процесс\n// user-service/     -> порт 8001\n// product-service/  -> порт 8002\n// order-service/    -> порт 8003\n// notification-service/ -> порт 8004\n\n// Каждый сервис:\n// - Деплоится независимо\n// - Имеет свою базу данных\n// - Масштабируется отдельно\n// - Может быть написан на разных языках' },
        { type: 'heading', value: 'Структура типичного Go микросервиса' },
        { type: 'code', language: 'go', value: '// user-service/\n// ├── main.go\n// ├── handler/\n// │   └── user_handler.go\n// ├── service/\n// │   └── user_service.go\n// ├── repository/\n// │   └── user_repo.go\n// ├── model/\n// │   └── user.go\n// ├── config/\n// │   └── config.go\n// ├── Dockerfile\n// └── go.mod\n\n// main.go\npackage main\n\nimport (\n    "fmt"\n    "log"\n    "net/http"\n    "os"\n)\n\nfunc main() {\n    port := os.Getenv("PORT")\n    if port == "" {\n        port = "8001"\n    }\n\n    mux := http.NewServeMux()\n    mux.HandleFunc("/health", func(w http.ResponseWriter, r *http.Request) {\n        w.WriteHeader(http.StatusOK)\n        fmt.Fprintln(w, `{"status":"ok","service":"user-service"}`)\n    })\n    mux.HandleFunc("/users", usersHandler)\n\n    log.Printf("User service запущен на :%s", port)\n    log.Fatal(http.ListenAndServe(":"+port, mux))\n}\n\nfunc usersHandler(w http.ResponseWriter, r *http.Request) {\n    fmt.Fprintln(w, `[{"id":1,"name":"Нурдаулет"}]`)\n}' },
        { type: 'note', value: 'Микросервисы увеличивают сложность системы. Начинайте с монолита и переходите к микросервисам только при реальной необходимости масштабирования или при росте команды.' }
      ]
    },
    {
      id: 2,
      title: 'Коммуникация между сервисами',
      type: 'theory',
      content: [
        { type: 'text', value: 'Микросервисы должны общаться друг с другом. Есть два основных паттерна: синхронная (HTTP/gRPC) и асинхронная (через очереди сообщений) коммуникация.' },
        { type: 'heading', value: 'HTTP клиент для вызова другого сервиса' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "context"\n    "encoding/json"\n    "fmt"\n    "io"\n    "log"\n    "net/http"\n    "time"\n)\n\ntype User struct {\n    ID    int    `json:"id"`\n    Name  string `json:"name"`\n    Email string `json:"email"`\n}\n\n// UserServiceClient — клиент для user-service\ntype UserServiceClient struct {\n    baseURL    string\n    httpClient *http.Client\n}\n\nfunc NewUserServiceClient(baseURL string) *UserServiceClient {\n    return &UserServiceClient{\n        baseURL: baseURL,\n        httpClient: &http.Client{\n            Timeout: 5 * time.Second, // важно! всегда задавайте timeout\n        },\n    }\n}\n\nfunc (c *UserServiceClient) GetUser(ctx context.Context, id int) (*User, error) {\n    url := fmt.Sprintf("%s/users/%d", c.baseURL, id)\n\n    req, err := http.NewRequestWithContext(ctx, http.MethodGet, url, nil)\n    if err != nil {\n        return nil, fmt.Errorf("создание запроса: %w", err)\n    }\n\n    req.Header.Set("Content-Type", "application/json")\n    // Передаём trace ID для распределённой трассировки\n    if traceID := ctx.Value("trace_id"); traceID != nil {\n        req.Header.Set("X-Trace-ID", fmt.Sprintf("%v", traceID))\n    }\n\n    resp, err := c.httpClient.Do(req)\n    if err != nil {\n        return nil, fmt.Errorf("HTTP запрос к user-service: %w", err)\n    }\n    defer resp.Body.Close()\n\n    if resp.StatusCode == http.StatusNotFound {\n        return nil, fmt.Errorf("пользователь %d не найден", id)\n    }\n    if resp.StatusCode != http.StatusOK {\n        body, _ := io.ReadAll(resp.Body)\n        return nil, fmt.Errorf("user-service вернул %d: %s", resp.StatusCode, body)\n    }\n\n    var user User\n    if err := json.NewDecoder(resp.Body).Decode(&user); err != nil {\n        return nil, fmt.Errorf("декодирование ответа: %w", err)\n    }\n\n    return &user, nil\n}\n\nfunc main() {\n    client := NewUserServiceClient("http://user-service:8001")\n    ctx := context.WithValue(context.Background(), "trace_id", "req-123")\n\n    user, err := client.GetUser(ctx, 1)\n    if err != nil {\n        log.Printf("Ошибка: %v", err)\n        return\n    }\n    fmt.Printf("Получен пользователь: %+v\\n", user)\n}' },
        { type: 'tip', value: 'Всегда используйте context для HTTP запросов между сервисами. Это позволяет отменять запросы при таймауте и передавать метаданные (trace ID, user ID) по всей цепочке вызовов.' }
      ]
    },
    {
      id: 3,
      title: 'Service Discovery',
      type: 'theory',
      content: [
        { type: 'text', value: 'Service Discovery — это механизм, позволяющий сервисам находить друг друга по имени, а не по жёстко заданному IP-адресу. Как телефонная книга: вместо "позвони на 8-701-123-45-67" говорите "позвони Нурдаулету".' },
        { type: 'heading', value: 'Простой service discovery через переменные окружения' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "os"\n    "strings"\n)\n\n// Простейший service discovery через ENV\n// В docker-compose сервисы обнаруживаются по имени контейнера\n\ntype ServiceRegistry struct {\n    services map[string]string\n}\n\nfunc NewServiceRegistry() *ServiceRegistry {\n    r := &ServiceRegistry{\n        services: make(map[string]string),\n    }\n    r.loadFromEnv()\n    return r\n}\n\n// loadFromEnv загружает адреса сервисов из ENV переменных\n// USER_SERVICE_URL=http://user-service:8001\n// PRODUCT_SERVICE_URL=http://product-service:8002\nfunc (r *ServiceRegistry) loadFromEnv() {\n    // Ищем все переменные вида *_SERVICE_URL\n    for _, env := range os.Environ() {\n        parts := strings.SplitN(env, "=", 2)\n        if len(parts) != 2 {\n            continue\n        }\n        key, val := parts[0], parts[1]\n        if strings.HasSuffix(key, "_SERVICE_URL") {\n            // USER_SERVICE_URL -> user-service\n            name := strings.ToLower(\n                strings.TrimSuffix(key, "_SERVICE_URL"),\n            )\n            name = strings.ReplaceAll(name, "_", "-")\n            r.services[name] = val\n            fmt.Printf("Зарегистрирован сервис: %s -> %s\\n", name, val)\n        }\n    }\n}\n\nfunc (r *ServiceRegistry) Get(serviceName string) (string, bool) {\n    url, ok := r.services[serviceName]\n    return url, ok\n}\n\nfunc main() {\n    // Эмулируем ENV переменные (в реальности задаются в docker-compose)\n    os.Setenv("USER_SERVICE_URL", "http://user-service:8001")\n    os.Setenv("PRODUCT_SERVICE_URL", "http://product-service:8002")\n    os.Setenv("ORDER_SERVICE_URL", "http://order-service:8003")\n\n    registry := NewServiceRegistry()\n\n    if url, ok := registry.Get("user-service"); ok {\n        fmt.Printf("User service URL: %s\\n", url)\n    }\n\n    if url, ok := registry.Get("product-service"); ok {\n        fmt.Printf("Product service URL: %s\\n", url)\n    }\n}' },
        { type: 'note', value: 'В production используют специализированные инструменты: Consul, etcd, Kubernetes DNS. В Docker Compose и Kubernetes сервисы автоматически обнаруживаются по имени контейнера/сервиса без дополнительной настройки.' }
      ]
    },
    {
      id: 4,
      title: 'API Gateway паттерн',
      type: 'theory',
      content: [
        { type: 'text', value: 'API Gateway — единая точка входа для всех клиентов. Это как стойка ресепшена в офисе: все посетители приходят через неё, и она направляет их к нужному отделу. Gateway берёт на себя авторизацию, роутинг, rate limiting и логирование.' },
        { type: 'heading', value: 'Реализация простого API Gateway' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "log"\n    "net/http"\n    "net/http/httputil"\n    "net/url"\n    "os"\n    "strings"\n    "time"\n)\n\ntype Gateway struct {\n    routes map[string]string // prefix -> service URL\n}\n\nfunc NewGateway() *Gateway {\n    return &Gateway{\n        routes: map[string]string{\n            "/api/users":    getEnv("USER_SERVICE_URL",    "http://localhost:8001"),\n            "/api/products": getEnv("PRODUCT_SERVICE_URL", "http://localhost:8002"),\n            "/api/orders":   getEnv("ORDER_SERVICE_URL",   "http://localhost:8003"),\n        },\n    }\n}\n\nfunc getEnv(key, def string) string {\n    if v := os.Getenv(key); v != "" {\n        return v\n    }\n    return def\n}\n\n// ServeHTTP — главный обработчик шлюза\nfunc (g *Gateway) ServeHTTP(w http.ResponseWriter, r *http.Request) {\n    // Логирование каждого запроса\n    start := time.Now()\n    log.Printf("-> %s %s", r.Method, r.URL.Path)\n\n    // Аутентификация (упрощённо)\n    if r.URL.Path != "/health" && r.URL.Path != "/" {\n        if err := g.authenticate(r); err != nil {\n            http.Error(w, "Unauthorized", http.StatusUnauthorized)\n            return\n        }\n    }\n\n    // Поиск подходящего маршрута\n    for prefix, serviceURL := range g.routes {\n        if strings.HasPrefix(r.URL.Path, prefix) {\n            g.proxy(w, r, serviceURL)\n            log.Printf("<- %s %s [%v]", r.Method, r.URL.Path, time.Since(start))\n            return\n        }\n    }\n\n    http.Error(w, "Route not found", http.StatusNotFound)\n}\n\nfunc (g *Gateway) authenticate(r *http.Request) error {\n    token := r.Header.Get("Authorization")\n    if token == "" {\n        return fmt.Errorf("no token")\n    }\n    // В реальности здесь JWT валидация\n    return nil\n}\n\nfunc (g *Gateway) proxy(w http.ResponseWriter, r *http.Request, targetURL string) {\n    target, err := url.Parse(targetURL)\n    if err != nil {\n        http.Error(w, "Bad gateway", http.StatusBadGateway)\n        return\n    }\n\n    proxy := httputil.NewSingleHostReverseProxy(target)\n    proxy.ErrorHandler = func(w http.ResponseWriter, r *http.Request, err error) {\n        log.Printf("Proxy error: %v", err)\n        http.Error(w, "Service Unavailable", http.StatusServiceUnavailable)\n    }\n\n    // Добавляем заголовки для downstream сервисов\n    r.Header.Set("X-Forwarded-For", r.RemoteAddr)\n    r.Header.Set("X-Gateway", "my-gateway/1.0")\n\n    proxy.ServeHTTP(w, r)\n}\n\nfunc main() {\n    gw := NewGateway()\n\n    mux := http.NewServeMux()\n    mux.HandleFunc("/health", func(w http.ResponseWriter, r *http.Request) {\n        fmt.Fprintln(w, `{"status":"ok","service":"gateway"}`)\n    })\n    mux.Handle("/", gw)\n\n    log.Println("API Gateway запущен на :8080")\n    log.Fatal(http.ListenAndServe(":8080", mux))\n}' },
        { type: 'tip', value: 'httputil.NewSingleHostReverseProxy — стандартный способ проксирования HTTP запросов в Go. В production используйте специализированные API Gateway: Kong, Traefik, или Nginx.' }
      ]
    },
    {
      id: 5,
      title: 'Circuit Breaker паттерн',
      type: 'theory',
      content: [
        { type: 'text', value: 'Circuit Breaker (автоматический выключатель) защищает систему от каскадных сбоев. Если сервис A вызывает сервис B, и B начинает падать, то Circuit Breaker "размыкает цепь" — прекращает вызовы к B на некоторое время.' },
        { type: 'heading', value: 'Реализация Circuit Breaker' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "errors"\n    "fmt"\n    "sync"\n    "time"\n)\n\ntype State int\n\nconst (\n    StateClosed   State = iota // Нормально: запросы проходят\n    StateOpen                  // Сбой: запросы блокируются\n    StateHalfOpen             // Тест: пропускаем один запрос\n)\n\nfunc (s State) String() string {\n    return [...]string{"CLOSED", "OPEN", "HALF_OPEN"}[s]\n}\n\ntype CircuitBreaker struct {\n    mu sync.Mutex\n\n    state        State\n    failureCount int\n    successCount int\n    lastFailure  time.Time\n\n    maxFailures  int           // сколько ошибок до размыкания\n    resetTimeout time.Duration // как долго ждать перед проверкой\n    halfOpenMax  int           // сколько успехов нужно для закрытия\n}\n\nfunc NewCircuitBreaker(maxFailures int, resetTimeout time.Duration) *CircuitBreaker {\n    return &CircuitBreaker{\n        state:        StateClosed,\n        maxFailures:  maxFailures,\n        resetTimeout: resetTimeout,\n        halfOpenMax:  2,\n    }\n}\n\nvar ErrCircuitOpen = errors.New("circuit breaker разомкнут — сервис недоступен")\n\nfunc (cb *CircuitBreaker) Call(fn func() error) error {\n    cb.mu.Lock()\n    state := cb.currentState()\n    cb.mu.Unlock()\n\n    if state == StateOpen {\n        return ErrCircuitOpen\n    }\n\n    err := fn() // выполняем реальный вызов\n\n    cb.mu.Lock()\n    defer cb.mu.Unlock()\n\n    if err != nil {\n        cb.onFailure()\n        return err\n    }\n    cb.onSuccess()\n    return nil\n}\n\nfunc (cb *CircuitBreaker) currentState() State {\n    if cb.state == StateOpen {\n        // Проверяем, не пора ли попробовать снова\n        if time.Since(cb.lastFailure) > cb.resetTimeout {\n            cb.state = StateHalfOpen\n            cb.successCount = 0\n            fmt.Println("Circuit Breaker: HALF_OPEN (пробуем снова...)")\n        }\n    }\n    return cb.state\n}\n\nfunc (cb *CircuitBreaker) onFailure() {\n    cb.failureCount++\n    cb.lastFailure = time.Now()\n\n    if cb.state == StateHalfOpen {\n        cb.state = StateOpen\n        fmt.Println("Circuit Breaker: OPEN (повторный сбой в half-open)")\n        return\n    }\n\n    if cb.failureCount >= cb.maxFailures {\n        cb.state = StateOpen\n        fmt.Printf("Circuit Breaker: OPEN (сбоев: %d)\\n", cb.failureCount)\n    }\n}\n\nfunc (cb *CircuitBreaker) onSuccess() {\n    cb.successCount++\n    if cb.state == StateHalfOpen && cb.successCount >= cb.halfOpenMax {\n        cb.state = StateClosed\n        cb.failureCount = 0\n        fmt.Println("Circuit Breaker: CLOSED (сервис восстановлен)")\n    }\n}\n\nfunc main() {\n    cb := NewCircuitBreaker(3, 2*time.Second)\n\n    callCount := 0\n    remoteService := func() error {\n        callCount++\n        if callCount <= 5 {\n            return fmt.Errorf("сервис недоступен (попытка %d)", callCount)\n        }\n        return nil\n    }\n\n    for i := 1; i <= 10; i++ {\n        err := cb.Call(remoteService)\n        if err != nil {\n            fmt.Printf("Запрос %d: ошибка — %v\\n", i, err)\n        } else {\n            fmt.Printf("Запрос %d: успех!\\n", i)\n        }\n        time.Sleep(300 * time.Millisecond)\n    }\n}' },
        { type: 'note', value: 'В production рекомендуется использовать готовые библиотеки: github.com/sony/gobreaker или github.com/afex/hystrix-go. Они обеспечивают более сложное поведение и метрики.' }
      ]
    },
    {
      id: 6,
      title: 'Health Checks',
      type: 'theory',
      content: [
        { type: 'text', value: 'Health Check — это endpoint, по которому оркестратор (Kubernetes, Docker) проверяет состояние сервиса. Как пульс у человека: если не бьётся — что-то не так. Kubernetes использует /healthz, /readyz и /livez.' },
        { type: 'heading', value: 'Реализация health check endpoint' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "context"\n    "database/sql"\n    "encoding/json"\n    "fmt"\n    "net/http"\n    "time"\n)\n\ntype HealthStatus struct {\n    Status  string            `json:"status"`\n    Service string            `json:"service"`\n    Version string            `json:"version"`\n    Checks  map[string]string `json:"checks"`\n}\n\ntype HealthChecker struct {\n    db      *sql.DB\n    service string\n    version string\n}\n\nfunc NewHealthChecker(db *sql.DB, service, version string) *HealthChecker {\n    return &HealthChecker{db: db, service: service, version: version}\n}\n\n// LivenessHandler — жив ли процесс?\n// Kubernetes перезапустит контейнер если этот endpoint недоступен\nfunc (h *HealthChecker) LivenessHandler(w http.ResponseWriter, r *http.Request) {\n    w.Header().Set("Content-Type", "application/json")\n    json.NewEncoder(w).Encode(HealthStatus{\n        Status:  "ok",\n        Service: h.service,\n        Version: h.version,\n    })\n}\n\n// ReadinessHandler — готов ли принимать трафик?\n// Kubernetes не будет отправлять трафик если этот endpoint возвращает ошибку\nfunc (h *HealthChecker) ReadinessHandler(w http.ResponseWriter, r *http.Request) {\n    checks := make(map[string]string)\n    allOk := true\n\n    // Проверяем подключение к БД\n    ctx, cancel := context.WithTimeout(r.Context(), 3*time.Second)\n    defer cancel()\n\n    if h.db != nil {\n        if err := h.db.PingContext(ctx); err != nil {\n            checks["database"] = fmt.Sprintf("error: %v", err)\n            allOk = false\n        } else {\n            checks["database"] = "ok"\n        }\n    } else {\n        checks["database"] = "not configured"\n    }\n\n    // Можно добавить проверки Redis, внешних API и т.д.\n    checks["memory"] = "ok" // упрощённо\n\n    w.Header().Set("Content-Type", "application/json")\n\n    status := HealthStatus{\n        Service: h.service,\n        Version: h.version,\n        Checks:  checks,\n    }\n\n    if allOk {\n        status.Status = "ready"\n        w.WriteHeader(http.StatusOK)\n    } else {\n        status.Status = "not ready"\n        w.WriteHeader(http.StatusServiceUnavailable)\n    }\n\n    json.NewEncoder(w).Encode(status)\n}\n\nfunc main() {\n    // db, _ := sql.Open("postgres", "...")\n    checker := NewHealthChecker(nil, "user-service", "1.0.0")\n\n    mux := http.NewServeMux()\n    mux.HandleFunc("/healthz", checker.LivenessHandler)  // liveness\n    mux.HandleFunc("/readyz",  checker.ReadinessHandler) // readiness\n\n    // Kubernetes Probe конфигурация (в YAML):\n    // livenessProbe:\n    //   httpGet:\n    //     path: /healthz\n    //     port: 8080\n    //   initialDelaySeconds: 5\n    //   periodSeconds: 10\n    // readinessProbe:\n    //   httpGet:\n    //     path: /readyz\n    //     port: 8080\n    //   initialDelaySeconds: 5\n    //   periodSeconds: 5\n\n    fmt.Println("Health check сервер на :8080")\n    http.ListenAndServe(":8080", mux)\n}' }
      ]
    },
    {
      id: 7,
      title: 'Конфигурация микросервисов',
      type: 'theory',
      content: [
        { type: 'text', value: 'Конфигурация микросервисов через переменные окружения — это стандарт (12-factor app). Это позволяет одинаковый бинарник запускать в dev, staging и production с разными настройками.' },
        { type: 'heading', value: 'Структурированная конфигурация' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "log"\n    "os"\n    "strconv"\n    "time"\n)\n\ntype Config struct {\n    // HTTP сервер\n    Port         string\n    ReadTimeout  time.Duration\n    WriteTimeout time.Duration\n\n    // База данных\n    DatabaseURL     string\n    DatabaseMaxConn int\n\n    // Другие сервисы\n    UserServiceURL    string\n    ProductServiceURL string\n\n    // Аутентификация\n    JWTSecret string\n    JWTExpiry time.Duration\n\n    // Окружение\n    Environment string\n    LogLevel    string\n}\n\nfunc LoadConfig() (*Config, error) {\n    cfg := &Config{\n        // Значения по умолчанию (для разработки)\n        Port:              getEnv("PORT", "8080"),\n        DatabaseURL:       getEnv("DATABASE_URL", "postgres://localhost:5432/myapp"),\n        DatabaseMaxConn:   getEnvInt("DATABASE_MAX_CONN", 10),\n        UserServiceURL:    getEnv("USER_SERVICE_URL", "http://localhost:8001"),\n        ProductServiceURL: getEnv("PRODUCT_SERVICE_URL", "http://localhost:8002"),\n        JWTSecret:         getEnv("JWT_SECRET", ""),\n        Environment:       getEnv("ENVIRONMENT", "development"),\n        LogLevel:          getEnv("LOG_LEVEL", "info"),\n        ReadTimeout:       getEnvDuration("READ_TIMEOUT", 10*time.Second),\n        WriteTimeout:      getEnvDuration("WRITE_TIMEOUT", 10*time.Second),\n        JWTExpiry:         getEnvDuration("JWT_EXPIRY", 24*time.Hour),\n    }\n\n    // Валидация обязательных полей\n    if cfg.JWTSecret == "" && cfg.Environment == "production" {\n        return nil, fmt.Errorf("JWT_SECRET обязателен в production")\n    }\n\n    return cfg, nil\n}\n\nfunc getEnv(key, defaultValue string) string {\n    if value, ok := os.LookupEnv(key); ok {\n        return value\n    }\n    return defaultValue\n}\n\nfunc getEnvInt(key string, defaultValue int) int {\n    if value, ok := os.LookupEnv(key); ok {\n        if n, err := strconv.Atoi(value); err == nil {\n            return n\n        }\n    }\n    return defaultValue\n}\n\nfunc getEnvDuration(key string, defaultValue time.Duration) time.Duration {\n    if value, ok := os.LookupEnv(key); ok {\n        if d, err := time.ParseDuration(value); err == nil {\n            return d\n        }\n    }\n    return defaultValue\n}\n\nfunc main() {\n    cfg, err := LoadConfig()\n    if err != nil {\n        log.Fatalf("Ошибка конфигурации: %v", err)\n    }\n\n    fmt.Printf("Сервис запускается:\\n")\n    fmt.Printf("  Порт: %s\\n", cfg.Port)\n    fmt.Printf("  Среда: %s\\n", cfg.Environment)\n    fmt.Printf("  БД: %s\\n", cfg.DatabaseURL)\n    fmt.Printf("  User service: %s\\n", cfg.UserServiceURL)\n    fmt.Printf("  Log level: %s\\n", cfg.LogLevel)\n}' },
        { type: 'tip', value: 'Используйте библиотеку github.com/kelseyhightower/envconfig или github.com/caarlos0/env для автоматической загрузки конфигурации из ENV в структуры с помощью тегов. Это убирает много boilerplate кода.' }
      ]
    },
    {
      id: 8,
      title: 'Практика: docker-compose для микросервисов',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создай систему из двух микросервисов (user-service и order-service), где order-service вызывает user-service для получения данных пользователя. Напиши docker-compose.yml для запуска обоих сервисов.',
      requirements: [
        'user-service: GET /users/{id} -> {id, name, email} на порту 8001',
        'order-service: GET /orders/{id} -> {id, product, userID, userName} на порту 8002',
        'order-service должен вызывать user-service для получения имени пользователя',
        'Оба сервиса должны иметь /health endpoint',
        'docker-compose.yml должен определять оба сервиса и сеть',
        'Используй переменные окружения для конфигурации URL сервисов',
        'Добавь retry логику при недоступности user-service'
      ],
      expectedOutput: 'GET http://localhost:8001/users/1 -> {"id":1,"name":"Нурдаулет","email":"n@mail.ru"}\nGET http://localhost:8002/orders/1 -> {"id":1,"product":"Ноутбук","userID":1,"userName":"Нурдаулет"}\nGET http://localhost:8001/health -> {"status":"ok"}\nGET http://localhost:8002/health -> {"status":"ok"}',
      hint: 'В docker-compose сервисы обнаруживаются по имени сервиса как DNS имени. Если user-service называется "user-service" в docker-compose, то из order-service он доступен по адресу http://user-service:8001. Используй depends_on для порядка запуска.',
      solution: '// user-service/main.go\npackage main\n\nimport (\n    "encoding/json"\n    "fmt"\n    "log"\n    "net/http"\n    "os"\n    "strconv"\n    "strings"\n)\n\ntype User struct {\n    ID    int    `json:"id"`\n    Name  string `json:"name"`\n    Email string `json:"email"`\n}\n\nvar users = map[int]User{\n    1: {ID: 1, Name: "Нурдаулет", Email: "n@mail.ru"},\n    2: {ID: 2, Name: "Айгерим",   Email: "a@mail.ru"},\n}\n\nfunc main() {\n    port := os.Getenv("PORT")\n    if port == "" { port = "8001" }\n\n    http.HandleFunc("/health", func(w http.ResponseWriter, r *http.Request) {\n        json.NewEncoder(w).Encode(map[string]string{"status": "ok", "service": "user-service"})\n    })\n\n    http.HandleFunc("/users/", func(w http.ResponseWriter, r *http.Request) {\n        idStr := strings.TrimPrefix(r.URL.Path, "/users/")\n        id, err := strconv.Atoi(idStr)\n        if err != nil {\n            http.Error(w, "bad id", 400)\n            return\n        }\n        u, ok := users[id]\n        if !ok {\n            http.Error(w, "not found", 404)\n            return\n        }\n        w.Header().Set("Content-Type", "application/json")\n        json.NewEncoder(w).Encode(u)\n    })\n\n    log.Printf("User service на :%s", port)\n    log.Fatal(http.ListenAndServe(":"+port, nil))\n}\n\n// ---\n// order-service/main.go\npackage main\n\nimport (\n    "context"\n    "encoding/json"\n    "fmt"\n    "log"\n    "net/http"\n    "os"\n    "strconv"\n    "strings"\n    "time"\n)\n\ntype User struct {\n    ID   int    `json:"id"`\n    Name string `json:"name"`\n}\n\ntype Order struct {\n    ID       int    `json:"id"`\n    Product  string `json:"product"`\n    UserID   int    `json:"userID"`\n    UserName string `json:"userName"`\n}\n\nvar orders = map[int]Order{\n    1: {ID: 1, Product: "Ноутбук",     UserID: 1},\n    2: {ID: 2, Product: "Смартфон",    UserID: 2},\n}\n\nfunc getUserName(ctx context.Context, userServiceURL string, userID int) (string, error) {\n    url := fmt.Sprintf("%s/users/%d", userServiceURL, userID)\n    req, _ := http.NewRequestWithContext(ctx, "GET", url, nil)\n    client := &http.Client{Timeout: 3 * time.Second}\n\n    for attempt := 1; attempt <= 3; attempt++ {\n        resp, err := client.Do(req)\n        if err != nil {\n            log.Printf("Попытка %d: ошибка %v", attempt, err)\n            time.Sleep(time.Duration(attempt) * 500 * time.Millisecond)\n            continue\n        }\n        defer resp.Body.Close()\n        var u User\n        json.NewDecoder(resp.Body).Decode(&u)\n        return u.Name, nil\n    }\n    return "Неизвестный", fmt.Errorf("user-service недоступен")\n}\n\nfunc main() {\n    port := os.Getenv("PORT")\n    if port == "" { port = "8002" }\n    userServiceURL := os.Getenv("USER_SERVICE_URL")\n    if userServiceURL == "" { userServiceURL = "http://localhost:8001" }\n\n    http.HandleFunc("/health", func(w http.ResponseWriter, r *http.Request) {\n        json.NewEncoder(w).Encode(map[string]string{"status": "ok", "service": "order-service"})\n    })\n\n    http.HandleFunc("/orders/", func(w http.ResponseWriter, r *http.Request) {\n        idStr := strings.TrimPrefix(r.URL.Path, "/orders/")\n        id, err := strconv.Atoi(idStr)\n        if err != nil {\n            http.Error(w, "bad id", 400)\n            return\n        }\n        o, ok := orders[id]\n        if !ok {\n            http.Error(w, "not found", 404)\n            return\n        }\n        name, _ := getUserName(r.Context(), userServiceURL, o.UserID)\n        o.UserName = name\n        w.Header().Set("Content-Type", "application/json")\n        json.NewEncoder(w).Encode(o)\n    })\n\n    log.Printf("Order service на :%s", port)\n    log.Fatal(http.ListenAndServe(":"+port, nil))\n}\n\n// ---\n// docker-compose.yml\n// version: "3.8"\n// services:\n//   user-service:\n//     build: ./user-service\n//     ports:\n//       - "8001:8001"\n//     environment:\n//       PORT: "8001"\n//     networks:\n//       - app-net\n//\n//   order-service:\n//     build: ./order-service\n//     ports:\n//       - "8002:8002"\n//     environment:\n//       PORT: "8002"\n//       USER_SERVICE_URL: "http://user-service:8001"\n//     depends_on:\n//       - user-service\n//     networks:\n//       - app-net\n//\n// networks:\n//   app-net:\n//     driver: bridge',
      explanation: 'os.Getenv для конфигурации позволяет менять поведение без перекомпиляции. В docker-compose сервис "user-service" доступен по DNS имени user-service из других контейнеров в той же сети. depends_on гарантирует порядок запуска, но не ждёт готовности сервиса — для этого нужен healthcheck. Retry логика в клиенте защищает от временных сбоев при старте системы.'
    }
  ]
}
