export default {
  id: 54,
  title: 'Шаблоны html/template',
  description: 'Пакет html/template для генерации HTML: безопасное экранирование, управляющие конструкции, функции шаблонов, вложенные шаблоны и паттерн layout для веб-приложений.',
  lessons: [
    {
      id: 1,
      title: 'Основы html/template',
      type: 'theory',
      content: [
        { type: 'text', value: 'html/template — это как форма с пробелами для заполнения. Вы создаёте HTML-шаблон с местами-заполнителями, а потом подставляете в них данные. Главное отличие от text/template — автоматическая защита от XSS.' },
        { type: 'heading', value: 'Первый шаблон' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "html/template"\n    "log"\n    "os"\n)\n\n// Данные для шаблона\ntype PageData struct {\n    Title   string\n    Message string\n    Name    string\n}\n\nfunc main() {\n    // Шаблон в строке\n    tmplStr := `<!DOCTYPE html>\n<html lang="ru">\n<head>\n    <title>{{.Title}}</title>\n</head>\n<body>\n    <h1>{{.Message}}</h1>\n    <p>Привет, {{.Name}}!</p>\n</body>\n</html>`\n\n    // Парсинг шаблона\n    tmpl, err := template.New("page").Parse(tmplStr)\n    if err != nil {\n        log.Fatal("Parse:", err)\n    }\n\n    // Данные\n    data := PageData{\n        Title:   "Мой сайт",\n        Message: "Добро пожаловать",\n        Name:    "<script>alert(\'XSS\')</script>", // опасные данные\n    }\n\n    // Выполнение: шаблон + данные -> вывод\n    // html/template автоматически экранирует опасный HTML!\n    if err := tmpl.Execute(os.Stdout, data); err != nil {\n        log.Fatal("Execute:", err)\n    }\n    // Вывод: <p>Привет, &lt;script&gt;alert(\'XSS\')&lt;/script&gt;!</p>\n}' },
        { type: 'heading', value: 'Шаблон из файла' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "html/template"\n    "log"\n    "net/http"\n)\n\ntype User struct {\n    Name  string\n    Email string\n    Admin bool\n}\n\nvar tmpl *template.Template\n\nfunc init() {\n    var err error\n    // ParseFiles загружает файлы шаблонов\n    tmpl, err = template.ParseFiles(\n        "templates/layout.html",\n        "templates/user.html",\n    )\n    if err != nil {\n        log.Fatal("Шаблоны не загружены:", err)\n    }\n}\n\nfunc userHandler(w http.ResponseWriter, r *http.Request) {\n    data := User{\n        Name:  "Нурдаулет",\n        Email: "n@example.com",\n        Admin: true,\n    }\n\n    w.Header().Set("Content-Type", "text/html; charset=utf-8")\n\n    // ExecuteTemplate выполняет конкретный именованный шаблон\n    if err := tmpl.ExecuteTemplate(w, "layout.html", data); err != nil {\n        http.Error(w, "Ошибка шаблона", http.StatusInternalServerError)\n        log.Printf("template: %v", err)\n        return\n    }\n}\n\nfunc main() {\n    http.HandleFunc("/user", userHandler)\n    log.Fatal(http.ListenAndServe(":8080", nil))\n}' },
        { type: 'warning', value: 'Никогда не используйте text/template для генерации HTML — он не экранирует данные и уязвим для XSS-атак. Всегда используйте html/template для веб-приложений.' }
      ]
    },
    {
      id: 2,
      title: 'Execute и данные шаблона',
      type: 'theory',
      content: [
        { type: 'text', value: 'В шаблоне данные доступны через точку (.). Точка — это "текущий контекст". Обращение к полям структуры: {{.FieldName}}. Вызов методов: {{.Method}}.' },
        { type: 'heading', value: 'Точка и доступ к данным' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "html/template"\n    "os"\n    "time"\n)\n\ntype Article struct {\n    ID        int\n    Title     string\n    Author    string\n    Content   string\n    Tags      []string\n    CreatedAt time.Time\n    Views     int\n}\n\n// Метод на структуре доступен в шаблоне\nfunc (a Article) IsPopular() bool {\n    return a.Views > 1000\n}\n\nfunc (a Article) FormattedDate() string {\n    return a.CreatedAt.Format("02.01.2006")\n}\n\nconst articleTmpl = `\n<article>\n    <h2>{{.Title}}</h2>\n    <p>Автор: {{.Author}} | Дата: {{.FormattedDate}}</p>\n    {{if .IsPopular}}\n        <span class="badge">Популярная статья ({{.Views}} просмотров)</span>\n    {{end}}\n    <div class="content">{{.Content}}</div>\n    <div class="tags">\n        {{range .Tags}}\n            <span class="tag">{{.}}</span>\n        {{end}}\n    </div>\n</article>\n`\n\nfunc main() {\n    tmpl := template.Must(template.New("article").Parse(articleTmpl))\n\n    article := Article{\n        ID:        1,\n        Title:     "Изучаем Go шаблоны",\n        Author:    "Нурдаулет",\n        Content:   "Шаблоны очень удобны...",\n        Tags:      []string{"Go", "web", "templates"},\n        CreatedAt: time.Now(),\n        Views:     1500,\n    }\n\n    tmpl.Execute(os.Stdout, article)\n}' },
        { type: 'heading', value: 'template.Must и обработка ошибок' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "html/template"\n    "log"\n    "os"\n)\n\nfunc main() {\n    // template.Must паникует если есть ошибка парсинга\n    // Используется при инициализации — ошибки в шаблонах недопустимы\n    tmpl := template.Must(template.New("test").Parse(`<p>{{.Name}}</p>`))\n    tmpl.Execute(os.Stdout, map[string]string{"Name": "Go"})\n\n    // Глобальные переменные шаблонов — безопасная инициализация\n    // var tmpl = template.Must(template.ParseFiles("templates/index.html"))\n\n    // Проверка ошибки вручную\n    t, err := template.New("safe").Parse(`{{.MissingMethod}}`)\n    if err != nil {\n        log.Fatal("Ошибка парсинга:", err)\n    }\n\n    // Ошибка выполнения (метод не найден)\n    if err := t.Execute(os.Stdout, struct{ Name string }{"test"}); err != nil {\n        log.Println("Ошибка выполнения (ожидаемая):", err)\n    }\n\n    // Передача nil как данных\n    tmplNull := template.Must(template.New("null").Parse(`<p>Страница</p>`))\n    tmplNull.Execute(os.Stdout, nil) // nil допустим если шаблон не использует данные\n}' },
        { type: 'tip', value: 'template.Must — идиома для инициализации глобальных шаблонов. Если шаблон не компилируется — это программная ошибка, паника при старте лучше чем непонятное поведение в рантайме.' }
      ]
    },
    {
      id: 3,
      title: 'Управляющие конструкции: if, range, with',
      type: 'theory',
      content: [
        { type: 'text', value: 'Шаблоны поддерживают условия, циклы и контекст-переключатель. Это позволяет создавать динамический HTML на основе данных.' },
        { type: 'heading', value: 'if, else if, else' },
        { type: 'code', language: 'go', value: '// templates/user.html\n//\n// {{if .IsAdmin}}\n//   <div class="admin-panel">Панель администратора</div>\n// {{else if .IsModerator}}\n//   <div class="mod-panel">Панель модератора</div>\n// {{else}}\n//   <div class="user-panel">Панель пользователя</div>\n// {{end}}\n//\n// Сравнения:\n// {{if eq .Role "admin"}}    -- равно\n// {{if ne .Age 0}}           -- не равно\n// {{if gt .Views 100}}       -- больше\n// {{if lt .Price 1000}}      -- меньше\n// {{if ge .Score 90}}        -- больше или равно\n// {{if le .Errors 0}}        -- меньше или равно\n//\n// Логические операции:\n// {{if and .IsActive .IsVerified}}\n// {{if or .IsAdmin .IsModerator}}\n// {{if not .IsBanned}}\n\npackage main\n\nimport (\n    "html/template"\n    "os"\n)\n\nfunc main() {\n    tmpl := template.Must(template.New("t").Parse(`\n{{- if gt .Score 90 -}}\n    Отлично: {{.Score}} баллов\n{{- else if gt .Score 70 -}}\n    Хорошо: {{.Score}} баллов\n{{- else -}}\n    Удовлетворительно: {{.Score}} баллов\n{{- end -}}\n`))\n    // {{- ... -}} убирает пробелы/переносы вокруг тега\n\n    tmpl.Execute(os.Stdout, map[string]int{"Score": 85})\n    // Вывод: Хорошо: 85 баллов\n}' },
        { type: 'heading', value: 'range — цикл по данным' },
        { type: 'code', language: 'go', value: 'const listTmpl = `\n<ul>\n{{range .Items}}\n    <li>{{.Name}} — {{.Price}} руб.</li>\n{{else}}\n    <li>Список пуст</li>\n{{end}}\n</ul>\n\n<ol>\n{{range $index, $item := .Items}}\n    <li>{{$index}}. {{$item.Name}}</li>\n{{end}}\n</ol>\n\n{{range $key, $val := .Config}}\n    <p>{{$key}}: {{$val}}</p>\n{{end}}\n`\n\npackage main\n\nimport (\n    "html/template"\n    "os"\n)\n\ntype Item struct {\n    Name  string\n    Price float64\n}\n\nfunc main() {\n    tmpl := template.Must(template.New("list").Parse(listTmpl))\n    tmpl.Execute(os.Stdout, map[string]interface{}{\n        "Items": []Item{\n            {"Яблоко", 50.0},\n            {"Банан", 35.5},\n        },\n        "Config": map[string]string{\n            "язык": "русский",\n            "тема": "тёмная",\n        },\n    })\n}' },
        { type: 'heading', value: 'with — переключение контекста' },
        { type: 'code', language: 'go', value: 'const profileTmpl = `\n{{with .User}}\n    <div class="profile">\n        <h2>{{.Name}}</h2>\n        <p>{{.Email}}</p>\n        {{with .Address}}\n            <address>{{.City}}, {{.Street}}</address>\n        {{else}}\n            <p>Адрес не указан</p>\n        {{end}}\n    </div>\n{{else}}\n    <p>Пользователь не найден</p>\n{{end}}\n`\n// with меняет точку (.) на значение выражения\n// Если значение пустое/nil/zero — выполняется {{else}}' }
      ]
    },
    {
      id: 4,
      title: 'Функции шаблонов',
      type: 'theory',
      content: [
        { type: 'text', value: 'Шаблоны поддерживают встроенные функции (len, printf, html, etc.) и пользовательские. Пользовательские функции добавляются через FuncMap.' },
        { type: 'heading', value: 'FuncMap — пользовательские функции' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "html/template"\n    "os"\n    "strings"\n    "time"\n)\n\nfunc main() {\n    // Регистрируем функции ДО парсинга шаблона\n    funcMap := template.FuncMap{\n        // Форматирование даты\n        "formatDate": func(t time.Time) string {\n            return t.Format("02 января 2006")\n        },\n        // Форматирование цены\n        "formatPrice": func(price float64) string {\n            return fmt.Sprintf("%.2f руб.", price)\n        },\n        // Обрезка строки\n        "truncate": func(s string, n int) string {\n            if len([]rune(s)) <= n {\n                return s\n            }\n            return string([]rune(s)[:n]) + "..."\n        },\n        // Верхний регистр\n        "upper": strings.ToUpper,\n        // Соединение слайса\n        "join": strings.Join,\n        // Последний элемент\n        "last": func(i int, slice []string) bool {\n            return i == len(slice)-1\n        },\n    }\n\n    tmplStr := `\n<article>\n    <h2>{{upper .Title}}</h2>\n    <p>Дата: {{formatDate .Date}}</p>\n    <p>Цена: {{formatPrice .Price}}</p>\n    <p>{{truncate .Content 100}}</p>\n    <p>Теги: {{join .Tags ", "}}</p>\n    <p>Длина заголовка: {{len .Title}}</p>\n    {{printf "ID: %05d" .ID}}\n</article>\n`\n\n    // Создаём шаблон с FuncMap\n    tmpl := template.Must(\n        template.New("article").Funcs(funcMap).Parse(tmplStr),\n    )\n\n    tmpl.Execute(os.Stdout, map[string]interface{}{\n        "ID":      42,\n        "Title":   "изучаем шаблоны go",\n        "Date":    time.Now(),\n        "Price":   1250.5,\n        "Content": "Это длинное описание статьи о шаблонах Go, которое нужно обрезать до ста символов",\n        "Tags":    []string{"Go", "web", "template"},\n    })\n}' },
        { type: 'heading', value: 'Встроенные функции' },
        { type: 'list', value: '{{len .Items}} — длина слайса/строки/map\n{{printf "%d" .ID}} — форматирование (как fmt.Sprintf)\n{{html .Content}} — экранировать как HTML (текст/template)\n{{js .Data}} — экранировать как JavaScript\n{{urlquery .Search}} — URL-кодирование параметра\n{{index .Slice 0}} — элемент слайса по индексу\n{{call .Func arg1 arg2}} — вызов функции из данных' }
      ]
    },
    {
      id: 5,
      title: 'Вложенные шаблоны и define',
      type: 'theory',
      content: [
        { type: 'text', value: 'Шаблоны можно вкладывать друг в друга через {{template "name" .}} и объявлять именованные блоки через {{define "name"}}. Это основа для повторного использования компонентов.' },
        { type: 'heading', value: 'define и template' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "html/template"\n    "os"\n)\n\n// Несколько именованных шаблонов в одном ParseFiles\nconst componentsTmpl = `\n{{define "header"}}\n<header>\n    <nav>\n        <a href="/">Главная</a>\n        <a href="/about">О нас</a>\n        {{if .User}}\n            <a href="/profile">{{.User.Name}}</a>\n            <a href="/logout">Выйти</a>\n        {{else}}\n            <a href="/login">Войти</a>\n        {{end}}\n    </nav>\n</header>\n{{end}}\n\n{{define "footer"}}\n<footer>\n    <p>© 2024 Мой сайт</p>\n</footer>\n{{end}}\n\n{{define "userCard"}}\n<div class="user-card">\n    <h3>{{.Name}}</h3>\n    <p>{{.Email}}</p>\n</div>\n{{end}}\n`\n\nconst pageTmpl = `\n<!DOCTYPE html>\n<html>\n<body>\n    {{template "header" .}}\n    <main>\n        <h1>{{.Title}}</h1>\n        <div class="users">\n            {{range .Users}}\n                {{template "userCard" .}}\n            {{end}}\n        </div>\n    </main>\n    {{template "footer" .}}\n</body>\n</html>\n`\n\ntype AppUser struct {\n    Name  string\n    Email string\n}\n\nfunc main() {\n    // Парсим все шаблоны вместе\n    tmpl := template.Must(\n        template.New("page").Parse(componentsTmpl + pageTmpl),\n    )\n\n    tmpl.Execute(os.Stdout, map[string]interface{}{\n        "Title": "Список пользователей",\n        "User":  &AppUser{Name: "Нурдаулет", Email: "n@example.com"},\n        "Users": []AppUser{\n            {Name: "Айгерим", Email: "a@example.com"},\n            {Name: "Берик", Email: "b@example.com"},\n        },\n    })\n}' },
        { type: 'tip', value: 'При вызове {{template "userCard" .}} точка передаётся в вложенный шаблон. Если нужно передать конкретное поле — {{template "userCard" .User}}. Если не нужен контекст — {{template "footer" nil}}.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: веб-приложение с шаблонами',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создай веб-приложение — каталог фильмов с HTML-шаблонами. Используй layout-паттерн с общей шапкой и подвалом, список фильмов и страницу деталей.',
      requirements: [
        'Структура Movie: ID int, Title string, Year int, Genre string, Rating float64',
        'Два маршрута: GET / (список фильмов) и GET /movie/{id} (детали фильма)',
        'Шаблоны в памяти (без файлов): layout, list, detail',
        'Layout: DOCTYPE, head с title, header, main ({{block "content" .}}), footer',
        'Список: таблица фильмов со ссылками на детали, форматированный рейтинг (1 знак после запятой)',
        'Детали: все поля фильма, кнопка "Назад", цвет рейтинга (>8 — зелёный, <6 — красный)',
        'FuncMap: formatRating (float64 -> "8.5/10"), colorClass (float64 -> CSS класс)',
        '5 предзаполненных фильмов'
      ],
      expectedOutput: 'GET / -> HTML таблица с 5 фильмами\nGET /movie/1 -> HTML страница с деталями фильма\nGET /movie/99 -> 404 Not Found',
      hint: 'Используй template.New("layout").Funcs(funcMap).Parse(layoutHTML). Для ParseGlob не нужен — парси строки через Parse. block "content" с default содержимым определяется через {{block "content" .}}{{end}} в layout и {{define "content"}}...{{end}} в дочерних шаблонах.',
      solution: 'package main\n\nimport (\n    "fmt"\n    "html/template"\n    "log"\n    "net/http"\n    "strconv"\n)\n\ntype Movie struct {\n    ID     int\n    Title  string\n    Year   int\n    Genre  string\n    Rating float64\n}\n\nvar movies = []Movie{\n    {1, "Интерстеллар", 2014, "Sci-Fi", 8.6},\n    {2, "Начало", 2010, "Sci-Fi", 8.8},\n    {3, "Тёмный рыцарь", 2008, "Action", 9.0},\n    {4, "Форрест Гамп", 1994, "Drama", 8.8},\n    {5, "Матрица", 1999, "Sci-Fi", 8.7},\n}\n\nvar funcMap = template.FuncMap{\n    "formatRating": func(r float64) string {\n        return fmt.Sprintf("%.1f/10", r)\n    },\n    "colorClass": func(r float64) string {\n        if r >= 8.0 {\n            return "green"\n        } else if r < 6.0 {\n            return "red"\n        }\n        return "orange"\n    },\n}\n\nconst layoutTmpl = `<!DOCTYPE html>\n<html lang="ru">\n<head>\n    <meta charset="UTF-8">\n    <title>{{.Title}} — Кинотека</title>\n    <style>\n        .green{color:green} .red{color:red} .orange{color:orange}\n        table{border-collapse:collapse;width:100%}\n        td,th{border:1px solid #ccc;padding:8px}\n    </style>\n</head>\n<body>\n    <header><h1><a href="/">Кинотека</a></h1></header>\n    <main>{{block "content" .}}{{end}}</main>\n    <footer><p>Go html/template пример</p></footer>\n</body>\n</html>`\n\nconst listTmpl = `{{define "content"}}\n<h2>Все фильмы ({{len .Movies}})</h2>\n<table>\n<tr><th>Название</th><th>Год</th><th>Жанр</th><th>Рейтинг</th></tr>\n{{range .Movies}}\n<tr>\n    <td><a href="/movie/{{.ID}}">{{.Title}}</a></td>\n    <td>{{.Year}}</td>\n    <td>{{.Genre}}</td>\n    <td class="{{colorClass .Rating}}">{{formatRating .Rating}}</td>\n</tr>\n{{end}}\n</table>\n{{end}}`\n\nconst detailTmpl = `{{define "content"}}\n<h2>{{.Movie.Title}}</h2>\n<p>Год: {{.Movie.Year}}</p>\n<p>Жанр: {{.Movie.Genre}}</p>\n<p>Рейтинг: <strong class="{{colorClass .Movie.Rating}}">{{formatRating .Movie.Rating}}</strong></p>\n<a href="/">← Назад к списку</a>\n{{end}}`\n\nfunc parseTemplates(content string) *template.Template {\n    return template.Must(\n        template.New("layout").Funcs(funcMap).Parse(layoutTmpl + content),\n    )\n}\n\nfunc findMovie(id int) (*Movie, bool) {\n    for i := range movies {\n        if movies[i].ID == id {\n            return &movies[i], true\n        }\n    }\n    return nil, false\n}\n\nfunc main() {\n    http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) {\n        if r.URL.Path != "/" {\n            http.NotFound(w, r)\n            return\n        }\n        tmpl := parseTemplates(listTmpl)\n        w.Header().Set("Content-Type", "text/html; charset=utf-8")\n        tmpl.Execute(w, map[string]interface{}{"Title": "Список фильмов", "Movies": movies})\n    })\n\n    http.HandleFunc("/movie/", func(w http.ResponseWriter, r *http.Request) {\n        idStr := r.URL.Path[len("/movie/"):]\n        id, err := strconv.Atoi(idStr)\n        if err != nil {\n            http.NotFound(w, r)\n            return\n        }\n        movie, ok := findMovie(id)\n        if !ok {\n            http.NotFound(w, r)\n            return\n        }\n        tmpl := parseTemplates(detailTmpl)\n        w.Header().Set("Content-Type", "text/html; charset=utf-8")\n        tmpl.Execute(w, map[string]interface{}{"Title": movie.Title, "Movie": movie})\n    })\n\n    fmt.Println("Кинотека на http://localhost:8080")\n    log.Fatal(http.ListenAndServe(":8080", nil))\n}',
      explanation: 'html/template автоматически экранирует все пользовательские данные — защита от XSS встроена. FuncMap позволяет добавлять произвольные Go-функции в шаблоны. Паттерн layout + block позволяет переиспользовать общую структуру страницы. template.Must паникует при ошибке парсинга — подходит для инициализации глобальных шаблонов, но для динамических шаблонов проверяйте ошибку явно.'
    }
  ]
}
