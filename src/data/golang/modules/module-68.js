export default {
  id: 68,
  title: 'Очереди сообщений: RabbitMQ и Kafka',
  description: 'Асинхронная обработка в Go: концепции очередей сообщений, RabbitMQ через amqp пакет, Kafka через segmentio/kafka-go, паттерны producer/consumer и обработка ошибок.',
  lessons: [
    {
      id: 1,
      title: 'Концепция очередей сообщений',
      type: 'theory',
      content: [
        { type: 'text', value: 'Очередь сообщений — это посредник между сервисами. Отправитель кладёт сообщение в очередь и не ждёт ответа. Получатель забирает сообщения, когда готов. Это как почтовый ящик: письмо можно положить в любое время, прочитают — когда удобно.' },
        { type: 'heading', value: 'Зачем нужны очереди сообщений' },
        { type: 'code', language: 'go', value: '// БЕЗ очереди: синхронный подход\n// Пользователь регистрируется -> ждёт пока:\n// 1. Запишется в БД\n// 2. Отправится email\n// 3. Начислятся бонусы\n// 4. Обновится аналитика\n// Всё это занимает 2-3 секунды -> плохой UX!\n\n// С очередью: асинхронный подход\n// 1. Пользователь регистрируется\n// 2. Записываемся в БД (100ms)\n// 3. Кладём событие "user_registered" в очередь\n// 4. Возвращаем ответ пользователю (200ms) <- быстро!\n// 5. В фоне: email, бонусы, аналитика обрабатываются независимо\n\n// Преимущества очередей:\n// - Развязка (decoupling): сервисы независимы\n// - Буферизация: consumer может обрабатывать медленнее producer\n// - Надёжность: сообщение не потеряется если consumer упал\n// - Масштабирование: несколько consumers параллельно\n// - Повтор: при сбое сообщение можно обработать снова\n\n// RabbitMQ vs Kafka:\n// RabbitMQ:\n//   - Традиционная очередь (FIFO)\n//   - Гарантированная доставка\n//   - Маршрутизация сообщений (exchanges)\n//   - Подходит для: задачи, уведомления, RPC\n//\n// Kafka:\n//   - Лог событий (log-based)\n//   - Огромная пропускная способность (миллионы событий/сек)\n//   - Хранит историю (retention)\n//   - Подходит для: event streaming, аналитика, audit log\n\npackage main\n\nimport "fmt"\n\nfunc main() {\n    fmt.Println("Это концептуальный урок — код в следующих уроках")\n}' },
        { type: 'note', value: 'Выбирайте RabbitMQ для задач с маршрутизацией, приоритетами и гарантированной доставкой. Kafka — для высоконагруженных потоков событий, когда нужна история сообщений и возможность "перемотать" и обработать заново.' }
      ]
    },
    {
      id: 2,
      title: 'RabbitMQ — подключение и базовые операции',
      type: 'theory',
      content: [
        { type: 'text', value: 'RabbitMQ использует протокол AMQP. Ключевые понятия: Exchange (маршрутизатор), Queue (очередь), Binding (правило маршрутизации). Producer отправляет в Exchange, который решает, в какую Queue положить сообщение.' },
        { type: 'heading', value: 'Подключение и объявление очереди' },
        { type: 'code', language: 'go', value: '// Установка:\n// go get github.com/rabbitmq/amqp091-go\n\npackage main\n\nimport (\n    "context"\n    "log"\n    "time"\n\n    amqp "github.com/rabbitmq/amqp091-go"\n)\n\nfunc connectRabbitMQ(url string) (*amqp.Connection, *amqp.Channel, error) {\n    // Подключение с retry\n    var conn *amqp.Connection\n    var err error\n\n    for attempt := 1; attempt <= 5; attempt++ {\n        conn, err = amqp.Dial(url)\n        if err == nil {\n            break\n        }\n        log.Printf("Попытка %d подключения к RabbitMQ: %v", attempt, err)\n        time.Sleep(time.Duration(attempt) * time.Second)\n    }\n    if err != nil {\n        return nil, nil, err\n    }\n\n    // Создаём канал (channel) — лёгкий объект на одно соединение\n    ch, err := conn.Channel()\n    if err != nil {\n        conn.Close()\n        return nil, nil, err\n    }\n\n    return conn, ch, nil\n}\n\nfunc declareQueue(ch *amqp.Channel, queueName string) (amqp.Queue, error) {\n    // QueueDeclare — создаём очередь (идемпотентно)\n    return ch.QueueDeclare(\n        queueName, // имя\n        true,      // durable: очередь выживет при рестарте RabbitMQ\n        false,     // auto-delete: не удалять когда нет consumers\n        false,     // exclusive: только для этого соединения\n        false,     // no-wait: не ждать подтверждения\n        nil,       // аргументы (TTL, max-length и т.д.)\n    )\n}\n\nfunc main() {\n    conn, ch, err := connectRabbitMQ("amqp://guest:guest@localhost:5672/")\n    if err != nil {\n        log.Fatal("Не могу подключиться:", err)\n    }\n    defer conn.Close()\n    defer ch.Close()\n\n    // Объявляем очередь\n    q, err := declareQueue(ch, "tasks")\n    if err != nil {\n        log.Fatal("Ошибка создания очереди:", err)\n    }\n\n    log.Printf("Очередь \"%s\" готова. Сообщений: %d, Consumers: %d",\n        q.Name, q.Messages, q.Consumers)\n\n    // Настраиваем QoS (Quality of Service)\n    // prefetchCount=1: consumer берёт только 1 сообщение за раз\n    err = ch.Qos(\n        1,     // prefetch count\n        0,     // prefetch size (0 = без ограничений)\n        false, // global\n    )\n    if err != nil {\n        log.Fatal("Ошибка QoS:", err)\n    }\n\n    _ = context.Background()\n    log.Println("RabbitMQ подключение готово!")\n}' }
      ]
    },
    {
      id: 3,
      title: 'RabbitMQ — публикация и потребление',
      type: 'theory',
      content: [
        { type: 'text', value: 'Publisher отправляет сообщения в очередь. Consumer читает и обрабатывает их. Важно: acknowledgment (ack) — подтверждение обработки. Пока consumer не отправит ack, RabbitMQ считает сообщение необработанным.' },
        { type: 'heading', value: 'Producer и Consumer' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "context"\n    "encoding/json"\n    "log"\n    "time"\n\n    amqp "github.com/rabbitmq/amqp091-go"\n)\n\ntype Task struct {\n    ID      string `json:"id"`\n    Type    string `json:"type"`\n    Payload string `json:"payload"`\n}\n\n// PRODUCER: отправка сообщений\nfunc publishTask(ch *amqp.Channel, task Task) error {\n    body, err := json.Marshal(task)\n    if err != nil {\n        return err\n    }\n\n    return ch.PublishWithContext(\n        context.Background(),\n        "",       // exchange: "" = default exchange\n        "tasks",  // routing key = имя очереди для default exchange\n        false,    // mandatory: вернуть ошибку если нет подходящей очереди\n        false,    // immediate: отправить только если есть consumer\n        amqp.Publishing{\n            ContentType:  "application/json",\n            DeliveryMode: amqp.Persistent, // пережить рестарт RabbitMQ\n            Body:         body,\n            Timestamp:    time.Now(),\n            MessageId:    task.ID,\n        },\n    )\n}\n\n// CONSUMER: получение и обработка сообщений\nfunc consumeTasks(ch *amqp.Channel, queueName string) error {\n    // Начинаем потреблять\n    msgs, err := ch.Consume(\n        queueName,    // очередь\n        "",           // consumer tag (пустой = автогенерация)\n        false,        // auto-ack: НЕ подтверждать автоматически!\n        false,        // exclusive\n        false,        // no-local\n        false,        // no-wait\n        nil,          // аргументы\n    )\n    if err != nil {\n        return err\n    }\n\n    log.Println("Consumer запущен, ожидание сообщений...")\n\n    // Обрабатываем сообщения из канала\n    for msg := range msgs {\n        var task Task\n        if err := json.Unmarshal(msg.Body, &task); err != nil {\n            log.Printf("Ошибка парсинга: %v", err)\n            msg.Nack(false, false) // отклонить, не возвращать в очередь\n            continue\n        }\n\n        log.Printf("Получена задача: %+v", task)\n\n        // Обрабатываем\n        if err := processTask(task); err != nil {\n            log.Printf("Ошибка обработки: %v, requeue=true", err)\n            msg.Nack(false, true) // вернуть в очередь для повтора\n            continue\n        }\n\n        // Успешно обработано — подтверждаем\n        msg.Ack(false) // false = подтвердить только это сообщение\n        log.Printf("Задача %s выполнена", task.ID)\n    }\n\n    return nil\n}\n\nfunc processTask(task Task) error {\n    // Имитируем обработку\n    time.Sleep(100 * time.Millisecond)\n    log.Printf("Обрабатываю задачу типа '%s': %s", task.Type, task.Payload)\n    return nil\n}\n\nfunc main() {\n    conn, err := amqp.Dial("amqp://guest:guest@localhost:5672/")\n    if err != nil {\n        log.Fatal(err)\n    }\n    defer conn.Close()\n\n    ch, _ := conn.Channel()\n    defer ch.Close()\n\n    ch.QueueDeclare("tasks", true, false, false, false, nil)\n    ch.Qos(1, 0, false)\n\n    // Публикуем задачи\n    for i := 1; i <= 3; i++ {\n        task := Task{\n            ID:      fmt.Sprintf("task-%d", i),\n            Type:    "email",\n            Payload: fmt.Sprintf("user%d@mail.ru", i),\n        }\n        publishTask(ch, task)\n        log.Printf("Опубликована задача: %s", task.ID)\n    }\n\n    // Запускаем consumer\n    go consumeTasks(ch, "tasks")\n\n    // Ждём\n    time.Sleep(2 * time.Second)\n}' },
        { type: 'warning', value: 'Всегда используйте manual acknowledgment (auto-ack: false) в production. Auto-ack подтверждает сообщение сразу при получении, даже если обработка упала — сообщение будет потеряно.' }
      ]
    },
    {
      id: 4,
      title: 'Kafka — основы и настройка',
      type: 'theory',
      content: [
        { type: 'text', value: 'Apache Kafka — это распределённый журнал событий. В отличие от RabbitMQ, Kafka хранит сообщения на диске (retention period). Каждая тема (topic) делится на партиции (partitions) для параллелизма. Consumer читает из партиции, запоминая позицию (offset).' },
        { type: 'heading', value: 'Ключевые концепции Kafka' },
        { type: 'code', language: 'go', value: '// Kafka архитектура:\n// Topic: логическая группа сообщений (как таблица в БД)\n// Partition: физическое разбиение topic (для масштабирования)\n//   - Сообщения внутри партиции строго упорядочены\n//   - Разные партиции могут обрабатываться параллельно\n// Offset: позиция сообщения в партиции (начиная с 0)\n// Consumer Group: группа consumers, которые вместе читают topic\n//   - Каждая партиция назначается только одному consumer в группе\n//   - Разные группы читают независимо (разные offset)\n// Broker: один сервер Kafka\n// Cluster: несколько brokers\n\n// Визуализация:\n// Topic "orders" (3 партиции):\n// Partition 0: [msg0, msg1, msg4, msg7, ...]\n// Partition 1: [msg2, msg5, msg8, ...]\n// Partition 2: [msg3, msg6, msg9, ...]\n//\n// Consumer Group "order-processors" (3 consumers):\n// Consumer A -> Partition 0\n// Consumer B -> Partition 1\n// Consumer C -> Partition 2\n\n// Установка:\n// go get github.com/segmentio/kafka-go\n\npackage main\n\nimport (\n    "context"\n    "fmt"\n    "log"\n    "time"\n\n    "github.com/segmentio/kafka-go"\n)\n\nfunc createTopic(brokers []string, topic string, numPartitions int) error {\n    conn, err := kafka.Dial("tcp", brokers[0])\n    if err != nil {\n        return fmt.Errorf("dial: %w", err)\n    }\n    defer conn.Close()\n\n    controller, err := conn.Controller()\n    if err != nil {\n        return fmt.Errorf("controller: %w", err)\n    }\n\n    controllerConn, err := kafka.Dial("tcp", fmt.Sprintf("%s:%d", controller.Host, controller.Port))\n    if err != nil {\n        return fmt.Errorf("controller dial: %w", err)\n    }\n    defer controllerConn.Close()\n\n    err = controllerConn.CreateTopics(kafka.TopicConfig{\n        Topic:             topic,\n        NumPartitions:     numPartitions,\n        ReplicationFactor: 1,\n    })\n    if err != nil {\n        return fmt.Errorf("create topic: %w", err)\n    }\n\n    log.Printf("Топик \"%s\" создан (%d партиций)", topic, numPartitions)\n    return nil\n}\n\nfunc main() {\n    brokers := []string{"localhost:9092"}\n    ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)\n    defer cancel()\n\n    if err := createTopic(brokers, "user-events", 3); err != nil {\n        log.Printf("Топик возможно уже существует: %v", err)\n    }\n\n    fmt.Println("Kafka готова!")\n    _ = ctx\n}' }
      ]
    },
    {
      id: 5,
      title: 'Kafka Producer и Consumer',
      type: 'theory',
      content: [
        { type: 'text', value: 'kafka-go предоставляет простой API для отправки и получения сообщений. Producer пишет с ключом (для партиционирования), Consumer читает с отслеживанием offset в группе.' },
        { type: 'heading', value: 'Producer и Consumer в kafka-go' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "context"\n    "encoding/json"\n    "fmt"\n    "log"\n    "time"\n\n    "github.com/segmentio/kafka-go"\n)\n\ntype UserEvent struct {\n    UserID    int    `json:"user_id"`\n    EventType string `json:"event_type"`\n    Data      string `json:"data"`\n    Timestamp string `json:"timestamp"`\n}\n\n// PRODUCER\nfunc newProducer(brokers []string, topic string) *kafka.Writer {\n    return &kafka.Writer{\n        Addr:         kafka.TCP(brokers...),\n        Topic:        topic,\n        Balancer:     &kafka.LeastBytes{}, // балансировка по партициям\n        BatchTimeout: 10 * time.Millisecond,\n        // Важные настройки надёжности:\n        RequiredAcks: kafka.RequireAll, // ждать подтверждения от всех реплик\n        Async:        false,            // синхронная отправка\n    }\n}\n\nfunc publishEvent(w *kafka.Writer, event UserEvent) error {\n    data, err := json.Marshal(event)\n    if err != nil {\n        return err\n    }\n\n    return w.WriteMessages(context.Background(), kafka.Message{\n        // Ключ определяет партицию: один user_id -> одна партиция\n        // Гарантирует порядок событий для одного пользователя\n        Key:   []byte(fmt.Sprintf("user-%d", event.UserID)),\n        Value: data,\n        Headers: []kafka.Header{\n            {Key: "event_type", Value: []byte(event.EventType)},\n            {Key: "source",     Value: []byte("user-service")},\n        },\n    })\n}\n\n// CONSUMER\nfunc newConsumer(brokers []string, topic, groupID string) *kafka.Reader {\n    return kafka.NewReader(kafka.ReaderConfig{\n        Brokers:        brokers,\n        Topic:          topic,\n        GroupID:        groupID,    // Consumer Group\n        MinBytes:       1,          // минимум 1 байт для чтения\n        MaxBytes:       1024*1024,  // максимум 1MB\n        MaxWait:        500*time.Millisecond, // ждать не более 500ms\n        CommitInterval: 1 * time.Second,      // авто-commit offset каждую секунду\n    })\n}\n\nfunc consumeEvents(r *kafka.Reader) {\n    log.Printf("Consumer запущен для топика %s", r.Config().Topic)\n    ctx := context.Background()\n\n    for {\n        // FetchMessage НЕ коммитит offset\n        msg, err := r.FetchMessage(ctx)\n        if err != nil {\n            log.Printf("Ошибка чтения: %v", err)\n            break\n        }\n\n        // Обрабатываем\n        var event UserEvent\n        if err := json.Unmarshal(msg.Value, &event); err != nil {\n            log.Printf("Ошибка парсинга: %v", err)\n            // При ошибке парсинга коммитим — сообщение "испорчено"\n            r.CommitMessages(ctx, msg)\n            continue\n        }\n\n        log.Printf("[Partition:%d Offset:%d] Событие: %+v",\n            msg.Partition, msg.Offset, event)\n\n        // Обрабатываем событие\n        if err := handleEvent(event); err != nil {\n            log.Printf("Ошибка обработки: %v (не коммитим, повторим)", err)\n            continue // не коммитим -> Kafka отдаст снова\n        }\n\n        // Успешно -> коммитим offset\n        if err := r.CommitMessages(ctx, msg); err != nil {\n            log.Printf("Ошибка коммита: %v", err)\n        }\n    }\n}\n\nfunc handleEvent(event UserEvent) error {\n    log.Printf("Обработка события '%s' для user %d", event.EventType, event.UserID)\n    return nil\n}\n\nfunc main() {\n    brokers := []string{"localhost:9092"}\n    topic   := "user-events"\n\n    w := newProducer(brokers, topic)\n    defer w.Close()\n\n    // Публикуем события\n    events := []UserEvent{\n        {UserID: 1, EventType: "login",    Data: "mobile",  Timestamp: time.Now().Format(time.RFC3339)},\n        {UserID: 2, EventType: "purchase", Data: "item_123", Timestamp: time.Now().Format(time.RFC3339)},\n        {UserID: 1, EventType: "logout",   Data: "",         Timestamp: time.Now().Format(time.RFC3339)},\n    }\n\n    for _, e := range events {\n        if err := publishEvent(w, e); err != nil {\n            log.Printf("Ошибка публикации: %v", err)\n        } else {\n            log.Printf("Опубликовано: %s для user %d", e.EventType, e.UserID)\n        }\n    }\n\n    // Запускаем consumer\n    r := newConsumer(brokers, topic, "analytics-group")\n    defer r.Close()\n    go consumeEvents(r)\n\n    time.Sleep(5 * time.Second)\n}' }
      ]
    },
    {
      id: 6,
      title: 'Топики, партиции и порядок сообщений',
      type: 'theory',
      content: [
        { type: 'text', value: 'Порядок сообщений в Kafka гарантируется только внутри одной партиции. Ключ сообщения определяет, в какую партицию оно попадёт (хеш ключа % количество партиций). Это как сортировка посылок: все посылки от одного отправителя едут в один грузовик.' },
        { type: 'heading', value: 'Работа с партициями и offset' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "context"\n    "fmt"\n    "log"\n    "time"\n\n    "github.com/segmentio/kafka-go"\n)\n\n// Ручная отправка в конкретную партицию\nfunc publishToPartition(brokers []string, topic string, partition int, key, value string) error {\n    // Подключаемся к конкретному лидеру партиции\n    conn, err := kafka.DialLeader(\n        context.Background(),\n        "tcp",\n        brokers[0],\n        topic,\n        partition,\n    )\n    if err != nil {\n        return fmt.Errorf("dial leader: %w", err)\n    }\n    defer conn.Close()\n\n    conn.SetWriteDeadline(time.Now().Add(10 * time.Second))\n\n    _, err = conn.WriteMessages(\n        kafka.Message{Key: []byte(key), Value: []byte(value)},\n    )\n    return err\n}\n\n// Стратегии ключей и партиционирования\nfunc partitioningExamples() {\n    // Ключ по UserID -> все события одного пользователя в одной партиции\n    // Порядок событий пользователя гарантирован\n    // user_key := fmt.Sprintf("user:%d", userID)\n\n    // Ключ по OrderID -> события заказа строго по порядку\n    // order_key := fmt.Sprintf("order:%d", orderID)\n\n    // Без ключа (nil) -> round-robin по партициям\n    // Максимальная параллельность, нет гарантий порядка\n\n    fmt.Println("Стратегии партиционирования объяснены")\n}\n\n// Consumer читающий с конкретного offset\nfunc readFromOffset(brokers []string, topic string, partition int, startOffset int64) {\n    r := kafka.NewReader(kafka.ReaderConfig{\n        Brokers:   brokers,\n        Topic:     topic,\n        Partition: partition, // читаем конкретную партицию\n        MinBytes:  1,\n        MaxBytes:  1024 * 1024,\n    })\n    defer r.Close()\n\n    // Устанавливаем позицию чтения\n    r.SetOffset(startOffset)\n\n    ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)\n    defer cancel()\n\n    for {\n        msg, err := r.ReadMessage(ctx) // ReadMessage = FetchMessage + CommitMessage\n        if err != nil {\n            break\n        }\n        log.Printf("[p:%d o:%d] key=%s value=%s",\n            msg.Partition, msg.Offset, msg.Key, msg.Value)\n    }\n}\n\n// Специальные offset значения\nfunc offsetConstants() {\n    // kafka.FirstOffset (-2) = начало топика (самое старое)\n    // kafka.LastOffset  (-1) = конец топика (новые сообщения)\n\n    r := kafka.NewReader(kafka.ReaderConfig{\n        Brokers:   []string{"localhost:9092"},\n        Topic:     "user-events",\n        Partition: 0,\n    })\n    defer r.Close()\n\n    // Читаем с начала (replay)\n    r.SetOffset(kafka.FirstOffset)\n\n    // Или только новые сообщения\n    r.SetOffset(kafka.LastOffset)\n\n    _ = r\n    fmt.Println("Offset примеры демонстрированы")\n}\n\nfunc main() {\n    partitioningExamples()\n    offsetConstants()\n    fmt.Println("Примеры партиционирования готовы")\n}' }
      ]
    },
    {
      id: 7,
      title: 'Практика: система уведомлений через RabbitMQ',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создай систему уведомлений: HTTP API принимает запросы на отправку уведомлений и кладёт их в RabbitMQ очередь. Воркер читает из очереди и "отправляет" уведомления (имитация) с retry логикой.',
      requirements: [
        'HTTP POST /notify принимает {type, recipient, message} и публикует в RabbitMQ',
        'Worker читает из очереди "notifications" и обрабатывает',
        'При ошибке обработки: retry до 3 раз с экспоненциальной задержкой',
        'После 3 неудач: перемести в dead-letter очередь "notifications.dlq"',
        'HTTP GET /stats возвращает статистику: обработано, ошибок, в очереди',
        'Используй manual acknowledgment',
        'Логируй каждый шаг обработки'
      ],
      expectedOutput: 'POST /notify -> 202 Accepted {id: "notif-1"}\nWorker: получено уведомление notif-1 для user@mail.ru\nWorker: успешно отправлено notif-1\nPOST /notify (broken) -> 202 Accepted {id: "notif-2"}\nWorker: попытка 1 для notif-2 - ошибка\nWorker: попытка 2 для notif-2 - ошибка\nWorker: попытка 3 для notif-2 - ошибка\nWorker: перемещено в DLQ: notif-2\nGET /stats -> {processed:1, errors:1, dlq:1}',
      hint: 'Для Dead Letter Queue в RabbitMQ: при объявлении основной очереди добавь аргументы x-dead-letter-exchange и x-dead-letter-routing-key. Для retry: храни счётчик попыток в заголовке сообщения x-retry-count.',
      solution: 'package main\n\nimport (\n    "context"\n    "encoding/json"\n    "fmt"\n    "log"\n    "net/http"\n    "sync/atomic"\n    "time"\n\n    amqp "github.com/rabbitmq/amqp091-go"\n)\n\nconst (\n    mainQueue = "notifications"\n    dlqName   = "notifications.dlq"\n    maxRetry  = 3\n)\n\ntype Notification struct {\n    ID        string `json:"id"`\n    Type      string `json:"type"`\n    Recipient string `json:"recipient"`\n    Message   string `json:"message"`\n    Attempt   int    `json:"attempt"`\n}\n\nvar (processed int64; errors int64; dlqCount int64)\n\nfunc setupQueues(ch *amqp.Channel) error {\n    // DLQ — простая очередь без TTL\n    _, err := ch.QueueDeclare(dlqName, true, false, false, false, nil)\n    if err != nil { return err }\n\n    // Основная очередь с dead-letter routing\n    _, err = ch.QueueDeclare(mainQueue, true, false, false, false,\n        amqp.Table{\n            "x-dead-letter-exchange":    "",\n            "x-dead-letter-routing-key": dlqName,\n        },\n    )\n    return err\n}\n\nfunc publishNotification(ch *amqp.Channel, notif Notification) error {\n    body, _ := json.Marshal(notif)\n    return ch.PublishWithContext(context.Background(), "", mainQueue, false, false,\n        amqp.Publishing{\n            ContentType:  "application/json",\n            DeliveryMode: amqp.Persistent,\n            Body:         body,\n            MessageId:    notif.ID,\n        },\n    )\n}\n\nfunc sendNotification(notif Notification) error {\n    // Имитируем: ID с "broken" всегда fails\n    if notif.Type == "broken" {\n        return fmt.Errorf("сервис уведомлений недоступен")\n    }\n    log.Printf("Отправлено уведомление %s -> %s: %s", notif.ID, notif.Recipient, notif.Message)\n    return nil\n}\n\nfunc startWorker(conn *amqp.Connection) {\n    ch, _ := conn.Channel()\n    defer ch.Close()\n    ch.Qos(1, 0, false)\n    setupQueues(ch)\n\n    msgs, _ := ch.Consume(mainQueue, "", false, false, false, false, nil)\n    log.Println("Worker запущен...")\n\n    for msg := range msgs {\n        var notif Notification\n        json.Unmarshal(msg.Body, &notif)\n        log.Printf("Получено: %s (попытка %d)", notif.ID, notif.Attempt+1)\n\n        err := sendNotification(notif)\n        if err == nil {\n            atomic.AddInt64(&processed, 1)\n            msg.Ack(false)\n            continue\n        }\n\n        log.Printf("Ошибка для %s: %v", notif.ID, err)\n        atomic.AddInt64(&errors, 1)\n\n        notif.Attempt++\n        if notif.Attempt < maxRetry {\n            // Retry с задержкой\n            delay := time.Duration(notif.Attempt) * 500 * time.Millisecond\n            log.Printf("Retry %d/%d для %s через %v", notif.Attempt, maxRetry, notif.ID, delay)\n            time.Sleep(delay)\n            body, _ := json.Marshal(notif)\n            ch.PublishWithContext(context.Background(), "", mainQueue, false, false,\n                amqp.Publishing{ContentType: "application/json", DeliveryMode: amqp.Persistent, Body: body})\n            msg.Ack(false)\n        } else {\n            // В DLQ\n            log.Printf("Перемещение в DLQ: %s", notif.ID)\n            atomic.AddInt64(&dlqCount, 1)\n            msg.Nack(false, false) // requeue=false -> уйдёт в DLQ\n        }\n    }\n}\n\nfunc main() {\n    conn, _ := amqp.Dial("amqp://guest:guest@localhost:5672/")\n    defer conn.Close()\n    ch, _ := conn.Channel()\n    defer ch.Close()\n    setupQueues(ch)\n\n    go startWorker(conn)\n\n    notifID := 0\n    http.HandleFunc("/notify", func(w http.ResponseWriter, r *http.Request) {\n        if r.Method != http.MethodPost {\n            http.Error(w, "только POST", 405)\n            return\n        }\n        var req struct { Type, Recipient, Message string }\n        json.NewDecoder(r.Body).Decode(&req)\n        notifID++\n        notif := Notification{ID: fmt.Sprintf("notif-%d", notifID),\n            Type: req.Type, Recipient: req.Recipient, Message: req.Message}\n        publishNotification(ch, notif)\n        w.WriteHeader(http.StatusAccepted)\n        json.NewEncoder(w).Encode(map[string]string{"id": notif.ID})\n    })\n\n    http.HandleFunc("/stats", func(w http.ResponseWriter, r *http.Request) {\n        json.NewEncoder(w).Encode(map[string]int64{\n            "processed": atomic.LoadInt64(&processed),\n            "errors":    atomic.LoadInt64(&errors),\n            "dlq":       atomic.LoadInt64(&dlqCount),\n        })\n    })\n\n    log.Println("API на :8080")\n    http.ListenAndServe(":8080", nil)\n}',
      explanation: 'Паттерн dead-letter queue: при Nack(false, false) RabbitMQ перемещает сообщение в DLQ (настраивается через x-dead-letter-routing-key). Retry с экспоненциальной задержкой предотвращает шторм повторных попыток при временных сбоях. Manual acknowledgment (auto-ack: false) гарантирует, что сообщение не потеряется при падении воркера — RabbitMQ вернёт его другому consumer. atomic операции для счётчиков защищают от гонок при параллельных запросах.'
    }
  ]
}
