export default {
  id: 84,
  title: 'Паттерн Factory',
  description: 'Паттерн Фабрика в Go во всех вариантах: простая фабрика, фабричная функция, реестр (registry), система плагинов и абстрактная фабрика через интерфейсы.',
  lessons: [
    {
      id: 1,
      title: 'Простая фабрика: централизованное создание объектов',
      type: 'theory',
      content: [
        { type: 'text', value: 'Простая фабрика — функция или метод, скрывающий детали создания объектов. Клиент запрашивает объект по типу, не зная о конкретных реализациях.' },
        { type: 'heading', value: 'Фабрика уведомлений' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "errors"\n    "fmt"\n)\n\n// Notifier — интерфейс уведомлений\ntype Notifier interface {\n    Send(to, message string) error\n    Type() string\n}\n\n// Конкретные реализации\ntype EmailNotifier struct{}\n\nfunc (e *EmailNotifier) Send(to, message string) error {\n    fmt.Printf("[EMAIL] -> %s: %s\\n", to, message)\n    return nil\n}\nfunc (e *EmailNotifier) Type() string { return "email" }\n\ntype SMSNotifier struct{}\n\nfunc (s *SMSNotifier) Send(to, message string) error {\n    fmt.Printf("[SMS] -> %s: %s\\n", to, message)\n    return nil\n}\nfunc (s *SMSNotifier) Type() string { return "sms" }\n\ntype PushNotifier struct{ appID string }\n\nfunc (p *PushNotifier) Send(to, message string) error {\n    fmt.Printf("[PUSH:%s] -> %s: %s\\n", p.appID, to, message)\n    return nil\n}\nfunc (p *PushNotifier) Type() string { return "push" }\n\n// NewNotifier — простая фабрика\nfunc NewNotifier(notifType string) (Notifier, error) {\n    switch notifType {\n    case "email":\n        return &EmailNotifier{}, nil\n    case "sms":\n        return &SMSNotifier{}, nil\n    case "push":\n        return &PushNotifier{appID: "com.example.app"}, nil\n    default:\n        return nil, fmt.Errorf("неизвестный тип уведомления: %q", notifType)\n    }\n}\n\n// NotificationService использует фабрику\ntype NotificationService struct{}\n\nfunc (ns *NotificationService) Notify(notifType, to, message string) error {\n    n, err := NewNotifier(notifType)\n    if err != nil {\n        return err\n    }\n    return n.Send(to, message)\n}\n\nfunc main() {\n    svc := &NotificationService{}\n\n    svc.Notify("email", "user@example.com", "Ваш заказ принят")\n    svc.Notify("sms", "+7 777 123 4567", "Код подтверждения: 1234")\n    svc.Notify("push", "device-token-abc", "Новое сообщение")\n\n    _, err := NewNotifier("telegram")\n    fmt.Println("Ошибка:", err)\n\n    // Все типы уведомлений без знания конкретных структур\n    types := []string{"email", "sms", "push"}\n    for _, t := range types {\n        n, _ := NewNotifier(t)\n        fmt.Printf("Тип %q реализован: %T\\n", t, n)\n    }\n\n    // Проверка ошибки типа\n    var target *errors.errorString\n    _ = target\n    _ = errors.New("test")\n}' },
        { type: 'note', value: 'Простая фабрика нарушает Open/Closed principle: добавление нового типа требует изменения switch. Registry pattern решает эту проблему.' }
      ]
    },
    {
      id: 2,
      title: 'Фабричная функция (Factory Function)',
      type: 'theory',
      content: [
        { type: 'text', value: 'В Go конструктор — это просто функция. Фабричная функция — функция, возвращающая интерфейс вместо конкретного типа. Это основной идиоматичный способ создания объектов в Go.' },
        { type: 'heading', value: 'Factory Functions — Go-идиома' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "io"\n    "strings"\n)\n\n// Logger — интерфейс\ntype Logger interface {\n    Info(msg string)\n    Error(msg string)\n    Debug(msg string)\n}\n\n// Конкретные реализации\ntype consoleLogger struct {\n    prefix string\n    level  string\n}\n\nfunc (l *consoleLogger) Info(msg string) {\n    if l.level == "debug" || l.level == "info" {\n        fmt.Printf("%s [INFO] %s\\n", l.prefix, msg)\n    }\n}\n\nfunc (l *consoleLogger) Error(msg string) {\n    fmt.Printf("%s [ERROR] %s\\n", l.prefix, msg)\n}\n\nfunc (l *consoleLogger) Debug(msg string) {\n    if l.level == "debug" {\n        fmt.Printf("%s [DEBUG] %s\\n", l.prefix, msg)\n    }\n}\n\ntype writerLogger struct {\n    w      io.Writer\n    prefix string\n}\n\nfunc (l *writerLogger) Info(msg string) {\n    fmt.Fprintf(l.w, "%s [INFO] %s\\n", l.prefix, msg)\n}\nfunc (l *writerLogger) Error(msg string) {\n    fmt.Fprintf(l.w, "%s [ERROR] %s\\n", l.prefix, msg)\n}\nfunc (l *writerLogger) Debug(msg string) {\n    fmt.Fprintf(l.w, "%s [DEBUG] %s\\n", l.prefix, msg)\n}\n\ntype noopLogger struct{}\n\nfunc (l *noopLogger) Info(msg string)  {}\nfunc (l *noopLogger) Error(msg string) {}\nfunc (l *noopLogger) Debug(msg string) {}\n\n// Фабричные функции — возвращают Logger, скрывают детали\nfunc NewConsoleLogger(prefix, level string) Logger {\n    return &consoleLogger{prefix: prefix, level: level}\n}\n\nfunc NewWriterLogger(w io.Writer, prefix string) Logger {\n    return &writerLogger{w: w, prefix: prefix}\n}\n\nfunc NewNopLogger() Logger {\n    return &noopLogger{}\n}\n\n// NewLogger — фабрика по строковому конфигу\nfunc NewLogger(cfg map[string]string) Logger {\n    logType := cfg["type"]\n    prefix := cfg["prefix"]\n    if prefix == "" {\n        prefix = \"APP\"\n    }\n\n    switch logType {\n    case "console":\n        level := cfg["level"]\n        if level == "" {\n            level = "info"\n        }\n        return NewConsoleLogger(prefix, level)\n    case "nop", "noop":\n        return NewNopLogger()\n    default:\n        return NewConsoleLogger(prefix, "info")\n    }\n}\n\nfunc main() {\n    // Прямые фабричные функции\n    consoleLog := NewConsoleLogger("SERVER", "debug")\n    consoleLog.Info("Сервер запущен")\n    consoleLog.Debug("Порт: 8080")\n\n    // Logger в strings.Builder для тестов\n    var buf strings.Builder\n    testLog := NewWriterLogger(&buf, "TEST")\n    testLog.Info("Тестовое сообщение")\n    testLog.Error("Тестовая ошибка")\n    fmt.Print("Захваченные логи:\\n" + buf.String())\n\n    // Nop logger для отключения логирования\n    nop := NewNopLogger()\n    nop.Info("это никуда не запишется")\n\n    // Конфиг-driven фабрика\n    cfg1 := map[string]string{"type": "console", "prefix": "API", "level": "info"}\n    log1 := NewLogger(cfg1)\n    log1.Info("Инициализация API")\n    log1.Debug("Debug в info-режиме — не выведется")\n}' },
        { type: 'tip', value: 'NewNopLogger() — нулевой логгер, полезен в тестах и как значение по умолчанию. Это идиома "Null Object" через фабрику.' }
      ]
    },
    {
      id: 3,
      title: 'Registry Pattern — реестр фабрик',
      type: 'theory',
      content: [
        { type: 'text', value: 'Registry (реестр) хранит фабричные функции, индексированные по ключу. Новые типы регистрируются без изменения существующего кода — это реализует Open/Closed Principle.' },
        { type: 'heading', value: 'Реестр хранилищ' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "sync"\n)\n\n// Storage — интерфейс хранилища\ntype Storage interface {\n    Get(key string) (string, bool)\n    Set(key, value string)\n    Name() string\n}\n\n// Фабричная функция\ntype StorageFactory func(config map[string]string) (Storage, error)\n\n// Registry — глобальный реестр\ntype StorageRegistry struct {\n    mu        sync.RWMutex\n    factories map[string]StorageFactory\n}\n\nvar globalRegistry = &StorageRegistry{\n    factories: make(map[string]StorageFactory),\n}\n\n// Register добавляет фабрику в реестр\nfunc Register(name string, factory StorageFactory) {\n    globalRegistry.mu.Lock()\n    defer globalRegistry.mu.Unlock()\n    globalRegistry.factories[name] = factory\n    fmt.Printf("[Registry] Зарегистрировано хранилище: %q\\n", name)\n}\n\n// Create создаёт хранилище по имени\nfunc Create(name string, config map[string]string) (Storage, error) {\n    globalRegistry.mu.RLock()\n    factory, ok := globalRegistry.factories[name]\n    globalRegistry.mu.RUnlock()\n    if !ok {\n        return nil, fmt.Errorf("хранилище %q не зарегистрировано", name)\n    }\n    return factory(config)\n}\n\n// Concrete implementations\ntype MemoryStorage struct {\n    data map[string]string\n}\n\nfunc (m *MemoryStorage) Get(k string) (string, bool) { v, ok := m.data[k]; return v, ok }\nfunc (m *MemoryStorage) Set(k, v string)             { m.data[k] = v }\nfunc (m *MemoryStorage) Name() string                { return "memory" }\n\ntype FileStorage struct {\n    path string\n    data map[string]string\n}\n\nfunc (f *FileStorage) Get(k string) (string, bool) { v, ok := f.data[k]; return v, ok }\nfunc (f *FileStorage) Set(k, v string)             { f.data[k] = v }\nfunc (f *FileStorage) Name() string                { return "file:" + f.path }\n\ntype RedisStorage struct {\n    addr string\n    data map[string]string\n}\n\nfunc (r *RedisStorage) Get(k string) (string, bool) { v, ok := r.data[k]; return v, ok }\nfunc (r *RedisStorage) Set(k, v string)             { r.data[k] = v }\nfunc (r *RedisStorage) Name() string                { return "redis:" + r.addr }\n\n// Регистрация через init()\nfunc init() {\n    Register("memory", func(cfg map[string]string) (Storage, error) {\n        return &MemoryStorage{data: make(map[string]string)}, nil\n    })\n\n    Register("file", func(cfg map[string]string) (Storage, error) {\n        path := cfg["path"]\n        if path == "" {\n            return nil, fmt.Errorf("file storage: path обязателен")\n        }\n        return &FileStorage{path: path, data: make(map[string]string)}, nil\n    })\n\n    Register("redis", func(cfg map[string]string) (Storage, error) {\n        addr := cfg["addr"]\n        if addr == "" {\n            addr = "localhost:6379"\n        }\n        return &RedisStorage{addr: addr, data: make(map[string]string)}, nil\n    })\n}\n\nfunc main() {\n    // Создание из реестра\n    mem, _ := Create("memory", nil)\n    mem.Set("greeting", "Привет")\n    v, _ := mem.Get("greeting")\n    fmt.Printf("%s: %s\\n", mem.Name(), v)\n\n    file, _ := Create("file", map[string]string{"path": "/tmp/cache.db"})\n    file.Set("key", "value")\n    fmt.Printf("%s работает\\n", file.Name())\n\n    redis, _ := Create("redis", map[string]string{"addr": "redis:6379"})\n    fmt.Printf("%s работает\\n", redis.Name())\n\n    // Неизвестный тип\n    _, err := Create("postgres", nil)\n    fmt.Println("Ошибка:", err)\n}' },
        { type: 'note', value: 'Функция init() в Go вызывается автоматически при инициализации пакета. Это стандартный способ саморегистрации: каждый пакет с реализацией регистрирует себя через init().' }
      ]
    },
    {
      id: 4,
      title: 'Система плагинов через фабрику',
      type: 'theory',
      content: [
        { type: 'text', value: 'Система плагинов — продвинутое применение Registry Pattern. Плагины регистрируют себя, и приложение загружает их динамически по конфигурации.' },
        { type: 'heading', value: 'Extensible Plugin System' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "strings"\n)\n\n// Plugin — интерфейс плагина\ntype Plugin interface {\n    Name() string\n    Version() string\n    Execute(input string) (string, error)\n}\n\n// PluginMeta — метаданные плагина\ntype PluginMeta struct {\n    Name        string\n    Version     string\n    Description string\n    Factory     func(config map[string]string) (Plugin, error)\n}\n\n// PluginRegistry — реестр плагинов\ntype PluginRegistry struct {\n    plugins map[string]PluginMeta\n}\n\nfunc NewPluginRegistry() *PluginRegistry {\n    return &PluginRegistry{plugins: make(map[string]PluginMeta)}\n}\n\nfunc (r *PluginRegistry) Register(meta PluginMeta) {\n    r.plugins[meta.Name] = meta\n}\n\nfunc (r *PluginRegistry) Create(name string, config map[string]string) (Plugin, error) {\n    meta, ok := r.plugins[name]\n    if !ok {\n        return nil, fmt.Errorf("плагин %q не найден", name)\n    }\n    return meta.Factory(config)\n}\n\nfunc (r *PluginRegistry) List() []PluginMeta {\n    list := make([]PluginMeta, 0, len(r.plugins))\n    for _, m := range r.plugins {\n        list = append(list, m)\n    }\n    return list\n}\n\n// Конкретные плагины\ntype UpperCasePlugin struct{ name, version string }\n\nfunc (p *UpperCasePlugin) Name() string    { return p.name }\nfunc (p *UpperCasePlugin) Version() string { return p.version }\nfunc (p *UpperCasePlugin) Execute(input string) (string, error) {\n    return strings.ToUpper(input), nil\n}\n\ntype ReversePlugin struct{ name, version string }\n\nfunc (p *ReversePlugin) Name() string    { return p.name }\nfunc (p *ReversePlugin) Version() string { return p.version }\nfunc (p *ReversePlugin) Execute(input string) (string, error) {\n    runes := []rune(input)\n    for i, j := 0, len(runes)-1; i < j; i, j = i+1, j-1 {\n        runes[i], runes[j] = runes[j], runes[i]\n    }\n    return string(runes), nil\n}\n\ntype TrimPlugin struct {\n    name, version string\n    cutset        string\n}\n\nfunc (p *TrimPlugin) Name() string    { return p.name }\nfunc (p *TrimPlugin) Version() string { return p.version }\nfunc (p *TrimPlugin) Execute(input string) (string, error) {\n    return strings.Trim(input, p.cutset), nil\n}\n\n// Pipeline применяет плагины последовательно\ntype Pipeline struct {\n    plugins []Plugin\n}\n\nfunc (pp *Pipeline) Add(p Plugin) { pp.plugins = append(pp.plugins, p) }\n\nfunc (pp *Pipeline) Process(input string) (string, error) {\n    result := input\n    for _, p := range pp.plugins {\n        var err error\n        result, err = p.Execute(result)\n        if err != nil {\n            return "", fmt.Errorf("плагин %s: %w", p.Name(), err)\n        }\n    }\n    return result, nil\n}\n\nfunc main() {\n    registry := NewPluginRegistry()\n\n    registry.Register(PluginMeta{\n        Name: "uppercase", Version: "1.0", Description: "Преобразует в верхний регистр",\n        Factory: func(cfg map[string]string) (Plugin, error) {\n            return &UpperCasePlugin{name: "uppercase", version: "1.0"}, nil\n        },\n    })\n\n    registry.Register(PluginMeta{\n        Name: "reverse", Version: "1.1", Description: "Переворачивает строку",\n        Factory: func(cfg map[string]string) (Plugin, error) {\n            return &ReversePlugin{name: "reverse", version: "1.1"}, nil\n        },\n    })\n\n    registry.Register(PluginMeta{\n        Name: "trim", Version: "1.0", Description: "Обрезает символы",\n        Factory: func(cfg map[string]string) (Plugin, error) {\n            cutset := cfg["cutset"]\n            if cutset == "" {\n                cutset = " "\n            }\n            return &TrimPlugin{name: "trim", version: "1.0", cutset: cutset}, nil\n        },\n    })\n\n    fmt.Println("Зарегистрированные плагины:")\n    for _, m := range registry.List() {\n        fmt.Printf("  - %s v%s: %s\\n", m.Name, m.Version, m.Description)\n    }\n\n    // Создаём пайплайн из конфигурации\n    pipeline := &Pipeline{}\n    for _, name := range []string{"trim", "uppercase", "reverse"} {\n        p, err := registry.Create(name, map[string]string{"cutset": "  "})\n        if err != nil {\n            fmt.Println("Ошибка:", err)\n            continue\n        }\n        pipeline.Add(p)\n    }\n\n    result, _ := pipeline.Process("  hello world  ")\n    fmt.Printf("Результат pipeline: %q\\n", result)\n}' },
        { type: 'tip', value: 'Pipeline + Registry — мощная комбинация. Конфигурация задаёт список плагинов, реестр их создаёт, пайплайн применяет последовательно. Это основа ETL-систем и обработчиков данных.' }
      ]
    },
    {
      id: 5,
      title: 'Абстрактная фабрика через интерфейсы',
      type: 'theory',
      content: [
        { type: 'text', value: 'Абстрактная фабрика создаёт семейства связанных объектов. В Go это реализуется через интерфейс фабрики, возвращающий интерфейсы продуктов.' },
        { type: 'heading', value: 'UI Abstract Factory' },
        { type: 'code', language: 'go', value: 'package main\n\nimport "fmt"\n\n// Абстрактные продукты\ntype Button interface {\n    Render() string\n    OnClick() string\n}\n\ntype Input interface {\n    Render() string\n    Placeholder() string\n}\n\ntype Dialog interface {\n    Render() string\n}\n\n// Абстрактная фабрика\ntype UIFactory interface {\n    CreateButton(label string) Button\n    CreateInput(placeholder string) Input\n    CreateDialog(title string) Dialog\n}\n\n// --- Web (HTML) фабрика ---\ntype WebButton struct{ label string }\n\nfunc (b *WebButton) Render() string   { return fmt.Sprintf("<button>%s</button>", b.label) }\nfunc (b *WebButton) OnClick() string  { return fmt.Sprintf("button_%s_click()", b.label) }\n\ntype WebInput struct{ placeholder string }\n\nfunc (i *WebInput) Render() string       { return fmt.Sprintf(`<input placeholder=%q>`, i.placeholder) }\nfunc (i *WebInput) Placeholder() string  { return i.placeholder }\n\ntype WebDialog struct{ title string }\n\nfunc (d *WebDialog) Render() string { return fmt.Sprintf("<dialog><h2>%s</h2></dialog>", d.title) }\n\ntype WebUIFactory struct{}\n\nfunc (f *WebUIFactory) CreateButton(label string) Button      { return &WebButton{label: label} }\nfunc (f *WebUIFactory) CreateInput(placeholder string) Input  { return &WebInput{placeholder: placeholder} }\nfunc (f *WebUIFactory) CreateDialog(title string) Dialog      { return &WebDialog{title: title} }\n\n// --- Mobile фабрика ---\ntype MobileButton struct{ label string }\n\nfunc (b *MobileButton) Render() string  { return fmt.Sprintf("[BTN: %s]", b.label) }\nfunc (b *MobileButton) OnClick() string { return fmt.Sprintf("touchEvent(%s)", b.label) }\n\ntype MobileInput struct{ placeholder string }\n\nfunc (i *MobileInput) Render() string      { return fmt.Sprintf("[INPUT: %s]", i.placeholder) }\nfunc (i *MobileInput) Placeholder() string { return i.placeholder }\n\ntype MobileDialog struct{ title string }\n\nfunc (d *MobileDialog) Render() string { return fmt.Sprintf("[MODAL: %s]", d.title) }\n\ntype MobileUIFactory struct{}\n\nfunc (f *MobileUIFactory) CreateButton(label string) Button      { return &MobileButton{label: label} }\nfunc (f *MobileUIFactory) CreateInput(placeholder string) Input  { return &MobileInput{placeholder: placeholder} }\nfunc (f *MobileUIFactory) CreateDialog(title string) Dialog      { return &MobileDialog{title: title} }\n\n// GetUIFactory — абстрактная фабрика по платформе\nfunc GetUIFactory(platform string) UIFactory {\n    switch platform {\n    case "mobile":\n        return &MobileUIFactory{}\n    default:\n        return &WebUIFactory{}\n    }\n}\n\n// renderLoginForm использует только абстрактный UIFactory\nfunc renderLoginForm(factory UIFactory) {\n    emailInput := factory.CreateInput("Email")\n    passInput := factory.CreateInput("Пароль")\n    submitBtn := factory.CreateButton("Войти")\n    dialog := factory.CreateDialog("Вход в систему")\n\n    fmt.Println(dialog.Render())\n    fmt.Println(emailInput.Render())\n    fmt.Println(passInput.Render())\n    fmt.Println(submitBtn.Render())\n    fmt.Printf("Обработчик кнопки: %s\\n", submitBtn.OnClick())\n}\n\nfunc main() {\n    fmt.Println("=== Web версия ===")\n    renderLoginForm(GetUIFactory("web"))\n\n    fmt.Println("\\n=== Mobile версия ===")\n    renderLoginForm(GetUIFactory("mobile"))\n}' },
        { type: 'note', value: 'renderLoginForm принимает UIFactory и не знает о конкретных платформах. Добавление новой платформы (Desktop, TV) не требует изменения renderLoginForm — только новой реализации UIFactory.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: фабрика обработчиков платежей',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй реестр фабрик платёжных процессоров. Поддержи Stripe, PayPal и Crypto. Каждый процессор создаётся по конфигурации через Registry Pattern.',
      requirements: [
        'Интерфейс PaymentProcessor: Charge(amount float64, currency, description string) (string, error), Refund(transactionID string) error, Name() string',
        'Три реализации: StripeProcessor (apiKey), PayPalProcessor (clientID, secret), CryptoProcessor (wallet, coin)',
        'PaymentRegistry с Register(name, factory) и Create(name, config) методами',
        'Каждый Charge возвращает строку-транзакцию вида "txn_<name>_<timestamp>", Refund печатает подтверждение',
        'В main зарегистрировать все три, создать по config, сделать Charge и Refund',
        'При попытке создать незарегистрированный процессор — вернуть ошибку'
      ],
      expectedOutput: 'Stripe charge: txn_stripe_..., 100.00 USD\nPayPal charge: txn_paypal_..., 50.00 EUR\nCrypto charge: txn_crypto_..., 0.001 BTC\nStripe refund: txn_stripe_... подтверждён\nОшибка: процессор "cash" не зарегистрирован',
      hint: 'Для генерации ID транзакции используй fmt.Sprintf("txn_%s_%d", name, time.Now().UnixNano()). Config — map[string]string с ключами типа "api_key", "client_id" и т.д.',
      solution: 'package main\n\nimport (\n    "fmt"\n    "time"\n)\n\ntype PaymentProcessor interface {\n    Charge(amount float64, currency, description string) (string, error)\n    Refund(transactionID string) error\n    Name() string\n}\n\ntype ProcessorFactory func(config map[string]string) (PaymentProcessor, error)\n\ntype PaymentRegistry struct {\n    factories map[string]ProcessorFactory\n}\n\nfunc NewPaymentRegistry() *PaymentRegistry {\n    return &PaymentRegistry{factories: make(map[string]ProcessorFactory)}\n}\n\nfunc (r *PaymentRegistry) Register(name string, f ProcessorFactory) {\n    r.factories[name] = f\n    fmt.Printf("[Registry] Зарегистрирован: %s\\n", name)\n}\n\nfunc (r *PaymentRegistry) Create(name string, cfg map[string]string) (PaymentProcessor, error) {\n    f, ok := r.factories[name]\n    if !ok {\n        return nil, fmt.Errorf("процессор %q не зарегистрирован", name)\n    }\n    return f(cfg)\n}\n\n// Stripe\ntype StripeProcessor struct{ apiKey string }\n\nfunc (p *StripeProcessor) Name() string { return "stripe" }\nfunc (p *StripeProcessor) Charge(amount float64, currency, desc string) (string, error) {\n    txn := fmt.Sprintf("txn_stripe_%d", time.Now().UnixNano())\n    fmt.Printf("Stripe charge: %s, %.2f %s (%s)\\n", txn, amount, currency, desc)\n    return txn, nil\n}\nfunc (p *StripeProcessor) Refund(txnID string) error {\n    fmt.Printf("Stripe refund: %s подтверждён\\n", txnID)\n    return nil\n}\n\n// PayPal\ntype PayPalProcessor struct{ clientID, secret string }\n\nfunc (p *PayPalProcessor) Name() string { return "paypal" }\nfunc (p *PayPalProcessor) Charge(amount float64, currency, desc string) (string, error) {\n    txn := fmt.Sprintf("txn_paypal_%d", time.Now().UnixNano())\n    fmt.Printf("PayPal charge: %s, %.2f %s (%s)\\n", txn, amount, currency, desc)\n    return txn, nil\n}\nfunc (p *PayPalProcessor) Refund(txnID string) error {\n    fmt.Printf("PayPal refund: %s подтверждён\\n", txnID)\n    return nil\n}\n\n// Crypto\ntype CryptoProcessor struct{ wallet, coin string }\n\nfunc (p *CryptoProcessor) Name() string { return "crypto" }\nfunc (p *CryptoProcessor) Charge(amount float64, currency, desc string) (string, error) {\n    txn := fmt.Sprintf("txn_crypto_%d", time.Now().UnixNano())\n    fmt.Printf("Crypto charge: %s, %g %s (%s)\\n", txn, amount, currency, desc)\n    return txn, nil\n}\nfunc (p *CryptoProcessor) Refund(txnID string) error {\n    fmt.Printf("Crypto refund: %s - вернуть вручную на кошелёк %s\\n", txnID, p.wallet)\n    return nil\n}\n\nfunc main() {\n    registry := NewPaymentRegistry()\n\n    registry.Register("stripe", func(cfg map[string]string) (PaymentProcessor, error) {\n        key := cfg["api_key"]\n        if key == "" {\n            return nil, fmt.Errorf("stripe: api_key обязателен")\n        }\n        return &StripeProcessor{apiKey: key}, nil\n    })\n\n    registry.Register("paypal", func(cfg map[string]string) (PaymentProcessor, error) {\n        return &PayPalProcessor{clientID: cfg["client_id"], secret: cfg["secret"]}, nil\n    })\n\n    registry.Register("crypto", func(cfg map[string]string) (PaymentProcessor, error) {\n        return &CryptoProcessor{wallet: cfg["wallet"], coin: cfg["coin"]}, nil\n    })\n\n    stripe, _ := registry.Create("stripe", map[string]string{"api_key": "sk_test_xxx"})\n    paypal, _ := registry.Create("paypal", map[string]string{"client_id": "pp_id", "secret": "pp_secret"})\n    crypto, _ := registry.Create("crypto", map[string]string{"wallet": "0xABC", "coin": "ETH"})\n\n    txn1, _ := stripe.Charge(100.00, "USD", "Заказ #1")\n    paypal.Charge(50.00, "EUR", "Подписка")\n    crypto.Charge(0.001, "BTC", "Оплата криптой")\n    stripe.Refund(txn1)\n\n    _, err := registry.Create("cash", nil)\n    fmt.Println("Ошибка:", err)\n}',
      explanation: 'Registry Pattern решает проблему добавления новых типов без изменения существующего кода. ProcessorFactory — тип функции, которая принимает конфигурацию и создаёт процессор. Регистрация через замыкание позволяет каждой фабрике иметь собственную логику валидации конфига. Это паттерн стратегии на уровне создания объектов.'
    }
  ]
}
