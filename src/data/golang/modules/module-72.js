export default {
  id: 72,
  title: 'Repository паттерн',
  description: 'Паттерн Repository в Go: определение интерфейса, in-memory и PostgreSQL реализации, тестирование с mock-репозиторием. Как абстрагировать доступ к данным от бизнес-логики.',
  lessons: [
    {
      id: 1,
      title: 'Что такое Repository паттерн',
      type: 'theory',
      content: [
        { type: 'text', value: 'Repository — паттерн, который скрывает детали хранения данных за интерфейсом. Бизнес-логика работает с репозиторием как с коллекцией объектов, не зная, где данные хранятся: в PostgreSQL, Redis, памяти или файле.' },
        { type: 'heading', value: 'Проблема без Repository' },
        { type: 'code', language: 'go', value: '// ПЛОХО: бизнес-логика напрямую работает с SQL\nfunc RegisterUser(db *sql.DB, email, name string) error {\n    // SQL прямо в бизнес-логике\n    var count int\n    db.QueryRow("SELECT COUNT(*) FROM users WHERE email=$1", email).Scan(&count)\n    if count > 0 {\n        return errors.New("email занят")\n    }\n    _, err := db.Exec("INSERT INTO users (email, name) VALUES ($1, $2)", email, name)\n    return err\n    // Проблемы:\n    // 1. Нельзя тестировать без реальной БД\n    // 2. Сменить PostgreSQL на MongoDB — переписать всю функцию\n    // 3. Логика смешана с SQL\n}' },
        { type: 'heading', value: 'Решение с Repository' },
        { type: 'code', language: 'go', value: '// ХОРОШО: бизнес-логика работает с интерфейсом\ntype UserRepository interface {\n    Create(ctx context.Context, user *User) error\n    ExistsByEmail(ctx context.Context, email string) (bool, error)\n}\n\nfunc RegisterUser(ctx context.Context, repo UserRepository, email, name string) error {\n    exists, err := repo.ExistsByEmail(ctx, email)\n    if err != nil {\n        return err\n    }\n    if exists {\n        return ErrEmailDuplicate\n    }\n    return repo.Create(ctx, &User{Email: email, Name: name})\n    // Теперь:\n    // 1. Тест: передать MemoryRepo без БД\n    // 2. Сменить БД: создать новую реализацию интерфейса\n    // 3. Логика чистая, без SQL\n}' },
        { type: 'tip', value: 'Repository — это не просто слой поверх БД. Это контракт: "дайте мне объект, который умеет сохранять и находить пользователей". Детали реализации — не ваша забота внутри бизнес-логики.' }
      ]
    },
    {
      id: 2,
      title: 'Определение интерфейса репозитория',
      type: 'theory',
      content: [
        { type: 'text', value: 'Интерфейс репозитория должен отражать потребности бизнес-логики, а не возможности базы данных. Не копируй SQL-методы — думай, какие операции нужны Use Case.' },
        { type: 'heading', value: 'Хороший интерфейс репозитория' },
        { type: 'code', language: 'go', value: 'package usecase\n\nimport (\n    "context"\n    "time"\n)\n\ntype Article struct {\n    ID          int64\n    Title       string\n    Content     string\n    AuthorID    int64\n    PublishedAt *time.Time\n    Tags        []string\n}\n\n// ArticleFilter — параметры поиска статей\ntype ArticleFilter struct {\n    AuthorID  *int64\n    Tag       *string\n    Published *bool\n    Limit     int\n    Offset    int\n}\n\n// ArticleRepository — интерфейс, отражающий нужды бизнес-логики\ntype ArticleRepository interface {\n    // Базовые CRUD\n    Create(ctx context.Context, a *Article) error\n    GetByID(ctx context.Context, id int64) (*Article, error)\n    Update(ctx context.Context, a *Article) error\n    Delete(ctx context.Context, id int64) error\n\n    // Специфичные запросы\n    List(ctx context.Context, filter ArticleFilter) ([]*Article, int, error)\n    GetByAuthor(ctx context.Context, authorID int64) ([]*Article, error)\n    Publish(ctx context.Context, id int64, publishedAt time.Time) error\n\n    // Счётчики\n    CountByAuthor(ctx context.Context, authorID int64) (int64, error)\n}\n\n// Чего НЕ должно быть в интерфейсе:\n// - ExecRaw(query string) - это SQL, не бизнес\n// - Begin() / Commit() - транзакции на уровне репо скрыты\n// - Scan() / Rows() - детали БД-драйвера' },
        { type: 'warning', value: 'Не делай интерфейс слишком широким. Если use case A использует только Create и GetByID — пусть он объявит маленький интерфейс с двумя методами. Большой "универсальный" интерфейс усложняет моки и нарушает ISP.' }
      ]
    },
    {
      id: 3,
      title: 'In-memory реализация',
      type: 'theory',
      content: [
        { type: 'text', value: 'In-memory реализация — первая, которую стоит написать. Она полезна для разработки без БД и для тестов. Должна полностью реализовывать интерфейс.' },
        { type: 'heading', value: 'MemoryArticleRepo' },
        { type: 'code', language: 'go', value: 'package memory\n\nimport (\n    "context"\n    "errors"\n    "sort"\n    "sync"\n    "time"\n\n    "myapp/internal/usecase"\n)\n\nvar ErrNotFound = errors.New("не найдено")\n\n// Compile-time проверка интерфейса\nvar _ usecase.ArticleRepository = (*ArticleRepo)(nil)\n\ntype ArticleRepo struct {\n    mu      sync.RWMutex\n    items   map[int64]*usecase.Article\n    counter int64\n}\n\nfunc New() *ArticleRepo {\n    return &ArticleRepo{items: make(map[int64]*usecase.Article)}\n}\n\nfunc (r *ArticleRepo) Create(ctx context.Context, a *usecase.Article) error {\n    r.mu.Lock()\n    defer r.mu.Unlock()\n    r.counter++\n    a.ID = r.counter\n    // Копируем, чтобы не было общих ссылок\n    cp := *a\n    r.items[a.ID] = &cp\n    return nil\n}\n\nfunc (r *ArticleRepo) GetByID(ctx context.Context, id int64) (*usecase.Article, error) {\n    r.mu.RLock()\n    defer r.mu.RUnlock()\n    a, ok := r.items[id]\n    if !ok {\n        return nil, ErrNotFound\n    }\n    cp := *a\n    return &cp, nil // возвращаем копию\n}\n\nfunc (r *ArticleRepo) Update(ctx context.Context, a *usecase.Article) error {\n    r.mu.Lock()\n    defer r.mu.Unlock()\n    if _, ok := r.items[a.ID]; !ok {\n        return ErrNotFound\n    }\n    cp := *a\n    r.items[a.ID] = &cp\n    return nil\n}\n\nfunc (r *ArticleRepo) Delete(ctx context.Context, id int64) error {\n    r.mu.Lock()\n    defer r.mu.Unlock()\n    delete(r.items, id)\n    return nil\n}\n\nfunc (r *ArticleRepo) List(ctx context.Context, f usecase.ArticleFilter) ([]*usecase.Article, int, error) {\n    r.mu.RLock()\n    defer r.mu.RUnlock()\n    var result []*usecase.Article\n    for _, a := range r.items {\n        if f.AuthorID != nil && a.AuthorID != *f.AuthorID {\n            continue\n        }\n        cp := *a\n        result = append(result, &cp)\n    }\n    sort.Slice(result, func(i, j int) bool { return result[i].ID < result[j].ID })\n    total := len(result)\n    if f.Offset < total {\n        result = result[f.Offset:]\n    }\n    if f.Limit > 0 && len(result) > f.Limit {\n        result = result[:f.Limit]\n    }\n    return result, total, nil\n}\n\nfunc (r *ArticleRepo) GetByAuthor(ctx context.Context, authorID int64) ([]*usecase.Article, error) {\n    f := usecase.ArticleFilter{AuthorID: &authorID}\n    items, _, err := r.List(ctx, f)\n    return items, err\n}\n\nfunc (r *ArticleRepo) Publish(ctx context.Context, id int64, at time.Time) error {\n    r.mu.Lock()\n    defer r.mu.Unlock()\n    a, ok := r.items[id]\n    if !ok {\n        return ErrNotFound\n    }\n    a.PublishedAt = &at\n    return nil\n}\n\nfunc (r *ArticleRepo) CountByAuthor(ctx context.Context, authorID int64) (int64, error) {\n    r.mu.RLock()\n    defer r.mu.RUnlock()\n    var count int64\n    for _, a := range r.items {\n        if a.AuthorID == authorID {\n            count++\n        }\n    }\n    return count, nil\n}' },
        { type: 'tip', value: 'Всегда возвращай копию объекта из in-memory репозитория (cp := *a; return &cp). Иначе вызывающий код сможет изменить внутреннее состояние хранилища, не вызывая Update.' }
      ]
    },
    {
      id: 4,
      title: 'PostgreSQL реализация',
      type: 'theory',
      content: [
        { type: 'text', value: 'PostgreSQL реализация использует database/sql или sqlx. Важно: она реализует тот же интерфейс, что и in-memory. Бизнес-логика не изменится при замене одной реализации на другую.' },
        { type: 'heading', value: 'PostgresArticleRepo с sqlx' },
        { type: 'code', language: 'go', value: 'package postgres\n\nimport (\n    "context"\n    "database/sql"\n    "errors"\n    "fmt"\n\n    "github.com/jmoiron/sqlx"\n\n    "myapp/internal/usecase"\n)\n\nvar _ usecase.ArticleRepository = (*ArticleRepo)(nil)\n\ntype ArticleRepo struct {\n    db *sqlx.DB\n}\n\nfunc New(db *sqlx.DB) *ArticleRepo {\n    return &ArticleRepo{db: db}\n}\n\ntype articleRow struct {\n    ID       int64  `db:"id"`\n    Title    string `db:"title"`\n    Content  string `db:"content"`\n    AuthorID int64  `db:"author_id"`\n}\n\nfunc (r *ArticleRepo) Create(ctx context.Context, a *usecase.Article) error {\n    query := `INSERT INTO articles (title, content, author_id)\n              VALUES (:title, :content, :author_id)\n              RETURNING id`\n    row := articleRow{Title: a.Title, Content: a.Content, AuthorID: a.AuthorID}\n    rows, err := r.db.NamedQueryContext(ctx, query, row)\n    if err != nil {\n        return fmt.Errorf("ArticleRepo.Create: %w", err)\n    }\n    defer rows.Close()\n    if rows.Next() {\n        rows.Scan(&a.ID)\n    }\n    return nil\n}\n\nfunc (r *ArticleRepo) GetByID(ctx context.Context, id int64) (*usecase.Article, error) {\n    var row articleRow\n    err := r.db.GetContext(ctx, &row,\n        `SELECT id, title, content, author_id FROM articles WHERE id = $1`, id)\n    if errors.Is(err, sql.ErrNoRows) {\n        return nil, usecase.ErrArticleNotFound\n    }\n    if err != nil {\n        return nil, fmt.Errorf("ArticleRepo.GetByID: %w", err)\n    }\n    return &usecase.Article{\n        ID: row.ID, Title: row.Title,\n        Content: row.Content, AuthorID: row.AuthorID,\n    }, nil\n}\n\nfunc (r *ArticleRepo) List(ctx context.Context, f usecase.ArticleFilter) ([]*usecase.Article, int, error) {\n    query := `SELECT id, title, content, author_id FROM articles WHERE 1=1`\n    args := []interface{}{}\n    argN := 1\n\n    if f.AuthorID != nil {\n        query += fmt.Sprintf(" AND author_id = $%d", argN)\n        args = append(args, *f.AuthorID)\n        argN++\n    }\n\n    var total int\n    countQuery := "SELECT COUNT(*) FROM articles WHERE 1=1"\n    r.db.QueryRowContext(ctx, countQuery).Scan(&total)\n\n    query += fmt.Sprintf(" ORDER BY id LIMIT $%d OFFSET $%d", argN, argN+1)\n    args = append(args, f.Limit, f.Offset)\n\n    var rows []articleRow\n    if err := r.db.SelectContext(ctx, &rows, query, args...); err != nil {\n        return nil, 0, fmt.Errorf("ArticleRepo.List: %w", err)\n    }\n\n    result := make([]*usecase.Article, len(rows))\n    for i, row := range rows {\n        result[i] = &usecase.Article{\n            ID: row.ID, Title: row.Title,\n            Content: row.Content, AuthorID: row.AuthorID,\n        }\n    }\n    return result, total, nil\n}' },
        { type: 'note', value: 'Оборачивай ошибки из БД через fmt.Errorf("ArticleRepo.Create: %w", err). Это добавляет контекст — при логировании ошибки сразу видно, в каком методе репозитория она возникла.' }
      ]
    },
    {
      id: 5,
      title: 'Тестирование с mock-репозиторием',
      type: 'theory',
      content: [
        { type: 'text', value: 'Главная ценность Repository паттерна — тестируемость. Вместо реальной БД используем mock, который возвращает заданные данные или ошибки.' },
        { type: 'heading', value: 'Ручной mock-репозиторий' },
        { type: 'code', language: 'go', value: 'package usecase_test\n\nimport (\n    "context"\n    "errors"\n    "testing"\n\n    "myapp/internal/usecase"\n)\n\n// MockArticleRepo — mock для тестов\ntype MockArticleRepo struct {\n    articles map[int64]*usecase.Article\n    counter  int64\n\n    // Можно настроить ошибки\n    CreateErr  error\n    GetByIDErr error\n}\n\nfunc NewMockRepo() *MockArticleRepo {\n    return &MockArticleRepo{articles: make(map[int64]*usecase.Article)}\n}\n\nfunc (m *MockArticleRepo) Create(ctx context.Context, a *usecase.Article) error {\n    if m.CreateErr != nil {\n        return m.CreateErr\n    }\n    m.counter++\n    a.ID = m.counter\n    cp := *a\n    m.articles[a.ID] = &cp\n    return nil\n}\n\nfunc (m *MockArticleRepo) GetByID(ctx context.Context, id int64) (*usecase.Article, error) {\n    if m.GetByIDErr != nil {\n        return nil, m.GetByIDErr\n    }\n    a, ok := m.articles[id]\n    if !ok {\n        return nil, usecase.ErrArticleNotFound\n    }\n    return a, nil\n}\n\n// Тест Use Case\nfunc TestCreateArticle_Success(t *testing.T) {\n    repo := NewMockRepo()\n    uc := usecase.NewArticleUseCase(repo)\n\n    article, err := uc.Create(context.Background(), "Заголовок", "Контент", 1)\n    if err != nil {\n        t.Fatalf("неожиданная ошибка: %v", err)\n    }\n    if article.ID != 1 {\n        t.Errorf("ожидали ID=1, получили %d", article.ID)\n    }\n    if article.Title != "Заголовок" {\n        t.Errorf("неверный заголовок: %s", article.Title)\n    }\n}\n\nfunc TestCreateArticle_RepoError(t *testing.T) {\n    repo := NewMockRepo()\n    repo.CreateErr = errors.New("БД недоступна") // симулируем ошибку БД\n    uc := usecase.NewArticleUseCase(repo)\n\n    _, err := uc.Create(context.Background(), "Заголовок", "Контент", 1)\n    if err == nil {\n        t.Fatal("ожидали ошибку, но её нет")\n    }\n}\n\nfunc TestGetArticle_NotFound(t *testing.T) {\n    repo := NewMockRepo()\n    uc := usecase.NewArticleUseCase(repo)\n\n    _, err := uc.GetByID(context.Background(), 999)\n    if !errors.Is(err, usecase.ErrArticleNotFound) {\n        t.Errorf("ожидали ErrArticleNotFound, получили: %v", err)\n    }\n}' },
        { type: 'tip', value: 'Для более мощных mock-объектов используй github.com/stretchr/testify/mock или github.com/golang/mock/gomock. Но для простых случаев ручной mock — проще и без зависимостей.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: репозиторий для заметок',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй паттерн Repository для сервиса заметок. Создай интерфейс, in-memory реализацию и напиши тесты через mock.',
      requirements: [
        'Структура Note{ID int64, Title string, Body string, UserID int64}',
        'Интерфейс NoteRepository с методами: Create, GetByID, ListByUser(userID), Delete',
        'In-memory реализация MemoryNoteRepo с потокобезопасностью (sync.RWMutex)',
        'Структура NoteService(repo NoteRepository) с методами: Create(title, body, userID), GetUserNotes(userID)',
        'Тесты для NoteService через MockNoteRepo: TestCreate_Success, TestCreate_EmptyTitle, TestGetUserNotes',
        'Ошибка ErrNoteNotFound и ErrEmptyTitle'
      ],
      expectedOutput: '--- PASS: TestCreate_Success\n--- PASS: TestCreate_EmptyTitle\n--- PASS: TestGetUserNotes\nok  myapp/notes',
      hint: 'MockNoteRepo не нужно полностью имитировать — только те методы, что используются в тестируемых сценариях. Тест TestCreate_EmptyTitle проверяет, что при пустом Title NoteService возвращает ErrEmptyTitle, не вызывая репозиторий.',
      solution: 'package main\n\nimport (\n    "context"\n    "errors"\n    "fmt"\n    "sync"\n    "testing"\n)\n\n// === domain ===\n\ntype Note struct {\n    ID     int64\n    Title  string\n    Body   string\n    UserID int64\n}\n\nvar (\n    ErrNoteNotFound = errors.New("заметка не найдена")\n    ErrEmptyTitle   = errors.New("заголовок не может быть пустым")\n)\n\n// === repository interface ===\n\ntype NoteRepository interface {\n    Create(ctx context.Context, note *Note) error\n    GetByID(ctx context.Context, id int64) (*Note, error)\n    ListByUser(ctx context.Context, userID int64) ([]*Note, error)\n    Delete(ctx context.Context, id int64) error\n}\n\n// === in-memory implementation ===\n\ntype MemoryNoteRepo struct {\n    mu     sync.RWMutex\n    notes  map[int64]*Note\n    nextID int64\n}\n\nfunc NewMemoryNoteRepo() *MemoryNoteRepo {\n    return &MemoryNoteRepo{notes: make(map[int64]*Note)}\n}\n\nfunc (r *MemoryNoteRepo) Create(ctx context.Context, note *Note) error {\n    r.mu.Lock()\n    defer r.mu.Unlock()\n    r.nextID++\n    note.ID = r.nextID\n    cp := *note\n    r.notes[note.ID] = &cp\n    return nil\n}\n\nfunc (r *MemoryNoteRepo) GetByID(ctx context.Context, id int64) (*Note, error) {\n    r.mu.RLock()\n    defer r.mu.RUnlock()\n    n, ok := r.notes[id]\n    if !ok {\n        return nil, ErrNoteNotFound\n    }\n    cp := *n\n    return &cp, nil\n}\n\nfunc (r *MemoryNoteRepo) ListByUser(ctx context.Context, userID int64) ([]*Note, error) {\n    r.mu.RLock()\n    defer r.mu.RUnlock()\n    var result []*Note\n    for _, n := range r.notes {\n        if n.UserID == userID {\n            cp := *n\n            result = append(result, &cp)\n        }\n    }\n    return result, nil\n}\n\nfunc (r *MemoryNoteRepo) Delete(ctx context.Context, id int64) error {\n    r.mu.Lock()\n    defer r.mu.Unlock()\n    delete(r.notes, id)\n    return nil\n}\n\n// === service ===\n\ntype NoteService struct {\n    repo NoteRepository\n}\n\nfunc NewNoteService(repo NoteRepository) *NoteService {\n    return &NoteService{repo: repo}\n}\n\nfunc (s *NoteService) Create(ctx context.Context, title, body string, userID int64) (*Note, error) {\n    if title == "" {\n        return nil, ErrEmptyTitle\n    }\n    note := &Note{Title: title, Body: body, UserID: userID}\n    if err := s.repo.Create(ctx, note); err != nil {\n        return nil, err\n    }\n    return note, nil\n}\n\nfunc (s *NoteService) GetUserNotes(ctx context.Context, userID int64) ([]*Note, error) {\n    return s.repo.ListByUser(ctx, userID)\n}\n\n// === mock for tests ===\n\ntype MockNoteRepo struct {\n    notes  map[int64]*Note\n    nextID int64\n}\n\nfunc NewMockNoteRepo() *MockNoteRepo {\n    return &MockNoteRepo{notes: make(map[int64]*Note)}\n}\n\nfunc (m *MockNoteRepo) Create(ctx context.Context, note *Note) error {\n    m.nextID++\n    note.ID = m.nextID\n    cp := *note\n    m.notes[note.ID] = &cp\n    return nil\n}\n\nfunc (m *MockNoteRepo) GetByID(ctx context.Context, id int64) (*Note, error) {\n    n, ok := m.notes[id]\n    if !ok {\n        return nil, ErrNoteNotFound\n    }\n    return n, nil\n}\n\nfunc (m *MockNoteRepo) ListByUser(ctx context.Context, userID int64) ([]*Note, error) {\n    var result []*Note\n    for _, n := range m.notes {\n        if n.UserID == userID {\n            result = append(result, n)\n        }\n    }\n    return result, nil\n}\n\nfunc (m *MockNoteRepo) Delete(ctx context.Context, id int64) error {\n    delete(m.notes, id)\n    return nil\n}\n\n// === tests ===\n\nfunc TestCreate_Success(t *testing.T) {\n    svc := NewNoteService(NewMockNoteRepo())\n    note, err := svc.Create(context.Background(), "Заметка 1", "Текст", 42)\n    if err != nil {\n        t.Fatalf("ошибка: %v", err)\n    }\n    if note.ID == 0 {\n        t.Error("ID должен быть установлен")\n    }\n    fmt.Println("--- PASS: TestCreate_Success")\n}\n\nfunc TestCreate_EmptyTitle(t *testing.T) {\n    svc := NewNoteService(NewMockNoteRepo())\n    _, err := svc.Create(context.Background(), "", "Текст", 42)\n    if !errors.Is(err, ErrEmptyTitle) {\n        t.Fatalf("ожидали ErrEmptyTitle, получили: %v", err)\n    }\n    fmt.Println("--- PASS: TestCreate_EmptyTitle")\n}\n\nfunc TestGetUserNotes(t *testing.T) {\n    svc := NewNoteService(NewMockNoteRepo())\n    ctx := context.Background()\n    svc.Create(ctx, "Заметка 1", "", 1)\n    svc.Create(ctx, "Заметка 2", "", 1)\n    svc.Create(ctx, "Заметка 3", "", 2) // другой пользователь\n\n    notes, err := svc.GetUserNotes(ctx, 1)\n    if err != nil {\n        t.Fatalf("ошибка: %v", err)\n    }\n    if len(notes) != 2 {\n        t.Errorf("ожидали 2 заметки, получили %d", len(notes))\n    }\n    fmt.Println("--- PASS: TestGetUserNotes")\n}\n\nfunc main() {\n    t := &testing.T{}\n    TestCreate_Success(t)\n    TestCreate_EmptyTitle(t)\n    TestGetUserNotes(t)\n    fmt.Println("ok  myapp/notes")\n}',
      explanation: 'NoteRepository интерфейс определяет контракт хранения. NoteService зависит только от этого интерфейса — не от конкретной реализации. MockNoteRepo в тестах полностью заменяет реальную БД. TestCreate_EmptyTitle проверяет, что валидация работает до вызова репозитория. Это демонстрирует ценность паттерна: тесты быстрые, не требуют инфраструктуры.'
    }
  ]
}
