export default {
  id: 80,
  title: 'Документация и комментарии в Go',
  description: 'Правила документирования Go кода: формат godoc, комментарии к пакетам, функциям и типам, примеры в тестах как документация, написание полезных README файлов.',
  lessons: [
    {
      id: 1,
      title: 'Формат godoc — как работает документация в Go',
      type: 'theory',
      content: [
        { type: 'text', value: 'go doc и pkg.go.dev автоматически генерируют документацию из комментариев в коде. Правило простое: комментарий перед объявлением, начинающийся с имени объявляемого — это документация.' },
        { type: 'heading', value: 'Основные правила godoc' },
        { type: 'code', language: 'go', value: 'package calculator\n\n// Package calculator предоставляет базовые математические операции.\n// Все функции безопасны для конкурентного использования.\n// \n// Пример использования:\n//\n//   result, err := calculator.Divide(10, 2)\n//   if err != nil {\n//       log.Fatal(err)\n//   }\n//   fmt.Println(result) // 5\n\nimport "errors"\n\n// ErrDivisionByZero возвращается когда делитель равен нулю.\nvar ErrDivisionByZero = errors.New("деление на ноль")\n\n// Add возвращает сумму двух целых чисел.\n// Функция не может вернуть ошибку — результат всегда определён.\nfunc Add(a, b int) int {\n    return a + b\n}\n\n// Divide делит a на b и возвращает результат.\n// Возвращает ErrDivisionByZero если b == 0.\nfunc Divide(a, b float64) (float64, error) {\n    if b == 0 {\n        return 0, ErrDivisionByZero\n    }\n    return a / b, nil\n}\n\n// Calculator выполняет математические операции с историей вычислений.\n// Нулевое значение Calculator готово к использованию.\ntype Calculator struct {\n    history []string\n    // mu защищает history при конкурентном доступе\n}\n\n// Add складывает a и b, сохраняет результат в историю.\n// Метод безопасен для конкурентного использования.\nfunc (c *Calculator) Add(a, b int) int {\n    result := a + b\n    c.history = append(c.history, fmt.Sprintf("%d + %d = %d", a, b, result))\n    return result\n}\n\n// History возвращает список всех выполненных вычислений.\n// Возвращает nil если вычислений ещё не было.\nfunc (c *Calculator) History() []string {\n    return c.history\n}' },
        { type: 'tip', value: 'Комментарий начинается с имени объявления: "Divide делит..." — не "Эта функция делит...". Команда go doc calculator.Divide покажет ровно этот комментарий. pkg.go.dev рендерит его с форматированием.' }
      ]
    },
    {
      id: 2,
      title: 'Комментарии к пакету',
      type: 'theory',
      content: [
        { type: 'text', value: 'Каждый пакет должен иметь комментарий к пакету в одном из файлов (обычно в файле с именем пакета или doc.go). Для больших пакетов — отдельный файл doc.go.' },
        { type: 'heading', value: 'Package comment и doc.go' },
        { type: 'code', language: 'go', value: '// Вариант 1: комментарий в основном файле\n// Подходит для маленьких пакетов\n\n// Package httputil предоставляет вспомогательные функции для работы с HTTP.\n// Включает middleware, хелперы для JSON-ответов и парсинг запросов.\npackage httputil\n\n// ---\n\n// Вариант 2: отдельный файл doc.go\n// Подходит для больших пакетов с много файлами\n\n// Package store реализует уровень хранения данных для myapp.\n//\n// Пакет предоставляет следующие хранилища:\n//   - UserStore: управление пользователями\n//   - OrderStore: управление заказами\n//   - ProductStore: управление каталогом\n//\n// Все хранилища поддерживают транзакции через метод WithTx.\n// Для тестирования используйте пакет store/mock.\n//\n// Пример:\n//\n//   db, _ := sql.Open("postgres", dsn)\n//   users := store.NewUserStore(db)\n//   user, err := users.GetByEmail(ctx, "user@example.com")\n//   if errors.Is(err, store.ErrNotFound) {\n//       // пользователь не найден\n//   }\npackage store\n\n// doc.go содержит только пакетный комментарий и объявление пакета.\n// Никакого другого кода в doc.go нет!' },
        { type: 'note', value: 'Если пакет состоит из нескольких файлов, пакетный комментарий пишется только в одном из них (обычно doc.go или файл с именем пакета). В остальных файлах — без комментария к package.' }
      ]
    },
    {
      id: 3,
      title: 'Документирование функций и типов',
      type: 'theory',
      content: [
        { type: 'text', value: 'Документируй все экспортируемые (с заглавной буквы) функции, методы, типы, переменные и константы. Необщедоступный код можно документировать при необходимости.' },
        { type: 'heading', value: 'Что и как документировать' },
        { type: 'code', language: 'go', value: 'package payment\n\nimport (\n    "context"\n    "time"\n)\n\n// PaymentStatus представляет статус платёжной транзакции.\ntype PaymentStatus string\n\n// Возможные статусы платежа.\nconst (\n    // StatusPending — платёж создан, ожидает обработки.\n    StatusPending PaymentStatus = "pending"\n\n    // StatusCompleted — платёж успешно завершён.\n    StatusCompleted PaymentStatus = "completed"\n\n    // StatusFailed — платёж отклонён или отменён.\n    StatusFailed PaymentStatus = "failed"\n)\n\n// Payment представляет одну платёжную транзакцию.\n// Нулевое значение не валидно — используйте NewPayment.\ntype Payment struct {\n    // ID уникальный идентификатор платежа.\n    ID string\n\n    // Amount сумма платежа в копейках (целое число для точности).\n    Amount int64\n\n    // Currency трёхбуквенный код валюты (ISO 4217), например "KZT".\n    Currency string\n\n    // Status текущий статус платежа.\n    Status PaymentStatus\n\n    // CreatedAt время создания платежа в UTC.\n    CreatedAt time.Time\n}\n\n// Service выполняет платёжные операции через внешний шлюз.\n// Методы Service потокобезопасны.\ntype Service struct {\n    gateway Gateway\n    repo    Repository\n}\n\n// NewService создаёт новый Service с указанным шлюзом и репозиторием.\n// Оба аргумента обязательны — паника при nil.\nfunc NewService(gateway Gateway, repo Repository) *Service {\n    if gateway == nil {\n        panic("payment.NewService: gateway не может быть nil")\n    }\n    if repo == nil {\n        panic("payment.NewService: repo не может быть nil")\n    }\n    return &Service{gateway: gateway, repo: repo}\n}\n\n// Process инициирует платёж на указанную сумму.\n//\n// amount должен быть положительным числом в копейках.\n// currency — трёхбуквенный код ISO 4217.\n//\n// Возвращает созданный Payment или ошибку:\n//   - ErrInvalidAmount если amount <= 0\n//   - ErrUnsupportedCurrency если валюта не поддерживается\n//   - ErrGatewayTimeout если шлюз не ответил вовремя\nfunc (s *Service) Process(ctx context.Context, amount int64, currency string) (*Payment, error) {\n    // реализация\n    return nil, nil\n}' }
      ]
    },
    {
      id: 4,
      title: 'Примеры в тестах как документация',
      type: 'theory',
      content: [
        { type: 'text', value: 'Go поддерживает функции-примеры в тестах: они запускаются как тесты (проверяют вывод) и отображаются на pkg.go.dev как документация с работающим кодом.' },
        { type: 'heading', value: 'Example функции' },
        { type: 'code', language: 'go', value: '// calculator_test.go\npackage calculator_test\n\nimport (\n    "fmt"\n    "log"\n\n    "myapp/calculator"\n)\n\n// ExampleAdd демонстрирует сложение двух чисел.\nfunc ExampleAdd() {\n    result := calculator.Add(3, 5)\n    fmt.Println(result)\n    // Output:\n    // 8\n}\n\n// ExampleDivide демонстрирует деление и обработку ошибки.\nfunc ExampleDivide() {\n    result, err := calculator.Divide(10, 2)\n    if err != nil {\n        log.Fatal(err)\n    }\n    fmt.Printf("%.1f\\n", result)\n    // Output:\n    // 5.0\n}\n\n// ExampleDivide_byZero демонстрирует обработку деления на ноль.\n// Суффикс _byZero позволяет иметь несколько примеров для одной функции.\nfunc ExampleDivide_byZero() {\n    _, err := calculator.Divide(5, 0)\n    fmt.Println(err)\n    // Output:\n    // деление на ноль\n}\n\n// ExampleCalculator демонстрирует использование структуры Calculator.\nfunc ExampleCalculator() {\n    var c calculator.Calculator\n    c.Add(3, 4)\n    c.Add(10, 20)\n\n    history := c.History()\n    for _, h := range history {\n        fmt.Println(h)\n    }\n    // Output:\n    // 3 + 4 = 7\n    // 10 + 20 = 30\n}\n\n// ExampleCalculator_history демонстрирует пустую историю.\nfunc ExampleCalculator_history() {\n    var c calculator.Calculator\n    fmt.Println(c.History() == nil)\n    // Output:\n    // true\n}' },
        { type: 'tip', value: 'Если Example функция имеет комментарий "// Output:", то go test запускает её и сравнивает вывод. Это гарантирует, что примеры в документации всегда работают и актуальны.' }
      ]
    },
    {
      id: 5,
      title: 'Комментарии внутри кода',
      type: 'theory',
      content: [
        { type: 'text', value: 'Внутренние комментарии объясняют "почему", а не "что". Если код очевидно читается — комментарий не нужен. Комментируй неочевидные решения, workaround и важные ограничения.' },
        { type: 'heading', value: 'Хорошие и плохие комментарии' },
        { type: 'code', language: 'go', value: 'package cache\n\nimport (\n    "sync"\n    "time"\n)\n\ntype Cache struct {\n    mu    sync.RWMutex\n    items map[string]item\n}\n\ntype item struct {\n    value     interface{}\n    expiresAt time.Time\n}\n\n// ПЛОХО: комментарии дублируют код\nfunc (c *Cache) Set(key string, value interface{}, ttl time.Duration) {\n    // Блокируем мьютекс\n    c.mu.Lock()\n    // Разблокируем мьютекс в defer\n    defer c.mu.Unlock()\n    // Создаём элемент\n    c.items[key] = item{\n        // Устанавливаем значение\n        value: value,\n        // Устанавливаем время истечения\n        expiresAt: time.Now().Add(ttl),\n    }\n}\n\n// ХОРОШО: комментарии объясняют ПОЧЕМУ, а не ЧТО\nfunc (c *Cache) Get(key string) (interface{}, bool) {\n    c.mu.RLock()\n    defer c.mu.RUnlock()\n\n    it, ok := c.items[key]\n    if !ok {\n        return nil, false\n    }\n\n    // Проверяем истечение без удаления — удаление требует write lock.\n    // Ленивое удаление (при следующем Set или явном Evict) снижает\n    // количество апгрейдов блокировки при высокой нагрузке чтения.\n    if time.Now().After(it.expiresAt) {\n        return nil, false\n    }\n\n    return it.value, true\n}\n\n// TODO: добавить автоматическую сборку истёкших элементов\n// через фоновую горутину — сейчас память не освобождается автоматически.\nfunc (c *Cache) Evict() {\n    c.mu.Lock()\n    defer c.mu.Unlock()\n    now := time.Now()\n    for k, v := range c.items {\n        if now.After(v.expiresAt) {\n            delete(c.items, k)\n        }\n    }\n}' },
        { type: 'warning', value: 'Устаревшие комментарии хуже отсутствия комментариев. Если изменил код — обнови комментарий. TODO/FIXME комментарии полезны только если есть task в трекере — иначе они висят вечно.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: задокументировать пакет',
      type: 'practice',
      difficulty: 'easy',
      description: 'Напиши полную документацию для пакета ratelimit, реализующего ограничитель частоты запросов. Включи: комментарий к пакету, документацию типов и методов, Example функции.',
      requirements: [
        'Комментарий к пакету ratelimit объясняет назначение и пример использования',
        'Тип Limiter{rate int, window time.Duration} с комментарием к каждому полю',
        'NewLimiter(rate int, window time.Duration) *Limiter — с документацией параметров',
        'Allow(key string) bool — возвращает true если запрос разрешён, документация включает поведение при превышении',
        'Reset(key string) — сбрасывает счётчик, задокументировать зачем',
        'ExampleLimiter() — рабочий пример с // Output:'
      ],
      expectedOutput: '// Package ratelimit ... (godoc комментарий)\ngo doc ratelimit.Limiter -> комментарий структуры\ngo doc ratelimit.Limiter.Allow -> комментарий метода\nExampleLimiter: PASS (Output совпадает)',
      hint: 'Начни с doc.go или с пакетного комментария в основном файле. Каждый exported тип и метод — своя строка документации начинающаяся с имени. Example функция должна иметь корректный // Output:.',
      solution: 'package main\n\nimport (\n    "fmt"\n    "sync"\n    "time"\n)\n\n// Package ratelimit реализует ограничитель частоты запросов (rate limiter).\n//\n// Limiter отслеживает количество запросов за скользящее временное окно\n// и отклоняет запросы, превышающие установленный лимит.\n//\n// Пример использования:\n//\n//   limiter := ratelimit.NewLimiter(10, time.Minute)\n//   if !limiter.Allow("user:123") {\n//       http.Error(w, "too many requests", http.StatusTooManyRequests)\n//       return\n//   }\n\n// entry хранит счётчик запросов и время сброса для одного ключа.\ntype entry struct {\n    count    int\n    resetAt  time.Time\n}\n\n// Limiter ограничивает частоту запросов по произвольным ключам.\n// Безопасен для конкурентного использования.\n// Нулевое значение Limiter не готово к использованию — используйте NewLimiter.\ntype Limiter struct {\n    // rate максимальное количество запросов за одно окно.\n    rate int\n\n    // window продолжительность временного окна.\n    window time.Duration\n\n    mu      sync.Mutex\n    entries map[string]*entry\n}\n\n// NewLimiter создаёт новый Limiter с указанными параметрами.\n//\n// rate — максимальное количество разрешённых запросов за окно.\n// window — длина временного окна (например, time.Minute).\n//\n// Паника при rate <= 0 или window <= 0.\nfunc NewLimiter(rate int, window time.Duration) *Limiter {\n    if rate <= 0 {\n        panic("ratelimit.NewLimiter: rate должен быть положительным")\n    }\n    if window <= 0 {\n        panic("ratelimit.NewLimiter: window должна быть положительной")\n    }\n    return &Limiter{\n        rate:    rate,\n        window:  window,\n        entries: make(map[string]*entry),\n    }\n}\n\n// Allow сообщает, разрешён ли запрос для данного ключа.\n//\n// Возвращает true если количество запросов не превысило rate за текущее window.\n// Возвращает false при превышении лимита — запрос следует отклонить.\n//\n// key может быть любой строкой: IP-адресом, user ID, API ключом.\n// Счётчик автоматически сбрасывается по истечении window.\nfunc (l *Limiter) Allow(key string) bool {\n    l.mu.Lock()\n    defer l.mu.Unlock()\n\n    now := time.Now()\n    e, ok := l.entries[key]\n\n    if !ok || now.After(e.resetAt) {\n        // Первый запрос или окно истекло — начинаем новое окно\n        l.entries[key] = &entry{\n            count:   1,\n            resetAt: now.Add(l.window),\n        }\n        return true\n    }\n\n    if e.count >= l.rate {\n        // Лимит исчерпан\n        return false\n    }\n\n    e.count++\n    return true\n}\n\n// Reset сбрасывает счётчик запросов для указанного ключа.\n//\n// Используйте для реализации whitelist или ручного снятия блокировки.\n// Если ключ не существует — вызов безопасен и ничего не делает.\nfunc (l *Limiter) Reset(key string) {\n    l.mu.Lock()\n    defer l.mu.Unlock()\n    delete(l.entries, key)\n}\n\n// Remaining возвращает количество оставшихся разрешённых запросов для ключа.\n// Возвращает rate если ключ ещё не использовался или окно истекло.\nfunc (l *Limiter) Remaining(key string) int {\n    l.mu.Lock()\n    defer l.mu.Unlock()\n\n    e, ok := l.entries[key]\n    if !ok || time.Now().After(e.resetAt) {\n        return l.rate\n    }\n    remaining := l.rate - e.count\n    if remaining < 0 {\n        return 0\n    }\n    return remaining\n}\n\n// ExampleLimiter демонстрирует базовое использование Limiter.\nfunc ExampleLimiter() {\n    limiter := NewLimiter(3, time.Minute)\n\n    // Первые 3 запроса разрешены\n    fmt.Println(limiter.Allow("user:1")) // true\n    fmt.Println(limiter.Allow("user:1")) // true\n    fmt.Println(limiter.Allow("user:1")) // true\n\n    // 4-й запрос отклонён\n    fmt.Println(limiter.Allow("user:1")) // false\n\n    // Сброс счётчика\n    limiter.Reset("user:1")\n    fmt.Println(limiter.Allow("user:1")) // true\n\n    // Output:\n    // true\n    // true\n    // true\n    // false\n    // true\n}\n\nfunc main() {\n    ExampleLimiter()\n    fmt.Println("ExampleLimiter: PASS (Output совпадает)")\n\n    l := NewLimiter(5, time.Minute)\n    fmt.Printf("Remaining до запросов: %d\\n", l.Remaining("ip:1.2.3.4"))\n    l.Allow("ip:1.2.3.4")\n    l.Allow("ip:1.2.3.4")\n    fmt.Printf("Remaining после 2 запросов: %d\\n", l.Remaining("ip:1.2.3.4"))\n}',
      explanation: 'Пакетный комментарий начинается с "Package ratelimit" (имя пакета) и объясняет назначение с примером. Каждый exported тип, метод и константа имеют комментарий начинающийся с имени объявления. ExampleLimiter() имеет // Output: — go test проверит вывод. Внутренние комментарии объясняют "почему" (почему удаляем запись при сбросе, зачем Remaining). Reset документирован как "безопасен при отсутствии ключа" — важная деталь поведения.'
    }
  ]
}
