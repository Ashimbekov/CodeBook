export default {
  id: 53,
  title: 'Миграции баз данных',
  description: 'Управление изменениями схемы базы данных: зачем нужны миграции, инструмент golang-migrate, написание up/down миграций, автоматический запуск и лучшие практики.',
  lessons: [
    {
      id: 1,
      title: 'Зачем нужны миграции',
      type: 'theory',
      content: [
        { type: 'text', value: 'Миграция базы данных — это как система контроля версий, но для схемы БД. Представьте, что вы работаете в команде над проектом: каждый разработчик вносит изменения в таблицы. Без миграций — хаос.' },
        { type: 'heading', value: 'Проблема без миграций' },
        { type: 'code', language: 'go', value: '// БЕЗ миграций:\n// Разработчик 1 добавил колонку phone в users в своей БД\n// Разработчик 2 ничего не знает об этом\n// На продакшене забыли добавить колонку -> приложение падает\n\n// С миграциями:\n// 1. Разработчик 1 создаёт файл миграции:\n//    migrations/000003_add_phone_to_users.up.sql\n// 2. Коммитит в git\n// 3. Разработчик 2 делает git pull и запускает migrate up\n// 4. На продакшене CI/CD автоматически применяет миграцию\n//\n// Принципы:\n// - Каждое изменение схемы = отдельный файл миграции\n// - Миграции пронумерованы (применяются по порядку)\n// - Каждая миграция имеет up (применить) и down (откатить)\n// - Применённые миграции записываются в таблицу schema_migrations\n// - В git хранится вся история изменений схемы\n\n// Пример истории миграций:\n// 000001_create_users.up.sql\n// 000002_create_posts.up.sql\n// 000003_add_phone_to_users.up.sql\n// 000004_add_index_posts_user_id.up.sql\n// 000005_create_tags.up.sql' },
        { type: 'heading', value: 'Таблица отслеживания миграций' },
        { type: 'code', language: 'go', value: '// golang-migrate создаёт таблицу schema_migrations:\n// CREATE TABLE schema_migrations (\n//     version bigint NOT NULL,\n//     dirty boolean NOT NULL,\n//     PRIMARY KEY (version)\n// );\n//\n// version — номер последней применённой миграции\n// dirty  — true если миграция прервалась на середине (нужен ручной откат)\n//\n// После migrate up до версии 3:\n// | version | dirty |\n// |---------|-------|\n// |       3 | false |\n//\n// Если миграция 4 упала на середине:\n// | version | dirty |\n// |---------|-------|\n// |       4 | true  |  <- надо вручную исправить' },
        { type: 'note', value: 'AutoMigrate GORM удобен в разработке, но не подходит для продакшена: он не может откатить изменения, не поддерживает перенос данных и не работает с командой разработчиков. Используйте golang-migrate или аналоги.' }
      ]
    },
    {
      id: 2,
      title: 'Инструмент golang-migrate',
      type: 'theory',
      content: [
        { type: 'text', value: 'golang-migrate — стандарт де-факто для миграций в Go-проектах. Поддерживает PostgreSQL, MySQL, SQLite, MongoDB и другие базы. Работает как CLI-инструмент и как библиотека Go.' },
        { type: 'heading', value: 'Установка и настройка' },
        { type: 'code', language: 'go', value: '// Установка CLI:\n// macOS:  brew install golang-migrate\n// Linux:  curl -L https://github.com/golang-migrate/migrate/releases/download/v4.17.0/migrate.linux-amd64.tar.gz | tar xvz\n// Go:     go install -tags "postgres" github.com/golang-migrate/migrate/v4/cmd/migrate@latest\n\n// Установка библиотеки:\n// go get github.com/golang-migrate/migrate/v4\n// go get github.com/golang-migrate/migrate/v4/database/postgres\n// go get github.com/golang-migrate/migrate/v4/source/file\n\n// Структура проекта:\n// myapp/\n// ├── main.go\n// ├── migrations/\n// │   ├── 000001_create_users.up.sql\n// │   ├── 000001_create_users.down.sql\n// │   ├── 000002_create_posts.up.sql\n// │   └── 000002_create_posts.down.sql\n// └── go.mod\n\n// Создание новой миграции (CLI):\n// migrate create -ext sql -dir migrations -seq create_users\n// Создаст:\n// migrations/000001_create_users.up.sql   (пустой)\n// migrations/000001_create_users.down.sql (пустой)\n\n// Команды CLI:\n// migrate -path migrations -database "postgres://..." up       -- применить все\n// migrate -path migrations -database "postgres://..." up 1     -- применить 1\n// migrate -path migrations -database "postgres://..." down     -- откатить все\n// migrate -path migrations -database "postgres://..." down 1   -- откатить 1\n// migrate -path migrations -database "postgres://..." version  -- текущая версия\n// migrate -path migrations -database "postgres://..." goto 3   -- перейти к версии 3\n// migrate -path migrations -database "postgres://..." force 3  -- принудительно выставить версию (при dirty)' },
        { type: 'tip', value: 'Используйте -seq флаг при создании миграций — он автоматически пронумерует файлы: 000001, 000002, 000003. Без него нужно вручную указывать timestamp. Порядковые номера надёжнее для командной работы.' }
      ]
    },
    {
      id: 3,
      title: 'Написание up и down миграций',
      type: 'theory',
      content: [
        { type: 'text', value: 'Каждая миграция состоит из двух файлов: .up.sql (что делаем) и .down.sql (как откатить). Down должен полностью отменять up — как кнопка "отменить" в текстовом редакторе.' },
        { type: 'heading', value: 'Примеры миграций' },
        { type: 'code', language: 'go', value: '// ====== 000001_create_users.up.sql ======\n// CREATE TABLE users (\n//     id         SERIAL PRIMARY KEY,\n//     name       VARCHAR(100) NOT NULL,\n//     email      VARCHAR(200) NOT NULL UNIQUE,\n//     password   VARCHAR(255) NOT NULL,\n//     is_active  BOOLEAN NOT NULL DEFAULT TRUE,\n//     created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),\n//     updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),\n//     deleted_at TIMESTAMP WITH TIME ZONE\n// );\n// CREATE INDEX idx_users_email ON users(email);\n// CREATE INDEX idx_users_deleted_at ON users(deleted_at);\n\n// ====== 000001_create_users.down.sql ======\n// DROP TABLE IF EXISTS users;\n\n// ====== 000002_create_posts.up.sql ======\n// CREATE TABLE posts (\n//     id         SERIAL PRIMARY KEY,\n//     user_id    INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,\n//     title      VARCHAR(200) NOT NULL,\n//     content    TEXT,\n//     published  BOOLEAN NOT NULL DEFAULT FALSE,\n//     created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),\n//     updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),\n//     deleted_at TIMESTAMP WITH TIME ZONE\n// );\n// CREATE INDEX idx_posts_user_id ON posts(user_id);\n\n// ====== 000002_create_posts.down.sql ======\n// DROP TABLE IF EXISTS posts;\n\n// ====== 000003_add_phone_to_users.up.sql ======\n// ALTER TABLE users ADD COLUMN phone VARCHAR(20);\n// ALTER TABLE users ADD COLUMN phone_verified BOOLEAN NOT NULL DEFAULT FALSE;\n\n// ====== 000003_add_phone_to_users.down.sql ======\n// ALTER TABLE users DROP COLUMN IF EXISTS phone;\n// ALTER TABLE users DROP COLUMN IF EXISTS phone_verified;\n\n// ====== 000004_rename_content_to_body.up.sql ======\n// ALTER TABLE posts RENAME COLUMN content TO body;\n\n// ====== 000004_rename_content_to_body.down.sql ======\n// ALTER TABLE posts RENAME COLUMN body TO content;\n\n// ====== 000005_add_full_text_index.up.sql ======\n// -- Добавляем полнотекстовый поиск (PostgreSQL)\n// ALTER TABLE posts ADD COLUMN search_vector TSVECTOR;\n// UPDATE posts SET search_vector = to_tsvector(title || \' \' || COALESCE(body, \'\'));\n// CREATE INDEX idx_posts_search ON posts USING GIN(search_vector);\n\n// ====== 000005_add_full_text_index.down.sql ======\n// DROP INDEX IF EXISTS idx_posts_search;\n// ALTER TABLE posts DROP COLUMN IF EXISTS search_vector;' },
        { type: 'warning', value: 'Down-миграция для переноса данных требует особой осторожности. Если up перемещала данные из колонки A в B, то down должна вернуть их обратно. Иногда down невозможен — тогда это необратимая миграция, и её нужно явно документировать.' }
      ]
    },
    {
      id: 4,
      title: 'Запуск миграций из Go кода',
      type: 'theory',
      content: [
        { type: 'text', value: 'golang-migrate можно использовать как библиотеку и запускать миграции программно при старте приложения. Это удобно для автоматического развёртывания.' },
        { type: 'heading', value: 'Программный запуск миграций' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "database/sql"\n    "errors"\n    "fmt"\n    "log"\n\n    "github.com/golang-migrate/migrate/v4"\n    "github.com/golang-migrate/migrate/v4/database/postgres"\n    _ "github.com/golang-migrate/migrate/v4/source/file"\n    _ "github.com/lib/pq"\n)\n\nfunc runMigrations(db *sql.DB, migrationsPath string) error {\n    // Создаём драйвер миграций поверх *sql.DB\n    driver, err := postgres.WithInstance(db, &postgres.Config{})\n    if err != nil {\n        return fmt.Errorf("migrate driver: %w", err)\n    }\n\n    // Создаём экземпляр migrate\n    m, err := migrate.NewWithDatabaseInstance(\n        "file://"+migrationsPath, // путь к папке с .sql файлами\n        "postgres",\n        driver,\n    )\n    if err != nil {\n        return fmt.Errorf("migrate.New: %w", err)\n    }\n    defer m.Close()\n\n    // Текущая версия\n    version, dirty, err := m.Version()\n    if err != nil && !errors.Is(err, migrate.ErrNilVersion) {\n        return fmt.Errorf("migrate version: %w", err)\n    }\n    if dirty {\n        return fmt.Errorf("база в грязном состоянии (версия %d dirty=true), нужен ручной откат", version)\n    }\n    log.Printf("Текущая версия миграций: %d", version)\n\n    // Применить все новые миграции\n    if err := m.Up(); err != nil {\n        if errors.Is(err, migrate.ErrNoChange) {\n            log.Println("Новых миграций нет")\n            return nil\n        }\n        return fmt.Errorf("migrate up: %w", err)\n    }\n\n    newVersion, _, _ := m.Version()\n    log.Printf("Миграции применены до версии: %d", newVersion)\n    return nil\n}\n\nfunc main() {\n    db, err := sql.Open("postgres", "host=localhost user=postgres dbname=mydb sslmode=disable")\n    if err != nil {\n        log.Fatal(err)\n    }\n    defer db.Close()\n\n    if err := runMigrations(db, "./migrations"); err != nil {\n        log.Fatalf("Ошибка миграций: %v", err)\n    }\n\n    log.Println("Приложение запущено")\n}' },
        { type: 'heading', value: 'Встроенные миграции (embed)' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "embed"\n    "errors"\n    "log"\n\n    "github.com/golang-migrate/migrate/v4"\n    "github.com/golang-migrate/migrate/v4/database/sqlite3"\n    "github.com/golang-migrate/migrate/v4/source/iofs"\n    _ "github.com/mattn/go-sqlite3"\n    "database/sql"\n)\n\n//go:embed migrations/*.sql\nvar migrationsFS embed.FS\n\nfunc runEmbeddedMigrations(db *sql.DB) error {\n    // Используем embed.FS вместо файловой системы\n    source, err := iofs.New(migrationsFS, "migrations")\n    if err != nil {\n        return err\n    }\n\n    driver, err := sqlite3.WithInstance(db, &sqlite3.Config{})\n    if err != nil {\n        return err\n    }\n\n    m, err := migrate.NewWithInstance("iofs", source, "sqlite3", driver)\n    if err != nil {\n        return err\n    }\n\n    if err := m.Up(); err != nil && !errors.Is(err, migrate.ErrNoChange) {\n        return err\n    }\n    return nil\n}\n\nfunc main() {\n    db, _ := sql.Open("sqlite3", "app.db")\n    defer db.Close()\n\n    if err := runEmbeddedMigrations(db); err != nil {\n        log.Fatal(err)\n    }\n    log.Println("Миграции применены (из встроенных файлов)")\n}' },
        { type: 'tip', value: 'Встроенные миграции (embed) позволяют собрать приложение в один бинарный файл без внешних зависимостей. Особенно удобно для Docker-образов и CI/CD.' }
      ]
    },
    {
      id: 5,
      title: 'Лучшие практики миграций',
      type: 'theory',
      content: [
        { type: 'text', value: 'Неправильные миграции могут уничтожить данные или сломать продакшен. Вот правила, которые защитят вас от катастроф.' },
        { type: 'heading', value: 'Правила безопасных миграций' },
        { type: 'code', language: 'go', value: '// ПРАВИЛО 1: Обратная совместимость\n// Никогда не удаляйте колонку, которую ещё использует старый код\n// Deploy нового кода ПЕРЕД удалением колонки\n//\n// ПРАВИЛО 2: Новые колонки — всегда nullable или с default\n// ПЛОХО:\n// ALTER TABLE users ADD COLUMN phone VARCHAR(20) NOT NULL;\n// -> Упадёт если таблица не пустая!\n//\n// ХОРОШО:\n// ALTER TABLE users ADD COLUMN phone VARCHAR(20); -- nullable\n// -- или\n// ALTER TABLE users ADD COLUMN phone VARCHAR(20) NOT NULL DEFAULT \'\';\n\n// ПРАВИЛО 3: Большие таблицы — осторожно с индексами\n// Обычный CREATE INDEX блокирует таблицу!\n// В PostgreSQL используйте CONCURRENTLY:\n//\n// 000006_add_index_users_phone.up.sql\n// CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_users_phone ON users(phone);\n//\n// 000006_add_index_users_phone.down.sql\n// DROP INDEX CONCURRENTLY IF EXISTS idx_users_phone;\n\n// ПРАВИЛО 4: Изменение типа колонки\n// Нельзя просто изменить тип с TEXT на INTEGER если данные несовместимы\n// Делайте в три шага:\n// Миграция 1 (up): ADD COLUMN age_int INTEGER;\n// Код 1: пишет в оба поля\n// Миграция 2 (up): UPDATE users SET age_int = CAST(age_text AS INTEGER);\n// Код 2: читает из нового поля\n// Миграция 3 (up): DROP COLUMN age_text;\n\n// ПРАВИЛО 5: Никогда не редактируйте применённые миграции\n// Если миграция уже применена в продакшене — создайте новую\n// Редактирование сломает контрольные суммы\n\n// ПРАВИЛО 6: Тестируйте down-миграции\n// migrate up && migrate down && migrate up\n// Убедитесь что откат работает корректно\n\n// ПРАВИЛО 7: Бэкап перед крупными миграциями\n// pg_dump mydb > backup_before_migration_$(date +%Y%m%d).sql' },
        { type: 'heading', value: 'Перенос данных в миграциях' },
        { type: 'code', language: 'go', value: '// ====== 000007_split_name_to_first_last.up.sql ======\n// Разбиваем поле name на first_name и last_name\n//\n// -- Шаг 1: добавляем новые колонки\n// ALTER TABLE users ADD COLUMN first_name VARCHAR(50);\n// ALTER TABLE users ADD COLUMN last_name  VARCHAR(50);\n//\n// -- Шаг 2: переносим данные\n// UPDATE users\n// SET\n//     first_name = SPLIT_PART(name, \' \', 1),\n//     last_name  = SPLIT_PART(name, \' \', 2)\n// WHERE name IS NOT NULL;\n//\n// -- Шаг 3: делаем обязательными (после проверки данных)\n// ALTER TABLE users ALTER COLUMN first_name SET NOT NULL;\n// ALTER TABLE users ALTER COLUMN first_name SET DEFAULT \'\';\n// ALTER TABLE users ALTER COLUMN last_name  SET NOT NULL;\n// ALTER TABLE users ALTER COLUMN last_name  SET DEFAULT \'\';\n//\n// -- Шаг 4: удаляем старую колонку (в следующей миграции!)\n\n// ====== 000007_split_name_to_first_last.down.sql ======\n// ALTER TABLE users DROP COLUMN IF EXISTS first_name;\n// ALTER TABLE users DROP COLUMN IF EXISTS last_name;' },
        { type: 'warning', value: 'Никогда не запускайте миграцию напрямую в продакшене без тестирования на копии данных. Всегда делайте резервную копию БД перед применением деструктивных миграций.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: система миграций для приложения',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создай систему миграций для приложения блога. Напиши SQL-файлы миграций и Go-код для их автоматического применения при старте.',
      requirements: [
        'Создай директорию migrations/ с 3 миграциями',
        'Миграция 1: создание таблицы users (id, name, email, created_at)',
        'Миграция 2: создание таблицы articles (id, user_id FK, title, body, published, created_at)',
        'Миграция 3: добавление колонки tags TEXT в articles (nullable)',
        'Go код: функция MigrateDB(db *sql.DB, path string) error',
        'MigrateDB должна логировать текущую версию и новую после применения',
        'В main: открой SQLite БД, вызови MigrateDB, выведи успех',
        'Обработай случай ErrNoChange (миграций нет — не ошибка)'
      ],
      expectedOutput: 'Текущая версия миграций: 0\nПрименяю миграции...\nМиграции успешно применены до версии: 3\nПриложение запущено',
      hint: 'Используй go get github.com/golang-migrate/migrate/v4 и go get github.com/golang-migrate/migrate/v4/database/sqlite3. Файлы именуй: 000001_create_users.up.sql, 000001_create_users.down.sql и т.д.',
      solution: '// Файл: migrations/000001_create_users.up.sql\n// CREATE TABLE IF NOT EXISTS users (\n//     id         INTEGER PRIMARY KEY AUTOINCREMENT,\n//     name       TEXT NOT NULL,\n//     email      TEXT NOT NULL UNIQUE,\n//     created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP\n// );\n\n// Файл: migrations/000001_create_users.down.sql\n// DROP TABLE IF EXISTS users;\n\n// Файл: migrations/000002_create_articles.up.sql\n// CREATE TABLE IF NOT EXISTS articles (\n//     id         INTEGER PRIMARY KEY AUTOINCREMENT,\n//     user_id    INTEGER NOT NULL REFERENCES users(id),\n//     title      TEXT NOT NULL,\n//     body       TEXT,\n//     published  INTEGER NOT NULL DEFAULT 0,\n//     created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP\n// );\n\n// Файл: migrations/000002_create_articles.down.sql\n// DROP TABLE IF EXISTS articles;\n\n// Файл: migrations/000003_add_tags_to_articles.up.sql\n// ALTER TABLE articles ADD COLUMN tags TEXT;\n\n// Файл: migrations/000003_add_tags_to_articles.down.sql\n// -- SQLite не поддерживает DROP COLUMN в старых версиях\n// -- Пересоздаём таблицу\n// CREATE TABLE articles_new AS SELECT id, user_id, title, body, published, created_at FROM articles;\n// DROP TABLE articles;\n// ALTER TABLE articles_new RENAME TO articles;\n\n// Файл: main.go\npackage main\n\nimport (\n    "database/sql"\n    "errors"\n    "fmt"\n    "log"\n\n    "github.com/golang-migrate/migrate/v4"\n    "github.com/golang-migrate/migrate/v4/database/sqlite3"\n    _ "github.com/golang-migrate/migrate/v4/source/file"\n    _ "github.com/mattn/go-sqlite3"\n)\n\nfunc MigrateDB(db *sql.DB, path string) error {\n    driver, err := sqlite3.WithInstance(db, &sqlite3.Config{})\n    if err != nil {\n        return fmt.Errorf("migrate driver: %w", err)\n    }\n\n    m, err := migrate.NewWithDatabaseInstance("file://"+path, "sqlite3", driver)\n    if err != nil {\n        return fmt.Errorf("migrate.New: %w", err)\n    }\n    defer m.Close()\n\n    version, _, err := m.Version()\n    if err != nil && !errors.Is(err, migrate.ErrNilVersion) {\n        return fmt.Errorf("version: %w", err)\n    }\n    fmt.Printf("Текущая версия миграций: %d\\n", version)\n\n    fmt.Println("Применяю миграции...")\n    if err := m.Up(); err != nil {\n        if errors.Is(err, migrate.ErrNoChange) {\n            fmt.Println("Новых миграций нет")\n            return nil\n        }\n        return fmt.Errorf("migrate up: %w", err)\n    }\n\n    newVersion, _, _ := m.Version()\n    fmt.Printf("Миграции успешно применены до версии: %d\\n", newVersion)\n    return nil\n}\n\nfunc main() {\n    db, err := sql.Open("sqlite3", "blog.db")\n    if err != nil {\n        log.Fatal("Открытие БД:", err)\n    }\n    defer db.Close()\n\n    if err := MigrateDB(db, "./migrations"); err != nil {\n        log.Fatalf("Ошибка миграций: %v", err)\n    }\n\n    fmt.Println("Приложение запущено")\n}',
      explanation: 'golang-migrate хранит текущую версию в таблице schema_migrations и применяет только новые файлы. Нумерация 000001_, 000002_ гарантирует порядок применения. ErrNoChange — не ошибка, это нормальная ситуация при повторном запуске. Встраивание SQL в отдельные файлы вместо Go-строк упрощает рецензирование кода и работу с SQL-инструментами.'
    }
  ]
}
