export default {
  id: 79,
  title: 'Структура проекта в Go',
  description: 'Организация Go проектов: стандартный layout (cmd/internal/pkg), плоская структура для маленьких проектов, domain-driven layout, назначение каждой директории, Makefile соглашения.',
  lessons: [
    {
      id: 1,
      title: 'Плоская структура — для маленьких проектов',
      type: 'theory',
      content: [
        { type: 'text', value: 'Маленькие Go проекты не нуждаются в сложной иерархии. Плоская структура — один-два пакета — проще для понимания и поддержки. Не усложняй структуру преждевременно.' },
        { type: 'heading', value: 'Плоская структура: CLI утилита' },
        { type: 'code', language: 'go', value: '// Маленькая CLI утилита — всё в одном пакете\n//\n// myapp/\n// ├── main.go          // точка входа\n// ├── handler.go       // HTTP хендлеры\n// ├── service.go       // бизнес-логика\n// ├── storage.go       // работа с файлами/БД\n// ├── config.go        // конфигурация\n// ├── go.mod\n// └── go.sum\n//\n// Один пакет "main" — всё просто и понятно\n\n// main.go\npackage main\n\nimport (\n    "log"\n    "net/http"\n    "os"\n)\n\nfunc main() {\n    cfg := loadConfig()\n    storage := newStorage(cfg.DBPath)\n    svc := newService(storage)\n    mux := setupRoutes(svc)\n\n    log.Printf("Сервер запущен на %s", cfg.Addr)\n    log.Fatal(http.ListenAndServe(cfg.Addr, mux))\n}\n\n// config.go — в том же пакете main\ntype Config struct {\n    Addr   string\n    DBPath string\n}\n\nfunc loadConfig() Config {\n    return Config{\n        Addr:   envOrDefault("ADDR", ":8080"),\n        DBPath: envOrDefault("DB_PATH", "./data.db"),\n    }\n}\n\nfunc envOrDefault(key, def string) string {\n    if v := os.Getenv(key); v != "" {\n        return v\n    }\n    return def\n}' },
        { type: 'tip', value: 'Не спеши создавать internal/ и pkg/ для маленького проекта. Если проект — одна бинарная утилита без библиотечных пакетов — плоская структура идеальна. Усложняй когда проект реально вырос.' }
      ]
    },
    {
      id: 2,
      title: 'cmd/ и internal/ — стандарт для сервисов',
      type: 'theory',
      content: [
        { type: 'text', value: 'Для сервисов с несколькими бинарными файлами или публичным API стандарт: cmd/ для точек входа, internal/ для закрытого кода, pkg/ для публичных библиотек.' },
        { type: 'heading', value: 'Структура production сервиса' },
        { type: 'code', language: 'go', value: '// Структура Go сервиса (medium+ размер):\n//\n// myapp/\n// ├── cmd/\n// │   ├── api/\n// │   │   └── main.go      // HTTP API сервер\n// │   ├── worker/\n// │   │   └── main.go      // фоновый воркер\n// │   └── migrate/\n// │       └── main.go      // утилита миграций\n// │\n// ├── internal/            // закрытый код — не импортируется снаружи\n// │   ├── domain/          // бизнес-сущности и ошибки\n// │   ├── usecase/         // бизнес-логика\n// │   ├── handler/         // HTTP хендлеры\n// │   ├── repository/      // реализации репозиториев\n// │   │   ├── postgres/\n// │   │   └── memory/\n// │   └── config/          // конфигурация приложения\n// │\n// ├── pkg/                 // публичные пакеты — можно импортировать\n// │   ├── logger/          // общий логгер\n// │   ├── validator/       // валидатор\n// │   └── pagination/      // хелперы пагинации\n// │\n// ├── migrations/          // SQL миграции\n// │   ├── 001_create_users.sql\n// │   └── 002_add_email_index.sql\n// │\n// ├── scripts/             // вспомогательные скрипты\n// │   └── seed.go\n// │\n// ├── .env.example\n// ├── docker-compose.yml\n// ├── Makefile\n// ├── go.mod\n// └── go.sum\n\n// cmd/api/main.go — точка входа\npackage main\n\nimport (\n    "log"\n    "myapp/internal/config"\n    "myapp/internal/handler"\n    "myapp/internal/repository/postgres"\n    "myapp/internal/usecase"\n)\n\nfunc main() {\n    cfg := config.Load()\n    db := postgres.Connect(cfg.DSN)\n    repo := postgres.NewUserRepo(db)\n    uc := usecase.NewUserUseCase(repo)\n    h := handler.NewUserHandler(uc)\n    log.Fatal(h.Serve(cfg.Addr))\n}' },
        { type: 'note', value: 'internal/ — ключевое соглашение Go: код в internal/ может быть импортирован ТОЛЬКО из модулей с тем же корневым путём. myapp/internal/... импортируем внутри myapp, но НЕ из other-module.' }
      ]
    },
    {
      id: 3,
      title: 'Domain-driven layout',
      type: 'theory',
      content: [
        { type: 'text', value: 'Для больших приложений с несколькими доменами структура может быть организована по доменам, а не по типам слоёв. Каждый домен содержит свои handler, service, repository.' },
        { type: 'heading', value: 'Структура по доменам' },
        { type: 'code', language: 'go', value: '// Domain-driven layout — для больших систем\n//\n// myapp/\n// ├── cmd/\n// │   └── api/main.go\n// │\n// ├── internal/\n// │   ├── users/           // домен "пользователи"\n// │   │   ├── domain.go    // User struct, ошибки\n// │   │   ├── handler.go   // HTTP хендлеры\n// │   │   ├── service.go   // бизнес-логика\n// │   │   ├── repository.go // интерфейс репозитория\n// │   │   ├── postgres.go  // реализация репозитория\n// │   │   └── service_test.go\n// │   │\n// │   ├── orders/          // домен "заказы"\n// │   │   ├── domain.go\n// │   │   ├── handler.go\n// │   │   ├── service.go\n// │   │   └── ...\n// │   │\n// │   ├── products/        // домен "продукты"\n// │   │   └── ...\n// │   │\n// │   └── platform/        // общая инфраструктура\n// │       ├── database/\n// │       ├── logger/\n// │       └── middleware/\n// │\n// ├── go.mod\n// └── Makefile\n\n// Пример internal/users/domain.go\npackage users\n\nimport (\n    "errors"\n    "time"\n)\n\ntype User struct {\n    ID        int64\n    Email     string\n    CreatedAt time.Time\n}\n\nvar (\n    ErrNotFound   = errors.New("пользователь не найден")\n    ErrDuplicate  = errors.New("пользователь уже существует")\n)\n\n// Интерфейс репозитория — в том же пакете, что потребитель\ntype Repository interface {\n    Create(u *User) error\n    GetByID(id int64) (*User, error)\n}\n\n// Пример internal/users/service.go\ntype Service struct {\n    repo Repository\n}\n\nfunc NewService(repo Repository) *Service {\n    return &Service{repo: repo}\n}' },
        { type: 'tip', value: 'Domain-driven layout хорош, когда команды работают над разными доменами независимо. Если у тебя 2-3 разработчика и 3 домена — лучше layer-based layout (usecase/, handler/, repository/). Не добавляй сложности раньше времени.' }
      ]
    },
    {
      id: 4,
      title: 'Пакет config — управление конфигурацией',
      type: 'theory',
      content: [
        { type: 'text', value: 'Конфигурация приложения читается один раз при старте и передаётся как структура. Используй переменные окружения с значениями по умолчанию. Популярные библиотеки: godotenv, viper, env.' },
        { type: 'heading', value: 'Паттерн конфигурации' },
        { type: 'code', language: 'go', value: '// internal/config/config.go\npackage config\n\nimport (\n    "fmt"\n    "os"\n    "strconv"\n    "time"\n)\n\n// Config — все настройки приложения в одной структуре\ntype Config struct {\n    // HTTP\n    HTTPAddr         string\n    HTTPReadTimeout  time.Duration\n    HTTPWriteTimeout time.Duration\n\n    // Database\n    DatabaseDSN     string\n    DatabaseMaxConn int\n\n    // App\n    LogLevel string\n    Debug    bool\n}\n\n// Load читает конфигурацию из переменных окружения\nfunc Load() (*Config, error) {\n    cfg := &Config{\n        HTTPAddr:         getEnv("HTTP_ADDR", ":8080"),\n        HTTPReadTimeout:  getDuration("HTTP_READ_TIMEOUT", 15*time.Second),\n        HTTPWriteTimeout: getDuration("HTTP_WRITE_TIMEOUT", 15*time.Second),\n        DatabaseDSN:      getEnv("DATABASE_DSN", ""),\n        DatabaseMaxConn:  getInt("DATABASE_MAX_CONN", 10),\n        LogLevel:         getEnv("LOG_LEVEL", "info"),\n        Debug:            getBool("DEBUG", false),\n    }\n\n    // Валидация обязательных полей\n    if cfg.DatabaseDSN == "" {\n        return nil, fmt.Errorf("DATABASE_DSN не задан")\n    }\n\n    return cfg, nil\n}\n\nfunc getEnv(key, def string) string {\n    if v := os.Getenv(key); v != "" {\n        return v\n    }\n    return def\n}\n\nfunc getInt(key string, def int) int {\n    if v := os.Getenv(key); v != "" {\n        n, err := strconv.Atoi(v)\n        if err == nil {\n            return n\n        }\n    }\n    return def\n}\n\nfunc getBool(key string, def bool) bool {\n    if v := os.Getenv(key); v != "" {\n        b, err := strconv.ParseBool(v)\n        if err == nil {\n            return b\n        }\n    }\n    return def\n}\n\nfunc getDuration(key string, def time.Duration) time.Duration {\n    if v := os.Getenv(key); v != "" {\n        d, err := time.ParseDuration(v)\n        if err == nil {\n            return d\n        }\n    }\n    return def\n}' }
      ]
    },
    {
      id: 5,
      title: 'Makefile — автоматизация рутины',
      type: 'theory',
      content: [
        { type: 'text', value: 'Makefile — стандартный инструмент автоматизации в Go-проектах. Команды make build, make test, make lint — заменяют длинные команды go build с флагами.' },
        { type: 'heading', value: 'Типичный Makefile для Go проекта' },
        { type: 'code', language: 'go', value: '# Makefile\n# Переменные\nAPP_NAME    := myapp\nBUILD_DIR   := ./bin\nMAIN_PKG    := ./cmd/api\nGO_FILES    := $(shell find . -name "*.go" -not -path "./vendor/*")\nGIT_COMMIT  := $(shell git rev-parse --short HEAD)\nBUILD_DATE  := $(shell date -u +"%Y-%m-%dT%H:%M:%SZ")\n\n# Флаги сборки\nLD_FLAGS := -ldflags "-X main.version=$(GIT_COMMIT) -X main.buildDate=$(BUILD_DATE)"\n\n.PHONY: build test lint clean run docker-build migrate\n\n# Сборка бинарного файла\nbuild:\n\t@echo "Сборка..."\n\tgo build $(LD_FLAGS) -o $(BUILD_DIR)/$(APP_NAME) $(MAIN_PKG)\n\n# Запуск тестов\ntest:\n\t@echo "Тесты..."\n\tgo test -v -race -coverprofile=coverage.out ./...\n\tgo tool cover -func=coverage.out\n\n# Тесты с интеграционными\ntest-integration:\n\t@echo "Интеграционные тесты..."\n\tgo test -v -tags=integration ./...\n\n# Линтер\nlint:\n\t@echo "Линтинг..."\n\tgolangci-lint run ./...\n\n# Форматирование\nfmt:\n\t@echo "Форматирование..."\n\tgofmt -w $(GO_FILES)\n\tgoimports -w $(GO_FILES)\n\n# Проверка перед коммитом\ncheck: fmt lint test\n\t@echo "Все проверки пройдены"\n\n# Запуск для разработки\nrun:\n\t@echo "Запуск..."\n\tgo run $(MAIN_PKG)\n\n# Очистка\nclean:\n\t@rm -rf $(BUILD_DIR)\n\t@rm -f coverage.out\n\n# Миграции\nmigrate-up:\n\tgoose -dir migrations postgres "$(DATABASE_DSN)" up\n\nmigrate-down:\n\tgoose -dir migrations postgres "$(DATABASE_DSN)" down\n\n# Docker\ndocker-build:\n\tdocker build -t $(APP_NAME):$(GIT_COMMIT) .\n\n# Генерация кода (wire, mockgen и т.д.)\ngenerate:\n\tgo generate ./...' },
        { type: 'tip', value: 'Команда make check (fmt + lint + test) — запускай перед каждым коммитом или добавь как git pre-commit hook. make build должна работать с нуля на чистой машине.' }
      ]
    },
    {
      id: 6,
      title: 'go.mod и управление зависимостями',
      type: 'theory',
      content: [
        { type: 'text', value: 'go.mod — файл манифеста Go модуля. Содержит имя модуля, версию Go и список зависимостей. Правила работы с зависимостями: минимальный набор, проверенные версии, vendor для воспроизводимости.' },
        { type: 'heading', value: 'Работа с зависимостями' },
        { type: 'code', language: 'go', value: '// go.mod — типичный файл\nmodule github.com/username/myapp\n\ngo 1.22\n\nrequire (\n    github.com/go-chi/chi/v5 v5.0.12\n    github.com/jmoiron/sqlx v1.3.5\n    github.com/lib/pq v1.10.9\n    golang.org/x/crypto v0.21.0\n)\n\nrequire (\n    // Косвенные зависимости\n    github.com/stretchr/testify v1.9.0 // indirect\n)\n\n// Полезные команды:\n// go mod tidy          — удалить неиспользуемые, добавить недостающие\n// go mod vendor        — копировать в vendor/ для воспроизводимости\n// go mod verify        — проверить checksums\n// go mod why pkg       — почему эта зависимость нужна\n// go get pkg@version   — обновить зависимость\n// go get -u ./...      — обновить все зависимости\n\n// Практические правила:\n// 1. Запускай go mod tidy перед каждым коммитом\n// 2. Коммить go.sum вместе с go.mod\n// 3. Для продакшн — рассмотри vendor/ для изоляции от сети\n// 4. Проверяй CHANGELOG при обновлении major версий\n\n// .gitignore для Go проекта:\n// /bin/          — скомпилированные бинарники\n// coverage.out   — данные покрытия\n// .env           — переменные окружения\n// *.test         — скомпилированные тестовые бинарники\n// # vendor/ НЕ игнорируем если используем vendor mode' }
      ]
    },
    {
      id: 7,
      title: 'Практика: организовать структуру проекта',
      type: 'practice',
      difficulty: 'medium',
      description: 'Организуй структуру небольшого REST API сервиса для блога. Раздели код на слои, создай правильную иерархию пакетов, напиши конфигурацию через переменные окружения.',
      requirements: [
        'Структура: cmd/api/main.go, internal/domain/post.go, internal/service/post_service.go, internal/handler/post_handler.go, internal/config/config.go',
        'domain/post.go: struct Post{ID, Title, Body, AuthorID}, var ErrPostNotFound, ErrEmptyTitle',
        'service/post_service.go: interface PostRepository, struct PostService с NewPostService и Create/GetByID',
        'handler/post_handler.go: struct PostHandler с NewPostHandler и методами Create, GetByID',
        'config/config.go: Config{Addr, LogLevel}, функция Load() с env переменными',
        'main.go: собрать всё через DI, использовать Config'
      ],
      expectedOutput: 'Конфигурация загружена: addr=:8080, log_level=info\nСервис и хендлер инициализированы\nGET /posts/1 -> 200\nPOST /posts -> 201\nGET /posts/999 -> 404',
      hint: 'Начни с domain — он ни от чего не зависит. Потом service с интерфейсом репозитория. Затем handler. В main.go собери всё вместе. Используй httptest для демонстрации без запуска сервера.',
      solution: 'package main\n\nimport (\n    "context"\n    "encoding/json"\n    "errors"\n    "fmt"\n    "net/http"\n    "net/http/httptest"\n    "os"\n    "strconv"\n    "strings"\n    "sync"\n)\n\n// === internal/config/config.go ===\n\ntype Config struct {\n    Addr     string\n    LogLevel string\n}\n\nfunc LoadConfig() *Config {\n    return &Config{\n        Addr:     getenv("HTTP_ADDR", ":8080"),\n        LogLevel: getenv("LOG_LEVEL", "info"),\n    }\n}\n\nfunc getenv(key, def string) string {\n    if v := os.Getenv(key); v != "" {\n        return v\n    }\n    return def\n}\n\n// === internal/domain/post.go ===\n\ntype Post struct {\n    ID       int64  `json:"id"`\n    Title    string `json:"title"`\n    Body     string `json:"body"`\n    AuthorID int64  `json:"author_id"`\n}\n\nvar (\n    ErrPostNotFound = errors.New("пост не найден")\n    ErrEmptyTitle   = errors.New("заголовок не может быть пустым")\n)\n\n// === internal/service/post_service.go ===\n\ntype PostRepository interface {\n    Create(ctx context.Context, p *Post) error\n    GetByID(ctx context.Context, id int64) (*Post, error)\n}\n\ntype PostService struct {\n    repo PostRepository\n}\n\nfunc NewPostService(repo PostRepository) *PostService {\n    return &PostService{repo: repo}\n}\n\nfunc (s *PostService) Create(ctx context.Context, title, body string, authorID int64) (*Post, error) {\n    if title == "" {\n        return nil, ErrEmptyTitle\n    }\n    p := &Post{Title: title, Body: body, AuthorID: authorID}\n    if err := s.repo.Create(ctx, p); err != nil {\n        return nil, fmt.Errorf("PostService.Create: %w", err)\n    }\n    return p, nil\n}\n\nfunc (s *PostService) GetByID(ctx context.Context, id int64) (*Post, error) {\n    p, err := s.repo.GetByID(ctx, id)\n    if err != nil {\n        return nil, fmt.Errorf("PostService.GetByID: %w", err)\n    }\n    return p, nil\n}\n\n// === internal/repository/memory/post_repo.go ===\n\ntype MemoryPostRepo struct {\n    mu      sync.RWMutex\n    posts   map[int64]*Post\n    counter int64\n}\n\nfunc NewMemoryPostRepo() *MemoryPostRepo {\n    return &MemoryPostRepo{posts: make(map[int64]*Post)}\n}\n\nfunc (r *MemoryPostRepo) Create(ctx context.Context, p *Post) error {\n    r.mu.Lock()\n    defer r.mu.Unlock()\n    r.counter++\n    p.ID = r.counter\n    cp := *p\n    r.posts[p.ID] = &cp\n    return nil\n}\n\nfunc (r *MemoryPostRepo) GetByID(ctx context.Context, id int64) (*Post, error) {\n    r.mu.RLock()\n    defer r.mu.RUnlock()\n    p, ok := r.posts[id]\n    if !ok {\n        return nil, ErrPostNotFound\n    }\n    cp := *p\n    return &cp, nil\n}\n\n// === internal/handler/post_handler.go ===\n\ntype PostHandler struct {\n    svc *PostService\n}\n\nfunc NewPostHandler(svc *PostService) *PostHandler {\n    return &PostHandler{svc: svc}\n}\n\nfunc (h *PostHandler) Create(w http.ResponseWriter, r *http.Request) {\n    var req struct {\n        Title    string `json:"title"`\n        Body     string `json:"body"`\n        AuthorID int64  `json:"author_id"`\n    }\n    if err := json.NewDecoder(r.Body).Decode(&req); err != nil {\n        http.Error(w, "bad request", http.StatusBadRequest)\n        return\n    }\n    p, err := h.svc.Create(r.Context(), req.Title, req.Body, req.AuthorID)\n    if err != nil {\n        if errors.Is(err, ErrEmptyTitle) {\n            http.Error(w, err.Error(), http.StatusBadRequest)\n            return\n        }\n        http.Error(w, "internal error", http.StatusInternalServerError)\n        return\n    }\n    w.Header().Set("Content-Type", "application/json")\n    w.WriteHeader(http.StatusCreated)\n    json.NewEncoder(w).Encode(p)\n}\n\nfunc (h *PostHandler) GetByID(w http.ResponseWriter, r *http.Request) {\n    parts := strings.Split(r.URL.Path, "/")\n    id, err := strconv.ParseInt(parts[len(parts)-1], 10, 64)\n    if err != nil {\n        http.Error(w, "bad id", http.StatusBadRequest)\n        return\n    }\n    p, err := h.svc.GetByID(r.Context(), id)\n    if err != nil {\n        if errors.Is(err, ErrPostNotFound) {\n            http.Error(w, "not found", http.StatusNotFound)\n            return\n        }\n        http.Error(w, "internal error", http.StatusInternalServerError)\n        return\n    }\n    w.Header().Set("Content-Type", "application/json")\n    json.NewEncoder(w).Encode(p)\n}\n\n// === cmd/api/main.go ===\n\nfunc main() {\n    cfg := LoadConfig()\n    fmt.Printf("Конфигурация загружена: addr=%s, log_level=%s\\n",\n        cfg.Addr, cfg.LogLevel)\n\n    // DI: Infrastructure -> Repository -> Service -> Handler\n    repo := NewMemoryPostRepo()\n    svc := NewPostService(repo)\n    h := NewPostHandler(svc)\n\n    fmt.Println("Сервис и хендлер инициализированы")\n\n    ctx := context.Background()\n\n    // Создаём тестовый пост\n    repo.Create(ctx, &Post{Title: "Привет Go", Body: "Первый пост", AuthorID: 1})\n\n    // Демо GET /posts/1\n    req1 := httptest.NewRequest("GET", "/posts/1", nil)\n    w1 := httptest.NewRecorder()\n    h.GetByID(w1, req1)\n    fmt.Printf("GET /posts/1 -> %d\\n", w1.Code)\n\n    // Демо POST /posts\n    body := `{"title":"Второй пост","body":"Контент","author_id":1}`\n    req2 := httptest.NewRequest("POST", "/posts", strings.NewReader(body))\n    w2 := httptest.NewRecorder()\n    h.Create(w2, req2)\n    fmt.Printf("POST /posts -> %d\\n", w2.Code)\n\n    // Демо GET /posts/999\n    req3 := httptest.NewRequest("GET", "/posts/999", nil)\n    w3 := httptest.NewRecorder()\n    h.GetByID(w3, req3)\n    fmt.Printf("GET /posts/999 -> %d\\n", w3.Code)\n}',
      explanation: 'Структура следует стандартному layout: domain (ни от чего не зависит), service (зависит от интерфейса репозитория), handler (зависит от конкретного сервиса через методы), repository (реализует интерфейс). Конфиг читается через env переменные с умолчаниями. main.go — единственное место, где всё собирается вместе. Каждый пакет имеет единственную ответственность.'
    }
  ]
}
