export default {
  id: 51,
  title: 'Работа с базами данных database/sql',
  description: 'Стандартный пакет database/sql: подключение к БД, выполнение запросов, сканирование строк, транзакции и пул соединений. Универсальный интерфейс для работы с любой реляционной базой данных.',
  lessons: [
    {
      id: 1,
      title: 'Пакет database/sql — обзор',
      type: 'theory',
      content: [
        { type: 'text', value: 'Пакет database/sql — стандартный интерфейс Go для работы с реляционными базами данных. Он похож на розетку: сама розетка стандартная, но вам нужен адаптер (драйвер) под конкретную БД — PostgreSQL, MySQL, SQLite.' },
        { type: 'heading', value: 'Архитектура database/sql' },
        { type: 'code', language: 'go', value: '// database/sql — только интерфейс, драйвер нужен отдельно\n//\n// ┌─────────────────────────────────────┐\n// │         Ваш код (Go)                │\n// ├─────────────────────────────────────┤\n// │        database/sql (stdlib)        │  <- стандартный пакет\n// ├─────────────────────────────────────┤\n// │  Драйвер (lib/pq, go-sqlite3, ...)  │  <- сторонняя библиотека\n// ├─────────────────────────────────────┤\n// │        База данных (PostgreSQL)     │\n// └─────────────────────────────────────┘\n\n// Популярные драйверы:\n// PostgreSQL:  github.com/lib/pq  или  github.com/jackc/pgx/v5\n// MySQL:       github.com/go-sql-driver/mysql\n// SQLite:      github.com/mattn/go-sqlite3\n\n// Установка драйвера для PostgreSQL:\n// go get github.com/lib/pq\n\npackage main\n\nimport (\n    "database/sql"\n    "fmt"\n    "log"\n\n    _ "github.com/lib/pq" // импорт только ради побочного эффекта — регистрации драйвера\n)\n\nfunc main() {\n    // sql.Open НЕ открывает соединение — только проверяет параметры\n    db, err := sql.Open("postgres", "host=localhost port=5432 user=postgres password=secret dbname=mydb sslmode=disable")\n    if err != nil {\n        log.Fatal("Ошибка конфигурации:", err)\n    }\n    defer db.Close()\n\n    // Ping реально проверяет соединение\n    if err := db.Ping(); err != nil {\n        log.Fatal("Нет соединения с БД:", err)\n    }\n\n    fmt.Println("Успешное подключение к PostgreSQL!")\n}' },
        { type: 'note', value: 'Импорт `_ "github.com/lib/pq"` — это импорт ради побочного эффекта. При загрузке пакета его функция init() регистрирует драйвер в database/sql под именем "postgres". Ваш код потом работает только через стандартный интерфейс.' },
        { type: 'heading', value: 'Типичная строка подключения' },
        { type: 'list', value: 'PostgreSQL: postgres://user:password@localhost:5432/dbname?sslmode=disable\nMySQL: user:password@tcp(127.0.0.1:3306)/dbname\nSQLite: /path/to/database.db' }
      ]
    },
    {
      id: 2,
      title: 'Open и Close — управление соединением',
      type: 'theory',
      content: [
        { type: 'text', value: 'sql.DB — это не одно соединение, а пул соединений. Думайте о нём как о таксопарке: у вас есть парк машин (соединений), и когда нужно выполнить запрос — диспетчер выдаёт свободную машину.' },
        { type: 'heading', value: 'Жизненный цикл *sql.DB' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "context"\n    "database/sql"\n    "fmt"\n    "log"\n    "time"\n\n    _ "github.com/lib/pq"\n)\n\nfunc connectDB() *sql.DB {\n    dsn := "host=localhost user=postgres password=secret dbname=mydb sslmode=disable"\n\n    db, err := sql.Open("postgres", dsn)\n    if err != nil {\n        log.Fatalf("sql.Open: %v", err)\n    }\n\n    // Настройка пула соединений\n    db.SetMaxOpenConns(25)               // максимум открытых соединений\n    db.SetMaxIdleConns(10)               // максимум простаивающих\n    db.SetConnMaxLifetime(5 * time.Minute) // время жизни соединения\n    db.SetConnMaxIdleTime(10 * time.Minute) // время простоя до закрытия\n\n    // Проверяем соединение с таймаутом\n    ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)\n    defer cancel()\n\n    if err := db.PingContext(ctx); err != nil {\n        log.Fatalf("Ping: %v", err)\n    }\n\n    fmt.Println("Пул соединений создан")\n    return db\n}\n\nfunc main() {\n    db := connectDB()\n    defer db.Close() // Close освобождает все соединения пула\n\n    // Статистика пула\n    stats := db.Stats()\n    fmt.Printf("Открыто: %d, В использовании: %d, Свободно: %d\\n",\n        stats.OpenConnections,\n        stats.InUse,\n        stats.Idle,\n    )\n}' },
        { type: 'warning', value: 'sql.DB создаётся один раз и разделяется между всеми горутинами. Никогда не создавайте новый *sql.DB для каждого запроса — это уничтожит всю пользу от пула соединений.' },
        { type: 'tip', value: 'SetMaxOpenConns обычно выставляют равным числу ядер сервера × 4, но для OLTP-нагрузок оптимум подбирается нагрузочным тестированием.' }
      ]
    },
    {
      id: 3,
      title: 'Query и QueryRow — чтение данных',
      type: 'theory',
      content: [
        { type: 'text', value: 'Для чтения данных из БД есть два метода: QueryRow для одной строки и Query для множества. Оба возвращают специальные объекты, из которых потом сканируются данные.' },
        { type: 'heading', value: 'QueryRow — одна строка' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "database/sql"\n    "fmt"\n    "log"\n\n    _ "github.com/lib/pq"\n)\n\ntype User struct {\n    ID    int\n    Name  string\n    Email string\n    Age   int\n}\n\nfunc getUserByID(db *sql.DB, id int) (*User, error) {\n    query := "SELECT id, name, email, age FROM users WHERE id = $1"\n\n    var u User\n    // QueryRow возвращает *sql.Row — всегда, даже если строки нет\n    // Ошибка проявится только при Scan()\n    err := db.QueryRow(query, id).Scan(&u.ID, &u.Name, &u.Email, &u.Age)\n    if err == sql.ErrNoRows {\n        return nil, fmt.Errorf("пользователь %d не найден", id)\n    }\n    if err != nil {\n        return nil, fmt.Errorf("queryRow: %w", err)\n    }\n    return &u, nil\n}\n\nfunc main() {\n    db, _ := sql.Open("postgres", "host=localhost user=postgres dbname=mydb sslmode=disable")\n    defer db.Close()\n\n    user, err := getUserByID(db, 1)\n    if err != nil {\n        log.Println(err)\n        return\n    }\n    fmt.Printf("Пользователь: %+v\\n", user)\n}' },
        { type: 'heading', value: 'Query — несколько строк' },
        { type: 'code', language: 'go', value: 'func getAllUsers(db *sql.DB) ([]*User, error) {\n    rows, err := db.Query("SELECT id, name, email, age FROM users ORDER BY name")\n    if err != nil {\n        return nil, fmt.Errorf("query: %w", err)\n    }\n    defer rows.Close() // ОБЯЗАТЕЛЬНО закрыть rows!\n\n    var users []*User\n    for rows.Next() { // итерируем по строкам\n        var u User\n        if err := rows.Scan(&u.ID, &u.Name, &u.Email, &u.Age); err != nil {\n            return nil, fmt.Errorf("scan: %w", err)\n        }\n        users = append(users, &u)\n    }\n\n    // Проверяем ошибку итерации (сеть, БД)\n    if err := rows.Err(); err != nil {\n        return nil, fmt.Errorf("rows.Err: %w", err)\n    }\n\n    return users, nil\n}' },
        { type: 'warning', value: 'Всегда вызывайте defer rows.Close() сразу после проверки ошибки от db.Query. Если забыть — соединение из пула не вернётся, и при достижении MaxOpenConns новые запросы будут зависать.' }
      ]
    },
    {
      id: 4,
      title: 'Exec — изменение данных',
      type: 'theory',
      content: [
        { type: 'text', value: 'Для INSERT, UPDATE, DELETE используется метод Exec. Он возвращает sql.Result, из которого можно получить ID вставленной строки или количество затронутых строк.' },
        { type: 'heading', value: 'Метод Exec' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "database/sql"\n    "fmt"\n    "log"\n    "time"\n\n    _ "github.com/lib/pq"\n)\n\nfunc createUser(db *sql.DB, name, email string, age int) (int64, error) {\n    // В PostgreSQL используем RETURNING для получения ID\n    var id int64\n    err := db.QueryRow(\n        "INSERT INTO users(name, email, age, created_at) VALUES($1, $2, $3, $4) RETURNING id",\n        name, email, age, time.Now(),\n    ).Scan(&id)\n    if err != nil {\n        return 0, fmt.Errorf("createUser: %w", err)\n    }\n    return id, nil\n}\n\nfunc updateUserName(db *sql.DB, id int, newName string) (int64, error) {\n    result, err := db.Exec(\n        "UPDATE users SET name = $1 WHERE id = $2",\n        newName, id,\n    )\n    if err != nil {\n        return 0, fmt.Errorf("updateUser: %w", err)\n    }\n\n    // Количество изменённых строк\n    affected, err := result.RowsAffected()\n    if err != nil {\n        return 0, err\n    }\n    return affected, nil\n}\n\nfunc deleteUser(db *sql.DB, id int) error {\n    result, err := db.Exec("DELETE FROM users WHERE id = $1", id)\n    if err != nil {\n        return fmt.Errorf("deleteUser: %w", err)\n    }\n\n    if n, _ := result.RowsAffected(); n == 0 {\n        return fmt.Errorf("пользователь %d не найден", id)\n    }\n    return nil\n}\n\nfunc main() {\n    db, _ := sql.Open("postgres", "host=localhost user=postgres dbname=mydb sslmode=disable")\n    defer db.Close()\n\n    id, err := createUser(db, "Нурдаулет", "n@example.com", 25)\n    if err != nil {\n        log.Fatal(err)\n    }\n    fmt.Printf("Создан пользователь ID=%d\\n", id)\n\n    affected, _ := updateUserName(db, int(id), "Нурдаулет Айдаров")\n    fmt.Printf("Изменено строк: %d\\n", affected)\n\n    if err := deleteUser(db, int(id)); err != nil {\n        log.Fatal(err)\n    }\n    fmt.Println("Пользователь удалён")\n}' },
        { type: 'note', value: 'В MySQL для получения ID вставленной строки используется result.LastInsertId(). В PostgreSQL этот метод не работает — вместо него используют RETURNING id в запросе и QueryRow вместо Exec.' }
      ]
    },
    {
      id: 5,
      title: 'Подготовленные запросы (Prepared Statements)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Подготовленный запрос — это как штамп для документов. Вы один раз создаёте штамп (отправляете SQL в БД), а потом много раз используете его, подставляя только данные. Это быстрее и безопаснее.' },
        { type: 'heading', value: 'Создание и использование Stmt' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "database/sql"\n    "fmt"\n    "log"\n\n    _ "github.com/lib/pq"\n)\n\ntype UserRepo struct {\n    db          *sql.DB\n    insertStmt  *sql.Stmt\n    getByIDStmt *sql.Stmt\n}\n\nfunc NewUserRepo(db *sql.DB) (*UserRepo, error) {\n    // Подготавливаем запросы один раз при старте\n    insertStmt, err := db.Prepare(\n        "INSERT INTO users(name, email) VALUES($1, $2) RETURNING id",\n    )\n    if err != nil {\n        return nil, fmt.Errorf("prepare insert: %w", err)\n    }\n\n    getByIDStmt, err := db.Prepare(\n        "SELECT id, name, email FROM users WHERE id = $1",\n    )\n    if err != nil {\n        insertStmt.Close()\n        return nil, fmt.Errorf("prepare getByID: %w", err)\n    }\n\n    return &UserRepo{\n        db:          db,\n        insertStmt:  insertStmt,\n        getByIDStmt: getByIDStmt,\n    }, nil\n}\n\nfunc (r *UserRepo) Close() {\n    r.insertStmt.Close()\n    r.getByIDStmt.Close()\n}\n\nfunc (r *UserRepo) Create(name, email string) (int64, error) {\n    var id int64\n    err := r.insertStmt.QueryRow(name, email).Scan(&id)\n    return id, err\n}\n\nfunc (r *UserRepo) GetByID(id int) (string, string, error) {\n    var userID int\n    var name, email string\n    err := r.getByIDStmt.QueryRow(id).Scan(&userID, &name, &email)\n    return name, email, err\n}\n\nfunc main() {\n    db, err := sql.Open("postgres", "host=localhost user=postgres dbname=mydb sslmode=disable")\n    if err != nil {\n        log.Fatal(err)\n    }\n    defer db.Close()\n\n    repo, err := NewUserRepo(db)\n    if err != nil {\n        log.Fatal(err)\n    }\n    defer repo.Close()\n\n    id, _ := repo.Create("Айгерим", "a@example.com")\n    fmt.Printf("Создан ID=%d\\n", id)\n\n    name, email, _ := repo.GetByID(int(id))\n    fmt.Printf("Найден: %s <%s>\\n", name, email)\n}' },
        { type: 'tip', value: 'Prepared statements автоматически защищают от SQL-инъекций — БД разделяет SQL-код и данные. Параметры $1, $2 (PostgreSQL) или ? (MySQL/SQLite) — никогда не подставляйте данные через fmt.Sprintf в SQL!' },
        { type: 'warning', value: 'Stmt привязан к конкретному соединению. Если соединение разорвётся, database/sql автоматически переподготовит запрос на другом соединении — вам не нужно беспокоиться об этом.' }
      ]
    },
    {
      id: 6,
      title: 'Сканирование строк — nullable типы',
      type: 'theory',
      content: [
        { type: 'text', value: 'В SQL есть NULL — отсутствие значения. В Go нет встроенного NULL, поэтому database/sql предоставляет специальные типы NullString, NullInt64 и другие для работы с возможно-отсутствующими значениями.' },
        { type: 'heading', value: 'sql.Null* типы' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "database/sql"\n    "fmt"\n    "log"\n\n    _ "github.com/lib/pq"\n)\n\ntype Profile struct {\n    ID       int\n    Name     string\n    Email    sql.NullString // может быть NULL\n    Age      sql.NullInt64  // может быть NULL\n    Bio      sql.NullString // может быть NULL\n}\n\nfunc getProfile(db *sql.DB, id int) (*Profile, error) {\n    query := `SELECT id, name, email, age, bio FROM profiles WHERE id = $1`\n\n    var p Profile\n    err := db.QueryRow(query, id).Scan(\n        &p.ID,\n        &p.Name,\n        &p.Email, // NullString сам обработает NULL\n        &p.Age,\n        &p.Bio,\n    )\n    if err != nil {\n        return nil, err\n    }\n    return &p, nil\n}\n\nfunc printProfile(p *Profile) {\n    fmt.Printf("ID: %d, Имя: %s\\n", p.ID, p.Name)\n\n    // NullString имеет поля Valid и String\n    if p.Email.Valid {\n        fmt.Printf("Email: %s\\n", p.Email.String)\n    } else {\n        fmt.Println("Email: не указан")\n    }\n\n    if p.Age.Valid {\n        fmt.Printf("Возраст: %d\\n", p.Age.Int64)\n    } else {\n        fmt.Println("Возраст: неизвестен")\n    }\n}\n\n// Вставка с NULL\nfunc createProfile(db *sql.DB, name string, email *string) error {\n    var emailVal sql.NullString\n    if email != nil {\n        emailVal = sql.NullString{String: *email, Valid: true}\n    }\n    // emailVal.Valid == false -> вставится NULL\n\n    _, err := db.Exec(\n        "INSERT INTO profiles(name, email) VALUES($1, $2)",\n        name, emailVal,\n    )\n    return err\n}\n\n// Современный подход: указатели вместо sql.Null*\ntype ProfilePtr struct {\n    ID    int\n    Name  string\n    Email *string // nil == NULL\n    Age   *int    // nil == NULL\n}\n\nfunc getProfilePtr(db *sql.DB, id int) (*ProfilePtr, error) {\n    var p ProfilePtr\n    err := db.QueryRow(\n        "SELECT id, name, email, age FROM profiles WHERE id = $1", id,\n    ).Scan(&p.ID, &p.Name, &p.Email, &p.Age)\n    return &p, err\n}\n\nfunc main() {\n    db, _ := sql.Open("postgres", "host=localhost user=postgres dbname=mydb sslmode=disable")\n    defer db.Close()\n\n    p, err := getProfile(db, 1)\n    if err != nil {\n        log.Fatal(err)\n    }\n    printProfile(p)\n}' },
        { type: 'note', value: 'Начиная с Go 1.22 можно сканировать прямо в указатель (*string, *int) — nil будет соответствовать NULL. Это чище, чем использование sql.NullString.' }
      ]
    },
    {
      id: 7,
      title: 'Транзакции',
      type: 'theory',
      content: [
        { type: 'text', value: 'Транзакция — это атомарная единица работы. Как банковский перевод: либо деньги ушли со счёта И пришли на другой, либо ничего не произошло. Нельзя снять деньги, но не зачислить.' },
        { type: 'heading', value: 'db.Begin и commit/rollback' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "context"\n    "database/sql"\n    "fmt"\n    "log"\n\n    _ "github.com/lib/pq"\n)\n\n// transferMoney переводит деньги между счетами атомарно\nfunc transferMoney(db *sql.DB, fromID, toID int, amount float64) error {\n    // Начинаем транзакцию\n    tx, err := db.Begin()\n    if err != nil {\n        return fmt.Errorf("begin: %w", err)\n    }\n    // defer: если функция завершится без Commit — автоматический откат\n    defer tx.Rollback()\n\n    // Снимаем со счёта отправителя\n    result, err := tx.Exec(\n        "UPDATE accounts SET balance = balance - $1 WHERE id = $2 AND balance >= $1",\n        amount, fromID,\n    )\n    if err != nil {\n        return fmt.Errorf("debit: %w", err)\n    }\n    if n, _ := result.RowsAffected(); n == 0 {\n        return fmt.Errorf("недостаточно средств на счёте %d", fromID)\n    }\n\n    // Зачисляем на счёт получателя\n    _, err = tx.Exec(\n        "UPDATE accounts SET balance = balance + $1 WHERE id = $2",\n        amount, toID,\n    )\n    if err != nil {\n        return fmt.Errorf("credit: %w", err)\n    }\n\n    // Записываем в лог переводов\n    _, err = tx.Exec(\n        "INSERT INTO transfers(from_id, to_id, amount) VALUES($1, $2, $3)",\n        fromID, toID, amount,\n    )\n    if err != nil {\n        return fmt.Errorf("log transfer: %w", err)\n    }\n\n    // Фиксируем транзакцию\n    return tx.Commit()\n}\n\n// Транзакция с контекстом (поддержка отмены)\nfunc transferWithCtx(ctx context.Context, db *sql.DB, fromID, toID int, amount float64) error {\n    tx, err := db.BeginTx(ctx, &sql.TxOptions{\n        Isolation: sql.LevelSerializable, // уровень изоляции\n        ReadOnly:  false,\n    })\n    if err != nil {\n        return err\n    }\n    defer tx.Rollback()\n\n    // Все запросы в транзакции используют ctx\n    _, err = tx.ExecContext(ctx,\n        "UPDATE accounts SET balance = balance - $1 WHERE id = $2",\n        amount, fromID,\n    )\n    if err != nil {\n        return err\n    }\n\n    _, err = tx.ExecContext(ctx,\n        "UPDATE accounts SET balance = balance + $1 WHERE id = $2",\n        amount, toID,\n    )\n    if err != nil {\n        return err\n    }\n\n    return tx.Commit()\n}\n\nfunc main() {\n    db, _ := sql.Open("postgres", "host=localhost user=postgres dbname=mydb sslmode=disable")\n    defer db.Close()\n\n    err := transferMoney(db, 1, 2, 1000.0)\n    if err != nil {\n        log.Printf("Ошибка перевода: %v", err)\n        return\n    }\n    fmt.Println("Перевод выполнен успешно")\n}' },
        { type: 'tip', value: 'Паттерн defer tx.Rollback() безопасен: если Commit был вызван успешно, последующий Rollback вернёт ошибку "транзакция уже завершена", которую можно проигнорировать. Этот паттерн гарантирует откат при панике или ранних return.' }
      ]
    },
    {
      id: 8,
      title: 'Практика: система хранения задач',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуй систему хранения задач (todo list) с использованием database/sql и SQLite. Поддержи полный CRUD и транзакционное удаление проекта.',
      requirements: [
        'Используй SQLite: go get github.com/mattn/go-sqlite3',
        'Структура Task: ID int64, Title string, Done bool, ProjectID int64',
        'Функция initDB: создаёт таблицы projects и tasks',
        'CreateProject(name string) (int64, error)',
        'CreateTask(projectID int64, title string) (int64, error)',
        'ListTasks(projectID int64) ([]Task, error)',
        'CompleteTask(taskID int64) error — устанавливает done=true',
        'DeleteProject(projectID int64) error — транзакционно удаляет задачи и проект',
        'main: создай проект, добавь 3 задачи, выполни 1, выведи список, удали проект'
      ],
      expectedOutput: 'Проект "Мой проект" создан (ID=1)\nЗадачи:\n  [_] Задача 1\n  [x] Задача 2\n  [_] Задача 3\nПроект удалён (удалено 3 задач)',
      hint: 'Для SQLite используй driver "sqlite3" и DSN "file:tasks.db?cache=shared". В транзакции сначала удали задачи (DELETE FROM tasks WHERE project_id=$1), потом проект. RowsAffected покажет сколько задач удалено.',
      solution: 'package main\n\nimport (\n    "database/sql"\n    "fmt"\n    "log"\n\n    _ "github.com/mattn/go-sqlite3"\n)\n\ntype Task struct {\n    ID        int64\n    Title     string\n    Done      bool\n    ProjectID int64\n}\n\nfunc initDB(db *sql.DB) error {\n    _, err := db.Exec(`\n        CREATE TABLE IF NOT EXISTS projects (\n            id   INTEGER PRIMARY KEY AUTOINCREMENT,\n            name TEXT NOT NULL\n        );\n        CREATE TABLE IF NOT EXISTS tasks (\n            id         INTEGER PRIMARY KEY AUTOINCREMENT,\n            project_id INTEGER NOT NULL REFERENCES projects(id),\n            title      TEXT NOT NULL,\n            done       BOOLEAN NOT NULL DEFAULT 0\n        );\n    `)\n    return err\n}\n\nfunc createProject(db *sql.DB, name string) (int64, error) {\n    res, err := db.Exec("INSERT INTO projects(name) VALUES(?)", name)\n    if err != nil {\n        return 0, err\n    }\n    return res.LastInsertId()\n}\n\nfunc createTask(db *sql.DB, projectID int64, title string) (int64, error) {\n    res, err := db.Exec("INSERT INTO tasks(project_id, title) VALUES(?, ?)", projectID, title)\n    if err != nil {\n        return 0, err\n    }\n    return res.LastInsertId()\n}\n\nfunc listTasks(db *sql.DB, projectID int64) ([]Task, error) {\n    rows, err := db.Query("SELECT id, title, done, project_id FROM tasks WHERE project_id = ?", projectID)\n    if err != nil {\n        return nil, err\n    }\n    defer rows.Close()\n\n    var tasks []Task\n    for rows.Next() {\n        var t Task\n        if err := rows.Scan(&t.ID, &t.Title, &t.Done, &t.ProjectID); err != nil {\n            return nil, err\n        }\n        tasks = append(tasks, t)\n    }\n    return tasks, rows.Err()\n}\n\nfunc completeTask(db *sql.DB, taskID int64) error {\n    _, err := db.Exec("UPDATE tasks SET done = 1 WHERE id = ?", taskID)\n    return err\n}\n\nfunc deleteProject(db *sql.DB, projectID int64) (int64, error) {\n    tx, err := db.Begin()\n    if err != nil {\n        return 0, err\n    }\n    defer tx.Rollback()\n\n    res, err := tx.Exec("DELETE FROM tasks WHERE project_id = ?", projectID)\n    if err != nil {\n        return 0, err\n    }\n    deleted, _ := res.RowsAffected()\n\n    _, err = tx.Exec("DELETE FROM projects WHERE id = ?", projectID)\n    if err != nil {\n        return 0, err\n    }\n\n    return deleted, tx.Commit()\n}\n\nfunc main() {\n    db, err := sql.Open("sqlite3", "file:tasks.db?cache=shared")\n    if err != nil {\n        log.Fatal(err)\n    }\n    defer db.Close()\n\n    if err := initDB(db); err != nil {\n        log.Fatal("initDB:", err)\n    }\n\n    projID, err := createProject(db, "Мой проект")\n    if err != nil {\n        log.Fatal(err)\n    }\n    fmt.Printf("Проект \\"Мой проект\\" создан (ID=%d)\\n", projID)\n\n    t1, _ := createTask(db, projID, "Задача 1")\n    t2, _ := createTask(db, projID, "Задача 2")\n    createTask(db, projID, "Задача 3")\n\n    completeTask(db, t2)\n    _ = t1\n\n    tasks, _ := listTasks(db, projID)\n    fmt.Println("Задачи:")\n    for _, t := range tasks {\n        mark := "_"\n        if t.Done {\n            mark = "x"\n        }\n        fmt.Printf("  [%s] %s\\n", mark, t.Title)\n    }\n\n    deleted, _ := deleteProject(db, projID)\n    fmt.Printf("Проект удалён (удалено %d задач)\\n", deleted)\n}',
      explanation: 'database/sql предоставляет единый интерфейс для любой БД. SQLite удобен для разработки — не требует отдельного сервера. Транзакция гарантирует, что удаление задач и проекта либо произойдёт целиком, либо не произойдёт вообще. rows.Close() и rows.Err() — обязательные шаги при работе с множеством строк.'
    }
  ]
}
