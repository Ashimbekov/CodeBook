export default {
  id: 85,
  title: 'Паттерн Strategy',
  description: 'Паттерн Стратегия в Go: стратегии через интерфейсы, платёжные стратегии, стратегии сортировки, выбор стратегии в рантайме и комбинация с фабрикой.',
  lessons: [
    {
      id: 1,
      title: 'Strategy через интерфейсы',
      type: 'theory',
      content: [
        { type: 'text', value: 'Паттерн Strategy определяет семейство алгоритмов, инкапсулирует каждый из них и делает их взаимозаменяемыми. В Go стратегия реализуется через интерфейс или функциональный тип.' },
        { type: 'heading', value: 'Базовый паттерн Strategy' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "math"\n)\n\n// Strategy — интерфейс алгоритма сжатия\ntype Compressor interface {\n    Compress(data []byte) ([]byte, error)\n    Decompress(data []byte) ([]byte, error)\n    Name() string\n}\n\n// Конкретные стратегии\ntype NoCompression struct{}\n\nfunc (c *NoCompression) Compress(data []byte) ([]byte, error) {\n    return data, nil\n}\nfunc (c *NoCompression) Decompress(data []byte) ([]byte, error) {\n    return data, nil\n}\nfunc (c *NoCompression) Name() string { return "none" }\n\ntype GzipCompression struct{ level int }\n\nfunc (c *GzipCompression) Compress(data []byte) ([]byte, error) {\n    // Симуляция: уменьшаем размер на 60%\n    compressed := data[:int(float64(len(data))*0.4)+1]\n    return compressed, nil\n}\nfunc (c *GzipCompression) Decompress(data []byte) ([]byte, error) {\n    return data, nil\n}\nfunc (c *GzipCompression) Name() string { return fmt.Sprintf("gzip(level=%d)", c.level) }\n\ntype ZstdCompression struct{ level int }\n\nfunc (c *ZstdCompression) Compress(data []byte) ([]byte, error) {\n    // Симуляция: уменьшаем размер на 70%\n    size := int(math.Max(1, float64(len(data))*0.3))\n    compressed := data[:size]\n    return compressed, nil\n}\nfunc (c *ZstdCompression) Decompress(data []byte) ([]byte, error) {\n    return data, nil\n}\nfunc (c *ZstdCompression) Name() string { return fmt.Sprintf("zstd(level=%d)", c.level) }\n\n// Context — использует стратегию\ntype FileStorage struct {\n    strategy Compressor\n}\n\nfunc NewFileStorage(strategy Compressor) *FileStorage {\n    return &FileStorage{strategy: strategy}\n}\n\n// SetStrategy — смена стратегии в рантайме\nfunc (fs *FileStorage) SetStrategy(s Compressor) {\n    fs.strategy = s\n}\n\nfunc (fs *FileStorage) Save(filename string, data []byte) {\n    compressed, err := fs.strategy.Compress(data)\n    if err != nil {\n        fmt.Printf("Ошибка сжатия: %v\\n", err)\n        return\n    }\n    ratio := float64(len(compressed)) / float64(len(data)) * 100\n    fmt.Printf("Сохранение %q: алгоритм=%s, размер=%d->%d байт (%.0f%%)\\n",\n        filename, fs.strategy.Name(), len(data), len(compressed), ratio)\n}\n\nfunc main() {\n    data := []byte("Lorem ipsum dolor sit amet, consectetur adipiscing elit. " +\n        "Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.")\n\n    // Стратегия без сжатия\n    storage := NewFileStorage(&NoCompression{})\n    storage.Save("readme.txt", data)\n\n    // Смена стратегии\n    storage.SetStrategy(&GzipCompression{level: 6})\n    storage.Save("readme.txt.gz", data)\n\n    storage.SetStrategy(&ZstdCompression{level: 3})\n    storage.Save("readme.txt.zst", data)\n}' },
        { type: 'note', value: 'SetStrategy позволяет менять алгоритм во время работы — это ключевая фишка паттерна Strategy. Context (FileStorage) не знает о конкретных алгоритмах, только об интерфейсе.' }
      ]
    },
    {
      id: 2,
      title: 'Платёжные стратегии',
      type: 'theory',
      content: [
        { type: 'text', value: 'Классический пример Strategy — платёжные системы. Каждый способ оплаты инкапсулирует свою логику, корзина использует любую стратегию через единый интерфейс.' },
        { type: 'heading', value: 'Платёжные стратегии для корзины' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "errors"\n    "fmt"\n    "time"\n)\n\n// PaymentStrategy — стратегия оплаты\ntype PaymentStrategy interface {\n    Pay(amount float64) error\n    ValidateDetails() error\n    MethodName() string\n}\n\n// CreditCard стратегия\ntype CreditCard struct {\n    number   string\n    expiry   string\n    cvv      string\n    holder   string\n}\n\nfunc NewCreditCard(number, expiry, cvv, holder string) *CreditCard {\n    return &CreditCard{number, expiry, cvv, holder}\n}\n\nfunc (c *CreditCard) ValidateDetails() error {\n    if len(c.number) < 16 {\n        return errors.New("неверный номер карты")\n    }\n    if c.cvv == "" {\n        return errors.New("CVV обязателен")\n    }\n    return nil\n}\n\nfunc (c *CreditCard) Pay(amount float64) error {\n    if err := c.ValidateDetails(); err != nil {\n        return err\n    }\n    maskedNum := "****" + c.number[len(c.number)-4:]\n    fmt.Printf("[CreditCard] Снято %.2f с карты %s (держатель: %s)\\n",\n        amount, maskedNum, c.holder)\n    return nil\n}\n\nfunc (c *CreditCard) MethodName() string { return "credit_card" }\n\n// PayPal стратегия\ntype PayPal struct {\n    email    string\n    password string\n}\n\nfunc (p *PayPal) ValidateDetails() error {\n    if p.email == "" || p.password == "" {\n        return errors.New("email и пароль PayPal обязательны")\n    }\n    return nil\n}\n\nfunc (p *PayPal) Pay(amount float64) error {\n    if err := p.ValidateDetails(); err != nil {\n        return err\n    }\n    fmt.Printf("[PayPal] Оплата %.2f через аккаунт %s\\n", amount, p.email)\n    return nil\n}\n\nfunc (p *PayPal) MethodName() string { return "paypal" }\n\n// Crypto стратегия\ntype CryptoWallet struct {\n    address string\n    coin    string\n}\n\nfunc (cw *CryptoWallet) ValidateDetails() error {\n    if len(cw.address) < 10 {\n        return errors.New("неверный адрес кошелька")\n    }\n    return nil\n}\n\nfunc (cw *CryptoWallet) Pay(amount float64) error {\n    if err := cw.ValidateDetails(); err != nil {\n        return err\n    }\n    fmt.Printf("[Crypto] Отправлено %.6f %s на адрес %s\\n", amount, cw.coin, cw.address)\n    return nil\n}\n\nfunc (cw *CryptoWallet) MethodName() string { return "crypto_" + cw.coin }\n\n// ShoppingCart — контекст, использующий стратегию\ntype ShoppingCart struct {\n    items   []CartItem\n    payment PaymentStrategy\n}\n\ntype CartItem struct {\n    Name     string\n    Price    float64\n    Quantity int\n}\n\nfunc (c *ShoppingCart) AddItem(item CartItem) {\n    c.items = append(c.items, item)\n}\n\nfunc (c *ShoppingCart) SetPayment(p PaymentStrategy) {\n    c.payment = p\n}\n\nfunc (c *ShoppingCart) Total() float64 {\n    var total float64\n    for _, item := range c.items {\n        total += item.Price * float64(item.Quantity)\n    }\n    return total\n}\n\nfunc (c *ShoppingCart) Checkout() error {\n    if c.payment == nil {\n        return errors.New("способ оплаты не выбран")\n    }\n    total := c.Total()\n    fmt.Printf("Корзина: %d позиций на %.2f руб\\n", len(c.items), total)\n    fmt.Printf("Способ оплаты: %s\\n", c.payment.MethodName())\n    return c.payment.Pay(total)\n}\n\nfunc main() {\n    cart := &ShoppingCart{}\n    cart.AddItem(CartItem{Name: "Go Programming", Price: 2500, Quantity: 1})\n    cart.AddItem(CartItem{Name: "Кофе", Price: 1200, Quantity: 2})\n\n    fmt.Printf("Итого: %.2f руб\\n\\n", cart.Total())\n\n    // Оплата картой\n    cart.SetPayment(NewCreditCard("1234567890123456", "12/26", "123", "Нурдаулет С."))\n    if err := cart.Checkout(); err != nil {\n        fmt.Println("Ошибка:", err)\n    }\n\n    fmt.Println()\n    // Смена стратегии на PayPal\n    cart.SetPayment(&PayPal{email: "user@example.com", password: "pass"})\n    if err := cart.Checkout(); err != nil {\n        fmt.Println("Ошибка:", err)\n    }\n\n    _ = time.Now() // suppress import warning in simplified version\n}' },
        { type: 'tip', value: 'ShoppingCart не содержит никакого if/switch для выбора метода оплаты — весь выбор снаружи через SetPayment(). Это и есть суть паттерна Strategy: замена алгоритма без изменения контекста.' }
      ]
    },
    {
      id: 3,
      title: 'Стратегии сортировки',
      type: 'theory',
      content: [
        { type: 'text', value: 'Стратегии сортировки — учебный классик. В Go стандартная библиотека sort.Interface сама является паттерном Strategy. Рассмотрим более явную реализацию.' },
        { type: 'heading', value: 'Явные стратегии сортировки' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "sort"\n)\n\n// SortStrategy — стратегия сортировки\ntype SortStrategy[T any] interface {\n    Sort(data []T)\n    Name() string\n}\n\n// BubbleSort стратегия\ntype BubbleSort[T any] struct {\n    less func(a, b T) bool\n}\n\nfunc NewBubbleSort[T any](less func(a, b T) bool) *BubbleSort[T] {\n    return &BubbleSort[T]{less: less}\n}\n\nfunc (s *BubbleSort[T]) Sort(data []T) {\n    n := len(data)\n    for i := 0; i < n-1; i++ {\n        for j := 0; j < n-1-i; j++ {\n            if !s.less(data[j], data[j+1]) {\n                data[j], data[j+1] = data[j+1], data[j]\n            }\n        }\n    }\n}\nfunc (s *BubbleSort[T]) Name() string { return "BubbleSort" }\n\n// QuickSort стратегия (через stdlib)\ntype StdSort[T any] struct {\n    less func(a, b T) bool\n}\n\nfunc NewStdSort[T any](less func(a, b T) bool) *StdSort[T] {\n    return &StdSort[T]{less: less}\n}\n\nfunc (s *StdSort[T]) Sort(data []T) {\n    sort.Slice(data, func(i, j int) bool {\n        return s.less(data[i], data[j])\n    })\n}\nfunc (s *StdSort[T]) Name() string { return "StdSort (introsort)" }\n\n// Sorter — контекст\ntype Sorter[T any] struct {\n    strategy SortStrategy[T]\n}\n\nfunc (s *Sorter[T]) SetStrategy(strategy SortStrategy[T]) {\n    s.strategy = strategy\n}\n\nfunc (s *Sorter[T]) Sort(data []T) {\n    fmt.Printf("Сортировка %d элементов алгоритмом %s...\\n", len(data), s.strategy.Name())\n    s.strategy.Sort(data)\n}\n\ntype Employee struct {\n    Name   string\n    Salary float64\n    Age    int\n}\n\nfunc main() {\n    employees := []Employee{\n        {Name: "Айгерим", Salary: 250000, Age: 28},\n        {Name: "Нурдаулет", Salary: 350000, Age: 32},\n        {Name: "Болат", Salary: 180000, Age: 25},\n        {Name: "Зарина", Salary: 420000, Age: 35},\n    }\n\n    sorter := &Sorter[Employee]{}\n\n    // Сортировка по зарплате\n    bySalary := make([]Employee, len(employees))\n    copy(bySalary, employees)\n    sorter.SetStrategy(NewStdSort(func(a, b Employee) bool {\n        return a.Salary < b.Salary\n    }))\n    sorter.Sort(bySalary)\n    fmt.Println("По зарплате:")\n    for _, e := range bySalary {\n        fmt.Printf("  %s: %.0f руб\\n", e.Name, e.Salary)\n    }\n\n    // Сортировка по возрасту (bubble sort)\n    byAge := make([]Employee, len(employees))\n    copy(byAge, employees)\n    sorter.SetStrategy(NewBubbleSort(func(a, b Employee) bool {\n        return a.Age < b.Age\n    }))\n    sorter.Sort(byAge)\n    fmt.Println("По возрасту:")\n    for _, e := range byAge {\n        fmt.Printf("  %s: %d лет\\n", e.Name, e.Age)\n    }\n\n    // Сортировка по имени\n    byName := make([]Employee, len(employees))\n    copy(byName, employees)\n    sorter.SetStrategy(NewStdSort(func(a, b Employee) bool {\n        return a.Name < b.Name\n    }))\n    sorter.Sort(byName)\n    fmt.Println("По имени:")\n    for _, e := range byName {\n        fmt.Printf("  %s\\n", e.Name)\n    }\n}' },
        { type: 'note', value: 'Go-дженерики делают Strategy более типобезопасной: SortStrategy[Employee] нельзя перепутать с SortStrategy[int]. Функция less передаётся в конструктор стратегии, а не в метод Sort — это удобно для повторного использования.' }
      ]
    },
    {
      id: 4,
      title: 'Выбор стратегии в рантайме',
      type: 'theory',
      content: [
        { type: 'text', value: 'Стратегия может выбираться динамически — на основе условий рантайма, пользовательских настроек или результатов предыдущих вычислений.' },
        { type: 'heading', value: 'Динамический выбор стратегии кэширования' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "time"\n)\n\n// CacheStrategy — стратегия кэширования\ntype CacheStrategy interface {\n    Get(key string) (interface{}, bool)\n    Set(key string, value interface{}, ttl time.Duration)\n    Evict(key string)\n    Name() string\n    Size() int\n}\n\n// LRU (симуляция)\ntype LRUCache struct {\n    capacity int\n    data     map[string]interface{}\n    order    []string\n}\n\nfunc NewLRU(capacity int) *LRUCache {\n    return &LRUCache{capacity: capacity, data: make(map[string]interface{})}\n}\n\nfunc (c *LRUCache) Get(key string) (interface{}, bool) {\n    v, ok := c.data[key]\n    return v, ok\n}\n\nfunc (c *LRUCache) Set(key string, value interface{}, ttl time.Duration) {\n    if len(c.data) >= c.capacity && len(c.order) > 0 {\n        oldest := c.order[0]\n        c.order = c.order[1:]\n        delete(c.data, oldest)\n    }\n    c.data[key] = value\n    c.order = append(c.order, key)\n}\n\nfunc (c *LRUCache) Evict(key string) { delete(c.data, key) }\nfunc (c *LRUCache) Name() string     { return fmt.Sprintf("LRU(cap=%d)", c.capacity) }\nfunc (c *LRUCache) Size() int        { return len(c.data) }\n\n// TTL Cache\ntype TTLCache struct {\n    data    map[string]ttlEntry\n    maxSize int\n}\n\ntype ttlEntry struct {\n    value     interface{}\n    expiresAt time.Time\n}\n\nfunc NewTTLCache(maxSize int) *TTLCache {\n    return &TTLCache{data: make(map[string]ttlEntry), maxSize: maxSize}\n}\n\nfunc (c *TTLCache) Get(key string) (interface{}, bool) {\n    e, ok := c.data[key]\n    if !ok || time.Now().After(e.expiresAt) {\n        return nil, false\n    }\n    return e.value, true\n}\n\nfunc (c *TTLCache) Set(key string, value interface{}, ttl time.Duration) {\n    c.data[key] = ttlEntry{value: value, expiresAt: time.Now().Add(ttl)}\n}\nfunc (c *TTLCache) Evict(key string) { delete(c.data, key) }\nfunc (c *TTLCache) Name() string     { return fmt.Sprintf("TTL(max=%d)", c.maxSize) }\nfunc (c *TTLCache) Size() int        { return len(c.data) }\n\n// SmartCacheManager — выбирает стратегию по условиям\ntype CacheProfile string\n\nconst (\n    ProfileMemoryOptimized CacheProfile = "memory"\n    ProfileSpeedOptimized  CacheProfile = "speed"\n    ProfileTTL             CacheProfile = "ttl"\n)\n\nfunc ChooseCacheStrategy(profile CacheProfile, dataSize int) CacheStrategy {\n    switch profile {\n    case ProfileMemoryOptimized:\n        // Маленький кэш для экономии памяти\n        capacity := dataSize / 10\n        if capacity < 10 {\n            capacity = 10\n        }\n        return NewLRU(capacity)\n    case ProfileSpeedOptimized:\n        // Большой кэш для скорости\n        return NewLRU(dataSize * 2)\n    case ProfileTTL:\n        return NewTTLCache(dataSize)\n    default:\n        return NewLRU(100)\n    }\n}\n\nfunc main() {\n    profiles := []CacheProfile{ProfileMemoryOptimized, ProfileSpeedOptimized, ProfileTTL}\n    dataSize := 1000\n\n    for _, profile := range profiles {\n        strategy := ChooseCacheStrategy(profile, dataSize)\n        fmt.Printf("Профиль %q -> стратегия: %s\\n", profile, strategy.Name())\n\n        // Используем стратегию\n        strategy.Set("key1", "value1", 5*time.Minute)\n        strategy.Set("key2", 42, time.Hour)\n\n        if v, ok := strategy.Get("key1"); ok {\n            fmt.Printf("  key1 = %v\\n", v)\n        }\n        fmt.Printf("  Размер кэша: %d\\n", strategy.Size())\n    }\n}' },
        { type: 'tip', value: 'ChooseCacheStrategy — фабрика стратегий. Это демонстрирует симбиоз паттернов Factory и Strategy: фабрика выбирает стратегию, стратегия применяется в контексте.' }
      ]
    },
    {
      id: 5,
      title: 'Strategy + Factory: комбинация паттернов',
      type: 'theory',
      content: [
        { type: 'text', value: 'Factory и Strategy отлично дополняют друг друга. Factory создаёт нужную стратегию на основе конфигурации, Strategy инкапсулирует алгоритм.' },
        { type: 'heading', value: 'Валидатор с configurable стратегиями' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "regexp"\n    "strings"\n)\n\n// ValidationStrategy — стратегия валидации\ntype ValidationStrategy interface {\n    Validate(value string) error\n    Description() string\n}\n\n// Конкретные стратегии\ntype MinLengthStrategy struct{ min int }\n\nfunc (s *MinLengthStrategy) Validate(v string) error {\n    if len([]rune(v)) < s.min {\n        return fmt.Errorf("минимальная длина %d символов", s.min)\n    }\n    return nil\n}\nfunc (s *MinLengthStrategy) Description() string {\n    return fmt.Sprintf("мин. длина %d", s.min)\n}\n\ntype MaxLengthStrategy struct{ max int }\n\nfunc (s *MaxLengthStrategy) Validate(v string) error {\n    if len([]rune(v)) > s.max {\n        return fmt.Errorf("максимальная длина %d символов", s.max)\n    }\n    return nil\n}\nfunc (s *MaxLengthStrategy) Description() string {\n    return fmt.Sprintf("макс. длина %d", s.max)\n}\n\ntype RegexStrategy struct {\n    pattern string\n    re      *regexp.Regexp\n    desc    string\n}\n\nfunc NewRegexStrategy(pattern, desc string) *RegexStrategy {\n    return &RegexStrategy{pattern: pattern, re: regexp.MustCompile(pattern), desc: desc}\n}\n\nfunc (s *RegexStrategy) Validate(v string) error {\n    if !s.re.MatchString(v) {\n        return fmt.Errorf("не соответствует формату: %s", s.desc)\n    }\n    return nil\n}\nfunc (s *RegexStrategy) Description() string { return s.desc }\n\ntype NoSpacesStrategy struct{}\n\nfunc (s *NoSpacesStrategy) Validate(v string) error {\n    if strings.ContainsAny(v, " \\t\\n") {\n        return fmt.Errorf("не должно содержать пробелы")\n    }\n    return nil\n}\nfunc (s *NoSpacesStrategy) Description() string { return "без пробелов" }\n\n// CompositeValidator — применяет все стратегии\ntype CompositeValidator struct {\n    name       string\n    strategies []ValidationStrategy\n}\n\nfunc (cv *CompositeValidator) Validate(value string) []error {\n    var errs []error\n    for _, s := range cv.strategies {\n        if err := s.Validate(value); err != nil {\n            errs = append(errs, err)\n        }\n    }\n    return errs\n}\n\n// ValidatorFactory — создаёт валидаторы по типу поля\nfunc NewValidatorForField(fieldType string) *CompositeValidator {\n    v := &CompositeValidator{name: fieldType}\n\n    switch fieldType {\n    case "username":\n        v.strategies = []ValidationStrategy{\n            &MinLengthStrategy{min: 3},\n            &MaxLengthStrategy{max: 20},\n            &NoSpacesStrategy{},\n            NewRegexStrategy(`^[a-zA-Z0-9_]+$`, "только буквы, цифры и _"),\n        }\n    case "email":\n        v.strategies = []ValidationStrategy{\n            &MinLengthStrategy{min: 5},\n            &MaxLengthStrategy{max: 100},\n            NewRegexStrategy(`^[^@]+@[^@]+\\.[^@]+$`, "формат email"),\n        }\n    case "password":\n        v.strategies = []ValidationStrategy{\n            &MinLengthStrategy{min: 8},\n            &NoSpacesStrategy{},\n            NewRegexStrategy(`[A-Z]`, "хотя бы одна заглавная буква"),\n            NewRegexStrategy(`[0-9]`, "хотя бы одна цифра"),\n        }\n    default:\n        v.strategies = []ValidationStrategy{&MinLengthStrategy{min: 1}}\n    }\n\n    return v\n}\n\nfunc validate(field, value string) {\n    v := NewValidatorForField(field)\n    errs := v.Validate(value)\n    if len(errs) == 0 {\n        fmt.Printf("%s=%q: OK\\n", field, value)\n    } else {\n        fmt.Printf("%s=%q: ОШИБКИ\\n", field, value)\n        for _, e := range errs {\n            fmt.Printf("  - %v\\n", e)\n        }\n    }\n}\n\nfunc main() {\n    validate("username", "ok")\n    validate("username", "valid_user_123")\n    validate("username", "invalid user!")\n    fmt.Println()\n    validate("email", "not-email")\n    validate("email", "user@example.com")\n    fmt.Println()\n    validate("password", "weak")\n    validate("password", "StrongPass1")\n}' },
        { type: 'note', value: 'CompositeValidator применяет все стратегии и собирает все ошибки. Это Composite + Strategy. ValidatorFactory создаёт нужный набор стратегий — это Factory + Strategy. Три паттерна в одном коде!' }
      ]
    },
    {
      id: 6,
      title: 'Практика: стратегии экспорта данных',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй систему экспорта данных с несколькими стратегиями: JSON, CSV и plain text. Контекст DataExporter использует стратегию через интерфейс.',
      requirements: [
        'Интерфейс ExportStrategy: Export(records []Record) ([]byte, error), ContentType() string, Extension() string',
        'Структура Record: ID int, Name string, Value float64, Active bool',
        'JSONExporter: сериализует в JSON с отступами',
        'CSVExporter: первая строка — заголовки, далее данные через запятую',
        'TextExporter: форматированная таблица с выравниванием',
        'DataExporter с SetStrategy и Export методами',
        'В main экспортировать 5 записей всеми тремя стратегиями, вывести результат'
      ],
      expectedOutput: '--- JSON (application/json, .json) ---\n[\n  {\n    "id": 1,\n    ...\n  }\n]\n--- CSV (text/csv, .csv) ---\nID,Name,Value,Active\n1,Alice,100.50,true\n--- TEXT (text/plain, .txt) ---\nID  | Name  | Value  | Active',
      hint: 'Для CSV используй encoding/csv пакет с csv.NewWriter(). Для JSON — json.MarshalIndent(). Для текстовой таблицы используй fmt.Sprintf с форматированием %-10s для выравнивания.',
      solution: 'package main\n\nimport (\n    "bytes"\n    "encoding/csv"\n    "encoding/json"\n    "fmt"\n    "strconv"\n)\n\ntype Record struct {\n    ID     int     `json:"id"`\n    Name   string  `json:"name"`\n    Value  float64 `json:"value"`\n    Active bool    `json:"active"`\n}\n\ntype ExportStrategy interface {\n    Export(records []Record) ([]byte, error)\n    ContentType() string\n    Extension() string\n}\n\n// JSON стратегия\ntype JSONExporter struct{}\n\nfunc (e *JSONExporter) Export(records []Record) ([]byte, error) {\n    return json.MarshalIndent(records, "", "  ")\n}\nfunc (e *JSONExporter) ContentType() string { return "application/json" }\nfunc (e *JSONExporter) Extension() string   { return ".json" }\n\n// CSV стратегия\ntype CSVExporter struct{}\n\nfunc (e *CSVExporter) Export(records []Record) ([]byte, error) {\n    var buf bytes.Buffer\n    w := csv.NewWriter(&buf)\n    w.Write([]string{"ID", "Name", "Value", "Active"})\n    for _, r := range records {\n        w.Write([]string{\n            strconv.Itoa(r.ID),\n            r.Name,\n            fmt.Sprintf("%.2f", r.Value),\n            strconv.FormatBool(r.Active),\n        })\n    }\n    w.Flush()\n    return buf.Bytes(), w.Error()\n}\nfunc (e *CSVExporter) ContentType() string { return "text/csv" }\nfunc (e *CSVExporter) Extension() string   { return ".csv" }\n\n// Text стратегия\ntype TextExporter struct{}\n\nfunc (e *TextExporter) Export(records []Record) ([]byte, error) {\n    var buf bytes.Buffer\n    fmt.Fprintf(&buf, "%-5s | %-15s | %-10s | %s\\n", "ID", "Name", "Value", "Active")\n    fmt.Fprintf(&buf, "%s\\n", "------+------------------+------------+--------")\n    for _, r := range records {\n        fmt.Fprintf(&buf, "%-5d | %-15s | %-10.2f | %v\\n",\n            r.ID, r.Name, r.Value, r.Active)\n    }\n    return buf.Bytes(), nil\n}\nfunc (e *TextExporter) ContentType() string { return "text/plain" }\nfunc (e *TextExporter) Extension() string   { return ".txt" }\n\n// DataExporter — контекст\ntype DataExporter struct {\n    strategy ExportStrategy\n}\n\nfunc (de *DataExporter) SetStrategy(s ExportStrategy) {\n    de.strategy = s\n}\n\nfunc (de *DataExporter) Export(records []Record) ([]byte, error) {\n    if de.strategy == nil {\n        return nil, fmt.Errorf("стратегия не установлена")\n    }\n    return de.strategy.Export(records)\n}\n\nfunc main() {\n    records := []Record{\n        {ID: 1, Name: "Alice", Value: 100.50, Active: true},\n        {ID: 2, Name: "Bob", Value: 250.00, Active: false},\n        {ID: 3, Name: "Charlie", Value: 75.25, Active: true},\n        {ID: 4, Name: "Diana", Value: 500.00, Active: true},\n        {ID: 5, Name: "Eve", Value: 30.00, Active: false},\n    }\n\n    exporter := &DataExporter{}\n\n    strategies := []ExportStrategy{\n        &JSONExporter{},\n        &CSVExporter{},\n        &TextExporter{},\n    }\n\n    for _, s := range strategies {\n        exporter.SetStrategy(s)\n        data, err := exporter.Export(records)\n        if err != nil {\n            fmt.Printf("Ошибка: %v\\n", err)\n            continue\n        }\n        name := s.ContentType()\n        fmt.Printf("--- %s (%s, %s) ---\\n", name, s.ContentType(), s.Extension())\n        fmt.Println(string(data))\n    }\n}',
      explanation: 'Паттерн Strategy позволяет DataExporter не знать ничего о форматах. Метод SetStrategy меняет алгоритм экспорта без изменения логики контекста. Каждая стратегия инкапсулирует специфику своего формата: JSON использует stdlib, CSV — encoding/csv с правильным escaping, Text — форматированный вывод. ContentType и Extension — часть стратегии, что позволяет HTTP-обработчику выставить правильный Content-Type и имя файла для скачивания.'
    }
  ]
}
