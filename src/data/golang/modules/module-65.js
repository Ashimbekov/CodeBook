export default {
  id: 65,
  title: 'Профилирование с pprof',
  description: 'Анализ производительности Go приложений: runtime/pprof, net/http/pprof, CPU и memory профилирование, анализ горутин, инструмент trace и построение flame graphs.',
  lessons: [
    {
      id: 1,
      title: 'Введение в pprof',
      type: 'theory',
      content: [
        { type: 'text', value: 'pprof — встроенный профилировщик Go. Это как МРТ для вашего приложения: он показывает, где именно тратится время и память. Без профилировщика оптимизация — это угадывание, с ним — точная наука.' },
        { type: 'heading', value: 'Два способа использования pprof' },
        { type: 'code', language: 'go', value: '// Способ 1: runtime/pprof — программный контроль\n// Запускаете профилирование в нужный момент кода\n\npackage main\n\nimport (\n    "log"\n    "os"\n    "runtime/pprof"\n    "time"\n)\n\nfunc main() {\n    // Создаём файл для CPU профиля\n    f, err := os.Create("cpu.prof")\n    if err != nil {\n        log.Fatal("Не могу создать CPU профиль:", err)\n    }\n    defer f.Close()\n\n    // Запускаем CPU профилирование\n    if err := pprof.StartCPUProfile(f); err != nil {\n        log.Fatal("Не могу запустить CPU профиль:", err)\n    }\n\n    // ВАШ КОД который нужно профилировать\n    doWork()\n\n    // Останавливаем профилирование\n    pprof.StopCPUProfile()\n\n    // Записываем Memory профиль\n    mf, err := os.Create("mem.prof")\n    if err != nil {\n        log.Fatal("Не могу создать mem профиль:", err)\n    }\n    defer mf.Close()\n    pprof.WriteHeapProfile(mf)\n\n    log.Println("Профили сохранены: cpu.prof, mem.prof")\n    log.Println("Анализ: go tool pprof cpu.prof")\n}\n\nfunc doWork() {\n    time.Sleep(100 * time.Millisecond)\n    // Имитируем работу\n    sum := 0\n    for i := 0; i < 1_000_000; i++ {\n        sum += i\n    }\n    _ = sum\n}' },
        { type: 'code', language: 'go', value: '// Способ 2: net/http/pprof — HTTP эндпоинты\n// Профилировщик доступен через браузер или go tool pprof\n\npackage main\n\nimport (\n    "fmt"\n    "net/http"\n    _ "net/http/pprof"  // важно! импорт с _ регистрирует endpoints\n)\n\nfunc main() {\n    // Ваш основной mux\n    mux := http.NewServeMux()\n    mux.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) {\n        fmt.Fprintln(w, "Основной сервис")\n    })\n\n    // Профилировщик на отдельном порту (НЕ открывайте публично!)\n    go func() {\n        // Автоматически добавляет:\n        // GET /debug/pprof/              - главная страница\n        // GET /debug/pprof/cmdline      - аргументы запуска\n        // GET /debug/pprof/profile      - CPU профиль (30 сек)\n        // GET /debug/pprof/trace        - trace\n        // GET /debug/pprof/heap         - heap профиль\n        // GET /debug/pprof/goroutine    - дамп всех горутин\n        // GET /debug/pprof/block        - блокировки\n        // GET /debug/pprof/mutex        - mutex профиль\n        fmt.Println("pprof на http://localhost:6060/debug/pprof/")\n        http.ListenAndServe(":6060", nil)  // nil использует DefaultServeMux\n    }()\n\n    http.ListenAndServe(":8080", mux)\n}' },
        { type: 'warning', value: 'Никогда не открывайте /debug/pprof на публичном порту в production! Это раскрывает внутреннюю информацию о вашем приложении. Запускайте pprof на отдельном внутреннем порту (например, 6060) или только при локальной разработке.' }
      ]
    },
    {
      id: 2,
      title: 'CPU профилирование',
      type: 'theory',
      content: [
        { type: 'text', value: 'CPU профиль показывает, где ваш код тратит процессорное время. pprof собирает стектрейсы каждые 10ms и строит статистику. Это как детальный журнал рабочего дня: видно, что занимало больше всего времени.' },
        { type: 'heading', value: 'Анализ CPU профиля' },
        { type: 'code', language: 'go', value: '// Программа с намеренно медленным кодом\npackage main\n\nimport (\n    "fmt"\n    "math"\n    "os"\n    "runtime/pprof"\n    "strings"\n)\n\n// Медленная функция: конкатенация строк в цикле\nfunc slowStringConcat(n int) string {\n    result := ""\n    for i := 0; i < n; i++ {\n        result += fmt.Sprintf("item%d,", i) // O(n²) !!!\n    }\n    return result\n}\n\n// Быстрая версия\nfunc fastStringConcat(n int) string {\n    var sb strings.Builder\n    for i := 0; i < n; i++ {\n        sb.WriteString(fmt.Sprintf("item%d,", i)) // O(n)\n    }\n    return sb.String()\n}\n\n// CPU-интенсивная операция\nfunc expensiveCalc(n int) float64 {\n    result := 0.0\n    for i := 0; i < n; i++ {\n        result += math.Sqrt(float64(i)) * math.Log(float64(i+1))\n    }\n    return result\n}\n\nfunc main() {\n    f, _ := os.Create("cpu.prof")\n    defer f.Close()\n\n    pprof.StartCPUProfile(f)\n    defer pprof.StopCPUProfile()\n\n    // Запускаем медленный код\n    s := slowStringConcat(5000)\n    _ = s\n    r := expensiveCalc(1_000_000)\n    _ = r\n}\n\n// Анализ:\n// go build -o myapp . && ./myapp\n// go tool pprof cpu.prof\n//\n// Команды в интерактивном режиме pprof:\n// (pprof) top           - топ функций по использованию CPU\n// (pprof) top10         - топ 10\n// (pprof) list slowStringConcat - исходный код с затратами\n// (pprof) web           - открыть граф вызовов (нужен graphviz)\n// (pprof) png > graph.png - сохранить в PNG\n\n// Пример вывода top:\n// Showing nodes accounting for 2.8s, 93.33% of 3s total\n//   flat  flat%   sum%        cum   cum%\n//  1.80s 60.00% 60.00%      1.80s 60.00%  main.slowStringConcat\n//  1.00s 33.33% 93.33%      1.00s 33.33%  main.expensiveCalc' },
        { type: 'tip', value: 'При анализе в pprof обращайте внимание на "flat" (время в самой функции) и "cum" (суммарное время включая вызовы). Функция с высоким cum и низким flat — это функция, вызывающая другие медленные функции.' }
      ]
    },
    {
      id: 3,
      title: 'Memory профилирование',
      type: 'theory',
      content: [
        { type: 'text', value: 'Memory профиль показывает, где выделяется память. Это помогает найти утечки памяти и места, где GC (сборщик мусора) работает слишком активно. Представьте детектив ищет, куда утекает вода в трубах.' },
        { type: 'heading', value: 'Анализ heap профиля' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "os"\n    "runtime"\n    "runtime/pprof"\n)\n\n// Намеренно расточительная функция\nfunc memoryLeak() {\n    // Создаём много объектов, которые не нужно хранить\n    result := make([][]byte, 0)\n    for i := 0; i < 1000; i++ {\n        // Каждый раз выделяем 1KB\n        buf := make([]byte, 1024)\n        buf[0] = byte(i)\n        result = append(result, buf)\n    }\n    _ = result // GC не сможет собрать, пока result жива\n}\n\n// Эффективное использование памяти\nfunc efficientMemory() {\n    // Переиспользуем один буфер\n    buf := make([]byte, 1024)\n    for i := 0; i < 1000; i++ {\n        buf[0] = byte(i)\n        process(buf)\n    }\n}\n\nfunc process(buf []byte) { /* обработка */ }\n\nfunc printMemStats(label string) {\n    var m runtime.MemStats\n    runtime.ReadMemStats(&m)\n    fmt.Printf("\\n[%s]\\n", label)\n    fmt.Printf("  Alloc:      %v KB\\n", m.Alloc/1024)\n    fmt.Printf("  TotalAlloc: %v KB\\n", m.TotalAlloc/1024)\n    fmt.Printf("  Sys:        %v KB\\n", m.Sys/1024)\n    fmt.Printf("  NumGC:      %v\\n", m.NumGC)\n    fmt.Printf("  HeapInuse:  %v KB\\n", m.HeapInuse/1024)\n}\n\nfunc main() {\n    printMemStats("Старт")\n\n    memoryLeak()\n    printMemStats("После memoryLeak")\n\n    runtime.GC() // принудительная сборка мусора\n    printMemStats("После GC")\n\n    efficientMemory()\n    printMemStats("После efficientMemory")\n\n    // Сохраняем heap профиль\n    f, _ := os.Create("heap.prof")\n    defer f.Close()\n    runtime.GC() // собираем мусор перед профилем\n    pprof.WriteHeapProfile(f)\n\n    fmt.Println("\\nАнализ: go tool pprof -alloc_space heap.prof")\n    fmt.Println("  (pprof) top")\n    fmt.Println("  (pprof) list memoryLeak")\n}' },
        { type: 'code', language: 'go', value: '// Виды heap профилей:\n// go tool pprof heap.prof          - по умолчанию: inuse_space\n// go tool pprof -alloc_space heap.prof  - выделенная память (все за время)\n// go tool pprof -alloc_objects heap.prof - количество объектов\n// go tool pprof -inuse_objects heap.prof - живые объекты\n\n// Метрики runtime.MemStats:\n// Alloc      - текущая выделенная heap память\n// TotalAlloc - всего выделено за всё время (не уменьшается!)\n// Sys        - память от ОС\n// NumGC      - количество сборок мусора\n// HeapInuse  - heap в использовании\n// HeapIdle   - heap не используется (но не вернули ОС)\n// StackInuse - память горутин на стеке\n// GCSys      - память самого GC\n\n// Пример вывода:\n// [Старт]\n//   Alloc:      120 KB\n//   NumGC:      0\n// [После memoryLeak]\n//   Alloc:      1150 KB  <- вырос!\n//   NumGC:      0\n// [После GC]\n//   Alloc:      130 KB   <- GC собрал мусор\n//   NumGC:      1' }
      ]
    },
    {
      id: 4,
      title: 'Профилирование горутин',
      type: 'theory',
      content: [
        { type: 'text', value: 'Горутинный профиль показывает все запущенные горутины и где они застряли. Это незаменимо при поиске goroutine leaks (горутин, которые никогда не завершаются) и deadlock-ов.' },
        { type: 'heading', value: 'Анализ горутин' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "net/http"\n    _ "net/http/pprof"\n    "runtime"\n    "sync"\n    "time"\n)\n\n// Утечка горутин — частая проблема\nfunc goroutineLeak() {\n    ch := make(chan int) // unbuffered канал\n    for i := 0; i < 100; i++ {\n        go func(id int) {\n            // Ждёт значения из канала, которое НИКОГДА не придёт\n            // Если никто не запишет в ch — горутина зависнет навсегда!\n            v := <-ch\n            fmt.Printf("Горутина %d получила %d\\n", id, v)\n        }(i)\n    }\n    // ch никто не закрывает и не пишет в него!\n    // 100 горутин зависли навсегда — утечка!\n}\n\n// Правильная версия с context отмены\nfunc properGoroutines() {\n    ch := make(chan int, 100) // буферизованный канал\n    var wg sync.WaitGroup\n\n    for i := 0; i < 100; i++ {\n        wg.Add(1)\n        go func(id int) {\n            defer wg.Done()\n            select {\n            case v := <-ch:\n                fmt.Printf("Горутина %d получила %d\\n", id, v)\n            case <-time.After(1 * time.Second):\n                // Таймаут — не зависнем навсегда\n                fmt.Printf("Горутина %d: таймаут\\n", id)\n            }\n        }(i)\n    }\n    wg.Wait()\n}\n\nfunc printGoroutineCount() {\n    fmt.Printf("Горутин запущено: %d\\n", runtime.NumGoroutine())\n}\n\nfunc main() {\n    printGoroutineCount() // ~1-2 системных\n\n    goroutineLeak()\n    time.Sleep(100 * time.Millisecond)\n    printGoroutineCount() // ~103 (утечка!)\n\n    // HTTP pprof для анализа\n    go func() {\n        http.ListenAndServe(":6060", nil)\n    }()\n\n    fmt.Println("Открой: http://localhost:6060/debug/pprof/goroutine?debug=1")\n    fmt.Println("Или запусти: go tool pprof http://localhost:6060/debug/pprof/goroutine")\n    time.Sleep(60 * time.Second)\n}' },
        { type: 'tip', value: 'Используйте runtime.NumGoroutine() для мониторинга количества горутин в тестах. Если после завершения тестового сценария количество горутин не вернулось к исходному — у вас утечка.' }
      ]
    },
    {
      id: 5,
      title: 'Инструмент trace',
      type: 'theory',
      content: [
        { type: 'text', value: 'go tool trace — более детальный инструмент, чем pprof. Он записывает все события: запуск горутин, GC паузы, syscall, scheduler. Это как видеозапись всего, что происходит в runtime, с точностью до наносекунд.' },
        { type: 'heading', value: 'Запись и анализ трейсов' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "os"\n    "runtime/trace"\n    "sync"\n)\n\nfunc main() {\n    // Создаём файл для трейса\n    f, err := os.Create("trace.out")\n    if err != nil {\n        fmt.Println("Ошибка:", err)\n        return\n    }\n    defer f.Close()\n\n    // Запускаем трейс\n    if err := trace.Start(f); err != nil {\n        fmt.Println("Не могу запустить трейс:", err)\n        return\n    }\n\n    // Ваш код\n    doParallelWork()\n\n    // Останавливаем трейс\n    trace.Stop()\n\n    fmt.Println("Трейс сохранён в trace.out")\n    fmt.Println("Просмотр: go tool trace trace.out")\n}\n\nfunc doParallelWork() {\n    var wg sync.WaitGroup\n    results := make(chan int, 10)\n\n    // Создаём несколько горутин\n    for i := 0; i < 5; i++ {\n        wg.Add(1)\n        go func(id int) {\n            defer wg.Done()\n            // Имитируем работу\n            sum := 0\n            for j := 0; j < 100000; j++ {\n                sum += j\n            }\n            results <- sum\n        }(i)\n    }\n\n    // Ждём и читаем результаты\n    go func() {\n        wg.Wait()\n        close(results)\n    }()\n\n    total := 0\n    for r := range results {\n        total += r\n    }\n    fmt.Printf("Сумма: %d\\n", total)\n}\n\n// go tool trace trace.out откроет браузер с:\n// - Goroutine analysis: сколько горутин, как долго ждали\n// - View trace: временная шкала всех событий\n// - Network blocking: ожидание сети\n// - Synchronization blocking: ожидание мьютексов\n// - Scheduler latency: задержки планировщика\n// - GC stats: статистика сборки мусора' }
      ]
    },
    {
      id: 6,
      title: 'Flame Graphs',
      type: 'theory',
      content: [
        { type: 'text', value: 'Flame Graph (граф пламени) — визуализация CPU профиля. Это как сложенные стопкой листы бумаги: ширина каждого блока пропорциональна времени, а вложенность — стеку вызовов. Широкие блоки внизу — "горячие точки".' },
        { type: 'heading', value: 'Генерация Flame Graph через pprof' },
        { type: 'code', language: 'go', value: '// Способ 1: Через HTTP pprof (самый простой)\n// Запустите приложение с net/http/pprof\n// Затем:\n//\n// go tool pprof -http=:8081 http://localhost:6060/debug/pprof/profile?seconds=30\n// Откроет браузер с интерактивным Flame Graph!\n//\n// Или для heap:\n// go tool pprof -http=:8081 http://localhost:6060/debug/pprof/heap\n\n// Способ 2: Из .prof файла\n// go tool pprof -http=:8081 cpu.prof\n\n// Способ 3: Сохранить как SVG\n// go tool pprof -svg cpu.prof > flamegraph.svg\n\npackage main\n\nimport (\n    "fmt"\n    "math/rand"\n    "net/http"\n    _ "net/http/pprof"\n    "time"\n)\n\n// Функции с разной интенсивностью\nfunc fastFunction() {\n    time.Sleep(1 * time.Millisecond)\n}\n\nfunc mediumFunction() {\n    for i := 0; i < 10000; i++ {\n        _ = rand.Intn(1000)\n    }\n}\n\nfunc slowFunction() {\n    for i := 0; i < 1000000; i++ {\n        _ = fmt.Sprintf("item_%d", i) // медленно из-за аллокации\n    }\n}\n\nfunc worker() {\n    for {\n        fastFunction()   // 10% времени\n        mediumFunction() // 30% времени\n        slowFunction()   // 60% времени\n    }\n}\n\nfunc main() {\n    // Запускаем рабочие горутины\n    for i := 0; i < 4; i++ {\n        go worker()\n    }\n\n    // pprof на :6060\n    fmt.Println("pprof на http://localhost:6060/debug/pprof/")\n    fmt.Println("Flame Graph: go tool pprof -http=:8081 http://localhost:6060/debug/pprof/profile?seconds=10")\n    http.ListenAndServe(":6060", nil)\n}' },
        { type: 'note', value: 'В pprof web интерфейсе (go tool pprof -http=:8081) доступны несколько режимов: "Flame Graph" (пламя), "Top" (таблица), "Graph" (граф вызовов), "Source" (исходный код с аннотациями). Flame Graph — самый наглядный для поиска горячих точек.' }
      ]
    },
    {
      id: 7,
      title: 'Практика: профилирование веб-сервера',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создай веб-сервер с намеренно медленным обработчиком, добавь pprof эндпоинты, найди узкое место с помощью CPU профилирования и оптимизируй его. Измерь улучшение через benchmarks.',
      requirements: [
        'Создай HTTP сервер с /slow эндпоинтом (конкатенация строк в цикле)',
        'Добавь pprof на порт :6060',
        'Напиши benchmark для медленного обработчика',
        'Найди узкое место через go test -bench и -cpuprofile',
        'Создай /fast эндпоинт с оптимизированной версией (strings.Builder)',
        'Добавь /metrics эндпоинт с runtime.MemStats',
        'Benchmark должен показать улучшение минимум в 5x'
      ],
      expectedOutput: 'BenchmarkSlow-8   500    2345678 ns/op    4194304 B/op    5000 allocs/op\nBenchmarkFast-8  5000    234567 ns/op     65536 B/op      2 allocs/op\nУлучшение: ~10x по скорости, ~64x по аллокациям',
      hint: 'strings.Builder использует байтовый буфер внутри и не копирует строку при каждой конкатенации. Для benchmark: go test -bench=. -benchmem -cpuprofile=cpu.prof, затем go tool pprof -http=:8081 cpu.prof.',
      solution: 'package main\n\nimport (\n    "fmt"\n    "net/http"\n    _ "net/http/pprof"\n    "runtime"\n    "strconv"\n    "strings"\n    "testing"\n    "encoding/json"\n)\n\n// МЕДЛЕННАЯ версия: O(n²) из-за копирования строк\nfunc slowHandler(n int) string {\n    result := ""\n    for i := 0; i < n; i++ {\n        result += strconv.Itoa(i) + ","\n    }\n    return result\n}\n\n// БЫСТРАЯ версия: O(n) с Builder\nfunc fastHandler(n int) string {\n    var sb strings.Builder\n    sb.Grow(n * 4) // предаллоцируем память\n    for i := 0; i < n; i++ {\n        sb.WriteString(strconv.Itoa(i))\n        sb.WriteByte(",")\n    }\n    return sb.String()\n}\n\nfunc memMetrics() map[string]uint64 {\n    var m runtime.MemStats\n    runtime.ReadMemStats(&m)\n    return map[string]uint64{\n        "alloc_kb":       m.Alloc / 1024,\n        "total_alloc_kb": m.TotalAlloc / 1024,\n        "sys_kb":         m.Sys / 1024,\n        "num_gc":         uint64(m.NumGC),\n        "goroutines":     uint64(runtime.NumGoroutine()),\n    }\n}\n\nfunc main() {\n    mux := http.NewServeMux()\n\n    mux.HandleFunc("/slow", func(w http.ResponseWriter, r *http.Request) {\n        n := 5000\n        result := slowHandler(n)\n        fmt.Fprintf(w, "Медленно (%d элементов): %d байт\\n", n, len(result))\n    })\n\n    mux.HandleFunc("/fast", func(w http.ResponseWriter, r *http.Request) {\n        n := 5000\n        result := fastHandler(n)\n        fmt.Fprintf(w, "Быстро (%d элементов): %d байт\\n", n, len(result))\n    })\n\n    mux.HandleFunc("/metrics", func(w http.ResponseWriter, r *http.Request) {\n        w.Header().Set("Content-Type", "application/json")\n        json.NewEncoder(w).Encode(memMetrics())\n    })\n\n    // pprof на отдельном порту\n    go func() {\n        fmt.Println("pprof: http://localhost:6060/debug/pprof/")\n        http.ListenAndServe(":6060", nil)\n    }()\n\n    fmt.Println("Сервер: http://localhost:8080")\n    http.ListenAndServe(":8080", mux)\n}\n\n// benchmark_test.go\nfunc BenchmarkSlow(b *testing.B) {\n    for i := 0; i < b.N; i++ {\n        slowHandler(5000)\n    }\n}\n\nfunc BenchmarkFast(b *testing.B) {\n    for i := 0; i < b.N; i++ {\n        fastHandler(5000)\n    }\n}\n// go test -bench=. -benchmem',
      explanation: 'Конкатенация строк через += в Go создаёт новую строку при каждой итерации, потому что строки в Go иммутабельны. При n=5000 это O(n²) операций и O(n²) аллокаций. strings.Builder накапливает данные в байтовый срез (мутабельный) и создаёт строку только в конце — O(n). sb.Grow() предаллоцирует память, избегая переаллокаций при росте буфера. Это фундаментальный паттерн оптимизации строк в Go.'
    }
  ]
}
