export default {
  id: 52,
  title: 'GORM — ORM для Go',
  description: 'GORM — самый популярный ORM для Go. Автоматизирует работу с базой данных: вы описываете структуры, GORM сам пишет SQL. Подходит для быстрой разработки CRUD-приложений.',
  lessons: [
    {
      id: 1,
      title: 'Что такое ORM и зачем GORM',
      type: 'theory',
      content: [
        { type: 'text', value: 'ORM (Object-Relational Mapping) — это переводчик между объектами вашего кода и таблицами базы данных. Как переводчик с русского на английский: вы говорите на Go, ORM "переводит" в SQL.' },
        { type: 'heading', value: 'Сравнение: database/sql vs GORM' },
        { type: 'code', language: 'go', value: '// database/sql — вы пишете SQL вручную\nrows, err := db.Query("SELECT id, name, email FROM users WHERE age > $1 ORDER BY name LIMIT $2", 18, 10)\nif err != nil { ... }\ndefer rows.Close()\nfor rows.Next() {\n    var u User\n    rows.Scan(&u.ID, &u.Name, &u.Email)\n    users = append(users, u)\n}\n\n// GORM — вы описываете намерение\nvar users []User\ndb.Where("age > ?", 18).Order("name").Limit(10).Find(&users)' },
        { type: 'heading', value: 'Установка GORM' },
        { type: 'code', language: 'go', value: '// Установка GORM и драйвера PostgreSQL\n// go get gorm.io/gorm\n// go get gorm.io/driver/postgres\n\n// Драйверы GORM:\n// PostgreSQL: gorm.io/driver/postgres\n// MySQL:      gorm.io/driver/mysql\n// SQLite:     gorm.io/driver/sqlite\n// SQL Server: gorm.io/driver/sqlserver\n\npackage main\n\nimport (\n    "fmt"\n    "log"\n\n    "gorm.io/driver/postgres"\n    "gorm.io/gorm"\n    "gorm.io/gorm/logger"\n)\n\nfunc main() {\n    dsn := "host=localhost user=postgres password=secret dbname=mydb port=5432 sslmode=disable"\n\n    db, err := gorm.Open(postgres.Open(dsn), &gorm.Config{\n        Logger: logger.Default.LogMode(logger.Info), // показывать SQL в логах\n    })\n    if err != nil {\n        log.Fatal("Ошибка подключения:", err)\n    }\n\n    fmt.Println("GORM подключён к PostgreSQL!")\n\n    // Получить *sql.DB для настройки пула\n    sqlDB, err := db.DB()\n    if err != nil {\n        log.Fatal(err)\n    }\n    sqlDB.SetMaxOpenConns(10)\n    _ = db\n}' },
        { type: 'tip', value: 'Logger: logger.Info в разработке показывает все SQL-запросы — полезно для понимания что делает GORM. В продакшене ставьте logger.Warn или logger.Error, чтобы не засорять логи.' }
      ]
    },
    {
      id: 2,
      title: 'Определение моделей',
      type: 'theory',
      content: [
        { type: 'text', value: 'Модель в GORM — это обычная Go структура. GORM использует теги `gorm:"..."` для настройки маппинга на таблицу. Есть встроенная базовая модель gorm.Model с полями ID, CreatedAt, UpdatedAt, DeletedAt.' },
        { type: 'heading', value: 'Структура модели' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "time"\n\n    "gorm.io/gorm"\n)\n\n// gorm.Model добавляет стандартные поля\ntype gormModel struct {\n    ID        uint           `gorm:"primarykey"`\n    CreatedAt time.Time\n    UpdatedAt time.Time\n    DeletedAt gorm.DeletedAt `gorm:"index"` // soft delete\n}\n\n// Полная модель пользователя\ntype User struct {\n    gorm.Model                          // встраиваем стандартные поля\n    Name        string  `gorm:"size:100;not null"`\n    Email       string  `gorm:"uniqueIndex;size:200;not null"`\n    Age         uint8\n    IsActive    bool    `gorm:"default:true"`\n    Bio         *string `gorm:"type:text"` // указатель = nullable\n    ProfileID   *uint\n    Profile     Profile\n    Orders      []Order  // HasMany\n}\n\ntype Profile struct {\n    gorm.Model\n    UserID  uint   `gorm:"uniqueIndex"` // внешний ключ\n    Avatar  string `gorm:"size:500"`\n    Website string `gorm:"size:300"`\n}\n\ntype Order struct {\n    gorm.Model\n    UserID uint    `gorm:"not null;index"` // внешний ключ\n    Total  float64 `gorm:"not null"`\n    Status string  `gorm:"size:50;default:pending"`\n    Items  []OrderItem\n}\n\ntype OrderItem struct {\n    gorm.Model\n    OrderID   uint    `gorm:"not null;index"`\n    ProductID uint    `gorm:"not null"`\n    Quantity  int     `gorm:"not null;default:1"`\n    Price     float64 `gorm:"not null"`\n}\n\n// Своя таблица и составной ключ\ntype Permission struct {\n    UserID     uint   `gorm:"primaryKey"`\n    ResourceID uint   `gorm:"primaryKey"`\n    Action     string `gorm:"primaryKey;size:50"`\n    // TableName определяет имя таблицы\n}\n\nfunc (Permission) TableName() string {\n    return "user_permissions"\n}\n\n// Теги GORM:\n// gorm:"primaryKey"         -- первичный ключ\n// gorm:"autoIncrement"      -- автоинкремент\n// gorm:"column:my_column"   -- имя колонки в БД\n// gorm:"type:varchar(200)"  -- SQL тип\n// gorm:"size:100"           -- размер строки\n// gorm:"not null"           -- NOT NULL ограничение\n// gorm:"unique"             -- UNIQUE\n// gorm:"uniqueIndex"        -- UNIQUE INDEX\n// gorm:"index"              -- обычный индекс\n// gorm:"default:true"       -- значение по умолчанию\n// gorm:"->:false;<-:create" -- только запись при создании\n// gorm:"-"                  -- игнорировать поле' },
        { type: 'note', value: 'gorm.DeletedAt — это soft delete (мягкое удаление). При Delete() GORM не удаляет строку, а устанавливает DeletedAt. Все запросы автоматически фильтруют такие записи через WHERE deleted_at IS NULL.' }
      ]
    },
    {
      id: 3,
      title: 'CRUD операции',
      type: 'theory',
      content: [
        { type: 'text', value: 'GORM предоставляет удобные методы для всех CRUD операций: Create, Find/First/Last, Save/Updates, Delete. Все они работают с указателями на структуры.' },
        { type: 'heading', value: 'Create — создание записей' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "log"\n\n    "gorm.io/driver/postgres"\n    "gorm.io/gorm"\n)\n\ntype User struct {\n    gorm.Model\n    Name  string\n    Email string\n    Age   uint8\n}\n\nfunc main() {\n    db, err := gorm.Open(postgres.Open("host=localhost user=postgres dbname=mydb sslmode=disable"), &gorm.Config{})\n    if err != nil {\n        log.Fatal(err)\n    }\n    db.AutoMigrate(&User{})\n\n    // CREATE — создание одной записи\n    user := User{Name: "Нурдаулет", Email: "n@example.com", Age: 25}\n    result := db.Create(&user) // передаём указатель!\n    if result.Error != nil {\n        log.Fatal("Ошибка создания:", result.Error)\n    }\n    fmt.Printf("Создан: ID=%d, затронуто строк=%d\\n", user.ID, result.RowsAffected)\n    // После Create поле user.ID заполнено автоматически!\n\n    // Создание нескольких записей\n    users := []User{\n        {Name: "Айгерим", Email: "a@example.com", Age: 23},\n        {Name: "Берик", Email: "b@example.com", Age: 30},\n    }\n    db.Create(&users)\n    // Все ID заполнены\n    for _, u := range users {\n        fmt.Printf("ID=%d Name=%s\\n", u.ID, u.Name)\n    }\n\n    // CreateInBatches — создание батчами (для больших вставок)\n    var batch []User\n    for i := 0; i < 100; i++ {\n        batch = append(batch, User{Name: fmt.Sprintf("User%d", i), Email: fmt.Sprintf("u%d@test.com", i)})\n    }\n    db.CreateInBatches(&batch, 10) // по 10 записей за раз\n}' },
        { type: 'heading', value: 'Read — чтение данных' },
        { type: 'code', language: 'go', value: 'func readExamples(db *gorm.DB) {\n    // First — первая запись по ID\n    var user User\n    db.First(&user, 1) // SELECT * FROM users WHERE id = 1 LIMIT 1\n    fmt.Println("First:", user.Name)\n\n    // First с условием\n    var u2 User\n    db.First(&u2, "email = ?", "n@example.com")\n\n    // Find — все записи\n    var users []User\n    db.Find(&users) // SELECT * FROM users WHERE deleted_at IS NULL\n\n    // Find с условием\n    var youngUsers []User\n    db.Where("age < ?", 25).Find(&youngUsers)\n    // SELECT * FROM users WHERE age < 25 AND deleted_at IS NULL\n\n    // Last — последняя запись\n    var last User\n    db.Last(&last)\n\n    // Take — первая запись без сортировки\n    var any User\n    db.Take(&any)\n\n    // Выбор конкретных полей\n    db.Select("name", "email").Find(&users)\n    // SELECT name, email FROM users\n\n    // Order, Limit, Offset\n    var page []User\n    db.Order("created_at DESC").Limit(10).Offset(20).Find(&page)\n\n    // Count\n    var count int64\n    db.Model(&User{}).Where("age > ?", 18).Count(&count)\n    fmt.Println("Взрослых:", count)\n\n    // Pluck — получить один столбец\n    var names []string\n    db.Model(&User{}).Pluck("name", &names)\n    fmt.Println("Имена:", names)\n}' },
        { type: 'heading', value: 'Update и Delete' },
        { type: 'code', language: 'go', value: 'func updateDeleteExamples(db *gorm.DB) {\n    // Save — обновляет все поля (включая нулевые)\n    var user User\n    db.First(&user, 1)\n    user.Name = "Новое имя"\n    db.Save(&user) // UPDATE users SET ... WHERE id = 1\n\n    // Update — обновить одно поле\n    db.Model(&user).Update("age", 26)\n    // UPDATE users SET age=26, updated_at=... WHERE id=1\n\n    // Updates — несколько полей через map\n    db.Model(&user).Updates(map[string]interface{}{\n        "name": "Другое имя",\n        "age":  27,\n    })\n\n    // Updates через структуру (нулевые поля игнорируются!)\n    db.Model(&user).Updates(User{Name: "Третье имя", Age: 0}) // Age не обновится!\n\n    // UpdateColumn — без хуков и updated_at\n    db.Model(&user).UpdateColumn("name", "Системное обновление")\n\n    // Массовое обновление\n    db.Model(&User{}).Where("age < ?", 18).Update("is_active", false)\n\n    // Delete (soft delete, если есть DeletedAt)\n    db.Delete(&user) // UPDATE users SET deleted_at=NOW() WHERE id=1\n    // Жёсткое удаление\n    db.Unscoped().Delete(&user) // DELETE FROM users WHERE id=1\n\n    // Удаление по условию\n    db.Where("age < ?", 13).Delete(&User{})\n}' }
      ]
    },
    {
      id: 4,
      title: 'Ассоциации HasOne и HasMany',
      type: 'theory',
      content: [
        { type: 'text', value: 'GORM поддерживает все типичные отношения между таблицами. HasOne — "у пользователя есть один профиль". HasMany — "у пользователя много заказов". BelongsTo — обратное HasMany.' },
        { type: 'heading', value: 'HasOne и BelongsTo' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n\n    "gorm.io/driver/postgres"\n    "gorm.io/gorm"\n)\n\ntype User struct {\n    gorm.Model\n    Name    string\n    Profile Profile // HasOne: у User есть один Profile\n    Orders  []Order // HasMany: у User много Orders\n}\n\ntype Profile struct {\n    gorm.Model\n    UserID uint   // внешний ключ для HasOne\n    Bio    string\n    Avatar string\n}\n\ntype Order struct {\n    gorm.Model\n    UserID uint    // внешний ключ для HasMany\n    Total  float64\n    User   User    // BelongsTo: Order принадлежит User\n}\n\nfunc main() {\n    db, _ := gorm.Open(postgres.Open("host=localhost user=postgres dbname=mydb sslmode=disable"), &gorm.Config{})\n    db.AutoMigrate(&User{}, &Profile{}, &Order{})\n\n    // Создание с ассоциациями\n    user := User{\n        Name: "Нурдаулет",\n        Profile: Profile{\n            Bio:    "Go разработчик",\n            Avatar: "avatar.jpg",\n        },\n    }\n    db.Create(&user) // создаст и User, и Profile за одну транзакцию\n\n    // Загрузка с ассоциациями — Preload (eager loading)\n    var loadedUser User\n    db.Preload("Profile").First(&loadedUser, user.ID)\n    fmt.Printf("Пользователь: %s, Профиль: %s\\n", loadedUser.Name, loadedUser.Profile.Bio)\n\n    // Preload нескольких ассоциаций\n    var userWithAll User\n    db.Preload("Profile").Preload("Orders").First(&userWithAll, user.ID)\n\n    // Preload с условием\n    var u User\n    db.Preload("Orders", "total > ?", 100).First(&u, user.ID)\n\n    // Обновление ассоциации\n    db.Model(&user).Association("Profile").Replace(&Profile{\n        Bio:    "Старший Go разработчик",\n        Avatar: "new_avatar.jpg",\n    })\n\n    // Добавление к HasMany\n    db.Model(&user).Association("Orders").Append(&Order{Total: 500.0})\n\n    // Подсчёт ассоциаций\n    count := db.Model(&user).Association("Orders").Count()\n    fmt.Println("Заказов:", count)\n}' },
        { type: 'heading', value: 'ManyToMany' },
        { type: 'code', language: 'go', value: 'type Product struct {\n    gorm.Model\n    Name string\n    Tags []Tag `gorm:"many2many:product_tags;"` // связующая таблица\n}\n\ntype Tag struct {\n    gorm.Model\n    Name     string    `gorm:"uniqueIndex"`\n    Products []Product `gorm:"many2many:product_tags;"`\n}\n\nfunc manyToManyExample(db *gorm.DB) {\n    db.AutoMigrate(&Product{}, &Tag{})\n\n    // Создание тегов\n    tags := []Tag{{Name: "Go"}, {Name: "Backend"}, {Name: "Database"}}\n    db.Create(&tags)\n\n    // Создание продукта с тегами\n    product := Product{\n        Name: "Go курс",\n        Tags: []Tag{{Name: "Go"}, {Name: "Backend"}},\n    }\n    db.Create(&product)\n\n    // Загрузка с тегами\n    var p Product\n    db.Preload("Tags").First(&p, product.ID)\n    for _, t := range p.Tags {\n        fmt.Println("Тег:", t.Name)\n    }\n\n    // Добавление тега\n    var goTag Tag\n    db.Where("name = ?", "Database").First(&goTag)\n    db.Model(&product).Association("Tags").Append(&goTag)\n}' },
        { type: 'warning', value: 'Избегайте N+1 проблемы: не загружайте пользователей в цикле. Всегда используйте Preload для связанных данных. Без Preload GORM не подгружает ассоциации — поле будет нулевым значением.' }
      ]
    },
    {
      id: 5,
      title: 'AutoMigrate и миграции',
      type: 'theory',
      content: [
        { type: 'text', value: 'AutoMigrate в GORM автоматически создаёт и обновляет таблицы по вашим структурам. Это удобно при разработке, но в продакшене лучше использовать явные SQL-миграции.' },
        { type: 'heading', value: 'AutoMigrate' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "log"\n\n    "gorm.io/driver/postgres"\n    "gorm.io/gorm"\n)\n\ntype User struct {\n    gorm.Model\n    Name   string `gorm:"size:100;not null"`\n    Email  string `gorm:"uniqueIndex;size:200"`\n    Age    uint8\n}\n\ntype Post struct {\n    gorm.Model\n    Title   string `gorm:"size:200;not null"`\n    Content string `gorm:"type:text"`\n    UserID  uint   `gorm:"not null;index"`\n    User    User\n}\n\nfunc main() {\n    db, err := gorm.Open(postgres.Open("host=localhost user=postgres dbname=mydb sslmode=disable"), &gorm.Config{})\n    if err != nil {\n        log.Fatal(err)\n    }\n\n    // AutoMigrate: создаёт таблицы, добавляет новые колонки,\n    // создаёт индексы — НО не удаляет колонки и индексы!\n    err = db.AutoMigrate(\n        &User{},\n        &Post{},\n    )\n    if err != nil {\n        log.Fatal("AutoMigrate:", err)\n    }\n\n    // Проверка существования таблицы\n    if db.Migrator().HasTable(&User{}) {\n        log.Println("Таблица users существует")\n    }\n\n    // Проверка колонки\n    if db.Migrator().HasColumn(&User{}, "email") {\n        log.Println("Колонка email существует")\n    }\n\n    // Ручное создание таблицы\n    if !db.Migrator().HasTable("legacy_data") {\n        db.Migrator().CreateTable(&User{})\n    }\n\n    // Добавить колонку вручную\n    // db.Migrator().AddColumn(&User{}, "phone")\n\n    // Переименовать колонку\n    // db.Migrator().RenameColumn(&User{}, "old_name", "new_name")\n\n    log.Println("Миграции применены")\n}' },
        { type: 'warning', value: 'AutoMigrate никогда не удаляет колонки — он безопасен для существующих данных. Но он не подходит для сложных изменений схемы: переименование колонок, изменение типов, перенос данных. Для этого нужны ручные миграции (см. модуль 53).' }
      ]
    },
    {
      id: 6,
      title: 'Query Builder — сложные запросы',
      type: 'theory',
      content: [
        { type: 'text', value: 'GORM поддерживает сложные запросы через цепочку методов: Where, Or, Not, Group, Having, Joins, Scopes. Если GORM не справляется — можно использовать сырой SQL.' },
        { type: 'heading', value: 'Сложные условия и группировка' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "time"\n\n    "gorm.io/gorm"\n)\n\ntype User struct {\n    gorm.Model\n    Name     string\n    Email    string\n    Age      uint8\n    IsActive bool\n    Role     string\n}\n\ntype Order struct {\n    gorm.Model\n    UserID    uint\n    Total     float64\n    CreatedAt time.Time\n}\n\nfunc complexQueries(db *gorm.DB) {\n    var users []User\n\n    // AND условия\n    db.Where("age > ? AND is_active = ?", 18, true).Find(&users)\n\n    // OR через несколько Where\n    db.Where("role = ?", "admin").Or("role = ?", "moderator").Find(&users)\n\n    // Not\n    db.Not("role = ?", "banned").Find(&users)\n\n    // Where через структуру (нулевые поля игнорируются)\n    db.Where(&User{Role: "admin", IsActive: true}).Find(&users)\n\n    // Where через map (нулевые поля НЕ игнорируются)\n    db.Where(map[string]interface{}{\n        "role":      "admin",\n        "is_active": true,\n        "age":       0, // age = 0 будет в условии!\n    }).Find(&users)\n\n    // Joins\n    type UserOrderStat struct {\n        UserID     uint\n        Name       string\n        OrderCount int64\n        TotalSpent float64\n    }\n    var stats []UserOrderStat\n    db.Model(&User{}).Select(\n        "users.id as user_id, users.name, COUNT(orders.id) as order_count, SUM(orders.total) as total_spent",\n    ).Joins("LEFT JOIN orders ON orders.user_id = users.id").Group("users.id").Scan(&stats)\n\n    // Scopes — переиспользуемые условия\n    activeUsers := func(db *gorm.DB) *gorm.DB {\n        return db.Where("is_active = ?", true)\n    }\n    recentUsers := func(db *gorm.DB) *gorm.DB {\n        return db.Where("created_at > ?", time.Now().AddDate(0, -1, 0))\n    }\n\n    db.Scopes(activeUsers, recentUsers).Find(&users)\n    fmt.Printf("Активных новых пользователей: %d\\n", len(users))\n\n    // Raw SQL\n    var result []map[string]interface{}\n    db.Raw("SELECT id, name, email FROM users WHERE age BETWEEN ? AND ?", 20, 30).Scan(&result)\n\n    // Exec для не-SELECT\n    db.Exec("UPDATE users SET last_login = NOW() WHERE id = ?", 1)\n}' },
        { type: 'tip', value: 'db.Scan() в отличие от db.Find() не требует GORM-модели — можно сканировать в произвольные структуры или []map[string]interface{}. Это удобно для сложных JOIN-запросов.' }
      ]
    },
    {
      id: 7,
      title: 'Практика: блог с GORM',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуй систему блога с GORM: пользователи, посты, теги. Поддержи CRUD для постов, связь ManyToMany с тегами, подсчёт постов на пользователя.',
      requirements: [
        'Модели: User(ID, Name, Email), Post(ID, Title, Content, UserID, User, Tags), Tag(ID, Name, Posts)',
        'Post-Tag связь ManyToMany через таблицу post_tags',
        'CreateUser(name, email) (User, error)',
        'CreatePost(userID uint, title, content string, tagNames []string) (Post, error) — создаёт пост с тегами',
        'GetUserPosts(userID uint) ([]Post, error) — посты с загруженными тегами через Preload',
        'GetPostCountByUser() — возвращает []struct{UserID, Name, PostCount} через Joins+Group',
        'DeletePost(postID uint) error — soft delete',
        'main: создай 2 пользователей, 3 поста с тегами, выведи статистику'
      ],
      expectedOutput: 'Создан пользователь: Нурдаулет (ID=1)\nСоздан пост: "Go и GORM" (ID=1) с тегами: [Go, Backend]\nСтатистика:\n  Нурдаулет: 2 поста\n  Айгерим: 1 пост',
      hint: 'При создании поста с тегами — сначала найди или создай теги через FirstOrCreate, потом создай Post со связью Tags. Preload("Tags") в GetUserPosts загрузит теги каждого поста.',
      solution: 'package main\n\nimport (\n    "fmt"\n    "log"\n\n    "gorm.io/driver/sqlite"\n    "gorm.io/gorm"\n    "gorm.io/gorm/logger"\n)\n\ntype User struct {\n    gorm.Model\n    Name  string `gorm:"size:100;not null"`\n    Email string `gorm:"uniqueIndex;size:200"`\n    Posts []Post\n}\n\ntype Post struct {\n    gorm.Model\n    Title   string `gorm:"size:200;not null"`\n    Content string `gorm:"type:text"`\n    UserID  uint   `gorm:"not null;index"`\n    User    User\n    Tags    []Tag `gorm:"many2many:post_tags;"`\n}\n\ntype Tag struct {\n    gorm.Model\n    Name  string `gorm:"uniqueIndex;size:50"`\n    Posts []Post `gorm:"many2many:post_tags;"`\n}\n\nfunc createUser(db *gorm.DB, name, email string) (User, error) {\n    u := User{Name: name, Email: email}\n    result := db.Create(&u)\n    return u, result.Error\n}\n\nfunc createPost(db *gorm.DB, userID uint, title, content string, tagNames []string) (Post, error) {\n    var tags []Tag\n    for _, name := range tagNames {\n        var tag Tag\n        db.FirstOrCreate(&tag, Tag{Name: name})\n        tags = append(tags, tag)\n    }\n\n    post := Post{Title: title, Content: content, UserID: userID, Tags: tags}\n    result := db.Create(&post)\n    return post, result.Error\n}\n\nfunc getUserPosts(db *gorm.DB, userID uint) ([]Post, error) {\n    var posts []Post\n    result := db.Preload("Tags").Preload("User").Where("user_id = ?", userID).Find(&posts)\n    return posts, result.Error\n}\n\ntype UserPostCount struct {\n    UserID    uint\n    Name      string\n    PostCount int64\n}\n\nfunc getPostCountByUser(db *gorm.DB) ([]UserPostCount, error) {\n    var stats []UserPostCount\n    result := db.Model(&User{}).\n        Select("users.id as user_id, users.name, COUNT(posts.id) as post_count").\n        Joins("LEFT JOIN posts ON posts.user_id = users.id AND posts.deleted_at IS NULL").\n        Group("users.id, users.name").\n        Scan(&stats)\n    return stats, result.Error\n}\n\nfunc deletePost(db *gorm.DB, postID uint) error {\n    return db.Delete(&Post{}, postID).Error\n}\n\nfunc main() {\n    db, err := gorm.Open(sqlite.Open("blog.db"), &gorm.Config{\n        Logger: logger.Default.LogMode(logger.Silent),\n    })\n    if err != nil {\n        log.Fatal(err)\n    }\n\n    db.AutoMigrate(&User{}, &Post{}, &Tag{})\n\n    u1, err := createUser(db, "Нурдаулет", "n@example.com")\n    if err != nil {\n        log.Fatal(err)\n    }\n    fmt.Printf("Создан пользователь: %s (ID=%d)\\n", u1.Name, u1.ID)\n\n    u2, _ := createUser(db, "Айгерим", "a@example.com")\n\n    p1, err := createPost(db, u1.ID, "Go и GORM", "Статья о GORM...", []string{"Go", "Backend"})\n    if err != nil {\n        log.Fatal(err)\n    }\n    tagNames := make([]string, len(p1.Tags))\n    for i, t := range p1.Tags {\n        tagNames[i] = t.Name\n    }\n    fmt.Printf("Создан пост: \\"%s\\" (ID=%d) с тегами: %v\\n", p1.Title, p1.ID, tagNames)\n\n    createPost(db, u1.ID, "Горутины в Go", "Конкурентность...", []string{"Go", "Concurrency"})\n    createPost(db, u2.ID, "REST API на Go", "Создание API...", []string{"Go", "API"})\n\n    stats, _ := getPostCountByUser(db)\n    fmt.Println("Статистика:")\n    for _, s := range stats {\n        fmt.Printf("  %s: %d пост(а)\\n", s.Name, s.PostCount)\n    }\n}',
      explanation: 'GORM автоматически управляет связующей таблицей post_tags для ManyToMany. FirstOrCreate находит запись по условию или создаёт её — избегает дублирования тегов. Preload("Tags") загружает теги одним дополнительным SELECT, а не N запросами. Joins+Group позволяет делать агрегацию через удобный API без написания SQL вручную.'
    }
  ]
}
