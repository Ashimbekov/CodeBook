export default {
  id: 77,
  title: 'Тестирование: best practices',
  description: 'Лучшие практики тестирования в Go: table-driven tests, вспомогательные функции, тестовые фикстуры, моки через интерфейсы, интеграционные тесты, стратегия покрытия кода.',
  lessons: [
    {
      id: 1,
      title: 'Table-driven tests — стандарт Go',
      type: 'theory',
      content: [
        { type: 'text', value: 'Table-driven tests — стандартный подход в Go: один тест с таблицей входных данных и ожидаемых результатов. Избегает дублирования кода, легко добавлять новые случаи.' },
        { type: 'heading', value: 'Классический table-driven test' },
        { type: 'code', language: 'go', value: 'package math_test\n\nimport (\n    "errors"\n    "testing"\n)\n\nfunc Divide(a, b float64) (float64, error) {\n    if b == 0 {\n        return 0, errors.New("деление на ноль")\n    }\n    return a / b, nil\n}\n\nfunc TestDivide(t *testing.T) {\n    // Таблица тестовых случаев\n    tests := []struct {\n        name      string  // описание случая\n        a, b      float64 // входные данные\n        want      float64 // ожидаемый результат\n        wantErr   bool    // ожидаем ли ошибку\n    }{\n        {\n            name: "положительные числа",\n            a: 10, b: 2,\n            want: 5,\n        },\n        {\n            name: "деление на ноль",\n            a: 5, b: 0,\n            wantErr: true,\n        },\n        {\n            name: "отрицательный делитель",\n            a: 10, b: -2,\n            want: -5,\n        },\n        {\n            name: "ноль делённый на число",\n            a: 0, b: 5,\n            want: 0,\n        },\n    }\n\n    for _, tt := range tests {\n        // t.Run создаёт подтест с именем\n        t.Run(tt.name, func(t *testing.T) {\n            got, err := Divide(tt.a, tt.b)\n\n            if tt.wantErr {\n                if err == nil {\n                    t.Errorf("ожидали ошибку, но её нет")\n                }\n                return\n            }\n\n            if err != nil {\n                t.Fatalf("неожиданная ошибка: %v", err)\n            }\n\n            if got != tt.want {\n                t.Errorf("Divide(%v, %v) = %v, хотели %v",\n                    tt.a, tt.b, got, tt.want)\n            }\n        })\n    }\n}' },
        { type: 'tip', value: 'Называй тестовые случаи с именем: tests := []struct{ name string; ... }. Тогда в выводе будет "TestDivide/деление_на_ноль" — понятно что упало. go test -run TestDivide/ноль запустит только этот случай.' }
      ]
    },
    {
      id: 2,
      title: 'Вспомогательные функции в тестах',
      type: 'theory',
      content: [
        { type: 'text', value: 'Вспомогательные функции (test helpers) уменьшают дублирование в тестах. В Go их принято помечать t.Helper() — тогда при ошибке указывается строка вызова, а не строка внутри helper.' },
        { type: 'heading', value: 'Хорошие test helpers' },
        { type: 'code', language: 'go', value: 'package service_test\n\nimport (\n    "context"\n    "testing"\n)\n\n// assertEqual — универсальный helper сравнения\nfunc assertEqual(t *testing.T, got, want interface{}) {\n    t.Helper() // При ошибке указывает на строку вызова, не на эту строку\n    if got != want {\n        t.Errorf("got %v, want %v", got, want)\n    }\n}\n\n// assertNoError — проверка отсутствия ошибки\nfunc assertNoError(t *testing.T, err error) {\n    t.Helper()\n    if err != nil {\n        t.Fatalf("неожиданная ошибка: %v", err)\n    }\n}\n\n// assertError — проверка наличия ошибки\nfunc assertError(t *testing.T, err error, want error) {\n    t.Helper()\n    if err == nil {\n        t.Fatal("ожидали ошибку, но её нет")\n    }\n    if want != nil {\n        if !errors.Is(err, want) {\n            t.Errorf("ошибка %v, хотели %v", err, want)\n        }\n    }\n}\n\n// newTestUser — фабрика тестового пользователя\nfunc newTestUser(t *testing.T, name, email string) *User {\n    t.Helper()\n    user, err := NewUser(name, email)\n    if err != nil {\n        t.Fatalf("не удалось создать тестового пользователя: %v", err)\n    }\n    return user\n}\n\n// Использование в тестах:\nfunc TestUserService_GetUser(t *testing.T) {\n    user := newTestUser(t, "Нурдаулет", "nur@example.com")\n\n    svc := NewUserService(NewMockRepo())\n    ctx := context.Background()\n\n    got, err := svc.GetUser(ctx, user.ID)\n    assertNoError(t, err)\n    assertEqual(t, got.Email, user.Email)\n    assertEqual(t, got.Name, user.Name)\n}' },
        { type: 'note', value: 't.Helper() важен: без него при ошибке в assertEqual будет указана строка внутри assertEqual, а не строка теста, где вызвали assertEqual. С t.Helper() сразу видно, в каком тесте упало.' }
      ]
    },
    {
      id: 3,
      title: 'Тестовые фикстуры',
      type: 'theory',
      content: [
        { type: 'text', value: 'Тестовые фикстуры — данные для тестов. В Go фикстуры размещают в директории testdata/ или инициализируют в TestMain. t.TempDir() создаёт временную директорию, очищаемую после теста.' },
        { type: 'heading', value: 'Файловые фикстуры и testdata' },
        { type: 'code', language: 'go', value: '// Структура проекта:\n// myapp/\n// ├── parser/\n// │   ├── parser.go\n// │   ├── parser_test.go\n// │   └── testdata/\n// │       ├── valid_config.json\n// │       ├── invalid_config.json\n// │       └── expected_output.txt\n\n// parser_test.go\npackage parser_test\n\nimport (\n    "os"\n    "path/filepath"\n    "testing"\n)\n\nfunc TestParseConfig_ValidFile(t *testing.T) {\n    // testdata/ — конвенция Go для тестовых данных\n    data, err := os.ReadFile(filepath.Join("testdata", "valid_config.json"))\n    if err != nil {\n        t.Fatalf("не удалось прочитать фикстуру: %v", err)\n    }\n\n    config, err := ParseConfig(data)\n    if err != nil {\n        t.Fatalf("ParseConfig: %v", err)\n    }\n\n    if config.Name != "test-app" {\n        t.Errorf("Name = %q, want %q", config.Name, "test-app")\n    }\n}\n\n// t.TempDir() — временная директория, автоматически удаляется\nfunc TestWriteFile(t *testing.T) {\n    tmpDir := t.TempDir() // Go создаёт и удалит после теста\n\n    path := filepath.Join(tmpDir, "output.txt")\n    err := WriteOutputFile(path, "hello")\n    if err != nil {\n        t.Fatal(err)\n    }\n\n    content, _ := os.ReadFile(path)\n    if string(content) != "hello" {\n        t.Errorf("got %q, want %q", content, "hello")\n    }\n}\n\n// TestMain — глобальная инициализация для всего пакета\nfunc TestMain(m *testing.M) {\n    // Инициализация перед всеми тестами\n    setupTestDB()\n\n    code := m.Run() // Запустить тесты\n\n    // Очистка после всех тестов\n    teardownTestDB()\n\n    os.Exit(code)\n}\n\nfunc setupTestDB() { /* ... */ }\nfunc teardownTestDB() { /* ... */ }' },
        { type: 'tip', value: 'Директория testdata/ — специальная конвенция Go: файлы в ней игнорируются компилятором, но доступны в тестах через относительный путь. Фикстуры в testdata/ — стандарт стандартной библиотеки.' }
      ]
    },
    {
      id: 4,
      title: 'Моки через интерфейсы',
      type: 'theory',
      content: [
        { type: 'text', value: 'В Go моки создаются через интерфейсы: тест передаёт фиктивную реализацию вместо реальной. Можно писать вручную или использовать github.com/stretchr/testify/mock.' },
        { type: 'heading', value: 'Ручной мок vs testify/mock' },
        { type: 'code', language: 'go', value: 'package service_test\n\nimport (\n    "context"\n    "errors"\n    "testing"\n)\n\n// === Ручной мок — просто и понятно ===\n\ntype mockEmailSender struct {\n    sentTo   []string\n    sendErr  error\n    sendCount int\n}\n\nfunc (m *mockEmailSender) Send(_ context.Context, to, subject, body string) error {\n    if m.sendErr != nil {\n        return m.sendErr\n    }\n    m.sentTo = append(m.sentTo, to)\n    m.sendCount++\n    return nil\n}\n\nfunc TestUserService_Register_SendsEmail(t *testing.T) {\n    mailer := &mockEmailSender{}\n    repo := NewMockUserRepo()\n    svc := NewUserService(repo, mailer)\n\n    _, err := svc.Register(context.Background(), "test@example.com", "Алия")\n    if err != nil {\n        t.Fatal(err)\n    }\n\n    // Проверяем, что письмо было отправлено\n    if mailer.sendCount != 1 {\n        t.Errorf("ожидали 1 письмо, отправлено %d", mailer.sendCount)\n    }\n    if len(mailer.sentTo) == 0 || mailer.sentTo[0] != "test@example.com" {\n        t.Errorf("письмо отправлено не тому адресату: %v", mailer.sentTo)\n    }\n}\n\nfunc TestUserService_Register_EmailFails(t *testing.T) {\n    // Симулируем отказ mailer\n    mailer := &mockEmailSender{sendErr: errors.New("SMTP недоступен")}\n    repo := NewMockUserRepo()\n    svc := NewUserService(repo, mailer)\n\n    // Регистрация должна пройти даже при отказе mailer\n    user, err := svc.Register(context.Background(), "user@example.com", "Бек")\n    if err != nil {\n        t.Errorf("ошибка mailer не должна блокировать регистрацию: %v", err)\n    }\n    if user == nil {\n        t.Fatal("пользователь должен быть создан")\n    }\n}' },
        { type: 'tip', value: 'Ручные моки проще и без зависимостей. testify/mock подходит, когда нужны сложные проверки: "был ли вызван с такими-то аргументами", "сколько раз вызван" и т.д.' }
      ]
    },
    {
      id: 5,
      title: 'Интеграционные тесты',
      type: 'theory',
      content: [
        { type: 'text', value: 'Интеграционные тесты проверяют взаимодействие нескольких компонентов или с внешними системами (БД, HTTP). В Go их помечают build tag или флагом -short.' },
        { type: 'heading', value: 'Организация интеграционных тестов' },
        { type: 'code', language: 'go', value: '//go:build integration\n// +build integration\n\n// Запуск только с: go test -tags=integration ./...\n\npackage repository_test\n\nimport (\n    "context"\n    "os"\n    "testing"\n\n    "github.com/jmoiron/sqlx"\n    _ "github.com/lib/pq"\n)\n\nvar testDB *sqlx.DB\n\nfunc TestMain(m *testing.M) {\n    dsn := os.Getenv("TEST_DATABASE_URL")\n    if dsn == "" {\n        dsn = "postgres://localhost/testdb?sslmode=disable"\n    }\n\n    var err error\n    testDB, err = sqlx.Connect("postgres", dsn)\n    if err != nil {\n        // В интеграционных тестах — можно skip, если БД недоступна\n        panic("не удалось подключиться к тестовой БД: " + err.Error())\n    }\n\n    code := m.Run()\n    testDB.Close()\n    os.Exit(code)\n}\n\nfunc TestUserRepo_CreateAndGet(t *testing.T) {\n    // Транзакция, откатываемая в конце теста — чистая БД\n    tx, err := testDB.BeginTxx(context.Background(), nil)\n    if err != nil {\n        t.Fatal(err)\n    }\n    defer tx.Rollback() // Всегда откатываем после теста\n\n    repo := NewUserRepo(tx)\n\n    user := &User{Email: "test@example.com", Name: "Тест"}\n    err = repo.Create(context.Background(), user)\n    if err != nil {\n        t.Fatalf("Create: %v", err)\n    }\n    if user.ID == 0 {\n        t.Fatal("ID должен быть установлен после Create")\n    }\n\n    got, err := repo.GetByID(context.Background(), user.ID)\n    if err != nil {\n        t.Fatalf("GetByID: %v", err)\n    }\n    if got.Email != user.Email {\n        t.Errorf("Email: got %q, want %q", got.Email, user.Email)\n    }\n}' },
        { type: 'note', value: 'Паттерн "транзакция в тесте + defer Rollback" обеспечивает изоляцию: каждый тест видит чистую БД, не зависит от порядка выполнения тестов и не оставляет мусора.' }
      ]
    },
    {
      id: 6,
      title: 'Стратегия покрытия кода',
      type: 'theory',
      content: [
        { type: 'text', value: '100% покрытие не равно 100% правильности. Стратегия: тестируй бизнес-логику подробно, инфраструктуру — интеграционно, хендлеры — через httptest.' },
        { type: 'heading', value: 'Измерение и улучшение покрытия' },
        { type: 'code', language: 'go', value: '// Команды для работы с покрытием:\n// go test -coverprofile=coverage.out ./...\n// go tool cover -html=coverage.out    // Открыть HTML отчёт\n// go tool cover -func=coverage.out   // Покрытие по функциям\n\n// Тестирование HTTP хендлера через httptest\npackage handler_test\n\nimport (\n    "encoding/json"\n    "net/http"\n    "net/http/httptest"\n    "strings"\n    "testing"\n)\n\nfunc TestUserHandler_Create(t *testing.T) {\n    tests := []struct {\n        name       string\n        body       string\n        wantStatus int\n        wantEmail  string\n    }{\n        {\n            name:       "успешное создание",\n            body:       `{"email":"test@example.com","name":"Алия"}`,\n            wantStatus: http.StatusCreated,\n            wantEmail:  "test@example.com",\n        },\n        {\n            name:       "пустой email",\n            body:       `{"email":"","name":"Алия"}`,\n            wantStatus: http.StatusBadRequest,\n        },\n        {\n            name:       "невалидный JSON",\n            body:       `{не_json`,\n            wantStatus: http.StatusBadRequest,\n        },\n    }\n\n    for _, tt := range tests {\n        t.Run(tt.name, func(t *testing.T) {\n            svc := NewMockUserService()\n            h := NewUserHandler(svc)\n\n            req := httptest.NewRequest(\n                http.MethodPost,\n                "/users",\n                strings.NewReader(tt.body),\n            )\n            req.Header.Set("Content-Type", "application/json")\n\n            w := httptest.NewRecorder()\n            h.Create(w, req)\n\n            if w.Code != tt.wantStatus {\n                t.Errorf("статус: got %d, want %d", w.Code, tt.wantStatus)\n            }\n\n            if tt.wantEmail != "" {\n                var resp map[string]interface{}\n                json.NewDecoder(w.Body).Decode(&resp)\n                if resp["email"] != tt.wantEmail {\n                    t.Errorf("email: got %v, want %s", resp["email"], tt.wantEmail)\n                }\n            }\n        })\n    }\n}' },
        { type: 'tip', value: 'Цель: 80% покрытие бизнес-логики (use cases, domain). Для repositories — интеграционные тесты или тесты с test containers. Для handlers — httptest. Не гонись за 100% в тривиальных геттерах.' }
      ]
    },
    {
      id: 7,
      title: 'Практика: полный тест-сьют для сервиса',
      type: 'practice',
      difficulty: 'hard',
      description: 'Напиши полный набор тестов для OrderService: table-driven тесты, helpers, моки через интерфейсы, тест HTTP хендлера.',
      requirements: [
        'OrderService{repo OrderRepository}: CreateOrder(ctx, userID, items), GetOrder(ctx, id)',
        'Table-driven тест TestCreateOrder с случаями: успех, пустые items, несуществующий пользователь',
        'Helper assertOrder(t, got, want) с t.Helper()',
        'MockOrderRepo: реализует OrderRepository, хранит в памяти',
        'Тест TestOrderHandler_Get: через httptest, проверяет 200 и 404'
      ],
      expectedOutput: '--- PASS: TestCreateOrder/успех\n--- PASS: TestCreateOrder/пустые_items\n--- PASS: TestCreateOrder/несуществующий_пользователь\n--- PASS: TestOrderHandler_Get/существующий_заказ\n--- PASS: TestOrderHandler_Get/несуществующий_заказ\nok  myapp/order',
      hint: 'MockOrderRepo хранит orders map[int64]*Order. TestOrderHandler_Get использует httptest.NewRequest и httptest.NewRecorder. В таблице случаев используй поля wantErr bool и wantErrIs error для проверки типа ошибки.',
      solution: 'package main\n\nimport (\n    "context"\n    "encoding/json"\n    "errors"\n    "fmt"\n    "net/http"\n    "net/http/httptest"\n    "strconv"\n    "strings"\n    "testing"\n)\n\n// === Domain ===\n\ntype Order struct {\n    ID     int64    `json:"id"`\n    UserID int64    `json:"user_id"`\n    Items  []string `json:"items"`\n}\n\nvar (\n    ErrOrderNotFound  = errors.New("заказ не найден")\n    ErrEmptyItems     = errors.New("список товаров пуст")\n    ErrUserNotFound   = errors.New("пользователь не найден")\n)\n\n// === Repository ===\n\ntype OrderRepository interface {\n    Create(ctx context.Context, o *Order) error\n    GetByID(ctx context.Context, id int64) (*Order, error)\n}\n\ntype MockOrderRepo struct {\n    orders  map[int64]*Order\n    counter int64\n    userErr error // для симуляции ошибки пользователя\n}\n\nfunc NewMockOrderRepo() *MockOrderRepo {\n    return &MockOrderRepo{orders: make(map[int64]*Order)}\n}\n\nfunc (m *MockOrderRepo) Create(ctx context.Context, o *Order) error {\n    if m.userErr != nil {\n        return m.userErr\n    }\n    m.counter++\n    o.ID = m.counter\n    cp := *o\n    m.orders[o.ID] = &cp\n    return nil\n}\n\nfunc (m *MockOrderRepo) GetByID(ctx context.Context, id int64) (*Order, error) {\n    o, ok := m.orders[id]\n    if !ok {\n        return nil, ErrOrderNotFound\n    }\n    cp := *o\n    return &cp, nil\n}\n\n// === Service ===\n\ntype OrderService struct {\n    repo OrderRepository\n}\n\nfunc NewOrderService(repo OrderRepository) *OrderService {\n    return &OrderService{repo: repo}\n}\n\nfunc (s *OrderService) CreateOrder(ctx context.Context, userID int64, items []string) (*Order, error) {\n    if len(items) == 0 {\n        return nil, ErrEmptyItems\n    }\n    o := &Order{UserID: userID, Items: items}\n    if err := s.repo.Create(ctx, o); err != nil {\n        return nil, fmt.Errorf("CreateOrder: %w", err)\n    }\n    return o, nil\n}\n\nfunc (s *OrderService) GetOrder(ctx context.Context, id int64) (*Order, error) {\n    o, err := s.repo.GetByID(ctx, id)\n    if err != nil {\n        return nil, fmt.Errorf("GetOrder: %w", err)\n    }\n    return o, nil\n}\n\n// === Handler ===\n\ntype OrderHandler struct {\n    svc *OrderService\n}\n\nfunc NewOrderHandler(svc *OrderService) *OrderHandler {\n    return &OrderHandler{svc: svc}\n}\n\nfunc (h *OrderHandler) Get(w http.ResponseWriter, r *http.Request) {\n    parts := strings.Split(r.URL.Path, "/")\n    id, err := strconv.ParseInt(parts[len(parts)-1], 10, 64)\n    if err != nil {\n        http.Error(w, "bad id", http.StatusBadRequest)\n        return\n    }\n    o, err := h.svc.GetOrder(r.Context(), id)\n    if err != nil {\n        if errors.Is(err, ErrOrderNotFound) {\n            http.Error(w, "not found", http.StatusNotFound)\n            return\n        }\n        http.Error(w, "internal error", http.StatusInternalServerError)\n        return\n    }\n    w.Header().Set("Content-Type", "application/json")\n    json.NewEncoder(w).Encode(o)\n}\n\n// === Test helpers ===\n\nfunc assertNoError(t *testing.T, err error) {\n    t.Helper()\n    if err != nil {\n        t.Fatalf("неожиданная ошибка: %v", err)\n    }\n}\n\nfunc assertOrder(t *testing.T, got *Order, wantUserID int64, wantItems []string) {\n    t.Helper()\n    if got == nil {\n        t.Fatal("заказ nil")\n    }\n    if got.UserID != wantUserID {\n        t.Errorf("UserID: got %d, want %d", got.UserID, wantUserID)\n    }\n    if len(got.Items) != len(wantItems) {\n        t.Errorf("Items len: got %d, want %d", len(got.Items), len(wantItems))\n    }\n}\n\n// === Tests ===\n\nfunc TestCreateOrder(t *testing.T) {\n    tests := []struct {\n        name      string\n        userID    int64\n        items     []string\n        repoErr   error\n        wantErr   bool\n        wantErrIs error\n    }{\n        {\n            name:   "успех",\n            userID: 1,\n            items:  []string{"товар1", "товар2"},\n        },\n        {\n            name:      "пустые items",\n            userID:    1,\n            items:     []string{},\n            wantErr:   true,\n            wantErrIs: ErrEmptyItems,\n        },\n        {\n            name:      "несуществующий пользователь",\n            userID:    999,\n            items:     []string{"товар"},\n            repoErr:   ErrUserNotFound,\n            wantErr:   true,\n            wantErrIs: ErrUserNotFound,\n        },\n    }\n\n    for _, tt := range tests {\n        t.Run(tt.name, func(t *testing.T) {\n            repo := NewMockOrderRepo()\n            repo.userErr = tt.repoErr\n            svc := NewOrderService(repo)\n\n            o, err := svc.CreateOrder(context.Background(), tt.userID, tt.items)\n\n            if tt.wantErr {\n                if err == nil {\n                    t.Fatal("ожидали ошибку")\n                }\n                if tt.wantErrIs != nil && !errors.Is(err, tt.wantErrIs) {\n                    t.Errorf("ошибка: got %v, want %v", err, tt.wantErrIs)\n                }\n                fmt.Printf("--- PASS: TestCreateOrder/%s\\n", tt.name)\n                return\n            }\n\n            assertNoError(t, err)\n            assertOrder(t, o, tt.userID, tt.items)\n            fmt.Printf("--- PASS: TestCreateOrder/%s\\n", tt.name)\n        })\n    }\n}\n\nfunc TestOrderHandler_Get(t *testing.T) {\n    repo := NewMockOrderRepo()\n    svc := NewOrderService(repo)\n    h := NewOrderHandler(svc)\n\n    // Создаём тестовый заказ\n    o, _ := svc.CreateOrder(context.Background(), 1, []string{"книга"})\n\n    tests := []struct {\n        name       string\n        path       string\n        wantStatus int\n    }{\n        {\n            name:       "существующий заказ",\n            path:       fmt.Sprintf("/orders/%d", o.ID),\n            wantStatus: http.StatusOK,\n        },\n        {\n            name:       "несуществующий заказ",\n            path:       "/orders/999",\n            wantStatus: http.StatusNotFound,\n        },\n    }\n\n    for _, tt := range tests {\n        t.Run(tt.name, func(t *testing.T) {\n            req := httptest.NewRequest("GET", tt.path, nil)\n            w := httptest.NewRecorder()\n            h.Get(w, req)\n\n            if w.Code != tt.wantStatus {\n                t.Errorf("статус: got %d, want %d", w.Code, tt.wantStatus)\n            }\n            fmt.Printf("--- PASS: TestOrderHandler_Get/%s\\n", tt.name)\n        })\n    }\n}\n\nfunc main() {\n    t := &testing.T{}\n    TestCreateOrder(t)\n    TestOrderHandler_Get(t)\n    fmt.Println("ok  myapp/order")\n}',
      explanation: 'Table-driven тест TestCreateOrder покрывает 3 сценария в одном цикле. assertNoError и assertOrder — helpers с t.Helper() для читаемых сообщений об ошибках. MockOrderRepo реализует OrderRepository через интерфейс — сервис не знает о моке. TestOrderHandler_Get использует httptest без запуска реального сервера. errors.Is в тесте проверяет sentinel ошибки через цепочку fmt.Errorf("%w").'
    }
  ]
}
