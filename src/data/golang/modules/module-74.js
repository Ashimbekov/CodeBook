export default {
  id: 74,
  title: 'SOLID принципы в Go',
  description: 'Применение SOLID принципов в Go: Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion. Для каждого принципа — плохой пример и правильное решение.',
  lessons: [
    {
      id: 1,
      title: 'SRP — принцип единственной ответственности',
      type: 'theory',
      content: [
        { type: 'text', value: 'Single Responsibility Principle: каждый тип или функция должна иметь одну причину для изменения. "Одну ответственность" — не значит "одно действие", а значит "одна область изменений".' },
        { type: 'heading', value: 'ПЛОХО: структура делает слишком много' },
        { type: 'code', language: 'go', value: '// ПЛОХО: UserManager отвечает за бизнес-логику, БД и HTTP одновременно\ntype UserManager struct {\n    db *sql.DB\n}\n\n// Метод 1: бизнес-логика + работа с БД\nfunc (um *UserManager) CreateUser(email, password string) error {\n    hash, _ := bcrypt.GenerateFromPassword([]byte(password), 14)\n    _, err := um.db.Exec("INSERT INTO users VALUES ($1, $2)", email, hash)\n    return err\n}\n\n// Метод 2: HTTP-ответ — совсем другая ответственность!\nfunc (um *UserManager) WriteUserJSON(w http.ResponseWriter, id int) {\n    row := um.db.QueryRow("SELECT email FROM users WHERE id=$1", id)\n    var email string\n    row.Scan(&email)\n    json.NewEncoder(w).Encode(map[string]string{"email": email})\n}\n\n// Метод 3: отправка email — третья ответственность!\nfunc (um *UserManager) SendWelcomeEmail(email string) error {\n    // ... SMTP логика ...\n    return nil\n}\n// Если меняется структура БД, HTTP формат, или SMTP провайдер —\n// все три причины затрагивают один класс. Это нарушение SRP.' },
        { type: 'heading', value: 'ХОРОШО: каждый тип — одна ответственность' },
        { type: 'code', language: 'go', value: '// ХОРОШО: разделили по ответственностям\n\n// 1. Репозиторий — только хранение\ntype UserRepo struct{ db *sql.DB }\n\nfunc (r *UserRepo) Save(email, passwordHash string) error {\n    _, err := r.db.Exec("INSERT INTO users VALUES ($1, $2)", email, passwordHash)\n    return err\n}\n\nfunc (r *UserRepo) FindByID(id int) (string, error) {\n    var email string\n    err := r.db.QueryRow("SELECT email FROM users WHERE id=$1", id).Scan(&email)\n    return email, err\n}\n\n// 2. Сервис — только бизнес-логика\ntype UserService struct{ repo *UserRepo }\n\nfunc (s *UserService) Register(email, password string) error {\n    hash, _ := bcrypt.GenerateFromPassword([]byte(password), 14)\n    return s.repo.Save(email, string(hash))\n}\n\n// 3. Хендлер — только HTTP\ntype UserHandler struct{ svc *UserService }\n\nfunc (h *UserHandler) GetUser(w http.ResponseWriter, r *http.Request) {\n    // ... только HTTP логика ...\n}\n\n// 4. Mailer — только отправка email\ntype Mailer struct{ smtpHost string }\n\nfunc (m *Mailer) SendWelcome(email string) error {\n    // ... только SMTP ...\n    return nil\n}\n// Теперь каждый тип изменяется по одной причине.' },
        { type: 'tip', value: 'Хороший индикатор нарушения SRP: если описание типа содержит слово "и" — "хранит данных И отправляет email И обрабатывает HTTP" — это три ответственности.' }
      ]
    },
    {
      id: 2,
      title: 'OCP — принцип открытости/закрытости',
      type: 'theory',
      content: [
        { type: 'text', value: 'Open/Closed Principle: код должен быть открыт для расширения, но закрыт для модификации. В Go это реализуется через интерфейсы: новое поведение добавляется новыми типами, а не изменением существующих.' },
        { type: 'heading', value: 'ПЛОХО: добавление нового типа требует изменения существующего кода' },
        { type: 'code', language: 'go', value: '// ПЛОХО: каждый новый тип скидки требует изменения CalculateDiscount\ntype DiscountType int\n\nconst (\n    NoDiscount      DiscountType = iota\n    SeasonalDiscount\n    LoyaltyDiscount\n    CouponDiscount\n    // Добавить новый тип? Нужно менять CalculateDiscount!\n)\n\nfunc CalculateDiscount(price float64, dt DiscountType, param interface{}) float64 {\n    switch dt {\n    case NoDiscount:\n        return price\n    case SeasonalDiscount:\n        return price * 0.9 // -10%\n    case LoyaltyDiscount:\n        years := param.(int)\n        return price * (1 - float64(years)*0.02)\n    case CouponDiscount:\n        code := param.(string)\n        if code == "SAVE20" {\n            return price * 0.8\n        }\n        return price\n    // Новый тип: FlashSaleDiscount — нужно лезть сюда и добавлять case!\n    default:\n        return price\n    }\n}' },
        { type: 'heading', value: 'ХОРОШО: новые типы без изменения существующего кода' },
        { type: 'code', language: 'go', value: '// ХОРОШО: Discount — интерфейс, новые типы не требуют изменений\n\ntype Discount interface {\n    Apply(price float64) float64\n}\n\n// Существующие реализации\ntype NoDiscount struct{}\nfunc (d NoDiscount) Apply(price float64) float64 { return price }\n\ntype SeasonalDiscount struct{ Percent float64 }\nfunc (d SeasonalDiscount) Apply(price float64) float64 {\n    return price * (1 - d.Percent/100)\n}\n\ntype LoyaltyDiscount struct{ YearsOfService int }\nfunc (d LoyaltyDiscount) Apply(price float64) float64 {\n    return price * (1 - float64(d.YearsOfService)*0.02)\n}\n\n// Новый тип — просто новая структура, ничего не меняем!\ntype FlashSaleDiscount struct{ HalfPrice bool }\nfunc (d FlashSaleDiscount) Apply(price float64) float64 {\n    if d.HalfPrice {\n        return price / 2\n    }\n    return price * 0.7\n}\n\n// CalculateTotal закрыта для изменений — открыта для расширения\nfunc CalculateTotal(price float64, discounts []Discount) float64 {\n    result := price\n    for _, d := range discounts {\n        result = d.Apply(result)\n    }\n    return result\n}\n\n// Использование:\n// total := CalculateTotal(1000, []Discount{\n//     SeasonalDiscount{Percent: 10},\n//     LoyaltyDiscount{YearsOfService: 3},\n// })' }
      ]
    },
    {
      id: 3,
      title: 'LSP — принцип подстановки Лисков',
      type: 'theory',
      content: [
        { type: 'text', value: 'Liskov Substitution Principle: если функция работает с интерфейсом, она должна корректно работать с любой его реализацией. Реализация не должна "сужать" контракт интерфейса.' },
        { type: 'heading', value: 'ПЛОХО: реализация нарушает контракт интерфейса' },
        { type: 'code', language: 'go', value: '// ПЛОХО: реализация ReadOnlyRepo нарушает контракт Repository\ntype Repository interface {\n    Save(item string) error\n    GetAll() []string\n}\n\ntype InMemoryRepo struct{ items []string }\nfunc (r *InMemoryRepo) Save(item string) error {\n    r.items = append(r.items, item)\n    return nil\n}\nfunc (r *InMemoryRepo) GetAll() []string { return r.items }\n\n// ReadOnlyRepo "реализует" интерфейс, но Save паникует!\ntype ReadOnlyRepo struct{ items []string }\nfunc (r *ReadOnlyRepo) Save(item string) error {\n    panic("запись запрещена!") // НАРУШЕНИЕ LSP!\n}\nfunc (r *ReadOnlyRepo) GetAll() []string { return r.items }\n\nfunc ProcessItems(repo Repository) {\n    repo.Save("item1") // Паника при ReadOnlyRepo — нарушение LSP\n}' },
        { type: 'heading', value: 'ХОРОШО: разделить интерфейсы по возможностям' },
        { type: 'code', language: 'go', value: '// ХОРОШО: разные интерфейсы для разных возможностей\n\ntype Reader interface {\n    GetAll() []string\n}\n\ntype Writer interface {\n    Save(item string) error\n}\n\ntype ReadWriter interface {\n    Reader\n    Writer\n}\n\ntype InMemoryRepo struct{ items []string }\nfunc (r *InMemoryRepo) Save(item string) error {\n    r.items = append(r.items, item)\n    return nil\n}\nfunc (r *InMemoryRepo) GetAll() []string { return r.items }\n\ntype ReadOnlyRepo struct{ items []string }\nfunc (r *ReadOnlyRepo) GetAll() []string { return r.items }\n// ReadOnlyRepo не реализует Writer — и это правильно!\n\n// Функции требуют только то, что им нужно\nfunc DisplayItems(r Reader) {\n    for _, item := range r.GetAll() {\n        fmt.Println(item)\n    }\n}\n\nfunc AddItem(w Writer, item string) error {\n    return w.Save(item)\n}\n\n// DisplayItems работает и с InMemoryRepo, и с ReadOnlyRepo\n// — LSP соблюдён' }
      ]
    },
    {
      id: 4,
      title: 'ISP — принцип разделения интерфейсов',
      type: 'theory',
      content: [
        { type: 'text', value: 'Interface Segregation Principle: лучше много маленьких интерфейсов, чем один большой. Клиент не должен зависеть от методов, которые он не использует. В Go ISP — это культура маленьких интерфейсов.' },
        { type: 'heading', value: 'ПЛОХО: жирный интерфейс' },
        { type: 'code', language: 'go', value: '// ПЛОХО: один большой интерфейс для всего\ntype UserStorage interface {\n    CreateUser(u User) error\n    GetUser(id int) (User, error)\n    UpdateUser(u User) error\n    DeleteUser(id int) error\n    ListUsers(page, size int) ([]User, error)\n    SearchUsers(query string) ([]User, error)\n    GetUserStats(id int) (UserStats, error)\n    ExportUsers(format string) ([]byte, error)\n    ImportUsers(data []byte) error\n    // ... ещё 10 методов ...\n}\n\n// Функция нужна только Get — но вынуждена работать со всем интерфейсом\nfunc ShowUserProfile(storage UserStorage, id int) {\n    user, _ := storage.GetUser(id)\n    fmt.Println(user.Name)\n    // Export, Import, Stats — нам не нужны, но Mock должен их реализовать\n}' },
        { type: 'heading', value: 'ХОРОШО: маленькие интерфейсы' },
        { type: 'code', language: 'go', value: '// ХОРОШО: маленькие интерфейсы по роли\n\ntype UserGetter interface {\n    GetUser(id int) (User, error)\n}\n\ntype UserCreator interface {\n    CreateUser(u User) error\n}\n\ntype UserLister interface {\n    ListUsers(page, size int) ([]User, error)\n}\n\ntype UserSearcher interface {\n    SearchUsers(query string) ([]User, error)\n}\n\n// Можно комбинировать при необходимости\ntype UserRepository interface {\n    UserGetter\n    UserCreator\n    UserLister\n}\n\n// Функции берут только то, что им нужно\nfunc ShowUserProfile(g UserGetter, id int) {\n    user, _ := g.GetUser(id)\n    fmt.Println(user.Name)\n    // Mock для теста: только один метод GetUser\n}\n\nfunc RegisterUser(c UserCreator, u User) error {\n    return c.CreateUser(u)\n}\n\n// io.Reader — идеальный пример из stdlib Go:\n// type Reader interface { Read(p []byte) (n int, err error) }\n// Один метод — полная composability!' },
        { type: 'tip', value: 'Стандартная библиотека Go — идеальный пример ISP: io.Reader (1 метод), io.Writer (1 метод), io.Closer (1 метод), io.ReadWriter (2 метода). Следуй этому примеру.' }
      ]
    },
    {
      id: 5,
      title: 'DIP — принцип инверсии зависимостей',
      type: 'theory',
      content: [
        { type: 'text', value: 'Dependency Inversion Principle: модули высокого уровня не должны зависеть от модулей низкого уровня. Оба должны зависеть от абстракций (интерфейсов). В Go DIP реализуется через интерфейсы и constructor injection.' },
        { type: 'heading', value: 'ПЛОХО: высокий уровень зависит от низкого' },
        { type: 'code', language: 'go', value: '// ПЛОХО: UserService (высокий уровень) зависит от PostgresDB (низкий уровень)\n\ntype PostgresUserDB struct{ db *sql.DB }\n\nfunc (p *PostgresUserDB) FindUser(id int) string {\n    var name string\n    p.db.QueryRow("SELECT name FROM users WHERE id=$1", id).Scan(&name)\n    return name\n}\n\n// UserService прямо зависит от PostgresUserDB — конкретной реализации!\ntype UserService struct {\n    db *PostgresUserDB // ПЛОХО: жёсткая зависимость\n}\n\nfunc NewUserService(db *PostgresUserDB) *UserService {\n    return &UserService{db: db}\n}\n\nfunc (s *UserService) GetUserName(id int) string {\n    return s.db.FindUser(id)\n    // Нельзя тестировать без реальной PostgreSQL\n    // Нельзя использовать MySQL или MongoDB\n    // UserService и PostgresUserDB "склеены" намертво\n}' },
        { type: 'heading', value: 'ХОРОШО: оба уровня зависят от интерфейса' },
        { type: 'code', language: 'go', value: '// ХОРОШО: оба зависят от абстракции\n\n// Абстракция (интерфейс) — определяем у потребителя\ntype UserFinder interface {\n    FindUser(id int) (string, error)\n}\n\n// Низкий уровень: реализует интерфейс\ntype PostgresUserDB struct{ db *sql.DB }\nfunc (p *PostgresUserDB) FindUser(id int) (string, error) {\n    var name string\n    err := p.db.QueryRow("SELECT name FROM users WHERE id=$1", id).Scan(&name)\n    return name, err\n}\n\ntype MongoUserDB struct{ col *mongo.Collection }\nfunc (m *MongoUserDB) FindUser(id int) (string, error) {\n    // ... MongoDB логика ...\n    return "User", nil\n}\n\n// Высокий уровень: зависит от интерфейса, не от конкретного типа\ntype UserService struct {\n    finder UserFinder // зависим от абстракции\n}\n\nfunc NewUserService(finder UserFinder) *UserService {\n    return &UserService{finder: finder}\n}\n\nfunc (s *UserService) GetUserName(id int) (string, error) {\n    return s.finder.FindUser(id)\n}\n\n// Тест: легко подменяется\ntype MockFinder struct{}\nfunc (m *MockFinder) FindUser(id int) (string, error) { return "Тест", nil }\n\n// PostgresUserDB, MongoUserDB, MockFinder — все реализуют UserFinder\n// UserService не изменяется при смене реализации' }
      ]
    },
    {
      id: 6,
      title: 'SOLID на практике — рефакторинг кода',
      type: 'theory',
      content: [
        { type: 'text', value: 'Рассмотрим реалистичный пример нарушения SOLID и поэтапный рефакторинг. Исходный код — "God object" отчётного сервиса.' },
        { type: 'heading', value: 'ПЛОХО: God object' },
        { type: 'code', language: 'go', value: '// ПЛОХО: ReportService нарушает SRP, OCP и DIP\ntype ReportService struct {\n    db     *sql.DB\n    mailer *smtp.Client\n}\n\nfunc (s *ReportService) GenerateAndSend(userID int, format string) error {\n    // Получение данных (зависимость от SQL)\n    rows, _ := s.db.Query("SELECT * FROM reports WHERE user_id=$1", userID)\n    var data []string\n    for rows.Next() {\n        var line string\n        rows.Scan(&line)\n        data = append(data, line)\n    }\n\n    // Форматирование (нарушение OCP: новый формат = менять код)\n    var content string\n    switch format {\n    case "csv":\n        content = strings.Join(data, "\\n")\n    case "json":\n        b, _ := json.Marshal(data)\n        content = string(b)\n    // Новый формат "xml" — нужно лезть сюда!\n    }\n\n    // Отправка (зависимость от smtp.Client)\n    return s.mailer.SendMail("reports@app.com", "Отчёт", content)\n}' },
        { type: 'heading', value: 'ХОРОШО: применение SOLID' },
        { type: 'code', language: 'go', value: '// ХОРОШО: каждый тип — одна ответственность, зависимости — от абстракций\n\n// SRP + DIP: репозиторий — только данные\ntype ReportRepository interface {\n    GetReportData(ctx context.Context, userID int) ([]string, error)\n}\n\n// OCP: новые форматы — новые типы\ntype ReportFormatter interface {\n    Format(data []string) (string, error)\n    ContentType() string\n}\n\ntype CSVFormatter struct{}\nfunc (f *CSVFormatter) Format(data []string) (string, error) {\n    return strings.Join(data, "\\n"), nil\n}\nfunc (f *CSVFormatter) ContentType() string { return "text/csv" }\n\ntype JSONFormatter struct{}\nfunc (f *JSONFormatter) Format(data []string) (string, error) {\n    b, err := json.Marshal(data)\n    return string(b), err\n}\nfunc (f *JSONFormatter) ContentType() string { return "application/json" }\n\n// Новый формат — просто новая структура!\ntype XMLFormatter struct{}\nfunc (f *XMLFormatter) Format(data []string) (string, error) {\n    return "<report>" + strings.Join(data, "") + "</report>", nil\n}\nfunc (f *XMLFormatter) ContentType() string { return "application/xml" }\n\n// SRP: отправщик — только отправка\ntype ReportSender interface {\n    Send(ctx context.Context, to, subject, body string) error\n}\n\n// ReportService: SRP (только оркестрация), DIP (все зависимости — интерфейсы)\ntype ReportService struct {\n    repo      ReportRepository\n    formatter ReportFormatter\n    sender    ReportSender\n}\n\nfunc NewReportService(r ReportRepository, f ReportFormatter, s ReportSender) *ReportService {\n    return &ReportService{repo: r, formatter: f, sender: s}\n}\n\nfunc (s *ReportService) GenerateAndSend(ctx context.Context, userID int, to string) error {\n    data, err := s.repo.GetReportData(ctx, userID)\n    if err != nil {\n        return fmt.Errorf("получение данных: %w", err)\n    }\n    content, err := s.formatter.Format(data)\n    if err != nil {\n        return fmt.Errorf("форматирование: %w", err)\n    }\n    return s.sender.Send(ctx, to, "Ваш отчёт", content)\n}' }
      ]
    },
    {
      id: 7,
      title: 'Практика: применить SOLID к коду',
      type: 'practice',
      difficulty: 'hard',
      description: 'Отрефакторируй нарушающий SOLID код PaymentProcessor. Примени SRP (разделить ответственности), OCP (добавить новый метод оплаты без изменений), DIP (зависеть от интерфейсов).',
      requirements: [
        'Исходный код: PaymentProcessor{db *sql.DB} с методом Process(amount, method string) - в одном методе: проверка лимита, проведение оплаты через if/else по методу, запись в БД, отправка email',
        'Применить SRP: разделить на LimitChecker, PaymentGateway, PaymentLogger, EmailNotifier',
        'Применить OCP: добавить новый метод оплаты "crypto" без изменения существующего кода',
        'Применить DIP: PaymentService зависит от интерфейсов, не от конкретных типов',
        'Написать тест, используя моки вместо реальных зависимостей'
      ],
      expectedOutput: 'Оплата 1000 через card: успешно\nОплата 500 через paypal: успешно\nОплата 200 через crypto: успешно\nТест с mock: PASS',
      hint: 'Начни с определения интерфейсов: что нужно PaymentService? LimitChecker.Check(amount), Gateway.Pay(amount, method), Logger.Log(payment), Notifier.Notify(email, amount). Каждый метод оплаты — отдельная структура, реализующая общий интерфейс.',
      solution: 'package main\n\nimport (\n    "context"\n    "errors"\n    "fmt"\n)\n\n// === Interfaces (DIP) ===\n\ntype LimitChecker interface {\n    Check(ctx context.Context, amount float64) error\n}\n\ntype PaymentGateway interface {\n    Pay(ctx context.Context, amount float64) error\n    Method() string\n}\n\ntype PaymentLogger interface {\n    Log(ctx context.Context, amount float64, method string, success bool)\n}\n\ntype PaymentNotifier interface {\n    Notify(ctx context.Context, amount float64, method string) error\n}\n\n// === Implementations (SRP) ===\n\ntype DefaultLimitChecker struct{ maxAmount float64 }\nfunc (c *DefaultLimitChecker) Check(_ context.Context, amount float64) error {\n    if amount > c.maxAmount {\n        return fmt.Errorf("сумма %.0f превышает лимит %.0f", amount, c.maxAmount)\n    }\n    return nil\n}\n\n// OCP: каждый метод оплаты — отдельная структура\ntype CardGateway struct{}\nfunc (g *CardGateway) Pay(_ context.Context, amount float64) error {\n    fmt.Printf("  [card] списываем %.0f\\n", amount)\n    return nil\n}\nfunc (g *CardGateway) Method() string { return "card" }\n\ntype PayPalGateway struct{}\nfunc (g *PayPalGateway) Pay(_ context.Context, amount float64) error {\n    fmt.Printf("  [paypal] перевод %.0f\\n", amount)\n    return nil\n}\nfunc (g *PayPalGateway) Method() string { return "paypal" }\n\n// Новый метод оплаты — просто новая структура! Ничего не меняем!\ntype CryptoGateway struct{}\nfunc (g *CryptoGateway) Pay(_ context.Context, amount float64) error {\n    fmt.Printf("  [crypto] транзакция %.0f\\n", amount)\n    return nil\n}\nfunc (g *CryptoGateway) Method() string { return "crypto" }\n\ntype ConsoleLogger struct{}\nfunc (l *ConsoleLogger) Log(_ context.Context, amount float64, method string, success bool) {\n    status := "OK"\n    if !success { status = "FAIL" }\n    fmt.Printf("  [log] %s: %.0f via %s\\n", status, amount, method)\n}\n\ntype ConsoleNotifier struct{}\nfunc (n *ConsoleNotifier) Notify(_ context.Context, amount float64, method string) error {\n    fmt.Printf("  [email] уведомление: %.0f via %s\\n", amount, method)\n    return nil\n}\n\n// === PaymentService (DIP + SRP) ===\n\ntype PaymentService struct {\n    checker  LimitChecker\n    gateways map[string]PaymentGateway\n    logger   PaymentLogger\n    notifier PaymentNotifier\n}\n\nfunc NewPaymentService(\n    checker LimitChecker,\n    logger PaymentLogger,\n    notifier PaymentNotifier,\n    gateways ...PaymentGateway,\n) *PaymentService {\n    gw := make(map[string]PaymentGateway)\n    for _, g := range gateways {\n        gw[g.Method()] = g\n    }\n    return &PaymentService{checker: checker, gateways: gw, logger: logger, notifier: notifier}\n}\n\nfunc (s *PaymentService) Process(ctx context.Context, amount float64, method string) error {\n    if err := s.checker.Check(ctx, amount); err != nil {\n        return err\n    }\n    gw, ok := s.gateways[method]\n    if !ok {\n        return fmt.Errorf("неизвестный метод оплаты: %s", method)\n    }\n    err := gw.Pay(ctx, amount)\n    s.logger.Log(ctx, amount, method, err == nil)\n    if err != nil {\n        return err\n    }\n    s.notifier.Notify(ctx, amount, method)\n    return nil\n}\n\n// === Mock for tests ===\n\ntype mockChecker struct{ err error }\nfunc (m *mockChecker) Check(_ context.Context, _ float64) error { return m.err }\n\ntype mockGateway struct{ method string; err error; called bool }\nfunc (m *mockGateway) Pay(_ context.Context, _ float64) error { m.called = true; return m.err }\nfunc (m *mockGateway) Method() string { return m.method }\n\ntype mockLogger struct{}\nfunc (m *mockLogger) Log(_ context.Context, _ float64, _ string, _ bool) {}\n\ntype mockNotifier struct{ called bool }\nfunc (m *mockNotifier) Notify(_ context.Context, _ float64, _ string) error {\n    m.called = true; return nil\n}\n\nfunc TestProcess(t interface{}) {\n    gw := &mockGateway{method: "card"}\n    notif := &mockNotifier{}\n    svc := NewPaymentService(\n        &mockChecker{},\n        &mockLogger{},\n        notif,\n        gw,\n    )\n    ctx := context.Background()\n    err := svc.Process(ctx, 100, "card")\n    if err != nil {\n        fmt.Println("FAIL:", err)\n        return\n    }\n    if !gw.called { fmt.Println("FAIL: gateway not called"); return }\n    if !notif.called { fmt.Println("FAIL: notifier not called"); return }\n\n    limitErr := errors.New("превышен лимит")\n    svc2 := NewPaymentService(&mockChecker{err: limitErr}, &mockLogger{}, &mockNotifier{}, gw)\n    err = svc2.Process(ctx, 99999, "card")\n    if err == nil { fmt.Println("FAIL: ожидали ошибку лимита"); return }\n\n    fmt.Println("Тест с mock: PASS")\n}\n\nfunc main() {\n    ctx := context.Background()\n    svc := NewPaymentService(\n        &DefaultLimitChecker{maxAmount: 10000},\n        &ConsoleLogger{},\n        &ConsoleNotifier{},\n        &CardGateway{},\n        &PayPalGateway{},\n        &CryptoGateway{},\n    )\n\n    for _, tc := range []struct{ amount float64; method string }{\n        {1000, "card"},\n        {500, "paypal"},\n        {200, "crypto"},\n    } {\n        fmt.Printf("Оплата %.0f через %s:\\n", tc.amount, tc.method)\n        if err := svc.Process(ctx, tc.amount, tc.method); err != nil {\n            fmt.Println("  ошибка:", err)\n        } else {\n            fmt.Println("  -> успешно")\n        }\n    }\n\n    TestProcess(nil)\n}',
      explanation: 'SRP: каждый тип имеет одну ответственность — проверка лимита, проведение платежа, логирование, уведомление. OCP: добавление CryptoGateway не потребовало изменения PaymentService — просто создали новую структуру. DIP: PaymentService зависит от интерфейсов (LimitChecker, PaymentGateway, ...), не от конкретных типов. ISP: интерфейсы маленькие (1-2 метода). В тесте все зависимости заменены моками.'
    }
  ]
}
