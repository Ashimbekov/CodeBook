export default {
  id: 82,
  title: 'Паттерн Builder',
  description: 'Пошаговое создание сложных объектов с помощью паттерна Builder в Go: method chaining, построение сложных структур, query builder, валидация в методе Build().',
  lessons: [
    {
      id: 1,
      title: 'Builder в Go: основная идея',
      type: 'theory',
      content: [
        { type: 'text', value: 'Паттерн Builder позволяет поэтапно создавать сложные объекты, отделяя процесс создания от представления. В Go Builder реализуется через отдельный тип с методами-сеттерами и финальным методом Build().' },
        { type: 'heading', value: 'Базовый Builder' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "errors"\n    "fmt"\n    "time"\n)\n\n// Product — сложный объект\ntype Email struct {\n    from        string\n    to          []string\n    cc          []string\n    subject     string\n    body        string\n    attachments []string\n    sendAt      time.Time\n    priority    int\n}\n\n// Builder — строитель\ntype EmailBuilder struct {\n    email Email\n    errs  []error\n}\n\nfunc NewEmailBuilder() *EmailBuilder {\n    return &EmailBuilder{\n        email: Email{\n            priority: 3, // нормальный приоритет по умолчанию\n        },\n    }\n}\n\nfunc (b *EmailBuilder) From(addr string) *EmailBuilder {\n    if addr == "" {\n        b.errs = append(b.errs, errors.New("поле From не может быть пустым"))\n    }\n    b.email.from = addr\n    return b\n}\n\nfunc (b *EmailBuilder) To(addrs ...string) *EmailBuilder {\n    b.email.to = append(b.email.to, addrs...)\n    return b\n}\n\nfunc (b *EmailBuilder) CC(addrs ...string) *EmailBuilder {\n    b.email.cc = append(b.email.cc, addrs...)\n    return b\n}\n\nfunc (b *EmailBuilder) Subject(s string) *EmailBuilder {\n    b.email.subject = s\n    return b\n}\n\nfunc (b *EmailBuilder) Body(body string) *EmailBuilder {\n    b.email.body = body\n    return b\n}\n\nfunc (b *EmailBuilder) Attach(files ...string) *EmailBuilder {\n    b.email.attachments = append(b.email.attachments, files...)\n    return b\n}\n\nfunc (b *EmailBuilder) ScheduleAt(t time.Time) *EmailBuilder {\n    b.email.sendAt = t\n    return b\n}\n\nfunc (b *EmailBuilder) Priority(p int) *EmailBuilder {\n    if p < 1 || p > 5 {\n        b.errs = append(b.errs, fmt.Errorf("приоритет должен быть 1-5, получено %d", p))\n        return b\n    }\n    b.email.priority = p\n    return b\n}\n\nfunc (b *EmailBuilder) Build() (Email, error) {\n    if len(b.errs) > 0 {\n        return Email{}, fmt.Errorf("ошибки построения: %v", b.errs)\n    }\n    if len(b.email.to) == 0 {\n        return Email{}, errors.New("нужен хотя бы один получатель")\n    }\n    if b.email.subject == "" {\n        return Email{}, errors.New("тема письма обязательна")\n    }\n    return b.email, nil\n}\n\nfunc main() {\n    email, err := NewEmailBuilder().\n        From("sender@example.com").\n        To("alice@example.com", "bob@example.com").\n        CC("manager@example.com").\n        Subject("Ежемесячный отчёт").\n        Body("Добрый день! Во вложении отчёт за месяц.").\n        Attach("report.pdf", "data.xlsx").\n        Priority(2).\n        Build()\n\n    if err != nil {\n        fmt.Printf("Ошибка: %v\\n", err)\n        return\n    }\n    fmt.Printf("Email: от %s, кому %v, тема: %q\\n",\n        email.from, email.to, email.subject)\n    fmt.Printf("Вложения: %v, приоритет: %d\\n",\n        email.attachments, email.priority)\n}' },
        { type: 'note', value: 'Паттерн "накопление ошибок" в Builder: каждый сеттер добавляет ошибки в слайс, а Build() проверяет их все. Это позволяет получить все проблемы сразу, а не останавливаться на первой.' }
      ]
    },
    {
      id: 2,
      title: 'Method Chaining — цепочки вызовов',
      type: 'theory',
      content: [
        { type: 'text', value: 'Method chaining делает Builder-код декларативным и читаемым. В Go это достигается возвратом указателя на Builder из каждого метода-сеттера.' },
        { type: 'heading', value: 'Fluent interface в Go' },
        { type: 'code', language: 'go', value: 'package main\n\nimport "fmt"\n\n// Person — сложная структура\ntype Person struct {\n    firstName  string\n    lastName   string\n    age        int\n    email      string\n    phone      string\n    address    Address\n    occupation string\n}\n\ntype Address struct {\n    street  string\n    city    string\n    country string\n    zip     string\n}\n\n// PersonBuilder — чистый флюент-интерфейс\ntype PersonBuilder struct {\n    p Person\n}\n\nfunc NewPerson(firstName, lastName string) *PersonBuilder {\n    return &PersonBuilder{\n        p: Person{\n            firstName: firstName,\n            lastName:  lastName,\n        },\n    }\n}\n\nfunc (b *PersonBuilder) Age(age int) *PersonBuilder {\n    b.p.age = age\n    return b\n}\n\nfunc (b *PersonBuilder) Email(email string) *PersonBuilder {\n    b.p.email = email\n    return b\n}\n\nfunc (b *PersonBuilder) Phone(phone string) *PersonBuilder {\n    b.p.phone = phone\n    return b\n}\n\nfunc (b *PersonBuilder) WorksAs(occ string) *PersonBuilder {\n    b.p.occupation = occ\n    return b\n}\n\n// LivesAt — вложенный builder через отдельный метод\nfunc (b *PersonBuilder) LivesAt(street, city, country, zip string) *PersonBuilder {\n    b.p.address = Address{\n        street:  street,\n        city:    city,\n        country: country,\n        zip:     zip,\n    }\n    return b\n}\n\nfunc (b *PersonBuilder) Build() Person {\n    return b.p\n}\n\nfunc (p Person) String() string {\n    return fmt.Sprintf("%s %s (%d лет), %s, %s, %s",\n        p.firstName, p.lastName, p.age,\n        p.occupation, p.address.city, p.email)\n}\n\nfunc main() {\n    person := NewPerson("Нурдаулет", "Сейткали").\n        Age(28).\n        Email("n.seitkali@example.kz").\n        Phone("+7 777 123 4567").\n        WorksAs("Go Developer").\n        LivesAt("ул. Абая 10", "Алматы", "Казахстан", "050010").\n        Build()\n\n    fmt.Println(person)\n    fmt.Printf("Адрес: %s, %s %s\\n",\n        person.address.street, person.address.city, person.address.zip)\n}' },
        { type: 'tip', value: 'Метод Build() может возвращать как значение (Person), так и ошибку (Person, error). Выбирайте в зависимости от того, нужна ли валидация. Если объект всегда валиден после цепочки сеттеров — можно без ошибки.' }
      ]
    },
    {
      id: 3,
      title: 'Построение сложных вложенных структур',
      type: 'theory',
      content: [
        { type: 'text', value: 'Для сложных вложенных структур можно использовать несколько стратегий: вложенные Builder-ы, Builder для каждого уровня или Builder с callback-методами.' },
        { type: 'heading', value: 'HTTP Request Builder с вложенными компонентами' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "encoding/json"\n    "fmt"\n    "net/url"\n)\n\ntype HTTPRequest struct {\n    method  string\n    url     string\n    headers map[string]string\n    params  url.Values\n    body    []byte\n}\n\ntype HTTPRequestBuilder struct {\n    req HTTPRequest\n}\n\nfunc NewRequest(method, rawURL string) *HTTPRequestBuilder {\n    return &HTTPRequestBuilder{\n        req: HTTPRequest{\n            method:  method,\n            url:     rawURL,\n            headers: make(map[string]string),\n            params:  make(url.Values),\n        },\n    }\n}\n\nfunc GET(url string) *HTTPRequestBuilder  { return NewRequest("GET", url) }\nfunc POST(url string) *HTTPRequestBuilder { return NewRequest("POST", url) }\nfunc PUT(url string) *HTTPRequestBuilder  { return NewRequest("PUT", url) }\n\nfunc (b *HTTPRequestBuilder) Header(key, value string) *HTTPRequestBuilder {\n    b.req.headers[key] = value\n    return b\n}\n\nfunc (b *HTTPRequestBuilder) Auth(token string) *HTTPRequestBuilder {\n    b.req.headers["Authorization"] = "Bearer " + token\n    return b\n}\n\nfunc (b *HTTPRequestBuilder) ContentType(ct string) *HTTPRequestBuilder {\n    b.req.headers["Content-Type"] = ct\n    return b\n}\n\nfunc (b *HTTPRequestBuilder) Param(key, value string) *HTTPRequestBuilder {\n    b.req.params.Set(key, value)\n    return b\n}\n\nfunc (b *HTTPRequestBuilder) JSONBody(v interface{}) *HTTPRequestBuilder {\n    data, err := json.Marshal(v)\n    if err == nil {\n        b.req.body = data\n        b.req.headers["Content-Type"] = "application/json"\n    }\n    return b\n}\n\nfunc (b *HTTPRequestBuilder) Build() HTTPRequest {\n    if len(b.req.params) > 0 {\n        b.req.url += "?" + b.req.params.Encode()\n    }\n    return b.req\n}\n\nfunc main() {\n    // GET запрос с параметрами\n    getReq := GET("https://api.example.com/users").\n        Auth("my-token-123").\n        Header("Accept", "application/json").\n        Param("page", "1").\n        Param("limit", "20").\n        Build()\n\n    fmt.Printf("GET %s\\n", getReq.url)\n    fmt.Printf("Headers: %v\\n", getReq.headers)\n\n    // POST запрос с JSON-телом\n    body := map[string]string{"name": "Нурдаулет", "role": "admin"}\n    postReq := POST("https://api.example.com/users").\n        Auth("my-token-123").\n        JSONBody(body).\n        Build()\n\n    fmt.Printf("POST %s\\nBody: %s\\n", postReq.url, postReq.body)\n}' },
        { type: 'note', value: 'Вспомогательные функции GET(), POST(), PUT() — это фасады над NewRequest. Они делают API более читаемым: GET(url).Auth(token).Build() vs NewRequest("GET", url).Auth(token).Build().' }
      ]
    },
    {
      id: 4,
      title: 'Query Builder — пример применения',
      type: 'theory',
      content: [
        { type: 'text', value: 'Query Builder — один из самых распространённых примеров паттерна Builder в реальных проектах. Позволяет строить SQL-запросы программно, избегая конкатенации строк.' },
        { type: 'heading', value: 'SQL Query Builder' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "strings"\n)\n\ntype QueryBuilder struct {\n    table      string\n    conditions []string\n    orderBy    []string\n    columns    []string\n    limitVal   int\n    offsetVal  int\n    args       []interface{}\n    argCount   int\n}\n\nfunc Select(columns ...string) *QueryBuilder {\n    qb := &QueryBuilder{\n        columns:  columns,\n        limitVal: -1,\n    }\n    return qb\n}\n\nfunc (qb *QueryBuilder) From(table string) *QueryBuilder {\n    qb.table = table\n    return qb\n}\n\nfunc (qb *QueryBuilder) Where(condition string, args ...interface{}) *QueryBuilder {\n    qb.argCount++\n    // Заменяем ? на $N для PostgreSQL стиля\n    condition = strings.Replace(condition, "?", fmt.Sprintf("$%d", qb.argCount), 1)\n    qb.conditions = append(qb.conditions, condition)\n    qb.args = append(qb.args, args...)\n    return qb\n}\n\nfunc (qb *QueryBuilder) AndWhere(condition string, args ...interface{}) *QueryBuilder {\n    return qb.Where(condition, args...)\n}\n\nfunc (qb *QueryBuilder) OrderBy(column string, desc bool) *QueryBuilder {\n    if desc {\n        qb.orderBy = append(qb.orderBy, column+" DESC")\n    } else {\n        qb.orderBy = append(qb.orderBy, column+" ASC")\n    }\n    return qb\n}\n\nfunc (qb *QueryBuilder) Limit(n int) *QueryBuilder {\n    qb.limitVal = n\n    return qb\n}\n\nfunc (qb *QueryBuilder) Offset(n int) *QueryBuilder {\n    qb.offsetVal = n\n    return qb\n}\n\nfunc (qb *QueryBuilder) Build() (string, []interface{}) {\n    var sb strings.Builder\n\n    // SELECT\n    if len(qb.columns) == 0 {\n        sb.WriteString("SELECT *")\n    } else {\n        sb.WriteString("SELECT " + strings.Join(qb.columns, ", "))\n    }\n\n    // FROM\n    sb.WriteString(" FROM " + qb.table)\n\n    // WHERE\n    if len(qb.conditions) > 0 {\n        sb.WriteString(" WHERE " + strings.Join(qb.conditions, " AND "))\n    }\n\n    // ORDER BY\n    if len(qb.orderBy) > 0 {\n        sb.WriteString(" ORDER BY " + strings.Join(qb.orderBy, ", "))\n    }\n\n    // LIMIT\n    if qb.limitVal >= 0 {\n        sb.WriteString(fmt.Sprintf(" LIMIT %d", qb.limitVal))\n    }\n\n    // OFFSET\n    if qb.offsetVal > 0 {\n        sb.WriteString(fmt.Sprintf(" OFFSET %d", qb.offsetVal))\n    }\n\n    return sb.String(), qb.args\n}\n\nfunc main() {\n    query, args := Select("id", "name", "email").\n        From("users").\n        Where("age > ?", 18).\n        AndWhere("active = ?", true).\n        OrderBy("created_at", true).\n        Limit(20).\n        Offset(40).\n        Build()\n\n    fmt.Println("SQL:", query)\n    fmt.Println("Args:", args)\n    // SQL: SELECT id, name, email FROM users\n    //      WHERE age > $1 AND active = $2\n    //      ORDER BY created_at DESC LIMIT 20 OFFSET 40\n    // Args: [18 true]\n\n    // Простой запрос\n    q2, _ := Select().From("products").Limit(10).Build()\n    fmt.Println("Simple:", q2)\n}' },
        { type: 'tip', value: 'В реальных проектах используйте готовые query builders: squirrel (github.com/Masterminds/squirrel) или sqlc для генерации типобезопасного SQL. Свой builder имеет смысл только для специфических нужд.' }
      ]
    },
    {
      id: 5,
      title: 'Валидация в Build() и паттерн Step Builder',
      type: 'theory',
      content: [
        { type: 'text', value: 'Продвинутая техника — Step Builder, где компилятор гарантирует порядок вызовов через интерфейсы. Каждый шаг возвращает интерфейс следующего шага.' },
        { type: 'heading', value: 'Валидация и Step Builder через интерфейсы' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "errors"\n    "fmt"\n    "time"\n)\n\ntype Connection struct {\n    host     string\n    port     int\n    database string\n    user     string\n    password string\n    timeout  time.Duration\n    ssl      bool\n}\n\n// Step Builder — каждый шаг через интерфейс\ntype HostStep interface {\n    Host(host string) PortStep\n}\n\ntype PortStep interface {\n    Port(port int) DatabaseStep\n}\n\ntype DatabaseStep interface {\n    Database(db string) UserStep\n}\n\ntype UserStep interface {\n    User(user string) BuildStep\n}\n\ntype BuildStep interface {\n    Password(pwd string) BuildStep\n    Timeout(d time.Duration) BuildStep\n    SSL(enabled bool) BuildStep\n    Build() (*Connection, error)\n}\n\ntype connectionBuilder struct {\n    conn Connection\n    errs []error\n}\n\nfunc NewConnectionBuilder() HostStep {\n    return &connectionBuilder{\n        conn: Connection{\n            port:    5432,\n            timeout: 30 * time.Second,\n        },\n    }\n}\n\nfunc (b *connectionBuilder) Host(host string) PortStep {\n    if host == "" {\n        b.errs = append(b.errs, errors.New("host обязателен"))\n    }\n    b.conn.host = host\n    return b\n}\n\nfunc (b *connectionBuilder) Port(port int) DatabaseStep {\n    if port <= 0 || port > 65535 {\n        b.errs = append(b.errs, fmt.Errorf("недопустимый порт: %d", port))\n    }\n    b.conn.port = port\n    return b\n}\n\nfunc (b *connectionBuilder) Database(db string) UserStep {\n    if db == "" {\n        b.errs = append(b.errs, errors.New("database обязательна"))\n    }\n    b.conn.database = db\n    return b\n}\n\nfunc (b *connectionBuilder) User(user string) BuildStep {\n    if user == "" {\n        b.errs = append(b.errs, errors.New("user обязателен"))\n    }\n    b.conn.user = user\n    return b\n}\n\nfunc (b *connectionBuilder) Password(pwd string) BuildStep {\n    b.conn.password = pwd\n    return b\n}\n\nfunc (b *connectionBuilder) Timeout(d time.Duration) BuildStep {\n    b.conn.timeout = d\n    return b\n}\n\nfunc (b *connectionBuilder) SSL(enabled bool) BuildStep {\n    b.conn.ssl = enabled\n    return b\n}\n\nfunc (b *connectionBuilder) Build() (*Connection, error) {\n    if len(b.errs) > 0 {\n        return nil, fmt.Errorf("ошибки конфигурации: %v", b.errs)\n    }\n    conn := b.conn\n    return &conn, nil\n}\n\nfunc main() {\n    // Компилятор заставляет вызывать в правильном порядке:\n    // Host -> Port -> Database -> User -> [опции] -> Build\n    conn, err := NewConnectionBuilder().\n        Host("db.example.com").\n        Port(5432).\n        Database("myapp").\n        User("admin").\n        Password("secret").\n        SSL(true).\n        Timeout(10*time.Second).\n        Build()\n\n    if err != nil {\n        fmt.Printf("Ошибка: %v\\n", err)\n        return\n    }\n    fmt.Printf("Подключение: %s@%s:%d/%s (ssl=%v, timeout=%v)\\n",\n        conn.user, conn.host, conn.port, conn.database,\n        conn.ssl, conn.timeout)\n}' },
        { type: 'warning', value: 'Step Builder усложняет код, но даёт гарантии на уровне типов. Используйте его только когда порядок шагов критически важен (например, билдер конфигурации подключения к БД).' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Builder для HTTP-запросов с валидацией',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй Builder для формирования HTTP-запросов с поддержкой заголовков, query-параметров, JSON-тела и обязательной валидацией в Build().',
      requirements: [
        'Структура Request: Method, URL, Headers map[string]string, QueryParams map[string]string, Body []byte',
        'RequestBuilder с method chaining',
        'Конструктор NewRequestBuilder(method, url string) *RequestBuilder',
        'Методы: Header(k,v), QueryParam(k,v), JSONBody(v interface{}), BasicAuth(user, pass)',
        'Build() возвращает (Request, error): валидировать Method (GET/POST/PUT/DELETE/PATCH), URL (не пустой)',
        'Если метод — GET или DELETE, тело должно быть пустым (предупреждение через note, не ошибка)',
        'В main создать минимум 3 разных запроса, включая невалидный'
      ],
      expectedOutput: 'GET запрос: GET https://api.example.com/users?limit=10&page=1\nPOST запрос: POST https://api.example.com/users, body={"name":"Alice"}\nОшибка: неверный метод: FETCH',
      hint: 'BasicAuth конвертируй через encoding/base64: base64.StdEncoding.EncodeToString([]byte(user+":"+pass)). Для валидации метода используй switch или map допустимых методов.',
      solution: 'package main\n\nimport (\n    "encoding/base64"\n    "encoding/json"\n    "fmt"\n    "strings"\n)\n\ntype Request struct {\n    Method      string\n    URL         string\n    Headers     map[string]string\n    QueryParams map[string]string\n    Body        []byte\n}\n\ntype RequestBuilder struct {\n    req Request\n    err error\n}\n\nfunc NewRequestBuilder(method, rawURL string) *RequestBuilder {\n    return &RequestBuilder{\n        req: Request{\n            Method:      strings.ToUpper(method),\n            URL:         rawURL,\n            Headers:     make(map[string]string),\n            QueryParams: make(map[string]string),\n        },\n    }\n}\n\nfunc (b *RequestBuilder) Header(key, value string) *RequestBuilder {\n    b.req.Headers[key] = value\n    return b\n}\n\nfunc (b *RequestBuilder) QueryParam(key, value string) *RequestBuilder {\n    b.req.QueryParams[key] = value\n    return b\n}\n\nfunc (b *RequestBuilder) JSONBody(v interface{}) *RequestBuilder {\n    data, err := json.Marshal(v)\n    if err != nil {\n        b.err = fmt.Errorf("ошибка сериализации тела: %w", err)\n        return b\n    }\n    b.req.Body = data\n    b.req.Headers["Content-Type"] = "application/json"\n    return b\n}\n\nfunc (b *RequestBuilder) BasicAuth(user, pass string) *RequestBuilder {\n    encoded := base64.StdEncoding.EncodeToString([]byte(user + ":" + pass))\n    b.req.Headers["Authorization"] = "Basic " + encoded\n    return b\n}\n\nfunc (b *RequestBuilder) Build() (Request, error) {\n    if b.err != nil {\n        return Request{}, b.err\n    }\n    if b.req.URL == "" {\n        return Request{}, fmt.Errorf("URL не может быть пустым")\n    }\n    validMethods := map[string]bool{\n        "GET": true, "POST": true, "PUT": true,\n        "DELETE": true, "PATCH": true, "HEAD": true,\n    }\n    if !validMethods[b.req.Method] {\n        return Request{}, fmt.Errorf("неверный метод: %s", b.req.Method)\n    }\n    // Формируем URL с параметрами\n    if len(b.req.QueryParams) > 0 {\n        parts := make([]string, 0, len(b.req.QueryParams))\n        for k, v := range b.req.QueryParams {\n            parts = append(parts, k+"="+v)\n        }\n        b.req.URL += "?" + strings.Join(parts, "&")\n    }\n    return b.req, nil\n}\n\nfunc main() {\n    // GET запрос\n    req1, err := NewRequestBuilder("GET", "https://api.example.com/users").\n        QueryParam("page", "1").\n        QueryParam("limit", "10").\n        Header("Accept", "application/json").\n        Build()\n    if err != nil {\n        fmt.Printf("Ошибка GET: %v\\n", err)\n    } else {\n        fmt.Printf("GET запрос: %s %s\\n", req1.Method, req1.URL)\n    }\n\n    // POST запрос с JSON\n    body := map[string]string{"name": "Alice"}\n    req2, err := NewRequestBuilder("POST", "https://api.example.com/users").\n        JSONBody(body).\n        BasicAuth("admin", "secret").\n        Build()\n    if err != nil {\n        fmt.Printf("Ошибка POST: %v\\n", err)\n    } else {\n        fmt.Printf("POST запрос: %s %s, body=%s\\n", req2.Method, req2.URL, req2.Body)\n    }\n\n    // Невалидный метод\n    _, err = NewRequestBuilder("FETCH", "https://api.example.com/data").Build()\n    fmt.Printf("Ошибка: %v\\n", err)\n}',
      explanation: 'Builder скрывает сложность сборки HTTP-запроса. Метод JSONBody сразу устанавливает Content-Type, BasicAuth кодирует учётные данные — всё это инкапсулировано. Накопление первой ошибки (b.err) останавливает дальнейшую обработку: если тело не сериализовалось, Build() вернёт ошибку. Такой подход проще, чем слайс ошибок, когда ошибки взаимозависимы. Query параметры собираются вручную в Build(), что позволяет добавлять параметры в любом порядке.'
    }
  ]
}
