export default {
  id: 78,
  title: 'Интерфейсы: best practices',
  description: 'Лучшие практики работы с интерфейсами в Go: принцип "принимай интерфейсы, возвращай структуры", маленькие интерфейсы, interface discovery, не экспортируй интерфейсы из пакета реализации, встраивание интерфейсов.',
  lessons: [
    {
      id: 1,
      title: 'Accept interfaces, return structs',
      type: 'theory',
      content: [
        { type: 'text', value: 'Главный принцип Go: функции и конструкторы принимают интерфейсы (гибкость, тестируемость), но возвращают конкретные структуры (ясность, доступность всех методов).' },
        { type: 'heading', value: 'ПЛОХО: принимать и возвращать интерфейсы' },
        { type: 'code', language: 'go', value: '// ПЛОХО: возвращаем интерфейс из конструктора\ntype UserService interface {\n    GetUser(id int) (*User, error)\n    CreateUser(email string) (*User, error)\n}\n\n// NewUserService возвращает интерфейс — плохая идея\nfunc NewUserService(db Database) UserService {\n    return &userServiceImpl{db: db}\n    // Проблемы:\n    // 1. Вызывающий код получает только методы интерфейса\n    //    — он не может вызвать другие методы userServiceImpl\n    // 2. Тестирование: нужен полный мок всего интерфейса\n    // 3. Интерфейс определён в том же пакете, что реализация\n    //    — нарушение принципа "определяй у потребителя"\n}\n\ntype userServiceImpl struct{ db Database }\nfunc (s *userServiceImpl) GetUser(id int) (*User, error) { return nil, nil }\nfunc (s *userServiceImpl) CreateUser(email string) (*User, error) { return nil, nil }' },
        { type: 'heading', value: 'ХОРОШО: возвращать конкретный тип' },
        { type: 'code', language: 'go', value: '// ХОРОШО: конструктор возвращает конкретную структуру\npackage service\n\n// UserRepository — интерфейс, принимаемый сервисом (определён ЗДЕСЬ)\ntype UserRepository interface {\n    GetByID(id int64) (*User, error)\n    Create(email string) (*User, error)\n}\n\n// UserService — конкретная структура\ntype UserService struct {\n    repo  UserRepository  // принимаем интерфейс\n    cache Cache           // принимаем интерфейс\n}\n\n// NewUserService возвращает *UserService (конкретный тип)\n// Вызывающий код получит доступ ко ВСЕМ методам UserService\nfunc NewUserService(repo UserRepository, cache Cache) *UserService {\n    return &UserService{repo: repo, cache: cache}\n}\n\nfunc (s *UserService) GetUser(id int64) (*User, error) { return s.repo.GetByID(id) }\nfunc (s *UserService) CreateUser(email string) (*User, error) { return s.repo.Create(email) }\nfunc (s *UserService) GetStats() Stats { return Stats{} } // доп. метод — доступен!\n\n// В пакете handler — определяем интерфейс под свои нужды:\npackage handler\n\ntype UserGetter interface {\n    GetUser(id int64) (*service.User, error)\n}\n\n// Хендлеру нужен только GetUser — определяем маленький интерфейс\nfunc NewUserHandler(svc UserGetter) *UserHandler {\n    return &UserHandler{svc: svc}\n}\n// *service.UserService реализует UserGetter — Go это видит автоматически' },
        { type: 'tip', value: 'Возвращай интерфейс из конструктора только если у тебя несколько совершенно разных реализаций одного контракта и ты хочешь их скрыть. В 95% случаев — возвращай конкретный тип.' }
      ]
    },
    {
      id: 2,
      title: 'Маленькие интерфейсы — сила Go',
      type: 'theory',
      content: [
        { type: 'text', value: 'Стандартная библиотека Go — образец маленьких интерфейсов: io.Reader (1 метод), io.Writer (1 метод), fmt.Stringer (1 метод). Чем меньше интерфейс — тем легче его реализовать и тем шире применимость.' },
        { type: 'heading', value: 'Пример: от большого к маленькому' },
        { type: 'code', language: 'go', value: 'package storage\n\nimport "context"\n\n// ПЛОХО: один большой интерфейс\ntype Storage interface {\n    Create(ctx context.Context, key string, data []byte) error\n    Read(ctx context.Context, key string) ([]byte, error)\n    Update(ctx context.Context, key string, data []byte) error\n    Delete(ctx context.Context, key string) error\n    List(ctx context.Context, prefix string) ([]string, error)\n    Exists(ctx context.Context, key string) (bool, error)\n    // Реализовать этот интерфейс сложно даже в моке!\n}\n\n// ХОРОШО: маленькие, компонуемые интерфейсы\ntype Reader interface {\n    Read(ctx context.Context, key string) ([]byte, error)\n}\n\ntype Writer interface {\n    Create(ctx context.Context, key string, data []byte) error\n    Update(ctx context.Context, key string, data []byte) error\n}\n\ntype Deleter interface {\n    Delete(ctx context.Context, key string) error\n}\n\ntype Lister interface {\n    List(ctx context.Context, prefix string) ([]string, error)\n}\n\n// Комбинированный интерфейс — только когда нужен весь набор\ntype ReadWriter interface {\n    Reader\n    Writer\n}\n\ntype FullStorage interface {\n    Reader\n    Writer\n    Deleter\n    Lister\n}\n\n// Функции принимают только то, что им нужно:\nfunc BackupData(ctx context.Context, src Reader, dst Writer) error {\n    data, err := src.Read(ctx, "backup_key")\n    if err != nil {\n        return err\n    }\n    return dst.Create(ctx, "backup_"+"20240101", data)\n}\n\n// BackupData работает с любым типом, реализующим Reader и Writer!\n// Мок для теста: нужны только 2 метода, не 6.' }
      ]
    },
    {
      id: 3,
      title: 'Interface discovery — интерфейсы у потребителя',
      type: 'theory',
      content: [
        { type: 'text', value: 'В Go интерфейсы "обнаруживаются" (discovered), а не объявляются заранее. Определяй интерфейс там, где он используется (у потребителя), а не где реализуется.' },
        { type: 'heading', value: 'Антипаттерн: интерфейс у реализации' },
        { type: 'code', language: 'go', value: '// ПЛОХО: интерфейс объявлен в пакете реализации\npackage postgres\n\n// UserRepo интерфейс — в том же пакете, что реализация!\ntype UserRepo interface {\n    GetByID(id int64) (*User, error)\n    Create(u *User) error\n    Update(u *User) error\n    Delete(id int64) error\n}\n\ntype postgresUserRepo struct{ db *sql.DB }\nfunc (r *postgresUserRepo) GetByID(id int64) (*User, error) { ... }\n// ...\n\n// Потребитель вынужден импортировать пакет postgres:\npackage service\n\nimport "myapp/postgres" // Зависим от конкретной реализации!\n\ntype UserService struct {\n    repo postgres.UserRepo // Привязаны к postgres\n}' },
        { type: 'heading', value: 'ХОРОШО: интерфейс у потребителя' },
        { type: 'code', language: 'go', value: '// ХОРОШО: интерфейс объявлен у потребителя\npackage service\n\n// UserRepository — объявлен В ЭТОМ пакете, не в postgres/\ntype UserRepository interface {\n    GetByID(id int64) (*User, error)\n    Create(u *User) error\n}\n\ntype UserService struct {\n    repo UserRepository // Независим от реализации\n}\n\n// ---\n\n// В пакете postgres — только реализация, без интерфейса\npackage postgres\n\ntype UserRepo struct{ db *sql.DB }\n\n// GetByID реализует service.UserRepository\n// (неявно — Go сам это видит)\nfunc (r *UserRepo) GetByID(id int64) (*User, error) {\n    var u User\n    err := r.db.QueryRow("SELECT id, email FROM users WHERE id=$1", id).Scan(&u.ID, &u.Email)\n    return &u, err\n}\n\nfunc (r *UserRepo) Create(u *User) error {\n    _, err := r.db.Exec("INSERT INTO users (email) VALUES ($1)", u.Email)\n    return err\n}\n\n// В main.go:\n// repo := postgres.NewUserRepo(db)     // конкретный тип\n// svc := service.NewUserService(repo)  // передаём как service.UserRepository\n// Go автоматически проверит, что *postgres.UserRepo реализует service.UserRepository' },
        { type: 'note', value: 'Это принцип из чистой архитектуры (DIP): интерфейс принадлежит потребителю, не поставщику. Пакет postgres не знает о service — зависимость только в одну сторону.' }
      ]
    },
    {
      id: 4,
      title: 'Встраивание интерфейсов',
      type: 'theory',
      content: [
        { type: 'text', value: 'Интерфейсы в Go можно встраивать (embedding), создавая более широкие интерфейсы из узких. Также встраивание интерфейса в структуру используется для частичной реализации.' },
        { type: 'heading', value: 'Встраивание для создания составных интерфейсов' },
        { type: 'code', language: 'go', value: 'package io\n\n// Стандартная библиотека — лучший пример встраивания:\n\ntype Reader interface {\n    Read(p []byte) (n int, err error)\n}\n\ntype Writer interface {\n    Write(p []byte) (n int, err error)\n}\n\ntype Closer interface {\n    Close() error\n}\n\n// Встраивание создаёт составной интерфейс\ntype ReadWriter interface {\n    Reader // включает Read()\n    Writer // включает Write()\n}\n\ntype ReadWriteCloser interface {\n    Reader\n    Writer\n    Closer\n}\n\n// Использование в своём коде:\npackage storage\n\ntype Storer interface {\n    Create(key string, data []byte) error\n    Read(key string) ([]byte, error)\n}\n\ntype VersionedStorer interface {\n    Storer                              // всё из Storer\n    ListVersions(key string) []string   // дополнительно\n    RestoreVersion(key, version string) error\n}\n\n// Встраивание интерфейса в структуру — для частичных моков\ntype partialMock struct {\n    Storer // встраиваем — нужно реализовать только нужные методы\n    createCalled bool\n}\n\nfunc (m *partialMock) Create(key string, data []byte) error {\n    m.createCalled = true\n    return nil\n    // Read() наследуется из встроенного Storer\n    // но вызовет панику если Storer == nil!\n}' },
        { type: 'warning', value: 'Встраивание интерфейса в структуру (partialMock) опасно: если встроенное поле nil, вызов нереализованного метода вызовет панику. Явно инициализируй или реализуй все методы.' }
      ]
    },
    {
      id: 5,
      title: 'Compile-time проверка интерфейса',
      type: 'theory',
      content: [
        { type: 'text', value: 'Go проверяет реализацию интерфейсов в рантайме при присвоении. Но можно добавить compile-time проверку, чтобы ошибка была найдена сразу при компиляции.' },
        { type: 'heading', value: 'var _ Interface = (*Struct)(nil)' },
        { type: 'code', language: 'go', value: 'package storage\n\nimport "context"\n\ntype Storage interface {\n    Create(ctx context.Context, key string, data []byte) error\n    Read(ctx context.Context, key string) ([]byte, error)\n    Delete(ctx context.Context, key string) error\n}\n\ntype S3Storage struct {\n    bucket string\n}\n\n// Compile-time проверка: если S3Storage не реализует Storage,\n// компилятор выдаст ошибку ЗДЕСЬ, до запуска программы\nvar _ Storage = (*S3Storage)(nil)\n\nfunc (s *S3Storage) Create(ctx context.Context, key string, data []byte) error {\n    // Загрузить в S3\n    return nil\n}\n\nfunc (s *S3Storage) Read(ctx context.Context, key string) ([]byte, error) {\n    // Скачать из S3\n    return nil, nil\n}\n\n// Delete забыли реализовать?\n// Компилятор: cannot use (*S3Storage)(nil) as type Storage:\n//             S3Storage does not implement Storage (missing Delete method)\n\nfunc (s *S3Storage) Delete(ctx context.Context, key string) error {\n    // Удалить из S3\n    return nil\n}\n\n// Такая же проверка полезна в тестах для моков:\nvar _ Storage = (*mockStorage)(nil)\n\ntype mockStorage struct{}\nfunc (m *mockStorage) Create(_ context.Context, _ string, _ []byte) error { return nil }\nfunc (m *mockStorage) Read(_ context.Context, _ string) ([]byte, error) { return nil, nil }\nfunc (m *mockStorage) Delete(_ context.Context, _ string) error { return nil }' },
        { type: 'tip', value: 'var _ Interface = (*Struct)(nil) — строка нулевых затрат. (*Struct)(nil) создаёт nil-указатель на Struct, присваивание проверяет интерфейс. В рантайме эта строка ничего не делает, только компилятор проверяет.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: правильный API с интерфейсами',
      type: 'practice',
      difficulty: 'medium',
      description: 'Спроектируй и реализуй систему хранения файлов с правильной структурой интерфейсов: маленькие интерфейсы у потребителей, конкретные типы из конструкторов, compile-time проверки.',
      requirements: [
        'Маленькие интерфейсы в пакете service: FileReader{Read(name) ([]byte, error)}, FileWriter{Write(name string, data []byte) error}',
        'Структура FileService{reader FileReader, writer FileWriter}: методы Save(name, data) и Load(name)',
        'Конкретная структура MemoryStorage — реализует оба интерфейса (in-memory)',
        'NewFileService принимает интерфейсы, возвращает *FileService (не интерфейс)',
        'Compile-time проверка: var _ FileReader = (*MemoryStorage)(nil)',
        'Тест: TestFileService_SaveAndLoad через мок или MemoryStorage'
      ],
      expectedOutput: 'Сохранено: hello.txt (11 байт)\nПрочитано: hello.txt -> "Hello World"\nTestFileService_SaveAndLoad: PASS\nCompile-time проверка: PASS',
      hint: 'MemoryStorage хранит map[string][]byte. FileService.Save записывает через writer, Load читает через reader. Compile-time проверка: var _ FileReader = (*MemoryStorage)(nil) — добавь после определения MemoryStorage.',
      solution: 'package main\n\nimport (\n    "errors"\n    "fmt"\n    "sync"\n    "testing"\n)\n\n// === Interfaces (определены у потребителя — в пакете service) ===\n\ntype FileReader interface {\n    Read(name string) ([]byte, error)\n}\n\ntype FileWriter interface {\n    Write(name string, data []byte) error\n}\n\n// === MemoryStorage — конкретная реализация ===\n\ntype MemoryStorage struct {\n    mu    sync.RWMutex\n    files map[string][]byte\n}\n\n// Compile-time проверки интерфейсов\nvar _ FileReader = (*MemoryStorage)(nil)\nvar _ FileWriter = (*MemoryStorage)(nil)\n\nfunc NewMemoryStorage() *MemoryStorage {\n    return &MemoryStorage{files: make(map[string][]byte)}\n}\n\nfunc (s *MemoryStorage) Read(name string) ([]byte, error) {\n    s.mu.RLock()\n    defer s.mu.RUnlock()\n    data, ok := s.files[name]\n    if !ok {\n        return nil, errors.New("файл не найден: " + name)\n    }\n    cp := make([]byte, len(data))\n    copy(cp, data)\n    return cp, nil\n}\n\nfunc (s *MemoryStorage) Write(name string, data []byte) error {\n    s.mu.Lock()\n    defer s.mu.Unlock()\n    cp := make([]byte, len(data))\n    copy(cp, data)\n    s.files[name] = cp\n    return nil\n}\n\n// === FileService ===\n\ntype FileService struct {\n    reader FileReader // принимаем интерфейс\n    writer FileWriter // принимаем интерфейс\n}\n\n// NewFileService возвращает конкретный *FileService\nfunc NewFileService(reader FileReader, writer FileWriter) *FileService {\n    return &FileService{reader: reader, writer: writer}\n}\n\nfunc (s *FileService) Save(name string, data []byte) error {\n    if err := s.writer.Write(name, data); err != nil {\n        return fmt.Errorf("Save(%q): %w", name, err)\n    }\n    fmt.Printf("Сохранено: %s (%d байт)\\n", name, len(data))\n    return nil\n}\n\nfunc (s *FileService) Load(name string) ([]byte, error) {\n    data, err := s.reader.Read(name)\n    if err != nil {\n        return nil, fmt.Errorf("Load(%q): %w", name, err)\n    }\n    return data, nil\n}\n\n// === Mock reader для теста ===\n\ntype mockReader struct {\n    files map[string][]byte\n}\n\nfunc (m *mockReader) Read(name string) ([]byte, error) {\n    data, ok := m.files[name]\n    if !ok {\n        return nil, errors.New("не найдено: " + name)\n    }\n    return data, nil\n}\n\ntype mockWriter struct {\n    files map[string][]byte\n}\n\nfunc (m *mockWriter) Write(name string, data []byte) error {\n    m.files[name] = data\n    return nil\n}\n\n// === Test ===\n\nfunc TestFileService_SaveAndLoad(t *testing.T) {\n    store := make(map[string][]byte)\n    r := &mockReader{files: store}\n    w := &mockWriter{files: store}\n\n    svc := NewFileService(r, w)\n\n    data := []byte("Hello World")\n    if err := svc.Save("hello.txt", data); err != nil {\n        t.Fatalf("Save: %v", err)\n    }\n\n    got, err := svc.Load("hello.txt")\n    if err != nil {\n        t.Fatalf("Load: %v", err)\n    }\n    if string(got) != string(data) {\n        t.Errorf("got %q, want %q", got, data)\n    }\n\n    _, err = svc.Load("nonexistent.txt")\n    if err == nil {\n        t.Error("ожидали ошибку для несуществующего файла")\n    }\n\n    fmt.Println("TestFileService_SaveAndLoad: PASS")\n}\n\nfunc main() {\n    // Реальное использование\n    storage := NewMemoryStorage()\n    svc := NewFileService(storage, storage) // MemoryStorage реализует оба интерфейса\n\n    svc.Save("hello.txt", []byte("Hello World"))\n\n    data, err := svc.Load("hello.txt")\n    if err != nil {\n        fmt.Println("Ошибка:", err)\n        return\n    }\n    fmt.Printf("Прочитано: hello.txt -> %q\\n", data)\n\n    // Тест\n    TestFileService_SaveAndLoad(&testing.T{})\n    fmt.Println("Compile-time проверка: PASS")\n}',
      explanation: 'FileReader и FileWriter — маленькие интерфейсы, определённые у потребителя (FileService). MemoryStorage реализует оба без явного объявления — неявная реализация интерфейсов. var _ FileReader = (*MemoryStorage)(nil) — compile-time проверка: ошибка компиляции если метод Read отсутствует или имеет неверную сигнатуру. NewFileService возвращает *FileService (конкретный тип), принимает интерфейсы. В тесте используются mockReader/mockWriter — 1 метод каждый.'
    }
  ]
}
