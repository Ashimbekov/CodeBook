export default {
  id: 95,
  title: 'Практикум: CLI утилиты',
  description: 'Создание практических CLI утилит на Go: клоны стандартных Unix-инструментов (wc, grep, find), утилиты для работы с JSON, CSV, портами, а также полноценное todo-приложение с постоянным хранением.',
  lessons: [
    {
      id: 1,
      title: 'Клон wc (подсчёт слов)',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй клон утилиты wc: подсчёт строк, слов и байт в файле или стандартном вводе.',
      requirements: [
        'Функция countFile(filename string) (lines, words, bytes int, err error)',
        'Поддержка флагов: -l (строки), -w (слова), -c (байты), без флагов — всё',
        'Поддержка нескольких файлов: итоговая строка "total" если файлов > 1',
        'Читай из stdin если файлы не указаны',
        'Программа принимает аргументы через os.Args',
        'Формат вывода: "{lines} {words} {bytes} {filename}"'
      ],
      expectedOutput: '  3  10  55 file1.txt\n  5  20  98 file2.txt\n  8  30 153 total',
      hint: 'bufio.Scanner с ScanLines считает строки. strings.Fields(line) разбивает на слова. len([]byte(content)) считает байты. Используй flag пакет для разбора флагов.',
      solution: 'package main\n\nimport (\n    "bufio"\n    "flag"\n    "fmt"\n    "os"\n    "strings"\n)\n\ntype Counts struct {\n    Lines, Words, Bytes int\n    Filename            string\n}\n\nfunc countFile(filename string) (Counts, error) {\n    var f *os.File\n    var err error\n    if filename == "" {\n        f = os.Stdin\n    } else {\n        f, err = os.Open(filename)\n        if err != nil {\n            return Counts{}, err\n        }\n        defer f.Close()\n    }\n    var c Counts\n    c.Filename = filename\n    scanner := bufio.NewScanner(f)\n    for scanner.Scan() {\n        line := scanner.Text()\n        c.Lines++\n        c.Words += len(strings.Fields(line))\n        c.Bytes += len(line) + 1\n    }\n    return c, scanner.Err()\n}\n\nfunc printCounts(c Counts, showLines, showWords, showBytes bool) {\n    if showLines {\n        fmt.Printf("%5d ", c.Lines)\n    }\n    if showWords {\n        fmt.Printf("%5d ", c.Words)\n    }\n    if showBytes {\n        fmt.Printf("%5d ", c.Bytes)\n    }\n    if c.Filename != "" {\n        fmt.Println(c.Filename)\n    } else {\n        fmt.Println()\n    }\n}\n\nfunc main() {\n    showLines := flag.Bool("l", false, "подсчёт строк")\n    showWords := flag.Bool("w", false, "подсчёт слов")\n    showBytes := flag.Bool("c", false, "подсчёт байт")\n    flag.Parse()\n    if !*showLines && !*showWords && !*showBytes {\n        *showLines = true\n        *showWords = true\n        *showBytes = true\n    }\n    files := flag.Args()\n    if len(files) == 0 {\n        c, _ := countFile("")\n        printCounts(c, *showLines, *showWords, *showBytes)\n        return\n    }\n    var total Counts\n    for _, f := range files {\n        c, err := countFile(f)\n        if err != nil {\n            fmt.Fprintf(os.Stderr, "wc: %s: %v\\n", f, err)\n            continue\n        }\n        printCounts(c, *showLines, *showWords, *showBytes)\n        total.Lines += c.Lines\n        total.Words += c.Words\n        total.Bytes += c.Bytes\n    }\n    if len(files) > 1 {\n        total.Filename = "total"\n        printCounts(total, *showLines, *showWords, *showBytes)\n    }\n}',
      explanation: 'flag.Parse() разбирает аргументы командной строки. flag.Args() возвращает не-флаговые аргументы (имена файлов). bufio.Scanner эффективно читает большие файлы. Передача "" как filename означает stdin — позволяет использовать одну функцию countFile для обоих случаев. Ошибки выводятся в stderr — стандартная практика для CLI утилит.'
    },
    {
      id: 2,
      title: 'Клон grep',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй упрощённый клон grep: поиск строк по регулярному выражению в файлах с поддержкой флагов.',
      requirements: [
        'Поддержка флагов: -i (игнорировать регистр), -n (показывать номера строк), -c (только счётчик), -r (рекурсивный поиск в директории)',
        'Функция grepFile(pattern, filename string, flags GrepFlags) ([]Match, error)',
        'Структура Match: Line string, LineNum int, Filename string',
        'При -r: рекурсивный обход директорий через filepath.Walk',
        'Выводи совпадения в формате: "filename:linenum:line" или "linenum:line" если один файл'
      ],
      expectedOutput: 'grep "func" main.go:\nmain.go:1:func main() {\nmain.go:5:func helper() {\nНайдено совпадений: 2',
      hint: 'regexp.MustCompile создаёт регулярное выражение. При -i добавь "(?i)" к паттерну. filepath.Walk обходит дерево директорий, вызывая callback для каждого файла.',
      solution: 'package main\n\nimport (\n    "bufio"\n    "flag"\n    "fmt"\n    "os"\n    "path/filepath"\n    "regexp"\n    "strings"\n)\n\ntype GrepFlags struct {\n    IgnoreCase bool\n    ShowNum    bool\n    CountOnly  bool\n    Recursive  bool\n}\n\ntype Match struct {\n    Line     string\n    LineNum  int\n    Filename string\n}\n\nfunc grepFile(pattern, filename string, flags GrepFlags) ([]Match, error) {\n    pat := pattern\n    if flags.IgnoreCase {\n        pat = "(?i)" + pattern\n    }\n    re, err := regexp.Compile(pat)\n    if err != nil {\n        return nil, err\n    }\n    var f *os.File\n    if filename == "" {\n        f = os.Stdin\n    } else {\n        f, err = os.Open(filename)\n        if err != nil {\n            return nil, err\n        }\n        defer f.Close()\n    }\n    var matches []Match\n    scanner := bufio.NewScanner(f)\n    lineNum := 0\n    for scanner.Scan() {\n        lineNum++\n        line := scanner.Text()\n        if re.MatchString(line) {\n            matches = append(matches, Match{Line: line, LineNum: lineNum, Filename: filename})\n        }\n    }\n    return matches, scanner.Err()\n}\n\nfunc printMatch(m Match, flags GrepFlags, multipleFiles bool) {\n    parts := []string{}\n    if multipleFiles && m.Filename != "" {\n        parts = append(parts, m.Filename)\n    }\n    if flags.ShowNum {\n        parts = append(parts, fmt.Sprintf("%d", m.LineNum))\n    }\n    parts = append(parts, m.Line)\n    fmt.Println(strings.Join(parts, ":"))\n}\n\nfunc main() {\n    ignoreCase := flag.Bool("i", false, "игнорировать регистр")\n    showNum := flag.Bool("n", false, "показывать номера строк")\n    countOnly := flag.Bool("c", false, "только счётчик")\n    recursive := flag.Bool("r", false, "рекурсивный поиск")\n    flag.Parse()\n    args := flag.Args()\n    if len(args) < 1 {\n        fmt.Fprintln(os.Stderr, "Использование: grep [флаги] паттерн [файл...]")\n        os.Exit(1)\n    }\n    pattern := args[0]\n    files := args[1:]\n    flags := GrepFlags{*ignoreCase, *showNum, *countOnly, *recursive}\n    if len(files) == 0 {\n        matches, err := grepFile(pattern, "", flags)\n        if err != nil {\n            fmt.Fprintln(os.Stderr, err)\n            return\n        }\n        if *countOnly {\n            fmt.Println(len(matches))\n            return\n        }\n        for _, m := range matches {\n            printMatch(m, flags, false)\n        }\n        return\n    }\n    var allFiles []string\n    for _, f := range files {\n        if *recursive {\n            filepath.Walk(f, func(path string, info os.FileInfo, err error) error {\n                if err == nil && !info.IsDir() {\n                    allFiles = append(allFiles, path)\n                }\n                return nil\n            })\n        } else {\n            allFiles = append(allFiles, f)\n        }\n    }\n    totalMatches := 0\n    for _, f := range allFiles {\n        matches, err := grepFile(pattern, f, flags)\n        if err != nil {\n            fmt.Fprintf(os.Stderr, "grep: %s: %v\\n", f, err)\n            continue\n        }\n        if *countOnly {\n            fmt.Printf("%s:%d\\n", f, len(matches))\n        } else {\n            for _, m := range matches {\n                printMatch(m, flags, len(allFiles) > 1)\n            }\n        }\n        totalMatches += len(matches)\n    }\n    fmt.Printf("Найдено совпадений: %d\\n", totalMatches)\n}',
      explanation: 'Grep — один из самых частых инструментов Unix, реализация на Go демонстрирует работу с флагами, файловой системой и регулярными выражениями. "(?i)" — флаг регулярного выражения для нечувствительности к регистру. filepath.Walk рекурсивно обходит дерево директорий через callback — идиоматичный Go-подход. Разделение grepFile и вывода улучшает тестируемость.'
    },
    {
      id: 3,
      title: 'Поиск файлов (find)',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй утилиту поиска файлов с поддержкой фильтрации по имени, расширению, размеру и дате модификации.',
      requirements: [
        'Структура FindOptions: Pattern string, Extension string, MinSize int64, MaxSize int64, ModifiedAfter time.Time',
        'Функция findFiles(root string, opts FindOptions) ([]string, error)',
        'Использует filepath.Walk для рекурсивного обхода',
        'Флаги CLI: -name (паттерн имени), -ext (расширение), -size (минимальный размер в байтах), -newer (файлы новее указанной даты)',
        'Выводи найденные пути, затем общее количество'
      ],
      expectedOutput: 'Поиск *.go файлов:\n./main.go\n./utils/helper.go\nНайдено: 2 файлов',
      hint: 'filepath.Ext(name) возвращает расширение файла. filepath.Match(pattern, name) проверяет glob-паттерн. Создай функцию matchesOptions для проверки всех условий.',
      solution: 'package main\n\nimport (\n    "flag"\n    "fmt"\n    "os"\n    "path/filepath"\n    "strings"\n    "time"\n)\n\ntype FindOptions struct {\n    Pattern       string\n    Extension     string\n    MinSize       int64\n    MaxSize       int64\n    ModifiedAfter time.Time\n}\n\nfunc matchesOptions(info os.FileInfo, name string, opts FindOptions) bool {\n    if opts.Pattern != "" {\n        matched, err := filepath.Match(opts.Pattern, info.Name())\n        if err != nil || !matched {\n            return false\n        }\n    }\n    if opts.Extension != "" {\n        ext := opts.Extension\n        if !strings.HasPrefix(ext, ".") {\n            ext = "." + ext\n        }\n        if filepath.Ext(info.Name()) != ext {\n            return false\n        }\n    }\n    if opts.MinSize > 0 && info.Size() < opts.MinSize {\n        return false\n    }\n    if opts.MaxSize > 0 && info.Size() > opts.MaxSize {\n        return false\n    }\n    if !opts.ModifiedAfter.IsZero() && info.ModTime().Before(opts.ModifiedAfter) {\n        return false\n    }\n    return true\n}\n\nfunc findFiles(root string, opts FindOptions) ([]string, error) {\n    var results []string\n    err := filepath.Walk(root, func(path string, info os.FileInfo, err error) error {\n        if err != nil {\n            return nil\n        }\n        if info.IsDir() {\n            return nil\n        }\n        if matchesOptions(info, path, opts) {\n            results = append(results, path)\n        }\n        return nil\n    })\n    return results, err\n}\n\nfunc main() {\n    pattern := flag.String("name", "", "паттерн имени файла")\n    ext := flag.String("ext", "", "расширение файла")\n    minSize := flag.Int64("size", 0, "минимальный размер в байтах")\n    maxSize := flag.Int64("maxsize", 0, "максимальный размер в байтах")\n    flag.Parse()\n    root := "."\n    if flag.NArg() > 0 {\n        root = flag.Arg(0)\n    }\n    opts := FindOptions{\n        Pattern:  *pattern,\n        Extension: *ext,\n        MinSize:  *minSize,\n        MaxSize:  *maxSize,\n    }\n    files, err := findFiles(root, opts)\n    if err != nil {\n        fmt.Fprintln(os.Stderr, "Ошибка:", err)\n        os.Exit(1)\n    }\n    for _, f := range files {\n        fmt.Println(f)\n    }\n    fmt.Printf("Найдено: %d файлов\\n", len(files))\n}',
      explanation: 'filepath.Walk обходит дерево директорий в лексикографическом порядке. Возврат nil из callback продолжает обход; ошибки от stat файлов мы игнорируем (например, нет доступа) — обычная практика для find-утилит. matchesOptions разделяет логику фильтрации от логики обхода — это облегчает добавление новых фильтров без изменения walk-логики.'
    },
    {
      id: 4,
      title: 'Форматировщик JSON',
      type: 'practice',
      difficulty: 'easy',
      description: 'Создай CLI утилиту для форматирования, минификации и валидации JSON файлов.',
      requirements: [
        'Флаги: -compact (минификация), -indent (размер отступа, по умолчанию 2), -validate (только проверка), -key "path" (извлечь значение по пути "a.b.c")',
        'Читает из stdin или из файла (первый аргумент)',
        'Форматирует JSON с отступами через json.MarshalIndent',
        'Режим -validate: выводит "valid" или сообщение об ошибке',
        'Режим -key: поддержи простой путь через точку (например "server.port")'
      ],
      expectedOutput: '{\n  "name": "Alice",\n  "age": 30\n}\n\nvalidate: valid\nextract server.port: 8080',
      hint: 'json.Valid(data) проверяет валидность. json.MarshalIndent(v, "", "  ") форматирует. Для извлечения по ключу: Unmarshal в map[string]interface{}, затем рекурсивно обходи по частям пути разбитого через strings.Split(path, ".").',
      solution: 'package main\n\nimport (\n    "encoding/json"\n    "flag"\n    "fmt"\n    "io"\n    "os"\n    "strings"\n)\n\nfunc extractByPath(data map[string]interface{}, path string) (interface{}, bool) {\n    parts := strings.Split(path, ".")\n    var current interface{} = data\n    for _, part := range parts {\n        m, ok := current.(map[string]interface{})\n        if !ok {\n            return nil, false\n        }\n        current, ok = m[part]\n        if !ok {\n            return nil, false\n        }\n    }\n    return current, true\n}\n\nfunc main() {\n    compact := flag.Bool("compact", false, "минификация")\n    indent := flag.Int("indent", 2, "размер отступа")\n    validate := flag.Bool("validate", false, "только валидация")\n    key := flag.String("key", "", "путь к значению (a.b.c)")\n    flag.Parse()\n    var input []byte\n    var err error\n    if flag.NArg() > 0 {\n        input, err = os.ReadFile(flag.Arg(0))\n    } else {\n        input, err = io.ReadAll(os.Stdin)\n    }\n    if err != nil {\n        fmt.Fprintln(os.Stderr, "Ошибка чтения:", err)\n        os.Exit(1)\n    }\n    if *validate {\n        if json.Valid(input) {\n            fmt.Println("valid")\n        } else {\n            fmt.Println("invalid: некорректный JSON")\n            os.Exit(1)\n        }\n        return\n    }\n    var v interface{}\n    if err := json.Unmarshal(input, &v); err != nil {\n        fmt.Fprintln(os.Stderr, "Ошибка парсинга:", err)\n        os.Exit(1)\n    }\n    if *key != "" {\n        m, ok := v.(map[string]interface{})\n        if !ok {\n            fmt.Fprintln(os.Stderr, "Корень не является объектом")\n            os.Exit(1)\n        }\n        val, found := extractByPath(m, *key)\n        if !found {\n            fmt.Fprintf(os.Stderr, "Ключ %q не найден\\n", *key)\n            os.Exit(1)\n        }\n        fmt.Println(val)\n        return\n    }\n    var out []byte\n    if *compact {\n        out, err = json.Marshal(v)\n    } else {\n        indentStr := strings.Repeat(" ", *indent)\n        out, err = json.MarshalIndent(v, "", indentStr)\n    }\n    if err != nil {\n        fmt.Fprintln(os.Stderr, "Ошибка форматирования:", err)\n        os.Exit(1)\n    }\n    fmt.Println(string(out))\n}',
      explanation: 'io.ReadAll(os.Stdin) читает весь стандартный ввод — стандартный паттерн для UNIX-утилит. json.Valid — быстрая проверка без Unmarshal. Извлечение по пути через strings.Split и рекурсивный обход map демонстрирует работу с interface{} и type assertion. Использование os.Exit(1) при ошибках — правильный сигнал для вызывающего процесса (скрипты, CI).'
    },
    {
      id: 5,
      title: 'Сканер портов',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй многопоточный TCP сканер портов с ограничением параллелизма и таймаутом.',
      requirements: [
        'Функция scanPort(host string, port int, timeout time.Duration) bool',
        'Функция scanRange(host string, startPort, endPort int, concurrency int, timeout time.Duration) []int — возвращает открытые порты',
        'Используй goroutines + buffered channel как семафор для ограничения параллелизма',
        'Флаги CLI: -host, -start (начальный порт), -end (конечный порт), -c (параллелизм), -t (таймаут в мс)',
        'Выводи прогресс и найденные открытые порты'
      ],
      expectedOutput: 'Сканирование localhost:1-1024...\nОткрыт: 22\nОткрыт: 80\nОткрыт: 443\nСканирование завершено. Открытых портов: 3\nВремя: 2.3s',
      hint: 'Семафор через buffered channel: sem := make(chan struct{}, concurrency). Перед горутиной: sem <- struct{}{}. После: <-sem. sync.WaitGroup ждёт завершения всех горутин.',
      solution: 'package main\n\nimport (\n    "flag"\n    "fmt"\n    "net"\n    "sort"\n    "sync"\n    "time"\n)\n\nfunc scanPort(host string, port int, timeout time.Duration) bool {\n    address := fmt.Sprintf("%s:%d", host, port)\n    conn, err := net.DialTimeout("tcp", address, timeout)\n    if err != nil {\n        return false\n    }\n    conn.Close()\n    return true\n}\n\nfunc scanRange(host string, startPort, endPort, concurrency int, timeout time.Duration) []int {\n    var mu sync.Mutex\n    var openPorts []int\n    var wg sync.WaitGroup\n    sem := make(chan struct{}, concurrency)\n    for port := startPort; port <= endPort; port++ {\n        wg.Add(1)\n        go func(p int) {\n            defer wg.Done()\n            sem <- struct{}{}\n            defer func() { <-sem }()\n            if scanPort(host, p, timeout) {\n                mu.Lock()\n                openPorts = append(openPorts, p)\n                mu.Unlock()\n                fmt.Printf("Открыт: %d\\n", p)\n            }\n        }(port)\n    }\n    wg.Wait()\n    sort.Ints(openPorts)\n    return openPorts\n}\n\nfunc main() {\n    host := flag.String("host", "localhost", "хост для сканирования")\n    start := flag.Int("start", 1, "начальный порт")\n    end := flag.Int("end", 1024, "конечный порт")\n    concurrency := flag.Int("c", 100, "количество параллельных запросов")\n    timeoutMs := flag.Int("t", 500, "таймаут в миллисекундах")\n    flag.Parse()\n    timeout := time.Duration(*timeoutMs) * time.Millisecond\n    fmt.Printf("Сканирование %s:%d-%d...\\n", *host, *start, *end)\n    startTime := time.Now()\n    openPorts := scanRange(*host, *start, *end, *concurrency, timeout)\n    elapsed := time.Since(startTime)\n    fmt.Printf("Сканирование завершено. Открытых портов: %d\\n", len(openPorts))\n    fmt.Printf("Время: %.1fs\\n", elapsed.Seconds())\n    if len(openPorts) > 0 {\n        fmt.Printf("Открытые порты: %v\\n", openPorts)\n    }\n}',
      explanation: 'Буферизованный канал как семафор — идиоматичный паттерн Go для ограничения параллелизма. sem <- struct{}{} блокирует горутину если канал полон (достигнут лимит). defer func(){ <-sem }() освобождает слот при завершении. Mutex защищает append к openPorts. Передача port как параметр горутины (не замыкание) избегает проблемы захвата переменной цикла.'
    },
    {
      id: 6,
      title: 'Todo CLI приложение',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создай полнофункциональное todo-приложение командной строки с хранением в JSON файле.',
      requirements: [
        'Команды: add "задача", list, done <id>, delete <id>, clear',
        'Структура Todo: ID int, Text string, Done bool, CreatedAt time.Time',
        'Хранение в ~/.todos.json',
        'list выводит: "[ ] 1. Задача" или "[x] 1. Задача"',
        'Поддержи флаг -all для list (показывать выполненные), -pending (только невыполненные)',
        'При добавлении: auto-increment ID даже после удаления'
      ],
      expectedOutput: 'todo add "Написать тесты"\nДобавлено: [1] Написать тесты\ntodo list\n[ ] 1. Написать тесты\ntodo done 1\nВыполнено: Написать тесты\ntodo list\nАктивных задач: 0',
      hint: 'Храни []Todo в JSON файле. Загружай при старте, сохраняй при любом изменении. MaxID вычисляй как max(todo.ID)+1 при добавлении. os.UserHomeDir() возвращает домашнюю директорию.',
      solution: 'package main\n\nimport (\n    "encoding/json"\n    "fmt"\n    "os"\n    "path/filepath"\n    "strconv"\n    "time"\n)\n\ntype Todo struct {\n    ID        int       `json:"id"`\n    Text      string    `json:"text"`\n    Done      bool      `json:"done"`\n    CreatedAt time.Time `json:"created_at"`\n}\n\ntype TodoList struct {\n    todos    []Todo\n    filename string\n}\n\nfunc NewTodoList() *TodoList {\n    home, _ := os.UserHomeDir()\n    filename := filepath.Join(home, ".todos.json")\n    tl := &TodoList{filename: filename}\n    tl.load()\n    return tl\n}\n\nfunc (tl *TodoList) load() {\n    data, err := os.ReadFile(tl.filename)\n    if err != nil {\n        return\n    }\n    json.Unmarshal(data, &tl.todos)\n}\n\nfunc (tl *TodoList) save() {\n    data, _ := json.MarshalIndent(tl.todos, "", "  ")\n    os.WriteFile(tl.filename, data, 0644)\n}\n\nfunc (tl *TodoList) nextID() int {\n    maxID := 0\n    for _, t := range tl.todos {\n        if t.ID > maxID {\n            maxID = t.ID\n        }\n    }\n    return maxID + 1\n}\n\nfunc (tl *TodoList) Add(text string) {\n    t := Todo{ID: tl.nextID(), Text: text, Done: false, CreatedAt: time.Now()}\n    tl.todos = append(tl.todos, t)\n    tl.save()\n    fmt.Printf("Добавлено: [%d] %s\\n", t.ID, t.Text)\n}\n\nfunc (tl *TodoList) List(showAll bool) {\n    count := 0\n    for _, t := range tl.todos {\n        if !showAll && t.Done {\n            continue\n        }\n        mark := " "\n        if t.Done {\n            mark = "x"\n        }\n        fmt.Printf("[%s] %d. %s\\n", mark, t.ID, t.Text)\n        if !t.Done {\n            count++\n        }\n    }\n    if !showAll {\n        fmt.Printf("Активных задач: %d\\n", count)\n    }\n}\n\nfunc (tl *TodoList) Done(id int) {\n    for i, t := range tl.todos {\n        if t.ID == id {\n            tl.todos[i].Done = true\n            tl.save()\n            fmt.Printf("Выполнено: %s\\n", t.Text)\n            return\n        }\n    }\n    fmt.Printf("Задача %d не найдена\\n", id)\n}\n\nfunc (tl *TodoList) Delete(id int) {\n    for i, t := range tl.todos {\n        if t.ID == id {\n            tl.todos = append(tl.todos[:i], tl.todos[i+1:]...)\n            tl.save()\n            fmt.Printf("Удалено: %s\\n", t.Text)\n            return\n        }\n    }\n    fmt.Printf("Задача %d не найдена\\n", id)\n}\n\nfunc (tl *TodoList) Clear() {\n    tl.todos = nil\n    tl.save()\n    fmt.Println("Все задачи удалены")\n}\n\nfunc main() {\n    tl := NewTodoList()\n    args := os.Args[1:]\n    if len(args) == 0 {\n        fmt.Println("Использование: todo <команда> [аргументы]")\n        fmt.Println("Команды: add, list, done, delete, clear")\n        return\n    }\n    switch args[0] {\n    case "add":\n        if len(args) < 2 {\n            fmt.Println("Укажите текст задачи")\n            return\n        }\n        tl.Add(args[1])\n    case "list":\n        showAll := len(args) > 1 && args[1] == "-all"\n        tl.List(showAll)\n    case "done":\n        if len(args) < 2 {\n            fmt.Println("Укажите ID задачи")\n            return\n        }\n        id, err := strconv.Atoi(args[1])\n        if err != nil {\n            fmt.Println("Неверный ID")\n            return\n        }\n        tl.Done(id)\n    case "delete":\n        if len(args) < 2 {\n            fmt.Println("Укажите ID задачи")\n            return\n        }\n        id, _ := strconv.Atoi(args[1])\n        tl.Delete(id)\n    case "clear":\n        tl.Clear()\n    default:\n        fmt.Printf("Неизвестная команда: %s\\n", args[0])\n    }\n}',
      explanation: 'os.UserHomeDir() находит домашнюю директорию кроссплатформенно. Паттерн load-modify-save: загружаем при старте, сохраняем после каждого изменения. nextID вычисляет через максимум существующих ID — корректно работает после удалений. append(todos[:i], todos[i+1:]...) — идиоматическое удаление элемента из срез по индексу.'
    },
    {
      id: 7,
      title: 'Конвертер CSV в JSON',
      type: 'practice',
      difficulty: 'easy',
      description: 'Создай CLI утилиту для конвертации CSV файлов в JSON с поддержкой разных разделителей и опциональным указанием типов данных.',
      requirements: [
        'Флаги: -sep (разделитель, по умолчанию ","), -pretty (форматированный вывод), -skip (пропустить N строк), -out (выходной файл)',
        'Первая строка CSV — заголовки (ключи JSON объектов)',
        'Числа (целые и дробные) автоматически определяются и конвертируются',
        'Пустые строки пропускаются',
        'Вывод: массив JSON объектов'
      ],
      expectedOutput: 'Конвертировано 5 строк\n[\n  {"age":30,"city":"Алматы","name":"Alice"},\n  {"age":25,"city":"Астана","name":"Bob"}\n]',
      hint: 'csv.NewReader(f).Comma = rune(sep[0]) задаёт разделитель. Для определения типа используй strconv.Atoi и strconv.ParseFloat. Создавай map[string]interface{} для каждой строки.',
      solution: 'package main\n\nimport (\n    "encoding/csv"\n    "encoding/json"\n    "flag"\n    "fmt"\n    "io"\n    "os"\n    "strconv"\n    "strings"\n)\n\nfunc parseValue(s string) interface{} {\n    if s == "" {\n        return nil\n    }\n    if i, err := strconv.Atoi(s); err == nil {\n        return i\n    }\n    if f, err := strconv.ParseFloat(s, 64); err == nil {\n        return f\n    }\n    if s == "true" {\n        return true\n    }\n    if s == "false" {\n        return false\n    }\n    return s\n}\n\nfunc csvToJSON(reader io.Reader, sep rune, skipRows int) ([]map[string]interface{}, error) {\n    r := csv.NewReader(reader)\n    r.Comma = sep\n    r.TrimLeadingSpace = true\n    records, err := r.ReadAll()\n    if err != nil {\n        return nil, err\n    }\n    if len(records) == 0 {\n        return nil, fmt.Errorf("пустой CSV файл")\n    }\n    headers := records[0]\n    var result []map[string]interface{}\n    for _, row := range records[1+skipRows:] {\n        if len(strings.Join(row, "")) == 0 {\n            continue\n        }\n        obj := make(map[string]interface{})\n        for i, h := range headers {\n            if i < len(row) {\n                obj[h] = parseValue(row[i])\n            }\n        }\n        result = append(result, obj)\n    }\n    return result, nil\n}\n\nfunc main() {\n    sep := flag.String("sep", ",", "разделитель")\n    pretty := flag.Bool("pretty", false, "форматированный вывод")\n    skip := flag.Int("skip", 0, "пропустить N строк после заголовка")\n    out := flag.String("out", "", "выходной файл")\n    flag.Parse()\n    var input io.Reader\n    if flag.NArg() > 0 {\n        f, err := os.Open(flag.Arg(0))\n        if err != nil {\n            fmt.Fprintln(os.Stderr, err)\n            os.Exit(1)\n        }\n        defer f.Close()\n        input = f\n    } else {\n        input = os.Stdin\n    }\n    sepRune := rune((*sep)[0])\n    data, err := csvToJSON(input, sepRune, *skip)\n    if err != nil {\n        fmt.Fprintln(os.Stderr, "Ошибка:", err)\n        os.Exit(1)\n    }\n    fmt.Printf("Конвертировано %d строк\\n", len(data))\n    var jsonBytes []byte\n    if *pretty {\n        jsonBytes, err = json.MarshalIndent(data, "", "  ")\n    } else {\n        jsonBytes, err = json.Marshal(data)\n    }\n    if err != nil {\n        fmt.Fprintln(os.Stderr, "Ошибка сериализации:", err)\n        os.Exit(1)\n    }\n    if *out != "" {\n        os.WriteFile(*out, jsonBytes, 0644)\n        fmt.Printf("Сохранено в %s\\n", *out)\n    } else {\n        fmt.Println(string(jsonBytes))\n    }\n}',
      explanation: 'csv.NewReader гибко настраивается: Comma задаёт разделитель, TrimLeadingSpace убирает пробелы. Автоопределение типов через parseValue улучшает качество JSON (числа вместо строк). io.Reader как параметр csvToJSON делает функцию тестируемой с любым источником данных (файл, stdin, strings.Reader в тестах).'
    },
    {
      id: 8,
      title: 'Слежение за логом с фильтрацией',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй утилиту tail -f с фильтрацией строк по паттерну и подсветкой совпадений.',
      requirements: [
        'Флаги: -n (последние N строк, по умолчанию 10), -f (follow: следить за новыми строками), -filter (регулярное выражение), -level (фильтр по уровню: ERROR/WARN/INFO)',
        'При запуске показывает последние N строк файла',
        'В режиме -f: ждёт новых строк (периодически опрашивает файл)',
        'Строки удовлетворяющие фильтру выводятся с префиксом уровня',
        'Ctrl+C завершает работу корректно (signal handling)'
      ],
      expectedOutput: '=== Последние 3 строки ===\n[ERROR] connection failed\n[WARN] high load\n[INFO] server started\n=== Слежение за новыми строками ===\n[ERROR] new error occurred',
      hint: 'Для tail-n: прочитай все строки, возьми последние n через slice[len-n:]. Для follow: используй os.Stat периодически, при изменении размера читай новые байты начиная с предыдущего offset. signal.Notify для Ctrl+C.',
      solution: 'package main\n\nimport (\n    "bufio"\n    "flag"\n    "fmt"\n    "io"\n    "os"\n    "os/signal"\n    "regexp"\n    "strings"\n    "syscall"\n    "time"\n)\n\nfunc readLastN(filename string, n int) ([]string, int64, error) {\n    f, err := os.Open(filename)\n    if err != nil {\n        return nil, 0, err\n    }\n    defer f.Close()\n    var lines []string\n    scanner := bufio.NewScanner(f)\n    for scanner.Scan() {\n        lines = append(lines, scanner.Text())\n    }\n    offset, _ := f.Seek(0, io.SeekEnd)\n    if n > len(lines) {\n        n = len(lines)\n    }\n    return lines[len(lines)-n:], offset, scanner.Err()\n}\n\nfunc matchLine(line, level string, re *regexp.Regexp) bool {\n    if level != "" && !strings.Contains(strings.ToUpper(line), "["+strings.ToUpper(level)+"]") {\n        return false\n    }\n    if re != nil && !re.MatchString(line) {\n        return false\n    }\n    return true\n}\n\nfunc followFile(filename string, offset int64, level string, re *regexp.Regexp, done <-chan struct{}) {\n    for {\n        select {\n        case <-done:\n            return\n        case <-time.After(200 * time.Millisecond):\n            info, err := os.Stat(filename)\n            if err != nil {\n                continue\n            }\n            if info.Size() > offset {\n                f, err := os.Open(filename)\n                if err != nil {\n                    continue\n                }\n                f.Seek(offset, 0)\n                scanner := bufio.NewScanner(f)\n                for scanner.Scan() {\n                    line := scanner.Text()\n                    if matchLine(line, level, re) {\n                        fmt.Println(line)\n                    }\n                }\n                offset = info.Size()\n                f.Close()\n            }\n        }\n    }\n}\n\nfunc main() {\n    n := flag.Int("n", 10, "последние N строк")\n    follow := flag.Bool("f", false, "следить за новыми строками")\n    filter := flag.String("filter", "", "регулярное выражение")\n    level := flag.String("level", "", "уровень лога (ERROR/WARN/INFO)")\n    flag.Parse()\n    if flag.NArg() == 0 {\n        fmt.Fprintln(os.Stderr, "Укажите файл")\n        os.Exit(1)\n    }\n    filename := flag.Arg(0)\n    var re *regexp.Regexp\n    if *filter != "" {\n        var err error\n        re, err = regexp.Compile(*filter)\n        if err != nil {\n            fmt.Fprintln(os.Stderr, "Неверное регулярное выражение:", err)\n            os.Exit(1)\n        }\n    }\n    lines, offset, err := readLastN(filename, *n)\n    if err != nil {\n        fmt.Fprintln(os.Stderr, "Ошибка:", err)\n        os.Exit(1)\n    }\n    fmt.Printf("=== Последние %d строк ===\\n", *n)\n    for _, line := range lines {\n        if matchLine(line, *level, re) {\n            fmt.Println(line)\n        }\n    }\n    if !*follow {\n        return\n    }\n    fmt.Println("=== Слежение за новыми строками ===")\n    sigCh := make(chan os.Signal, 1)\n    signal.Notify(sigCh, syscall.SIGINT, syscall.SIGTERM)\n    done := make(chan struct{})\n    go followFile(filename, offset, *level, re, done)\n    <-sigCh\n    close(done)\n    fmt.Println("\\nЗавершено")\n}',
      explanation: 'Паттерн периодического опроса файла (polling) — простейший способ отслеживания изменений. Сохраняем offset после первоначального чтения и при каждой проверке ищем новые байты. signal.Notify перехватывает SIGINT (Ctrl+C) и SIGTERM — это позволяет корректно завершиться: закрыть файлы, вывести итог. done-канал завершает горутину слежения.'
    }
  ]
}
