export default {
  id: 83,
  title: 'Паттерн Observer',
  description: 'Система событий в Go: реализация EventBus, подписка и публикация событий, типизированные события с дженериками, потокобезопасность и практические примеры использования.',
  lessons: [
    {
      id: 1,
      title: 'Паттерн Observer: система событий',
      type: 'theory',
      content: [
        { type: 'text', value: 'Observer (Наблюдатель) — паттерн, при котором объект (Publisher) уведомляет подписчиков (Subscribers) об изменениях своего состояния. В Go это реализуется через интерфейсы и каналы.' },
        { type: 'heading', value: 'Базовый Observer через интерфейс' },
        { type: 'code', language: 'go', value: 'package main\n\nimport "fmt"\n\n// Observer — интерфейс подписчика\ntype Observer interface {\n    OnEvent(event Event)\n}\n\n// Event — базовый тип события\ntype Event struct {\n    Type    string\n    Payload interface{}\n}\n\n// Subject — объект, за которым наблюдают\ntype Subject struct {\n    observers []Observer\n}\n\nfunc (s *Subject) Subscribe(o Observer) {\n    s.observers = append(s.observers, o)\n}\n\nfunc (s *Subject) Unsubscribe(o Observer) {\n    for i, obs := range s.observers {\n        if obs == o {\n            s.observers = append(s.observers[:i], s.observers[i+1:]...)\n            return\n        }\n    }\n}\n\nfunc (s *Subject) Notify(event Event) {\n    for _, o := range s.observers {\n        o.OnEvent(event)\n    }\n}\n\n// Конкретные наблюдатели\ntype Logger struct{ name string }\n\nfunc (l *Logger) OnEvent(e Event) {\n    fmt.Printf("[Logger:%s] Событие %q: %v\\n", l.name, e.Type, e.Payload)\n}\n\ntype Alerter struct{ threshold int }\n\nfunc (a *Alerter) OnEvent(e Event) {\n    if e.Type == "temperature" {\n        temp := e.Payload.(int)\n        if temp > a.threshold {\n            fmt.Printf("[ALERT] Температура %d превышает порог %d!\\n", temp, a.threshold)\n        }\n    }\n}\n\n// Конкретный Subject — термостат\ntype Thermostat struct {\n    Subject\n    temp int\n}\n\nfunc (t *Thermostat) SetTemperature(temp int) {\n    old := t.temp\n    t.temp = temp\n    if old != temp {\n        t.Notify(Event{Type: "temperature", Payload: temp})\n    }\n}\n\nfunc main() {\n    thermo := &Thermostat{}\n    logger := &Logger{name: "main"}\n    alerter := &Alerter{threshold: 30}\n\n    thermo.Subscribe(logger)\n    thermo.Subscribe(alerter)\n\n    thermo.SetTemperature(20)\n    thermo.SetTemperature(25)\n    thermo.SetTemperature(35) // должен сработать alert\n\n    fmt.Println("--- Отписываем logger ---")\n    thermo.Unsubscribe(logger)\n    thermo.SetTemperature(40) // только alerter\n}' },
        { type: 'note', value: 'Базовый Observer через интерфейс прост, но не потокобезопасен. Для многопоточных приложений нужен EventBus с мьютексами или каналами.' }
      ]
    },
    {
      id: 2,
      title: 'EventBus: реализация шины событий',
      type: 'theory',
      content: [
        { type: 'text', value: 'EventBus — централизованный брокер событий. Подписчики регистрируются на определённые типы событий. Publisher публикует событие не зная о подписчиках.' },
        { type: 'heading', value: 'Потокобезопасный EventBus' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "sync"\n)\n\ntype HandlerFunc func(event interface{})\n\ntype EventBus struct {\n    mu       sync.RWMutex\n    handlers map[string][]HandlerFunc\n}\n\nfunc NewEventBus() *EventBus {\n    return &EventBus{\n        handlers: make(map[string][]HandlerFunc),\n    }\n}\n\n// Subscribe регистрирует обработчик для типа события\nfunc (eb *EventBus) Subscribe(eventType string, handler HandlerFunc) {\n    eb.mu.Lock()\n    defer eb.mu.Unlock()\n    eb.handlers[eventType] = append(eb.handlers[eventType], handler)\n}\n\n// Publish рассылает событие всем подписчикам\nfunc (eb *EventBus) Publish(eventType string, data interface{}) {\n    eb.mu.RLock()\n    handlers := make([]HandlerFunc, len(eb.handlers[eventType]))\n    copy(handlers, eb.handlers[eventType])\n    eb.mu.RUnlock()\n\n    for _, h := range handlers {\n        h(data)\n    }\n}\n\n// PublishAsync публикует событие асинхронно\nfunc (eb *EventBus) PublishAsync(eventType string, data interface{}) {\n    eb.mu.RLock()\n    handlers := make([]HandlerFunc, len(eb.handlers[eventType]))\n    copy(handlers, eb.handlers[eventType])\n    eb.mu.RUnlock()\n\n    for _, h := range handlers {\n        go h(data)\n    }\n}\n\n// Unsubscribe удаляет все обработчики для типа события\nfunc (eb *EventBus) UnsubscribeAll(eventType string) {\n    eb.mu.Lock()\n    defer eb.mu.Unlock()\n    delete(eb.handlers, eventType)\n}\n\n// Типы событий (константы)\nconst (\n    EventUserCreated = "user.created"\n    EventUserDeleted = "user.deleted"\n    EventOrderPlaced = "order.placed"\n)\n\ntype User struct{ ID int; Name string }\ntype Order struct{ ID int; UserID int; Amount float64 }\n\nfunc main() {\n    bus := NewEventBus()\n\n    // Подписываемся на события пользователя\n    bus.Subscribe(EventUserCreated, func(data interface{}) {\n        user := data.(User)\n        fmt.Printf("[Email] Добро пожаловать, %s!\\n", user.Name)\n    })\n\n    bus.Subscribe(EventUserCreated, func(data interface{}) {\n        user := data.(User)\n        fmt.Printf("[Analytics] Новый пользователь: ID=%d\\n", user.ID)\n    })\n\n    bus.Subscribe(EventOrderPlaced, func(data interface{}) {\n        order := data.(Order)\n        fmt.Printf("[Payment] Обработка заказа #%d на %.2f руб.\\n", order.ID, order.Amount)\n    })\n\n    bus.Subscribe(EventOrderPlaced, func(data interface{}) {\n        order := data.(Order)\n        fmt.Printf("[Inventory] Резервирование товаров для заказа #%d\\n", order.ID)\n    })\n\n    // Публикуем события\n    bus.Publish(EventUserCreated, User{ID: 1, Name: "Нурдаулет"})\n    bus.Publish(EventOrderPlaced, Order{ID: 101, UserID: 1, Amount: 2500.00})\n    bus.Publish(EventUserDeleted, User{ID: 2}) // нет подписчиков — ничего не произойдёт\n}' },
        { type: 'tip', value: 'При чтении handlers используем copy() перед снятием блокировки. Это позволяет вызывать обработчики без удержания RLock и избегает дедлоков, если обработчик сам публикует событие.' }
      ]
    },
    {
      id: 3,
      title: 'Subscribe и Publish: управление подписками',
      type: 'theory',
      content: [
        { type: 'text', value: 'Для production-систем важно иметь возможность отписаться от конкретного обработчика. Это достигается через возврат функции-отписки (unsubscribe token).' },
        { type: 'heading', value: 'EventBus с возможностью отписки' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "sync"\n    "sync/atomic"\n)\n\ntype SubscriptionID uint64\n\ntype subscription struct {\n    id      SubscriptionID\n    handler func(interface{})\n}\n\ntype SafeEventBus struct {\n    mu      sync.RWMutex\n    subs    map[string][]subscription\n    counter atomic.Uint64\n}\n\nfunc NewSafeEventBus() *SafeEventBus {\n    return &SafeEventBus{\n        subs: make(map[string][]subscription),\n    }\n}\n\n// Subscribe возвращает функцию отписки\nfunc (eb *SafeEventBus) Subscribe(topic string, handler func(interface{})) func() {\n    eb.mu.Lock()\n    defer eb.mu.Unlock()\n\n    id := SubscriptionID(eb.counter.Add(1))\n    eb.subs[topic] = append(eb.subs[topic], subscription{id: id, handler: handler})\n\n    // Возвращаем функцию-отписку (замыкание)\n    return func() {\n        eb.mu.Lock()\n        defer eb.mu.Unlock()\n        subs := eb.subs[topic]\n        for i, s := range subs {\n            if s.id == id {\n                eb.subs[topic] = append(subs[:i], subs[i+1:]...)\n                return\n            }\n        }\n    }\n}\n\nfunc (eb *SafeEventBus) Publish(topic string, data interface{}) {\n    eb.mu.RLock()\n    subs := make([]subscription, len(eb.subs[topic]))\n    copy(subs, eb.subs[topic])\n    eb.mu.RUnlock()\n\n    for _, s := range subs {\n        s.handler(data)\n    }\n}\n\nfunc main() {\n    bus := NewSafeEventBus()\n\n    // Подписываемся — получаем функцию отписки\n    unsubEmail := bus.Subscribe("user.login", func(data interface{}) {\n        fmt.Printf("[Email] Вход пользователя: %v\\n", data)\n    })\n\n    unsubLog := bus.Subscribe("user.login", func(data interface{}) {\n        fmt.Printf("[Log] Запись о входе: %v\\n", data)\n    })\n\n    fmt.Println("--- Первый вход (2 подписчика) ---")\n    bus.Publish("user.login", "alice@example.com")\n\n    // Отписываем email-уведомления\n    unsubEmail()\n    fmt.Println("--- Второй вход (1 подписчик) ---")\n    bus.Publish("user.login", "bob@example.com")\n\n    // Отписываем логирование\n    unsubLog()\n    fmt.Println("--- Третий вход (0 подписчиков) ---")\n    bus.Publish("user.login", "carol@example.com") // тихо\n    fmt.Println("Нет вывода — все отписаны")\n}' },
        { type: 'note', value: 'Паттерн "возврат функции отписки" — идиоматичный Go-подход. Вместо ID подписки клиент держит функцию unsubscribe(), которую вызывает когда нужно. Это проще чем управлять ID вручную.' }
      ]
    },
    {
      id: 4,
      title: 'Типизированные события с дженериками',
      type: 'theory',
      content: [
        { type: 'text', value: 'С приходом дженериков в Go 1.18 можно создавать типобезопасный EventBus, избавляясь от приведения interface{} к конкретному типу.' },
        { type: 'heading', value: 'Типобезопасный EventBus с дженериками' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "sync"\n)\n\n// TypedBus — типобезопасный EventBus для конкретного типа события\ntype TypedBus[T any] struct {\n    mu       sync.RWMutex\n    handlers []func(T)\n}\n\nfunc (b *TypedBus[T]) Subscribe(handler func(T)) func() {\n    b.mu.Lock()\n    defer b.mu.Unlock()\n    b.handlers = append(b.handlers, handler)\n    idx := len(b.handlers) - 1\n    return func() {\n        b.mu.Lock()\n        defer b.mu.Unlock()\n        b.handlers = append(b.handlers[:idx], b.handlers[idx+1:]...)\n    }\n}\n\nfunc (b *TypedBus[T]) Publish(event T) {\n    b.mu.RLock()\n    handlers := make([]func(T), len(b.handlers))\n    copy(handlers, b.handlers)\n    b.mu.RUnlock()\n    for _, h := range handlers {\n        h(event)\n    }\n}\n\n// Конкретные типы событий\ntype UserCreatedEvent struct {\n    UserID   int\n    Username string\n    Email    string\n}\n\ntype OrderPlacedEvent struct {\n    OrderID  int\n    UserID   int\n    Amount   float64\n    Items    []string\n}\n\ntype PaymentEvent struct {\n    OrderID int\n    Status  string\n    Amount  float64\n}\n\n// AppEvents — все шины событий приложения\ntype AppEvents struct {\n    UserCreated  TypedBus[UserCreatedEvent]\n    OrderPlaced  TypedBus[OrderPlacedEvent]\n    PaymentDone  TypedBus[PaymentEvent]\n}\n\nfunc main() {\n    events := &AppEvents{}\n\n    // Подписка — компилятор знает типы, приведение не нужно!\n    events.UserCreated.Subscribe(func(e UserCreatedEvent) {\n        fmt.Printf("[Welcome] Добро пожаловать, %s! Письмо на %s\\n",\n            e.Username, e.Email)\n    })\n\n    events.UserCreated.Subscribe(func(e UserCreatedEvent) {\n        fmt.Printf("[Analytics] Новый юзер #%d: %s\\n", e.UserID, e.Username)\n    })\n\n    events.OrderPlaced.Subscribe(func(e OrderPlacedEvent) {\n        fmt.Printf("[Order] Заказ #%d от юзера #%d на %.2f руб: %v\\n",\n            e.OrderID, e.UserID, e.Amount, e.Items)\n    })\n\n    events.PaymentDone.Subscribe(func(e PaymentEvent) {\n        fmt.Printf("[Payment] Заказ #%d: %s (%.2f руб)\\n",\n            e.OrderID, e.Status, e.Amount)\n    })\n\n    // Публикация\n    events.UserCreated.Publish(UserCreatedEvent{\n        UserID: 1, Username: "nurdaulet", Email: "n@example.kz",\n    })\n\n    events.OrderPlaced.Publish(OrderPlacedEvent{\n        OrderID: 101, UserID: 1, Amount: 3500.00,\n        Items: []string{"Книга Go", "Кофе"},\n    })\n\n    events.PaymentDone.Publish(PaymentEvent{\n        OrderID: 101, Status: "success", Amount: 3500.00,\n    })\n}' },
        { type: 'tip', value: 'AppEvents — агрегатор всех шин событий. Это упрощает внедрение зависимостей: передаёте *AppEvents в сервисы вместо набора отдельных шин.' }
      ]
    },
    {
      id: 5,
      title: 'Реальное применение Observer в приложениях',
      type: 'theory',
      content: [
        { type: 'text', value: 'Observer широко используется в Go-приложениях: мониторинг метрик, lifecycle хуки, audit log, нотификации. Рассмотрим паттерн Domain Events.' },
        { type: 'heading', value: 'Domain Events в DDD-стиле' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "time"\n)\n\n// DomainEvent — базовый интерфейс доменного события\ntype DomainEvent interface {\n    EventType() string\n    OccurredAt() time.Time\n}\n\ntype BaseEvent struct {\n    eventType  string\n    occurredAt time.Time\n}\n\nfunc NewBaseEvent(t string) BaseEvent {\n    return BaseEvent{eventType: t, occurredAt: time.Now()}\n}\nfunc (e BaseEvent) EventType() string     { return e.eventType }\nfunc (e BaseEvent) OccurredAt() time.Time { return e.occurredAt }\n\n// Конкретные доменные события\ntype AccountCreated struct {\n    BaseEvent\n    AccountID string\n    Email     string\n}\n\ntype MoneyDeposited struct {\n    BaseEvent\n    AccountID string\n    Amount    float64\n    Balance   float64\n}\n\ntype MoneyWithdrawn struct {\n    BaseEvent\n    AccountID string\n    Amount    float64\n    Balance   float64\n}\n\n// EventDispatcher — диспетчер доменных событий\ntype EventDispatcher struct {\n    handlers map[string][]func(DomainEvent)\n}\n\nfunc NewEventDispatcher() *EventDispatcher {\n    return &EventDispatcher{handlers: make(map[string][]func(DomainEvent))}\n}\n\nfunc (d *EventDispatcher) Register(eventType string, handler func(DomainEvent)) {\n    d.handlers[eventType] = append(d.handlers[eventType], handler)\n}\n\nfunc (d *EventDispatcher) Dispatch(event DomainEvent) {\n    for _, h := range d.handlers[event.EventType()] {\n        h(event)\n    }\n}\n\n// Account — доменный объект\ntype Account struct {\n    ID         string\n    balance    float64\n    dispatcher *EventDispatcher\n    events     []DomainEvent\n}\n\nfunc NewAccount(id, email string, d *EventDispatcher) *Account {\n    a := &Account{ID: id, dispatcher: d}\n    evt := AccountCreated{BaseEvent: NewBaseEvent("account.created"), AccountID: id, Email: email}\n    a.events = append(a.events, evt)\n    d.Dispatch(evt)\n    return a\n}\n\nfunc (a *Account) Deposit(amount float64) {\n    a.balance += amount\n    evt := MoneyDeposited{BaseEvent: NewBaseEvent("money.deposited"), AccountID: a.ID, Amount: amount, Balance: a.balance}\n    a.dispatcher.Dispatch(evt)\n}\n\nfunc (a *Account) Withdraw(amount float64) error {\n    if amount > a.balance {\n        return fmt.Errorf("недостаточно средств: баланс %.2f", a.balance)\n    }\n    a.balance -= amount\n    evt := MoneyWithdrawn{BaseEvent: NewBaseEvent("money.withdrawn"), AccountID: a.ID, Amount: amount, Balance: a.balance}\n    a.dispatcher.Dispatch(evt)\n    return nil\n}\n\nfunc main() {\n    dispatcher := NewEventDispatcher()\n\n    // Audit log\n    dispatcher.Register("account.created", func(e DomainEvent) {\n        ev := e.(AccountCreated)\n        fmt.Printf("[Audit] Создан счёт %s для %s в %s\\n",\n            ev.AccountID, ev.Email, ev.OccurredAt().Format("15:04:05"))\n    })\n    dispatcher.Register("money.deposited", func(e DomainEvent) {\n        ev := e.(MoneyDeposited)\n        fmt.Printf("[Audit] Пополнение: счёт %s +%.2f, баланс: %.2f\\n",\n            ev.AccountID, ev.Amount, ev.Balance)\n    })\n    dispatcher.Register("money.withdrawn", func(e DomainEvent) {\n        ev := e.(MoneyWithdrawn)\n        fmt.Printf("[Audit] Снятие: счёт %s -%.2f, баланс: %.2f\\n",\n            ev.AccountID, ev.Amount, ev.Balance)\n    })\n\n    acc := NewAccount("ACC-001", "user@example.com", dispatcher)\n    acc.Deposit(10000)\n    acc.Deposit(5000)\n    if err := acc.Withdraw(3000); err != nil {\n        fmt.Println("Ошибка:", err)\n    }\n    if err := acc.Withdraw(20000); err != nil {\n        fmt.Println("Ошибка:", err)\n    }\n}' },
        { type: 'note', value: 'Domain Events — это события внутри доменной логики. Они отделяют побочные эффекты (email, audit) от основной бизнес-логики. Это ключевой принцип DDD (Domain-Driven Design).' }
      ]
    },
    {
      id: 6,
      title: 'Практика: EventBus для системы мониторинга',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй EventBus для системы мониторинга сервера. Публикуй события о метриках CPU, памяти и ошибках. Подписчики логируют, отправляют алерты и ведут статистику.',
      requirements: [
        'Тип EventBus с методами Subscribe(topic, handler) func() и Publish(topic, data)',
        'Три типа событий: MetricEvent{Name, Value float64}, AlertEvent{Level, Message string}, ErrorEvent{Code int, Err string}',
        'Константы топиков: TopicMetrics, TopicAlerts, TopicErrors',
        'Подписчик-логгер: выводит все события с временной меткой',
        'Подписчик-алертер: при MetricEvent CPU > 80% публикует AlertEvent в TopicAlerts',
        'Подписчик-счётчик: считает количество ошибок по коду',
        'В main симулировать 5 метрик, 2 ошибки и проверить счётчик'
      ],
      expectedOutput: '[15:04:05] metrics: CPU=45.00\n[15:04:05] metrics: CPU=92.00\n[ALERT] HIGH: CPU критически высок: 92.00%\n[15:04:05] alerts: HIGH CPU критически высок: 92.00%\nОшибок с кодом 500: 2',
      hint: 'Алертер подписывается на TopicMetrics и сам вызывает bus.Publish(TopicAlerts, ...). Это демонстрирует цепочку событий. Для счётчика используй map[int]int защищённый sync.Mutex.',
      solution: 'package main\n\nimport (\n    "fmt"\n    "sync"\n    "time"\n)\n\nconst (\n    TopicMetrics = "metrics"\n    TopicAlerts  = "alerts"\n    TopicErrors  = "errors"\n)\n\ntype MetricEvent struct{ Name string; Value float64 }\ntype AlertEvent struct{ Level, Message string }\ntype ErrorEvent struct{ Code int; Err string }\n\ntype EventBus struct {\n    mu       sync.RWMutex\n    handlers map[string][]func(interface{})\n}\n\nfunc NewEventBus() *EventBus {\n    return &EventBus{handlers: make(map[string][]func(interface{}))}\n}\n\nfunc (eb *EventBus) Subscribe(topic string, handler func(interface{})) func() {\n    eb.mu.Lock()\n    defer eb.mu.Unlock()\n    eb.handlers[topic] = append(eb.handlers[topic], handler)\n    idx := len(eb.handlers[topic]) - 1\n    return func() {\n        eb.mu.Lock()\n        defer eb.mu.Unlock()\n        h := eb.handlers[topic]\n        eb.handlers[topic] = append(h[:idx], h[idx+1:]...)\n    }\n}\n\nfunc (eb *EventBus) Publish(topic string, data interface{}) {\n    eb.mu.RLock()\n    handlers := make([]func(interface{}), len(eb.handlers[topic]))\n    copy(handlers, eb.handlers[topic])\n    eb.mu.RUnlock()\n    for _, h := range handlers {\n        h(data)\n    }\n}\n\ntype ErrorCounter struct {\n    mu     sync.Mutex\n    counts map[int]int\n}\n\nfunc NewErrorCounter() *ErrorCounter {\n    return &ErrorCounter{counts: make(map[int]int)}\n}\n\nfunc (ec *ErrorCounter) Handle(data interface{}) {\n    e := data.(ErrorEvent)\n    ec.mu.Lock()\n    ec.counts[e.Code]++\n    ec.mu.Unlock()\n}\n\nfunc (ec *ErrorCounter) Count(code int) int {\n    ec.mu.Lock()\n    defer ec.mu.Unlock()\n    return ec.counts[code]\n}\n\nfunc main() {\n    bus := NewEventBus()\n    counter := NewErrorCounter()\n\n    // Логгер — все топики\n    logHandler := func(topic string) func(interface{}) {\n        return func(data interface{}) {\n            ts := time.Now().Format("15:04:05")\n            switch v := data.(type) {\n            case MetricEvent:\n                fmt.Printf("[%s] %s: %s=%.2f\\n", ts, topic, v.Name, v.Value)\n            case AlertEvent:\n                fmt.Printf("[%s] %s: %s %s\\n", ts, topic, v.Level, v.Message)\n            case ErrorEvent:\n                fmt.Printf("[%s] %s: code=%d %s\\n", ts, topic, v.Code, v.Err)\n            }\n        }\n    }\n    bus.Subscribe(TopicMetrics, logHandler(TopicMetrics))\n    bus.Subscribe(TopicAlerts, logHandler(TopicAlerts))\n    bus.Subscribe(TopicErrors, logHandler(TopicErrors))\n\n    // Алертер CPU\n    bus.Subscribe(TopicMetrics, func(data interface{}) {\n        e := data.(MetricEvent)\n        if e.Name == "CPU" && e.Value > 80 {\n            msg := fmt.Sprintf("CPU критически высок: %.2f%%", e.Value)\n            fmt.Printf("[ALERT] HIGH: %s\\n", msg)\n            bus.Publish(TopicAlerts, AlertEvent{Level: "HIGH", Message: msg})\n        }\n    })\n\n    // Счётчик ошибок\n    bus.Subscribe(TopicErrors, counter.Handle)\n\n    // Симуляция\n    bus.Publish(TopicMetrics, MetricEvent{Name: "CPU", Value: 45.0})\n    bus.Publish(TopicMetrics, MetricEvent{Name: "Memory", Value: 60.0})\n    bus.Publish(TopicMetrics, MetricEvent{Name: "CPU", Value: 92.0})\n    bus.Publish(TopicErrors, ErrorEvent{Code: 500, Err: "internal server error"})\n    bus.Publish(TopicErrors, ErrorEvent{Code: 500, Err: "database timeout"})\n    bus.Publish(TopicErrors, ErrorEvent{Code: 404, Err: "not found"})\n\n    fmt.Printf("Ошибок с кодом 500: %d\\n", counter.Count(500))\n    fmt.Printf("Ошибок с кодом 404: %d\\n", counter.Count(404))\n}',
      explanation: 'EventBus копирует слайс обработчиков перед вызовом (copy(handlers, ...)), что позволяет обработчику безопасно публиковать новые события без дедлока. Алертер демонстрирует цепочку событий: MetricEvent -> AlertEvent. Счётчик использует свой мьютекс — это правильно, так как его состояние независимо от EventBus. Возврат функции отписки удобен для lifecycle управления: компонент отписывается при завершении работы.'
    }
  ]
}
