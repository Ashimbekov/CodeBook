export default {
  id: 81,
  title: 'Паттерн Functional Options',
  description: 'Элегантное решение проблемы множества параметров конструктора через функциональные опции: option-функции, паттерны WithTimeout/WithRetry, значения по умолчанию и реальный пример конфигурации HTTP-клиента.',
  lessons: [
    {
      id: 1,
      title: 'Проблема множества параметров конструктора',
      type: 'theory',
      content: [
        { type: 'text', value: 'Когда структура имеет много опциональных параметров, конструктор становится неудобным. Golang не поддерживает перегрузку функций, поэтому приходится искать другие решения.' },
        { type: 'heading', value: 'Антипаттерн: конструктор с множеством параметров' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "time"\n)\n\n// Плохой подход: каждый новый параметр ломает все вызовы\ntype Server struct {\n    host           string\n    port           int\n    timeout        time.Duration\n    maxConnections int\n    tls            bool\n    certFile       string\n    keyFile        string\n    readTimeout    time.Duration\n    writeTimeout   time.Duration\n}\n\n// Конструктор с 9 параметрами — кошмар для вызывающего кода\nfunc NewServerBad(host string, port int, timeout time.Duration,\n    maxConns int, tls bool, certFile, keyFile string,\n    readTimeout, writeTimeout time.Duration) *Server {\n    return &Server{\n        host:           host,\n        port:           port,\n        timeout:        timeout,\n        maxConnections: maxConns,\n        tls:            tls,\n        certFile:       certFile,\n        keyFile:        keyFile,\n        readTimeout:    readTimeout,\n        writeTimeout:   writeTimeout,\n    }\n}\n\nfunc main() {\n    // Вызов невозможно читать: что значит каждый аргумент?\n    s := NewServerBad("localhost", 8080, 30*time.Second,\n        100, false, "", "", 5*time.Second, 10*time.Second)\n    fmt.Printf("Server: %s:%d\\n", s.host, s.port)\n}\n// Если добавить новый параметр — придётся менять все вызовы' },
        { type: 'warning', value: 'Конструктор с более чем 3-4 параметрами — признак того, что нужен другой подход. Функциональные опции решают эту проблему элегантно.' },
        { type: 'heading', value: 'Промежуточное решение: Config-структура' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "time"\n)\n\ntype ServerConfig struct {\n    Host           string\n    Port           int\n    Timeout        time.Duration\n    MaxConnections int\n}\n\ntype Server struct {\n    config ServerConfig\n}\n\nfunc NewServer(cfg ServerConfig) *Server {\n    // Устанавливаем значения по умолчанию вручную\n    if cfg.Port == 0 {\n        cfg.Port = 8080\n    }\n    if cfg.Timeout == 0 {\n        cfg.Timeout = 30 * time.Second\n    }\n    if cfg.MaxConnections == 0 {\n        cfg.MaxConnections = 100\n    }\n    return &Server{config: cfg}\n}\n\nfunc main() {\n    // Лучше, но нужно передавать всю структуру\n    s := NewServer(ServerConfig{\n        Host:    "localhost",\n        Port:    9090,\n        Timeout: 60 * time.Second,\n    })\n    fmt.Printf("Server: %s:%d\\n", s.config.Host, s.config.Port)\n}\n// Config-структура лучше, но у неё тоже есть недостатки:\n// нельзя валидировать каждую опцию отдельно' },
        { type: 'tip', value: 'Config-структура лучше позиционных параметров, но функциональные опции дают больше гибкости: каждая опция инкапсулирует логику установки и валидации.' }
      ]
    },
    {
      id: 2,
      title: 'Функциональные опции — основной паттерн',
      type: 'theory',
      content: [
        { type: 'text', value: 'Functional Options — паттерн, предложенный Робом Пайком. Каждая опция — функция, которая модифицирует конфигурацию. Конструктор принимает вариативный список таких функций.' },
        { type: 'heading', value: 'Базовая реализация' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "time"\n)\n\ntype Server struct {\n    host           string\n    port           int\n    timeout        time.Duration\n    maxConnections int\n}\n\n// Option — тип функции, изменяющей Server\ntype Option func(*Server)\n\n// Конструктор принимает вариативный список опций\nfunc NewServer(host string, opts ...Option) *Server {\n    // Значения по умолчанию\n    s := &Server{\n        host:           host,\n        port:           8080,\n        timeout:        30 * time.Second,\n        maxConnections: 100,\n    }\n\n    // Применяем каждую опцию\n    for _, opt := range opts {\n        opt(s)\n    }\n\n    return s\n}\n\n// Опция: задать порт\nfunc WithPort(port int) Option {\n    return func(s *Server) {\n        s.port = port\n    }\n}\n\n// Опция: задать таймаут\nfunc WithTimeout(d time.Duration) Option {\n    return func(s *Server) {\n        s.timeout = d\n    }\n}\n\n// Опция: задать максимальные подключения\nfunc WithMaxConnections(n int) Option {\n    return func(s *Server) {\n        s.maxConnections = n\n    }\n}\n\nfunc main() {\n    // Минимальный вызов — только обязательный параметр\n    s1 := NewServer("localhost")\n    fmt.Printf("s1: %s:%d (timeout=%v, maxConn=%d)\\n",\n        s1.host, s1.port, s1.timeout, s1.maxConnections)\n\n    // С несколькими опциями\n    s2 := NewServer("0.0.0.0",\n        WithPort(9090),\n        WithTimeout(60*time.Second),\n        WithMaxConnections(500),\n    )\n    fmt.Printf("s2: %s:%d (timeout=%v, maxConn=%d)\\n",\n        s2.host, s2.port, s2.timeout, s2.maxConnections)\n}' },
        { type: 'note', value: 'Обязательные параметры (host) остаются позиционными. Всё опциональное передаётся через With-функции. Добавление новой опции не ломает существующий код.' }
      ]
    },
    {
      id: 3,
      title: 'WithTimeout и WithRetry паттерны',
      type: 'theory',
      content: [
        { type: 'text', value: 'Паттерны WithTimeout и WithRetry — классические примеры functional options для клиентов и сервисов. Они особенно полезны при работе с сетью.' },
        { type: 'heading', value: 'Клиент с опциями таймаута и повторных попыток' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "errors"\n    "fmt"\n    "time"\n)\n\ntype Client struct {\n    baseURL    string\n    timeout    time.Duration\n    retries    int\n    retryDelay time.Duration\n    headers    map[string]string\n    debug      bool\n}\n\ntype ClientOption func(*Client)\n\nfunc NewClient(baseURL string, opts ...ClientOption) *Client {\n    c := &Client{\n        baseURL:    baseURL,\n        timeout:    10 * time.Second,\n        retries:    0,\n        retryDelay: time.Second,\n        headers:    make(map[string]string),\n        debug:      false,\n    }\n    for _, opt := range opts {\n        opt(c)\n    }\n    return c\n}\n\n// WithTimeout задаёт таймаут запроса\nfunc WithTimeout(d time.Duration) ClientOption {\n    return func(c *Client) {\n        c.timeout = d\n    }\n}\n\n// WithRetry задаёт количество повторных попыток и задержку\nfunc WithRetry(attempts int, delay time.Duration) ClientOption {\n    return func(c *Client) {\n        c.retries = attempts\n        c.retryDelay = delay\n    }\n}\n\n// WithHeader добавляет заголовок по умолчанию\nfunc WithHeader(key, value string) ClientOption {\n    return func(c *Client) {\n        c.headers[key] = value\n    }\n}\n\n// WithDebug включает отладочный вывод\nfunc WithDebug() ClientOption {\n    return func(c *Client) {\n        c.debug = true\n    }\n}\n\n// Do симулирует выполнение запроса с retry-логикой\nfunc (c *Client) Do(method, path string) (string, error) {\n    url := c.baseURL + path\n    var lastErr error\n\n    for attempt := 0; attempt <= c.retries; attempt++ {\n        if c.debug && attempt > 0 {\n            fmt.Printf("[DEBUG] Повтор %d/%d для %s %s\\n", attempt, c.retries, method, url)\n        }\n\n        // Симуляция запроса (в реальности — http.Do с timeout)\n        result := fmt.Sprintf("200 OK: %s %s", method, url)\n        if c.debug {\n            fmt.Printf("[DEBUG] %s\\n", result)\n        }\n        return result, nil\n    }\n    return "", fmt.Errorf("все попытки исчерпаны: %w", lastErr)\n}\n\nfunc main() {\n    // Базовый клиент\n    c1 := NewClient("https://api.example.com")\n    res, _ := c1.Do("GET", "/users")\n    fmt.Println(res)\n\n    // Клиент с таймаутом, ретраями и заголовком\n    c2 := NewClient("https://api.example.com",\n        WithTimeout(5*time.Second),\n        WithRetry(3, 500*time.Millisecond),\n        WithHeader("Authorization", "Bearer token123"),\n        WithHeader("Accept", "application/json"),\n        WithDebug(),\n    )\n    res, err := c2.Do("GET", "/orders")\n    if errors.Is(err, nil) {\n        fmt.Println(res)\n    }\n}' },
        { type: 'tip', value: 'Опции можно комбинировать в наборы для разных окружений. Например: devOpts и prodOpts как срезы []ClientOption.' }
      ]
    },
    {
      id: 4,
      title: 'Значения по умолчанию и валидация',
      type: 'theory',
      content: [
        { type: 'text', value: 'Важная часть паттерна — разумные значения по умолчанию и валидация. Функциональные опции позволяют добавить валидацию прямо внутри option-функции или в конструкторе.' },
        { type: 'heading', value: 'Паттерн с возвращаемой ошибкой' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "errors"\n    "fmt"\n    "time"\n)\n\ntype DatabasePool struct {\n    dsn         string\n    maxOpen     int\n    maxIdle     int\n    maxLifetime time.Duration\n    maxIdleTime time.Duration\n}\n\n// Option возвращает ошибку для валидации\ntype DBOption func(*DatabasePool) error\n\nfunc NewDatabasePool(dsn string, opts ...DBOption) (*DatabasePool, error) {\n    if dsn == "" {\n        return nil, errors.New("dsn не может быть пустым")\n    }\n    pool := &DatabasePool{\n        dsn:         dsn,\n        maxOpen:     25,\n        maxIdle:     5,\n        maxLifetime: 5 * time.Minute,\n        maxIdleTime: 10 * time.Minute,\n    }\n    for _, opt := range opts {\n        if err := opt(pool); err != nil {\n            return nil, fmt.Errorf("ошибка конфигурации: %w", err)\n        }\n    }\n    // Финальная валидация\n    if pool.maxIdle > pool.maxOpen {\n        return nil, errors.New("maxIdle не может превышать maxOpen")\n    }\n    return pool, nil\n}\n\nfunc WithMaxOpenConns(n int) DBOption {\n    return func(p *DatabasePool) error {\n        if n <= 0 {\n            return fmt.Errorf("maxOpenConns должен быть > 0, получено %d", n)\n        }\n        if n > 1000 {\n            return fmt.Errorf("maxOpenConns слишком велик: %d (макс 1000)", n)\n        }\n        p.maxOpen = n\n        return nil\n    }\n}\n\nfunc WithMaxIdleConns(n int) DBOption {\n    return func(p *DatabasePool) error {\n        if n < 0 {\n            return fmt.Errorf("maxIdleConns не может быть отрицательным")\n        }\n        p.maxIdle = n\n        return nil\n    }\n}\n\nfunc WithConnMaxLifetime(d time.Duration) DBOption {\n    return func(p *DatabasePool) error {\n        if d < time.Second {\n            return fmt.Errorf("lifetime слишком мал: %v (мин 1s)", d)\n        }\n        p.maxLifetime = d\n        return nil\n    }\n}\n\nfunc main() {\n    // Валидная конфигурация\n    pool, err := NewDatabasePool("postgres://localhost/mydb",\n        WithMaxOpenConns(50),\n        WithMaxIdleConns(10),\n        WithConnMaxLifetime(30*time.Minute),\n    )\n    if err != nil {\n        fmt.Printf("Ошибка: %v\\n", err)\n        return\n    }\n    fmt.Printf("Pool: maxOpen=%d, maxIdle=%d, lifetime=%v\\n",\n        pool.maxOpen, pool.maxIdle, pool.maxLifetime)\n\n    // Невалидная конфигурация\n    _, err = NewDatabasePool("postgres://localhost/mydb",\n        WithMaxOpenConns(-1),\n    )\n    fmt.Printf("Ожидаемая ошибка: %v\\n", err)\n\n    // maxIdle > maxOpen\n    _, err = NewDatabasePool("postgres://localhost/mydb",\n        WithMaxOpenConns(5),\n        WithMaxIdleConns(10),\n    )\n    fmt.Printf("Ожидаемая ошибка: %v\\n", err)\n}' },
        { type: 'note', value: 'Версия Option с возвращаемой ошибкой (func(*T) error) предпочтительна для production-кода, где важна валидация параметров.' }
      ]
    },
    {
      id: 5,
      title: 'Реальный пример: конфигурация HTTP-клиента',
      type: 'theory',
      content: [
        { type: 'text', value: 'Рассмотрим полноценный production-паттерн: HTTP-клиент с функциональными опциями, включающий пул соединений, TLS, прокси и middleware.' },
        { type: 'heading', value: 'Production HTTP-клиент' },
        { type: 'code', language: 'go', value: 'package main\n\nimport (\n    "fmt"\n    "net/http"\n    "time"\n)\n\ntype HTTPClient struct {\n    baseURL     string\n    httpClient  *http.Client\n    retries     int\n    retryDelay  time.Duration\n    userAgent   string\n    middlewares []func(http.RoundTripper) http.RoundTripper\n}\n\ntype HTTPOption func(*HTTPClient)\n\nfunc NewHTTPClient(baseURL string, opts ...HTTPOption) *HTTPClient {\n    c := &HTTPClient{\n        baseURL: baseURL,\n        httpClient: &http.Client{\n            Timeout: 30 * time.Second,\n            Transport: &http.Transport{\n                MaxIdleConns:        100,\n                IdleConnTimeout:     90 * time.Second,\n                DisableCompression:  false,\n            },\n        },\n        retries:    0,\n        retryDelay: time.Second,\n        userAgent:  "MyApp/1.0",\n    }\n    for _, opt := range opts {\n        opt(c)\n    }\n    return c\n}\n\nfunc WithClientTimeout(d time.Duration) HTTPOption {\n    return func(c *HTTPClient) {\n        c.httpClient.Timeout = d\n    }\n}\n\nfunc WithClientRetry(attempts int, delay time.Duration) HTTPOption {\n    return func(c *HTTPClient) {\n        c.retries = attempts\n        c.retryDelay = delay\n    }\n}\n\nfunc WithUserAgent(ua string) HTTPOption {\n    return func(c *HTTPClient) {\n        c.userAgent = ua\n    }\n}\n\nfunc WithTransport(maxConns int, idleTimeout time.Duration) HTTPOption {\n    return func(c *HTTPClient) {\n        c.httpClient.Transport = &http.Transport{\n            MaxIdleConns:    maxConns,\n            IdleConnTimeout: idleTimeout,\n        }\n    }\n}\n\nfunc WithMiddleware(mw func(http.RoundTripper) http.RoundTripper) HTTPOption {\n    return func(c *HTTPClient) {\n        c.middlewares = append(c.middlewares, mw)\n    }\n}\n\n// Предопределённые наборы опций для разных окружений\nfunc DevelopmentOptions() []HTTPOption {\n    return []HTTPOption{\n        WithClientTimeout(5 * time.Second),\n        WithUserAgent("MyApp/dev"),\n    }\n}\n\nfunc ProductionOptions() []HTTPOption {\n    return []HTTPOption{\n        WithClientTimeout(30 * time.Second),\n        WithClientRetry(3, 500*time.Millisecond),\n        WithTransport(200, 120*time.Second),\n        WithUserAgent("MyApp/1.0"),\n    }\n}\n\nfunc main() {\n    // Dev окружение\n    devClient := NewHTTPClient("http://localhost:8080",\n        DevelopmentOptions()...)\n    fmt.Printf("Dev client: timeout=%v, retries=%d\\n",\n        devClient.httpClient.Timeout, devClient.retries)\n\n    // Prod окружение с дополнительными опциями\n    prodClient := NewHTTPClient("https://api.example.com",\n        append(ProductionOptions(),\n            WithMiddleware(func(next http.RoundTripper) http.RoundTripper {\n                return nil // logging middleware\n            }),\n        )...,\n    )\n    fmt.Printf("Prod client: timeout=%v, retries=%d, agent=%s\\n",\n        prodClient.httpClient.Timeout, prodClient.retries, prodClient.userAgent)\n}' },
        { type: 'tip', value: 'Группируйте опции в предопределённые наборы (DevelopmentOptions, ProductionOptions). Используйте spread-оператор ... для разворачивания слайса в вариативные аргументы.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: конфигурация кэша с Functional Options',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй in-memory кэш с паттерном Functional Options. Кэш должен поддерживать TTL, максимальный размер и callback при вытеснении элементов.',
      requirements: [
        'Структура Cache[K comparable, V any] с полями: maxSize int, defaultTTL time.Duration, onEvict func(K, V)',
        'Тип Option func(*Cache[K, V])',
        'NewCache(opts ...Option) *Cache — конструктор с дефолтами: maxSize=100, defaultTTL=5 минут',
        'WithMaxSize(n int) Option — максимальный размер кэша',
        'WithDefaultTTL(d time.Duration) Option — TTL по умолчанию',
        'WithEvictionCallback(fn func(K, V)) Option — callback при вытеснении',
        'Методы Set(key K, value V) и Get(key K) (V, bool)',
        'В main продемонстрировать создание кэша с разными опциями'
      ],
      expectedOutput: 'Cache создан: maxSize=50, ttl=1m0s\nSet: user:1 = Alice\nGet user:1: Alice (found=true)\nGet user:99: (found=false)\nEviction callback: user:1 -> Alice',
      hint: 'Для хранения используй map[K]cacheEntry где cacheEntry содержит value и expiresAt. В Set проверяй размер и вызывай onEvict если нужно вытеснить. Если onEvict == nil — пропускай вызов.',
      solution: 'package main\n\nimport (\n    "fmt"\n    "time"\n)\n\ntype cacheEntry[V any] struct {\n    value     V\n    expiresAt time.Time\n}\n\ntype Cache[K comparable, V any] struct {\n    maxSize    int\n    defaultTTL time.Duration\n    onEvict    func(K, V)\n    items      map[K]cacheEntry[V]\n}\n\ntype Option[K comparable, V any] func(*Cache[K, V])\n\nfunc NewCache[K comparable, V any](opts ...Option[K, V]) *Cache[K, V] {\n    c := &Cache[K, V]{\n        maxSize:    100,\n        defaultTTL: 5 * time.Minute,\n        items:      make(map[K]cacheEntry[V]),\n    }\n    for _, opt := range opts {\n        opt(c)\n    }\n    return c\n}\n\nfunc WithMaxSize[K comparable, V any](n int) Option[K, V] {\n    return func(c *Cache[K, V]) {\n        c.maxSize = n\n    }\n}\n\nfunc WithDefaultTTL[K comparable, V any](d time.Duration) Option[K, V] {\n    return func(c *Cache[K, V]) {\n        c.defaultTTL = d\n    }\n}\n\nfunc WithEvictionCallback[K comparable, V any](fn func(K, V)) Option[K, V] {\n    return func(c *Cache[K, V]) {\n        c.onEvict = fn\n    }\n}\n\nfunc (c *Cache[K, V]) Set(key K, value V) {\n    if len(c.items) >= c.maxSize {\n        // Вытесняем первый найденный элемент\n        for k, v := range c.items {\n            if c.onEvict != nil {\n                c.onEvict(k, v.value)\n            }\n            delete(c.items, k)\n            break\n        }\n    }\n    c.items[key] = cacheEntry[V]{\n        value:     value,\n        expiresAt: time.Now().Add(c.defaultTTL),\n    }\n}\n\nfunc (c *Cache[K, V]) Get(key K) (V, bool) {\n    entry, ok := c.items[key]\n    if !ok {\n        var zero V\n        return zero, false\n    }\n    if time.Now().After(entry.expiresAt) {\n        delete(c.items, key)\n        var zero V\n        return zero, false\n    }\n    return entry.value, true\n}\n\nfunc main() {\n    cache := NewCache[string, string](\n        WithMaxSize[string, string](50),\n        WithDefaultTTL[string, string](time.Minute),\n        WithEvictionCallback[string, string](func(k, v string) {\n            fmt.Printf("Eviction callback: %s -> %s\\n", k, v)\n        }),\n    )\n    fmt.Printf("Cache создан: maxSize=%d, ttl=%v\\n", cache.maxSize, cache.defaultTTL)\n\n    cache.Set("user:1", "Alice")\n    fmt.Println("Set: user:1 = Alice")\n\n    val, found := cache.Get("user:1")\n    fmt.Printf("Get user:1: %s (found=%v)\\n", val, found)\n\n    _, found = cache.Get("user:99")\n    fmt.Printf("Get user:99: (found=%v)\\n", found)\n\n    // Заполняем кэш до лимита для демонстрации вытеснения\n    for i := 0; i < 50; i++ {\n        cache.Set(fmt.Sprintf("key:%d", i), fmt.Sprintf("val:%d", i))\n    }\n}',
      explanation: 'Паттерн Functional Options особенно хорошо работает с дженерик-типами в Go 1.18+. Option[K, V] — тип функции, модифицирующей Cache[K, V]. Конструктор NewCache устанавливает разумные дефолты, а каждая опция переопределяет только нужный параметр. Важно: вызов onEvict стоит проверять на nil, чтобы callback был действительно опциональным. Валидация (например, maxSize > 0) должна происходить либо в опции, либо в конструкторе после применения всех опций.'
    }
  ]
}
