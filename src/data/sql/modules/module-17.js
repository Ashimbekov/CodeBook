export default {
  id: 17,
  title: 'Хранимые процедуры и функции',
  description: 'Создание функций и процедур на PL/pgSQL. Параметры IN/OUT, переменные, условия IF/CASE, циклы FOR/WHILE/LOOP, обработка ошибок. Разница между функциями и процедурами в PostgreSQL.',
  lessons: [
    {
      id: 1,
      title: 'Функции в PostgreSQL: CREATE FUNCTION',
      type: 'theory',
      content: [
        { type: 'text', value: 'Функция в PostgreSQL — именованный блок кода, возвращающий значение. Языки: SQL (простые выражения), PL/pgSQL (полноценный процедурный язык с переменными, условиями и циклами), Python, JavaScript и другие.' },
        { type: 'code', language: 'sql', value: '-- Простая SQL-функция\nCREATE OR REPLACE FUNCTION get_full_name(first_name TEXT, last_name TEXT)\nRETURNS TEXT\nLANGUAGE SQL\nIMMUTABLE  -- Не зависит от БД, результат детерминирован\nAS $$\n    SELECT first_name || \' \' || last_name;\n$$;\n\n-- Использование:\nSELECT get_full_name(\'Алия\', \'Бекова\');  -- "Алия Бекова"\nSELECT get_full_name(first_name, last_name) FROM employees;\n\n-- PL/pgSQL функция с логикой\nCREATE OR REPLACE FUNCTION calculate_discount(\n    p_price   DECIMAL,\n    p_loyalty_years INTEGER\n)\nRETURNS DECIMAL\nLANGUAGE plpgsql\nSTABLE  -- Читает БД, но одинаковый результат для одних параметров в транзакции\nAS $$\nDECLARE\n    v_discount_rate DECIMAL;\nBEGIN\n    -- Скидка по стажу\n    v_discount_rate := CASE\n        WHEN p_loyalty_years >= 10 THEN 0.20\n        WHEN p_loyalty_years >= 5  THEN 0.15\n        WHEN p_loyalty_years >= 2  THEN 0.10\n        WHEN p_loyalty_years >= 1  THEN 0.05\n        ELSE 0.00\n    END;\n\n    RETURN p_price * (1 - v_discount_rate);\nEND;\n$$;\n\nSELECT calculate_discount(10000, 6);  -- 8500 (скидка 15%)\n\n-- Функция возвращающая таблицу (SETOF / TABLE)\nCREATE OR REPLACE FUNCTION get_top_customers(p_limit INTEGER DEFAULT 10)\nRETURNS TABLE (customer_id INT, name TEXT, total_spent DECIMAL)\nLANGUAGE SQL\nSTABLE\nAS $$\n    SELECT c.id, c.name, SUM(o.total)\n    FROM customers c\n    JOIN orders o ON c.id = o.customer_id\n    GROUP BY c.id, c.name\n    ORDER BY SUM(o.total) DESC\n    LIMIT p_limit;\n$$;\n\nSELECT * FROM get_top_customers(5);' },
        { type: 'tip', value: 'Volatility markers важны для оптимизации: IMMUTABLE — результат зависит только от аргументов (можно кэшировать, использовать в индексах). STABLE — не меняет БД, одинаковый результат в рамках транзакции. VOLATILE (по умолчанию) — может делать что угодно.' }
      ]
    },
    {
      id: 2,
      title: 'PL/pgSQL: переменные, IF/CASE, циклы',
      type: 'theory',
      content: [
        { type: 'text', value: 'PL/pgSQL — процедурное расширение SQL в PostgreSQL. Позволяет писать функции с переменными, условными операторами, циклами и обработкой ошибок.' },
        { type: 'code', language: 'sql', value: 'CREATE OR REPLACE FUNCTION process_order(p_order_id INTEGER)\nRETURNS TEXT\nLANGUAGE plpgsql\nAS $$\nDECLARE\n    -- Объявление переменных\n    v_order        orders%ROWTYPE;    -- Строка таблицы orders\n    v_customer_name TEXT;\n    v_items_count  INTEGER := 0;      -- Инициализация\n    v_total        DECIMAL := 0;\n    v_status       TEXT;\n    v_item         RECORD;            -- Неопределённая запись\nBEGIN\n    -- Получить заказ\n    SELECT * INTO STRICT v_order FROM orders WHERE id = p_order_id;\n    -- STRICT: вызовет ошибку если 0 или 2+ строк\n\n    -- Получить имя клиента\n    SELECT name INTO v_customer_name FROM customers WHERE id = v_order.customer_id;\n\n    -- IF/ELSIF/ELSE\n    IF v_order.status = \'cancelled\' THEN\n        RETURN \'Заказ уже отменён\';\n    ELSIF v_order.status = \'completed\' THEN\n        RETURN \'Заказ уже завершён\';\n    END IF;\n\n    -- Цикл FOR по запросу\n    FOR v_item IN\n        SELECT product_id, quantity, unit_price\n        FROM order_items WHERE order_id = p_order_id\n    LOOP\n        v_items_count := v_items_count + 1;\n        v_total := v_total + (v_item.quantity * v_item.unit_price);\n    END LOOP;\n\n    -- CASE выражение\n    v_status := CASE\n        WHEN v_total > 100000 THEN \'VIP заказ\'\n        WHEN v_total > 50000  THEN \'Крупный заказ\'\n        WHEN v_total > 10000  THEN \'Средний заказ\'\n        ELSE \'Малый заказ\'\n    END;\n\n    -- WHILE цикл\n    -- WHILE условие LOOP ... END LOOP;\n\n    -- LOOP с EXIT (бесконечный с выходом)\n    -- LOOP\n    --   EXIT WHEN условие;\n    --   CONTINUE WHEN условие; -- следующая итерация\n    -- END LOOP;\n\n    -- Форматированный вывод:\n    RETURN FORMAT(\n        \'Клиент: %s | Товаров: %s | Сумма: %s ₸ | %s\',\n        v_customer_name, v_items_count, v_total, v_status\n    );\n\nEXCEPTION\n    WHEN NO_DATA_FOUND THEN\n        RETURN \'Заказ #\' || p_order_id || \' не найден\';\n    WHEN OTHERS THEN\n        RETURN \'Ошибка: \' || SQLERRM;\nEND;\n$$;\n\nSELECT process_order(42);' },
        { type: 'note', value: 'SELECT INTO для одной строки. SELECT INTO с STRICT генерирует исключение если строк 0 или >1 — это хорошая практика, чтобы не молчать об ошибках. Для нескольких строк используй FOR ... IN ... LOOP или RETURN QUERY.' }
      ]
    },
    {
      id: 3,
      title: 'Процедуры: CREATE PROCEDURE',
      type: 'theory',
      content: [
        { type: 'text', value: 'В PostgreSQL 11+ появились процедуры (PROCEDURE). Отличие от функций: процедуры не возвращают значение, вызываются через CALL, могут содержать COMMIT/ROLLBACK внутри.' },
        { type: 'code', language: 'sql', value: '-- Процедура: не возвращает значение, вызывается через CALL\nCREATE OR REPLACE PROCEDURE archive_old_orders(\n    p_older_than_days INTEGER DEFAULT 365\n)\nLANGUAGE plpgsql\nAS $$\nDECLARE\n    v_count INTEGER;\nBEGIN\n    -- Перенос старых заказов в архив\n    WITH archived AS (\n        DELETE FROM orders\n        WHERE created_at < NOW() - (p_older_than_days || \' days\')::INTERVAL\n          AND status = \'completed\'\n        RETURNING *\n    )\n    INSERT INTO orders_archive SELECT * FROM archived;\n\n    GET DIAGNOSTICS v_count = ROW_COUNT;\n    RAISE NOTICE \'Архивировано % заказов\', v_count;\n\n    -- Процедура МОЖЕТ делать промежуточные COMMIT!\n    COMMIT;\n\n    -- Очистить старые логи\n    DELETE FROM audit_log\n    WHERE created_at < NOW() - INTERVAL \'2 years\';\n\n    COMMIT;  -- Второй COMMIT в процедуре!\n    RAISE NOTICE \'Очистка завершена\';\nEND;\n$$;\n\n-- Вызов процедуры:\nCALL archive_old_orders(180);  -- Архивировать заказы старше 180 дней\n\n-- Функция VS Процедура:\n-- | Параметр            | FUNCTION   | PROCEDURE  |\n-- | Возврат значения    | ДА         | НЕТ        |\n-- | Вызов               | SELECT     | CALL       |\n-- | COMMIT внутри       | НЕТ        | ДА         |\n-- | Использование в SQL | ДА         | НЕТ        |\n-- | OUT параметры       | Ограничено | ДА         |\n\n-- Процедура с OUT параметрами\nCREATE OR REPLACE PROCEDURE transfer_money(\n    p_from_id  INTEGER,\n    p_to_id    INTEGER,\n    p_amount   DECIMAL,\n    OUT p_success BOOLEAN,\n    OUT p_message TEXT\n)\nLANGUAGE plpgsql\nAS $$\nDECLARE\n    v_balance DECIMAL;\nBEGIN\n    SELECT balance INTO v_balance FROM accounts WHERE id = p_from_id FOR UPDATE;\n\n    IF v_balance < p_amount THEN\n        p_success := FALSE;\n        p_message := \'Недостаточно средств\';\n        RETURN;\n    END IF;\n\n    UPDATE accounts SET balance = balance - p_amount WHERE id = p_from_id;\n    UPDATE accounts SET balance = balance + p_amount WHERE id = p_to_id;\n\n    p_success := TRUE;\n    p_message := \'Перевод выполнен\';\nEND;\n$$;\n\nCALL transfer_money(1, 2, 5000, NULL, NULL);' },
        { type: 'tip', value: 'Используй функцию когда нужно вернуть результат и использовать в SELECT. Используй процедуру для административных задач (очистка, архивирование, массовые обновления) где нужны промежуточные COMMIT или OUT параметры.' }
      ]
    },
    {
      id: 4,
      title: 'Обработка ошибок и RAISE',
      type: 'theory',
      content: [
        { type: 'text', value: 'PL/pgSQL предоставляет мощную систему обработки исключений через блок EXCEPTION. RAISE позволяет генерировать сообщения разных уровней и пользовательские ошибки.' },
        { type: 'code', language: 'sql', value: '-- RAISE: уровни сообщений\nDO $$\nBEGIN\n    RAISE DEBUG   \'Отладка: значение = %\', 42;    -- Только при log_min_messages = debug\n    RAISE INFO    \'Информация: начало обработки\'; -- Выводится клиенту\n    RAISE NOTICE  \'Уведомление: обработано % строк\', 100; -- Наиболее частый\n    RAISE WARNING \'Предупреждение: данные устарели\';\n    RAISE EXCEPTION \'Критическая ошибка: %\', \'деление на ноль\';\n    -- EXCEPTION прерывает выполнение!\nEND;\n$$;\n\n-- Пользовательские коды ошибок (SQLSTATE)\nRAISE EXCEPTION \'Товар % не найден\', p_product_id\n    USING ERRCODE = \'P0001\',         -- Пользовательский код\n          DETAIL  = \'Проверьте ID\',  -- Подробности\n          HINT    = \'Используйте GET /products для списка\';\n\n-- Обработка конкретных исключений:\nCREATE OR REPLACE FUNCTION safe_divide(a DECIMAL, b DECIMAL)\nRETURNS DECIMAL\nLANGUAGE plpgsql\nAS $$\nBEGIN\n    RETURN a / b;\nEXCEPTION\n    WHEN division_by_zero THEN\n        RAISE NOTICE \'Деление на ноль, возвращаем NULL\';\n        RETURN NULL;\n    WHEN numeric_value_out_of_range THEN\n        RETURN 0;\nEND;\n$$;\n\n-- Полная обработка с получением деталей ошибки:\nCREATE OR REPLACE FUNCTION risky_operation(p_id INTEGER)\nRETURNS VOID\nLANGUAGE plpgsql\nAS $$\nDECLARE\n    v_state   TEXT;\n    v_msg     TEXT;\n    v_detail  TEXT;\n    v_hint    TEXT;\n    v_context TEXT;\nBEGIN\n    -- Рискованная операция\n    UPDATE critical_table SET value = value * 2 WHERE id = p_id;\n\nEXCEPTION\n    WHEN OTHERS THEN\n        GET STACKED DIAGNOSTICS\n            v_state   = RETURNED_SQLSTATE,\n            v_msg     = MESSAGE_TEXT,\n            v_detail  = PG_EXCEPTION_DETAIL,\n            v_hint    = PG_EXCEPTION_HINT,\n            v_context = PG_EXCEPTION_CONTEXT;\n\n        -- Записать в лог ошибок\n        INSERT INTO error_log (sqlstate, message, detail, context, created_at)\n        VALUES (v_state, v_msg, v_detail, v_context, NOW());\n\n        RAISE;  -- Перебросить исключение дальше\nEND;\n$$;\n\n-- Распространённые коды исключений:\n-- unique_violation (23505)   -> нарушение UNIQUE\n-- foreign_key_violation (23503) -> нарушение FK\n-- not_null_violation (23502)  -> NULL в NOT NULL\n-- check_violation (23514)    -> нарушение CHECK\n-- NO_DATA_FOUND             -> SELECT INTO нашёл 0 строк\n-- TOO_MANY_ROWS             -> SELECT INTO нашёл 2+ строк' },
        { type: 'warning', value: 'EXCEPTION блок неявно создаёт savepoint. Если ошибка поймана в EXCEPTION, транзакция продолжается (не откатывается целиком). Если нужно откатить транзакцию при ошибке — используй RAISE для переброса исключения или оберни в отдельную транзакцию.' }
      ]
    },
    {
      id: 5,
      title: 'Функции возвращающие множества: SETOF и TABLE',
      type: 'theory',
      content: [
        { type: 'text', value: 'Функции могут возвращать не одно значение, а набор строк. Три способа: RETURNS SETOF тип, RETURNS TABLE(...), RETURNS SETOF RECORD с OUT параметрами.' },
        { type: 'code', language: 'sql', value: '-- RETURNS SETOF скалярный тип\nCREATE OR REPLACE FUNCTION get_customer_emails(p_min_orders INTEGER)\nRETURNS SETOF TEXT\nLANGUAGE SQL\nSTABLE\nAS $$\n    SELECT DISTINCT c.email\n    FROM customers c\n    JOIN orders o ON c.id = o.customer_id\n    GROUP BY c.id, c.email\n    HAVING COUNT(o.id) >= p_min_orders;\n$$;\n\nSELECT * FROM get_customer_emails(5);\n\n-- RETURNS TABLE: явное определение структуры\nCREATE OR REPLACE FUNCTION get_order_details(p_order_id INTEGER)\nRETURNS TABLE (\n    product_name TEXT,\n    quantity     INTEGER,\n    unit_price   DECIMAL,\n    subtotal     DECIMAL\n)\nLANGUAGE SQL\nSTABLE\nAS $$\n    SELECT\n        p.name,\n        oi.quantity,\n        oi.unit_price,\n        oi.quantity * oi.unit_price\n    FROM order_items oi\n    JOIN products p ON oi.product_id = p.id\n    WHERE oi.order_id = p_order_id;\n$$;\n\nSELECT * FROM get_order_details(42);\n-- Использование как таблица:\nSELECT product_name, subtotal\nFROM get_order_details(42)\nWHERE subtotal > 5000;\n\n-- RETURN QUERY и RETURN NEXT в PL/pgSQL\nCREATE OR REPLACE FUNCTION generate_report(p_year INTEGER)\nRETURNS TABLE (month INTEGER, revenue DECIMAL, orders_count BIGINT)\nLANGUAGE plpgsql\nSTABLE\nAS $$\nBEGIN\n    -- RETURN QUERY: вернуть результат запроса\n    RETURN QUERY\n        SELECT\n            EXTRACT(MONTH FROM created_at)::INTEGER,\n            SUM(total),\n            COUNT(*)\n        FROM orders\n        WHERE EXTRACT(YEAR FROM created_at) = p_year\n        GROUP BY EXTRACT(MONTH FROM created_at)\n        ORDER BY 1;\n\n    -- Можно несколько RETURN QUERY:\n    -- RETURN QUERY SELECT ...; добавит строки к результату\n\n    -- RETURN NEXT: добавить одну строку\n    -- month := 13; revenue := 0; orders_count := 0;\n    -- RETURN NEXT;\nEND;\n$$;\n\nSELECT * FROM generate_report(2024);' },
        { type: 'note', value: 'Функция с RETURNS TABLE используется в FROM как таблица. Это мощный инструмент: параметризованные представления (view не принимают параметры, а функция — да). Пример: get_sales_by_region(\'KZ\', \'2024-01-01\', \'2024-12-31\').' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Создание пакета бизнес-функций',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создай набор функций и процедур для системы управления складом.',
      requirements: [
        'Функция check_stock(product_id, quantity) -> BOOLEAN: проверяет наличие товара на складе',
        'Функция reserve_stock(product_id, quantity, order_id) -> TEXT: резервирует товар или возвращает причину отказа',
        'Процедура process_shipment(order_id): подтверждает отгрузку, обновляет статус заказа и остатки',
        'Функция get_low_stock_report() RETURNS TABLE: товары с остатком меньше минимального',
        'Функция calculate_order_total(order_id) -> DECIMAL: считает сумму с учётом скидок и налогов',
        'Обработка ошибок: все функции должны корректно обрабатывать несуществующие ID'
      ],
      hint: 'Для резервирования: SELECT ... FOR UPDATE чтобы заблокировать строку. RAISE EXCEPTION с USING ERRCODE для бизнес-ошибок. GET DIAGNOSTICS ROW_COUNT для проверки числа обновлённых строк.',
      expectedOutput: 'SELECT get_product_info(3):\n (3,"Ноутбук Dell",85000.00,"Ноутбуки",12)\n\nSELECT reserve_stock(3, 5) -- успешное резервирование:\n reserve_stock\n--------------\n t\n(1 row)\nstock уменьшен: 12 -> 7\n\nSELECT reserve_stock(3, 10) -- недостаточно:\nERROR: Недостаточно товара: запрошено 10, доступно 7\n\nCALL place_order(1, 3, 2, 85000.00):\nDO\n(order создан, stock уменьшен на 2: 7 -> 5)\n\nSELECT update_category_stats():\n update_category_stats\n----------------------\n               (2 rows updated)\n\nSELECT get_top_products(\'Ноутбуки\', 3):\n name          | revenue   | units_sold\n---------------+-----------+------------\n Ноутбук Dell  | 170000.00 |          2\n Ноутбук HP    | 90000.00  |          2\n(2 rows)',
      solution: '-- Функция проверки остатка\nCREATE OR REPLACE FUNCTION check_stock(\n    p_product_id INTEGER,\n    p_quantity   INTEGER\n)\nRETURNS BOOLEAN\nLANGUAGE plpgsql\nSTABLE\nAS $$\nDECLARE\n    v_available INTEGER;\nBEGIN\n    SELECT COALESCE(stock_quantity, 0) - COALESCE(reserved_quantity, 0)\n    INTO v_available\n    FROM products\n    WHERE id = p_product_id;\n\n    IF NOT FOUND THEN\n        RAISE EXCEPTION \'Товар % не найден\', p_product_id\n            USING ERRCODE = \'P0001\';\n    END IF;\n\n    RETURN v_available >= p_quantity;\nEND;\n$$;\n\n-- Функция резервирования\nCREATE OR REPLACE FUNCTION reserve_stock(\n    p_product_id INTEGER,\n    p_quantity   INTEGER,\n    p_order_id   INTEGER\n)\nRETURNS TEXT\nLANGUAGE plpgsql\nAS $$\nDECLARE\n    v_available  INTEGER;\n    v_product    TEXT;\nBEGIN\n    SELECT name,\n           COALESCE(stock_quantity, 0) - COALESCE(reserved_quantity, 0)\n    INTO STRICT v_product, v_available\n    FROM products\n    WHERE id = p_product_id\n    FOR UPDATE;  -- Блокируем строку\n\n    IF v_available < p_quantity THEN\n        RETURN FORMAT(\n            \'ОТКАЗ: товар "%s" - доступно %s, запрошено %s\',\n            v_product, v_available, p_quantity\n        );\n    END IF;\n\n    UPDATE products\n    SET reserved_quantity = COALESCE(reserved_quantity, 0) + p_quantity\n    WHERE id = p_product_id;\n\n    INSERT INTO stock_reservations (product_id, order_id, quantity, reserved_at)\n    VALUES (p_product_id, p_order_id, p_quantity, NOW())\n    ON CONFLICT (product_id, order_id)\n    DO UPDATE SET quantity = EXCLUDED.quantity;\n\n    RETURN FORMAT(\'УСПЕХ: зарезервировано %s ед. товара "%s"\', p_quantity, v_product);\n\nEXCEPTION\n    WHEN NO_DATA_FOUND THEN\n        RETURN \'ОШИБКА: товар #\' || p_product_id || \' не найден\';\n    WHEN OTHERS THEN\n        RETURN \'ОШИБКА: \' || SQLERRM;\nEND;\n$$;\n\n-- Процедура обработки отгрузки\nCREATE OR REPLACE PROCEDURE process_shipment(p_order_id INTEGER)\nLANGUAGE plpgsql\nAS $$\nDECLARE\n    v_item  RECORD;\n    v_count INTEGER := 0;\nBEGIN\n    -- Проверяем статус заказа\n    IF NOT EXISTS (\n        SELECT 1 FROM orders WHERE id = p_order_id AND status = \'paid\'\n    ) THEN\n        RAISE EXCEPTION \'Заказ % не найден или не оплачен\', p_order_id;\n    END IF;\n\n    -- Списываем товары со склада\n    FOR v_item IN\n        SELECT product_id, quantity\n        FROM order_items\n        WHERE order_id = p_order_id\n    LOOP\n        UPDATE products\n        SET stock_quantity    = stock_quantity    - v_item.quantity,\n            reserved_quantity = COALESCE(reserved_quantity, 0) - v_item.quantity\n        WHERE id = v_item.product_id;\n\n        v_count := v_count + 1;\n    END LOOP;\n\n    -- Обновляем статус заказа\n    UPDATE orders\n    SET status = \'shipped\', shipped_at = NOW()\n    WHERE id = p_order_id;\n\n    RAISE NOTICE \'Заказ % отгружен, обработано % позиций\', p_order_id, v_count;\nEND;\n$$;\n\n-- Функция отчёта по низким остаткам\nCREATE OR REPLACE FUNCTION get_low_stock_report()\nRETURNS TABLE (\n    product_id   INTEGER,\n    product_name TEXT,\n    current_stock INTEGER,\n    min_stock     INTEGER,\n    deficit       INTEGER\n)\nLANGUAGE SQL\nSTABLE\nAS $$\n    SELECT\n        id,\n        name,\n        stock_quantity,\n        min_stock_level,\n        min_stock_level - stock_quantity AS deficit\n    FROM products\n    WHERE stock_quantity < COALESCE(min_stock_level, 5)\n    ORDER BY deficit DESC;\n$$;\n\nSELECT * FROM get_low_stock_report();\n\n-- Функция расчёта суммы заказа\nCREATE OR REPLACE FUNCTION calculate_order_total(p_order_id INTEGER)\nRETURNS DECIMAL\nLANGUAGE plpgsql\nSTABLE\nAS $$\nDECLARE\n    v_subtotal   DECIMAL;\n    v_tax_rate   DECIMAL := 0.12;  -- НДС 12%\n    v_discount   DECIMAL := 0;\n    v_customer_tier TEXT;\nBEGIN\n    -- Подитог\n    SELECT SUM(quantity * unit_price)\n    INTO STRICT v_subtotal\n    FROM order_items\n    WHERE order_id = p_order_id;\n\n    -- Скидка VIP клиента\n    SELECT COALESCE(c.tier, \'regular\')\n    INTO v_customer_tier\n    FROM orders o\n    JOIN customers c ON o.customer_id = c.id\n    WHERE o.id = p_order_id;\n\n    v_discount := CASE v_customer_tier\n        WHEN \'vip\'      THEN 0.15\n        WHEN \'premium\'  THEN 0.10\n        WHEN \'silver\'   THEN 0.05\n        ELSE 0\n    END;\n\n    RETURN ROUND(v_subtotal * (1 - v_discount) * (1 + v_tax_rate), 2);\n\nEXCEPTION\n    WHEN NO_DATA_FOUND THEN\n        RAISE EXCEPTION \'Заказ % не найден или пуст\', p_order_id;\nEND;\n$$;\n\nSELECT calculate_order_total(1);',
      explanation: 'Хранимые функции инкапсулируют бизнес-логику в БД: check_stock (безопасное чтение), reserve_stock (атомарное изменение с FOR UPDATE), process_shipment (процедура с промежуточным состоянием). Каждая функция обрабатывает ошибки явно — нет "тихих" сбоев.'
    }
  ]
}
