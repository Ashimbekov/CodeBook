export default {
  id: 24,
  title: 'Практикум: JOIN запросы',
  description: 'Интенсивный практикум по JOIN всех типов. INNER, LEFT, RIGHT, FULL OUTER, CROSS, SELF JOIN. Многоуровневые JOIN, оптимизация соединений, антипаттерны. Только практика на реальных сценариях.',
  lessons: [
    {
      id: 1,
      title: 'INNER JOIN: базовые соединения',
      type: 'practice',
      difficulty: 'easy',
      description: 'Практика INNER JOIN на схеме интернет-магазина.',
      requirements: [
        'Список заказов с именем клиента и его email',
        'Список товаров с названием их категории',
        'Детализация заказа: название товара, количество, цена за единицу, сумма строки',
        'Заказы с полными данными: клиент, адрес доставки, список товаров',
        'Сотрудники с названием их отдела и именем менеджера'
      ],
      hint: 'INNER JOIN возвращает только строки с совпадением в обеих таблицах. Псевдонимы (o, c, p) делают запрос короче. Для менеджера нужен SELF JOIN: JOIN employees manager ON e.manager_id = manager.id.',
      solution: '-- 1. Заказы с клиентом\nSELECT\n    o.id AS order_id,\n    o.total,\n    o.status,\n    o.created_at,\n    c.name AS customer_name,\n    c.email\nFROM orders o\nJOIN customers c ON o.customer_id = c.id\nORDER BY o.created_at DESC;\n\n-- 2. Товары с категорией\nSELECT\n    p.id,\n    p.name AS product_name,\n    p.price,\n    cat.name AS category_name,\n    cat.description AS category_desc\nFROM products p\nJOIN categories cat ON p.category_id = cat.id\nORDER BY cat.name, p.name;\n\n-- 3. Детализация заказа\nSELECT\n    oi.order_id,\n    p.name AS product_name,\n    oi.quantity,\n    oi.unit_price,\n    oi.quantity * oi.unit_price AS line_total\nFROM order_items oi\nJOIN products p ON oi.product_id = p.id\nWHERE oi.order_id = 42\nORDER BY p.name;\n\n-- 4. Полные данные заказа (4 JOIN)\nSELECT\n    o.id AS order_id,\n    c.name AS customer,\n    c.email,\n    COALESCE(a.street || \', \' || a.city, \'Самовывоз\') AS delivery_address,\n    p.name AS product,\n    oi.quantity,\n    oi.unit_price,\n    o.status,\n    o.created_at\nFROM orders o\nJOIN customers c   ON o.customer_id = c.id\nJOIN order_items oi ON o.id = oi.order_id\nJOIN products p    ON oi.product_id = p.id\nLEFT JOIN addresses a ON o.address_id = a.id  -- Может не быть адреса\nORDER BY o.id, p.name;\n\n-- 5. SELF JOIN: сотрудник + менеджер\nSELECT\n    e.name AS employee,\n    e.position,\n    d.name AS department,\n    m.name AS manager_name\nFROM employees e\nJOIN departments d ON e.department_id = d.id\nJOIN employees m ON e.manager_id = m.id  -- SELF JOIN\nORDER BY d.name, e.name;',
      explanation: 'INNER JOIN = только строки с совпадением в обеих таблицах. Для менеджеров: таблица employees присоединяется сама к себе (SELF JOIN) с псевдонимами e (сотрудник) и m (менеджер). Порядок JOIN не влияет на результат, но влияет на производительность.'
    },
    {
      id: 2,
      title: 'LEFT JOIN: включая строки без совпадений',
      type: 'practice',
      difficulty: 'easy',
      description: 'Практика LEFT JOIN для нахождения несвязанных записей.',
      requirements: [
        'Все клиенты с количеством заказов (включая клиентов без заказов)',
        'Все товары с количеством продаж (включая товары которые никто не купил)',
        'Клиенты без заказов (LEFT JOIN + WHERE IS NULL)',
        'Категории без товаров',
        'Товары без отзывов с их ценой и датой добавления'
      ],
      hint: 'LEFT JOIN включает все строки из левой таблицы даже если нет совпадения справа. Для "без X": LEFT JOIN ... WHERE right_id IS NULL. COUNT(o.id) не посчитает NULL — для клиентов без заказов вернёт 0.',
      solution: '-- 1. Клиенты с количеством заказов (включая 0)\nSELECT\n    c.id,\n    c.name,\n    c.email,\n    COUNT(o.id)         AS orders_count,\n    COALESCE(SUM(o.total), 0) AS total_spent\nFROM customers c\nLEFT JOIN orders o ON c.id = o.customer_id\nGROUP BY c.id, c.name, c.email\nORDER BY orders_count DESC;\n\n-- 2. Товары с количеством продаж (включая непроданные)\nSELECT\n    p.id,\n    p.name,\n    p.price,\n    COALESCE(SUM(oi.quantity), 0) AS total_sold,\n    COALESCE(SUM(oi.quantity * oi.unit_price), 0) AS revenue\nFROM products p\nLEFT JOIN order_items oi ON p.id = oi.product_id\nGROUP BY p.id, p.name, p.price\nORDER BY total_sold DESC;\n\n-- 3. Клиенты БЕЗ заказов\nSELECT\n    c.id, c.name, c.email, c.created_at\nFROM customers c\nLEFT JOIN orders o ON c.id = o.customer_id\nWHERE o.id IS NULL  -- Нет совпадения = нет заказов\nORDER BY c.created_at DESC;\n\n-- 4. Категории без товаров\nSELECT\n    cat.id, cat.name\nFROM categories cat\nLEFT JOIN products p ON cat.id = p.category_id\nWHERE p.id IS NULL\nORDER BY cat.name;\n\n-- 5. Товары без отзывов\nSELECT\n    p.id,\n    p.name,\n    p.price,\n    p.created_at AS added_date\nFROM products p\nLEFT JOIN reviews r ON p.id = r.product_id\nWHERE r.id IS NULL\nORDER BY p.created_at DESC;',
      explanation: 'LEFT JOIN + WHERE right_side IS NULL — стандартный паттерн для "записи без связанных данных". Используй COUNT(o.id) а не COUNT(*): COUNT(*) считает строки включая NULL, COUNT(o.id) не считает NULL — возвращает 0 для клиентов без заказов.'
    },
    {
      id: 3,
      title: 'Многотабличные JOIN и агрегации',
      type: 'practice',
      difficulty: 'medium',
      description: 'Соединение трёх и более таблиц с агрегацией.',
      requirements: [
        'Полный отчёт по заказам: клиент, город, количество товаров, сумма, категории товаров',
        'Топ-10 клиентов по выручке с их городом и количеством уникальных категорий купленных товаров',
        'Отчёт по менеджерам продаж: имя, отдел, количество клиентов, общая выручка',
        'Для каждой категории: выручка, количество уникальных клиентов, средняя цена заданного товара'
      ],
      hint: 'JOIN можно цеплять столько таблиц сколько нужно. COUNT(DISTINCT c.city) считает уникальные города. STRING_AGG(DISTINCT cat.name, \', \') агрегирует в строку.',
      solution: '-- 1. Полный отчёт заказов\nSELECT\n    o.id AS order_id,\n    c.name AS customer,\n    c.city,\n    COUNT(oi.id) AS items_count,\n    SUM(oi.quantity * oi.unit_price) AS order_total,\n    o.status,\n    STRING_AGG(DISTINCT cat.name, \', \' ORDER BY cat.name) AS categories\nFROM orders o\nJOIN customers c     ON o.customer_id = c.id\nJOIN order_items oi  ON o.id = oi.order_id\nJOIN products p      ON oi.product_id = p.id\nJOIN categories cat  ON p.category_id = cat.id\nGROUP BY o.id, c.name, c.city, o.status\nORDER BY o.id;\n\n-- 2. Топ-10 клиентов\nSELECT\n    c.name,\n    c.city,\n    SUM(oi.quantity * oi.unit_price) AS total_revenue,\n    COUNT(DISTINCT o.id)             AS orders_count,\n    COUNT(DISTINCT cat.id)           AS unique_categories\nFROM customers c\nJOIN orders o       ON c.id = o.customer_id\nJOIN order_items oi ON o.id = oi.order_id\nJOIN products p     ON oi.product_id = p.id\nJOIN categories cat ON p.category_id = cat.id\nGROUP BY c.id, c.name, c.city\nORDER BY total_revenue DESC\nLIMIT 10;\n\n-- 3. Отчёт по менеджерам продаж\nSELECT\n    m.name AS manager,\n    d.name AS department,\n    COUNT(DISTINCT c.id) AS total_customers,\n    SUM(o.total)         AS total_revenue,\n    ROUND(AVG(o.total), 2) AS avg_order\nFROM employees m\nJOIN departments d     ON m.department_id = d.id\nJOIN customers c       ON c.manager_id = m.id  -- Если клиенты прикреплены к менеджеру\nJOIN orders o          ON c.id = o.customer_id\nWHERE m.position ILIKE \'%manager%\'\nGROUP BY m.id, m.name, d.name\nORDER BY total_revenue DESC;\n\n-- 4. Отчёт по категориям\nSELECT\n    cat.name AS category,\n    COUNT(DISTINCT o.customer_id) AS unique_customers,\n    COUNT(DISTINCT o.id)          AS orders_count,\n    SUM(oi.quantity * oi.unit_price) AS total_revenue,\n    ROUND(AVG(p.price), 2) AS avg_product_price\nFROM categories cat\nJOIN products p     ON p.category_id = cat.id\nJOIN order_items oi ON p.id = oi.product_id\nJOIN orders o       ON oi.order_id = o.id\nGROUP BY cat.id, cat.name\nORDER BY total_revenue DESC;',
      explanation: 'При многотабличном JOIN каждый следующий JOIN добавляет столбцы. Порядок JOIN подсказывает PostgreSQL путь — помести самую ограничивающую таблицу первой. STRING_AGG с DISTINCT и ORDER BY агрегирует уникальные значения в отсортированную строку.'
    },
    {
      id: 4,
      title: 'FULL OUTER JOIN и CROSS JOIN',
      type: 'practice',
      difficulty: 'medium',
      description: 'Использование полного внешнего соединения и декартова произведения.',
      requirements: [
        'FULL OUTER JOIN: все клиенты и все заказы, включая клиентов без заказов И заказы без клиентов (осиротевшие)',
        'Найди несоответствия: заказы где customer_id не существует в таблице customers',
        'CROSS JOIN: создай прайс-лист — все комбинации товаров и размеров/цветов',
        'Матрица совместных покупок: какие пары товаров чаще всего покупают вместе',
        'FULL OUTER JOIN двух источников данных для сравнения (reconciliation)'
      ],
      hint: 'FULL OUTER JOIN включает строки из обеих сторон без совпадения. Осиротевшие заказы: WHERE c.id IS NULL. CROSS JOIN на двух таблицах без ON = все комбинации. Пары товаров: SELF JOIN order_items на один order_id.',
      solution: '-- 1. FULL OUTER JOIN клиенты + заказы\nSELECT\n    COALESCE(c.id::TEXT, \'ОТС\') AS customer_id,\n    c.name AS customer_name,\n    o.id AS order_id,\n    o.total,\n    CASE\n        WHEN c.id IS NULL THEN \'Осиротевший заказ\'\n        WHEN o.id IS NULL THEN \'Клиент без заказов\'\n        ELSE \'Нормально\'\n    END AS status\nFROM customers c\nFULL OUTER JOIN orders o ON c.id = o.customer_id\nWHERE c.id IS NULL OR o.id IS NULL  -- Только несоответствия\nORDER BY status, COALESCE(c.id, -1);\n\n-- 2. Осиротевшие заказы\nSELECT o.id, o.customer_id, o.total, o.created_at\nFROM orders o\nLEFT JOIN customers c ON o.customer_id = c.id\nWHERE c.id IS NULL;\n\n-- 3. CROSS JOIN: прайс-лист вариантов\nSELECT\n    p.name AS product,\n    s.size_name,\n    p.base_price * s.price_multiplier AS variant_price\nFROM products p\nCROSS JOIN sizes s\nWHERE p.has_sizes = TRUE\nORDER BY p.name, s.sort_order;\n\n-- 4. Совместные покупки (что покупают вместе)\nSELECT\n    p1.name AS product_1,\n    p2.name AS product_2,\n    COUNT(*) AS times_bought_together\nFROM order_items oi1\nJOIN order_items oi2 ON oi1.order_id = oi2.order_id\n                     AND oi1.product_id < oi2.product_id  -- Избегаем дублей A-B и B-A\nJOIN products p1 ON oi1.product_id = p1.id\nJOIN products p2 ON oi2.product_id = p2.id\nGROUP BY p1.id, p1.name, p2.id, p2.name\nORDER BY times_bought_together DESC\nLIMIT 20;\n\n-- 5. Reconciliation: сравнение двух таблиц\nSELECT\n    COALESCE(a.id, b.id) AS id,\n    a.amount AS source_amount,\n    b.amount AS target_amount,\n    CASE\n        WHEN a.id IS NULL THEN \'Только в target\'\n        WHEN b.id IS NULL THEN \'Только в source\'\n        WHEN a.amount <> b.amount THEN \'Расхождение\'\n        ELSE \'Совпадает\'\n    END AS diff_type\nFROM source_payments a\nFULL OUTER JOIN target_payments b ON a.id = b.id\nWHERE a.id IS NULL OR b.id IS NULL OR a.amount <> b.amount;',
      explanation: 'FULL OUTER JOIN нужен редко — для reconciliation (сравнение двух источников) и поиска несоответствий. CROSS JOIN генерирует все комбинации — используй осторожно (N×M строк). SELF JOIN с oi1.product_id < oi2.product_id — элегантный способ получить пары без дублей.'
    },
    {
      id: 5,
      title: 'JOIN с подзапросами и CTE',
      type: 'practice',
      difficulty: 'medium',
      description: 'Комбинирование JOIN с подзапросами в FROM и CTE.',
      requirements: [
        'JOIN с подзапросом: присоединить агрегат — статистику клиента к основной таблице',
        'CTE + JOIN: для каждого товара показать его рейтинг внутри категории',
        'Lateral JOIN: для каждого клиента получить его последние 3 заказа',
        'JOIN на нескольких условиях: товары проданные в том же городе где находится склад',
        'Присоединить данные из двух разных периодов для сравнения (текущий vs прошлый год)'
      ],
      hint: 'Подзапрос в FROM ведёт себя как таблица. LATERAL JOIN позволяет подзапросу ссылаться на строки внешней таблицы — ORDER BY + LIMIT на каждую строку внешней таблицы. JOIN ON a.x = b.x AND a.y = b.y для составных условий.',
      solution: '-- 1. JOIN с агрегатом\nSELECT\n    c.id, c.name, c.email,\n    stats.orders_count,\n    stats.total_spent,\n    stats.avg_order\nFROM customers c\nJOIN (\n    SELECT\n        customer_id,\n        COUNT(*) AS orders_count,\n        SUM(total) AS total_spent,\n        ROUND(AVG(total), 2) AS avg_order\n    FROM orders\n    GROUP BY customer_id\n) stats ON c.id = stats.customer_id\nORDER BY stats.total_spent DESC;\n\n-- 2. CTE + JOIN: рейтинг товара в категории\nWITH product_revenue AS (\n    SELECT\n        p.id AS product_id,\n        p.category_id,\n        SUM(oi.quantity * oi.unit_price) AS revenue\n    FROM products p\n    JOIN order_items oi ON p.id = oi.product_id\n    GROUP BY p.id, p.category_id\n)\nSELECT\n    p.name,\n    cat.name AS category,\n    pr.revenue,\n    RANK() OVER (PARTITION BY p.category_id ORDER BY pr.revenue DESC) AS category_rank\nFROM products p\nJOIN categories cat ON p.category_id = cat.id\nJOIN product_revenue pr ON p.id = pr.product_id\nORDER BY cat.name, category_rank;\n\n-- 3. LATERAL JOIN: последние 3 заказа каждого клиента\nSELECT\n    c.name AS customer,\n    last_orders.id AS order_id,\n    last_orders.total,\n    last_orders.created_at\nFROM customers c\nCROSS JOIN LATERAL (\n    SELECT id, total, created_at\n    FROM orders\n    WHERE customer_id = c.id  -- Ссылка на внешнюю таблицу!\n    ORDER BY created_at DESC\n    LIMIT 3\n) last_orders\nORDER BY c.name, last_orders.created_at DESC;\n\n-- 4. JOIN на нескольких условиях\nSELECT\n    o.id AS order_id,\n    p.name AS product,\n    s.city AS warehouse_city,\n    c.city AS customer_city\nFROM orders o\nJOIN customers c     ON o.customer_id = c.id\nJOIN order_items oi  ON o.id = oi.order_id\nJOIN products p      ON oi.product_id = p.id\nJOIN stock s         ON s.product_id = p.id\n                     AND s.city = c.city  -- Второе условие JOIN!\nORDER BY s.city;\n\n-- 5. Сравнение текущего и прошлого года\nSELECT\n    COALESCE(cy.month_num, py.month_num) AS month,\n    cy.revenue AS current_year,\n    py.revenue AS prev_year,\n    cy.revenue - py.revenue AS delta,\n    ROUND((cy.revenue - py.revenue) / NULLIF(py.revenue, 0) * 100, 1) AS yoy_pct\nFROM (\n    SELECT EXTRACT(MONTH FROM created_at)::INTEGER AS month_num, SUM(total) AS revenue\n    FROM orders WHERE EXTRACT(YEAR FROM created_at) = EXTRACT(YEAR FROM NOW())\n    GROUP BY EXTRACT(MONTH FROM created_at)\n) cy\nFULL OUTER JOIN (\n    SELECT EXTRACT(MONTH FROM created_at)::INTEGER AS month_num, SUM(total) AS revenue\n    FROM orders WHERE EXTRACT(YEAR FROM created_at) = EXTRACT(YEAR FROM NOW()) - 1\n    GROUP BY EXTRACT(MONTH FROM created_at)\n) py ON cy.month_num = py.month_num\nORDER BY COALESCE(cy.month_num, py.month_num);',
      explanation: 'LATERAL JOIN — мощный инструмент для "top N на каждую строку". Без LATERAL нельзя ссылаться на внешнюю таблицу в подзапросе в FROM. CROSS JOIN LATERAL корректно работает и возвращает строки только если подзапрос вернул хотя бы одну строку (в отличие от LEFT JOIN LATERAL).'
    },
    {
      id: 6,
      title: 'Оптимизация JOIN запросов',
      type: 'practice',
      difficulty: 'hard',
      description: 'Анализ и оптимизация медленных JOIN запросов.',
      requirements: [
        'Медленный JOIN трёх таблиц: добавь покрывающие индексы, измерь улучшение через EXPLAIN ANALYZE',
        'Замени коррелированный подзапрос на JOIN (должно быть быстрее)',
        'Избегание cartesian product: проверь что нет лишних строк после JOIN',
        'JOIN с LIMIT: убедись что LIMIT применяется ПОСЛЕ JOIN а не до',
        'Hash Join vs Nested Loop: когда какой лучше, продемонстрируй на примерах'
      ],
      hint: 'EXPLAIN ANALYZE покажет какой тип JOIN выбран. Для ускорения Nested Loop: индекс на inner таблицу по join condition. Для Hash Join: work_mem. Cartesian product: COUNT(*) до и после JOIN — если резко вырос, где-то нет ON условия.',
      solution: '-- 1. До оптимизации\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT c.name, COUNT(oi.id) AS items, SUM(oi.unit_price * oi.quantity) AS rev\nFROM customers c\nJOIN orders o       ON c.id = o.customer_id\nJOIN order_items oi ON o.id = oi.order_id\nWHERE c.country = \'KZ\'\nGROUP BY c.id, c.name;\n-- Вероятно: Seq Scan + Hash Join\n\n-- Добавить индексы\nCREATE INDEX IF NOT EXISTS idx_customers_country ON customers(country);\nCREATE INDEX IF NOT EXISTS idx_orders_customer   ON orders(customer_id);\nCREATE INDEX IF NOT EXISTS idx_order_items_order ON order_items(order_id);\n-- Покрывающий для агрегата:\nCREATE INDEX IF NOT EXISTS idx_oi_order_covering\n    ON order_items(order_id) INCLUDE (id, unit_price, quantity);\n\n-- После:\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT c.name, COUNT(oi.id) AS items, SUM(oi.unit_price * oi.quantity) AS rev\nFROM customers c\nJOIN orders o       ON c.id = o.customer_id\nJOIN order_items oi ON o.id = oi.order_id\nWHERE c.country = \'KZ\'\nGROUP BY c.id, c.name;\n-- Ожидаем: Index Scan + Nested Loop (или Hash Join с меньшим набором)\n\n-- 2. Заменить коррелированный подзапрос на JOIN\n-- Медленно: O(N) подзапросов\nSELECT\n    p.name,\n    (SELECT SUM(quantity * unit_price) FROM order_items WHERE product_id = p.id) AS revenue\nFROM products p;\n\n-- Быстро: один JOIN + GROUP BY\nSELECT\n    p.name,\n    COALESCE(SUM(oi.quantity * oi.unit_price), 0) AS revenue\nFROM products p\nLEFT JOIN order_items oi ON p.id = oi.product_id\nGROUP BY p.id, p.name;\n\nEXPLAIN ANALYZE\nSELECT p.name, COALESCE(SUM(oi.quantity * oi.unit_price), 0) AS revenue\nFROM products p LEFT JOIN order_items oi ON p.id = oi.product_id\nGROUP BY p.id, p.name;\n-- Hash Join или Index Scan вместо N коррелированных подзапросов\n\n-- 3. Проверка cartesian product\n-- ПЛОХО: забытое ON условие!\nSELECT COUNT(*) FROM orders o, customers c;\n-- Может вернуть миллионы строк! N_orders * N_customers\n\n-- ХОРОШО:\nSELECT COUNT(*) FROM orders o JOIN customers c ON o.customer_id = c.id;\n\n-- 4. LIMIT с JOIN: применяется ПОСЛЕ JOIN\n-- Неверное ожидание:\n-- "LIMIT 10 заказов, потом JOIN с клиентами"\nSELECT o.id, c.name FROM orders o JOIN customers c ON o.customer_id = c.id\nORDER BY o.created_at DESC LIMIT 10;\n-- PostgreSQL: Limit (rows=10) -> Sort -> Hash Join -> Seq Scan...\n-- Если нужен LIMIT до JOIN -> подзапрос:\nSELECT sq.id, c.name\nFROM (SELECT id, customer_id FROM orders ORDER BY created_at DESC LIMIT 10) sq\nJOIN customers c ON sq.customer_id = c.id;\n\n-- 5. Nested Loop vs Hash Join\nSET work_mem = \'4MB\';\nEXPLAIN ANALYZE\nSELECT * FROM orders o JOIN customers c ON o.customer_id = c.id\nWHERE c.country = \'KZ\' LIMIT 100;\n-- Маленький результат + индекс на country -> Nested Loop\n\nSET work_mem = \'256MB\';\nEXPLAIN ANALYZE\nSELECT COUNT(*) FROM orders o JOIN customers c ON o.customer_id = c.id;\n-- Все строки, нет фильтра -> Hash Join\nRESET work_mem;',
      explanation: 'Оптимизация JOIN: 1) индексы на join columns (ON a.id = b.a_id -> индекс на b.a_id), 2) фильтруй данные до JOIN через WHERE или подзапросы, 3) покрывающие индексы устраняют обращения к Heap. Cartesian product (без ON) = главная ошибка начинающих — всегда проверяй количество строк результата.'
    },
    {
      id: 7,
      title: 'SELF JOIN и сложные иерархии',
      type: 'practice',
      difficulty: 'hard',
      description: 'Практика SELF JOIN для работы с иерархическими и последовательными данными.',
      requirements: [
        'Organisational chart: сотрудник + его прямой руководитель + руководитель руководителя (2 уровня)',
        'Найди пары сотрудников из одного отдела работающих на одной должности',
        'SELF JOIN по дате: для каждой продажи найди предыдущую продажу того же продавца',
        'Найди клиентов сделавших заказ после другого конкретного клиента в тот же день',
        'Сравни зарплаты сотрудников с медианой по отделу используя SELF JOIN'
      ],
      hint: 'SELF JOIN: JOIN employees m ON e.manager_id = m.id — псевдоним m для "менеджер". Для предыдущей продажи: SELF JOIN по product_id + условие на дату: s2.date < s1.date, MAX для ближайшей.',
      solution: '-- 1. Два уровня иерархии\nSELECT\n    e.name AS employee,\n    e.position AS emp_position,\n    mgr.name AS direct_manager,\n    mgr.position AS mgr_position,\n    gm.name AS manager_of_manager\nFROM employees e\nJOIN employees mgr ON e.manager_id = mgr.id\nLEFT JOIN employees gm ON mgr.manager_id = gm.id  -- LEFT: может не быть уровня выше\nORDER BY gm.name NULLS LAST, mgr.name, e.name;\n\n-- 2. Пары с одинаковой должностью в одном отделе\nSELECT\n    e1.name AS employee_1,\n    e2.name AS employee_2,\n    e1.department_id,\n    e1.position\nFROM employees e1\nJOIN employees e2 ON e1.department_id = e2.department_id\n                  AND e1.position = e2.position\n                  AND e1.id < e2.id  -- Только уникальные пары (A-B не B-A)\nORDER BY e1.department_id, e1.position;\n\n-- 3. Предыдущая продажа того же продавца\nSELECT\n    s1.id,\n    s1.seller_id,\n    s1.amount AS current_sale,\n    s1.sale_date,\n    s2.amount AS prev_sale,\n    s2.sale_date AS prev_date,\n    s1.amount - s2.amount AS growth\nFROM sales s1\nLEFT JOIN LATERAL (\n    SELECT amount, sale_date\n    FROM sales\n    WHERE seller_id = s1.seller_id\n      AND sale_date < s1.sale_date\n    ORDER BY sale_date DESC\n    LIMIT 1\n) s2 ON TRUE\nORDER BY s1.seller_id, s1.sale_date;\n\n-- 4. Клиенты заказавшие после другого клиента в тот же день\nSELECT\n    o1.customer_id AS customer,\n    o2.customer_id AS ordered_after_customer,\n    DATE(o1.created_at) AS order_date\nFROM orders o1\nJOIN orders o2 ON DATE(o1.created_at) = DATE(o2.created_at)\n              AND o1.created_at > o2.created_at  -- o1 после o2\n              AND o1.customer_id <> o2.customer_id\nWHERE o2.customer_id = 42  -- Конкретный клиент\nORDER BY o1.created_at;\n\n-- 5. Зарплаты vs медиана по отделу\nSELECT\n    e.name,\n    e.department_id,\n    e.salary,\n    dept_stats.median_salary,\n    e.salary - dept_stats.median_salary AS vs_median,\n    CASE\n        WHEN e.salary > dept_stats.median_salary THEN \'выше медианы\'\n        WHEN e.salary < dept_stats.median_salary THEN \'ниже медианы\'\n        ELSE \'равна медиане\'\n    END AS position_vs_median\nFROM employees e\nJOIN (\n    SELECT\n        department_id,\n        PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY salary) AS median_salary\n    FROM employees\n    GROUP BY department_id\n) dept_stats ON e.department_id = dept_stats.department_id\nORDER BY e.department_id, e.salary DESC;',
      explanation: 'SELF JOIN открывает возможности работы с относительными данными: иерархии, последовательности, сравнения в рамках группы. LATERAL + LIMIT 1 — более читаемая альтернатива SELF JOIN для "предыдущая запись". e1.id < e2.id — обязательно для уникальных пар без дублей.'
    },
    {
      id: 8,
      title: 'JOIN в реальных сценариях',
      type: 'practice',
      difficulty: 'hard',
      description: 'Решение реальных бизнес-задач с использованием сложных JOIN.',
      requirements: [
        'ABC анализ товаров: A (топ 20% по выручке), B (следующие 30%), C (остальные 50%) — используй оконные функции + JOIN с категориями',
        'Воронка продаж: JOIN трёх таблиц (лиды -> сделки -> оплаты) с конверсией на каждом шаге',
        'Матрица RFM: Recency (дней с последнего заказа), Frequency (кол-во заказов), Monetary (сумма) для каждого клиента — JOIN + агрегация + CASE',
        'Анализ корзины: топ-10 категорий которые чаще всего покупают вместе одним заказом'
      ],
      hint: 'ABC анализ: NTILE(10) OVER (ORDER BY revenue DESC) -> 1-2 это A, 3-5 это B, 6-10 это C. RFM: LEFT JOIN orders для Recency, COUNT для Frequency, SUM для Monetary.',
      solution: '-- 1. ABC анализ\nWITH product_revenue AS (\n    SELECT\n        p.id,\n        p.name,\n        cat.name AS category,\n        SUM(oi.quantity * oi.unit_price) AS revenue,\n        SUM(SUM(oi.quantity * oi.unit_price)) OVER () AS total_revenue\n    FROM products p\n    JOIN categories cat   ON p.category_id = cat.id\n    JOIN order_items oi   ON p.id = oi.product_id\n    GROUP BY p.id, p.name, cat.name\n),\nranked AS (\n    SELECT *,\n        SUM(revenue) OVER (ORDER BY revenue DESC) / total_revenue AS cumulative_pct\n    FROM product_revenue\n)\nSELECT\n    name, category, revenue,\n    ROUND(revenue / total_revenue * 100, 2) AS revenue_pct,\n    ROUND(cumulative_pct * 100, 1) AS cumulative_pct,\n    CASE\n        WHEN cumulative_pct <= 0.80 THEN \'A\'\n        WHEN cumulative_pct <= 0.95 THEN \'B\'\n        ELSE \'C\'\n    END AS abc_class\nFROM ranked\nORDER BY revenue DESC;\n\n-- 2. Воронка продаж\nSELECT\n    COUNT(DISTINCT l.id) AS leads,\n    COUNT(DISTINCT d.id) AS deals,\n    COUNT(DISTINCT p.id) AS payments,\n    ROUND(COUNT(DISTINCT d.id)::NUMERIC / NULLIF(COUNT(DISTINCT l.id), 0) * 100, 1)\n        AS lead_to_deal_pct,\n    ROUND(COUNT(DISTINCT p.id)::NUMERIC / NULLIF(COUNT(DISTINCT d.id), 0) * 100, 1)\n        AS deal_to_payment_pct,\n    ROUND(COUNT(DISTINCT p.id)::NUMERIC / NULLIF(COUNT(DISTINCT l.id), 0) * 100, 1)\n        AS overall_conversion_pct\nFROM leads l\nLEFT JOIN deals d     ON l.id = d.lead_id\nLEFT JOIN payments p  ON d.id = p.deal_id;\n\n-- 3. RFM матрица\nWITH rfm AS (\n    SELECT\n        c.id AS customer_id,\n        c.name,\n        EXTRACT(DAY FROM NOW() - MAX(o.created_at))::INTEGER AS recency_days,\n        COUNT(o.id)    AS frequency,\n        SUM(o.total)   AS monetary\n    FROM customers c\n    LEFT JOIN orders o ON c.id = o.customer_id\n    GROUP BY c.id, c.name\n),\nrfm_scored AS (\n    SELECT *,\n        NTILE(5) OVER (ORDER BY recency_days ASC)   AS r_score, -- 5 = недавно\n        NTILE(5) OVER (ORDER BY frequency DESC)     AS f_score,\n        NTILE(5) OVER (ORDER BY monetary DESC)      AS m_score\n    FROM rfm\n)\nSELECT\n    customer_id, name,\n    recency_days, frequency, ROUND(monetary, 0) AS monetary,\n    r_score, f_score, m_score,\n    r_score + f_score + m_score AS rfm_total,\n    CASE\n        WHEN r_score >= 4 AND f_score >= 4 THEN \'Чемпионы\'\n        WHEN r_score >= 3 AND f_score >= 3 THEN \'Лояльные\'\n        WHEN r_score >= 4 AND f_score <= 2 THEN \'Новые\'\n        WHEN r_score <= 2 AND f_score >= 3 THEN \'Под угрозой\'\n        WHEN r_score <= 2 AND f_score <= 2 THEN \'Потерянные\'\n        ELSE \'Обычные\'\n    END AS segment\nFROM rfm_scored\nORDER BY rfm_total DESC;\n\n-- 4. Совместные категории в корзине\nSELECT\n    c1.name AS category_1,\n    c2.name AS category_2,\n    COUNT(DISTINCT oi1.order_id) AS orders_together\nFROM order_items oi1\nJOIN order_items oi2 ON oi1.order_id = oi2.order_id\nJOIN products p1 ON oi1.product_id = p1.id\nJOIN products p2 ON oi2.product_id = p2.id\nJOIN categories c1 ON p1.category_id = c1.id\nJOIN categories c2 ON p2.category_id = c2.id\nWHERE p1.category_id < p2.category_id  -- Уникальные пары\nGROUP BY c1.name, c2.name\nORDER BY orders_together DESC\nLIMIT 10;',
      explanation: 'RFM — классический инструмент сегментации клиентов. NTILE(5) делит на 5 равных квантилей. ABC анализ через накопительный % выручки: первые 80% выручки = категория A. Пары категорий в корзине = рекомендации к товару (market basket analysis).'
    },
    {
      id: 9,
      title: 'JOIN и NULL: типичные ошибки',
      type: 'practice',
      difficulty: 'medium',
      description: 'Разбор типичных ошибок при работе с JOIN и NULL значениями.',
      requirements: [
        'Ошибка: посчитать количество заказов клиента через COUNT(*) с LEFT JOIN (даст 1 для клиентов без заказов)',
        'Исправь: использовать COUNT(o.id) или COUNT(o.*)  вместо COUNT(*)',
        'Ошибка: NOT IN с NULL — подзапрос возвращает NULL и NOT IN работает неправильно',
        'Исправь: используй NOT EXISTS вместо NOT IN когда подзапрос может вернуть NULL',
        'Ошибка: фильтр после LEFT JOIN переводит его в INNER JOIN'
      ],
      hint: 'COUNT(*) считает все строки включая NULL. COUNT(o.id) не считает NULL. NOT IN (... NULL ...) всегда возвращает 0 строк. WHERE o.status = \'paid\' после LEFT JOIN исключит строки где o = NULL.',
      solution: '-- 1. Ошибка COUNT(*)\n-- НЕВЕРНО: клиенты без заказов получат count=1!\nSELECT c.name, COUNT(*) AS wrong_count\nFROM customers c\nLEFT JOIN orders o ON c.id = o.customer_id\nGROUP BY c.id, c.name;\n\n-- ВЕРНО: COUNT(o.id) не считает NULL\nSELECT c.name, COUNT(o.id) AS correct_count\nFROM customers c\nLEFT JOIN orders o ON c.id = o.customer_id\nGROUP BY c.id, c.name;\n\n-- 2. Ошибка NOT IN с NULL\n-- Таблица cancelled_customers содержит строки с NULL customer_id!\n-- ПРОБЛЕМА:\nSELECT * FROM orders\nWHERE customer_id NOT IN (\n    SELECT customer_id FROM cancelled_accounts\n    -- Если хотя бы одно customer_id = NULL -> NOT IN вернёт 0 строк!\n);\n\n-- РЕШЕНИЕ: NOT EXISTS\nSELECT * FROM orders o\nWHERE NOT EXISTS (\n    SELECT 1 FROM cancelled_accounts ca\n    WHERE ca.customer_id = o.customer_id\n    -- NULL в ca.customer_id просто не совпадёт = не включится\n);\n\n-- ИЛИ: фильтровать NULL в подзапросе\nSELECT * FROM orders\nWHERE customer_id NOT IN (\n    SELECT customer_id FROM cancelled_accounts\n    WHERE customer_id IS NOT NULL\n);\n\n-- 3. Ошибка: WHERE переводит LEFT в INNER JOIN\n-- НЕВЕРНО: WHERE на колонку из правой таблицы отфильтрует NULL строки!\nSELECT c.name, o.id, o.status\nFROM customers c\nLEFT JOIN orders o ON c.id = o.customer_id\nWHERE o.status = \'paid\';  -- Клиенты без заказов ИСЧЕЗНУТ!\n-- Теперь это INNER JOIN по эффекту!\n\n-- ВЕРНО: фильтровать в ON или использовать OR IS NULL\nSELECT c.name, o.id, o.status\nFROM customers c\nLEFT JOIN orders o ON c.id = o.customer_id\n                  AND o.status = \'paid\';  -- Фильтр в ON: клиенты без paid заказов остаются!\n\n-- ИЛИ:\nSELECT c.name, o.id, o.status\nFROM customers c\nLEFT JOIN orders o ON c.id = o.customer_id\nWHERE o.status = \'paid\' OR o.id IS NULL;  -- Клиенты без заказов сохраняются',
      explanation: 'Три главные ловушки с JOIN и NULL: 1) COUNT(*) vs COUNT(col) — используй COUNT(правая.id) для корректного счёта с LEFT JOIN. 2) NOT IN с NULL подзапросом — всегда возвращает 0 строк, используй NOT EXISTS. 3) WHERE на колонку правой таблицы превращает LEFT JOIN в INNER — перенеси условие в ON.'
    },
    {
      id: 10,
      title: 'Финальный практикум JOIN',
      type: 'practice',
      difficulty: 'hard',
      description: 'Комплексные задачи объединяющие все типы JOIN с реальными бизнес-сценариями.',
      requirements: [
        'Полный дашборд: по каждому клиенту — статистика заказов, последний заказ, наиболее купленная категория, сегмент',
        'Сетевой анализ: найди пары клиентов купивших одни и те же 3+ товара',
        'Отчёт по воронке для каждого рекламного канала: клики -> регистрации -> первые заказы -> повторные заказы',
        'Иерархический отчёт: выручка по регионам -> городам -> клиентам с нарастающим итогом'
      ],
      hint: 'Наиболее купленная категория: DISTINCT ON (customer_id) + ORDER BY revenue DESC. Пары клиентов с 3+ общими товарами: SELF JOIN order_items по product_id, GROUP BY customer pair, HAVING COUNT DISTINCT >= 3.',
      solution: '-- 1. Полный дашборд клиента\nWITH customer_orders AS (\n    SELECT\n        c.id, c.name, c.email, c.city,\n        COUNT(o.id) AS orders_count,\n        COALESCE(SUM(o.total), 0) AS total_spent,\n        MAX(o.created_at) AS last_order_date\n    FROM customers c\n    LEFT JOIN orders o ON c.id = o.customer_id\n    GROUP BY c.id, c.name, c.email, c.city\n),\ntop_category AS (\n    SELECT DISTINCT ON (o.customer_id)\n        o.customer_id,\n        cat.name AS fav_category\n    FROM orders o\n    JOIN order_items oi ON o.id = oi.order_id\n    JOIN products p ON oi.product_id = p.id\n    JOIN categories cat ON p.category_id = cat.id\n    GROUP BY o.customer_id, cat.id, cat.name\n    ORDER BY o.customer_id, SUM(oi.quantity * oi.unit_price) DESC\n)\nSELECT\n    co.id, co.name, co.email, co.city,\n    co.orders_count, co.total_spent, co.last_order_date,\n    tc.fav_category,\n    CASE\n        WHEN co.total_spent >= 500000 THEN \'Platinum\'\n        WHEN co.total_spent >= 100000 THEN \'Gold\'\n        WHEN co.total_spent >= 20000  THEN \'Silver\'\n        ELSE \'Bronze\'\n    END AS segment\nFROM customer_orders co\nLEFT JOIN top_category tc ON co.id = tc.customer_id\nORDER BY co.total_spent DESC;\n\n-- 2. Клиенты с 3+ общими товарами\nSELECT\n    oi1.customer_id AS customer_1,\n    oi2.customer_id AS customer_2,\n    COUNT(DISTINCT oi1.product_id) AS common_products,\n    ARRAY_AGG(DISTINCT p.name ORDER BY p.name) AS common_product_names\nFROM (\n    SELECT DISTINCT o.customer_id, oi.product_id\n    FROM order_items oi JOIN orders o ON oi.order_id = o.id\n) oi1\nJOIN (\n    SELECT DISTINCT o.customer_id, oi.product_id\n    FROM order_items oi JOIN orders o ON oi.order_id = o.id\n) oi2 ON oi1.product_id = oi2.product_id\n     AND oi1.customer_id < oi2.customer_id\nJOIN products p ON oi1.product_id = p.id\nGROUP BY oi1.customer_id, oi2.customer_id\nHAVING COUNT(DISTINCT oi1.product_id) >= 3\nORDER BY common_products DESC;\n\n-- 3. Воронка по каналам\nSELECT\n    ch.name AS channel,\n    COUNT(DISTINCT cl.id)  AS clicks,\n    COUNT(DISTINCT u.id)   AS registrations,\n    COUNT(DISTINCT fo.customer_id) AS first_orders,\n    COUNT(DISTINCT ro.customer_id) AS repeat_orders,\n    ROUND(COUNT(DISTINCT u.id)::NUMERIC /\n          NULLIF(COUNT(DISTINCT cl.id), 0) * 100, 1) AS reg_conv_pct\nFROM channels ch\nLEFT JOIN clicks cl    ON cl.channel_id = ch.id\nLEFT JOIN users u      ON u.click_id = cl.id\nLEFT JOIN (\n    SELECT customer_id, MIN(id) AS first_order_id\n    FROM orders GROUP BY customer_id\n) first_ord ON first_ord.customer_id = u.id\nLEFT JOIN orders fo    ON fo.id = first_ord.first_order_id\nLEFT JOIN (\n    SELECT DISTINCT customer_id FROM orders o2\n    WHERE EXISTS (\n        SELECT 1 FROM orders o3 WHERE o3.customer_id = o2.customer_id AND o3.id <> o2.id\n    )\n) ro ON ro.customer_id = u.id\nGROUP BY ch.id, ch.name\nORDER BY clicks DESC;\n\n-- 4. Иерархический отчёт с нарастающим итогом\nSELECT\n    c.country,\n    c.city,\n    c.id AS customer_id,\n    c.name AS customer_name,\n    SUM(o.total) AS customer_revenue,\n    SUM(SUM(o.total)) OVER (PARTITION BY c.country, c.city) AS city_total,\n    SUM(SUM(o.total)) OVER (PARTITION BY c.country) AS country_total,\n    SUM(SUM(o.total)) OVER () AS grand_total,\n    ROUND(\n        SUM(o.total) / SUM(SUM(o.total)) OVER (PARTITION BY c.country) * 100\n    , 1) AS pct_of_country\nFROM customers c\nJOIN orders o ON c.id = o.customer_id\nGROUP BY c.country, c.city, c.id, c.name\nORDER BY c.country, c.city, customer_revenue DESC;',
      explanation: 'DISTINCT ON — PostgreSQL-специфичный способ получить первую строку по группе. Матрица пересечений клиентов — это SELF JOIN + HAVING COUNT. Иерархические нарастающие итоги: SUM OVER с разными PARTITION BY (клиент -> город -> страна -> всё).'
    }
  ]
}
