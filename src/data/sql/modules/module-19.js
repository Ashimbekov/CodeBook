export default {
  id: 19,
  title: 'Оптимизация запросов',
  description: 'Индексы B-tree/Hash/GIN/BRIN, частичные и покрывающие индексы, статистика таблиц, анализ медленных запросов, антипаттерны производительности. Стратегии оптимизации на реальных данных.',
  lessons: [
    {
      id: 1,
      title: 'Индексы: как работают и когда использовать',
      type: 'theory',
      content: [
        { type: 'text', value: 'Индекс — структура данных ускоряющая поиск строк. Без индекса PostgreSQL делает Sequential Scan: читает каждую строку. С индексом — Index Scan: переходит сразу к нужным строкам. Цена: дополнительное место и замедление INSERT/UPDATE/DELETE.' },
        { type: 'code', language: 'sql', value: '-- Создание базового индекса (B-tree по умолчанию)\nCREATE INDEX idx_orders_customer_id ON orders(customer_id);\n-- B-tree: для =, <, >, <=, >=, BETWEEN, LIKE \'prefix%\'\n\n-- Составной индекс\nCREATE INDEX idx_orders_status_date ON orders(status, created_at DESC);\n-- Важен ПОРЯДОК столбцов! Запросы:\n-- WHERE status = \'paid\'                  -> использует индекс\n-- WHERE status = \'paid\' AND created_at > \'2024-01-01\' -> использует индекс\n-- WHERE created_at > \'2024-01-01\'        -> НЕ использует (нет status в начале!)\n\n-- UNIQUE индекс\nCREATE UNIQUE INDEX idx_users_email ON users(email);\n-- Одновременно гарантирует уникальность и создаёт индекс\n\n-- Частичный индекс (Partial Index)\nCREATE INDEX idx_orders_active ON orders(customer_id, created_at)\nWHERE status = \'active\';\n-- Индексирует ТОЛЬКО активные заказы\n-- Меньше размер, быстрее обновление, используется только с WHERE status=\'active\'\n\n-- Покрывающий индекс (Covering Index / INCLUDE)\nCREATE INDEX idx_orders_covering ON orders(customer_id)\nINCLUDE (total, status, created_at);\n-- INCLUDE: лишние столбцы хранятся в индексе но не участвуют в сортировке\n-- Позволяет Index Only Scan (данные читаются из индекса без обращения к таблице!)\n\n-- Функциональный индекс\nCREATE INDEX idx_users_email_lower ON users(LOWER(email));\n-- Используется при: WHERE LOWER(email) = \'aliya@mail.ru\'\n-- Без него: WHERE LOWER(email) = ... -> Sequential Scan!\n\n-- Список индексов таблицы:\nSELECT indexname, indexdef FROM pg_indexes\nWHERE tablename = \'orders\' AND schemaname = \'public\';' },
        { type: 'tip', value: 'Индексы нужны на: столбцы в WHERE/JOIN ON/ORDER BY. НЕ нужны: маленькие таблицы (<10000 строк), столбцы с низкой кардинальностью (status с 3-5 значениями), столбцы которые редко запрашиваются. Каждый индекс = дополнительный overhead при INSERT/UPDATE.' }
      ]
    },
    {
      id: 2,
      title: 'Типы индексов: Hash, GIN, BRIN, GiST',
      type: 'theory',
      content: [
        { type: 'text', value: 'PostgreSQL поддерживает несколько типов индексов для разных случаев. Выбор типа индекса значительно влияет на производительность.' },
        { type: 'code', language: 'sql', value: '-- HASH индекс: только для =, быстрее B-tree для point lookups\nCREATE INDEX idx_sessions_token ON sessions USING HASH (token);\n-- WHERE token = \'abc123\'  -> Hash, быстро\n-- WHERE token LIKE \'abc%\' -> НЕ работает с Hash!\n-- Примечание: в PostgreSQL 10+ Hash индексы стали надёжными (WAL-safe)\n\n-- GIN (Generalized Inverted Index): для массивов, JSONB, full-text search\nCREATE INDEX idx_products_tags ON products USING GIN (tags);\n-- tags: TEXT[]\n-- WHERE tags @> ARRAY[\'электроника\', \'смартфон\']  -> использует GIN\n-- WHERE tags && ARRAY[\'sale\']                      -> использует GIN\n\nCREATE INDEX idx_products_attributes ON products USING GIN (attributes);\n-- attributes: JSONB\n-- WHERE attributes @> \'{"color": "black"}\'         -> использует GIN\n-- WHERE attributes ? \'color\'                       -> использует GIN (ключ существует)\n\nCREATE INDEX idx_articles_search ON articles USING GIN (to_tsvector(\'russian\', body));\n-- Полнотекстовый поиск:\n-- WHERE to_tsvector(\'russian\', body) @@ to_tsquery(\'russian\', \'postgresql\') -> GIN\n\n-- BRIN (Block Range INdex): для огромных таблиц с коррелированными данными\n-- Очень маленький размер, используется для временных рядов\nCREATE INDEX idx_logs_created_at ON logs USING BRIN (created_at);\n-- Идеально если строки физически упорядочены по created_at (аппенд-логи)\n-- Размер: в 100-1000 раз меньше B-tree!\n-- Работает для: >, <, BETWEEN на монотонно возрастающих данных\n\n-- GiST (Generalized Search Tree): геометрия, диапазоны, full-text\nCREATE INDEX idx_places_location ON places USING GiST (location);\n-- location: POINT\n-- WHERE location <-> point(55.75, 37.61) < 10  -> GiST (nearest neighbor)\n\nCREATE INDEX idx_events_period ON events USING GiST (period);\n-- period: TSTZRANGE\n-- WHERE period && \'[2024-01-01, 2024-12-31]\'  -> GiST (overlap)\n\n-- SP-GiST: для пространственных данных нерегулярной структуры\n-- Bloom фильтр: для составных условий на многих столбцах\nCREATE EXTENSION IF NOT EXISTS bloom;\nCREATE INDEX idx_bloom ON wide_table USING bloom (col1, col2, col3, col4);\n-- WHERE col1 = X AND col3 = Y (случайные комбинации) -> Bloom' },
        { type: 'note', value: 'Правило выбора: B-tree (по умолчанию) — для большинства задач. Hash — только для равенства, экономит место. GIN — массивы/JSONB/full-text. BRIN — огромные append-only таблицы. GiST — геометрия, диапазоны, full-text с ранжированием.' }
      ]
    },
    {
      id: 3,
      title: 'Антипаттерны: запросы убивающие производительность',
      type: 'theory',
      content: [
        { type: 'text', value: 'Есть несколько паттернов SQL-кода которые всегда или часто приводят к медленным запросам. Знание антипаттернов позволяет писать эффективный код сразу.' },
        { type: 'code', language: 'sql', value: '-- АНТИПАТТЕРН 1: Функция на индексированном столбце в WHERE\n-- МЕДЛЕННО (Sequential Scan, индекс не используется!):\nSELECT * FROM orders WHERE YEAR(created_at) = 2024;\nSELECT * FROM users  WHERE LOWER(email) = \'test@mail.ru\';  -- Без функц.индекса\nSELECT * FROM orders WHERE DATE(created_at) = \'2024-01-15\';\n\n-- БЫСТРО (сравниваем сам столбец, индекс работает):\nSELECT * FROM orders\nWHERE created_at >= \'2024-01-01\' AND created_at < \'2025-01-01\';\n\nSELECT * FROM users WHERE email = LOWER(\'Test@Mail.ru\');\n-- или: индекс на LOWER(email) + WHERE LOWER(email) = ...\n\nSELECT * FROM orders\nWHERE created_at >= \'2024-01-15\' AND created_at < \'2024-01-16\';\n\n-- АНТИПАТТЕРН 2: LIKE с ведущим %\n-- Нельзя использовать индекс при ведущем %:\nSELECT * FROM products WHERE name LIKE \'%телефон%\';  -- Seq Scan!\n-- Решения: pg_trgm индекс или full-text search:\nCREATE EXTENSION IF NOT EXISTS pg_trgm;\nCREATE INDEX idx_products_name_trgm ON products USING GIN (name gin_trgm_ops);\nSELECT * FROM products WHERE name LIKE \'%телефон%\';  -- Теперь GIN!\n\n-- АНТИПАТТЕРН 3: N+1 запросов (в приложении)\n-- ПЛОХО: один SELECT + N SELECT для каждого заказа:\n-- orders = SELECT * FROM orders LIMIT 100\n-- for order in orders: SELECT * FROM customers WHERE id = order.customer_id\n-- Итого: 101 запрос!\n\n-- ХОРОШО: один JOIN:\nSELECT o.*, c.name, c.email\nFROM orders o\nJOIN customers c ON o.customer_id = c.id\nLIMIT 100;\n-- Один запрос!\n\n-- АНТИПАТТЕРН 4: SELECT * в продакшене\nSELECT * FROM orders;  -- Читает ВСЕ столбцы, даже ненужные\n-- Решение: указывай только нужные столбцы\nSELECT id, customer_id, total, status, created_at FROM orders;\n\n-- АНТИПАТТЕРН 5: Неявное преобразование типов\n-- id INTEGER, но передаём TEXT -> PostgreSQL преобразует все id в TEXT!\nSELECT * FROM orders WHERE id = \'42\';  -- Может не использовать индекс\n-- Решение: передавай правильный тип:\nSELECT * FROM orders WHERE id = 42;    -- INTEGER\n\n-- АНТИПАТТЕРН 6: OR на индексированных столбцах\n-- МЕДЛЕННО:\nSELECT * FROM orders WHERE status = \'paid\' OR status = \'shipped\';\n-- БЫСТРО (если есть индекс):\nSELECT * FROM orders WHERE status IN (\'paid\', \'shipped\');\n-- Или UNION ALL для разных индексов:\nSELECT * FROM orders WHERE customer_id = 1\nUNION ALL\nSELECT * FROM orders WHERE product_id = 5;' },
        { type: 'warning', value: 'N+1 — самый частый убийца производительности в веб-приложениях. Всегда проверяй сколько SQL-запросов делает страница. Инструменты: pg_stat_statements (счётчик запросов), django-debug-toolbar, EXPLAIN ANALYZE.' }
      ]
    },
    {
      id: 4,
      title: 'Статистика и ANALYZE',
      type: 'theory',
      content: [
        { type: 'text', value: 'Планировщик PostgreSQL принимает решения на основе статистики таблиц. Устаревшая статистика = плохие планы запросов. ANALYZE обновляет статистику.' },
        { type: 'code', language: 'sql', value: '-- ANALYZE: обновить статистику таблицы\nANALYZE;               -- Все таблицы в текущей БД\nANALYZE employees;     -- Конкретная таблица\nANALYZE employees (salary, department_id);  -- Конкретные столбцы\n\n-- AutoVacuum автоматически запускает ANALYZE при 20% изменённых строк\n-- Можно изменить порог:\nALTER TABLE orders SET (autovacuum_analyze_scale_factor = 0.01);\n-- Запускать ANALYZE при изменении 1% строк (вместо 20%)\n\n-- Просмотр статистики столбца:\nSELECT\n    attname,\n    n_distinct,       -- Количество уникальных значений (-1.0 = 100%)\n    correlation,      -- Корреляция порядка строк (1 = полностью упорядочено)\n    most_common_vals, -- Наиболее частые значения\n    most_common_freqs -- Их частоты\nFROM pg_stats\nWHERE tablename = \'orders\' AND schemaname = \'public\';\n\n-- Увеличить детализацию статистики для важного столбца:\nALTER TABLE orders ALTER COLUMN status SET STATISTICS 500;\n-- По умолчанию: 100. Больше = лучше оценки планировщика, медленнее ANALYZE\n\n-- Размер таблиц и индексов:\nSELECT\n    relname                         AS table_name,\n    pg_size_pretty(pg_relation_size(relid)) AS table_size,\n    pg_size_pretty(pg_indexes_size(relid))  AS indexes_size,\n    pg_size_pretty(pg_total_relation_size(relid)) AS total_size\nFROM pg_stat_user_tables\nORDER BY pg_total_relation_size(relid) DESC;\n\n-- Неиспользуемые индексы:\nSELECT\n    schemaname,\n    relname,\n    indexrelname,\n    idx_scan,  -- 0 = никогда не использовался!\n    pg_size_pretty(pg_relation_size(indexrelid)) AS index_size\nFROM pg_stat_user_indexes\nWHERE idx_scan = 0\n  AND relname NOT LIKE \'pg_%\'\nORDER BY pg_relation_size(indexrelid) DESC;\n-- Кандидаты на удаление!' },
        { type: 'tip', value: 'Неиспользуемые индексы — зря занятое место и лишняя нагрузка при INSERT/UPDATE. Регулярно проверяй pg_stat_user_indexes.idx_scan = 0. Перед удалением убедись что счётчик собран за достаточно долгий период (pg_stat_reset() сбрасывает).' }
      ]
    },
    {
      id: 5,
      title: 'Slow Query Log и pg_stat_statements',
      type: 'theory',
      content: [
        { type: 'text', value: 'Для поиска медленных запросов используют log_min_duration_statement в postgresql.conf и расширение pg_stat_statements. Эти инструменты показывают какие запросы тратят больше всего времени.' },
        { type: 'code', language: 'sql', value: '-- pg_stat_statements: топ-10 запросов по общему времени\nCREATE EXTENSION IF NOT EXISTS pg_stat_statements;\n\nSELECT\n    LEFT(query, 80)                            AS query_snippet,\n    calls,\n    ROUND(total_exec_time::NUMERIC, 2)         AS total_ms,\n    ROUND(mean_exec_time::NUMERIC, 2)          AS avg_ms,\n    ROUND(stddev_exec_time::NUMERIC, 2)        AS stddev_ms,\n    ROUND(100 * total_exec_time\n        / SUM(total_exec_time) OVER(), 2)      AS pct_of_total,\n    rows\nFROM pg_stat_statements\nWHERE userid = (SELECT usesysid FROM pg_user WHERE usename = current_user)\nORDER BY total_exec_time DESC\nLIMIT 10;\n\n-- Самые медленные запросы (по среднему времени):\nSELECT\n    LEFT(query, 100) AS query,\n    calls,\n    ROUND(mean_exec_time::NUMERIC, 2) AS avg_ms,\n    ROUND(max_exec_time::NUMERIC, 2)  AS max_ms\nFROM pg_stat_statements\nWHERE calls > 10  -- Только часто вызываемые\nORDER BY mean_exec_time DESC\nLIMIT 10;\n\n-- Сбросить статистику:\nSELECT pg_stat_statements_reset();\n\n-- Slow Query Log в postgresql.conf:\n-- log_min_duration_statement = 1000  # Логировать запросы дольше 1 секунды\n-- log_min_duration_statement = 0     # Логировать ВСЕ запросы (осторожно!)\n-- log_min_duration_statement = -1    # Отключить логирование\n\n-- Анализ логов через pgBadger (внешний инструмент):\n-- pgbadger /var/log/postgresql/postgresql-2024-01-15_000000.log\n-- Генерирует HTML-отчёт с топом медленных запросов, пиковой нагрузкой\n\n-- pg_stat_activity: что сейчас выполняется:\nSELECT\n    pid,\n    query_start,\n    NOW() - query_start AS duration,\n    state,\n    LEFT(query, 100) AS query\nFROM pg_stat_activity\nWHERE state = \'active\'\n  AND NOW() - query_start > INTERVAL \'5 seconds\'\nORDER BY query_start;\n-- Показывает "застрявшие" запросы\n\n-- Убить зависший запрос:\nSELECT pg_cancel_backend(pid);  -- Мягко: ждёт точки прерывания\nSELECT pg_terminate_backend(pid);  -- Жёстко: немедленно' },
        { type: 'note', value: 'pg_stat_statements — must-have расширение для любой продакшен-БД. Показывает: total_exec_time (где тратится больше всего времени суммарно), mean_exec_time (какие запросы медленные в среднем), calls (как часто вызывается).' }
      ]
    },
    {
      id: 6,
      title: 'Партиционирование таблиц',
      type: 'theory',
      content: [
        { type: 'text', value: 'Партиционирование (partitioning) разбивает большую таблицу на меньшие физические части. PostgreSQL 10+ поддерживает декларативное партиционирование. Улучшает производительность запросов к большим таблицам за счёт partition pruning.' },
        { type: 'code', language: 'sql', value: '-- RANGE партиционирование по дате (частый случай — логи, события)\nCREATE TABLE events (\n    id         BIGSERIAL,\n    type       TEXT NOT NULL,\n    user_id    INTEGER,\n    data       JSONB,\n    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()\n) PARTITION BY RANGE (created_at);\n\n-- Создаём партиции (можно автоматизировать через pg_partman):\nCREATE TABLE events_2024_01 PARTITION OF events\n    FOR VALUES FROM (\'2024-01-01\') TO (\'2024-02-01\');\n\nCREATE TABLE events_2024_02 PARTITION OF events\n    FOR VALUES FROM (\'2024-02-01\') TO (\'2024-03-01\');\n\nCREATE TABLE events_default PARTITION OF events DEFAULT;\n\n-- Индексы создаются на каждую партицию автоматически если на родительской:\nCREATE INDEX ON events (created_at, type);\n-- Создаёт индексы на events_2024_01, events_2024_02, events_default\n\n-- Запрос использует только нужные партиции (partition pruning):\nSELECT COUNT(*) FROM events\nWHERE created_at BETWEEN \'2024-01-01\' AND \'2024-01-31\';\n-- PostgreSQL читает только events_2024_01, игнорирует остальные!\n\nEXPLAIN SELECT COUNT(*) FROM events\nWHERE created_at >= \'2024-01-01\' AND created_at < \'2024-02-01\';\n-- -> Seq Scan on events_2024_01 (partition pruning сработал!)\n\n-- LIST партиционирование по категории\nCREATE TABLE orders_partitioned (\n    id     BIGSERIAL,\n    status TEXT NOT NULL,\n    total  DECIMAL\n) PARTITION BY LIST (status);\n\nCREATE TABLE orders_active   PARTITION OF orders_partitioned FOR VALUES IN (\'pending\', \'paid\');\nCREATE TABLE orders_complete PARTITION OF orders_partitioned FOR VALUES IN (\'shipped\', \'completed\');\nCREATE TABLE orders_other    PARTITION OF orders_partitioned DEFAULT;\n\n-- HASH партиционирование (равномерное распределение)\nCREATE TABLE user_sessions (\n    id      BIGSERIAL,\n    user_id INTEGER\n) PARTITION BY HASH (user_id);\n\nCREATE TABLE user_sessions_0 PARTITION OF user_sessions FOR VALUES WITH (MODULUS 4, REMAINDER 0);\nCREATE TABLE user_sessions_1 PARTITION OF user_sessions FOR VALUES WITH (MODULUS 4, REMAINDER 1);\nCREATE TABLE user_sessions_2 PARTITION OF user_sessions FOR VALUES WITH (MODULUS 4, REMAINDER 2);\nCREATE TABLE user_sessions_3 PARTITION OF user_sessions FOR VALUES WITH (MODULUS 4, REMAINDER 3);\n\n-- Удалить старые данные мгновенно: DROP TABLE вместо DELETE!\nDROP TABLE events_2023_01;  -- Мгновенно! DELETE занял бы минуты' },
        { type: 'tip', value: 'Партиционирование даёт преимущества при таблицах от нескольких десятков миллионов строк. Ключевые плюсы: 1) partition pruning (запрос читает только нужные партиции), 2) быстрое удаление старых данных через DROP PARTITION, 3) параллельные запросы по партициям.' }
      ]
    },
    {
      id: 7,
      title: 'Практика: Оптимизация медленных запросов',
      type: 'practice',
      difficulty: 'hard',
      description: 'Проанализируй и оптимизируй набор медленных запросов.',
      requirements: [
        'Запрос 1: SELECT * FROM orders WHERE EXTRACT(YEAR FROM created_at) = 2024 — убери функцию из WHERE, добавь индекс',
        'Запрос 2: Поиск по email без индекса — измерь до/после через EXPLAIN ANALYZE',
        'Запрос 3: JOIN трёх больших таблиц — добавь покрывающий индекс, проверь Index Only Scan',
        'Частичный индекс: индексировать только незавершённые заказы (status != \'completed\')',
        'GIN индекс для поиска по тегам (массив TEXT[])',
        'Найди и удали неиспользуемые индексы через pg_stat_user_indexes',
        'pg_stat_statements: найди топ-5 запросов по total_exec_time'
      ],
      hint: 'EXPLAIN (ANALYZE, BUFFERS) показывает Heap Fetches для покрывающего индекса. Index Only Scan = Heap Fetches = 0. Частичный индекс WHERE status != \'completed\' будет использован только при WHERE status IN (\'pending\', \'paid\', ...).',
      expectedOutput: '-- Запрос 1: убрать функцию из WHERE\nДо: EXPLAIN SELECT * FROM orders WHERE EXTRACT(YEAR FROM created_at) = 2024;\n -> Seq Scan on orders (cost=0.00..4820.00 rows=2000) actual time=0.042..45.820 ms\n\nПосле (диапазон + индекс):\n -> Index Scan using idx_orders_created_at (cost=0.43..312.50 rows=2000) actual time=0.021..1.250 ms\nУскорение: ~37x\n\n-- Запрос 2: индекс по email\nДо: Seq Scan on users (cost=0.00..21400.00 rows=1) actual time=0.050..485.200 ms\nCREATE INDEX idx_users_email ON users(email); -- CREATE INDEX\nПосле: Index Scan using idx_users_email (cost=0.43..8.45 rows=1) actual time=0.015..0.018 ms\nУскорение: ~26000x\n\n-- Запрос 3: покрывающий индекс (Index Only Scan)\nДо: Index Scan, Heap Fetches: 850\nПосле CREATE INDEX ... INCLUDE (id, customer_id, total):\n -> Index Only Scan on orders (Heap Fetches: 0)\n\n-- Частичный индекс (незавершённые заказы)\nCREATE INDEX idx_orders_active ... WHERE status NOT IN (\'completed\',\'cancelled\');\n-- CREATE INDEX\nРазмер полного индекса: 42 MB\nРазмер частичного индекса: 8 MB (только 19% строк)\n\n-- GIN индекс для тегов\nCREATE INDEX idx_products_tags USING GIN (tags); -- CREATE INDEX\nBitmap Index Scan using idx_products_tags (время: 0.3ms вместо 120ms)\n\n-- Неиспользуемые индексы (pg_stat_user_indexes, idx_scan=0)\n table_name | index_name         | times_used | size\n------------+--------------------+------------+------\n orders     | idx_orders_old     |          0 | 18 MB\n products   | idx_prod_legacy    |          0 | 7 MB\nDROP INDEX CONCURRENTLY idx_orders_old; -- DROP INDEX\n\n-- pg_stat_statements: топ-5 по total_exec_time\n total_sec | calls | avg_ms | query\n----------+-------+--------+------------------------------------\n   4821.3 | 95203 |   50.6 | SELECT * FROM orders WHERE ...\n   1204.7 | 12048 |  100.0 | SELECT o.*, c.* FROM orders o JOIN\n    890.2 |  8901 |  100.0 | UPDATE orders SET status=...\n    445.1 | 44510 |   10.0 | SELECT COUNT(*) FROM order_items\n    312.0 |  3120 |  100.0 | INSERT INTO order_items VALUES',
      solution: '-- 1. Убрать функцию из WHERE\n-- Было (Seq Scan):\nSELECT * FROM orders WHERE EXTRACT(YEAR FROM created_at) = 2024;\n\n-- Стало (Index Scan):\nSELECT * FROM orders\nWHERE created_at >= \'2024-01-01\' AND created_at < \'2025-01-01\';\n\nCREATE INDEX idx_orders_created_at ON orders(created_at);\n\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT * FROM orders\nWHERE created_at >= \'2024-01-01\' AND created_at < \'2025-01-01\';\n-- Ожидаем: Index Scan using idx_orders_created_at\n\n-- 2. Поиск по email\n-- До индекса:\nEXPLAIN (ANALYZE, BUFFERS, FORMAT TEXT)\nSELECT id, name FROM users WHERE email = \'aliya@mail.ru\';\n-- Seq Scan: 500ms на 1M строк\n\nCREATE INDEX idx_users_email ON users(email);\n\n-- После индекса:\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT id, name FROM users WHERE email = \'aliya@mail.ru\';\n-- Index Scan: 0.1ms\n\n-- 3. Покрывающий индекс для JOIN\n-- Медленный запрос:\nSELECT o.id, o.total, c.name, c.email\nFROM orders o\nJOIN customers c ON o.customer_id = c.id\nWHERE o.status = \'completed\'\nORDER BY o.created_at DESC\nLIMIT 50;\n\n-- Покрывающий индекс (все нужные поля):\nCREATE INDEX idx_orders_covering ON orders(status, created_at DESC)\nINCLUDE (id, customer_id, total);\n\nCREATE INDEX idx_customers_covering ON customers(id)\nINCLUDE (name, email);\n\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT o.id, o.total, c.name, c.email\nFROM orders o\nJOIN customers c ON o.customer_id = c.id\nWHERE o.status = \'completed\'\nORDER BY o.created_at DESC LIMIT 50;\n-- Ожидаем: Index Only Scan (Heap Fetches: 0)\n\n-- 4. Частичный индекс\nCREATE INDEX idx_orders_active ON orders(customer_id, created_at)\nWHERE status NOT IN (\'completed\', \'cancelled\');\n\n-- Используется только для:\nSELECT * FROM orders\nWHERE customer_id = 42 AND status = \'pending\';\n-- Размер индекса в 5-10 раз меньше полного!\n\n-- 5. GIN для тегов\nALTER TABLE products ADD COLUMN IF NOT EXISTS tags TEXT[] DEFAULT ARRAY[]::TEXT[];\n\nCREATE INDEX idx_products_tags ON products USING GIN (tags);\n\n-- Теперь быстрый поиск по тегам:\nSELECT id, name FROM products WHERE tags @> ARRAY[\'смартфон\', \'5G\'];\nSELECT id, name FROM products WHERE tags && ARRAY[\'распродажа\'];\n\nEXPLAIN ANALYZE SELECT id, name FROM products\nWHERE tags @> ARRAY[\'смартфон\'];\n-- Bitmap Index Scan using idx_products_tags\n\n-- 6. Неиспользуемые индексы\nSELECT\n    relname AS table_name,\n    indexrelname AS index_name,\n    idx_scan AS times_used,\n    pg_size_pretty(pg_relation_size(indexrelid)) AS size\nFROM pg_stat_user_indexes\nWHERE idx_scan = 0\n  AND relname NOT LIKE \'pg_%\'\n  AND indexrelname NOT LIKE \'%_pkey\'  -- Не удаляем PRIMARY KEY!\n  AND indexrelname NOT LIKE \'%_unique\' -- Не удаляем UNIQUE\nORDER BY pg_relation_size(indexrelid) DESC;\n\n-- Удаление неиспользуемого индекса:\n-- DROP INDEX CONCURRENTLY idx_old_unused;  -- CONCURRENTLY = без блокировки!\n\n-- 7. pg_stat_statements\nSELECT\n    ROUND(total_exec_time::NUMERIC / 1000, 2) AS total_sec,\n    calls,\n    ROUND(mean_exec_time::NUMERIC, 1) AS avg_ms,\n    LEFT(query, 80) AS query\nFROM pg_stat_statements\nORDER BY total_exec_time DESC\nLIMIT 5;',
      explanation: 'Оптимизация запросов: 1) убери функции с индексированных столбцов в WHERE, 2) покрывающие индексы устраняют Heap Fetches, 3) частичные индексы меньше по размеру и быстрее. Всегда измеряй до/после через EXPLAIN ANALYZE. pg_stat_statements покажет где реально тратится время.'
    }
  ]
}
