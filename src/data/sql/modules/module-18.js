export default {
  id: 18,
  title: 'Триггеры',
  description: 'Триггеры — автоматический код на события INSERT/UPDATE/DELETE. BEFORE/AFTER триггеры, FOR EACH ROW vs STATEMENT, доступ к NEW и OLD, практические паттерны: аудит, ограничения, автозаполнение.',
  lessons: [
    {
      id: 1,
      title: 'Что такое триггер и синтаксис CREATE TRIGGER',
      type: 'theory',
      content: [
        { type: 'text', value: 'Триггер — код, автоматически вызываемый при событии INSERT, UPDATE или DELETE на таблице. Не нужно явно вызывать — PostgreSQL запускает триггер сам. Используется для аудита, валидации, автозаполнения, синхронизации данных.' },
        { type: 'code', language: 'sql', value: '-- Создание триггерной функции\nCREATE OR REPLACE FUNCTION trg_set_updated_at()\nRETURNS TRIGGER\nLANGUAGE plpgsql\nAS $$\nBEGIN\n    -- NEW: новые значения строки (при INSERT/UPDATE)\n    -- OLD: старые значения строки (при UPDATE/DELETE)\n    NEW.updated_at := NOW();\n    RETURN NEW;  -- Вернуть модифицированную строку\nEND;\n$$;\n\n-- Создание триггера на таблицу\nCREATE TRIGGER set_updated_at\n    BEFORE UPDATE ON employees  -- Событие: перед UPDATE на таблице employees\n    FOR EACH ROW               -- Для каждой изменённой строки\n    EXECUTE FUNCTION trg_set_updated_at();\n\n-- Теперь при каждом UPDATE updated_at автоматически обновляется:\nUPDATE employees SET salary = 150000 WHERE id = 5;\n-- updated_at == NOW() без явного указания!\n\n-- Анатомия CREATE TRIGGER:\n-- CREATE [CONSTRAINT] TRIGGER имя\n-- { BEFORE | AFTER | INSTEAD OF }\n-- { INSERT | UPDATE | DELETE | TRUNCATE } [OR ...]\n-- ON таблица\n-- [FOR EACH { ROW | STATEMENT }]\n-- [WHEN (условие)]\n-- EXECUTE FUNCTION функция();\n\n-- UPDATE с WHEN: триггер только при реальном изменении salary\nCREATE TRIGGER audit_salary_change\n    AFTER UPDATE OF salary ON employees  -- Только при изменении salary\n    FOR EACH ROW\n    WHEN (OLD.salary IS DISTINCT FROM NEW.salary)  -- Только если значение изменилось!\n    EXECUTE FUNCTION trg_audit_salary();\n\n-- Просмотр триггеров:\nSELECT trigger_name, event_manipulation, event_object_table, action_timing\nFROM information_schema.triggers\nWHERE trigger_schema = \'public\'\nORDER BY event_object_table, action_timing;' },
        { type: 'tip', value: 'BEFORE триггер может модифицировать NEW (изменить значения перед сохранением) или вернуть NULL (отменить операцию). AFTER триггер не может изменить данные текущей строки — только выполнить побочные эффекты.' }
      ]
    },
    {
      id: 2,
      title: 'BEFORE vs AFTER, ROW vs STATEMENT',
      type: 'theory',
      content: [
        { type: 'text', value: 'Четыре комбинации: BEFORE/AFTER × ROW/STATEMENT. Каждая подходит для разных задач. ROW: доступ к NEW/OLD. STATEMENT: один вызов на весь запрос.' },
        { type: 'code', language: 'sql', value: '-- BEFORE FOR EACH ROW:\n-- + Может изменить NEW (модифицировать данные перед сохранением)\n-- + Может отменить операцию (RETURN NULL)\n-- + Может выполнить валидацию\n-- Используй для: автозаполнение полей, валидация, нормализация\n\nCREATE OR REPLACE FUNCTION trg_normalize_email()\nRETURNS TRIGGER AS $$\nBEGIN\n    -- Нормализовать email: в нижний регистр, убрать пробелы\n    NEW.email := LOWER(TRIM(NEW.email));\n\n    -- Валидация\n    IF NEW.email !~ \'^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}$\' THEN\n        RAISE EXCEPTION \'Неверный формат email: %\', NEW.email\n            USING ERRCODE = \'23514\';\n    END IF;\n\n    RETURN NEW;  -- Вернуть модифицированную строку\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER normalize_email\n    BEFORE INSERT OR UPDATE ON users\n    FOR EACH ROW EXECUTE FUNCTION trg_normalize_email();\n\n-- AFTER FOR EACH ROW:\n-- + Видит уже сохранённые данные (можно ссылаться на ID вставленной строки)\n-- + Для побочных эффектов: уведомления, аудит, обновление связанных таблиц\n-- - Не может изменить текущую строку\n\nCREATE OR REPLACE FUNCTION trg_notify_new_order()\nRETURNS TRIGGER AS $$\nBEGIN\n    -- ID уже назначен (SERIAL сработал)\n    INSERT INTO notifications (type, reference_id, message, created_at)\n    VALUES (\n        \'new_order\',\n        NEW.id,\n        FORMAT(\'Новый заказ #%s на сумму %s ₸\', NEW.id, NEW.total),\n        NOW()\n    );\n    RETURN NULL;  -- AFTER триггер: возврат игнорируется (используют NULL)\nEND;\n$$ LANGUAGE plpgsql;\n\n-- BEFORE FOR EACH STATEMENT:\n-- Один вызов на весь INSERT/UPDATE/DELETE, НЕТ NEW/OLD\n-- Редко используется (лучше AFTER STATEMENT обычно)\n\n-- AFTER FOR EACH STATEMENT:\n-- После всей операции, один раз\n-- Для: обновление агрегатов, инвалидация кэша\n\nCREATE OR REPLACE FUNCTION trg_invalidate_cache()\nRETURNS TRIGGER AS $$\nBEGIN\n    -- TG_TABLE_NAME: имя таблицы, TG_OP: операция (INSERT/UPDATE/DELETE)\n    PERFORM pg_notify(\n        \'cache_invalidation\',\n        json_build_object(\'table\', TG_TABLE_NAME, \'op\', TG_OP)::TEXT\n    );\n    RETURN NULL;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER invalidate_product_cache\n    AFTER INSERT OR UPDATE OR DELETE ON products\n    FOR EACH STATEMENT\n    EXECUTE FUNCTION trg_invalidate_cache();' },
        { type: 'note', value: 'Специальные переменные триггера: TG_OP (\'INSERT\'|\'UPDATE\'|\'DELETE\'), TG_TABLE_NAME, TG_WHEN (\'BEFORE\'|\'AFTER\'), TG_LEVEL (\'ROW\'|\'STATEMENT\'), TG_NARGS, TG_ARGV[] (аргументы). В BEFORE ROW триггере RETURN NULL отменяет операцию.' }
      ]
    },
    {
      id: 3,
      title: 'Практический паттерн: Аудит изменений',
      type: 'theory',
      content: [
        { type: 'text', value: 'Аудит — запись всех изменений данных с указанием кто, когда и что изменил. Триггеры идеальны для этого: изменения записываются автоматически, приложение не может их пропустить.' },
        { type: 'code', language: 'sql', value: '-- Универсальная таблица аудита\nCREATE TABLE audit_log (\n    id          BIGSERIAL PRIMARY KEY,\n    table_name  TEXT NOT NULL,\n    operation   TEXT NOT NULL CHECK (operation IN (\'INSERT\', \'UPDATE\', \'DELETE\')),\n    old_data    JSONB,    -- Старые значения (NULL для INSERT)\n    new_data    JSONB,    -- Новые значения (NULL для DELETE)\n    changed_by  TEXT DEFAULT current_user,\n    changed_at  TIMESTAMPTZ DEFAULT NOW(),\n    app_user    TEXT,     -- Пользователь приложения (из session)\n    ip_address  INET\n);\n\n-- Универсальная триггерная функция (работает для любой таблицы!)\nCREATE OR REPLACE FUNCTION trg_audit()\nRETURNS TRIGGER\nLANGUAGE plpgsql\nSECURITY DEFINER  -- Запускается с правами создателя (может писать в audit_log)\nAS $$\nBEGIN\n    INSERT INTO audit_log (table_name, operation, old_data, new_data, app_user)\n    VALUES (\n        TG_TABLE_NAME,\n        TG_OP,\n        CASE TG_OP WHEN \'INSERT\' THEN NULL ELSE row_to_json(OLD)::JSONB END,\n        CASE TG_OP WHEN \'DELETE\' THEN NULL ELSE row_to_json(NEW)::JSONB END,\n        current_setting(\'app.current_user\', TRUE)  -- Из настроек сессии\n    );\n\n    -- Для BEFORE: вернуть NEW (INSERT/UPDATE) или OLD (DELETE)\n    -- Для AFTER: возврат игнорируется\n    RETURN COALESCE(NEW, OLD);\nEND;\n$$;\n\n-- Подключить аудит к таблице\nCREATE TRIGGER audit_employees\n    AFTER INSERT OR UPDATE OR DELETE ON employees\n    FOR EACH ROW\n    EXECUTE FUNCTION trg_audit();\n\nCREATE TRIGGER audit_orders\n    AFTER INSERT OR UPDATE OR DELETE ON orders\n    FOR EACH ROW\n    EXECUTE FUNCTION trg_audit();\n\n-- Просмотр истории изменений:\nSELECT\n    operation,\n    old_data ->> \'salary\' AS old_salary,\n    new_data ->> \'salary\' AS new_salary,\n    changed_by,\n    changed_at\nFROM audit_log\nWHERE table_name = \'employees\'\n  AND (old_data ->> \'id\')::INTEGER = 42\n  AND operation = \'UPDATE\'\nORDER BY changed_at DESC;\n\n-- Установить app_user в приложении:\n-- SET app.current_user = \'aliya.bekova@company.kz\';\n-- PostgreSQL сохраняет эту настройку для текущей сессии' },
        { type: 'tip', value: 'row_to_json() конвертирует строку таблицы в JSON — это позволяет одной триггерной функции работать с любой таблицей. current_setting(\'app.current_user\', TRUE) читает переменную сессии (TRUE = не вызывать ошибку если не задана).' }
      ]
    },
    {
      id: 4,
      title: 'INSTEAD OF триггеры для View',
      type: 'theory',
      content: [
        { type: 'text', value: 'INSTEAD OF триггеры заменяют операцию на View. Позволяют сделать сложный View обновляемым: INSERT/UPDATE/DELETE перехватываются триггером и транслируются в операции с базовыми таблицами.' },
        { type: 'code', language: 'sql', value: '-- Сложный View (обычно не обновляемый):\nCREATE VIEW employee_full AS\nSELECT\n    e.id,\n    e.name,\n    e.salary,\n    d.name AS department_name,\n    d.id   AS department_id\nFROM employees e\nJOIN departments d ON e.department_id = d.id;\n\n-- INSTEAD OF INSERT: INSERT через View\nCREATE OR REPLACE FUNCTION trg_employee_full_insert()\nRETURNS TRIGGER\nLANGUAGE plpgsql\nAS $$\nDECLARE\n    v_dept_id INTEGER;\nBEGIN\n    -- Найти или создать отдел\n    SELECT id INTO v_dept_id FROM departments WHERE name = NEW.department_name;\n\n    IF NOT FOUND THEN\n        INSERT INTO departments (name) VALUES (NEW.department_name)\n        RETURNING id INTO v_dept_id;\n    END IF;\n\n    -- Вставить сотрудника\n    INSERT INTO employees (name, salary, department_id)\n    VALUES (NEW.name, NEW.salary, v_dept_id);\n\n    RETURN NEW;\nEND;\n$$;\n\nCREATE TRIGGER employee_full_insert\n    INSTEAD OF INSERT ON employee_full\n    FOR EACH ROW\n    EXECUTE FUNCTION trg_employee_full_insert();\n\n-- Теперь INSERT работает через View!\nINSERT INTO employee_full (name, salary, department_name)\nVALUES (\'Алия Сейт\', 120000, \'Инженерный отдел\');\n-- Автоматически: нашёл отдел "Инженерный отдел" и вставил сотрудника\n\n-- INSTEAD OF UPDATE:\nCREATE OR REPLACE FUNCTION trg_employee_full_update()\nRETURNS TRIGGER\nLANGUAGE plpgsql\nAS $$\nBEGIN\n    UPDATE employees\n    SET name   = NEW.name,\n        salary = NEW.salary\n    WHERE id = OLD.id;\n\n    IF NEW.department_name <> OLD.department_name THEN\n        UPDATE employees\n        SET department_id = (SELECT id FROM departments WHERE name = NEW.department_name)\n        WHERE id = OLD.id;\n    END IF;\n\n    RETURN NEW;\nEND;\n$$;\n\nCREATE TRIGGER employee_full_update\n    INSTEAD OF UPDATE ON employee_full\n    FOR EACH ROW\n    EXECUTE FUNCTION trg_employee_full_update();\n\n-- Теперь UPDATE через View работает:\nUPDATE employee_full SET salary = 150000 WHERE id = 42;' },
        { type: 'note', value: 'INSTEAD OF триггеры существуют ТОЛЬКО для View (не для таблиц). Они полностью заменяют операцию — исходное действие не выполняется. Это стандартный способ сделать обновляемый View с JOIN.' }
      ]
    },
    {
      id: 5,
      title: 'Управление триггерами и производительность',
      type: 'theory',
      content: [
        { type: 'text', value: 'Триггеры влияют на производительность — каждое событие вызывает дополнительный код. Умелое управление: отключение при массовых загрузках, избегание каскадных цепочек, правильный порядок выполнения.' },
        { type: 'code', language: 'sql', value: '-- Отключить/включить триггер (без удаления)\nALTER TABLE employees DISABLE TRIGGER audit_employees;\n-- Массовая загрузка данных (без аудита)\nCOPY employees FROM \'/tmp/employees.csv\' CSV HEADER;\nALTER TABLE employees ENABLE TRIGGER audit_employees;\n\n-- Отключить ВСЕ триггеры (только superuser/table owner):\nALTER TABLE employees DISABLE TRIGGER ALL;\nALTER TABLE employees ENABLE TRIGGER ALL;\n\n-- Удалить триггер\nDROP TRIGGER IF EXISTS audit_employees ON employees;\n\n-- Порядок выполнения при нескольких триггерах:\n-- 1. BEFORE FOR EACH STATEMENT (в алфавитном порядке имён)\n-- 2. BEFORE FOR EACH ROW (в алфавитном порядке имён)\n-- 3. Сама операция\n-- 4. AFTER FOR EACH ROW (в алфавитном порядке имён)\n-- 5. AFTER FOR EACH STATEMENT (в алфавитном порядке имён)\n\n-- Рекомендация по именованию для управления порядком:\n-- 001_validate_..., 002_normalize_..., 099_audit_...\n\n-- Проверка производительности триггеров:\nEXPLAIN (ANALYZE, BUFFERS)\nINSERT INTO orders (customer_id, total) VALUES (1, 5000);\n-- В плане: Trigger for constraint, Trigger audit_orders\n-- Видно время каждого триггера!\n\n-- Антипаттерны триггеров:\n-- 1. Каскадные триггеры: А -> Б -> В -> А (бесконечный цикл)\n-- 2. Тяжёлая логика в триггере на горячей таблице (миллионы вставок/день)\n-- 3. HTTP-запросы в триггере (используй NOTIFY + воркер)\n-- 4. Мьютабельные функции (VOLATILE) в BEFORE STATEMENT на больших батчах\n\n-- Безопасная рекурсия: флаг для предотвращения бесконечного цикла\nCREATE OR REPLACE FUNCTION trg_safe_recursive()\nRETURNS TRIGGER AS $$\nBEGIN\n    -- Проверить флаг рекурсии\n    IF current_setting(\'app.in_trigger\', TRUE) = \'true\' THEN\n        RETURN NEW;  -- Уже внутри триггера, пропустить\n    END IF;\n\n    SET LOCAL app.in_trigger = \'true\';  -- Установить флаг\n    -- Выполнить логику...\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;' },
        { type: 'warning', value: 'Каскадные триггеры — один из самых опасных антипаттернов. Триггер на таблице А изменяет таблицу Б, триггер на Б изменяет А — бесконечный цикл и переполнение стека. Всегда проверяй нет ли цикличности при добавлении нового триггера.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Система аудита и автоматизации',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создай полную систему триггеров для интернет-магазина.',
      requirements: [
        'Триггер updated_at: автоматически обновляет updated_at при каждом UPDATE на таблицах orders и products',
        'Триггер аудита: записывает все INSERT/UPDATE/DELETE в audit_log с JSONB данными и app_user из сессии',
        'Триггер валидации: BEFORE INSERT/UPDATE на orders — проверяет что total > 0 и customer существует',
        'Триггер синхронизации: AFTER INSERT на order_items — обновляет total в таблице orders',
        'INSTEAD OF триггер на View orders_with_customer для INSERT: создаёт заказ через View',
        'Проверь производительность: EXPLAIN ANALYZE для INSERT в orders с включёнными триггерами'
      ],
      hint: 'Для синхронизации total: в AFTER INSERT на order_items пересчитай SUM(quantity * unit_price) для order_id = NEW.order_id и обнови orders.total. Это избегает дрейфа данных.',
      expectedOutput: 'Создание триггеров:\nCREATE TRIGGER trg_audit_products    -- AFTER INSERT/UPDATE/DELETE\nCREATE TRIGGER trg_sync_order_total  -- AFTER INSERT/UPDATE/DELETE ON order_items\nCREATE TRIGGER trg_validate_stock    -- BEFORE INSERT ON order_items\nCREATE TRIGGER trg_update_timestamp  -- BEFORE UPDATE ON products\n\nINSERT INTO order_items (order_id=1, product_id=3, quantity=2, unit_price=85000):\n-- trg_validate_stock: stock=12 >= 2 -- OK\n-- trg_sync_order_total: orders.total = SUM(85000*2) = 170000\nSELECT total FROM orders WHERE id=1: 170000.00\n\nINSERT INTO order_items с quantity=100 (stock=5):\nERROR: Недостаточно товара на складе: product_id=3, запрошено=100, доступно=5\n\nUPDATE products SET price=90000 WHERE id=3:\n-- trg_update_timestamp: updated_at = NOW()\n-- trg_audit_products: INSERT INTO product_audit (action=\'UPDATE\', old_price=85000, new_price=90000)\n\nSELECT * FROM product_audit ORDER BY changed_at DESC LIMIT 2:\n product_id | action | old_price | new_price | changed_at\n------------+--------+-----------+-----------+-------------------\n          3 | UPDATE |  85000.00 |  90000.00 | 2026-03-21 10:05',
      solution: '-- 1. Триггер updated_at (универсальный)\nCREATE OR REPLACE FUNCTION trg_set_updated_at()\nRETURNS TRIGGER AS $$\nBEGIN\n    NEW.updated_at := NOW();\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER set_updated_at_orders\n    BEFORE UPDATE ON orders\n    FOR EACH ROW EXECUTE FUNCTION trg_set_updated_at();\n\nCREATE TRIGGER set_updated_at_products\n    BEFORE UPDATE ON products\n    FOR EACH ROW EXECUTE FUNCTION trg_set_updated_at();\n\n-- 2. Универсальный аудит\nCREATE TABLE audit_log (\n    id         BIGSERIAL PRIMARY KEY,\n    table_name TEXT NOT NULL,\n    operation  TEXT NOT NULL,\n    old_data   JSONB,\n    new_data   JSONB,\n    db_user    TEXT DEFAULT current_user,\n    app_user   TEXT,\n    changed_at TIMESTAMPTZ DEFAULT NOW()\n);\n\nCREATE OR REPLACE FUNCTION trg_audit()\nRETURNS TRIGGER AS $$\nBEGIN\n    INSERT INTO audit_log (table_name, operation, old_data, new_data, app_user)\n    VALUES (\n        TG_TABLE_NAME,\n        TG_OP,\n        CASE WHEN TG_OP = \'INSERT\' THEN NULL ELSE row_to_json(OLD)::JSONB END,\n        CASE WHEN TG_OP = \'DELETE\' THEN NULL ELSE row_to_json(NEW)::JSONB END,\n        current_setting(\'app.current_user\', TRUE)\n    );\n    RETURN COALESCE(NEW, OLD);\nEND;\n$$ LANGUAGE plpgsql SECURITY DEFINER;\n\nCREATE TRIGGER audit_orders\n    AFTER INSERT OR UPDATE OR DELETE ON orders\n    FOR EACH ROW EXECUTE FUNCTION trg_audit();\n\n-- 3. Триггер валидации\nCREATE OR REPLACE FUNCTION trg_validate_order()\nRETURNS TRIGGER AS $$\nBEGIN\n    IF NEW.total <= 0 THEN\n        RAISE EXCEPTION \'Сумма заказа должна быть положительной, получено: %\', NEW.total\n            USING ERRCODE = \'23514\';\n    END IF;\n\n    IF NOT EXISTS (SELECT 1 FROM customers WHERE id = NEW.customer_id) THEN\n        RAISE EXCEPTION \'Клиент % не существует\', NEW.customer_id\n            USING ERRCODE = \'23503\';\n    END IF;\n\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER validate_order\n    BEFORE INSERT OR UPDATE ON orders\n    FOR EACH ROW EXECUTE FUNCTION trg_validate_order();\n\n-- 4. Триггер синхронизации total\nCREATE OR REPLACE FUNCTION trg_sync_order_total()\nRETURNS TRIGGER AS $$\nDECLARE\n    v_order_id INTEGER;\n    v_new_total DECIMAL;\nBEGIN\n    v_order_id := COALESCE(NEW.order_id, OLD.order_id);\n\n    SELECT COALESCE(SUM(quantity * unit_price), 0)\n    INTO v_new_total\n    FROM order_items\n    WHERE order_id = v_order_id;\n\n    UPDATE orders SET total = v_new_total WHERE id = v_order_id;\n\n    RETURN COALESCE(NEW, OLD);\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER sync_order_total\n    AFTER INSERT OR UPDATE OR DELETE ON order_items\n    FOR EACH ROW EXECUTE FUNCTION trg_sync_order_total();\n\n-- 5. INSTEAD OF INSERT на View\nCREATE VIEW orders_with_customer AS\nSELECT o.id, o.total, o.status, o.created_at,\n       c.name AS customer_name, c.email\nFROM orders o\nJOIN customers c ON o.customer_id = c.id;\n\nCREATE OR REPLACE FUNCTION trg_orders_view_insert()\nRETURNS TRIGGER AS $$\nDECLARE\n    v_customer_id INTEGER;\nBEGIN\n    SELECT id INTO v_customer_id\n    FROM customers WHERE email = NEW.email;\n\n    IF NOT FOUND THEN\n        RAISE EXCEPTION \'Клиент с email % не найден\', NEW.email;\n    END IF;\n\n    INSERT INTO orders (customer_id, total, status)\n    VALUES (v_customer_id, NEW.total, COALESCE(NEW.status, \'pending\'));\n\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER orders_view_insert\n    INSTEAD OF INSERT ON orders_with_customer\n    FOR EACH ROW EXECUTE FUNCTION trg_orders_view_insert();\n\n-- Тест:\nINSERT INTO orders_with_customer (total, email)\nVALUES (15000, \'aliya@mail.ru\');\n\n-- 6. Проверка производительности\nEXPLAIN (ANALYZE, BUFFERS)\nINSERT INTO orders (customer_id, total) VALUES (1, 5000);',
      explanation: 'Триггерная система создаёт "невидимую" инфраструктуру: updated_at обновляется всегда, аудит не пропустит ни одного изменения, total в orders всегда актуален. Ключевое правило: триггеры должны быть быстрыми и не иметь побочных эффектов во внешних системах.'
    }
  ]
}
