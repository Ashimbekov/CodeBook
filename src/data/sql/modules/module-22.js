export default {
  id: 22,
  title: 'SQL Best Practices',
  description: 'Лучшие практики разработки с SQL и PostgreSQL: именование, безопасность, миграции, соглашения по коду, обработка ошибок, мониторинг и поддержка баз данных в продакшене.',
  lessons: [
    {
      id: 1,
      title: 'Конвенции именования и стиль кода',
      type: 'theory',
      content: [
        { type: 'text', value: 'Единый стиль кода критически важен для поддерживаемости. PostgreSQL не чувствителен к регистру для ключевых слов, но чувствителен к регистру для идентификаторов в кавычках. Придерживайся одного соглашения по именованию.' },
        { type: 'code', language: 'sql', value: '-- ХОРОШЕЕ именование:\n-- Таблицы: lowercase_snake_case, множественное число\n-- Столбцы: lowercase_snake_case\n-- Индексы: idx_{table}_{column(s)}\n-- FK: fk_{table}_{referenced_table}\n-- Функции: verb_noun или get_/set_/calc_/create_\n\nCREATE TABLE user_profiles (         -- НЕ UserProfiles, НЕ user_profile\n    id          BIGSERIAL PRIMARY KEY,\n    user_id     BIGINT NOT NULL,\n    first_name  VARCHAR(100) NOT NULL,  -- НЕ firstname, НЕ FirstName\n    last_name   VARCHAR(100) NOT NULL,\n    birth_date  DATE,\n    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    CONSTRAINT fk_user_profiles_users\n        FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE\n);\n\nCREATE INDEX idx_user_profiles_user_id ON user_profiles(user_id);\n\n-- Стиль запросов: ключевые слова в UPPER CASE\n-- ХОРОШО:\nSELECT\n    u.id,\n    u.email,\n    up.first_name || \' \' || up.last_name AS full_name,\n    COUNT(o.id) AS orders_count\nFROM users u\nJOIN user_profiles up ON u.id = up.user_id\nLEFT JOIN orders o    ON u.id = o.customer_id\nWHERE u.created_at >= \'2024-01-01\'\n  AND u.status = \'active\'\nGROUP BY u.id, u.email, up.first_name, up.last_name\nHAVING COUNT(o.id) > 0\nORDER BY orders_count DESC\nLIMIT 100;\n\n-- Псевдонимы: короткие и понятные (u, up, o — ОК для локального запроса)\n-- В сложных запросах: users u, user_profiles up, orders o\n\n-- НЕ: однобуквенные для больших запросов\n-- НЕ: a, b, c, d — непонятно к чему относятся\n\n-- Комментарии в SQL:\n-- Однострочный: --\n/* Многострочный: /* ... */ */\n\n-- Документируй сложные запросы!\n-- Плохо: SELECT * FROM orders o JOIN customers c ON o.customer_id = c.id WHERE ...\n-- Хорошо:\n-- Получить заказы VIP клиентов за последние 30 дней\n-- VIP = total_spent > 100000 за всё время\nSELECT o.*\nFROM orders o\nJOIN (\n    -- Подзапрос: определить VIP клиентов\n    SELECT customer_id\n    FROM orders\n    GROUP BY customer_id\n    HAVING SUM(total) > 100000\n) vip ON o.customer_id = vip.customer_id\nWHERE o.created_at >= NOW() - INTERVAL \'30 days\';' },
        { type: 'tip', value: 'Используй snake_case для всего: таблицы, столбцы, функции, индексы. Избегай зарезервированных слов PostgreSQL как имён столбцов (name, value, date, type, order). Если нужно — используй кавычки: "order", но лучше переименуй.' }
      ]
    },
    {
      id: 2,
      title: 'Безопасность: SQL-инъекции и права доступа',
      type: 'theory',
      content: [
        { type: 'text', value: 'SQL-инъекция — одна из самых распространённых уязвимостей. PostgreSQL предоставляет инструменты для защиты: параметризованные запросы, роли и минимальные привилегии, Row-Level Security.' },
        { type: 'code', language: 'sql', value: '-- НИКОГДА не строй запросы конкатенацией строк:\n-- УЯЗВИМО:\n-- query = "SELECT * FROM users WHERE id = " + user_input\n-- user_input = "1 OR 1=1 --"  ->  все пользователи!\n-- user_input = "1; DROP TABLE users; --"  -> удаление таблицы!\n\n-- В PL/pgSQL: EXECUTE с параметрами\nCREATE OR REPLACE FUNCTION get_user(p_email TEXT)\nRETURNS TABLE (id INTEGER, name TEXT)\nLANGUAGE plpgsql\nAS $$\nBEGIN\n    -- БЕЗОПАСНО: параметр $1, не конкатенация\n    RETURN QUERY\n        SELECT u.id, u.name FROM users u\n        WHERE u.email = p_email;  -- Прямая подстановка безопасна в PL/pgSQL\nEND;\n$$;\n\n-- Динамический SQL БЕЗОПАСНО (с format + %L для quoted literal):\nCREATE OR REPLACE FUNCTION search_table(\n    p_table TEXT,\n    p_column TEXT,\n    p_value TEXT\n)\nRETURNS TABLE (result TEXT)\nLANGUAGE plpgsql\nAS $$\nBEGIN\n    -- %I: quoted identifier (для имён таблиц/столбцов)\n    -- %L: quoted literal (для значений) - защищает от инъекции!\n    RETURN QUERY EXECUTE\n        FORMAT(\'SELECT %I::TEXT FROM %I WHERE %I = %L\',\n               p_column, p_table, p_column, p_value);\nEND;\n$$;\n\n-- Роли и минимальные привилегии (Principle of Least Privilege)\nCREATE ROLE app_readonly;\nGRANT CONNECT ON DATABASE mydb TO app_readonly;\nGRANT USAGE ON SCHEMA public TO app_readonly;\nGRANT SELECT ON ALL TABLES IN SCHEMA public TO app_readonly;\n-- Только чтение!\n\nCREATE ROLE app_write;\nGRANT CONNECT ON DATABASE mydb TO app_write;\nGRANT USAGE ON SCHEMA public TO app_write;\nGRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO app_write;\nGRANT USAGE ON ALL SEQUENCES IN SCHEMA public TO app_write;\n\n-- Row-Level Security: каждый пользователь видит только свои данные\nALTER TABLE orders ENABLE ROW LEVEL SECURITY;\n\nCREATE POLICY orders_user_policy ON orders\n    FOR ALL\n    TO app_write\n    USING (customer_id = current_setting(\'app.current_user_id\')::INTEGER);\n\n-- Запрет на прямой SELECT (только через view):\nREVOKE SELECT ON employees FROM PUBLIC;\nGRANT SELECT ON employee_public_info TO PUBLIC;  -- Только через view!' },
        { type: 'warning', value: 'FORMAT с %L экранирует значения для использования как SQL литерал. Используй %I для имён идентификаторов (таблицы, столбцы). Никогда не конкатенируй пользовательский ввод в EXECUTE строку напрямую — это уязвимость к SQL-инъекции.' }
      ]
    },
    {
      id: 3,
      title: 'Миграции: управление изменениями схемы',
      type: 'theory',
      content: [
        { type: 'text', value: 'Миграции — версионированные изменения схемы базы данных. Без миграций синхронизация схемы БД между разработчиками и средами (dev/staging/prod) становится хаосом.' },
        { type: 'code', language: 'sql', value: '-- Принципы хороших миграций:\n\n-- 1. ОБРАТИМЫЕ миграции: всегда можно откатить (UP и DOWN)\n-- 001_add_users_table.up.sql\nCREATE TABLE users (\n    id         BIGSERIAL PRIMARY KEY,\n    email      VARCHAR(255) UNIQUE NOT NULL,\n    created_at TIMESTAMPTZ DEFAULT NOW()\n);\n\n-- 001_add_users_table.down.sql\nDROP TABLE IF EXISTS users;\n\n-- 2. ДОБАВЛЯЙ столбцы как nullable сначала\n-- 002_add_phone_to_users.up.sql\nALTER TABLE users ADD COLUMN phone VARCHAR(50);  -- nullable!\n-- Потом заполни данные:\nUPDATE users SET phone = \'Не указан\' WHERE phone IS NULL;\n-- Потом добавь NOT NULL:\nALTER TABLE users ALTER COLUMN phone SET NOT NULL;\n-- Эти три шага = БЕЗОПАСНАЯ миграция (без downtime)\n\n-- 3. КОНКУРЕНТНОЕ создание индексов (без блокировки!)\nCREATE INDEX CONCURRENTLY idx_users_email ON users(email);\n-- CONCURRENTLY: создаёт индекс без блокировки таблицы на запись\n-- Медленнее но не мешает работе приложения в продакшене\n-- НЕЛЬЗЯ в транзакции! Запускай вне BEGIN...COMMIT\n\n-- 4. Переименование столбца безопасно:\n-- Шаг 1: добавить новый столбец\nALTER TABLE users ADD COLUMN full_name TEXT;\n-- Шаг 2: заполнить данными (триггер или одноразовый скрипт)\nUPDATE users SET full_name = first_name || \' \' || last_name;\n-- Шаг 3: обновить приложение (не использует старый столбец)\n-- Шаг 4: удалить старый столбец\nALTER TABLE users DROP COLUMN first_name;\nALTER TABLE users DROP COLUMN last_name;\n\n-- 5. Большие DELETE или UPDATE: батчами!\n-- НЕ: DELETE FROM logs WHERE created_at < NOW() - INTERVAL \'1 year\';\n-- (блокирует таблицу на долгое время, раздувает таблицу транзакций)\n\n-- ДА: удалять батчами\nDO $$\nBEGIN\n    LOOP\n        DELETE FROM logs\n        WHERE id IN (\n            SELECT id FROM logs\n            WHERE created_at < NOW() - INTERVAL \'1 year\'\n            LIMIT 1000\n        );\n        EXIT WHEN NOT FOUND;\n        PERFORM pg_sleep(0.1);  -- Небольшая пауза\n    END LOOP;\nEND;\n$$;\n\n-- 6. Версионирование схемы:\nCREATE TABLE schema_migrations (\n    version    VARCHAR(50) PRIMARY KEY,\n    applied_at TIMESTAMPTZ DEFAULT NOW()\n);\nINSERT INTO schema_migrations (version) VALUES (\'001_add_users_table\');' },
        { type: 'tip', value: 'Инструменты миграций для PostgreSQL: Flyway (Java/любые языки), Liquibase, Alembic (Python/SQLAlchemy), golang-migrate, node-pg-migrate. Все они отслеживают применённые миграции и умеют rollback. Выбери один и используй постоянно.' }
      ]
    },
    {
      id: 4,
      title: 'Производительность: чеклист',
      type: 'theory',
      content: [
        { type: 'text', value: 'Систематический подход к производительности: от очевидных проблем к тонкой настройке. Следуй чеклисту перед деплоем в продакшен.' },
        { type: 'code', language: 'sql', value: '-- ЧЕКЛИСТ ПРОИЗВОДИТЕЛЬНОСТИ:\n\n-- ✅ 1. Индексы на FK столбцы\n-- PostgreSQL НЕ создаёт индексы на FK автоматически!\nSELECT\n    c.conname AS fk_name,\n    c.conrelid::regclass AS table_name,\n    a.attname AS column_name\nFROM pg_constraint c\nJOIN pg_attribute a ON a.attrelid = c.conrelid AND a.attnum = ANY(c.conkey)\nWHERE c.contype = \'f\'  -- Foreign Key\nAND NOT EXISTS (\n    SELECT 1 FROM pg_index i\n    WHERE i.indrelid = c.conrelid\n      AND i.indkey[0] = ANY(c.conkey)\n)\nORDER BY table_name;\n-- Список FK без индексов — добавь их!\n\n-- ✅ 2. Не используй SELECT * в производительных запросах\n-- ПЛОХО: SELECT * FROM orders ORDER BY created_at LIMIT 100;\n-- ХОРОШО: SELECT id, customer_id, total, status, created_at ...\n\n-- ✅ 3. Используй LIMIT в запросах к большим таблицам\n-- SELECT * FROM logs; -> может вернуть 100 миллионов строк!\n-- SELECT * FROM logs ORDER BY created_at DESC LIMIT 100;\n\n-- ✅ 4. Пагинация: OFFSET плохо работает на больших смещениях\n-- МЕДЛЕННО:\nSELECT * FROM orders ORDER BY id LIMIT 20 OFFSET 100000;\n-- БЫСТРО (cursor-based pagination):\nSELECT * FROM orders WHERE id > 100000 ORDER BY id LIMIT 20;\n-- Требует: индекс на id (обычно есть - PK)\n\n-- ✅ 5. connection pooling\n-- Не открывай новое соединение на каждый запрос!\n-- PostgreSQL: max_connections = 100-200 (ограничен)\n-- Используй PgBouncer (connection pooler)\n-- Приложение -> PgBouncer -> PostgreSQL\n-- PgBouncer умеет мультиплексировать 1000 соединений в 20\n\n-- ✅ 6. VACUUM и AUTOVACUUM\n-- Мёртвые строки накапливаются -> table bloat -> медленнее\nSELECT relname, n_dead_tup, n_live_tup,\n    ROUND(n_dead_tup::NUMERIC / NULLIF(n_live_tup + n_dead_tup, 0) * 100, 1)\n    AS dead_pct\nFROM pg_stat_user_tables\nWHERE n_dead_tup > 1000\nORDER BY n_dead_tup DESC;\n-- dead_pct > 20%: нужен VACUUM\n\nVACUUM ANALYZE orders;  -- Очистить мёртвые строки + обновить статистику\nVACUUM FULL orders;     -- Полная перестройка (блокирует таблицу!)\n\n-- ✅ 7. Настройка postgresql.conf для продакшена:\n-- shared_buffers = 25% ОЗУ (например, 8GB из 32GB)\n-- effective_cache_size = 75% ОЗУ\n-- work_mem = RAM / (max_connections * 2) = ~64MB типично\n-- maintenance_work_mem = 512MB-2GB (для VACUUM, CREATE INDEX)\n-- max_wal_size = 4GB (меньше checkpoint frequency)\n-- checkpoint_completion_target = 0.9\n-- wal_buffers = 64MB\n-- random_page_cost = 1.1 (для SSD)' },
        { type: 'note', value: 'Самые частые проблемы производительности: 1) нет индексов на FK, 2) N+1 запросов, 3) OFFSET на больших данных, 4) нет connection pooling, 5) устаревшая статистика. Исправь эти пять — и большинство проблем уйдёт.' }
      ]
    },
    {
      id: 5,
      title: 'Резервное копирование и восстановление',
      type: 'theory',
      content: [
        { type: 'text', value: 'Резервное копирование — обязательная часть работы с продакшен БД. PostgreSQL предоставляет pg_dump/pg_restore для логических бэкапов и pg_basebackup + WAL для физических (Point-In-Time Recovery).' },
        { type: 'code', language: 'sql', value: '-- pg_dump: логический бэкап\n-- (запускается из командной строки, не из psql)\n\n-- Бэкап одной БД в SQL формат:\n-- pg_dump -h localhost -U postgres -d mydb -f backup.sql\n\n-- Бэкап в custom формат (сжатый, быстрый restore):\n-- pg_dump -h localhost -U postgres -d mydb -F c -f backup.dump\n\n-- Бэкап только схемы (без данных):\n-- pg_dump -h localhost -U postgres -d mydb --schema-only -f schema.sql\n\n-- Бэкап только данных (без схемы):\n-- pg_dump -h localhost -U postgres -d mydb --data-only -f data.sql\n\n-- Восстановление из SQL:\n-- psql -h localhost -U postgres -d mydb_restored < backup.sql\n\n-- Восстановление из custom формата (параллельно!):\n-- pg_restore -h localhost -U postgres -d mydb_restored\n--            -F c -j 8 backup.dump  (-j 8 = 8 параллельных потоков)\n\n-- pg_dumpall: бэкап всего кластера (все БД + роли)\n-- pg_dumpall -h localhost -U postgres -f all_databases.sql\n\n-- Мониторинг репликации (для HA setup):\nSELECT\n    client_addr,\n    state,\n    sent_lsn,\n    write_lsn,\n    flush_lsn,\n    replay_lsn,\n    write_lag,\n    replay_lag\nFROM pg_stat_replication;\n\n-- Проверить размер отставания реплики:\nSELECT\n    client_addr,\n    pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), replay_lsn))\n        AS replication_lag\nFROM pg_stat_replication;\n\n-- Point-in-Time Recovery (PITR) — только физический бэкап:\n-- 1. pg_basebackup создаёт полный снапшот\n-- 2. PostgreSQL архивирует WAL файлы (archive_command)\n-- 3. Для восстановления: basebackup + применение WAL до нужного момента\n-- Позволяет восстановить БД на ЛЮБОЙ момент времени!\n-- Например, откатить случайный DROP TABLE за 5 минут до...\n\n-- Стратегия бэкапов:\n-- Ежедневно: pg_dump в custom формат (сжатый)\n-- Непрерывно: WAL архивирование (для PITR)\n-- Еженедельно: pg_basebackup (полный физический снапшот)\n-- Хранить: минимум 30 дней дампов + WAL для PITR' },
        { type: 'warning', value: 'Бэкап без проверки восстановления — это не бэкап. Регулярно тестируй восстановление в тестовой среде. pg_restore --list backup.dump показывает содержимое без восстановления. Всегда храни бэкапы отдельно от БД-сервера.' }
      ]
    },
    {
      id: 6,
      title: 'Мониторинг PostgreSQL',
      type: 'theory',
      content: [
        { type: 'text', value: 'PostgreSQL предоставляет богатую статистику через системные представления pg_stat_*. Мониторинг позволяет обнаружить проблемы до того как они повлияют на пользователей.' },
        { type: 'code', language: 'sql', value: '-- Текущие активные запросы\nSELECT\n    pid,\n    usename,\n    client_addr,\n    NOW() - query_start AS duration,\n    state,\n    wait_event_type,\n    wait_event,\n    LEFT(query, 80) AS query\nFROM pg_stat_activity\nWHERE state <> \'idle\'\nORDER BY duration DESC NULLS LAST;\n\n-- Блокировки\nSELECT\n    pid,\n    relation::regclass,\n    mode,\n    granted\nFROM pg_locks\nWHERE NOT granted;  -- Ожидающие блокировки\n\n-- Кто кого блокирует\nSELECT\n    blocked.pid     AS blocked_pid,\n    blocked.query   AS blocked_query,\n    blocking.pid    AS blocking_pid,\n    blocking.query  AS blocking_query\nFROM pg_stat_activity blocked\nJOIN pg_stat_activity blocking\n    ON blocking.pid = ANY(pg_blocking_pids(blocked.pid))\nWHERE blocked.cardinality(pg_blocking_pids(blocked.pid)) > 0;\n\n-- Размеры объектов БД\nSELECT\n    relname,\n    pg_size_pretty(pg_total_relation_size(relid)) AS total,\n    pg_size_pretty(pg_relation_size(relid))       AS table_only,\n    pg_size_pretty(pg_indexes_size(relid))        AS indexes\nFROM pg_stat_user_tables\nORDER BY pg_total_relation_size(relid) DESC\nLIMIT 20;\n\n-- Hit ratio кэша (должен быть > 99%)\nSELECT\n    sum(heap_blks_read)  AS heap_read,\n    sum(heap_blks_hit)   AS heap_hit,\n    ROUND(\n        sum(heap_blks_hit)::NUMERIC /\n        NULLIF(sum(heap_blks_hit + heap_blks_read), 0) * 100\n    , 2) AS cache_hit_ratio\nFROM pg_statio_user_tables;\n-- < 95%: shared_buffers слишком мал!\n\n-- Checkpoint статистика (признаки проблем с I/O):\nSELECT\n    checkpoints_timed,\n    checkpoints_req,  -- Forced checkpoints: плохо, нужен больший max_wal_size\n    checkpoint_write_time / 1000 AS write_sec,\n    checkpoint_sync_time  / 1000 AS sync_sec\nFROM pg_stat_bgwriter;' },
        { type: 'tip', value: 'Метрики для мониторинга: cache hit ratio > 99% (shared_buffers), deadlocks (pg_stat_database.deadlocks), temp_files (work_mem мал), transactions per second, replication lag < 1 минута. Grafana + postgres_exporter = хороший dashboard.' }
      ]
    },
    {
      id: 7,
      title: 'Практика: Аудит схемы и оптимизация',
      type: 'practice',
      difficulty: 'medium',
      description: 'Проведи аудит схемы базы данных и примени best practices.',
      requirements: [
        'Найди все FK без индексов в текущей схеме',
        'Проверь hit ratio кэша и определи нужно ли увеличить shared_buffers',
        'Найди таблицы с высоким процентом мёртвых строк (нужен VACUUM)',
        'Проверь неиспользуемые индексы (idx_scan = 0)',
        'Напиши запрос мониторинга долгих транзакций (> 5 минут)',
        'Создай роль readonly и предоставь права на схему public',
        'Проверь настройку autovacuum для самой большой таблицы'
      ],
      hint: 'pg_stat_user_tables содержит n_dead_tup, last_autovacuum. pg_statio_user_tables — для cache hit ratio. pg_stat_activity — для долгих запросов. information_schema.table_constraints + pg_indexes для FK без индексов.',
      solution: '-- 1. FK без индексов\nSELECT\n    tc.table_name,\n    kcu.column_name,\n    ccu.table_name AS references_table,\n    ccu.column_name AS references_column\nFROM information_schema.table_constraints tc\nJOIN information_schema.key_column_usage kcu\n    ON tc.constraint_name = kcu.constraint_name\nJOIN information_schema.constraint_column_usage ccu\n    ON tc.constraint_name = ccu.constraint_name\nWHERE tc.constraint_type = \'FOREIGN KEY\'\n  AND tc.table_schema = \'public\'\n  AND NOT EXISTS (\n    SELECT 1 FROM pg_indexes i\n    WHERE i.tablename = tc.table_name\n      AND i.indexdef ILIKE \'%\' || kcu.column_name || \'%\'\n  );\n\n-- 2. Cache hit ratio\nSELECT\n    ROUND(\n        sum(heap_blks_hit)::NUMERIC /\n        NULLIF(sum(heap_blks_hit + heap_blks_read), 0) * 100\n    , 2) AS cache_hit_ratio,\n    CASE\n        WHEN sum(heap_blks_hit)::NUMERIC /\n             NULLIF(sum(heap_blks_hit + heap_blks_read), 0) > 0.99\n        THEN \'OK\'\n        ELSE \'shared_buffers нужно увеличить!\'\n    END AS recommendation\nFROM pg_statio_user_tables;\n\n-- 3. Таблицы с мёртвыми строками\nSELECT\n    relname,\n    n_live_tup,\n    n_dead_tup,\n    ROUND(n_dead_tup::NUMERIC / NULLIF(n_live_tup + n_dead_tup, 0) * 100, 1)\n        AS dead_pct,\n    last_autovacuum,\n    last_autoanalyze\nFROM pg_stat_user_tables\nWHERE n_dead_tup > 100\nORDER BY dead_pct DESC NULLS LAST;\n-- dead_pct > 20%: VACUUM ANALYZE needed!\n\n-- 4. Неиспользуемые индексы\nSELECT\n    relname AS table_name,\n    indexrelname AS index_name,\n    idx_scan,\n    pg_size_pretty(pg_relation_size(indexrelid)) AS index_size\nFROM pg_stat_user_indexes\nWHERE idx_scan = 0\n  AND indexrelname NOT LIKE \'%_pkey\'\nORDER BY pg_relation_size(indexrelid) DESC;\n\n-- 5. Долгие транзакции\nSELECT\n    pid,\n    usename,\n    xact_start,\n    NOW() - xact_start AS transaction_duration,\n    state,\n    LEFT(query, 100) AS current_query\nFROM pg_stat_activity\nWHERE xact_start IS NOT NULL\n  AND NOW() - xact_start > INTERVAL \'5 minutes\'\nORDER BY xact_start;\n\n-- 6. Создать роль readonly\nDO $$\nBEGIN\n    IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = \'readonly\') THEN\n        CREATE ROLE readonly;\n    END IF;\nEND;\n$$;\n\nGRANT CONNECT ON DATABASE current_database() TO readonly;\nGRANT USAGE ON SCHEMA public TO readonly;\nGRANT SELECT ON ALL TABLES IN SCHEMA public TO readonly;\nALTER DEFAULT PRIVILEGES IN SCHEMA public\n    GRANT SELECT ON TABLES TO readonly;\n\n-- 7. Настройка autovacuum для большой таблицы\nSELECT relname, reloptions\nFROM pg_class\nWHERE relname = \'orders\' AND relkind = \'r\';\n\n-- Более агрессивный autovacuum для горячей таблицы:\nALTER TABLE orders SET (\n    autovacuum_vacuum_scale_factor = 0.01,   -- 1% вместо 20%\n    autovacuum_analyze_scale_factor = 0.005, -- 0.5%\n    autovacuum_vacuum_cost_delay = 2         -- Меньше пауз\n);',
      explanation: 'Аудит схемы — регулярная процедура для поддержания здоровья БД. FK без индексов = медленные JOIN и каскадные операции. dead_pct > 20% = раздутые таблицы. Неиспользуемые индексы = лишний overhead на запись. Регулярный аудит + мониторинг = проактивное обслуживание вместо аварийного.'
    }
  ]
}
