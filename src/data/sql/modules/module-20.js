export default {
  id: 20,
  title: 'EXPLAIN и анализ планов выполнения',
  description: 'Команда EXPLAIN и EXPLAIN ANALYZE — основные инструменты отладки производительности PostgreSQL. Чтение планов запросов, узлы Seq Scan/Index Scan/Hash Join/Nested Loop, cost и actual time.',
  lessons: [
    {
      id: 1,
      title: 'EXPLAIN: синтаксис и базовые понятия',
      type: 'theory',
      content: [
        { type: 'text', value: 'EXPLAIN показывает план выполнения запроса без его фактического исполнения. EXPLAIN ANALYZE выполняет запрос и показывает реальное время. Это главный инструмент понимания и оптимизации производительности PostgreSQL.' },
        { type: 'code', language: 'sql', value: '-- EXPLAIN: показать план (запрос НЕ выполняется)\nEXPLAIN SELECT * FROM orders WHERE customer_id = 42;\n\n-- EXPLAIN ANALYZE: выполнить + показать план с реальным временем\nEXPLAIN ANALYZE SELECT * FROM orders WHERE customer_id = 42;\n\n-- Полная информация (BUFFERS = чтение буферов):\nEXPLAIN (ANALYZE, BUFFERS, FORMAT TEXT) SELECT * FROM orders;\n\n-- Другие опции:\n-- VERBOSE: дополнительная информация (имена столбцов, схемы)\n-- COSTS: показывать оценку стоимости (по умолчанию ON)\n-- SETTINGS: показывать изменённые параметры PostgreSQL\n-- SUMMARY: итоговое время планирования и выполнения\nEXPLAIN (ANALYZE, BUFFERS, VERBOSE, SUMMARY)\nSELECT o.id, c.name\nFROM orders o JOIN customers c ON o.customer_id = c.id\nWHERE o.status = \'paid\';\n\n-- Формат JSON (для программной обработки):\nEXPLAIN (FORMAT JSON, ANALYZE)\nSELECT * FROM orders WHERE id = 42;\n\n-- ВНИМАНИЕ: EXPLAIN ANALYZE ВЫПОЛНЯЕТ ЗАПРОС!\n-- Для опасных запросов (DELETE, UPDATE) оберни в транзакцию:\nBEGIN;\nEXPLAIN ANALYZE DELETE FROM orders WHERE status = \'test\';\nROLLBACK;  -- Откатить изменения, но результат EXPLAIN уже получен\n\n-- Пример вывода EXPLAIN:\n-- Seq Scan on orders  (cost=0.00..154.00 rows=5000 width=89)\n-- ^^^^^^               ^^^^^ ^^^^^^      ^^^^^^^^^^^  ^^^^^\n-- Узел (Node)          start  total       оценка       средний\n--                      cost   cost       строк        размер строки\n\n-- С ANALYZE:\n-- Seq Scan on orders  (cost=0.00..154.00 rows=5000 width=89)\n--                     (actual time=0.012..8.234 rows=4856 loops=1)\n--                               ^^^^^^  ^^^^^  ^^^^^^^^^^^^^^\n--                               start   total  реальные строки' },
        { type: 'tip', value: 'Используй explain.depesz.com или pganalyze.com для визуализации планов EXPLAIN. Эти онлайн-инструменты раскрашивают узлы по стоимости и помогают найти "горлышко бутылки". Формат: вставь вывод EXPLAIN ANALYZE на сайт.' }
      ]
    },
    {
      id: 2,
      title: 'Узлы плана: Seq Scan, Index Scan, Bitmap Scan',
      type: 'theory',
      content: [
        { type: 'text', value: 'Каждый узел плана — один шаг выполнения запроса. Узлы scan определяют как данные читаются из таблицы. Понимание разницы между ними критично для оптимизации.' },
        { type: 'code', language: 'sql', value: '-- SEQ SCAN (Последовательное чтение): читает ВСЮ таблицу\n-- Хорошо: маленькие таблицы, выборка >10-20% строк\n-- Плохо: большие таблицы с отбором малого % строк\nEXPLAIN SELECT * FROM orders;  -- Нет WHERE -> всегда Seq Scan\n-- Seq Scan on orders (cost=0.00..1540.00 rows=100000 width=89)\n\n-- INDEX SCAN: переходит по индексу к строкам в таблице\n-- Хорошо: отбор малого % строк, есть подходящий индекс\n-- Два обращения: сначала индекс, потом Heap (таблица) для каждой строки\nEXPLAIN SELECT * FROM orders WHERE customer_id = 42;\n-- Index Scan using idx_orders_customer_id on orders\n--   (cost=0.42..18.50 rows=5 width=89)\n--   Index Cond: (customer_id = 42)\n\n-- INDEX ONLY SCAN: все нужные данные в индексе, Heap не читается!\n-- Требует: INCLUDE в индексе или все SELECT столбцы в индексе\nCREATE INDEX idx_orders_cov ON orders(customer_id) INCLUDE (total, status);\nEXPLAIN SELECT customer_id, total, status FROM orders WHERE customer_id = 42;\n-- Index Only Scan using idx_orders_cov on orders\n--   Heap Fetches: 0  <- НОЛЬ обращений к таблице!\n\n-- BITMAP INDEX SCAN + BITMAP HEAP SCAN:\n-- PostgreSQL сначала строит bitmap из индекса, потом читает heap по блокам\n-- Эффективно для выборки 1-10% строк (между Index Scan и Seq Scan)\nEXPLAIN SELECT * FROM orders WHERE status = \'paid\';\n-- Bitmap Heap Scan on orders (cost=89.50..780.00 rows=4500 width=89)\n--   Recheck Cond: (status = \'paid\')\n--   -> Bitmap Index Scan on idx_orders_status\n--       Index Cond: (status = \'paid\')\n\n-- Почему Bitmap а не Index Scan?\n-- Bitmap собирает все подходящие блоки, читает их по порядку\n-- Это эффективнее чем Index Scan при большом числе строк\n-- (Index Scan = случайные обращения к диску = медленно)\n\n-- Когда PostgreSQL выбирает Seq Scan вместо Index Scan?\n-- 1. Мало строк в таблице (порог ~8000 для random_page_cost=4)\n-- 2. Возвращается >5-10% строк\n-- 3. Статистика устарела (ANALYZE нужен)\n-- 4. enable_indexscan = off (параметр планировщика)\n\n-- Форсировать Index Scan (для тестирования):\nSET enable_seqscan = off;\nEXPLAIN SELECT * FROM orders;  -- Принудительно Index Scan (если есть индекс)\nRESET enable_seqscan;' },
        { type: 'note', value: 'Index Only Scan — самый быстрый тип сканирования. Достигается через покрывающие индексы (INCLUDE). Проверяй "Heap Fetches: 0" — это значит нет обращений к таблице, всё из индекса.' }
      ]
    },
    {
      id: 3,
      title: 'Узлы JOIN: Nested Loop, Hash Join, Merge Join',
      type: 'theory',
      content: [
        { type: 'text', value: 'PostgreSQL выбирает стратегию JOIN исходя из размеров таблиц и наличия индексов. Три алгоритма: Nested Loop (маленькие таблицы), Hash Join (большие без индекса), Merge Join (отсортированные данные).' },
        { type: 'code', language: 'sql', value: '-- NESTED LOOP JOIN: для каждой строки внешней таблицы ищет строки внутренней\n-- Сложность: O(N * M) — хорошо только если M маленькое или есть индекс\n-- Использует Index Scan на внутренней таблице\nEXPLAIN\nSELECT o.id, c.name\nFROM orders o\nJOIN customers c ON o.customer_id = c.id\nWHERE o.id = 42;  -- orders очень мала (1 строка) -> Nested Loop отлично\n\n-- Nested Loop  (cost=0.42..16.84 rows=1 width=50)\n--   -> Index Scan on orders (cost=0.42..8.44 rows=1)\n--         Index Cond: (id = 42)\n--   -> Index Scan on customers (cost=0.42..8.44 rows=1)\n--         Index Cond: (id = orders.customer_id)\n\n-- HASH JOIN: строит хэш-таблицу из меньшей таблицы, сканирует большую\n-- Сложность: O(N + M) — отлично для больших таблиц без индекса\n-- Требует памяти work_mem для хэш-таблицы\nEXPLAIN ANALYZE\nSELECT COUNT(*)\nFROM orders o\nJOIN customers c ON o.customer_id = c.id;\n-- Hash Join  (cost=185.00..1890.00 rows=50000 width=0)\n--             (actual time=12.5..89.3 rows=50000 loops=1)\n--   Hash Cond: (o.customer_id = c.id)\n--   Batches: 1  Memory Usage: 2048kB  <- Используемая память\n--   -> Seq Scan on orders (rows=50000)\n--   -> Hash  (rows=10000)\n--         -> Seq Scan on customers (rows=10000)\n\n-- MERGE JOIN: обе таблицы сортируются, потом сканируются параллельно\n-- Эффективно если обе стороны уже отсортированы (по индексу)\n-- Сложность: O(N log N + M log N)\nEXPLAIN\nSELECT * FROM orders o JOIN customers c ON o.customer_id = c.id\nORDER BY c.id;\n-- Merge Join  (cost=...)\n--   Merge Cond: (c.id = o.customer_id)\n--   -> Index Scan on customers (uже отсортировано по id!)\n--   -> Sort + Index Scan on orders (сортировка по customer_id)\n\n-- Проблема: Hash Join "Batches: 4" означает хэш-таблица не влезла в work_mem\n-- Решение: увеличить work_mem для сессии:\nSET work_mem = \'256MB\';\nEXPLAIN ANALYZE SELECT COUNT(*) FROM orders o JOIN customers c ON o.customer_id = c.id;\n-- Batches: 1  <- Теперь в памяти\nRESET work_mem;\n\n-- Сравнение JOIN алгоритмов:\n-- | Метод       | Когда используется              | Требования        |\n-- | Nested Loop | Маленькая таблица / индекс      | Индекс на inner   |\n-- | Hash Join   | Большие таблицы без индекса     | Память work_mem   |\n-- | Merge Join  | Обе стороны отсортированы       | Sort или индекс   |' },
        { type: 'tip', value: 'Hash Join с Batches > 1 означает spilling на диск — данные не влезли в work_mem. Это медленно. Решение: SET work_mem = \'256MB\' или добавить индекс для Nested Loop. Оптимальный work_mem: смотри "Peak Memory Usage" в EXPLAIN ANALYZE.' }
      ]
    },
    {
      id: 4,
      title: 'Чтение cost и rows: оценки vs реальность',
      type: 'theory',
      content: [
        { type: 'text', value: 'Планировщик PostgreSQL оценивает стоимость (cost) и количество строк (rows). Большое расхождение оценки и реальности = плохая статистика = плохой план. Умение читать эти числа критично.' },
        { type: 'code', language: 'sql', value: '-- Формат: (cost=start_cost..total_cost rows=estimated_rows width=bytes)\n-- start_cost: стоимость до вывода первой строки\n-- total_cost: стоимость вывода всех строк\n-- rows: оценка количества строк\n-- width: средний размер строки в байтах\n\n-- ПРИМЕР: хороший план (оценка ≈ реальность)\nEXPLAIN ANALYZE\nSELECT * FROM orders WHERE customer_id = 42;\n-- Index Scan  (cost=0.42..18.50 rows=5 width=89)\n--             (actual time=0.018..0.089 rows=6 loops=1)\n-- Оценка: 5, реальность: 6 -> ХОРОШО\n\n-- ПРИМЕР: плохой план (сильное расхождение оценки)\nEXPLAIN ANALYZE\nSELECT * FROM orders\nWHERE customer_id = 42 AND DATE(created_at) = \'2024-01-15\';\n-- Seq Scan (cost=0.00..1540.00 rows=1250 width=89)\n--          (actual time=8.5..245.0 rows=3 loops=1)\n-- Оценка: 1250, реальность: 3 -> ПЛОХО (функция DATE() мешает статистике)\n\n-- Расхождение > 10x: нужен ANALYZE или перепись запроса\n-- Расхождение > 100x: серьёзная проблема со статистикой или запросом\n\n-- Стоимость единиц:\n-- seq_page_cost = 1.0 (по умолчанию)\n-- random_page_cost = 4.0 (для HDD; для SSD ставь 1.1-2.0)\n-- cpu_tuple_cost = 0.01 (обработка одной строки)\n-- cpu_index_tuple_cost = 0.005\n-- cpu_operator_cost = 0.0025\n\n-- Настройка для SSD-дисков:\n-- ALTER SYSTEM SET random_page_cost = 1.1;\n-- SELECT pg_reload_conf();\n\n-- Посмотреть настройки планировщика:\nSHOW random_page_cost;\nSHOW work_mem;\nSHOW effective_cache_size;\n\n-- Почему планировщик ошибается в оценке rows?\n-- 1. Скоррелированные условия: WHERE city = \'Алматы\' AND country = \'KZ\'\n--    Статистика по каждому столбцу отдельная, не знает корреляцию\n-- 2. Функции в WHERE: DATE(created_at) = ...\n-- 3. Устаревшая статистика (ANALYZE давно не запускался)\n-- 4. Неравномерное распределение данных\n\n-- Расширенная статистика для коррелированных столбцов:\nCREATE STATISTICS orders_customer_status (ndistinct, dependencies)\n    ON customer_id, status FROM orders;\nANALYZE orders;\n-- Теперь планировщик знает корреляцию customer_id и status' },
        { type: 'warning', value: 'random_page_cost = 4.0 оптимально для HDD. Для SSD нужно 1.1-2.0. При завышенном random_page_cost PostgreSQL избегает Index Scan и делает Seq Scan — это неправильный выбор на SSD. Это одна из самых частых причин неоптимальных планов на современных серверах.' }
      ]
    },
    {
      id: 5,
      title: 'Агрегации и сортировки в EXPLAIN',
      type: 'theory',
      content: [
        { type: 'text', value: 'GROUP BY, ORDER BY и DISTINCT порождают дополнительные узлы плана: Sort, HashAggregate, GroupAggregate. Их стоимость зависит от объёма данных и доступной памяти.' },
        { type: 'code', language: 'sql', value: '-- GROUP BY: HashAggregate или GroupAggregate\nEXPLAIN ANALYZE\nSELECT customer_id, COUNT(*), SUM(total)\nFROM orders\nGROUP BY customer_id;\n\n-- HashAggregate  (cost=780.00..830.00 rows=5000 width=24)\n--                (actual time=45.2..67.3 rows=4890 loops=1)\n--   Group Key: customer_id\n--   Batches: 1  Peak Memory Usage: 4096kB\n--   -> Seq Scan on orders (rows=50000)\n\n-- HashAggregate: строит хэш-таблицу в памяти. Быстро, требует work_mem\n-- GroupAggregate: требует сортировки, но меньше памяти\n\n-- ORDER BY: Sort узел\nEXPLAIN ANALYZE\nSELECT * FROM orders ORDER BY created_at DESC LIMIT 10;\n\n-- Limit  (cost=0.42..0.76 rows=10 width=89)\n--   -> Index Scan Backward using idx_orders_created_at on orders\n-- PostgreSQL использует индекс для ORDER BY, узла Sort нет!\n\n-- Когда Sort появляется:\nEXPLAIN ANALYZE\nSELECT * FROM orders ORDER BY total DESC LIMIT 10;\n-- Sort  (cost=1456.58..1581.58 rows=50000 width=89)\n--       (actual time=89.3..89.5 rows=10 loops=1)\n--   Sort Key: total DESC\n--   Sort Method: top-N heapsort  Memory: 27kB  <- Маленький (LIMIT 10!)\n--   -> Seq Scan on orders\n\n-- Sort Methods:\n-- quicksort   - весь набор в памяти (хорошо)\n-- external sort - сортировка на диске (плохо! нужен больший work_mem)\n-- top-N heapsort - только N верхних (эффективно для ORDER BY + LIMIT)\n\n-- Увидеть Sort Mode: external sort -> проблема, увеличь work_mem\nEXPLAIN ANALYZE\nSELECT * FROM large_table ORDER BY some_column;\n-- Sort Method: external merge  Disk: 24582kB  <- МЕДЛЕННО! Диск!\n\nSET work_mem = \'512MB\';\nEXPLAIN ANALYZE\nSELECT * FROM large_table ORDER BY some_column;\n-- Sort Method: quicksort  Memory: 48120kB  <- БЫСТРО! Память!\nRESET work_mem;\n\n-- Параллельные планы:\nEXPLAIN ANALYZE\nSELECT COUNT(*) FROM large_table;\n-- Finalize Aggregate  (rows=1)\n--   -> Gather  (workers: 3)  <- 3 параллельных воркера!\n--         -> Partial Aggregate\n--               -> Parallel Seq Scan on large_table\n-- Параллелизм ускоряет на ~3x для Seq Scan на больших таблицах' },
        { type: 'tip', value: 'Признаки проблем в EXPLAIN: "Sort Method: external merge Disk:" (нужен work_mem больше), "Batches: N > 1" для HashAggregate/HashJoin (нужен work_mem), большое расхождение estimated vs actual rows (нужен ANALYZE).' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Анализ и оптимизация через EXPLAIN',
      type: 'practice',
      difficulty: 'hard',
      description: 'Проанализируй реальные планы выполнения и устрани узкие места.',
      requirements: [
        'Сравни планы одного запроса с WHERE created_at по-старому (YEAR(created_at)=2024) и по-новому (диапазон)',
        'Найди запрос где планировщик выбирает Seq Scan вместо Index Scan — исправь через ANALYZE или добавление индекса',
        'JOIN двух таблиц: измерь производительность с разными значениями work_mem (16MB vs 256MB)',
        'Покажи разницу между Index Scan и Index Only Scan добавив INCLUDE в индекс',
        'Запрос с GROUP BY: определи HashAggregate vs GroupAggregate, увидь Batches при малом work_mem',
        'Используй EXPLAIN с BUFFERS: найди запрос с высоким числом buffer hits/reads'
      ],
      hint: 'SET enable_seqscan = off форсирует Index Scan для тестирования. BUFFERS покажет "shared hit" (из кэша) и "shared read" (с диска). Много "read" = данные не в кэше = медленно.',
      expectedOutput: '-- 1. Сравнение планов: EXTRACT vs диапазон\nСтарый запрос (EXTRACT):\n Seq Scan on orders (cost=0.00..4820.00 rows=9800 width=48)\n   Filter: (date_part(\'year\', created_at) = 2024)\n   Rows Removed by Filter: 190200\n Execution Time: 48.320 ms\n\nНовый запрос (диапазон):\n Index Only Scan using idx_orders_created_at\n   Index Cond: (created_at >= \'2024-01-01\' AND created_at < \'2025-01-01\')\n   Heap Fetches: 0\n Execution Time: 1.250 ms\n\n-- 2. Seq Scan из-за устаревшей статистики -> ANALYZE\nДо ANALYZE: Seq Scan on orders_test (cost=0.00..6200.00)\nANALYZE orders_test;\nПосле ANALYZE: Index Scan using idx_orders_test_customer (cost=0.43..8.80)\n\n-- 3. work_mem и Hash Join Batches\nSET work_mem = \'16MB\':\n Hash Join (cost=2841.00..18420.00) actual time=1240.500 ms\n   Batches: 8  Peak Memory Usage: 16384 kB  -- spilling на диск!\n\nSET work_mem = \'256MB\':\n Hash Join (cost=2841.00..18420.00) actual time=320.200 ms\n   Batches: 1  Peak Memory Usage: 182340 kB  -- всё в памяти!\n\n-- 4. Index Scan vs Index Only Scan\nIndex Scan (без INCLUDE):\n Heap Fetches: 1240  -- читает строки таблицы\n Execution Time: 8.540 ms\n\nIndex Only Scan (с INCLUDE (total, status)):\n Heap Fetches: 0  -- всё из индекса!\n Execution Time: 0.480 ms\n\n-- 5. HashAggregate Batches\nSET work_mem=\'1MB\':\n HashAggregate (Batches: 8)  Disk: 24576 kB  -- медленно!\n\nSET work_mem=\'64MB\':\n HashAggregate (Batches: 1)  Peak Memory: 48200 kB  -- быстро!\n\n-- 6. BUFFERS анализ\nBuffers: shared hit=12840 read=5620\n-- Много read: данные не в кэше shared_buffers\nПовторный запуск:\nBuffers: shared hit=18460 read=0\n-- Все данные теперь в кэше',
      solution: '-- 1. Сравнение планов\n-- Плохой запрос:\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT COUNT(*) FROM orders WHERE EXTRACT(YEAR FROM created_at) = 2024;\n-- Seq Scan (читает ВСЮ таблицу, функция не позволяет использовать индекс)\n\n-- Хороший запрос:\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT COUNT(*) FROM orders\nWHERE created_at >= \'2024-01-01\' AND created_at < \'2025-01-01\';\n-- Index Only Scan (если есть индекс на created_at)\n\n-- 2. Seq Scan из-за устаревшей статистики\nINSERT INTO orders_test SELECT * FROM orders;  -- Большая вставка\nEXPLAIN SELECT * FROM orders_test WHERE customer_id = 1;\n-- Может быть Seq Scan (статистика устарела)\nANALYZE orders_test;\nEXPLAIN SELECT * FROM orders_test WHERE customer_id = 1;\n-- После ANALYZE -> Index Scan\n\n-- 3. work_mem влияние на Hash Join\nSET work_mem = \'16MB\';\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT COUNT(*) FROM orders o JOIN order_items oi ON o.id = oi.order_id;\n-- Hash Join, Batches: 4+ (spilling на диск)\n\nSET work_mem = \'256MB\';\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT COUNT(*) FROM orders o JOIN order_items oi ON o.id = oi.order_id;\n-- Hash Join, Batches: 1 (всё в памяти)\nRESET work_mem;\n\n-- 4. Index Scan vs Index Only Scan\nCREATE INDEX idx_test_scan ON orders(customer_id);\nEXPLAIN ANALYZE\nSELECT customer_id, total, status FROM orders WHERE customer_id = 42;\n-- Index Scan (Heap Fetches > 0: нужно читать таблицу для total, status)\n\nDROP INDEX idx_test_scan;\nCREATE INDEX idx_test_covering ON orders(customer_id)\nINCLUDE (total, status);\n\nEXPLAIN ANALYZE\nSELECT customer_id, total, status FROM orders WHERE customer_id = 42;\n-- Index Only Scan (Heap Fetches: 0 - всё из индекса!)\n\n-- 5. HashAggregate с Batches\nSET work_mem = \'1MB\';  -- Очень мало\nEXPLAIN ANALYZE\nSELECT customer_id, SUM(total) FROM orders GROUP BY customer_id;\n-- HashAggregate Batches: 8  (spilling на диск!)\n\nSET work_mem = \'64MB\';\nEXPLAIN ANALYZE\nSELECT customer_id, SUM(total) FROM orders GROUP BY customer_id;\n-- HashAggregate Batches: 1  (в памяти)\nRESET work_mem;\n\n-- 6. BUFFERS анализ\nEXPLAIN (ANALYZE, BUFFERS, FORMAT TEXT)\nSELECT o.id, c.name, SUM(oi.unit_price * oi.quantity)\nFROM orders o\nJOIN customers c ON o.customer_id = c.id\nJOIN order_items oi ON o.id = oi.order_id\nWHERE o.created_at >= NOW() - INTERVAL \'30 days\'\nGROUP BY o.id, c.name;\n-- Смотрим:\n-- Buffers: shared hit=X read=Y\n-- Если read >> hit: данные не в кэше\n-- После повторного запуска: все hit (данные закэшированы)\n-- Много read на hot-запросе: увеличить shared_buffers',
      explanation: 'EXPLAIN ANALYZE — главный инструмент оптимизации. Ключевые сигналы: "Seq Scan" на большой таблице + WHERE = нужен индекс. "Batches > 1" = нужен work_mem. "Sort: external merge Disk" = нужен work_mem. "rows=1000 actual=100000" = нужен ANALYZE. "Heap Fetches > 0" = нет покрывающего индекса.'
    }
  ]
}
