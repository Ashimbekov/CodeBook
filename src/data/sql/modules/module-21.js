export default {
  id: 21,
  title: 'PostgreSQL особенности: JSONB, массивы, full-text search',
  description: 'Уникальные возможности PostgreSQL: JSONB с операторами и индексами, массивы и их функции, полнотекстовый поиск tsvector/tsquery, диапазоны и расширения pg_trgm.',
  lessons: [
    {
      id: 1,
      title: 'JSONB: хранение и запросы к JSON',
      type: 'theory',
      content: [
        { type: 'text', value: 'PostgreSQL поддерживает два типа JSON: JSON (текст, точный порядок ключей) и JSONB (бинарный, индексируемый, без дубликатов ключей). Для большинства случаев используй JSONB — он значительно быстрее для запросов.' },
        { type: 'code', language: 'sql', value: '-- Создание таблицы с JSONB\nCREATE TABLE products (\n    id         SERIAL PRIMARY KEY,\n    name       TEXT NOT NULL,\n    attributes JSONB  -- Гибкие атрибуты (цвет, размер, характеристики)\n);\n\nINSERT INTO products (name, attributes) VALUES\n    (\'iPhone 15\', \'{"color": "black", "storage": "256GB", "weight": 171, "5G": true, "colors": ["black", "white", "blue"]}\'),\n    (\'MacBook Pro\', \'{"color": "silver", "ram": "16GB", "cpu": "M3 Pro", "ports": ["USB-C", "HDMI", "SD"]}\'),\n    (\'AirPods\', \'{"color": "white", "battery": 6, "noise_cancellation": true}\');\n\n-- Операторы JSONB:\n-- -> : получить значение по ключу как JSON\n-- ->> : получить значение как TEXT\n-- #> : по пути как JSON\n-- #>> : по пути как TEXT\n\nSELECT\n    name,\n    attributes -> \'color\'   AS color_json,   -- "black" (с кавычками)\n    attributes ->> \'color\'  AS color_text,   -- black (без кавычек)\n    attributes -> \'weight\'  AS weight_json,  -- 171\n    (attributes ->> \'weight\')::INTEGER AS weight_int,  -- 171 (числом)\n    attributes -> \'colors\' -> 0 AS first_color  -- "black"\nFROM products;\n\n-- Поиск по JSONB\nSELECT name FROM products\nWHERE attributes @> \'{"color": "black"}\';   -- содержит {color: black}\n\nSELECT name FROM products\nWHERE attributes ? \'5G\';                      -- ключ существует\n\nSELECT name FROM products\nWHERE attributes ?| ARRAY[\'5G\', \'noise_cancellation\']; -- любой из ключей\n\nSELECT name FROM products\nWHERE attributes ?& ARRAY[\'color\', \'weight\']; -- все ключи существуют\n\n-- Числовые сравнения:\nSELECT name FROM products\nWHERE (attributes ->> \'weight\')::INTEGER > 100;\n\n-- Обновление JSONB\nUPDATE products\nSET attributes = attributes || \'{"warranty": "2 years"}\'\nWHERE name = \'iPhone 15\';\n-- || : объединение (перезаписывает дублирующиеся ключи)\n\n-- Удалить ключ:\nUPDATE products\nSET attributes = attributes - \'weight\';\n\n-- Глубокое обновление (jsonb_set):\nUPDATE products\nSET attributes = jsonb_set(attributes, \'{color}\', \'\"space gray\"\')\nWHERE id = 1;\n\n-- GIN индекс для быстрых @>, ?, ?|, ?& операций\nCREATE INDEX idx_products_attrs ON products USING GIN (attributes);\n-- Теперь поиск по JSONB очень быстрый!' },
        { type: 'tip', value: 'JSONB подходит для: переменного числа атрибутов (EAV паттерн), конфигурационных данных, схем которые часто меняются. НЕ подходит для: данных которые нужно часто сравнивать/агрегировать по полям (лучше отдельные столбцы).' }
      ]
    },
    {
      id: 2,
      title: 'JSONB: функции и агрегации',
      type: 'theory',
      content: [
        { type: 'text', value: 'PostgreSQL предоставляет богатый набор функций для работы с JSONB: разбивка на строки, построение JSON, агрегации.' },
        { type: 'code', language: 'sql', value: '-- jsonb_each: развернуть JSONB в набор пар ключ-значение\nSELECT key, value\nFROM products,\n     jsonb_each(attributes)  -- LATERAL JOIN к функции\nWHERE id = 1;\n-- key         | value\n-- color       | "black"\n-- storage     | "256GB"\n-- weight      | 171\n\n-- jsonb_array_elements: развернуть массив в строки\nSELECT name, elem\nFROM products,\n     jsonb_array_elements(attributes -> \'colors\') AS elem\nWHERE attributes ? \'colors\';\n-- name    | elem\n-- iPhone  | "black"\n-- iPhone  | "white"\n-- iPhone  | "blue"\n\n-- Построить JSON из данных\nSELECT\n    jsonb_build_object(\n        \'id\', id,\n        \'name\', name,\n        \'color\', attributes ->> \'color\'\n    ) AS product_json\nFROM products;\n\n-- jsonb_agg: агрегировать строки в JSON-массив\nSELECT\n    (attributes ->> \'color\') AS color,\n    jsonb_agg(name) AS products_with_color\nFROM products\nWHERE attributes ? \'color\'\nGROUP BY attributes ->> \'color\';\n\n-- jsonb_object_agg: агрегировать в JSON-объект\nSELECT\n    jsonb_object_agg(name, attributes ->> \'color\') AS product_colors\nFROM products;\n-- {\"iPhone 15\": \"black\", \"MacBook Pro\": \"silver\", ...}\n\n-- Работа с вложенными объектами:\nSELECT\n    id,\n    attributes #> \'{specs,display,resolution}\' AS resolution\nFROM products\nWHERE attributes #>> \'{specs,display,resolution}\' = \'2560x1664\';\n-- #> : путь через массив строк\n\n-- jsonb_strip_nulls: удалить ключи с null значениями\nSELECT jsonb_strip_nulls(\'{"a": 1, "b": null, "c": 3}\'::JSONB);\n-- {\"a\": 1, \"c\": 3}\n\n-- jsonb_pretty: форматированный вывод\nSELECT jsonb_pretty(attributes) FROM products WHERE id = 1;' },
        { type: 'note', value: 'jsonb_each() и jsonb_array_elements() используются с LATERAL JOIN (или implicit LATERAL). Они возвращают набор строк — PostgreSQL автоматически делает CROSS JOIN LATERAL с каждой строкой таблицы.' }
      ]
    },
    {
      id: 3,
      title: 'Массивы в PostgreSQL',
      type: 'theory',
      content: [
        { type: 'text', value: 'PostgreSQL поддерживает массивы любого типа как столбцы таблицы. Массивы подходят для тегов, списков ID, наборов значений. Индексируются через GIN.' },
        { type: 'code', language: 'sql', value: '-- Объявление массивов\nCREATE TABLE articles (\n    id         SERIAL PRIMARY KEY,\n    title      TEXT,\n    tags       TEXT[],          -- Одномерный массив\n    scores     INTEGER[],       -- Массив чисел\n    matrix     INTEGER[][]      -- Двумерный массив\n);\n\nINSERT INTO articles (title, tags, scores) VALUES\n    (\'PostgreSQL Guide\', ARRAY[\'database\', \'sql\', \'postgresql\'], ARRAY[95, 87, 92]),\n    (\'Docker Tutorial\', \'{"docker", "devops", "containers"}\', ARRAY[88]),\n    (\'Python Tips\', ARRAY[\'python\', \'programming\'], ARRAY[91, 78, 85, 90]);\n\n-- Доступ к элементам (индексация с 1!)\nSELECT title, tags[1] AS first_tag, tags[2] AS second_tag FROM articles;\nSELECT title, tags[1:2] AS first_two_tags FROM articles;  -- Срез\n\n-- Операторы массивов\nSELECT * FROM articles WHERE tags @> ARRAY[\'sql\'];        -- Содержит элемент\nSELECT * FROM articles WHERE tags <@ ARRAY[\'sql\', \'database\', \'postgresql\'];\n-- Является подмножеством\nSELECT * FROM articles WHERE tags && ARRAY[\'sql\', \'python\']; -- Пересекаются\n\n-- ANY / ALL\nSELECT * FROM articles WHERE \'postgresql\' = ANY(tags);    -- Эквивалентно @>\nSELECT * FROM articles WHERE 90 <= ALL(scores);           -- Все оценки >= 90\n\n-- Функции массивов\nSELECT\n    title,\n    array_length(tags, 1)          AS tags_count,\n    array_append(tags, \'new-tag\')  AS with_new_tag,\n    array_prepend(\'front\', tags)   AS with_front,\n    array_cat(tags, ARRAY[\'extra\']) AS concatenated,\n    unnest(tags)                   AS each_tag\nFROM articles;\n-- unnest() разворачивает массив в строки!\n\n-- Агрегация в массив\nSELECT\n    customer_id,\n    array_agg(id ORDER BY created_at) AS order_ids,\n    array_agg(DISTINCT status) AS statuses\nFROM orders\nGROUP BY customer_id;\n\n-- GIN индекс для операторов @>, <@, &&, ANY\nCREATE INDEX idx_articles_tags ON articles USING GIN (tags);\n\n-- Поиск по тегу с GIN (быстро!):\nEXPLAIN SELECT * FROM articles WHERE tags @> ARRAY[\'postgresql\'];\n-- Bitmap Index Scan using idx_articles_tags\n\n-- string_to_array / array_to_string:\nSELECT string_to_array(\'a,b,c,d\', \',\');  -- {a,b,c,d}\nSELECT array_to_string(ARRAY[\'a\', \'b\', \'c\'], \', \');  -- a, b, c' },
        { type: 'tip', value: 'Массивы отлично подходят для тегов и небольших списков. Избегай массивов больших размеров (>100 элементов) и массивов которые нужно часто изменять поэлементно — лучше отдельная таблица. unnest() + LATERAL = мощный паттерн для работы с массивами как с таблицами.' }
      ]
    },
    {
      id: 4,
      title: 'Full-Text Search: tsvector и tsquery',
      type: 'theory',
      content: [
        { type: 'text', value: 'Полнотекстовый поиск в PostgreSQL — встроенный механизм для поиска по тексту с морфологическим анализом, стоп-словами и ранжированием результатов. Поддерживает русский язык.' },
        { type: 'code', language: 'sql', value: '-- tsvector: документ подготовленный для поиска\nSELECT to_tsvector(\'russian\', \'PostgreSQL это мощная база данных с поддержкой JSON\');\n-- \'base\':6 \'baz\':6 \'данных\':7 \'json\':9 \'мощная\':3 \'поддержка\':8 \'postgresql\':1\n\n-- tsquery: поисковый запрос\nSELECT to_tsquery(\'russian\', \'база & данных\');  -- база AND данных\nSELECT to_tsquery(\'russian\', \'база | файл\');   -- база OR файл\nSELECT to_tsquery(\'russian\', \'!удалить\');      -- NOT удалить\nSELECT to_tsquery(\'russian\', \'postgres:*\');    -- Префикс postgres*\n\n-- plainto_tsquery: не требует операторов, автоматически AND\nSELECT plainto_tsquery(\'russian\', \'база данных postgresql\');\n-- \'база\' & \'данных\' & \'postgresql\'\n\n-- Поиск:\nSELECT title FROM articles\nWHERE to_tsvector(\'russian\', body) @@ to_tsquery(\'russian\', \'postgresql & база\');\n\n-- Более эффективно: хранить tsvector в отдельном столбце\nALTER TABLE articles ADD COLUMN search_vector tsvector;\n\n-- Заполнить:\nUPDATE articles\nSET search_vector = to_tsvector(\'russian\', title || \' \' || body);\n-- Или с весами (A=заголовок важнее чем B=тело):\nUPDATE articles\nSET search_vector =\n    setweight(to_tsvector(\'russian\', COALESCE(title, \'\')), \'A\') ||\n    setweight(to_tsvector(\'russian\', COALESCE(body,  \'\')), \'B\');\n\n-- GIN индекс:\nCREATE INDEX idx_articles_search ON articles USING GIN (search_vector);\n\n-- Поиск с индексом:\nSELECT title FROM articles\nWHERE search_vector @@ plainto_tsquery(\'russian\', \'база данных\');\n\n-- Ранжирование результатов:\nSELECT\n    title,\n    ts_rank(search_vector, query)          AS rank,\n    ts_rank_cd(search_vector, query)       AS rank_cd,\n    ts_headline(\'russian\', body, query,\n        \'MaxWords=20, MinWords=10\')         AS snippet\nFROM articles,\n     plainto_tsquery(\'russian\', \'postgresql база\') AS query\nWHERE search_vector @@ query\nORDER BY rank DESC;\n\n-- Автоматическое обновление search_vector через триггер:\nCREATE OR REPLACE FUNCTION trg_articles_search_update()\nRETURNS TRIGGER AS $$\nBEGIN\n    NEW.search_vector :=\n        setweight(to_tsvector(\'russian\', COALESCE(NEW.title, \'\')), \'A\') ||\n        setweight(to_tsvector(\'russian\', COALESCE(NEW.body,  \'\')), \'B\');\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER update_search_vector\n    BEFORE INSERT OR UPDATE OF title, body ON articles\n    FOR EACH ROW EXECUTE FUNCTION trg_articles_search_update();' },
        { type: 'tip', value: 'Для автодополнения (autocomplete) используй pg_trgm + GIN индекс: CREATE INDEX ON articles USING GIN (title gin_trgm_ops). Затем WHERE title ILIKE \'%поиск%\' или similarity(title, \'поиск\') > 0.3. Работает даже с подстроками.' }
      ]
    },
    {
      id: 5,
      title: 'Диапазонные типы и расширения',
      type: 'theory',
      content: [
        { type: 'text', value: 'PostgreSQL имеет встроенные диапазонные типы: int4range, int8range, numrange, tsrange, tstzrange, daterange. Плюс расширения: pg_trgm (нечёткий поиск), uuid-ossp, hstore, ltree (иерархии).' },
        { type: 'code', language: 'sql', value: '-- Диапазонные типы\nCREATE TABLE hotel_bookings (\n    id          SERIAL PRIMARY KEY,\n    room_id     INTEGER,\n    guest_name  TEXT,\n    period      DATERANGE NOT NULL,  -- [from, to)\n    CONSTRAINT no_overlap EXCLUDE USING GiST (room_id WITH =, period WITH &&)\n    -- EXCLUDE: запрещает пересечение периодов для одной комнаты!\n);\n\nINSERT INTO hotel_bookings (room_id, guest_name, period) VALUES\n    (101, \'Алия Бекова\', \'[2024-03-01, 2024-03-05)\'),\n    (101, \'Нурлан Сейт\', \'[2024-03-10, 2024-03-15)\');\n\n-- Это вызовет ошибку (пересечение периодов для room_id=101):\n-- INSERT INTO hotel_bookings VALUES (101, \'Тест\', \'[2024-03-03, 2024-03-08)\');\n\n-- Операторы диапазонов:\nSELECT * FROM hotel_bookings\nWHERE period @> CURRENT_DATE;   -- Дата в диапазоне\n\nSELECT * FROM hotel_bookings\nWHERE period && \'[2024-03-12, 2024-03-20)\'::DATERANGE;  -- Пересечение\n\nSELECT lower(period), upper(period) FROM hotel_bookings;  -- Границы\n\n-- pg_trgm: нечёткий поиск (опечатки, похожие строки)\nCREATE EXTENSION IF NOT EXISTS pg_trgm;\n\n-- Схожесть (0 - нет совпадений, 1 - полное совпадение):\nSELECT similarity(\'postgresql\', \'postgresql\');  -- 1.0\nSELECT similarity(\'postgresql\', \'postgresgl\');  -- 0.55 (опечатка)\nSELECT similarity(\'postgresql\', \'mysql\');       -- 0.0\n\nCREATE INDEX idx_products_name_trgm ON products USING GIN (name gin_trgm_ops);\n\n-- Поиск похожих (опечатки, частичные совпадения):\nSELECT name, similarity(name, \'iphone\') AS sim\nFROM products\nWHERE name % \'iphone\'  -- Оператор схожести\nORDER BY sim DESC;\n\n-- uuid-ossp: генерация UUID\nCREATE EXTENSION IF NOT EXISTS \"uuid-ossp\";\nSELECT uuid_generate_v4();  -- a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11\n\n-- ltree: иерархические пути\nCREATE EXTENSION IF NOT EXISTS ltree;\nCREATE TABLE categories_tree (\n    id    SERIAL PRIMARY KEY,\n    name  TEXT,\n    path  LTREE  -- Путь: \'Electronics.Phones.Smartphones\'\n);\n\nINSERT INTO categories_tree (name, path) VALUES\n    (\'Все товары\',        \'Root\'),\n    (\'Электроника\',       \'Root.Electronics\'),\n    (\'Смартфоны\',         \'Root.Electronics.Phones.Smartphones\');\n\nSELECT name FROM categories_tree\nWHERE path <@ \'Root.Electronics\';  -- Все потомки Electronics\nSELECT name FROM categories_tree\nWHERE path ~ \'Root.Electronics.*{1}\';  -- Прямые дети Electronics' },
        { type: 'note', value: 'EXCLUDE с GiST индексом для диапазонов — элегантный способ запрещать пересечение бронирований/периодов без триггеров. Работает как UNIQUE но для диапазонов: EXCLUDE USING GiST (room_id WITH =, period WITH &&).' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Гибкий каталог товаров с JSONB и поиском',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создай гибкий каталог товаров используя JSONB, массивы и полнотекстовый поиск.',
      requirements: [
        'Таблица products с JSONB атрибутами (specs) и массивом тегов (tags TEXT[])',
        'GIN индексы для specs (@>) и tags (@>), и для полнотекстового поиска',
        'Запросы: найти все товары где specs содержит {color: "black"}, теги содержат "sale"',
        'Полнотекстовый поиск: найти товары по ключевому слову с ранжированием и сниппетом',
        'Агрегация: подсчитать количество товаров по каждому цвету из specs',
        'unnest тегов: список всех уникальных тегов с количеством товаров'
      ],
      hint: 'Для агрегации по цвету из JSONB: GROUP BY specs ->> \'color\'. Для unnest тегов: SELECT unnest(tags) AS tag, COUNT(*) FROM products GROUP BY tag. Для full-text: ts_rank() + ORDER BY.',
      expectedOutput: '-- Черные товары (specs @> {color: black}):\n name          | color\n---------------+-------\n iPhone 15 Pro | black\n(1 row)\n\n-- Товары с тегом "sale":\n name           | price\n----------------+--------\n AirPods Pro    | 120000\n iPhone 15 Pro  | 450000\n MacBook Air M3 | 680000\n(3 rows)\n\n-- Полнотекстовый поиск "смартфон apple":\n name            | price  | rank   | snippet\n-----------------+--------+--------+----------------------------------------------\n iPhone 15 Pro   | 450000 | 0.1026 | Флагманский <b>смартфон</b> <b>Apple</b> ...\n Samsung Galaxy  | 380000 | 0.0475 | <b>Смартфон</b> Samsung с камерой...\n(2 rows)\n\n-- Агрегация по цвету:\n color  | products_count | avg_price\n--------+----------------+-----------\n white  |              2 |    250000\n black  |              1 |    450000\n silver |              1 |    680000\n(3 rows)\n\n-- Теги (unnest) с количеством товаров:\n tag        | product_count\n------------+---------------\n apple      |             3\n sale       |             2\n смартфон  |             2\n android    |             1\n ноутбук   |             1\n naushniki  |             1\n wireless   |             1\n(7 rows)',
      solution: '-- 1. Таблица\nCREATE TABLE products (\n    id          SERIAL PRIMARY KEY,\n    name        TEXT NOT NULL,\n    description TEXT,\n    price       DECIMAL(12, 2),\n    specs       JSONB DEFAULT \'{}\'::JSONB,\n    tags        TEXT[] DEFAULT \'{}\'::TEXT[],\n    search_vec  TSVECTOR,\n    created_at  TIMESTAMPTZ DEFAULT NOW()\n);\n\n-- 2. Индексы\nCREATE INDEX idx_products_specs  ON products USING GIN (specs);\nCREATE INDEX idx_products_tags   ON products USING GIN (tags);\nCREATE INDEX idx_products_search ON products USING GIN (search_vec);\nCREATE INDEX idx_products_name_trgm ON products USING GIN (name gin_trgm_ops);\n\n-- Триггер для search_vec\nCREATE OR REPLACE FUNCTION update_product_search()\nRETURNS TRIGGER AS $$\nBEGIN\n    NEW.search_vec :=\n        setweight(to_tsvector(\'russian\', COALESCE(NEW.name, \'\')), \'A\') ||\n        setweight(to_tsvector(\'russian\', COALESCE(NEW.description, \'\')), \'B\') ||\n        setweight(to_tsvector(\'english\', COALESCE(NEW.name, \'\')), \'A\');\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER products_search_update\n    BEFORE INSERT OR UPDATE OF name, description ON products\n    FOR EACH ROW EXECUTE FUNCTION update_product_search();\n\n-- 3. Данные\nINSERT INTO products (name, description, price, specs, tags) VALUES\n    (\'iPhone 15 Pro\', \'Флагманский смартфон Apple с чипом A17 Pro\', 450000,\n     \'{"color": "black", "storage": "256GB", "5G": true, "battery": 3274}\',\n     ARRAY[\'смартфон\', \'apple\', \'sale\', \'популярное\']),\n    (\'Samsung Galaxy S24\', \'Смартфон Samsung с камерой 200MP\', 380000,\n     \'{"color": "white", "storage": "128GB", "5G": true, "battery": 4000}\',\n     ARRAY[\'смартфон\', \'samsung\', \'android\']),\n    (\'MacBook Air M3\', \'Ноутбук Apple с чипом M3\', 680000,\n     \'{"color": "silver", "ram": "16GB", "storage": "512GB"}\',\n     ARRAY[\'ноутбук\', \'apple\', \'sale\']),\n    (\'AirPods Pro\', \'Беспроводные наушники с шумоподавлением\', 120000,\n     \'{"color": "white", "battery": 6, "noise_cancellation": true}\',\n     ARRAY[\'наушники\', \'apple\', \'wireless\']);\n\n-- 4. Запросы\n-- Черные товары:\nSELECT name, specs ->> \'color\' AS color\nFROM products\nWHERE specs @> \'{"color": "black"}\';\n\n-- Товары на распродаже:\nSELECT name, price FROM products WHERE tags @> ARRAY[\'sale\']\nORDER BY price;\n\n-- 5. Полнотекстовый поиск\nSELECT\n    name,\n    price,\n    ts_rank(search_vec, query) AS rank,\n    ts_headline(\'russian\', description, query, \'MaxWords=15,MinWords=5\') AS snippet\nFROM products,\n     plainto_tsquery(\'russian\', \'смартфон apple\') AS query\nWHERE search_vec @@ query\nORDER BY rank DESC;\n\n-- 6. Агрегация по цвету\nSELECT\n    specs ->> \'color\' AS color,\n    COUNT(*) AS products_count,\n    ROUND(AVG(price), 0) AS avg_price\nFROM products\nWHERE specs ? \'color\'\nGROUP BY specs ->> \'color\'\nORDER BY products_count DESC;\n\n-- 7. Все теги с количеством\nSELECT\n    tag,\n    COUNT(*) AS product_count\nFROM (\n    SELECT unnest(tags) AS tag FROM products\n) t\nGROUP BY tag\nORDER BY product_count DESC, tag;\n\n-- 8. Нечёткий поиск (опечатки)\nSELECT name, similarity(name, \'iphone\') AS sim\nFROM products\nWHERE name % \'iphone\'\nORDER BY sim DESC;',
      explanation: 'JSONB + GIN + full-text = полноценный гибкий каталог без ORM-магии. Ключевые паттерны: specs @> \'{"key": "val"}\' для поиска по атрибутам, unnest(tags) для работы с массивом как таблицей, setweight() для приоритизации заголовков над описаниями при поиске.'
    },
    {
      id: 7,
      title: 'Практика: Система бронирования с диапазонами',
      type: 'practice',
      difficulty: 'medium',
      description: 'Используй диапазонные типы PostgreSQL для системы бронирования.',
      requirements: [
        'Таблица bookings с TSTZRANGE для периодов, EXCLUDE USING GiST для предотвращения пересечений',
        'Проверь что двойное бронирование одной комнаты вызывает ошибку',
        'Найди свободные комнаты на заданный период',
        'Вычисли длительность бронирования в днях используя функции диапазонов',
        'Найди все бронирования пересекающиеся с текущим месяцем'
      ],
      hint: 'EXCLUDE USING GiST (room_id WITH =, period WITH &&) — ключевое ограничение. Длительность: EXTRACT(EPOCH FROM upper(period) - lower(period))/86400. Свободные комнаты: NOT EXISTS с period && запрошенный_период.',
      expectedOutput: '-- Создание таблицы бронирований:\nCREATE TABLE\nCREATE INDEX\nCREATE INDEX\n\n-- Добавление данных (3 бронирования для комнат 101, 101, 102):\nINSERT 0 3\n\n-- Попытка двойного бронирования комнаты 101 (03-07 мар):\nERROR: conflicting key value violates exclusion constraint "room_bookings_room_id_period_excl"\nDETAIL: Key (room_id, period)=(101, [\"2024-03-03 14:00\",\"2024-03-07 12:00\")) conflicts with existing key (room_id, period)=(101, [\"2024-03-01 14:00\",\"2024-03-05 12:00\")).\n\n-- Свободные комнаты 01-05 апреля (все свободны):\n room_id\n--------\n    103\n    104\n(2 rows)\n\n-- Длительность бронирований:\n guest_name      | check_in            | check_out           | nights | total_price\n-----------------+---------------------+---------------------+--------+------------\n Алия Бекова    | 2024-03-01 14:00:00 | 2024-03-05 12:00:00 |    3.9 |    40000.00\n Нурлан Сейт    | 2024-03-10 14:00:00 | 2024-03-15 12:00:00 |    4.9 |    50000.00\n Фарида Омар    | 2024-03-01 14:00:00 | 2024-03-20 12:00:00 |   18.9 |   190000.00\n(3 rows)\n\n-- Бронирования в текущем месяце (март 2024):\n guest_name   | room_id | period\n--------------+---------+---------------------------------------------\n Алия Бекова | 101     | [\"2024-03-01 14:00\",\"2024-03-05 12:00\")\n Нурлан Сейт | 101     | [\"2024-03-10 14:00\",\"2024-03-15 12:00\")\n Фарида Омар | 102     | [\"2024-03-01 14:00\",\"2024-03-20 12:00\")\n(3 rows)',
      solution: '-- Таблица бронирований\nCREATE TABLE room_bookings (\n    id          SERIAL PRIMARY KEY,\n    room_id     INTEGER NOT NULL,\n    guest_name  TEXT NOT NULL,\n    period      TSTZRANGE NOT NULL,\n    total_price DECIMAL(10, 2),\n    CONSTRAINT no_double_booking\n        EXCLUDE USING GiST (room_id WITH =, period WITH &&)\n);\n\nCREATE INDEX idx_bookings_period ON room_bookings USING GiST (period);\nCREATE INDEX idx_bookings_room   ON room_bookings (room_id);\n\n-- Тестовые данные\nINSERT INTO room_bookings (room_id, guest_name, period, total_price) VALUES\n    (101, \'Алия Бекова\',  \'[2024-03-01 14:00, 2024-03-05 12:00)\', 40000),\n    (101, \'Нурлан Сейт\',  \'[2024-03-10 14:00, 2024-03-15 12:00)\', 50000),\n    (102, \'Фарида Омар\',  \'[2024-03-01 14:00, 2024-03-20 12:00)\', 190000);\n\n-- Попытка двойного бронирования (вызовет ошибку):\n-- INSERT INTO room_bookings VALUES (101, \'Тест\', \'[2024-03-03, 2024-03-07)\', 40000);\n-- ERROR: conflicting key value violates exclusion constraint\n\n-- Свободные комнаты на период:\nSELECT r.id AS room_id\nFROM rooms r\nWHERE NOT EXISTS (\n    SELECT 1 FROM room_bookings b\n    WHERE b.room_id = r.id\n      AND b.period && \'[2024-04-01 14:00, 2024-04-05 12:00)\'::TSTZRANGE\n);\n\n-- Длительность бронирований в днях:\nSELECT\n    guest_name,\n    lower(period) AS check_in,\n    upper(period) AS check_out,\n    ROUND(\n        EXTRACT(EPOCH FROM upper(period) - lower(period)) / 86400\n    , 1) AS nights,\n    total_price\nFROM room_bookings\nORDER BY lower(period);\n\n-- Бронирования в текущем месяце:\nSELECT guest_name, room_id, period\nFROM room_bookings\nWHERE period && tstzrange(\n    DATE_TRUNC(\'month\', NOW()),\n    DATE_TRUNC(\'month\', NOW()) + INTERVAL \'1 month\'\n);',
      explanation: 'EXCLUDE USING GiST с диапазонами — декларативный способ обеспечить непересечение. Это лучше триггеров: работает на уровне БД и атомарно. GiST индекс ускоряет как EXCLUDE, так и запросы с && (пересечение) и @> (вхождение).'
    }
  ]
}
