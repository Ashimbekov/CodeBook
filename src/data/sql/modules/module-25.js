export default {
  id: 25,
  title: 'Практикум: Проектирование баз данных',
  description: 'Практикум по проектированию реальных схем баз данных. ER-диаграммы в SQL, нормализация, выбор типов данных, индексы, ограничения целостности. Проекты от простых к сложным.',
  lessons: [
    {
      id: 1,
      title: 'Проект: Система управления задачами (Task Tracker)',
      type: 'practice',
      difficulty: 'medium',
      description: 'Спроектируй схему базы данных для системы управления задачами типа Jira.',
      requirements: [
        'Сущности: users, projects, sprints, tasks, comments, attachments, task_history',
        'Задача может иметь подзадачи (иерархия через parent_id)',
        'История изменений статуса задачи: кто изменил, когда, с какого на какой статус',
        'Пользователь может быть назначен на несколько задач, задача может иметь несколько исполнителей',
        'Добавь все необходимые FK, CHECK ограничения, индексы',
        'Напиши 5 типичных запросов: задачи спринта, мои задачи, история задачи, задачи по приоритету, поиск'
      ],
      hint: 'tasks.parent_id -> tasks.id (self-reference). Несколько исполнителей -> промежуточная таблица task_assignees. История изменений -> task_history с old_status/new_status.',
      solution: '-- СХЕМА Task Tracker\n\nCREATE TABLE users (\n    id          BIGSERIAL PRIMARY KEY,\n    username    VARCHAR(100) UNIQUE NOT NULL,\n    email       VARCHAR(255) UNIQUE NOT NULL,\n    full_name   VARCHAR(200) NOT NULL,\n    avatar_url  TEXT,\n    role        VARCHAR(50) NOT NULL DEFAULT \'developer\'\n                CHECK (role IN (\'admin\', \'manager\', \'developer\', \'tester\')),\n    active      BOOLEAN NOT NULL DEFAULT TRUE,\n    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\nCREATE TABLE projects (\n    id          BIGSERIAL PRIMARY KEY,\n    key         VARCHAR(10) UNIQUE NOT NULL,  -- PROJ, DEV, BUG\n    name        VARCHAR(200) NOT NULL,\n    description TEXT,\n    owner_id    BIGINT NOT NULL REFERENCES users(id),\n    status      VARCHAR(20) NOT NULL DEFAULT \'active\'\n                CHECK (status IN (\'active\', \'archived\', \'closed\')),\n    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\nCREATE TABLE sprints (\n    id          BIGSERIAL PRIMARY KEY,\n    project_id  BIGINT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,\n    name        VARCHAR(200) NOT NULL,\n    goal        TEXT,\n    start_date  DATE NOT NULL,\n    end_date    DATE NOT NULL,\n    status      VARCHAR(20) NOT NULL DEFAULT \'planned\'\n                CHECK (status IN (\'planned\', \'active\', \'completed\')),\n    CONSTRAINT sprint_dates_valid CHECK (end_date > start_date)\n);\n\nCREATE TABLE tasks (\n    id          BIGSERIAL PRIMARY KEY,\n    project_id  BIGINT NOT NULL REFERENCES projects(id),\n    sprint_id   BIGINT REFERENCES sprints(id),\n    parent_id   BIGINT REFERENCES tasks(id),  -- Подзадачи!\n    title       VARCHAR(500) NOT NULL,\n    description TEXT,\n    type        VARCHAR(20) NOT NULL DEFAULT \'task\'\n                CHECK (type IN (\'epic\', \'story\', \'task\', \'bug\', \'subtask\')),\n    status      VARCHAR(30) NOT NULL DEFAULT \'todo\'\n                CHECK (status IN (\'todo\', \'in_progress\', \'review\', \'testing\', \'done\', \'cancelled\')),\n    priority    SMALLINT NOT NULL DEFAULT 3\n                CHECK (priority BETWEEN 1 AND 5),  -- 1=критичный, 5=низкий\n    story_points SMALLINT CHECK (story_points > 0),\n    reporter_id BIGINT NOT NULL REFERENCES users(id),\n    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    due_date    DATE,\n    resolved_at TIMESTAMPTZ\n);\n\nCREATE TABLE task_assignees (\n    task_id     BIGINT NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,\n    user_id     BIGINT NOT NULL REFERENCES users(id),\n    assigned_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    PRIMARY KEY (task_id, user_id)\n);\n\nCREATE TABLE comments (\n    id          BIGSERIAL PRIMARY KEY,\n    task_id     BIGINT NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,\n    author_id   BIGINT NOT NULL REFERENCES users(id),\n    body        TEXT NOT NULL,\n    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\nCREATE TABLE task_history (\n    id          BIGSERIAL PRIMARY KEY,\n    task_id     BIGINT NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,\n    changed_by  BIGINT NOT NULL REFERENCES users(id),\n    field_name  VARCHAR(50) NOT NULL,\n    old_value   TEXT,\n    new_value   TEXT,\n    changed_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\n-- ИНДЕКСЫ\nCREATE INDEX idx_tasks_project   ON tasks(project_id, status);\nCREATE INDEX idx_tasks_sprint    ON tasks(sprint_id);\nCREATE INDEX idx_tasks_parent    ON tasks(parent_id);\nCREATE INDEX idx_tasks_priority  ON tasks(priority, status);\nCREATE INDEX idx_task_hist_task  ON task_history(task_id, changed_at DESC);\nCREATE INDEX idx_assignees_user  ON task_assignees(user_id);\n\n-- ЗАПРОСЫ\n\n-- 1. Задачи текущего спринта\nSELECT\n    t.id,\n    t.title,\n    t.type,\n    t.status,\n    t.priority,\n    t.story_points,\n    STRING_AGG(u.full_name, \', \') AS assignees\nFROM tasks t\nLEFT JOIN task_assignees ta ON t.id = ta.task_id\nLEFT JOIN users u ON ta.user_id = u.id\nWHERE t.sprint_id = (\n    SELECT id FROM sprints\n    WHERE project_id = 1 AND status = \'active\'\n    LIMIT 1\n)\nGROUP BY t.id, t.title, t.type, t.status, t.priority, t.story_points\nORDER BY t.priority, t.status;\n\n-- 2. Мои задачи (назначены на меня)\nSELECT t.id, t.title, t.status, p.name AS project\nFROM tasks t\nJOIN task_assignees ta ON t.id = ta.task_id\nJOIN projects p ON t.project_id = p.id\nWHERE ta.user_id = 42 AND t.status NOT IN (\'done\', \'cancelled\')\nORDER BY t.priority;\n\n-- 3. История задачи\nSELECT th.changed_at, u.full_name, th.field_name, th.old_value, th.new_value\nFROM task_history th\nJOIN users u ON th.changed_by = u.id\nWHERE th.task_id = 100\nORDER BY th.changed_at;\n\n-- 4. Задачи по приоритету с подзадачами\nWITH RECURSIVE task_tree AS (\n    SELECT t.*, 0 AS depth\n    FROM tasks t\n    WHERE t.parent_id IS NULL AND t.project_id = 1\n    UNION ALL\n    SELECT t.*, tt.depth + 1\n    FROM tasks t JOIN task_tree tt ON t.parent_id = tt.id\n)\nSELECT\n    REPEAT(\'  \', depth) || title AS task_title,\n    status, priority, story_points\nFROM task_tree\nORDER BY priority, depth;\n\n-- 5. Поиск задач\nSELECT id, title, status\nFROM tasks\nWHERE title ILIKE \'%баг%\' OR description ILIKE \'%баг%\'\nORDER BY created_at DESC LIMIT 20;',
      explanation: 'Задача с подзадачами — иерархия через self-reference (parent_id). Многие-ко-многим (задача <-> исполнители) через task_assignees. История изменений как append-only лог. Индексы: (project_id, status) для "задачи проекта по статусу", sprint_id для "задачи спринта".'
    },
    {
      id: 2,
      title: 'Проект: Платёжная система',
      type: 'practice',
      difficulty: 'hard',
      description: 'Спроектируй схему платёжной системы с поддержкой мультивалютности.',
      requirements: [
        'Сущности: accounts (счета), transactions (транзакции), currency_rates (курсы валют)',
        'Каждый счёт имеет валюту. Переводы между разными валютами конвертируются',
        'Транзакция может быть: transfer (перевод), deposit (пополнение), withdrawal (вывод), fee (комиссия)',
        'Полная трассируемость: каждая запись неизменна (append-only), двойная запись (double-entry)',
        'Запросы: баланс счёта, история транзакций, выписка, курсы конвертации'
      ],
      hint: 'Двойная запись: каждая операция создаёт 2 записи — дебет и кредит. Баланс = SUM(amount) где account_id = X. Append-only: нет UPDATE, только INSERT. CHECK: сумма транзакции > 0.',
      solution: 'CREATE TABLE currencies (\n    code        CHAR(3) PRIMARY KEY,  -- USD, KZT, EUR\n    name        VARCHAR(100) NOT NULL,\n    symbol      VARCHAR(5) NOT NULL,\n    decimals    SMALLINT NOT NULL DEFAULT 2\n);\n\nINSERT INTO currencies VALUES\n    (\'KZT\', \'Казахстанский тенге\', \'₸\', 2),\n    (\'USD\', \'US Dollar\', \'$\', 2),\n    (\'EUR\', \'Euro\', \'€\', 2);\n\nCREATE TABLE currency_rates (\n    id            BIGSERIAL PRIMARY KEY,\n    from_currency CHAR(3) NOT NULL REFERENCES currencies(code),\n    to_currency   CHAR(3) NOT NULL REFERENCES currencies(code),\n    rate          DECIMAL(18, 8) NOT NULL CHECK (rate > 0),\n    valid_from    TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    valid_to      TIMESTAMPTZ,\n    CHECK (from_currency <> to_currency)\n);\n\nCREATE TABLE accounts (\n    id            BIGSERIAL PRIMARY KEY,\n    owner_id      BIGINT NOT NULL,\n    currency_code CHAR(3) NOT NULL REFERENCES currencies(code),\n    type          VARCHAR(20) NOT NULL\n                  CHECK (type IN (\'personal\', \'business\', \'savings\', \'system\')),\n    status        VARCHAR(20) NOT NULL DEFAULT \'active\'\n                  CHECK (status IN (\'active\', \'frozen\', \'closed\')),\n    created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\n-- Двойная запись: каждая операция = минимум 2 записи\nCREATE TABLE journal_entries (\n    id              BIGSERIAL PRIMARY KEY,\n    transaction_ref  UUID NOT NULL DEFAULT gen_random_uuid(),\n    account_id      BIGINT NOT NULL REFERENCES accounts(id),\n    amount          DECIMAL(18, 2) NOT NULL,  -- + кредит, - дебет\n    currency_code   CHAR(3) NOT NULL REFERENCES currencies(code),\n    type            VARCHAR(20) NOT NULL\n                    CHECK (type IN (\'transfer\', \'deposit\', \'withdrawal\', \'fee\', \'conversion\')),\n    description     TEXT,\n    balance_after   DECIMAL(18, 2) NOT NULL,  -- Баланс после операции\n    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    -- Append-only: запрет на UPDATE/DELETE\n    CONSTRAINT journal_immutable CHECK (TRUE)  -- Усилить через триггер\n);\n\n-- Запрет на изменение записей журнала\nCREATE OR REPLACE FUNCTION prevent_journal_modification()\nRETURNS TRIGGER AS $$\nBEGIN\n    RAISE EXCEPTION \'Записи журнала неизменны!';\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER journal_immutable\n    BEFORE UPDATE OR DELETE ON journal_entries\n    FOR EACH ROW EXECUTE FUNCTION prevent_journal_modification();\n\nCREATE INDEX idx_journal_account  ON journal_entries(account_id, created_at DESC);\nCREATE INDEX idx_journal_txn_ref  ON journal_entries(transaction_ref);\n\n-- ЗАПРОСЫ\n\n-- 1. Текущий баланс счёта\nSELECT\n    a.id,\n    a.currency_code,\n    SUM(je.amount) AS balance\nFROM accounts a\nJOIN journal_entries je ON a.id = je.account_id\nWHERE a.id = 42\nGROUP BY a.id, a.currency_code;\n-- Или более эффективно: последний balance_after\nSELECT balance_after AS current_balance\nFROM journal_entries\nWHERE account_id = 42\nORDER BY created_at DESC LIMIT 1;\n\n-- 2. История транзакций (выписка)\nSELECT\n    je.created_at,\n    je.type,\n    je.description,\n    je.amount,\n    je.currency_code,\n    je.balance_after\nFROM journal_entries je\nWHERE je.account_id = 42\n  AND je.created_at >= \'2024-01-01\'\nORDER BY je.created_at DESC;\n\n-- 3. Перевод с конвертацией валюты\nBEGIN;\n-- Списать с KZT счёта\nINSERT INTO journal_entries (account_id, amount, currency_code, type, description, balance_after)\nVALUES (\n    1,                    -- Счёт отправителя (KZT)\n    -450000,              -- Сумма в KZT\n    \'KZT\',\n    \'conversion\',\n    \'Конвертация KZT -> USD\',\n    (SELECT balance_after FROM journal_entries WHERE account_id = 1 ORDER BY id DESC LIMIT 1) - 450000\n);\n-- Зачислить на USD счёт (курс 450 KZT/USD)\nINSERT INTO journal_entries (account_id, amount, currency_code, type, description, balance_after)\nVALUES (\n    2,                    -- Счёт получателя (USD)\n    1000,                 -- 450000 / 450 = 1000 USD\n    \'USD\',\n    \'conversion\',\n    \'Конвертация KZT -> USD\',\n    (SELECT balance_after FROM journal_entries WHERE account_id = 2 ORDER BY id DESC LIMIT 1) + 1000\n);\nCOMMIT;',
      explanation: 'Двойная запись (double-entry): каждая операция создаёт взаимосвязанные записи. Баланс всегда вычисляется как SUM(amount) — нет отдельного поля которое нужно обновлять. Append-only журнал + триггер защищает от модификации истории. transaction_ref связывает части одной операции.'
    },
    {
      id: 3,
      title: 'Проект: Социальная сеть (упрощённая)',
      type: 'practice',
      difficulty: 'hard',
      description: 'Спроектируй схему социальной сети: пользователи, посты, лайки, подписки, комментарии, лента.',
      requirements: [
        'users, posts (с media), comments (иерархические), likes (полиморфные), follows',
        'Лайки полиморфные: можно лайкнуть и пост и комментарий',
        'Подписки: пользователь A подписан на пользователя B',
        'Лента: посты пользователей на которых я подписан',
        'Популярные посты: лайки за последние 24 часа',
        'Общие друзья: подписчики которые подписаны и на A и на B'
      ],
      hint: 'Полиморфные лайки: (target_type TEXT, target_id BIGINT) или отдельные таблицы post_likes/comment_likes. Лента: WHERE user_id IN (SELECT following_id FROM follows WHERE follower_id = me). Общие подписчики: INTERSECT или JOIN.',
      solution: 'CREATE TABLE users (\n    id          BIGSERIAL PRIMARY KEY,\n    username    VARCHAR(50) UNIQUE NOT NULL,\n    email       VARCHAR(255) UNIQUE NOT NULL,\n    bio         TEXT,\n    avatar_url  TEXT,\n    is_private  BOOLEAN NOT NULL DEFAULT FALSE,\n    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\nCREATE TABLE posts (\n    id          BIGSERIAL PRIMARY KEY,\n    author_id   BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,\n    body        TEXT,\n    media_urls  TEXT[] DEFAULT ARRAY[]::TEXT[],\n    visibility  VARCHAR(20) NOT NULL DEFAULT \'public\'\n                CHECK (visibility IN (\'public\', \'followers\', \'private\')),\n    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\nCREATE TABLE comments (\n    id          BIGSERIAL PRIMARY KEY,\n    post_id     BIGINT NOT NULL REFERENCES posts(id) ON DELETE CASCADE,\n    author_id   BIGINT NOT NULL REFERENCES users(id),\n    parent_id   BIGINT REFERENCES comments(id),  -- Вложенные комментарии\n    body        TEXT NOT NULL,\n    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\n-- Полиморфные лайки\nCREATE TABLE likes (\n    id          BIGSERIAL PRIMARY KEY,\n    user_id     BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,\n    target_type VARCHAR(20) NOT NULL CHECK (target_type IN (\'post\', \'comment\')),\n    target_id   BIGINT NOT NULL,\n    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    UNIQUE (user_id, target_type, target_id)\n);\n\nCREATE TABLE follows (\n    follower_id  BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,\n    following_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,\n    created_at   TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    PRIMARY KEY (follower_id, following_id),\n    CHECK (follower_id <> following_id)\n);\n\nCREATE INDEX idx_posts_author     ON posts(author_id, created_at DESC);\nCREATE INDEX idx_comments_post    ON comments(post_id, created_at);\nCREATE INDEX idx_likes_target     ON likes(target_type, target_id);\nCREATE INDEX idx_follows_follower ON follows(follower_id);\nCREATE INDEX idx_follows_following ON follows(following_id);\n\n-- ЗАПРОСЫ\n\n-- 1. Лента пользователя\nSELECT\n    p.id, p.body, p.media_urls, p.created_at,\n    u.username, u.avatar_url,\n    COUNT(l.id) AS likes_count\nFROM posts p\nJOIN users u ON p.author_id = u.id\nLEFT JOIN likes l ON l.target_type = \'post\' AND l.target_id = p.id\nWHERE p.author_id IN (\n    SELECT following_id FROM follows WHERE follower_id = 42\n)\nGROUP BY p.id, u.username, u.avatar_url\nORDER BY p.created_at DESC\nLIMIT 20;\n\n-- 2. Популярные посты за 24 часа\nSELECT\n    p.id, p.body, u.username,\n    COUNT(l.id) AS recent_likes\nFROM posts p\nJOIN users u ON p.author_id = u.id\nJOIN likes l ON l.target_type = \'post\' AND l.target_id = p.id\nWHERE l.created_at >= NOW() - INTERVAL \'24 hours\'\nGROUP BY p.id, p.body, u.username\nORDER BY recent_likes DESC\nLIMIT 10;\n\n-- 3. Общие подписчики (взаимные)\nSELECT u.id, u.username\nFROM follows f1\nJOIN follows f2 ON f1.following_id = f2.following_id\nJOIN users u ON f1.following_id = u.id\nWHERE f1.follower_id = 1   -- Я\n  AND f2.follower_id = 2;  -- Другой пользователь\n-- ИЛИ через INTERSECT:\n-- SELECT following_id FROM follows WHERE follower_id = 1\n-- INTERSECT\n-- SELECT following_id FROM follows WHERE follower_id = 2;\n\n-- 4. Профиль пользователя\nSELECT\n    u.username, u.bio, u.avatar_url,\n    COUNT(DISTINCT p.id) AS posts_count,\n    COUNT(DISTINCT f1.follower_id) AS followers_count,\n    COUNT(DISTINCT f2.following_id) AS following_count\nFROM users u\nLEFT JOIN posts p ON u.id = p.author_id\nLEFT JOIN follows f1 ON u.id = f1.following_id\nLEFT JOIN follows f2 ON u.id = f2.follower_id\nWHERE u.id = 42\nGROUP BY u.id, u.username, u.bio, u.avatar_url;',
      explanation: 'Полиморфные лайки (target_type + target_id) — гибкость ценой невозможности FK. Альтернатива: отдельные таблицы post_likes и comment_likes с FK — более строгая целостность. INTERSECT — элегантный способ найти пересечение двух множеств. Индекс на likes(target_type, target_id) критичен для подсчёта лайков.'
    },
    {
      id: 4,
      title: 'Рефакторинг: Нормализация реального проекта',
      type: 'practice',
      difficulty: 'hard',
      description: 'Возьми ненормализованную схему и приведи к 3NF без потери данных.',
      requirements: [
        'Дана flat-таблица: orders_flat (order_id, order_date, customer_name, customer_email, customer_city, product_names, product_prices, quantities, total)',
        'Определи нарушения 1NF, 2NF, 3NF',
        'Создай нормализованную схему: customers, products, orders, order_items',
        'Мигрируй данные из flat в нормализованную схему',
        'Создай View воссоздающий исходную flat-таблицу для обратной совместимости',
        'Проверь что данные мигрированы корректно через сверку итогов'
      ],
      hint: 'Для разбивки product_names (список через запятую): UNNEST(STRING_TO_ARRAY(product_names, \',\')). При миграции используй INSERT ... SELECT с JOIN или подзапросами. View для обратной совместимости позволяет старому коду работать без изменений.',
      solution: '-- ИСХОДНАЯ ТАБЛИЦА (денормализованная)\nCREATE TABLE orders_flat (\n    order_id      INTEGER,\n    order_date    DATE,\n    customer_name VARCHAR(200),\n    customer_email VARCHAR(255),\n    customer_city VARCHAR(100),\n    product_names TEXT,    -- "iPhone, Чехол, Наушники"\n    product_prices TEXT,   -- "95000, 5000, 12000"\n    quantities    TEXT,    -- "1, 2, 1"\n    total         DECIMAL\n);\n\n-- АНАЛИЗ НАРУШЕНИЙ:\n-- 1NF: product_names, product_prices, quantities = списки в одной ячейке\n-- 2NF: customer_name/email/city зависят только от customer (через order_id)\n-- 3NF: нет отдельных транзитивных зависимостей после нормализации до 2NF\n\n-- НОРМАЛИЗОВАННАЯ СХЕМА\nCREATE TABLE customers (\n    id    BIGSERIAL PRIMARY KEY,\n    name  VARCHAR(200) NOT NULL,\n    email VARCHAR(255) UNIQUE NOT NULL,\n    city  VARCHAR(100)\n);\n\nCREATE TABLE products (\n    id    BIGSERIAL PRIMARY KEY,\n    name  VARCHAR(300) NOT NULL,\n    price DECIMAL(12, 2) NOT NULL\n);\n\nCREATE TABLE orders (\n    id          INTEGER PRIMARY KEY,  -- Сохраняем исходные ID\n    customer_id BIGINT NOT NULL REFERENCES customers(id),\n    order_date  DATE NOT NULL,\n    total       DECIMAL(15, 2) NOT NULL\n);\n\nCREATE TABLE order_items (\n    order_id   INTEGER NOT NULL REFERENCES orders(id),\n    product_id BIGINT NOT NULL REFERENCES products(id),\n    quantity   INTEGER NOT NULL CHECK (quantity > 0),\n    unit_price DECIMAL(12, 2) NOT NULL,\n    PRIMARY KEY (order_id, product_id)\n);\n\n-- МИГРАЦИЯ ДАННЫХ\n\n-- Шаг 1: Уникальные клиенты\nINSERT INTO customers (name, email, city)\nSELECT DISTINCT customer_name, customer_email, customer_city\nFROM orders_flat\nON CONFLICT (email) DO NOTHING;\n\n-- Шаг 2: Уникальные продукты\n-- Разбить списки в строки:\nINSERT INTO products (name, price)\nSELECT DISTINCT\n    TRIM(names.name),\n    TRIM(prices.price)::DECIMAL\nFROM orders_flat f,\n     UNNEST(STRING_TO_ARRAY(f.product_names, \',\')) WITH ORDINALITY AS names(name, ord),\n     UNNEST(STRING_TO_ARRAY(f.product_prices, \',\')) WITH ORDINALITY AS prices(price, ord)\nWHERE names.ord = prices.ord\nON CONFLICT DO NOTHING;\n\n-- Шаг 3: Заказы\nINSERT INTO orders (id, customer_id, order_date, total)\nSELECT\n    f.order_id,\n    c.id,\n    f.order_date,\n    f.total\nFROM orders_flat f\nJOIN customers c ON c.email = f.customer_email;\n\n-- Шаг 4: Позиции заказов\nINSERT INTO order_items (order_id, product_id, quantity, unit_price)\nSELECT\n    f.order_id,\n    p.id,\n    TRIM(qtys.qty)::INTEGER,\n    TRIM(prices.price)::DECIMAL\nFROM orders_flat f,\n     UNNEST(STRING_TO_ARRAY(f.product_names, \',\'))  WITH ORDINALITY AS names(name, ord),\n     UNNEST(STRING_TO_ARRAY(f.product_prices, \',\')) WITH ORDINALITY AS prices(price, ord),\n     UNNEST(STRING_TO_ARRAY(f.quantities, \',\'))     WITH ORDINALITY AS qtys(qty, ord)\nJOIN products p ON p.name = TRIM(names.name)\nWHERE names.ord = prices.ord AND prices.ord = qtys.ord;\n\n-- VIEW для обратной совместимости\nCREATE VIEW orders_flat_v AS\nSELECT\n    o.id AS order_id,\n    o.order_date,\n    c.name  AS customer_name,\n    c.email AS customer_email,\n    c.city  AS customer_city,\n    STRING_AGG(p.name, \', \' ORDER BY p.name)              AS product_names,\n    STRING_AGG(oi.unit_price::TEXT, \', \' ORDER BY p.name) AS product_prices,\n    STRING_AGG(oi.quantity::TEXT, \', \' ORDER BY p.name)   AS quantities,\n    o.total\nFROM orders o\nJOIN customers c    ON o.customer_id = c.id\nJOIN order_items oi ON o.id = oi.order_id\nJOIN products p     ON oi.product_id = p.id\nGROUP BY o.id, o.order_date, c.name, c.email, c.city, o.total;\n\n-- ПРОВЕРКА МИГРАЦИИ\nSELECT\n    \'Исходная\'   AS source,\n    COUNT(*) AS rows,\n    SUM(total) AS total_revenue\nFROM orders_flat\nUNION ALL\nSELECT\n    \'Нормализованная\',\n    COUNT(*),\n    SUM(total)\nFROM orders;',
      explanation: 'UNNEST с ORDINALITY — ключевой паттерн для разбивки нескольких списков одновременно сохраняя позиционное соответствие (имя[1] соответствует цене[1]). View для обратной совместимости позволяет не ломать существующий код. Всегда верифицируй миграцию сверкой итоговых сумм.'
    },
    {
      id: 5,
      title: 'Проект: Система инвентаризации склада',
      type: 'practice',
      difficulty: 'medium',
      description: 'Спроектируй схему складской системы с отслеживанием движения товаров.',
      requirements: [
        'warehouses, locations (стеллаж/ячейка), products, stock_movements (приход/расход/перемещение)',
        'Текущий остаток = сумма всех движений по продукту в локации',
        'Инвентаризация: snapshot остатков на определённую дату',
        'FIFO или LIFO стратегия списания',
        'Минимальные остатки: уведомление когда stock < min_stock'
      ],
      hint: 'Остатки как materialized view или view на SUM(quantity) по движениям. Для FIFO: ближайший приход с позитивным остатком. Движения: +quantity для прихода, -quantity для расхода.',
      solution: 'CREATE TABLE warehouses (\n    id          SERIAL PRIMARY KEY,\n    name        VARCHAR(200) NOT NULL,\n    city        VARCHAR(100),\n    address     TEXT\n);\n\nCREATE TABLE locations (\n    id           SERIAL PRIMARY KEY,\n    warehouse_id INTEGER NOT NULL REFERENCES warehouses(id),\n    aisle        VARCHAR(10) NOT NULL,\n    rack         VARCHAR(10) NOT NULL,\n    position     VARCHAR(10) NOT NULL,\n    max_capacity DECIMAL(10, 2),\n    UNIQUE (warehouse_id, aisle, rack, position)\n);\n\nCREATE TABLE products (\n    id          BIGSERIAL PRIMARY KEY,\n    sku         VARCHAR(100) UNIQUE NOT NULL,\n    name        VARCHAR(300) NOT NULL,\n    unit        VARCHAR(20) NOT NULL DEFAULT \'шт\',\n    min_stock   DECIMAL(10, 2) NOT NULL DEFAULT 0,\n    cost_price  DECIMAL(12, 2)\n);\n\nCREATE TABLE stock_movements (\n    id              BIGSERIAL PRIMARY KEY,\n    product_id      BIGINT NOT NULL REFERENCES products(id),\n    location_id     INTEGER NOT NULL REFERENCES locations(id),\n    quantity        DECIMAL(10, 3) NOT NULL,  -- + приход, - расход\n    movement_type   VARCHAR(20) NOT NULL\n                    CHECK (movement_type IN (\'receipt\', \'shipment\', \'transfer_in\', \'transfer_out\', \'adjustment\', \'inventory\')),\n    reference_doc   VARCHAR(100),  -- Номер накладной\n    unit_cost       DECIMAL(12, 2),\n    created_by      BIGINT,\n    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    notes           TEXT\n);\n\nCREATE INDEX idx_movements_product  ON stock_movements(product_id, created_at);\nCREATE INDEX idx_movements_location ON stock_movements(location_id);\n\n-- VIEW: текущие остатки\nCREATE VIEW current_stock AS\nSELECT\n    sm.product_id,\n    sm.location_id,\n    p.sku,\n    p.name AS product_name,\n    w.name AS warehouse_name,\n    l.aisle || \'-\' || l.rack || \'-\' || l.position AS location_code,\n    SUM(sm.quantity) AS current_quantity,\n    p.min_stock,\n    CASE WHEN SUM(sm.quantity) < p.min_stock THEN TRUE ELSE FALSE END AS below_min\nFROM stock_movements sm\nJOIN products p   ON sm.product_id = p.id\nJOIN locations l  ON sm.location_id = l.id\nJOIN warehouses w ON l.warehouse_id = w.id\nGROUP BY sm.product_id, sm.location_id, p.sku, p.name, w.name, l.aisle, l.rack, l.position, p.min_stock;\n\n-- Товары ниже минимума\nSELECT product_name, location_code, current_quantity, min_stock\nFROM current_stock\nWHERE below_min = TRUE\nORDER BY (current_quantity / NULLIF(min_stock, 0));\n\n-- Snapshot остатков на дату\nSELECT\n    product_id,\n    location_id,\n    SUM(quantity) AS quantity_at_date\nFROM stock_movements\nWHERE created_at < \'2024-06-01\'  -- До нужной даты\nGROUP BY product_id, location_id\nHAVING SUM(quantity) > 0;\n\n-- FIFO: стоимость списания\nWITH receipts AS (\n    SELECT\n        id,\n        product_id,\n        quantity AS original_qty,\n        quantity AS remaining_qty,\n        unit_cost,\n        created_at,\n        ROW_NUMBER() OVER (PARTITION BY product_id ORDER BY created_at) AS fifo_order\n    FROM stock_movements\n    WHERE movement_type = \'receipt\' AND product_id = 42\n)\nSELECT\n    SUM(LEAST(remaining_qty, 10) * unit_cost) AS fifo_cost\n    -- Списать 10 единиц по FIFO\nFROM receipts\nWHERE fifo_order <= (\n    SELECT MIN(r2.fifo_order)\n    FROM receipts r2\n    WHERE SUM(r2.remaining_qty) OVER (ORDER BY r2.fifo_order) >= 10\n);',
      explanation: 'Складская система построена на принципе "учёт движений" — текущий остаток всегда вычисляется как SUM(quantity). Это надёжнее хранения текущего остатка: история движений неизменна, остаток всегда можно пересчитать на любую дату. View current_stock — удобный инструмент для работы с текущими данными.'
    },
    {
      id: 6,
      title: 'Оптимизация существующей схемы',
      type: 'practice',
      difficulty: 'hard',
      description: 'Проанализируй медленную схему и предложи оптимизации с измерением результата.',
      requirements: [
        'Найди все FK без индексов используя системные таблицы',
        'Найди запросы с функциями в WHERE (по pg_stat_statements), перепиши без функций',
        'Добавь частичные индексы для горячих запросов (активные заказы, незакрытые задачи)',
        'Конвертируй часто запрашиваемые агрегаты в Materialized View с автообновлением',
        'Сравни размер таблицы до и после VACUUM FULL + ANALYZE',
        'Создай составные индексы покрывающие самые частые запросы'
      ],
      hint: 'pg_stat_statements.query содержит текст запроса. Ищи EXTRACT, DATE, LOWER, UPPER в тексте запроса. Частичный индекс WHERE status = \'active\' в 5-10x меньше полного.',
      solution: '-- 1. FK без индексов\nSELECT\n    conname AS constraint_name,\n    conrelid::regclass AS table_name,\n    a.attname AS column_name,\n    confrelid::regclass AS references_table\nFROM pg_constraint c\nJOIN pg_attribute a ON a.attrelid = c.conrelid\n                    AND a.attnum = c.conkey[1]\nWHERE c.contype = \'f\'\n  AND NOT EXISTS (\n    SELECT 1 FROM pg_index i\n    JOIN pg_attribute ia ON ia.attrelid = i.indrelid\n                         AND ia.attnum = ANY(i.indkey)\n    WHERE i.indrelid = c.conrelid\n      AND ia.attnum = c.conkey[1]\n  )\nORDER BY table_name;\n\n-- 2. Запросы с функциями в WHERE\nSELECT\n    LEFT(query, 100) AS query_snippet,\n    calls,\n    ROUND(mean_exec_time::NUMERIC, 1) AS avg_ms\nFROM pg_stat_statements\nWHERE query ILIKE \'%EXTRACT(%\' OR query ILIKE \'%DATE(%\'\n   OR query ILIKE \'%LOWER(%\' OR query ILIKE \'%UPPER(%\'\n  AND calls > 10\nORDER BY total_exec_time DESC LIMIT 10;\n\n-- 3. Частичные индексы\nCREATE INDEX CONCURRENTLY idx_orders_active_partial\n    ON orders(customer_id, created_at DESC)\n    WHERE status NOT IN (\'completed\', \'cancelled\');\n-- Только незавершённые заказы (~10% таблицы)\n\nCREATE INDEX CONCURRENTLY idx_tasks_open\n    ON tasks(assignee_id, priority)\n    WHERE status NOT IN (\'done\', \'cancelled\') AND deleted_at IS NULL;\n\n-- Проверить размер частичного vs полного:\nSELECT\n    indexrelname,\n    pg_size_pretty(pg_relation_size(indexrelid)) AS size\nFROM pg_stat_user_indexes\nWHERE relname = \'orders\'\nORDER BY pg_relation_size(indexrelid) DESC;\n\n-- 4. Materialized View для агрегатов\nCREATE MATERIALIZED VIEW product_sales_summary AS\nSELECT\n    p.id AS product_id,\n    p.name,\n    cat.name AS category,\n    COUNT(DISTINCT oi.order_id) AS orders_count,\n    SUM(oi.quantity) AS total_sold,\n    SUM(oi.quantity * oi.unit_price) AS total_revenue,\n    ROUND(AVG(oi.unit_price), 2) AS avg_price\nFROM products p\nJOIN categories cat ON p.category_id = cat.id\nLEFT JOIN order_items oi ON p.id = oi.product_id\nGROUP BY p.id, p.name, cat.name;\n\nCREATE UNIQUE INDEX ON product_sales_summary(product_id);\n\n-- Автообновление через pg_cron (если установлен):\n-- SELECT cron.schedule(\'refresh-product-sales\', \'0 */6 * * *\',\n--   $$REFRESH MATERIALIZED VIEW CONCURRENTLY product_sales_summary$$);\n\n-- 5. Размер до и после VACUUM\nSELECT\n    pg_size_pretty(pg_total_relation_size(\'orders\')) AS before_vacuum;\n\nVACUUM ANALYZE orders;\n\nSELECT\n    pg_size_pretty(pg_total_relation_size(\'orders\')) AS after_vacuum;\n-- VACUUM FULL для максимальной компактизации (блокирует!):\n-- VACUUM FULL ANALYZE orders;\n\n-- 6. Составные покрывающие индексы\n-- Анализ частых запросов:\n-- WHERE status = \'paid\' ORDER BY created_at DESC (+ нужны total, customer_id)\nCREATE INDEX CONCURRENTLY idx_orders_status_date_covering\n    ON orders(status, created_at DESC)\n    INCLUDE (id, customer_id, total)\n    WHERE status IN (\'pending\', \'paid\', \'shipped\');\n\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT id, customer_id, total\nFROM orders\nWHERE status = \'paid\'\nORDER BY created_at DESC\nLIMIT 100;\n-- Index Only Scan using idx_orders_status_date_covering\n-- Heap Fetches: 0',
      explanation: 'Оптимизация схемы — непрерывный процесс. Порядок приоритетов: 1) FK без индексов (мгновенное ускорение JOIN), 2) устранение функций в WHERE (Index Scan вместо Seq Scan), 3) частичные индексы для горячих запросов, 4) Materialized View для тяжёлых агрегатов. Всегда измеряй до и после!'
    },
    {
      id: 7,
      title: 'Финальный проект: E-commerce платформа',
      type: 'practice',
      difficulty: 'hard',
      description: 'Комплексное проектирование полной схемы e-commerce платформы от нуля.',
      requirements: [
        'Полная схема: пользователи, продавцы, товары (с вариантами), заказы, платежи, отзывы, промокоды',
        'Товар с вариантами: base product + variants (color, size, SKU)',
        'Промокоды: процентные и фиксированные, ограничение по использованию, срок',
        'Мультивендорность: несколько продавцов, комиссия платформы',
        'Напиши 5 ключевых запросов: страница товара, корзина, расчёт заказа с промокодом, топ продавцов, рекомендации',
        'Добавь все индексы и ограничения, обоснуй каждое решение'
      ],
      hint: 'Товар с вариантами: products + product_variants (product_id + attr_values JSONB + sku + price). Промокод: CHECK (discount_type IN (percent, fixed)), CHECK (discount_value > 0). Комиссия: orders.platform_fee = total * seller.commission_rate.',
      solution: '-- Полная схема e-commerce\n\nCREATE TABLE users (\n    id            BIGSERIAL PRIMARY KEY,\n    email         VARCHAR(255) UNIQUE NOT NULL,\n    phone         VARCHAR(20),\n    password_hash VARCHAR(255) NOT NULL,\n    full_name     VARCHAR(200) NOT NULL,\n    created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\nCREATE TABLE sellers (\n    id              BIGSERIAL PRIMARY KEY,\n    user_id         BIGINT NOT NULL UNIQUE REFERENCES users(id),\n    shop_name       VARCHAR(200) NOT NULL,\n    commission_rate DECIMAL(4, 2) NOT NULL DEFAULT 0.10\n                    CHECK (commission_rate BETWEEN 0 AND 0.50),\n    status          VARCHAR(20) NOT NULL DEFAULT \'active\'\n                    CHECK (status IN (\'active\', \'suspended\', \'pending\'))\n);\n\nCREATE TABLE categories (\n    id        SERIAL PRIMARY KEY,\n    name      VARCHAR(200) NOT NULL,\n    parent_id INTEGER REFERENCES categories(id)\n);\n\nCREATE TABLE products (\n    id          BIGSERIAL PRIMARY KEY,\n    seller_id   BIGINT NOT NULL REFERENCES sellers(id),\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    name        VARCHAR(500) NOT NULL,\n    description TEXT,\n    base_price  DECIMAL(12, 2) NOT NULL CHECK (base_price >= 0),\n    status      VARCHAR(20) NOT NULL DEFAULT \'active\'\n                CHECK (status IN (\'draft\', \'active\', \'archived\'))\n);\n\nCREATE TABLE product_variants (\n    id          BIGSERIAL PRIMARY KEY,\n    product_id  BIGINT NOT NULL REFERENCES products(id) ON DELETE CASCADE,\n    sku         VARCHAR(100) UNIQUE NOT NULL,\n    attributes  JSONB NOT NULL DEFAULT \'{}\'::JSONB,  -- {color: red, size: XL}\n    price       DECIMAL(12, 2) NOT NULL CHECK (price >= 0),\n    stock       INTEGER NOT NULL DEFAULT 0 CHECK (stock >= 0)\n);\n\nCREATE TABLE promo_codes (\n    id              SERIAL PRIMARY KEY,\n    code            VARCHAR(50) UNIQUE NOT NULL,\n    discount_type   VARCHAR(10) NOT NULL CHECK (discount_type IN (\'percent\', \'fixed\')),\n    discount_value  DECIMAL(10, 2) NOT NULL CHECK (discount_value > 0),\n    min_order_total DECIMAL(10, 2) DEFAULT 0,\n    max_uses        INTEGER,  -- NULL = безлимитный\n    used_count      INTEGER NOT NULL DEFAULT 0,\n    valid_from      TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    valid_to        TIMESTAMPTZ,\n    active          BOOLEAN NOT NULL DEFAULT TRUE\n);\n\nCREATE TABLE orders (\n    id            BIGSERIAL PRIMARY KEY,\n    buyer_id      BIGINT NOT NULL REFERENCES users(id),\n    status        VARCHAR(30) NOT NULL DEFAULT \'cart\'\n                  CHECK (status IN (\'cart\', \'checkout\', \'paid\', \'processing\', \'shipped\', \'delivered\', \'cancelled\', \'refunded\')),\n    subtotal      DECIMAL(15, 2) NOT NULL DEFAULT 0,\n    discount      DECIMAL(15, 2) NOT NULL DEFAULT 0,\n    total         DECIMAL(15, 2) NOT NULL DEFAULT 0,\n    promo_code_id INTEGER REFERENCES promo_codes(id),\n    created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\nCREATE TABLE order_items (\n    id                  BIGSERIAL PRIMARY KEY,\n    order_id            BIGINT NOT NULL REFERENCES orders(id) ON DELETE CASCADE,\n    variant_id          BIGINT NOT NULL REFERENCES product_variants(id),\n    seller_id           BIGINT NOT NULL REFERENCES sellers(id),\n    quantity            INTEGER NOT NULL CHECK (quantity > 0),\n    unit_price          DECIMAL(12, 2) NOT NULL,\n    platform_fee        DECIMAL(12, 2) NOT NULL DEFAULT 0,\n    seller_revenue      DECIMAL(12, 2) NOT NULL DEFAULT 0\n);\n\nCREATE TABLE reviews (\n    id          BIGSERIAL PRIMARY KEY,\n    product_id  BIGINT NOT NULL REFERENCES products(id),\n    user_id     BIGINT NOT NULL REFERENCES users(id),\n    rating      SMALLINT NOT NULL CHECK (rating BETWEEN 1 AND 5),\n    body        TEXT,\n    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    UNIQUE (product_id, user_id)  -- Один отзыв на товар\n);\n\n-- Индексы\nCREATE INDEX idx_products_seller    ON products(seller_id, status);\nCREATE INDEX idx_products_category  ON products(category_id);\nCREATE INDEX idx_variants_product   ON product_variants(product_id);\nCREATE INDEX idx_variants_attrs     ON product_variants USING GIN (attributes);\nCREATE INDEX idx_orders_buyer       ON orders(buyer_id, status);\nCREATE INDEX idx_order_items_order  ON order_items(order_id);\nCREATE INDEX idx_order_items_seller ON order_items(seller_id);\nCREATE INDEX idx_reviews_product    ON reviews(product_id);\n\n-- ЗАПРОСЫ\n\n-- 1. Страница товара\nSELECT\n    p.name, p.description,\n    s.shop_name AS seller,\n    json_agg(json_build_object(\n        \'sku\', pv.sku, \'attrs\', pv.attributes,\n        \'price\', pv.price, \'stock\', pv.stock\n    )) AS variants,\n    ROUND(AVG(r.rating), 1) AS avg_rating,\n    COUNT(r.id) AS reviews_count\nFROM products p\nJOIN sellers s ON p.seller_id = s.id\nJOIN product_variants pv ON p.id = pv.product_id\nLEFT JOIN reviews r ON p.id = r.product_id\nWHERE p.id = 100\nGROUP BY p.id, p.name, p.description, s.shop_name;\n\n-- 2. Расчёт заказа с промокодом\nWITH cart AS (\n    SELECT\n        SUM(oi.quantity * oi.unit_price) AS subtotal\n    FROM order_items oi\n    WHERE oi.order_id = 42\n),\npromo AS (\n    SELECT pc.discount_type, pc.discount_value\n    FROM orders o\n    JOIN promo_codes pc ON o.promo_code_id = pc.id\n    WHERE o.id = 42 AND pc.active AND (pc.valid_to IS NULL OR pc.valid_to > NOW())\n        AND (pc.max_uses IS NULL OR pc.used_count < pc.max_uses)\n)\nSELECT\n    c.subtotal,\n    CASE\n        WHEN p.discount_type = \'percent\' THEN ROUND(c.subtotal * p.discount_value / 100, 2)\n        WHEN p.discount_type = \'fixed\'   THEN LEAST(p.discount_value, c.subtotal)\n        ELSE 0\n    END AS discount,\n    c.subtotal - COALESCE(\n        CASE\n            WHEN p.discount_type = \'percent\' THEN ROUND(c.subtotal * p.discount_value / 100, 2)\n            WHEN p.discount_type = \'fixed\'   THEN LEAST(p.discount_value, c.subtotal)\n        END, 0\n    ) AS final_total\nFROM cart c\nLEFT JOIN promo p ON TRUE;\n\n-- 3. Топ продавцов\nSELECT\n    s.shop_name,\n    COUNT(DISTINCT oi.order_id) AS orders_count,\n    SUM(oi.seller_revenue) AS seller_revenue,\n    SUM(oi.platform_fee) AS platform_revenue,\n    ROUND(AVG(r.rating), 2) AS avg_rating\nFROM sellers s\nJOIN order_items oi ON s.id = oi.seller_id\nJOIN orders o ON oi.order_id = o.id\nLEFT JOIN products p ON p.seller_id = s.id\nLEFT JOIN reviews r ON p.id = r.product_id\nWHERE o.status = \'delivered\'\nGROUP BY s.id, s.shop_name\nORDER BY seller_revenue DESC\nLIMIT 10;\n\n-- 4. Рекомендации (покупали вместе)\nSELECT\n    pv2.product_id AS recommended_id,\n    p2.name AS recommended_name,\n    COUNT(*) AS co_purchase_count\nFROM order_items oi1\nJOIN order_items oi2 ON oi1.order_id = oi2.order_id\n                     AND oi1.variant_id <> oi2.variant_id\nJOIN product_variants pv1 ON oi1.variant_id = pv1.id\nJOIN product_variants pv2 ON oi2.variant_id = pv2.id\nJOIN products p2 ON pv2.product_id = p2.id\nWHERE pv1.product_id = 100  -- Текущий товар\n  AND pv2.product_id <> 100  -- Другие товары\nGROUP BY pv2.product_id, p2.name\nORDER BY co_purchase_count DESC\nLIMIT 6;',
      explanation: 'Полная e-commerce схема демонстрирует ключевые паттерны: product + variants (один товар, много SKU), промокоды с валидацией в запросе, двусторонний учёт финансов (seller_revenue + platform_fee в каждой позиции). Рекомендации через co-purchase analysis — стандартный подход без ML.'
    },
    {
      id: 8,
      title: 'Code Review схем: типичные ошибки',
      type: 'practice',
      difficulty: 'medium',
      description: 'Проанализируй проблемные схемы и предложи улучшения.',
      requirements: [
        'Проблема 1: таблица users с VARCHAR(MAX) для всех полей — определи правильные типы',
        'Проблема 2: отсутствие created_at/updated_at — добавь и создай триггер',
        'Проблема 3: хранение массива ID через запятую в строке — нормализуй',
        'Проблема 4: нет CHECK ограничений на критичные поля (email формат, цена > 0)',
        'Проблема 5: таблица с 50 столбцами — как разбить на связанные группы',
        'Документируй исправления через COMMENT ON'
      ],
      hint: 'VARCHAR(MAX) или TEXT для всего — обычно плохо. Выбирай подходящие типы: email VARCHAR(255), price DECIMAL(12,2), status VARCHAR(20) + CHECK. COMMENT ON TABLE/COLUMN — встроенная документация в PostgreSQL.',
      solution: '-- ПРОБЛЕМА 1: Неправильные типы данных\n-- Было:\n-- CREATE TABLE users (id TEXT, name TEXT, email TEXT, age TEXT, salary TEXT, active TEXT);\n\n-- Исправление:\nCREATE TABLE users_fixed (\n    id       BIGSERIAL PRIMARY KEY,                    -- НЕ TEXT\n    name     VARCHAR(200) NOT NULL,                    -- Ограниченная длина\n    email    VARCHAR(255) UNIQUE NOT NULL,              -- Стандарт: макс 254 символа\n    age      SMALLINT CHECK (age BETWEEN 0 AND 150),  -- НЕ TEXT\n    salary   DECIMAL(15, 2) CHECK (salary >= 0),      -- НЕ TEXT\n    active   BOOLEAN NOT NULL DEFAULT TRUE             -- НЕ TEXT \'true\'/\'false\'!\n);\n\n-- ПРОБЛЕМА 2: Нет audit timestamps\nALTER TABLE products\n    ADD COLUMN IF NOT EXISTS created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    ADD COLUMN IF NOT EXISTS updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW();\n\nCREATE OR REPLACE FUNCTION set_updated_at()\nRETURNS TRIGGER AS $$\nBEGIN\n    NEW.updated_at := NOW();\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER products_updated_at\n    BEFORE UPDATE ON products\n    FOR EACH ROW EXECUTE FUNCTION set_updated_at();\n\n-- ПРОБЛЕМА 3: Массив ID в строке\n-- Было: orders.product_ids TEXT = \'1,5,12,30\'\n\n-- Исправление: нормализованная связующая таблица\nCREATE TABLE order_products (\n    order_id   BIGINT NOT NULL REFERENCES orders(id),\n    product_id BIGINT NOT NULL REFERENCES products(id),\n    quantity   INTEGER NOT NULL DEFAULT 1,\n    PRIMARY KEY (order_id, product_id)\n);\n-- Мигрируй данные:\n-- INSERT INTO order_products (order_id, product_id)\n-- SELECT id, unnest(string_to_array(product_ids, \',\'))::BIGINT\n-- FROM orders;\n\n-- ПРОБЛЕМА 4: Нет CHECK ограничений\nALTER TABLE products\n    ADD CONSTRAINT chk_price_positive CHECK (price > 0),\n    ADD CONSTRAINT chk_stock_non_negative CHECK (stock >= 0);\n\nALTER TABLE users\n    ADD CONSTRAINT chk_email_format\n    CHECK (email ~* \'^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}$\');\n\n-- ПРОБЛЕМА 5: Таблица с 50 столбцами (God Table)\n-- Было: users (id, name, email, ..., shipping_address, billing_address,\n--   payment_method, card_number, ..., preferences, notifications_email,\n--   notifications_sms, ..., stats_orders_count, stats_total_spent, ...)\n\n-- Исправление: разбить по доменам\nCREATE TABLE users (id, name, email, ...);               -- Основные данные\nCREATE TABLE user_addresses (id, user_id, type, ...);   -- Адреса\nCREATE TABLE user_payment_methods (id, user_id, ...);   -- Платёжные данные\nCREATE TABLE user_preferences (user_id PRIMARY KEY, ...); -- 1:1 настройки\n-- stats_* не хранить! Вычислять через запросы или materialized view\n\n-- ДОКУМЕНТАЦИЯ через COMMENT\nCOMMENT ON TABLE users IS \'Зарегистрированные пользователи платформы\';\nCOMMENT ON COLUMN users.email IS \'Уникальный email для входа и уведомлений\';\nCOMMENT ON COLUMN products.price IS \'Цена в тиынах (умножить на 0.01 для тенге)\';\nCOMMENT ON COLUMN orders.status IS \'Статусы: cart -> checkout -> paid -> processing -> shipped -> delivered\';\nCOMMENT ON CONSTRAINT chk_price_positive ON products IS \'Цена всегда положительная\';\n\n-- Просмотр всех комментариев:\nSELECT\n    c.relname AS table_name,\n    a.attname AS column_name,\n    d.description\nFROM pg_description d\nJOIN pg_class c ON d.objoid = c.oid\nJOIN pg_attribute a ON a.attrelid = c.oid AND a.attnum = d.objsubid\nWHERE c.relnamespace = \'public\'::regnamespace\n  AND d.objsubid > 0\nORDER BY c.relname, a.attnum;',
      explanation: 'Типичные ошибки проектирования: TEXT/VARCHAR(MAX) везде (теряем валидацию и оптимизацию), нет timestamps (нельзя отследить когда изменилось), массивы в строках (нельзя использовать FK и JOIN), God Table (нарушает SRP). COMMENT ON — встроенная документация которая всегда рядом с схемой.'
    }
  ]
}
