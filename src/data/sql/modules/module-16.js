export default {
  id: 16,
  title: 'CTE: Common Table Expressions (WITH)',
  description: 'CTE (обобщённые табличные выражения) — именованные временные результаты запросов. Простые и рекурсивные CTE. Улучшение читаемости, рефакторинг сложных запросов и обход иерархических данных.',
  lessons: [
    {
      id: 1,
      title: 'Что такое CTE и синтаксис WITH',
      type: 'theory',
      content: [
        { type: 'text', value: 'CTE (Common Table Expression) — временный именованный результат запроса, определённый в блоке WITH. CTE существует только в рамках одного SQL-запроса и не хранит данные. Основная цель — читаемость и структурирование сложных запросов.' },
        { type: 'code', language: 'sql', value: '-- Без CTE: сложно читать\nSELECT c.name, sub.total_spent\nFROM customers c\nJOIN (\n    SELECT customer_id, SUM(total) AS total_spent\n    FROM orders\n    WHERE status = \'completed\'\n    GROUP BY customer_id\n    HAVING SUM(total) > 50000\n) sub ON c.id = sub.customer_id\nJOIN (\n    SELECT DISTINCT customer_id FROM orders\n    WHERE created_at >= NOW() - INTERVAL \'30 days\'\n) recent ON c.id = recent.customer_id;\n\n-- С CTE: структурировано и понятно\nWITH\ncompleted_orders AS (\n    SELECT customer_id, SUM(total) AS total_spent\n    FROM orders\n    WHERE status = \'completed\'\n    GROUP BY customer_id\n    HAVING SUM(total) > 50000\n),\nrecent_customers AS (\n    SELECT DISTINCT customer_id\n    FROM orders\n    WHERE created_at >= NOW() - INTERVAL \'30 days\'\n)\nSELECT c.name, co.total_spent\nFROM customers c\nJOIN completed_orders co  ON c.id = co.customer_id\nJOIN recent_customers rc  ON c.id = rc.customer_id\nORDER BY co.total_spent DESC;\n\n-- Несколько CTE разделяются запятой\n-- Каждый следующий CTE может ссылаться на предыдущие!\nWITH\nstep1 AS (\n    SELECT id, revenue FROM orders WHERE year = 2024\n),\nstep2 AS (\n    SELECT SUM(revenue) AS total FROM step1  -- Использует step1!\n),\nstep3 AS (\n    SELECT AVG(revenue) AS avg FROM step1    -- Тоже использует step1\n)\nSELECT total, avg FROM step2, step3;' },
        { type: 'tip', value: 'CTE — это не подзапрос с синтаксическим сахаром. В PostgreSQL 12+ планировщик может встраивать (inline) CTE в основной запрос или материализовывать их. Флаг MATERIALIZED/NOT MATERIALIZED управляет этим поведением.' }
      ]
    },
    {
      id: 2,
      title: 'CTE для UPDATE, INSERT, DELETE',
      type: 'theory',
      content: [
        { type: 'text', value: 'CTE работают не только с SELECT, но и с DML-операциями. Особенно полезны для UPDATE с результатами вычислений и для цепочек изменений данных.' },
        { type: 'code', language: 'sql', value: '-- CTE в UPDATE: обновить цены на основе вычислений\nWITH price_adjustments AS (\n    SELECT\n        p.id,\n        p.price * 1.1 AS new_price  -- Повысить на 10%\n    FROM products p\n    JOIN categories c ON p.category_id = c.id\n    WHERE c.name = \'Электроника\'\n      AND p.price < 10000\n)\nUPDATE products p\nSET price = pa.new_price\nFROM price_adjustments pa\nWHERE p.id = pa.id;\n\n-- CTE с RETURNING: получить изменённые строки\nWITH updated AS (\n    UPDATE orders\n    SET status = \'shipped\', shipped_at = NOW()\n    WHERE status = \'paid\' AND payment_confirmed_at < NOW() - INTERVAL \'1 day\'\n    RETURNING id, customer_id, total\n)\nSELECT\n    u.id,\n    c.email,\n    u.total\nFROM updated u\nJOIN customers c ON u.customer_id = c.id;\n-- Обновляет заказы И сразу возвращает данные для уведомлений\n\n-- CTE для INSERT с проверкой дублей\nWITH new_products AS (\n    SELECT * FROM staging_products  -- Временная таблица с импортом\n),\nfiltered AS (\n    SELECT np.*\n    FROM new_products np\n    WHERE NOT EXISTS (\n        SELECT 1 FROM products p WHERE p.sku = np.sku\n    )\n)\nINSERT INTO products (name, sku, price, category_id)\nSELECT name, sku, price, category_id FROM filtered;\n\n-- Цепочка DML: удалить старые заказы и записать в архив\nWITH old_orders AS (\n    DELETE FROM orders\n    WHERE created_at < NOW() - INTERVAL \'1 year\'\n      AND status = \'completed\'\n    RETURNING *\n)\nINSERT INTO orders_archive\nSELECT * FROM old_orders;' },
        { type: 'note', value: 'CTE с RETURNING позволяет атомарно выполнить DML и использовать изменённые строки в следующем шаге. Всё в одной транзакции — либо всё, либо ничего. Это элегантная замена двух отдельных запросов.' }
      ]
    },
    {
      id: 3,
      title: 'Рекурсивные CTE: обход иерархий',
      type: 'theory',
      content: [
        { type: 'text', value: 'Рекурсивный CTE (WITH RECURSIVE) позволяет делать запросы к иерархическим данным: деревья, графы, орг.структура компании, файловые системы. Состоит из базового случая (anchor) и рекурсивной части.' },
        { type: 'code', language: 'sql', value: '-- Таблица сотрудников с иерархией (менеджер -> подчинённые)\n-- employees: id, name, manager_id (NULL для CEO)\n\n-- Рекурсивный CTE: найти всех подчинённых менеджера\nWITH RECURSIVE org_chart AS (\n    -- Базовый случай (anchor): начинаем с CEO (manager_id IS NULL)\n    SELECT\n        id, name, manager_id,\n        0 AS level,                    -- Уровень иерархии\n        name::TEXT AS path             -- Путь от корня\n    FROM employees\n    WHERE manager_id IS NULL\n\n    UNION ALL  -- UNION ALL (не UNION!) для рекурсии\n\n    -- Рекурсивная часть: подчинённые текущего уровня\n    SELECT\n        e.id, e.name, e.manager_id,\n        oc.level + 1,\n        oc.path || \' -> \' || e.name\n    FROM employees e\n    JOIN org_chart oc ON e.manager_id = oc.id  -- Связь с предыдущим уровнем\n)\nSELECT\n    REPEAT(\'    \', level) || name AS org_tree,  -- Отступы по уровню\n    level,\n    path\nFROM org_chart\nORDER BY path;\n\n-- Рекурсия с глубиной пути и циклозащитой:\nWITH RECURSIVE category_tree AS (\n    SELECT id, name, parent_id, 1 AS depth,\n           ARRAY[id] AS visited   -- Массив посещённых узлов\n    FROM categories\n    WHERE parent_id IS NULL\n\n    UNION ALL\n\n    SELECT c.id, c.name, c.parent_id,\n           ct.depth + 1,\n           ct.visited || c.id\n    FROM categories c\n    JOIN category_tree ct ON c.parent_id = ct.id\n    WHERE c.id <> ALL(ct.visited)  -- Защита от циклов!\n      AND ct.depth < 10            -- Ограничение глубины\n)\nSELECT id, name, depth FROM category_tree ORDER BY depth, name;\n\n-- Генерация последовательностей (числа 1..N):\nWITH RECURSIVE nums AS (\n    SELECT 1 AS n\n    UNION ALL\n    SELECT n + 1 FROM nums WHERE n < 100\n)\nSELECT n FROM nums;\n\n-- Фибоначчи:\nWITH RECURSIVE fib(n, a, b) AS (\n    SELECT 0, 0, 1\n    UNION ALL\n    SELECT n + 1, b, a + b FROM fib WHERE n < 20\n)\nSELECT n, a AS fibonacci FROM fib;' },
        { type: 'warning', value: 'Рекурсивный CTE без ограничения глубины может зациклиться при циклах в данных. Всегда добавляй: 1) защиту от циклов через ARRAY посещённых узлов, 2) максимальную глубину (WHERE depth < N). PostgreSQL по умолчанию не ограничивает рекурсию.' }
      ]
    },
    {
      id: 4,
      title: 'MATERIALIZED vs NOT MATERIALIZED CTE',
      type: 'theory',
      content: [
        { type: 'text', value: 'PostgreSQL 12+ позволяет явно управлять материализацией CTE. По умолчанию простые CTE могут встраиваться в план (inline). MATERIALIZED сохраняет результат в память — полезно когда CTE используется много раз или содержит дорогой запрос.' },
        { type: 'code', language: 'sql', value: '-- NOT MATERIALIZED (по умолчанию в PG12+)\n-- Планировщик встраивает CTE в основной запрос\n-- Может использовать индексы внешнего запроса!\nWITH recent_orders AS NOT MATERIALIZED (\n    SELECT * FROM orders WHERE status = \'completed\'\n)\nSELECT * FROM recent_orders\nWHERE customer_id = 42 AND total > 1000;\n-- Планировщик: сначала WHERE customer_id = 42 (по индексу),\n-- потом фильтр status\n\n-- MATERIALIZED: результат вычисляется один раз и кэшируется\n-- Полезно если CTE используется несколько раз в запросе\nWITH MATERIALIZED expensive_calc AS (\n    SELECT\n        customer_id,\n        SUM(total) AS total_spent,\n        COUNT(*)   AS orders_count\n    FROM orders\n    WHERE created_at >= \'2024-01-01\'  -- Миллионы строк\n    GROUP BY customer_id\n)\nSELECT\n    c.name,\n    ec.total_spent,\n    ec.orders_count,\n    ec.total_spent / ec.orders_count AS avg_order\nFROM customers c\nJOIN expensive_calc ec ON c.id = ec.customer_id\nWHERE ec.orders_count >= 5  -- Используем дважды\n  AND ec.total_spent > 100000;\n-- Без MATERIALIZED: expensive_calc вычислялся бы дважды\n\n-- Когда использовать MATERIALIZED:\n-- 1. CTE используется 2+ раз в запросе\n-- 2. CTE содержит дорогой запрос (агрегации, JOIN)\n-- 3. Нужны стабильные промежуточные результаты\n-- 4. CTe с побочными эффектами (DML с RETURNING)\n\n-- Когда NOT MATERIALIZED:\n-- 1. CTE используется один раз\n-- 2. Внешний запрос фильтрует по индексированному полю\n-- 3. Нужна оптимизация через predicate pushdown\n\nEXPLAIN (ANALYZE, COSTS)\nWITH top_customers AS MATERIALIZED (\n    SELECT customer_id, SUM(total) AS rev\n    FROM orders GROUP BY customer_id\n)\nSELECT * FROM top_customers WHERE rev > 100000;\n-- CTE Scan (материализован) vs Index Scan (встроен)' },
        { type: 'tip', value: 'Правило: если CTE используется 1 раз и есть хорошие индексы у внешнего запроса — НЕ материализуй. Если CTE используется 2+ раз или содержит агрегации на миллионах строк — материализуй явно. EXPLAIN покажет "CTE Scan" (материализован) или встроенный план.' }
      ]
    },
    {
      id: 5,
      title: 'Паттерны использования CTE',
      type: 'theory',
      content: [
        { type: 'text', value: 'CTE решают несколько классических задач: пошаговое преобразование данных, построение отчётов, разбивка монструозных запросов на понятные части.' },
        { type: 'code', language: 'sql', value: '-- Паттерн 1: Пошаговое преобразование (pipeline)\nWITH\n-- Шаг 1: Сырые данные за месяц\nraw_data AS (\n    SELECT o.*, c.city, c.country\n    FROM orders o JOIN customers c ON o.customer_id = c.id\n    WHERE DATE_TRUNC(\'month\', o.created_at) = DATE_TRUNC(\'month\', NOW())\n),\n-- Шаг 2: Агрегация по городам\ncity_stats AS (\n    SELECT city, country,\n        COUNT(*)    AS orders,\n        SUM(total)  AS revenue\n    FROM raw_data\n    GROUP BY city, country\n),\n-- Шаг 3: Ранжирование\nranked AS (\n    SELECT *,\n        RANK() OVER (PARTITION BY country ORDER BY revenue DESC) AS rank_in_country\n    FROM city_stats\n)\n-- Финальный SELECT\nSELECT * FROM ranked WHERE rank_in_country <= 5\nORDER BY country, rank_in_country;\n\n-- Паттерн 2: CTE как замена временных таблиц\nWITH\nbase_salaries AS (\n    SELECT dept_id, AVG(salary) AS avg_sal, STDDEV(salary) AS std_sal\n    FROM employees GROUP BY dept_id\n),\noutliers AS (\n    SELECT e.id, e.name, e.salary, b.avg_sal,\n        ABS(e.salary - b.avg_sal) / NULLIF(b.std_sal, 0) AS z_score\n    FROM employees e\n    JOIN base_salaries b ON e.dept_id = b.dept_id\n)\nSELECT * FROM outliers\nWHERE z_score > 2  -- Зарплата отклоняется более чем на 2 стандартных отклонения\nORDER BY z_score DESC;\n\n-- Паттерн 3: CTE для upsert (INSERT ON CONFLICT)\nWITH new_data AS (\n    SELECT \'user123\'::TEXT AS username, 42 AS score, NOW() AS ts\n)\nINSERT INTO leaderboard (username, score, updated_at)\nSELECT username, score, ts FROM new_data\nON CONFLICT (username)\nDO UPDATE SET\n    score      = GREATEST(leaderboard.score, EXCLUDED.score),\n    updated_at = EXCLUDED.updated_at;' },
        { type: 'note', value: 'CTE существенно улучшают читаемость — называй их как переменные в коде: raw_orders, filtered_customers, ranked_products. Будущий разработчик (и ты сам через 3 месяца) скажет спасибо.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Анализ иерархических данных',
      type: 'practice',
      difficulty: 'hard',
      description: 'Используй рекурсивные CTE для анализа организационной структуры и иерархии категорий.',
      requirements: [
        'Создай таблицы employees (id, name, position, salary, manager_id) и categories (id, name, parent_id)',
        'Рекурсивный CTE: вывести всю org-chart с уровнем, путём и отступами',
        'Найди глубину дерева категорий и полный путь каждой категории (путь вида "Электроника / Телефоны / Смартфоны")',
        'Вычисли сумму зарплат всех подчинённых для каждого менеджера (включая всех уровней)',
        'CTE pipeline: подготовь отчёт топ-5 отделов по средней зарплате, используя 3 CTE-шага',
        'Обнаружи циклы в иерархии (если есть) с помощью массива посещённых ID'
      ],
      hint: 'Для пути категорий: в рекурсии конкатенируй parent_path || \' / \' || name. Для суммы подчинённых: в CTE накапливай salary, потом GROUP BY manager агрегируй SUM. Флаг visited = ARRAY[id] — защита от цикла.',
      expectedOutput: 'Иерархия категорий с полными путями:\n id | name           | depth | path\n----+----------------+-------+-------------------------------\n  1 | Электроника    |     1 | Электроника\n  2 | Ноутбуки       |     2 | Электроника / Ноутбуки\n  5 | Игровые        |     3 | Электроника / Ноутбуки / Игровые\n  3 | Телефоны       |     2 | Электроника / Телефоны\n(4 rows)\n\nФонд зарплат подчинённых каждого менеджера:\n manager    | team_size | total_salary\n------------+-----------+--------------\n Нурлан     |         5 |   3500000.00\n Алия       |         2 |   1200000.00\n(2 rows)\n\nОрганизационная цепочка для сотрудника id=7:\n level | name     | position\n-------+----------+----------\n     1 | Болат    | Developer\n     2 | Алия     | Team Lead\n     3 | Нурлан   | CTO\n(3 rows)',
      solution: '-- 1. Структура таблиц\nCREATE TABLE employees (\n    id         SERIAL PRIMARY KEY,\n    name       VARCHAR(200) NOT NULL,\n    position   VARCHAR(200),\n    salary     DECIMAL(12, 2),\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE categories (\n    id        SERIAL PRIMARY KEY,\n    name      VARCHAR(200) NOT NULL,\n    parent_id INTEGER REFERENCES categories(id)\n);\n\n-- Данные\nINSERT INTO employees (id, name, position, salary, manager_id) VALUES\n    (1, \'Арман Сейтов\',   \'CEO\',              500000, NULL),\n    (2, \'Айгуль Бекова\',  \'CTO\',              350000, 1),\n    (3, \'Данияр Ахмет\',   \'CFO\',              320000, 1),\n    (4, \'Нурлан Жаксыбек\',\'Tech Lead\',        200000, 2),\n    (5, \'Фарида Омар\',    \'Senior Dev\',       180000, 4),\n    (6, \'Алия Ержан\',     \'Junior Dev\',        90000, 4),\n    (7, \'Берик Садвакас\', \'Accountant\',       110000, 3);\n\nINSERT INTO categories (id, name, parent_id) VALUES\n    (1, \'Все товары\',    NULL),\n    (2, \'Электроника\',   1),\n    (3, \'Одежда\',        1),\n    (4, \'Смартфоны\',     2),\n    (5, \'Ноутбуки\',      2),\n    (6, \'Мужская одежда\',3),\n    (7, \'iPhone\',        4),\n    (8, \'Android\',       4);\n\n-- 2. Org-chart с уровнями\nWITH RECURSIVE org_chart AS (\n    SELECT id, name, position, salary, manager_id,\n           0 AS level,\n           name::TEXT AS path\n    FROM employees\n    WHERE manager_id IS NULL\n\n    UNION ALL\n\n    SELECT e.id, e.name, e.position, e.salary, e.manager_id,\n           oc.level + 1,\n           oc.path || \' -> \' || e.name\n    FROM employees e\n    JOIN org_chart oc ON e.manager_id = oc.id\n)\nSELECT\n    REPEAT(\'    \', level) || name AS org_tree,\n    position,\n    salary,\n    level,\n    path\nFROM org_chart\nORDER BY path;\n\n-- 3. Полный путь категорий\nWITH RECURSIVE cat_tree AS (\n    SELECT id, name, parent_id, name::TEXT AS full_path, 1 AS depth\n    FROM categories WHERE parent_id IS NULL\n\n    UNION ALL\n\n    SELECT c.id, c.name, c.parent_id,\n           ct.full_path || \' / \' || c.name,\n           ct.depth + 1\n    FROM categories c\n    JOIN cat_tree ct ON c.parent_id = ct.id\n)\nSELECT id, name, depth, full_path\nFROM cat_tree\nORDER BY full_path;\n\n-- 4. Сумма зарплат всех подчинённых\nWITH RECURSIVE subordinates AS (\n    SELECT id, name, manager_id, salary\n    FROM employees\n\n    UNION ALL\n\n    SELECT e.id, e.name, s.manager_id AS root_manager_id, e.salary\n    FROM employees e\n    JOIN subordinates s ON e.manager_id = s.id\n    WHERE e.id <> s.id\n)\nSELECT\n    m.id AS manager_id,\n    m.name AS manager_name,\n    COUNT(DISTINCT s.id) AS subordinate_count,\n    SUM(s.salary) AS total_subordinate_salary\nFROM employees m\nLEFT JOIN subordinates s ON s.manager_id = m.id\nGROUP BY m.id, m.name\nORDER BY total_subordinate_salary DESC NULLS LAST;\n\n-- 5. Pipeline отчёт по отделам\nWITH\nteams AS (\n    -- Шаг 1: Найти топ-менеджеров (depth=1)\n    SELECT id, name FROM employees WHERE manager_id = 1\n),\nteam_members AS (\n    -- Шаг 2: Все члены каждой команды\n    SELECT e.id, e.salary, t.name AS team_name\n    FROM employees e\n    JOIN teams t ON e.manager_id = t.id OR e.id = t.id\n),\nteam_stats AS (\n    -- Шаг 3: Статистика по командам\n    SELECT\n        team_name,\n        COUNT(*) AS headcount,\n        AVG(salary) AS avg_salary,\n        SUM(salary) AS total_salary\n    FROM team_members\n    GROUP BY team_name\n)\nSELECT * FROM team_stats\nORDER BY avg_salary DESC\nLIMIT 5;\n\n-- 6. Обнаружение циклов\nWITH RECURSIVE cycle_check AS (\n    SELECT id, name, manager_id, ARRAY[id] AS visited, FALSE AS has_cycle\n    FROM employees WHERE manager_id IS NULL\n\n    UNION ALL\n\n    SELECT e.id, e.name, e.manager_id,\n           cc.visited || e.id,\n           e.id = ANY(cc.visited)\n    FROM employees e\n    JOIN cycle_check cc ON e.manager_id = cc.id\n    WHERE NOT cc.has_cycle AND ARRAY_LENGTH(cc.visited, 1) < 20\n)\nSELECT id, name, visited AS path\nFROM cycle_check\nWHERE has_cycle;\n-- Если пустой результат -> циклов нет',
      explanation: 'Рекурсивные CTE — единственный чистый SQL-способ обхода деревьев произвольной глубины. Anchor = стартовые узлы, рекурсивная часть = переход к потомкам. Обязательно добавляй защиту от циклов через ARRAY посещённых узлов — особенно в данных где иерархия создаётся пользователями.'
    }
  ]
}
