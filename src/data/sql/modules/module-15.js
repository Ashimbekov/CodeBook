export default {
  id: 15,
  title: 'Оконные функции: ROW_NUMBER, RANK, LAG, LEAD, SUM OVER',
  description: 'Оконные функции (Window Functions) выполняют вычисления по группе строк без схлопывания результата. ROW_NUMBER, RANK, DENSE_RANK, LAG, LEAD, FIRST_VALUE, SUM/AVG OVER PARTITION BY.',
  lessons: [
    {
      id: 1,
      title: 'Введение в оконные функции: синтаксис OVER',
      type: 'theory',
      content: [
        { type: 'text', value: 'Оконная функция вычисляет значение для каждой строки относительно "окна" — набора связанных строк. В отличие от GROUP BY, строки не схлопываются: результат содержит столько же строк, сколько и исходная таблица.' },
        { type: 'code', language: 'sql', value: '-- Разница между GROUP BY и оконной функцией:\n\n-- GROUP BY схлопывает строки (одна строка на группу):\nSELECT department_id, AVG(salary) AS avg_salary\nFROM employees\nGROUP BY department_id;\n-- Результат: 5 строк (5 отделов)\n\n-- Оконная функция НЕ схлопывает (одна строка на сотрудника):\nSELECT\n    name,\n    department_id,\n    salary,\n    AVG(salary) OVER (PARTITION BY department_id) AS dept_avg_salary\nFROM employees;\n-- Результат: 50 строк (50 сотрудников), у каждого avg_salary его отдела\n\n-- Синтаксис оконной функции:\n-- функция() OVER (\n--   PARTITION BY столбец       -- Разделить на группы (как GROUP BY)\n--   ORDER BY столбец           -- Порядок строк в окне\n--   ROWS/RANGE BETWEEN ...     -- Рамка окна (frame)\n-- )\n\n-- Без PARTITION BY: всё как одна группа\nSELECT\n    name,\n    salary,\n    AVG(salary) OVER () AS company_avg,  -- Среднее по всей компании\n    salary - AVG(salary) OVER () AS diff_from_avg\nFROM employees;\n\n-- Именованное окно (WINDOW clause): DRY\nSELECT\n    name,\n    department_id,\n    salary,\n    AVG(salary)    OVER w AS dept_avg,\n    MAX(salary)    OVER w AS dept_max,\n    MIN(salary)    OVER w AS dept_min,\n    COUNT(*)       OVER w AS dept_size\nFROM employees\nWINDOW w AS (PARTITION BY department_id);' },
        { type: 'tip', value: 'Оконные функции вычисляются ПОСЛЕ WHERE, GROUP BY и HAVING, но ПЕРЕД ORDER BY и LIMIT. Поэтому нельзя использовать оконную функцию в WHERE — нужен подзапрос или CTE.' }
      ]
    },
    {
      id: 2,
      title: 'ROW_NUMBER, RANK, DENSE_RANK: нумерация строк',
      type: 'theory',
      content: [
        { type: 'text', value: 'Три функции для нумерации строк с разным поведением при одинаковых значениях (ties). ROW_NUMBER — уникальный номер. RANK — одинаковый номер при равенстве + пропуск. DENSE_RANK — одинаковый номер без пропуска.' },
        { type: 'code', language: 'sql', value: '-- Данные: продажи менеджеров\n-- name  | dept    | sales\n-- Алия  | IT      | 150000\n-- Нурлан| IT      | 120000\n-- Фарида| IT      | 120000  <- равенство с Нурланом!\n-- Асет  | Sales   | 200000\n-- Гульнар| Sales  | 180000\n\nSELECT\n    name,\n    dept,\n    sales,\n    ROW_NUMBER() OVER (PARTITION BY dept ORDER BY sales DESC) AS row_num,\n    -- IT: Алия=1, Нурлан=2, Фарида=3 (уникальные, произвольный порядок при равенстве)\n\n    RANK()       OVER (PARTITION BY dept ORDER BY sales DESC) AS rank,\n    -- IT: Алия=1, Нурлан=2, Фарида=2 (одинаково), следующий=4 (ПРОПУСК!)\n\n    DENSE_RANK() OVER (PARTITION BY dept ORDER BY sales DESC) AS dense_rank\n    -- IT: Алия=1, Нурлан=2, Фарида=2 (одинаково), следующий=3 (БЕЗ пропуска)\nFROM salespeople\nORDER BY dept, sales DESC;\n\n-- Применение: TOP-N записей по каждой группе\n-- Топ-2 продавца в каждом отделе:\nSELECT * FROM (\n    SELECT\n        name,\n        dept,\n        sales,\n        ROW_NUMBER() OVER (PARTITION BY dept ORDER BY sales DESC) AS rn\n    FROM salespeople\n) t\nWHERE rn <= 2;\n\n-- NTILE(n): разбить на n равных частей (квантили)\nSELECT\n    name,\n    salary,\n    NTILE(4) OVER (ORDER BY salary) AS quartile\n    -- 1=нижняя четверть, 4=верхняя четверть\nFROM employees;\n\n-- PERCENT_RANK: относительный ранг (0.0 - 1.0)\n-- CUME_DIST: накопительное распределение\nSELECT\n    name,\n    salary,\n    ROUND(PERCENT_RANK() OVER (ORDER BY salary) * 100, 1) AS percent_rank,\n    ROUND(CUME_DIST()    OVER (ORDER BY salary) * 100, 1) AS percentile\nFROM employees;' },
        { type: 'note', value: 'ROW_NUMBER используется для постраничной навигации (pagination). RANK/DENSE_RANK — для рейтингов и топ-N запросов. NTILE — для разбивки на квантили (квартили, децили). При одинаковых значениях ROW_NUMBER выбирает порядок произвольно.' }
      ]
    },
    {
      id: 3,
      title: 'LAG и LEAD: доступ к соседним строкам',
      type: 'theory',
      content: [
        { type: 'text', value: 'LAG() обращается к предыдущей строке в окне. LEAD() — к следующей. Незаменимы для вычисления изменений: разность продаж за период, сравнение с предыдущим/следующим значением.' },
        { type: 'code', language: 'sql', value: '-- Ежемесячная выручка магазина\n-- month      | revenue\n-- 2024-01-01 | 150000\n-- 2024-02-01 | 180000\n-- 2024-03-01 | 165000\n-- 2024-04-01 | 210000\n\nSELECT\n    month,\n    revenue,\n    -- LAG: значение предыдущей строки (offset=1, default=0)\n    LAG(revenue, 1, 0)  OVER (ORDER BY month) AS prev_month_revenue,\n\n    -- Изменение по сравнению с прошлым месяцем\n    revenue - LAG(revenue, 1) OVER (ORDER BY month) AS mom_change,\n\n    -- Процентное изменение\n    ROUND(\n        (revenue - LAG(revenue, 1) OVER (ORDER BY month))\n        / LAG(revenue, 1) OVER (ORDER BY month) * 100,\n    1) AS mom_change_pct,\n\n    -- LEAD: следующий месяц\n    LEAD(revenue, 1) OVER (ORDER BY month) AS next_month_revenue\n\nFROM monthly_revenue\nORDER BY month;\n\n-- LAG с PARTITION BY: сравнение в рамках категории\nSELECT\n    category,\n    month,\n    revenue,\n    LAG(revenue) OVER (PARTITION BY category ORDER BY month) AS prev_revenue,\n    revenue - LAG(revenue) OVER (PARTITION BY category ORDER BY month) AS change\nFROM category_revenue\nORDER BY category, month;\n\n-- FIRST_VALUE / LAST_VALUE: первое/последнее значение в окне\nSELECT\n    name,\n    department_id,\n    salary,\n    FIRST_VALUE(name) OVER (PARTITION BY department_id ORDER BY salary DESC)\n        AS top_earner,\n    LAST_VALUE(name) OVER (\n        PARTITION BY department_id\n        ORDER BY salary DESC\n        ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING\n    ) AS lowest_earner\nFROM employees;\n-- ВНИМАНИЕ: LAST_VALUE требует явного frame ROWS BETWEEN ... UNBOUNDED FOLLOWING\n-- По умолчанию frame = ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n\n-- NTH_VALUE: N-е значение\nSELECT\n    name,\n    salary,\n    NTH_VALUE(name, 2) OVER (PARTITION BY dept ORDER BY salary DESC\n        ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING)\n        AS second_highest_earner\nFROM employees;' },
        { type: 'tip', value: 'LAG/LEAD принимают три аргумента: значение, смещение (default=1), значение по умолчанию при отсутствии строки (default=NULL). Пример: LAG(revenue, 1, 0) — предыдущее значение, если нет предыдущей строки то 0.' }
      ]
    },
    {
      id: 4,
      title: 'SUM OVER, AVG OVER: скользящие агрегаты',
      type: 'theory',
      content: [
        { type: 'text', value: 'Агрегатные функции SUM, AVG, MIN, MAX, COUNT можно использовать как оконные с OVER. Особенно мощны с рамкой окна (frame) для скользящих сумм и средних.' },
        { type: 'code', language: 'sql', value: '-- Накопленная сумма (Running Total / Cumulative Sum)\nSELECT\n    date,\n    daily_revenue,\n    SUM(daily_revenue) OVER (ORDER BY date) AS cumulative_revenue,\n    -- По умолчанию frame = ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    -- Это и есть накопленная сумма!\n\n    AVG(daily_revenue) OVER (ORDER BY date) AS cumulative_avg\n\nFROM daily_sales;\n\n-- Скользящее среднее за 7 дней (Moving Average)\nSELECT\n    date,\n    daily_revenue,\n    ROUND(AVG(daily_revenue) OVER (\n        ORDER BY date\n        ROWS BETWEEN 6 PRECEDING AND CURRENT ROW  -- 7 строк (6 пред. + текущая)\n    ), 2) AS moving_avg_7d,\n\n    -- Скользящая сумма за 30 дней:\n    SUM(daily_revenue) OVER (\n        ORDER BY date\n        ROWS BETWEEN 29 PRECEDING AND CURRENT ROW\n    ) AS moving_sum_30d\n\nFROM daily_sales\nORDER BY date;\n\n-- Рамки окна (Frame Specifications):\n-- ROWS BETWEEN x AND y   -- по количеству строк (физические)\n-- RANGE BETWEEN x AND y  -- по значениям (логические)\n\n-- Варианты x и y:\n-- UNBOUNDED PRECEDING    -- от начала раздела\n-- n PRECEDING            -- n строк/значений до текущей\n-- CURRENT ROW            -- текущая строка\n-- n FOLLOWING            -- n строк/значений после текущей\n-- UNBOUNDED FOLLOWING    -- до конца раздела\n\n-- Нарастающий итог по категориям:\nSELECT\n    category,\n    month,\n    revenue,\n    SUM(revenue) OVER (\n        PARTITION BY category\n        ORDER BY month\n    ) AS cumulative_by_category,\n\n    -- Доля от суммы в разделе:\n    ROUND(revenue / SUM(revenue) OVER (PARTITION BY category) * 100, 1)\n        AS pct_of_category_total\n\nFROM category_monthly_revenue\nORDER BY category, month;' },
        { type: 'note', value: 'ROWS BETWEEN работает с физическими строками (по порядку). RANGE BETWEEN работает с логическими значениями (все строки с одинаковым значением ORDER BY как одна группа). Для скользящих сумм по датам с пропусками RANGE DATE более корректен.' }
      ]
    },
    {
      id: 5,
      title: 'Практические паттерны с оконными функциями',
      type: 'theory',
      content: [
        { type: 'text', value: 'Оконные функции решают задачи, которые сложно или невозможно решить простыми запросами: дедупликация (оставить последнюю запись), gap detection (найти пропуски), сравнение с предыдущим периодом.' },
        { type: 'code', language: 'sql', value: '-- Паттерн 1: Дедупликация - оставить последнюю запись по каждому клиенту\nDELETE FROM customer_events\nWHERE id IN (\n    SELECT id FROM (\n        SELECT\n            id,\n            ROW_NUMBER() OVER (\n                PARTITION BY customer_id\n                ORDER BY created_at DESC\n            ) AS rn\n        FROM customer_events\n    ) t\n    WHERE rn > 1  -- Удалить всё кроме последней (rn=1)\n);\n\n-- Паттерн 2: Медиана с PERCENTILE_CONT\nSELECT\n    department_id,\n    PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY salary) AS median_salary,\n    PERCENTILE_DISC(0.5) WITHIN GROUP (ORDER BY salary) AS median_salary_disc\nFROM employees\nGROUP BY department_id;\n-- Это НЕ оконные функции! Но работают похоже.\n\n-- Паттерн 3: Разница между текущей и средней по отделу\nSELECT\n    name,\n    department_id,\n    salary,\n    ROUND(AVG(salary) OVER (PARTITION BY department_id), 2) AS dept_avg,\n    salary - AVG(salary) OVER (PARTITION BY department_id) AS diff_from_avg,\n    CASE\n        WHEN salary > AVG(salary) OVER (PARTITION BY department_id) THEN \'выше среднего\'\n        WHEN salary < AVG(salary) OVER (PARTITION BY department_id) THEN \'ниже среднего\'\n        ELSE \'среднее\'\n    END AS vs_avg\nFROM employees;\n\n-- Паттерн 4: Нумерация без разрывов в последовательности (GAP detection)\nSELECT\n    id,\n    id - ROW_NUMBER() OVER (ORDER BY id) AS grp\n    -- Одинаковый grp = одна непрерывная последовательность\nFROM orders;\n\n-- Паттерн 5: Топ-3 товара по каждой категории (пагинация внутри групп)\nSELECT category, product_name, revenue\nFROM (\n    SELECT\n        cat.name AS category,\n        p.name   AS product_name,\n        SUM(oi.quantity * oi.unit_price) AS revenue,\n        RANK() OVER (\n            PARTITION BY cat.id\n            ORDER BY SUM(oi.quantity * oi.unit_price) DESC\n        ) AS category_rank\n    FROM products p\n    JOIN categories cat   ON p.category_id = cat.id\n    JOIN order_items oi   ON p.id = oi.product_id\n    GROUP BY cat.id, cat.name, p.id, p.name\n) ranked\nWHERE category_rank <= 3\nORDER BY category, revenue DESC;' },
        { type: 'tip', value: 'Оконные функции нельзя использовать в WHERE и HAVING напрямую. Решение: обернуть в подзапрос или CTE. Также нельзя вкладывать оконные функции друг в друга.' }
      ]
    },
    {
      id: 6,
      title: 'Производительность оконных функций',
      type: 'theory',
      content: [
        { type: 'text', value: 'Оконные функции могут быть дорогими при больших данных. Ключевые факторы производительности: правильные индексы для ORDER BY в OVER, уменьшение набора данных до оконной функции через WHERE, избегание лишних оконных проходов.' },
        { type: 'code', language: 'sql', value: '-- Проблема: несколько оконных функций с разными ORDER BY\n-- Каждый уникальный OVER (...) = отдельный проход по данным!\n\n-- Медленно (4 разных прохода):\nSELECT\n    name, salary,\n    ROW_NUMBER() OVER (PARTITION BY dept ORDER BY salary DESC)  AS rn_by_salary,\n    ROW_NUMBER() OVER (PARTITION BY dept ORDER BY name)         AS rn_by_name,\n    SUM(salary)  OVER (PARTITION BY dept ORDER BY salary)       AS running_sum,\n    AVG(salary)  OVER (PARTITION BY dept)                       AS avg_salary\nFROM employees;\n\n-- Лучше: одинаковые OVER() объединяются в один проход\n-- PostgreSQL автоматически объединяет идентичные окна\nSELECT\n    name, salary,\n    SUM(salary)  OVER w AS running_sum,\n    AVG(salary)  OVER w AS avg_salary,\n    MAX(salary)  OVER w AS max_salary,\n    COUNT(*)     OVER w AS dept_count\nFROM employees\nWINDOW w AS (PARTITION BY dept ORDER BY salary);\n-- Один проход для всех функций с одинаковым WINDOW w!\n\n-- Индексы для оконных функций:\n-- Если OVER (PARTITION BY dept_id ORDER BY salary DESC)\n-- Нужен индекс: CREATE INDEX ON employees (dept_id, salary DESC);\n\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT\n    name, dept_id, salary,\n    ROW_NUMBER() OVER (PARTITION BY dept_id ORDER BY salary DESC) AS rn\nFROM employees;\n-- Ищи WindowAgg в плане. Sort перед WindowAgg = нет подходящего индекса\n\n-- Фильтрация ДО оконной функции через CTE:\nWITH recent_orders AS (\n    SELECT *\n    FROM orders\n    WHERE created_at >= NOW() - INTERVAL \'30 days\'  -- Сначала фильтр!\n)\nSELECT\n    customer_id,\n    total,\n    SUM(total) OVER (PARTITION BY customer_id ORDER BY created_at)\n        AS customer_running_total\nFROM recent_orders;\n-- Оконная функция работает на маленьком наборе данных, не на всей таблице' },
        { type: 'warning', value: 'Не используй SELECT * вместе с оконными функциями на больших таблицах — сначала отбери нужные столбцы. ROWS BETWEEN работает быстрее RANGE BETWEEN. EXPLAIN ANALYZE покажет Sort и WindowAgg — на них смотри при оптимизации.' }
      ]
    },
    {
      id: 7,
      title: 'Практика: Аналитика продаж с оконными функциями',
      type: 'practice',
      difficulty: 'hard',
      description: 'Используй оконные функции для глубокого анализа данных продаж.',
      requirements: [
        'Рейтинг продавцов внутри каждого отдела: ROW_NUMBER, RANK, DENSE_RANK по total_sales',
        'Анализ роста выручки: LAG для сравнения с прошлым месяцем, вычисли MoM% изменение',
        'Скользящее среднее продаж за 3 месяца по каждому менеджеру',
        'Нарастающий итог (cumulative sum) выручки по месяцам в рамках года',
        'Топ-3 клиента по сумме заказов в каждой стране (RANK + фильтр)',
        'Процент от общей суммы отдела для каждого сотрудника',
        'Найди сотрудников чья зарплата выше средней по отделу (оконная функция в CTE)'
      ],
      hint: 'Для топ-N по группам: ROW_NUMBER OVER (PARTITION BY ...) в подзапросе, потом WHERE rn <= N. Для MoM%: (revenue - LAG(revenue)) / LAG(revenue) * 100. Оконные функции нельзя использовать в WHERE напрямую.',
      expectedOutput: 'Топ-2 продавца по выручке в каждой категории:\n category   | name     | revenue   | rn\n------------+----------+-----------+----\n Ноутбуки   | Алибек   | 250000.00 |  1\n Ноутбуки   | Дана     | 180000.00 |  2\n Телефоны   | Ерлан    | 190000.00 |  1\n Телефоны   | Мадина   | 145000.00 |  2\n(4 rows)\n\nМесячный рост выручки (LAG):\n month    | revenue   | prev_month | mom_pct\n----------+-----------+------------+---------\n 2025-12   | 980000.00 | NULL       | NULL\n 2026-01   | 1050000.00 | 980000.00  |    7.14\n 2026-02   | 1230000.00 | 1050000.00 |   17.14\n 2026-03   | 1100000.00 | 1230000.00 |  -10.57\n(4 rows)',
      solution: '-- 1. Рейтинг продавцов\nSELECT\n    name,\n    department,\n    total_sales,\n    ROW_NUMBER() OVER (PARTITION BY department ORDER BY total_sales DESC) AS row_num,\n    RANK()       OVER (PARTITION BY department ORDER BY total_sales DESC) AS rank,\n    DENSE_RANK() OVER (PARTITION BY department ORDER BY total_sales DESC) AS dense_rank\nFROM (\n    SELECT\n        e.name,\n        d.name AS department,\n        SUM(s.amount) AS total_sales\n    FROM employees e\n    JOIN departments d ON e.dept_id = d.id\n    JOIN sales s ON e.id = s.employee_id\n    GROUP BY e.id, e.name, d.name\n) t\nORDER BY department, total_sales DESC;\n\n-- 2. MoM анализ выручки\nSELECT\n    month,\n    revenue,\n    LAG(revenue) OVER (ORDER BY month)              AS prev_month,\n    revenue - LAG(revenue) OVER (ORDER BY month)    AS absolute_change,\n    ROUND(\n        (revenue - LAG(revenue) OVER (ORDER BY month))\n        / NULLIF(LAG(revenue) OVER (ORDER BY month), 0) * 100\n    , 1) AS mom_change_pct\nFROM monthly_revenue\nORDER BY month;\n\n-- 3. Скользящее среднее за 3 месяца\nSELECT\n    manager_id,\n    month,\n    monthly_sales,\n    ROUND(AVG(monthly_sales) OVER (\n        PARTITION BY manager_id\n        ORDER BY month\n        ROWS BETWEEN 2 PRECEDING AND CURRENT ROW\n    ), 2) AS moving_avg_3m\nFROM manager_monthly_sales\nORDER BY manager_id, month;\n\n-- 4. Нарастающий итог по годам\nSELECT\n    EXTRACT(YEAR FROM sale_date)  AS year,\n    EXTRACT(MONTH FROM sale_date) AS month,\n    monthly_revenue,\n    SUM(monthly_revenue) OVER (\n        PARTITION BY EXTRACT(YEAR FROM sale_date)\n        ORDER BY sale_date\n    ) AS ytd_revenue\nFROM monthly_revenue_data\nORDER BY sale_date;\n\n-- 5. Топ-3 клиента по стране\nSELECT country, customer_name, total_spent\nFROM (\n    SELECT\n        c.country,\n        c.name AS customer_name,\n        SUM(o.total) AS total_spent,\n        RANK() OVER (\n            PARTITION BY c.country\n            ORDER BY SUM(o.total) DESC\n        ) AS country_rank\n    FROM customers c\n    JOIN orders o ON c.id = o.customer_id\n    GROUP BY c.country, c.id, c.name\n) ranked\nWHERE country_rank <= 3\nORDER BY country, total_spent DESC;\n\n-- 6. Доля зарплаты от суммы отдела\nSELECT\n    name,\n    dept_id,\n    salary,\n    SUM(salary) OVER (PARTITION BY dept_id) AS dept_total,\n    ROUND(salary / SUM(salary) OVER (PARTITION BY dept_id) * 100, 1) AS pct_of_dept\nFROM employees\nORDER BY dept_id, salary DESC;\n\n-- 7. Сотрудники выше средней зарплаты по отделу\nWITH employee_stats AS (\n    SELECT\n        name,\n        dept_id,\n        salary,\n        AVG(salary) OVER (PARTITION BY dept_id) AS dept_avg_salary\n    FROM employees\n)\nSELECT name, dept_id, salary, ROUND(dept_avg_salary, 2) AS dept_avg\nFROM employee_stats\nWHERE salary > dept_avg_salary\nORDER BY dept_id, salary DESC;',
      explanation: 'Оконные функции позволяют делать аналитику в один запрос без самосоединений и подзапросов. Ключевой паттерн: PARTITION BY = GROUP BY без схлопывания, ORDER BY внутри OVER = порядок для ранжирования и накопления. Всегда фильтруй данные ДО оконных функций через WHERE или CTE.'
    },
    {
      id: 8,
      title: 'ПРАКТИКА: оконные функции в реальных задачах',
      type: 'practice',
      difficulty: 'medium',
      description: 'Решение практических задач с оконными функциями на данных e-commerce платформы.',
      requirements: [
        'Для каждого заказа вычисли: порядковый номер заказа клиента (1-й, 2-й...) и сумму всех предыдущих заказов клиента',
        'Найди первый и последний заказ каждого клиента используя FIRST_VALUE/LAST_VALUE',
        'Вычисли разрыв в днях между последовательными заказами одного клиента (LAG на дате)',
        'Выяви клиентов с тенденцией роста заказов: каждый следующий заказ больше предыдущего'
      ],
      hint: 'ROW_NUMBER() OVER (PARTITION BY customer_id ORDER BY created_at) даёт порядковый номер заказа. LAG(created_at) OVER (PARTITION BY customer_id ORDER BY created_at) — дата предыдущего заказа. Для тенденции роста: amount > LAG(amount).',
      expectedOutput: 'Первый заказ каждого клиента (ROW_NUMBER = 1):\n customer   | first_order_date | first_amount\n------------+------------------+--------------\n Алия       | 2025-11-05       |    45000.00\n Нурлан     | 2025-12-12       |    85000.00\n Фарида     | 2026-01-03       |    32000.00\n(3 rows)\n\nДни между заказами (LAG):\n customer | order_date  | days_since_prev\n----------+-------------+-----------------\n Алия     | 2025-11-05  | NULL\n Алия     | 2025-12-20  |              45\n Алия     | 2026-02-14  |              56\n(3 rows)\n\nТренд роста (последовательные заказы > предыдущих):\n customer | growing_streak\n----------+----------------\n Нурлан   |              3\n(1 row)',
      solution: '-- 1. Порядковый номер и нарастающая сумма заказов клиента\nSELECT\n    o.id AS order_id,\n    o.customer_id,\n    c.name AS customer_name,\n    o.total,\n    o.created_at,\n    ROW_NUMBER() OVER (\n        PARTITION BY o.customer_id\n        ORDER BY o.created_at\n    ) AS order_sequence,\n    SUM(o.total) OVER (\n        PARTITION BY o.customer_id\n        ORDER BY o.created_at\n        ROWS BETWEEN UNBOUNDED PRECEDING AND 1 PRECEDING\n    ) AS prev_orders_total  -- сумма ПРЕДЫДУЩИХ (без текущего)\nFROM orders o\nJOIN customers c ON o.customer_id = c.id\nORDER BY o.customer_id, o.created_at;\n\n-- 2. Первый и последний заказ клиента\nSELECT DISTINCT\n    customer_id,\n    FIRST_VALUE(id) OVER w AS first_order_id,\n    FIRST_VALUE(created_at) OVER w AS first_order_date,\n    LAST_VALUE(id) OVER (\n        PARTITION BY customer_id ORDER BY created_at\n        ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING\n    ) AS last_order_id\nFROM orders\nWINDOW w AS (PARTITION BY customer_id ORDER BY created_at);\n\n-- 3. Разрыв в днях между заказами\nSELECT\n    customer_id,\n    id AS order_id,\n    created_at,\n    LAG(created_at) OVER (PARTITION BY customer_id ORDER BY created_at) AS prev_order_date,\n    created_at - LAG(created_at) OVER (PARTITION BY customer_id ORDER BY created_at)\n        AS days_since_prev\nFROM orders\nORDER BY customer_id, created_at;\n\n-- 4. Клиенты с тенденцией роста\nWITH order_growth AS (\n    SELECT\n        customer_id,\n        id AS order_id,\n        total,\n        LAG(total) OVER (PARTITION BY customer_id ORDER BY created_at) AS prev_total,\n        ROW_NUMBER() OVER (PARTITION BY customer_id ORDER BY created_at DESC) AS rn\n    FROM orders\n)\nSELECT DISTINCT customer_id\nFROM order_growth\nWHERE rn <= 3  -- Последние 3 заказа\nGROUP BY customer_id\nHAVING MIN(CASE WHEN total > COALESCE(prev_total, 0) THEN 1 ELSE 0 END) = 1;',
      explanation: 'ROWS BETWEEN UNBOUNDED PRECEDING AND 1 PRECEDING исключает текущую строку из накопления — полезно для суммы "всех предыдущих". DISTINCT в связке с FIRST_VALUE/LAST_VALUE убирает дубли. Тенденция роста проверяется через LAG на последних N заказах.'
    }
  ]
}
