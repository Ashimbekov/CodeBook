export default {
  id: 30,
  title: 'Рекомендательные системы',
  description: 'Collaborative filtering, content-based фильтрация, матричная факторизация SVD, гибридные системы и метрики оценки.',
  lessons: [
    {
      id: 1,
      title: 'Collaborative Filtering',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Коллаборативная фильтрация' },
        { type: 'text', value: 'Collaborative filtering — самый распространённый подход в рекомендательных системах. Идея проста: рекомендуем пользователю то, что понравилось похожим пользователям (user-based) или похожие товары (item-based). Не нужно знать содержание — достаточно истории взаимодействий.' },
        { type: 'code', language: 'python', value: 'import numpy as np\nfrom sklearn.metrics.pairwise import cosine_similarity\n\n# Матрица оценок: пользователи x товары (0 = нет оценки)\nratings = np.array([\n    [5, 4, 0, 1, 0],  # User 0\n    [4, 0, 0, 1, 2],  # User 1\n    [0, 3, 5, 4, 0],  # User 2\n    [1, 0, 4, 5, 3],  # User 3\n    [0, 4, 0, 0, 5],  # User 4\n])\n\nitems = ["Фильм A", "Фильм B", "Фильм C", "Фильм D", "Фильм E"]\n\n# === User-Based Collaborative Filtering ===\nprint("=== User-Based CF ===\")\n\n# Заменяем 0 на NaN для корректного вычисления\nratings_nan = ratings.astype(float)\nratings_nan[ratings_nan == 0] = np.nan\n\n# Средняя оценка каждого пользователя\nuser_means = np.nanmean(ratings_nan, axis=1)\nprint(f"Средние оценки пользователей: {user_means.round(2)}")\n\n# Cosine similarity между пользователями (по ненулевым оценкам)\nratings_filled = np.where(ratings == 0, user_means.reshape(-1, 1), ratings)\nuser_sim = cosine_similarity(ratings_filled)\nnp.fill_diagonal(user_sim, 0)  # убираем самоподобие\n\nprint(f"\\nСхожесть с User 0:")\nfor i in range(1, 5):\n    print(f"  User {i}: {user_sim[0, i]:.4f}")\n\n# Предсказание для User 0, Фильм C (index 2)\ntarget_user = 0\ntarget_item = 2\n\nsimilarities = user_sim[target_user]\nweighted_sum = 0\nsim_sum = 0\nfor u in range(len(ratings)):\n    if u != target_user and ratings[u, target_item] > 0:\n        weighted_sum += similarities[u] * ratings[u, target_item]\n        sim_sum += abs(similarities[u])\n\npredicted = weighted_sum / sim_sum if sim_sum > 0 else user_means[target_user]\nprint(f"\\nПредсказание для User 0, {items[target_item]}: {predicted:.2f}")\n\n# === Item-Based CF ===\nprint("\\n=== Item-Based CF ===\")\nitem_sim = cosine_similarity(ratings.T)\nnp.fill_diagonal(item_sim, 0)\n\nprint(f"Похожие на {items[0]}:")\nfor i in range(1, 5):\n    print(f"  {items[i]}: {item_sim[0, i]:.4f}")' },
        { type: 'note', value: 'Item-based CF более стабилен, чем user-based, так как предпочтения товаров меняются реже, чем предпочтения пользователей. Amazon использует именно item-based подход.' }
      ]
    },
    {
      id: 2,
      title: 'Content-Based фильтрация',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Рекомендации на основе контента' },
        { type: 'text', value: 'Content-based фильтрация рекомендует товары, похожие на те, которые пользователь уже оценил положительно. Для этого нужны признаки (features) товаров: жанр, автор, ключевые слова. Профиль пользователя строится на основе признаков понравившихся товаров.' },
        { type: 'code', language: 'python', value: 'import numpy as np\nfrom sklearn.feature_extraction.text import TfidfVectorizer\nfrom sklearn.metrics.pairwise import cosine_similarity\n\n# Каталог фильмов с описаниями\nmovies = [\n    {"id": 0, "title": "Начало", "desc": "сон разум фантастика триллер головоломка"},\n    {"id": 1, "title": "Матрица", "desc": "виртуальная реальность фантастика боевик киберпанк"},\n    {"id": 2, "title": "Интерстеллар", "desc": "космос фантастика наука время путешествие"},\n    {"id": 3, "title": "Крёстный отец", "desc": "мафия драма семья преступление классика"},\n    {"id": 4, "title": "Побег из Шоушенка", "desc": "тюрьма драма надежда дружба свобода"},\n    {"id": 5, "title": "Бойцовский клуб", "desc": "драма триллер психология раздвоение личность"},\n    {"id": 6, "title": "Тёмный рыцарь", "desc": "супергерой боевик триллер злодей справедливость"},\n    {"id": 7, "title": "Довод", "desc": "время фантастика триллер инверсия шпион"},\n]\n\n# TF-IDF для описаний\ndescriptions = [m["desc"] for m in movies]\ntfidf = TfidfVectorizer()\ntfidf_matrix = tfidf.fit_transform(descriptions)\n\n# Cosine similarity между фильмами\nmovie_sim = cosine_similarity(tfidf_matrix)\n\ndef recommend(movie_id, n=3):\n    # Рекомендации на основе похожести контента\n    sim_scores = list(enumerate(movie_sim[movie_id]))\n    sim_scores = sorted(sim_scores, key=lambda x: x[1], reverse=True)\n    sim_scores = [s for s in sim_scores if s[0] != movie_id][:n]\n    return [(movies[i]["title"], score) for i, score in sim_scores]\n\nprint("Content-Based рекомендации:")\nfor movie in [0, 3, 6]:\n    print(f"\\nПохожие на \\\"{movies[movie][\'title\']}\\\":")\n    for title, score in recommend(movie):\n        print(f"  {title}: {score:.4f}")\n\n# Профиль пользователя\nprint("\\n--- Персонализированные рекомендации ---")\nuser_liked = [0, 2, 7]  # Начало, Интерстеллар, Довод\nuser_profile = tfidf_matrix[user_liked].mean(axis=0)\nuser_sim = cosine_similarity(user_profile, tfidf_matrix).flatten()\n\nprint(f"Пользователю нравятся: {[movies[i][\'title\'] for i in user_liked]}")\nprint("Рекомендации:")\nfor idx in np.argsort(user_sim)[::-1]:\n    if idx not in user_liked:\n        print(f"  {movies[idx][\'title\']}: {user_sim[idx]:.4f}")' },
        { type: 'list', items: [
          'Преимущества: не нужны данные других пользователей, объяснимые рекомендации',
          'Недостатки: ограничена профилем пользователя, не открывает новое',
          'Cold start для товаров решается (есть описание), для пользователей — нет',
          'Часто комбинируется с collaborative filtering в гибридных системах'
        ] }
      ]
    },
    {
      id: 3,
      title: 'Матричная факторизация (SVD)',
      type: 'theory',
      content: [
        { type: 'heading', value: 'SVD для рекомендательных систем' },
        { type: 'text', value: 'Матричная факторизация разлагает матрицу оценок на произведение двух матриц меньшего ранга: пользовательских и товарных эмбеддингов. SVD (Singular Value Decomposition) — классический метод. Библиотека Surprise предоставляет удобный API для рекомендательных систем.' },
        { type: 'code', language: 'python', value: 'from surprise import Dataset, Reader, SVD, SVDpp, NMF\nfrom surprise.model_selection import cross_validate\nimport pandas as pd\nimport numpy as np\n\n# Создаём датасет рейтингов\nnp.random.seed(42)\nn_users, n_items = 100, 50\nratings_data = []\n\nfor user in range(n_users):\n    n_rated = np.random.randint(5, 20)\n    items = np.random.choice(n_items, n_rated, replace=False)\n    for item in items:\n        rating = np.clip(np.random.normal(3.5, 1), 1, 5)\n        ratings_data.append((f"user_{user}", f"item_{item}", round(rating, 1)))\n\ndf = pd.DataFrame(ratings_data, columns=["user", "item", "rating"])\nprint(f"Датасет: {len(df)} оценок, {n_users} пользователей, {n_items} товаров")\nprint(f"Средняя оценка: {df[\'rating\'].mean():.2f}")\n\n# Surprise Dataset\nreader = Reader(rating_scale=(1, 5))\ndata = Dataset.load_from_df(df[[\"user\", \"item\", \"rating\"]], reader)\n\n# Сравнение алгоритмов\nalgorithms = {\n    "SVD": SVD(n_factors=50, n_epochs=20, random_state=42),\n    "SVD++": SVDpp(n_factors=50, n_epochs=20, random_state=42),\n    "NMF": NMF(n_factors=50, n_epochs=20, random_state=42)\n}\n\nprint("\\nСравнение алгоритмов (5-fold CV):")\nfor name, algo in algorithms.items():\n    results = cross_validate(algo, data, measures=["RMSE", "MAE"], cv=5, verbose=False)\n    print(f"  {name}: RMSE={results[\'test_rmse\'].mean():.4f}, MAE={results[\'test_mae\'].mean():.4f}")\n\n# Обучение лучшей модели и предсказания\ntrainset = data.build_full_trainset()\nsvd = SVD(n_factors=50, n_epochs=20, random_state=42)\nsvd.fit(trainset)\n\n# Top-N рекомендации для пользователя\nuser = "user_0"\nrated_items = set(df[df["user"] == user]["item"])\nall_items = set(df["item"].unique())\nunrated = all_items - rated_items\n\npredictions = [(item, svd.predict(user, item).est) for item in unrated]\npredictions.sort(key=lambda x: x[1], reverse=True)\n\nprint(f"\\nТоп-5 рекомендаций для {user}:")\nfor item, score in predictions[:5]:\n    print(f"  {item}: {score:.2f}")' },
        { type: 'tip', value: 'SVD++ учитывает не только оценки, но и сам факт взаимодействия (implicit feedback). Это улучшает качество на 2-5% по сравнению с обычным SVD.' }
      ]
    },
    {
      id: 4,
      title: 'Гибридные системы',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Комбинирование подходов' },
        { type: 'text', value: 'Гибридные рекомендательные системы комбинируют collaborative filtering и content-based подходы. Это решает проблемы каждого метода: cold start (CF не может рекомендовать новым пользователям) и ограниченность профиля (content-based не открывает новое).' },
        { type: 'code', language: 'python', value: 'import numpy as np\nfrom sklearn.metrics.pairwise import cosine_similarity\nfrom sklearn.feature_extraction.text import TfidfVectorizer\n\n# Данные\nnp.random.seed(42)\nn_users, n_items = 20, 10\n\n# Матрица оценок (0 = нет оценки)\nratings = np.random.choice([0, 1, 2, 3, 4, 5], size=(n_users, n_items),\n                            p=[0.5, 0.05, 0.1, 0.1, 0.15, 0.1])\n\n# Контентные признаки товаров\nitem_descriptions = [\n    "боевик фантастика космос",\n    "драма романтика любовь",\n    "комедия семья приключения",\n    "ужасы триллер мистика",\n    "фантастика роботы будущее",\n    "драма война история",\n    "комедия романтика свидание",\n    "боевик супергерой спасение",\n    "триллер детектив расследование",\n    "фантастика путешествие время"\n]\n\ntfidf = TfidfVectorizer()\ncontent_matrix = tfidf.fit_transform(item_descriptions)\ncontent_sim = cosine_similarity(content_matrix)\n\nclass HybridRecommender:\n    def __init__(self, ratings, content_sim, alpha=0.5):\n        self.ratings = ratings\n        self.content_sim = content_sim\n        self.alpha = alpha  # вес CF vs content\n        self._fit_cf()\n    \n    def _fit_cf(self):\n        # Collaborative filtering scores\n        # Item-based CF similarity\n        ratings_filled = self.ratings.copy().astype(float)\n        item_means = np.where(self.ratings > 0, self.ratings, np.nan)\n        self.item_means = np.nanmean(item_means, axis=0)\n        ratings_filled[ratings_filled == 0] = self.item_means[np.newaxis, :].repeat(self.ratings.shape[0], 0)[ratings_filled == 0]\n        self.cf_sim = cosine_similarity(ratings_filled.T)\n    \n    def recommend(self, user_id, n=5):\n        rated = np.where(self.ratings[user_id] > 0)[0]\n        unrated = np.where(self.ratings[user_id] == 0)[0]\n        \n        scores = []\n        for item in unrated:\n            # CF score\n            cf_score = 0\n            cf_weight = 0\n            for r_item in rated:\n                cf_score += self.cf_sim[item, r_item] * self.ratings[user_id, r_item]\n                cf_weight += abs(self.cf_sim[item, r_item])\n            cf_score = cf_score / cf_weight if cf_weight > 0 else self.item_means[item]\n            \n            # Content score\n            content_score = 0\n            for r_item in rated:\n                content_score += self.content_sim[item, r_item] * self.ratings[user_id, r_item]\n            content_score /= max(len(rated), 1)\n            \n            # Гибридный score\n            hybrid_score = self.alpha * cf_score + (1 - self.alpha) * content_score\n            scores.append((item, hybrid_score))\n        \n        scores.sort(key=lambda x: x[1], reverse=True)\n        return scores[:n]\n\n# Тестирование\nfor alpha in [0.0, 0.5, 1.0]:\n    hybrid = HybridRecommender(ratings, content_sim, alpha=alpha)\n    recs = hybrid.recommend(user_id=0, n=3)\n    label = {0.0: "Content-only", 0.5: "Hybrid", 1.0: "CF-only"}[alpha]\n    print(f"\\n{label} (alpha={alpha}):")\n    for item_id, score in recs:\n        print(f"  Item {item_id}: score={score:.3f}")' },
        { type: 'list', items: [
          'Weighted hybrid: комбинация scores от CF и content-based',
          'Switching hybrid: выбор метода в зависимости от ситуации',
          'Feature augmentation: признаки одного метода используются в другом',
          'Meta-level: выход одной модели — вход другой'
        ] }
      ]
    },
    {
      id: 5,
      title: 'Метрики: NDCG, MAP',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Оценка рекомендательных систем' },
        { type: 'text', value: 'Для оценки рекомендательных систем используются ranking-метрики: NDCG учитывает позицию релевантных элементов, MAP — среднюю точность по всем пользователям, Hit Rate — долю пользователей с хотя бы одной правильной рекомендацией.' },
        { type: 'code', language: 'python', value: 'import numpy as np\n\ndef precision_at_k(recommended, relevant, k):\n    # Precision@K: доля релевантных в Top-K\n    rec_k = recommended[:k]\n    return len(set(rec_k) & set(relevant)) / k\n\ndef recall_at_k(recommended, relevant, k):\n    # Recall@K: доля найденных релевантных в Top-K\n    rec_k = recommended[:k]\n    return len(set(rec_k) & set(relevant)) / len(relevant) if relevant else 0\n\ndef ap_at_k(recommended, relevant, k):\n    # Average Precision@K\n    score = 0\n    hits = 0\n    for i, item in enumerate(recommended[:k]):\n        if item in relevant:\n            hits += 1\n            score += hits / (i + 1)\n    return score / min(len(relevant), k) if relevant else 0\n\ndef ndcg_at_k(recommended, relevant, k):\n    # Normalized Discounted Cumulative Gain@K\n    dcg = 0\n    for i, item in enumerate(recommended[:k]):\n        if item in relevant:\n            dcg += 1 / np.log2(i + 2)  # позиция начинается с 1\n    \n    # Ideal DCG\n    idcg = sum(1 / np.log2(i + 2) for i in range(min(len(relevant), k)))\n    return dcg / idcg if idcg > 0 else 0\n\ndef hit_rate_at_k(recommendations_list, relevant_list, k):\n    # Hit Rate@K: доля пользователей с хотя бы 1 попаданием\n    hits = 0\n    for rec, rel in zip(recommendations_list, relevant_list):\n        if set(rec[:k]) & set(rel):\n            hits += 1\n    return hits / len(recommendations_list)\n\n# Пример\nnp.random.seed(42)\nn_users = 100\nall_recs = []\nall_relevant = []\n\nfor _ in range(n_users):\n    # Рекомендации (10 товаров)\n    rec = list(np.random.permutation(50)[:10])\n    # Релевантные товары\n    rel = list(np.random.choice(50, np.random.randint(2, 8), replace=False))\n    all_recs.append(rec)\n    all_relevant.append(rel)\n\nfor k in [3, 5, 10]:\n    prec = np.mean([precision_at_k(r, rel, k) for r, rel in zip(all_recs, all_relevant)])\n    rec_score = np.mean([recall_at_k(r, rel, k) for r, rel in zip(all_recs, all_relevant)])\n    map_score = np.mean([ap_at_k(r, rel, k) for r, rel in zip(all_recs, all_relevant)])\n    ndcg = np.mean([ndcg_at_k(r, rel, k) for r, rel in zip(all_recs, all_relevant)])\n    hr = hit_rate_at_k(all_recs, all_relevant, k)\n    \n    print(f"@{k}: P={prec:.4f} R={rec_score:.4f} MAP={map_score:.4f} NDCG={ndcg:.4f} HR={hr:.4f}")\n\nprint("\\n--- Выбор метрики ---")\nprint("NDCG: учитывает позицию (top важнее)")\nprint("MAP: средняя точность, хороша для binary relevance")\nprint("Hit Rate: есть ли хоть что-то полезное в рекомендациях")\nprint("Precision@K: доля полезных в Top-K")' },
        { type: 'note', value: 'NDCG — стандартная метрика для ranking задач, так как она учитывает позицию: релевантный элемент на 1-м месте ценнее, чем на 10-м. Логарифмическое затухание моделирует поведение пользователя, который смотрит преимущественно первые результаты.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Рекомендательная система',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте простую рекомендательную систему на основе item-based collaborative filtering.',
      requirements: [
        'Создайте матрицу оценок (10 пользователей x 8 товаров) с пропусками (0)',
        'Вычислите cosine similarity между товарами',
        'Реализуйте функцию predict(user_id, item_id) для предсказания оценки',
        'Реализуйте функцию recommend(user_id, n=3) для Top-N рекомендаций',
        'Покажите рекомендации для 2 разных пользователей'
      ],
      hint: 'Предсказание = взвешенное среднее оценок похожих товаров, которые пользователь уже оценил. Вес = cosine similarity. Рекомендуйте только неоценённые товары.',
      expectedOutput: 'Item-Based CF:\nСхожесть товаров вычислена: 8x8\n\nUser 0 рекомендации:\n  Item X: predicted=X.XX\n  Item Y: predicted=X.XX\n  Item Z: predicted=X.XX\n\nUser 1 рекомендации:\n  ...',
      solution: 'import numpy as np\nfrom sklearn.metrics.pairwise import cosine_similarity\n\nnp.random.seed(42)\n\n# Матрица оценок (0 = нет оценки)\nratings = np.array([\n    [5, 4, 0, 1, 0, 2, 0, 3],\n    [4, 0, 3, 1, 2, 0, 4, 0],\n    [0, 3, 5, 4, 0, 5, 0, 2],\n    [1, 0, 4, 5, 3, 0, 2, 0],\n    [0, 4, 0, 0, 5, 3, 0, 4],\n    [5, 0, 0, 2, 0, 4, 3, 0],\n    [0, 5, 4, 0, 3, 0, 5, 1],\n    [3, 0, 5, 4, 0, 2, 0, 5],\n    [0, 2, 0, 3, 4, 0, 1, 0],\n    [4, 3, 0, 0, 0, 5, 0, 2],\n])\n\n# Item similarity\nitem_sim = cosine_similarity(ratings.T)\nnp.fill_diagonal(item_sim, 0)\n\nprint("Item-Based CF:")\nprint(f"Схожесть товаров вычислена: {item_sim.shape[0]}x{item_sim.shape[1]}")\n\ndef predict(user_id, item_id):\n    rated = np.where(ratings[user_id] > 0)[0]\n    if len(rated) == 0:\n        return 0\n    sims = item_sim[item_id, rated]\n    scores = ratings[user_id, rated]\n    if sims.sum() == 0:\n        return scores.mean()\n    return np.dot(sims, scores) / np.abs(sims).sum()\n\ndef recommend(user_id, n=3):\n    unrated = np.where(ratings[user_id] == 0)[0]\n    predictions = [(item, predict(user_id, item)) for item in unrated]\n    predictions.sort(key=lambda x: x[1], reverse=True)\n    return predictions[:n]\n\nfor user in [0, 1]:\n    print(f"\\nUser {user} рекомендации:")\n    for item, score in recommend(user, n=3):\n        print(f"  Item {item}: predicted={score:.2f}")',
      explanation: 'Item-based collaborative filtering предсказывает оценку как взвешенное среднее оценок похожих товаров. Cosine similarity между столбцами матрицы оценок определяет похожесть товаров. Этот подход стабильнее user-based, так как предпочтения товаров меняются реже.'
    }
  ]
}
