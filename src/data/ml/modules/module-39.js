export default {
  id: 39,
  title: 'Аугментация данных',
  description: 'Аугментация изображений (Albumentations), текстов, SMOTE для табличных данных и Mixup/CutMix.',
  lessons: [
    {
      id: 1,
      title: 'Зачем нужна аугментация',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Data Augmentation: больше данных без сбора' },
        { type: 'text', value: 'Data augmentation увеличивает размер обучающей выборки путём создания модифицированных копий существующих данных. Это борется с overfitting, улучшает робастность модели и особенно эффективно при малом количестве данных. Каждый тип данных требует своих методов аугментации.' },
        { type: 'code', language: 'python', value: 'import numpy as np\nfrom sklearn.datasets import make_classification\nfrom sklearn.model_selection import cross_val_score\nfrom sklearn.ensemble import RandomForestClassifier\n\n# Демонстрация эффекта аугментации\nnp.random.seed(42)\n\n# Маленький датасет\nX_small, y_small = make_classification(n_samples=100, n_features=10, random_state=42)\nX_large, y_large = make_classification(n_samples=1000, n_features=10, random_state=42)\n\nmodel = RandomForestClassifier(n_estimators=100, random_state=42)\n\nscore_small = cross_val_score(model, X_small, y_small, cv=5, scoring="roc_auc").mean()\nscore_large = cross_val_score(model, X_large, y_large, cv=5, scoring="roc_auc").mean()\n\n# Простая аугментация: добавление шума\ndef augment_with_noise(X, y, n_augmented=5, noise_std=0.1):\n    X_aug = [X]\n    y_aug = [y]\n    for _ in range(n_augmented):\n        noise = np.random.normal(0, noise_std, X.shape)\n        X_aug.append(X + noise)\n        y_aug.append(y)\n    return np.vstack(X_aug), np.concatenate(y_aug)\n\nX_aug, y_aug = augment_with_noise(X_small, y_small, n_augmented=9)\nscore_aug = cross_val_score(model, X_aug, y_aug, cv=5, scoring="roc_auc").mean()\n\nprint("Эффект аугментации:")\nprint(f"  Маленький датасет (100): AUC = {score_small:.4f}")\nprint(f"  С аугментацией (1000):   AUC = {score_aug:.4f}")\nprint(f"  Большой датасет (1000):  AUC = {score_large:.4f}")\nprint(f"  Улучшение от аугментации: +{(score_aug - score_small)*100:.2f}%")\n\nprint("\\n--- Типы аугментации ---")\nprint("Изображения: flip, rotate, crop, color jitter, blur")\nprint("Текст: synonym replacement, back-translation, insertion")\nprint("Табличные: SMOTE, noise injection, mixup")\nprint("Аудио: time stretch, pitch shift, noise addition")\nprint("Временные ряды: window slicing, jittering, scaling")' },
        { type: 'note', value: 'Аугментация должна создавать реалистичные данные! Для медицинских изображений вертикальный flip может быть неуместен. Всегда учитывайте domain knowledge при выборе аугментаций.' }
      ]
    },
    {
      id: 2,
      title: 'Image Augmentation (Albumentations)',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Аугментация изображений' },
        { type: 'text', value: 'Albumentations — самая быстрая библиотека для аугментации изображений. Она поддерживает одновременное преобразование изображений, масок, bounding boxes и keypoints. Встроенная интеграция с PyTorch и TensorFlow.' },
        { type: 'code', language: 'python', value: 'import albumentations as A\nimport numpy as np\n\n# Тестовое изображение\nnp.random.seed(42)\nimage = np.random.randint(0, 256, (256, 256, 3), dtype=np.uint8)\nmask = np.random.randint(0, 2, (256, 256), dtype=np.uint8)  # для сегментации\nbbox = [50, 50, 200, 200]  # [x_min, y_min, x_max, y_max]\n\n# 1. Лёгкая аугментация (для начала)\nlight_transform = A.Compose([\n    A.HorizontalFlip(p=0.5),\n    A.RandomBrightnessContrast(p=0.3),\n    A.Resize(224, 224),\n    A.Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225]),\n])\n\n# 2. Средняя аугментация\nmedium_transform = A.Compose([\n    A.RandomResizedCrop(224, 224, scale=(0.8, 1.0)),\n    A.HorizontalFlip(p=0.5),\n    A.ShiftScaleRotate(shift_limit=0.1, scale_limit=0.1, rotate_limit=15, p=0.5),\n    A.ColorJitter(brightness=0.2, contrast=0.2, saturation=0.2, hue=0.1, p=0.5),\n    A.GaussNoise(var_limit=(10, 30), p=0.3),\n    A.Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225]),\n])\n\n# 3. Агрессивная аугментация\nheavy_transform = A.Compose([\n    A.RandomResizedCrop(224, 224, scale=(0.5, 1.0)),\n    A.HorizontalFlip(p=0.5),\n    A.VerticalFlip(p=0.2),\n    A.ShiftScaleRotate(shift_limit=0.2, scale_limit=0.2, rotate_limit=45, p=0.7),\n    A.OneOf([\n        A.GaussNoise(var_limit=(10, 50)),\n        A.GaussianBlur(blur_limit=7),\n        A.MotionBlur(blur_limit=7),\n    ], p=0.5),\n    A.OneOf([\n        A.OpticalDistortion(distort_limit=0.3),\n        A.GridDistortion(),\n        A.ElasticTransform(alpha=120, sigma=120 * 0.05),\n    ], p=0.3),\n    A.CoarseDropout(max_holes=8, max_height=32, max_width=32, fill_value=0, p=0.5),\n    A.ColorJitter(brightness=0.3, contrast=0.3, saturation=0.3, hue=0.15, p=0.7),\n    A.Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225]),\n])\n\n# Тест\nfor name, transform in [(\"Light\", light_transform), (\"Medium\", medium_transform), (\"Heavy\", heavy_transform)]:\n    aug = transform(image=image)\n    img = aug[\"image\"]\n    print(f\"{name:8s}: shape={img.shape}, range=[{img.min():.2f}, {img.max():.2f}]\")\n\n# С bbox и mask\ntransform_with_bbox = A.Compose([\n    A.HorizontalFlip(p=0.5),\n    A.Resize(224, 224),\n], bbox_params=A.BboxParams(format="pascal_voc\", label_fields=[\"labels\"]))\n\naug = transform_with_bbox(image=image, bboxes=[bbox], labels=[\"cat\"])\nprint(f\"\\nС bbox: image={aug[\'image\'].shape}, bbox={aug[\'bboxes\']}\")' },
        { type: 'tip', value: 'CoarseDropout (аналог CutOut) — очень эффективная аугментация. Она случайно вырезает прямоугольные области, заставляя модель не полагаться на одну конкретную часть изображения.' }
      ]
    },
    {
      id: 3,
      title: 'Text Augmentation',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Аугментация текстов' },
        { type: 'text', value: 'Текстовая аугментация сложнее, чем для изображений, так как даже небольшие изменения могут изменить смысл. Основные методы: замена синонимами, back-translation, random insertion/deletion, EDA (Easy Data Augmentation).' },
        { type: 'code', language: 'python', value: 'import numpy as np\nimport random\n\n# EDA — Easy Data Augmentation\nclass EDAugmenter:\n    def __init__(self):\n        # Словарь синонимов\n        self.synonyms = {\n            "хороший": ["отличный", "прекрасный", "замечательный"],\n            "плохой": ["ужасный", "скверный", "отвратительный"],\n            "большой": ["огромный", "крупный", "гигантский"],\n            "маленький": ["крошечный", "миниатюрный", "небольшой"],\n            "быстрый": ["скоростной", "стремительный", "молниеносный"],\n            "модель": ["алгоритм", "система"],\n            "данные": ["информация", "сведения"],\n        }\n    \n    def synonym_replacement(self, text, n=1):\n        # Замена n слов синонимами\n        words = text.split()\n        new_words = words.copy()\n        replaceable = [w for w in words if w.lower() in self.synonyms]\n        \n        for _ in range(min(n, len(replaceable))):\n            word = random.choice(replaceable)\n            synonym = random.choice(self.synonyms[word.lower()])\n            idx = new_words.index(word)\n            new_words[idx] = synonym\n        \n        return " ".join(new_words)\n    \n    def random_insertion(self, text, n=1):\n        # Вставка случайных синонимов\n        words = text.split()\n        for _ in range(n):\n            all_synonyms = [s for syns in self.synonyms.values() for s in syns]\n            new_word = random.choice(all_synonyms)\n            pos = random.randint(0, len(words))\n            words.insert(pos, new_word)\n        return " ".join(words)\n    \n    def random_swap(self, text, n=1):\n        # Перестановка n пар слов\n        words = text.split()\n        for _ in range(n):\n            if len(words) >= 2:\n                i, j = random.sample(range(len(words)), 2)\n                words[i], words[j] = words[j], words[i]\n        return " ".join(words)\n    \n    def random_deletion(self, text, p=0.1):\n        # Удаление слов с вероятностью p\n        words = text.split()\n        if len(words) <= 1:\n            return text\n        remaining = [w for w in words if random.random() > p]\n        return " ".join(remaining) if remaining else random.choice(words)\n    \n    def augment(self, text, num_aug=4):\n        # Генерация num_aug аугментаций\n        augmented = set()\n        methods = [self.synonym_replacement, self.random_insertion,\n                   self.random_swap, self.random_deletion]\n        \n        for method in methods:\n            try:\n                aug_text = method(text)\n                if aug_text != text:\n                    augmented.add(aug_text)\n            except:\n                pass\n        \n        return list(augmented)[:num_aug]\n\n# Тест\neda = EDAugmenter()\ntext = "хороший алгоритм работает с большой моделью и данные обрабатывает быстрый"\n\nprint(f"Оригинал: {text}")\nprint(f"\\nАугментации:")\nfor method_name, method in [(\"Synonym\", eda.synonym_replacement),\n                             (\"Insert\", eda.random_insertion),\n                             (\"Swap\", eda.random_swap),\n                             (\"Delete\", eda.random_deletion)]:\n    result = method(text)\n    print(f"  {method_name:8s}: {result}")\n\nprint("\\n--- Другие методы ---")\nprint("Back-translation: RU -> EN -> RU (через API перевода)")\nprint("Contextual augmentation: BERT заменяет слова по контексту")\nprint("Paraphrase: T5/GPT генерирует парафразы")\nprint("nlpaug: библиотека для текстовой аугментации")' },
        { type: 'note', value: 'Back-translation (перевод текста на другой язык и обратно) — один из самых эффективных методов текстовой аугментации. Он создаёт семантически эквивалентные, но лексически разные тексты.' }
      ]
    },
    {
      id: 4,
      title: 'SMOTE для табличных данных',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Oversampling с SMOTE' },
        { type: 'text', value: 'SMOTE (Synthetic Minority Over-sampling Technique) создаёт синтетические примеры миноритарного класса, интерполируя между существующими примерами. Это решает проблему дисбаланса классов, когда один класс значительно меньше другого.' },
        { type: 'code', language: 'python', value: 'import numpy as np\nfrom sklearn.datasets import make_classification\nfrom sklearn.model_selection import cross_val_score, StratifiedKFold\nfrom sklearn.ensemble import RandomForestClassifier\nfrom sklearn.metrics import classification_report\nfrom imblearn.over_sampling import SMOTE, ADASYN, BorderlineSMOTE\nfrom imblearn.under_sampling import RandomUnderSampler\nfrom imblearn.combine import SMOTETomek\nfrom imblearn.pipeline import Pipeline as ImbPipeline\n\n# Несбалансированный датасет (95% / 5%)\nX, y = make_classification(n_samples=2000, n_features=10, n_informative=5,\n                           weights=[0.95], flip_y=0, random_state=42)\n\nprint(f"Баланс классов: {np.bincount(y)} ({np.mean(y==1)*100:.1f}% minority)")\n\n# 1. Без SMOTE\nmodel = RandomForestClassifier(n_estimators=100, random_state=42)\nscore_base = cross_val_score(model, X, y, cv=5, scoring="f1").mean()\nprint(f"\\nБез SMOTE: F1={score_base:.4f}")\n\n# 2. SMOTE\nsmote = SMOTE(random_state=42)\nX_smote, y_smote = smote.fit_resample(X, y)\nprint(f"\\nSMOTE: {np.bincount(y)} -> {np.bincount(y_smote)}")\nscore_smote = cross_val_score(model, X_smote, y_smote, cv=5, scoring="f1").mean()\nprint(f"SMOTE: F1={score_smote:.4f}")\n\n# 3. Варианты SMOTE\nvariants = {\n    "SMOTE": SMOTE(random_state=42),\n    "BorderlineSMOTE": BorderlineSMOTE(random_state=42),\n    "ADASYN": ADASYN(random_state=42),\n    "SMOTETomek": SMOTETomek(random_state=42),\n}\n\nprint(f"\\nСравнение методов (F1-score):")\nfor name, sampler in variants.items():\n    X_res, y_res = sampler.fit_resample(X, y)\n    score = cross_val_score(model, X_res, y_res, cv=5, scoring="f1").mean()\n    print(f"  {name:20s}: F1={score:.4f} (samples: {len(y_res)})")\n\n# 4. Правильный pipeline (SMOTE внутри CV!)\nprint("\\n=== Правильный Pipeline ===\")\nimb_pipeline = ImbPipeline([\n    ("smote", SMOTE(random_state=42)),\n    ("classifier", RandomForestClassifier(n_estimators=100, random_state=42))\n])\n\ncv = StratifiedKFold(n_splits=5, shuffle=True, random_state=42)\nscore_correct = cross_val_score(imb_pipeline, X, y, cv=cv, scoring="f1").mean()\nprint(f"SMOTE в Pipeline: F1={score_correct:.4f}")' },
        { type: 'warning', value: 'SMOTE нужно применять ТОЛЬКО к train данным, не к test! Используйте imblearn.pipeline.Pipeline для правильной интеграции с cross-validation. Иначе вы получите утечку данных.' }
      ]
    },
    {
      id: 5,
      title: 'Mixup и CutMix',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Mixup, CutMix и CutOut' },
        { type: 'text', value: 'Mixup создаёт новые примеры путём линейной интерполяции между парами. CutMix вырезает часть одного изображения и вставляет в другое. CutOut (Dropout на входе) случайно маскирует прямоугольные области. Эти методы значительно улучшают обобщение.' },
        { type: 'code', language: 'python', value: 'import numpy as np\n\n# 1. Mixup\ndef mixup(X, y, alpha=0.2):\n    # Mixup: линейная интерполяция между парами\n    batch_size = len(X)\n    lam = np.random.beta(alpha, alpha, batch_size)  # lambda ~ Beta(alpha, alpha)\n    \n    # Случайная перестановка\n    indices = np.random.permutation(batch_size)\n    \n    # Линейная комбинация\n    X_mixed = np.array([lam[i] * X[i] + (1 - lam[i]) * X[indices[i]] for i in range(batch_size)])\n    y_mixed_a = y\n    y_mixed_b = y[indices]\n    \n    return X_mixed, y_mixed_a, y_mixed_b, lam\n\n# 2. CutMix (для изображений)\ndef cutmix(images, labels, alpha=1.0):\n    # CutMix: вырезаем часть одного изображения и вставляем в другое\n    batch_size, h, w, c = images.shape\n    indices = np.random.permutation(batch_size)\n    lam = np.random.beta(alpha, alpha)\n    \n    # Размер вырезаемой области\n    cut_ratio = np.sqrt(1 - lam)\n    cut_h = int(h * cut_ratio)\n    cut_w = int(w * cut_ratio)\n    \n    # Координаты\n    cx = np.random.randint(0, w)\n    cy = np.random.randint(0, h)\n    x1 = max(0, cx - cut_w // 2)\n    y1 = max(0, cy - cut_h // 2)\n    x2 = min(w, cx + cut_w // 2)\n    y2 = min(h, cy + cut_h // 2)\n    \n    # Применяем CutMix\n    images_mixed = images.copy()\n    images_mixed[:, y1:y2, x1:x2, :] = images[indices, y1:y2, x1:x2, :]\n    \n    # Корректируем lambda по реальной площади\n    lam_adjusted = 1 - (y2-y1) * (x2-x1) / (h * w)\n    \n    return images_mixed, labels, labels[indices], lam_adjusted\n\n# 3. CutOut\ndef cutout(images, n_holes=1, hole_size=16):\n    # CutOut: маскируем прямоугольные области\n    h, w = images.shape[1], images.shape[2]\n    masked = images.copy()\n    \n    for _ in range(n_holes):\n        cx = np.random.randint(0, w)\n        cy = np.random.randint(0, h)\n        x1 = max(0, cx - hole_size // 2)\n        y1 = max(0, cy - hole_size // 2)\n        x2 = min(w, cx + hole_size // 2)\n        y2 = min(h, cy + hole_size // 2)\n        masked[:, y1:y2, x1:x2, :] = 0  # заполняем нулями\n    \n    return masked\n\n# Демонстрация\nnp.random.seed(42)\nX = np.random.randn(8, 32, 32, 3).astype(np.float32)\ny = np.array([0, 0, 1, 1, 0, 1, 0, 1])\n\n# Mixup\nX_mix, y_a, y_b, lam = mixup(X.reshape(8, -1), y)\nprint("Mixup:")\nfor i in range(3):\n    print(f"  Sample {i}: class_a={y_a[i]}, class_b={y_b[i]}, lambda={lam[i]:.3f}")\n\n# CutMix\nX_cm, y_a_cm, y_b_cm, lam_cm = cutmix(X, y)\nprint(f"\\nCutMix: lambda={lam_cm:.3f}")\n\n# CutOut\nX_co = cutout(X, n_holes=2, hole_size=8)\nzero_ratio = (X_co == 0).mean()\nprint(f"\\nCutOut: {zero_ratio*100:.1f}% пикселей замаскировано")\n\nprint("\\n--- Сравнение ---")\nprint("Mixup: мягкие метки, помогает калибровке")\nprint("CutMix: сохраняет пространственную информацию")\nprint("CutOut: простой, но эффективный dropout на входе")' },
        { type: 'tip', value: 'Mixup с alpha=0.2 — хороший default. При Mixup loss вычисляется как: loss = lambda * loss(pred, y_a) + (1-lambda) * loss(pred, y_b). Это создаёт "мягкие" метки, что улучшает калибровку модели.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: SMOTE для дисбаланса',
      type: 'practice',
      difficulty: 'easy',
      description: 'Примените SMOTE для балансировки классов и покажите улучшение F1-score.',
      requirements: [
        'Создайте несбалансированный датасет (1000 сэмплов, 5% minority)',
        'Обучите RandomForest без SMOTE и вычислите F1',
        'Примените SMOTE для балансировки',
        'Обучите RandomForest с SMOTE и вычислите F1',
        'Выведите баланс классов до и после SMOTE и разницу в F1'
      ],
      hint: 'make_classification с weights=[0.95] создаёт дисбаланс. imblearn.over_sampling.SMOTE балансирует. Используйте scoring="f1" в cross_val_score.',
      expectedOutput: 'SMOTE Balancing:\nДо SMOTE: [950, 50], F1=0.XX\nПосле SMOTE: [950, 950], F1=0.XX\nУлучшение F1: +X.XX',
      solution: 'import numpy as np\nfrom sklearn.datasets import make_classification\nfrom sklearn.model_selection import cross_val_score\nfrom sklearn.ensemble import RandomForestClassifier\nfrom imblearn.over_sampling import SMOTE\n\nX, y = make_classification(n_samples=1000, n_features=10, n_informative=5,\n                           weights=[0.95], flip_y=0, random_state=42)\n\nmodel = RandomForestClassifier(n_estimators=100, random_state=42)\nf1_before = cross_val_score(model, X, y, cv=5, scoring="f1").mean()\n\nsmote = SMOTE(random_state=42)\nX_res, y_res = smote.fit_resample(X, y)\n\nf1_after = cross_val_score(model, X_res, y_res, cv=5, scoring="f1").mean()\n\nprint("SMOTE Balancing:")\nprint(f"До SMOTE: {list(np.bincount(y))}, F1={f1_before:.4f}")\nprint(f"После SMOTE: {list(np.bincount(y_res))}, F1={f1_after:.4f}")\nprint(f"Улучшение F1: +{f1_after - f1_before:.4f}")',
      explanation: 'SMOTE создаёт синтетические примеры миноритарного класса путём интерполяции между ближайшими соседями. Это балансирует классы и позволяет модели лучше выучить minority class. F1-score чувствителен к дисбалансу, поэтому улучшение заметно.'
    }
  ]
}
