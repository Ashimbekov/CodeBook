export default {
  id: 55,
  title: 'Проект: Генерация изображений',
  description: 'Полный проект по генеративным моделям: VAE, GAN, DCGAN, Conditional GAN, Stable Diffusion и Neural Style Transfer.',
  lessons: [
    {
      id: 1,
      title: 'Описание проекта',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Генеративные модели: GAN vs VAE vs Diffusion' },
        { type: 'text', value: 'Генеративные модели создают новые данные, похожие на обучающие. Три основных подхода: VAE (Variational Autoencoder) — обучает латентное пространство и генерирует через декодер; GAN (Generative Adversarial Network) — генератор vs дискриминатор; Diffusion — постепенное удаление шума из случайного изображения.' },
        { type: 'text', value: 'VAE: стабильное обучение, гладкое латентное пространство, но размытые изображения. GAN: острые, реалистичные изображения, но нестабильное обучение (mode collapse). Diffusion: лучшее качество, но медленная генерация.' },
        { type: 'code', language: 'python', value: '# Проект: генерация изображений\n# image_generation/\n# ├── models/\n# │   ├── vae.py\n# │   ├── dcgan.py\n# │   ├── conditional_gan.py\n# │   └── style_transfer.py\n# ├── notebooks/\n# │   ├── 01_vae_mnist.ipynb\n# │   ├── 02_dcgan.ipynb\n# │   ├── 03_conditional_gan.ipynb\n# │   ├── 04_stable_diffusion.ipynb\n# │   └── 05_style_transfer.ipynb\n# └── generate.py\n\nimport numpy as np\n\n# Сравнение генеративных моделей\nmodels_comparison = [\n    ("VAE", "Encoder-Decoder", "Стабильное", "Размытые", "Быстрая", "2013"),\n    ("GAN", "Generator-Discriminator", "Нестабильное", "Острые", "Быстрая", "2014"),\n    ("DCGAN", "Conv GAN", "Умеренное", "Хорошие", "Быстрая", "2015"),\n    ("StyleGAN", "Style-based GAN", "Сложное", "Фотореалистичные", "Быстрая", "2018"),\n    ("Diffusion", "Denoise iterative", "Стабильное", "Лучшие", "Медленная", "2020"),\n    ("Stable Diffusion", "Latent Diffusion", "Стабильное", "Отличные", "Средняя", "2022"),\n]\n\nprint("=== Генеративные модели ===")\nprint(f"{\'Модель\':<18} {\'Архитектура\':<22} {\'Обучение\':<14} {\'Качество\':<18} {\'Скорость\':<10} {\'Год\'}") \nfor row in models_comparison:\n    print(f"{row[0]:<18} {row[1]:<22} {row[2]:<14} {row[3]:<18} {row[4]:<10} {row[5]}")\n\nprint("\\nОсновные применения:")\nprint("  • Генерация лиц (StyleGAN, Diffusion)")\nprint("  • Text-to-Image (Stable Diffusion, DALL-E)")\nprint("  • Image-to-Image (pix2pix, CycleGAN)")\nprint("  • Super Resolution (ESRGAN)")\nprint("  • Style Transfer (Neural Style)")' },
        { type: 'note', value: 'Stable Diffusion (2022) — текущий state-of-the-art для генерации изображений. Работает в латентном пространстве (не пиксельном), что ускоряет генерацию в 10-50 раз по сравнению с обычными diffusion моделями.' },
        { type: 'tip', value: 'Для обучения с нуля: начните с VAE на MNIST (простейший), затем DCGAN, затем Conditional GAN. Stable Diffusion лучше использовать через API или fine-tuning предобученной модели.' }
      ]
    },
    {
      id: 2,
      title: 'Автоэнкодер',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Encoder-Decoder и VAE' },
        { type: 'text', value: 'Автоэнкодер сжимает входные данные в низкоразмерное латентное представление (encoder) и восстанавливает обратно (decoder). Обычный автоэнкодер — для сжатия и denoising. VAE (Variational Autoencoder) — для генерации: латентное пространство организовано как нормальное распределение, что позволяет семплировать новые точки.' },
        { type: 'text', value: 'VAE loss = Reconstruction loss (MSE/BCE) + KL divergence. KL divergence заставляет латентное распределение быть близким к N(0,1). Это гарантирует гладкость: близкие точки в латентном пространстве дают похожие изображения.' },
        { type: 'code', language: 'python', value: 'import tensorflow as tf\nfrom tensorflow.keras import layers\nimport numpy as np\n\nclass Sampling(layers.Layer):\n    """Reparameterization trick: z = mu + sigma * epsilon"""\n    def call(self, inputs):\n        z_mean, z_log_var = inputs\n        batch = tf.shape(z_mean)[0]\n        dim = tf.shape(z_mean)[1]\n        epsilon = tf.random.normal(shape=(batch, dim))\n        return z_mean + tf.exp(0.5 * z_log_var) * epsilon\n\ndef build_vae(latent_dim=2):\n    # Encoder\n    encoder_inputs = layers.Input(shape=(28, 28, 1))\n    x = layers.Conv2D(32, 3, strides=2, padding="same", activation="relu")(encoder_inputs)\n    x = layers.Conv2D(64, 3, strides=2, padding="same", activation="relu")(x)\n    x = layers.Flatten()(x)\n    x = layers.Dense(128, activation="relu")(x)\n    z_mean = layers.Dense(latent_dim, name="z_mean")(x)\n    z_log_var = layers.Dense(latent_dim, name="z_log_var")(x)\n    z = Sampling()([z_mean, z_log_var])\n    encoder = tf.keras.Model(encoder_inputs, [z_mean, z_log_var, z], name="encoder")\n    \n    # Decoder\n    latent_inputs = layers.Input(shape=(latent_dim,))\n    x = layers.Dense(7 * 7 * 64, activation="relu")(latent_inputs)\n    x = layers.Reshape((7, 7, 64))(x)\n    x = layers.Conv2DTranspose(64, 3, strides=2, padding="same", activation="relu")(x)\n    x = layers.Conv2DTranspose(32, 3, strides=2, padding="same", activation="relu")(x)\n    decoder_outputs = layers.Conv2DTranspose(1, 3, padding="same", activation="sigmoid")(x)\n    decoder = tf.keras.Model(latent_inputs, decoder_outputs, name="decoder")\n    \n    return encoder, decoder\n\nlatent_dim = 2\nencoder, decoder = build_vae(latent_dim)\n\nprint("=== VAE Architecture ===")\nprint(f"Latent dim: {latent_dim}")\nprint(f"Encoder params: {encoder.count_params():,}")\nprint(f"Decoder params: {decoder.count_params():,}")\nprint(f"Total params: {encoder.count_params() + decoder.count_params():,}")\nprint(f"\\nEncoder: (28,28,1) → z_mean({latent_dim}), z_log_var({latent_dim})")\nprint(f"Decoder: z({latent_dim}) → (28,28,1)")' },
        { type: 'code', language: 'python', value: '# VAE Training Loop\nimport tensorflow as tf\nimport numpy as np\n\nclass VAE(tf.keras.Model):\n    def __init__(self, encoder, decoder, **kwargs):\n        super().__init__(**kwargs)\n        self.encoder = encoder\n        self.decoder = decoder\n    \n    def train_step(self, data):\n        with tf.GradientTape() as tape:\n            z_mean, z_log_var, z = self.encoder(data)\n            reconstruction = self.decoder(z)\n            \n            # Reconstruction loss\n            recon_loss = tf.reduce_mean(\n                tf.reduce_sum(\n                    tf.keras.losses.binary_crossentropy(data, reconstruction),\n                    axis=(1, 2)\n                )\n            )\n            \n            # KL divergence\n            kl_loss = -0.5 * tf.reduce_mean(\n                tf.reduce_sum(\n                    1 + z_log_var - tf.square(z_mean) - tf.exp(z_log_var),\n                    axis=1\n                )\n            )\n            \n            total_loss = recon_loss + kl_loss\n        \n        grads = tape.gradient(total_loss, self.trainable_weights)\n        self.optimizer.apply_gradients(zip(grads, self.trainable_weights))\n        return {"loss": total_loss, "recon_loss": recon_loss, "kl_loss": kl_loss}\n\nprint("=== VAE Loss ===")\nprint("Total Loss = Reconstruction Loss + KL Divergence")\nprint("  Reconstruction: как точно восстанавливаем входные данные")\nprint("  KL Divergence: насколько z_mean, z_log_var близки к N(0,1)")\nprint("\\nReparameterization trick:")\nprint("  z = z_mean + exp(0.5 * z_log_var) * epsilon")\nprint("  epsilon ~ N(0,1)")\nprint("  Позволяет backpropagation через семплирование")' },
        { type: 'tip', value: 'Latent dim=2 позволяет визуализировать латентное пространство. Для лучшего качества генерации используйте latent_dim=32-128. KL weight (beta-VAE): увеличьте для более гладкого пространства, уменьшите для более чётких изображений.' }
      ]
    },
    {
      id: 3,
      title: 'Практика: VAE на MNIST',
      type: 'practice',
      difficulty: 'medium',
      description: 'Постройте и обучите VAE для генерации рукописных цифр MNIST.',
      requirements: [
        'Загрузите MNIST и нормализуйте (0-1)',
        'Постройте encoder (Conv2D → Dense → z_mean, z_log_var) с latent_dim=2',
        'Постройте decoder (Dense → Conv2DTranspose → выход 28x28)',
        'Обучите VAE на 10 эпох с loss = reconstruction + KL divergence',
        'Сгенерируйте 5 изображений из случайных точек латентного пространства и выведите reconstruction loss'
      ],
      hint: 'Reparameterization trick: z = mean + exp(0.5*log_var) * epsilon, где epsilon ~ N(0,1). Используйте binary_crossentropy для reconstruction loss (изображения нормализованы 0-1). Для генерации: sample z ~ N(0,1), подайте в decoder.',
      expectedOutput: 'VAE на MNIST:\nEncoder params: XX,XXX\nDecoder params: XX,XXX\n\nОбучение (10 эпох):\n  Epoch 10: loss=XXX, recon=XXX, kl=XX\n\nГенерация 5 изображений:\n  z=[X.XX, X.XX] → pixel_mean=0.XX\n  ...',
      solution: 'import numpy as np\nimport tensorflow as tf\nfrom tensorflow.keras import layers\n\ntf.random.set_seed(42)\nnp.random.seed(42)\n\n(x_train, _), (x_test, _) = tf.keras.datasets.mnist.load_data()\nx_train = x_train.astype("float32") / 255.0\nx_train = x_train.reshape(-1, 28, 28, 1)\nx_test = x_test.astype("float32") / 255.0\nx_test = x_test.reshape(-1, 28, 28, 1)\n\nlatent_dim = 2\n\n# Encoder\nenc_input = layers.Input(shape=(28, 28, 1))\nx = layers.Conv2D(32, 3, strides=2, padding="same", activation="relu")(enc_input)\nx = layers.Conv2D(64, 3, strides=2, padding="same", activation="relu")(x)\nx = layers.Flatten()(x)\nx = layers.Dense(128, activation="relu")(x)\nz_mean = layers.Dense(latent_dim)(x)\nz_log_var = layers.Dense(latent_dim)(x)\n\ndef sampling(args):\n    mu, logvar = args\n    eps = tf.random.normal(shape=tf.shape(mu))\n    return mu + tf.exp(0.5 * logvar) * eps\n\nz = layers.Lambda(sampling)([z_mean, z_log_var])\nencoder = tf.keras.Model(enc_input, [z_mean, z_log_var, z])\n\n# Decoder\ndec_input = layers.Input(shape=(latent_dim,))\nx = layers.Dense(7*7*64, activation="relu")(dec_input)\nx = layers.Reshape((7, 7, 64))(x)\nx = layers.Conv2DTranspose(64, 3, strides=2, padding="same", activation="relu")(x)\nx = layers.Conv2DTranspose(32, 3, strides=2, padding="same", activation="relu")(x)\ndec_output = layers.Conv2DTranspose(1, 3, padding="same", activation="sigmoid")(x)\ndecoder = tf.keras.Model(dec_input, dec_output)\n\nprint("VAE на MNIST:")\nprint(f"Encoder params: {encoder.count_params():,}")\nprint(f"Decoder params: {decoder.count_params():,}")\n\n# Training\noptimizer = tf.keras.optimizers.Adam(1e-3)\n\nprint("\\nОбучение (10 эпох):")\nfor epoch in range(10):\n    total_loss, recon_l, kl_l, n = 0, 0, 0, 0\n    for i in range(0, len(x_train), 128):\n        batch = x_train[i:i+128]\n        with tf.GradientTape() as tape:\n            mu, logvar, z_s = encoder(batch, training=True)\n            recon = decoder(z_s, training=True)\n            r_loss = tf.reduce_sum(tf.keras.losses.binary_crossentropy(batch, recon), axis=(1,2))\n            k_loss = -0.5 * tf.reduce_sum(1 + logvar - tf.square(mu) - tf.exp(logvar), axis=1)\n            loss = tf.reduce_mean(r_loss + k_loss)\n        grads = tape.gradient(loss, encoder.trainable_weights + decoder.trainable_weights)\n        optimizer.apply_gradients(zip(grads, encoder.trainable_weights + decoder.trainable_weights))\n        total_loss += loss.numpy()\n        recon_l += tf.reduce_mean(r_loss).numpy()\n        kl_l += tf.reduce_mean(k_loss).numpy()\n        n += 1\n    if epoch % 3 == 0 or epoch == 9:\n        print(f"  Epoch {epoch+1}: loss={total_loss/n:.1f}, recon={recon_l/n:.1f}, kl={kl_l/n:.1f}")\n\n# Генерация\nprint("\\nГенерация 5 изображений:")\nfor i in range(5):\n    z_sample = np.random.randn(1, latent_dim).astype("float32")\n    generated = decoder.predict(z_sample, verbose=0)[0, :, :, 0]\n    print(f"  z=[{z_sample[0,0]:.2f}, {z_sample[0,1]:.2f}] → pixel_mean={generated.mean():.3f}, "\n          f"non_zero={np.sum(generated > 0.3)}")',
      explanation: 'VAE обучается реконструировать MNIST и одновременно организовывать латентное пространство. С latent_dim=2 можно визуализировать: каждая цифра занимает свою область. Для генерации: семплируем z ~ N(0,1) и пропускаем через decoder.'
    },
    {
      id: 4,
      title: 'GAN архитектура',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Generator vs Discriminator' },
        { type: 'text', value: 'GAN (Generative Adversarial Network) состоит из двух сетей, обучающихся в игровом процессе: Generator создаёт фейковые изображения из шума, Discriminator отличает реальные от фейковых. Generator учится обманывать Discriminator, а Discriminator — не быть обманутым. В равновесии Generator создаёт неотличимые от реальных изображения.' },
        { type: 'text', value: 'Adversarial loss: min_G max_D [E[log(D(x))] + E[log(1-D(G(z)))]]. Generator минимизирует, Discriminator максимизирует. На практике: тренируем D на реальных (label=1) и фейковых (label=0), затем тренируем G чтобы D предсказывал 1 для фейковых.' },
        { type: 'code', language: 'python', value: 'import tensorflow as tf\nfrom tensorflow.keras import layers\nimport numpy as np\n\ndef build_generator(latent_dim=100):\n    """Generator: noise → image (28x28x1)"""\n    model = tf.keras.Sequential([\n        layers.Dense(7 * 7 * 128, input_dim=latent_dim),\n        layers.Reshape((7, 7, 128)),\n        layers.BatchNormalization(),\n        \n        layers.Conv2DTranspose(128, 4, strides=2, padding="same"),\n        layers.BatchNormalization(),\n        layers.LeakyReLU(0.2),\n        \n        layers.Conv2DTranspose(64, 4, strides=2, padding="same"),\n        layers.BatchNormalization(),\n        layers.LeakyReLU(0.2),\n        \n        layers.Conv2D(1, 7, padding="same", activation="tanh"),  # [-1, 1]\n    ])\n    return model\n\ndef build_discriminator():\n    """Discriminator: image (28x28x1) → real/fake"""\n    model = tf.keras.Sequential([\n        layers.Conv2D(64, 4, strides=2, padding="same", input_shape=(28, 28, 1)),\n        layers.LeakyReLU(0.2),\n        layers.Dropout(0.3),\n        \n        layers.Conv2D(128, 4, strides=2, padding="same"),\n        layers.LeakyReLU(0.2),\n        layers.Dropout(0.3),\n        \n        layers.Flatten(),\n        layers.Dense(1, activation="sigmoid"),  # probability real\n    ])\n    return model\n\nlatent_dim = 100\ngenerator = build_generator(latent_dim)\ndiscriminator = build_discriminator()\n\nprint("=== GAN Architecture ===")\nprint(f"Generator: z({latent_dim}) → image(28,28,1)")\nprint(f"  Params: {generator.count_params():,}")\nprint(f"Discriminator: image(28,28,1) → prob(real)")\nprint(f"  Params: {discriminator.count_params():,}")\n\n# Тест\nz = np.random.randn(4, latent_dim).astype("float32")\nfake_images = generator(z)\nprint(f"\\nГенерация: {fake_images.shape}")\nprint(f"Pixel range: [{fake_images.numpy().min():.2f}, {fake_images.numpy().max():.2f}]")\n\nscores = discriminator(fake_images)\nprint(f"D(fake): {scores.numpy().flatten()}")' },
        { type: 'heading', value: 'Проблемы обучения GAN' },
        { type: 'code', language: 'python', value: '# Распространённые проблемы и решения GAN\nproblems = {\n    "Mode Collapse": {\n        "описание": "Generator генерирует одинаковые изображения",\n        "решения": [\n            "Mini-batch discrimination",\n            "Unrolled GAN",\n            "Wasserstein loss (WGAN)",\n        ]\n    },\n    "Training Instability": {\n        "описание": "Loss осциллирует, не сходится",\n        "решения": [\n            "Spectral normalization",\n            "Two-timescale update (разные LR)",\n            "Label smoothing (0.9 вместо 1.0)",\n        ]\n    },\n    "Vanishing Gradients": {\n        "описание": "D слишком хорош, G не обучается",\n        "решения": [\n            "Wasserstein distance",\n            "Feature matching",\n            "Обучайте G чаще чем D",\n        ]\n    },\n}\n\nprint("=== Проблемы обучения GAN ===")\nfor problem, info in problems.items():\n    print(f"\\n{problem}: {info[\'описание\']}")\n    for s in info["решения"]:\n        print(f"  → {s}")\n\nprint("\\n=== Лучшие практики ===")\nprint("  1. LeakyReLU в Discriminator (не ReLU)")\nprint("  2. BatchNorm в Generator (не в D)")\nprint("  3. Adam(lr=0.0002, beta1=0.5)")\nprint("  4. tanh активация в Generator (output [-1,1])")\nprint("  5. Label smoothing: real=0.9, fake=0.1")\nprint("  6. Тренировать D 1-2 раза на каждую итерацию G")' },
        { type: 'tip', value: 'Ключ к стабильному обучению GAN: LeakyReLU вместо ReLU, BatchNorm в Generator, Dropout в Discriminator, Adam(lr=0.0002, beta1=0.5), label smoothing. Если D loss → 0 — D победил, G не учится.' }
      ]
    },
    {
      id: 5,
      title: 'Практика: DCGAN',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте и обучите DCGAN для генерации цифр MNIST.',
      requirements: [
        'Постройте Generator: Dense → Reshape → Conv2DTranspose (output: 28x28, tanh)',
        'Постройте Discriminator: Conv2D → LeakyReLU → Dropout → Dense(1, sigmoid)',
        'Реализуйте GAN training loop: (1) train D на real+fake, (2) train G через frozen D',
        'Обучите на 10 эпох MNIST и выведите D loss и G loss',
        'Сгенерируйте 5 изображений и выведите статистики пикселей'
      ],
      hint: 'Training loop: (1) D получает real (label=0.9) и fake (label=0.1); (2) Для G: подаём noise → Generator → Discriminator, label=1 (хотим обмануть D). Нормализация MNIST: (x - 0.5) / 0.5 для range [-1,1].',
      expectedOutput: 'DCGAN на MNIST:\nGenerator: XX,XXX params\nDiscriminator: XX,XXX params\n\nОбучение (10 эпох):\n  Epoch 1: D_loss=X.XX, G_loss=X.XX\n  ...\n  Epoch 10: D_loss=X.XX, G_loss=X.XX\n\nГенерация:\n  img1: mean=X.XX, std=X.XX\n  ...',
      solution: 'import numpy as np\nimport tensorflow as tf\nfrom tensorflow.keras import layers\n\ntf.random.set_seed(42)\nnp.random.seed(42)\nlatent_dim = 100\n\n# Generator\ngenerator = tf.keras.Sequential([\n    layers.Dense(7*7*128, input_dim=latent_dim),\n    layers.Reshape((7, 7, 128)),\n    layers.BatchNormalization(),\n    layers.Conv2DTranspose(128, 4, strides=2, padding="same"),\n    layers.BatchNormalization(),\n    layers.LeakyReLU(0.2),\n    layers.Conv2DTranspose(64, 4, strides=2, padding="same"),\n    layers.BatchNormalization(),\n    layers.LeakyReLU(0.2),\n    layers.Conv2D(1, 7, padding="same", activation="tanh"),\n])\n\n# Discriminator\ndiscriminator = tf.keras.Sequential([\n    layers.Conv2D(64, 4, strides=2, padding="same", input_shape=(28, 28, 1)),\n    layers.LeakyReLU(0.2),\n    layers.Dropout(0.3),\n    layers.Conv2D(128, 4, strides=2, padding="same"),\n    layers.LeakyReLU(0.2),\n    layers.Dropout(0.3),\n    layers.Flatten(),\n    layers.Dense(1, activation="sigmoid"),\n])\n\nprint("DCGAN на MNIST:")\nprint(f"Generator: {generator.count_params():,} params")\nprint(f"Discriminator: {discriminator.count_params():,} params")\n\n# Data\n(x_train, _), _ = tf.keras.datasets.mnist.load_data()\nx_train = (x_train.astype("float32") - 127.5) / 127.5\nx_train = x_train.reshape(-1, 28, 28, 1)\n\nbce = tf.keras.losses.BinaryCrossentropy()\nd_opt = tf.keras.optimizers.Adam(2e-4, beta_1=0.5)\ng_opt = tf.keras.optimizers.Adam(2e-4, beta_1=0.5)\nbatch_size = 256\n\nprint("\\nОбучение (10 эпох):")\nfor epoch in range(10):\n    d_losses, g_losses = [], []\n    indices = np.random.permutation(len(x_train))\n    for i in range(0, min(5000, len(x_train)), batch_size):\n        real = x_train[indices[i:i+batch_size]]\n        bs = len(real)\n        noise = tf.random.normal([bs, latent_dim])\n\n        with tf.GradientTape() as tape:\n            fake = generator(noise, training=True)\n            real_out = discriminator(real, training=True)\n            fake_out = discriminator(fake, training=True)\n            d_loss = bce(tf.ones_like(real_out)*0.9, real_out) + bce(tf.zeros_like(fake_out)+0.1, fake_out)\n        d_grads = tape.gradient(d_loss, discriminator.trainable_weights)\n        d_opt.apply_gradients(zip(d_grads, discriminator.trainable_weights))\n\n        noise = tf.random.normal([bs, latent_dim])\n        with tf.GradientTape() as tape:\n            fake = generator(noise, training=True)\n            fake_out = discriminator(fake, training=True)\n            g_loss = bce(tf.ones_like(fake_out), fake_out)\n        g_grads = tape.gradient(g_loss, generator.trainable_weights)\n        g_opt.apply_gradients(zip(g_grads, generator.trainable_weights))\n\n        d_losses.append(d_loss.numpy())\n        g_losses.append(g_loss.numpy())\n\n    if epoch % 3 == 0 or epoch == 9:\n        print(f"  Epoch {epoch+1}: D_loss={np.mean(d_losses):.4f}, G_loss={np.mean(g_losses):.4f}")\n\nprint("\\nГенерация:")\nfor i in range(5):\n    z = tf.random.normal([1, latent_dim])\n    img = generator(z, training=False).numpy()[0, :, :, 0]\n    print(f"  img{i+1}: mean={img.mean():.3f}, std={img.std():.3f}, range=[{img.min():.2f}, {img.max():.2f}]")',
      explanation: 'DCGAN использует convolutional слои: Conv2DTranspose в Generator (upsampling), Conv2D в Discriminator (downsampling). Ключевые техники: BatchNorm в G, Dropout в D, LeakyReLU, label smoothing. После обучения Generator создаёт цифры из случайного шума.'
    },
    {
      id: 6,
      title: 'Conditional GAN',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Условная генерация по классу' },
        { type: 'text', value: 'Conditional GAN (cGAN) добавляет условие (class label) к генератору и дискриминатору. Это позволяет контролировать, ЧТО генерировать. Generator получает шум + метку класса, Discriminator — изображение + метку. Можно генерировать конкретную цифру: "сгенерируй цифру 7".' },
        { type: 'code', language: 'python', value: 'import tensorflow as tf\nfrom tensorflow.keras import layers\nimport numpy as np\n\ndef build_conditional_generator(latent_dim=100, num_classes=10):\n    # Вход: noise + class label\n    noise_input = layers.Input(shape=(latent_dim,))\n    label_input = layers.Input(shape=(1,), dtype="int32")\n    \n    # Embedding для класса\n    label_emb = layers.Embedding(num_classes, 50)(label_input)\n    label_emb = layers.Flatten()(label_emb)\n    \n    # Конкатенация noise + label\n    x = layers.Concatenate()([noise_input, label_emb])\n    x = layers.Dense(7 * 7 * 128, activation="relu")(x)\n    x = layers.Reshape((7, 7, 128))(x)\n    x = layers.BatchNormalization()(x)\n    x = layers.Conv2DTranspose(128, 4, strides=2, padding="same")(x)\n    x = layers.BatchNormalization()(x)\n    x = layers.LeakyReLU(0.2)(x)\n    x = layers.Conv2DTranspose(64, 4, strides=2, padding="same")(x)\n    x = layers.BatchNormalization()(x)\n    x = layers.LeakyReLU(0.2)(x)\n    output = layers.Conv2D(1, 7, padding="same", activation="tanh")(x)\n    \n    return tf.keras.Model([noise_input, label_input], output)\n\ndef build_conditional_discriminator(num_classes=10):\n    # Вход: image + class label\n    img_input = layers.Input(shape=(28, 28, 1))\n    label_input = layers.Input(shape=(1,), dtype="int32")\n    \n    # Label → image-sized map\n    label_emb = layers.Embedding(num_classes, 28*28)(label_input)\n    label_emb = layers.Reshape((28, 28, 1))(label_emb)\n    \n    # Конкатенация image + label channel\n    x = layers.Concatenate()([img_input, label_emb])\n    x = layers.Conv2D(64, 4, strides=2, padding="same")(x)\n    x = layers.LeakyReLU(0.2)(x)\n    x = layers.Dropout(0.3)(x)\n    x = layers.Conv2D(128, 4, strides=2, padding="same")(x)\n    x = layers.LeakyReLU(0.2)(x)\n    x = layers.Dropout(0.3)(x)\n    x = layers.Flatten()(x)\n    output = layers.Dense(1, activation="sigmoid")(x)\n    \n    return tf.keras.Model([img_input, label_input], output)\n\ncgen = build_conditional_generator()\ncdisc = build_conditional_discriminator()\n\nprint("=== Conditional GAN ===")\nprint(f"Generator params: {cgen.count_params():,}")\nprint(f"Discriminator params: {cdisc.count_params():,}")\n\n# Тест: генерация конкретных цифр\nnoise = np.random.randn(10, 100).astype("float32")\nlabels = np.arange(10).reshape(-1, 1)  # цифры 0-9\nfake = cgen.predict([noise, labels], verbose=0)\n\nprint("\\nГенерация цифр 0-9:")\nfor i in range(10):\n    img = fake[i, :, :, 0]\n    print(f"  Цифра {i}: shape={img.shape}, pixel_mean={img.mean():.3f}")' },
        { type: 'text', value: 'Conditional GAN расширяет возможности: (1) генерация по классу (цифра, категория); (2) text-to-image (текст → изображение); (3) image-to-image (эскиз → фото, pix2pix); (4) class-conditional super resolution.' },
        { type: 'tip', value: 'В cGAN и Generator, и Discriminator получают label. Без label в D модель может игнорировать условие. Embedding слой преобразует целочисленный label в плотный вектор, который конкатенируется с входом.' }
      ]
    },
    {
      id: 7,
      title: 'Практика: Conditional GAN',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте Conditional GAN для генерации конкретных цифр MNIST по заданному классу.',
      requirements: [
        'Постройте Conditional Generator: noise(100) + label(embed) → image(28x28)',
        'Постройте Conditional Discriminator: image + label → real/fake',
        'Реализуйте cGAN training loop с передачей labels в обе сети',
        'Обучите на 10 эпох MNIST',
        'Сгенерируйте по одному изображению для каждой цифры (0-9) и выведите статистики'
      ],
      hint: 'В training loop: real_labels = y_train batch; fake_labels = random или те же real_labels. Для генерации: noise + target_label → Generator → image. Label embedding: Embedding(10, 50) → Flatten → Concatenate с noise.',
      expectedOutput: 'Conditional GAN:\nGenerator: XX,XXX params\nDiscriminator: XX,XXX params\n\nОбучение:\n  Epoch 10: D_loss=X.XX, G_loss=X.XX\n\nУсловная генерация:\n  Цифра 0: mean=X.XX\n  Цифра 1: mean=X.XX\n  ...',
      solution: 'import numpy as np\nimport tensorflow as tf\nfrom tensorflow.keras import layers\n\ntf.random.set_seed(42)\nnp.random.seed(42)\nlatent_dim = 100\nnum_classes = 10\n\n# Conditional Generator\nni = layers.Input(shape=(latent_dim,))\nli = layers.Input(shape=(1,), dtype="int32")\nle = layers.Flatten()(layers.Embedding(num_classes, 50)(li))\nx = layers.Concatenate()([ni, le])\nx = layers.Dense(7*7*128, activation="relu")(x)\nx = layers.Reshape((7, 7, 128))(x)\nx = layers.BatchNormalization()(x)\nx = layers.Conv2DTranspose(128, 4, strides=2, padding="same")(x)\nx = layers.BatchNormalization()(x)\nx = layers.LeakyReLU(0.2)(x)\nx = layers.Conv2DTranspose(64, 4, strides=2, padding="same")(x)\nx = layers.BatchNormalization()(x)\nx = layers.LeakyReLU(0.2)(x)\ngo = layers.Conv2D(1, 7, padding="same", activation="tanh")(x)\ngen = tf.keras.Model([ni, li], go)\n\n# Conditional Discriminator\nii = layers.Input(shape=(28, 28, 1))\ndli = layers.Input(shape=(1,), dtype="int32")\ndle = layers.Reshape((28, 28, 1))(layers.Embedding(num_classes, 28*28)(dli))\nx = layers.Concatenate()([ii, dle])\nx = layers.Conv2D(64, 4, strides=2, padding="same")(x)\nx = layers.LeakyReLU(0.2)(x)\nx = layers.Dropout(0.3)(x)\nx = layers.Conv2D(128, 4, strides=2, padding="same")(x)\nx = layers.LeakyReLU(0.2)(x)\nx = layers.Flatten()(x)\ndo = layers.Dense(1, activation="sigmoid")(x)\ndisc = tf.keras.Model([ii, dli], do)\n\nprint("Conditional GAN:")\nprint(f"Generator: {gen.count_params():,} params")\nprint(f"Discriminator: {disc.count_params():,} params")\n\n(x_train, y_train), _ = tf.keras.datasets.mnist.load_data()\nx_train = (x_train.astype("float32") - 127.5) / 127.5\nx_train = x_train.reshape(-1, 28, 28, 1)\ny_train = y_train.reshape(-1, 1)\n\nbce = tf.keras.losses.BinaryCrossentropy()\nd_opt = tf.keras.optimizers.Adam(2e-4, beta_1=0.5)\ng_opt = tf.keras.optimizers.Adam(2e-4, beta_1=0.5)\nbs = 256\n\nprint("\\nОбучение:")\nfor epoch in range(10):\n    dl, gl = [], []\n    idx = np.random.permutation(len(x_train))[:5000]\n    for i in range(0, len(idx), bs):\n        bi = idx[i:i+bs]\n        real_img = x_train[bi]\n        real_lbl = y_train[bi]\n        n = len(bi)\n        noise = tf.random.normal([n, latent_dim])\n\n        with tf.GradientTape() as tape:\n            fake = gen([noise, real_lbl], training=True)\n            ro = disc([real_img, real_lbl], training=True)\n            fo = disc([fake, real_lbl], training=True)\n            d_loss = bce(tf.ones_like(ro)*0.9, ro) + bce(tf.zeros_like(fo)+0.1, fo)\n        d_opt.apply_gradients(zip(tape.gradient(d_loss, disc.trainable_weights), disc.trainable_weights))\n\n        noise = tf.random.normal([n, latent_dim])\n        with tf.GradientTape() as tape:\n            fake = gen([noise, real_lbl], training=True)\n            fo = disc([fake, real_lbl], training=True)\n            g_loss = bce(tf.ones_like(fo), fo)\n        g_opt.apply_gradients(zip(tape.gradient(g_loss, gen.trainable_weights), gen.trainable_weights))\n        dl.append(d_loss.numpy())\n        gl.append(g_loss.numpy())\n\n    if epoch % 3 == 0 or epoch == 9:\n        print(f"  Epoch {epoch+1}: D_loss={np.mean(dl):.4f}, G_loss={np.mean(gl):.4f}")\n\nprint("\\nУсловная генерация:")\nfor digit in range(10):\n    z = tf.random.normal([1, latent_dim])\n    lbl = np.array([[digit]])\n    img = gen.predict([z, lbl], verbose=0)[0, :, :, 0]\n    print(f"  Цифра {digit}: mean={img.mean():.3f}, std={img.std():.3f}")',
      explanation: 'Conditional GAN контролирует генерацию через class label. И Generator, и Discriminator получают label: G учится генерировать конкретные классы, D проверяет соответствие. Это основа для text-to-image (label = текст) и image-to-image (label = входное изображение).'
    },
    {
      id: 8,
      title: 'Stable Diffusion',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Diffusion Models и Stable Diffusion' },
        { type: 'text', value: 'Diffusion модели работают в два этапа: Forward process — постепенно добавляют гауссов шум к изображению за T шагов, пока не получится чистый шум. Reverse process — нейросеть (U-Net) учится удалять шум на каждом шаге, восстанавливая изображение. Генерация: начинаем с чистого шума и итеративно очищаем.' },
        { type: 'text', value: 'Stable Diffusion работает в латентном пространстве (не пиксельном): VAE encoder сжимает изображение 512x512 → 64x64 latent, diffusion работает в latent space, VAE decoder восстанавливает в пиксели. Это ускоряет генерацию в 10-50 раз.' },
        { type: 'code', language: 'python', value: '# Концептуальный код Diffusion Model\nimport numpy as np\n\nclass SimpleDiffusion:\n    """Упрощённая демонстрация diffusion process."""\n    \n    def __init__(self, n_steps=1000):\n        self.n_steps = n_steps\n        # Линейный noise schedule\n        self.betas = np.linspace(1e-4, 0.02, n_steps)\n        self.alphas = 1 - self.betas\n        self.alpha_bar = np.cumprod(self.alphas)  # кумулятивное произведение\n    \n    def forward_process(self, x0, t):\n        """Добавить шум на шаге t: q(x_t | x_0)"""\n        noise = np.random.randn(*x0.shape)\n        alpha_bar_t = self.alpha_bar[t]\n        x_t = np.sqrt(alpha_bar_t) * x0 + np.sqrt(1 - alpha_bar_t) * noise\n        return x_t, noise\n    \n    def reverse_step(self, x_t, predicted_noise, t):\n        """Один шаг reverse process: убрать шум."""\n        beta_t = self.betas[t]\n        alpha_t = self.alphas[t]\n        alpha_bar_t = self.alpha_bar[t]\n        \n        # Предсказанный x_{t-1}\n        x_prev = (1 / np.sqrt(alpha_t)) * (\n            x_t - (beta_t / np.sqrt(1 - alpha_bar_t)) * predicted_noise\n        )\n        \n        # Добавить немного шума (кроме последнего шага)\n        if t > 0:\n            noise = np.random.randn(*x_t.shape)\n            x_prev += np.sqrt(beta_t) * noise\n        \n        return x_prev\n\n# Демонстрация forward process\ndiffusion = SimpleDiffusion(n_steps=1000)\nx0 = np.random.randn(8, 8)  # "изображение" 8x8\n\nprint("=== Diffusion Forward Process ===")\nprint(f"Noise schedule: {diffusion.n_steps} steps")\nprint(f"Beta: [{diffusion.betas[0]:.4f}, ..., {diffusion.betas[-1]:.4f}]")\n\nfor t in [0, 100, 250, 500, 750, 999]:\n    x_t, _ = diffusion.forward_process(x0, t)\n    alpha_bar = diffusion.alpha_bar[t]\n    print(f"  t={t:4d}: alpha_bar={alpha_bar:.4f}, "\n          f"signal={np.sqrt(alpha_bar)*100:.1f}%, "\n          f"noise={np.sqrt(1-alpha_bar)*100:.1f}%, "\n          f"x_t_std={x_t.std():.3f}")' },
        { type: 'heading', value: 'Использование Stable Diffusion' },
        { type: 'code', language: 'python', value: '# Stable Diffusion через diffusers library\n# pip install diffusers transformers accelerate torch\nfrom diffusers import StableDiffusionPipeline\nimport torch\n\n# Загрузка модели\npipe = StableDiffusionPipeline.from_pretrained(\n    "runwayml/stable-diffusion-v1-5",\n    torch_dtype=torch.float16\n)\npipe = pipe.to("cuda")  # GPU\n\n# Генерация по тексту\nimage = pipe(\n    prompt="a beautiful sunset over mountains, oil painting style",\n    negative_prompt="blurry, low quality, distorted",\n    num_inference_steps=50,    # количество шагов denoising\n    guidance_scale=7.5,        # сила следования prompt (7-12 оптимально)\n    width=512,\n    height=512,\n    generator=torch.Generator("cuda").manual_seed(42)\n).images[0]\n\nimage.save("generated_sunset.png")\n\n# Параметры генерации\nprint("=== Stable Diffusion параметры ===")\nprint("num_inference_steps: 20-50 (больше = лучше, но медленнее)")\nprint("guidance_scale: 1-20 (больше = строже следует prompt)")\nprint("  1-3: творческая свобода, может игнорировать prompt")\nprint("  7-9: оптимальный баланс")\nprint("  12+: строгое следование, но может быть перенасыщенным")\nprint("negative_prompt: что НЕ должно быть на изображении")\nprint("seed: для воспроизводимости результатов")' },
        { type: 'tip', value: 'Для Stable Diffusion: GPU с 8+ GB VRAM. Используйте torch.float16 для экономии памяти. num_inference_steps=25 — хороший баланс скорости и качества. guidance_scale=7.5 — стандарт.' }
      ]
    },
    {
      id: 9,
      title: 'Практика: Генерация через Stable Diffusion API',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создайте pipeline генерации изображений с использованием Stable Diffusion: подготовка промптов, параметры генерации, пост-обработка.',
      requirements: [
        'Реализуйте SimpleDiffusion class с forward и reverse process',
        'Продемонстрируйте forward process: исходное изображение → чистый шум за T шагов',
        'Реализуйте PromptEngineer с шаблонами для разных стилей (oil painting, photo, anime)',
        'Создайте генерационный pipeline с параметрами (steps, guidance, seed)',
        'Покажите код для Stable Diffusion API (diffusers) и выведите 5 промптов с параметрами'
      ],
      hint: 'Forward process: x_t = sqrt(alpha_bar_t) * x_0 + sqrt(1-alpha_bar_t) * noise. PromptEngineer: template = "{subject}, {style}, {quality}". Для API: StableDiffusionPipeline.from_pretrained().',
      expectedOutput: 'Stable Diffusion Pipeline:\nForward process: 1000 steps\n  t=0: signal=100%, noise=0%\n  t=999: signal=0.3%, noise=99.7%\n\nПромпты:\n  1. "sunset over mountains, oil painting, masterpiece"\n  2. ...\n\nAPI код готов (diffusers library)',
      solution: 'import numpy as np\n\nclass SimpleDiffusion:\n    def __init__(self, n_steps=1000):\n        self.n_steps = n_steps\n        self.betas = np.linspace(1e-4, 0.02, n_steps)\n        self.alphas = 1 - self.betas\n        self.alpha_bar = np.cumprod(self.alphas)\n\n    def forward(self, x0, t):\n        noise = np.random.randn(*x0.shape)\n        ab = self.alpha_bar[t]\n        return np.sqrt(ab) * x0 + np.sqrt(1-ab) * noise, noise\n\n    def reverse_step(self, x_t, pred_noise, t):\n        b = self.betas[t]\n        a = self.alphas[t]\n        ab = self.alpha_bar[t]\n        x_prev = (1/np.sqrt(a)) * (x_t - (b/np.sqrt(1-ab)) * pred_noise)\n        if t > 0:\n            x_prev += np.sqrt(b) * np.random.randn(*x_t.shape)\n        return x_prev\n\nnp.random.seed(42)\ndiff = SimpleDiffusion(1000)\nx0 = np.random.randn(8, 8) * 0.5\n\nprint("Stable Diffusion Pipeline:")\nprint(f"Forward process: {diff.n_steps} steps")\nfor t in [0, 100, 250, 500, 750, 999]:\n    ab = diff.alpha_bar[t]\n    print(f"  t={t:4d}: signal={np.sqrt(ab)*100:.1f}%, noise={np.sqrt(1-ab)*100:.1f}%")\n\n# PromptEngineer\nclass PromptEngineer:\n    def __init__(self):\n        self.styles = {\n            "oil_painting": "oil painting, rich colors, textured brushstrokes",\n            "photo": "professional photograph, 8k, sharp focus, bokeh",\n            "anime": "anime style, vibrant colors, detailed, studio ghibli",\n            "watercolor": "watercolor painting, soft edges, dreamy atmosphere",\n            "3d_render": "3D render, octane render, volumetric lighting",\n        }\n        self.quality = "masterpiece, best quality, highly detailed"\n        self.negative = "blurry, low quality, distorted, deformed, ugly"\n\n    def generate_prompt(self, subject, style="photo"):\n        style_text = self.styles.get(style, style)\n        return {\n            "prompt": f"{subject}, {style_text}, {self.quality}",\n            "negative_prompt": self.negative,\n            "steps": 30 if style in ("photo", "3d_render") else 25,\n            "guidance": 8.0 if style == "photo" else 7.5,\n        }\n\npe = PromptEngineer()\nsubjects = [\n    ("sunset over mountains", "oil_painting"),\n    ("cat sitting on a windowsill", "photo"),\n    ("futuristic city at night", "anime"),\n    ("forest with a river", "watercolor"),\n    ("robot in a garden", "3d_render"),\n]\n\nprint("\\nПромпты:")\nfor i, (subj, style) in enumerate(subjects, 1):\n    p = pe.generate_prompt(subj, style)\n    print(f"  {i}. [{style}] \\"{p[\'prompt\'][:70]}...\\"\\n"\n          f"     steps={p[\'steps\']}, guidance={p[\'guidance\']}")\n\n# API код\napi_code = """\nfrom diffusers import StableDiffusionPipeline\nimport torch\n\npipe = StableDiffusionPipeline.from_pretrained(\n    "runwayml/stable-diffusion-v1-5", torch_dtype=torch.float16\n).to("cuda")\n\nfor subject, style in subjects:\n    p = prompt_engineer.generate_prompt(subject, style)\n    image = pipe(\n        prompt=p["prompt"],\n        negative_prompt=p["negative_prompt"],\n        num_inference_steps=p["steps"],\n        guidance_scale=p["guidance"],\n        generator=torch.Generator("cuda").manual_seed(42)\n    ).images[0]\n    image.save(f"{subject.replace(\' \', \'_\')}.png")\n"""\nprint(f"\\nAPI код готов ({len(api_code)} символов, diffusers library)")',
      explanation: 'Stable Diffusion pipeline: текстовый промпт → CLIP encoder → U-Net denoising в latent space → VAE decoder → изображение. PromptEngineer помогает составлять эффективные промпты с описанием стиля, качества и негативных атрибутов. guidance_scale контролирует силу следования промпту.'
    },
    {
      id: 10,
      title: 'Практика: Стилизация изображений',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте Neural Style Transfer: перенос стиля одного изображения на содержимое другого.',
      requirements: [
        'Объясните концепцию NST: content loss + style loss (Gram matrix)',
        'Реализуйте вычисление Gram matrix для стиля',
        'Реализуйте content_loss (MSE feature maps) и style_loss (MSE Gram matrices)',
        'Создайте NST pipeline с VGG19 feature extractor (симуляция)',
        'Продемонстрируйте оптимизацию изображения за 100 итераций и выведите loss'
      ],
      hint: 'Gram matrix: G = F^T * F, где F — feature map (reshape в 2D). Content loss: MSE между feature maps content и generated. Style loss: MSE между Gram matrices style и generated. Оптимизируем пиксели изображения, не веса сети.',
      expectedOutput: 'Neural Style Transfer:\nContent layers: [conv4_2]\nStyle layers: [conv1_1, conv2_1, conv3_1, conv4_1, conv5_1]\n\nОптимизация (100 итераций):\n  Iter 1: total=XXXX, content=XXX, style=XXXX\n  Iter 50: total=XXX, content=XXX, style=XXX\n  Iter 100: total=XXX, content=XXX, style=XXX\n\nStyle transfer завершён!',
      solution: 'import numpy as np\n\ndef gram_matrix(features):\n    """Gram matrix: корреляция между каналами.\n    features: (H*W, C) — развёрнутая feature map\n    """\n    G = features.T @ features\n    G /= features.shape[0]  # нормализация\n    return G\n\ndef content_loss(content_features, generated_features):\n    """MSE между feature maps."""\n    return np.mean((content_features - generated_features) ** 2)\n\ndef style_loss(style_gram, generated_features):\n    """MSE между Gram matrices."""\n    gen_gram = gram_matrix(generated_features)\n    return np.mean((style_gram - gen_gram) ** 2)\n\n# Симуляция VGG19 features\nnp.random.seed(42)\n\n# Слои VGG19 для NST\ncontent_layers = ["conv4_2"]\nstyle_layers = ["conv1_1", "conv2_1", "conv3_1", "conv4_1", "conv5_1"]\n\nprint("Neural Style Transfer:")\nprint(f"Content layers: {content_layers}")\nprint(f"Style layers: {style_layers}")\n\n# Симуляция feature maps\ndef simulate_features(image, layer_name):\n    sizes = {"conv1_1": (64, 64, 64), "conv2_1": (32, 32, 128),\n             "conv3_1": (16, 16, 256), "conv4_1": (8, 8, 512),\n             "conv4_2": (8, 8, 512), "conv5_1": (4, 4, 512)}\n    h, w, c = sizes[layer_name]\n    return np.random.randn(h*w, c) * 0.5 + image.mean()\n\n# "Изображения"\ncontent_img = np.random.randn(64, 64, 3) * 0.3\nstyle_img = np.random.randn(64, 64, 3) * 0.5 + 0.5\ngenerated = content_img.copy() + np.random.randn(64, 64, 3) * 0.01\n\n# Извлечение features\ncontent_feat = simulate_features(content_img, "conv4_2")\nstyle_grams = {}\nfor layer in style_layers:\n    feat = simulate_features(style_img, layer)\n    style_grams[layer] = gram_matrix(feat)\n\n# Оптимизация\nalpha = 1.0       # вес content\nbeta = 1000.0     # вес style\nlr = 0.02\n\nprint(f"\\nОптимизация (100 итераций):")\nprint(f"  alpha (content)={alpha}, beta (style)={beta}")\n\nfor iteration in range(100):\n    gen_content = simulate_features(generated, "conv4_2")\n    c_loss = content_loss(content_feat, gen_content)\n\n    s_loss = 0\n    for layer in style_layers:\n        gen_style = simulate_features(generated, layer)\n        s_loss += style_loss(style_grams[layer], gen_style)\n    s_loss /= len(style_layers)\n\n    total = alpha * c_loss + beta * s_loss\n\n    # Градиентный шаг (упрощённо)\n    grad = np.random.randn(*generated.shape) * lr * 0.1\n    generated -= grad * (total / 1000)\n    generated = np.clip(generated, -1, 1)\n\n    if iteration == 0 or iteration == 49 or iteration == 99:\n        print(f"  Iter {iteration+1:3d}: total={total:.2f}, "\n              f"content={c_loss:.2f}, style={s_loss:.4f}")\n\nprint(f"\\nStyle transfer завершён!")\nprint(f"  Output shape: {generated.shape}")\nprint(f"  Pixel range: [{generated.min():.2f}, {generated.max():.2f}]")\n\n# Реальный код NST\nnst_code = """\nimport torch\nimport torchvision.models as models\nimport torchvision.transforms as T\nfrom PIL import Image\n\nvgg = models.vgg19(pretrained=True).features.eval()\n\ndef get_features(image, model, layers):\n    features = {}\n    x = image\n    for name, layer in model._modules.items():\n        x = layer(x)\n        if name in layers:\n            features[name] = x\n    return features\n\n# content_features = get_features(content, vgg, content_layers)\n# style_features = get_features(style, vgg, style_layers)\n# generated = content.clone().requires_grad_(True)\n# optimizer = torch.optim.Adam([generated], lr=0.003)\n# for i in range(300):\n#     optimizer.zero_grad()\n#     loss = alpha*content_loss + beta*style_loss\n#     loss.backward()\n#     optimizer.step()\n"""\nprint(f"\\nPyTorch NST код: {len(nst_code)} символов")',
      explanation: 'Neural Style Transfer оптимизирует пиксели изображения, минимизируя content loss (сохранение структуры) и style loss (перенос текстур через Gram matrix). Gram matrix описывает корреляции между каналами — это "стиль". В production: быстрый NST с отдельной feed-forward сетью за 1 forward pass.'
    }
  ]
}
