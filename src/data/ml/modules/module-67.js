export default {
  id: 67,
  title: 'Мультимодальные модели (CLIP, LLaVA)',
  description: 'Мультимодальный AI: CLIP, BLIP, LLaVA, GPT-4V. Contrastive learning, zero-shot classification, VQA, multimodal search.',
  lessons: [
    {
      id: 1,
      title: 'Мультимодальность',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Мультимодальные модели: объединение текста и изображений' },
        { type: 'text', value: 'Мультимодальные модели (Multimodal Models) работают с несколькими типами данных одновременно: текст + изображения, текст + аудио, видео + текст. Ключевая идея — contrastive learning: обучение единого embedding-пространства, где похожие пары (текст, изображение) близки, а непохожие — далеки. Это позволяет искать изображения по тексту и наоборот.' },
        { type: 'code', language: 'python', value: 'import numpy as np\n\nprint("=== Мультимодальные модели ===")\nprint("""\nОсновные подходы:\n\n1. Contrastive Learning (CLIP, ALIGN)\n   - Два энкодера: image + text\n   - Обучение: max similarity(image, matching_text)\n   - min similarity(image, non_matching_text)\n   - Результат: общее embedding пространство\n\n2. Generative (BLIP, Flamingo)\n   - Image encoder + Language Model decoder\n   - Генерация описания изображения\n   - Ответы на вопросы по изображению\n\n3. Unified (GPT-4V, Gemini, LLaVA)\n   - Единая модель для понимания и генерации\n   - Image + text -> text (VQA, captioning)\n   - Instruction following с визуальным контекстом\n""")\n\n# Демо: Contrastive Learning\nprint("=== Contrastive Learning ===")\nnp.random.seed(42)\n\n# Симулируем image и text embeddings (в реальности — от CLIP)\ndim = 512\nn_pairs = 5\n\n# Пары (image, text) — должны быть близки\nimages = ["кот на диване", "собака в парке", "машина на дороге",\n          "закат над морем", "горы зимой"]\ntexts = ["cat sitting on sofa", "dog playing in park", "car driving on road",\n         "sunset over ocean", "mountains in winter"]\n\n# Симуляция: matching пары имеют высокую similarity\nimage_embs = np.random.randn(n_pairs, dim).astype(np.float32)\n# Text embeddings: matching = image + noise, non-matching = random\ntext_embs = image_embs + np.random.randn(n_pairs, dim) * 0.3\n\n# Нормализация\nimage_embs /= np.linalg.norm(image_embs, axis=1, keepdims=True)\ntext_embs /= np.linalg.norm(text_embs, axis=1, keepdims=True)\n\n# Cosine similarity matrix\nsimilarity = image_embs @ text_embs.T\n\nprint("Similarity Matrix (image x text):")\nprint("         ", "  ".join([f"t{i}" for i in range(n_pairs)]))\nfor i in range(n_pairs):\n    row = "  ".join([f"{similarity[i,j]:.2f}" for j in range(n_pairs)])\n    print(f"  img_{i}: {row}")\n\nprint("\\nДиагональ (matching пары) имеет наибольшие значения")\ndiag_mean = np.mean(np.diag(similarity))\noff_diag = similarity[~np.eye(n_pairs, dtype=bool)].mean()\nprint(f"  Matching: mean={diag_mean:.3f}")\nprint(f"  Non-matching: mean={off_diag:.3f}")\n\n# InfoNCE Loss\nprint("\\n=== InfoNCE / CLIP Loss ===")\ntemperature = 0.07\nlogits = similarity / temperature\nprint(f"temperature={temperature} (маленькая -> более резкие вероятности)")\nprint("Loss = CrossEntropy(logits, labels=[0,1,2,3,4])")\nprint("Обучаем оба энкодера чтобы matching пары были ближе")' },
        { type: 'list', items: [
          'Contrastive learning: единое пространство для текста и изображений',
          'InfoNCE loss: max similarity matching пар, min для non-matching',
          'Temperature: контролирует резкость распределения сходства',
          'Zero-shot: классификация без обучения на конкретных классах',
          'CLIP обучен на 400M пар (image, text) из интернета'
        ] }
      ]
    },
    {
      id: 2,
      title: 'CLIP: архитектура и обучение',
      type: 'theory',
      content: [
        { type: 'heading', value: 'CLIP — Contrastive Language-Image Pretraining' },
        { type: 'text', value: 'CLIP (OpenAI, 2021) — модель, обученная на 400 миллионах пар (image, text). Два энкодера: Vision Transformer (ViT) для изображений и Transformer для текста. Обучение через contrastive loss: максимизировать cosine similarity matching пар в батче. CLIP демонстрирует удивительный zero-shot transfer: классифицирует изображения по текстовым описаниям классов без дообучения.' },
        { type: 'code', language: 'python', value: 'import numpy as np\n\nprint("=== CLIP Architecture ===")\nprint("""\n┌────────────────┐        ┌────────────────┐\n│  Image Encoder  │        │  Text Encoder   │\n│  (ViT-L/14)    │        │  (Transformer)  │\n│                │        │                │\n│  Input: 224x224│        │  Input: 77 tok  │\n│  Patch: 14x14  │        │  Vocab: 49152   │\n│  Layers: 24    │        │  Layers: 12     │\n│  Heads: 16     │        │  Heads: 12      │\n│  Dim: 1024     │        │  Dim: 768       │\n│                │        │                │\n│  Output: 768D  │        │  Output: 768D   │\n│  embedding     │        │  embedding      │\n└───────┬────────┘        └───────┬────────┘\n        │                         │\n        └─────── cosine ──────────┘\n              similarity\n""")\n\n# Zero-shot Classification\nprint("=== Zero-shot Classification ===")\nprint("""\nПроцесс:\n1. Текстовые промпты для каждого класса:\n   "a photo of a cat", "a photo of a dog", "a photo of a car"\n\n2. Получаем text embeddings для всех классов\n\n3. Получаем image embedding для тестового изображения\n\n4. Cosine similarity между image и каждым text\n\n5. argmax -> предсказанный класс\n""")\n\n# Симуляция zero-shot classification\nnp.random.seed(42)\nclasses = ["cat", "dog", "car", "airplane", "flower"]\nn_classes = len(classes)\n\n# Симулируем embeddings\ndim = 512\ntext_embs = np.random.randn(n_classes, dim)\ntext_embs /= np.linalg.norm(text_embs, axis=1, keepdims=True)\n\n# Тестовое "изображение кота" — близко к text embedding "cat"\nimage_emb = text_embs[0] + np.random.randn(dim) * 0.2  # похоже на "cat"\nimage_emb /= np.linalg.norm(image_emb)\n\nsimilarities = image_emb @ text_embs.T\nprobs = np.exp(similarities * 100) / np.sum(np.exp(similarities * 100))\n\nprint("Тестовое изображение (кот):")\nfor cls, sim, prob in sorted(zip(classes, similarities, probs), key=lambda x: -x[2]):\n    bar = "█" * int(prob * 50)\n    print(f"  {cls:10s}: sim={sim:.4f}, prob={prob:.4f} {bar}")\n\n# Prompt engineering для CLIP\nprint("\\n=== Prompt Engineering ===")\nprint("Базовый: \'a photo of a {class}\'")\nprint("\\nУлучшенные шаблоны (ensembling):\")\ntemplates = [\n    \"a photo of a {}.\",\n    \"a blurry photo of a {}.\",\n    \"a photo of the large {}.\",\n    \"a photo of the small {}.\",\n    \"a photo of a {} in the wild.\",\n    \"an image of a {}.\",\n    \"a close-up photo of a {}.\",\n]\nfor t in templates:\n    print(f\"  \\\"{t.format(\'cat\')}\\\"\")\nprint(\"\\nEnsemble: усредняем embeddings всех шаблонов\")\nprint(\"Это улучшает accuracy на 3-5% на ImageNet\")' },
        { type: 'tip', value: 'CLIP zero-shot достигает 76.2% top-1 accuracy на ImageNet — сравнимо с supervised ResNet-50. Для своих задач попробуйте prompt ensembling: создайте 5-10 шаблонов и усредните embeddings. Это стабильно даёт +3-5% accuracy.' }
      ]
    },
    {
      id: 3,
      title: 'Практика: CLIP для классификации',
      type: 'practice',
      difficulty: 'medium',
      description: 'Используйте CLIP для zero-shot классификации и поиска похожих изображений по тексту.',
      requirements: [
        'Загрузите CLIP модель через HuggingFace transformers (openai/clip-vit-base-patch32)',
        'Реализуйте zero-shot классификацию: дайте 5 классов и классифицируйте 3+ изображений',
        'Реализуйте text-to-image similarity search: дайте текстовый запрос, найдите самое похожее изображение',
        'Используйте prompt ensembling (3+ шаблонов) и сравните accuracy с одним шаблоном',
        'Выведите similarity scores и predicted classes'
      ],
      hint: 'from transformers import CLIPProcessor, CLIPModel. processor(text=texts, images=image, return_tensors="pt"). model.get_image_features(), model.get_text_features() для embeddings.',
      expectedOutput: 'CLIP Zero-shot Classification:\n\nImage 1 (cat):\n  cat: 0.92, dog: 0.05, car: 0.01, ...\n  Predicted: cat ✓\n\nImage 2 (car):\n  car: 0.88, airplane: 0.06, ...\n  Predicted: car ✓\n\nPrompt Ensembling:\n  Single template: accuracy=X/N\n  Ensemble (5 templates): accuracy=X/N\n\nSimilarity Search:\n  Query: "sunset on beach"\n  Most similar: image_5 (score=0.XX)',
      solution: 'import torch\nfrom transformers import CLIPProcessor, CLIPModel\nfrom PIL import Image\nimport numpy as np\n\n# Загрузка модели\nmodel = CLIPModel.from_pretrained("openai/clip-vit-base-patch32")\nprocessor = CLIPProcessor.from_pretrained("openai/clip-vit-base-patch32")\nmodel.eval()\n\nprint("CLIP Zero-shot Classification:")\n\n# Создаём тестовые изображения (в реальности — настоящие фото)\nnp.random.seed(42)\ntest_images = []\nlabels = ["cat", "car", "flower"]\nfor label in labels:\n    img = Image.fromarray(np.random.randint(0, 255, (224, 224, 3), dtype=np.uint8))\n    test_images.append((img, label))\n\n# Классы для zero-shot\nclasses = ["cat", "dog", "car", "airplane", "flower"]\n\n# 1. Zero-shot с одним шаблоном\nsingle_template = "a photo of a {}"\nsingle_correct = 0\n\nfor img, true_label in test_images:\n    texts = [single_template.format(cls) for cls in classes]\n    inputs = processor(text=texts, images=img, return_tensors="pt", padding=True)\n    \n    with torch.no_grad():\n        outputs = model(**inputs)\n        logits = outputs.logits_per_image[0]\n        probs = logits.softmax(dim=0).numpy()\n    \n    pred_idx = probs.argmax()\n    pred_class = classes[pred_idx]\n    \n    print(f"\\nImage ({true_label}):")\n    for cls, prob in sorted(zip(classes, probs), key=lambda x: -x[1]):\n        mark = "✓" if cls == true_label else ""\n        print(f"  {cls:10s}: {prob:.4f} {mark}")\n    print(f"  Predicted: {pred_class}")\n    if pred_class == true_label:\n        single_correct += 1\n\n# 2. Prompt ensembling\ntemplates = [\n    "a photo of a {}.",\n    "a close-up photo of a {}.",\n    "a photo of a {} in nature.",\n    "an image of a {}.",\n    "a picture of the {}.",\n]\n\nensemble_correct = 0\nfor img, true_label in test_images:\n    # Усредняем embeddings всех шаблонов для каждого класса\n    class_embs = []\n    for cls in classes:\n        embs = []\n        for tmpl in templates:\n            text = tmpl.format(cls)\n            inputs = processor(text=[text], return_tensors="pt")\n            with torch.no_grad():\n                text_emb = model.get_text_features(**inputs)\n            embs.append(text_emb[0].numpy())\n        avg_emb = np.mean(embs, axis=0)\n        avg_emb /= np.linalg.norm(avg_emb)\n        class_embs.append(avg_emb)\n    class_embs = np.array(class_embs)\n    \n    img_inputs = processor(images=img, return_tensors="pt")\n    with torch.no_grad():\n        img_emb = model.get_image_features(**img_inputs)[0].numpy()\n    img_emb /= np.linalg.norm(img_emb)\n    \n    sims = img_emb @ class_embs.T\n    pred_class = classes[sims.argmax()]\n    if pred_class == true_label:\n        ensemble_correct += 1\n\nprint(f"\\nPrompt Ensembling:")\nprint(f"  Single template: accuracy={single_correct}/{len(test_images)}")\nprint(f"  Ensemble ({len(templates)} templates): accuracy={ensemble_correct}/{len(test_images)}")\n\n# 3. Similarity Search\nprint("\\nSimilarity Search:")\nquery = "a beautiful sunset on the beach"\nprint(f"  Query: \\"{query}\\"")\n\nimage_names = ["forest.jpg", "city.jpg", "beach_sunset.jpg", "mountain.jpg", "cat.jpg"]\nimage_embs = []\nfor name in image_names:\n    img = Image.fromarray(np.random.randint(0, 255, (224, 224, 3), dtype=np.uint8))\n    inputs = processor(images=img, return_tensors="pt")\n    with torch.no_grad():\n        emb = model.get_image_features(**inputs)[0].numpy()\n    emb /= np.linalg.norm(emb)\n    image_embs.append(emb)\n\ntext_inputs = processor(text=[query], return_tensors="pt")\nwith torch.no_grad():\n    query_emb = model.get_text_features(**text_inputs)[0].numpy()\nquery_emb /= np.linalg.norm(query_emb)\n\nsims = query_emb @ np.array(image_embs).T\nfor name, sim in sorted(zip(image_names, sims), key=lambda x: -x[1]):\n    print(f"  {name}: score={sim:.4f}")\nprint(f"  Most similar: {image_names[sims.argmax()]}")',
      explanation: 'CLIP zero-shot classification работает через сравнение image embedding с text embeddings классов. Prompt ensembling усредняет embeddings нескольких текстовых шаблонов, что делает classification более робастной. Similarity search использует тот же принцип для поиска изображений по текстовому запросу.'
    },
    {
      id: 4,
      title: 'Visual Question Answering',
      type: 'theory',
      content: [
        { type: 'heading', value: 'VQA и Image Captioning' },
        { type: 'text', value: 'Visual Question Answering (VQA) — задача ответа на вопросы по изображению. Image Captioning — генерация текстового описания изображения. Модели BLIP, BLIP-2, GIT объединяют vision encoder (ViT) с language model для генерации текста, обусловленного изображением.' },
        { type: 'code', language: 'python', value: 'import numpy as np\n\nprint("=== Visual Question Answering ===")\nprint("""\nЗадачи:\n\n1. Image Captioning:\n   Image -> "A cat sitting on a red sofa"\n\n2. Visual QA:\n   Image + "What color is the cat?" -> "The cat is orange"\n\n3. Visual Grounding:\n   Image + "the red car" -> bounding box [x1, y1, x2, y2]\n\n4. Image-Text Matching:\n   Image + Text -> match score (0-1)\n""")\n\n# BLIP Architecture\nprint("=== BLIP (Bootstrapping Language-Image Pre-training) ===")\nprint("""\n┌──────────────┐\n│  ViT Image   │\n│  Encoder     │──────┐\n└──────────────┘      │\n                      ├──> Image-Text Contrastive (ITC)\n┌──────────────┐      │\n│  Text        │──────┘\n│  Encoder     │\n└──────┬───────┘\n       │\n       ├──> Image-grounded Text Encoder (ITM)\n       │    Q: cross-attention image + text -> match/no-match\n       │\n       └──> Image-grounded Text Decoder (LM)\n            Генерация текста по изображению\n""")\n\n# BLIP-2\nprint("\\n=== BLIP-2: Q-Former ===")\nprint("""\nBLIP-2 добавляет Q-Former — лёгкий модуль между frozen image encoder\nи frozen LLM:\n\n  Frozen ViT ──> Q-Former (trainable) ──> Frozen LLM\n  (EVA-CLIP)    32 learnable queries     (OPT/FlanT5)\n                cross-attention\n\nQ-Former:\n- 32 learnable query tokens\n- Self-attention между queries\n- Cross-attention к image features\n- Результат: 32 visual tokens для LLM\n\nПреимущество:\n- Обучается только Q-Former (~100M params)\n- Image encoder (1B) и LLM (3-7B) заморожены\n- Эффективное обучение, хорошее качество\n""")\n\n# Сравнение моделей\nprint("=== Сравнение VQA моделей ===")\nmodels = [\n    ("BLIP", "ViT + Transformer", "2022", "Image captioning, VQA"),\n    ("BLIP-2", "ViT + Q-Former + LLM", "2023", "VQA, captioning, dialogue"),\n    ("GIT", "ViT + GPT-like decoder", "2022", "Captioning, VQA"),\n    ("LLaVA", "CLIP ViT + Llama", "2023", "Visual instruction following"),\n    ("GPT-4V", "Unified multimodal", "2023", "General vision-language"),\n    ("Gemini", "Unified multimodal", "2024", "General vision-language"),\n]\n\nfor name, arch, year, task in models:\n    print(f"  {name:8s} ({year}): {arch}")\n    print(f"           Задачи: {task}")' },
        { type: 'note', value: 'BLIP-2 показал, что не нужно обучать огромную модель с нуля. Лёгкий Q-Former (100M params) может эффективно соединить frozen image encoder с frozen LLM. Этот подход доминирует в multimodal research.' }
      ]
    },
    {
      id: 5,
      title: 'Практика: Image Captioning с BLIP',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте image captioning и VQA с помощью модели BLIP/BLIP-2.',
      requirements: [
        'Загрузите BLIP модель через HuggingFace (Salesforce/blip-image-captioning-base)',
        'Сгенерируйте подписи (captions) для 3+ изображений',
        'Реализуйте VQA: задайте 3+ вопроса к изображению и получите ответы',
        'Сравните conditional captioning (с подсказкой) и unconditional',
        'Выведите все результаты'
      ],
      hint: 'BlipProcessor и BlipForConditionalGeneration для captioning. BlipForQuestionAnswering для VQA. conditional_caption = processor(image, text="a photo of", return_tensors="pt").',
      expectedOutput: 'Image Captioning (BLIP):\n\nImage 1:\n  Unconditional: "a cat sitting on a couch"\n  Conditional ("a photo of"): "a photo of a cat on a red sofa"\n\nVisual QA:\n  Q: "What animal is in the image?" -> A: "cat"\n  Q: "What color is the sofa?" -> A: "red"\n  Q: "How many cats?" -> A: "one"',
      solution: 'import torch\nfrom transformers import BlipProcessor, BlipForConditionalGeneration, BlipForQuestionAnswering\nfrom PIL import Image\nimport numpy as np\n\n# === Image Captioning ===\nprint("Image Captioning (BLIP):")\n\n# Загрузка модели для captioning\nprocessor_cap = BlipProcessor.from_pretrained("Salesforce/blip-image-captioning-base")\nmodel_cap = BlipForConditionalGeneration.from_pretrained("Salesforce/blip-image-captioning-base")\nmodel_cap.eval()\n\n# Тестовые изображения\nnp.random.seed(42)\ntest_images = [\n    ("cat_sofa.jpg", Image.fromarray(np.random.randint(0, 255, (384, 384, 3), dtype=np.uint8))),\n    ("city_night.jpg", Image.fromarray(np.random.randint(0, 255, (384, 384, 3), dtype=np.uint8))),\n    ("food_plate.jpg", Image.fromarray(np.random.randint(0, 255, (384, 384, 3), dtype=np.uint8))),\n]\n\nfor name, img in test_images:\n    # Unconditional captioning\n    inputs = processor_cap(images=img, return_tensors="pt")\n    with torch.no_grad():\n        out = model_cap.generate(**inputs, max_new_tokens=50)\n    uncond_caption = processor_cap.decode(out[0], skip_special_tokens=True)\n    \n    # Conditional captioning\n    inputs_cond = processor_cap(images=img, text="a photograph of", return_tensors="pt")\n    with torch.no_grad():\n        out_cond = model_cap.generate(**inputs_cond, max_new_tokens=50)\n    cond_caption = processor_cap.decode(out_cond[0], skip_special_tokens=True)\n    \n    print(f"\\n{name}:")\n    print(f"  Unconditional: \\"{uncond_caption}\\"")\n    print(f"  Conditional: \\"{cond_caption}\\"")\n\n# === Visual Question Answering ===\nprint("\\n\\nVisual Question Answering:")\n\nprocessor_vqa = BlipProcessor.from_pretrained("Salesforce/blip-vqa-base")\nmodel_vqa = BlipForQuestionAnswering.from_pretrained("Salesforce/blip-vqa-base")\nmodel_vqa.eval()\n\n# VQA для первого изображения\nimg = test_images[0][1]\nquestions = [\n    "What animal is in the image?",\n    "What color is the background?",\n    "How many objects are visible?",\n    "Is this indoors or outdoors?",\n    "What is the main subject?",\n]\n\nfor q in questions:\n    inputs = processor_vqa(images=img, text=q, return_tensors="pt")\n    with torch.no_grad():\n        out = model_vqa.generate(**inputs, max_new_tokens=20)\n    answer = processor_vqa.decode(out[0], skip_special_tokens=True)\n    print(f"  Q: {q}")\n    print(f"  A: {answer}\\n")\n\n# === Batch processing ===\nprint("Batch Captioning:")\nimages_batch = [img for _, img in test_images]\ninputs_batch = processor_cap(images=images_batch, return_tensors="pt", padding=True)\nwith torch.no_grad():\n    outputs_batch = model_cap.generate(**inputs_batch, max_new_tokens=50)\nfor i, out in enumerate(outputs_batch):\n    caption = processor_cap.decode(out, skip_special_tokens=True)\n    print(f"  Image {i+1}: {caption}")',
      explanation: 'BLIP предоставляет готовые модели для captioning и VQA. Unconditional captioning генерирует описание с нуля, conditional — продолжает заданный текст. VQA модель обучена отвечать на вопросы по изображению. Batch processing позволяет обрабатывать несколько изображений одновременно для ускорения.'
    },
    {
      id: 6,
      title: 'LLaVA и GPT-4V',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Multimodal LLMs: LLaVA, GPT-4V, Gemini' },
        { type: 'text', value: 'LLaVA (Large Language and Vision Assistant) — open-source multimodal LLM, соединяющий CLIP visual encoder с Llama/Vicuna LLM через projection layer. GPT-4V и Gemini — закрытые multimodal модели, поддерживающие сложные visual reasoning задачи. Эти модели следуют инструкциям и могут анализировать изображения в контексте разговора.' },
        { type: 'code', language: 'python', value: 'import numpy as np\n\nprint("=== LLaVA Architecture ===")\nprint("""\n┌──────────────┐     ┌──────────────┐     ┌──────────────┐\n│  CLIP ViT    │     │  Linear      │     │   Llama/     │\n│  (Frozen)    │────>│  Projection  │────>│   Vicuna     │\n│              │     │  (Trainable) │     │   (LLM)      │\n│  Image ->    │     │  ViT dim ->  │     │              │\n│  patch embs  │     │  LLM dim     │     │  Visual      │\n└──────────────┘     └──────────────┘     │  tokens +    │\n                                          │  Text tokens │\n                                          │  -> Response │\n                                          └──────────────┘\n\nОбучение LLaVA (2 этапа):\n1. Pre-training: обучаем только projection layer\n   на 595K image-text пар (captioning)\n   \n2. Fine-tuning: обучаем projection + LLM\n   на 158K visual instruction данных\n""")\n\n# LLaVA использование\nprint("\\n=== Использование LLaVA ===")\nprint("""\nfrom transformers import LlavaForConditionalGeneration, AutoProcessor\n\nmodel = LlavaForConditionalGeneration.from_pretrained(\n    "llava-hf/llava-1.5-7b-hf",\n    torch_dtype=torch.float16,\n    device_map="auto"\n)\nprocessor = AutoProcessor.from_pretrained("llava-hf/llava-1.5-7b-hf")\n\n# Разговор с изображением\nprompt = "<image>\\nОпиши что ты видишь на изображении детально."\ninputs = processor(text=prompt, images=image, return_tensors="pt").to("cuda")\noutput = model.generate(**inputs, max_new_tokens=200)\nresponse = processor.decode(output[0], skip_special_tokens=True)\n""")\n\n# GPT-4V через API\nprint("\\n=== GPT-4V (OpenAI API) ===")\nprint("""\nimport openai\nimport base64\n\nclient = openai.OpenAI()\n\n# Отправляем изображение\nwith open("image.jpg", "rb") as f:\n    image_data = base64.b64encode(f.read()).decode()\n\nresponse = client.chat.completions.create(\n    model="gpt-4o",\n    messages=[\n        {"role": "user", "content": [\n            {"type": "text", "text": "Что изображено? Опиши детально."},\n            {"type": "image_url", "image_url": {\n                "url": f"data:image/jpeg;base64,{image_data}"\n            }}\n        ]}\n    ],\n    max_tokens=500\n)\nprint(response.choices[0].message.content)\n""")\n\n# Сравнение\nprint("\\n=== Сравнение Multimodal LLMs ===")\nmodels_cmp = [\n    {"name": "LLaVA 1.5 7B", "type": "Open-source", "params": "7B",\n     "pros": "Бесплатный, локальный, хороший для задач", "cons": "Уступает GPT-4V"},\n    {"name": "LLaVA 1.5 13B", "type": "Open-source", "params": "13B",\n     "pros": "Лучше 7B, всё ещё локальный", "cons": "Нужно больше VRAM"},\n    {"name": "GPT-4V/4o", "type": "Closed API", "params": "~1.7T",\n     "pros": "Лучшее качество, complex reasoning", "cons": "Платный API"},\n    {"name": "Gemini Pro", "type": "Closed API", "params": "unknown",\n     "pros": "Long context, multimodal native", "cons": "Платный API"},\n    {"name": "Claude 3", "type": "Closed API", "params": "unknown",\n     "pros": "Хорошее понимание, safety", "cons": "Платный API"},\n]\n\nfor m in models_cmp:\n    print(f"\\n{m[\'name\']} ({m[\'type\']}, {m[\'params\']}):")\n    print(f"  + {m[\'pros\']}")\n    print(f"  - {m[\'cons\']}")' },
        { type: 'tip', value: 'Для production задач: GPT-4o/Claude 3 через API — лучшее качество. Для research/privacy: LLaVA 1.5 13B — лучший open-source вариант. Для edge/mobile: MobileVLM, TinyLLaVA. Multimodal LLM — одна из самых быстроразвивающихся областей AI.' }
      ]
    },
    {
      id: 7,
      title: 'Практика: Multimodal search engine',
      type: 'practice',
      difficulty: 'hard',
      description: 'Постройте multimodal search engine с CLIP embeddings и FAISS для быстрого поиска изображений по тексту.',
      requirements: [
        'Загрузите CLIP модель для создания embeddings',
        'Создайте FAISS индекс из image embeddings (10+ изображений)',
        'Реализуйте text-to-image search: текстовый запрос -> top-K похожих изображений',
        'Реализуйте image-to-image search: изображение -> top-K похожих',
        'Измерьте latency поиска и выведите результаты'
      ],
      hint: 'CLIP image/text embeddings нормализуйте (L2). FAISS IndexFlatIP для cosine similarity. model.get_image_features() и model.get_text_features() для embeddings.',
      expectedOutput: 'Multimodal Search Engine:\n  Images indexed: 100\n  Embedding dim: 512\n  FAISS index: IndexFlatIP\n\nText-to-Image Search:\n  Query: "a cat playing"\n  Results: [img_12 (0.32), img_45 (0.28), img_3 (0.25)]\n  Latency: 0.5ms\n\nImage-to-Image Search:\n  Query: image_1\n  Results: [img_7 (0.85), img_23 (0.72), ...]\n  Latency: 0.3ms',
      solution: 'import torch\nimport numpy as np\nimport faiss\nimport time\nfrom transformers import CLIPProcessor, CLIPModel\nfrom PIL import Image\n\n# Загрузка CLIP\nmodel = CLIPModel.from_pretrained("openai/clip-vit-base-patch32")\nprocessor = CLIPProcessor.from_pretrained("openai/clip-vit-base-patch32")\nmodel.eval()\n\nprint("Multimodal Search Engine:")\n\n# Генерация тестовых изображений\nnp.random.seed(42)\nn_images = 100\nimage_names = [f"img_{i}" for i in range(n_images)]\nimages = [Image.fromarray(np.random.randint(0, 255, (224, 224, 3), dtype=np.uint8))\n          for _ in range(n_images)]\n\n# Создаём embeddings для всех изображений\nimage_embeddings = []\nbatch_size = 16\nfor i in range(0, n_images, batch_size):\n    batch = images[i:i+batch_size]\n    inputs = processor(images=batch, return_tensors="pt", padding=True)\n    with torch.no_grad():\n        embs = model.get_image_features(**inputs)\n    embs = embs / embs.norm(dim=-1, keepdim=True)  # L2 normalize\n    image_embeddings.append(embs.numpy())\n\nimage_embeddings = np.concatenate(image_embeddings, axis=0).astype("float32")\nemb_dim = image_embeddings.shape[1]\n\nprint(f"  Images indexed: {n_images}")\nprint(f"  Embedding dim: {emb_dim}")\n\n# Создаём FAISS индекс\nindex = faiss.IndexFlatIP(emb_dim)  # Inner Product = cosine для normalized vectors\nindex.add(image_embeddings)\nprint(f"  FAISS index: IndexFlatIP ({index.ntotal} vectors)")\n\n# === Text-to-Image Search ===\ndef text_search(query, top_k=5):\n    inputs = processor(text=[query], return_tensors="pt", padding=True)\n    with torch.no_grad():\n        text_emb = model.get_text_features(**inputs)\n    text_emb = text_emb / text_emb.norm(dim=-1, keepdim=True)\n    text_emb = text_emb.numpy().astype("float32")\n    \n    start = time.time()\n    scores, indices = index.search(text_emb, top_k)\n    latency = (time.time() - start) * 1000\n    \n    results = [(image_names[idx], score) for idx, score in zip(indices[0], scores[0])]\n    return results, latency\n\nprint("\\nText-to-Image Search:")\ntext_queries = [\n    "a cat playing with a toy",\n    "beautiful sunset over the ocean",\n    "a person riding a bicycle",\n    "delicious food on a plate",\n]\n\nfor query in text_queries:\n    results, latency = text_search(query, top_k=3)\n    result_str = ", ".join([f"{name} ({score:.3f})" for name, score in results])\n    print(f"  Query: \\"{query}\\"")\n    print(f"  Results: [{result_str}]")\n    print(f"  Latency: {latency:.2f}ms\\n")\n\n# === Image-to-Image Search ===\ndef image_search(query_idx, top_k=5):\n    query_emb = image_embeddings[query_idx:query_idx+1]\n    \n    start = time.time()\n    scores, indices = index.search(query_emb, top_k + 1)  # +1 чтобы исключить себя\n    latency = (time.time() - start) * 1000\n    \n    results = [(image_names[idx], score) for idx, score in zip(indices[0], scores[0])\n               if idx != query_idx][:top_k]\n    return results, latency\n\nprint("Image-to-Image Search:")\nfor query_idx in [0, 25, 50]:\n    results, latency = image_search(query_idx, top_k=3)\n    result_str = ", ".join([f"{name} ({score:.3f})" for name, score in results])\n    print(f"  Query: {image_names[query_idx]}")\n    print(f"  Results: [{result_str}]")\n    print(f"  Latency: {latency:.2f}ms\\n")\n\n# Benchmark\nprint("Benchmark:")\nlatencies = []\nfor _ in range(100):\n    q = np.random.randn(1, emb_dim).astype("float32")\n    faiss.normalize_L2(q)\n    start = time.time()\n    index.search(q, 5)\n    latencies.append((time.time() - start) * 1000)\nprint(f"  Avg latency ({n_images} images): {np.mean(latencies):.3f}ms")\nprint(f"  P99 latency: {np.percentile(latencies, 99):.3f}ms")',
      explanation: 'Multimodal search engine использует CLIP для создания embeddings и FAISS для быстрого поиска ближайших соседей. CLIP помещает тексты и изображения в одно пространство, поэтому text-to-image и image-to-image поиск работают одинаково: через cosine similarity. IndexFlatIP — точный поиск, для миллионов изображений используйте IndexIVFFlat или IndexHNSW.'
    },
    {
      id: 8,
      title: 'Практика: VQA бот',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создайте VQA бота, который отвечает на вопросы по изображениям, комбинируя BLIP для VQA и CLIP для понимания контекста.',
      requirements: [
        'Загрузите BLIP VQA и CLIP модели',
        'Реализуйте класс VQABot с методами: describe(image), answer(image, question), classify(image, classes)',
        'describe() — генерирует caption через BLIP',
        'answer() — отвечает на вопрос через BLIP VQA',
        'classify() — zero-shot классификация через CLIP',
        'Протестируйте бота на 3+ изображениях с разными вопросами'
      ],
      hint: 'Комбинируйте BLIP captioning, BLIP VQA и CLIP classification в одном классе. Каждый метод использует соответствующую модель.',
      expectedOutput: 'VQA Bot:\n\nImage: test_1.jpg\n  Description: "a dog playing in the park"\n  Q: "What animal?" -> "dog"\n  Q: "Where?" -> "park"\n  Classification: dog (0.85), cat (0.08), ...\n\nImage: test_2.jpg\n  Description: "a red car on a highway"\n  ...',
      solution: 'import torch\nimport numpy as np\nfrom transformers import (\n    BlipProcessor, BlipForConditionalGeneration, BlipForQuestionAnswering,\n    CLIPProcessor, CLIPModel\n)\nfrom PIL import Image\n\nclass VQABot:\n    def __init__(self):\n        print("Загрузка моделей...")\n        # BLIP Captioning\n        self.cap_processor = BlipProcessor.from_pretrained("Salesforce/blip-image-captioning-base")\n        self.cap_model = BlipForConditionalGeneration.from_pretrained("Salesforce/blip-image-captioning-base")\n        self.cap_model.eval()\n        \n        # BLIP VQA\n        self.vqa_processor = BlipProcessor.from_pretrained("Salesforce/blip-vqa-base")\n        self.vqa_model = BlipForQuestionAnswering.from_pretrained("Salesforce/blip-vqa-base")\n        self.vqa_model.eval()\n        \n        # CLIP\n        self.clip_processor = CLIPProcessor.from_pretrained("openai/clip-vit-base-patch32")\n        self.clip_model = CLIPModel.from_pretrained("openai/clip-vit-base-patch32")\n        self.clip_model.eval()\n        \n        print("Модели загружены!\\n")\n    \n    def describe(self, image, prompt=None):\n        """Генерирует описание изображения"""\n        if prompt:\n            inputs = self.cap_processor(images=image, text=prompt, return_tensors="pt")\n        else:\n            inputs = self.cap_processor(images=image, return_tensors="pt")\n        with torch.no_grad():\n            out = self.cap_model.generate(**inputs, max_new_tokens=60)\n        return self.cap_processor.decode(out[0], skip_special_tokens=True)\n    \n    def answer(self, image, question):\n        """Отвечает на вопрос по изображению"""\n        inputs = self.vqa_processor(images=image, text=question, return_tensors="pt")\n        with torch.no_grad():\n            out = self.vqa_model.generate(**inputs, max_new_tokens=30)\n        return self.vqa_processor.decode(out[0], skip_special_tokens=True)\n    \n    def classify(self, image, classes, top_k=3):\n        """Zero-shot классификация через CLIP"""\n        texts = [f"a photo of a {cls}" for cls in classes]\n        inputs = self.clip_processor(text=texts, images=image, return_tensors="pt", padding=True)\n        with torch.no_grad():\n            outputs = self.clip_model(**inputs)\n            probs = outputs.logits_per_image[0].softmax(dim=0).numpy()\n        \n        ranked = sorted(zip(classes, probs), key=lambda x: -x[1])[:top_k]\n        return ranked\n    \n    def analyze(self, image, questions, classes):\n        """Полный анализ изображения"""\n        result = {\n            "description": self.describe(image),\n            "answers": {},\n            "classification": self.classify(image, classes)\n        }\n        for q in questions:\n            result["answers"][q] = self.answer(image, q)\n        return result\n\n# === Тестирование ===\nbot = VQABot()\nprint("VQA Bot:\\n")\n\n# Тестовые данные\nnp.random.seed(42)\ntest_cases = [\n    {\n        "name": "test_1.jpg",\n        "image": Image.fromarray(np.random.randint(0, 255, (384, 384, 3), dtype=np.uint8)),\n        "questions": ["What animal is this?", "Where is it?", "What color?"],\n        "classes": ["dog", "cat", "bird", "car", "flower"]\n    },\n    {\n        "name": "test_2.jpg",\n        "image": Image.fromarray(np.random.randint(0, 255, (384, 384, 3), dtype=np.uint8)),\n        "questions": ["What vehicle?", "What time of day?", "Is it moving?"],\n        "classes": ["car", "bus", "bicycle", "airplane", "train"]\n    },\n    {\n        "name": "test_3.jpg",\n        "image": Image.fromarray(np.random.randint(0, 255, (384, 384, 3), dtype=np.uint8)),\n        "questions": ["What food is this?", "Is it healthy?", "What cuisine?"],\n        "classes": ["pizza", "sushi", "salad", "burger", "pasta"]\n    },\n]\n\nfor case in test_cases:\n    print(f"Image: {case[\'name\']}")\n    result = bot.analyze(case["image"], case["questions"], case["classes"])\n    \n    print(f"  Description: \\"{result[\'description\']}\\"")\n    for q, a in result["answers"].items():\n        print(f"  Q: \\"{q}\\" -> \\"{a}\\"")\n    cls_str = ", ".join([f"{c} ({p:.2f})" for c, p in result["classification"]])\n    print(f"  Classification: {cls_str}\\n")\n\nprint("Бот объединяет 3 модели для полного visual understanding!")',
      explanation: 'VQA бот комбинирует три модели: BLIP captioning для описания, BLIP VQA для ответов на вопросы, CLIP для zero-shot классификации. Такая архитектура позволяет решать разные задачи одним ботом. В production можно заменить все три на единый multimodal LLM (LLaVA, GPT-4V), который справится со всеми задачами через разные промпты.'
    }
  ]
}
