export default {
  id: 43,
  title: 'Работа с большими данными (PySpark ML)',
  description: 'PySpark основы, Spark MLlib, распределённое обучение, feature engineering at scale и model serving.',
  lessons: [
    {
      id: 1,
      title: 'PySpark основы',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Apache Spark для больших данных' },
        { type: 'text', value: 'Apache Spark — распределённый фреймворк для обработки больших данных. PySpark — Python API для Spark. В отличие от pandas (одна машина), Spark работает на кластере из сотен машин, обрабатывая петабайты данных. Spark MLlib — встроенная библиотека ML.' },
        { type: 'code', language: 'python', value: 'from pyspark.sql import SparkSession\nfrom pyspark.sql import functions as F\nfrom pyspark.sql.types import StructType, StructField, FloatType, IntegerType, StringType\n\n# Создание Spark сессии\nspark = SparkSession.builder \\\n    .appName("ML_Pipeline") \\\n    .master("local[*]") \\\n    .config("spark.driver.memory", "4g") \\\n    .getOrCreate()\n\nprint(f"Spark version: {spark.version}")\n\n# Создание DataFrame\nimport numpy as np\nnp.random.seed(42)\nn = 10000\n\ndata = []\nfor i in range(n):\n    data.append((\n        float(np.random.uniform(20000, 150000)),  # income\n        int(np.random.randint(18, 70)),            # age\n        float(np.random.uniform(0, 1)),            # debt_ratio\n        int(np.random.randint(300, 850)),           # credit_score\n        np.random.choice(["A", "B", "C", "D"]),    # category\n        int(np.random.randint(0, 2))               # target\n    ))\n\nschema = StructType([\n    StructField("income", FloatType()),\n    StructField("age", IntegerType()),\n    StructField("debt_ratio", FloatType()),\n    StructField("credit_score", IntegerType()),\n    StructField("category", StringType()),\n    StructField("target", IntegerType())\n])\n\ndf = spark.createDataFrame(data, schema)\n\nprint(f"\\nDataFrame: {df.count()} строк, {len(df.columns)} столбцов\")\ndf.printSchema()\ndf.show(5)\n\n# Основные операции\nprint(\"\\nСтатистика:\")\ndf.describe().show()\n\n# Группировка\nprint(\"По категориям:\")\ndf.groupBy(\"category\").agg(\n    F.count(\"*\").alias(\"count\"),\n    F.avg(\"income\").alias(\"avg_income\"),\n    F.avg(\"target\").alias(\"target_rate\")\n).show()\n\n# SQL запросы\ndf.createOrReplaceTempView(\"clients\")\nresult = spark.sql(\"\"\"\n    SELECT category, \n           COUNT(*) as cnt,\n           AVG(income) as avg_income\n    FROM clients \n    WHERE credit_score > 600 \n    GROUP BY category\n\"\"\")\nresult.show()\n\nspark.stop()' },
        { type: 'note', value: 'Spark использует lazy evaluation: операции не выполняются сразу, а формируют DAG (Directed Acyclic Graph). Выполнение начинается при action (show, count, collect). Это позволяет оптимизировать план выполнения.' }
      ]
    },
    {
      id: 2,
      title: 'Spark MLlib',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Машинное обучение в Spark' },
        { type: 'text', value: 'Spark MLlib предоставляет ML-алгоритмы, работающие на распределённых данных: classification, regression, clustering, recommendation. Pipeline API похож на sklearn, но масштабируется на терабайты. Все преобразования работают с колонкой features (Vector type).' },
        { type: 'code', language: 'python', value: 'from pyspark.sql import SparkSession\nfrom pyspark.ml.feature import (\n    VectorAssembler, StringIndexer, OneHotEncoder,\n    StandardScaler, MinMaxScaler\n)\nfrom pyspark.ml.classification import (\n    LogisticRegression, RandomForestClassifier,\n    GBTClassifier\n)\nfrom pyspark.ml import Pipeline\nfrom pyspark.ml.evaluation import BinaryClassificationEvaluator\n\nspark = SparkSession.builder.appName("MLlib").master("local[*]").getOrCreate()\n\n# Создаём данные\nimport numpy as np\nnp.random.seed(42)\ndata = [(float(np.random.randn()), float(np.random.randn()),\n         float(np.random.randn()), np.random.choice(["A","B","C"]),\n         int(np.random.randint(0,2))) for _ in range(5000)]\n\ndf = spark.createDataFrame(data, ["f1", "f2", "f3", "category", "label"])\ntrain, test = df.randomSplit([0.8, 0.2], seed=42)\n\nprint(f"Train: {train.count()}, Test: {test.count()}")\n\n# Pipeline\n# 1. Encode категориальные\nindexer = StringIndexer(inputCol="category", outputCol="category_idx")\nencoder = OneHotEncoder(inputCols=["category_idx"], outputCols=["category_vec"])\n\n# 2. Собираем все признаки в вектор\nassembler = VectorAssembler(\n    inputCols=["f1", "f2", "f3", "category_vec"],\n    outputCol="features_raw"\n)\n\n# 3. Масштабирование\nscaler = StandardScaler(inputCol="features_raw", outputCol="features\")\n\n# 4. Модели\nmodels = {\n    \"LogisticRegression\": LogisticRegression(\n        featuresCol=\"features\", labelCol=\"label\", maxIter=100\n    ),\n    \"RandomForest\": RandomForestClassifier(\n        featuresCol=\"features\", labelCol=\"label\", numTrees=100\n    ),\n    \"GBT\": GBTClassifier(\n        featuresCol=\"features\", labelCol=\"label\", maxIter=50\n    )\n}\n\nevaluator = BinaryClassificationEvaluator(\n    labelCol=\"label\", metricName=\"areaUnderROC\"\n)\n\nprint(\"\\nСравнение моделей:\")\nfor name, classifier in models.items():\n    pipeline = Pipeline(stages=[indexer, encoder, assembler, scaler, classifier])\n    model = pipeline.fit(train)\n    predictions = model.transform(test)\n    auc = evaluator.evaluate(predictions)\n    print(f\"  {name:25s}: AUC = {auc:.4f}\")\n\nspark.stop()' },
        { type: 'tip', value: 'VectorAssembler — обязательный шаг в Spark ML. Он собирает все признаки в единый вектор. В отличие от sklearn, где модели принимают DataFrame/numpy, Spark ML работает с колонкой features типа Vector.' }
      ]
    },
    {
      id: 3,
      title: 'Распределённое обучение',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Масштабирование обучения' },
        { type: 'text', value: 'Для больших данных и моделей используют распределённое обучение: Data Parallelism (данные распределяются между GPU), Model Parallelism (модель распределяется), Pipeline Parallelism. Horovod, PyTorch DistributedDataParallel, DeepSpeed — основные инструменты.' },
        { type: 'code', language: 'python', value: 'import numpy as np\n\n# Симуляция Data Parallelism\nclass DataParallelTrainer:\n    def __init__(self, n_workers=4, n_features=5):\n        self.n_workers = n_workers\n        self.global_weights = np.zeros(n_features)\n        self.lr = 0.01\n    \n    def _worker_train(self, X_batch, y_batch, weights):\n        \"\"\"Обучение на одном worker (GPU)\"\"\"\n        pred = 1 / (1 + np.exp(-X_batch @ weights))\n        gradient = X_batch.T @ (pred - y_batch) / len(y_batch)\n        return gradient\n    \n    def train_step(self, X, y):\n        \"\"\"Один шаг Data Parallel обучения\"\"\"\n        # Split данных между workers\n        batch_size = len(X) // self.n_workers\n        gradients = []\n        \n        for i in range(self.n_workers):\n            start = i * batch_size\n            end = start + batch_size\n            grad = self._worker_train(\n                X[start:end], y[start:end], self.global_weights\n            )\n            gradients.append(grad)\n        \n        # AllReduce: усреднение градиентов\n        avg_gradient = np.mean(gradients, axis=0)\n        \n        # Обновление весов\n        self.global_weights -= self.lr * avg_gradient\n    \n    def fit(self, X, y, epochs=50):\n        for epoch in range(epochs):\n            indices = np.random.permutation(len(X))\n            self.train_step(X[indices], y[indices])\n        return self\n    \n    def predict(self, X):\n        return (X @ self.global_weights > 0).astype(int)\n\n# Тест\nnp.random.seed(42)\nX = np.random.randn(10000, 5)\nw_true = np.array([1, -1, 0.5, -0.5, 0.3])\ny = (X @ w_true + np.random.randn(10000) * 0.5 > 0).astype(float)\n\nprint("Data Parallel Training:")\nfor n_workers in [1, 2, 4, 8]:\n    trainer = DataParallelTrainer(n_workers=n_workers, n_features=5)\n    trainer.fit(X[:8000], y[:8000], epochs=50)\n    pred = trainer.predict(X[8000:])\n    acc = (pred == y[8000:]).mean()\n    speedup = n_workers  # идеальный speedup\n    print(f"  Workers={n_workers}: accuracy={acc:.4f}, theoretical speedup={speedup}x")\n\nprint("\\n--- Стратегии распределённого обучения ---")\nprint("Data Parallelism: каждый GPU обрабатывает часть данных")\nprint("  AllReduce: усреднение градиентов (Horovod, PyTorch DDP)")\nprint("  Parameter Server: центральный сервер хранит веса")\nprint("\\nModel Parallelism: большая модель на нескольких GPU")\nprint("  Tensor Parallelism: один слой на нескольких GPU")\nprint("  Pipeline Parallelism: разные слои на разных GPU")\nprint("\\nИнструменты: Horovod, DeepSpeed, FSDP, Megatron-LM")' },
        { type: 'note', value: 'Data Parallelism масштабируется линейно до 8-16 GPU, дальше communication overhead становится значительным. Для LLM с миллиардами параметров используют комбинацию Data + Model + Pipeline Parallelism.' }
      ]
    },
    {
      id: 4,
      title: 'Feature Engineering at Scale',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Признаки на больших данных' },
        { type: 'text', value: 'Feature engineering на больших данных требует специальных инструментов: PySpark для распределённых преобразований, Dask для параллельных pandas-операций, Feature Store для хранения и переиспользования признаков. Оконные функции и агрегации — основа feature engineering at scale.' },
        { type: 'code', language: 'python', value: 'from pyspark.sql import SparkSession, Window\nfrom pyspark.sql import functions as F\nfrom pyspark.ml.feature import VectorAssembler, Bucketizer\nimport numpy as np\n\nspark = SparkSession.builder.appName("FE_Scale").master("local[*]").getOrCreate()\n\n# Данные транзакций\nnp.random.seed(42)\ndata = [(int(np.random.randint(1, 100)),        # user_id\n         float(np.random.uniform(1, 1000)),       # amount\n         f"2024-{np.random.randint(1,13):02d}-{np.random.randint(1,29):02d}\",\n         np.random.choice([\"food\",\"tech\",\"travel\",\"health\"]),\n         int(np.random.randint(0, 2)))             # fraud\n        for _ in range(50000)]\n\ndf = spark.createDataFrame(data, [\"user_id\", \"amount\", \"date\", \"category\", \"fraud\"])\ndf = df.withColumn(\"date\", F.to_date(\"date\"))\n\nprint(f\"Данные: {df.count()} транзакций\")\n\n# 1. Агрегатные признаки по пользователю\nuser_features = df.groupBy(\"user_id\").agg(\n    F.count(\"*\").alias(\"total_transactions\"),\n    F.avg(\"amount\").alias(\"avg_amount\"),\n    F.stddev(\"amount\").alias(\"std_amount\"),\n    F.max(\"amount\").alias(\"max_amount\"),\n    F.min(\"amount\").alias(\"min_amount\"),\n    F.sum(\"amount\").alias(\"total_amount\"),\n    F.countDistinct(\"category\").alias(\"unique_categories\"),\n    F.avg(\"fraud\").alias(\"fraud_rate\")\n)\n\nprint(\"\\nПользовательские признаки:\")\nuser_features.show(5)\n\n# 2. Оконные функции — скользящие агрегации\nwindow_user = Window.partitionBy(\"user_id\").orderBy(\"date\").rowsBetween(-6, 0)\n\ndf_windowed = df.withColumn(\n    \"rolling_avg_amount\", F.avg(\"amount\").over(window_user)\n).withColumn(\n    \"rolling_count\", F.count(\"*\").over(window_user)\n).withColumn(\n    \"rolling_max\", F.max(\"amount\").over(window_user)\n).withColumn(\n    \"amount_vs_rolling_avg\", F.col(\"amount\") / F.col(\"rolling_avg_amount\")\n)\n\nprint(\"Оконные признаки:\")\ndf_windowed.select(\"user_id\", \"date\", \"amount\", \"rolling_avg_amount\", \n                    \"rolling_count\", \"amount_vs_rolling_avg\").show(5)\n\n# 3. Бинаризация\nbucketizer = Bucketizer(\n    splits=[0, 100, 500, 1000, float(\"inf\")],\n    inputCol=\"amount\",\n    outputCol=\"amount_bucket\"\n)\ndf_bucketed = bucketizer.transform(df)\nprint(\"Бинаризация amount:\")\ndf_bucketed.groupBy(\"amount_bucket\").count().orderBy(\"amount_bucket\").show()\n\nspark.stop()\n\nprint(\"\\n--- Feature Store ---\")\nprint(\"Feast: open-source feature store\")\nprint(\"Hopsworks: managed feature store\")\nprint(\"Tecton: enterprise feature platform\")\nprint(\"Цель: переиспользование признаков между проектами\")' },
        { type: 'tip', value: 'Window functions в Spark — мощный инструмент для создания признаков из временных рядов: скользящие средние, кумулятивные суммы, lag/lead значения. Они выполняются распределённо без collect().' }
      ]
    },
    {
      id: 5,
      title: 'Model Serving',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Деплой моделей в production' },
        { type: 'text', value: 'Model serving — доставка предсказаний модели пользователям. Варианты: REST API (FastAPI/Flask), batch inference (Spark), streaming (Kafka + Spark Streaming), edge deployment (ONNX Runtime, TensorRT). MLflow управляет жизненным циклом модели.' },
        { type: 'code', language: 'python', value: 'import numpy as np\nimport json\n\n# 1. FastAPI Model Serving\nprint("=== FastAPI Model Serving ===\")\nprint(\"\"\"\nfrom fastapi import FastAPI\nfrom pydantic import BaseModel\nimport joblib\nimport numpy as np\n\napp = FastAPI()\nmodel = joblib.load("model.pkl")\n\nclass PredictRequest(BaseModel):\n    features: list[float]\n\nclass PredictResponse(BaseModel):\n    prediction: int\n    probability: float\n\n@app.post("/predict", response_model=PredictResponse)\nasync def predict(request: PredictRequest):\n    X = np.array(request.features).reshape(1, -1)\n    pred = model.predict(X)[0]\n    prob = model.predict_proba(X)[0].max()\n    return PredictResponse(prediction=int(pred), probability=float(prob))\n\n@app.get("/health\")\nasync def health():\n    return {\"status\": \"ok\"}\n\"\"\")\n\n# 2. MLflow Model Registry\nprint(\"\\n=== MLflow ===\")\nprint(\"\"\"\nimport mlflow\nimport mlflow.sklearn\n\n# Логирование эксперимента\nwith mlflow.start_run():\n    mlflow.log_params({\"n_estimators\": 100, \"max_depth\": 5})\n    mlflow.log_metrics({\"auc\": 0.95, \"accuracy\": 0.92})\n    mlflow.sklearn.log_model(model, \"model\")\n\n# Загрузка модели\nmodel = mlflow.sklearn.load_model(\"runs:/<run_id>/model\")\n\n# Model Registry\nmlflow.register_model(\"runs:/<run_id>/model\", \"MyProductionModel\")\n\"\"\")\n\n# 3. Batch Inference с Spark\nprint(\"\\n=== Batch Inference ===\")\nprint(\"\"\"\nfrom pyspark.ml import PipelineModel\n\n# Загрузка модели\nmodel = PipelineModel.load(\"hdfs:///models/my_model\")\n\n# Batch prediction на миллионах записей\nnew_data = spark.read.parquet(\"hdfs:///data/new_customers\")\npredictions = model.transform(new_data)\npredictions.write.parquet(\"hdfs:///results/predictions\")\n\"\"\")\n\n# 4. ONNX для оптимизации\nprint(\"\\n=== ONNX Runtime ===\")\nprint(\"\"\"\nimport onnxruntime as ort\n\n# Конвертация из sklearn/PyTorch в ONNX\n# skl2onnx, torch.onnx.export\n\nsession = ort.InferenceSession(\"model.onnx\")\ninput_name = session.get_inputs()[0].name\nresult = session.run(None, {input_name: X.astype(np.float32)})\n# ONNX Runtime в 2-5x быстрее PyTorch inference\n\"\"\")\n\nprint(\"\\n--- Deployment стратегии ---\")\nprint(\"Online: REST API, <100ms latency (FastAPI, TF Serving)\")\nprint(\"Batch: периодический запуск (Spark, Airflow)\")\nprint(\"Streaming: real-time (Kafka, Flink)\")\nprint(\"Edge: на устройстве (ONNX, TFLite, CoreML)\")\nprint(\"\\n--- Мониторинг ---\")\nprint(\"Data drift: изменение распределения входных данных\")\nprint(\"Concept drift: изменение связи между X и y\")\nprint(\"Model performance: отслеживание метрик в production\")' },
        { type: 'warning', value: 'В production всегда проверяйте input validation (формат, диапазоны), обрабатывайте ошибки (try/except), логируйте предсказания для мониторинга и устанавливайте rate limits для API.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: PySpark ML Pipeline',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создайте ML pipeline в PySpark для классификации.',
      requirements: [
        'Создайте PySpark DataFrame с 5000 записями (3 числовых + 1 категориальный признак)',
        'Постройте Pipeline: StringIndexer -> OneHotEncoder -> VectorAssembler -> RandomForestClassifier',
        'Разделите на train/test (80/20)',
        'Обучите pipeline и оцените AUC',
        'Выведите feature importances'
      ],
      hint: 'В PySpark ML все трансформации оборачиваются в Pipeline stages. VectorAssembler собирает признаки в один вектор. BinaryClassificationEvaluator вычисляет AUC.',
      expectedOutput: 'PySpark ML Pipeline:\nTrain: XXXX, Test: XXXX\nAUC: 0.XX\nFeature importances: ...',
      solution: 'from pyspark.sql import SparkSession\nfrom pyspark.ml.feature import StringIndexer, OneHotEncoder, VectorAssembler\nfrom pyspark.ml.classification import RandomForestClassifier\nfrom pyspark.ml import Pipeline\nfrom pyspark.ml.evaluation import BinaryClassificationEvaluator\nimport numpy as np\n\nspark = SparkSession.builder.appName("Practice").master("local[*]").getOrCreate()\n\nnp.random.seed(42)\ndata = [(float(np.random.randn()), float(np.random.randn()),\n         float(np.random.randn()), np.random.choice(["A","B","C"]),\n         int(np.random.randint(0,2))) for _ in range(5000)]\n\ndf = spark.createDataFrame(data, ["f1", "f2", "f3", "cat", "label"])\ntrain, test = df.randomSplit([0.8, 0.2], seed=42)\n\npipeline = Pipeline(stages=[\n    StringIndexer(inputCol="cat", outputCol="cat_idx"),\n    OneHotEncoder(inputCols=["cat_idx"], outputCols=["cat_vec"]),\n    VectorAssembler(inputCols=["f1", "f2", "f3", "cat_vec"], outputCol="features"),\n    RandomForestClassifier(featuresCol="features", labelCol="label", numTrees=100, seed=42)\n])\n\nmodel = pipeline.fit(train)\npredictions = model.transform(test)\n\nevaluator = BinaryClassificationEvaluator(labelCol="label", metricName="areaUnderROC")\nauc = evaluator.evaluate(predictions)\n\nrf_model = model.stages[-1]\nimportances = rf_model.featureImportances.toArray()\n\nprint("PySpark ML Pipeline:")\nprint(f"Train: {train.count()}, Test: {test.count()}")\nprint(f"AUC: {auc:.4f}")\nprint(f"Feature importances: {importances.round(4).tolist()}")\n\nspark.stop()',
      explanation: 'PySpark ML Pipeline автоматически применяет все шаги предобработки и обучения. StringIndexer преобразует категории в числа, OneHotEncoder — в вектора, VectorAssembler — собирает все в один feature vector. Pipeline сохраняется и загружается как единый объект, что упрощает deployment.'
    }
  ]
}
