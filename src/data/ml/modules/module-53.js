export default {
  id: 53,
  title: 'Проект: Object Detection (YOLO)',
  description: 'Полный проект по детекции объектов: архитектура YOLO, inference, подготовка данных, fine-tuning, оценка mAP и real-time detection.',
  lessons: [
    {
      id: 1,
      title: 'Описание проекта',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Object Detection: от классификации к детекции' },
        { type: 'text', value: 'Object Detection — задача одновременного нахождения и классификации объектов на изображении. В отличие от classification (один класс на изображение), detection возвращает bounding boxes (x, y, w, h) и класс для каждого объекта. Ключевые метрики: IoU (Intersection over Union) и mAP (mean Average Precision).' },
        { type: 'text', value: 'YOLO (You Only Look Once) — самая популярная архитектура для real-time detection. YOLOv8 (Ultralytics) — последняя версия, достигающая отличного баланса скорости и точности. Она обрабатывает изображение за один проход сети, в отличие от двухстадийных детекторов (Faster R-CNN).' },
        { type: 'code', language: 'python', value: '# Структура проекта Object Detection\n# object_detection/\n# ├── data/\n# │   ├── images/\n# │   │   ├── train/\n# │   │   └── val/\n# │   ├── labels/\n# │   │   ├── train/\n# │   │   └── val/\n# │   └── data.yaml\n# ├── models/\n# │   └── best.pt\n# ├── notebooks/\n# │   ├── 01_inference.ipynb\n# │   ├── 02_prepare_data.ipynb\n# │   └── 03_train.ipynb\n# └── detect_realtime.py\n\nimport numpy as np\n\n# Ключевые концепции Object Detection\ndef compute_iou(box1, box2):\n    """IoU (Intersection over Union) двух bounding boxes.\n    box = [x1, y1, x2, y2]\n    """\n    x1 = max(box1[0], box2[0])\n    y1 = max(box1[1], box2[1])\n    x2 = min(box1[2], box2[2])\n    y2 = min(box1[3], box2[3])\n    \n    intersection = max(0, x2 - x1) * max(0, y2 - y1)\n    area1 = (box1[2] - box1[0]) * (box1[3] - box1[1])\n    area2 = (box2[2] - box2[0]) * (box2[3] - box2[1])\n    union = area1 + area2 - intersection\n    \n    return intersection / union if union > 0 else 0\n\n# Пример\ngt_box = [100, 100, 300, 300]   # ground truth\npred_box = [120, 110, 310, 290]  # prediction\niou = compute_iou(gt_box, pred_box)\nprint(f"IoU = {iou:.4f}")\nprint(f"IoU > 0.5 → True Positive" if iou > 0.5 else "IoU <= 0.5 → False Positive")\n\n# Сравнение архитектур\nprint("\\n=== Архитектуры Object Detection ===")\narchitectures = [\n    ("YOLOv8n", "nano", "3.2M", "~80 FPS", "37.3 mAP"),\n    ("YOLOv8s", "small", "11.2M", "~50 FPS", "44.9 mAP"),\n    ("YOLOv8m", "medium", "25.9M", "~30 FPS", "50.2 mAP"),\n    ("YOLOv8l", "large", "43.7M", "~18 FPS", "52.9 mAP"),\n    ("YOLOv8x", "xlarge", "68.2M", "~12 FPS", "53.9 mAP"),\n]\nprint(f"{\'Модель\':<12} {\'Размер\':<8} {\'Параметры\':<10} {\'Скорость\':<10} {\'COCO mAP\'}")\nfor name, size, params, fps, mAP in architectures:\n    print(f"{name:<12} {size:<8} {params:<10} {fps:<10} {mAP}")' },
        { type: 'note', value: 'IoU (Intersection over Union) — основная метрика совпадения bounding boxes. IoU > 0.5 считается True Positive (TP). mAP@0.5 — среднее AP по всем классам при пороге IoU=0.5.' },
        { type: 'tip', value: 'Для начала используйте YOLOv8n (nano) — самая быстрая модель. Для production — YOLOv8m (medium) как оптимальный баланс. YOLOv8x — максимальная точность, но медленная.' }
      ]
    },
    {
      id: 2,
      title: 'YOLO архитектура',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Как работает YOLO' },
        { type: 'text', value: 'YOLO делит изображение на сетку (grid) и для каждой ячейки предсказывает bounding boxes с вероятностями классов за один проход сети. Архитектура: Backbone (извлечение признаков, CSPDarknet) → Neck (агрегация, FPN+PAN) → Head (предсказание boxes + classes).' },
        { type: 'text', value: 'Anchor boxes — предопределённые размеры boxes для разных масштабов объектов. YOLO предсказывает смещения относительно anchors. YOLOv8 использует anchor-free подход, предсказывая центр объекта и расстояния до границ напрямую.' },
        { type: 'code', language: 'python', value: 'import numpy as np\n\n# Симуляция работы YOLO\nclass YOLOSimulator:\n    """Упрощённая симуляция YOLO detection pipeline."""\n    \n    def __init__(self, grid_size=7, num_classes=3, num_boxes=2):\n        self.grid_size = grid_size\n        self.num_classes = num_classes\n        self.num_boxes = num_boxes\n        self.class_names = ["car", "person", "dog"]\n    \n    def simulate_prediction(self, image_size=416):\n        """Симулирует предсказание YOLO на изображении."""\n        cell_size = image_size / self.grid_size\n        predictions = []\n        \n        # Случайные предсказания для демонстрации\n        np.random.seed(42)\n        for i in range(self.grid_size):\n            for j in range(self.grid_size):\n                for b in range(self.num_boxes):\n                    confidence = np.random.random()\n                    if confidence > 0.7:  # порог уверенности\n                        cx = (j + np.random.random()) * cell_size\n                        cy = (i + np.random.random()) * cell_size\n                        w = np.random.uniform(30, 150)\n                        h = np.random.uniform(30, 150)\n                        class_probs = np.random.dirichlet([1]*self.num_classes)\n                        class_id = class_probs.argmax()\n                        \n                        predictions.append({\n                            "bbox": [cx - w/2, cy - h/2, cx + w/2, cy + h/2],\n                            "confidence": confidence * class_probs.max(),\n                            "class_id": class_id,\n                            "class_name": self.class_names[class_id]\n                        })\n        return predictions\n    \n    def non_max_suppression(self, predictions, iou_threshold=0.5):\n        """NMS — удаление дублирующих boxes."""\n        preds = sorted(predictions, key=lambda x: -x["confidence"])\n        keep = []\n        \n        while preds:\n            best = preds.pop(0)\n            keep.append(best)\n            preds = [\n                p for p in preds\n                if p["class_id"] != best["class_id"] or\n                   self._iou(best["bbox"], p["bbox"]) < iou_threshold\n            ]\n        return keep\n    \n    def _iou(self, b1, b2):\n        x1 = max(b1[0], b2[0])\n        y1 = max(b1[1], b2[1])\n        x2 = min(b1[2], b2[2])\n        y2 = min(b1[3], b2[3])\n        inter = max(0, x2-x1) * max(0, y2-y1)\n        a1 = (b1[2]-b1[0]) * (b1[3]-b1[1])\n        a2 = (b2[2]-b2[0]) * (b2[3]-b2[1])\n        return inter / (a1 + a2 - inter) if (a1+a2-inter) > 0 else 0\n\nyolo = YOLOSimulator()\nraw_preds = yolo.simulate_prediction()\nprint(f"Raw predictions: {len(raw_preds)}")\n\nnms_preds = yolo.non_max_suppression(raw_preds)\nprint(f"After NMS: {len(nms_preds)}")\n\nprint("\\nДетекции:")\nfor p in nms_preds[:5]:\n    bbox = [f"{x:.0f}" for x in p["bbox"]]\n    print(f"  {p[\"class_name\"]:8s} conf={p[\"confidence\"]:.3f} bbox=[{\\", \\".join(bbox)}]")' },
        { type: 'text', value: 'Non-Maximum Suppression (NMS) — критический шаг post-processing. Когда несколько boxes детектируют один объект, NMS оставляет только box с наибольшей уверенностью, удаляя перекрывающиеся (IoU > threshold).' },
        { type: 'tip', value: 'YOLOv8 обрабатывает изображения размером 640x640 по умолчанию. Больший размер (1280) улучшает детекцию мелких объектов, но замедляет inference. Для real-time используйте 416 или 640.' }
      ]
    },
    {
      id: 3,
      title: 'Практика: Inference с YOLOv8',
      type: 'practice',
      difficulty: 'easy',
      description: 'Запустите inference YOLOv8 на изображении: загрузка модели, предсказание, разбор результатов.',
      requirements: [
        'Реализуйте функцию compute_iou для двух bounding boxes',
        'Реализуйте Non-Maximum Suppression (NMS) с порогом IoU',
        'Создайте симуляцию YOLOv8 inference: случайные детекции на изображении 640x640',
        'Примените NMS и выведите финальные детекции с class, confidence, bbox',
        'Покажите код использования ultralytics: from ultralytics import YOLO'
      ],
      hint: 'IoU = intersection_area / union_area. NMS: сортируйте по confidence desc, берите лучший, удаляйте все с IoU > threshold того же класса, повторяйте.',
      expectedOutput: 'YOLOv8 Inference:\nRaw detections: XX\nAfter NMS: X\n\nРезультаты:\n  car    conf=0.XX bbox=[x1, y1, x2, y2]\n  person conf=0.XX bbox=[x1, y1, x2, y2]\n  ...\n\nUltralytics код:\n  model = YOLO("yolov8n.pt")\n  results = model("image.jpg")',
      solution: 'import numpy as np\n\ndef compute_iou(b1, b2):\n    x1 = max(b1[0], b2[0])\n    y1 = max(b1[1], b2[1])\n    x2 = min(b1[2], b2[2])\n    y2 = min(b1[3], b2[3])\n    inter = max(0, x2-x1) * max(0, y2-y1)\n    a1 = (b1[2]-b1[0]) * (b1[3]-b1[1])\n    a2 = (b2[2]-b2[0]) * (b2[3]-b2[1])\n    union = a1 + a2 - inter\n    return inter / union if union > 0 else 0\n\ndef nms(detections, iou_thresh=0.5):\n    dets = sorted(detections, key=lambda d: -d["conf"])\n    keep = []\n    while dets:\n        best = dets.pop(0)\n        keep.append(best)\n        dets = [d for d in dets\n                if d["cls"] != best["cls"] or compute_iou(best["bbox"], d["bbox"]) < iou_thresh]\n    return keep\n\n# Симуляция YOLOv8 inference\nnp.random.seed(42)\nclass_names = ["car", "person", "dog", "bicycle", "cat"]\nimg_size = 640\n\nraw = []\nfor _ in range(25):\n    cx, cy = np.random.uniform(50, 590, 2)\n    w, h = np.random.uniform(40, 200, 2)\n    bbox = [cx-w/2, cy-h/2, cx+w/2, cy+h/2]\n    cls = np.random.randint(0, len(class_names))\n    conf = np.random.uniform(0.1, 0.99)\n    raw.append({"bbox": bbox, "cls": cls, "cls_name": class_names[cls], "conf": conf})\n\n# Фильтр по confidence\nfiltered = [d for d in raw if d["conf"] > 0.4]\nfinal = nms(filtered, iou_thresh=0.5)\n\nprint("YOLOv8 Inference:")\nprint(f"Raw detections: {len(raw)}")\nprint(f"After confidence filter (>0.4): {len(filtered)}")\nprint(f"After NMS: {len(final)}")\n\nprint("\\nРезультаты:")\nfor d in final[:6]:\n    bb = ", ".join(f"{x:.0f}" for x in d["bbox"])\n    print(f"  {d[\"cls_name\"]:8s} conf={d[\"conf\"]:.3f} bbox=[{bb}]")\n\nprint("\\nUltralytics код:")\nprint("  from ultralytics import YOLO")\nprint("  model = YOLO(\'yolov8n.pt\')")\nprint("  results = model(\'image.jpg\')")\nprint("  for r in results:")\nprint("      boxes = r.boxes")\nprint("      for box in boxes:")\nprint("          xyxy = box.xyxy[0].tolist()")\nprint("          conf = box.conf[0].item()")\nprint("          cls = int(box.cls[0].item())")',
      explanation: 'YOLO inference: загружаем модель, подаём изображение, получаем raw detections, фильтруем по confidence threshold, применяем NMS для удаления дублей. Ultralytics делает всё это в одну строку: model("image.jpg").'
    },
    {
      id: 4,
      title: 'Подготовка данных',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Аннотирование и форматы данных' },
        { type: 'text', value: 'Для обучения YOLO нужны размеченные данные: изображения + bounding boxes с классами. Форматы: YOLO (txt, нормализованные координаты), COCO (JSON, абсолютные координаты), Pascal VOC (XML). Data augmentation критически важен для малых датасетов.' },
        { type: 'code', language: 'python', value: 'import json\nimport numpy as np\n\n# === YOLO формат ===\n# Каждое изображение → txt файл с тем же именем\n# Каждая строка: class_id center_x center_y width height (нормализованные 0-1)\nyolo_label = \"\"\"0 0.5 0.4 0.3 0.5\n1 0.2 0.6 0.15 0.3\n2 0.8 0.7 0.1 0.2\"\"\"\nprint("=== YOLO формат (label.txt) ===")\nprint(yolo_label)\n\n# === COCO формат ===\ncoco_annotation = {\n    "images": [\n        {"id": 1, "file_name": "image_001.jpg", "width": 640, "height": 480}\n    ],\n    "annotations": [\n        {"id": 1, "image_id": 1, "category_id": 0,\n         "bbox": [160, 72, 192, 240], "area": 46080},  # x, y, w, h (абс.)\n        {"id": 2, "image_id": 1, "category_id": 1,\n         "bbox": [80, 216, 96, 144], "area": 13824},\n    ],\n    "categories": [\n        {"id": 0, "name": "car"},\n        {"id": 1, "name": "person"},\n        {"id": 2, "name": "dog"}\n    ]\n}\nprint("\\n=== COCO формат (annotations.json) ===")\nprint(json.dumps(coco_annotation, indent=2)[:300] + "...")\n\n# Конвертация COCO → YOLO\ndef coco_to_yolo(coco_data):\n    """Конвертация COCO аннотаций в YOLO формат."""\n    img_map = {img["id"]: img for img in coco_data["images"]}\n    results = {}  # image_id → список строк YOLO\n    \n    for ann in coco_data["annotations"]:\n        img = img_map[ann["image_id"]]\n        img_w, img_h = img["width"], img["height"]\n        x, y, w, h = ann["bbox"]  # COCO: x,y — верхний левый угол\n        \n        # Нормализация для YOLO: center_x, center_y, width, height\n        cx = (x + w / 2) / img_w\n        cy = (y + h / 2) / img_h\n        nw = w / img_w\n        nh = h / img_h\n        \n        label = f"{ann[\'category_id\']} {cx:.6f} {cy:.6f} {nw:.6f} {nh:.6f}"\n        img_id = ann["image_id"]\n        if img_id not in results:\n            results[img_id] = []\n        results[img_id].append(label)\n    \n    return results\n\nyolo_labels = coco_to_yolo(coco_annotation)\nprint("\\n=== Конвертация COCO → YOLO ===")\nfor img_id, labels in yolo_labels.items():\n    print(f"Image {img_id}:")\n    for label in labels:\n        print(f"  {label}")' },
        { type: 'heading', value: 'Data Augmentation для Detection' },
        { type: 'code', language: 'python', value: '# Data Augmentation для Object Detection\n# pip install albumentations\nimport numpy as np\n\n# Albumentations — лучшая библиотека для augmentation в detection\n# Автоматически трансформирует bounding boxes вместе с изображением\n\naugmentation_config = {\n    "horizontal_flip": {"p": 0.5},\n    "vertical_flip": {"p": 0.1},\n    "random_brightness_contrast": {"brightness_limit": 0.2, "contrast_limit": 0.2, "p": 0.5},\n    "hue_saturation_value": {"hue_shift": 20, "sat_shift": 30, "val_shift": 20, "p": 0.5},\n    "blur": {"blur_limit": 3, "p": 0.1},\n    "random_scale": {"scale_limit": 0.2, "p": 0.5},\n    "mosaic": {"p": 0.5},  # YOLO-специфичный\n    "mixup": {"p": 0.1},   # YOLO-специфичный\n}\n\nprint("=== Data Augmentation для Detection ===")\nfor aug_name, params in augmentation_config.items():\n    print(f"  {aug_name}: {params}")\n\nprint("\\n--- Albumentations пример ---")\nprint(\"\"\"\nimport albumentations as A\n\ntransform = A.Compose([\n    A.HorizontalFlip(p=0.5),\n    A.RandomBrightnessContrast(p=0.3),\n    A.HueSaturationValue(p=0.3),\n    A.Blur(blur_limit=3, p=0.1),\n    A.RandomSizedBBoxSafeCrop(width=640, height=640, p=0.5),\n], bbox_params=A.BboxParams(\n    format="yolo",          # yolo: center_x, center_y, w, h (normalized)\n    label_fields=["class_labels"],\n    min_visibility=0.3      # удалять boxes с <30% видимости\n))\n\nresult = transform(\n    image=image,\n    bboxes=[[0.5, 0.4, 0.3, 0.5]],\n    class_labels=[0]\n)\naugmented_image = result["image"]\naugmented_bboxes = result["bboxes"]\n\"\"\")' },
        { type: 'tip', value: 'Mosaic augmentation (склейка 4 изображений) — фирменная техника YOLO, увеличивающая разнообразие и контекст. YOLOv8 применяет mosaic автоматически при обучении.' }
      ]
    },
    {
      id: 5,
      title: 'Практика: Подготовка custom dataset',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создайте и подготовьте custom dataset в формате YOLO для обучения модели.',
      requirements: [
        'Создайте синтетический COCO-датасет (20 изображений, 3 класса, 50+ аннотаций)',
        'Реализуйте coco_to_yolo() — конвертация COCO → YOLO формат',
        'Разделите на train/val (80/20)',
        'Сгенерируйте data.yaml конфигурацию для YOLOv8',
        'Выведите статистику: объектов на класс, среднее boxes на изображение'
      ],
      hint: 'COCO bbox: [x, y, width, height] (абсолютные, верхний левый угол). YOLO: class_id cx cy w h (нормализованные, центр). data.yaml содержит path, train, val, nc (кол-во классов), names.',
      expectedOutput: 'Custom Dataset:\nИзображений: 20, Аннотаций: 50+\n\nКонвертация COCO → YOLO: OK\nTrain: 16 images, Val: 4 images\n\nСтатистика:\n  car: XX объектов\n  person: XX объектов\n  dog: XX объектов\n  Среднее boxes/image: X.X\n\ndata.yaml сгенерирован',
      solution: 'import numpy as np\nimport json\n\nnp.random.seed(42)\nclass_names = ["car", "person", "dog"]\nn_images = 20\nimg_w, img_h = 640, 480\n\n# Генерация COCO датасета\nimages = [{"id": i, "file_name": f"img_{i:04d}.jpg", "width": img_w, "height": img_h}\n          for i in range(n_images)]\n\nannotations = []\nann_id = 0\nfor img_id in range(n_images):\n    n_objects = np.random.randint(2, 5)\n    for _ in range(n_objects):\n        cls = np.random.randint(0, len(class_names))\n        w = np.random.randint(50, 200)\n        h = np.random.randint(50, 200)\n        x = np.random.randint(0, img_w - w)\n        y = np.random.randint(0, img_h - h)\n        annotations.append({\n            "id": ann_id, "image_id": img_id, "category_id": cls,\n            "bbox": [int(x), int(y), int(w), int(h)], "area": int(w*h)\n        })\n        ann_id += 1\n\ncoco = {\n    "images": images,\n    "annotations": annotations,\n    "categories": [{"id": i, "name": n} for i, n in enumerate(class_names)]\n}\n\n# Конвертация COCO → YOLO\ndef coco_to_yolo(coco_data):\n    img_map = {img["id"]: img for img in coco_data["images"]}\n    results = {}\n    for ann in coco_data["annotations"]:\n        img = img_map[ann["image_id"]]\n        iw, ih = img["width"], img["height"]\n        x, y, w, h = ann["bbox"]\n        cx = (x + w/2) / iw\n        cy = (y + h/2) / ih\n        nw = w / iw\n        nh = h / ih\n        label = f"{ann[\'category_id\']} {cx:.6f} {cy:.6f} {nw:.6f} {nh:.6f}"\n        fid = img["file_name"].replace(".jpg", "")\n        if fid not in results:\n            results[fid] = []\n        results[fid].append(label)\n    return results\n\nyolo_labels = coco_to_yolo(coco)\n\n# Train/Val split\nimg_ids = list(range(n_images))\nnp.random.shuffle(img_ids)\nsplit = int(0.8 * n_images)\ntrain_ids = set(img_ids[:split])\nval_ids = set(img_ids[split:])\n\n# Статистика\nclass_counts = {n: 0 for n in class_names}\nfor ann in annotations:\n    class_counts[class_names[ann["category_id"]]] += 1\n\nprint("Custom Dataset:")\nprint(f"Изображений: {n_images}, Аннотаций: {len(annotations)}")\nprint(f"\\nКонвертация COCO → YOLO: OK ({len(yolo_labels)} файлов)")\nprint(f"Train: {len(train_ids)} images, Val: {len(val_ids)} images")\nprint(f"\\nСтатистика:")\nfor cls_name, count in class_counts.items():\n    print(f"  {cls_name}: {count} объектов")\nprint(f"  Среднее boxes/image: {len(annotations)/n_images:.1f}")\n\ndata_yaml = f"""path: ./dataset\ntrain: images/train\nval: images/val\nnc: {len(class_names)}\nnames: {class_names}\n"""\nprint(f"\\ndata.yaml:\\n{data_yaml}")',
      explanation: 'Подготовка данных — самый важный этап в object detection. COCO → YOLO конвертация: переводим абсолютные координаты верхнего левого угла в нормализованные координаты центра. data.yaml связывает пути к данным с конфигурацией классов для YOLOv8.'
    },
    {
      id: 6,
      title: 'Обучение кастомной модели',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Fine-tuning YOLOv8' },
        { type: 'text', value: 'Fine-tuning — дообучение предобученной модели на своих данных. YOLOv8 предобучена на COCO (80 классов, 330K изображений). Fine-tuning использует эти знания и адаптирует модель к вашему домену. Обычно достаточно 100-500 изображений на класс.' },
        { type: 'code', language: 'python', value: '# Fine-tuning YOLOv8 (ultralytics)\nfrom ultralytics import YOLO\n\n# 1. Загрузка предобученной модели\nmodel = YOLO("yolov8n.pt")  # nano — самая быстрая для экспериментов\n\n# 2. Fine-tuning на custom данных\nresults = model.train(\n    data="data.yaml",       # путь к конфигурации\n    epochs=100,             # количество эпох\n    imgsz=640,              # размер изображения\n    batch=16,               # размер батча\n    lr0=0.01,               # начальный learning rate\n    lrf=0.01,               # финальный LR (cosine scheduler)\n    momentum=0.937,         # SGD momentum\n    weight_decay=0.0005,    # L2 регуляризация\n    warmup_epochs=3,        # warmup эпох\n    augment=True,           # data augmentation (mosaic, mixup)\n    patience=20,            # early stopping\n    device=0,               # GPU (0) или "cpu"\n    project="runs/detect",  # папка для результатов\n    name="custom_yolo",     # имя эксперимента\n)\n\n# 3. Результаты\nprint(f"Лучшая модель: runs/detect/custom_yolo/weights/best.pt")\nprint(f"mAP50: {results.results_dict[\"metrics/mAP50(B)\"]:.4f}")\nprint(f"mAP50-95: {results.results_dict[\"metrics/mAP50-95(B)\"]:.4f}")\n\n# 4. Валидация\nmetrics = model.val()\nprint(f"\\nВалидация:")\nprint(f"  mAP50: {metrics.box.map50:.4f}")\nprint(f"  mAP50-95: {metrics.box.map:.4f}")\n\n# 5. Inference с обученной моделью\nmodel = YOLO("runs/detect/custom_yolo/weights/best.pt")\nresults = model("test_image.jpg", conf=0.5)\nfor r in results:\n    for box in r.boxes:\n        print(f"  {model.names[int(box.cls)]} conf={box.conf[0]:.2f}")' },
        { type: 'heading', value: 'Гиперпараметры обучения' },
        { type: 'code', language: 'python', value: '# Ключевые гиперпараметры YOLOv8\nhyperparams = {\n    "epochs": {\n        "default": 100,\n        "tip": "50-300 в зависимости от объёма данных. Используйте early stopping (patience=20)"\n    },\n    "imgsz": {\n        "default": 640,\n        "tip": "320 для скорости, 640 стандарт, 1280 для мелких объектов"\n    },\n    "batch": {\n        "default": 16,\n        "tip": "Максимум, который помещается в GPU RAM. -1 для auto-batch"\n    },\n    "lr0": {\n        "default": 0.01,\n        "tip": "0.001-0.01. Уменьшайте для fine-tuning (0.001)"\n    },\n    "freeze": {\n        "default": 0,\n        "tip": "Количество замороженных слоёв. freeze=10 для малых данных"\n    },\n    "augment": {\n        "default": True,\n        "tip": "Mosaic, mixup, HSV, flip. Отключите close_mosaic=0 в конце"\n    },\n}\n\nprint("=== Гиперпараметры YOLOv8 ===")\nfor param, info in hyperparams.items():\n    print(f"  {param} (default={info[\'default\']}): {info[\'tip\']}")\n\nprint("\\n=== Стратегии для малых данных ===")\nprint("  1. freeze=10-15 — заморозить backbone")\nprint("  2. lr0=0.001 — маленький learning rate")\nprint("  3. augment=True — обязательно")\nprint("  4. patience=50 — долгий early stopping")\nprint("  5. Начните с yolov8n, затем перейдите к yolov8s/m")' },
        { type: 'tip', value: 'Для малых датасетов (<200 изображений): заморозьте backbone (freeze=10), уменьшите LR (lr0=0.001), увеличьте augmentation. Transfer learning из COCO даёт хорошую базу даже для необычных доменов.' }
      ]
    },
    {
      id: 7,
      title: 'Практика: Fine-tuning YOLOv8',
      type: 'practice',
      difficulty: 'hard',
      description: 'Подготовьте полный pipeline fine-tuning YOLOv8: конфигурация, обучение, валидация, экспорт.',
      requirements: [
        'Создайте data.yaml для custom dataset (3 класса, пути train/val)',
        'Напишите training script с оптимальными гиперпараметрами для малых данных',
        'Реализуйте функцию оценки: mAP50, mAP50-95, per-class AP',
        'Добавьте inference на тестовых изображениях с визуализацией результатов',
        'Покажите экспорт модели в ONNX формат для deployment'
      ],
      hint: 'Для малых данных: freeze backbone слои, уменьшите lr0 до 0.001, увеличьте patience до 50. ONNX экспорт: model.export(format="onnx", imgsz=640).',
      expectedOutput: 'YOLOv8 Fine-tuning Pipeline:\ndata.yaml: 3 classes, train+val paths\n\nTraining config:\n  model: yolov8n.pt\n  epochs: 100, batch: 16\n  lr0: 0.001, freeze: 10\n\nОценка:\n  mAP50: 0.XX, mAP50-95: 0.XX\n  car: AP=0.XX\n  person: AP=0.XX\n  dog: AP=0.XX\n\nExport: model.onnx',
      solution: 'import numpy as np\n\n# 1. data.yaml\ndata_yaml = """\npath: /data/custom_dataset\ntrain: images/train\nval: images/val\nnc: 3\nnames: [\'car\', \'person\', \'dog\']\n"""\nprint("YOLOv8 Fine-tuning Pipeline:")\nprint(f"data.yaml: 3 classes, train+val paths")\n\n# 2. Training script\ntraining_code = """\nfrom ultralytics import YOLO\n\nmodel = YOLO("yolov8n.pt")\nresults = model.train(\n    data="data.yaml",\n    epochs=100,\n    imgsz=640,\n    batch=16,\n    lr0=0.001,\n    lrf=0.01,\n    freeze=10,\n    patience=50,\n    augment=True,\n    device=0,\n    project="runs/detect",\n    name="custom_v1",\n)\n"""\nprint("\\nTraining config:")\nprint("  model: yolov8n.pt")\nprint("  epochs: 100, batch: 16")\nprint("  lr0: 0.001, freeze: 10")\n\n# 3. Симуляция оценки\nnp.random.seed(42)\nclass_names = ["car", "person", "dog"]\nclass_ap50 = {n: np.random.uniform(0.65, 0.92) for n in class_names}\nmap50 = np.mean(list(class_ap50.values()))\nmap50_95 = map50 * np.random.uniform(0.55, 0.7)\n\ndef evaluate_model(class_ap, map50, map50_95):\n    print(f"\\nОценка:")\n    print(f"  mAP50: {map50:.4f}, mAP50-95: {map50_95:.4f}")\n    for cls_name, ap in class_ap.items():\n        print(f"  {cls_name}: AP={ap:.4f}")\n\nevaluate_model(class_ap50, map50, map50_95)\n\n# 4. Inference\ninference_code = """\nmodel = YOLO("runs/detect/custom_v1/weights/best.pt")\nresults = model("test_images/", conf=0.5, save=True)\nfor r in results:\n    for box in r.boxes:\n        cls = model.names[int(box.cls)]\n        conf = box.conf[0].item()\n        xyxy = box.xyxy[0].tolist()\n        print(f"{cls} {conf:.2f} {xyxy}")\n"""\nprint("\\nInference: код готов")\n\n# 5. Export\nexport_code = """\nmodel = YOLO("runs/detect/custom_v1/weights/best.pt")\nmodel.export(format="onnx", imgsz=640, simplify=True)\nmodel.export(format="tflite", imgsz=640)  # для мобильных\nmodel.export(format="engine", imgsz=640)  # TensorRT для GPU\n"""\nprint("\\nExport форматы:")\nformats = [\n    ("ONNX", "model.onnx", "Универсальный, CPU/GPU"),\n    ("TFLite", "model.tflite", "Мобильные устройства"),\n    ("TensorRT", "model.engine", "NVIDIA GPU, максимум скорости"),\n    ("CoreML", "model.mlpackage", "Apple устройства"),\n]\nfor fmt, fname, desc in formats:\n    print(f"  {fmt:10s} → {fname:20s} ({desc})")',
      explanation: 'Fine-tuning YOLOv8 pipeline: data.yaml конфигурация → training с оптимальными гиперпараметрами → оценка mAP → export в ONNX/TFLite/TensorRT. Для малых данных ключевые техники: freeze backbone, малый LR, сильная augmentation.'
    },
    {
      id: 8,
      title: 'Оценка',
      type: 'theory',
      content: [
        { type: 'heading', value: 'mAP, Precision-Recall для Object Detection' },
        { type: 'text', value: 'Оценка object detection сложнее classification. Нужно оценить: (1) правильность классификации, (2) точность локализации (IoU). Метрики: Precision (TP / (TP+FP)), Recall (TP / (TP+FN)), AP (Area under Precision-Recall curve), mAP (mean AP по классам).' },
        { type: 'code', language: 'python', value: 'import numpy as np\n\ndef compute_ap(precisions, recalls):\n    """Вычисление Average Precision (AP) через интерполяцию."""\n    # Добавляем граничные точки\n    precisions = np.concatenate([[0], precisions, [0]])\n    recalls = np.concatenate([[0], recalls, [1]])\n    \n    # Монотонно убывающий precision\n    for i in range(len(precisions) - 2, -1, -1):\n        precisions[i] = max(precisions[i], precisions[i + 1])\n    \n    # Находим точки изменения recall\n    indices = np.where(recalls[1:] != recalls[:-1])[0] + 1\n    ap = np.sum((recalls[indices] - recalls[indices - 1]) * precisions[indices])\n    return ap\n\ndef evaluate_detections(gt_boxes, pred_boxes, iou_threshold=0.5):\n    """Оценка детекций для одного класса.\n    gt_boxes: list of [x1, y1, x2, y2]\n    pred_boxes: list of [x1, y1, x2, y2, confidence]\n    """\n    # Сортировка по confidence\n    preds = sorted(pred_boxes, key=lambda x: -x[4])\n    tp = np.zeros(len(preds))\n    fp = np.zeros(len(preds))\n    matched_gt = set()\n    \n    for i, pred in enumerate(preds):\n        best_iou = 0\n        best_gt = -1\n        for j, gt in enumerate(gt_boxes):\n            iou = compute_iou_eval(pred[:4], gt)\n            if iou > best_iou:\n                best_iou = iou\n                best_gt = j\n        \n        if best_iou >= iou_threshold and best_gt not in matched_gt:\n            tp[i] = 1\n            matched_gt.add(best_gt)\n        else:\n            fp[i] = 1\n    \n    # Cumulative TP и FP\n    tp_cumsum = np.cumsum(tp)\n    fp_cumsum = np.cumsum(fp)\n    \n    precisions = tp_cumsum / (tp_cumsum + fp_cumsum)\n    recalls = tp_cumsum / len(gt_boxes)\n    \n    ap = compute_ap(precisions, recalls)\n    return ap, precisions[-1] if len(precisions) > 0 else 0, recalls[-1] if len(recalls) > 0 else 0\n\ndef compute_iou_eval(b1, b2):\n    x1 = max(b1[0], b2[0])\n    y1 = max(b1[1], b2[1])\n    x2 = min(b1[2], b2[2])\n    y2 = min(b1[3], b2[3])\n    inter = max(0, x2-x1) * max(0, y2-y1)\n    a1 = (b1[2]-b1[0]) * (b1[3]-b1[1])\n    a2 = (b2[2]-b2[0]) * (b2[3]-b2[1])\n    return inter / (a1+a2-inter) if (a1+a2-inter) > 0 else 0\n\n# Пример оценки\nnp.random.seed(42)\ngt = [[100, 100, 250, 300], [400, 200, 550, 400], [50, 50, 150, 180]]\npreds = [\n    [105, 95, 260, 310, 0.95],  # хорошее совпадение с gt[0]\n    [395, 195, 560, 410, 0.88], # хорошее совпадение с gt[1]\n    [300, 300, 400, 400, 0.7],  # FP — нет совпадения\n    [55, 45, 155, 175, 0.6],    # хорошее совпадение с gt[2]\n    [200, 200, 300, 300, 0.3],  # FP\n]\n\nap, prec, rec = evaluate_detections(gt, preds, iou_threshold=0.5)\nprint("=== Object Detection Evaluation ===")\nprint(f"Ground truth boxes: {len(gt)}")\nprint(f"Predicted boxes: {len(preds)}")\nprint(f"AP@0.5: {ap:.4f}")\nprint(f"Precision: {prec:.4f}")\nprint(f"Recall: {rec:.4f}")' },
        { type: 'text', value: 'mAP@0.5:0.95 (COCO metric) — среднее AP по порогам IoU от 0.5 до 0.95 с шагом 0.05. Это более строгая метрика, требующая точной локализации.' },
        { type: 'note', value: 'Confusion matrix для detection: TP — правильный класс + IoU > порог; FP — неправильный класс или IoU < порог; FN — пропущенный ground truth объект; Background FP — детекция там, где нет объекта.' }
      ]
    },
    {
      id: 9,
      title: 'Практика: Полный pipeline object detection',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте полный pipeline оценки object detection: IoU, matching, AP, mAP.',
      requirements: [
        'Реализуйте compute_iou для двух bounding boxes',
        'Реализуйте evaluate_class: matching predictions с ground truth, расчёт TP/FP',
        'Реализуйте compute_ap через PR-кривую с интерполяцией',
        'Рассчитайте mAP для 3 классов с синтетическими данными (50+ gt boxes)',
        'Выведите per-class AP, mAP@0.5 и confusion-подобную статистику'
      ],
      hint: 'Порядок: (1) сортируйте predictions по confidence desc; (2) для каждого pred найдите лучший GT по IoU; (3) если IoU > threshold и GT не matched — TP, иначе FP; (4) рассчитайте cumulative precision/recall; (5) AP = площадь под PR-кривой.',
      expectedOutput: 'Object Detection Evaluation:\nGT boxes: 50+, Predictions: 60+\n\nPer-class AP@0.5:\n  car: 0.XXXX\n  person: 0.XXXX\n  dog: 0.XXXX\n\nmAP@0.5: 0.XXXX\nTP: XX, FP: XX, FN: XX',
      solution: 'import numpy as np\n\ndef compute_iou(b1, b2):\n    x1 = max(b1[0], b2[0])\n    y1 = max(b1[1], b2[1])\n    x2 = min(b1[2], b2[2])\n    y2 = min(b1[3], b2[3])\n    inter = max(0, x2-x1) * max(0, y2-y1)\n    a1 = (b1[2]-b1[0]) * (b1[3]-b1[1])\n    a2 = (b2[2]-b2[0]) * (b2[3]-b2[1])\n    return inter / (a1+a2-inter) if (a1+a2-inter) > 0 else 0\n\ndef compute_ap(precisions, recalls):\n    p = np.concatenate([[0], precisions, [0]])\n    r = np.concatenate([[0], recalls, [1]])\n    for i in range(len(p)-2, -1, -1):\n        p[i] = max(p[i], p[i+1])\n    idx = np.where(r[1:] != r[:-1])[0] + 1\n    return np.sum((r[idx] - r[idx-1]) * p[idx])\n\ndef evaluate_class(gt_boxes, pred_boxes, iou_thresh=0.5):\n    preds = sorted(pred_boxes, key=lambda x: -x[4])\n    tp = np.zeros(len(preds))\n    fp = np.zeros(len(preds))\n    matched = set()\n    for i, pred in enumerate(preds):\n        best_iou, best_j = 0, -1\n        for j, gt in enumerate(gt_boxes):\n            iou = compute_iou(pred[:4], gt)\n            if iou > best_iou:\n                best_iou = iou\n                best_j = j\n        if best_iou >= iou_thresh and best_j not in matched:\n            tp[i] = 1\n            matched.add(best_j)\n        else:\n            fp[i] = 1\n    tp_cum = np.cumsum(tp)\n    fp_cum = np.cumsum(fp)\n    precisions = tp_cum / (tp_cum + fp_cum + 1e-8)\n    recalls = tp_cum / max(len(gt_boxes), 1)\n    ap = compute_ap(precisions, recalls)\n    return ap, int(tp.sum()), int(fp.sum()), len(gt_boxes) - int(tp.sum())\n\n# Синтетические данные\nnp.random.seed(42)\nclass_names = ["car", "person", "dog"]\nall_gt = {c: [] for c in range(3)}\nall_pred = {c: [] for c in range(3)}\n\nfor _ in range(20):\n    for c in range(3):\n        n_gt = np.random.randint(1, 4)\n        for _ in range(n_gt):\n            x1, y1 = np.random.randint(0, 400, 2)\n            w, h = np.random.randint(50, 200, 2)\n            all_gt[c].append([x1, y1, x1+w, y1+h])\n            if np.random.random() > 0.2:\n                noise = np.random.randint(-20, 20, 4)\n                pred_box = [x1+noise[0], y1+noise[1], x1+w+noise[2], y1+h+noise[3]]\n                conf = np.random.uniform(0.3, 0.99)\n                all_pred[c].append(pred_box + [conf])\n        if np.random.random() > 0.7:\n            fp_box = list(np.random.randint(0, 500, 4))\n            fp_box.sort()\n            all_pred[c].append([fp_box[0], fp_box[1], fp_box[2]+50, fp_box[3]+50, np.random.uniform(0.2, 0.6)])\n\ntotal_gt = sum(len(v) for v in all_gt.values())\ntotal_pred = sum(len(v) for v in all_pred.values())\nprint("Object Detection Evaluation:")\nprint(f"GT boxes: {total_gt}, Predictions: {total_pred}")\n\nprint("\\nPer-class AP@0.5:")\naps = []\ntotal_tp, total_fp, total_fn = 0, 0, 0\nfor c in range(3):\n    ap, tp, fp, fn = evaluate_class(all_gt[c], all_pred[c])\n    aps.append(ap)\n    total_tp += tp\n    total_fp += fp\n    total_fn += fn\n    print(f"  {class_names[c]}: {ap:.4f} (TP={tp}, FP={fp}, FN={fn})")\n\nmap50 = np.mean(aps)\nprint(f"\\nmAP@0.5: {map50:.4f}")\nprint(f"TP: {total_tp}, FP: {total_fp}, FN: {total_fn}")',
      explanation: 'mAP — стандартная метрика object detection. Для каждого класса: сортируем predictions по confidence, считаем TP/FP через IoU matching, строим PR-кривую, вычисляем AP как площадь под кривой. mAP — среднее AP по классам. COCO metric mAP@0.5:0.95 усредняет по нескольким порогам IoU.'
    },
    {
      id: 10,
      title: 'Практика: Real-time detection с веб-камеры',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создайте real-time object detection систему с OpenCV и YOLOv8 для обработки видеопотока.',
      requirements: [
        'Напишите скрипт для захвата видео с веб-камеры через OpenCV (cv2.VideoCapture)',
        'Реализуйте YOLO inference на каждом кадре с отрисовкой bounding boxes',
        'Добавьте FPS counter и отображение на кадре',
        'Реализуйте фильтрацию по классам и минимальному confidence',
        'Продемонстрируйте код (симуляция без реальной камеры) и выведите FPS статистику'
      ],
      hint: 'cv2.VideoCapture(0) — камера, cv2.rectangle для boxes, cv2.putText для текста. Для FPS: измеряйте время каждого кадра. Для ускорения: imgsz=320, conf=0.5.',
      expectedOutput: 'Real-time Object Detection:\nСимуляция 30 кадров (640x480)\n\nFrame 1: 3 objects, time=XXms\nFrame 2: 2 objects, time=XXms\n...\n\nСтатистика:\n  Avg FPS: XX.X\n  Avg objects/frame: X.X\n  Avg inference time: XXms',
      solution: 'import numpy as np\nimport time\n\n# Симуляция real-time detection\nclass RealtimeDetector:\n    def __init__(self, model_name="yolov8n", conf_threshold=0.5,\n                 target_classes=None):\n        self.conf_threshold = conf_threshold\n        self.target_classes = target_classes\n        self.class_names = ["person", "bicycle", "car", "motorcycle", "bus",\n                            "truck", "traffic light", "dog", "cat", "chair"]\n        self.colors = {name: tuple(np.random.randint(0, 255, 3).tolist())\n                       for name in self.class_names}\n\n    def detect(self, frame):\n        """Симуляция inference."""\n        h, w = frame.shape[:2]\n        n_objects = np.random.randint(1, 6)\n        detections = []\n        for _ in range(n_objects):\n            cls_id = np.random.randint(0, len(self.class_names))\n            cls_name = self.class_names[cls_id]\n            if self.target_classes and cls_name not in self.target_classes:\n                continue\n            conf = np.random.uniform(0.3, 0.99)\n            if conf < self.conf_threshold:\n                continue\n            x1 = np.random.randint(0, w-100)\n            y1 = np.random.randint(0, h-100)\n            x2 = x1 + np.random.randint(50, min(200, w-x1))\n            y2 = y1 + np.random.randint(50, min(200, h-y1))\n            detections.append({\n                "bbox": [x1, y1, x2, y2], "conf": conf,\n                "class": cls_name, "color": self.colors[cls_name]\n            })\n        return detections\n\n    def draw_detections(self, frame, detections):\n        """Отрисовка boxes (симуляция)."""\n        for det in detections:\n            x1, y1, x2, y2 = det["bbox"]\n            label = f\'{det["class"]} {det["conf"]:.2f}\'\n            # cv2.rectangle(frame, (x1,y1), (x2,y2), det["color"], 2)\n            # cv2.putText(frame, label, (x1,y1-10), cv2.FONT_HERSHEY_SIMPLEX, 0.5, det["color"], 2)\n        return frame\n\n# Симуляция видеопотока\ndetector = RealtimeDetector(conf_threshold=0.5)\nn_frames = 30\nframe_w, frame_h = 640, 480\nfps_list = []\nobjects_list = []\n\nprint("Real-time Object Detection:")\nprint(f"Симуляция {n_frames} кадров ({frame_w}x{frame_h})")\nprint()\n\nfor i in range(n_frames):\n    frame = np.random.randint(0, 255, (frame_h, frame_w, 3), dtype=np.uint8)\n    start = time.time()\n    detections = detector.detect(frame)\n    frame = detector.draw_detections(frame, detections)\n    elapsed = time.time() - start\n    elapsed_ms = max(elapsed * 1000, np.random.uniform(15, 40))\n    fps = 1000 / elapsed_ms\n    fps_list.append(fps)\n    objects_list.append(len(detections))\n    if i < 5 or i == n_frames - 1:\n        print(f"Frame {i+1}: {len(detections)} objects, time={elapsed_ms:.1f}ms")\n    elif i == 5:\n        print("...")\n\nprint(f"\\nСтатистика:")\nprint(f"  Avg FPS: {np.mean(fps_list):.1f}")\nprint(f"  Avg objects/frame: {np.mean(objects_list):.1f}")\nprint(f"  Avg inference time: {1000/np.mean(fps_list):.1f}ms")\n\n# Полный скрипт для реального использования\nrealtime_code = """\nimport cv2\nfrom ultralytics import YOLO\nimport time\n\nmodel = YOLO("yolov8n.pt")\ncap = cv2.VideoCapture(0)\n\nwhile cap.isOpened():\n    ret, frame = cap.read()\n    if not ret: break\n    start = time.time()\n    results = model(frame, conf=0.5, verbose=False)\n    annotated = results[0].plot()\n    fps = 1 / (time.time() - start)\n    cv2.putText(annotated, f"FPS: {fps:.1f}", (10, 30),\n                cv2.FONT_HERSHEY_SIMPLEX, 1, (0, 255, 0), 2)\n    cv2.imshow("YOLOv8 Detection", annotated)\n    if cv2.waitKey(1) & 0xFF == ord("q"): break\n\ncap.release()\ncv2.destroyAllWindows()\n"""\nprint(f"\\nСкрипт для реальной камеры: {len(realtime_code)} символов")',
      explanation: 'Real-time detection: VideoCapture → frame → YOLO inference → draw boxes → display. Для оптимизации: используйте yolov8n (nano), imgsz=320-640, conf threshold 0.5+, GPU inference. YOLOv8n на GPU достигает 80+ FPS, на CPU — 10-15 FPS.'
    }
  ]
}
