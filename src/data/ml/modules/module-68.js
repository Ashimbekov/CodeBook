export default {
  id: 68,
  title: 'RLHF и Alignment',
  description: 'Reinforcement Learning from Human Feedback: SFT, LoRA/QLoRA, reward model, PPO, DPO. Alignment больших языковых моделей.',
  lessons: [
    {
      id: 1,
      title: 'Что такое RLHF',
      type: 'theory',
      content: [
        { type: 'heading', value: 'RLHF — Reinforcement Learning from Human Feedback' },
        { type: 'text', value: 'RLHF — метод дообучения LLM на основе человеческих предпочтений. Проблема: LLM после pre-training генерирует текст, но не обязательно полезный, безопасный или следующий инструкциям. RLHF решает alignment problem — выравнивание поведения модели с человеческими ценностями. Три этапа: SFT (supervised fine-tuning на инструкциях), обучение reward model на предпочтениях, оптимизация LLM через PPO.' },
        { type: 'code', language: 'python', value: 'import numpy as np\n\nprint("=== RLHF Pipeline ===")\nprint("""\n┌────────────────────────────────────────────────┐\n│              Этап 1: SFT                       │\n│  Pre-trained LLM -> Fine-tune на инструкциях   │\n│  Data: (instruction, response) пары             │\n│  Результат: модель следует инструкциям          │\n└───────────────────┬────────────────────────────┘\n                    │\n┌───────────────────▼────────────────────────────┐\n│              Этап 2: Reward Model               │\n│  Люди ранжируют ответы: response_A > response_B │\n│  Обучаем модель предсказывать человеческие      │\n│  предпочтения: reward(prompt, response) -> R    │\n└───────────────────┬────────────────────────────┘\n                    │\n┌───────────────────▼────────────────────────────┐\n│              Этап 3: PPO                        │\n│  Оптимизируем LLM чтобы максимизировать reward  │\n│  + KL divergence penalty (не уходить далеко от  │\n│    SFT модели)                                  │\n│  objective = E[reward(x,y)] - β*KL(π||π_ref)    │\n└────────────────────────────────────────────────┘\n""")\n\n# Alignment Problem\nprint("=== Alignment Problem ===")\nprint("""\nБез alignment LLM может:\n  ❌ Генерировать вредный контент\n  ❌ Галлюцинировать (уверенно врать)\n  ❌ Не следовать инструкциям\n  ❌ Быть многословным и неинформативным\n\nС RLHF LLM:\n  ✓ Следует инструкциям\n  ✓ Отказывает в вредных запросах\n  ✓ Более точна и полезна\n  ✓ Признаёт неуверенность\n""")\n\n# Reward Model математика\nprint("=== Reward Model ===")\nprint("Bradley-Terry модель предпочтений:")\nprint("P(y_w > y_l | x) = sigmoid(r(x, y_w) - r(x, y_l))")\nprint("")\nprint("y_w = preferred (winning) response")\nprint("y_l = rejected (losing) response")\nprint("r(x, y) = reward model score")\nprint("")\nprint("Loss = -E[log sigmoid(r(x, y_w) - r(x, y_l))]")\n\n# Пример reward scores\nnp.random.seed(42)\nprompt = "Как приготовить кофе?"\nresponses = [\n    ("Налейте кипяток в чашку с кофе. Добавьте сахар по вкусу.", 0.85),\n    ("Кофе готовится путём заваривания.", 0.45),\n    ("Я не знаю.", 0.10),\n    ("Возьмите свежемолотый кофе, нагрейте воду до 93°C, залейте кофе водой в пропорции 1:15, настаивайте 4 минуты.", 0.95),\n]\n\nprint(f"\\nПример: \\"{prompt}\\"")\nfor resp, score in sorted(responses, key=lambda x: -x[1]):\n    bar = "█" * int(score * 20)\n    print(f"  [{score:.2f}] {bar} {resp[:60]}")\n\n# PPO objective\nprint("\\n=== PPO Objective ===")\nprint("maximize E_{x~D, y~π}[r(x,y)] - β * KL(π || π_ref)")\nprint("")\nprint("r(x,y) — reward model score (хотим максимизировать)")\nprint("KL(π || π_ref) — отклонение от SFT модели (хотим минимизировать)")\nprint("β — коэффициент KL penalty (обычно 0.01-0.1)")\nprint("")\nprint("Без KL penalty модель \'reward hacks\' — находит")\nprint("лазейки для высокого reward без реального качества")' },
        { type: 'note', value: 'RLHF использовался для обучения ChatGPT, Claude, Llama 2-Chat. Ключевой инсайт: проще показать людям два ответа и спросить "какой лучше?", чем просить писать идеальные ответы. KL penalty критически важен — без него модель быстро деградирует.' }
      ]
    },
    {
      id: 2,
      title: 'SFT — Supervised Fine-Tuning',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Instruction Tuning: первый этап RLHF' },
        { type: 'text', value: 'SFT (Supervised Fine-Tuning) — дообучение LLM на парах (instruction, response). Это превращает base model (просто предсказывающую следующий токен) в assistant model (следующую инструкциям). Ключевые датасеты: Alpaca (52K), ShareGPT (90K), OASST (10K), UltraChat (1.5M). Формат данных: chat template с ролями system/user/assistant.' },
        { type: 'code', language: 'python', value: 'import json\nimport numpy as np\n\nprint("=== SFT: Instruction Tuning ===")\n\n# Формат данных для SFT\nprint("--- Форматы данных ---")\n\n# 1. Alpaca format\nalpaca_example = {\n    "instruction": "Объясни разницу между списком и кортежем в Python",\n    "input": "",\n    "output": "Список (list) — изменяемая коллекция, создаётся через []. "\n             "Кортеж (tuple) — неизменяемая коллекция, создаётся через (). "\n             "Списки медленнее, но можно добавлять/удалять элементы. "\n             "Кортежи быстрее и используются как ключи словарей."\n}\nprint("1. Alpaca format:")\nprint(json.dumps(alpaca_example, ensure_ascii=False, indent=2))\n\n# 2. Chat format (ShareGPT)\nchat_example = {\n    "conversations": [\n        {"from": "system", "value": "Ты — полезный AI-ассистент."},\n        {"from": "human", "value": "Что такое list comprehension?"},\n        {"from": "gpt", "value": "List comprehension — краткий способ создания списков в Python:\\n"\n                                  "squares = [x**2 for x in range(10)]\\n"\n                                  "Эквивалент цикла for с append, но компактнее и быстрее."}\n    ]\n}\nprint("\\n2. Chat format (ShareGPT):")\nprint(json.dumps(chat_example, ensure_ascii=False, indent=2))\n\n# Chat Template\nprint("\\n--- Chat Templates ---")\ntemplates = {\n    "Llama 2": (\n        "[INST] <<SYS>>\\n{system}\\n<</SYS>>\\n\\n{user} [/INST] {assistant}"\n    ),\n    "ChatML": (\n        "<|im_start|>system\\n{system}<|im_end|>\\n"\n        "<|im_start|>user\\n{user}<|im_end|>\\n"\n        "<|im_start|>assistant\\n{assistant}<|im_end|>"\n    ),\n    "Vicuna": (\n        "USER: {user}\\nASSISTANT: {assistant}"\n    ),\n}\n\nfor name, template in templates.items():\n    print(f"\\n{name}:")\n    print(f"  {template}")\n\n# SFT Training\nprint("\\n--- SFT Training Pipeline ---")\nprint("""\nfrom transformers import AutoModelForCausalLM, AutoTokenizer, TrainingArguments\nfrom trl import SFTTrainer\nfrom datasets import load_dataset\n\n# 1. Загрузка модели\nmodel = AutoModelForCausalLM.from_pretrained("meta-llama/Llama-2-7b-hf")\ntokenizer = AutoTokenizer.from_pretrained("meta-llama/Llama-2-7b-hf")\n\n# 2. Загрузка данных\ndataset = load_dataset("tatsu-lab/alpaca", split="train")\n\n# 3. Formatting function\ndef format_prompt(example):\n    if example["input"]:\n        text = f"### Instruction:\\n{example[\'instruction\']}\\n\\n### Input:\\n{example[\'input\']}\\n\\n### Response:\\n{example[\'output\']}"\n    else:\n        text = f"### Instruction:\\n{example[\'instruction\']}\\n\\n### Response:\\n{example[\'output\']}"\n    return {"text": text}\n\ndataset = dataset.map(format_prompt)\n\n# 4. Обучение\ntrainer = SFTTrainer(\n    model=model,\n    train_dataset=dataset,\n    dataset_text_field="text",\n    max_seq_length=2048,\n    args=TrainingArguments(\n        output_dir="./sft_model",\n        num_train_epochs=3,\n        per_device_train_batch_size=4,\n        gradient_accumulation_steps=4,\n        learning_rate=2e-5,\n        warmup_ratio=0.03,\n        fp16=True,\n        logging_steps=10,\n        save_strategy="epoch",\n    )\n)\ntrainer.train()\n""")\n\nprint("\\n--- Ключевые датасеты для SFT ---")\ndatasets_info = [\n    ("Alpaca", "52K", "Stanford", "Сгенерирован GPT-3.5"),\n    ("ShareGPT", "90K", "Community", "Реальные диалоги с ChatGPT"),\n    ("OASST", "10K", "Open Assistant", "Human-written, multilingual"),\n    ("UltraChat", "1.5M", "Tsinghua", "Сгенерирован, diverse"),\n    ("Dolly", "15K", "Databricks", "Human-written, commercial use"),\n]\nfor name, size, source, desc in datasets_info:\n    print(f"  {name:12s} ({size:>5s}): {desc} [{source}]")' },
        { type: 'tip', value: 'Для SFT качество данных важнее количества. 10K высококачественных пар (OASST) часто лучше 100K шумных. Chat template должен точно совпадать с тем, который использовался при pre-training. Пакуйте короткие примеры вместе (packing) для эффективности.' }
      ]
    },
    {
      id: 3,
      title: 'Практика: Fine-tuning с LoRA',
      type: 'practice',
      difficulty: 'medium',
      description: 'Дообучите LLM с помощью LoRA/QLoRA для следования инструкциям.',
      requirements: [
        'Загрузите базовую модель с 4-bit квантизацией (QLoRA)',
        'Настройте LoRA конфигурацию (r, alpha, target_modules, dropout)',
        'Подготовьте датасет инструкций в правильном chat формате',
        'Запустите обучение через SFTTrainer с правильными гиперпараметрами',
        'Выведите количество trainable параметров и конфигурацию'
      ],
      hint: 'BitsAndBytesConfig(load_in_4bit=True, bnb_4bit_quant_type="nf4") для QLoRA. LoraConfig(r=16, lora_alpha=32, target_modules=["q_proj","v_proj","k_proj","o_proj"]). learning_rate=2e-4 для LoRA.',
      expectedOutput: 'QLoRA Fine-tuning Setup:\n  Model: TinyLlama-1.1B\n  Quantization: 4-bit NF4\n  LoRA rank: 16\n  Trainable params: X,XXX,XXX (X.XX%)\n\nDataset:\n  Format: ChatML\n  Examples: 100\n\nTraining config:\n  Epochs: 3\n  Batch size: 4\n  LR: 2e-4\n  Gradient accumulation: 4',
      solution: 'import torch\nfrom transformers import (\n    AutoModelForCausalLM, AutoTokenizer,\n    BitsAndBytesConfig, TrainingArguments\n)\nfrom peft import LoraConfig, get_peft_model, TaskType, prepare_model_for_kbit_training\nfrom trl import SFTTrainer\nfrom datasets import Dataset\n\n# === 1. Загрузка модели с QLoRA ===\nmodel_name = "TinyLlama/TinyLlama-1.1B-Chat-v1.0"\n\nbnb_config = BitsAndBytesConfig(\n    load_in_4bit=True,\n    bnb_4bit_quant_type="nf4",\n    bnb_4bit_compute_dtype=torch.float16,\n    bnb_4bit_use_double_quant=True\n)\n\ntokenizer = AutoTokenizer.from_pretrained(model_name)\ntokenizer.pad_token = tokenizer.eos_token\ntokenizer.padding_side = "right"\n\nmodel = AutoModelForCausalLM.from_pretrained(\n    model_name,\n    quantization_config=bnb_config,\n    device_map="auto"\n)\nmodel = prepare_model_for_kbit_training(model)\n\nprint("QLoRA Fine-tuning Setup:")\nprint(f"  Model: {model_name}")\nprint(f"  Quantization: 4-bit NF4")\n\n# === 2. LoRA конфигурация ===\nlora_config = LoraConfig(\n    r=16,\n    lora_alpha=32,\n    target_modules=["q_proj", "v_proj", "k_proj", "o_proj", "gate_proj", "up_proj", "down_proj"],\n    lora_dropout=0.05,\n    bias="none",\n    task_type=TaskType.CAUSAL_LM\n)\n\nmodel = get_peft_model(model, lora_config)\n\ntrainable = sum(p.numel() for p in model.parameters() if p.requires_grad)\ntotal = sum(p.numel() for p in model.parameters())\nprint(f"  LoRA rank: {lora_config.r}")\nprint(f"  LoRA alpha: {lora_config.lora_alpha}")\nprint(f"  Target modules: {lora_config.target_modules}")\nprint(f"  Trainable params: {trainable:,} ({trainable/total*100:.2f}%)")\nprint(f"  Total params: {total:,}")\n\n# === 3. Подготовка данных ===\ndef create_dataset():\n    examples = [\n        {"instruction": "Что такое машинное обучение?",\n         "response": "Машинное обучение — раздел ИИ, где алгоритмы обучаются на данных и улучшают свою производительность без явного программирования."},\n        {"instruction": "Напиши функцию сортировки пузырьком на Python",\n         "response": "def bubble_sort(arr):\\n    n = len(arr)\\n    for i in range(n):\\n        for j in range(0, n-i-1):\\n            if arr[j] > arr[j+1]:\\n                arr[j], arr[j+1] = arr[j+1], arr[j]\\n    return arr"},\n        {"instruction": "Объясни разницу между стеком и очередью",\n         "response": "Стек (Stack) — LIFO: последний пришёл, первый вышел. Очередь (Queue) — FIFO: первый пришёл, первый вышел."},\n    ] * 34  # 102 примера\n    \n    texts = []\n    for ex in examples:\n        text = f"<|im_start|>system\\nТы полезный AI-ассистент.<|im_end|>\\n<|im_start|>user\\n{ex[\'instruction\']}<|im_end|>\\n<|im_start|>assistant\\n{ex[\'response\']}<|im_end|>"\n        texts.append(text)\n    \n    return Dataset.from_dict({"text": texts})\n\ndataset = create_dataset()\nprint(f"\\nDataset:")\nprint(f"  Format: ChatML")\nprint(f"  Examples: {len(dataset)}")\n\n# === 4. Обучение ===\ntraining_args = TrainingArguments(\n    output_dir="./qlora_sft",\n    num_train_epochs=3,\n    per_device_train_batch_size=4,\n    gradient_accumulation_steps=4,\n    learning_rate=2e-4,\n    warmup_ratio=0.03,\n    fp16=True,\n    logging_steps=10,\n    save_strategy="epoch",\n    optim="paged_adamw_8bit",\n    max_grad_norm=0.3,\n    lr_scheduler_type="cosine",\n)\n\nprint(f"\\nTraining config:")\nprint(f"  Epochs: {training_args.num_train_epochs}")\nprint(f"  Batch size: {training_args.per_device_train_batch_size}")\nprint(f"  LR: {training_args.learning_rate}")\nprint(f"  Gradient accumulation: {training_args.gradient_accumulation_steps}")\nprint(f"  Optimizer: {training_args.optim}")\n\ntrainer = SFTTrainer(\n    model=model,\n    train_dataset=dataset,\n    dataset_text_field="text",\n    max_seq_length=1024,\n    args=training_args,\n    tokenizer=tokenizer,\n)\n\n# trainer.train()  # раскомментировать для обучения\nprint("\\nГотово к обучению! Вызовите trainer.train()")\nprint("\\nПосле обучения:")\nprint("  model.save_pretrained(\'./qlora_adapter\')")\nprint("  # Загрузка: model = PeftModel.from_pretrained(base_model, \'./qlora_adapter\')")',
      explanation: 'QLoRA = 4-bit квантизация (NF4) + LoRA адаптеры. Позволяет fine-tune модель 7B на GPU с 16GB VRAM. Ключевые параметры: r=16 (rank LoRA матриц), lora_alpha=32 (scaling), target_modules (какие слои адаптируем). paged_adamw_8bit экономит ещё больше памяти. После обучения сохраняются только LoRA веса (~50MB вместо 14GB).'
    },
    {
      id: 4,
      title: 'Reward Model',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Reward Model: обучение на предпочтениях' },
        { type: 'text', value: 'Reward Model (RM) — нейросеть, предсказывающая, какой ответ человек предпочтёт. Обучается на данных вида (prompt, chosen_response, rejected_response). Использует Bradley-Terry модель: P(chosen > rejected) = sigmoid(r(chosen) - r(rejected)). RM обычно основана на той же архитектуре, что и LLM, с regression head вместо language model head.' },
        { type: 'code', language: 'python', value: 'import numpy as np\nimport torch\nimport torch.nn as nn\n\nprint("=== Reward Model ===")\n\n# Архитектура\nprint("""\nReward Model:\n┌──────────────┐\n│  LLM Backbone │  (замороженный или fine-tuned)\n│  (Llama 7B)   │\n└──────┬───────┘\n       │ hidden state последнего токена\n       ▼\n┌──────────────┐\n│  Linear Head  │  hidden_dim -> 1\n│  (Trainable)  │\n└──────┬───────┘\n       │\n       ▼\n    scalar reward r(x, y)\n""")\n\n# Bradley-Terry модель\nprint("=== Bradley-Terry Model ===")\nprint("P(y_w ≻ y_l | x) = σ(r(x, y_w) - r(x, y_l))")\nprint("Loss = -log σ(r(x, y_w) - r(x, y_l))")\nprint()\n\n# Симуляция обучения RM\nnp.random.seed(42)\n\nclass SimpleRewardModel(nn.Module):\n    def __init__(self, input_dim=64):\n        super().__init__()\n        self.backbone = nn.Sequential(\n            nn.Linear(input_dim, 128),\n            nn.ReLU(),\n            nn.Linear(128, 64),\n            nn.ReLU(),\n        )\n        self.reward_head = nn.Linear(64, 1)\n    \n    def forward(self, x):\n        features = self.backbone(x)\n        reward = self.reward_head(features)\n        return reward.squeeze(-1)\n\n# Создаём синтетические данные предпочтений\nn_pairs = 1000\ndim = 64\n\n# Chosen responses (лучшие)\nchosen_embs = torch.randn(n_pairs, dim) + 0.5  # сдвиг = "лучше"\nrejected_embs = torch.randn(n_pairs, dim) - 0.5  # сдвиг = "хуже"\n\n# Обучение\nmodel = SimpleRewardModel(dim)\noptimizer = torch.optim.Adam(model.parameters(), lr=1e-3)\n\nprint("Training Reward Model:")\nfor epoch in range(5):\n    r_chosen = model(chosen_embs)\n    r_rejected = model(rejected_embs)\n    \n    # Bradley-Terry loss\n    loss = -torch.log(torch.sigmoid(r_chosen - r_rejected)).mean()\n    \n    optimizer.zero_grad()\n    loss.backward()\n    optimizer.step()\n    \n    accuracy = ((r_chosen > r_rejected).float().mean().item())\n    print(f"  Epoch {epoch+1}: loss={loss.item():.4f}, accuracy={accuracy:.4f}")\n\n# Оценка\nwith torch.no_grad():\n    test_chosen = model(torch.randn(100, dim) + 0.5)\n    test_rejected = model(torch.randn(100, dim) - 0.5)\n    test_acc = (test_chosen > test_rejected).float().mean()\n    print(f"\\nTest accuracy: {test_acc:.4f}")\n    print(f"Mean reward (chosen): {test_chosen.mean():.4f}")\n    print(f"Mean reward (rejected): {test_rejected.mean():.4f}")\n\n# Human Preference Collection\nprint("\\n=== Сбор предпочтений ===")\nprint("Процесс:")\nprint("  1. Генерируем K ответов на каждый промпт (K=4-8)")\nprint("  2. Показываем пары ответов аннотаторам")\nprint("  3. Аннотаторы выбирают лучший ответ")\nprint("  4. Формируем пары (chosen, rejected)")\nprint("  5. Обучаем RM на этих парах")\nprint(f"\\nДатасеты предпочтений:")\nprint("  Anthropic HH-RLHF: 170K пар (helpfulness + harmlessness)")\nprint("  OpenAI WebGPT: 20K comparisons")\nprint("  UltraFeedback: 64K prompts x 4 responses = 256K")' },
        { type: 'note', value: 'Качество Reward Model — bottleneck всего RLHF pipeline. Плохой RM ведёт к reward hacking: модель находит лазейки для высокого reward без реального улучшения качества. Inter-annotator agreement обычно ~70-80%, что ограничивает потолок RM.' }
      ]
    },
    {
      id: 5,
      title: 'Практика: Обучение reward model',
      type: 'practice',
      difficulty: 'hard',
      description: 'Обучите reward model на синтетических данных предпочтений для ранжирования ответов LLM.',
      requirements: [
        'Создайте датасет предпочтений: 500+ пар (prompt, chosen, rejected) с текстовыми признаками',
        'Реализуйте RewardModel на основе простой сети (или используйте LLM backbone)',
        'Обучите модель с Bradley-Terry loss',
        'Оцените accuracy на тестовой выборке',
        'Покажите ранжирование 5+ ответов по reward score для нового промпта'
      ],
      hint: 'Для простоты используйте TF-IDF embeddings текстов как входы. Bradley-Terry loss: -log(sigmoid(r_chosen - r_rejected)). Хорошая RM: accuracy > 70%.',
      expectedOutput: 'Reward Model Training:\n  Dataset: 500 preference pairs\n  Train/Test: 400/100\n\nTraining:\n  Epoch 1: loss=0.XX, acc=0.XX\n  ...\n  Epoch 10: loss=0.XX, acc=0.XX\n\nTest accuracy: 0.XX\n\nRanking example:\n  Prompt: "Что такое Python?"\n  1. [1.25] "Python — язык программирования..." ✓\n  2. [0.73] "Это язык для ML" \n  3. [-0.12] "Не знаю"',
      solution: 'import numpy as np\nimport torch\nimport torch.nn as nn\nfrom sklearn.feature_extraction.text import TfidfVectorizer\nfrom sklearn.model_selection import train_test_split\n\nnp.random.seed(42)\ntorch.manual_seed(42)\n\n# === 1. Создание датасета предпочтений ===\nprompts = [\n    "Что такое машинное обучение?",\n    "Как работает нейронная сеть?",\n    "Объясни градиентный спуск",\n    "Что такое переобучение?",\n    "Зачем нужна регуляризация?",\n] * 100  # 500 примеров\n\n# Chosen: подробные, точные ответы\nchosen_templates = [\n    "Машинное обучение — раздел ИИ, в котором алгоритмы автоматически улучшаются на основе опыта и данных. Включает supervised, unsupervised и reinforcement learning.",\n    "Нейронная сеть состоит из слоёв нейронов, связанных весами. Входные данные проходят через скрытые слои с нелинейными активациями. Обучение через backpropagation.",\n    "Градиентный спуск — итеративный алгоритм оптимизации. На каждом шаге вычисляем градиент функции потерь и обновляем параметры в направлении антиградиента.",\n    "Переобучение — когда модель слишком хорошо запоминает обучающие данные, но плохо обобщает на новых. Признак: высокий train accuracy, низкий test accuracy.",\n    "Регуляризация предотвращает переобучение, добавляя штраф за сложность модели. L1 (Lasso) даёт разреженные веса, L2 (Ridge) уменьшает веса.",\n] * 100\n\n# Rejected: краткие, неинформативные ответы\nrejected_templates = [\n    "Это про данные и компьютеры.",\n    "Она учится на данных.",\n    "Это оптимизация.",\n    "Это плохо для модели.",\n    "Чтобы модель работала лучше.",\n] * 100\n\n# TF-IDF embeddings\nall_texts = chosen_templates + rejected_templates + prompts\nvectorizer = TfidfVectorizer(max_features=200)\nvectorizer.fit(all_texts)\n\nprompt_embs = vectorizer.transform(prompts).toarray()\nchosen_embs = vectorizer.transform(chosen_templates).toarray()\nrejected_embs = vectorizer.transform(rejected_templates).toarray()\n\n# Конкатенируем prompt + response\nchosen_features = np.concatenate([prompt_embs, chosen_embs], axis=1)\nrejected_features = np.concatenate([prompt_embs, rejected_embs], axis=1)\n\ninput_dim = chosen_features.shape[1]\n\n# Train/test split\nidx = np.arange(len(chosen_features))\ntrain_idx, test_idx = train_test_split(idx, test_size=0.2, random_state=42)\n\nprint("Reward Model Training:")\nprint(f"  Dataset: {len(prompts)} preference pairs")\nprint(f"  Feature dim: {input_dim}")\nprint(f"  Train/Test: {len(train_idx)}/{len(test_idx)}")\n\n# === 2. Reward Model ===\nclass RewardModel(nn.Module):\n    def __init__(self, input_dim):\n        super().__init__()\n        self.net = nn.Sequential(\n            nn.Linear(input_dim, 256),\n            nn.ReLU(),\n            nn.Dropout(0.1),\n            nn.Linear(256, 128),\n            nn.ReLU(),\n            nn.Dropout(0.1),\n            nn.Linear(128, 1)\n        )\n    \n    def forward(self, x):\n        return self.net(x).squeeze(-1)\n\nmodel = RewardModel(input_dim)\noptimizer = torch.optim.Adam(model.parameters(), lr=1e-3)\n\n# === 3. Обучение ===\nchosen_train = torch.FloatTensor(chosen_features[train_idx])\nrejected_train = torch.FloatTensor(rejected_features[train_idx])\nchosen_test = torch.FloatTensor(chosen_features[test_idx])\nrejected_test = torch.FloatTensor(rejected_features[test_idx])\n\nprint("\\nTraining:")\nfor epoch in range(10):\n    model.train()\n    r_chosen = model(chosen_train)\n    r_rejected = model(rejected_train)\n    \n    loss = -torch.log(torch.sigmoid(r_chosen - r_rejected) + 1e-8).mean()\n    \n    optimizer.zero_grad()\n    loss.backward()\n    optimizer.step()\n    \n    with torch.no_grad():\n        acc = (r_chosen > r_rejected).float().mean().item()\n    print(f"  Epoch {epoch+1:2d}: loss={loss.item():.4f}, acc={acc:.4f}")\n\n# === 4. Оценка ===\nmodel.eval()\nwith torch.no_grad():\n    r_ch = model(chosen_test)\n    r_rj = model(rejected_test)\n    test_acc = (r_ch > r_rj).float().mean().item()\n\nprint(f"\\nTest accuracy: {test_acc:.4f}")\n\n# === 5. Ранжирование ===\nprint("\\nRanking example:")\ntest_prompt = "Что такое Python?"\ncandidates = [\n    "Python — высокоуровневый язык программирования общего назначения с динамической типизацией и автоматическим управлением памятью.",\n    "Python используется для ML, web-разработки и автоматизации.",\n    "Это язык программирования.",\n    "Не знаю.",\n    "Python создан Гвидо ван Россумом в 1991 году. Основные принципы: читаемость, простота, \'batteries included\'.",\n]\n\ntest_prompt_emb = vectorizer.transform([test_prompt]).toarray()\nscores = []\nfor cand in candidates:\n    cand_emb = vectorizer.transform([cand]).toarray()\n    features = np.concatenate([test_prompt_emb, cand_emb], axis=1)\n    with torch.no_grad():\n        score = model(torch.FloatTensor(features)).item()\n    scores.append(score)\n\nprint(f"  Prompt: \\"{test_prompt}\\"")\nfor score, cand in sorted(zip(scores, candidates), reverse=True):\n    print(f"  [{score:+.2f}] {cand[:70]}")',
      explanation: 'Reward Model обучается различать хорошие и плохие ответы через Bradley-Terry loss. TF-IDF embeddings — простой способ представления текста для демонстрации. В production используется LLM backbone (Llama, Mistral) с regression head. Accuracy > 70% считается приемлемой, учитывая уровень согласия между людьми.'
    },
    {
      id: 6,
      title: 'PPO для LLM',
      type: 'theory',
      content: [
        { type: 'heading', value: 'PPO: Proximal Policy Optimization для LLM' },
        { type: 'text', value: 'PPO (Proximal Policy Optimization) — алгоритм reinforcement learning, используемый для оптимизации LLM с reward model. LLM рассматривается как policy, генерирующая текст (action) по промпту (state). PPO обновляет policy чтобы максимизировать reward, ограничивая размер обновлений через clipping. KL divergence penalty предотвращает отдаление от SFT модели.' },
        { type: 'code', language: 'python', value: 'import numpy as np\n\nprint("=== PPO для LLM ===")\nprint("""\nRL формулировка LLM:\n  State (s): prompt x\n  Action (a): сгенерированный ответ y (последовательность токенов)\n  Policy π(y|x): LLM (вероятности следующего токена)\n  Reward r(x, y): reward model score\n  Reference policy π_ref: SFT модель (фиксированная)\n\nObjective:\n  max_π E_{x~D, y~π}[r(x,y)] - β * KL(π || π_ref)\n\nPPO Clipped Objective:\n  L_clip = min(ratio * A, clip(ratio, 1-ε, 1+ε) * A)\n  ratio = π(y|x) / π_old(y|x)\n  A = advantage (reward - baseline)\n""")\n\n# PPO Algorithm\nprint("=== PPO Algorithm для LLM ===")\nprint("""\nfor iteration in range(num_iterations):\n    # 1. Collect rollouts\n    for prompt in batch:\n        response = policy.generate(prompt)  # sample from π\n        reward = reward_model(prompt, response)\n        ref_logprob = ref_model.log_prob(prompt, response)  # π_ref\n        policy_logprob = policy.log_prob(prompt, response)  # π\n    \n    # 2. Compute advantages\n    kl_penalty = policy_logprob - ref_logprob  # per-token KL\n    modified_reward = reward - β * kl_penalty.sum()\n    advantages = compute_gae(modified_reward, values)\n    \n    # 3. PPO update (multiple epochs on same batch)\n    for epoch in range(ppo_epochs):\n        ratio = exp(new_logprob - old_logprob)\n        clipped_ratio = clip(ratio, 1-ε, 1+ε)\n        policy_loss = -min(ratio * advantages, clipped_ratio * advantages)\n        value_loss = MSE(values, returns)\n        loss = policy_loss + 0.5 * value_loss\n        loss.backward()\n        optimizer.step()\n""")\n\n# KL Divergence\nprint("=== KL Divergence ===")\nprint("KL(π || π_ref) = E_π[log π(y|x) - log π_ref(y|x)]")\nprint()\n\n# Симуляция PPO обновления\nnp.random.seed(42)\nprint("Симуляция PPO:")\n\n# Начальные "rewards" от SFT модели\ninitial_reward = 0.5\nrewards_history = [initial_reward]\nkl_history = [0.0]\n\nbeta = 0.1  # KL penalty\nfor step in range(20):\n    reward = initial_reward + step * 0.05 + np.random.normal(0, 0.05)\n    kl = step * 0.02 + np.random.normal(0, 0.01)\n    modified_reward = reward - beta * kl\n    rewards_history.append(reward)\n    kl_history.append(kl)\n\nprint(f"  Step  0: reward={rewards_history[0]:.3f}, KL={kl_history[0]:.3f}")\nprint(f"  Step 10: reward={rewards_history[10]:.3f}, KL={kl_history[10]:.3f}")\nprint(f"  Step 20: reward={rewards_history[20]:.3f}, KL={kl_history[20]:.3f}")\n\nprint(f"\\n  Reward increase: {rewards_history[-1] - rewards_history[0]:.3f}")\nprint(f"  KL drift: {kl_history[-1]:.3f}")\nprint(f"  Modified reward: {rewards_history[-1] - beta * kl_history[-1]:.3f}")\n\n# Гиперпараметры PPO\nprint("\\n=== Ключевые гиперпараметры ===")\nhparams = {\n    "β (KL coef)": ("0.01-0.1", "Штраф за отклонение от π_ref"),\n    "ε (clip range)": ("0.2", "Ограничение размера обновления"),\n    "γ (discount)": ("1.0", "Для текста обычно без дисконта"),\n    "λ (GAE)": ("0.95", "Generalized Advantage Estimation"),\n    "PPO epochs": ("2-4", "Сколько раз обучаем на одном batch"),\n    "Batch size": ("64-256", "Промптов в одном batch"),\n    "LR": ("1e-6 - 5e-6", "Маленький для стабильности"),\n}\nfor name, (val, desc) in hparams.items():\n    print(f"  {name:20s}: {val:10s} — {desc}")' },
        { type: 'note', value: 'PPO для LLM — это 4 модели в памяти одновременно: policy (обучаемая LLM), reference (SFT модель, frozen), reward model (frozen), value head (обучаемый). Это требует огромных ресурсов. DPO решает эту проблему, обходясь без reward model и RL.' }
      ]
    },
    {
      id: 7,
      title: 'Практика: RLHF с TRL',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте RLHF pipeline с использованием библиотеки TRL (Transformer Reinforcement Learning).',
      requirements: [
        'Настройте PPOTrainer из библиотеки trl с конфигурацией PPOConfig',
        'Подготовьте модель (policy), reference model и reward model/function',
        'Реализуйте training loop: generate -> reward -> PPO step',
        'Логируйте reward, KL divergence и loss на каждом шаге',
        'Покажите конфигурацию и структуру pipeline'
      ],
      hint: 'from trl import PPOTrainer, PPOConfig, AutoModelForCausalLMWithValueHead. PPOConfig(batch_size=16, learning_rate=1e-5, ppo_epochs=4). Reward function может быть простой (длина, keywords) для демо.',
      expectedOutput: 'RLHF with TRL:\n  Model: TinyLlama-1.1B\n  PPO config:\n    batch_size: 16\n    ppo_epochs: 4\n    learning_rate: 1e-5\n\nTraining:\n  Step 1: reward=0.XX, KL=0.XX\n  Step 2: reward=0.XX, KL=0.XX\n  ...\n\nReward improved: 0.XX -> 0.XX',
      solution: 'import torch\nfrom trl import PPOTrainer, PPOConfig, AutoModelForCausalLMWithValueHead\nfrom transformers import AutoTokenizer\nfrom datasets import Dataset\nimport numpy as np\n\n# === Конфигурация ===\nmodel_name = "TinyLlama/TinyLlama-1.1B-Chat-v1.0"\n\nppo_config = PPOConfig(\n    model_name=model_name,\n    batch_size=16,\n    mini_batch_size=4,\n    ppo_epochs=4,\n    learning_rate=1e-5,\n    kl_penalty="kl",\n    init_kl_coef=0.2,\n    target=6.0,\n    log_with=None,\n    accelerator_kwargs={"device_placement": False},\n)\n\nprint("RLHF with TRL:")\nprint(f"  Model: {model_name}")\nprint(f"  PPO config:")\nfor key in ["batch_size", "mini_batch_size", "ppo_epochs", "learning_rate", "init_kl_coef"]:\n    print(f"    {key}: {getattr(ppo_config, key)}")\n\n# === Загрузка модели ===\ntokenizer = AutoTokenizer.from_pretrained(model_name)\ntokenizer.pad_token = tokenizer.eos_token\n\n# Policy model с value head\nmodel = AutoModelForCausalLMWithValueHead.from_pretrained(\n    model_name,\n    torch_dtype=torch.float16,\n    device_map="auto"\n)\n\n# Reference model (frozen copy)\nref_model = AutoModelForCausalLMWithValueHead.from_pretrained(\n    model_name,\n    torch_dtype=torch.float16,\n    device_map="auto"\n)\n\n# === Dataset ===\nprompts = [\n    "Объясни что такое Python",\n    "Как работает нейронная сеть?",\n    "Что такое gradient descent?",\n    "Зачем нужна нормализация данных?",\n    "Объясни что такое overfitting",\n    "Как работает random forest?",\n    "Что такое cross-validation?",\n    "Объясни метод опорных векторов",\n] * 4  # 32 промпта\n\ndataset = Dataset.from_dict({"query": prompts})\n\ndef tokenize(example):\n    return tokenizer(example["query"], padding="max_length",\n                     max_length=64, truncation=True)\n\ndataset = dataset.map(tokenize, batched=True)\n\n# === Reward Function ===\ndef reward_fn(responses):\n    """Простая reward функция для демо:\n    - длинные ответы лучше (но не слишком)\n    - наличие ключевых слов\n    """\n    rewards = []\n    keywords = ["потому что", "например", "это", "означает", "используется"]\n    for response in responses:\n        text = response.lower()\n        length_reward = min(len(text.split()) / 50.0, 1.0)  # до 50 слов\n        keyword_reward = sum(0.1 for kw in keywords if kw in text)\n        total = length_reward + keyword_reward\n        rewards.append(torch.tensor(total))\n    return rewards\n\n# === PPO Trainer ===\nppo_trainer = PPOTrainer(\n    model=model,\n    ref_model=ref_model,\n    tokenizer=tokenizer,\n    config=ppo_config,\n    dataset=dataset,\n)\n\n# === Training Loop ===\nprint("\\nTraining:")\nreward_history = []\n\nfor step in range(5):  # 5 шагов для демо\n    # 1. Sample batch\n    batch = next(iter(ppo_trainer.dataloader))\n    query_tensors = [torch.tensor(q) for q in batch["input_ids"]]\n    \n    # 2. Generate responses\n    response_tensors = []\n    for query in query_tensors:\n        gen = ppo_trainer.generate(query, max_new_tokens=64, do_sample=True,\n                                   temperature=0.7, top_p=0.9)\n        response_tensors.append(gen.squeeze())\n    \n    # 3. Decode responses\n    responses = [tokenizer.decode(r, skip_special_tokens=True) for r in response_tensors]\n    \n    # 4. Compute rewards\n    rewards = reward_fn(responses)\n    avg_reward = np.mean([r.item() for r in rewards])\n    reward_history.append(avg_reward)\n    \n    # 5. PPO step\n    stats = ppo_trainer.step(query_tensors, response_tensors, rewards)\n    \n    kl = stats.get("objective/kl", 0)\n    print(f"  Step {step+1}: reward={avg_reward:.3f}, KL={kl:.4f}")\n\nif len(reward_history) > 1:\n    print(f"\\nReward improved: {reward_history[0]:.3f} -> {reward_history[-1]:.3f}")\n\n# Сохранение\nprint("\\nСохранение модели:")\nprint("  model.save_pretrained(\'./rlhf_model\')")\nprint("  tokenizer.save_pretrained(\'./rlhf_model\')")',
      explanation: 'TRL (Transformer Reinforcement Learning) от HuggingFace упрощает RLHF. PPOTrainer управляет policy и reference моделями, вычисляет KL penalty и выполняет PPO обновления. AutoModelForCausalLMWithValueHead добавляет value head к LLM для оценки advantage. В production reward function заменяется на обученную reward model.'
    },
    {
      id: 8,
      title: 'DPO и альтернативы RLHF',
      type: 'theory',
      content: [
        { type: 'heading', value: 'DPO: Direct Preference Optimization' },
        { type: 'text', value: 'DPO (Direct Preference Optimization, Rafailov et al. 2023) — альтернатива RLHF, которая обходится без отдельной reward model и RL. DPO переформулирует RLHF objective в supervised loss, который напрямую оптимизирует policy на данных предпочтений. Результат сравним с PPO, но проще, стабильнее и дешевле в обучении.' },
        { type: 'code', language: 'python', value: 'import torch\nimport torch.nn.functional as F\nimport numpy as np\n\nprint("=== DPO: Direct Preference Optimization ===")\nprint("""\nКлючевая идея:\nRLHF: train RM -> train policy with PPO (сложно, нестабильно)\nDPO: train policy directly on preferences (просто, стабильно)\n\nDPO Loss:\nL_DPO = -E[log σ(β * (log π(y_w|x)/π_ref(y_w|x) - log π(y_l|x)/π_ref(y_l|x)))]\n\nГде:\n  π = обучаемая policy\n  π_ref = reference (SFT) model\n  y_w = preferred response\n  y_l = rejected response\n  β = temperature (обычно 0.1-0.5)\n""")\n\n# Сравнение RLHF vs DPO\nprint("=== RLHF vs DPO ===")\nprint("""\n                    RLHF (PPO)              DPO\n─────────────────────────────────────────────────────\nМодели в памяти    4 (policy, ref,         2 (policy, ref)\n                    reward, value)          \nСложность          Высокая (RL)            Низкая (supervised)\nСтабильность       Нестабильно             Стабильно\nГиперпараметры     Много (KL, clip, LR..)  Мало (β, LR)\nКачество           ≈ Одинаковое            ≈ Одинаковое\nТребования GPU     4x модель               2x модель\nВремя обучения     Долгое                  Быстрое\n""")\n\n# Реализация DPO Loss\nprint("=== DPO Loss Implementation ===")\n\ndef dpo_loss(policy_chosen_logps, policy_rejected_logps,\n             reference_chosen_logps, reference_rejected_logps,\n             beta=0.1):\n    """\n    DPO loss function.\n    \n    Args:\n        policy_chosen_logps: log π(y_w|x) - log probs выбранных ответов\n        policy_rejected_logps: log π(y_l|x)\n        reference_chosen_logps: log π_ref(y_w|x)\n        reference_rejected_logps: log π_ref(y_l|x)\n        beta: temperature parameter\n    """\n    # Log ratios\n    chosen_logratios = policy_chosen_logps - reference_chosen_logps\n    rejected_logratios = policy_rejected_logps - reference_rejected_logps\n    \n    # DPO loss\n    logits = beta * (chosen_logratios - rejected_logratios)\n    loss = -F.logsigmoid(logits).mean()\n    \n    # Метрики\n    chosen_rewards = beta * chosen_logratios.detach()\n    rejected_rewards = beta * rejected_logratios.detach()\n    reward_accuracy = (chosen_rewards > rejected_rewards).float().mean()\n    \n    return loss, chosen_rewards.mean(), rejected_rewards.mean(), reward_accuracy\n\n# Симуляция\ntorch.manual_seed(42)\nn = 100\n\npolicy_chosen = torch.randn(n) * 0.5 - 2.0\npolicy_rejected = torch.randn(n) * 0.5 - 2.5\nref_chosen = torch.randn(n) * 0.5 - 2.0\nref_rejected = torch.randn(n) * 0.5 - 2.0\n\nloss, r_chosen, r_rejected, acc = dpo_loss(\n    policy_chosen, policy_rejected, ref_chosen, ref_rejected, beta=0.1\n)\n\nprint(f"DPO Loss: {loss.item():.4f}")\nprint(f"Mean reward (chosen): {r_chosen.item():.4f}")\nprint(f"Mean reward (rejected): {r_rejected.item():.4f}")\nprint(f"Reward accuracy: {acc.item():.4f}")\n\n# DPO с TRL\nprint("\\n=== DPO с TRL ===")\nprint("""\nfrom trl import DPOTrainer, DPOConfig\nfrom transformers import AutoModelForCausalLM, AutoTokenizer\n\nmodel = AutoModelForCausalLM.from_pretrained("meta-llama/Llama-2-7b-hf")\nref_model = AutoModelForCausalLM.from_pretrained("meta-llama/Llama-2-7b-hf")\ntokenizer = AutoTokenizer.from_pretrained("meta-llama/Llama-2-7b-hf")\n\n# Dataset format: {prompt, chosen, rejected}\ndataset = load_dataset("Anthropic/hh-rlhf")\n\ntraining_args = DPOConfig(\n    output_dir="./dpo_model",\n    beta=0.1,\n    learning_rate=5e-7,\n    per_device_train_batch_size=4,\n    gradient_accumulation_steps=4,\n    num_train_epochs=1,\n    fp16=True,\n)\n\ntrainer = DPOTrainer(\n    model=model,\n    ref_model=ref_model,\n    args=training_args,\n    train_dataset=dataset["train"],\n    tokenizer=tokenizer,\n)\ntrainer.train()\n""")\n\n# Другие альтернативы\nprint("\\n=== Альтернативы RLHF ===")\nalternatives = [\n    ("DPO", "Direct optimization на preferences", "Просто, стабильно, β единственный гиперпараметр"),\n    ("IPO", "Identity Preference Optimization", "Robustнее DPO к шумным предпочтениям"),\n    ("KTO", "Kahneman-Tversky Optimization", "Не нужны пары — достаточно good/bad labels"),\n    ("ORPO", "Odds Ratio Preference Optimization", "Без reference model, ещё проще"),\n    ("SimPO", "Simple Preference Optimization", "Reference-free, length-normalized"),\n]\nfor name, desc, pros in alternatives:\n    print(f"\\n  {name}: {desc}")\n    print(f"    + {pros}")' },
        { type: 'tip', value: 'DPO стал де-факто стандартом для alignment в 2024+. Он проще PPO, требует меньше GPU, даёт сравнимое качество. Для начала используйте DPO с TRL. β=0.1 — хороший default. KTO подходит когда нет парных предпочтений, а есть только thumbs up/down.' }
      ]
    }
  ]
}
