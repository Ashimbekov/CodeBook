export default {
  id: 31,
  title: 'Генеративные модели (GANs)',
  description: 'Архитектура GAN, процесс обучения, DCGAN, Conditional GAN и Wasserstein GAN.',
  lessons: [
    {
      id: 1,
      title: 'Архитектура GAN',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Generative Adversarial Networks' },
        { type: 'text', value: 'GAN (Generative Adversarial Network) — генеративная модель, состоящая из двух нейронных сетей, соревнующихся друг с другом. Generator создаёт фейковые данные из шума, Discriminator отличает настоящие данные от фейковых. Они обучаются одновременно, улучшая друг друга, пока Generator не начнёт создавать реалистичные данные.' },
        { type: 'code', language: 'python', value: 'import tensorflow as tf\nfrom tensorflow.keras import layers, Model\nimport numpy as np\n\n# Простой GAN для генерации 1D данных\nlatent_dim = 16  # размерность шума\ndata_dim = 1     # размерность данных\n\n# Generator: шум -> данные\ndef build_generator():\n    model = tf.keras.Sequential([\n        layers.Dense(32, activation="relu", input_shape=(latent_dim,)),\n        layers.Dense(64, activation="relu"),\n        layers.Dense(32, activation="relu"),\n        layers.Dense(data_dim, activation="linear")\n    ])\n    return model\n\n# Discriminator: данные -> вероятность (настоящий/фейк)\ndef build_discriminator():\n    model = tf.keras.Sequential([\n        layers.Dense(32, activation="relu", input_shape=(data_dim,)),\n        layers.Dense(64, activation="relu"),\n        layers.Dense(32, activation="relu"),\n        layers.Dense(1, activation="sigmoid")\n    ])\n    return model\n\ngenerator = build_generator()\ndiscriminator = build_discriminator()\n\nprint("Generator:")\ngenerator.summary()\nprint("\\nDiscriminator:")\ndiscriminator.summary()\n\n# Тест\nnoise = tf.random.normal([5, latent_dim])\nfake_data = generator(noise)\nprint(f"\\nГенерированные данные: {fake_data.numpy().flatten().round(3)}")\n\nvalidity = discriminator(fake_data)\nprint(f"Оценка дискриминатора: {validity.numpy().flatten().round(3)}")' },
        { type: 'note', value: 'GAN — это игра двух игроков (minimax game). Generator минимизирует, Discriminator максимизирует: min_G max_D [E[log D(x)] + E[log(1 - D(G(z)))]]' }
      ]
    },
    {
      id: 2,
      title: 'Процесс обучения GAN',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Обучение генератора и дискриминатора' },
        { type: 'text', value: 'Обучение GAN — это чередование двух шагов: (1) обучаем Discriminator отличать реальные данные от сгенерированных, (2) обучаем Generator обманывать Discriminator. Это нестабильный процесс, требующий тонкой настройки.' },
        { type: 'code', language: 'python', value: 'import tensorflow as tf\nfrom tensorflow.keras import layers\nimport numpy as np\n\nlatent_dim = 16\n\n# Модели\ngenerator = tf.keras.Sequential([\n    layers.Dense(64, activation="relu", input_shape=(latent_dim,)),\n    layers.Dense(128, activation="relu"),\n    layers.Dense(1, activation="linear")\n])\n\ndiscriminator = tf.keras.Sequential([\n    layers.Dense(64, activation="relu", input_shape=(1,)),\n    layers.Dense(128, activation="relu"),\n    layers.Dense(1, activation="sigmoid")\n])\n\ng_optimizer = tf.keras.optimizers.Adam(learning_rate=0.0002, beta_1=0.5)\nd_optimizer = tf.keras.optimizers.Adam(learning_rate=0.0002, beta_1=0.5)\nloss_fn = tf.keras.losses.BinaryCrossentropy()\n\n# Целевое распределение: нормальное N(5, 1)\ndef get_real_data(batch_size):\n    return np.random.normal(5, 1, (batch_size, 1)).astype(np.float32)\n\n@tf.function\ndef train_step(real_data, batch_size):\n    noise = tf.random.normal([batch_size, latent_dim])\n    \n    # === Обучение Discriminator ===\n    with tf.GradientTape() as d_tape:\n        fake_data = generator(noise, training=False)\n        real_output = discriminator(real_data, training=True)\n        fake_output = discriminator(fake_data, training=True)\n        \n        d_loss_real = loss_fn(tf.ones_like(real_output), real_output)\n        d_loss_fake = loss_fn(tf.zeros_like(fake_output), fake_output)\n        d_loss = d_loss_real + d_loss_fake\n    \n    d_grads = d_tape.gradient(d_loss, discriminator.trainable_variables)\n    d_optimizer.apply_gradients(zip(d_grads, discriminator.trainable_variables))\n    \n    # === Обучение Generator ===\n    noise = tf.random.normal([batch_size, latent_dim])\n    with tf.GradientTape() as g_tape:\n        fake_data = generator(noise, training=True)\n        fake_output = discriminator(fake_data, training=False)\n        g_loss = loss_fn(tf.ones_like(fake_output), fake_output)\n    \n    g_grads = g_tape.gradient(g_loss, generator.trainable_variables)\n    g_optimizer.apply_gradients(zip(g_grads, generator.trainable_variables))\n    \n    return d_loss, g_loss\n\n# Обучение\nbatch_size = 64\nfor epoch in range(500):\n    real_data = get_real_data(batch_size)\n    d_loss, g_loss = train_step(real_data, batch_size)\n    \n    if (epoch + 1) % 100 == 0:\n        noise = tf.random.normal([1000, latent_dim])\n        generated = generator(noise).numpy().flatten()\n        print(f"Epoch {epoch+1}: D_loss={d_loss:.4f}, G_loss={g_loss:.4f}, "\n              f"Generated mean={generated.mean():.3f}, std={generated.std():.3f}")\n\nprint(f"\\nЦелевое распределение: mean=5.000, std=1.000")' },
        { type: 'warning', value: 'Mode collapse — главная проблема GAN: генератор начинает создавать только один тип данных. Решения: WGAN, спектральная нормализация, label smoothing, более сложные архитектуры.' }
      ]
    },
    {
      id: 3,
      title: 'DCGAN',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Deep Convolutional GAN' },
        { type: 'text', value: 'DCGAN (Deep Convolutional GAN) — стандартная архитектура для генерации изображений. Generator использует transposed convolutions (deconvolutions) для увеличения размера, Discriminator — обычные convolutions для уменьшения. Это первая GAN-архитектура, которая стабильно генерировала качественные изображения.' },
        { type: 'code', language: 'python', value: 'import tensorflow as tf\nfrom tensorflow.keras import layers\n\nlatent_dim = 100\n\n# DCGAN Generator: шум 100 -> изображение 28x28x1\ndef build_dcgan_generator():\n    model = tf.keras.Sequential([\n        # Dense -> 7x7x256\n        layers.Dense(7 * 7 * 256, use_bias=False, input_shape=(latent_dim,)),\n        layers.BatchNormalization(),\n        layers.LeakyReLU(0.2),\n        layers.Reshape((7, 7, 256)),\n        \n        # 7x7 -> 14x14\n        layers.Conv2DTranspose(128, (5, 5), strides=(2, 2), padding="same", use_bias=False),\n        layers.BatchNormalization(),\n        layers.LeakyReLU(0.2),\n        \n        # 14x14 -> 28x28\n        layers.Conv2DTranspose(64, (5, 5), strides=(2, 2), padding="same", use_bias=False),\n        layers.BatchNormalization(),\n        layers.LeakyReLU(0.2),\n        \n        # 28x28x1\n        layers.Conv2DTranspose(1, (5, 5), strides=(1, 1), padding="same",\n                               activation="tanh", use_bias=False)\n    ])\n    return model\n\n# DCGAN Discriminator: изображение 28x28x1 -> вероятность\ndef build_dcgan_discriminator():\n    model = tf.keras.Sequential([\n        layers.Conv2D(64, (5, 5), strides=(2, 2), padding="same",\n                      input_shape=[28, 28, 1]),\n        layers.LeakyReLU(0.2),\n        layers.Dropout(0.3),\n        \n        layers.Conv2D(128, (5, 5), strides=(2, 2), padding="same"),\n        layers.LeakyReLU(0.2),\n        layers.Dropout(0.3),\n        \n        layers.Flatten(),\n        layers.Dense(1, activation="sigmoid\")\n    ])\n    return model\n\ngenerator = build_dcgan_generator()\ndiscriminator = build_dcgan_discriminator()\n\nprint("DCGAN Generator:")\nprint(f"  Input: noise ({latent_dim},)")\nfor layer in generator.layers:\n    print(f"  {layer.name}: {layer.output_shape}")\n\nprint(f"\\nDCGAN Discriminator:")\nfor layer in discriminator.layers:\n    print(f"  {layer.name}: {layer.output_shape}")\n\n# Тест\nnoise = tf.random.normal([4, latent_dim])\nfake_images = generator(noise, training=False)\nprint(f"\\nСгенерированные изображения: {fake_images.shape}")\nprint(f"Диапазон значений: [{fake_images.numpy().min():.3f}, {fake_images.numpy().max():.3f}]")' },
        { type: 'list', items: [
          'Используйте BatchNormalization в Generator (но не в последнем слое)',
          'Используйте LeakyReLU вместо ReLU в Discriminator',
          'Conv2DTranspose (stride=2) для upsampling в Generator',
          'Удалите полносвязные слои, кроме входа Generator и выхода Discriminator',
          'Tanh активация на выходе Generator, Sigmoid — на выходе Discriminator'
        ] }
      ]
    },
    {
      id: 4,
      title: 'Conditional GAN (cGAN)',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Условная генерация' },
        { type: 'text', value: 'Conditional GAN (cGAN) позволяет контролировать, что генерируется, добавляя условие (label) на вход генератора и дискриминатора. Например, для MNIST: генерируем не случайную цифру, а конкретную (0-9).' },
        { type: 'code', language: 'python', value: 'import tensorflow as tf\nfrom tensorflow.keras import layers, Model\n\nlatent_dim = 100\nn_classes = 10\nimg_shape = (28, 28, 1)\n\n# Conditional Generator\ndef build_cgan_generator():\n    # Noise input\n    noise_input = layers.Input(shape=(latent_dim,))\n    \n    # Label input (embedded)\n    label_input = layers.Input(shape=(1,), dtype="int32")\n    label_embed = layers.Embedding(n_classes, 50)(label_input)\n    label_embed = layers.Flatten()(label_embed)\n    \n    # Concatenate noise + label\n    merged = layers.Concatenate()([noise_input, label_embed])\n    \n    x = layers.Dense(7 * 7 * 128)(merged)\n    x = layers.LeakyReLU(0.2)(x)\n    x = layers.Reshape((7, 7, 128))(x)\n    \n    x = layers.Conv2DTranspose(128, (4, 4), strides=(2, 2), padding="same")(x)\n    x = layers.BatchNormalization()(x)\n    x = layers.LeakyReLU(0.2)(x)\n    \n    x = layers.Conv2DTranspose(64, (4, 4), strides=(2, 2), padding="same")(x)\n    x = layers.BatchNormalization()(x)\n    x = layers.LeakyReLU(0.2)(x)\n    \n    output = layers.Conv2D(1, (7, 7), padding="same", activation="tanh")(x)\n    \n    return Model([noise_input, label_input], output)\n\n# Conditional Discriminator\ndef build_cgan_discriminator():\n    # Image input\n    img_input = layers.Input(shape=img_shape)\n    \n    # Label input\n    label_input = layers.Input(shape=(1,), dtype="int32")\n    label_embed = layers.Embedding(n_classes, 28 * 28)(label_input)\n    label_embed = layers.Reshape((28, 28, 1))(label_embed)\n    \n    # Concatenate image + label\n    merged = layers.Concatenate()([img_input, label_embed])\n    \n    x = layers.Conv2D(64, (3, 3), strides=(2, 2), padding="same")(merged)\n    x = layers.LeakyReLU(0.2)(x)\n    x = layers.Dropout(0.3)(x)\n    \n    x = layers.Conv2D(128, (3, 3), strides=(2, 2), padding="same")(x)\n    x = layers.LeakyReLU(0.2)(x)\n    x = layers.Dropout(0.3)(x)\n    \n    x = layers.Flatten()(x)\n    output = layers.Dense(1, activation="sigmoid")(x)\n    \n    return Model([img_input, label_input], output)\n\ngen = build_cgan_generator()\ndisc = build_cgan_discriminator()\n\nprint("Conditional GAN:")\nprint(f"  Generator inputs: noise {latent_dim}D + label")\nprint(f"  Generator output: {gen.output_shape}")\nprint(f"  Discriminator inputs: image {img_shape} + label")\nprint(f"  Discriminator output: {disc.output_shape}")\n\n# Генерация конкретных цифр\nnoise = tf.random.normal([10, latent_dim])\nlabels = tf.constant([[i] for i in range(10)])  # цифры 0-9\nfake_images = gen.predict([noise, labels], verbose=0)\nprint(f"\\nСгенерировано 10 изображений (по одному на цифру): {fake_images.shape}")' },
        { type: 'tip', value: 'cGAN — основа для многих приложений: image-to-image translation (Pix2Pix), text-to-image генерация, super-resolution. Условие может быть не только классом, но и текстом, другим изображением или любым вектором.' }
      ]
    },
    {
      id: 5,
      title: 'Wasserstein GAN',
      type: 'theory',
      content: [
        { type: 'heading', value: 'WGAN — стабильное обучение GAN' },
        { type: 'text', value: 'Wasserstein GAN (WGAN) решает проблему нестабильного обучения GAN. Вместо binary cross-entropy используется Wasserstein distance (Earth Mover Distance). Это даёт более информативные градиенты и устраняет mode collapse. WGAN-GP добавляет gradient penalty вместо weight clipping.' },
        { type: 'code', language: 'python', value: 'import tensorflow as tf\nfrom tensorflow.keras import layers\nimport numpy as np\n\nlatent_dim = 64\n\n# Critic (не discriminator!) в WGAN\ndef build_critic():\n    return tf.keras.Sequential([\n        layers.Dense(128, input_shape=(1,)),\n        layers.LeakyReLU(0.2),\n        layers.Dense(256),\n        layers.LeakyReLU(0.2),\n        layers.Dense(128),\n        layers.LeakyReLU(0.2),\n        layers.Dense(1)  # БЕЗ sigmoid! Выход — score, не вероятность\n    ])\n\ndef build_wgan_generator():\n    return tf.keras.Sequential([\n        layers.Dense(128, activation="relu", input_shape=(latent_dim,)),\n        layers.Dense(256, activation="relu"),\n        layers.Dense(128, activation="relu"),\n        layers.Dense(1)  # linear output\n    ])\n\ngenerator = build_wgan_generator()\ncritic = build_critic()\n\ng_opt = tf.keras.optimizers.Adam(1e-4, beta_1=0.5, beta_2=0.9)\nc_opt = tf.keras.optimizers.Adam(1e-4, beta_1=0.5, beta_2=0.9)\n\ndef gradient_penalty(critic, real, fake, batch_size):\n    # WGAN-GP: штраф за градиент != 1\n    epsilon = tf.random.uniform([batch_size, 1], 0, 1)\n    interpolated = epsilon * real + (1 - epsilon) * fake\n    \n    with tf.GradientTape() as tape:\n        tape.watch(interpolated)\n        pred = critic(interpolated)\n    \n    grads = tape.gradient(pred, interpolated)\n    grad_norm = tf.sqrt(tf.reduce_sum(tf.square(grads), axis=1) + 1e-8)\n    return tf.reduce_mean((grad_norm - 1.0) ** 2)\n\n# Обучение WGAN-GP\nbatch_size = 64\nn_critic = 5  # Critic обучается чаще Generator\nlambda_gp = 10.0\n\ndef get_real_data(n):\n    return np.random.normal(5, 1, (n, 1)).astype(np.float32)\n\nfor epoch in range(300):\n    # === Обучение Critic (n_critic шагов) ===\n    for _ in range(n_critic):\n        real = get_real_data(batch_size)\n        noise = tf.random.normal([batch_size, latent_dim])\n        \n        with tf.GradientTape() as tape:\n            fake = generator(noise, training=False)\n            c_real = critic(real, training=True)\n            c_fake = critic(fake, training=True)\n            \n            # Wasserstein loss + gradient penalty\n            w_loss = tf.reduce_mean(c_fake) - tf.reduce_mean(c_real)\n            gp = gradient_penalty(critic, real, fake, batch_size)\n            c_loss = w_loss + lambda_gp * gp\n        \n        c_grads = tape.gradient(c_loss, critic.trainable_variables)\n        c_opt.apply_gradients(zip(c_grads, critic.trainable_variables))\n    \n    # === Обучение Generator ===\n    noise = tf.random.normal([batch_size, latent_dim])\n    with tf.GradientTape() as tape:\n        fake = generator(noise, training=True)\n        g_loss = -tf.reduce_mean(critic(fake, training=False))\n    \n    g_grads = tape.gradient(g_loss, generator.trainable_variables)\n    g_opt.apply_gradients(zip(g_grads, generator.trainable_variables))\n    \n    if (epoch + 1) % 100 == 0:\n        samples = generator(tf.random.normal([1000, latent_dim])).numpy().flatten()\n        print(f"Epoch {epoch+1}: W_loss={w_loss:.4f}, G_loss={g_loss:.4f}, "\n              f"mean={samples.mean():.3f}, std={samples.std():.3f}")' },
        { type: 'list', items: [
          'WGAN: Critic без sigmoid, Wasserstein distance вместо BCE',
          'WGAN-GP: gradient penalty вместо weight clipping (стабильнее)',
          'Critic обучается n_critic шагов на каждый шаг Generator (обычно n=5)',
          'Wasserstein distance коррелирует с качеством генерации (можно мониторить)',
          'WGAN решает mode collapse и vanishing gradients'
        ] }
      ]
    },
    {
      id: 6,
      title: 'Практика: Простой GAN',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте GAN для генерации данных из нормального распределения N(3, 0.5).',
      requirements: [
        'Создайте Generator: Input(16) -> Dense(64, relu) -> Dense(32, relu) -> Dense(1, linear)',
        'Создайте Discriminator: Input(1) -> Dense(64, relu) -> Dense(32, relu) -> Dense(1, sigmoid)',
        'Обучите на 500 эпох с batch_size=64',
        'Каждые 100 эпох выводите mean и std сгенерированных данных',
        'В конце покажите, что сгенерированные данные близки к N(3, 0.5)'
      ],
      hint: 'Обучение GAN: сначала Discriminator (реальные=1, фейковые=0), затем Generator (фейковые должны получить оценку 1). Learning rate=0.0002, optimizer=Adam с beta_1=0.5.',
      expectedOutput: 'GAN Training:\nEpoch 100: mean=X.XX, std=X.XX\nEpoch 200: mean=X.XX, std=X.XX\n...\nЦель: mean=3.0, std=0.5\nРезультат: mean~3.0, std~0.5',
      solution: 'import tensorflow as tf\nfrom tensorflow.keras import layers\nimport numpy as np\n\nlatent_dim = 16\n\ngenerator = tf.keras.Sequential([\n    layers.Dense(64, activation="relu", input_shape=(latent_dim,)),\n    layers.Dense(32, activation="relu"),\n    layers.Dense(1)\n])\n\ndiscriminator = tf.keras.Sequential([\n    layers.Dense(64, activation="relu", input_shape=(1,)),\n    layers.Dense(32, activation="relu"),\n    layers.Dense(1, activation="sigmoid")\n])\n\ng_opt = tf.keras.optimizers.Adam(0.0002, beta_1=0.5)\nd_opt = tf.keras.optimizers.Adam(0.0002, beta_1=0.5)\nloss_fn = tf.keras.losses.BinaryCrossentropy()\n\nbatch_size = 64\n\nprint("GAN Training:")\nfor epoch in range(500):\n    real = np.random.normal(3, 0.5, (batch_size, 1)).astype(np.float32)\n    noise = tf.random.normal([batch_size, latent_dim])\n    \n    with tf.GradientTape() as tape:\n        fake = generator(noise, training=False)\n        d_real = discriminator(real, training=True)\n        d_fake = discriminator(fake, training=True)\n        d_loss = loss_fn(tf.ones_like(d_real), d_real) + loss_fn(tf.zeros_like(d_fake), d_fake)\n    d_grads = tape.gradient(d_loss, discriminator.trainable_variables)\n    d_opt.apply_gradients(zip(d_grads, discriminator.trainable_variables))\n    \n    noise = tf.random.normal([batch_size, latent_dim])\n    with tf.GradientTape() as tape:\n        fake = generator(noise, training=True)\n        d_fake = discriminator(fake, training=False)\n        g_loss = loss_fn(tf.ones_like(d_fake), d_fake)\n    g_grads = tape.gradient(g_loss, generator.trainable_variables)\n    g_opt.apply_gradients(zip(g_grads, generator.trainable_variables))\n    \n    if (epoch + 1) % 100 == 0:\n        samples = generator(tf.random.normal([1000, latent_dim])).numpy().flatten()\n        print(f"Epoch {epoch+1}: mean={samples.mean():.3f}, std={samples.std():.3f}")\n\nprint(f"\\nЦель: mean=3.0, std=0.5")',
      explanation: 'GAN обучается через adversarial game: Generator учится создавать данные, неотличимые от реальных, а Discriminator — отличать реальные от фейковых. После обучения Generator аппроксимирует целевое распределение N(3, 0.5). Adam с beta_1=0.5 стабилизирует обучение GAN.'
    }
  ]
}
