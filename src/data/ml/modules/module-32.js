export default {
  id: 32,
  title: 'Автоэнкодеры',
  description: 'Автоэнкодеры, вариационные автоэнкодеры (VAE), denoising автоэнкодеры, применения и латентное пространство.',
  lessons: [
    {
      id: 1,
      title: 'Основы автоэнкодеров',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Что такое автоэнкодер' },
        { type: 'text', value: 'Автоэнкодер (Autoencoder) — нейронная сеть, которая учится сжимать данные (encoder) и восстанавливать их (decoder). Bottleneck (узкое место) заставляет сеть выучивать наиболее важные признаки данных. Это unsupervised метод — не нужны метки.' },
        { type: 'code', language: 'python', value: 'import tensorflow as tf\nfrom tensorflow.keras import layers, Model\nimport numpy as np\n\n# Простой автоэнкодер\ninput_dim = 784  # 28x28 (MNIST)\nencoding_dim = 32  # bottleneck\n\n# Encoder\ninputs = layers.Input(shape=(input_dim,))\nencoded = layers.Dense(256, activation="relu")(inputs)\nencoded = layers.Dense(128, activation="relu")(encoded)\nencoded = layers.Dense(encoding_dim, activation="relu")(encoded)\n\n# Decoder\ndecoded = layers.Dense(128, activation="relu")(encoded)\ndecoded = layers.Dense(256, activation="relu")(decoded)\ndecoded = layers.Dense(input_dim, activation="sigmoid")(decoded)\n\n# Модели\nautoencoder = Model(inputs, decoded)\nencoder = Model(inputs, encoded)\n\n# Decoder как отдельная модель\nencoded_input = layers.Input(shape=(encoding_dim,))\ndecoder_layer = autoencoder.layers[-3](encoded_input)\ndecoder_layer = autoencoder.layers[-2](decoder_layer)\ndecoder_layer = autoencoder.layers[-1](decoder_layer)\ndecoder = Model(encoded_input, decoder_layer)\n\nautoencoder.compile(optimizer="adam", loss="binary_crossentropy")\n\nprint("Autoencoder Architecture:")\nprint(f"  Input: {input_dim} -> Encoded: {encoding_dim} -> Output: {input_dim}")\nprint(f"  Compression ratio: {input_dim/encoding_dim:.1f}x")\nautoencoder.summary()\n\n# Обучение на MNIST\n(x_train, _), (x_test, _) = tf.keras.datasets.mnist.load_data()\nx_train = x_train.reshape(-1, 784).astype("float32") / 255.0\nx_test = x_test.reshape(-1, 784).astype("float32") / 255.0\n\nautoencoder.fit(x_train, x_train, epochs=10, batch_size=256,\n                validation_data=(x_test, x_test), verbose=0)\n\n# Оценка\nreconstructed = autoencoder.predict(x_test[:10], verbose=0)\nmse = np.mean((x_test[:10] - reconstructed) ** 2, axis=1)\nprint(f"\\nReconstruction MSE (первые 10): {mse.mean():.6f}")\n\n# Encoding\nencoded_data = encoder.predict(x_test[:5], verbose=0)\nprint(f"\\nEncoded shape: {encoded_data.shape}")\nprint(f"Encoded values (sample): {encoded_data[0][:8].round(3)}")' },
        { type: 'note', value: 'Автоэнкодер — не просто сжатие. Он учится выделять наиболее важные признаки (latent features). Encoded представление можно использовать для визуализации, кластеризации, обнаружения аномалий.' }
      ]
    },
    {
      id: 2,
      title: 'Вариационные автоэнкодеры (VAE)',
      type: 'theory',
      content: [
        { type: 'heading', value: 'VAE — генеративный автоэнкодер' },
        { type: 'text', value: 'Variational Autoencoder (VAE) — вероятностный автоэнкодер, который кодирует данные не в точки, а в распределения (mean и std). Это позволяет генерировать новые данные: сэмплируем из латентного пространства и декодируем. VAE оптимизирует ELBO = reconstruction loss + KL divergence.' },
        { type: 'code', language: 'python', value: 'import tensorflow as tf\nfrom tensorflow.keras import layers, Model, backend as K\nimport numpy as np\n\nlatent_dim = 2  # 2D для визуализации\ninput_dim = 784\n\n# Reparameterization trick\nclass Sampling(layers.Layer):\n    def call(self, inputs):\n        z_mean, z_log_var = inputs\n        epsilon = tf.random.normal(shape=tf.shape(z_mean))\n        return z_mean + tf.exp(0.5 * z_log_var) * epsilon\n\n# Encoder\ninputs = layers.Input(shape=(input_dim,))\nx = layers.Dense(256, activation="relu")(inputs)\nx = layers.Dense(128, activation="relu")(x)\nz_mean = layers.Dense(latent_dim, name="z_mean")(x)\nz_log_var = layers.Dense(latent_dim, name="z_log_var")(x)\nz = Sampling()([z_mean, z_log_var])\nencoder = Model(inputs, [z_mean, z_log_var, z], name="encoder")\n\n# Decoder\nlatent_inputs = layers.Input(shape=(latent_dim,))\nx = layers.Dense(128, activation="relu")(latent_inputs)\nx = layers.Dense(256, activation="relu")(x)\noutputs = layers.Dense(input_dim, activation="sigmoid")(x)\ndecoder = Model(latent_inputs, outputs, name="decoder")\n\n# VAE Model\nclass VAE(Model):\n    def __init__(self, encoder, decoder):\n        super().__init__()\n        self.encoder = encoder\n        self.decoder = decoder\n    \n    def call(self, inputs):\n        z_mean, z_log_var, z = self.encoder(inputs)\n        return self.decoder(z)\n    \n    def train_step(self, data):\n        with tf.GradientTape() as tape:\n            z_mean, z_log_var, z = self.encoder(data)\n            reconstruction = self.decoder(z)\n            \n            # Reconstruction loss\n            recon_loss = tf.reduce_mean(\n                tf.keras.losses.binary_crossentropy(data, reconstruction)\n            ) * input_dim\n            \n            # KL divergence\n            kl_loss = -0.5 * tf.reduce_mean(\n                1 + z_log_var - tf.square(z_mean) - tf.exp(z_log_var)\n            )\n            \n            total_loss = recon_loss + kl_loss\n        \n        grads = tape.gradient(total_loss, self.trainable_variables)\n        self.optimizer.apply_gradients(zip(grads, self.trainable_variables))\n        return {"loss": total_loss, "recon_loss": recon_loss, "kl_loss": kl_loss}\n\nvae = VAE(encoder, decoder)\nvae.compile(optimizer="adam")\n\n# Обучение\n(x_train, y_train), (x_test, y_test) = tf.keras.datasets.mnist.load_data()\nx_train = x_train.reshape(-1, 784).astype("float32") / 255.0\n\nvae.fit(x_train, epochs=5, batch_size=128, verbose=0)\n\n# Генерация новых изображений\nz_samples = np.random.normal(0, 1, (10, latent_dim))\ngenerated = decoder.predict(z_samples, verbose=0)\nprint(f"Сгенерировано {len(z_samples)} новых изображений")\nprint(f"Shape: {generated.shape}")' },
        { type: 'tip', value: 'KL divergence заставляет латентное пространство быть гладким и непрерывным: близкие точки в латентном пространстве дают похожие изображения. Это ключевое отличие от обычного автоэнкодера.' }
      ]
    },
    {
      id: 3,
      title: 'Denoising Autoencoder',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Автоэнкодер для удаления шума' },
        { type: 'text', value: 'Denoising Autoencoder (DAE) обучается восстанавливать чистые данные из зашумлённых. На вход подаются данные с добавленным шумом, а на выход ожидаются оригинальные чистые данные. Это заставляет сеть выучивать более робастные представления.' },
        { type: 'code', language: 'python', value: 'import tensorflow as tf\nfrom tensorflow.keras import layers, Model\nimport numpy as np\n\n# Convolutional Denoising Autoencoder\ndef build_denoising_ae():\n    # Encoder\n    inputs = layers.Input(shape=(28, 28, 1))\n    x = layers.Conv2D(32, (3, 3), activation="relu", padding="same")(inputs)\n    x = layers.MaxPooling2D((2, 2))(x)\n    x = layers.Conv2D(64, (3, 3), activation="relu", padding="same")(x)\n    encoded = layers.MaxPooling2D((2, 2))(x)\n    \n    # Decoder\n    x = layers.Conv2D(64, (3, 3), activation="relu", padding="same")(encoded)\n    x = layers.UpSampling2D((2, 2))(x)\n    x = layers.Conv2D(32, (3, 3), activation="relu", padding="same")(x)\n    x = layers.UpSampling2D((2, 2))(x)\n    decoded = layers.Conv2D(1, (3, 3), activation="sigmoid", padding="same")(x)\n    \n    return Model(inputs, decoded)\n\nmodel = build_denoising_ae()\nmodel.compile(optimizer="adam", loss="binary_crossentropy")\nmodel.summary()\n\n# Данные\n(x_train, _), (x_test, _) = tf.keras.datasets.mnist.load_data()\nx_train = x_train.reshape(-1, 28, 28, 1).astype("float32") / 255.0\nx_test = x_test.reshape(-1, 28, 28, 1).astype("float32") / 255.0\n\n# Добавляем шум\nnoise_factor = 0.3\nx_train_noisy = x_train + noise_factor * np.random.normal(size=x_train.shape)\nx_test_noisy = x_test + noise_factor * np.random.normal(size=x_test.shape)\nx_train_noisy = np.clip(x_train_noisy, 0., 1.)\nx_test_noisy = np.clip(x_test_noisy, 0., 1.)\n\nprint(f"\\nОбучение: вход=зашумлённые, выход=чистые")\nmodel.fit(x_train_noisy, x_train, epochs=5, batch_size=128,\n          validation_data=(x_test_noisy, x_test), verbose=1)\n\n# Оценка\ndenoised = model.predict(x_test_noisy[:10], verbose=0)\nmse_noisy = np.mean((x_test[:10] - x_test_noisy[:10]) ** 2)\nmse_denoised = np.mean((x_test[:10] - denoised) ** 2)\n\nprint(f"\\nMSE (зашумлённые vs чистые): {mse_noisy:.6f}")\nprint(f"MSE (восстановленные vs чистые): {mse_denoised:.6f}")\nprint(f"Улучшение: {mse_noisy/mse_denoised:.1f}x")' },
        { type: 'note', value: 'Denoising autoencoders не просто убирают шум — они учатся выделять устойчивые паттерны в данных. Это делает их representations более качественными для downstream задач.' }
      ]
    },
    {
      id: 4,
      title: 'Применения автоэнкодеров',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Практические применения' },
        { type: 'text', value: 'Автоэнкодеры применяются в множестве задач: сжатие данных, генерация, обнаружение аномалий, удаление шума, заполнение пропусков, извлечение признаков, рекомендательные системы.' },
        { type: 'code', language: 'python', value: 'import numpy as np\nfrom tensorflow.keras import layers, Model\nimport tensorflow as tf\n\n# 1. Автоэнкодер для снижения размерности (замена PCA)\nprint("=== Снижение размерности ===\")\nnp.random.seed(42)\nX = np.random.randn(1000, 50).astype("float32")\n\ninputs = layers.Input(shape=(50,))\nencoded = layers.Dense(20, activation="relu")(inputs)\nencoded = layers.Dense(2, activation="linear")(encoded)  # 2D\ndecoded = layers.Dense(20, activation="relu")(encoded)\ndecoded = layers.Dense(50, activation="linear")(decoded)\n\nae_dim = Model(inputs, decoded)\nencoder_dim = Model(inputs, encoded)\n\nae_dim.compile(optimizer="adam", loss="mse")\nae_dim.fit(X, X, epochs=50, batch_size=64, verbose=0)\n\nX_encoded = encoder_dim.predict(X, verbose=0)\nprint(f"  50D -> 2D: {X_encoded.shape}")\nprint(f"  Reconstruction MSE: {np.mean((X - ae_dim.predict(X, verbose=0))**2):.6f}")\n\n# 2. Автоэнкодер для обнаружения аномалий\nprint("\\n=== Обнаружение аномалий ===\")\nX_normal = np.random.normal(0, 1, (800, 10)).astype("float32")\nX_anomaly = np.random.uniform(-5, 5, (50, 10)).astype("float32")\n\ninputs = layers.Input(shape=(10,))\nx = layers.Dense(8, activation="relu")(inputs)\nx = layers.Dense(3, activation="relu")(x)  # bottleneck\nx = layers.Dense(8, activation="relu")(x)\noutputs = layers.Dense(10, activation="linear")(x)\n\nae_anom = Model(inputs, outputs)\nae_anom.compile(optimizer="adam", loss="mse")\nae_anom.fit(X_normal, X_normal, epochs=50, batch_size=32, verbose=0)\n\nerror_normal = np.mean((X_normal - ae_anom.predict(X_normal, verbose=0))**2, axis=1)\nerror_anomaly = np.mean((X_anomaly - ae_anom.predict(X_anomaly, verbose=0))**2, axis=1)\n\nthreshold = np.percentile(error_normal, 95)\ndetected = (error_anomaly > threshold).sum()\nprint(f"  Порог (95th percentile): {threshold:.4f}")\nprint(f"  Обнаружено аномалий: {detected}/{len(X_anomaly)}")\n\n# 3. Sparse Autoencoder\nprint("\\n=== Sparse Autoencoder ===\")\ninputs = layers.Input(shape=(784,))\nencoded = layers.Dense(128, activation="relu",\n                       activity_regularizer=tf.keras.regularizers.l1(1e-5))(inputs)\ndecoded = layers.Dense(784, activation="sigmoid")(encoded)\n\nsparse_ae = Model(inputs, encoded)\nprint(f"  Sparsity регуляризация: L1(1e-5)")\nprint(f"  Активные нейроны будут более разреженными")' },
        { type: 'list', items: [
          'Снижение размерности — нелинейная альтернатива PCA',
          'Обнаружение аномалий — высокая reconstruction error = аномалия',
          'Удаление шума — denoising autoencoders',
          'Генерация данных — VAE для создания новых образцов',
          'Feature extraction — encoded представление для других моделей',
          'Рекомендации — автоэнкодер для user/item embeddings'
        ] }
      ]
    },
    {
      id: 5,
      title: 'Латентное пространство',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Исследование латентного пространства' },
        { type: 'text', value: 'Латентное пространство (latent space) — это сжатое представление данных в bottleneck автоэнкодера. В VAE латентное пространство структурировано: похожие данные находятся рядом, а интерполяция между точками даёт осмысленные промежуточные результаты.' },
        { type: 'code', language: 'python', value: 'import numpy as np\nimport tensorflow as tf\nfrom tensorflow.keras import layers, Model\n\n# VAE с 2D латентным пространством\nclass Sampling(layers.Layer):\n    def call(self, inputs):\n        z_mean, z_log_var = inputs\n        eps = tf.random.normal(shape=tf.shape(z_mean))\n        return z_mean + tf.exp(0.5 * z_log_var) * eps\n\nlatent_dim = 2\n\n# Encoder\ninputs = layers.Input(shape=(784,))\nx = layers.Dense(256, activation="relu")(inputs)\nz_mean = layers.Dense(latent_dim)(x)\nz_log_var = layers.Dense(latent_dim)(x)\nz = Sampling()([z_mean, z_log_var])\nencoder = Model(inputs, [z_mean, z_log_var, z])\n\n# Decoder\nlatent_in = layers.Input(shape=(latent_dim,))\nx = layers.Dense(256, activation="relu")(latent_in)\nout = layers.Dense(784, activation="sigmoid")(x)\ndecoder = Model(latent_in, out)\n\n# Обучение (упрощённо)\n(x_train, y_train), (x_test, y_test) = tf.keras.datasets.mnist.load_data()\nx_train = x_train.reshape(-1, 784).astype("float32") / 255.0\nx_test = x_test.reshape(-1, 784).astype("float32") / 255.0\n\n# Визуализация латентного пространства\nz_mean_test, _, _ = encoder.predict(x_test, verbose=0)\n\nprint("Латентное пространство VAE (2D):")\nprint(f"  Shape: {z_mean_test.shape}")\nprint(f"  z_mean range: [{z_mean_test.min():.2f}, {z_mean_test.max():.2f}]")\n\n# Центры кластеров по классам\nfor digit in range(10):\n    mask = y_test == digit\n    center = z_mean_test[mask].mean(axis=0)\n    print(f"  Цифра {digit}: center=({center[0]:.2f}, {center[1]:.2f})")\n\n# Интерполяция в латентном пространстве\nprint("\\n--- Интерполяция ---")\nz_start = np.array([[0, -2]])  # начальная точка\nz_end = np.array([[2, 2]])     # конечная точка\n\nfor alpha in np.linspace(0, 1, 5):\n    z_interp = (1 - alpha) * z_start + alpha * z_end\n    img = decoder.predict(z_interp, verbose=0)\n    nonzero = (img > 0.5).sum()\n    print(f"  alpha={alpha:.2f}: z=({z_interp[0,0]:.2f}, {z_interp[0,1]:.2f}), active_pixels={nonzero}")\n\n# Генерация из сетки латентного пространства\nprint("\\n--- Генерация из сетки ---")\ngrid_x = np.linspace(-3, 3, 5)\ngrid_y = np.linspace(-3, 3, 5)\nfor y in grid_y:\n    for x in grid_x:\n        z = np.array([[x, y]])\n        img = decoder.predict(z, verbose=0)\n        intensity = img.mean()\n        symbol = "#" if intensity > 0.1 else "."\n        print(symbol, end=" ")\n    print()' },
        { type: 'tip', value: 'Для визуализации латентного пространства используйте t-SNE или UMAP, если размерность > 2. Для генерации лучше всего работают VAE с latent_dim=2 (можно визуализировать) или latent_dim=32-128 (лучшее качество).' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Автоэнкодер для сжатия',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте автоэнкодер для сжатия данных и оцените качество восстановления.',
      requirements: [
        'Загрузите MNIST и нормализуйте (reshape в 784, /255)',
        'Создайте автоэнкодер: 784 -> 128 -> 32 -> 128 -> 784',
        'Обучите на 10 эпох с loss="binary_crossentropy"',
        'Вычислите reconstruction MSE на тестовом наборе',
        'Выведите compression ratio и MSE'
      ],
      hint: 'Используйте sigmoid на выходе decoder (данные нормализованы в [0, 1]). binary_crossentropy лучше mse для изображений, нормализованных в [0, 1].',
      expectedOutput: 'Autoencoder для сжатия:\n  Input dim: 784\n  Encoded dim: 32\n  Compression ratio: 24.5x\n  Reconstruction MSE: 0.XXXX',
      solution: 'import tensorflow as tf\nfrom tensorflow.keras import layers, Model\nimport numpy as np\n\n(x_train, _), (x_test, _) = tf.keras.datasets.mnist.load_data()\nx_train = x_train.reshape(-1, 784).astype("float32") / 255.0\nx_test = x_test.reshape(-1, 784).astype("float32") / 255.0\n\ninputs = layers.Input(shape=(784,))\nencoded = layers.Dense(128, activation="relu")(inputs)\nencoded = layers.Dense(32, activation="relu")(encoded)\ndecoded = layers.Dense(128, activation="relu")(encoded)\ndecoded = layers.Dense(784, activation="sigmoid")(decoded)\n\nautoencoder = Model(inputs, decoded)\nautoencoder.compile(optimizer="adam", loss="binary_crossentropy")\nautoencoder.fit(x_train, x_train, epochs=10, batch_size=256,\n                validation_data=(x_test, x_test), verbose=0)\n\nreconstructed = autoencoder.predict(x_test, verbose=0)\nmse = np.mean((x_test - reconstructed) ** 2)\n\nprint("Autoencoder для сжатия:")\nprint(f"  Input dim: 784")\nprint(f"  Encoded dim: 32")\nprint(f"  Compression ratio: {784/32:.1f}x")\nprint(f"  Reconstruction MSE: {mse:.6f}")',
      explanation: 'Автоэнкодер сжимает 784-мерные данные в 32-мерное представление (24.5x сжатие) и восстанавливает их. Низкая MSE показывает, что bottleneck в 32 нейрона сохраняет достаточно информации для реконструкции. Это нелинейное сжатие, более мощное, чем PCA.'
    }
  ]
}
