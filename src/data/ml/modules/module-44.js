export default {
  id: 44,
  title: 'LLM: Fine-tuning и RAG',
  description: 'Основы LLM, LoRA/QLoRA fine-tuning, RAG архитектура, vector databases и LangChain.',
  lessons: [
    {
      id: 1,
      title: 'Основы LLM',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Large Language Models — большие языковые модели' },
        { type: 'text', value: 'LLM (Large Language Models) — нейронные сети с миллиардами параметров, обученные на огромных текстовых корпусах. Они способны понимать и генерировать текст, отвечать на вопросы, писать код, переводить. Ключевые модели: GPT-4, Claude, Llama, Mistral. Основа — архитектура Transformer с decoder-only подходом.' },
        { type: 'code', language: 'python', value: 'from transformers import AutoTokenizer, AutoModelForCausalLM\nimport torch\n\n# Загрузка небольшой LLM\nmodel_name = "TinyLlama/TinyLlama-1.1B-Chat-v1.0"\ntokenizer = AutoTokenizer.from_pretrained(model_name)\nmodel = AutoModelForCausalLM.from_pretrained(model_name, torch_dtype=torch.float16)\n\nprint("LLM Architecture:")\nprint(f"  Model: {model_name}")\nprint(f"  Parameters: {sum(p.numel() for p in model.parameters()):,}")\nprint(f"  Vocab size: {model.config.vocab_size}")\nprint(f"  Hidden size: {model.config.hidden_size}")\nprint(f"  Layers: {model.config.num_hidden_layers}")\nprint(f"  Attention heads: {model.config.num_attention_heads}")\n\n# Генерация текста\ndef generate(prompt, max_tokens=100):\n    messages = [\n        {"role": "user", "content": prompt}\n    ]\n    text = tokenizer.apply_chat_template(messages, tokenize=False, add_generation_prompt=True)\n    inputs = tokenizer(text, return_tensors="pt")\n    \n    with torch.no_grad():\n        outputs = model.generate(\n            **inputs, max_new_tokens=max_tokens,\n            temperature=0.7, top_p=0.9, do_sample=True\n        )\n    \n    response = tokenizer.decode(outputs[0][inputs["input_ids"].shape[1]:],\n                                skip_special_tokens=True)\n    return response\n\nprint("\\nГенерация:")\nfor prompt in ["What is machine learning?\", \"Write a Python function to sort a list\"]:\n    response = generate(prompt, max_tokens=50)\n    print(f\"  Q: {prompt}\")\n    print(f\"  A: {response[:100]}...\")\n\nprint(\"\\n--- Масштаб LLM ---\")\nprint(\"GPT-3: 175B params, обучен на 300B+ токенов\")\nprint(\"Llama 2: 7B/13B/70B params\")\nprint(\"Mistral 7B: efficient, sliding window attention\")\nprint(\"GPT-4: ~1.7T params (estimated), multimodal\")' },
        { type: 'note', value: 'LLM обучаются на триллионах токенов текста. Pre-training стоит миллионы долларов (GPU). Fine-tuning на ваших данных — эффективный способ адаптации за разумные деньги (LoRA/QLoRA на 1 GPU).' }
      ]
    },
    {
      id: 2,
      title: 'LoRA и QLoRA Fine-tuning',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Эффективное дообучение LLM' },
        { type: 'text', value: 'LoRA (Low-Rank Adaptation) — метод fine-tuning, который обучает только маленькие adapter-матрицы (rank decomposition) вместо всех весов модели. QLoRA добавляет 4-bit квантизацию, позволяя fine-tuning модели 7B на одной GPU с 16GB памяти. Обучается <1% параметров, но качество близко к full fine-tuning.' },
        { type: 'code', language: 'python', value: 'from transformers import AutoModelForCausalLM, AutoTokenizer, TrainingArguments\nfrom peft import LoraConfig, get_peft_model, TaskType\nfrom trl import SFTTrainer\nimport torch\n\n# 1. Загрузка модели с 4-bit квантизацией (QLoRA)\nfrom transformers import BitsAndBytesConfig\n\nbnb_config = BitsAndBytesConfig(\n    load_in_4bit=True,\n    bnb_4bit_quant_type="nf4",\n    bnb_4bit_compute_dtype=torch.float16,\n    bnb_4bit_use_double_quant=True\n)\n\nmodel_name = "TinyLlama/TinyLlama-1.1B-Chat-v1.0"\ntokenizer = AutoTokenizer.from_pretrained(model_name)\nmodel = AutoModelForCausalLM.from_pretrained(\n    model_name,\n    quantization_config=bnb_config,\n    device_map="auto\"\n)\n\nprint(\"Модель загружена с 4-bit квантизацией\")\nprint(f\"  Memory: ~{sum(p.numel() for p in model.parameters()) * 0.5 / 1e9:.1f} GB (4-bit)\")\n\n# 2. LoRA конфигурация\nlora_config = LoraConfig(\n    r=16,                      # rank (чем больше, тем больше параметров)\n    lora_alpha=32,             # scaling factor\n    target_modules=[\"q_proj\", \"v_proj\", \"k_proj\", \"o_proj\"],  # какие слои адаптируем\n    lora_dropout=0.05,\n    bias=\"none\",\n    task_type=TaskType.CAUSAL_LM\n)\n\nmodel = get_peft_model(model, lora_config)\n\ntrainable = sum(p.numel() for p in model.parameters() if p.requires_grad)\ntotal = sum(p.numel() for p in model.parameters())\nprint(f\"\\nLoRA параметры:\")\nprint(f\"  Trainable: {trainable:,} ({trainable/total*100:.2f}%)\")\nprint(f\"  Total: {total:,}\")\nprint(f\"  Rank: {lora_config.r}\")\n\n# 3. Данные для fine-tuning\ntrain_data = [\n    {\"text\": \"<|user|>Что такое ML?<|assistant|>Machine Learning — это раздел ИИ, обучающий компьютеры на данных.\"},\n    {\"text\": \"<|user|>Объясни градиентный спуск<|assistant|>Градиентный спуск — метод оптимизации, итеративно обновляющий параметры в направлении антиградиента функции потерь.\"},\n]\n\n# 4. Training\ntraining_args = TrainingArguments(\n    output_dir=\"./lora_model\",\n    num_train_epochs=3,\n    per_device_train_batch_size=4,\n    gradient_accumulation_steps=4,\n    learning_rate=2e-4,\n    warmup_steps=10,\n    logging_steps=10,\n    fp16=True,\n    save_strategy=\"epoch\",\n)\n\nprint(\"\\n--- LoRA vs Full Fine-tuning ---\")\nprint(f\"Full FT: {total:,} params, 20+ GB VRAM\")\nprint(f\"LoRA r=16: {trainable:,} params, 6-8 GB VRAM\")\nprint(f\"LoRA r=8: ~{trainable//2:,} params, 4-6 GB VRAM\")\nprint(\"\\nQLoRA = 4-bit квантизация + LoRA\")\nprint(\"Можно fine-tune 7B модель на 1x RTX 3090 (24GB)\")' },
        { type: 'tip', value: 'LoRA rank r=8-16 обычно достаточно для большинства задач. Увеличение r даёт marginal improvement, но значительно увеличивает память и время обучения. lora_alpha обычно = 2*r.' }
      ]
    },
    {
      id: 3,
      title: 'RAG Architecture',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Retrieval-Augmented Generation' },
        { type: 'text', value: 'RAG (Retrieval-Augmented Generation) дополняет LLM внешними знаниями. Вместо того чтобы "помнить" всё, модель ищет релевантную информацию в базе знаний и использует её для генерации ответа. Это решает проблемы hallucination и outdated knowledge.' },
        { type: 'code', language: 'python', value: 'import numpy as np\nfrom sklearn.metrics.pairwise import cosine_similarity\n\n# Простая RAG система\nclass SimpleRAG:\n    def __init__(self, embedding_dim=64):\n        self.documents = []\n        self.embeddings = []\n        self.embedding_dim = embedding_dim\n    \n    def _embed(self, text):\n        \"\"\"Простой embedding (в реальности — sentence-transformers)\"\"\"\n        np.random.seed(hash(text) % 2**31)\n        return np.random.randn(self.embedding_dim)\n    \n    def add_documents(self, docs):\n        \"\"\"Добавляем документы в базу знаний\"\"\"\n        for doc in docs:\n            self.documents.append(doc)\n            self.embeddings.append(self._embed(doc))\n        self.embeddings_matrix = np.array(self.embeddings)\n    \n    def retrieve(self, query, top_k=3):\n        \"\"\"Находим наиболее релевантные документы\"\"\"\n        query_emb = self._embed(query).reshape(1, -1)\n        similarities = cosine_similarity(query_emb, self.embeddings_matrix)[0]\n        top_indices = np.argsort(similarities)[::-1][:top_k]\n        return [(self.documents[i], similarities[i]) for i in top_indices]\n    \n    def generate(self, query, top_k=3):\n        \"\"\"RAG: Retrieve + Generate\"\"\"\n        # 1. Retrieve\n        relevant_docs = self.retrieve(query, top_k)\n        \n        # 2. Формируем prompt с контекстом\n        context = \"\\n\".join([doc for doc, _ in relevant_docs])\n        prompt = f\"\"\"Контекст:\\n{context}\\n\\nВопрос: {query}\\n\\nОтвет на основе контекста:\"\"\"\n        \n        return prompt, relevant_docs\n\n# База знаний о ML\nknowledge_base = [\n    \"Random Forest — ансамбль деревьев решений с bagging. Каждое дерево обучаетс�� на случайной подвыборке данных и признаков.\",\n    \"Gradient Boosting последовательно строит деревья, каждое исправляет ошибки предыдущих. XGBoost, LightGBM, CatBoost — популярные реализации.\",\n    \"BERT — bidirectional трансформер от Google для NLP задач. Предобучен на MLM и NSP задачах.\",\n    \"CNN используют свёрточные слои для извлечения пространственных признаков из изображений.\",\n    \"LSTM — тип RNN с механизмом gates для запоминания долгосрочных зависимостей.\",\n    \"Transfer Learning позволяет использовать предобученную модель для новой задачи с малым количеством данных.\",\n    \"Cross-validation делит данные на K фолдов для надёжной оценки модели.\",\n    \"SHAP values объясняют вклад каждого призна��а в предсказание модели на основе теории игр Шепли.\",\n    \"PCA снижает размерность данных, находя главные компоненты максимальной дисперсии.\",\n    \"Batch Normalization нормализует активации между слоями, ускоряя обучение нейронных сетей.\",\n]\n\nrag = SimpleRAG()\nrag.add_documents(knowledge_base)\n\nprint(\"RAG System:\")\nprint(f\"  Документов в базе: {len(knowledge_base)}\")\n\nqueries = [\n    \"Как работает gradient boosting?\",\n    \"Что такое transfer learning?\",\n    \"Как объяснить предсказания модели?\"\n]\n\nfor query in queries:\n    prompt, docs = rag.generate(query, top_k=2)\n    print(f\"\\nВопрос: {query}\")\n    print(f\"Найденные документы:\")\n    for doc, score in docs:\n        print(f\"  [{score:.3f}] {doc[:60]}...\")' },
        { type: 'list', items: [
          'Chunking: разбиваем документы на куски (500-1000 tokens)',
          'Embedding: конвертируем chunks в vectors (sentence-transformers)',
          'Vector Store: храним embeddings (ChromaDB, FAISS, Pinecone)',
          'Retrieval: находим top-k похожих chunks по cosine similarity',
          'Generation: LLM генерирует ответ на основе найденного контекста'
        ] }
      ]
    },
    {
      id: 4,
      title: 'Vector Databases: ChromaDB и FAISS',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Векторные базы данных для RAG' },
        { type: 'text', value: 'Vector database хранит и быстро ищет векторные представления (embeddings). ChromaDB — простая и удобная для прототипов. FAISS от Meta — оптимизированная для больших масштабов. Pinecone, Weaviate, Qdrant — managed solutions.' },
        { type: 'code', language: 'python', value: 'import chromadb\nfrom sentence_transformers import SentenceTransformer\n\n# 1. ChromaDB — простая vector DB\nprint("=== ChromaDB ===\")\nclient = chromadb.Client()  # in-memory\n\ncollection = client.create_collection(\n    name="ml_knowledge",\n    metadata={"hnsw:space": "cosine"}  # cosine similarity\n)\n\n# Добавляем документы\ndocuments = [\n    "Random Forest — ансамбль деревьев решений",\n    "Gradient Boosting строит деревья последовательно",\n    "BERT — трансформер для NLP",\n    "CNN используются для изображений",\n    "Transfer Learning — перенос обучения"\n]\n\ncollection.add(\n    documents=documents,\n    ids=[f"doc_{i}" for i in range(len(documents))],\n    metadatas=[{"topic": "ml"} for _ in documents]\n)\n\n# Поиск\nresults = collection.query(\n    query_texts=["Какие модели используются для текстов?\"],\n    n_results=3\n)\n\nprint(\"Запрос: Какие модели используются для текстов?\")\nfor doc, dist in zip(results[\"documents\"][0], results[\"distances\"][0]):\n    print(f\"  [{dist:.4f}] {doc}\")\n\n# 2. FAISS — быстрый поиск\nprint(\"\\n=== FAISS ===\")\nimport faiss\nimport numpy as np\n\n# Генерируем embeddings (в реальности — sentence-transformers)\nnp.random.seed(42)\nn_docs = 10000\ndim = 384  # размерность embedding\n\n# Создаём embeddings\nembeddings = np.random.randn(n_docs, dim).astype(\"float32\")\nfaiss.normalize_L2(embeddings)  # для cosine similarity\n\n# Создаём индекс\nindex = faiss.IndexFlatIP(dim)  # Inner Product (= cosine для нормализованных)\nindex.add(embeddings)\n\nprint(f\"FAISS индекс: {index.ntotal} документов, {dim}D\")\n\n# Поиск\nquery = np.random.randn(1, dim).astype(\"float32\")\nfaiss.normalize_L2(query)\n\nimport time\nstart = time.time()\nscores, indices = index.search(query, k=5)\nlatency = (time.time() - start) * 1000\n\nprint(f\"Top-5 результатов (latency: {latency:.2f}ms):\")\nfor idx, score in zip(indices[0], scores[0]):\n    print(f\"  Doc {idx}: similarity={score:.4f}\")\n\n# Оптимизированные индексы\nprint(\"\\n--- FAISS индексы ---\")\nprint(\"IndexFlatIP: точный поиск, O(n)\")\nprint(\"IndexIVFFlat: approximate, кластеры (быстрее)\")\nprint(\"IndexHNSWFlat: approximate, HNSW граф (балансирует)\")\nprint(\"IndexIVFPQ: approximate + compression (для миллионов)\")\n\n# IVF индекс для больших данных\nnlist = 100  # количество кластеров\nquantizer = faiss.IndexFlatIP(dim)\nindex_ivf = faiss.IndexIVFFlat(quantizer, dim, nlist)\nindex_ivf.train(embeddings)\nindex_ivf.add(embeddings)\nindex_ivf.nprobe = 10  # сколько кластеров проверять\n\nscores_ivf, indices_ivf = index_ivf.search(query, k=5)\nprint(f\"\\nIVF поиск: {indices_ivf[0].tolist()}\")' },
        { type: 'tip', value: 'Для прототипов используйте ChromaDB (просто) или FAISS (быстро). Для production с >1M документов — FAISS с IVF/HNSW индексом или managed solutions (Pinecone, Qdrant). sentence-transformers/all-MiniLM-L6-v2 — хороший default embedding model.' }
      ]
    },
    {
      id: 5,
      title: 'LangChain основы',
      type: 'theory',
      content: [
        { type: 'heading', value: 'LangChain — фреймворк для LLM приложений' },
        { type: 'text', value: 'LangChain — фреймворк для построения приложений на основе LLM. Он предоставляет абстракции для: chains (последовательности вызовов), agents (LLM принимает решения), memory (контекст диалога), tools (внешние инструменты) и retrievers (RAG).' },
        { type: 'code', language: 'python', value: 'from langchain.llms import OpenAI\nfrom langchain.chat_models import ChatOpenAI\nfrom langchain.prompts import PromptTemplate, ChatPromptTemplate\nfrom langchain.chains import LLMChain, RetrievalQA\nfrom langchain.document_loaders import TextLoader\nfrom langchain.text_splitter import RecursiveCharacterTextSplitter\nfrom langchain.embeddings import HuggingFaceEmbeddings\nfrom langchain.vectorstores import Chroma\n\n# 1. Prompt Templates\nprint("=== Prompt Templates ===\")\ntemplate = PromptTemplate(\n    input_variables=["topic", "level"],\n    template="Объясни {topic} для {level} уровня. Используй примеры на Python."\n)\n\nprompt = template.format(topic="gradient boosting", level="начинающего")\nprint(f"Prompt: {prompt}")\n\n# 2. RAG Pipeline с LangChain\nprint("\\n=== RAG Pipeline ===\")\n\n# Загрузка и разбиение документов\ntext_splitter = RecursiveCharacterTextSplitter(\n    chunk_size=500,\n    chunk_overlap=50,\n    separators=["\\n\\n", "\\n", ". ", " "]\n)\n\n# Пример документов\ndocs = [\n    "Random Forest — ансамблевый метод, использующий множество деревь��в решений. Каждое дерево обучается на случайной подвыборке данных (bagging) и случайном подмножестве признаков. Финальное предсказание — голосование (классификация) или среднее (регрессия).",\n    "Gradient Boosting последовательно строит слабые модели (деревья), каждая из которых исправляет ошибки предыдущих. XGBoost, LightGBM, CatBoost — три ведущие реализации. LightGBM — самый быстрый, CatBoost — лучший для категорий.",\n    "BERT (Bidirectional Encoder Representations from Transformers) — модель для NLP задач. Pre-training на MLM и NSP, затем fine-tuning на конкретной задаче. HuggingFace Transformers — стандартная библиотека для работы с BERT.",\n]\n\nchunks = text_splitter.create_documents(docs)\nprint(f"Документов: {len(docs)} -> Chunks: {len(chunks)}")\n\n# Embeddings\nembeddings = HuggingFaceEmbeddings(\n    model_name="sentence-transformers/all-MiniLM-L6-v2"\n)\n\n# Vector Store\nvectorstore = Chroma.from_documents(chunks, embeddings)\n\n# Retriever\nretriever = vectorstore.as_retriever(search_kwargs={"k": 2})\n\n# RAG Chain\n# qa_chain = RetrievalQA.from_chain_type(\n#     llm=ChatOpenAI(model="gpt-3.5-turbo"),\n#     chain_type="stuff\",\n#     retriever=retriever\n# )\n# answer = qa_chain.run(\"Что такое gradient boosting?\")\n\n# 3. Agents\nprint(\"\\n=== LangChain Agents ===\")\nprint(\"\"\"\nfrom langchain.agents import load_tools, initialize_agent\n\n# Agent с инструментами\ntools = load_tools([\"wikipedia\", \"llm-math\"], llm=llm)\nagent = initialize_agent(tools, llm, agent=\"zero-shot-react-description\")\nagent.run(\"Сколько параметров в GPT-3? Умножь на 2.\")\n\"\"\")\n\nprint(\"\\n--- LangChain компоненты ---\")\nprint(\"Models: OpenAI, HuggingFace, local LLMs\")\nprint(\"Prompts: templates, few-shot, output parsers\")\nprint(\"Chains: sequential, router, retrieval QA\")\nprint(\"Memory: conversation buffer, summary, entity\")\nprint(\"Agents: ReAct, function calling, tools\")\nprint(\"Retrievers: vector store, BM25, ensemble\")' },
        { type: 'note', value: 'LangChain быстро развивается и API часто меняется. Для production рассмотрите LlamaIndex (фокус на RAG) или прямое использование API (OpenAI, Anthropic) с минимальными абстракциями.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Простая RAG система',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте простую RAG систему для ответов на вопросы по базе знаний.',
      requirements: [
        'Создайте базу знаний из 10+ документов о ML (строки текста)',
        'Реализуйте embedding функцию (TF-IDF или случайные для демо)',
        'Реализуйте поиск top-3 релевантных документов по cosine similarity',
        'Сформируйте RAG prompt с контекстом для 3 вопросов',
        'Выведите найденные документы и сформированный prompt'
      ],
      hint: 'Используйте TfidfVectorizer для простых embeddings. cosine_similarity из sklearn для поиска. RAG prompt = "Context: {docs}\\nQuestion: {query}\\nAnswer:".',
      expectedOutput: 'RAG System:\nБаза знаний: 10+ документов\n\nВопрос: "..."\nTop-3 документа:\n  [0.XX] doc1...\n  [0.XX] doc2...\n  [0.XX] doc3...\n\nPrompt сформирован!',
      solution: 'from sklearn.feature_extraction.text import TfidfVectorizer\nfrom sklearn.metrics.pairwise import cosine_similarity\nimport numpy as np\n\nknowledge_base = [\n    "Random Forest — ансамбль деревьев решений с bagging",\n    "Gradient Boosting последовательно строит деревья для исправления ошибок",\n    "BERT — bidirectional трансформер от Google для NLP",\n    "CNN используют свёрточные слои для изображений",\n    "LSTM запоминает долгосрочные зависимости в последовательностях",\n    "Transfer Learning переносит знания между задачами",\n    "SHAP объясняет вклад признаков в предсказание",\n    "PCA снижает размерность данных через главные компоненты",\n    "Cross-validation оценивает модель на K фолдах",\n    "Dropout регуляризирует нейросети, случайно отключая нейроны",\n    "Batch Normalization ускоряет обучение нормализацией активаций",\n]\n\nvectorizer = TfidfVectorizer()\ndoc_vectors = vectorizer.fit_transform(knowledge_base)\n\ndef retrieve(query, top_k=3):\n    query_vec = vectorizer.transform([query])\n    sims = cosine_similarity(query_vec, doc_vectors)[0]\n    top_idx = np.argsort(sims)[::-1][:top_k]\n    return [(knowledge_base[i], sims[i]) for i in top_idx]\n\ndef make_rag_prompt(query, top_k=3):\n    docs = retrieve(query, top_k)\n    context = "\\n".join([d for d, _ in docs])\n    return f"Context:\\n{context}\\n\\nQuestion: {query}\\nAnswer:", docs\n\nprint("RAG System:")\nprint(f"База знаний: {len(knowledge_base)} документов\\n")\n\nqueries = ["Как работает gradient boosting?", "Что делает BERT?", "Как оценить модель?"]\nfor query in queries:\n    prompt, docs = make_rag_prompt(query)\n    print(f"Вопрос: \\"{query}\\"")\n    print("Top-3 документа:")\n    for doc, score in docs:\n        print(f"  [{score:.4f}] {doc}")\n    print(f"Prompt сформирован! ({len(prompt)} символов)\\n")',
      explanation: 'RAG система: (1) embeds документы и query, (2) находит похожие документы через cosine similarity, (3) формирует prompt с контекстом для LLM. TF-IDF даёт простые, но рабочие embeddings. В production используйте sentence-transformers для более качественного семантического поиска.'
    }
  ]
}
