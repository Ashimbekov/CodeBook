export default {
  id: 37,
  title: 'Компьютерное зрение: практика',
  description: 'Image preprocessing, data augmentation, object detection (YOLO), image segmentation и transfer learning для CV.',
  lessons: [
    {
      id: 1,
      title: 'Image Preprocessing',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Предобработка изображений' },
        { type: 'text', value: 'Предобработка изображений — критический этап в Computer Vision. Правильная нормализация, ресайз и преобразование форматов влияют на качество модели. OpenCV и PIL — основные библиотеки для работы с изображениями в Python.' },
        { type: 'code', language: 'python', value: 'import numpy as np\nfrom PIL import Image\nimport cv2\n\n# Создаём тестовое изображение\nnp.random.seed(42)\nimg = np.random.randint(0, 256, (100, 150, 3), dtype=np.uint8)\n\n# 1. Resize\nresized = cv2.resize(img, (224, 224))  # стандартный размер для CNN\nprint(f"Resize: {img.shape} -> {resized.shape}")\n\n# 2. Нормализация\n# ImageNet нормализация\nmean = np.array([0.485, 0.456, 0.406])\nstd = np.array([0.229, 0.224, 0.225])\n\nnormalized = (resized / 255.0 - mean) / std\nprint(f"Нормализация: range [{normalized.min():.2f}, {normalized.max():.2f}]")\n\n# 3. Цветовые пространства\ngray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)\nhsv = cv2.cvtColor(img, cv2.COLOR_BGR2HSV)\nprint(f"Grayscale: {gray.shape}")\nprint(f"HSV: {hsv.shape}")\n\n# 4. Фильтры\nblurred = cv2.GaussianBlur(img, (5, 5), 0)\nsharpened = cv2.filter2D(img, -1, np.array([[-1,-1,-1],[-1,9,-1],[-1,-1,-1]]))\nedges = cv2.Canny(gray, 100, 200)\nprint(f"Edges: {edges.shape}, non-zero pixels: {np.count_nonzero(edges)}")\n\n# 5. Гистограмма и выравнивание\nhistogram = cv2.calcHist([gray], [0], None, [256], [0, 256]).flatten()\nequalized = cv2.equalizeHist(gray)\nprint(f"Histogram equalization: std before={gray.std():.1f}, after={equalized.std():.1f}")\n\n# 6. Pipeline предобработки\ndef preprocess_pipeline(image, target_size=(224, 224)):\n    # Стандартный pipeline для CNN\n    # Resize\n    img = cv2.resize(image, target_size)\n    # BGR -> RGB\n    img = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)\n    # Нормализация [0, 1]\n    img = img.astype(np.float32) / 255.0\n    # ImageNet нормализация\n    img = (img - mean) / std\n    # Добавляем batch dimension\n    img = np.expand_dims(img, axis=0)\n    return img\n\nprocessed = preprocess_pipeline(img)\nprint(f"\\nPipeline output: {processed.shape}, dtype={processed.dtype}")' },
        { type: 'tip', value: 'Всегда используйте ту же нормализацию при inference, что и при обучении! Для pretrained моделей (ResNet, VGG) используйте ImageNet mean/std. Для моделей, обученных с нуля, вычисляйте статистику по вашим данным.' }
      ]
    },
    {
      id: 2,
      title: 'Data Augmentation для изображений',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Аугментация для Computer Vision' },
        { type: 'text', value: 'Data augmentation — ключевой приём для увеличения эффективного размера обучающей выборки. Albumentations — быстрая и удобная библиотека для аугментации изображений с поддержкой bounding boxes и масок.' },
        { type: 'code', language: 'python', value: 'import albumentations as A\nimport numpy as np\n\n# Создаём тестовое изображение\nnp.random.seed(42)\nimage = np.random.randint(0, 256, (256, 256, 3), dtype=np.uint8)\n\n# 1. Базовые трансформации\ntransform_basic = A.Compose([\n    A.HorizontalFlip(p=0.5),\n    A.VerticalFlip(p=0.5),\n    A.Rotate(limit=30, p=0.5),\n    A.RandomBrightnessContrast(brightness_limit=0.2, contrast_limit=0.2, p=0.5),\n])\n\naugmented = transform_basic(image=image)\nprint(f"Базовая аугментация: {augmented[\'image\'].shape}")\n\n# 2. Продвинутые трансформации\ntransform_advanced = A.Compose([\n    A.OneOf([\n        A.GaussNoise(var_limit=(10, 50)),\n        A.GaussianBlur(blur_limit=7),\n        A.MotionBlur(blur_limit=7),\n    ], p=0.3),\n    A.OneOf([\n        A.OpticalDistortion(distort_limit=0.3),\n        A.GridDistortion(num_steps=5),\n        A.ElasticTransform(alpha=1, sigma=50),\n    ], p=0.3),\n    A.OneOf([\n        A.CLAHE(clip_limit=2),\n        A.Sharpen(),\n        A.Emboss(),\n    ], p=0.3),\n    A.HueSaturationValue(hue_shift_limit=20, sat_shift_limit=30, val_shift_limit=20, p=0.3),\n    A.CoarseDropout(max_holes=8, max_height=32, max_width=32, p=0.3),\n])\n\naugmented_adv = transform_advanced(image=image)\nprint(f"Продвинутая аугментация: {augmented_adv[\'image\'].shape}")\n\n# 3. Pipeline для обучения\ntrain_transform = A.Compose([\n    A.RandomResizedCrop(height=224, width=224, scale=(0.8, 1.0)),\n    A.HorizontalFlip(p=0.5),\n    A.ColorJitter(brightness=0.2, contrast=0.2, saturation=0.2, hue=0.1, p=0.5),\n    A.GaussNoise(var_limit=(10, 30), p=0.3),\n    A.Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225]),\n])\n\nval_transform = A.Compose([\n    A.Resize(224, 224),\n    A.Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225]),\n])\n\n# Симуляция батча\nbatch_images = []\nfor _ in range(8):\n    aug = train_transform(image=image)\n    batch_images.append(aug["image"])\n\nbatch = np.stack(batch_images)\nprint(f"\\nBatch shape: {batch.shape}")\nprint(f"Range: [{batch.min():.2f}, {batch.max():.2f}]")\n\nprint("\\nTrain: аугментация + нормализация")\nprint("Val/Test: только resize + нормализация (БЕЗ аугментации!)")' },
        { type: 'warning', value: 'НИКОГДА не применяйте аугментацию к validation/test данным! Аугментация только для training set. Test-time augmentation (TTA) — исключение: предсказываем на нескольких версиях изображения и усредняем.' }
      ]
    },
    {
      id: 3,
      title: 'Object Detection: YOLO',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Обнаружение объектов с YOLO' },
        { type: 'text', value: 'YOLO (You Only Look Once) — один из самых популярных алгоритмов object detection. Он обрабатывает изображение за один проход (в отличие от двухстадийных моделей типа Faster R-CNN), что делает его очень быстрым. Ultralytics YOLOv8 — современная реализация с простым API.' },
        { type: 'code', language: 'python', value: 'from ultralytics import YOLO\nimport numpy as np\n\n# Загрузка предобученной модели\nmodel = YOLO("yolov8n.pt")  # nano — самая лёгкая\n\nprint("YOLOv8 модель загружена")\nprint(f"  Архитектура: YOLOv8n (nano)")\nprint(f"  Классы: {len(model.names)}")\nprint(f"  Примеры классов: {list(model.names.values())[:10]}")\n\n# Inference на изображении\n# results = model("image.jpg")\n# results = model.predict(source="image.jpg", conf=0.5, iou=0.45)\n\n# Обработка результатов\nprint("\\n--- Формат результатов ---")\nprint("boxes.xyxy:  [x1, y1, x2, y2] — координаты bbox\")\nprint("boxes.conf:  confidence score\")\nprint(\"boxes.cls:   class ID\")\n\n# Тренировка на своих данных\nprint(\"\\n--- Тренировка YOLO ---\")\nprint(\"\"\"\n# Формат датасета (YOLO format):\n# images/train/img001.jpg\n# labels/train/img001.txt  (class x_center y_center width height)\n\n# Файл data.yaml:\n# train: ./images/train\n# val: ./images/val\n# nc: 3  # количество классов\n# names: [\"cat\", \"dog\", \"bird\"]\n\n# Тренировка:\nmodel = YOLO(\"yolov8n.pt\")  # pretrained\nresults = model.train(\n    data=\"data.yaml\",\n    epochs=50,\n    imgsz=640,\n    batch=16,\n    lr0=0.01,\n    augment=True\n)\n\n# Валидация:\nmetrics = model.val()\nprint(f\"mAP50: {metrics.box.map50}\")\nprint(f\"mAP50-95: {metrics.box.map}\")\n\"\"\")\n\n# Метрики\nprint(\"--- Метрики Object Detection ---\")\nprint(\"IoU (Intersection over Union): overlap между predicted и ground truth bbox\")\nprint(\"mAP50: mean Average Precision при IoU > 0.5\")\nprint(\"mAP50-95: средний mAP при IoU от 0.5 до 0.95 (шаг 0.05)\")' },
        { type: 'tip', value: 'YOLOv8 имеет 5 размеров: n (nano), s (small), m (medium), l (large), x (extra large). Начните с nano для прототипирования, используйте large/x для production, если нужна максимальная точность.' }
      ]
    },
    {
      id: 4,
      title: 'Image Segmentation',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Сегментация изображений' },
        { type: 'text', value: 'Image segmentation — классификация каждого пикселя изображения. Semantic segmentation назначает класс каждому пикселю, instance segmentation различает отдельные объекты. U-Net — классическая архитектура для сегментации, особенно в медицине.' },
        { type: 'code', language: 'python', value: 'import tensorflow as tf\nfrom tensorflow.keras import layers, Model\nimport numpy as np\n\n# U-Net архитектура\ndef build_unet(input_shape=(256, 256, 3), n_classes=2):\n    inputs = layers.Input(shape=input_shape)\n    \n    # Encoder (downsampling)\n    # Block 1\n    c1 = layers.Conv2D(64, (3,3), activation="relu", padding="same")(inputs)\n    c1 = layers.Conv2D(64, (3,3), activation="relu", padding="same")(c1)\n    p1 = layers.MaxPooling2D((2,2))(c1)\n    \n    # Block 2\n    c2 = layers.Conv2D(128, (3,3), activation="relu", padding="same")(p1)\n    c2 = layers.Conv2D(128, (3,3), activation="relu", padding="same")(c2)\n    p2 = layers.MaxPooling2D((2,2))(c2)\n    \n    # Block 3\n    c3 = layers.Conv2D(256, (3,3), activation="relu", padding="same")(p2)\n    c3 = layers.Conv2D(256, (3,3), activation="relu", padding="same")(c3)\n    p3 = layers.MaxPooling2D((2,2))(c3)\n    \n    # Bottleneck\n    c4 = layers.Conv2D(512, (3,3), activation="relu", padding="same")(p3)\n    c4 = layers.Conv2D(512, (3,3), activation="relu", padding="same")(c4)\n    \n    # Decoder (upsampling)\n    # Block 3\n    u3 = layers.Conv2DTranspose(256, (2,2), strides=(2,2), padding="same")(c4)\n    u3 = layers.Concatenate()([u3, c3])  # skip connection!\n    c5 = layers.Conv2D(256, (3,3), activation="relu", padding="same")(u3)\n    c5 = layers.Conv2D(256, (3,3), activation="relu", padding="same")(c5)\n    \n    # Block 2\n    u2 = layers.Conv2DTranspose(128, (2,2), strides=(2,2), padding="same")(c5)\n    u2 = layers.Concatenate()([u2, c2])\n    c6 = layers.Conv2D(128, (3,3), activation="relu", padding="same")(u2)\n    c6 = layers.Conv2D(128, (3,3), activation="relu", padding="same")(c6)\n    \n    # Block 1\n    u1 = layers.Conv2DTranspose(64, (2,2), strides=(2,2), padding="same")(c6)\n    u1 = layers.Concatenate()([u1, c1])\n    c7 = layers.Conv2D(64, (3,3), activation="relu", padding="same")(u1)\n    c7 = layers.Conv2D(64, (3,3), activation="relu", padding="same")(c7)\n    \n    # Output\n    outputs = layers.Conv2D(n_classes, (1,1), activation="softmax")(c7)\n    \n    return Model(inputs, outputs)\n\nmodel = build_unet(input_shape=(256, 256, 3), n_classes=3)\nprint("U-Net Architecture:")\nprint(f"  Input: (256, 256, 3)")\nprint(f"  Output: {model.output_shape}")\nprint(f"  Parameters: {model.count_params():,}")\n\n# Метрики сегментации\ndef dice_coefficient(y_true, y_pred, smooth=1):\n    # Dice = 2*|A∩B| / (|A| + |B|)\n    y_true_f = y_true.flatten()\n    y_pred_f = y_pred.flatten()\n    intersection = np.sum(y_true_f * y_pred_f)\n    return (2 * intersection + smooth) / (np.sum(y_true_f) + np.sum(y_pred_f) + smooth)\n\ndef iou_score(y_true, y_pred, smooth=1):\n    # IoU = |A∩B| / |A∪B|\n    intersection = np.sum(y_true * y_pred)\n    union = np.sum(y_true) + np.sum(y_pred) - intersection\n    return (intersection + smooth) / (union + smooth)\n\n# Пример\nmask_true = np.random.randint(0, 2, (256, 256))\nmask_pred = np.random.randint(0, 2, (256, 256))\nprint(f"\\nDice: {dice_coefficient(mask_true, mask_pred):.4f}")\nprint(f"IoU: {iou_score(mask_true, mask_pred):.4f}")' },
        { type: 'note', value: 'Skip connections в U-Net — ключевой элемент: они передают детали из encoder в decoder, позволяя точнее восстановить границы объектов. Без них сегментация была бы размытой.' }
      ]
    },
    {
      id: 5,
      title: 'Transfer Learning для CV',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Перенос обучения для изображений' },
        { type: 'text', value: 'Transfer learning — использование модели, обученной на большом датасете (ImageNet), для вашей задачи. Это работает, потому что первые слои CNN выучивают универсальные признаки (грани, текстуры), а последние — специфичные для задачи. Fine-tuning на 100-1000 изображений может дать отличные результаты.' },
        { type: 'code', language: 'python', value: 'import tensorflow as tf\nfrom tensorflow.keras import layers, Model\nfrom tensorflow.keras.applications import (\n    ResNet50, EfficientNetB0, MobileNetV2, VGG16\n)\n\n# 1. Feature Extraction (замороженный backbone)\nprint("=== Feature Extraction ===\")\nbase_model = ResNet50(weights="imagenet", include_top=False, input_shape=(224, 224, 3))\nbase_model.trainable = False  # замораживаем!\n\nmodel = tf.keras.Sequential([\n    base_model,\n    layers.GlobalAveragePooling2D(),\n    layers.Dropout(0.5),\n    layers.Dense(256, activation="relu"),\n    layers.Dropout(0.3),\n    layers.Dense(10, activation="softmax\")  # 10 классов\n])\n\nmodel.compile(optimizer=\"adam\", loss=\"categorical_crossentropy\", metrics=[\"accuracy\"])\n\nprint(f\"Total params: {model.count_params():,}\")\ntrainable = sum(p.numpy().size for p in model.trainable_variables)\nprint(f\"Trainable params: {trainable:,}\")\nprint(f\"Frozen params: {model.count_params() - trainable:,}\")\n\n# 2. Fine-tuning (размораживаем часть)\nprint(\"\\n=== Fine-tuning ===\")\nbase_model.trainable = True\n\n# Замораживаем первые N слоёв\nfor layer in base_model.layers[:140]:\n    layer.trainable = False\n\nmodel.compile(\n    optimizer=tf.keras.optimizers.Adam(1e-5),  # маленький lr!\n    loss=\"categorical_crossentropy\",\n    metrics=[\"accuracy\"]\n)\n\ntrainable_ft = sum(p.numpy().size for p in model.trainable_variables)\nprint(f\"Trainable params (fine-tune): {trainable_ft:,}\")\n\n# 3. Сравнение архитектур\nprint(\"\\n=== Популярные backbone ===\")\nmodels_info = [\n    (\"ResNet50\", ResNet50, 25.6),\n    (\"EfficientNetB0\", EfficientNetB0, 5.3),\n    (\"MobileNetV2\", MobileNetV2, 3.4),\n    (\"VGG16\", VGG16, 138.4),\n]\n\nprint(f\"{\'Модель\':<20} {\'Параметры (M)\':<15} {\'Top-1 Acc\':<12}\")\nprint(\"-\" * 47)\nfor name, cls, params in models_info:\n    # Примерные accuracy на ImageNet\n    acc = {\"ResNet50\": 76.1, \"EfficientNetB0\": 77.1, \"MobileNetV2\": 71.8, \"VGG16\": 71.3}\n    print(f\"{name:<20} {params:<15.1f} {acc[name]:<12.1f}%\")\n\nprint(\"\\n--- Стратегия ---\")\nprint(\"1. Feature extraction (frozen backbone) — мало данных (<1000)\")\nprint(\"2. Fine-tuning (unfreeze top layers) — средне данных (1000-10000)\")\nprint(\"3. Full training — много данных (>10000) или специфичная задача\")' },
        { type: 'tip', value: 'EfficientNet — лучший баланс accuracy/параметры. MobileNet — для мобильных устройств и edge. ResNet — надёжный выбор. При fine-tuning используйте lr в 10-100 раз меньше, чем для training from scratch.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Классификация изображений',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте pipeline классификации изображений с transfer learning на CIFAR-10.',
      requirements: [
        'Загрузите CIFAR-10 и нормализуйте данные',
        'Создайте модель с MobileNetV2 backbone (frozen) и custom head',
        'Добавьте GlobalAveragePooling2D, Dropout и Dense слои',
        'Обучите на 5 эпох и выведите accuracy',
        'Выведите architecture summary (trainable vs frozen params)'
      ],
      hint: 'CIFAR-10 имеет размер 32x32, а MobileNetV2 ожидает минимум 96x96. Используйте layers.Resizing(96, 96) или tf.image.resize. Не забудьте include_top=False.',
      expectedOutput: 'Transfer Learning (CIFAR-10):\nTotal params: X,XXX,XXX\nTrainable params: XXX,XXX\nFrozen params: X,XXX,XXX\n\nEpoch 5: accuracy=0.XX, val_accuracy=0.XX',
      solution: 'import tensorflow as tf\nfrom tensorflow.keras import layers\nfrom tensorflow.keras.applications import MobileNetV2\n\n(x_train, y_train), (x_test, y_test) = tf.keras.datasets.cifar10.load_data()\nx_train = x_train.astype("float32") / 255.0\nx_test = x_test.astype("float32") / 255.0\ny_train = tf.keras.utils.to_categorical(y_train, 10)\ny_test = tf.keras.utils.to_categorical(y_test, 10)\n\n# Используем подмножество для скорости\nx_train = x_train[:5000]\ny_train = y_train[:5000]\n\nbase_model = MobileNetV2(weights="imagenet", include_top=False, input_shape=(96, 96, 3))\nbase_model.trainable = False\n\nmodel = tf.keras.Sequential([\n    layers.Input(shape=(32, 32, 3)),\n    layers.Resizing(96, 96),\n    base_model,\n    layers.GlobalAveragePooling2D(),\n    layers.Dropout(0.5),\n    layers.Dense(128, activation="relu"),\n    layers.Dropout(0.3),\n    layers.Dense(10, activation="softmax")\n])\n\nmodel.compile(optimizer="adam", loss="categorical_crossentropy", metrics=["accuracy"])\n\ntotal = model.count_params()\ntrainable = sum(p.numpy().size for p in model.trainable_variables)\nprint("Transfer Learning (CIFAR-10):")\nprint(f"Total params: {total:,}")\nprint(f"Trainable params: {trainable:,}")\nprint(f"Frozen params: {total - trainable:,}")\n\nhistory = model.fit(x_train, y_train, epochs=5, batch_size=32,\n                    validation_data=(x_test[:1000], y_test[:1000]), verbose=1)\n\nprint(f"\\nFinal: accuracy={history.history[\'accuracy\'][-1]:.4f}, val_accuracy={history.history[\'val_accuracy\'][-1]:.4f}")',
      explanation: 'Transfer learning с MobileNetV2 даёт хорошие результаты даже на маленьких изображениях CIFAR-10. Заморозка backbone предотвращает разрушение pretrained features. GlobalAveragePooling2D уменьшает параметры по сравнению с Flatten. Dropout предотвращает переобучение.'
    }
  ]
}
