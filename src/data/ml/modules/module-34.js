export default {
  id: 34,
  title: 'Feature Engineering продвинутый',
  description: 'Создание признаков, методы отбора (mutual info, RFE), target encoding, взаимодействия признаков и автоматическая генерация.',
  lessons: [
    {
      id: 1,
      title: 'Создание признаков',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Feature Creation — искусство создания признаков' },
        { type: 'text', value: 'Feature Engineering — самый важный этап ML-проекта. Хорошие признаки могут значительно улучшить качество модели без изменения алгоритма. Создание признаков требует domain knowledge и понимания данных: агрегации, полиномиальные признаки, временные признаки, статистики по группам.' },
        { type: 'code', language: 'python', value: 'import pandas as pd\nimport numpy as np\nfrom sklearn.preprocessing import PolynomialFeatures\n\nnp.random.seed(42)\nn = 1000\n\ndf = pd.DataFrame({\n    "price": np.random.uniform(100, 1000, n),\n    "quantity": np.random.randint(1, 50, n),\n    "category": np.random.choice(["A", "B", "C", "D"], n),\n    "date": pd.date_range("2023-01-01", periods=n, freq="D"),\n    "customer_id": np.random.randint(1, 100, n),\n    "rating": np.random.uniform(1, 5, n)\n})\n\n# 1. Арифметические признаки\ndf["total_value"] = df["price"] * df["quantity"]\ndf["price_per_unit"] = df["price"] / df["quantity"]\ndf["log_price"] = np.log1p(df["price"])\ndf["price_squared"] = df["price"] ** 2\n\n# 2. Временные признаки\ndf["day_of_week"] = df["date"].dt.dayofweek\ndf["month"] = df["date"].dt.month\ndf["quarter"] = df["date"].dt.quarter\ndf["is_weekend"] = (df["day_of_week"] >= 5).astype(int)\ndf["day_of_year"] = df["date"].dt.dayofyear\n# Циклические признаки\ndf["month_sin"] = np.sin(2 * np.pi * df["month"] / 12)\ndf["month_cos"] = np.cos(2 * np.pi * df["month"] / 12)\n\n# 3. Агрегатные признаки по группам\ngroup_stats = df.groupby("category").agg(\n    category_mean_price=("price", "mean"),\n    category_std_price=("price", "std"),\n    category_count=("price", "count")\n).reset_index()\ndf = df.merge(group_stats, on="category")\ndf["price_vs_category_mean"] = df["price"] / df["category_mean_price"]\n\n# 4. Признаки по клиенту\ncustomer_stats = df.groupby("customer_id").agg(\n    customer_avg_price=("price", "mean"),\n    customer_total_orders=("quantity", "sum"),\n    customer_avg_rating=("rating", "mean\")\n).reset_index()\ndf = df.merge(customer_stats, on="customer_id")\n\n# 5. Бинарные/категориальные\ndf["is_expensive"] = (df["price"] > df["price"].median()).astype(int)\ndf["price_bin"] = pd.qcut(df["price"], q=5, labels=[\"cheap\", \"low\", \"mid\", \"high\", \"premium\"])\n\nprint(f"Исходные признаки: {6}\")\nprint(f\"Созданные признаки: {df.shape[1]}\")\nprint(f\"\\nНовые колонки:\")\nfor col in df.columns[6:]:\n    print(f\"  {col}: {df[col].dtype}\")' },
        { type: 'tip', value: 'Циклические признаки (sin/cos) лучше числового месяца для моделей: декабрь (12) и январь (1) должны быть близки, а числовое представление делает их далёкими.' }
      ]
    },
    {
      id: 2,
      title: 'Feature Selection: Mutual Information, RFE',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Отбор признаков' },
        { type: 'text', value: 'Отбор признаков (Feature Selection) удаляет неинформативные или избыточные признаки. Это ускоряет обучение, уменьшает переобучение и улучшает интерпретируемость. Методы: фильтрация (mutual information, correlation), wrapper (RFE), embedded (L1 регуляризация).' },
        { type: 'code', language: 'python', value: 'import numpy as np\nimport pandas as pd\nfrom sklearn.datasets import make_classification\nfrom sklearn.feature_selection import (\n    mutual_info_classif, SelectKBest, RFE,\n    SequentialFeatureSelector\n)\nfrom sklearn.ensemble import RandomForestClassifier\nfrom sklearn.linear_model import LogisticRegression\n\n# Данные с информативными и шумовыми признаками\nX, y = make_classification(n_samples=500, n_features=20, n_informative=8,\n                           n_redundant=4, n_classes=2, random_state=42)\nfeature_names = [f"f_{i}" for i in range(20)]\n\n# 1. Mutual Information\nprint("=== Mutual Information ===\")\nmi_scores = mutual_info_classif(X, y, random_state=42)\nmi_ranking = sorted(zip(feature_names, mi_scores), key=lambda x: -x[1])\nfor name, score in mi_ranking[:10]:\n    print(f"  {name}: MI={score:.4f}")\n\n# SelectKBest\nselector = SelectKBest(mutual_info_classif, k=8)\nX_selected = selector.fit_transform(X, y)\nselected_idx = selector.get_support(indices=True)\nprint(f"\\nВыбрано {len(selected_idx)} признаков: {[feature_names[i] for i in selected_idx]}")\n\n# 2. Recursive Feature Elimination (RFE)\nprint("\\n=== RFE (Recursive Feature Elimination) ===\")\nestimator = RandomForestClassifier(n_estimators=100, random_state=42)\nrfe = RFE(estimator, n_features_to_select=8, step=1)\nrfe.fit(X, y)\n\nrfe_selected = [feature_names[i] for i in range(20) if rfe.support_[i]]\nprint(f"RFE выбрал: {rfe_selected}")\nprint(f"Ранжирование: {dict(zip(feature_names, rfe.ranking_))}")\n\n# 3. L1 (Lasso) Selection\nprint("\\n=== L1 Selection ===\")\nfrom sklearn.feature_selection import SelectFromModel\nlasso = LogisticRegression(C=0.1, penalty="l1", solver="saga", max_iter=5000, random_state=42)\nlasso.fit(X, y)\n\nl1_selector = SelectFromModel(lasso, prefit=True)\nX_l1 = l1_selector.transform(X)\nl1_selected = [feature_names[i] for i in range(20) if l1_selector.get_support()[i]]\nprint(f"L1 выбрал {len(l1_selected)} признаков: {l1_selected}")\n\n# 4. Корреляция с целевой переменной\nprint("\\n=== Correlation ===\")\ncorr = np.array([np.corrcoef(X[:, i], y)[0, 1] for i in range(20)])\ntop_corr = sorted(zip(feature_names, np.abs(corr)), key=lambda x: -x[1])[:8]\nprint(f"Top-8 по корреляции:")\nfor name, c in top_corr:\n    print(f"  {name}: |corr|={c:.4f}")' },
        { type: 'note', value: 'Mutual Information обнаруживает как линейные, так и нелинейные зависимости (в отличие от корреляции). RFE учитывает взаимодействия признаков, но медленнее. L1 — самый быстрый для embedded selection.' }
      ]
    },
    {
      id: 3,
      title: 'Target Encoding',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Target Encoding для категориальных признаков' },
        { type: 'text', value: 'Target Encoding заменяет категориальное значение средним значением целевой переменной для этой категории. Это мощный метод, который сохраняет информацию о связи категории с таргетом, но требует осторожности из-за риска утечки данных (data leakage).' },
        { type: 'code', language: 'python', value: 'import numpy as np\nimport pandas as pd\nfrom sklearn.model_selection import KFold\n\nnp.random.seed(42)\nn = 1000\n\ndf = pd.DataFrame({\n    "city": np.random.choice(["Москва", "Питер", "Казань", "Новосибирск", "Сочи"], n,\n                              p=[0.3, 0.25, 0.2, 0.15, 0.1]),\n    "category": np.random.choice(["A", "B", "C", "D"], n),\n    "price": np.random.uniform(50, 500, n)\n})\n# Таргет зависит от города\ncity_effect = {"Москва": 50, "Питер": 30, "Казань": 10, "Новосибирск": 0, "Сочи": 40}\ndf["target"] = df["city"].map(city_effect) + np.random.normal(0, 20, n)\n\n# 1. Простой Target Encoding (ОПАСНО — утечка!)\nprint("=== Простой Target Encoding (с утечкой) ===\")\nmeans = df.groupby("city")["target"].mean()\ndf["city_target_naive"] = df["city"].map(means)\nprint(df.groupby("city")[["target", "city_target_naive"]].mean().round(2))\n\n# 2. Target Encoding с K-Fold (без утечки)\nprint("\\n=== Target Encoding с K-Fold ===\")\ndef target_encode_kfold(df, col, target, n_splits=5):\n    df[f"{col}_te"] = 0.0\n    kf = KFold(n_splits=n_splits, shuffle=True, random_state=42)\n    \n    for train_idx, val_idx in kf.split(df):\n        means = df.iloc[train_idx].groupby(col)[target].mean()\n        df.loc[df.index[val_idx], f"{col}_te"] = df.iloc[val_idx][col].map(means)\n    \n    # Заполняем NaN глобальным средним\n    global_mean = df[target].mean()\n    df[f"{col}_te\"].fillna(global_mean, inplace=True)\n    return df\n\ndf = target_encode_kfold(df, "city", "target")\nprint(df.groupby("city")[["target", "city_te"]].mean().round(2))\n\n# 3. Smoothed Target Encoding (с регуляризацией)\nprint("\\n=== Smoothed Target Encoding ===\")\ndef smoothed_target_encode(df, col, target, alpha=10):\n    global_mean = df[target].mean()\n    group_stats = df.groupby(col)[target].agg([\"mean\", \"count\"])\n    \n    # Smoothing: чем меньше наблюдений, тем ближе к global mean\n    smoothed = (group_stats[\"count\"] * group_stats[\"mean\"] + alpha * global_mean) / \\\n               (group_stats[\"count\"] + alpha)\n    return smoothed\n\nsmoothed = smoothed_target_encode(df, \"city\", \"target\", alpha=10)\nprint(\"Smoothed encoding:\")\nfor city, val in smoothed.items():\n    count = len(df[df[\"city\"] == city])\n    print(f\"  {city} (n={count}): {val:.2f}\")\n\nprint(f\"\\nGlobal mean: {df[\'target\'].mean():.2f}\")\nprint(\"Редкие категории ближе к global mean (регуляризация)\")' },
        { type: 'warning', value: 'НИКОГДА не используйте простой target encoding без K-Fold или smoothing! Это приводит к утечке данных (data leakage) и переобучению. Всегда кодируйте на out-of-fold данных.' }
      ]
    },
    {
      id: 4,
      title: 'Feature Interactions',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Взаимодействия признаков' },
        { type: 'text', value: 'Feature interactions — это комбинации нескольких признаков, которые совместно несут больше информации, чем по отдельности. Линейные модели не могут автоматически обнаружить взаимодействия — их нужно создавать вручную. Tree-based модели находят взаимодействия автоматически.' },
        { type: 'code', language: 'python', value: 'import numpy as np\nimport pandas as pd\nfrom sklearn.preprocessing import PolynomialFeatures\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.model_selection import cross_val_score\nfrom sklearn.datasets import make_classification\n\n# Данные, где важны взаимодействия\nnp.random.seed(42)\nn = 1000\nX = np.random.randn(n, 4)\n# y зависит от ВЗАИМОДЕЙСТВИЯ x0*x1, а не от x0 и x1 по отдельности\ny = (X[:, 0] * X[:, 1] + X[:, 2] * X[:, 3] > 0).astype(int)\n\nfeature_names = ["x0", "x1", "x2", "x3"]\n\n# 1. Без взаимодействий\nmodel = LogisticRegression(max_iter=1000)\nscore_base = cross_val_score(model, X, y, cv=5, scoring="roc_auc").mean()\nprint(f"Без взаимодействий: AUC={score_base:.4f}")\n\n# 2. Полиномиальные взаимодействия\npoly = PolynomialFeatures(degree=2, interaction_only=True, include_bias=False)\nX_poly = poly.fit_transform(X)\nscore_poly = cross_val_score(model, X_poly, y, cv=5, scoring="roc_auc").mean()\nprint(f"С полиномиальными: AUC={score_poly:.4f} ({X_poly.shape[1]} признаков)")\n\npoly_names = poly.get_feature_names_out(feature_names)\nprint(f"Признаки: {list(poly_names)}")\n\n# 3. Ручные взаимодействия\ndf = pd.DataFrame(X, columns=feature_names)\ndf["x0_x_x1"] = df["x0"] * df["x1"]\ndf["x2_x_x3"] = df["x2"] * df["x3"]\ndf["x0_plus_x1"] = df["x0"] + df["x1"]\ndf["x0_div_x1"] = df["x0"] / (df["x1"] + 1e-8)\ndf["x0_minus_x1"] = df["x0"] - df["x1"]\n\nX_manual = df[["x0", "x1", "x2", "x3", "x0_x_x1", "x2_x_x3"]].values\nscore_manual = cross_val_score(model, X_manual, y, cv=5, scoring="roc_auc").mean()\nprint(f"Ручные взаимодействия: AUC={score_manual:.4f} (6 признаков)")\n\n# 4. Обнаружение важных взаимодействий через деревья\nfrom sklearn.ensemble import RandomForestClassifier\n\nrf = RandomForestClassifier(n_estimators=100, random_state=42)\nrf.fit(X_poly, y)\n\nimportances = sorted(zip(poly_names, rf.feature_importances_), key=lambda x: -x[1])\nprint(f"\\nТоп-6 признаков (Random Forest):")\nfor name, imp in importances[:6]:\n    print(f"  {name}: {imp:.4f}")' },
        { type: 'tip', value: 'Для обнаружения важных взаимодействий: (1) обучите Random Forest на полиномиальных признаках, (2) посмотрите feature importance, (3) оставьте только важные взаимодействия. Это эффективнее, чем использовать все комбинации.' }
      ]
    },
    {
      id: 5,
      title: 'Автоматический Feature Engineering',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Автоматическое создание признаков' },
        { type: 'text', value: 'Автоматизация feature engineering экономит время и может найти признаки, которые человек пропустил. Библиотеки Featuretools и OpenFE автоматически генерируют и отбирают признаки. Deep Feature Synthesis создаёт признаки на основе связей между таблицами.' },
        { type: 'code', language: 'python', value: 'import numpy as np\nimport pandas as pd\nfrom sklearn.model_selection import cross_val_score\nfrom sklearn.ensemble import GradientBoostingClassifier\nfrom itertools import combinations\n\nnp.random.seed(42)\nn = 500\n\ndf = pd.DataFrame({\n    "age": np.random.randint(18, 70, n),\n    "income": np.random.uniform(20000, 150000, n),\n    "credit_score": np.random.randint(300, 850, n),\n    "debt": np.random.uniform(0, 50000, n),\n    "years_employed": np.random.randint(0, 30, n)\n})\ndf["target"] = ((df["income"] / (df["debt"] + 1) > 5) &\n                (df["credit_score"] > 600)).astype(int)\n\noriginal_features = ["age", "income", "credit_score", "debt", "years_employed"]\n\n# Автоматическая генерация признаков\ndef auto_feature_engineering(df, features, target):\n    \"\"\"Автоматическое создание признаков\"\"\"\n    new_features = {}\n    \n    for f in features:\n        # Унарные трансформации\n        new_features[f"{f}_log"] = np.log1p(df[f].clip(0))\n        new_features[f"{f}_sqrt"] = np.sqrt(df[f].clip(0))\n        new_features[f"{f}_squared"] = df[f] ** 2\n    \n    for f1, f2 in combinations(features, 2):\n        # Бинарные операции\n        new_features[f"{f1}_x_{f2}"] = df[f1] * df[f2]\n        new_features[f"{f1}_div_{f2}"] = df[f1] / (df[f2] + 1e-8)\n        new_features[f"{f1}_plus_{f2}"] = df[f1] + df[f2]\n        new_features[f"{f1}_minus_{f2}"] = df[f1] - df[f2]\n    \n    return pd.DataFrame(new_features)\n\nauto_df = auto_feature_engineering(df, original_features, "target")\nprint(f"Сгенерировано {auto_df.shape[1]} признаков")\n\n# Отбор лучших\nfrom sklearn.feature_selection import mutual_info_classif\n\nall_features = pd.concat([df[original_features], auto_df], axis=1)\ny = df["target"]\n\nmi_scores = mutual_info_classif(all_features, y, random_state=42)\nmi_ranking = sorted(zip(all_features.columns, mi_scores), key=lambda x: -x[1])\n\nprint(f"\\nТоп-10 признаков по Mutual Information:")\nfor name, score in mi_ranking[:10]:\n    print(f"  {name}: MI={score:.4f}")\n\n# Сравнение\nmodel = GradientBoostingClassifier(n_estimators=100, random_state=42)\n\nscore_original = cross_val_score(model, df[original_features], y, cv=5, scoring="roc_auc").mean()\n\ntop_features = [name for name, _ in mi_ranking[:15]]\nscore_auto = cross_val_score(model, all_features[top_features], y, cv=5, scoring="roc_auc").mean()\n\nprint(f"\\nОригинальные признаки ({len(original_features)}): AUC={score_original:.4f}")\nprint(f"С авто-признаками ({len(top_features)}): AUC={score_auto:.4f}")\nprint(f"Улучшение: +{(score_auto - score_original)*100:.2f}%")' },
        { type: 'list', items: [
          'Featuretools — Deep Feature Synthesis для реляционных данных',
          'OpenFE — автоматическая генерация + отбор через LightGBM',
          'TSFRESH — автоматические признаки для временных рядов',
          'Ручные признаки на domain knowledge часто лучше автоматических',
          'Всегда проверяйте: не создают ли авто-признаки data leakage'
        ] }
      ]
    },
    {
      id: 6,
      title: 'Практика: Feature Engineering Pipeline',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создайте pipeline feature engineering для задачи классификации и покажите улучшение качества.',
      requirements: [
        'Создайте датасет с 5 числовыми признаками и бинарным таргетом (500 сэмплов)',
        'Сгенерируйте минимум 10 новых признаков (log, sqrt, взаимодействия)',
        'Отберите лучшие признаки с помощью mutual_info_classif',
        'Сравните AUC модели с оригинальными и новыми признаками',
        'Выведите топ-5 лучших признаков и разницу в AUC'
      ],
      hint: 'Используйте itertools.combinations для пар признаков. np.log1p безопаснее np.log для нулей. SelectKBest автоматически отберёт лучшие.',
      expectedOutput: 'Feature Engineering Pipeline:\nСгенерировано: XX новых признаков\nТоп-5: ...\nОригинал: AUC=0.XX\nС новыми: AUC=0.XX\nУлучшение: +X.XX%',
      solution: 'import numpy as np\nimport pandas as pd\nfrom sklearn.datasets import make_classification\nfrom sklearn.model_selection import cross_val_score\nfrom sklearn.ensemble import GradientBoostingClassifier\nfrom sklearn.feature_selection import mutual_info_classif\nfrom itertools import combinations\n\nX, y = make_classification(n_samples=500, n_features=5, n_informative=3, random_state=42)\ndf = pd.DataFrame(X, columns=[f"f{i}" for i in range(5)])\nfeatures = df.columns.tolist()\n\nnew = {}\nfor f in features:\n    new[f"{f}_log"] = np.log1p(np.abs(df[f]))\n    new[f"{f}_sq"] = df[f] ** 2\nfor f1, f2 in combinations(features, 2):\n    new[f"{f1}_x_{f2}"] = df[f1] * df[f2]\n\nauto_df = pd.DataFrame(new)\nall_df = pd.concat([df, auto_df], axis=1)\n\nmi = mutual_info_classif(all_df, y, random_state=42)\nranking = sorted(zip(all_df.columns, mi), key=lambda x: -x[1])\n\nprint("Feature Engineering Pipeline:")\nprint(f"Сгенерировано: {auto_df.shape[1]} новых признаков")\nprint(f"\\nТоп-5:")\nfor name, score in ranking[:5]:\n    print(f"  {name}: MI={score:.4f}")\n\nmodel = GradientBoostingClassifier(n_estimators=100, random_state=42)\nscore_orig = cross_val_score(model, df, y, cv=5, scoring="roc_auc").mean()\n\ntop_names = [n for n, _ in ranking[:10]]\nscore_new = cross_val_score(model, all_df[top_names], y, cv=5, scoring="roc_auc").mean()\n\nprint(f"\\nОригинал: AUC={score_orig:.4f}")\nprint(f"С новыми: AUC={score_new:.4f}")\nprint(f"Улучшение: +{(score_new - score_orig)*100:.2f}%")',
      explanation: 'Feature engineering создаёт новые признаки из существующих: логарифмы помогают при скошенных распределениях, квадраты улавливают нелинейности, взаимодействия — совместное влияние признаков. Отбор через Mutual Information оставляет только информативные признаки. Даже для GradientBoosting (который сам находит взаимодействия) дополнительные признаки могут улучшить качество.'
    }
  ]
}
