export default {
  id: 61,
  title: 'Практикум: EDA и визуализация',
  description: 'Практические задачи по разведочному анализу данных (EDA): описательная статистика, визуализация распределений, корреляции, выбросы, пропуски, парные графики, временной и текстовый анализ, географические данные.',
  lessons: [
    {
      id: 1,
      title: 'Описательная статистика',
      type: 'practice',
      difficulty: 'easy',
      description: 'Получите полную описательную статистику датасета: describe, info, shape, dtypes.',
      requirements: [
        'Создайте датасет из 500 записей: age, salary, experience, education (категория), department (категория), rating',
        'Выведите shape, dtypes, info (количество non-null)',
        'Получите describe() для числовых и категориальных столбцов',
        'Выведите количество уникальных значений для каждого столбца',
        'Выведите процент пропущенных значений по каждому столбцу'
      ],
      hint: 'df.describe(include="all") покажет статистику и для категориальных столбцов. df.isnull().sum()/len(df)*100 — процент пропусков.',
      expectedOutput: 'Описательная статистика:\nShape: (500, 6)\nDtypes: ...\n\nЧисловые столбцы:\n  age: mean=XX, std=XX, min=XX, max=XX\n  salary: mean=XX, std=XX\n  ...\n\nКатегориальные:\n  education: 4 уникальных\n  department: 5 уникальных\n\nПропуски (%):\n  age: X.X%\n  salary: X.X%\n  ...',
      solution: 'import numpy as np\nimport pandas as pd\n\nnp.random.seed(42)\nn = 500\ndf = pd.DataFrame({\n    "age": np.where(np.random.random(n) < 0.05, np.nan, np.random.randint(22, 65, n).astype(float)),\n    "salary": np.where(np.random.random(n) < 0.08, np.nan, np.random.uniform(30000, 150000, n)),\n    "experience": np.where(np.random.random(n) < 0.03, np.nan, np.random.randint(0, 40, n).astype(float)),\n    "education": np.random.choice(["Bachelor", "Master", "PhD", "High School"], n, p=[0.4, 0.3, 0.1, 0.2]),\n    "department": np.random.choice(["IT", "HR", "Sales", "Finance", "Marketing"], n),\n    "rating": np.where(np.random.random(n) < 0.06, np.nan, np.round(np.random.uniform(1, 5, n), 1)),\n})\n\nprint("Описательная статистика:")\nprint(f"Shape: {df.shape}")\nprint(f"Dtypes:\\n{df.dtypes}\\n")\n\nprint("Non-null counts:")\nfor col in df.columns:\n    print(f"  {col}: {df[col].notna().sum()} / {len(df)}")\n\nprint("\\nЧисловые столбцы:")\nfor col in df.select_dtypes(include=[np.number]).columns:\n    print(f"  {col}: mean={df[col].mean():.1f}, std={df[col].std():.1f}, min={df[col].min():.1f}, max={df[col].max():.1f}")\n\nprint("\\nКатегориальные:")\nfor col in df.select_dtypes(include=["object"]).columns:\n    print(f"  {col}: {df[col].nunique()} уникальных — {df[col].value_counts().to_dict()}")\n\nprint("\\nПропуски (%):")\nfor col in df.columns:\n    pct = df[col].isnull().sum() / len(df) * 100\n    print(f"  {col}: {pct:.1f}%")\n\nprint("\\ndescribe():")\nprint(df.describe(include="all").to_string())',
      explanation: 'Описательная статистика — первый шаг EDA. shape показывает размер, dtypes — типы данных, describe() — распределение числовых и категориальных переменных. Проверка пропусков определяет стратегию обработки данных. Всегда начинайте анализ с этих шагов.'
    },
    {
      id: 2,
      title: 'Распределения фичей',
      type: 'practice',
      difficulty: 'easy',
      description: 'Визуализируйте распределения числовых признаков с помощью гистограмм и KDE plots.',
      requirements: [
        'Создайте датасет из 1000 записей с 4 числовыми столбцами разных распределений (нормальное, скошенное, бимодальное, равномерное)',
        'Постройте гистограммы с KDE для каждого столбца в сетке 2x2',
        'Посчитайте skewness и kurtosis для каждого столбца',
        'Определите тип распределения по значениям skewness',
        'Выведите рекомендации по трансформации для каждого столбца'
      ],
      hint: 'scipy.stats.skew() и kurtosis() для расчёта. |skewness| > 1 — сильная асимметрия, log-transform помогает. Бимодальное распределение видно на KDE.',
      expectedOutput: 'Распределения фичей:\n  normal: skew=0.XX, kurtosis=0.XX — нормальное ✓\n  skewed: skew=X.XX, kurtosis=X.XX — правый хвост → log-transform\n  bimodal: skew=0.XX, kurtosis=-X.XX — бимодальное → разделить\n  uniform: skew=0.XX, kurtosis=-X.XX — равномерное ✓\n\nГрафики сохранены: distributions.png',
      solution: 'import numpy as np\nimport pandas as pd\nfrom scipy import stats\nimport matplotlib\nmatplotlib.use("Agg")\nimport matplotlib.pyplot as plt\nimport seaborn as sns\n\nnp.random.seed(42)\nn = 1000\ndf = pd.DataFrame({\n    "normal": np.random.normal(50, 10, n),\n    "skewed": np.random.exponential(2, n),\n    "bimodal": np.concatenate([np.random.normal(30, 5, n//2), np.random.normal(60, 5, n//2)]),\n    "uniform": np.random.uniform(0, 100, n)\n})\n\nfig, axes = plt.subplots(2, 2, figsize=(12, 10))\nfig.suptitle("Распределения фичей", fontsize=14)\n\nprint("Распределения фичей:")\nfor idx, col in enumerate(df.columns):\n    ax = axes[idx // 2][idx % 2]\n    sns.histplot(df[col], kde=True, ax=ax, bins=30, color=["steelblue","coral","green","purple"][idx])\n    ax.set_title(f"{col}")\n    ax.set_xlabel("")\n\n    sk = stats.skew(df[col])\n    ku = stats.kurtosis(df[col])\n\n    if abs(sk) < 0.5:\n        if ku < -1:\n            dist_type = "бимодальное → разделить"\n        elif ku < -0.5:\n            dist_type = "равномерное ✓"\n        else:\n            dist_type = "нормальное ✓"\n    elif sk > 1:\n        dist_type = "правый хвост → log-transform"\n    elif sk < -1:\n        dist_type = "левый хвост → power-transform"\n    else:\n        dist_type = "умеренная асимметрия → StandardScaler"\n\n    print(f"  {col}: skew={sk:.2f}, kurtosis={ku:.2f} — {dist_type}")\n\nplt.tight_layout()\nplt.savefig("distributions.png", dpi=100)\nprint("\\nГрафики сохранены: distributions.png")',
      explanation: 'Анализ распределений показывает, нужны ли трансформации. Нормальное распределение — идеально для линейных моделей. Скошенные данные (skew > 1) нуждаются в log/sqrt-transform. Бимодальное распределение может указывать на два подкласса. KDE plot визуально подтверждает тип распределения.'
    },
    {
      id: 3,
      title: 'Корреляционная матрица',
      type: 'practice',
      difficulty: 'easy',
      description: 'Постройте корреляционную матрицу и выделите топ-корреляции.',
      requirements: [
        'Создайте датасет из 500 записей с 8 числовыми столбцами, часть из которых сильно коррелированы',
        'Вычислите Pearson и Spearman корреляции',
        'Постройте heatmap с аннотациями',
        'Найдите и выведите топ-5 пар по абсолютной корреляции',
        'Определите мультиколлинеарные пары (|r| > 0.8)'
      ],
      hint: 'np.triu_indices_from() для маски верхнего треугольника. Unstack + sort_values для нахождения топ-корреляций. VIF > 10 тоже указывает на мультиколлинеарность.',
      expectedOutput: 'Корреляционная матрица:\nТоп-5 пар (Pearson):\n  1. feature_a & feature_b: r=0.XX\n  2. ...\n\nМультиколлинеарные (|r| > 0.8):\n  feature_a & feature_b: r=0.XX → удалить одну\n\nHeatmap сохранён: correlation.png',
      solution: 'import numpy as np\nimport pandas as pd\nimport matplotlib\nmatplotlib.use("Agg")\nimport matplotlib.pyplot as plt\nimport seaborn as sns\n\nnp.random.seed(42)\nn = 500\nbase1 = np.random.randn(n)\nbase2 = np.random.randn(n)\ndf = pd.DataFrame({\n    "height": base1 * 10 + 170,\n    "weight": base1 * 8 + 70 + np.random.randn(n) * 3,\n    "age": np.random.randint(20, 65, n).astype(float),\n    "income": base2 * 20000 + 60000,\n    "spending": base2 * 15000 + 40000 + np.random.randn(n) * 5000,\n    "education_years": np.random.randint(10, 22, n).astype(float),\n    "experience": np.random.randint(0, 40, n).astype(float),\n    "satisfaction": np.random.uniform(1, 10, n)\n})\n\npearson_corr = df.corr(method="pearson")\nspearman_corr = df.corr(method="spearman")\n\nfig, axes = plt.subplots(1, 2, figsize=(18, 7))\nfor ax, corr, title in zip(axes, [pearson_corr, spearman_corr], ["Pearson", "Spearman"]):\n    mask = np.triu(np.ones_like(corr, dtype=bool), k=1)\n    sns.heatmap(corr, mask=mask, annot=True, fmt=".2f", cmap="coolwarm",\n                center=0, vmin=-1, vmax=1, ax=ax)\n    ax.set_title(f"{title} Correlation")\nplt.tight_layout()\nplt.savefig("correlation.png", dpi=100)\n\n# Топ пары\nupper = pearson_corr.where(np.triu(np.ones_like(pearson_corr, dtype=bool), k=1))\npairs = upper.unstack().dropna().reset_index()\npairs.columns = ["feature_1", "feature_2", "correlation"]\npairs["abs_corr"] = pairs["correlation"].abs()\npairs = pairs.sort_values("abs_corr", ascending=False)\n\nprint("Корреляционная матрица:")\nprint("Топ-5 пар (Pearson):")\nfor i, row in pairs.head(5).iterrows():\n    print(f"  {row[\'feature_1\']} & {row[\'feature_2\']}: r={row[\'correlation\']:.4f}")\n\nmulticol = pairs[pairs["abs_corr"] > 0.8]\nprint(f"\\nМультиколлинеарные (|r| > 0.8): {len(multicol)} пар")\nfor _, row in multicol.iterrows():\n    print(f"  {row[\'feature_1\']} & {row[\'feature_2\']}: r={row[\'correlation\']:.4f} → удалить одну")\n\nprint("\\nHeatmap сохранён: correlation.png")',
      explanation: 'Корреляционная матрица выявляет связи между признаками. Pearson — для линейных связей, Spearman — для монотонных. Мультиколлинеарность (|r| > 0.8) вредит линейным моделям: удалите один из коррелированных признаков. Heatmap даёт быстрый визуальный обзор.'
    },
    {
      id: 4,
      title: 'Анализ выбросов',
      type: 'practice',
      difficulty: 'medium',
      description: 'Обнаружьте и проанализируйте выбросы с помощью IQR, z-score и boxplots.',
      requirements: [
        'Создайте датасет из 500 записей с несколькими столбцами, включая выбросы (5-10%)',
        'Обнаружьте выбросы методом IQR (Q1 - 1.5*IQR, Q3 + 1.5*IQR)',
        'Обнаружьте выбросы методом z-score (|z| > 3)',
        'Постройте boxplots для каждого столбца',
        'Сравните результаты двух методов и выведите рекомендации'
      ],
      hint: 'IQR чувствителен к распределению — для скошенных данных используйте Modified Z-score (на основе MAD). np.percentile(data, [25, 75]) для квартилей.',
      expectedOutput: 'Анализ выбросов:\n\nМетод IQR:\n  salary: XX выбросов (X.X%)\n  age: XX выбросов (X.X%)\n  ...\n\nМетод Z-score (|z| > 3):\n  salary: XX выбросов (X.X%)\n  ...\n\nСравнение: IQR нашёл XX, Z-score нашёл XX\nРекомендации: ...',
      solution: 'import numpy as np\nimport pandas as pd\nfrom scipy import stats\nimport matplotlib\nmatplotlib.use("Agg")\nimport matplotlib.pyplot as plt\n\nnp.random.seed(42)\nn = 500\nnormal_salary = np.random.normal(60000, 15000, int(n * 0.92))\noutlier_salary = np.random.uniform(150000, 300000, int(n * 0.08))\nsalary = np.concatenate([normal_salary, outlier_salary])\nnp.random.shuffle(salary)\n\nnormal_age = np.random.normal(35, 8, int(n * 0.95))\noutlier_age = np.array([90, 95, 92, 88, 91, 5, 4, 6, 3, 7, 85, 89, 93, 94, 2, 1, 96, 97, 98, 99, 100, 87, 86, 84, 91])\nage = np.concatenate([normal_age, outlier_age])\nnp.random.shuffle(age)\nage = age[:n]\n\ndf = pd.DataFrame({\n    "salary": salary[:n],\n    "age": age,\n    "experience": np.where(np.random.random(n) < 0.07, np.random.uniform(45, 60, n), np.random.uniform(0, 35, n)),\n    "rating": np.where(np.random.random(n) < 0.05, np.random.choice([0.1, 0.2, 9.9, 10.0], n), np.random.uniform(2, 8, n)),\n})\n\nprint("Анализ выбросов:\\n")\n\n# IQR метод\nprint("Метод IQR:")\niqr_counts = {}\nfor col in df.columns:\n    Q1 = df[col].quantile(0.25)\n    Q3 = df[col].quantile(0.75)\n    IQR = Q3 - Q1\n    lower = Q1 - 1.5 * IQR\n    upper = Q3 + 1.5 * IQR\n    outliers = df[(df[col] < lower) | (df[col] > upper)]\n    iqr_counts[col] = len(outliers)\n    print(f"  {col}: {len(outliers)} выбросов ({len(outliers)/n*100:.1f}%) — bounds: [{lower:.1f}, {upper:.1f}]")\n\n# Z-score метод\nprint("\\nМетод Z-score (|z| > 3):")\nz_counts = {}\nfor col in df.columns:\n    z = np.abs(stats.zscore(df[col]))\n    outliers_z = np.sum(z > 3)\n    z_counts[col] = outliers_z\n    print(f"  {col}: {outliers_z} выбросов ({outliers_z/n*100:.1f}%)")\n\ntotal_iqr = sum(iqr_counts.values())\ntotal_z = sum(z_counts.values())\nprint(f"\\nСравнение: IQR нашёл {total_iqr} выбросов, Z-score нашёл {total_z}")\nprint("Рекомендации:")\nprint("  - IQR лучше для скошенных распределений")\nprint("  - Z-score лучше для нормальных распределений")\nprint("  - Для робастности используйте Modified Z-score (MAD)")\nprint("  - Не удаляйте выбросы автоматически — проверьте бизнес-логику")\n\nfig, axes = plt.subplots(1, len(df.columns), figsize=(16, 5))\nfor i, col in enumerate(df.columns):\n    axes[i].boxplot(df[col].dropna())\n    axes[i].set_title(col)\n    axes[i].set_ylabel("Value")\nplt.suptitle("Boxplots для обнаружения выбросов", fontsize=14)\nplt.tight_layout()\nplt.savefig("outliers_boxplots.png", dpi=100)\nprint("\\nBoxplots сохранены: outliers_boxplots.png")',
      explanation: 'Выбросы искажают статистики и вредят моделям. IQR-метод (1.5*IQR от квартилей) — робастный к распределению. Z-score (|z| > 3) предполагает нормальность. На практике комбинируйте оба метода и всегда проверяйте выбросы вручную — они могут быть реальными (VIP-клиенты) или ошибками ввода.'
    },
    {
      id: 5,
      title: 'Анализ пропущенных значений',
      type: 'practice',
      difficulty: 'medium',
      description: 'Проанализируйте паттерны пропущенных значений и примените стратегии заполнения.',
      requirements: [
        'Создайте датасет из 500 записей с разными паттернами пропусков (MCAR, MAR, MNAR)',
        'Визуализируйте паттерны пропусков (матрица и bar chart)',
        'Определите тип пропусков для каждого столбца',
        'Примените 3 стратегии: median, mode, KNN imputer',
        'Сравните распределения до и после заполнения'
      ],
      hint: 'MCAR — пропуски случайны. MAR — зависят от других столбцов. MNAR — зависят от самого столбца. KNNImputer из sklearn для умного заполнения.',
      expectedOutput: 'Анализ пропущенных значений:\nПропуски по столбцам:\n  age: XX (X.X%) — MCAR\n  income: XX (X.X%) — MAR\n  satisfaction: XX (X.X%) — MNAR\n\nСтратегии заполнения:\n  Median: mean_diff=X.XX\n  Mode: mean_diff=X.XX\n  KNN: mean_diff=X.XX\nЛучшая: KNN',
      solution: 'import numpy as np\nimport pandas as pd\nfrom sklearn.impute import KNNImputer\nfrom sklearn.preprocessing import LabelEncoder\nimport matplotlib\nmatplotlib.use("Agg")\nimport matplotlib.pyplot as plt\n\nnp.random.seed(42)\nn = 500\nage = np.random.normal(40, 12, n)\nincome = np.random.normal(60000, 20000, n)\neducation = np.random.choice(["High School", "Bachelor", "Master", "PhD"], n, p=[0.2, 0.4, 0.3, 0.1])\nsatisfaction = np.random.uniform(1, 10, n)\nexperience = np.random.uniform(0, 35, n)\n\ndf = pd.DataFrame({"age": age, "income": income, "education": education,\n                    "satisfaction": satisfaction, "experience": experience})\ndf_original = df.copy()\n\n# MCAR — случайные пропуски в age\nmcar_mask = np.random.random(n) < 0.10\ndf.loc[mcar_mask, "age"] = np.nan\n\n# MAR — income пропущен для молодых\nmar_mask = (df_original["age"] < 30) & (np.random.random(n) < 0.40)\ndf.loc[mar_mask, "income"] = np.nan\n\n# MNAR — satisfaction пропущен для недовольных\nmnar_mask = (df_original["satisfaction"] < 3) & (np.random.random(n) < 0.60)\ndf.loc[mnar_mask, "satisfaction"] = np.nan\n\nprint("Анализ пропущенных значений:")\nprint("Пропуски по столбцам:")\nfor col in df.columns:\n    miss = df[col].isnull().sum()\n    pct = miss / n * 100\n    miss_type = "MCAR" if col == "age" else ("MAR" if col == "income" else ("MNAR" if col == "satisfaction" else "нет"))\n    if miss > 0:\n        print(f"  {col}: {miss} ({pct:.1f}%) — {miss_type}")\n\n# Визуализация\nfig, axes = plt.subplots(1, 2, figsize=(14, 5))\nmissing_pct = df.isnull().sum() / n * 100\nmissing_pct[missing_pct > 0].plot(kind="bar", ax=axes[0], color="coral")\naxes[0].set_title("Процент пропусков")\naxes[0].set_ylabel("%")\n\nmissing_matrix = df.isnull().astype(int)\naxes[1].imshow(missing_matrix.values[:50].T, aspect="auto", cmap="YlOrRd", interpolation="none")\naxes[1].set_yticks(range(len(df.columns)))\naxes[1].set_yticklabels(df.columns)\naxes[1].set_title("Паттерн пропусков (первые 50 строк)")\naxes[1].set_xlabel("Строки")\nplt.tight_layout()\nplt.savefig("missing_values.png", dpi=100)\n\n# Стратегии заполнения\nnumeric_cols = ["age", "income", "satisfaction", "experience"]\noriginal_means = df_original[numeric_cols].mean()\n\nprint("\\nСтратегии заполнения:")\nresults = {}\n\n# Median\ndf_median = df.copy()\nfor col in numeric_cols:\n    df_median[col].fillna(df_median[col].median(), inplace=True)\nmedian_diff = (df_median[numeric_cols].mean() - original_means).abs().mean()\nresults["Median"] = median_diff\nprint(f"  Median: mean_diff={median_diff:.2f}")\n\n# Mode\ndf_mode = df.copy()\nfor col in df_mode.columns:\n    df_mode[col].fillna(df_mode[col].mode()[0], inplace=True)\nmode_diff = (df_mode[numeric_cols].mean() - original_means).abs().mean()\nresults["Mode"] = mode_diff\nprint(f"  Mode: mean_diff={mode_diff:.2f}")\n\n# KNN\ndf_knn = df.copy()\nle = LabelEncoder()\ndf_knn["education_enc"] = le.fit_transform(df_knn["education"].fillna("Unknown"))\nknn_data = df_knn[["age", "income", "satisfaction", "experience", "education_enc"]]\nimputer = KNNImputer(n_neighbors=5)\nknn_filled = imputer.fit_transform(knn_data)\ndf_knn_result = pd.DataFrame(knn_filled, columns=knn_data.columns)\nknn_diff = (df_knn_result[numeric_cols].mean() - original_means).abs().mean()\nresults["KNN"] = knn_diff\nprint(f"  KNN: mean_diff={knn_diff:.2f}")\n\nbest = min(results, key=results.get)\nprint(f"Лучшая: {best}")\nprint("\\nГрафик сохранён: missing_values.png")',
      explanation: 'Паттерн пропусков определяет стратегию: MCAR — можно удалять строки безопасно; MAR — median/KNN imputer; MNAR — нужна доменная логика. KNNImputer учитывает соседние объекты и обычно сохраняет распределение лучше, чем простое заполнение медианой.'
    },
    {
      id: 6,
      title: 'Парные графики',
      type: 'practice',
      difficulty: 'medium',
      description: 'Постройте парные графики (pairplot) и scatter matrix с раскраской по целевой переменной.',
      requirements: [
        'Создайте датасет из 300 записей с 4 числовыми фичами и бинарным target, чтобы классы были визуально разделимы',
        'Постройте sns.pairplot с hue по target',
        'Постройте scatter matrix с pd.plotting',
        'Определите визуально наиболее разделяющие фичи',
        'Подтвердите вывод числовым расчётом (разница средних / std)'
      ],
      hint: 'Хорошая разделяющая фича — та, где распределения для двух классов минимально пересекаются. Cohen\'s d = |mean1 - mean2| / pooled_std.',
      expectedOutput: 'Парные графики:\nДатасет: 300 x 5 (4 фичи + target)\n\nРазделяющая способность (Cohen d):\n  feature_1: d=X.XX\n  feature_2: d=X.XX\n  ...\nНаиболее разделяющая: feature_X\n\nPairplot сохранён: pairplot.png',
      solution: 'import numpy as np\nimport pandas as pd\nimport matplotlib\nmatplotlib.use("Agg")\nimport matplotlib.pyplot as plt\nimport seaborn as sns\n\nnp.random.seed(42)\nn = 300\nn0, n1 = 150, 150\n\nf1_0, f1_1 = np.random.normal(2, 1, n0), np.random.normal(5, 1, n1)\nf2_0, f2_1 = np.random.normal(10, 3, n0), np.random.normal(12, 3, n1)\nf3_0, f3_1 = np.random.normal(0, 1, n0), np.random.normal(0.5, 1, n1)\nf4_0, f4_1 = np.random.normal(50, 10, n0), np.random.normal(80, 10, n1)\n\ndf = pd.DataFrame({\n    "feature_1": np.concatenate([f1_0, f1_1]),\n    "feature_2": np.concatenate([f2_0, f2_1]),\n    "feature_3": np.concatenate([f3_0, f3_1]),\n    "feature_4": np.concatenate([f4_0, f4_1]),\n    "target": np.array([0]*n0 + [1]*n1)\n})\n\nprint("Парные графики:")\nprint(f"Датасет: {df.shape[0]} x {df.shape[1]} (4 фичи + target)")\n\n# Pairplot\ng = sns.pairplot(df, hue="target", palette="Set1", diag_kind="kde",\n                 plot_kws={"alpha": 0.6, "s": 20})\ng.fig.suptitle("Pairplot по target", y=1.02)\nplt.savefig("pairplot.png", dpi=100, bbox_inches="tight")\nplt.close()\n\n# Cohen\'s d\nprint("\\nРазделяющая способность (Cohen d):")\nfeatures = ["feature_1", "feature_2", "feature_3", "feature_4"]\ncohen_d_values = {}\nfor f in features:\n    g0 = df[df["target"] == 0][f]\n    g1 = df[df["target"] == 1][f]\n    pooled_std = np.sqrt((g0.std()**2 + g1.std()**2) / 2)\n    d = abs(g0.mean() - g1.mean()) / pooled_std\n    cohen_d_values[f] = d\n    strength = "сильная" if d > 0.8 else ("средняя" if d > 0.5 else "слабая")\n    print(f"  {f}: d={d:.2f} ({strength})")\n\nbest_feature = max(cohen_d_values, key=cohen_d_values.get)\nprint(f"\\nНаиболее разделяющая: {best_feature} (d={cohen_d_values[best_feature]:.2f})")\nprint("Pairplot сохранён: pairplot.png")',
      explanation: 'Pairplot показывает все пары scatter plots с раскраской по классу — быстро видно, какие фичи разделяют классы. Cohen\'s d количественно оценивает разделение: d > 0.8 — сильная разница, d > 0.5 — средняя. Лучшие фичи для классификации — те, где d максимален.'
    },
    {
      id: 7,
      title: 'Временной анализ',
      type: 'practice',
      difficulty: 'medium',
      description: 'Проанализируйте временные тренды и сезонность из датированных данных.',
      requirements: [
        'Создайте датасет продаж за 3 года (1095 дней) с трендом и недельной/годовой сезонностью',
        'Визуализируйте тренд с помощью скользящего среднего (7 и 30 дней)',
        'Выделите недельную сезонность: средние продажи по дням недели',
        'Выделите месячную сезонность: средние продажи по месяцам',
        'Выполните декомпозицию ряда: trend + seasonal + residual'
      ],
      hint: 'statsmodels.tsa.seasonal_decompose() для декомпозиции. rolling(7).mean() для недельного скользящего среднего. groupby(day_of_week) для недельной сезонности.',
      expectedOutput: 'Временной анализ продаж:\nПериод: 2021-01-01 — 2023-12-31 (1095 дней)\n\nТренд: рост на XX% за 3 года\nНедельная сезонность:\n  Пн: XXX, Вт: XXX, ..., Вс: XXX\n  Пик: суббота\nМесячная сезонность:\n  Пик: декабрь (XXX)\n  Минимум: январь (XXX)',
      solution: 'import numpy as np\nimport pandas as pd\nfrom statsmodels.tsa.seasonal import seasonal_decompose\nimport matplotlib\nmatplotlib.use("Agg")\nimport matplotlib.pyplot as plt\n\nnp.random.seed(42)\nn = 1095\ndates = pd.date_range("2021-01-01", periods=n, freq="D")\n\ntrend = np.linspace(100, 160, n)\nweekly = 15 * np.sin(2 * np.pi * np.arange(n) / 7)\nmonthly = 25 * np.sin(2 * np.pi * (dates.month - 1) / 12)\nnoise = np.random.normal(0, 8, n)\nsales = trend + weekly + monthly + noise\n\ndf = pd.DataFrame({"date": dates, "sales": sales})\ndf.set_index("date", inplace=True)\n\nprint("Временной анализ продаж:")\nprint(f"Период: {df.index[0].date()} — {df.index[-1].date()} ({len(df)} дней)")\n\n# Тренд\ngrowth = (df[\"sales\"].iloc[-30:].mean() / df[\"sales\"].iloc[:30].mean() - 1) * 100\nprint(f"\\nТренд: рост на {growth:.1f}% за 3 года")\n\n# Скользящие средние\ndf[\"ma_7\"] = df[\"sales\"].rolling(7).mean()\ndf[\"ma_30\"] = df[\"sales\"].rolling(30).mean()\n\n# Недельная сезонность\ndf[\"day_of_week\"] = df.index.dayofweek\nday_names = [\"Пн\", \"Вт\", \"Ср\", \"Чт\", \"Пт\", \"Сб\", \"Вс\"]\nweekly_avg = df.groupby(\"day_of_week\")[\"sales\"].mean()\nprint(\"\\nНедельная сезонность:\")\nfor i, avg in weekly_avg.items():\n    print(f\"  {day_names[i]}: {avg:.1f}\")\nprint(f\"  Пик: {day_names[weekly_avg.idxmax()]}\")\n\n# Месячная сезонность\ndf[\"month\"] = df.index.month\nmonth_names = [\"Янв\",\"Фев\",\"Мар\",\"Апр\",\"Май\",\"Июн\",\"Июл\",\"Авг\",\"Сен\",\"Окт\",\"Ноя\",\"Дек\"]\nmonthly_avg = df.groupby(\"month\")[\"sales\"].mean()\nprint(\"\\nМесячная сезонность:\")\nfor m, avg in monthly_avg.items():\n    print(f\"  {month_names[m-1]}: {avg:.1f}\")\nprint(f\"  Пик: {month_names[monthly_avg.idxmax()-1]} ({monthly_avg.max():.1f})\")\nprint(f\"  Минимум: {month_names[monthly_avg.idxmin()-1]} ({monthly_avg.min():.1f})\")\n\n# Декомпозиция\nresult = seasonal_decompose(df[\"sales\"], model=\"additive\", period=7)\nfig, axes = plt.subplots(4, 1, figsize=(14, 10))\nresult.observed.plot(ax=axes[0], title=\"Observed\")\nresult.trend.plot(ax=axes[1], title=\"Trend\")\nresult.seasonal.plot(ax=axes[2], title=\"Seasonal\")\nresult.resid.plot(ax=axes[3], title=\"Residual\")\nplt.tight_layout()\nplt.savefig(\"time_analysis.png\", dpi=100)\nprint(\"\\nДекомпозиция сохранена: time_analysis.png\")',
      explanation: 'Временной анализ раскладывает ряд на компоненты: тренд (долгосрочное направление), сезонность (повторяющиеся паттерны) и шум. Скользящие средние сглаживают шум и показывают тренд. Группировка по дням/месяцам выявляет сезонность. seasonal_decompose автоматизирует этот процесс.'
    },
    {
      id: 8,
      title: 'Текстовый EDA',
      type: 'practice',
      difficulty: 'medium',
      description: 'Проведите разведочный анализ текстовых данных: частоты слов, длины, wordcloud.',
      requirements: [
        'Создайте датасет из 200 текстовых отзывов (положительных и отрицательных)',
        'Посчитайте базовые статистики: средняя длина, количество слов, количество уникальных слов',
        'Найдите топ-20 самых частых слов (без стоп-слов)',
        'Постройте wordcloud для положительных и отрицательных отзывов',
        'Сравните распределения длин текстов по классам'
      ],
      hint: 'collections.Counter для подсчёта частот. WordCloud(width=800, height=400).generate(text) для облака слов. Стоп-слова: предлоги, союзы, местоимения.',
      expectedOutput: 'Текстовый EDA:\nВсего отзывов: 200\nПоложительных: XX, Отрицательных: XX\n\nСтатистики:\n  Средняя длина: XX.X символов\n  Среднее кол-во слов: XX.X\n  Уникальных слов: XXXX\n\nТоп-10 слов (positive): ...\nТоп-10 слов (negative): ...',
      solution: 'import numpy as np\nimport pandas as pd\nfrom collections import Counter\nimport re\nimport matplotlib\nmatplotlib.use("Agg")\nimport matplotlib.pyplot as plt\n\nnp.random.seed(42)\n\npositive_templates = [\n    "Отличный продукт рекомендую всем превосходное качество",\n    "Замечательная вещь доволен покупкой быстрая доставка",\n    "Превосходный сервис приятный персонал вернусь снова",\n    "Лучшее качество за эту цену очень рекомендую друзьям",\n    "Прекрасный товар полностью соответствует описанию спасибо",\n    "Великолепно работает без нареканий отличный выбор",\n    "Идеальное решение для задачи качество на высоте",\n    "Очень доволен покупкой превзошло ожидания рекомендую",\n    "Шикарный продукт отличная упаковка быстро доставили",\n    "Потрясающее качество удобный интерфейс всё работает"\n]\n\nnegative_templates = [\n    "Ужасное качество сломался через неделю не рекомендую",\n    "Плохой товар не соответствует описанию разочарован",\n    "Отвратительный сервис долгая доставка повреждённая упаковка",\n    "Деньги на ветер худшая покупка в жизни",\n    "Не работает вообще брак возврат денег требую",\n    "Очень плохое качество материалов дешёвая подделка",\n    "Ужасная поддержка никто не отвечает проблема не решена",\n    "Разочарован полностью бесполезный товар пустая трата",\n    "Некачественный продукт запах химии опасно для здоровья",\n    "Худший опыт покупки грубый персонал больше не приду"\n]\n\ntexts, labels = [], []\nfor _ in range(100):\n    tmpl = np.random.choice(positive_templates)\n    words = tmpl.split()\n    n_words = np.random.randint(4, len(words) + 1)\n    selected = list(np.random.choice(words, n_words, replace=False))\n    extra = np.random.choice(["очень", "крайне", "весьма", "действительно", "невероятно", ""], 1)[0]\n    text = extra + " " + " ".join(selected) if extra else " ".join(selected)\n    texts.append(text.strip())\n    labels.append(1)\n\nfor _ in range(100):\n    tmpl = np.random.choice(negative_templates)\n    words = tmpl.split()\n    n_words = np.random.randint(4, len(words) + 1)\n    selected = list(np.random.choice(words, n_words, replace=False))\n    extra = np.random.choice(["очень", "крайне", "абсолютно", "совершенно", ""], 1)[0]\n    text = extra + " " + " ".join(selected) if extra else " ".join(selected)\n    texts.append(text.strip())\n    labels.append(0)\n\ndf = pd.DataFrame({"text": texts, "label": labels})\ndf["text_len"] = df["text"].apply(len)\ndf["word_count"] = df["text"].apply(lambda x: len(x.split()))\n\nprint("Текстовый EDA:")\nprint(f"Всего отзывов: {len(df)}")\nprint(f"Положительных: {df[\'label\'].sum()}, Отрицательных: {(df[\'label\']==0).sum()}")\n\nall_words = " ".join(df["text"]).lower().split()\nunique_words = set(all_words)\nprint(f"\\nСтатистики:")\nprint(f"  Средняя длина: {df[\'text_len\'].mean():.1f} символов")\nprint(f"  Среднее кол-во слов: {df[\'word_count\'].mean():.1f}")\nprint(f"  Уникальных слов: {len(unique_words)}")\n\nstop_words = {"и", "в", "на", "с", "по", "для", "не", "из", "за", "к", "от", "до", "о", "а", "но", "же", "ли", "бы", "что", "это", "как", "так"}\n\nfor label_val, label_name in [(1, "positive"), (0, "negative")]:\n    subset = df[df["label"] == label_val]\n    words = " ".join(subset["text"]).lower().split()\n    words_clean = [w for w in words if w not in stop_words and len(w) > 2]\n    counter = Counter(words_clean)\n    top10 = counter.most_common(10)\n    print(f"\\nТоп-10 слов ({label_name}):")\n    for word, count in top10:\n        print(f"  {word}: {count}")\n\nfig, axes = plt.subplots(1, 2, figsize=(14, 5))\nfor label_val, label_name, ax in zip([1, 0], ["Положительные", "Отрицательные"], axes):\n    subset = df[df["label"] == label_val]\n    ax.hist(subset["word_count"], bins=15, alpha=0.7, color="green" if label_val else "red")\n    ax.set_title(f"{label_name}: распределение длин")\n    ax.set_xlabel("Количество слов")\n    ax.set_ylabel("Частота")\nplt.tight_layout()\nplt.savefig("text_eda.png", dpi=100)\nprint("\\nГрафики сохранены: text_eda.png")',
      explanation: 'Текстовый EDA выявляет паттерны в тексте: частые слова (показывают тематику), длины текстов (могут различаться по классам), уникальные слова (словарный запас). Wordcloud даёт визуальный обзор. Удаление стоп-слов критично для корректного анализа частот.'
    },
    {
      id: 9,
      title: 'Географический EDA',
      type: 'practice',
      difficulty: 'medium',
      description: 'Визуализируйте географические данные с координатами на карте.',
      requirements: [
        'Создайте датасет из 200 точек с координатами (lat, lon), категорией и значением в разных городах мира',
        'Визуализируйте точки на scatter plot с цветом по категории и размером по значению',
        'Рассчитайте статистики по регионам: количество, среднее значение',
        'Найдите кластеры точек по близости (простой distance-based)',
        'Постройте heatmap плотности на 2D-пространстве координат'
      ],
      hint: 'Для scatter на координатах: plt.scatter(lon, lat, c=category, s=value). scipy.spatial.distance для расчёта расстояний. plt.hexbin для heatmap плотности.',
      expectedOutput: 'Географический EDA:\nТочек: 200\nРегионов: X\n\nСтатистики по регионам:\n  Europe: XX точек, mean_value=XX.X\n  Asia: XX точек, mean_value=XX.X\n  ...\n\nКластеров найдено: X\nГрафики сохранены: geo_eda.png',
      solution: 'import numpy as np\nimport pandas as pd\nfrom scipy.spatial.distance import cdist\nfrom sklearn.cluster import DBSCAN\nimport matplotlib\nmatplotlib.use("Agg")\nimport matplotlib.pyplot as plt\n\nnp.random.seed(42)\n\ncities = {\n    "Europe": {"lat": (48, 55), "lon": (2, 20), "n": 50},\n    "Asia": {"lat": (30, 45), "lon": (100, 140), "n": 50},\n    "North America": {"lat": (30, 48), "lon": (-120, -75), "n": 40},\n    "South America": {"lat": (-35, -5), "lon": (-75, -40), "n": 30},\n    "Africa": {"lat": (-10, 15), "lon": (10, 40), "n": 30},\n}\n\nrows = []\nfor region, params in cities.items():\n    for _ in range(params["n"]):\n        lat = np.random.uniform(*params["lat"])\n        lon = np.random.uniform(*params["lon"])\n        value = np.random.uniform(10, 100)\n        category = np.random.choice(["store", "warehouse", "office"])\n        rows.append({"lat": lat, "lon": lon, "region": region, "category": category, "value": value})\n\ndf = pd.DataFrame(rows)\nprint("Географический EDA:")\nprint(f"Точек: {len(df)}")\nprint(f"Регионов: {df[\'region\'].nunique()}")\n\nprint("\\nСтатистики по регионам:")\nfor region, group in df.groupby("region"):\n    print(f"  {region}: {len(group)} точек, mean_value={group[\'value\'].mean():.1f}")\n\nprint(f"\\nПо категориям:")\nfor cat, group in df.groupby("category"):\n    print(f"  {cat}: {len(group)} точек")\n\n# Кластеризация\ncoords = df[["lat", "lon"]].values\ndbscan = DBSCAN(eps=10, min_samples=5)\ndf["cluster"] = dbscan.fit_predict(coords)\nn_clusters = len(set(df["cluster"])) - (1 if -1 in df["cluster"].values else 0)\nprint(f"\\nКластеров найдено (DBSCAN): {n_clusters}")\n\nfig, axes = plt.subplots(1, 3, figsize=(20, 6))\n\n# Scatter по регионам\ncolors_map = {"Europe": "blue", "Asia": "red", "North America": "green", "South America": "orange", "Africa": "purple"}\nfor region, group in df.groupby("region"):\n    axes[0].scatter(group["lon"], group["lat"], c=colors_map[region],\n                    s=group["value"], alpha=0.6, label=region)\naxes[0].set_title("Точки по регионам")\naxes[0].set_xlabel("Longitude")\naxes[0].set_ylabel("Latitude")\naxes[0].legend(fontsize=8)\n\n# По категориям\ncat_colors = {"store": "green", "warehouse": "blue", "office": "red"}\nfor cat, group in df.groupby("category"):\n    axes[1].scatter(group["lon"], group["lat"], c=cat_colors[cat],\n                    s=40, alpha=0.6, label=cat)\naxes[1].set_title("Точки по категориям")\naxes[1].set_xlabel("Longitude")\naxes[1].legend(fontsize=8)\n\n# Heatmap плотности\nhb = axes[2].hexbin(df["lon"], df["lat"], gridsize=15, cmap="YlOrRd\", mincnt=1)\naxes[2].set_title("Плотность точек")\naxes[2].set_xlabel("Longitude")\nplt.colorbar(hb, ax=axes[2])\n\nplt.tight_layout()\nplt.savefig("geo_eda.png", dpi=100)\nprint("Графики сохранены: geo_eda.png")',
      explanation: 'Географический EDA визуализирует пространственные паттерны. Scatter plot с размером по значению показывает распределение и важность точек. DBSCAN находит кластеры без задания числа кластеров — подходит для гео-данных. Hexbin heatmap показывает плотность точек.'
    },
    {
      id: 10,
      title: 'Полный EDA отчёт',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создайте автоматический полный EDA-отчёт с выводами и рекомендациями.',
      requirements: [
        'Создайте реалистичный датасет клиентов банка (1000 записей, 10+ столбцов, mix числовых и категориальных)',
        'Автоматически определите типы столбцов и примените соответствующий анализ',
        'Для числовых: describe, распределения, выбросы, корреляции',
        'Для категориальных: value_counts, top/rare categories, связь с target',
        'Сформируйте текстовый отчёт с выводами и рекомендациями для моделирования'
      ],
      hint: 'Автоматизируйте через функции: analyze_numeric(col), analyze_categorical(col), analyze_target_relation(col, target). Выводы: перечислите проблемы и действия.',
      expectedOutput: 'ПОЛНЫЙ EDA ОТЧЁТ\n================\n1. Обзор датасета: 1000 x 12\n2. Типы: 7 числовых, 5 категориальных\n3. Пропуски: X столбцов с пропусками\n4. Выбросы: найдены в X столбцах\n5. Корреляции: X сильных пар\n6. Рекомендации:\n   - Заполнить пропуски в ...\n   - Удалить/трансформировать выбросы в ...\n   - Закодировать категории: ...\n   - Удалить мультиколлинеарные: ...',
      solution: 'import numpy as np\nimport pandas as pd\nfrom scipy import stats\nimport matplotlib\nmatplotlib.use("Agg")\nimport matplotlib.pyplot as plt\nimport seaborn as sns\n\nnp.random.seed(42)\nn = 1000\n\ndf = pd.DataFrame({\n    "age": np.where(np.random.random(n) < 0.03, np.nan, np.random.randint(18, 75, n).astype(float)),\n    "income": np.where(np.random.random(n) < 0.07, np.nan, np.random.lognormal(10.5, 0.8, n)),\n    "credit_score": np.random.randint(300, 850, n),\n    "balance": np.where(np.random.random(n) < 0.05, np.nan, np.random.exponential(30000, n)),\n    "num_products": np.random.choice([1,2,3,4], n, p=[0.5, 0.3, 0.15, 0.05]),\n    "tenure": np.random.randint(0, 15, n),\n    "has_credit_card": np.random.choice([0, 1], n, p=[0.3, 0.7]),\n    "is_active": np.random.choice([0, 1], n, p=[0.4, 0.6]),\n    "gender": np.random.choice(["M", "F"], n),\n    "geography": np.random.choice(["France", "Germany", "Spain"], n, p=[0.5, 0.25, 0.25]),\n    "education": np.where(np.random.random(n) < 0.04, np.nan,\n                          np.random.choice(["High School", "Bachelor", "Master", "PhD"], n, p=[0.2, 0.4, 0.3, 0.1])),\n})\nchurn_prob = 0.15 + 0.002 * (df["age"].fillna(40) - 40) - 0.00001 * df["income"].fillna(40000) + 0.1 * (df["num_products"] > 2)\ndf["churn"] = (np.random.random(n) < churn_prob.clip(0.05, 0.6)).astype(int)\n\ndef analyze_numeric(col_name, series):\n    report = []\n    clean = series.dropna()\n    report.append(f"  Среднее: {clean.mean():.2f}, Медиана: {clean.median():.2f}, Std: {clean.std():.2f}")\n    sk = stats.skew(clean)\n    report.append(f"  Skewness: {sk:.2f}" + (" → нужен log-transform" if abs(sk) > 1 else " ✓"))\n    Q1, Q3 = clean.quantile(0.25), clean.quantile(0.75)\n    IQR = Q3 - Q1\n    outliers = ((clean < Q1 - 1.5*IQR) | (clean > Q3 + 1.5*IQR)).sum()\n    report.append(f"  Выбросы (IQR): {outliers} ({outliers/len(clean)*100:.1f}%)")\n    miss = series.isnull().sum()\n    if miss > 0:\n        report.append(f"  Пропуски: {miss} ({miss/len(series)*100:.1f}%)")\n    return report, outliers > 0, miss > 0, abs(sk) > 1\n\ndef analyze_categorical(col_name, series):\n    report = []\n    clean = series.dropna()\n    report.append(f"  Уникальных: {clean.nunique()}")\n    vc = clean.value_counts()\n    report.append(f"  Top: {vc.index[0]} ({vc.iloc[0]})")\n    rare = vc[vc < len(clean) * 0.05]\n    if len(rare) > 0:\n        report.append(f"  Редкие (<5%): {list(rare.index)}")\n    miss = series.isnull().sum()\n    if miss > 0:\n        report.append(f"  Пропуски: {miss} ({miss/len(series)*100:.1f}%)")\n    return report, miss > 0\n\nprint("=" * 50)\nprint("ПОЛНЫЙ EDA ОТЧЁТ")\nprint("=" * 50)\nprint(f"\\n1. ОБЗОР ДАТАСЕТА: {df.shape[0]} строк x {df.shape[1]} столбцов")\n\nnumeric_cols = df.select_dtypes(include=[np.number]).columns.tolist()\ncat_cols = df.select_dtypes(include=["object"]).columns.tolist()\nprint(f"\\n2. ТИПЫ: {len(numeric_cols)} числовых, {len(cat_cols)} категориальных")\nprint(f"   Числовые: {numeric_cols}")\nprint(f"   Категориальные: {cat_cols}")\n\nprint(f"\\n3. TARGET (churn):")\nprint(f"   Churn rate: {df[\'churn\'].mean()*100:.1f}%")\nprint(f"   Баланс классов: 0={( df[\'churn\']==0).sum()}, 1={(df[\'churn\']==1).sum()}")\n\nrecommendations = []\noutlier_cols, missing_cols, skewed_cols = [], [], []\n\nprint(f"\\n4. ЧИСЛОВЫЕ СТОЛБЦЫ:")\nfor col in numeric_cols:\n    if col == "churn":\n        continue\n    print(f"\\n  [{col}]")\n    report, has_outliers, has_missing, is_skewed = analyze_numeric(col, df[col])\n    for line in report:\n        print(line)\n    if has_outliers:\n        outlier_cols.append(col)\n    if has_missing:\n        missing_cols.append(col)\n    if is_skewed:\n        skewed_cols.append(col)\n\nprint(f"\\n5. КАТЕГОРИАЛЬНЫЕ СТОЛБЦЫ:")\nfor col in cat_cols:\n    print(f"\\n  [{col}]")\n    report, has_missing = analyze_categorical(col, df[col])\n    for line in report:\n        print(line)\n    if has_missing:\n        missing_cols.append(col)\n\n# Корреляции\nprint(f"\\n6. КОРРЕЛЯЦИИ:")\ncorr = df[numeric_cols].corr()\nupper = corr.where(np.triu(np.ones_like(corr, dtype=bool), k=1))\nstrong = []\nfor col in upper.columns:\n    for idx in upper.index:\n        val = upper.loc[idx, col]\n        if pd.notna(val) and abs(val) > 0.5:\n            strong.append((idx, col, val))\n            if abs(val) > 0.8:\n                recommendations.append(f"Удалить одну из мультиколлинеарных: {idx} & {col} (r={val:.2f})")\nprint(f"   Сильных пар (|r|>0.5): {len(strong)}")\nfor f1, f2, r in sorted(strong, key=lambda x: -abs(x[2])):\n    print(f"   {f1} & {f2}: r={r:.3f}")\n\nprint(f"\\n7. РЕКОМЕНДАЦИИ:")\nif missing_cols:\n    recommendations.append(f"Заполнить пропуски в: {missing_cols}")\nif outlier_cols:\n    recommendations.append(f"Обработать выбросы в: {outlier_cols}")\nif skewed_cols:\n    recommendations.append(f"Log-transform для: {skewed_cols}")\nif cat_cols:\n    recommendations.append(f"Закодировать категории: {cat_cols}")\nif df["churn"].mean() < 0.3:\n    recommendations.append("Дисбаланс классов → SMOTE или class_weight")\n\nfor i, rec in enumerate(recommendations, 1):\n    print(f"   {i}. {rec}")\n\nprint(f"\\n{\'=\' * 50}")\nprint("Отчёт завершён")',
      explanation: 'Автоматический EDA-отчёт — must-have инструмент. Он систематизирует анализ: сначала обзор, затем каждый столбец (числовой/категориальный), корреляции, и наконец рекомендации. В продакшене используйте pandas-profiling/ydata-profiling для генерации HTML-отчёта за одну строку кода.'
    }
  ]
}
