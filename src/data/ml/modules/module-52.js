export default {
  id: 52,
  title: 'Проект: Чат-бот с NLP',
  description: 'Полный проект NLP чат-бота: intent classification, named entity recognition, dialogue management, Transformer-based подходы и интеграция с Telegram.',
  lessons: [
    {
      id: 1,
      title: 'Описание проекта',
      type: 'theory',
      content: [
        { type: 'heading', value: 'NLP Chatbot: от intent до Telegram' },
        { type: 'text', value: 'Чат-бот с ML — это система, которая понимает намерения пользователя (intent), извлекает ключевую информацию (entities) и ведёт диалог с учётом контекста. В отличие от rule-based ботов, ML-бот обобщает знания и понимает перефразирования.' },
        { type: 'text', value: 'Архитектура NLP чат-бота состоит из трёх основных компонентов: (1) NLU — Natural Language Understanding (intent classification + entity extraction), (2) Dialogue Manager — управление состоянием диалога и выбор действий, (3) NLG — Natural Language Generation (формирование ответа).' },
        { type: 'code', language: 'python', value: '# Архитектура NLP Chatbot\n# chatbot/\n# ├── data/\n# │   ├── intents.json       # намерения и примеры\n# │   ├── entities.json      # шаблоны сущностей\n# │   └── responses.json     # шаблоны ответов\n# ├── nlu/\n# │   ├── intent_classifier.py\n# │   ├── entity_extractor.py\n# │   └── preprocessor.py\n# ├── dialogue/\n# │   ├── manager.py\n# │   ├── state.py\n# │   └── actions.py\n# ├── bot/\n# │   └── telegram_bot.py\n# └── main.py\n\n# Пример intents.json\nimport json\n\nintents = {\n    "greet": {\n        "examples": [\n            "привет", "здравствуйте", "добрый день",\n            "хай", "приветик", "hello", "доброе утро"\n        ],\n        "responses": ["Привет! Чем могу помочь?", "Здравствуйте! Как я могу помочь?"]\n    },\n    "book_flight": {\n        "examples": [\n            "забронировать рейс", "хочу билет на самолёт",\n            "найди рейс в Москву", "лететь из Алматы в Астану",\n            "купить авиабилет", "нужен рейс на завтра"\n        ],\n        "responses": ["Куда вы хотите лететь?", "Откуда и куда вам нужен рейс?"]\n    },\n    "check_weather": {\n        "examples": [\n            "какая погода", "прогноз погоды", "будет ли дождь",\n            "температура на завтра", "погода в Москве"\n        ],\n        "responses": ["В каком городе проверить погоду?"]\n    },\n    "goodbye": {\n        "examples": ["пока", "до свидания", "выход", "спасибо, всё"],\n        "responses": ["До свидания!", "Рад был помочь! До встречи!"]\n    }\n}\n\nprint("Chatbot Intents:")\nfor intent, data in intents.items():\n    print(f"  {intent}: {len(data[\'examples\'])} примеров, "\n          f"{len(data[\'responses\'])} ответов")\nprint(f"\\nВсего интентов: {len(intents)}")\nprint(f"Всего обучающих примеров: {sum(len(d[\'examples\']) for d in intents.values())}") ' },
        { type: 'tip', value: 'Для качественного intent classifier нужно минимум 10–20 примеров на каждый intent. Добавляйте синонимы, опечатки и разговорные формы. Чем больше разнообразие — тем лучше обобщение.' },
        { type: 'note', value: 'Основные компоненты чат-бота: NLU (понимание текста), DM (управление диалогом), NLG (генерация ответа). В production часто используют Rasa, Dialogflow или собственные решения на базе BERT/GPT.' }
      ]
    },
    {
      id: 2,
      title: 'Intent Classification',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Классификация намерений пользователя' },
        { type: 'text', value: 'Intent classification — определение, что хочет пользователь. Это задача многоклассовой классификации текста. Базовый подход: TF-IDF + классификатор (LogReg, SVM). Продвинутый: word embeddings + neural network. State-of-the-art: BERT fine-tuning.' },
        { type: 'code', language: 'python', value: 'import numpy as np\nfrom sklearn.feature_extraction.text import TfidfVectorizer\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.svm import LinearSVC\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.model_selection import cross_val_score\nimport re\n\n# Данные для обучения\ntrain_data = [\n    ("привет", "greet"), ("здравствуйте", "greet"),\n    ("добрый день", "greet"), ("хай", "greet"),\n    ("приветик", "greet"), ("доброе утро", "greet"),\n    ("забронировать рейс", "book_flight"), ("хочу билет", "book_flight"),\n    ("найди рейс в Москву", "book_flight"), ("лететь в Астану", "book_flight"),\n    ("купить авиабилет", "book_flight"), ("рейс на завтра", "book_flight"),\n    ("какая погода", "check_weather"), ("прогноз погоды", "check_weather"),\n    ("будет ли дождь", "check_weather"), ("температура завтра", "check_weather"),\n    ("погода в Москве", "check_weather"), ("тепло ли на улице", "check_weather"),\n    ("пока", "goodbye"), ("до свидания", "goodbye"),\n    ("выход", "goodbye"), ("спасибо всё", "goodbye"),\n    ("хочу в отель", "book_hotel"), ("забронировать номер", "book_hotel"),\n    ("найди отель", "book_hotel"), ("нужна гостиница", "book_hotel"),\n    ("отмени бронь", "cancel"), ("отменить заказ", "cancel"),\n    ("не хочу лететь", "cancel"), ("верни деньги", "cancel"),\n]\n\ntexts = [t for t, _ in train_data]\nlabels = [l for _, l in train_data]\n\n# Preprocessing\ndef preprocess(text):\n    text = text.lower().strip()\n    text = re.sub(r"[^а-яёa-z\\s]", "", text)\n    return text\n\ntexts_clean = [preprocess(t) for t in texts]\n\n# TF-IDF + LogisticRegression\npipeline_lr = Pipeline([\n    ("tfidf", TfidfVectorizer(ngram_range=(1, 2), analyzer="char_wb",\n                               min_df=1, max_features=3000)),\n    ("clf", LogisticRegression(max_iter=1000, C=10))\n])\n\n# TF-IDF + SVM\npipeline_svm = Pipeline([\n    ("tfidf", TfidfVectorizer(ngram_range=(1, 2), analyzer="char_wb",\n                               min_df=1, max_features=3000)),\n    ("clf", LinearSVC(max_iter=2000))\n])\n\nprint("=== Intent Classification ===")\nfor name, pipe in [(\"LogReg\", pipeline_lr), (\"SVM\", pipeline_svm)]:\n    scores = cross_val_score(pipe, texts_clean, labels, cv=3, scoring="accuracy")\n    print(f"  {name}: accuracy={scores.mean():.4f} (+/-{scores.std():.4f})")\n\n# Обучаем лучшую модель\npipeline_lr.fit(texts_clean, labels)\n\n# Тестирование\ntest_phrases = [\n    "привет, как дела",\n    "хочу полететь в Париж",\n    "какая температура сейчас",\n    "отмени мой рейс",\n    "до встречи"\n]\n\nprint("\\nТестовые предсказания:")\nfor phrase in test_phrases:\n    clean = preprocess(phrase)\n    intent = pipeline_lr.predict([clean])[0]\n    proba = pipeline_lr.predict_proba([clean])[0]\n    confidence = proba.max()\n    print(f"  \"{phrase}\" → {intent} (conf={confidence:.2f})")' },
        { type: 'text', value: 'Используем char_wb n-grams вместо слов, так как для русского языка это лучше работает с малым объёмом данных — улавливает морфологические вариации (лететь, лечу, полететь) без стемминга.' },
        { type: 'tip', value: 'analyzer="char_wb" с ngram_range=(2,4) — мощный baseline для русскоязычных intent classifier. Char n-grams учитывают морфологию и устойчивы к опечаткам.' }
      ]
    },
    {
      id: 3,
      title: 'Практика: Intent classifier',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создайте intent classifier для чат-бота с минимум 6 интентами и TF-IDF + ML.',
      requirements: [
        'Определите минимум 6 интентов (greet, goodbye, book_flight, check_weather, book_hotel, cancel) с 5+ примерами каждый',
        'Реализуйте preprocessing: lowercase, удаление спецсимволов',
        'Обучите Pipeline(TfidfVectorizer + LogisticRegression) с char_wb n-grams',
        'Оцените cross-validation accuracy (3-fold)',
        'Предскажите intent и confidence для 5 тестовых фраз'
      ],
      hint: 'TfidfVectorizer(analyzer="char_wb", ngram_range=(2,4)) лучше работает для русского текста с малым датасетом. LogisticRegression(C=10) с большим C даёт меньше регуляризации, что полезно при малых данных.',
      expectedOutput: 'Intent Classifier:\nИнтентов: 6, Примеров: 30+\nCV Accuracy: 0.XX\n\nПредсказания:\n  "привет как дела" → greet (0.XX)\n  "хочу полететь в Париж" → book_flight (0.XX)\n  ...',
      solution: 'import numpy as np\nimport re\nfrom sklearn.feature_extraction.text import TfidfVectorizer\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.model_selection import cross_val_score\n\ntrain_data = [\n    ("привет", "greet"), ("здравствуйте", "greet"), ("добрый день", "greet"),\n    ("хай", "greet"), ("приветик", "greet"), ("доброе утро", "greet"),\n    ("забронировать рейс", "book_flight"), ("хочу билет на самолёт", "book_flight"),\n    ("найди рейс в Москву", "book_flight"), ("лететь из Алматы", "book_flight"),\n    ("купить авиабилет", "book_flight"), ("нужен рейс на завтра", "book_flight"),\n    ("какая погода", "check_weather"), ("прогноз погоды", "check_weather"),\n    ("будет ли дождь", "check_weather"), ("температура завтра", "check_weather"),\n    ("погода в Москве", "check_weather"), ("тепло на улице", "check_weather"),\n    ("пока", "goodbye"), ("до свидания", "goodbye"), ("выход", "goodbye"),\n    ("спасибо всё", "goodbye"), ("до встречи", "goodbye"),\n    ("хочу в отель", "book_hotel"), ("забронировать номер", "book_hotel"),\n    ("найди гостиницу", "book_hotel"), ("нужен отель", "book_hotel"),\n    ("бронь номера", "book_hotel"),\n    ("отмени бронь", "cancel"), ("отменить заказ", "cancel"),\n    ("не хочу лететь", "cancel"), ("верни деньги", "cancel"),\n    ("отмена рейса", "cancel"),\n]\n\ntexts = [t for t, _ in train_data]\nlabels = [l for _, l in train_data]\n\ndef preprocess(text):\n    text = text.lower().strip()\n    text = re.sub(r"[^а-яёa-z\\s]", "", text)\n    return text\n\ntexts_clean = [preprocess(t) for t in texts]\n\npipeline = Pipeline([\n    ("tfidf", TfidfVectorizer(analyzer="char_wb", ngram_range=(2, 4),\n                               min_df=1, max_features=3000)),\n    ("clf", LogisticRegression(max_iter=1000, C=10))\n])\n\nscores = cross_val_score(pipeline, texts_clean, labels, cv=3, scoring="accuracy")\nprint("Intent Classifier:")\nprint(f"Интентов: {len(set(labels))}, Примеров: {len(texts)}")\nprint(f"CV Accuracy: {scores.mean():.4f}")\n\npipeline.fit(texts_clean, labels)\n\ntest_phrases = [\n    "привет как дела", "хочу полететь в Париж",\n    "какая температура сейчас", "отмени мой рейс", "до встречи"\n]\n\nprint("\\nПредсказания:")\nfor phrase in test_phrases:\n    clean = preprocess(phrase)\n    intent = pipeline.predict([clean])[0]\n    conf = pipeline.predict_proba([clean]).max()\n    print(f"  \"{phrase}\" → {intent} ({conf:.2f})")',
      explanation: 'Intent classifier на TF-IDF + LogReg — сильный baseline. Char n-grams для русского языка компенсируют морфологическую сложность. При 30+ примерах на 6 интентов модель уже хорошо обобщает. Для production нужно больше данных и BERT-based подход.'
    },
    {
      id: 4,
      title: 'Named Entity Recognition',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Извлечение сущностей из текста' },
        { type: 'text', value: 'Named Entity Recognition (NER) — извлечение структурированной информации из неструктурированного текста. Для чат-бота это: города (location), даты (date), числа (number), имена (person). Подходы: regex-паттерны (простые), SpaCy NER (средние), BERT-based NER (лучшие).' },
        { type: 'code', language: 'python', value: 'import re\nfrom datetime import datetime, timedelta\n\nclass EntityExtractor:\n    """Извлечение сущностей из текста с помощью regex + словарей."""\n    \n    def __init__(self):\n        self.cities = {\n            "москва": "Москва", "москву": "Москва",\n            "астана": "Астана", "астану": "Астана",\n            "алматы": "Алматы",\n            "париж": "Париж",\n            "лондон": "Лондон",\n            "нью-йорк": "Нью-Йорк",\n            "токио": "Токио",\n            "берлин": "Берлин",\n        }\n        self.date_patterns = {\n            r"\\bсегодня\\b": 0,\n            r"\\bзавтра\\b": 1,\n            r"\\bпослезавтра\\b": 2,\n            r"\\bчерез (\\d+) дн": None,  # dynamic\n        }\n    \n    def extract_cities(self, text):\n        \"\"\"Извлечение городов.\"\"\"\n        text_lower = text.lower()\n        found = []\n        for pattern, city in self.cities.items():\n            if pattern in text_lower:\n                found.append(city)\n        return list(set(found))\n    \n    def extract_dates(self, text):\n        \"\"\"Извлечение дат.\"\"\"\n        text_lower = text.lower()\n        today = datetime(2024, 6, 15)  # фиксированная дата для примера\n        dates = []\n        \n        if "сегодня" in text_lower:\n            dates.append(("сегодня", today.strftime("%Y-%m-%d")))\n        if "завтра" in text_lower:\n            dates.append(("завтра", (today + timedelta(1)).strftime("%Y-%m-%d")))\n        if "послезавтра" in text_lower:\n            dates.append(("послезавтра", (today + timedelta(2)).strftime("%Y-%m-%d")))\n        \n        # "через N дней"\n        match = re.search(r"через (\\d+) дн", text_lower)\n        if match:\n            days = int(match.group(1))\n            date = (today + timedelta(days)).strftime("%Y-%m-%d")\n            dates.append((f"через {days} дней", date))\n        \n        # Конкретная дата DD.MM\n        match = re.search(r"(\\d{1,2})\\.(\\d{1,2})", text)\n        if match:\n            day, month = int(match.group(1)), int(match.group(2))\n            date = f"2024-{month:02d}-{day:02d}"\n            dates.append((match.group(0), date))\n        \n        return dates\n    \n    def extract_numbers(self, text):\n        \"\"\"Извлечение чисел (количество, номера).\"\"\"\n        numbers = re.findall(r"\\b\\d+\\b", text)\n        return [int(n) for n in numbers]\n    \n    def extract_all(self, text):\n        \"\"\"Извлечение всех сущностей.\"\"\"\n        return {\n            "cities": self.extract_cities(text),\n            "dates": self.extract_dates(text),\n            "numbers": self.extract_numbers(text)\n        }\n\n# Тестирование\nextractor = EntityExtractor()\ntest_texts = [\n    "Хочу лететь из Алматы в Москву завтра",\n    "Найди рейс в Париж на 25.12",\n    "Бронь в Лондон через 5 дней на 2 человека",\n    "Погода в Токио сегодня",\n]\n\nprint("=== Named Entity Recognition ===")\nfor text in test_texts:\n    entities = extractor.extract_all(text)\n    print(f"\\n\"{text}\":")\n    for entity_type, values in entities.items():\n        if values:\n            print(f"  {entity_type}: {values}")' },
        { type: 'heading', value: 'SpaCy для NER' },
        { type: 'text', value: 'SpaCy — мощная NLP-библиотека с предобученными моделями для NER. Русскоязычная модель ru_core_news_sm распознаёт LOC (места), PER (имена), ORG (организации) и другие сущности из коробки.' },
        { type: 'code', language: 'python', value: '# SpaCy NER (требует: pip install spacy && python -m spacy download ru_core_news_sm)\nimport spacy\n\nnlp = spacy.load("ru_core_news_sm")\n\ntexts = [\n    "Иван Петров хочет лететь из Москвы в Париж 15 июня",\n    "Забронировать отель Hilton в Лондоне на 3 ночи",\n    "Компания Google открыла офис в Алматы",\n]\n\nprint("=== SpaCy NER ===")\nfor text in texts:\n    doc = nlp(text)\n    print(f"\\n\"{text}\":")\n    for ent in doc.ents:\n        print(f"  {ent.text} → {ent.label_} ({spacy.explain(ent.label_)})")\n\n# Пример без SpaCy — упрощённый NER\nprint("\\n--- Упрощённый NER (без SpaCy) ---")\nimport re\n\ndef simple_ner(text):\n    entities = []\n    # Города (словарь)\n    cities = ["Москва", "Москвы", "Париж", "Лондон", "Алматы", "Астана"]\n    for city in cities:\n        if city.lower() in text.lower():\n            entities.append((city, "LOC"))\n    # Имена (заглавные слова, не в начале)\n    words = text.split()\n    for i, word in enumerate(words):\n        if i > 0 and word[0].isupper() and word not in cities:\n            entities.append((word, "PER_CANDIDATE"))\n    # Числа\n    for match in re.finditer(r"\\b\\d+\\b", text):\n        entities.append((match.group(), "NUMBER"))\n    return entities\n\nfor text in texts:\n    ents = simple_ner(text)\n    print(f"  \"{text[:40]}...\": {ents}")' },
        { type: 'tip', value: 'Комбинируйте подходы: regex для структурированных сущностей (даты, телефоны), словари для domain-specific (города, товары), SpaCy/BERT для общих (имена, организации).' }
      ]
    },
    {
      id: 5,
      title: 'Практика: Извлечение сущностей',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте EntityExtractor, который извлекает города, даты, числа и email из пользовательских сообщений.',
      requirements: [
        'Создайте класс EntityExtractor с методами: extract_cities, extract_dates, extract_numbers, extract_emails',
        'extract_cities: словарь из 10+ городов (учитывая падежи: Москву, Москвы)',
        'extract_dates: распознавание "сегодня", "завтра", "через N дней", формата DD.MM',
        'extract_numbers и extract_emails: regex-паттерны',
        'Протестируйте на 5+ фразах и выведите найденные сущности'
      ],
      hint: 'Для городов создайте словарь с падежными формами, ведущими к нормализованному названию. Для email: r"[\\w.+-]+@[\\w-]+\\.[\\w.]+" — простой, но рабочий паттерн.',
      expectedOutput: 'Entity Extractor:\n"Лететь из Алматы в Москву завтра":\n  cities: [Алматы, Москва]\n  dates: [(завтра, 2024-06-16)]\n"Напиши на test@mail.ru через 3 дня":\n  emails: [test@mail.ru]\n  dates: [(через 3 дней, 2024-06-18)]\n...',
      solution: 'import re\nfrom datetime import datetime, timedelta\n\nclass EntityExtractor:\n    def __init__(self):\n        self.city_map = {\n            "москва": "Москва", "москву": "Москва", "москвы": "Москва",\n            "алматы": "Алматы", "астана": "Астана", "астану": "Астана",\n            "париж": "Париж", "лондон": "Лондон", "лондона": "Лондон",\n            "берлин": "Берлин", "берлина": "Берлин",\n            "токио": "Токио", "нью-йорк": "Нью-Йорк",\n            "стамбул": "Стамбул", "стамбула": "Стамбул",\n        }\n        self.today = datetime(2024, 6, 15)\n\n    def extract_cities(self, text):\n        words = text.lower().replace(",", " ").split()\n        found = set()\n        for w in words:\n            if w in self.city_map:\n                found.add(self.city_map[w])\n        return list(found)\n\n    def extract_dates(self, text):\n        t = text.lower()\n        dates = []\n        if "сегодня" in t:\n            dates.append(("сегодня", self.today.strftime("%Y-%m-%d")))\n        if "завтра" in t and "послезавтра" not in t:\n            dates.append(("завтра", (self.today + timedelta(1)).strftime("%Y-%m-%d")))\n        if "послезавтра" in t:\n            dates.append(("послезавтра", (self.today + timedelta(2)).strftime("%Y-%m-%d")))\n        m = re.search(r"через (\\d+) дн", t)\n        if m:\n            d = int(m.group(1))\n            dates.append((f"через {d} дней", (self.today + timedelta(d)).strftime("%Y-%m-%d")))\n        m = re.search(r"(\\d{1,2})\\.(\\d{1,2})", text)\n        if m:\n            dates.append((m.group(0), f"2024-{int(m.group(2)):02d}-{int(m.group(1)):02d}"))\n        return dates\n\n    def extract_numbers(self, text):\n        return [int(n) for n in re.findall(r"\\b\\d+\\b", text)]\n\n    def extract_emails(self, text):\n        return re.findall(r"[\\w.+-]+@[\\w-]+\\.[\\w.]+", text)\n\n    def extract_all(self, text):\n        return {\n            "cities": self.extract_cities(text),\n            "dates": self.extract_dates(text),\n            "numbers": self.extract_numbers(text),\n            "emails": self.extract_emails(text)\n        }\n\next = EntityExtractor()\ntest_texts = [\n    "Лететь из Алматы в Москву завтра",\n    "Напиши на test@mail.ru через 3 дня",\n    "Бронь в Лондон на 25.12 для 2 человек",\n    "Погода в Токио и Берлине сегодня",\n    "Связаться: user@gmail.com, 3 билета в Париж",\n]\n\nprint("Entity Extractor:")\nfor text in test_texts:\n    entities = ext.extract_all(text)\n    print(f"\\n\"{text}\":")\n    for etype, vals in entities.items():\n        if vals:\n            print(f"  {etype}: {vals}")',
      explanation: 'Regex-based NER хорошо работает для структурированных сущностей (даты, email, числа) и domain-specific словарей (города). Для более сложных задач (имена, организации) нужны ML-модели: SpaCy NER или BERT-based Token Classification.'
    },
    {
      id: 6,
      title: 'Dialogue Management',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Управление диалогом' },
        { type: 'text', value: 'Dialogue Manager (DM) — мозг чат-бота. Он решает, что делать дальше: задать уточняющий вопрос, выполнить действие или сгенерировать ответ. Основные подходы: State Machine (конечный автомат), Frame-based (слоты) и ML-based (policy network).' },
        { type: 'text', value: 'Frame-based подход: для каждого intent определяем набор слотов (обязательных сущностей). Бот заполняет слоты из сообщений пользователя. Когда все слоты заполнены — выполняет действие. Если слот пустой — задаёт уточняющий вопрос.' },
        { type: 'code', language: 'python', value: 'class DialogueState:\n    """Состояние диалога для одного пользователя."""\n    def __init__(self):\n        self.current_intent = None\n        self.slots = {}\n        self.history = []\n        self.context = {}\n    \n    def reset(self):\n        self.current_intent = None\n        self.slots = {}\n\n\nclass DialogueManager:\n    """Frame-based Dialogue Manager."""\n    \n    def __init__(self):\n        # Определяем фреймы: intent → обязательные слоты\n        self.frames = {\n            "book_flight": {\n                "slots": {\n                    "origin": {"question": "Откуда вы летите?", "type": "city"},\n                    "destination": {"question": "Куда хотите лететь?", "type": "city"},\n                    "date": {"question": "На какую дату?", "type": "date"},\n                },\n                "action": self._book_flight\n            },\n            "book_hotel": {\n                "slots": {\n                    "city": {"question": "В каком городе нужен отель?", "type": "city"},\n                    "nights": {"question": "На сколько ночей?", "type": "number"},\n                },\n                "action": self._book_hotel\n            },\n            "check_weather": {\n                "slots": {\n                    "city": {"question": "В каком городе проверить погоду?", "type": "city"},\n                },\n                "action": self._check_weather\n            },\n        }\n        self.states = {}  # user_id → DialogueState\n    \n    def get_state(self, user_id):\n        if user_id not in self.states:\n            self.states[user_id] = DialogueState()\n        return self.states[user_id]\n    \n    def process(self, user_id, intent, entities, text):\n        \"\"\"Обработка сообщения пользователя.\"\"\"\n        state = self.get_state(user_id)\n        state.history.append({"role": "user", "text": text})\n        \n        # Простые интенты без слотов\n        if intent == "greet":\n            return "Привет! Чем могу помочь?"\n        if intent == "goodbye":\n            state.reset()\n            return "До свидания!"\n        if intent == "cancel":\n            state.reset()\n            return "Отменено. Чем ещё могу помочь?"\n        \n        # Интенты со слотами\n        if intent in self.frames:\n            state.current_intent = intent\n            state.slots = {}\n            # Заполняем слоты из entities\n            self._fill_slots(state, entities)\n        elif state.current_intent:\n            # Продолжаем заполнять слоты текущего intent\n            self._fill_slots(state, entities)\n        else:\n            return "Не совсем понял. Попробуйте переформулировать."\n        \n        # Проверяем, все ли слоты заполнены\n        return self._check_and_respond(state)\n    \n    def _fill_slots(self, state, entities):\n        frame = self.frames[state.current_intent]\n        for slot_name, slot_info in frame["slots"].items():\n            if slot_name in state.slots:\n                continue\n            if slot_info["type"] == "city" and entities.get("cities"):\n                state.slots[slot_name] = entities["cities"].pop(0)\n            elif slot_info["type"] == "date" and entities.get("dates"):\n                state.slots[slot_name] = entities["dates"][0][1]\n            elif slot_info["type"] == "number" and entities.get("numbers"):\n                state.slots[slot_name] = entities["numbers"].pop(0)\n    \n    def _check_and_respond(self, state):\n        frame = self.frames[state.current_intent]\n        for slot_name, slot_info in frame["slots"].items():\n            if slot_name not in state.slots:\n                return slot_info["question"]\n        # Все слоты заполнены — выполняем действие\n        result = frame["action"](state.slots)\n        state.reset()\n        return result\n    \n    def _book_flight(self, slots):\n        return f"Ищу рейс {slots[\'origin\']} → {slots[\'destination\']} на {slots[\'date\']}..."\n    \n    def _book_hotel(self, slots):\n        return f"Ищу отель в {slots[\'city\']} на {slots[\'nights\']} ночей..."\n    \n    def _check_weather(self, slots):\n        return f"Погода в {slots[\'city\']}: +22°C, солнечно ☀"\n\n# Тестирование диалога\ndm = DialogueManager()\nuser = "user_1"\n\nconversation = [\n    ("greet", {}, "Привет"),\n    ("book_flight", {"cities": ["Алматы", "Москва"]}, "Хочу из Алматы в Москву"),\n    (None, {"dates": [("завтра", "2024-06-16")]}, "На завтра"),\n    ("check_weather", {"cities": ["Токио"]}, "Какая погода в Токио"),\n]\n\nprint("=== Dialogue Manager ===")\nfor intent, entities, text in conversation:\n    response = dm.process(user, intent, entities, text)\n    print(f"User: {text}")\n    print(f"Bot: {response}\\n")' },
        { type: 'note', value: 'Frame-based подход прост и предсказуем, но не обрабатывает сложные диалоги (переключение между интентами, уточнения). Для более гибкого управления используйте Rasa с ML-policy или Reinforcement Learning.' }
      ]
    },
    {
      id: 7,
      title: 'Практика: Диалоговый менеджер',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте frame-based Dialogue Manager с поддержкой нескольких интентов и слотов.',
      requirements: [
        'Создайте класс DialogueState с полями: current_intent, slots, history',
        'Создайте DialogueManager с фреймами для 3+ интентов (book_flight, book_hotel, check_weather)',
        'Каждый фрейм: обязательные слоты с уточняющими вопросами',
        'Реализуйте _fill_slots (заполнение из entities) и _check_and_respond (проверка + действие)',
        'Проведите тестовый диалог из 5+ шагов с заполнением слотов'
      ],
      hint: 'Ключевая логика: если текущий intent имеет незаполненные слоты — задаём вопрос. Если все заполнены — выполняем action и сбрасываем state. Entities передаются как dict с ключами cities, dates, numbers.',
      expectedOutput: 'Dialogue Manager:\nUser: Привет\nBot: Привет! Чем могу помочь?\nUser: Хочу из Алматы в Москву\nBot: На какую дату?\nUser: Завтра\nBot: Ищу рейс Алматы → Москва на 2024-06-16...\n...',
      solution: 'class DialogueState:\n    def __init__(self):\n        self.current_intent = None\n        self.slots = {}\n        self.history = []\n\n    def reset(self):\n        self.current_intent = None\n        self.slots = {}\n\n\nclass DialogueManager:\n    def __init__(self):\n        self.frames = {\n            "book_flight": {\n                "slots": {\n                    "origin": {"q": "Откуда вы летите?", "type": "city"},\n                    "destination": {"q": "Куда хотите лететь?", "type": "city"},\n                    "date": {"q": "На какую дату?", "type": "date"},\n                },\n                "action": lambda s: f"Ищу рейс {s[\'origin\']} → {s[\'destination\']} на {s[\'date\']}..."\n            },\n            "book_hotel": {\n                "slots": {\n                    "city": {"q": "В каком городе?", "type": "city"},\n                    "nights": {"q": "На сколько ночей?", "type": "number"},\n                },\n                "action": lambda s: f"Ищу отель в {s[\'city\']} на {s[\'nights\']} ночей..."\n            },\n            "check_weather": {\n                "slots": {\n                    "city": {"q": "В каком городе?", "type": "city"},\n                },\n                "action": lambda s: f"Погода в {s[\'city\']}: +22°C, солнечно"\n            },\n        }\n        self.states = {}\n\n    def get_state(self, uid):\n        if uid not in self.states:\n            self.states[uid] = DialogueState()\n        return self.states[uid]\n\n    def process(self, uid, intent, entities, text):\n        state = self.get_state(uid)\n        state.history.append(text)\n        if intent == "greet":\n            return "Привет! Чем могу помочь?"\n        if intent == "goodbye":\n            state.reset()\n            return "До свидания!"\n        if intent in self.frames:\n            state.current_intent = intent\n            state.slots = {}\n            self._fill(state, entities)\n        elif state.current_intent:\n            self._fill(state, entities)\n        else:\n            return "Не понял, попробуйте снова."\n        return self._respond(state)\n\n    def _fill(self, state, entities):\n        frame = self.frames[state.current_intent]\n        for name, info in frame["slots"].items():\n            if name in state.slots:\n                continue\n            if info["type"] == "city" and entities.get("cities"):\n                state.slots[name] = entities["cities"].pop(0)\n            elif info["type"] == "date" and entities.get("dates"):\n                state.slots[name] = entities["dates"][0][1]\n            elif info["type"] == "number" and entities.get("numbers"):\n                state.slots[name] = entities["numbers"].pop(0)\n\n    def _respond(self, state):\n        frame = self.frames[state.current_intent]\n        for name, info in frame["slots"].items():\n            if name not in state.slots:\n                return info["q"]\n        result = frame["action"](state.slots)\n        state.reset()\n        return result\n\ndm = DialogueManager()\nconversation = [\n    ("greet", {}, "Привет"),\n    ("book_flight", {"cities": ["Алматы", "Москва"]}, "Хочу из Алматы в Москву"),\n    (None, {"dates": [("завтра", "2024-06-16")]}, "Завтра"),\n    ("check_weather", {"cities": ["Токио"]}, "Погода в Токио"),\n    ("book_hotel", {"cities": ["Лондон"]}, "Отель в Лондоне"),\n    (None, {"numbers": [3]}, "На 3 ночи"),\n    ("goodbye", {}, "Пока"),\n]\n\nprint("Dialogue Manager:")\nfor intent, entities, text in conversation:\n    resp = dm.process("user1", intent, entities, text)\n    print(f"User: {text}")\n    print(f"Bot: {resp}\\n")',
      explanation: 'Frame-based DM — стандартный подход для task-oriented ботов. Фреймы определяют обязательные слоты для каждого intent. Бот последовательно заполняет слоты и выполняет действие, когда все слоты заполнены. Для production добавьте: confirm перед действием, возможность изменить слот, timeout.'
    },
    {
      id: 8,
      title: 'Transformer-based бот',
      type: 'theory',
      content: [
        { type: 'heading', value: 'BERT для Intent + GPT для генерации' },
        { type: 'text', value: 'Transformer-модели кардинально улучшают качество NLU. BERT fine-tuned на intent classification обрабатывает сложные перефразирования, неграмотные сообщения и контекст. GPT-модели могут генерировать естественные ответы вместо шаблонов.' },
        { type: 'code', language: 'python', value: '# BERT Intent Classifier (transformers library)\n# pip install transformers torch\nfrom transformers import AutoTokenizer, AutoModelForSequenceClassification\nimport torch\nimport numpy as np\n\n# Пример fine-tuning BERT для intent classification\nclass BERTIntentClassifier:\n    def __init__(self, model_name="bert-base-multilingual-cased", num_labels=6):\n        self.tokenizer = AutoTokenizer.from_pretrained(model_name)\n        self.model = AutoModelForSequenceClassification.from_pretrained(\n            model_name, num_labels=num_labels\n        )\n        self.label_map = {\n            0: "greet", 1: "goodbye", 2: "book_flight",\n            3: "book_hotel", 4: "check_weather", 5: "cancel"\n        }\n    \n    def predict(self, text):\n        inputs = self.tokenizer(text, return_tensors="pt", truncation=True,\n                                max_length=64, padding=True)\n        with torch.no_grad():\n            outputs = self.model(**inputs)\n            probs = torch.softmax(outputs.logits, dim=-1)[0]\n        \n        pred_idx = probs.argmax().item()\n        confidence = probs[pred_idx].item()\n        return self.label_map[pred_idx], confidence\n    \n    def fine_tune(self, texts, labels, epochs=3, lr=2e-5):\n        \"\"\"Fine-tuning BERT на intent данных.\"\"\"\n        from torch.utils.data import DataLoader, TensorDataset\n        from torch.optim import AdamW\n        \n        encodings = self.tokenizer(texts, return_tensors="pt", truncation=True,\n                                    max_length=64, padding=True)\n        labels_tensor = torch.tensor(labels)\n        dataset = TensorDataset(encodings["input_ids"],\n                                 encodings["attention_mask"], labels_tensor)\n        loader = DataLoader(dataset, batch_size=8, shuffle=True)\n        \n        optimizer = AdamW(self.model.parameters(), lr=lr)\n        self.model.train()\n        \n        for epoch in range(epochs):\n            total_loss = 0\n            for batch in loader:\n                input_ids, attention_mask, batch_labels = batch\n                outputs = self.model(input_ids=input_ids,\n                                      attention_mask=attention_mask,\n                                      labels=batch_labels)\n                loss = outputs.loss\n                loss.backward()\n                optimizer.step()\n                optimizer.zero_grad()\n                total_loss += loss.item()\n            print(f"Epoch {epoch+1}: loss={total_loss/len(loader):.4f}")\n\nprint("=== BERT Intent Classifier ===")\nprint("Преимущества BERT для чат-бота:")\nprint("  1. Понимает контекст: \'не хочу лететь\' → cancel (не book_flight)")\nprint("  2. Устойчив к опечаткам: \'прввет\' → greet")\nprint("  3. Мультиязычность: multilingual BERT понимает 100+ языков")\nprint("  4. Transfer learning: pre-trained на огромном корпусе")\nprint("\\nFine-tuning на 100-200 примерах обычно достаточно")\nprint("для accuracy >95% на 5-10 интентов")' },
        { type: 'heading', value: 'GPT для генерации ответов' },
        { type: 'code', language: 'python', value: '# Генерация ответов с GPT (OpenAI API)\nimport json\n\ndef generate_response_gpt(intent, entities, slots, conversation_history):\n    \"\"\"\n    Генерация ответа через GPT API.\n    В production используйте openai.ChatCompletion.create()\n    \"\"\"\n    system_prompt = \"\"\"Ты — помощник для бронирования авиабилетов и отелей.\n    Отвечай кратко и по делу. Используй формальный, но дружелюбный тон.\n    Если не хватает информации — задай уточняющий вопрос.\"\"\"\n    \n    # Формируем контекст для GPT\n    context = {\n        "intent": intent,\n        "entities": entities,\n        "filled_slots": slots,\n        "history": conversation_history[-5:]  # последние 5 сообщений\n    }\n    \n    # Пример вызова API (псевдокод)\n    # response = openai.ChatCompletion.create(\n    #     model="gpt-3.5-turbo",\n    #     messages=[\n    #         {"role": "system", "content": system_prompt},\n    #         {"role": "user", "content": json.dumps(context, ensure_ascii=False)}\n    #     ],\n    #     max_tokens=150,\n    #     temperature=0.7\n    # )\n    # return response.choices[0].message.content\n    \n    # Демо-ответ\n    demo_responses = {\n        "book_flight": f"Отлично! Ищу рейсы. Найдено 3 варианта от 25 000 руб.",\n        "book_hotel": f"Подобрал 5 отелей. Рекомендую Hotel Plaza — рейтинг 4.8.",\n        "check_weather": f"Сейчас +22°C, ясно. Завтра ожидается +25°C.",\n    }\n    return demo_responses.get(intent, "Чем могу помочь?")\n\n# Гибридный подход: BERT (intent) + Rule (entity) + GPT (response)\nclass HybridChatbot:\n    \"\"\"BERT для intent + Regex для entities + GPT для ответов.\"\"\"\n    \n    def __init__(self):\n        self.intent_model = None  # BERTIntentClassifier\n        self.entity_extractor = None  # EntityExtractor\n        self.dialogue_manager = None  # DialogueManager\n    \n    def process(self, user_id, text):\n        # 1. Intent Classification (BERT)\n        intent, confidence = "book_flight", 0.95  # self.intent_model.predict(text)\n        \n        # 2. Entity Extraction (regex + SpaCy)\n        entities = {"cities": ["Москва"], "dates": [("завтра", "2024-06-16")]}\n        \n        # 3. Dialogue Management (frame-based)\n        # dm_response = self.dialogue_manager.process(user_id, intent, entities, text)\n        \n        # 4. Response Generation (GPT)\n        response = generate_response_gpt(intent, entities, {}, [])\n        \n        return {\n            "intent": intent,\n            "confidence": confidence,\n            "entities": entities,\n            "response": response\n        }\n\nprint("=== Hybrid Chatbot Architecture ===")\nresult = HybridChatbot().process("user1", "Хочу лететь в Москву завтра")\nfor key, value in result.items():\n    print(f"  {key}: {value}")' },
        { type: 'tip', value: 'Гибридный подход (BERT intent + Regex entities + GPT response) даёт лучшие результаты: BERT точно классифицирует intent, regex надёжно извлекает структурированные entities, GPT генерирует естественные ответы.' }
      ]
    },
    {
      id: 9,
      title: 'Практика: BERT intent + генерация ответов',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создайте гибридный чат-бот: BERT-like intent classifier (имитация через neural network) + шаблонная генерация ответов с учётом контекста.',
      requirements: [
        'Создайте датасет из 60+ фраз по 6 интентам',
        'Обучите neural network (TF/Keras) для intent classification: Embedding → GlobalAveragePooling → Dense',
        'Реализуйте response generator с шаблонами для каждого intent (3+ вариантов)',
        'Объедините в класс Chatbot с методом process(text) → {intent, confidence, response}',
        'Проведите тестирование на 8+ фразах, включая перефразирования'
      ],
      hint: 'Для простого neural intent classifier: токенизируйте слова, создайте vocabulary, Embedding(vocab_size, 32) → GlobalAveragePooling1D → Dense(32, relu) → Dense(num_intents, softmax). Обучите на 20 эпох.',
      expectedOutput: 'Neural Intent Chatbot:\nМодель: Embedding+Dense, accuracy=0.XX\n\nТестирование:\n  "привет" → greet (0.XX): Здравствуйте! Чем помочь?\n  "хочу полететь" → book_flight (0.XX): Куда вы хотите лететь?\n  ...',
      solution: 'import numpy as np\nimport tensorflow as tf\nfrom tensorflow.keras import layers\nfrom collections import Counter\n\n# Данные\ntrain_data = [\n    ("привет", 0), ("здравствуйте", 0), ("добрый день", 0),\n    ("хай", 0), ("приветик", 0), ("доброе утро", 0),\n    ("салют", 0), ("здорово", 0), ("добрый вечер", 0), ("хэллоу", 0),\n    ("пока", 1), ("до свидания", 1), ("выход", 1),\n    ("до встречи", 1), ("всего доброго", 1), ("спасибо пока", 1),\n    ("прощай", 1), ("увидимся", 1), ("бай", 1), ("конец", 1),\n    ("забронировать рейс", 2), ("хочу билет", 2), ("лететь в москву", 2),\n    ("купить авиабилет", 2), ("нужен рейс", 2), ("хочу полететь", 2),\n    ("билет на самолёт", 2), ("перелёт", 2), ("авиарейс", 2), ("рейс", 2),\n    ("хочу в отель", 3), ("забронировать номер", 3), ("нужна гостиница", 3),\n    ("отель", 3), ("бронь номера", 3), ("гостиница", 3),\n    ("нужен номер", 3), ("жильё", 3), ("заселиться", 3), ("хостел", 3),\n    ("какая погода", 4), ("прогноз погоды", 4), ("будет дождь", 4),\n    ("температура", 4), ("погода сегодня", 4), ("тепло на улице", 4),\n    ("холодно завтра", 4), ("осадки", 4), ("солнечно", 4), ("погодка", 4),\n    ("отмени бронь", 5), ("отменить заказ", 5), ("не хочу лететь", 5),\n    ("верни деньги", 5), ("отказ", 5), ("отмена", 5),\n    ("убери бронь", 5), ("передумал", 5), ("не надо", 5), ("возврат", 5),\n]\n\ntexts = [t for t, _ in train_data]\nlabels = np.array([l for _, l in train_data])\nlabel_names = ["greet", "goodbye", "book_flight", "book_hotel", "check_weather", "cancel"]\n\n# Токенизация\nall_words = " ".join(texts).lower().split()\nvocab = {w: i+1 for i, w in enumerate(set(all_words))}\nvocab_size = len(vocab) + 1\nmax_len = 5\n\ndef tokenize(text):\n    tokens = [vocab.get(w, 0) for w in text.lower().split()]\n    tokens = tokens[:max_len]\n    return tokens + [0] * (max_len - len(tokens))\n\nX = np.array([tokenize(t) for t in texts])\n\n# Модель\ntf.random.set_seed(42)\nmodel = tf.keras.Sequential([\n    layers.Embedding(vocab_size, 32, input_length=max_len),\n    layers.GlobalAveragePooling1D(),\n    layers.Dense(32, activation="relu"),\n    layers.Dropout(0.3),\n    layers.Dense(len(label_names), activation="softmax")\n])\nmodel.compile(optimizer="adam", loss="sparse_categorical_crossentropy", metrics=["accuracy"])\nmodel.fit(X, labels, epochs=30, verbose=0)\n\nacc = model.evaluate(X, labels, verbose=0)[1]\n\n# Response templates\nresponses = {\n    "greet": ["Здравствуйте! Чем помочь?", "Привет! Что ищем?", "Добрый день!"],\n    "goodbye": ["До свидания!", "Пока! Хорошего дня!", "До встречи!"],\n    "book_flight": ["Куда вы хотите лететь?", "Откуда и куда нужен рейс?"],\n    "book_hotel": ["В каком городе ищем отель?", "Какой отель предпочитаете?"],\n    "check_weather": ["В каком городе проверить?", "Сейчас проверю погоду!"],\n    "cancel": ["Отменяю. Что-то ещё?", "Бронь отменена."],\n}\n\nclass Chatbot:\n    def __init__(self, model, vocab, responses, label_names):\n        self.model = model\n        self.vocab = vocab\n        self.responses = responses\n        self.label_names = label_names\n\n    def process(self, text):\n        tokens = np.array([tokenize(text)])\n        probs = self.model.predict(tokens, verbose=0)[0]\n        idx = probs.argmax()\n        intent = self.label_names[idx]\n        conf = probs[idx]\n        resp = np.random.choice(self.responses[intent])\n        return {"intent": intent, "confidence": float(conf), "response": resp}\n\nbot = Chatbot(model, vocab, responses, label_names)\n\nprint("Neural Intent Chatbot:")\nprint(f"Модель: Embedding+Dense, accuracy={acc:.4f}")\nprint("\\nТестирование:")\ntest = ["привет", "хочу полететь", "погода", "отмени бронь",\n        "нужен номер", "до свидания", "рейс в москву", "будет дождь"]\nfor t in test:\n    r = bot.process(t)\n    print(f"  \"{t}\" → {r[\"intent\"]} ({r[\"confidence\"]:.2f}): {r[\"response\"]}")',
      explanation: 'Neural intent classifier (Embedding + Dense) обучается на малых данных и обобщает лучше TF-IDF для коротких фраз. В production используйте BERT fine-tuning (transformers library) — он даёт >95% accuracy на 6-10 интентах с 100-200 примерами.'
    },
    {
      id: 10,
      title: 'Практика: Telegram бот с ML',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создайте полноценного Telegram-бота с ML-based intent classification, entity extraction и dialogue management.',
      requirements: [
        'Реализуйте IntentClassifier (TF-IDF + LogReg) с 6+ интентами',
        'Реализуйте EntityExtractor (regex для городов, дат, чисел)',
        'Реализуйте DialogueManager (frame-based с 3+ интентами)',
        'Создайте класс TelegramBot, объединяющий все компоненты и обрабатывающий сообщения',
        'Продемонстрируйте полный диалог (8+ шагов) с заполнением слотов'
      ],
      hint: 'Используйте python-telegram-bot library: Updater, CommandHandler, MessageHandler. Бот получает update.message.text, прогоняет через NLU pipeline, получает ответ от DM и отправляет обратно через update.message.reply_text().',
      expectedOutput: 'Telegram ML Chatbot:\nIntentClassifier: 6 intents, 36 examples\nEntityExtractor: cities, dates, numbers\nDialogueManager: 3 frames\n\nДиалог:\nUser: Привет\nBot: Здравствуйте! Чем помочь?\nUser: Хочу из Алматы в Москву\nBot: На какую дату?\n...',
      solution: 'import re\nimport numpy as np\nfrom datetime import datetime, timedelta\nfrom sklearn.feature_extraction.text import TfidfVectorizer\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.pipeline import Pipeline\n\n# 1. Intent Classifier\nclass IntentClassifier:\n    def __init__(self):\n        self.data = [\n            ("привет", "greet"), ("здравствуйте", "greet"), ("добрый день", "greet"),\n            ("хай", "greet"), ("приветик", "greet"), ("доброе утро", "greet"),\n            ("пока", "goodbye"), ("до свидания", "goodbye"), ("выход", "goodbye"),\n            ("до встречи", "goodbye"), ("всего доброго", "goodbye"), ("бай", "goodbye"),\n            ("забронировать рейс", "book_flight"), ("хочу билет", "book_flight"),\n            ("лететь в москву", "book_flight"), ("купить авиабилет", "book_flight"),\n            ("нужен рейс", "book_flight"), ("хочу полететь", "book_flight"),\n            ("хочу в отель", "book_hotel"), ("забронировать номер", "book_hotel"),\n            ("нужна гостиница", "book_hotel"), ("отель", "book_hotel"),\n            ("бронь номера", "book_hotel"), ("нужен номер", "book_hotel"),\n            ("какая погода", "weather"), ("прогноз погоды", "weather"),\n            ("температура", "weather"), ("будет дождь", "weather"),\n            ("погода сегодня", "weather"), ("тепло на улице", "weather"),\n            ("отмени бронь", "cancel"), ("отменить заказ", "cancel"),\n            ("не хочу", "cancel"), ("верни деньги", "cancel"),\n            ("отказ", "cancel"), ("отмена", "cancel"),\n        ]\n        texts = [re.sub(r"[^а-яёa-z\\s]", "", t.lower()) for t, _ in self.data]\n        labels = [l for _, l in self.data]\n        self.pipe = Pipeline([\n            ("tfidf", TfidfVectorizer(analyzer="char_wb", ngram_range=(2, 4))),\n            ("clf", LogisticRegression(C=10, max_iter=1000))\n        ])\n        self.pipe.fit(texts, labels)\n        self.intents = set(labels)\n\n    def predict(self, text):\n        clean = re.sub(r"[^а-яёa-z\\s]", "", text.lower())\n        intent = self.pipe.predict([clean])[0]\n        conf = self.pipe.predict_proba([clean]).max()\n        return intent, conf\n\n# 2. Entity Extractor\nclass EntityExtractor:\n    def __init__(self):\n        self.cities = {"москва": "Москва", "москву": "Москва", "алматы": "Алматы",\n                       "астана": "Астана", "астану": "Астана", "париж": "Париж",\n                       "лондон": "Лондон", "токио": "Токио"}\n        self.today = datetime(2024, 6, 15)\n\n    def extract(self, text):\n        t = text.lower()\n        cities = list(set(self.cities[w] for w in t.split() if w in self.cities))\n        dates = []\n        if "завтра" in t:\n            dates.append(("завтра", (self.today + timedelta(1)).strftime("%Y-%m-%d")))\n        if "сегодня" in t:\n            dates.append(("сегодня", self.today.strftime("%Y-%m-%d")))\n        m = re.search(r"через (\\d+) дн", t)\n        if m:\n            d = int(m.group(1))\n            dates.append((f"через {d} дней", (self.today + timedelta(d)).strftime("%Y-%m-%d")))\n        numbers = [int(n) for n in re.findall(r"\\b\\d+\\b", text)]\n        return {"cities": cities, "dates": dates, "numbers": numbers}\n\n# 3. Dialogue Manager\nclass DialogueManager:\n    def __init__(self):\n        self.frames = {\n            "book_flight": {"slots": {"origin": "city", "dest": "city", "date": "date"},\n                            "questions": {"origin": "Откуда?", "dest": "Куда?", "date": "Когда?"},\n                            "done": lambda s: f"Ищу рейс {s[\'origin\']}→{s[\'dest\']} на {s[\'date\']}. Найдено 3 варианта!"},\n            "book_hotel": {"slots": {"city": "city", "nights": "number"},\n                          "questions": {"city": "В каком городе?", "nights": "На сколько ночей?"},\n                          "done": lambda s: f"Бронирую отель в {s[\'city\']} на {s[\'nights\']} ночей."},\n            "weather": {"slots": {"city": "city"},\n                       "questions": {"city": "В каком городе?"},\n                       "done": lambda s: f"Погода в {s[\'city\']}: +22°C, ясно."},\n        }\n        self.state = {}\n\n    def process(self, uid, intent, entities):\n        if intent in ("greet",):\n            return "Здравствуйте! Чем могу помочь?"\n        if intent in ("goodbye",):\n            self.state.pop(uid, None)\n            return "До свидания!"\n        if intent in ("cancel",):\n            self.state.pop(uid, None)\n            return "Отменено."\n        if intent in self.frames:\n            self.state[uid] = {"intent": intent, "slots": {}}\n        s = self.state.get(uid)\n        if not s:\n            return "Не понял. Чем помочь?"\n        frame = self.frames[s["intent"]]\n        for name, stype in frame["slots"].items():\n            if name not in s["slots"]:\n                if stype == "city" and entities["cities"]:\n                    s["slots"][name] = entities["cities"].pop(0)\n                elif stype == "date" and entities["dates"]:\n                    s["slots"][name] = entities["dates"][0][1]\n                elif stype == "number" and entities["numbers"]:\n                    s["slots"][name] = entities["numbers"].pop(0)\n        for name in frame["slots"]:\n            if name not in s["slots"]:\n                return frame["questions"][name]\n        result = frame["done"](s["slots"])\n        self.state.pop(uid, None)\n        return result\n\n# 4. Telegram Bot (симуляция)\nclass TelegramBot:\n    def __init__(self):\n        self.ic = IntentClassifier()\n        self.ee = EntityExtractor()\n        self.dm = DialogueManager()\n\n    def handle_message(self, uid, text):\n        intent, conf = self.ic.predict(text)\n        entities = self.ee.extract(text)\n        response = self.dm.process(uid, intent, entities)\n        return intent, conf, response\n\n# Telegram bot code:\ntelegram_code = \"\"\"\nfrom telegram.ext import Updater, MessageHandler, Filters\nbot_logic = TelegramBot()\ndef handle(update, context):\n    uid = update.message.chat_id\n    text = update.message.text\n    _, _, response = bot_logic.handle_message(uid, text)\n    update.message.reply_text(response)\nupdater = Updater(\"YOUR_TOKEN\")\nupdater.dispatcher.add_handler(MessageHandler(Filters.text, handle))\nupdater.start_polling()\n\"\"\"\n\nbot = TelegramBot()\nprint("Telegram ML Chatbot:")\nprint(f"IntentClassifier: {len(bot.ic.intents)} intents, {len(bot.ic.data)} examples")\nprint(f"EntityExtractor: cities, dates, numbers")\nprint(f"DialogueManager: {len(bot.dm.frames)} frames")\n\ndialogue = [\n    "Привет", "Хочу из Алматы в Москву", "Завтра",\n    "Погода в Токио", "Отель в Лондоне", "На 3 ночи",\n    "Отмени", "Пока"\n]\n\nprint("\\nДиалог:")\nfor text in dialogue:\n    intent, conf, resp = bot.handle_message("user1", text)\n    print(f"User: {text}")\n    print(f"Bot [{intent} {conf:.2f}]: {resp}\\n")',
      explanation: 'Полноценный Telegram ML-бот объединяет три компонента: IntentClassifier (TF-IDF + LogReg), EntityExtractor (regex), DialogueManager (frame-based). Для production: замените TF-IDF на BERT, добавьте SpaCy для NER, используйте GPT для генерации ответов, добавьте логирование и мониторинг.'
    }
  ]
}
