export default {
  id: 54,
  title: 'Проект: Прогноз временных рядов',
  description: 'Полный проект прогнозирования временных рядов: EDA, декомпозиция, ARIMA/SARIMA, Prophet, LSTM и ensemble dashboard.',
  lessons: [
    {
      id: 1,
      title: 'Описание проекта',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Time Series Forecasting' },
        { type: 'text', value: 'Прогнозирование временных рядов — предсказание будущих значений на основе исторических данных. Ключевые компоненты ряда: тренд (долгосрочное направление), сезонность (повторяющиеся паттерны), циклы (нерегулярные колебания) и шум (случайные отклонения).' },
        { type: 'text', value: 'Стационарность — важнейшее свойство для многих моделей. Стационарный ряд: постоянное среднее, постоянная дисперсия, автокорреляция зависит только от лага. ARIMA требует стационарности, Prophet и LSTM — нет.' },
        { type: 'code', language: 'python', value: '# Проект: прогнозирование продаж\n# time_series/\n# ├── data/\n# │   └── sales.csv\n# ├── notebooks/\n# │   ├── 01_eda.ipynb\n# │   ├── 02_arima.ipynb\n# │   ├── 03_prophet.ipynb\n# │   ├── 04_lstm.ipynb\n# │   └── 05_ensemble.ipynb\n# ├── src/\n# │   ├── features.py\n# │   ├── models.py\n# │   └── evaluation.py\n# └── dashboard.py\n\nimport numpy as np\nimport pandas as pd\n\n# Генерация реалистичного временного ряда продаж\nnp.random.seed(42)\nn_days = 730  # 2 года\ndates = pd.date_range("2022-01-01", periods=n_days, freq="D")\n\n# Компоненты ряда\ntrend = np.linspace(100, 180, n_days)  # растущий тренд\nyearly_season = 25 * np.sin(2 * np.pi * np.arange(n_days) / 365)  # годовая сезонность\nweekly_season = 15 * np.sin(2 * np.pi * np.arange(n_days) / 7)    # недельная сезонность\nholiday_effect = np.zeros(n_days)\nfor i, d in enumerate(dates):\n    if d.month == 12 and d.day >= 20:  # новогодние продажи\n        holiday_effect[i] = 40\n    if d.month == 3 and d.day == 8:  # 8 марта\n        holiday_effect[i] = 20\nnoise = np.random.normal(0, 8, n_days)\n\nsales = trend + yearly_season + weekly_season + holiday_effect + noise\nsales = np.maximum(sales, 10)  # продажи >= 10\n\ndf = pd.DataFrame({"date": dates, "sales": sales})\ndf.set_index("date", inplace=True)\n\nprint("=== Временной ряд продаж ===")\nprint(f"Период: {df.index[0].date()} — {df.index[-1].date()}")\nprint(f"Дней: {len(df)}")\nprint(f"Среднее: {df[\'sales\'].mean():.1f}")\nprint(f"Мин: {df[\'sales\'].min():.1f}, Макс: {df[\'sales\'].max():.1f}")\nprint(f"Std: {df[\'sales\'].std():.1f}")\nprint(f"\\nПервые 5 дней:")\nprint(df.head())' },
        { type: 'note', value: 'Реальные временные ряды содержат: тренд (рост бизнеса), сезонность (лето/зима, выходные), праздники (Новый год, 8 марта), аномалии (COVID, кризисы). Хорошая модель должна учитывать все компоненты.' },
        { type: 'tip', value: 'Всегда разделяйте данные хронологически: train = прошлое, test = будущее. Никогда не используйте случайный split для временных рядов — это приведёт к data leakage!' }
      ]
    },
    {
      id: 2,
      title: 'EDA временных рядов',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Декомпозиция, ACF/PACF, тест Дики-Фуллера' },
        { type: 'text', value: 'EDA временных рядов включает: декомпозицию на компоненты (trend, seasonal, residual), анализ автокорреляции (ACF — сколько прошлых значений влияют, PACF — прямое влияние каждого лага), тест стационарности (ADF тест).' },
        { type: 'code', language: 'python', value: 'import numpy as np\nimport pandas as pd\nfrom statsmodels.tsa.seasonal import seasonal_decompose\nfrom statsmodels.tsa.stattools import adfuller, acf, pacf\n\nnp.random.seed(42)\nn = 730\ndates = pd.date_range("2022-01-01", periods=n, freq="D")\ntrend = np.linspace(100, 180, n)\nseason = 25 * np.sin(2 * np.pi * np.arange(n) / 365)\nweekly = 15 * np.sin(2 * np.pi * np.arange(n) / 7)\nnoise = np.random.normal(0, 8, n)\nsales = trend + season + weekly + noise\ndf = pd.DataFrame({"sales": sales}, index=dates)\n\n# 1. Декомпозиция\nprint("=== Декомпозиция временного ряда ===")\ndecomp = seasonal_decompose(df["sales"], model="additive", period=7)\nprint(f"Тренд: {decomp.trend.dropna().iloc[0]:.1f} → {decomp.trend.dropna().iloc[-1]:.1f}")\nprint(f"Сезонность (амплитуда): {decomp.seasonal.max():.1f}")\nprint(f"Остатки (std): {decomp.resid.dropna().std():.1f}")\n\n# 2. Тест Дики-Фуллера (ADF) — проверка стационарности\nprint("\\n=== Тест Дики-Фуллера (ADF) ===")\nadf_result = adfuller(df["sales"])\nprint(f"ADF статистика: {adf_result[0]:.4f}")\nprint(f"p-value: {adf_result[1]:.4f}")\nprint(f"Стационарный: {\'Да\' if adf_result[1] < 0.05 else \'Нет\'}")\n\n# Дифференцирование для стационарности\ndf["sales_diff"] = df["sales"].diff().dropna()\nadf_diff = adfuller(df["sales_diff"].dropna())\nprint(f"\\nПосле дифференцирования (d=1):")\nprint(f"ADF статистика: {adf_diff[0]:.4f}")\nprint(f"p-value: {adf_diff[1]:.6f}")\nprint(f"Стационарный: {\'Да\' if adf_diff[1] < 0.05 else \'Нет\'}")\n\n# 3. ACF и PACF\nprint("\\n=== ACF (первые 10 лагов) ===")\nacf_vals = acf(df["sales_diff"].dropna(), nlags=10)\nfor i, val in enumerate(acf_vals):\n    bar = "█" * int(abs(val) * 20)\n    print(f"  Lag {i:2d}: {val:+.3f} {bar}")\n\nprint("\\n=== PACF (первые 10 лагов) ===")\npacf_vals = pacf(df["sales_diff"].dropna(), nlags=10)\nfor i, val in enumerate(pacf_vals[:8]):\n    bar = "█" * int(abs(val) * 20)\n    print(f"  Lag {i:2d}: {val:+.3f} {bar}")' },
        { type: 'text', value: 'Интерпретация ACF/PACF: ACF затухает плавно + PACF обрывается на лаге p → AR(p). ACF обрывается на лаге q + PACF затухает → MA(q). Оба затухают → ARMA(p,q). Для ARIMA: определите d (порядок дифференцирования) по ADF тесту.' },
        { type: 'tip', value: 'Если p-value ADF теста > 0.05 — ряд нестационарный, нужно дифференцировать (d=1). Если после первого дифференцирования p-value < 0.05 — используйте ARIMA(p, 1, q).' }
      ]
    },
    {
      id: 3,
      title: 'Практика: Анализ временного ряда',
      type: 'practice',
      difficulty: 'easy',
      description: 'Проведите полный EDA временного ряда: декомпозиция, тест стационарности, ACF/PACF анализ.',
      requirements: [
        'Создайте временной ряд продаж (730 дней) с трендом, годовой и недельной сезонностью',
        'Выполните декомпозицию (seasonal_decompose) и выведите характеристики компонент',
        'Проведите ADF тест до и после дифференцирования',
        'Рассчитайте ACF и PACF для первых 10 лагов',
        'Определите рекомендуемые параметры ARIMA (p, d, q)'
      ],
      hint: 'seasonal_decompose(series, period=7) для недельной сезонности. adfuller() возвращает (adf_stat, p_value, ...). Если PACF обрывается на лаге 2 → p=2. Если ACF обрывается на лаге 1 → q=1.',
      expectedOutput: 'EDA Временного Ряда:\nПериод: 730 дней\nТренд: 100 → 180\nСезонность: амплитуда=XX\n\nADF тест:\n  Исходный: p=X.XX (нестационарный)\n  После diff: p=0.00XX (стационарный)\n\nРекомендуемые ARIMA: (p=X, d=1, q=X)',
      solution: 'import numpy as np\nimport pandas as pd\nfrom statsmodels.tsa.seasonal import seasonal_decompose\nfrom statsmodels.tsa.stattools import adfuller, acf, pacf\n\nnp.random.seed(42)\nn = 730\ndates = pd.date_range("2022-01-01", periods=n, freq="D")\ntrend = np.linspace(100, 180, n)\nseason = 25 * np.sin(2 * np.pi * np.arange(n) / 365)\nweekly = 15 * np.sin(2 * np.pi * np.arange(n) / 7)\nnoise = np.random.normal(0, 8, n)\nsales = trend + season + weekly + noise\ndf = pd.DataFrame({"sales": np.maximum(sales, 10)}, index=dates)\n\nprint("EDA Временного Ряда:")\nprint(f"Период: {len(df)} дней")\n\ndecomp = seasonal_decompose(df["sales"], model="additive", period=7)\ntrend_vals = decomp.trend.dropna()\nprint(f"Тренд: {trend_vals.iloc[0]:.0f} → {trend_vals.iloc[-1]:.0f}")\nprint(f"Сезонность: амплитуда={decomp.seasonal.max():.1f}")\nprint(f"Остатки: std={decomp.resid.dropna().std():.1f}")\n\nprint("\\nADF тест:")\nadf1 = adfuller(df["sales"])\nprint(f"  Исходный: p={adf1[1]:.4f} ({\'стационарный\' if adf1[1] < 0.05 else \'нестационарный\'})")\n\ndf["diff"] = df["sales"].diff()\nadf2 = adfuller(df["diff"].dropna())\nprint(f"  После diff: p={adf2[1]:.6f} ({\'стационарный\' if adf2[1] < 0.05 else \'нестационарный\'})")\n\nd = 0 if adf1[1] < 0.05 else 1\n\nprint("\\nACF (лаги 1-7):")\nacf_v = acf(df["diff"].dropna(), nlags=7)\nfor i in range(1, 8):\n    print(f"  Lag {i}: {acf_v[i]:+.3f}")\n\nprint("\\nPACF (лаги 1-7):")\npacf_v = pacf(df["diff"].dropna(), nlags=7)\nfor i in range(1, 8):\n    print(f"  Lag {i}: {pacf_v[i]:+.3f}")\n\n# Определение p и q\np = sum(1 for i in range(1, 5) if abs(pacf_v[i]) > 0.1)\nq = sum(1 for i in range(1, 5) if abs(acf_v[i]) > 0.1)\np = min(p, 3)\nq = min(q, 3)\n\nprint(f"\\nРекомендуемые ARIMA: (p={p}, d={d}, q={q})")',
      explanation: 'EDA временных рядов: декомпозиция показывает компоненты, ADF тест определяет нужно ли дифференцирование (d), ACF/PACF помогают выбрать p и q для ARIMA. Это фундамент для выбора правильной модели.'
    },
    {
      id: 4,
      title: 'ARIMA и SARIMA',
      type: 'theory',
      content: [
        { type: 'heading', value: 'ARIMA(p,d,q) и SARIMA(p,d,q)(P,D,Q,s)' },
        { type: 'text', value: 'ARIMA = AutoRegressive (AR) + Integrated (I) + Moving Average (MA). AR(p) — зависимость от p прошлых значений. I(d) — дифференцирование d раз. MA(q) — зависимость от q прошлых ошибок. SARIMA добавляет сезонные параметры (P,D,Q) с периодом s.' },
        { type: 'code', language: 'python', value: 'import numpy as np\nimport pandas as pd\nfrom statsmodels.tsa.arima.model import ARIMA\nfrom statsmodels.tsa.statespace.sarimax import SARIMAX\nimport warnings\nwarnings.filterwarnings("ignore")\n\nnp.random.seed(42)\nn = 365\ndates = pd.date_range("2023-01-01", periods=n, freq="D")\ntrend = np.linspace(100, 150, n)\nseason = 20 * np.sin(2 * np.pi * np.arange(n) / 7)\nnoise = np.random.normal(0, 5, n)\ndf = pd.DataFrame({"sales": trend + season + noise}, index=dates)\n\ntrain = df.iloc[:300]\ntest = df.iloc[300:]\n\n# ARIMA\nprint("=== ARIMA(2,1,1) ===")\narima = ARIMA(train["sales"], order=(2, 1, 1))\narima_fit = arima.fit()\n\narima_pred = arima_fit.forecast(steps=len(test))\narima_mae = np.mean(np.abs(test["sales"].values - arima_pred.values))\narima_rmse = np.sqrt(np.mean((test["sales"].values - arima_pred.values)**2))\n\nprint(f"AIC: {arima_fit.aic:.1f}")\nprint(f"MAE: {arima_mae:.2f}")\nprint(f"RMSE: {arima_rmse:.2f}")\n\n# SARIMA с недельной сезонностью\nprint("\\n=== SARIMA(1,1,1)(1,1,1,7) ===")\nsarima = SARIMAX(train["sales"], order=(1, 1, 1),\n                 seasonal_order=(1, 1, 1, 7))\nsarima_fit = sarima.fit(disp=False)\n\nsarima_pred = sarima_fit.forecast(steps=len(test))\nsarima_mae = np.mean(np.abs(test["sales"].values - sarima_pred.values))\nsarima_rmse = np.sqrt(np.mean((test["sales"].values - sarima_pred.values)**2))\n\nprint(f"AIC: {sarima_fit.aic:.1f}")\nprint(f"MAE: {sarima_mae:.2f}")\nprint(f"RMSE: {sarima_rmse:.2f}")\n\nprint(f"\\nСравнение:")\nprint(f"  ARIMA  MAE={arima_mae:.2f}, RMSE={arima_rmse:.2f}")\nprint(f"  SARIMA MAE={sarima_mae:.2f}, RMSE={sarima_rmse:.2f}")\nprint(f"  SARIMA лучше на {((arima_mae - sarima_mae) / arima_mae * 100):.1f}%")' },
        { type: 'heading', value: 'Автоматический подбор параметров' },
        { type: 'code', language: 'python', value: '# Auto ARIMA — автоматический подбор (p,d,q)\n# pip install pmdarima\nimport pmdarima as pm\nimport numpy as np\nimport pandas as pd\nimport warnings\nwarnings.filterwarnings("ignore")\n\nnp.random.seed(42)\nn = 365\ntrend = np.linspace(100, 150, n)\nseason = 20 * np.sin(2 * np.pi * np.arange(n) / 7)\nnoise = np.random.normal(0, 5, n)\nsales = trend + season + noise\n\ntrain = sales[:300]\ntest = sales[300:]\n\n# Auto ARIMA\nauto_model = pm.auto_arima(\n    train,\n    start_p=0, max_p=5,\n    start_q=0, max_q=5,\n    d=None,          # автоопределение d\n    seasonal=True,\n    m=7,             # период сезонности\n    start_P=0, max_P=2,\n    start_Q=0, max_Q=2,\n    D=None,          # автоопределение D\n    trace=True,      # показывать процесс поиска\n    error_action="ignore",\n    suppress_warnings=True,\n    stepwise=True    # ускоренный поиск\n)\n\nprint(f"\\nЛучшая модель: {auto_model.summary().tables[0]}")\nprint(f"Order: {auto_model.order}")\nprint(f"Seasonal order: {auto_model.seasonal_order}")\nprint(f"AIC: {auto_model.aic():.1f}")\n\npred = auto_model.predict(n_periods=len(test))\nmae = np.mean(np.abs(test - pred))\nprint(f"MAE: {mae:.2f}")' },
        { type: 'tip', value: 'pmdarima.auto_arima — лучший способ выбрать параметры. Он перебирает комбинации (p,d,q)(P,D,Q,s) и выбирает минимальный AIC. Параметр stepwise=True ускоряет поиск в 10x.' }
      ]
    },
    {
      id: 5,
      title: 'Практика: ARIMA прогноз',
      type: 'practice',
      difficulty: 'medium',
      description: 'Постройте ARIMA и SARIMA модели для прогнозирования продаж и сравните их.',
      requirements: [
        'Создайте временной ряд (365 дней) с трендом и недельной сезонностью',
        'Разделите на train (300 дней) и test (65 дней)',
        'Обучите ARIMA(2,1,1) и SARIMA(1,1,1)(1,1,1,7)',
        'Рассчитайте MAE, RMSE, MAPE для обеих моделей',
        'Выведите сравнение и определите лучшую модель'
      ],
      hint: 'ARIMA: from statsmodels.tsa.arima.model import ARIMA. SARIMA: from statsmodels.tsa.statespace.sarimax import SARIMAX. forecast(steps=N) для предсказания. MAPE = mean(|actual-pred|/actual)*100.',
      expectedOutput: 'ARIMA vs SARIMA:\nTrain: 300 дней, Test: 65 дней\n\nARIMA(2,1,1):\n  MAE=X.XX, RMSE=X.XX, MAPE=X.X%\n\nSARIMA(1,1,1)(1,1,1,7):\n  MAE=X.XX, RMSE=X.XX, MAPE=X.X%\n\nЛучшая: SARIMA',
      solution: 'import numpy as np\nimport pandas as pd\nfrom statsmodels.tsa.arima.model import ARIMA\nfrom statsmodels.tsa.statespace.sarimax import SARIMAX\nimport warnings\nwarnings.filterwarnings("ignore")\n\nnp.random.seed(42)\nn = 365\ndates = pd.date_range("2023-01-01", periods=n, freq="D")\ntrend = np.linspace(100, 150, n)\nseason = 20 * np.sin(2 * np.pi * np.arange(n) / 7)\nnoise = np.random.normal(0, 5, n)\ndf = pd.DataFrame({"sales": trend + season + noise}, index=dates)\n\ntrain = df.iloc[:300]\ntest = df.iloc[300:]\n\ndef calc_metrics(actual, predicted):\n    mae = np.mean(np.abs(actual - predicted))\n    rmse = np.sqrt(np.mean((actual - predicted)**2))\n    mape = np.mean(np.abs((actual - predicted) / actual)) * 100\n    return mae, rmse, mape\n\nprint("ARIMA vs SARIMA:")\nprint(f"Train: {len(train)} дней, Test: {len(test)} дней")\n\n# ARIMA\narima = ARIMA(train["sales"], order=(2, 1, 1)).fit()\narima_pred = arima.forecast(steps=len(test)).values\nmae_a, rmse_a, mape_a = calc_metrics(test["sales"].values, arima_pred)\nprint(f"\\nARIMA(2,1,1):")\nprint(f"  MAE={mae_a:.2f}, RMSE={rmse_a:.2f}, MAPE={mape_a:.1f}%")\n\n# SARIMA\nsarima = SARIMAX(train["sales"], order=(1,1,1), seasonal_order=(1,1,1,7)).fit(disp=False)\nsarima_pred = sarima.forecast(steps=len(test)).values\nmae_s, rmse_s, mape_s = calc_metrics(test["sales"].values, sarima_pred)\nprint(f"\\nSARIMA(1,1,1)(1,1,1,7):")\nprint(f"  MAE={mae_s:.2f}, RMSE={rmse_s:.2f}, MAPE={mape_s:.1f}%")\n\nbest = "SARIMA" if mae_s < mae_a else "ARIMA"\nprint(f"\\nЛучшая: {best}")',
      explanation: 'SARIMA учитывает сезонность и поэтому превосходит ARIMA на данных с регулярными паттернами. Параметр s=7 для недельной сезонности, s=12 для месячной, s=365 для годовой. Auto ARIMA (pmdarima) автоматически подбирает оптимальные параметры.'
    },
    {
      id: 6,
      title: 'Prophet',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Facebook Prophet' },
        { type: 'text', value: 'Prophet — библиотека Meta (Facebook) для прогнозирования временных рядов. Преимущества: автоматическая обработка трендов и сезонности, встроенная поддержка праздников, устойчивость к пропускам и аномалиям, интуитивно понятные параметры.' },
        { type: 'text', value: 'Модель Prophet: y(t) = trend(t) + seasonality(t) + holidays(t) + error(t). Тренд моделируется как кусочно-линейная или логистическая функция. Сезонность — ряд Фурье. Праздники — отдельные эффекты с окном влияния.' },
        { type: 'code', language: 'python', value: 'from prophet import Prophet\nimport pandas as pd\nimport numpy as np\n\nnp.random.seed(42)\nn = 730\ndates = pd.date_range("2022-01-01", periods=n, freq="D")\ntrend = np.linspace(100, 180, n)\nseason = 25 * np.sin(2 * np.pi * np.arange(n) / 365)\nweekly = 15 * np.sin(2 * np.pi * np.arange(n) / 7)\nnoise = np.random.normal(0, 8, n)\nsales = trend + season + weekly + noise\n\n# Prophet требует колонки ds (дата) и y (значение)\ndf = pd.DataFrame({"ds": dates, "y": sales})\n\ntrain = df.iloc[:660]\ntest = df.iloc[660:]\n\n# Обучение Prophet\nmodel = Prophet(\n    yearly_seasonality=True,   # годовая сезонность\n    weekly_seasonality=True,   # недельная сезонность\n    daily_seasonality=False,   # дневная (для почасовых данных)\n    changepoint_prior_scale=0.05,  # гибкость тренда\n    seasonality_prior_scale=10,    # сила сезонности\n)\n\n# Добавление праздников\nholidays = pd.DataFrame({\n    "holiday": "new_year",\n    "ds": pd.to_datetime(["2022-12-31", "2023-12-31"]),\n    "lower_window": -5,  # 5 дней до\n    "upper_window": 5,   # 5 дней после\n})\nmodel = Prophet(holidays=holidays, yearly_seasonality=True,\n                weekly_seasonality=True)\n\nmodel.fit(train)\n\n# Прогноз\nfuture = model.make_future_dataframe(periods=len(test))\nforecast = model.predict(future)\n\n# Оценка\npred = forecast.iloc[-len(test):]["yhat"].values\nactual = test["y"].values\nmae = np.mean(np.abs(actual - pred))\nrmse = np.sqrt(np.mean((actual - pred)**2))\n\nprint("=== Prophet Forecast ===")\nprint(f"Train: {len(train)} дней")\nprint(f"Test: {len(test)} дней")\nprint(f"MAE: {mae:.2f}")\nprint(f"RMSE: {rmse:.2f}")\n\n# Компоненты модели\nprint("\\nКомпоненты:")\nprint(f"  Тренд: {forecast[\'trend\'].iloc[0]:.1f} → {forecast[\'trend\'].iloc[-1]:.1f}")\nprint(f"  Годовая сезонность: {forecast[\'yearly\'].max():.1f}")\nprint(f"  Недельная сезонность: {forecast[\'weekly\'].max():.1f}")' },
        { type: 'heading', value: 'Настройка Prophet' },
        { type: 'code', language: 'python', value: '# Настройка гиперпараметров Prophet\nfrom prophet import Prophet\nfrom prophet.diagnostics import cross_validation, performance_metrics\nimport pandas as pd\nimport numpy as np\nimport itertools\nimport warnings\nwarnings.filterwarnings("ignore")\n\n# Данные\nnp.random.seed(42)\nn = 365\ndf = pd.DataFrame({\n    "ds": pd.date_range("2023-01-01", periods=n, freq="D"),\n    "y": np.linspace(100, 150, n) + 20*np.sin(2*np.pi*np.arange(n)/7) + np.random.normal(0, 5, n)\n})\n\n# Cross-validation Prophet\nmodel = Prophet(weekly_seasonality=True, yearly_seasonality=False)\nmodel.fit(df)\n\n# Prophet cross_validation: initial=180 дней train, period=30 дней между fold,\n# horizon=30 дней прогноз\ndf_cv = cross_validation(model, initial="180 days", period="30 days",\n                          horizon="30 days")\ndf_p = performance_metrics(df_cv)\nprint("=== Prophet Cross-Validation ===")\nprint(f"MAE: {df_p[\'mae\'].mean():.2f}")\nprint(f"RMSE: {df_p[\'rmse\'].mean():.2f}")\nprint(f"MAPE: {df_p[\'mape\'].mean()*100:.1f}%")\n\n# Grid search\nprint("\\n=== Hyperparameter Tuning ===")\nparam_grid = {\n    "changepoint_prior_scale": [0.01, 0.05, 0.1],\n    "seasonality_prior_scale": [1, 5, 10],\n}\n\nbest_mae = float("inf")\nbest_params = {}\nfor cps in param_grid["changepoint_prior_scale"]:\n    for sps in param_grid["seasonality_prior_scale"]:\n        m = Prophet(changepoint_prior_scale=cps, seasonality_prior_scale=sps,\n                    weekly_seasonality=True, yearly_seasonality=False)\n        m.fit(df)\n        cv = cross_validation(m, initial="180 days", period="60 days",\n                               horizon="30 days")\n        metrics = performance_metrics(cv)\n        mae = metrics["mae"].mean()\n        print(f"  cps={cps}, sps={sps}: MAE={mae:.2f}")\n        if mae < best_mae:\n            best_mae = mae\n            best_params = {"cps": cps, "sps": sps}\n\nprint(f"\\nЛучшие параметры: {best_params} (MAE={best_mae:.2f})")' },
        { type: 'tip', value: 'changepoint_prior_scale контролирует гибкость тренда: маленькое значение (0.01) → гладкий тренд, большое (0.5) → тренд следует за данными. seasonality_prior_scale: больше → сильнее сезонность.' }
      ]
    },
    {
      id: 7,
      title: 'Практика: Prophet прогноз',
      type: 'practice',
      difficulty: 'medium',
      description: 'Постройте прогноз с помощью Prophet: обучение, добавление праздников, cross-validation.',
      requirements: [
        'Создайте временной ряд (730 дней) с трендом, сезонностью и шумом',
        'Обучите Prophet с yearly и weekly сезонностью',
        'Добавьте минимум 2 праздника (Новый год, 8 марта) с окнами влияния',
        'Выполните cross-validation и выведите MAE, RMSE',
        'Предскажите на 70 дней и сравните с test данными'
      ],
      hint: 'Prophet требует DataFrame с колонками ds (datetime) и y (float). Праздники: DataFrame с holiday, ds, lower_window, upper_window. cross_validation(model, initial="365 days", period="30 days", horizon="70 days").',
      expectedOutput: 'Prophet Forecast:\nТренировка: 660 дней\nТест: 70 дней\n\nCross-Validation:\n  MAE=X.XX, RMSE=X.XX\n\nПрогноз на тест:\n  MAE=X.XX, RMSE=X.XX, MAPE=X.X%\n\nКомпоненты:\n  Тренд: XXX → XXX\n  Сезонность: амплитуда=XX',
      solution: 'import numpy as np\nimport pandas as pd\nfrom prophet import Prophet\nfrom prophet.diagnostics import cross_validation, performance_metrics\nimport warnings\nwarnings.filterwarnings("ignore")\n\nnp.random.seed(42)\nn = 730\ndates = pd.date_range("2022-01-01", periods=n, freq="D")\ntrend = np.linspace(100, 180, n)\nseason = 25 * np.sin(2 * np.pi * np.arange(n) / 365)\nweekly = 15 * np.sin(2 * np.pi * np.arange(n) / 7)\nnoise = np.random.normal(0, 8, n)\ndf = pd.DataFrame({"ds": dates, "y": trend + season + weekly + noise})\n\ntrain = df.iloc[:660]\ntest = df.iloc[660:]\n\n# Праздники\nholidays = pd.DataFrame([\n    {"holiday": "new_year", "ds": "2022-12-31", "lower_window": -5, "upper_window": 5},\n    {"holiday": "new_year", "ds": "2023-12-31", "lower_window": -5, "upper_window": 5},\n    {"holiday": "womens_day", "ds": "2022-03-08", "lower_window": -1, "upper_window": 1},\n    {"holiday": "womens_day", "ds": "2023-03-08", "lower_window": -1, "upper_window": 1},\n])\nholidays["ds"] = pd.to_datetime(holidays["ds"])\n\nmodel = Prophet(holidays=holidays, yearly_seasonality=True,\n                weekly_seasonality=True, changepoint_prior_scale=0.05)\nmodel.fit(train)\n\nprint("Prophet Forecast:")\nprint(f"Тренировка: {len(train)} дней")\nprint(f"Тест: {len(test)} дней")\n\n# Cross-validation\ndf_cv = cross_validation(model, initial="365 days", period="30 days", horizon="70 days")\nmetrics = performance_metrics(df_cv)\nprint(f"\\nCross-Validation:")\nprint(f"  MAE={metrics[\'mae\'].mean():.2f}, RMSE={metrics[\'rmse\'].mean():.2f}")\n\n# Прогноз на тест\nfuture = model.make_future_dataframe(periods=len(test))\nforecast = model.predict(future)\npred = forecast.iloc[-len(test):]["yhat"].values\nactual = test["y"].values\n\nmae = np.mean(np.abs(actual - pred))\nrmse = np.sqrt(np.mean((actual - pred)**2))\nmape = np.mean(np.abs((actual - pred) / actual)) * 100\n\nprint(f"\\nПрогноз на тест:")\nprint(f"  MAE={mae:.2f}, RMSE={rmse:.2f}, MAPE={mape:.1f}%")\n\nprint(f"\\nКомпоненты:")\nprint(f"  Тренд: {forecast[\'trend\'].iloc[0]:.0f} → {forecast[\'trend\'].iloc[-1]:.0f}")\nprint(f"  Сезонность: амплитуда={forecast[\'yearly\'].max():.1f}")',
      explanation: 'Prophet автоматически обрабатывает тренд и множественные сезонности. Добавление праздников улучшает прогноз в периоды аномальных продаж. Cross-validation с expanding window — правильный способ оценки для временных рядов.'
    },
    {
      id: 8,
      title: 'Deep Learning',
      type: 'theory',
      content: [
        { type: 'heading', value: 'LSTM и Transformer для временных рядов' },
        { type: 'text', value: 'LSTM (Long Short-Term Memory) — рекуррентная нейросеть, способная запоминать долгосрочные зависимости. Для временных рядов: входная последовательность (lookback window) → предсказание следующих значений. Transformer архитектуры (Temporal Fusion Transformer) показывают state-of-the-art результаты.' },
        { type: 'code', language: 'python', value: 'import numpy as np\nimport tensorflow as tf\nfrom tensorflow.keras import layers\nfrom sklearn.preprocessing import MinMaxScaler\n\n# Подготовка данных для LSTM\ndef create_sequences(data, lookback=30, forecast=1):\n    """Создание последовательностей для LSTM.\n    lookback: сколько прошлых точек использовать\n    forecast: сколько будущих точек предсказывать\n    """\n    X, y = [], []\n    for i in range(lookback, len(data) - forecast + 1):\n        X.append(data[i - lookback:i])\n        y.append(data[i:i + forecast])\n    return np.array(X), np.array(y)\n\n# Данные\nnp.random.seed(42)\nn = 500\ntrend = np.linspace(100, 180, n)\nseason = 20 * np.sin(2 * np.pi * np.arange(n) / 30)\nnoise = np.random.normal(0, 5, n)\nsales = trend + season + noise\n\n# Нормализация\nscaler = MinMaxScaler()\nsales_scaled = scaler.fit_transform(sales.reshape(-1, 1)).flatten()\n\n# Последовательности\nlookback = 30\nX, y = create_sequences(sales_scaled, lookback=lookback, forecast=1)\ny = y.flatten()\n\n# Train/Test split (хронологический!)\nsplit = int(0.85 * len(X))\nX_train, X_test = X[:split], X[split:]\ny_train, y_test = y[:split], y[split:]\n\n# Reshape для LSTM: (samples, timesteps, features)\nX_train = X_train.reshape(-1, lookback, 1)\nX_test = X_test.reshape(-1, lookback, 1)\n\nprint(f"=== LSTM для Time Series ===")\nprint(f"Lookback: {lookback} шагов")\nprint(f"Train: {len(X_train)}, Test: {len(X_test)}")\n\n# LSTM модель\nmodel = tf.keras.Sequential([\n    layers.LSTM(64, return_sequences=True, input_shape=(lookback, 1)),\n    layers.Dropout(0.2),\n    layers.LSTM(32),\n    layers.Dropout(0.2),\n    layers.Dense(16, activation="relu"),\n    layers.Dense(1)\n])\n\nmodel.compile(optimizer="adam", loss="mse", metrics=["mae"])\nprint(f"Параметры модели: {model.count_params():,}")\n\nhistory = model.fit(X_train, y_train, epochs=30, batch_size=32,\n                    validation_split=0.1, verbose=0)\n\n# Оценка\ny_pred_scaled = model.predict(X_test, verbose=0).flatten()\ny_pred = scaler.inverse_transform(y_pred_scaled.reshape(-1, 1)).flatten()\ny_actual = scaler.inverse_transform(y_test.reshape(-1, 1)).flatten()\n\nmae = np.mean(np.abs(y_actual - y_pred))\nrmse = np.sqrt(np.mean((y_actual - y_pred)**2))\nprint(f"\\nРезультаты:")\nprint(f"MAE: {mae:.2f}")\nprint(f"RMSE: {rmse:.2f}")' },
        { type: 'heading', value: 'Многошаговый прогноз' },
        { type: 'code', language: 'python', value: '# Стратегии многошагового прогноза\n\n# 1. Recursive (авторегрессионный) — предсказание по одному шагу\ndef recursive_forecast(model, last_sequence, n_steps, scaler):\n    """Предсказываем один шаг, добавляем в окно, повторяем."""\n    predictions = []\n    current = last_sequence.copy()\n    \n    for _ in range(n_steps):\n        pred = model.predict(current.reshape(1, -1, 1), verbose=0)[0, 0]\n        predictions.append(pred)\n        current = np.append(current[1:], pred)\n    \n    predictions = np.array(predictions).reshape(-1, 1)\n    return scaler.inverse_transform(predictions).flatten()\n\n# 2. Direct — отдельная модель для каждого горизонта\n# model_1 предсказывает t+1, model_2 предсказывает t+2, ...\n\n# 3. Multi-output — одна модель предсказывает все шаги сразу\ndef build_multi_output_lstm(lookback, forecast_steps):\n    model = tf.keras.Sequential([\n        layers.LSTM(64, return_sequences=True, input_shape=(lookback, 1)),\n        layers.LSTM(32),\n        layers.Dense(32, activation="relu"),\n        layers.Dense(forecast_steps)  # N выходов = N шагов прогноза\n    ])\n    model.compile(optimizer="adam", loss="mse")\n    return model\n\nprint("=== Стратегии многошагового прогноза ===")\nprint("1. Recursive: предсказываем по одному → ошибки накапливаются")\nprint("2. Direct: N моделей → точнее, но дорого")\nprint("3. Multi-output: одна модель на N шагов → баланс")\nprint("\\nРекомендация: Multi-output LSTM для 7-30 шагов,\\n  Recursive для 1-3 шагов")' },
        { type: 'tip', value: 'Для LSTM критично: нормализация данных (MinMaxScaler), правильный lookback (обычно 2-3x период сезонности), Dropout для регуляризации. Не забудьте inverse_transform для финальных предсказаний!' }
      ]
    },
    {
      id: 9,
      title: 'Практика: LSTM прогноз',
      type: 'practice',
      difficulty: 'hard',
      description: 'Постройте LSTM модель для прогнозирования временного ряда с подготовкой последовательностей и оценкой.',
      requirements: [
        'Создайте временной ряд (500 точек) с трендом и сезонностью',
        'Нормализуйте данные MinMaxScaler и создайте последовательности (lookback=30)',
        'Постройте LSTM: 2 слоя LSTM(64, 32) + Dropout + Dense',
        'Обучите на 30 эпох, разделив хронологически (85% train)',
        'Выведите MAE, RMSE и сравните с naive forecast (предыдущее значение)'
      ],
      hint: 'create_sequences: для каждой позиции i берём X=data[i-lookback:i], y=data[i]. Reshape X для LSTM: (samples, timesteps, 1). Naive forecast: предсказание = предыдущее значение (baseline).',
      expectedOutput: 'LSTM Time Series Forecast:\nLookback: 30, Train: XXX, Test: XX\n\nОбучение (30 эпох): loss=0.XXXX\n\nРезультаты:\n  LSTM: MAE=X.XX, RMSE=X.XX\n  Naive: MAE=X.XX, RMSE=X.XX\n  Улучшение: +X.X%',
      solution: 'import numpy as np\nimport tensorflow as tf\nfrom tensorflow.keras import layers\nfrom sklearn.preprocessing import MinMaxScaler\n\nnp.random.seed(42)\ntf.random.set_seed(42)\n\nn = 500\ntrend = np.linspace(100, 180, n)\nseason = 20 * np.sin(2 * np.pi * np.arange(n) / 30)\nnoise = np.random.normal(0, 5, n)\nsales = trend + season + noise\n\nscaler = MinMaxScaler()\nsales_sc = scaler.fit_transform(sales.reshape(-1, 1)).flatten()\n\nlookback = 30\nX, y = [], []\nfor i in range(lookback, len(sales_sc)):\n    X.append(sales_sc[i-lookback:i])\n    y.append(sales_sc[i])\nX, y = np.array(X), np.array(y)\n\nsplit = int(0.85 * len(X))\nX_train, X_test = X[:split].reshape(-1, lookback, 1), X[split:].reshape(-1, lookback, 1)\ny_train, y_test = y[:split], y[split:]\n\nprint("LSTM Time Series Forecast:")\nprint(f"Lookback: {lookback}, Train: {len(X_train)}, Test: {len(X_test)}")\n\nmodel = tf.keras.Sequential([\n    layers.LSTM(64, return_sequences=True, input_shape=(lookback, 1)),\n    layers.Dropout(0.2),\n    layers.LSTM(32),\n    layers.Dropout(0.2),\n    layers.Dense(16, activation="relu"),\n    layers.Dense(1)\n])\nmodel.compile(optimizer="adam", loss="mse")\n\nhistory = model.fit(X_train, y_train, epochs=30, batch_size=32,\n                    validation_split=0.1, verbose=0)\nprint(f"\\nОбучение (30 эпох): loss={history.history[\'loss\'][-1]:.4f}")\n\ny_pred_sc = model.predict(X_test, verbose=0).flatten()\ny_pred = scaler.inverse_transform(y_pred_sc.reshape(-1, 1)).flatten()\ny_actual = scaler.inverse_transform(y_test.reshape(-1, 1)).flatten()\n\nmae_lstm = np.mean(np.abs(y_actual - y_pred))\nrmse_lstm = np.sqrt(np.mean((y_actual - y_pred)**2))\n\n# Naive baseline\nnaive_pred = scaler.inverse_transform(X_test[:, -1, 0].reshape(-1, 1)).flatten()\nmae_naive = np.mean(np.abs(y_actual - naive_pred))\nrmse_naive = np.sqrt(np.mean((y_actual - naive_pred)**2))\n\nimprovement = (mae_naive - mae_lstm) / mae_naive * 100\n\nprint(f"\\nРезультаты:")\nprint(f"  LSTM: MAE={mae_lstm:.2f}, RMSE={rmse_lstm:.2f}")\nprint(f"  Naive: MAE={mae_naive:.2f}, RMSE={rmse_naive:.2f}")\nprint(f"  Улучшение: {improvement:+.1f}%")',
      explanation: 'LSTM запоминает паттерны в последовательности (lookback=30 шагов) и предсказывает следующее значение. Нормализация обязательна для стабильного обучения. Naive forecast — важный baseline: если LSTM не лучше — модель не работает.'
    },
    {
      id: 10,
      title: 'Практика: Ensemble прогноз + dashboard',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создайте ensemble из нескольких моделей прогнозирования и визуальный dashboard с метриками.',
      requirements: [
        'Создайте временной ряд (730 дней), train/test split (660/70)',
        'Обучите 3 модели: SARIMA, GradientBoosting (с lag features), простой LSTM',
        'Создайте weighted ensemble (оптимальные веса по MAE на validation)',
        'Выведите метрики всех моделей и ensemble (MAE, RMSE, MAPE)',
        'Сформируйте текстовый dashboard с результатами и рекомендациями'
      ],
      hint: 'Для ensemble: веса пропорциональны 1/MAE каждой модели (нормализованные). GradientBoosting с lag features: lag_7, lag_14, rolling_mean_7, day_of_week, month. Простой LSTM с lookback=14.',
      expectedOutput: 'Ensemble Forecast Dashboard:\n═══════════════════════════\nМодели:\n  SARIMA:  MAE=X.XX, RMSE=X.XX\n  GBT:    MAE=X.XX, RMSE=X.XX\n  LSTM:   MAE=X.XX, RMSE=X.XX\n  Ensemble: MAE=X.XX, RMSE=X.XX\n\nВеса: SARIMA=0.XX, GBT=0.XX, LSTM=0.XX\nЛучшая модель: Ensemble',
      solution: 'import numpy as np\nimport pandas as pd\nfrom statsmodels.tsa.statespace.sarimax import SARIMAX\nfrom sklearn.ensemble import GradientBoostingRegressor\nimport tensorflow as tf\nfrom tensorflow.keras import layers\nfrom sklearn.preprocessing import MinMaxScaler\nimport warnings\nwarnings.filterwarnings("ignore")\n\nnp.random.seed(42)\ntf.random.set_seed(42)\n\nn = 730\ndates = pd.date_range("2022-01-01", periods=n, freq="D")\ntrend = np.linspace(100, 180, n)\nseason = 25 * np.sin(2 * np.pi * np.arange(n) / 365)\nweekly = 15 * np.sin(2 * np.pi * np.arange(n) / 7)\nnoise = np.random.normal(0, 8, n)\ndf = pd.DataFrame({"sales": trend + season + weekly + noise}, index=dates)\n\ntrain = df.iloc[:660]\ntest = df.iloc[660:]\nactual = test["sales"].values\n\ndef metrics(actual, pred):\n    mae = np.mean(np.abs(actual - pred))\n    rmse = np.sqrt(np.mean((actual - pred)**2))\n    mape = np.mean(np.abs((actual - pred) / actual)) * 100\n    return mae, rmse, mape\n\n# 1. SARIMA\nsarima = SARIMAX(train["sales"], order=(1,1,1), seasonal_order=(1,1,1,7)).fit(disp=False)\npred_sarima = sarima.forecast(steps=len(test)).values\n\n# 2. GradientBoosting с lag features\ndf_gb = df.copy()\ndf_gb["lag_7"] = df_gb["sales"].shift(7)\ndf_gb["lag_14"] = df_gb["sales"].shift(14)\ndf_gb["roll_mean_7"] = df_gb["sales"].shift(1).rolling(7).mean()\ndf_gb["dow"] = df_gb.index.dayofweek\ndf_gb["month"] = df_gb.index.month\ndf_gb = df_gb.dropna()\nfeats = ["lag_7", "lag_14", "roll_mean_7", "dow", "month"]\ntrain_gb = df_gb.iloc[:660-14]\ntest_gb = df_gb.iloc[660-14:660-14+len(test)]\ngbt = GradientBoostingRegressor(n_estimators=200, max_depth=5, random_state=42)\ngbt.fit(train_gb[feats], train_gb["sales"])\npred_gbt = gbt.predict(test_gb[feats])\nif len(pred_gbt) > len(actual):\n    pred_gbt = pred_gbt[:len(actual)]\nelif len(pred_gbt) < len(actual):\n    actual = actual[:len(pred_gbt)]\n    pred_sarima = pred_sarima[:len(pred_gbt)]\n\n# 3. LSTM\nscaler = MinMaxScaler()\nall_sc = scaler.fit_transform(df["sales"].values.reshape(-1, 1)).flatten()\nlb = 14\nX_all, y_all = [], []\nfor i in range(lb, len(all_sc)):\n    X_all.append(all_sc[i-lb:i])\n    y_all.append(all_sc[i])\nX_all, y_all = np.array(X_all), np.array(y_all)\nsp = 660 - lb\nX_tr = X_all[:sp].reshape(-1, lb, 1)\ny_tr = y_all[:sp]\nX_te = X_all[sp:sp+len(actual)].reshape(-1, lb, 1)\n\nlstm = tf.keras.Sequential([\n    layers.LSTM(32, input_shape=(lb, 1)),\n    layers.Dense(16, activation="relu"),\n    layers.Dense(1)\n])\nlstm.compile(optimizer="adam", loss="mse")\nlstm.fit(X_tr, y_tr, epochs=20, batch_size=32, verbose=0)\npred_lstm_sc = lstm.predict(X_te, verbose=0).flatten()\npred_lstm = scaler.inverse_transform(pred_lstm_sc.reshape(-1, 1)).flatten()\n\nmin_len = min(len(actual), len(pred_sarima), len(pred_gbt), len(pred_lstm))\nactual = actual[:min_len]\npred_sarima = pred_sarima[:min_len]\npred_gbt = pred_gbt[:min_len]\npred_lstm = pred_lstm[:min_len]\n\n# Ensemble\nmae_s, _, _ = metrics(actual, pred_sarima)\nmae_g, _, _ = metrics(actual, pred_gbt)\nmae_l, _, _ = metrics(actual, pred_lstm)\ninv_mae = np.array([1/mae_s, 1/mae_g, 1/mae_l])\nweights = inv_mae / inv_mae.sum()\npred_ens = weights[0]*pred_sarima + weights[1]*pred_gbt + weights[2]*pred_lstm\n\nprint("Ensemble Forecast Dashboard:")\nprint("═" * 40)\nmodels = {"SARIMA": pred_sarima, "GBT": pred_gbt, "LSTM": pred_lstm, "Ensemble": pred_ens}\nprint("Модели:")\nbest_name, best_mae = "", float("inf")\nfor name, pred in models.items():\n    m, r, p = metrics(actual, pred)\n    print(f"  {name:10s}: MAE={m:.2f}, RMSE={r:.2f}, MAPE={p:.1f}%")\n    if m < best_mae:\n        best_mae = m\n        best_name = name\n\nprint(f"\\nВеса: SARIMA={weights[0]:.2f}, GBT={weights[1]:.2f}, LSTM={weights[2]:.2f}")\nprint(f"Лучшая модель: {best_name}")',
      explanation: 'Ensemble — комбинация нескольких моделей — обычно точнее любой отдельной модели. Веса, пропорциональные 1/MAE, дают больший вес лучшим моделям. В production: SARIMA для стабильной базы, GBT для нелинейных паттернов, LSTM для сложных зависимостей.'
    }
  ]
}
