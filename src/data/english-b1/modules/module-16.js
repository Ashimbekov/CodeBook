export default {
  id: 16,
  title: 'IT: базы данных',
  description: 'Словарь баз данных: query, index, migration, transaction, schema, normalization, sharding и другие ключевые термины.',
  lessons: [
    {
      id: 1,
      title: 'Query: запросы к базе данных',
      type: 'theory',
      content: [
        {
          type: 'text',
          value: 'QUERY (запрос) — команда для получения или изменения данных в БД.\n\nОсновные SQL операции:\nSELECT — получить данные\nINSERT — добавить данные\nUPDATE — обновить данные\nDELETE — удалить данные\n\nКлючевые фразы:\n- to run / execute a query\n- to write a query\n- query optimization\n- slow query (медленный запрос)\n- query plan / execution plan\n- to profile a query\n- JOIN (объединение таблиц)\n- subquery (подзапрос)\n- aggregate function (агрегатная функция)\n\nExamples:\n1. "The query takes 5 seconds — we need to optimize it."\n2. "Always use parameterized queries to prevent SQL injection."\n3. "The query execution plan shows that a full table scan is being performed."\n4. "We use JOINs to combine data from multiple tables."\n5. "The N+1 query problem causes excessive database calls — use eager loading."\n6. "Aggregate queries like COUNT and SUM can be expensive on large tables."\n7. "We use query profiling to identify slow queries in production."'
        },
        {
          type: 'text',
          value: 'Типы запросов и проблемы:\n\nN+1 QUERY PROBLEM:\n"The N+1 problem occurs when you fetch N records and then make one additional query per record."\n"We solved the N+1 problem by using JOIN instead of lazy loading."\n"ORM frameworks like Django or ActiveRecord can hide N+1 problems."\n\nFULL TABLE SCAN:\n"A full table scan reads every row in the table — very slow on large tables."\n"Adding an index prevented the full table scan."\n"The query planner performs a full table scan when no suitable index exists."\n\nRAW QUERY vs ORM QUERY:\n"We use ORM for simple CRUD operations and raw SQL for complex analytics."\n"The ORM generated an inefficient query — we had to write raw SQL."\n"Always check the SQL generated by your ORM using logging."'
        }
      ]
    },
    {
      id: 2,
      title: 'Index: индексы',
      type: 'theory',
      content: [
        {
          type: 'text',
          value: 'INDEX (индекс) — структура данных, ускоряющая поиск в таблице.\n\nАналогия: индекс в книге — позволяет быстро найти страницу по ключевому слову.\n\nТипы индексов:\n- B-tree index (дефолтный в PostgreSQL/MySQL)\n- Hash index (для поиска по равенству)\n- Composite index / multi-column index (по нескольким колонкам)\n- Partial index (по подмножеству строк)\n- Unique index (гарантирует уникальность)\n- Full-text index (для поиска по тексту)\n\nКлючевые фразы:\n- to create an index\n- to add an index on a column\n- to drop an index\n- index scan vs full table scan\n- index cardinality (кардинальность — уникальность значений)\n- covering index (покрывающий индекс)\n- to rebuild an index\n- missing index\n\nExamples:\n1. "We added an index on the email column to speed up user lookups."\n2. "The query was performing a full table scan because of a missing index."\n3. "Indices speed up reads but slow down writes — there\'s a trade-off."\n4. "A composite index on (user_id, created_at) optimizes time-filtered user queries."\n5. "We identified three missing indices using the slow query log."\n6. "Unique indices enforce data integrity at the database level."'
        },
        {
          type: 'text',
          value: 'Когда использовать индексы:\n\nСоздавайте индексы на:\n1. Колонки в условиях WHERE\n2. Колонки в JOIN условиях\n3. Колонки в ORDER BY\n4. Foreign key колонки\n5. Колонки с высокой кардинальностью\n\nОсторожно с индексами:\n1. "Too many indices slow down INSERT/UPDATE/DELETE operations."\n2. "Unused indices waste disk space and slow down writes."\n3. "Index maintenance during VACUUM operations can be expensive."\n4. "The query planner may not use an index if selectivity is low."\n\nПример обсуждения:\n"Our user search query was taking 3 seconds. We looked at the execution plan and found it was doing a full table scan on the email column. We added an index and the query now runs in 5 milliseconds. However, we noticed that the table has 20 indices — we need to audit which ones are actually being used."'
        }
      ]
    },
    {
      id: 3,
      title: 'Migration: миграции',
      type: 'theory',
      content: [
        {
          type: 'text',
          value: 'MIGRATION (миграция) — версионированное изменение структуры базы данных.\n\nMigrations are like version control for the database schema.\n\nКлючевые фразы:\n- to run / apply a migration\n- to roll back a migration\n- migration script\n- schema migration\n- data migration\n- to create a migration\n- migration history / migration table\n- up / down migration (применить / откатить)\n- pending migrations\n- zero-downtime migration\n\nExamples:\n1. "We run database migrations as part of the deployment process."\n2. "Always test migration rollback before applying to production."\n3. "The migration adds a new column to the users table."\n4. "Schema migrations are applied automatically during deployment."\n5. "We have 47 migration files tracking all database changes since launch."\n6. "A zero-downtime migration adds a column with a default value, then backfills existing rows."'
        },
        {
          type: 'text',
          value: 'Типы миграций:\n\nSCHEMA MIGRATION — изменение структуры:\n"This migration adds a \'phone_number\' column to the users table."\n"We dropped the \'legacy_field\' column in migration 0023."\n"The migration renames the \'address\' table to \'addresses\'."\n\nDATA MIGRATION — перемещение или трансформация данных:\n"The data migration backfills the \'full_name\' column from \'first_name\' and \'last_name\'."\n"We ran a data migration to normalize phone number formats."\n\nDANGEROUS MIGRATIONS (требуют осторожности):\n1. "Dropping a column — ensure no code references it first."\n2. "Adding a NOT NULL column without a default — can break existing rows."\n3. "Renaming a table — requires updating all references atomically."\n4. "Adding a large index — can lock the table for minutes in production."\n\nПример планирования миграции:\n"Before adding the \'verified_at\' NOT NULL column, we need to: (1) deploy code that populates the column, (2) run the data migration to backfill existing rows, (3) then add the NOT NULL constraint. This avoids locking the table."'
        }
      ]
    },
    {
      id: 4,
      title: 'Transaction: транзакции',
      type: 'theory',
      content: [
        {
          type: 'text',
          value: 'TRANSACTION (транзакция) — группа операций, выполняющихся как единое целое.\n\nACID принципы:\n- Atomicity (Атомарность) — всё или ничего\n- Consistency (Согласованность) — данные остаются корректными\n- Isolation (Изолированность) — транзакции не мешают друг другу\n- Durability (Долговечность) — завершённые данные сохраняются\n\nКлючевые фразы:\n- to start / begin a transaction\n- to commit a transaction\n- to roll back a transaction\n- transaction isolation level\n- concurrent transactions\n- deadlock (взаимная блокировка)\n- lock (блокировка)\n- ACID compliance\n\nExamples:\n1. "Wrap the payment process in a transaction to ensure atomicity."\n2. "If any step fails, we roll back the entire transaction."\n3. "We commit the transaction only after all operations succeed."\n4. "A deadlock occurred when two transactions tried to lock the same rows in opposite order."\n5. "The database uses MVCC to provide isolation without blocking reads."\n6. "We use database transactions to maintain data consistency in financial operations."'
        },
        {
          type: 'text',
          value: 'Уровни изоляции (Isolation Levels):\n\nREAD UNCOMMITTED — читать незафиксированные данные (dirty reads).\nREAD COMMITTED — читать только зафиксированные данные (стандарт в PostgreSQL).\nREPEATABLE READ — гарантия одинакового результата при повторном чтении.\nSERIALIZABLE — строгая изоляция, как если бы транзакции выполнялись последовательно.\n\nProblems:\n- Dirty read — чтение незафиксированных данных другой транзакции\n- Non-repeatable read — данные изменились между двумя чтениями\n- Phantom read — новые строки появились в результате запроса\n\nExamples:\n1. "We use SERIALIZABLE isolation for financial transactions to prevent phantom reads."\n2. "READ COMMITTED is sufficient for most of our application queries."\n3. "Phantom reads caused incorrect inventory counts — we increased the isolation level."\n4. "Deadlocks are automatically detected and one transaction is rolled back."'
        }
      ]
    },
    {
      id: 5,
      title: 'Schema, Normalization, Denormalization',
      type: 'theory',
      content: [
        {
          type: 'text',
          value: 'SCHEMA (схема) — структура базы данных: таблицы, колонки, типы данных, связи.\n\nКлючевые фразы:\n- database schema\n- to design a schema\n- schema changes\n- entity-relationship diagram (ERD)\n- foreign key (внешний ключ)\n- primary key (первичный ключ)\n- constraints (ограничения)\n\nNORMALIZATION (нормализация) — организация данных для минимизации избыточности.\n\nНормальные формы:\n1NF — нет повторяющихся групп, атомарные значения\n2NF — нет частичных зависимостей\n3NF — нет транзитивных зависимостей\nBCNF — каждый детерминант является ключом\n\nExamples:\n1. "The schema was poorly designed — we had to normalize it."\n2. "Third normal form (3NF) eliminates redundant data."\n3. "We violated 1NF by storing multiple phone numbers in one column."\n4. "The ERD shows the relationships between entities in our data model."'
        },
        {
          type: 'text',
          value: 'DENORMALIZATION (денормализация) — намеренное добавление избыточности для производительности.\n\n"We denormalized by adding a \'total_orders\' counter to the users table."\n"Denormalization improves read performance at the cost of write complexity."\n"The analytics database uses a denormalized star schema for fast aggregations."\n"We cached the computed total in the row to avoid expensive JOIN queries."\n\nNOSQL DATABASES — другой подход к схемам:\n\nDocument databases (MongoDB):\n"NoSQL documents store related data together, avoiding JOINs."\n"The document schema is flexible — no migration needed for new fields."\n"We embed order items in the order document instead of using a separate table."\n\nKey-Value stores (Redis):\n"Redis stores data as key-value pairs — perfect for caching and sessions."\n"We use Redis to store user session data with a TTL of 24 hours."\n\nKey phrases:\n"SQL vs NoSQL is a trade-off between consistency and flexibility."\n"Choose SQL for relational, transactional data; NoSQL for unstructured, high-volume data."'
        }
      ]
    },
    {
      id: 6,
      title: 'Sharding, Replication, Backup',
      type: 'theory',
      content: [
        {
          type: 'text',
          value: 'REPLICATION (репликация) — копирование данных на несколько серверов.\n\nТипы репликации:\n- Primary-replica (master-slave) — запись в primary, чтение из replicas\n- Multi-primary — запись в несколько серверов\n- Synchronous — данные синхронизируются мгновенно\n- Asynchronous — небольшая задержка в синхронизации\n\nФразы:\n- read replica (реплика для чтения)\n- primary/master database\n- replication lag (задержка репликации)\n- to failover to replica\n- read/write split\n\nExamples:\n1. "We direct all read queries to the replica to reduce load on the primary."\n2. "Replication lag of 2 seconds means replicas may return slightly stale data."\n3. "In case of primary failure, the system automatically fails over to a replica."\n4. "We have three read replicas to handle the high read traffic."\n\nSHARDING (шардинг) — горизонтальное разбиение данных по нескольким серверам.\n"User data is sharded by user ID — shard 1 stores users 1-1M, shard 2 stores 1M-2M."\n"Sharding allows the database to scale beyond the capacity of a single server."\n"Cross-shard queries are complex and slow — design the shard key carefully."\n\nBACKUP (резервное копирование):\n"We run automated daily backups retained for 30 days."\n"Always test your backup restore procedure — untested backups are not backups."\n"Point-in-time recovery allows restoring to any moment within the retention window."'
        }
      ]
    },
    {
      id: 7,
      title: 'Практика: обсуждение базы данных',
      type: 'practice',
      description: 'Используй профессиональные фразы для обсуждения производительности базы данных.',
      solution: 'Примеры правильных ответов:\n- "The query is slow — let\'s check the execution plan."\n- "We need an index on the user_id column to speed up this lookup."\n- "This migration adds a new column — we\'ll need to backfill existing rows."\n- "Wrap these operations in a transaction to ensure consistency."\n- При обсуждении проблемы: выявить медленный запрос, проверить индексы, написать миграцию для оптимизации.',
      content: [
        {
          type: 'text',
          value: 'Ключевые фразы для обсуждения БД:\n\n"The query is slow — let\'s check the execution plan."\n"We need an index on the user_id column to speed up this lookup."\n"This migration adds a new column — we\'ll need to backfill existing rows."\n"Wrap these operations in a transaction to ensure consistency."\n"If the payment fails, we roll back the entire transaction."\n"We have a replica for read traffic to reduce load on the primary."\n"The schema needs normalization — we have a lot of duplicate data."\n"Let\'s add a composite index on (created_at, status) for this query."\n\nОбсуждение проблемы производительности:\n\nA: "The orders page is loading slowly."\nB: "Let me check the slow query log."\nA: "Did you find anything?"\nB: "Yes — there\'s a query that runs in 8 seconds. It\'s doing a full table scan."\nA: "What\'s the query doing?"\nB: "It joins the orders and users tables and filters by created_at. There\'s no index on that column."\nA: "Can we add one?"\nB: "Yes. Adding an index on created_at should bring it down to under 50ms. I\'ll write a migration."'
        }
      ]
    }
  ]
}
