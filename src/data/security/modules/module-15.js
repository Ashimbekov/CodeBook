export default {
  id: 15,
  title: 'Практикум: Реальные сценарии',
  description: 'Практические задачи по реализации полных auth-систем для реальных приложений: e-commerce, банкинг, admin-панель, API gateway, multi-tenant',
  lessons: [
    {
      id: 1,
      title: 'E-Commerce: Аутентификация покупателя',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй систему аутентификации интернет-магазина: регистрация, логин, гостевой checkout, привязка гостевого заказа к аккаунту.',
      requirements: [
        'Создай класс Main с методом main',
        'Регистрация покупателя (email, пароль, имя)',
        'Логин -> JWT-токен',
        'Гостевой checkout: создание заказа без регистрации (по email)',
        'Привязка гостевого заказа к аккаунту при регистрации',
        'Покажи весь флоу: гость -> заказ -> регистрация -> заказ привязан'
      ],
      expectedOutput: 'Гостевой заказ #1: item=Laptop, email=guest@mail.com\nРегистрация guest@mail.com: УСПЕХ\nЗаказ #1 привязан к аккаунту guest@mail.com\nЛогин: AT=tok_xxx\nМои заказы: [#1 Laptop]',
      hint: 'Храни заказы с guestEmail. При регистрации найди все заказы с этим email и привяжи к userId.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<String, Map<String, Object>> users = new LinkedHashMap<>();\n    static Map<String, String> emailToUser = new HashMap<>();\n    static List<Map<String, Object>> orders = new ArrayList<>();\n    static int orderSeq = 0;\n    static int tokenSeq = 0;\n    \n    public static void main(String[] args) {\n        System.out.println("=== E-Commerce Auth ===\\n");\n        \n        // 1. Гостевой checkout\n        System.out.println("--- 1. Гостевой checkout ---");\n        createGuestOrder("guest@mail.com", "Laptop", 450000);\n        createGuestOrder("guest@mail.com", "Мышь", 5000);\n        createGuestOrder("other@mail.com", "Клавиатура", 15000);\n        \n        // 2. Регистрация (привязка заказов)\n        System.out.println("\\n--- 2. Регистрация ---");\n        register("guest@mail.com", "GuestPass1!", "Нурлан");\n        register("buyer@mail.com", "BuyerPass2!", "Айгуль");\n        \n        // 3. Логин\n        System.out.println("\\n--- 3. Логин ---");\n        String token = login("guest@mail.com", "GuestPass1!");\n        login("guest@mail.com", "wrongPass");\n        \n        // 4. Мои заказы\n        System.out.println("\\n--- 4. Мои заказы ---");\n        viewOrders(token);\n        \n        // 5. Заказ от авторизованного пользователя\n        System.out.println("\\n--- 5. Заказ от авторизованного ---");\n        createAuthOrder(token, "Наушники", 25000);\n        viewOrders(token);\n        \n        // 6. Логин и заказы buyer\n        System.out.println("\\n--- 6. Другой пользователь ---");\n        String token2 = login("buyer@mail.com", "BuyerPass2!");\n        viewOrders(token2);\n        createAuthOrder(token2, "Монитор", 120000);\n        viewOrders(token2);\n    }\n    \n    static void register(String email, String password, String name) {\n        if (emailToUser.containsKey(email)) {\n            System.out.println("Регистрация " + email + ": ОШИБКА (email занят)");\n            return;\n        }\n        String userId = "user_" + (users.size() + 1);\n        Map<String, Object> user = new HashMap<>();\n        user.put("email", email);\n        user.put("name", name);\n        user.put("passwordHash", hash(password));\n        users.put(userId, user);\n        emailToUser.put(email, userId);\n        \n        // Привязка гостевых заказов\n        int linked = 0;\n        for (Map<String, Object> order : orders) {\n            if (email.equals(order.get("guestEmail")) && order.get("userId") == null) {\n                order.put("userId", userId);\n                order.remove("guestEmail");\n                linked++;\n            }\n        }\n        System.out.println("Регистрация " + email + " (" + name + "): УСПЕХ" +\n            (linked > 0 ? " (привязано заказов: " + linked + ")" : ""));\n    }\n    \n    static String login(String email, String password) {\n        String userId = emailToUser.get(email);\n        if (userId == null) { System.out.println("Логин " + email + ": ОТКАЗ (не найден)"); return null; }\n        Map<String, Object> user = users.get(userId);\n        if (!user.get("passwordHash").equals(hash(password))) {\n            System.out.println("Логин " + email + ": ОТКАЗ (неверный пароль)");\n            return null;\n        }\n        String token = "tok_" + Integer.toHexString(++tokenSeq * 7919);\n        System.out.println("Логин " + email + ": УСПЕХ (token=" + token + ")");\n        return userId + "|" + token;\n    }\n    \n    static void createGuestOrder(String email, String item, int price) {\n        Map<String, Object> order = new LinkedHashMap<>();\n        order.put("id", ++orderSeq);\n        order.put("guestEmail", email);\n        order.put("userId", null);\n        order.put("item", item);\n        order.put("price", price);\n        order.put("status", "CREATED");\n        orders.add(order);\n        System.out.println("Гостевой заказ #" + orderSeq + ": " + item + " (" + price + " тг) email=" + email);\n    }\n    \n    static void createAuthOrder(String authToken, String item, int price) {\n        if (authToken == null) { System.out.println("Заказ: ОШИБКА (не авторизован)"); return; }\n        String userId = authToken.split("\\\\|")[0];\n        Map<String, Object> order = new LinkedHashMap<>();\n        order.put("id", ++orderSeq);\n        order.put("userId", userId);\n        order.put("item", item);\n        order.put("price", price);\n        order.put("status", "CREATED");\n        orders.add(order);\n        System.out.println("Заказ #" + orderSeq + ": " + item + " (" + price + " тг) user=" + userId);\n    }\n    \n    static void viewOrders(String authToken) {\n        if (authToken == null) { System.out.println("Мои заказы: не авторизован"); return; }\n        String userId = authToken.split("\\\\|")[0];\n        Map<String, Object> user = users.get(userId);\n        System.out.println("Заказы " + user.get("name") + ":");\n        int count = 0;\n        for (Map<String, Object> order : orders) {\n            if (userId.equals(order.get("userId"))) {\n                count++;\n                System.out.println("  #" + order.get("id") + " | " + order.get("item") + " | " +\n                    order.get("price") + " тг | " + order.get("status"));\n            }\n        }\n        if (count == 0) System.out.println("  (нет заказов)");\n    }\n    \n    static String hash(String s) {\n        long h = 0; for (char c : s.toCharArray()) { h = h * 31 + c; h ^= (h >>> 16); }\n        return String.format("%016x", Math.abs(h));\n    }\n}',
      explanation: 'E-Commerce auth: гостевой checkout позволяет покупать без регистрации (снижает барьер). При регистрации система находит все гостевые заказы по email и привязывает к аккаунту. Авторизованные пользователи видят историю заказов. Это модель Wildberries, Kaspi Shop, Amazon -- guest checkout + привязка при регистрации.'
    },
    {
      id: 2,
      title: 'Банкинг: Двухфакторная аутентификация',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуй банковскую систему аутентификации: логин с 2FA, подтверждение транзакций по OTP, лимиты по суммам, аудит.',
      requirements: [
        'Создай класс Main с методом main',
        'Логин: пароль + OTP (обязательно для всех)',
        'Транзакции < 50000: без дополнительного подтверждения',
        'Транзакции >= 50000: требуют OTP подтверждение',
        'Дневной лимит переводов (500000)',
        'Полный аудит всех операций'
      ],
      expectedOutput: 'Логин: пароль OK -> OTP 123456 -> ВХОД\nПеревод 10000 -> OK (без доп. подтверждения)\nПеревод 100000 -> OTP -> OK\nПеревод 450000 -> ОТКАЗ (дневной лимит)',
      hint: 'Добавь поле dailyTransferred к каждому пользователю. Проверяй dailyTransferred + amount <= DAILY_LIMIT.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<String, Map<String, Object>> accounts = new LinkedHashMap<>();\n    static List<String> auditLog = new ArrayList<>();\n    static Random rng = new Random(42);\n    static final long CONFIRM_THRESHOLD = 50000;\n    static final long DAILY_LIMIT = 500000;\n    \n    public static void main(String[] args) {\n        // Создаём аккаунты\n        createAccount("KZ001", "Нурлан", "BankP@ss1", 1000000);\n        createAccount("KZ002", "Айгуль", "SecureP@ss2", 500000);\n        \n        System.out.println("=== Банкинг: 2FA + OTP ===\\n");\n        \n        // 1. Логин с 2FA\n        System.out.println("--- 1. Логин ---");\n        String session1 = bankLogin("KZ001", "BankP@ss1");\n        bankLogin("KZ001", "wrongPassword");\n        \n        // 2. Малый перевод (без OTP)\n        System.out.println("\\n--- 2. Малый перевод (< " + CONFIRM_THRESHOLD + ") ---");\n        transfer(session1, "KZ002", 10000);\n        transfer(session1, "KZ002", 30000);\n        \n        // 3. Крупный перевод (с OTP)\n        System.out.println("\\n--- 3. Крупный перевод (>= " + CONFIRM_THRESHOLD + ") ---");\n        transfer(session1, "KZ002", 100000);\n        transfer(session1, "KZ002", 200000);\n        \n        // 4. Дневной лимит\n        System.out.println("\\n--- 4. Дневной лимит (" + DAILY_LIMIT + ") ---");\n        transfer(session1, "KZ002", 300000); // превысит лимит\n        \n        // 5. Баланс\n        System.out.println("\\n--- 5. Баланс ---");\n        showBalance(session1);\n        String session2 = bankLogin("KZ002", "SecureP@ss2");\n        showBalance(session2);\n        \n        // 6. Аудит\n        System.out.println("\\n--- 6. Аудит ---");\n        System.out.println("-".repeat(80));\n        for (String entry : auditLog) {\n            System.out.println(entry);\n        }\n    }\n    \n    static void createAccount(String id, String name, String password, long balance) {\n        Map<String, Object> acc = new HashMap<>();\n        acc.put("name", name);\n        acc.put("passwordHash", hash(password));\n        acc.put("balance", balance);\n        acc.put("dailyTransferred", 0L);\n        accounts.put(id, acc);\n    }\n    \n    static String bankLogin(String accountId, String password) {\n        Map<String, Object> acc = accounts.get(accountId);\n        if (acc == null) {\n            audit("LOGIN_FAIL", accountId, "Аккаунт не найден");\n            System.out.println("Логин " + accountId + ": ОТКАЗ (не найден)");\n            return null;\n        }\n        \n        if (!acc.get("passwordHash").equals(hash(password))) {\n            audit("LOGIN_FAIL", accountId, "Неверный пароль");\n            System.out.println("Логин " + accountId + ": ОТКАЗ (неверный пароль)");\n            return null;\n        }\n        \n        // 2FA: OTP обязателен\n        String otp = String.format("%06d", rng.nextInt(1000000));\n        System.out.println("Логин " + accountId + ": пароль OK -> OTP: " + otp + " (отправлен на телефон)");\n        System.out.println("  OTP подтверждён -> ВХОД ВЫПОЛНЕН");\n        \n        audit("LOGIN_OK", accountId, "Успешный вход с 2FA");\n        return accountId;\n    }\n    \n    static void transfer(String session, String toAccount, long amount) {\n        if (session == null) {\n            System.out.println("Перевод: не авторизован");\n            return;\n        }\n        \n        Map<String, Object> from = accounts.get(session);\n        Map<String, Object> to = accounts.get(toAccount);\n        String fromName = (String) from.get("name");\n        \n        if (to == null) {\n            System.out.println("Перевод -> " + toAccount + ": ОШИБКА (получатель не найден)");\n            return;\n        }\n        \n        // Проверка баланса\n        long balance = (long) from.get("balance");\n        if (amount > balance) {\n            audit("TRANSFER_FAIL", session, "Недостаточно средств: " + amount + " > " + balance);\n            System.out.println("Перевод " + amount + " тг: ОТКАЗ (недостаточно средств)");\n            return;\n        }\n        \n        // Дневной лимит\n        long daily = (long) from.get("dailyTransferred");\n        if (daily + amount > DAILY_LIMIT) {\n            audit("TRANSFER_FAIL", session, "Дневной лимит: " + (daily + amount) + " > " + DAILY_LIMIT);\n            System.out.println("Перевод " + amount + " тг: ОТКАЗ (дневной лимит: уже " + daily + "/" + DAILY_LIMIT + ")");\n            return;\n        }\n        \n        // OTP для крупных переводов\n        if (amount >= CONFIRM_THRESHOLD) {\n            String otp = String.format("%06d", rng.nextInt(1000000));\n            System.out.println("Перевод " + amount + " тг -> " + toAccount + ": требуется OTP");\n            System.out.println("  OTP: " + otp + " -> подтверждён");\n        }\n        \n        // Выполняем перевод\n        from.put("balance", balance - amount);\n        to.put("balance", (long) to.get("balance") + amount);\n        from.put("dailyTransferred", daily + amount);\n        \n        String toName = (String) to.get("name");\n        audit("TRANSFER_OK", session, fromName + " -> " + toName + ": " + amount + " тг");\n        System.out.println("Перевод " + amount + " тг (" + fromName + " -> " + toName + "): УСПЕХ" +\n            " (дневной: " + (daily + amount) + "/" + DAILY_LIMIT + ")");\n    }\n    \n    static void showBalance(String session) {\n        if (session == null) return;\n        Map<String, Object> acc = accounts.get(session);\n        System.out.println("Баланс " + acc.get("name") + " (" + session + "): " + acc.get("balance") + " тг");\n    }\n    \n    static void audit(String event, String accountId, String details) {\n        String entry = String.format("[%s] %-15s account=%-6s %s",\n            new Date().toString().substring(11, 19), event, accountId, details);\n        auditLog.add(entry);\n    }\n    \n    static String hash(String s) {\n        long h = 0; for (char c : s.toCharArray()) { h = h * 31 + c; h ^= (h >>> 16); }\n        return String.format("%016x", Math.abs(h));\n    }\n}',
      explanation: 'Банковская 2FA: логин ВСЕГДА требует OTP (в отличие от обычных приложений). Транзакции имеют два уровня: малые проходят без подтверждения, крупные (>= 50000) требуют OTP. Дневной лимит ограничивает общую сумму. Полный аудит всех операций. Это модель Kaspi, Halyk Bank -- OTP при логине и при крупных переводах.'
    },
    {
      id: 3,
      title: 'Admin Panel: Разграничение доступа',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй систему доступа для admin-панели: Super Admin, Admin, Moderator, Support -- каждый видит только свои разделы.',
      requirements: [
        'Создай класс Main с методом main',
        'Разделы: Dashboard, Users, Orders, Content, Settings, Logs, Billing',
        'Super Admin: все разделы',
        'Admin: Users, Orders, Content, Dashboard',
        'Moderator: Content, Dashboard',
        'Support: Orders, Dashboard',
        'Покажи: навигация для каждой роли, попытка доступа к запрещённому разделу'
      ],
      expectedOutput: 'super_admin: [Dashboard, Users, Orders, Content, Settings, Logs, Billing]\nadmin: [Dashboard, Users, Orders, Content]\nmoderator: [Dashboard, Content]\nПопытка moderator -> Settings: ОТКАЗ',
      hint: 'Map<role, List<section>> для определения видимых разделов каждой роли.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<String, List<String>> roleSections = new LinkedHashMap<>();\n    static Map<String, Map<String, Set<String>>> sectionActions = new LinkedHashMap<>();\n    static Map<String, String> userRoles = new LinkedHashMap<>();\n    \n    public static void main(String[] args) {\n        // Разделы по ролям\n        roleSections.put("SUPER_ADMIN", Arrays.asList("Dashboard", "Users", "Orders", "Content", "Settings", "Logs", "Billing"));\n        roleSections.put("ADMIN", Arrays.asList("Dashboard", "Users", "Orders", "Content"));\n        roleSections.put("MODERATOR", Arrays.asList("Dashboard", "Content"));\n        roleSections.put("SUPPORT", Arrays.asList("Dashboard", "Orders"));\n        \n        // Действия в разделах\n        addSectionActions("Users", "SUPER_ADMIN", "view", "create", "edit", "delete", "ban");\n        addSectionActions("Users", "ADMIN", "view", "create", "edit");\n        addSectionActions("Orders", "SUPER_ADMIN", "view", "edit", "refund", "cancel");\n        addSectionActions("Orders", "ADMIN", "view", "edit", "refund");\n        addSectionActions("Orders", "SUPPORT", "view", "edit");\n        addSectionActions("Content", "SUPER_ADMIN", "view", "create", "edit", "delete", "publish");\n        addSectionActions("Content", "ADMIN", "view", "create", "edit", "delete", "publish");\n        addSectionActions("Content", "MODERATOR", "view", "edit", "publish");\n        addSectionActions("Settings", "SUPER_ADMIN", "view", "edit");\n        addSectionActions("Logs", "SUPER_ADMIN", "view", "export");\n        addSectionActions("Billing", "SUPER_ADMIN", "view", "manage");\n        \n        // Пользователи\n        userRoles.put("root", "SUPER_ADMIN");\n        userRoles.put("alice", "ADMIN");\n        userRoles.put("bob", "MODERATOR");\n        userRoles.put("carol", "SUPPORT");\n        \n        System.out.println("=== Admin Panel: Разграничение доступа ===\\n");\n        \n        // 1. Навигация для каждой роли\n        System.out.println("--- Меню навигации ---");\n        for (Map.Entry<String, String> u : userRoles.entrySet()) {\n            String role = u.getValue();\n            List<String> sections = roleSections.getOrDefault(role, Collections.emptyList());\n            System.out.println(u.getKey() + " (" + role + "): " + sections);\n        }\n        \n        // 2. Доступ к разделам\n        System.out.println("\\n--- Доступ к разделам ---");\n        accessSection("root", "Settings", "edit");\n        accessSection("alice", "Users", "create");\n        accessSection("alice", "Users", "delete"); // нет\n        accessSection("alice", "Settings", "view"); // нет доступа к разделу\n        accessSection("bob", "Content", "publish");\n        accessSection("bob", "Content", "delete"); // нет действия\n        accessSection("bob", "Users", "view"); // нет доступа к разделу\n        accessSection("carol", "Orders", "view");\n        accessSection("carol", "Orders", "refund"); // нет\n        accessSection("carol", "Users", "view"); // нет доступа к разделу\n        \n        // 3. API endpoint проверка\n        System.out.println("\\n--- API Endpoints ---");\n        apiCall("root", "DELETE", "/admin/users/123");\n        apiCall("alice", "DELETE", "/admin/users/123"); // admin не может delete\n        apiCall("bob", "PUT", "/admin/content/456");\n        apiCall("bob", "DELETE", "/admin/content/456"); // moderator не может delete\n        apiCall("carol", "GET", "/admin/orders/789");\n        apiCall("carol", "POST", "/admin/orders/789/refund"); // support не может refund\n    }\n    \n    static void addSectionActions(String section, String role, String... actions) {\n        sectionActions.computeIfAbsent(section, k -> new LinkedHashMap<>())\n            .put(role, new LinkedHashSet<>(Arrays.asList(actions)));\n    }\n    \n    static void accessSection(String user, String section, String action) {\n        String role = userRoles.get(user);\n        if (role == null) { System.out.println(user + ": пользователь не найден"); return; }\n        \n        List<String> sections = roleSections.getOrDefault(role, Collections.emptyList());\n        if (!sections.contains(section)) {\n            System.out.println(user + " (" + role + ") -> " + section + "/" + action + ": ОТКАЗ (нет доступа к разделу)");\n            return;\n        }\n        \n        Map<String, Set<String>> actionsMap = sectionActions.getOrDefault(section, Collections.emptyMap());\n        Set<String> allowedActions = actionsMap.getOrDefault(role, Collections.emptySet());\n        boolean allowed = allowedActions.contains(action);\n        System.out.println(user + " (" + role + ") -> " + section + "/" + action + ": " +\n            (allowed ? "OK" : "ОТКАЗ (нет действия " + action + ")"));\n    }\n    \n    static void apiCall(String user, String method, String path) {\n        String role = userRoles.get(user);\n        String section;\n        String action;\n        \n        if (path.contains("/users")) { section = "Users"; }\n        else if (path.contains("/orders")) { section = "Orders"; }\n        else if (path.contains("/content")) { section = "Content"; }\n        else { section = "Unknown"; }\n        \n        switch (method) {\n            case "GET": action = "view"; break;\n            case "POST": action = path.contains("refund") ? "refund" : "create"; break;\n            case "PUT": action = "edit"; break;\n            case "DELETE": action = "delete"; break;\n            default: action = "unknown";\n        }\n        \n        List<String> sections = roleSections.getOrDefault(role, Collections.emptyList());\n        if (!sections.contains(section)) {\n            System.out.println("API " + method + " " + path + " (" + user + "): 403 (нет доступа к " + section + ")");\n            return;\n        }\n        \n        Map<String, Set<String>> actionsMap = sectionActions.getOrDefault(section, Collections.emptyMap());\n        Set<String> allowed = actionsMap.getOrDefault(role, Collections.emptySet());\n        if (allowed.contains(action)) {\n            System.out.println("API " + method + " " + path + " (" + user + "): 200 OK");\n        } else {\n            System.out.println("API " + method + " " + path + " (" + user + "): 403 (нет права " + action + ")");\n        }\n    }\n}',
      explanation: 'Admin Panel разграничение: каждая роль видит только свои разделы, а внутри раздела имеет свои действия. Super Admin видит всё, Support видит только Orders (просмотр/редактирование). Проверка идёт на двух уровнях: 1) доступ к разделу, 2) доступ к действию. Это стандартная архитектура CMS (WordPress, Strapi, Django Admin).'
    },
    {
      id: 4,
      title: 'API Gateway: Маршрутизация и авторизация',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуй API Gateway: единая точка входа для микросервисов с JWT-валидацией, rate limiting, маршрутизацией и CORS.',
      requirements: [
        'Создай класс Main с методом main',
        'Маршруты: /api/users -> user-service, /api/orders -> order-service, /api/products -> product-service',
        'JWT-валидация для защищённых маршрутов',
        'Публичные маршруты (без JWT): GET /api/products',
        'Rate limiting per client',
        'Logging каждого запроса'
      ],
      expectedOutput: 'GET /api/products -> product-service: 200 (public)\nGET /api/users -> user-service: 401 (нет JWT)\nGET /api/users [JWT]: -> user-service: 200\nPOST /api/orders [JWT]: -> order-service: 200',
      hint: 'Определи маршруты как Map<pattern, ServiceConfig>. ServiceConfig = {service, public, requiredRole}.',
      solution: 'import java.util.*;\n\npublic class Main {\n    // Маршруты\n    static List<Map<String, Object>> routes = new ArrayList<>();\n    // Токены\n    static Map<String, Map<String, String>> tokens = new HashMap<>();\n    // Rate limiting\n    static Map<String, Integer> clientRequests = new HashMap<>();\n    static final int RATE_LIMIT = 10;\n    // Логи\n    static List<String> gatewayLogs = new ArrayList<>();\n    \n    public static void main(String[] args) {\n        // Настройка маршрутов\n        addRoute("GET", "/api/products", "product-service", true, null);\n        addRoute("GET", "/api/products/*", "product-service", true, null);\n        addRoute("POST", "/api/products", "product-service", false, "ADMIN");\n        addRoute("GET", "/api/users", "user-service", false, "ADMIN");\n        addRoute("GET", "/api/users/me", "user-service", false, "USER");\n        addRoute("POST", "/api/orders", "order-service", false, "USER");\n        addRoute("GET", "/api/orders", "order-service", false, "USER");\n        addRoute("GET", "/api/admin/stats", "analytics-service", false, "ADMIN");\n        \n        // Токены\n        tokens.put("tok-admin", Map.of("userId", "admin", "role", "ADMIN"));\n        tokens.put("tok-user1", Map.of("userId", "user1", "role", "USER"));\n        tokens.put("tok-user2", Map.of("userId", "user2", "role", "USER"));\n        \n        System.out.println("=== API Gateway ===\\n");\n        \n        // 1. Публичные маршруты\n        System.out.println("--- 1. Публичные маршруты ---");\n        gateway(null, "GET", "/api/products", "client-1");\n        gateway(null, "GET", "/api/products/123", "client-1");\n        \n        // 2. Защищённые маршруты без JWT\n        System.out.println("\\n--- 2. Без JWT ---");\n        gateway(null, "GET", "/api/users", "client-1");\n        gateway(null, "POST", "/api/orders", "client-1");\n        \n        // 3. Защищённые маршруты с JWT\n        System.out.println("\\n--- 3. С JWT ---");\n        gateway("tok-admin", "GET", "/api/users", "client-1");\n        gateway("tok-user1", "GET", "/api/users/me", "client-1");\n        gateway("tok-user1", "POST", "/api/orders", "client-1");\n        gateway("tok-user1", "GET", "/api/admin/stats", "client-1"); // USER -> нет доступа\n        gateway("tok-admin", "GET", "/api/admin/stats", "client-1");\n        \n        // 4. Невалидный JWT\n        System.out.println("\\n--- 4. Невалидный JWT ---");\n        gateway("invalid-token", "GET", "/api/users", "client-1");\n        \n        // 5. Rate Limiting\n        System.out.println("\\n--- 5. Rate Limiting ---");\n        for (int i = 0; i < 5; i++) {\n            gateway(null, "GET", "/api/products", "client-2");\n        }\n        \n        // 6. Неизвестный маршрут\n        System.out.println("\\n--- 6. Неизвестный маршрут ---");\n        gateway("tok-admin", "GET", "/api/unknown", "client-1");\n        \n        // 7. Gateway логи\n        System.out.println("\\n--- 7. Gateway Logs ---");\n        System.out.println("-".repeat(90));\n        for (String log : gatewayLogs) {\n            System.out.println(log);\n        }\n    }\n    \n    static void addRoute(String method, String path, String service, boolean isPublic, String requiredRole) {\n        Map<String, Object> route = new LinkedHashMap<>();\n        route.put("method", method);\n        route.put("path", path);\n        route.put("service", service);\n        route.put("public", isPublic);\n        route.put("requiredRole", requiredRole);\n        routes.add(route);\n    }\n    \n    static void gateway(String token, String method, String path, String clientId) {\n        long start = System.nanoTime();\n        String result;\n        \n        // 1. Rate Limiting\n        int requests = clientRequests.getOrDefault(clientId, 0);\n        if (requests >= RATE_LIMIT) {\n            result = "429 Too Many Requests";\n            log(clientId, method, path, result, null, 0);\n            System.out.println(method + " " + path + ": " + result);\n            return;\n        }\n        clientRequests.put(clientId, requests + 1);\n        \n        // 2. Маршрутизация\n        Map<String, Object> route = findRoute(method, path);\n        if (route == null) {\n            result = "404 Not Found";\n            log(clientId, method, path, result, null, elapsed(start));\n            System.out.println(method + " " + path + ": " + result);\n            return;\n        }\n        \n        String service = (String) route.get("service");\n        boolean isPublic = (boolean) route.get("public");\n        String requiredRole = (String) route.get("requiredRole");\n        \n        // 3. Аутентификация\n        String userId = null;\n        String userRole = null;\n        if (token != null) {\n            Map<String, String> tokenInfo = tokens.get(token);\n            if (tokenInfo == null) {\n                result = "401 Invalid Token";\n                log(clientId, method, path, result, null, elapsed(start));\n                System.out.println(method + " " + path + ": " + result);\n                return;\n            }\n            userId = tokenInfo.get("userId");\n            userRole = tokenInfo.get("role");\n        } else if (!isPublic) {\n            result = "401 Unauthorized (JWT required)";\n            log(clientId, method, path, result, null, elapsed(start));\n            System.out.println(method + " " + path + ": " + result);\n            return;\n        }\n        \n        // 4. Авторизация\n        if (requiredRole != null && !hasRole(userRole, requiredRole)) {\n            result = "403 Forbidden (need " + requiredRole + ", have " + userRole + ")";\n            log(clientId, method, path, result, userId, elapsed(start));\n            System.out.println(method + " " + path + ": " + result);\n            return;\n        }\n        \n        // 5. Проксирование к сервису\n        long ms = elapsed(start);\n        result = "200 OK -> " + service;\n        log(clientId, method, path, result, userId, ms);\n        System.out.println(method + " " + path + " -> " + service + ": 200 OK" +\n            (userId != null ? " (user=" + userId + ")" : " (public)") + " [" + ms + "ms]");\n    }\n    \n    static Map<String, Object> findRoute(String method, String path) {\n        for (Map<String, Object> route : routes) {\n            String rMethod = (String) route.get("method");\n            String rPath = (String) route.get("path");\n            if (!rMethod.equals(method)) continue;\n            if (rPath.endsWith("/*")) {\n                String prefix = rPath.substring(0, rPath.length() - 2);\n                if (path.startsWith(prefix)) return route;\n            } else if (rPath.equals(path)) {\n                return route;\n            }\n        }\n        return null;\n    }\n    \n    static boolean hasRole(String userRole, String required) {\n        if (userRole == null) return false;\n        if ("ADMIN".equals(userRole)) return true;\n        return userRole.equals(required);\n    }\n    \n    static long elapsed(long startNano) {\n        return (System.nanoTime() - startNano) / 1_000_000;\n    }\n    \n    static void log(String client, String method, String path, String result, String userId, long ms) {\n        gatewayLogs.add(String.format("%-10s %-4s %-25s %-8s %s [%dms]",\n            client, method, path, userId != null ? userId : "-", result, ms));\n    }\n}',
      explanation: 'API Gateway -- единая точка входа для всех микросервисов. Выполняет: 1) Rate Limiting (per client), 2) Маршрутизацию (path -> service), 3) JWT-валидацию, 4) Авторизацию (role check), 5) Проксирование к сервису. Публичные маршруты не требуют JWT. Это архитектура Kong, AWS API Gateway, Spring Cloud Gateway -- стандарт в микросервисах.'
    },
    {
      id: 5,
      title: 'Multi-Tenant SaaS: Изоляция данных',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуй multi-tenant SaaS с полной изоляцией: каждый tenant имеет свои данные, пользователей и настройки.',
      requirements: [
        'Создай класс Main с методом main',
        'Tenant = организация (workspace)',
        'Приглашение пользователя в tenant',
        'Роли внутри tenant: OWNER, ADMIN, MEMBER',
        'Данные изолированы: запрос всегда фильтруется по tenantId',
        'Покажи: создание tenant, приглашение, CRUD с изоляцией'
      ],
      expectedOutput: 'Создан tenant "Startup A"\nuser1 приглашён в "Startup A" (ADMIN)\nuser1 создал задачу в "Startup A": OK\nuser2 (tenant B) -> задачи "Startup A": ОТКАЗ',
      hint: 'Каждый запрос должен передавать tenantId. Middleware проверяет user.tenantId === requestedTenantId.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<String, Map<String, Object>> tenants = new LinkedHashMap<>();\n    static Map<String, Map<String, Object>> users = new LinkedHashMap<>();\n    static Map<String, List<Map<String, Object>>> tenantTasks = new LinkedHashMap<>();\n    static int taskSeq = 0;\n    \n    public static void main(String[] args) {\n        System.out.println("=== Multi-Tenant SaaS ===\\n");\n        \n        // 1. Создание tenant\n        System.out.println("--- 1. Создание Tenant ---");\n        String t1 = createTenant("Startup A", "alice");\n        String t2 = createTenant("Corp B", "diana");\n        \n        // 2. Приглашение пользователей\n        System.out.println("\\n--- 2. Приглашения ---");\n        inviteUser(t1, "alice", "bob", "ADMIN");\n        inviteUser(t1, "alice", "charlie", "MEMBER");\n        inviteUser(t2, "diana", "eve", "MEMBER");\n        inviteUser(t1, "bob", "diana", "MEMBER"); // bob (ADMIN) приглашает\n        inviteUser(t1, "charlie", "frank", "ADMIN"); // charlie (MEMBER) -- нет прав\n        \n        // 3. CRUD задач\n        System.out.println("\\n--- 3. Создание задач ---");\n        createTask("alice", t1, "Настроить CI/CD");\n        createTask("bob", t1, "Написать API");\n        createTask("charlie", t1, "Тестирование");\n        createTask("diana", t2, "Бизнес-план");\n        createTask("eve", t2, "Маркетинг");\n        \n        // 4. Просмотр задач (изоляция)\n        System.out.println("\\n--- 4. Просмотр задач (изоляция) ---");\n        listTasks("alice", t1);\n        listTasks("diana", t2);\n        listTasks("alice", t2); // чужой tenant\n        listTasks("eve", t1); // чужой tenant\n        \n        // 5. Редактирование задач\n        System.out.println("\\n--- 5. Редактирование ---");\n        updateTask("alice", t1, 1, "Настроить CI/CD (DONE)");\n        updateTask("eve", t1, 1, "Hacked!"); // чужой tenant\n        deleteTask("charlie", t1, 2); // MEMBER не может удалять\n        deleteTask("bob", t1, 2); // ADMIN может\n        \n        // 6. Статистика\n        System.out.println("\\n--- 6. Статистика tenant ---");\n        tenantStats(t1);\n        tenantStats(t2);\n    }\n    \n    static String createTenant(String name, String ownerUsername) {\n        String tenantId = "ten_" + (tenants.size() + 1);\n        Map<String, Object> tenant = new HashMap<>();\n        tenant.put("name", name);\n        tenant.put("ownerId", ownerUsername);\n        tenant.put("members", new LinkedHashMap<String, String>()); // userId -> role\n        tenants.put(tenantId, tenant);\n        tenantTasks.put(tenantId, new ArrayList<>());\n        \n        // Владелец автоматически добавлен\n        @SuppressWarnings("unchecked")\n        Map<String, String> members = (Map<String, String>) tenant.get("members");\n        members.put(ownerUsername, "OWNER");\n        \n        Map<String, Object> user = users.computeIfAbsent(ownerUsername, k -> new HashMap<>());\n        @SuppressWarnings("unchecked")\n        Set<String> userTenants = (Set<String>) user.computeIfAbsent("tenants", k -> new HashSet<String>());\n        userTenants.add(tenantId);\n        \n        System.out.println("Создан tenant \\"" + name + "\\" (id=" + tenantId + ", owner=" + ownerUsername + ")");\n        return tenantId;\n    }\n    \n    @SuppressWarnings("unchecked")\n    static void inviteUser(String tenantId, String inviterId, String inviteeId, String role) {\n        Map<String, Object> tenant = tenants.get(tenantId);\n        Map<String, String> members = (Map<String, String>) tenant.get("members");\n        \n        // Проверка: может ли inviter приглашать?\n        String inviterRole = members.get(inviterId);\n        if (inviterRole == null) {\n            System.out.println("Приглашение " + inviteeId + ": ОТКАЗ (" + inviterId + " не в tenant)");\n            return;\n        }\n        if (!"OWNER".equals(inviterRole) && !"ADMIN".equals(inviterRole)) {\n            System.out.println("Приглашение " + inviteeId + ": ОТКАЗ (" + inviterId + " = " + inviterRole + ", нет прав)");\n            return;\n        }\n        \n        members.put(inviteeId, role);\n        Map<String, Object> user = users.computeIfAbsent(inviteeId, k -> new HashMap<>());\n        Set<String> userTenants = (Set<String>) user.computeIfAbsent("tenants", k -> new HashSet<String>());\n        userTenants.add(tenantId);\n        \n        System.out.println(inviteeId + " приглашён в \\"" + tenant.get("name") + "\\" как " + role + " (от " + inviterId + ")");\n    }\n    \n    @SuppressWarnings("unchecked")\n    static void createTask(String userId, String tenantId, String title) {\n        Map<String, Object> tenant = tenants.get(tenantId);\n        Map<String, String> members = (Map<String, String>) tenant.get("members");\n        \n        if (!members.containsKey(userId)) {\n            System.out.println("Задача: ОТКАЗ (" + userId + " не в tenant)");\n            return;\n        }\n        \n        Map<String, Object> task = new LinkedHashMap<>();\n        task.put("id", ++taskSeq);\n        task.put("title", title);\n        task.put("createdBy", userId);\n        task.put("status", "TODO");\n        tenantTasks.get(tenantId).add(task);\n        \n        System.out.println("Задача #" + taskSeq + " \\"" + title + "\\" создана (" + userId + " в \\"" + tenant.get("name") + "\\")");\n    }\n    \n    @SuppressWarnings("unchecked")\n    static void listTasks(String userId, String tenantId) {\n        Map<String, Object> tenant = tenants.get(tenantId);\n        Map<String, String> members = (Map<String, String>) tenant.get("members");\n        \n        if (!members.containsKey(userId)) {\n            System.out.println(userId + " -> задачи \\"" + tenant.get("name") + "\\": ОТКАЗ (не член tenant)");\n            return;\n        }\n        \n        List<Map<String, Object>> tasks = tenantTasks.get(tenantId);\n        System.out.println(userId + " -> задачи \\"" + tenant.get("name") + "\\" (" + tasks.size() + "):");\n        for (Map<String, Object> t : tasks) {\n            System.out.println("  #" + t.get("id") + " " + t.get("title") + " [" + t.get("status") + "] by " + t.get("createdBy"));\n        }\n    }\n    \n    @SuppressWarnings("unchecked")\n    static void updateTask(String userId, String tenantId, int taskId, String newTitle) {\n        Map<String, Object> tenant = tenants.get(tenantId);\n        Map<String, String> members = (Map<String, String>) tenant.get("members");\n        \n        if (!members.containsKey(userId)) {\n            System.out.println("Обновление #" + taskId + ": ОТКАЗ (" + userId + " не в tenant)");\n            return;\n        }\n        \n        for (Map<String, Object> t : tenantTasks.get(tenantId)) {\n            if ((int) t.get("id") == taskId) {\n                t.put("title", newTitle);\n                System.out.println("Обновление #" + taskId + ": \\"" + newTitle + "\\" (" + userId + ")");\n                return;\n            }\n        }\n        System.out.println("Обновление #" + taskId + ": не найдена");\n    }\n    \n    @SuppressWarnings("unchecked")\n    static void deleteTask(String userId, String tenantId, int taskId) {\n        Map<String, Object> tenant = tenants.get(tenantId);\n        Map<String, String> members = (Map<String, String>) tenant.get("members");\n        String role = members.get(userId);\n        \n        if (role == null) {\n            System.out.println("Удаление #" + taskId + ": ОТКАЗ (не в tenant)");\n            return;\n        }\n        if ("MEMBER".equals(role)) {\n            System.out.println("Удаление #" + taskId + ": ОТКАЗ (" + userId + " = MEMBER, нет прав)");\n            return;\n        }\n        \n        boolean removed = tenantTasks.get(tenantId).removeIf(t -> (int) t.get("id") == taskId);\n        System.out.println("Удаление #" + taskId + ": " + (removed ? "OK (" + userId + ")" : "не найдена"));\n    }\n    \n    @SuppressWarnings("unchecked")\n    static void tenantStats(String tenantId) {\n        Map<String, Object> tenant = tenants.get(tenantId);\n        Map<String, String> members = (Map<String, String>) tenant.get("members");\n        List<Map<String, Object>> tasks = tenantTasks.get(tenantId);\n        System.out.println("\\"" + tenant.get("name") + "\\": " + members.size() + " участников, " + tasks.size() + " задач");\n    }\n}',
      explanation: 'Multi-Tenant SaaS: каждый tenant (workspace) полностью изолирован. Данные хранятся с tenantId, все запросы фильтруются. Приглашение возможно только от OWNER/ADMIN. MEMBER не может удалять или приглашать. Это архитектура Slack (workspace), Notion (workspace), Jira (project) -- каждая организация видит только свои данные.'
    },
    {
      id: 6,
      title: 'OAuth 2.0: Authorization Code Flow',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуй полный OAuth 2.0 Authorization Code Flow: регистрация приложения, авторизация пользователя, обмен кода на токен.',
      requirements: [
        'Создай класс Main с методом main',
        'Регистрация OAuth-приложения (client_id, client_secret, redirect_uri)',
        'Authorization endpoint: генерация auth code',
        'Token endpoint: обмен code на access_token + refresh_token',
        'Userinfo endpoint: получение данных пользователя',
        'Покажи: полный флоу от авторизации до получения данных'
      ],
      expectedOutput: 'Приложение зарегистрировано: client_id=app_xxx\nАвторизация: code=auth_xxx\nОбмен code -> access_token=at_xxx\nGET /userinfo: {name: "Нурлан", email: "..."}',
      hint: 'Имитируй все endpoints как методы: authorize(), token(), userinfo().',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<String, Map<String, String>> apps = new LinkedHashMap<>(); // clientId -> app info\n    static Map<String, Map<String, Object>> authCodes = new LinkedHashMap<>(); // code -> info\n    static Map<String, Map<String, Object>> accessTokens = new LinkedHashMap<>();\n    static Map<String, Map<String, String>> users = new LinkedHashMap<>();\n    static int seqCounter = 0;\n    \n    public static void main(String[] args) {\n        // Пользователи\n        users.put("user1", Map.of("name", "Нурлан", "email", "nurlan@mail.com"));\n        users.put("user2", Map.of("name", "Айгуль", "email", "aigul@mail.com"));\n        \n        System.out.println("=== OAuth 2.0 Authorization Code Flow ===\\n");\n        \n        // 1. Регистрация приложения\n        System.out.println("--- 1. Регистрация приложения ---");\n        String[] app = registerApp("MyApp", "https://myapp.com/callback");\n        String clientId = app[0], clientSecret = app[1];\n        \n        String[] app2 = registerApp("EvilApp", "https://evil.com/steal");\n        \n        // 2. Авторизация (пользователь разрешает доступ)\n        System.out.println("\\n--- 2. Авторизация ---");\n        String code = authorize(clientId, "https://myapp.com/callback", "user1",\n            "profile email");\n        \n        // 3. Обмен кода на токен\n        System.out.println("\\n--- 3. Обмен кода на токен ---");\n        String[] tokens = exchangeCode(code, clientId, clientSecret, "https://myapp.com/callback");\n        \n        // 4. Получение данных пользователя\n        System.out.println("\\n--- 4. Userinfo ---");\n        if (tokens != null) userinfo(tokens[0]);\n        \n        // 5. Ошибки\n        System.out.println("\\n--- 5. Ошибки ---");\n        \n        // Неверный redirect_uri\n        System.out.println("\\nНеверный redirect_uri:");\n        authorize(clientId, "https://evil.com/steal", "user1", "profile");\n        \n        // Повторное использование code\n        System.out.println("\\nПовторный code:");\n        exchangeCode(code, clientId, clientSecret, "https://myapp.com/callback");\n        \n        // Неверный client_secret\n        System.out.println("\\nНеверный secret:");\n        String code2 = authorize(clientId, "https://myapp.com/callback", "user2", "profile");\n        exchangeCode(code2, clientId, "wrong-secret", "https://myapp.com/callback");\n        \n        // 6. Полный успешный флоу (user2)\n        System.out.println("\\n--- 6. Полный флоу (user2) ---");\n        String code3 = authorize(clientId, "https://myapp.com/callback", "user2", "profile email");\n        String[] tokens2 = exchangeCode(code3, clientId, clientSecret, "https://myapp.com/callback");\n        if (tokens2 != null) userinfo(tokens2[0]);\n    }\n    \n    static String[] registerApp(String name, String redirectUri) {\n        String clientId = "app_" + Integer.toHexString(++seqCounter * 4231);\n        String clientSecret = "secret_" + UUID.randomUUID().toString().substring(0, 16);\n        apps.put(clientId, Map.of("name", name, "redirectUri", redirectUri, "secret", clientSecret));\n        System.out.println("Зарегистрировано: \\"" + name + "\\" client_id=" + clientId);\n        System.out.println("  redirect_uri=" + redirectUri);\n        return new String[]{clientId, clientSecret};\n    }\n    \n    static String authorize(String clientId, String redirectUri, String userId, String scope) {\n        Map<String, String> app = apps.get(clientId);\n        if (app == null) {\n            System.out.println("Авторизация: ОШИБКА (приложение не найдено)");\n            return null;\n        }\n        \n        // Проверка redirect_uri\n        if (!app.get("redirectUri").equals(redirectUri)) {\n            System.out.println("Авторизация: ОШИБКА (redirect_uri не совпадает)");\n            System.out.println("  Ожидается: " + app.get("redirectUri"));\n            System.out.println("  Получено: " + redirectUri);\n            return null;\n        }\n        \n        // Генерация auth code\n        String code = "code_" + Integer.toHexString(++seqCounter * 8461);\n        Map<String, Object> codeInfo = new HashMap<>();\n        codeInfo.put("clientId", clientId);\n        codeInfo.put("userId", userId);\n        codeInfo.put("scope", scope);\n        codeInfo.put("redirectUri", redirectUri);\n        codeInfo.put("used", false);\n        codeInfo.put("expiresAt", System.currentTimeMillis() + 600_000); // 10 мин\n        authCodes.put(code, codeInfo);\n        \n        Map<String, String> user = users.get(userId);\n        System.out.println("Пользователь " + user.get("name") + " разрешает доступ приложению \\"" + app.get("name") + "\\"");\n        System.out.println("  Scopes: " + scope);\n        System.out.println("  -> code=" + code);\n        System.out.println("  Redirect: " + redirectUri + "?code=" + code);\n        return code;\n    }\n    \n    static String[] exchangeCode(String code, String clientId, String clientSecret, String redirectUri) {\n        if (code == null) {\n            System.out.println("Token: ОШИБКА (нет кода)");\n            return null;\n        }\n        \n        Map<String, Object> codeInfo = authCodes.get(code);\n        if (codeInfo == null) {\n            System.out.println("Token: ОШИБКА (невалидный код)");\n            return null;\n        }\n        \n        // Проверка: код уже использован?\n        if ((boolean) codeInfo.get("used")) {\n            System.out.println("Token: ОШИБКА (код уже использован)");\n            return null;\n        }\n        \n        // Проверка client_id\n        if (!clientId.equals(codeInfo.get("clientId"))) {\n            System.out.println("Token: ОШИБКА (client_id не совпадает)");\n            return null;\n        }\n        \n        // Проверка client_secret\n        Map<String, String> app = apps.get(clientId);\n        if (!app.get("secret").equals(clientSecret)) {\n            System.out.println("Token: ОШИБКА (неверный client_secret)");\n            return null;\n        }\n        \n        // Проверка redirect_uri\n        if (!redirectUri.equals(codeInfo.get("redirectUri"))) {\n            System.out.println("Token: ОШИБКА (redirect_uri не совпадает)");\n            return null;\n        }\n        \n        // Помечаем код как использованный\n        codeInfo.put("used", true);\n        \n        // Выдаём токены\n        String at = "at_" + Integer.toHexString(++seqCounter * 6173);\n        String rt = "rt_" + Integer.toHexString(++seqCounter * 5189);\n        \n        Map<String, Object> tokenInfo = new HashMap<>();\n        tokenInfo.put("userId", codeInfo.get("userId"));\n        tokenInfo.put("scope", codeInfo.get("scope"));\n        tokenInfo.put("clientId", clientId);\n        accessTokens.put(at, tokenInfo);\n        \n        System.out.println("Token: УСПЕХ");\n        System.out.println("  access_token: " + at);\n        System.out.println("  refresh_token: " + rt);\n        System.out.println("  scope: " + codeInfo.get("scope"));\n        return new String[]{at, rt};\n    }\n    \n    static void userinfo(String accessToken) {\n        Map<String, Object> tokenInfo = accessTokens.get(accessToken);\n        if (tokenInfo == null) {\n            System.out.println("Userinfo: 401 (невалидный токен)");\n            return;\n        }\n        \n        String userId = (String) tokenInfo.get("userId");\n        String scope = (String) tokenInfo.get("scope");\n        Map<String, String> user = users.get(userId);\n        \n        System.out.println("GET /userinfo: 200 OK");\n        if (scope.contains("profile")) System.out.println("  name: " + user.get("name"));\n        if (scope.contains("email")) System.out.println("  email: " + user.get("email"));\n    }\n}',
      explanation: 'Полный OAuth 2.0 Authorization Code Flow: 1) Регистрация приложения (client_id, secret, redirect_uri). 2) Авторизация: пользователь разрешает доступ, получает auth code. 3) Обмен кода на токен (с проверкой secret, redirect_uri, одноразовость кода). 4) Доступ к данным по access_token с учётом scopes. Это стандарт Google, GitHub, Facebook OAuth.'
    },
    {
      id: 7,
      title: 'Корпоративный SSO (Single Sign-On)',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуй корпоративный SSO: один логин для доступа ко всем внутренним сервисам компании.',
      requirements: [
        'Создай класс Main с методом main',
        'SSO-сервер: хранит пользователей, выдаёт SSO-токен',
        'Несколько сервисов: CRM, HR, Email',
        'Логин на SSO -> доступ ко всем сервисам',
        'Logout на SSO -> разлогин из всех сервисов',
        'Покажи: SSO логин, доступ к 3 сервисам, SSO logout'
      ],
      expectedOutput: 'SSO Логин admin: sso_token=xxx\nCRM: 200 OK (через SSO)\nHR: 200 OK (через SSO)\nSSO Logout -> CRM, HR, Email: разлогинены',
      hint: 'SSO-сервер хранит Map<ssoToken, userId>. Сервисы вызывают SSO-сервер для валидации токена.',
      solution: 'import java.util.*;\n\npublic class Main {\n    // SSO Server\n    static Map<String, String> ssoUsers = new LinkedHashMap<>(); // username -> passwordHash\n    static Map<String, Map<String, Object>> ssoTokens = new LinkedHashMap<>(); // token -> info\n    static Map<String, Set<String>> activeSessions = new HashMap<>(); // ssoToken -> services\n    \n    // Services\n    static final String[] SERVICES = {"CRM", "HR", "Email", "Wiki"};\n    static Map<String, Set<String>> servicePermissions = new LinkedHashMap<>();\n    static int tokenSeq = 0;\n    \n    public static void main(String[] args) {\n        // SSO Users\n        ssoUsers.put("admin", hash("AdminP@ss1"));\n        ssoUsers.put("manager", hash("ManagerP2!"));\n        ssoUsers.put("employee", hash("EmplP@ss3"));\n        \n        // Service permissions per role\n        servicePermissions.put("admin", new HashSet<>(Arrays.asList("CRM", "HR", "Email", "Wiki")));\n        servicePermissions.put("manager", new HashSet<>(Arrays.asList("CRM", "HR", "Email")));\n        servicePermissions.put("employee", new HashSet<>(Arrays.asList("Email", "Wiki")));\n        \n        System.out.println("=== Корпоративный SSO ===\\n");\n        \n        // 1. SSO Логин\n        System.out.println("--- 1. SSO Логин ---");\n        String ssoToken1 = ssoLogin("admin", "AdminP@ss1");\n        String ssoToken2 = ssoLogin("employee", "EmplP@ss3");\n        ssoLogin("admin", "wrongPass");\n        \n        // 2. Доступ к сервисам (admin)\n        System.out.println("\\n--- 2. Доступ admin ко всем сервисам ---");\n        for (String service : SERVICES) {\n            accessService(ssoToken1, service);\n        }\n        \n        // 3. Доступ к сервисам (employee)\n        System.out.println("\\n--- 3. Доступ employee ---");\n        for (String service : SERVICES) {\n            accessService(ssoToken2, service);\n        }\n        \n        // 4. Без SSO токена\n        System.out.println("\\n--- 4. Без SSO токена ---");\n        accessService(null, "CRM");\n        accessService("invalid-token", "CRM");\n        \n        // 5. SSO-сессии\n        System.out.println("\\n--- 5. Активные SSO-сессии ---");\n        showSessions();\n        \n        // 6. SSO Logout (admin)\n        System.out.println("\\n--- 6. SSO Logout admin ---");\n        ssoLogout(ssoToken1);\n        \n        // 7. После logout\n        System.out.println("\\n--- 7. После logout ---");\n        accessService(ssoToken1, "CRM");\n        accessService(ssoToken2, "Email"); // employee всё ещё залогинен\n        \n        // 8. Manager логин\n        System.out.println("\\n--- 8. Manager ---");\n        String ssoToken3 = ssoLogin("manager", "ManagerP2!");\n        for (String service : SERVICES) {\n            accessService(ssoToken3, service);\n        }\n        \n        // 9. Финальное состояние\n        System.out.println("\\n--- 9. Финальное состояние ---");\n        showSessions();\n    }\n    \n    static String ssoLogin(String username, String password) {\n        String storedHash = ssoUsers.get(username);\n        if (storedHash == null || !storedHash.equals(hash(password))) {\n            System.out.println("SSO Логин " + username + ": ОТКАЗ");\n            return null;\n        }\n        \n        String token = "sso_" + Integer.toHexString(++tokenSeq * 9173);\n        Map<String, Object> tokenInfo = new HashMap<>();\n        tokenInfo.put("userId", username);\n        tokenInfo.put("issuedAt", System.currentTimeMillis());\n        ssoTokens.put(token, tokenInfo);\n        activeSessions.put(token, new LinkedHashSet<>());\n        \n        System.out.println("SSO Логин " + username + ": УСПЕХ (token=" + token + ")");\n        return token;\n    }\n    \n    static void accessService(String ssoToken, String service) {\n        // Сервис валидирует SSO-токен\n        if (ssoToken == null) {\n            System.out.println(service + ": 401 (нет SSO-токена) -> redirect /sso/login");\n            return;\n        }\n        \n        Map<String, Object> tokenInfo = ssoTokens.get(ssoToken);\n        if (tokenInfo == null) {\n            System.out.println(service + ": 401 (невалидный SSO-токен) -> redirect /sso/login");\n            return;\n        }\n        \n        String userId = (String) tokenInfo.get("userId");\n        \n        // Проверка доступа к сервису\n        Set<String> allowed = servicePermissions.getOrDefault(userId, Collections.emptySet());\n        if (!allowed.contains(service)) {\n            System.out.println(service + ": 403 (" + userId + " -- нет доступа к " + service + ")");\n            return;\n        }\n        \n        // Регистрация сессии в сервисе\n        activeSessions.get(ssoToken).add(service);\n        System.out.println(service + ": 200 OK (user=" + userId + ", через SSO)");\n    }\n    \n    static void ssoLogout(String ssoToken) {\n        Map<String, Object> tokenInfo = ssoTokens.remove(ssoToken);\n        if (tokenInfo == null) {\n            System.out.println("SSO Logout: токен не найден");\n            return;\n        }\n        \n        String userId = (String) tokenInfo.get("userId");\n        Set<String> services = activeSessions.remove(ssoToken);\n        \n        System.out.println("SSO Logout " + userId + ":");\n        if (services != null && !services.isEmpty()) {\n            for (String service : services) {\n                System.out.println("  " + service + ": сессия завершена");\n            }\n        }\n        System.out.println("  SSO-токен инвалидирован");\n    }\n    \n    static void showSessions() {\n        if (ssoTokens.isEmpty()) {\n            System.out.println("Нет активных сессий");\n            return;\n        }\n        for (Map.Entry<String, Map<String, Object>> e : ssoTokens.entrySet()) {\n            String userId = (String) e.getValue().get("userId");\n            Set<String> services = activeSessions.getOrDefault(e.getKey(), Collections.emptySet());\n            System.out.println("  " + userId + " (" + e.getKey() + "): сервисы=" + services);\n        }\n    }\n    \n    static String hash(String s) {\n        long h = 0; for (char c : s.toCharArray()) { h = h * 31 + c; h ^= (h >>> 16); }\n        return String.format("%016x", Math.abs(h));\n    }\n}',
      explanation: 'SSO (Single Sign-On) -- один логин для всех сервисов. SSO-сервер выдаёт токен, каждый сервис валидирует его через SSO-сервер. При logout SSO-токен инвалидируется -- все сервисы автоматически разлогинивают пользователя. Права на сервисы определяются ролью. Это модель Okta, Keycloak, Google Workspace -- один аккаунт = все корпоративные сервисы.'
    },
    {
      id: 8,
      title: 'Audit Trail: Журнал безопасности',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй полноценный Audit Trail -- неизменяемый журнал всех действий с возможностью фильтрации, поиска и экспорта.',
      requirements: [
        'Создай класс Main с методом main',
        'Логирование: кто, когда, откуда (IP), что сделал, результат',
        'Категории: AUTH, DATA, ADMIN, SECURITY',
        'Уровни: INFO, WARN, CRITICAL',
        'Фильтрация по категории, уровню, пользователю, дате',
        'Обнаружение аномалий: множество неудачных логинов, доступ в необычное время'
      ],
      expectedOutput: 'Logged: AUTH INFO admin login from 10.0.0.1\nLogged: SECURITY CRITICAL brute force detected\nFilter(CRITICAL): 2 entries\nAnomaly: 5 failed logins from 10.0.0.50',
      hint: 'Каждая запись = Map<String, Object> с полями: timestamp, category, level, userId, ip, action, result.',
      solution: 'import java.util.*;\nimport java.time.LocalDateTime;\nimport java.time.format.DateTimeFormatter;\n\npublic class Main {\n    static List<Map<String, Object>> auditTrail = new ArrayList<>();\n    static DateTimeFormatter fmt = DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss");\n    \n    public static void main(String[] args) {\n        System.out.println("=== Audit Trail ===\\n");\n        \n        // Имитация событий\n        System.out.println("--- Запись событий ---");\n        log("AUTH", "INFO", "admin", "10.0.0.1", "Успешный логин", "OK");\n        log("AUTH", "INFO", "user1", "10.0.0.2", "Успешный логин", "OK");\n        log("AUTH", "WARN", "admin", "10.0.0.50", "Неудачный логин", "FAIL");\n        log("AUTH", "WARN", "admin", "10.0.0.50", "Неудачный логин (попытка 2)", "FAIL");\n        log("AUTH", "WARN", "admin", "10.0.0.50", "Неудачный логин (попытка 3)", "FAIL");\n        log("AUTH", "WARN", "admin", "10.0.0.50", "Неудачный логин (попытка 4)", "FAIL");\n        log("AUTH", "WARN", "admin", "10.0.0.50", "Неудачный логин (попытка 5)", "FAIL");\n        log("SECURITY", "CRITICAL", "system", "10.0.0.50", "Brute force обнаружен, IP заблокирован", "BLOCKED");\n        log("DATA", "INFO", "user1", "10.0.0.2", "Просмотр списка пользователей", "OK");\n        log("DATA", "INFO", "user1", "10.0.0.2", "Экспорт данных клиентов (1500 записей)", "OK");\n        log("ADMIN", "INFO", "admin", "10.0.0.1", "Создан пользователь: new_user (роль: VIEWER)", "OK");\n        log("ADMIN", "WARN", "admin", "10.0.0.1", "Изменена политика паролей: minLength 8 -> 6", "OK");\n        log("SECURITY", "CRITICAL", "system", "10.0.0.99", "Попытка SQL injection в /api/users", "BLOCKED");\n        log("DATA", "INFO", "admin", "10.0.0.1", "Просмотр audit trail", "OK");\n        \n        // Полный лог\n        System.out.println("\\n--- Полный Audit Trail (" + auditTrail.size() + " записей) ---");\n        printEntries(auditTrail);\n        \n        // Фильтрация по уровню\n        System.out.println("\\n--- Фильтр: CRITICAL ---");\n        List<Map<String, Object>> critical = filter("level", "CRITICAL");\n        printEntries(critical);\n        \n        // Фильтрация по категории\n        System.out.println("\\n--- Фильтр: AUTH ---");\n        List<Map<String, Object>> auth = filter("category", "AUTH");\n        printEntries(auth);\n        \n        // Фильтрация по пользователю\n        System.out.println("\\n--- Фильтр: user1 ---");\n        List<Map<String, Object>> user1 = filter("userId", "user1");\n        printEntries(user1);\n        \n        // Аномалии\n        System.out.println("\\n--- Обнаружение аномалий ---");\n        detectAnomalies();\n        \n        // Статистика\n        System.out.println("\\n--- Статистика ---");\n        printStats();\n    }\n    \n    static void log(String category, String level, String userId, String ip, String action, String result) {\n        Map<String, Object> entry = new LinkedHashMap<>();\n        entry.put("id", auditTrail.size() + 1);\n        entry.put("timestamp", LocalDateTime.now().format(fmt));\n        entry.put("category", category);\n        entry.put("level", level);\n        entry.put("userId", userId);\n        entry.put("ip", ip);\n        entry.put("action", action);\n        entry.put("result", result);\n        auditTrail.add(entry);\n        \n        String icon = "INFO".equals(level) ? "i" : "WARN".equals(level) ? "!" : "X";\n        System.out.println("[" + icon + "] " + category + "/" + level + " " + userId + "@" + ip + ": " + action + " -> " + result);\n    }\n    \n    static List<Map<String, Object>> filter(String field, String value) {\n        List<Map<String, Object>> result = new ArrayList<>();\n        for (Map<String, Object> entry : auditTrail) {\n            if (value.equals(entry.get(field))) {\n                result.add(entry);\n            }\n        }\n        return result;\n    }\n    \n    static void printEntries(List<Map<String, Object>> entries) {\n        for (Map<String, Object> e : entries) {\n            System.out.printf("  #%-2s [%-8s] %-8s %-8s %-12s %s -> %s%n",\n                e.get("id"), e.get("level"), e.get("category"),\n                e.get("userId"), e.get("ip"), e.get("action"), e.get("result"));\n        }\n        System.out.println("  Всего: " + entries.size() + " записей");\n    }\n    \n    static void detectAnomalies() {\n        // 1. Множественные неудачные логины с одного IP\n        Map<String, Integer> failedByIp = new HashMap<>();\n        for (Map<String, Object> e : auditTrail) {\n            if ("AUTH".equals(e.get("category")) && "FAIL".equals(e.get("result"))) {\n                String ip = (String) e.get("ip");\n                failedByIp.merge(ip, 1, Integer::sum);\n            }\n        }\n        for (Map.Entry<String, Integer> entry : failedByIp.entrySet()) {\n            if (entry.getValue() >= 3) {\n                System.out.println("  АНОМАЛИЯ: " + entry.getValue() + " неудачных логинов с IP " + entry.getKey());\n            }\n        }\n        \n        // 2. Массовый экспорт данных\n        for (Map<String, Object> e : auditTrail) {\n            String action = (String) e.get("action");\n            if (action.contains("Экспорт") && action.contains("1500")) {\n                System.out.println("  АНОМАЛИЯ: массовый экспорт данных пользователем " + e.get("userId"));\n            }\n        }\n        \n        // 3. Ослабление политик безопасности\n        for (Map<String, Object> e : auditTrail) {\n            String action = (String) e.get("action");\n            if (action.contains("политика") && action.contains("8 -> 6")) {\n                System.out.println("  АНОМАЛИЯ: ослабление политики паролей пользователем " + e.get("userId"));\n            }\n        }\n    }\n    \n    static void printStats() {\n        Map<String, Integer> byCat = new LinkedHashMap<>();\n        Map<String, Integer> byLevel = new LinkedHashMap<>();\n        for (Map<String, Object> e : auditTrail) {\n            byCat.merge((String) e.get("category"), 1, Integer::sum);\n            byLevel.merge((String) e.get("level"), 1, Integer::sum);\n        }\n        System.out.println("  По категориям: " + byCat);\n        System.out.println("  По уровням:    " + byLevel);\n        System.out.println("  Всего записей: " + auditTrail.size());\n    }\n}',
      explanation: 'Audit Trail -- неизменяемый журнал всех действий безопасности. Каждая запись содержит: кто, когда, откуда, что, результат. Поддерживает фильтрацию и обнаружение аномалий: brute force (5+ неудачных логинов), массовый экспорт данных, ослабление политик. В production используют SIEM-системы (Splunk, ELK) для анализа логов в реальном времени.'
    },
    {
      id: 9,
      title: 'Webhook Security: Подпись и проверка',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй безопасную систему вебхуков: подпись исходящих вебхуков, проверка входящих, retry с экспоненциальным backoff.',
      requirements: [
        'Создай класс Main с методом main',
        'Подпись вебхука: HMAC-SHA256(payload, secret)',
        'Проверка подписи при получении',
        'Timestamp для защиты от replay attack',
        'Retry: 3 попытки с экспоненциальным backoff (1с, 2с, 4с)',
        'Покажи: отправку, верификацию, подделку, replay'
      ],
      expectedOutput: 'Webhook отправлен: signature=xxx\nВерификация: OK\nПоддельный payload: FAIL (подпись не совпадает)\nReplay (старый timestamp): FAIL',
      hint: 'Подпись = HMAC(timestamp + "." + payload, secret). При проверке пересчитай и сравни.',
      solution: 'import javax.crypto.Mac;\nimport javax.crypto.spec.SecretKeySpec;\nimport java.util.*;\n\npublic class Main {\n    static final String WEBHOOK_SECRET = "whsec_demo_secret_key_for_signing_256b";\n    static final long MAX_AGE_SEC = 300; // 5 минут\n    static List<String> deliveryLog = new ArrayList<>();\n    \n    public static void main(String[] args) throws Exception {\n        System.out.println("=== Webhook Security ===\\n");\n        \n        // 1. Отправка и верификация\n        System.out.println("--- 1. Успешная доставка ---");\n        String payload1 = "{\\"event\\":\\"order.paid\\",\\"orderId\\":\\"ORD-001\\",\\"amount\\":45000}";\n        Map<String, String> webhook1 = sendWebhook(payload1);\n        verifyWebhook(webhook1.get("payload"), webhook1.get("signature"), webhook1.get("timestamp"));\n        \n        // 2. Подделанный payload\n        System.out.println("\\n--- 2. Подделанный payload ---");\n        String tampered = "{\\"event\\":\\"order.paid\\",\\"orderId\\":\\"ORD-001\\",\\"amount\\":999999}";\n        verifyWebhook(tampered, webhook1.get("signature"), webhook1.get("timestamp"));\n        \n        // 3. Подделанная подпись\n        System.out.println("\\n--- 3. Подделанная подпись ---");\n        verifyWebhook(payload1, "fake_signature_abc123", webhook1.get("timestamp"));\n        \n        // 4. Replay атака\n        System.out.println("\\n--- 4. Replay атака ---");\n        long oldTimestamp = System.currentTimeMillis() / 1000 - 600;\n        String oldSig = sign(oldTimestamp + "." + payload1);\n        verifyWebhook(payload1, oldSig, String.valueOf(oldTimestamp));\n        \n        // 5. Retry с backoff\n        System.out.println("\\n--- 5. Retry с exponential backoff ---");\n        String payload2 = "{\\"event\\":\\"user.created\\",\\"userId\\":\\"USR-042\\"}";\n        deliverWithRetry(payload2, "https://myapp.com/webhooks", 3);\n        \n        // 6. Множественные события\n        System.out.println("\\n--- 6. Множественные события ---");\n        String[] events = {\n            "{\\"event\\":\\"order.created\\",\\"id\\":\\"ORD-002\\"}",\n            "{\\"event\\":\\"payment.success\\",\\"id\\":\\"PAY-001\\"}",\n            "{\\"event\\":\\"order.shipped\\",\\"id\\":\\"ORD-002\\"}"\n        };\n        for (String event : events) {\n            Map<String, String> wh = sendWebhook(event);\n            verifyWebhook(wh.get("payload"), wh.get("signature"), wh.get("timestamp"));\n            System.out.println();\n        }\n        \n        // 7. Лог доставки\n        System.out.println("--- 7. Лог доставки ---");\n        for (String entry : deliveryLog) {\n            System.out.println("  " + entry);\n        }\n    }\n    \n    static Map<String, String> sendWebhook(String payload) throws Exception {\n        long timestamp = System.currentTimeMillis() / 1000;\n        String dataToSign = timestamp + "." + payload;\n        String signature = sign(dataToSign);\n        \n        System.out.println("Отправка webhook:");\n        System.out.println("  Payload: " + payload.substring(0, Math.min(60, payload.length())) + (payload.length() > 60 ? "..." : ""));\n        System.out.println("  X-Webhook-Timestamp: " + timestamp);\n        System.out.println("  X-Webhook-Signature: " + signature.substring(0, 20) + "...");\n        \n        deliveryLog.add("SENT " + payload.substring(0, Math.min(40, payload.length())) + "... sig=" + signature.substring(0, 10) + "...");\n        \n        Map<String, String> result = new HashMap<>();\n        result.put("payload", payload);\n        result.put("signature", signature);\n        result.put("timestamp", String.valueOf(timestamp));\n        return result;\n    }\n    \n    static void verifyWebhook(String payload, String signature, String timestampStr) throws Exception {\n        // 1. Проверка timestamp (anti-replay)\n        long timestamp = Long.parseLong(timestampStr);\n        long now = System.currentTimeMillis() / 1000;\n        if (Math.abs(now - timestamp) > MAX_AGE_SEC) {\n            System.out.println("Верификация: FAIL (timestamp устарел: " + (now - timestamp) + " сек)");\n            deliveryLog.add("VERIFY_FAIL replay_attack");\n            return;\n        }\n        \n        // 2. Пересчитываем подпись\n        String dataToSign = timestamp + "." + payload;\n        String expectedSig = sign(dataToSign);\n        \n        if (!expectedSig.equals(signature)) {\n            System.out.println("Верификация: FAIL (подпись не совпадает)");\n            deliveryLog.add("VERIFY_FAIL invalid_signature");\n            return;\n        }\n        \n        System.out.println("Верификация: OK (подпись и timestamp валидны)");\n        deliveryLog.add("VERIFY_OK " + payload.substring(0, Math.min(30, payload.length())) + "...");\n    }\n    \n    static void deliverWithRetry(String payload, String url, int maxRetries) throws Exception {\n        Map<String, String> webhook = sendWebhook(payload);\n        Random rng = new Random(42);\n        \n        for (int attempt = 1; attempt <= maxRetries; attempt++) {\n            // Имитация: первые 2 попытки -- ошибка, 3-я -- успех\n            boolean success = attempt == maxRetries;\n            long backoff = (long) Math.pow(2, attempt - 1) * 1000; // 1s, 2s, 4s\n            \n            if (success) {\n                System.out.println("  Попытка " + attempt + "/" + maxRetries + ": 200 OK (доставлено)");\n                deliveryLog.add("DELIVERED attempt=" + attempt + " url=" + url);\n                return;\n            } else {\n                int errorCode = rng.nextBoolean() ? 500 : 503;\n                System.out.println("  Попытка " + attempt + "/" + maxRetries + ": " + errorCode +\n                    " -> retry через " + (backoff / 1000) + "с");\n                deliveryLog.add("RETRY attempt=" + attempt + " status=" + errorCode + " backoff=" + (backoff / 1000) + "s");\n            }\n        }\n        \n        System.out.println("  Все попытки исчерпаны, webhook не доставлен!");\n        deliveryLog.add("FAILED all retries exhausted");\n    }\n    \n    static String sign(String data) throws Exception {\n        Mac mac = Mac.getInstance("HmacSHA256");\n        mac.init(new SecretKeySpec(WEBHOOK_SECRET.getBytes(), "HmacSHA256"));\n        return Base64.getUrlEncoder().withoutPadding().encodeToString(mac.doFinal(data.getBytes()));\n    }\n}',
      explanation: 'Webhook Security: каждый вебхук подписывается HMAC-SHA256(timestamp.payload, secret). Получатель пересчитывает подпись и проверяет: 1) подпись совпадает (payload не изменён), 2) timestamp свежий (не replay). Retry с exponential backoff: 1с -> 2с -> 4с при ошибках. Это модель Stripe, GitHub, Twilio -- все подписывают webhooks.'
    },
    {
      id: 10,
      title: 'Полная система безопасности приложения',
      type: 'practice',
      difficulty: 'hard',
      description: 'Объедини все концепции курса: построй полную систему безопасности с регистрацией, MFA, RBAC, API-ключами, audit trail и rate limiting.',
      requirements: [
        'Создай класс Main с методом main',
        'Регистрация и логин с 2FA',
        'RBAC: роли и права',
        'API-ключи для внешних интеграций',
        'Rate limiting',
        'Audit trail всех действий',
        'Покажи полный жизненный цикл: register -> login -> access -> api key -> audit'
      ],
      expectedOutput: 'Register admin: OK\nLogin: password OK -> OTP -> JWT\nRBAC: admin -> delete: OK\nAPI Key: pk_xxx -> GET /api: 200\nAudit: 15 events logged',
      hint: 'Создай отдельные "модули" (методы) для каждого аспекта безопасности и объедини в main.',
      solution: 'import java.util.*;\n\npublic class Main {\n    // Users & Auth\n    static Map<String, Map<String, Object>> users = new LinkedHashMap<>();\n    static Map<String, Map<String, Object>> sessions = new LinkedHashMap<>();\n    // RBAC\n    static Map<String, Set<String>> rolePerms = new LinkedHashMap<>();\n    // API Keys\n    static Map<String, Map<String, Object>> apiKeys = new LinkedHashMap<>();\n    // Rate Limiting\n    static Map<String, Integer> rateLimits = new HashMap<>();\n    static final int RATE_LIMIT = 10;\n    // Audit\n    static List<String> audit = new ArrayList<>();\n    // Counters\n    static int seq = 0;\n    static Random rng = new Random(42);\n    \n    public static void main(String[] args) {\n        // Настройка RBAC\n        rolePerms.put("ADMIN", new HashSet<>(Arrays.asList("read", "write", "delete", "manage_users", "view_audit", "manage_keys")));\n        rolePerms.put("USER", new HashSet<>(Arrays.asList("read", "write")));\n        rolePerms.put("VIEWER", new HashSet<>(Arrays.asList("read")));\n        \n        System.out.println("=== Полная система безопасности ===\\n");\n        \n        // ===== 1. Регистрация =====\n        System.out.println("=== 1. Регистрация ===");\n        register("admin", "admin@co.com", "AdminP@ss1", "ADMIN", true);\n        register("user1", "user1@co.com", "UserPass2!", "USER", false);\n        register("viewer", "viewer@co.com", "ViewP@ss3", "VIEWER", false);\n        register("admin", "dup@co.com", "Pass1234!", "USER", false); // дубликат\n        \n        // ===== 2. Логин с MFA =====\n        System.out.println("\\n=== 2. Логин ===");\n        String adminSession = login("admin", "AdminP@ss1");\n        String userSession = login("user1", "UserPass2!");\n        login("admin", "wrongPass"); // неудачный\n        login("admin", "wrongPass"); // неудачный\n        \n        // ===== 3. RBAC =====\n        System.out.println("\\n=== 3. RBAC проверка ===");\n        checkPermission(adminSession, "delete");\n        checkPermission(adminSession, "manage_users");\n        checkPermission(userSession, "read");\n        checkPermission(userSession, "write");\n        checkPermission(userSession, "delete"); // нет прав\n        checkPermission(userSession, "manage_users"); // нет прав\n        \n        // ===== 4. API Keys =====\n        System.out.println("\\n=== 4. API Keys ===");\n        String apiKey1 = createApiKey(adminSession, "integration-A", "read", "write");\n        String apiKey2 = createApiKey(adminSession, "monitoring", "read");\n        createApiKey(userSession, "my-app", "read"); // USER не может создавать ключи\n        \n        // Использование API Key\n        System.out.println();\n        useApiKey(apiKey1, "GET", "/api/data");\n        useApiKey(apiKey1, "POST", "/api/data");\n        useApiKey(apiKey2, "POST", "/api/data"); // нет scope write\n        useApiKey("invalid_key", "GET", "/api/data");\n        \n        // ===== 5. Rate Limiting =====\n        System.out.println("\\n=== 5. Rate Limiting ===");\n        for (int i = 0; i < 12; i++) {\n            useApiKey(apiKey1, "GET", "/api/data");\n        }\n        \n        // ===== 6. Protected Resources =====\n        System.out.println("\\n=== 6. Защищённые ресурсы ===");\n        accessResource(adminSession, "GET", "/api/users");\n        accessResource(adminSession, "DELETE", "/api/users/123");\n        accessResource(userSession, "GET", "/api/users");\n        accessResource(userSession, "DELETE", "/api/users/123"); // нет прав\n        accessResource(null, "GET", "/api/users"); // нет сессии\n        \n        // ===== 7. Audit Trail =====\n        System.out.println("\\n=== 7. Audit Trail ===");\n        System.out.println("-".repeat(90));\n        for (String entry : audit) {\n            System.out.println(entry);\n        }\n        System.out.println("-".repeat(90));\n        System.out.println("Всего событий: " + audit.size());\n    }\n    \n    // ----- Registration -----\n    static void register(String username, String email, String password, String role, boolean mfa) {\n        if (users.containsKey(username)) {\n            logAudit("REGISTER", "WARN", username, "Дубликат username");\n            System.out.println("Регистрация " + username + ": ОШИБКА (username занят)");\n            return;\n        }\n        Map<String, Object> user = new HashMap<>();\n        user.put("email", email);\n        user.put("hash", hash(password));\n        user.put("role", role);\n        user.put("mfa", mfa);\n        users.put(username, user);\n        logAudit("REGISTER", "INFO", username, "Зарегистрирован как " + role + " (MFA=" + mfa + ")");\n        System.out.println("Регистрация " + username + " (" + role + ", MFA=" + mfa + "): УСПЕХ");\n    }\n    \n    // ----- Login -----\n    static String login(String username, String password) {\n        Map<String, Object> user = users.get(username);\n        if (user == null || !user.get("hash").equals(hash(password))) {\n            logAudit("LOGIN", "WARN", username, "Неудачный логин");\n            System.out.println("Логин " + username + ": ОТКАЗ");\n            return null;\n        }\n        \n        System.out.print("Логин " + username + ": пароль OK");\n        \n        if ((boolean) user.get("mfa")) {\n            String otp = String.format("%06d", rng.nextInt(1000000));\n            System.out.print(" -> OTP " + otp + " OK");\n        }\n        \n        String sessionId = "sess_" + Integer.toHexString(++seq * 7919);\n        Map<String, Object> session = new HashMap<>();\n        session.put("userId", username);\n        session.put("role", user.get("role"));\n        session.put("createdAt", System.currentTimeMillis());\n        sessions.put(sessionId, session);\n        \n        System.out.println(" -> session=" + sessionId);\n        logAudit("LOGIN", "INFO", username, "Успешный вход (session=" + sessionId + ")");\n        return sessionId;\n    }\n    \n    // ----- RBAC -----\n    static void checkPermission(String sessionId, String permission) {\n        if (sessionId == null) { System.out.println("RBAC: нет сессии -> ОТКАЗ"); return; }\n        Map<String, Object> session = sessions.get(sessionId);\n        if (session == null) { System.out.println("RBAC: невалидная сессия -> ОТКАЗ"); return; }\n        \n        String userId = (String) session.get("userId");\n        String role = (String) session.get("role");\n        Set<String> perms = rolePerms.getOrDefault(role, Collections.emptySet());\n        boolean allowed = perms.contains(permission);\n        \n        System.out.println("RBAC: " + userId + " (" + role + ") -> " + permission + ": " + (allowed ? "OK" : "ОТКАЗ"));\n        if (!allowed) logAudit("RBAC", "WARN", userId, "Отказ в " + permission);\n    }\n    \n    // ----- API Keys -----\n    @SuppressWarnings("unchecked")\n    static String createApiKey(String sessionId, String name, String... scopes) {\n        if (sessionId == null) return null;\n        Map<String, Object> session = sessions.get(sessionId);\n        String userId = (String) session.get("userId");\n        String role = (String) session.get("role");\n        \n        if (!rolePerms.getOrDefault(role, Collections.emptySet()).contains("manage_keys")) {\n            System.out.println("API Key: ОТКАЗ (" + userId + " не может создавать ключи)");\n            logAudit("API_KEY", "WARN", userId, "Попытка создать API key без прав");\n            return null;\n        }\n        \n        String key = "pk_" + UUID.randomUUID().toString().replace("-", "").substring(0, 20);\n        Map<String, Object> keyInfo = new HashMap<>();\n        keyInfo.put("name", name);\n        keyInfo.put("owner", userId);\n        keyInfo.put("scopes", new HashSet<>(Arrays.asList(scopes)));\n        apiKeys.put(key, keyInfo);\n        \n        System.out.println("API Key \\"" + name + "\\": " + key.substring(0, 15) + "... (scopes=" + Arrays.asList(scopes) + ")");\n        logAudit("API_KEY", "INFO", userId, "Создан API key \\"" + name + "\\"");\n        return key;\n    }\n    \n    @SuppressWarnings("unchecked")\n    static void useApiKey(String key, String method, String path) {\n        if (key == null) return;\n        Map<String, Object> info = apiKeys.get(key);\n        if (info == null) {\n            System.out.println("  " + method + " " + path + ": 401 (невалидный ключ)");\n            return;\n        }\n        \n        // Rate limiting по ключу\n        int count = rateLimits.getOrDefault(key, 0);\n        if (count >= RATE_LIMIT) {\n            System.out.println("  " + method + " " + path + ": 429 (лимит " + RATE_LIMIT + ")");\n            return;\n        }\n        rateLimits.put(key, count + 1);\n        \n        // Scope check\n        String requiredScope = "GET".equals(method) ? "read" : "write";\n        Set<String> scopes = (Set<String>) info.get("scopes");\n        if (!scopes.contains(requiredScope)) {\n            System.out.println("  " + method + " " + path + ": 403 (нет scope " + requiredScope + ")");\n            return;\n        }\n        \n        System.out.println("  " + method + " " + path + ": 200 OK (key=\\"" + info.get("name") + "\\", " + (count + 1) + "/" + RATE_LIMIT + ")");\n    }\n    \n    // ----- Protected Resources -----\n    static void accessResource(String sessionId, String method, String path) {\n        if (sessionId == null) {\n            System.out.println(method + " " + path + ": 401 (нет сессии)");\n            return;\n        }\n        Map<String, Object> session = sessions.get(sessionId);\n        if (session == null) {\n            System.out.println(method + " " + path + ": 401 (невалидная сессия)");\n            return;\n        }\n        \n        String userId = (String) session.get("userId");\n        String role = (String) session.get("role");\n        String requiredPerm = "DELETE".equals(method) ? "delete" : "read";\n        \n        Set<String> perms = rolePerms.getOrDefault(role, Collections.emptySet());\n        if (!perms.contains(requiredPerm)) {\n            System.out.println(method + " " + path + ": 403 (" + userId + " нет права " + requiredPerm + ")");\n            logAudit("ACCESS", "WARN", userId, "403 " + method + " " + path);\n            return;\n        }\n        \n        System.out.println(method + " " + path + ": 200 OK (user=" + userId + ")");\n        logAudit("ACCESS", "INFO", userId, "200 " + method + " " + path);\n    }\n    \n    // ----- Audit -----\n    static void logAudit(String category, String level, String userId, String message) {\n        String entry = String.format("[%-8s] %-5s %-10s %s", category, level, userId, message);\n        audit.add(entry);\n    }\n    \n    static String hash(String s) {\n        long h = 0; for (char c : s.toCharArray()) { h = h * 31 + c; h ^= (h >>> 16); }\n        return String.format("%016x", Math.abs(h));\n    }\n}',
      explanation: 'Полная система безопасности объединяет все концепции курса: 1) Регистрация с валидацией. 2) Логин с MFA. 3) RBAC для проверки прав. 4) API Keys для внешних интеграций с scope-контролем. 5) Rate Limiting. 6) Audit Trail всех событий. Каждый компонент работает независимо, но вместе они создают многоуровневую защиту (Defense in Depth). Это архитектура production-приложений.'
    }
  ]
}
