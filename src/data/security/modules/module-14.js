export default {
  id: 14,
  title: 'Практикум: Авторизация и RBAC',
  description: 'Практические задачи по реализации авторизации: RBAC, проверка прав, иерархия ролей, ресурсный доступ, API ключи, scope-валидация',
  lessons: [
    {
      id: 1,
      title: 'Базовый RBAC',
      type: 'practice',
      difficulty: 'easy',
      description: 'Реализуй базовую систему RBAC с ролями и правами. Каждая роль имеет набор разрешений, пользователь проверяется по роли.',
      requirements: [
        'Создай класс Main с методом main',
        'Определи 4 роли: ADMIN, MANAGER, EDITOR, VIEWER',
        'Каждая роль имеет свой набор прав: create, read, update, delete, manage_users',
        'Проверяй доступ пользователя к операции через его роль',
        'Выведи матрицу доступа для всех ролей и операций'
      ],
      expectedOutput: 'Матрица доступа:\n         create  read  update  delete  manage_users\nADMIN      +      +      +       +         +\nMANAGER    +      +      +       +         -\nEDITOR     +      +      +       -         -\nVIEWER     -      +      -       -         -',
      hint: 'Используй Map<String, Set<String>> для хранения прав каждой роли.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<String, Set<String>> rolePermissions = new LinkedHashMap<>();\n    static Map<String, String> userRoles = new LinkedHashMap<>();\n    \n    public static void main(String[] args) {\n        // Определяем роли\n        rolePermissions.put("ADMIN", new LinkedHashSet<>(Arrays.asList("create", "read", "update", "delete", "manage_users")));\n        rolePermissions.put("MANAGER", new LinkedHashSet<>(Arrays.asList("create", "read", "update", "delete")));\n        rolePermissions.put("EDITOR", new LinkedHashSet<>(Arrays.asList("create", "read", "update")));\n        rolePermissions.put("VIEWER", new LinkedHashSet<>(Arrays.asList("read")));\n        \n        // Назначаем роли пользователям\n        userRoles.put("alice", "ADMIN");\n        userRoles.put("bob", "MANAGER");\n        userRoles.put("charlie", "EDITOR");\n        userRoles.put("dave", "VIEWER");\n        \n        // Матрица доступа\n        String[] allPerms = {"create", "read", "update", "delete", "manage_users"};\n        System.out.println("=== Матрица доступа (RBAC) ===\\n");\n        System.out.printf("%-10s", "");\n        for (String p : allPerms) System.out.printf("%-14s", p);\n        System.out.println();\n        System.out.println("-".repeat(80));\n        \n        for (Map.Entry<String, Set<String>> role : rolePermissions.entrySet()) {\n            System.out.printf("%-10s", role.getKey());\n            for (String p : allPerms) {\n                System.out.printf("%-14s", role.getValue().contains(p) ? "+" : "-");\n            }\n            System.out.println();\n        }\n        \n        // Проверка доступа\n        System.out.println("\\n=== Проверка доступа ===\\n");\n        checkAccess("alice", "manage_users");\n        checkAccess("bob", "delete");\n        checkAccess("bob", "manage_users");\n        checkAccess("charlie", "update");\n        checkAccess("charlie", "delete");\n        checkAccess("dave", "read");\n        checkAccess("dave", "create");\n        checkAccess("unknown", "read");\n    }\n    \n    static void checkAccess(String user, String permission) {\n        String role = userRoles.get(user);\n        if (role == null) {\n            System.out.println(user + " -> " + permission + ": ОТКАЗ (пользователь не найден)");\n            return;\n        }\n        Set<String> perms = rolePermissions.getOrDefault(role, Collections.emptySet());\n        boolean allowed = perms.contains(permission);\n        System.out.println(user + " (" + role + ") -> " + permission + ": " + (allowed ? "OK" : "ОТКАЗ"));\n    }\n}',
      explanation: 'RBAC (Role-Based Access Control) -- пользователь получает права через роль. Вместо назначения прав каждому пользователю отдельно, мы назначаем роль, а роль содержит набор разрешений. Это упрощает управление: добавить нового EDITOR -- просто назначить роль, не перечисляя каждое право.'
    },
    {
      id: 2,
      title: 'Иерархия ролей',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй RBAC с иерархией: ADMIN наследует все права MANAGER, MANAGER наследует EDITOR, и т.д.',
      requirements: [
        'Создай класс Main с методом main',
        'Иерархия: ADMIN > MANAGER > EDITOR > VIEWER',
        'Каждая роль наследует ВСЕ права нижестоящих ролей',
        'Добавь собственные уникальные права каждой роли',
        'Покажи effective permissions (все права с учётом наследования)'
      ],
      expectedOutput: 'VIEWER: own=[read] -> effective=[read]\nEDITOR: own=[create,update] -> effective=[read,create,update]\nMANAGER: own=[delete,reports] -> effective=[read,create,update,delete,reports]\nADMIN: own=[manage_users,settings] -> effective=[все]',
      hint: 'Создай Map<String, String> parentRole для иерархии. Рекурсивно собирай права от текущей роли вверх по иерархии.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<String, Set<String>> ownPermissions = new LinkedHashMap<>();\n    static Map<String, String> parentRole = new LinkedHashMap<>();\n    \n    public static void main(String[] args) {\n        // Иерархия ролей\n        parentRole.put("EDITOR", "VIEWER");\n        parentRole.put("MANAGER", "EDITOR");\n        parentRole.put("ADMIN", "MANAGER");\n        // VIEWER -- корневая роль, без parent\n        \n        // Собственные права каждой роли\n        ownPermissions.put("VIEWER", new LinkedHashSet<>(Arrays.asList("read")));\n        ownPermissions.put("EDITOR", new LinkedHashSet<>(Arrays.asList("create", "update")));\n        ownPermissions.put("MANAGER", new LinkedHashSet<>(Arrays.asList("delete", "view_reports")));\n        ownPermissions.put("ADMIN", new LinkedHashSet<>(Arrays.asList("manage_users", "settings", "audit_log")));\n        \n        // Effective permissions\n        System.out.println("=== Иерархия ролей ===\\n");\n        String[] roles = {"VIEWER", "EDITOR", "MANAGER", "ADMIN"};\n        for (String role : roles) {\n            Set<String> own = ownPermissions.getOrDefault(role, Collections.emptySet());\n            Set<String> effective = getEffectivePermissions(role);\n            String parent = parentRole.get(role);\n            System.out.println(role + (parent != null ? " (наследует " + parent + ")" : " (базовая роль)") + ":");\n            System.out.println("  Собственные: " + own);\n            System.out.println("  Итоговые:    " + effective);\n            System.out.println();\n        }\n        \n        // Проверка доступа\n        System.out.println("=== Проверка доступа ===\\n");\n        Map<String, String> users = new LinkedHashMap<>();\n        users.put("alice", "ADMIN");\n        users.put("bob", "MANAGER");\n        users.put("charlie", "EDITOR");\n        users.put("dave", "VIEWER");\n        \n        String[] actions = {"read", "create", "delete", "manage_users"};\n        for (Map.Entry<String, String> u : users.entrySet()) {\n            System.out.print(u.getKey() + " (" + u.getValue() + "): ");\n            List<String> results = new ArrayList<>();\n            for (String a : actions) {\n                boolean ok = hasPermission(u.getValue(), a);\n                results.add(a + "=" + (ok ? "+" : "-"));\n            }\n            System.out.println(String.join(", ", results));\n        }\n    }\n    \n    static Set<String> getEffectivePermissions(String role) {\n        Set<String> effective = new LinkedHashSet<>();\n        String current = role;\n        while (current != null) {\n            Set<String> own = ownPermissions.getOrDefault(current, Collections.emptySet());\n            effective.addAll(own);\n            current = parentRole.get(current);\n        }\n        return effective;\n    }\n    \n    static boolean hasPermission(String role, String permission) {\n        return getEffectivePermissions(role).contains(permission);\n    }\n}',
      explanation: 'Иерархический RBAC: каждая роль наследует все права нижестоящих. ADMIN автоматически получает права MANAGER + EDITOR + VIEWER + свои собственные. Это избавляет от дублирования -- не нужно каждой роли явно прописывать read, если она наследует VIEWER. Большинство enterprise-систем (Keycloak, Active Directory) поддерживают иерархию ролей.'
    },
    {
      id: 3,
      title: 'Resource-Based Access Control',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй контроль доступа на уровне ресурсов: пользователь может редактировать только свои записи, а ADMIN -- любые.',
      requirements: [
        'Создай класс Main с методом main',
        'Ресурс (документ) имеет владельца (ownerId)',
        'Владелец может read, update, delete свой документ',
        'Другие пользователи могут только read (если документ public)',
        'ADMIN может всё с любым документом',
        'Покажи проверку доступа для owner, non-owner, admin'
      ],
      expectedOutput: 'user1 -> read doc1 (owner): OK\nuser1 -> delete doc1 (owner): OK\nuser2 -> read doc1 (public): OK\nuser2 -> delete doc1 (not owner): ОТКАЗ\nadmin -> delete doc1: OK (ADMIN)',
      hint: 'Создай класс Document с полями: id, ownerId, isPublic. Проверяй owner + role при каждом действии.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<String, Map<String, Object>> documents = new LinkedHashMap<>();\n    static Map<String, String> userRoles = new LinkedHashMap<>();\n    \n    public static void main(String[] args) {\n        // Пользователи\n        userRoles.put("user1", "USER");\n        userRoles.put("user2", "USER");\n        userRoles.put("user3", "USER");\n        userRoles.put("admin", "ADMIN");\n        \n        // Документы\n        createDocument("doc1", "user1", "Отчёт Q1", true);\n        createDocument("doc2", "user1", "Личные заметки", false);\n        createDocument("doc3", "user2", "Проект X", true);\n        \n        System.out.println("=== Resource-Based Access Control ===\\n");\n        System.out.println("Документы:");\n        for (Map.Entry<String, Map<String, Object>> d : documents.entrySet()) {\n            Map<String, Object> doc = d.getValue();\n            System.out.println("  " + d.getKey() + ": \\"" + doc.get("title") + "\\" owner=" + doc.get("ownerId") + " public=" + doc.get("isPublic"));\n        }\n        \n        // Проверка доступа -- владелец\n        System.out.println("\\n--- Владелец (user1) ---");\n        checkAccess("user1", "doc1", "read");\n        checkAccess("user1", "doc1", "update");\n        checkAccess("user1", "doc1", "delete");\n        checkAccess("user1", "doc2", "read"); // private, но owner\n        \n        // Проверка доступа -- другой пользователь\n        System.out.println("\\n--- Другой пользователь (user2) ---");\n        checkAccess("user2", "doc1", "read"); // public\n        checkAccess("user2", "doc1", "update"); // not owner\n        checkAccess("user2", "doc1", "delete"); // not owner\n        checkAccess("user2", "doc2", "read"); // private + not owner\n        \n        // Проверка доступа -- admin\n        System.out.println("\\n--- Admin ---");\n        checkAccess("admin", "doc1", "read");\n        checkAccess("admin", "doc1", "delete");\n        checkAccess("admin", "doc2", "read"); // private, но admin\n        checkAccess("admin", "doc2", "delete");\n        \n        // Несуществующий ресурс\n        System.out.println("\\n--- Ошибки ---");\n        checkAccess("user1", "doc999", "read");\n        checkAccess("unknown", "doc1", "read");\n    }\n    \n    static void createDocument(String id, String ownerId, String title, boolean isPublic) {\n        Map<String, Object> doc = new HashMap<>();\n        doc.put("ownerId", ownerId);\n        doc.put("title", title);\n        doc.put("isPublic", isPublic);\n        documents.put(id, doc);\n    }\n    \n    static void checkAccess(String userId, String docId, String action) {\n        String role = userRoles.get(userId);\n        if (role == null) {\n            System.out.println(userId + " -> " + action + " " + docId + ": ОТКАЗ (пользователь не найден)");\n            return;\n        }\n        \n        Map<String, Object> doc = documents.get(docId);\n        if (doc == null) {\n            System.out.println(userId + " -> " + action + " " + docId + ": ОТКАЗ (документ не найден)");\n            return;\n        }\n        \n        boolean isOwner = userId.equals(doc.get("ownerId"));\n        boolean isAdmin = "ADMIN".equals(role);\n        boolean isPublic = (boolean) doc.get("isPublic");\n        boolean allowed = false;\n        String reason = "";\n        \n        if (isAdmin) {\n            allowed = true;\n            reason = "ADMIN-доступ";\n        } else if (isOwner) {\n            allowed = true;\n            reason = "владелец";\n        } else if ("read".equals(action) && isPublic) {\n            allowed = true;\n            reason = "публичный документ";\n        } else {\n            reason = isPublic ? "не владелец" : "приватный + не владелец";\n        }\n        \n        System.out.println(userId + " (" + role + ") -> " + action + " " + docId + ": " + (allowed ? "OK (" + reason + ")" : "ОТКАЗ (" + reason + ")"));\n    }\n}',
      explanation: 'Resource-Based Access Control проверяет не только роль пользователя, но и владельца ресурса. Владелец может управлять своим ресурсом, остальные видят только публичные. ADMIN обходит ограничения владельца. Это используется везде: Google Docs (владелец + shared), GitHub (repo owner + collaborators), соцсети (свои посты).'
    },
    {
      id: 4,
      title: 'Управление API ключами',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй систему API-ключей: генерация, проверка, ротация, отзыв. Каждый ключ привязан к клиенту и имеет свои права.',
      requirements: [
        'Создай класс Main с методом main',
        'Генерация API ключа с prefix (pk_ для production, sk_ для staging)',
        'Привязка scopes к ключу (read, write, delete)',
        'Rate limiting для каждого ключа',
        'Ротация (замена) ключа без downtime',
        'Отзыв (revoke) ключа'
      ],
      expectedOutput: 'Ключ pk_xxx создан (scopes: read,write)\nGET /api/data: 200 OK\nDELETE /api/data: 403 (нет scope)\nРотация: старый ключ ещё работает 24ч\nОтзыв: ключ деактивирован',
      hint: 'Создай Map<apiKey, KeyInfo> где KeyInfo = {clientId, scopes, rateLimit, status, expiresAt}.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<String, Map<String, Object>> apiKeys = new LinkedHashMap<>();\n    static int keyCounter = 0;\n    \n    public static void main(String[] args) {\n        System.out.println("=== Управление API ключами ===\\n");\n        \n        // 1. Генерация ключей\n        System.out.println("--- 1. Генерация ---");\n        String pk1 = createKey("client-A", "pk", 100, "read", "write");\n        String sk1 = createKey("client-A", "sk", 1000, "read", "write", "delete");\n        String pk2 = createKey("client-B", "pk", 50, "read");\n        \n        // 2. Использование\n        System.out.println("\\n--- 2. Использование ---");\n        useKey(pk1, "GET", "/api/data", "read");\n        useKey(pk1, "POST", "/api/data", "write");\n        useKey(pk1, "DELETE", "/api/data", "delete"); // нет scope\n        useKey(sk1, "DELETE", "/api/data", "delete"); // ok\n        useKey(pk2, "GET", "/api/data", "read");\n        useKey("invalid_key", "GET", "/api/data", "read");\n        \n        // 3. Rate limiting\n        System.out.println("\\n--- 3. Rate Limiting (pk2: лимит 50) ---");\n        Map<String, Object> keyInfo = apiKeys.get(pk2);\n        keyInfo.put("requestCount", 49);\n        useKey(pk2, "GET", "/api/data", "read"); // 50/50\n        useKey(pk2, "GET", "/api/data", "read"); // превышен\n        \n        // 4. Ротация\n        System.out.println("\\n--- 4. Ротация ключа ---");\n        String pk1New = rotateKey(pk1);\n        useKey(pk1, "GET", "/api/data", "read"); // старый ещё работает\n        useKey(pk1New, "GET", "/api/data", "read"); // новый тоже\n        \n        // 5. Отзыв\n        System.out.println("\\n--- 5. Отзыв ключа ---");\n        revokeKey(pk1);\n        useKey(pk1, "GET", "/api/data", "read"); // отозван\n        useKey(pk1New, "GET", "/api/data", "read"); // новый работает\n        \n        // 6. Список ключей\n        System.out.println("\\n--- 6. Все ключи ---");\n        for (Map.Entry<String, Map<String, Object>> e : apiKeys.entrySet()) {\n            Map<String, Object> info = e.getValue();\n            String display = e.getKey().substring(0, Math.min(15, e.getKey().length())) + "...";\n            System.out.println("  " + display + " | client=" + info.get("clientId") +\n                " | status=" + info.get("status") + " | scopes=" + info.get("scopes"));\n        }\n    }\n    \n    @SuppressWarnings("unchecked")\n    static String createKey(String clientId, String prefix, int rateLimit, String... scopes) {\n        String key = prefix + "_" + UUID.randomUUID().toString().replace("-", "").substring(0, 24);\n        Map<String, Object> info = new HashMap<>();\n        info.put("clientId", clientId);\n        info.put("scopes", new LinkedHashSet<>(Arrays.asList(scopes)));\n        info.put("rateLimit", rateLimit);\n        info.put("requestCount", 0);\n        info.put("status", "ACTIVE");\n        info.put("createdAt", System.currentTimeMillis());\n        apiKeys.put(key, info);\n        System.out.println("Создан: " + key.substring(0, 15) + "... (client=" + clientId + ", scopes=" + Arrays.asList(scopes) + ", limit=" + rateLimit + ")");\n        return key;\n    }\n    \n    @SuppressWarnings("unchecked")\n    static void useKey(String key, String method, String path, String requiredScope) {\n        Map<String, Object> info = apiKeys.get(key);\n        String prefix = method + " " + path + ": ";\n        \n        if (info == null) {\n            System.out.println(prefix + "401 (невалидный ключ)");\n            return;\n        }\n        if (!"ACTIVE".equals(info.get("status")) && !"ROTATING".equals(info.get("status"))) {\n            System.out.println(prefix + "401 (ключ " + info.get("status") + ")");\n            return;\n        }\n        Set<String> scopes = (Set<String>) info.get("scopes");\n        if (!scopes.contains(requiredScope)) {\n            System.out.println(prefix + "403 (нет scope: " + requiredScope + ")");\n            return;\n        }\n        int count = (int) info.get("requestCount");\n        int limit = (int) info.get("rateLimit");\n        if (count >= limit) {\n            System.out.println(prefix + "429 (лимит " + limit + " исчерпан)");\n            return;\n        }\n        info.put("requestCount", count + 1);\n        System.out.println(prefix + "200 OK (" + (count + 1) + "/" + limit + ")");\n    }\n    \n    @SuppressWarnings("unchecked")\n    static String rotateKey(String oldKey) {\n        Map<String, Object> oldInfo = apiKeys.get(oldKey);\n        if (oldInfo == null) { System.out.println("Ротация: ключ не найден"); return null; }\n        \n        // Старый ключ помечается как ROTATING (работает ещё 24ч)\n        oldInfo.put("status", "ROTATING");\n        \n        // Создаём новый ключ с теми же параметрами\n        String prefix = oldKey.substring(0, 2);\n        Set<String> scopes = (Set<String>) oldInfo.get("scopes");\n        int limit = (int) oldInfo.get("rateLimit");\n        String clientId = (String) oldInfo.get("clientId");\n        \n        String newKey = createKey(clientId, prefix, limit, scopes.toArray(new String[0]));\n        System.out.println("Ротация: старый ключ работает ещё 24ч, новый ключ активен");\n        return newKey;\n    }\n    \n    static void revokeKey(String key) {\n        Map<String, Object> info = apiKeys.get(key);\n        if (info == null) { System.out.println("Отзыв: ключ не найден"); return; }\n        info.put("status", "REVOKED");\n        System.out.println("Отзыв: ключ " + key.substring(0, 15) + "... деактивирован");\n    }\n}',
      explanation: 'Система API-ключей включает: генерацию с prefix (pk_ / sk_), привязку scopes, rate limiting, ротацию (замена без downtime -- старый ключ работает ещё 24ч) и отзыв. Это стандартная модель Stripe, OpenAI, SendGrid: у каждого клиента свой набор ключей с разными правами и лимитами.'
    },
    {
      id: 5,
      title: 'Scope-Based Authorization',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй авторизацию на основе scopes (как в OAuth 2.0): каждый токен имеет список scopes, API проверяет наличие нужного scope.',
      requirements: [
        'Создай класс Main с методом main',
        'Определи scopes: user:read, user:write, posts:read, posts:write, admin:all',
        'Выдача токена с запрошенными scopes (consent)',
        'Проверка scope при каждом запросе к API',
        'Wildcard scope: admin:all даёт доступ ко всему',
        'Покажи: запрос scopes, проверка, недостаточные scopes'
      ],
      expectedOutput: 'Токен: scopes=[user:read, posts:read]\nGET /api/users: OK (scope user:read)\nPOST /api/users: 403 (требуется user:write)\nAdmin токен: admin:all -> всё разрешено',
      hint: 'Парси scope как "resource:action". Wildcard "admin:all" матчит любой scope.',
      solution: 'import java.util.*;\n\npublic class Main {\n    // Доступные scopes\n    static final Set<String> ALL_SCOPES = new LinkedHashSet<>(Arrays.asList(\n        "user:read", "user:write", "posts:read", "posts:write",\n        "comments:read", "comments:write", "admin:all"\n    ));\n    \n    // Требуемые scopes для каждого endpoint\n    static Map<String, String> endpointScopes = new LinkedHashMap<>();\n    static Map<String, Set<String>> tokens = new LinkedHashMap<>();\n    static int tokenSeq = 0;\n    \n    public static void main(String[] args) {\n        // Настройка endpoints\n        endpointScopes.put("GET /api/users", "user:read");\n        endpointScopes.put("POST /api/users", "user:write");\n        endpointScopes.put("GET /api/posts", "posts:read");\n        endpointScopes.put("POST /api/posts", "posts:write");\n        endpointScopes.put("DELETE /api/posts", "posts:write");\n        endpointScopes.put("GET /api/comments", "comments:read");\n        endpointScopes.put("DELETE /api/admin/users", "admin:all");\n        \n        System.out.println("=== Scope-Based Authorization ===\\n");\n        \n        // 1. Токен с ограниченными scopes (типичный OAuth)\n        System.out.println("--- 1. Ограниченный токен (read-only) ---");\n        String readToken = issueToken("user:read", "posts:read", "comments:read");\n        callAPI(readToken, "GET /api/users");\n        callAPI(readToken, "POST /api/users");\n        callAPI(readToken, "GET /api/posts");\n        callAPI(readToken, "POST /api/posts");\n        \n        // 2. Токен с write scopes\n        System.out.println("\\n--- 2. Токен с write ---");\n        String writeToken = issueToken("user:read", "user:write", "posts:read", "posts:write");\n        callAPI(writeToken, "GET /api/users");\n        callAPI(writeToken, "POST /api/users");\n        callAPI(writeToken, "POST /api/posts");\n        callAPI(writeToken, "DELETE /api/admin/users");\n        \n        // 3. Admin токен (wildcard)\n        System.out.println("\\n--- 3. Admin токен (admin:all) ---");\n        String adminToken = issueToken("admin:all");\n        callAPI(adminToken, "GET /api/users");\n        callAPI(adminToken, "POST /api/users");\n        callAPI(adminToken, "DELETE /api/admin/users");\n        \n        // 4. Consent: запрос scopes\n        System.out.println("\\n--- 4. Consent (запрос scopes) ---");\n        requestConsent("mobile-app", "user:read", "posts:read", "posts:write");\n        requestConsent("admin-tool", "admin:all");\n        \n        // 5. Невалидный токен\n        System.out.println("\\n--- 5. Невалидный токен ---");\n        callAPI("invalid_token", "GET /api/users");\n    }\n    \n    static String issueToken(String... scopes) {\n        String token = "tok_" + Integer.toHexString(++tokenSeq * 4919);\n        Set<String> scopeSet = new LinkedHashSet<>(Arrays.asList(scopes));\n        tokens.put(token, scopeSet);\n        System.out.println("Выдан токен " + token + " scopes=" + scopeSet);\n        return token;\n    }\n    \n    static void callAPI(String token, String endpoint) {\n        Set<String> tokenScopes = tokens.get(token);\n        if (tokenScopes == null) {\n            System.out.println("  " + endpoint + ": 401 (невалидный токен)");\n            return;\n        }\n        \n        String requiredScope = endpointScopes.get(endpoint);\n        if (requiredScope == null) {\n            System.out.println("  " + endpoint + ": 404 (endpoint не найден)");\n            return;\n        }\n        \n        boolean hasScope = checkScope(tokenScopes, requiredScope);\n        if (hasScope) {\n            System.out.println("  " + endpoint + ": 200 OK (scope: " + requiredScope + ")");\n        } else {\n            System.out.println("  " + endpoint + ": 403 Forbidden (требуется: " + requiredScope + ", есть: " + tokenScopes + ")");\n        }\n    }\n    \n    static boolean checkScope(Set<String> tokenScopes, String required) {\n        // Прямое совпадение\n        if (tokenScopes.contains(required)) return true;\n        \n        // Wildcard: admin:all даёт доступ ко всему\n        if (tokenScopes.contains("admin:all")) return true;\n        \n        // Resource wildcard: user:* даёт user:read + user:write\n        String resource = required.split(":")[0];\n        if (tokenScopes.contains(resource + ":*")) return true;\n        \n        return false;\n    }\n    \n    static void requestConsent(String app, String... requestedScopes) {\n        System.out.println("Приложение \\"" + app + "\\" запрашивает доступ:");\n        for (String scope : requestedScopes) {\n            String desc = describeScopeRu(scope);\n            System.out.println("  [" + (ALL_SCOPES.contains(scope) ? "v" : "?") + "] " + scope + " -- " + desc);\n        }\n        System.out.println("  -> Пользователь подтверждает / отклоняет");\n    }\n    \n    static String describeScopeRu(String scope) {\n        switch (scope) {\n            case "user:read": return "Чтение профиля";\n            case "user:write": return "Изменение профиля";\n            case "posts:read": return "Чтение постов";\n            case "posts:write": return "Создание и удаление постов";\n            case "comments:read": return "Чтение комментариев";\n            case "comments:write": return "Написание комментариев";\n            case "admin:all": return "Полный доступ (администратор)";\n            default: return "Неизвестный scope";\n        }\n    }\n}',
      explanation: 'Scope-Based Authorization: каждый токен содержит список scopes (разрешений). API endpoint требует конкретный scope. Токен с user:read не может вызвать POST /api/users (нужен user:write). Wildcard admin:all даёт доступ ко всему. Это модель OAuth 2.0: GitHub, Google, Facebook -- все используют scopes для ограничения доступа приложений.'
    },
    {
      id: 6,
      title: 'Policy-Based Access Control',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуй PBAC (Policy-Based Access Control) -- гибкую систему правил доступа с условиями (время, IP, атрибуты).',
      requirements: [
        'Создай класс Main с методом main',
        'Политика = набор правил: кто, что, при каких условиях',
        'Условия: время суток, IP-адрес, роль, владелец ресурса',
        'Комбинация правил: AND (все условия), OR (любое)',
        'Покажи: доступ по времени, по IP, по комбинации условий'
      ],
      expectedOutput: 'Policy "работа": admin + 9:00-18:00 + office IP -> OK\nPolicy "работа": admin + 22:00 + office IP -> ОТКАЗ (не рабочее время)\nPolicy "anywhere": admin + любое время + любой IP -> OK',
      hint: 'Создай интерфейс Condition (check), реализуй: TimeCondition, IpCondition, RoleCondition. Policy содержит List<Condition>.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static List<Map<String, Object>> policies = new ArrayList<>();\n    \n    public static void main(String[] args) {\n        System.out.println("=== Policy-Based Access Control ===\\n");\n        \n        // Определяем политики\n        addPolicy("office-hours", "Доступ в рабочее время из офиса",\n            "ALLOW", Arrays.asList("ADMIN", "MANAGER", "EDITOR"),\n            "write", 9, 18, Arrays.asList("10.0.0.0/24", "192.168.1.0/24"));\n        \n        addPolicy("read-anywhere", "Чтение отовсюду в любое время",\n            "ALLOW", Arrays.asList("ADMIN", "MANAGER", "EDITOR", "VIEWER"),\n            "read", 0, 24, Arrays.asList("*"));\n        \n        addPolicy("admin-full", "Админ: полный доступ всегда",\n            "ALLOW", Arrays.asList("ADMIN"),\n            "any", 0, 24, Arrays.asList("*"));\n        \n        addPolicy("block-external", "Блокировка записи извне",\n            "DENY", Arrays.asList("EDITOR", "VIEWER"),\n            "write", 0, 24, Arrays.asList("external"));\n        \n        // Выводим политики\n        System.out.println("Политики:");\n        for (Map<String, Object> p : policies) {\n            System.out.println("  [" + p.get("effect") + "] " + p.get("name") + ": " + p.get("description"));\n        }\n        \n        // Проверка доступа\n        System.out.println("\\n=== Проверка доступа ===\\n");\n        \n        evaluate("alice", "ADMIN", "write", 10, "10.0.0.5");\n        evaluate("alice", "ADMIN", "write", 22, "10.0.0.5");\n        evaluate("alice", "ADMIN", "write", 22, "85.90.1.100"); // admin: всегда\n        \n        System.out.println();\n        evaluate("bob", "MANAGER", "write", 14, "10.0.0.10");\n        evaluate("bob", "MANAGER", "write", 21, "10.0.0.10");\n        evaluate("bob", "MANAGER", "read", 21, "85.90.1.100");\n        \n        System.out.println();\n        evaluate("charlie", "EDITOR", "write", 11, "10.0.0.15");\n        evaluate("charlie", "EDITOR", "write", 11, "85.90.1.100"); // external\n        evaluate("charlie", "EDITOR", "read", 23, "85.90.1.100");\n        \n        System.out.println();\n        evaluate("dave", "VIEWER", "read", 15, "192.168.1.5");\n        evaluate("dave", "VIEWER", "write", 15, "10.0.0.20");\n    }\n    \n    @SuppressWarnings("unchecked")\n    static void addPolicy(String name, String description, String effect,\n                           List<String> roles, String action, int hourFrom, int hourTo, List<String> allowedIps) {\n        Map<String, Object> policy = new LinkedHashMap<>();\n        policy.put("name", name);\n        policy.put("description", description);\n        policy.put("effect", effect);\n        policy.put("roles", new HashSet<>(roles));\n        policy.put("action", action);\n        policy.put("hourFrom", hourFrom);\n        policy.put("hourTo", hourTo);\n        policy.put("allowedIps", new HashSet<>(allowedIps));\n        policies.add(policy);\n    }\n    \n    @SuppressWarnings("unchecked")\n    static void evaluate(String user, String role, String action, int hour, String ip) {\n        String result = "DENY"; // default deny\n        String matchedPolicy = "default";\n        \n        for (Map<String, Object> policy : policies) {\n            Set<String> roles = (Set<String>) policy.get("roles");\n            String pAction = (String) policy.get("action");\n            int hourFrom = (int) policy.get("hourFrom");\n            int hourTo = (int) policy.get("hourTo");\n            Set<String> allowedIps = (Set<String>) policy.get("allowedIps");\n            String effect = (String) policy.get("effect");\n            \n            // Проверяем роль\n            if (!roles.contains(role)) continue;\n            \n            // Проверяем действие\n            if (!"any".equals(pAction) && !pAction.equals(action)) continue;\n            \n            // Проверяем время\n            if (hour < hourFrom || hour >= hourTo) continue;\n            \n            // Проверяем IP\n            if (!allowedIps.contains("*") && !matchIp(ip, allowedIps)) {\n                if ("DENY".equals(effect)) { result = "DENY"; matchedPolicy = (String) policy.get("name"); }\n                continue;\n            }\n            \n            // Политика сработала\n            if ("DENY".equals(effect)) {\n                result = "DENY";\n                matchedPolicy = (String) policy.get("name");\n                break; // DENY имеет приоритет\n            }\n            result = "ALLOW";\n            matchedPolicy = (String) policy.get("name");\n        }\n        \n        System.out.println(user + " (" + role + ") " + action + " @ " + hour + ":00 from " + ip +\n            " -> " + result + " [policy: " + matchedPolicy + "]");\n    }\n    \n    static boolean matchIp(String ip, Set<String> patterns) {\n        for (String pattern : patterns) {\n            if ("external".equals(pattern)) {\n                if (!ip.startsWith("10.") && !ip.startsWith("192.168.")) return true;\n            } else if (pattern.contains("/")) {\n                String prefix = pattern.substring(0, pattern.lastIndexOf(\'.\'));\n                if (ip.startsWith(prefix)) return true;\n            } else if (pattern.equals(ip)) {\n                return true;\n            }\n        }\n        return false;\n    }\n}',
      explanation: 'PBAC (Policy-Based Access Control) -- гибче RBAC. Политика проверяет не только роль, но и условия: время суток (рабочие часы), IP (офисная сеть), действие (read/write). DENY имеет приоритет над ALLOW. Это подход AWS IAM, Google Cloud IAM, Azure RBAC -- у них политики могут проверять любые атрибуты запроса.'
    },
    {
      id: 7,
      title: 'Multi-Tenant Authorization',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуй авторизацию для multi-tenant приложения: каждый tenant (организация) изолирован, пользователь видит только данные своего tenant.',
      requirements: [
        'Создай класс Main с методом main',
        'Tenant = организация (компания)',
        'Пользователь принадлежит одному tenant',
        'Роли внутри tenant: OWNER, ADMIN, MEMBER',
        'Данные изолированы: пользователь не видит данные чужого tenant',
        'Super Admin видит все tenant (платформенный admin)'
      ],
      expectedOutput: 'user1 (tenant-A, ADMIN) -> read data-A: OK\nuser1 (tenant-A, ADMIN) -> read data-B: ОТКАЗ (чужой tenant)\nsuper-admin -> read data-B: OK (super admin)',
      hint: 'Каждый ресурс имеет tenantId. При проверке сравнивай user.tenantId === resource.tenantId.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<String, Map<String, Object>> users = new LinkedHashMap<>();\n    static Map<String, Map<String, Object>> resources = new LinkedHashMap<>();\n    static Map<String, Set<String>> tenantRolePermissions = new LinkedHashMap<>();\n    \n    public static void main(String[] args) {\n        // Права для tenant-ролей\n        tenantRolePermissions.put("OWNER", new HashSet<>(Arrays.asList("read", "write", "delete", "manage_members", "billing")));\n        tenantRolePermissions.put("ADMIN", new HashSet<>(Arrays.asList("read", "write", "delete", "manage_members")));\n        tenantRolePermissions.put("MEMBER", new HashSet<>(Arrays.asList("read", "write")));\n        tenantRolePermissions.put("VIEWER", new HashSet<>(Arrays.asList("read")));\n        tenantRolePermissions.put("SUPER_ADMIN", new HashSet<>(Arrays.asList("read", "write", "delete", "manage_members", "billing", "platform_admin")));\n        \n        // Пользователи\n        addUser("alice", "tenant-A", "OWNER");\n        addUser("bob", "tenant-A", "ADMIN");\n        addUser("charlie", "tenant-A", "MEMBER");\n        addUser("diana", "tenant-B", "OWNER");\n        addUser("eve", "tenant-B", "MEMBER");\n        addUser("super", null, "SUPER_ADMIN"); // платформенный админ\n        \n        // Ресурсы\n        addResource("proj-1", "tenant-A", "Проект Alpha");\n        addResource("proj-2", "tenant-A", "Проект Beta");\n        addResource("proj-3", "tenant-B", "Проект Gamma");\n        addResource("proj-4", "tenant-B", "Проект Delta");\n        \n        System.out.println("=== Multi-Tenant Authorization ===\\n");\n        System.out.println("Tenants:");\n        System.out.println("  tenant-A: alice(OWNER), bob(ADMIN), charlie(MEMBER)");\n        System.out.println("  tenant-B: diana(OWNER), eve(MEMBER)");\n        System.out.println("  platform: super(SUPER_ADMIN)\\n");\n        \n        // Доступ внутри своего tenant\n        System.out.println("--- Доступ внутри tenant ---");\n        checkAccess("alice", "proj-1", "read");\n        checkAccess("alice", "proj-1", "delete");\n        checkAccess("charlie", "proj-1", "read");\n        checkAccess("charlie", "proj-1", "delete"); // MEMBER нет delete\n        \n        // Изоляция: доступ к чужому tenant\n        System.out.println("\\n--- Изоляция tenant ---");\n        checkAccess("alice", "proj-3", "read"); // чужой tenant\n        checkAccess("bob", "proj-4", "write"); // чужой tenant\n        checkAccess("eve", "proj-1", "read"); // чужой tenant\n        \n        // Super Admin\n        System.out.println("\\n--- Super Admin ---");\n        checkAccess("super", "proj-1", "read");\n        checkAccess("super", "proj-3", "read");\n        checkAccess("super", "proj-3", "delete");\n        \n        // Управление участниками\n        System.out.println("\\n--- Управление участниками ---");\n        checkAccess("alice", "proj-1", "manage_members"); // OWNER: да\n        checkAccess("bob", "proj-1", "manage_members"); // ADMIN: да\n        checkAccess("charlie", "proj-1", "manage_members"); // MEMBER: нет\n        checkAccess("alice", "proj-1", "billing"); // OWNER: да\n        checkAccess("bob", "proj-1", "billing"); // ADMIN: нет\n    }\n    \n    static void addUser(String id, String tenantId, String role) {\n        Map<String, Object> user = new HashMap<>();\n        user.put("tenantId", tenantId);\n        user.put("role", role);\n        users.put(id, user);\n    }\n    \n    static void addResource(String id, String tenantId, String name) {\n        Map<String, Object> res = new HashMap<>();\n        res.put("tenantId", tenantId);\n        res.put("name", name);\n        resources.put(id, res);\n    }\n    \n    static void checkAccess(String userId, String resourceId, String action) {\n        Map<String, Object> user = users.get(userId);\n        Map<String, Object> resource = resources.get(resourceId);\n        \n        if (user == null) { System.out.println(userId + ": пользователь не найден"); return; }\n        if (resource == null) { System.out.println(resourceId + ": ресурс не найден"); return; }\n        \n        String userTenant = (String) user.get("tenantId");\n        String resTenant = (String) resource.get("tenantId");\n        String role = (String) user.get("role");\n        \n        // Super Admin -- доступ ко всему\n        if ("SUPER_ADMIN".equals(role)) {\n            System.out.println(userId + " (SUPER_ADMIN) -> " + action + " " + resourceId + ": OK (платформенный админ)");\n            return;\n        }\n        \n        // Проверка tenant\n        if (!Objects.equals(userTenant, resTenant)) {\n            System.out.println(userId + " (" + userTenant + ", " + role + ") -> " + action + " " + resourceId + \n                " (" + resTenant + "): ОТКАЗ (чужой tenant)");\n            return;\n        }\n        \n        // Проверка прав роли внутри tenant\n        Set<String> perms = tenantRolePermissions.getOrDefault(role, Collections.emptySet());\n        boolean allowed = perms.contains(action);\n        System.out.println(userId + " (" + userTenant + ", " + role + ") -> " + action + " " + resourceId +\n            ": " + (allowed ? "OK" : "ОТКАЗ (нет права " + action + ")"));\n    }\n}',
      explanation: 'Multi-Tenant: каждый tenant (организация) полностью изолирован. Пользователь tenant-A не может видеть данные tenant-B. Роли (OWNER, ADMIN, MEMBER) работают ВНУТРИ tenant. Super Admin -- платформенный администратор с доступом ко всем tenant. Это архитектура Slack, Jira, Notion: каждый workspace -- отдельный tenant.'
    },
    {
      id: 8,
      title: 'Permission Caching',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй кеширование прав доступа для ускорения проверок. Права вычисляются один раз и кешируются с TTL.',
      requirements: [
        'Создай класс Main с методом main',
        'Кеш: userId + action -> boolean (результат проверки)',
        'TTL: 5 минут для кеша',
        'Cache hit / miss статистика',
        'Инвалидация кеша при изменении роли',
        'Покажи разницу: с кешем и без (количество вычислений)'
      ],
      expectedOutput: 'Без кеша: 1000 проверок = 1000 вычислений\nС кешем: 1000 проверок = 10 вычислений + 990 cache hits\nИзменение роли -> кеш инвалидирован',
      hint: 'Map<String, CacheEntry> где CacheEntry = {result, expiresAt}. Ключ = userId:action.',
      solution: 'import java.util.*;\n\npublic class Main {\n    // RBAC\n    static Map<String, String> userRoles = new HashMap<>();\n    static Map<String, Set<String>> rolePerms = new HashMap<>();\n    \n    // Кеш\n    static Map<String, Map<String, Object>> cache = new LinkedHashMap<>();\n    static final long CACHE_TTL = 5 * 60 * 1000; // 5 минут\n    static int computeCount = 0;\n    static int cacheHits = 0;\n    static int cacheMisses = 0;\n    \n    public static void main(String[] args) {\n        // Настройка\n        rolePerms.put("ADMIN", new HashSet<>(Arrays.asList("read", "write", "delete")));\n        rolePerms.put("USER", new HashSet<>(Arrays.asList("read", "write")));\n        rolePerms.put("VIEWER", new HashSet<>(Arrays.asList("read")));\n        \n        userRoles.put("alice", "ADMIN");\n        userRoles.put("bob", "USER");\n        userRoles.put("charlie", "VIEWER");\n        \n        System.out.println("=== Permission Caching ===\\n");\n        \n        // 1. Без кеша\n        System.out.println("--- 1. Без кеша ---");\n        computeCount = 0;\n        String[] actions = {"read", "write", "delete"};\n        for (int i = 0; i < 100; i++) {\n            for (String action : actions) {\n                checkPermissionNoCache("alice", action);\n            }\n        }\n        System.out.println("300 проверок = " + computeCount + " вычислений\\n");\n        \n        // 2. С кешем\n        System.out.println("--- 2. С кешем ---");\n        computeCount = 0;\n        cacheHits = 0;\n        cacheMisses = 0;\n        for (int i = 0; i < 100; i++) {\n            for (String action : actions) {\n                checkPermissionCached("alice", action);\n            }\n        }\n        System.out.println("300 проверок: " + computeCount + " вычислений, " + cacheHits + " cache hits, " + cacheMisses + " cache misses");\n        \n        // 3. Разные пользователи\n        System.out.println("\\n--- 3. Разные пользователи ---");\n        resetStats();\n        String[] users = {"alice", "bob", "charlie"};\n        for (int i = 0; i < 50; i++) {\n            for (String user : users) {\n                for (String action : actions) {\n                    checkPermissionCached(user, action);\n                }\n            }\n        }\n        System.out.println("450 проверок: " + computeCount + " вычислений, " + cacheHits + " hits, " + cacheMisses + " misses");\n        \n        // 4. Инвалидация при изменении роли\n        System.out.println("\\n--- 4. Инвалидация кеша ---");\n        System.out.println("Кеш до изменения: " + cache.size() + " записей");\n        \n        System.out.println("Меняем роль bob: USER -> ADMIN");\n        userRoles.put("bob", "ADMIN");\n        invalidateCache("bob");\n        \n        System.out.println("Кеш после инвалидации: " + cache.size() + " записей");\n        \n        resetStats();\n        checkPermissionCached("bob", "delete"); // был MISS (кеш очищен)\n        checkPermissionCached("bob", "delete"); // HIT\n        checkPermissionCached("alice", "read"); // HIT (не инвалидирован)\n        System.out.println("Вычислений: " + computeCount + ", hits: " + cacheHits + ", misses: " + cacheMisses);\n        \n        // 5. TTL\n        System.out.println("\\n--- 5. TTL (имитация просрочки) ---");\n        // Искусственно просрочиваем кеш alice\n        for (Map.Entry<String, Map<String, Object>> e : cache.entrySet()) {\n            if (e.getKey().startsWith("alice:")) {\n                e.getValue().put("expiresAt", System.currentTimeMillis() - 1000);\n            }\n        }\n        resetStats();\n        checkPermissionCached("alice", "read"); // MISS (просрочен)\n        checkPermissionCached("alice", "read"); // HIT (новый кеш)\n        System.out.println("Вычислений: " + computeCount + ", hits: " + cacheHits);\n    }\n    \n    static boolean checkPermissionNoCache(String user, String action) {\n        computeCount++;\n        String role = userRoles.get(user);\n        if (role == null) return false;\n        return rolePerms.getOrDefault(role, Collections.emptySet()).contains(action);\n    }\n    \n    static boolean checkPermissionCached(String user, String action) {\n        String cacheKey = user + ":" + action;\n        Map<String, Object> entry = cache.get(cacheKey);\n        \n        // Cache hit?\n        if (entry != null && System.currentTimeMillis() < (long) entry.get("expiresAt")) {\n            cacheHits++;\n            return (boolean) entry.get("result");\n        }\n        \n        // Cache miss -- вычисляем\n        cacheMisses++;\n        boolean result = checkPermissionNoCache(user, action);\n        \n        // Сохраняем в кеш\n        Map<String, Object> newEntry = new HashMap<>();\n        newEntry.put("result", result);\n        newEntry.put("expiresAt", System.currentTimeMillis() + CACHE_TTL);\n        cache.put(cacheKey, newEntry);\n        \n        return result;\n    }\n    \n    static void invalidateCache(String user) {\n        cache.entrySet().removeIf(e -> e.getKey().startsWith(user + ":"));\n        System.out.println("Кеш для " + user + " инвалидирован");\n    }\n    \n    static void resetStats() {\n        computeCount = 0;\n        cacheHits = 0;\n        cacheMisses = 0;\n    }\n}',
      explanation: 'Кеширование прав ускоряет авторизацию: вместо вычисления прав при каждом запросе (запрос к БД, обход иерархии), результат сохраняется в кеше с TTL (5 мин). При изменении роли кеш инвалидируется. В production используют Redis для кеша прав. 300 запросов = 3 вычисления + 297 cache hits -- огромная экономия.'
    },
    {
      id: 9,
      title: 'Access Control List (ACL)',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуй ACL -- гранулярный контроль доступа к каждому ресурсу с explicit allow/deny для каждого пользователя.',
      requirements: [
        'Создай класс Main с методом main',
        'ACL для каждого ресурса: список записей (userId, permission, allow/deny)',
        'DENY имеет приоритет над ALLOW',
        'Поддержка группового доступа (group)',
        'Наследование ACL от родительского ресурса (папка -> файл)',
        'Покажи: прямой доступ, групповой, наследование, deny override'
      ],
      expectedOutput: 'user1 -> read file1: OK (direct ACL)\nuser2 -> read file1: OK (group: editors)\nuser3 -> write file1: DENY (explicit deny)\nuser1 -> read file2: OK (inherited from folder1)',
      hint: 'ACL = List<ACLEntry> где ACLEntry = {subjectType(user/group), subjectId, permission, effect(allow/deny)}.',
      solution: 'import java.util.*;\n\npublic class Main {\n    // ACL для каждого ресурса\n    static Map<String, List<Map<String, String>>> resourceACLs = new LinkedHashMap<>();\n    // Группы\n    static Map<String, Set<String>> groups = new LinkedHashMap<>();\n    // Иерархия ресурсов (child -> parent)\n    static Map<String, String> resourceParent = new LinkedHashMap<>();\n    \n    public static void main(String[] args) {\n        // Группы\n        groups.put("editors", new HashSet<>(Arrays.asList("user1", "user2")));\n        groups.put("viewers", new HashSet<>(Arrays.asList("user3", "user4")));\n        groups.put("managers", new HashSet<>(Arrays.asList("user1", "user5")));\n        \n        // Ресурсы и ACL\n        // folder1\n        addACL("folder1", "group", "editors", "read", "ALLOW");\n        addACL("folder1", "group", "editors", "write", "ALLOW");\n        addACL("folder1", "group", "viewers", "read", "ALLOW");\n        \n        // file1 (в folder1)\n        resourceParent.put("file1", "folder1");\n        addACL("file1", "user", "user1", "delete", "ALLOW"); // дополнительное право\n        addACL("file1", "user", "user3", "write", "DENY"); // явный запрет\n        \n        // file2 (в folder1) -- наследует ACL от folder1\n        resourceParent.put("file2", "folder1");\n        \n        // file3 -- отдельный файл\n        addACL("file3", "user", "user5", "read", "ALLOW");\n        addACL("file3", "user", "user5", "write", "ALLOW");\n        \n        System.out.println("=== Access Control List (ACL) ===\\n");\n        \n        // Показываем ACL\n        System.out.println("ACL:");\n        for (Map.Entry<String, List<Map<String, String>>> e : resourceACLs.entrySet()) {\n            System.out.println("  " + e.getKey() + ":");\n            for (Map<String, String> acl : e.getValue()) {\n                System.out.println("    " + acl.get("effect") + " " + acl.get("subjectType") + ":" + acl.get("subjectId") + " -> " + acl.get("permission"));\n            }\n        }\n        System.out.println("  file2: (наследует от folder1)");\n        \n        // Проверки\n        System.out.println("\\n--- Прямой доступ ---");\n        checkAccess("user1", "file1", "read");\n        checkAccess("user1", "file1", "delete");\n        \n        System.out.println("\\n--- Групповой доступ ---");\n        checkAccess("user2", "file1", "read"); // через группу editors\n        checkAccess("user4", "file1", "read"); // через группу viewers\n        \n        System.out.println("\\n--- DENY override ---");\n        checkAccess("user3", "file1", "read"); // viewers -> ALLOW\n        checkAccess("user3", "file1", "write"); // explicit DENY\n        \n        System.out.println("\\n--- Наследование ---");\n        checkAccess("user1", "file2", "read"); // наследует от folder1\n        checkAccess("user1", "file2", "write"); // наследует от folder1\n        checkAccess("user4", "file2", "read"); // viewers через folder1\n        \n        System.out.println("\\n--- Нет доступа ---");\n        checkAccess("user5", "file1", "read"); // не в ACL file1 и не в группе\n        checkAccess("user3", "file3", "read"); // не в ACL file3\n    }\n    \n    static void addACL(String resourceId, String subjectType, String subjectId, String permission, String effect) {\n        List<Map<String, String>> acls = resourceACLs.computeIfAbsent(resourceId, k -> new ArrayList<>());\n        Map<String, String> entry = new LinkedHashMap<>();\n        entry.put("subjectType", subjectType);\n        entry.put("subjectId", subjectId);\n        entry.put("permission", permission);\n        entry.put("effect", effect);\n        acls.add(entry);\n    }\n    \n    static void checkAccess(String userId, String resourceId, String permission) {\n        String result = evaluateACL(userId, resourceId, permission, new ArrayList<>());\n        System.out.println(userId + " -> " + permission + " " + resourceId + ": " + result);\n    }\n    \n    static String evaluateACL(String userId, String resourceId, String permission, List<String> trace) {\n        List<Map<String, String>> acls = resourceACLs.getOrDefault(resourceId, Collections.emptyList());\n        \n        Boolean directResult = null;\n        boolean hasDeny = false;\n        boolean hasAllow = false;\n        \n        for (Map<String, String> acl : acls) {\n            if (!acl.get("permission").equals(permission)) continue;\n            \n            boolean matches = false;\n            String matchReason = "";\n            \n            if ("user".equals(acl.get("subjectType")) && userId.equals(acl.get("subjectId"))) {\n                matches = true;\n                matchReason = "direct";\n            } else if ("group".equals(acl.get("subjectType"))) {\n                Set<String> members = groups.getOrDefault(acl.get("subjectId"), Collections.emptySet());\n                if (members.contains(userId)) {\n                    matches = true;\n                    matchReason = "group:" + acl.get("subjectId");\n                }\n            }\n            \n            if (matches) {\n                if ("DENY".equals(acl.get("effect"))) {\n                    hasDeny = true;\n                    trace.add("DENY@" + resourceId + "(" + matchReason + ")");\n                } else {\n                    hasAllow = true;\n                    trace.add("ALLOW@" + resourceId + "(" + matchReason + ")");\n                }\n            }\n        }\n        \n        // DENY приоритет\n        if (hasDeny) return "ОТКАЗ (explicit DENY) " + trace;\n        if (hasAllow) return "OK " + trace;\n        \n        // Наследование от родителя\n        String parent = resourceParent.get(resourceId);\n        if (parent != null) {\n            trace.add("inherit:" + parent);\n            return evaluateACL(userId, parent, permission, trace);\n        }\n        \n        return "ОТКАЗ (нет ACL) " + trace;\n    }\n}',
      explanation: 'ACL (Access Control List) -- каждый ресурс имеет свой список прав. Записи могут быть для пользователя или группы, ALLOW или DENY. DENY всегда имеет приоритет. Если у ресурса нет ACL-записи, проверяется родительский ресурс (наследование). Это модель файловых систем (NTFS, ext4), Google Drive, SharePoint.'
    },
    {
      id: 10,
      title: 'Authorization Middleware',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуй цепочку middleware для авторизации: каждый middleware проверяет свой аспект (authentication, rate limit, role, scope, resource ownership).',
      requirements: [
        'Создай класс Main с методом main',
        'Цепочка middleware: Auth -> RateLimit -> Role -> Scope -> Owner',
        'Каждый middleware может пропустить или заблокировать запрос',
        'Если один middleware блокирует -- следующие не выполняются',
        'Логирование: какие middleware пройдены, где заблокировано',
        'Покажи запросы, проходящие разное количество middleware'
      ],
      expectedOutput: 'GET /api/users: Auth OK -> Rate OK -> Role OK -> 200\nGET /api/admin: Auth OK -> Rate OK -> Role DENY (нет роли ADMIN)\nPOST /api/data: Auth OK -> Rate DENY (лимит исчерпан)',
      hint: 'Создай интерфейс Middleware с методом process(Request). Каждый middleware вызывает next.process() или возвращает ошибку.',
      solution: 'import java.util.*;\n\npublic class Main {\n    // "База данных"\n    static Map<String, Map<String, Object>> tokens = new HashMap<>();\n    static Map<String, Integer> rateLimits = new HashMap<>();\n    static final int RATE_LIMIT = 5;\n    \n    public static void main(String[] args) {\n        // Токены\n        createToken("tok-admin", "admin", "ADMIN", new HashSet<>(Arrays.asList("read", "write", "delete")));\n        createToken("tok-user", "user1", "USER", new HashSet<>(Arrays.asList("read", "write")));\n        createToken("tok-viewer", "viewer1", "VIEWER", new HashSet<>(Arrays.asList("read")));\n        \n        System.out.println("=== Authorization Middleware Chain ===\\n");\n        \n        // Определяем middleware цепочки для разных endpoints\n        System.out.println("--- 1. Успешный запрос (все middleware пройдены) ---");\n        processRequest("tok-admin", "GET", "/api/users", "ADMIN", "read", null);\n        \n        System.out.println("\\n--- 2. Нет аутентификации ---");\n        processRequest(null, "GET", "/api/users", "ADMIN", "read", null);\n        \n        System.out.println("\\n--- 3. Невалидный токен ---");\n        processRequest("invalid-token", "GET", "/api/users", "ADMIN", "read", null);\n        \n        System.out.println("\\n--- 4. Недостаточная роль ---");\n        processRequest("tok-user", "GET", "/api/admin/settings", "ADMIN", "read", null);\n        \n        System.out.println("\\n--- 5. Нет нужного scope ---");\n        processRequest("tok-viewer", "DELETE", "/api/posts/1", "VIEWER", "delete", null);\n        \n        System.out.println("\\n--- 6. Не владелец ресурса ---");\n        processRequest("tok-user", "PUT", "/api/posts/1", "USER", "write", "admin"); // owner=admin\n        processRequest("tok-user", "PUT", "/api/posts/2", "USER", "write", "user1"); // owner=user1\n        \n        System.out.println("\\n--- 7. Rate Limiting ---");\n        for (int i = 1; i <= 7; i++) {\n            processRequest("tok-user", "GET", "/api/data", "USER", "read", null);\n        }\n        \n        System.out.println("\\n--- 8. Admin обходит owner check ---");\n        processRequest("tok-admin", "DELETE", "/api/posts/1", "USER", "delete", "user1");\n    }\n    \n    @SuppressWarnings("unchecked")\n    static void processRequest(String token, String method, String path,\n                                String requiredRole, String requiredScope, String resourceOwner) {\n        List<String> chain = new ArrayList<>();\n        String prefix = method + " " + path + ": ";\n        \n        // Middleware 1: Authentication\n        if (token == null) {\n            chain.add("Auth FAIL");\n            System.out.println(prefix + String.join(" -> ", chain) + " => 401 (нет токена)");\n            return;\n        }\n        Map<String, Object> tokenInfo = tokens.get(token);\n        if (tokenInfo == null) {\n            chain.add("Auth FAIL");\n            System.out.println(prefix + String.join(" -> ", chain) + " => 401 (невалидный токен)");\n            return;\n        }\n        String userId = (String) tokenInfo.get("userId");\n        String userRole = (String) tokenInfo.get("role");\n        chain.add("Auth OK(" + userId + ")");\n        \n        // Middleware 2: Rate Limiting\n        int requests = rateLimits.getOrDefault(userId, 0);\n        if (requests >= RATE_LIMIT) {\n            chain.add("Rate FAIL");\n            System.out.println(prefix + String.join(" -> ", chain) + " => 429 (лимит " + RATE_LIMIT + ")");\n            return;\n        }\n        rateLimits.put(userId, requests + 1);\n        chain.add("Rate OK(" + (requests + 1) + "/" + RATE_LIMIT + ")");\n        \n        // Middleware 3: Role Check\n        int roleLevel = roleLevel(userRole);\n        int requiredLevel = roleLevel(requiredRole);\n        if (roleLevel < requiredLevel) {\n            chain.add("Role FAIL");\n            System.out.println(prefix + String.join(" -> ", chain) + " => 403 (роль " + userRole + " < " + requiredRole + ")");\n            return;\n        }\n        chain.add("Role OK(" + userRole + ")");\n        \n        // Middleware 4: Scope Check\n        Set<String> scopes = (Set<String>) tokenInfo.get("scopes");\n        if (!scopes.contains(requiredScope)) {\n            chain.add("Scope FAIL");\n            System.out.println(prefix + String.join(" -> ", chain) + " => 403 (нет scope: " + requiredScope + ")");\n            return;\n        }\n        chain.add("Scope OK(" + requiredScope + ")");\n        \n        // Middleware 5: Owner Check (если применимо)\n        if (resourceOwner != null) {\n            boolean isOwner = userId.equals(resourceOwner);\n            boolean isAdmin = "ADMIN".equals(userRole);\n            if (!isOwner && !isAdmin) {\n                chain.add("Owner FAIL");\n                System.out.println(prefix + String.join(" -> ", chain) + " => 403 (не владелец)");\n                return;\n            }\n            chain.add("Owner OK" + (isAdmin ? "(admin)" : ""));\n        }\n        \n        System.out.println(prefix + String.join(" -> ", chain) + " => 200 OK");\n    }\n    \n    static void createToken(String token, String userId, String role, Set<String> scopes) {\n        Map<String, Object> info = new HashMap<>();\n        info.put("userId", userId);\n        info.put("role", role);\n        info.put("scopes", scopes);\n        tokens.put(token, info);\n    }\n    \n    static int roleLevel(String role) {\n        switch (role) {\n            case "ADMIN": return 3;\n            case "USER": return 2;\n            case "VIEWER": return 1;\n            default: return 0;\n        }\n    }\n}',
      explanation: 'Middleware Chain -- каждый middleware проверяет свой аспект: 1) Authentication (валидный токен?), 2) Rate Limiting (не превышен лимит?), 3) Role Check (достаточная роль?), 4) Scope Check (есть нужный scope?), 5) Owner Check (владелец ресурса?). Если один middleware возвращает ошибку -- цепочка прерывается. Это архитектура Express.js, Spring Security, Django -- все используют middleware/filter chain.'
    }
  ]
}
