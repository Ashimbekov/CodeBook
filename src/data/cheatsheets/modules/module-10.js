export default {
  id: 10,
  title: 'Шпаргалка: Kubernetes',
  description: 'kubectl команды, манифесты Pod/Deployment/Service, ConfigMap/Secret/Ingress, отладка и Helm — всё в одном месте.',
  lessons: [
    {
      id: 1,
      title: 'kubectl команды (get/describe/apply/delete/logs)',
      type: 'theory',
      content: [
        { type: 'text', value: 'kubectl — основной CLI для управления Kubernetes кластером. Команды взаимодействуют с API-сервером и управляют всеми ресурсами кластера.' },
        { type: 'code', language: 'bash', value: '# === GET — получить информацию о ресурсах ===\nkubectl get pods\nkubectl get pods -n default          # конкретный namespace\nkubectl get pods -A                  # все namespaces\nkubectl get pods -o wide             # + NODE, IP\nkubectl get pods -o yaml             # полный YAML\nkubectl get pods -o json             # полный JSON\nkubectl get pods -w                  # watch (реальное время)\nkubectl get pods -l app=myapp        # по label\nkubectl get pods --field-selector status.phase=Running\n\nkubectl get deployments\nkubectl get services\nkubectl get nodes\nkubectl get namespaces\nkubectl get configmaps\nkubectl get secrets\nkubectl get ingress\nkubectl get all                      # всё в namespace\nkubectl get all -A                   # всё во всех namespaces\n\n# === DESCRIBE — детальная информация ===\nkubectl describe pod mypod\nkubectl describe pod mypod -n default\nkubectl describe node mynode\nkubectl describe deployment myapp\n\n# === APPLY — применить манифест ===\nkubectl apply -f deployment.yaml\nkubectl apply -f .\n                                     # все .yaml в директории\nkubectl apply -f https://example.com/manifest.yaml\nkubectl apply -k ./kustomize/        # Kustomize\n\n# === DELETE — удалить ресурсы ===\nkubectl delete pod mypod\nkubectl delete -f deployment.yaml\nkubectl delete pod mypod --grace-period=0 --force\nkubectl delete pods -l app=myapp    # по label\nkubectl delete all -l app=myapp     # все ресурсы с label\n\n# === LOGS — логи контейнера ===\nkubectl logs mypod\nkubectl logs mypod -f               # следить\nkubectl logs mypod --tail=100       # последние 100 строк\nkubectl logs mypod --since=1h       # за последний час\nkubectl logs mypod -c mycontainer   # конкретный контейнер (multi-container)\nkubectl logs -l app=myapp           # логи по label (все поды)\nkubectl logs mypod --previous       # предыдущий контейнер (после краша)\n\n# === КОНТЕКСТ И NAMESPACE ===\nkubectl config get-contexts\nkubectl config current-context\nkubectl config use-context my-cluster\nkubectl config set-context --current --namespace=mynamespace\n\n# Быстрый namespace (alias ns):\nkubectl get pods -n kube-system\n\n# === ДРУГИЕ КОМАНДЫ ===\nkubectl create namespace mynamespace\nkubectl scale deployment myapp --replicas=3\nkubectl rollout status deployment/myapp\nkubectl rollout history deployment/myapp\nkubectl rollout undo deployment/myapp\nkubectl rollout undo deployment/myapp --to-revision=2\nkubectl set image deployment/myapp myapp=myapp:2.0.0\nkubectl top pods\nkubectl top nodes' },
        { type: 'tip', value: 'Установи kubectx и kubens для быстрого переключения контекстов и namespace. Также полезен k9s — терминальный UI для Kubernetes.' }
      ]
    },
    {
      id: 2,
      title: 'Pod/Deployment/Service манифесты',
      type: 'theory',
      content: [
        { type: 'text', value: 'Манифесты — YAML-описания Kubernetes ресурсов. Pod — минимальная единица, Deployment — управляет репликами и обновлениями, Service — стабильный сетевой эндпоинт.' },
        { type: 'code', language: 'yaml', value: '# === POD ===\napiVersion: v1\nkind: Pod\nmetadata:\n  name: mypod\n  namespace: default\n  labels:\n    app: myapp\n    version: "1.0"\nspec:\n  containers:\n    - name: myapp\n      image: myapp:1.0.0\n      ports:\n        - containerPort: 3000\n      env:\n        - name: NODE_ENV\n          value: production\n        - name: DB_PASSWORD\n          valueFrom:\n            secretKeyRef:\n              name: myapp-secret\n              key: db_password\n      resources:\n        requests:\n          memory: "64Mi"\n          cpu: "250m"\n        limits:\n          memory: "128Mi"\n          cpu: "500m"\n      readinessProbe:\n        httpGet:\n          path: /health\n          port: 3000\n        initialDelaySeconds: 5\n        periodSeconds: 10\n      livenessProbe:\n        httpGet:\n          path: /health\n          port: 3000\n        initialDelaySeconds: 15\n        periodSeconds: 20\n  restartPolicy: Always\n\n---\n# === DEPLOYMENT ===\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: myapp\n  namespace: default\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: myapp\n  strategy:\n    type: RollingUpdate\n    rollingUpdate:\n      maxUnavailable: 1\n      maxSurge: 1\n  template:\n    metadata:\n      labels:\n        app: myapp\n    spec:\n      containers:\n        - name: myapp\n          image: myapp:1.0.0\n          ports:\n            - containerPort: 3000\n          resources:\n            requests:\n              memory: "128Mi"\n              cpu: "100m"\n            limits:\n              memory: "256Mi"\n              cpu: "500m"\n\n---\n# === SERVICE ===\n# ClusterIP — внутри кластера:\napiVersion: v1\nkind: Service\nmetadata:\n  name: myapp-service\nspec:\n  selector:\n    app: myapp\n  ports:\n    - protocol: TCP\n      port: 80\n      targetPort: 3000\n  type: ClusterIP\n\n---\n# NodePort — порт на каждой ноде (30000-32767):\napiVersion: v1\nkind: Service\nmetadata:\n  name: myapp-nodeport\nspec:\n  selector:\n    app: myapp\n  ports:\n    - port: 80\n      targetPort: 3000\n      nodePort: 30080\n  type: NodePort\n\n---\n# LoadBalancer — облачный балансировщик нагрузки:\napiVersion: v1\nkind: Service\nmetadata:\n  name: myapp-lb\nspec:\n  selector:\n    app: myapp\n  ports:\n    - port: 80\n      targetPort: 3000\n  type: LoadBalancer' },
        { type: 'note', value: 'selector в Deployment и Service должны совпадать с labels в Pod template. Без совпадения Service не найдёт поды, а Deployment не будет управлять ими.' }
      ]
    },
    {
      id: 3,
      title: 'ConfigMap/Secret/Ingress',
      type: 'theory',
      content: [
        { type: 'text', value: 'ConfigMap хранит конфигурацию (незащищённые данные), Secret — секретные данные (base64). Ingress управляет входящим HTTP/HTTPS трафиком и маршрутизацией.' },
        { type: 'code', language: 'yaml', value: '# === CONFIGMAP ===\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: myapp-config\ndata:\n  NODE_ENV: production\n  LOG_LEVEL: info\n  config.json: |\n    {\n      "host": "0.0.0.0",\n      "port": 3000\n    }\n  nginx.conf: |\n    server {\n      listen 80;\n      location / { proxy_pass http://app:3000; }\n    }\n\n# Использование ConfigMap в Pod:\n# Как переменные окружения:\nenv:\n  - name: NODE_ENV\n    valueFrom:\n      configMapKeyRef:\n        name: myapp-config\n        key: NODE_ENV\n# Все ключи как переменные:\nenvFrom:\n  - configMapRef:\n      name: myapp-config\n# Как файл в Volume:\nvolumes:\n  - name: config-volume\n    configMap:\n      name: myapp-config\nvolumeMounts:\n  - name: config-volume\n    mountPath: /etc/config\n\n---\n# === SECRET ===\n# Значения кодируются в base64 (НЕ шифруются!)\napiVersion: v1\nkind: Secret\nmetadata:\n  name: myapp-secret\ntype: Opaque\ndata:\n  db_password: cGFzc3dvcmQxMjM=   # base64\n  api_key: c2VjcmV0a2V5\nstringData:                        # автоматически кодируется\n  db_url: postgresql://user:pass@db:5432/mydb\n\n# Создать Secret из командной строки:\n# kubectl create secret generic myapp-secret \\\n#   --from-literal=db_password=password123 \\\n#   --from-file=tls.crt=./cert.crt\n\n# Типы Secret:\n# Opaque           — произвольные данные\n# kubernetes.io/dockerconfigjson — credentials для registry\n# kubernetes.io/tls — TLS сертификат и ключ\n\n---\n# === INGRESS ===\napiVersion: networking.k8s.io/v1\nkind: Ingress\nmetadata:\n  name: myapp-ingress\n  annotations:\n    nginx.ingress.kubernetes.io/rewrite-target: /\n    cert-manager.io/cluster-issuer: letsencrypt-prod\nspec:\n  ingressClassName: nginx\n  tls:\n    - hosts:\n        - myapp.example.com\n      secretName: myapp-tls\n  rules:\n    - host: myapp.example.com\n      http:\n        paths:\n          - path: /\n            pathType: Prefix\n            backend:\n              service:\n                name: myapp-service\n                port:\n                  number: 80\n          - path: /api\n            pathType: Prefix\n            backend:\n              service:\n                name: api-service\n                port:\n                  number: 8080' },
        { type: 'warning', value: 'Secret хранится в etcd в base64, что НЕ является шифрованием. Для production используй Sealed Secrets, Vault или cloud KMS. Никогда не коммить файлы Secret в git.' }
      ]
    },
    {
      id: 4,
      title: 'Отладка (logs/exec/port-forward/describe)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Отладка в Kubernetes — ключевой навык. Используй logs, exec, port-forward, describe и события для диагностики проблем с подами и сервисами.' },
        { type: 'code', language: 'bash', value: '# === ЛОГИ ===\nkubectl logs mypod\nkubectl logs mypod -f                      # стримить\nkubectl logs mypod --tail=200\nkubectl logs mypod --since=30m\nkubectl logs mypod -c sidecar             # конкретный контейнер\nkubectl logs mypod --previous             # предыдущий (после краша)\nkubectl logs -l app=myapp --all-containers # все поды с label\n\n# === EXEC — войти в контейнер ===\nkubectl exec -it mypod -- bash\nkubectl exec -it mypod -- sh              # alpine/minimal\nkubectl exec -it mypod -c mycontainer -- bash  # multi-container\nkubectl exec mypod -- ls /app\nkubectl exec mypod -- cat /etc/config/app.conf\n\n# === PORT-FORWARD — пробросить порт ===\nkubectl port-forward mypod 8080:3000\nkubectl port-forward service/myapp-service 8080:80\nkubectl port-forward deployment/myapp 8080:3000\n# Доступ: http://localhost:8080\n\n# === DESCRIBE — детальная информация ===\nkubectl describe pod mypod         # Events помогают диагностировать\nkubectl describe deployment myapp\nkubectl describe service myapp-service\nkubectl describe node mynode\n\n# === СОБЫТИЯ ===\nkubectl get events\nkubectl get events -n default\nkubectl get events --sort-by=.lastTimestamp\nkubectl get events --field-selector reason=Failed\n\n# === ДИАГНОСТИКА ТИПИЧНЫХ ПРОБЛЕМ ===\n# CrashLoopBackOff — контейнер падает:\nkubectl logs mypod --previous    # логи до краша\nkubectl describe pod mypod       # смотреть Exit Code и Last State\n# Exit 1 = ошибка приложения, Exit 137 = OOMKilled, Exit 143 = SIGTERM\n\n# Pending — не может запуститься:\nkubectl describe pod mypod       # смотреть Events\n# Причины: нет ресурсов, нет подходящей ноды, PVC не создан\n\n# ImagePullBackOff — не может скачать образ:\nkubectl describe pod mypod | grep -A5 "Events"\n# Причины: неверный тег, нет доступа к registry, нет imagePullSecret\n\n# Error in Service — поды не получают трафик:\nkubectl get endpoints myapp-service   # должны быть IP подов\nkubectl describe service myapp-service\n# Проверь selector совпадает с labels подов\n\n# === ВРЕМЕННЫЙ DEBUG ПОД ===\nkubectl run debug --rm -it --image=busybox -- sh\nkubectl run debug --rm -it --image=nicolaka/netshoot -- bash\n# Полезен для диагностики DNS и сетевых проблем внутри кластера\n\n# Проверить DNS:\n# nslookup myapp-service.default.svc.cluster.local\n# curl http://myapp-service.default.svc.cluster.local' },
        { type: 'tip', value: 'Формат DNS в кластере: <service-name>.<namespace>.svc.cluster.local. Внутри одного namespace достаточно просто имени сервиса. kubectl describe pod всегда смотри в раздел Events — там основная информация о причинах ошибок.' }
      ]
    },
    {
      id: 5,
      title: 'Helm команды',
      type: 'theory',
      content: [
        { type: 'text', value: 'Helm — пакетный менеджер для Kubernetes. Chart — пакет с шаблонами манифестов. Release — установленный экземпляр chart. Позволяет управлять сложными приложениями как единым целым.' },
        { type: 'code', language: 'bash', value: '# === УПРАВЛЕНИЕ РЕПОЗИТОРИЯМИ ===\nhelm repo list\nhelm repo add stable https://charts.helm.sh/stable\nhelm repo add bitnami https://charts.bitnami.com/bitnami\nhelm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx\nhelm repo update                    # обновить индексы\nhelm repo remove stable\n\n# Поиск chart:\nhelm search repo nginx\nhelm search repo nginx --versions   # все версии\nhelm search hub wordpress           # поиск в Artifact Hub\n\n# === УСТАНОВКА И УПРАВЛЕНИЕ ===\n# Установить chart:\nhelm install myrelease bitnami/nginx\nhelm install myrelease bitnami/nginx -n mynamespace\nhelm install myrelease ./mychart    # из локального chart\nhelm install myrelease bitnami/nginx --version 15.0.0\n\n# С переопределением values:\nhelm install myrelease bitnami/nginx \\\n  --set service.type=LoadBalancer \\\n  --set replicaCount=3\n\nhelm install myrelease bitnami/nginx \\\n  -f values.yaml \\\n  -f values-prod.yaml\n\n# Upgrade (обновить или установить если нет):\nhelm upgrade myrelease bitnami/nginx\nhelm upgrade --install myrelease bitnami/nginx   # idempotent\nhelm upgrade myrelease bitnami/nginx --set image.tag=2.0\n\n# Откат:\nhelm rollback myrelease 1           # к ревизии 1\nhelm rollback myrelease             # к предыдущей\n\n# Удалить release:\nhelm uninstall myrelease\nhelm uninstall myrelease -n mynamespace\n\n# === ИНСПЕКЦИЯ ===\nhelm list\nhelm list -A                        # все namespaces\nhelm list --failed\nhelm status myrelease\nhelm history myrelease\nhelm get values myrelease           # применённые values\nhelm get values myrelease --all     # все (включая defaults)\nhelm get manifest myrelease         # сгенерированные манифесты\n\n# === CHART РАЗРАБОТКА ===\nhelm create mychart                 # создать scaffold chart\nhelm lint mychart                   # проверить синтаксис\nhelm template myrelease ./mychart   # рендер без установки\nhelm template myrelease ./mychart -f values-prod.yaml\nhelm package mychart                # создать .tgz\nhelm dependency update mychart      # скачать зависимости\n\n# Dry run (симуляция установки):\nhelm install myrelease ./mychart --dry-run\nhelm upgrade myrelease ./mychart --dry-run' },
        { type: 'code', language: 'yaml', value: '# Структура chart:\n# mychart/\n#   Chart.yaml       — метаданные chart\n#   values.yaml      — значения по умолчанию\n#   templates/       — шаблоны манифестов\n#     deployment.yaml\n#     service.yaml\n#     ingress.yaml\n#     _helpers.tpl   — вспомогательные шаблоны\n#   charts/          — зависимости\n#   .helmignore\n\n# Chart.yaml:\napiVersion: v2\nname: myapp\ndescription: My application chart\ntype: application\nversion: 0.1.0\nappVersion: "1.0.0"\ndependencies:\n  - name: postgresql\n    version: "13.x.x"\n    repository: "https://charts.bitnami.com/bitnami"\n\n# values.yaml:\nreplicaCount: 2\nimage:\n  repository: myapp\n  tag: "1.0.0"\n  pullPolicy: IfNotPresent\nservice:\n  type: ClusterIP\n  port: 80\ningress:\n  enabled: true\n  host: myapp.example.com\nresources:\n  limits:\n    memory: 256Mi\n    cpu: 500m' },
        { type: 'note', value: 'helm upgrade --install — идемпотентная команда, устанавливает если нет, обновляет если есть. Используй в CI/CD вместо разных install/upgrade.' }
      ]
    }
  ]
}
