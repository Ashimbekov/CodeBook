export default {
  id: 13,
  title: 'Шпаргалка: Алгоритмы и Big O',
  description: 'Big O нотация, сложность структур данных, алгоритмы сортировки и поиска, паттерны решения задач, деревья и графы.',
  lessons: [
    {
      id: 1,
      title: 'Big O сравнение (O(1)/O(log n)/O(n)/O(n log n)/O(n²))',
      type: 'theory',
      content: [
        { type: 'text', value: 'Big O нотация описывает верхнюю границу роста сложности алгоритма относительно размера входных данных n. Константы и незначительные члены отбрасываются.' },
        { type: 'code', language: 'python', value: '# Big O от лучшего к худшему:\n# O(1)       — константная:    1 миллиард элементов = 1 шаг\n# O(log n)   — логарифмическая: 1 миллиард элементов ≈ 30 шагов\n# O(n)       — линейная:       1 миллиард элементов = 1 млрд шагов\n# O(n log n) — линейно-логар.: 1 миллиард ≈ 30 млрд шагов\n# O(n²)      — квадратичная:  1 миллион = 1 трлн шагов\n# O(2^n)     — экспоненциальная: n=40 → 1 трлн шагов\n# O(n!)      — факториальная: n=20 → 2.4 квинтиллиона шагов\n\n# === O(1) — константная ===\ndef get_first(arr):\n    return arr[0]                 # всегда 1 операция\n\ndef is_even(n):\n    return n % 2 == 0             # 1 операция\n\n# === O(log n) — логарифмическая ===\ndef binary_search(arr, target):\n    left, right = 0, len(arr) - 1\n    while left <= right:           # каждая итерация делит пополам\n        mid = (left + right) // 2\n        if arr[mid] == target:\n            return mid\n        elif arr[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n\n# === O(n) — линейная ===\ndef find_max(arr):\n    max_val = arr[0]\n    for x in arr:                 # n итераций\n        if x > max_val:\n            max_val = x\n    return max_val\n\n# === O(n log n) — линейно-логарифмическая ===\n# merge sort, heap sort, sort() в Python (Timsort)\nimport heapq\ndef heap_sort(arr):\n    heapq.heapify(arr)             # O(n)\n    return [heapq.heappop(arr) for _ in arr]  # n * O(log n)\n\n# === O(n²) — квадратичная ===\ndef bubble_sort(arr):\n    n = len(arr)\n    for i in range(n):             # n итераций\n        for j in range(n - i - 1):  # n итераций каждая\n            if arr[j] > arr[j + 1]:\n                arr[j], arr[j + 1] = arr[j + 1], arr[j]\n\n# === ПРАВИЛА УПРОЩЕНИЯ ===\n# 1. Константы отбрасываются: O(3n) → O(n)\n# 2. Берём доминирующий член: O(n² + n) → O(n²)\n# 3. Последовательные блоки: O(n) + O(n) = O(n)\n# 4. Вложенные циклы: O(n) * O(n) = O(n²)\n# 5. Разные переменные: O(a + b), не O(n)\n\n# Таблица n → количество операций:\n# n=10:     O(1)=1    O(log n)=3    O(n)=10      O(n²)=100\n# n=100:    O(1)=1    O(log n)=7    O(n)=100     O(n²)=10000\n# n=1000:   O(1)=1    O(log n)=10   O(n)=1000    O(n²)=1000000\n# n=10^6:   O(1)=1    O(log n)=20   O(n)=1000000 O(n²)=10^12\n# n=10^9:   O(1)=1    O(log n)=30   O(n)=10^9    O(n²)=10^18 (невозможно)' },
        { type: 'tip', value: 'Ориентир для LeetCode: O(1) — любой n, O(log n) — n до 10^18, O(n) — n до 10^8, O(n log n) — n до 10^6, O(n²) — n до 10^4, O(2^n) — n до 20, O(n!) — n до 10.' }
      ]
    },
    {
      id: 2,
      title: 'Структуры данных (сложность операций)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Выбор структуры данных определяет сложность алгоритма. Ниже сравнение основных структур данных по сложности операций.' },
        { type: 'code', language: 'python', value: '# =============================================\n# СТРУКТУРА     | Access | Search | Insert | Delete\n# =============================================\n# Array         | O(1)   | O(n)   | O(n)   | O(n)\n# Linked List   | O(n)   | O(n)   | O(1)*  | O(1)*\n# Stack         | O(n)   | O(n)   | O(1)   | O(1)   * вершина\n# Queue         | O(n)   | O(n)   | O(1)   | O(1)   * конец/начало\n# Hash Table    | N/A    | O(1)** | O(1)** | O(1)**\n# BST (balanced)| O(log n)| O(log n)| O(log n)| O(log n)\n# Heap          | O(1)***| O(n)   | O(log n)| O(log n)\n# Trie          | O(k)   | O(k)   | O(k)   | O(k)   * k=длина слова\n# =============================================\n# * вставка/удаление в начало/конец при наличии указателя\n# ** среднее, O(n) worst case при коллизиях\n# *** только min/max (вершина кучи)\n\n# === ARRAY (list в Python) ===\narr = [1, 2, 3, 4, 5]\narr[2]            # O(1)  доступ по индексу\narr.append(6)     # O(1)  amortized\narr.pop()         # O(1)  с конца\narr.insert(0, 0)  # O(n)  вставка в начало\narr.pop(0)        # O(n)  удаление с начала\n2 in arr          # O(n)  линейный поиск\nlen(arr)          # O(1)\n\n# === LINKED LIST (collections.deque) ===\nfrom collections import deque\ndq = deque([1, 2, 3])\ndq.appendleft(0)  # O(1)  вставка в начало\ndq.popleft()      # O(1)  удаление с начала\ndq.append(4)      # O(1)  вставка в конец\ndq.pop()          # O(1)  удаление с конца\ndq[2]             # O(n)  доступ по индексу (медленно!)\n\n# === HASH TABLE (dict, set) ===\nd = {}\nd["key"] = "value"  # O(1) average\nd["key"]            # O(1) average\n"key" in d          # O(1) average\ndel d["key"]        # O(1) average\n\ns = set()\ns.add(1)            # O(1) average\n1 in s              # O(1) average  ← намного быстрее list!\ns.remove(1)         # O(1) average\n\n# === HEAP (heapq — min-heap) ===\nimport heapq\nh = [3, 1, 4, 1, 5]\nheapq.heapify(h)      # O(n)  построить кучу\nheapq.heappush(h, 2)  # O(log n)\nheapq.heappop(h)      # O(log n)  вернёт минимум\nh[0]                  # O(1)  посмотреть минимум\n\n# Max-heap через отрицательные числа:\nheapq.heappush(h, -5)  # -5 станет вершиной (максимум 5)\n\n# nlargest/nsmallest:\nheapq.nlargest(3, [1, 5, 2, 8, 3])   # O(n log k)\nheapq.nsmallest(3, [1, 5, 2, 8, 3])  # O(n log k)' },
        { type: 'note', value: 'set и dict в Python реализованы как hash table. Проверка вхождения (in) работает за O(1) для set, но O(n) для list. Для частых поисков всегда используй set вместо list.' }
      ]
    },
    {
      id: 3,
      title: 'Сортировки (сравнение алгоритмов)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Алгоритмы сортировки — классическая тема. Важно знать сложность, устойчивость и место применения каждого.' },
        { type: 'code', language: 'python', value: '# =============================================\n# АЛГОРИТМ    | Best     | Average  | Worst    | Space  | Stable\n# =============================================\n# Bubble      | O(n)     | O(n²)    | O(n²)    | O(1)   | Да\n# Selection   | O(n²)    | O(n²)    | O(n²)    | O(1)   | Нет\n# Insertion   | O(n)     | O(n²)    | O(n²)    | O(1)   | Да\n# Merge Sort  | O(n log n)| O(n log n)| O(n log n)| O(n) | Да\n# Quick Sort  | O(n log n)| O(n log n)| O(n²)   | O(log n)| Нет\n# Heap Sort   | O(n log n)| O(n log n)| O(n log n)| O(1) | Нет\n# Tim Sort    | O(n)     | O(n log n)| O(n log n)| O(n) | Да\n# Counting    | O(n+k)   | O(n+k)   | O(n+k)   | O(k)   | Да\n# Radix       | O(nk)    | O(nk)    | O(nk)    | O(n+k) | Да\n# =============================================\n# Tim Sort = Python sort() / sorted() — гибрид Insertion + Merge\n\n# === BUBBLE SORT — O(n²) ===\ndef bubble_sort(arr):\n    n = len(arr)\n    for i in range(n):\n        swapped = False\n        for j in range(n - i - 1):\n            if arr[j] > arr[j + 1]:\n                arr[j], arr[j + 1] = arr[j + 1], arr[j]\n                swapped = True\n        if not swapped:            # уже отсортирован — O(n) best case\n            break\n    return arr\n\n# === INSERTION SORT — O(n²), хорош для почти отсортированных ===\ndef insertion_sort(arr):\n    for i in range(1, len(arr)):\n        key = arr[i]\n        j = i - 1\n        while j >= 0 and arr[j] > key:\n            arr[j + 1] = arr[j]\n            j -= 1\n        arr[j + 1] = key\n    return arr\n\n# === MERGE SORT — O(n log n), стабильный ===\ndef merge_sort(arr):\n    if len(arr) <= 1:\n        return arr\n    mid = len(arr) // 2\n    left = merge_sort(arr[:mid])\n    right = merge_sort(arr[mid:])\n    return merge(left, right)\n\ndef merge(left, right):\n    result = []\n    i = j = 0\n    while i < len(left) and j < len(right):\n        if left[i] <= right[j]:\n            result.append(left[i])\n            i += 1\n        else:\n            result.append(right[j])\n            j += 1\n    result.extend(left[i:])\n    result.extend(right[j:])\n    return result\n\n# === QUICK SORT — O(n log n) среднее, O(n²) worst ===\ndef quick_sort(arr):\n    if len(arr) <= 1:\n        return arr\n    pivot = arr[len(arr) // 2]     # медиана лучше крайнего элемента\n    left  = [x for x in arr if x < pivot]\n    mid   = [x for x in arr if x == pivot]\n    right = [x for x in arr if x > pivot]\n    return quick_sort(left) + mid + quick_sort(right)\n\n# === PYTHON ВСТРОЕННАЯ СОРТИРОВКА ===\narr = [3, 1, 4, 1, 5, 9, 2, 6]\nsorted_arr = sorted(arr)           # новый список\narr.sort()                         # in-place\narr.sort(reverse=True)             # убывание\narr.sort(key=lambda x: -x)         # по ключу\nstudents = [("Alice", 90), ("Bob", 85)]\nstudents.sort(key=lambda x: x[1])  # по оценке' },
        { type: 'tip', value: 'Для практики: используй sorted() / .sort() в Python — это Timsort O(n log n), стабильный и очень быстрый на практике. Реализуй merge sort на собеседовании — он прост и предсказуем.' }
      ]
    },
    {
      id: 4,
      title: 'Поиск (linear/binary/BFS/DFS)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Алгоритмы поиска: в массивах (linear, binary) и в графах/деревьях (BFS, DFS). BFS находит кратчайший путь, DFS — для обхода и нахождения компонент.' },
        { type: 'code', language: 'python', value: '# === LINEAR SEARCH — O(n) ===\ndef linear_search(arr, target):\n    for i, val in enumerate(arr):  # n итераций\n        if val == target:\n            return i\n    return -1\n# Когда использовать: несортированный массив, малые данные\n\n# === BINARY SEARCH — O(log n), требует сортировки ===\ndef binary_search(arr, target):\n    left, right = 0, len(arr) - 1\n    while left <= right:\n        mid = left + (right - left) // 2  # избежать overflow\n        if arr[mid] == target:\n            return mid\n        elif arr[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n\n# Bisect — встроенный binary search в Python:\nimport bisect\narr = [1, 3, 5, 7, 9]\nbisect.bisect_left(arr, 5)         # 2 — левая позиция для вставки\nbisect.bisect_right(arr, 5)        # 3 — правая позиция\nbisect.insort(arr, 6)              # вставить с сохранением порядка\n\n# Binary search на ответе (бинарный поиск по условию):\ndef min_days_to_bloom(bloomDay, m, k):\n    def can_bloom(days):\n        flowers = bouquets = 0\n        for d in bloomDay:\n            flowers = flowers + 1 if d <= days else 0\n            if flowers == k:\n                bouquets += 1\n                flowers = 0\n        return bouquets >= m\n    lo, hi = min(bloomDay), max(bloomDay)\n    while lo < hi:\n        mid = (lo + hi) // 2\n        if can_bloom(mid):\n            hi = mid\n        else:\n            lo = mid + 1\n    return lo\n\n# === BFS — Breadth-First Search — O(V + E) ===\nfrom collections import deque\n\ndef bfs(graph, start):\n    visited = set([start])\n    queue = deque([start])\n    result = []\n    while queue:\n        node = queue.popleft()     # O(1) — deque!\n        result.append(node)\n        for neighbor in graph[node]:\n            if neighbor not in visited:\n                visited.add(neighbor)\n                queue.append(neighbor)\n    return result\n\n# BFS для кратчайшего пути:\ndef bfs_shortest_path(graph, start, end):\n    queue = deque([(start, [start])])\n    visited = {start}\n    while queue:\n        node, path = queue.popleft()\n        if node == end:\n            return path\n        for neighbor in graph[node]:\n            if neighbor not in visited:\n                visited.add(neighbor)\n                queue.append((neighbor, path + [neighbor]))\n    return None\n# BFS: гарантирует кратчайший путь в невзвешенном графе\n\n# === DFS — Depth-First Search — O(V + E) ===\n# Рекурсивный:\ndef dfs(graph, node, visited=None):\n    if visited is None:\n        visited = set()\n    visited.add(node)\n    result = [node]\n    for neighbor in graph[node]:\n        if neighbor not in visited:\n            result.extend(dfs(graph, neighbor, visited))\n    return result\n\n# Итеративный (стек):\ndef dfs_iterative(graph, start):\n    visited = set()\n    stack = [start]\n    result = []\n    while stack:\n        node = stack.pop()         # LIFO — в отличие от BFS\n        if node not in visited:\n            visited.add(node)\n            result.append(node)\n            for neighbor in graph[node]:\n                if neighbor not in visited:\n                    stack.append(neighbor)\n    return result\n# DFS использует O(h) памяти, где h = глубина (высота дерева)' },
        { type: 'note', value: 'BFS использует очередь (FIFO) → обходит уровень за уровнем → кратчайший путь. DFS использует стек (LIFO) → уходит вглубь → подходит для поиска пути, топологической сортировки, обнаружения циклов.' }
      ]
    },
    {
      id: 5,
      title: 'Паттерны (two pointers/sliding window/divide and conquer/DP)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Паттерны решения алгоритмических задач — шаблоны мышления, которые помогают быстро находить оптимальные решения.' },
        { type: 'code', language: 'python', value: '# === TWO POINTERS — два указателя ===\n# Когда: сортированный массив, найти пару/подмассив\n# Сложность: O(n) вместо O(n²)\n\n# Пример: найти два числа дающих сумму target:\ndef two_sum_sorted(arr, target):\n    left, right = 0, len(arr) - 1\n    while left < right:\n        s = arr[left] + arr[right]\n        if s == target:\n            return [left, right]\n        elif s < target:\n            left += 1\n        else:\n            right -= 1\n    return []\n\n# Развернуть строку in-place:\ndef reverse_string(s):\n    left, right = 0, len(s) - 1\n    while left < right:\n        s[left], s[right] = s[right], s[left]\n        left += 1\n        right -= 1\n\n# === SLIDING WINDOW — скользящее окно ===\n# Когда: подмассив/подстрока с условием, непрерывный диапазон\n# Сложность: O(n) вместо O(n²) или O(n³)\n\n# Фиксированное окно: максимальная сумма k элементов:\ndef max_sum_k(arr, k):\n    window_sum = sum(arr[:k])\n    max_sum = window_sum\n    for i in range(k, len(arr)):\n        window_sum += arr[i] - arr[i - k]  # добавить правый, убрать левый\n        max_sum = max(max_sum, window_sum)\n    return max_sum\n\n# Динамическое окно: длиннейшая подстрока без повторений:\ndef length_of_longest_substring(s):\n    char_set = set()\n    left = max_len = 0\n    for right in range(len(s)):\n        while s[right] in char_set:   # сужаем окно\n            char_set.remove(s[left])\n            left += 1\n        char_set.add(s[right])\n        max_len = max(max_len, right - left + 1)\n    return max_len\n\n# === DIVIDE AND CONQUER — разделяй и властвуй ===\n# Когда: задача делится на независимые подзадачи\n# Сложность: обычно O(n log n)\n\n# Максимальная разница: max(arr[j] - arr[i]) для j > i:\ndef max_profit(prices):\n    if len(prices) < 2:\n        return 0\n    min_price = prices[0]\n    max_profit = 0\n    for price in prices[1:]:\n        max_profit = max(max_profit, price - min_price)\n        min_price = min(min_price, price)\n    return max_profit  # O(n) — по сути тоже два указателя\n\n# === DYNAMIC PROGRAMMING — динамическое программирование ===\n# Когда: задача разбивается на перекрывающиеся подзадачи\n# Мантра: "оптимальное решение строится из оптимальных подзадач"\n\n# Числа Фибоначчи: O(2^n) → O(n) с мемоизацией:\nfrom functools import lru_cache\n\n@lru_cache(maxsize=None)           # мемоизация через декоратор\ndef fib(n):\n    if n <= 1:\n        return n\n    return fib(n - 1) + fib(n - 2)\n\n# DP снизу вверх (табуляция):\ndef fib_dp(n):\n    if n <= 1:\n        return n\n    dp = [0] * (n + 1)\n    dp[1] = 1\n    for i in range(2, n + 1):\n        dp[i] = dp[i-1] + dp[i-2]\n    return dp[n]\n\n# Задача 0/1 Knapsack — классика DP:\ndef knapsack(weights, values, capacity):\n    n = len(weights)\n    dp = [[0] * (capacity + 1) for _ in range(n + 1)]\n    for i in range(1, n + 1):\n        for w in range(capacity + 1):\n            dp[i][w] = dp[i-1][w]  # не берём предмет i\n            if weights[i-1] <= w:\n                dp[i][w] = max(dp[i][w],\n                               dp[i-1][w - weights[i-1]] + values[i-1])\n    return dp[n][capacity]\n\n# Longest Common Subsequence (LCS):\ndef lcs(s1, s2):\n    m, n = len(s1), len(s2)\n    dp = [[0] * (n + 1) for _ in range(m + 1)]\n    for i in range(1, m + 1):\n        for j in range(1, n + 1):\n            if s1[i-1] == s2[j-1]:\n                dp[i][j] = dp[i-1][j-1] + 1\n            else:\n                dp[i][j] = max(dp[i-1][j], dp[i][j-1])\n    return dp[m][n]' },
        { type: 'tip', value: 'Шаги решения DP задачи: 1) Определить состояние dp[i] что означает. 2) Написать рекуррентное соотношение. 3) Определить базовые случаи. 4) Выбрать top-down (мемоизация) или bottom-up (табуляция). Сначала реши рекурсией, потом добавь кэш.' }
      ]
    },
    {
      id: 6,
      title: 'Деревья и графы (BST/AVL/BFS/DFS/Dijkstra)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Деревья и графы — важнейшие структуры данных. BST для упорядоченных данных, BFS/DFS для обхода, Dijkstra для кратчайших путей.' },
        { type: 'code', language: 'python', value: '# === BINARY TREE ===\nclass TreeNode:\n    def __init__(self, val=0, left=None, right=None):\n        self.val = val\n        self.left = left\n        self.right = right\n\n# Обходы дерева:\ndef inorder(root):    # left → root → right (даёт отсортированный вывод для BST)\n    if not root:\n        return []\n    return inorder(root.left) + [root.val] + inorder(root.right)\n\ndef preorder(root):   # root → left → right (копирование дерева)\n    if not root:\n        return []\n    return [root.val] + preorder(root.left) + preorder(root.right)\n\ndef postorder(root):  # left → right → root (удаление дерева)\n    if not root:\n        return []\n    return postorder(root.left) + postorder(root.right) + [root.val]\n\n# BFS по дереву (обход по уровням):\nfrom collections import deque\ndef level_order(root):\n    if not root:\n        return []\n    result, queue = [], deque([root])\n    while queue:\n        level = []\n        for _ in range(len(queue)):  # обрабатываем уровень целиком\n            node = queue.popleft()\n            level.append(node.val)\n            if node.left:  queue.append(node.left)\n            if node.right: queue.append(node.right)\n        result.append(level)\n    return result\n\n# Высота дерева:\ndef height(root):\n    if not root:\n        return 0\n    return 1 + max(height(root.left), height(root.right))\n\n# === BST (Binary Search Tree) ===\n# Свойство: left < root < right для всех узлов\n# Search, Insert, Delete: O(log n) средний, O(n) worst (несбалансированное)\n# AVL/Red-Black Tree гарантируют O(log n) в худшем случае\n\ndef bst_search(root, val):\n    if not root or root.val == val:\n        return root\n    if val < root.val:\n        return bst_search(root.left, val)\n    return bst_search(root.right, val)\n\ndef bst_insert(root, val):\n    if not root:\n        return TreeNode(val)\n    if val < root.val:\n        root.left = bst_insert(root.left, val)\n    elif val > root.val:\n        root.right = bst_insert(root.right, val)\n    return root\n\n# === ГРАФ (представление) ===\n# Список смежности (adjacency list) — O(V + E) память:\ngraph = {\n    "A": ["B", "C"],\n    "B": ["A", "D", "E"],\n    "C": ["A", "F"],\n    "D": ["B"],\n    "E": ["B", "F"],\n    "F": ["C", "E"]\n}\n\n# Матрица смежности (adjacency matrix) — O(V²) память:\n# Удобна для плотных графов и быстрой проверки edge(u,v)\n\n# === DIJKSTRA — кратчайший путь (без отрицательных рёбер) ===\nimport heapq\n\ndef dijkstra(graph, start):\n    # graph: {node: [(neighbor, weight), ...]}\n    dist = {node: float("inf") for node in graph}\n    dist[start] = 0\n    pq = [(0, start)]              # min-heap: (distance, node)\n\n    while pq:\n        d, u = heapq.heappop(pq)\n        if d > dist[u]:            # устаревшая запись, пропустить\n            continue\n        for v, weight in graph[u]:\n            if dist[u] + weight < dist[v]:\n                dist[v] = dist[u] + weight\n                heapq.heappush(pq, (dist[v], v))\n\n    return dist\n\n# Сложность Dijkstra: O((V + E) log V) с min-heap\n\n# === ТОПОЛОГИЧЕСКАЯ СОРТИРОВКА — Kahn\'s algorithm ===\nfrom collections import deque\n\ndef topological_sort(graph, in_degree):\n    queue = deque([v for v in graph if in_degree[v] == 0])\n    result = []\n    while queue:\n        node = queue.popleft()\n        result.append(node)\n        for neighbor in graph[node]:\n            in_degree[neighbor] -= 1\n            if in_degree[neighbor] == 0:\n                queue.append(neighbor)\n    return result if len(result) == len(graph) else []  # пустой = цикл\n\n# === UNION-FIND (Disjoint Set) — O(α(n)) ≈ O(1) ===\nclass UnionFind:\n    def __init__(self, n):\n        self.parent = list(range(n))\n        self.rank = [0] * n\n\n    def find(self, x):             # path compression\n        if self.parent[x] != x:\n            self.parent[x] = self.find(self.parent[x])\n        return self.parent[x]\n\n    def union(self, x, y):         # union by rank\n        px, py = self.find(x), self.find(y)\n        if px == py:\n            return False           # уже в одном множестве (цикл!)\n        if self.rank[px] < self.rank[py]:\n            px, py = py, px\n        self.parent[py] = px\n        if self.rank[px] == self.rank[py]:\n            self.rank[px] += 1\n        return True' },
        { type: 'note', value: 'Union-Find — незаменим для задач на связность: Minimum Spanning Tree (Kruskal), поиск циклов в графе, задачи на перегородки (number of islands вариации). find() с path compression работает практически за O(1).' }
      ]
    }
  ]
}
