export default {
  id: 9,
  title: 'Шпаргалка: Docker',
  description: 'Команды образов и контейнеров, Dockerfile, Docker Compose, volumes и networking — всё в одном месте.',
  lessons: [
    {
      id: 1,
      title: 'Команды образов (pull/build/push/tag)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Образ (image) — неизменяемый шаблон контейнера. Все операции с образами: скачивание, сборка, публикация и тегирование.' },
        { type: 'code', language: 'bash', value: '# Скачать образ из реестра:\ndocker pull nginx:latest\ndocker pull postgres:15-alpine\ndocker pull node:18          # latest по умолчанию\n\n# Список локальных образов:\ndocker images\ndocker image ls\ndocker images --filter "dangling=true"   # висячие (без тега)\n\n# Собрать образ из Dockerfile:\ndocker build -t myapp:1.0.0 .\ndocker build -t myapp:1.0.0 -f Dockerfile.prod .\ndocker build --no-cache -t myapp:latest .\ndocker build --build-arg ENV=prod -t myapp:prod .\n\n# Тегировать образ:\ndocker tag myapp:latest user/myapp:1.0.0\ndocker tag abc123def user/myapp:stable\n\n# Опубликовать образ:\ndocker login\ndocker login ghcr.io -u USERNAME\ndocker push user/myapp:1.0.0\ndocker push user/myapp --all-tags\n\n# Удалить образ:\ndocker rmi myapp:latest\ndocker rmi abc123def\ndocker image prune          # удалить висячие\ndocker image prune -a       # удалить все неиспользуемые\n\n# Инспектировать образ:\ndocker inspect myapp:latest\ndocker history myapp:latest  # слои образа\n\n# Сохранить/загрузить образ в файл:\ndocker save -o myapp.tar myapp:latest\ndocker load -i myapp.tar' },
        { type: 'tip', value: 'Используй конкретные теги (1.0.0, 1.0.0-alpine), а не latest в production. latest меняется при каждом push и может сломать deployment.' }
      ]
    },
    {
      id: 2,
      title: 'Команды контейнеров (run/exec/logs/stop/rm)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Контейнер — запущенный экземпляр образа. Основные операции жизненного цикла: запуск, управление, логи и удаление.' },
        { type: 'code', language: 'bash', value: '# Запустить контейнер:\ndocker run nginx\ndocker run -d nginx                    # в фоне (detached)\ndocker run -it ubuntu bash             # интерактивно\ndocker run --rm alpine echo "hello"    # удалить после завершения\ndocker run -p 8080:80 nginx            # пробросить порт\ndocker run -p 8080:80 -p 443:443 nginx # несколько портов\ndocker run --name mycontainer nginx    # задать имя\ndocker run -e DB_HOST=localhost nginx  # переменная окружения\ndocker run --env-file .env nginx       # переменные из файла\ndocker run -v /host/path:/container/path nginx  # bind mount\ndocker run -v myvolume:/data nginx     # named volume\ndocker run --network mynet nginx       # подключить к сети\ndocker run --restart=always nginx      # политика перезапуска\ndocker run --memory=512m nginx         # ограничить память\ndocker run --cpus=1.5 nginx            # ограничить CPU\n\n# Список контейнеров:\ndocker ps                # запущенные\ndocker ps -a             # все (включая остановленные)\ndocker ps -q             # только ID\n\n# Управление контейнером:\ndocker stop mycontainer           # SIGTERM -> SIGKILL через 10с\ndocker kill mycontainer           # немедленно SIGKILL\ndocker start mycontainer\ndocker restart mycontainer\npause mycontainer\ndocker unpause mycontainer\n\n# Выполнить команду в контейнере:\ndocker exec mycontainer ls /app\ndocker exec -it mycontainer bash       # интерактивная оболочка\ndocker exec -it mycontainer sh         # для alpine\ndocker exec -e VAR=val mycontainer cmd # с переменной\n\n# Логи:\ndocker logs mycontainer\ndocker logs -f mycontainer             # следить в реальном времени\ndocker logs --tail 100 mycontainer     # последние 100 строк\ndocker logs --since 1h mycontainer     # за последний час\ndocker logs --timestamps mycontainer   # со временем\n\n# Удалить контейнер:\ndocker rm mycontainer\ndocker rm -f mycontainer               # принудительно (остановит)\ndocker container prune                 # удалить все остановленные\n\n# Копировать файлы:\ndocker cp mycontainer:/app/config.json .\ndocker cp ./config.json mycontainer:/app/\n\n# Статистика и инспекция:\ndocker stats                           # ресурсы в реальном времени\ndocker stats mycontainer\ndocker top mycontainer                 # процессы\ndocker inspect mycontainer' },
        { type: 'note', value: 'docker stop → SIGTERM (контейнер может завершиться корректно). docker kill → SIGKILL (немедленно). Всегда предпочитай stop, если не нужно kill.' }
      ]
    },
    {
      id: 3,
      title: 'Dockerfile (FROM/COPY/RUN/CMD/ENTRYPOINT)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Dockerfile — текстовый файл с инструкциями для сборки образа. Каждая инструкция создаёт новый слой. Порядок инструкций влияет на эффективность кэширования.' },
        { type: 'code', language: 'bash', value: '# Структура Dockerfile:\n\n# FROM — базовый образ (обязательная первая инструкция):\nFROM ubuntu:22.04\nFROM node:18-alpine\nFROM scratch           # пустой образ\n\n# WORKDIR — рабочая директория:\nWORKDIR /app\n\n# COPY — копировать файлы в образ:\nCOPY . .\nCOPY package*.json ./\nCOPY --chown=node:node . .\n\n# ADD — как COPY, но поддерживает URL и автоматически распаковывает tar:\nADD https://example.com/file.tar.gz /tmp/\nADD archive.tar.gz /app/   # распакует автоматически\n# Предпочитай COPY для простого копирования\n\n# RUN — выполнить команду при сборке:\nRUN apt-get update && apt-get install -y curl\nRUN npm install\n# Объединяй команды через && для меньшего числа слоёв:\nRUN apt-get update \\\n    && apt-get install -y curl git \\\n    && rm -rf /var/lib/apt/lists/*\n\n# ENV — переменные окружения:\nENV NODE_ENV=production\nENV PORT=3000 HOST=0.0.0.0\n\n# ARG — аргументы сборки (не попадают в образ):\nARG VERSION=1.0.0\nARG BUILD_DATE\nRUN echo "Building version ${VERSION}"\n\n# EXPOSE — документировать порты (не публикует!):\nEXPOSE 3000\nEXPOSE 3000/tcp 8080/tcp\n\n# VOLUME — точки монтирования:\nVOLUME /data\nVOLUME ["/data", "/logs"]\n\n# USER — сменить пользователя:\nRUN useradd -r appuser\nUSER appuser\n\n# CMD — команда по умолчанию (переопределяется при docker run):\nCMD ["node", "server.js"]     # exec форма (предпочтительно)\nCMD node server.js            # shell форма\n\n# ENTRYPOINT — точка входа (не переопределяется без --entrypoint):\nENTRYPOINT ["nginx", "-g", "daemon off;"]\n# CMD дополняет ENTRYPOINT как аргументы по умолчанию:\nENTRYPOINT ["docker-entrypoint.sh"]\nCMD ["postgres"]              # аргумент entrypoint\n\n# HEALTHCHECK — проверка состояния:\nHEALTHCHECK --interval=30s --timeout=10s --retries=3 \\\n  CMD curl -f http://localhost:3000/health || exit 1\n\n# LABEL — метаданные:\nLABEL maintainer="dev@company.com"\nLABEL version="1.0.0"\n\n# ONBUILD — выполнить при сборке производного образа:\nONBUILD COPY . /app/src' },
        { type: 'code', language: 'bash', value: '# Multi-stage build — минимальный финальный образ:\nFROM node:18 AS builder\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci\nCOPY . .\nRUN npm run build\n\nFROM node:18-alpine AS production\nWORKDIR /app\nCOPY --from=builder /app/dist ./dist\nCOPY --from=builder /app/node_modules ./node_modules\nUSER node\nCMD ["node", "dist/server.js"]\n\n# Собрать конкретный stage:\ndocker build --target builder -t myapp:builder .\ndocker build --target production -t myapp:latest .' },
        { type: 'tip', value: 'Порядок инструкций важен для кэша. Сначала копируй package.json и запускай npm install, потом копируй исходники. Так слой с зависимостями не пересобирается при изменении кода.' }
      ]
    },
    {
      id: 4,
      title: 'Docker Compose (services/volumes/networks)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Docker Compose — инструмент для описания и запуска многоконтейнерных приложений через YAML-файл. compose.yaml (или docker-compose.yml) описывает сервисы, сети и тома.' },
        { type: 'code', language: 'yaml', value: '# compose.yaml — полный пример:\nservices:\n  app:\n    build:\n      context: .\n      dockerfile: Dockerfile\n      args:\n        NODE_ENV: production\n    image: myapp:latest\n    container_name: myapp\n    ports:\n      - "3000:3000"\n      - "127.0.0.1:9229:9229"  # только localhost\n    environment:\n      - NODE_ENV=production\n      - DB_HOST=db\n    env_file:\n      - .env\n    volumes:\n      - ./src:/app/src          # bind mount\n      - app_logs:/app/logs      # named volume\n    depends_on:\n      db:\n        condition: service_healthy\n    networks:\n      - frontend\n      - backend\n    restart: unless-stopped\n    healthcheck:\n      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]\n      interval: 30s\n      timeout: 10s\n      retries: 3\n    deploy:\n      resources:\n        limits:\n          memory: 512M\n          cpus: "1.0"\n\n  db:\n    image: postgres:15-alpine\n    container_name: myapp_db\n    environment:\n      POSTGRES_DB: mydb\n      POSTGRES_USER: user\n      POSTGRES_PASSWORD: password\n    volumes:\n      - db_data:/var/lib/postgresql/data\n      - ./init.sql:/docker-entrypoint-initdb.d/init.sql\n    networks:\n      - backend\n    healthcheck:\n      test: ["CMD-SHELL", "pg_isready -U user -d mydb"]\n      interval: 10s\n      timeout: 5s\n      retries: 5\n\n  nginx:\n    image: nginx:alpine\n    ports:\n      - "80:80"\n      - "443:443"\n    volumes:\n      - ./nginx.conf:/etc/nginx/nginx.conf:ro\n      - ./certs:/etc/ssl/certs:ro\n    depends_on:\n      - app\n    networks:\n      - frontend\n\nvolumes:\n  db_data:\n    driver: local\n  app_logs:\n\nnetworks:\n  frontend:\n    driver: bridge\n  backend:\n    driver: bridge\n    internal: true    # нет доступа к интернету' },
        { type: 'code', language: 'bash', value: '# Основные команды Compose:\ndocker compose up                   # запустить все сервисы\ndocker compose up -d                # в фоне\ndocker compose up --build           # пересобрать образы\ndocker compose up app db            # только указанные сервисы\n\ndocker compose down                 # остановить и удалить контейнеры\ndocker compose down -v              # + удалить тома\ndocker compose down --rmi all       # + удалить образы\n\ndocker compose ps                   # состояние сервисов\ndocker compose logs                 # логи всех сервисов\ndocker compose logs -f app          # следить за логами сервиса\ndocker compose exec app bash        # выполнить команду\ndocker compose run app npm test     # запустить одноразовый контейнер\n\ndocker compose build                # собрать образы\ndocker compose pull                 # скачать образы\ndocker compose restart app          # перезапустить сервис\ndocker compose stop                 # остановить (не удалять)\ndocker compose start                # запустить остановленные\n\n# Несколько файлов (переопределение):\ndocker compose -f compose.yaml -f compose.override.yaml up\n# compose.override.yaml применяется автоматически при наличии' },
        { type: 'note', value: 'depends_on по умолчанию ждёт только запуска контейнера, не готовности сервиса. Используй condition: service_healthy вместе с healthcheck для надёжного порядка запуска.' }
      ]
    },
    {
      id: 5,
      title: 'Volumes и networking',
      type: 'theory',
      content: [
        { type: 'text', value: 'Volumes обеспечивают сохранность данных и обмен файлами. Networks изолируют и связывают контейнеры. Правильная настройка — основа безопасной архитектуры.' },
        { type: 'code', language: 'bash', value: '# === VOLUMES ===\n\n# Типы монтирования:\n# 1. Named volume — управляется Docker, рекомендуется для данных БД:\ndocker volume create mydata\ndocker run -v mydata:/app/data nginx\n\n# 2. Bind mount — монтирует директорию хоста:\ndocker run -v /host/path:/container/path nginx\ndocker run -v $(pwd)/src:/app/src nginx\ndocker run -v ./src:/app/src nginx          # относительный путь в Compose\ndocker run -v ./config.json:/app/config.json:ro nginx  # :ro = read-only\n\n# 3. tmpfs — только в RAM, не сохраняется:\ndocker run --tmpfs /tmp nginx\ndocker run --mount type=tmpfs,target=/tmp nginx\n\n# Команды volumes:\ndocker volume ls\ndocker volume create myvolume\ndocker volume inspect myvolume\ndocker volume rm myvolume\ndocker volume prune              # удалить неиспользуемые\n\n# Бэкап volume:\ndocker run --rm -v mydata:/data -v $(pwd):/backup alpine \\\n  tar czf /backup/data_backup.tar.gz -C /data .\n\n# Восстановление:\ndocker run --rm -v mydata:/data -v $(pwd):/backup alpine \\\n  tar xzf /backup/data_backup.tar.gz -C /data' },
        { type: 'code', language: 'bash', value: '# === NETWORKING ===\n\n# Типы сетей:\n# bridge   — сеть по умолчанию, изолированная, контейнеры общаются по IP\n# host     — контейнер использует сеть хоста (без изоляции)\n# none     — нет сетевого доступа\n# overlay  — для Docker Swarm, multi-host\n# macvlan  — контейнер получает MAC-адрес, выглядит как физическое устройство\n\n# Команды сетей:\ndocker network ls\ndocker network create mynet\ndocker network create --driver bridge --subnet 172.18.0.0/16 mynet\ndocker network create --internal backend     # без доступа к интернету\ndocker network inspect mynet\ndocker network rm mynet\ndocker network prune\n\n# Подключить/отключить контейнер:\ndocker network connect mynet mycontainer\ndocker network disconnect mynet mycontainer\n\n# Запустить в нескольких сетях:\ndocker run --network frontend mycontainer\ndocker network connect backend mycontainer\n\n# DNS в пользовательских bridge сетях:\n# Контейнеры в одной network общаются по имени контейнера:\n# app -> http://db:5432 (вместо IP)\n\n# Пример: app недоступен извне, nginx — прокси:\n# frontend: nginx\n# backend: app, db (internal=true)\n# nginx подключён к обеим сетям' },
        { type: 'tip', value: 'Всегда создавай пользовательские bridge-сети вместо дефолтной. В пользовательских сетях работает DNS-резолюция по имени контейнера. Дефолтная сеть bridge не поддерживает DNS между контейнерами.' }
      ]
    },
    {
      id: 6,
      title: 'Полезные флаги и трюки',
      type: 'theory',
      content: [
        { type: 'text', value: 'Сборник полезных команд и приёмов для повседневной работы с Docker: очистка системы, отладка, оптимизация и автоматизация.' },
        { type: 'code', language: 'bash', value: '# === ОЧИСТКА СИСТЕМЫ ===\ndocker system df                    # сколько места занимает Docker\ndocker system prune                 # удалить всё неиспользуемое\ndocker system prune -a              # + неиспользуемые образы\ndocker system prune -a --volumes    # + тома\ndocker system prune -a -f           # без подтверждения\n\n# Удалить всё агрессивно:\ndocker stop $(docker ps -q)\ndocker rm $(docker ps -aq)\ndocker rmi $(docker images -q)\ndocker volume prune -f\ndocker network prune -f\n\n# === ОТЛАДКА ===\n# Войти в работающий контейнер:\ndocker exec -it mycontainer bash\ndocker exec -it mycontainer sh      # alpine\n\n# Запустить образ интерактивно (временный контейнер):\ndocker run --rm -it nginx bash\ndocker run --rm -it --entrypoint bash myapp:latest\n\n# Просмотр файлов в остановленном контейнере:\ndocker create --name tmp myapp:latest\ndocker cp tmp:/app/logs .\ndocker rm tmp\n\n# Смотреть события Docker в реальном времени:\ndocker events\ndocker events --filter type=container\ndocker events --since 1h\n\n# Ресурсы контейнеров:\ndocker stats\ndocker stats --no-stream           # один снимок\ndocker stats --format "table {{.Name}}\\t{{.CPUPerc}}\\t{{.MemUsage}}"\n\n# === ФОРМАТИРОВАНИЕ ВЫВОДА ===\ndocker ps --format "table {{.Names}}\\t{{.Image}}\\t{{.Status}}"\ndocker images --format "{{.Repository}}:{{.Tag}} {{.Size}}"\n\n# === ПОЛЕЗНЫЕ ФЛАГИ docker run ===\n# --rm           удалить после завершения\n# -d             фоновый режим\n# -it            интерактивный терминал\n# --name         имя контейнера\n# -p 8080:80     проброс порта\n# -e VAR=value   переменная окружения\n# -v path:path   монтирование\n# --network      сеть\n# --restart      политика: no | always | on-failure | unless-stopped\n# --memory       лимит памяти: --memory=512m\n# --cpus         лимит CPU: --cpus=1.5\n# --user         пользователь: --user=node\n# --read-only    файловая система только для чтения\n# --cap-drop ALL убрать все capabilities (безопасность)\n\n# === .dockerignore ===\n# Создай .dockerignore для ускорения сборки:\n# node_modules\n# .git\n# .env\n# *.log\n# dist\n# coverage\n# README.md' },
        { type: 'code', language: 'bash', value: '# === HEALTHCHECK ПАТТЕРНЫ ===\n# В Dockerfile:\nHEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \\\n  CMD curl -f http://localhost:3000/health || exit 1\n\n# Проверить статус здоровья:\ndocker inspect --format "{{.State.Health.Status}}" mycontainer\n\n# === ПЕРЕМЕННЫЕ СРЕДЫ ===\n# Из файла .env:\ndocker run --env-file .env myapp\n\n# В Docker Compose с .env файлом (автоматически):\n# .env файл в директории с compose.yaml подставляется в compose.yaml\n# Переменные в compose.yaml: ${VAR_NAME}\n\n# === ОБРАЗЫ: ПОЛЕЗНЫЕ КОМАНДЫ ===\n# Информация о слоях образа:\ndocker history --no-trunc myapp:latest\ndocker inspect --format "{{json .RootFS.Layers}}" myapp:latest\n\n# Запустить образ как другой пользователь:\ndocker run --user 1000:1000 myapp bash\n\n# Тест безопасности образа (dive — анализ слоёв):\n# docker run --rm -it wagoodman/dive myapp:latest' },
        { type: 'note', value: 'docker system prune -a --volumes удалит ВСЁ: остановленные контейнеры, неиспользуемые сети, образы без тегов и без запущенных контейнеров, тома без контейнеров. Используй с осторожностью.' }
      ]
    }
  ]
}
