export default {
  id: 12,
  title: 'Шпаргалка: Linux CLI',
  description: 'Файлы и навигация, работа с текстом, процессы, права доступа, сеть, архивы и диски — полный справочник командной строки Linux.',
  lessons: [
    {
      id: 1,
      title: 'Файлы и навигация (ls/cd/pwd/cp/mv/rm/mkdir)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Файловая система Linux — иерархическая структура от корня /. Навигация и управление файлами — базовые операции CLI.' },
        { type: 'code', language: 'bash', value: '# === НАВИГАЦИЯ ===\npwd                               # текущая директория (Print Working Directory)\ncd /var/log                       # перейти по абсолютному пути\ncd src/app                        # перейти по относительному пути\ncd ..                             # на уровень выше\ncd ../..                          # на два уровня выше\ncd ~                              # домашняя директория\ncd -                              # предыдущая директория\n\n# === LS — список файлов ===\nls                                # файлы в текущей директории\nls -l                             # подробный список\nls -la                            # + скрытые файлы (начинаются с .)\nls -lh                            # с читаемыми размерами (K, M, G)\nls -lS                            # сортировка по размеру\nls -lt                            # сортировка по времени (новые первые)\nls -lr                            # обратная сортировка\nls -R                             # рекурсивно\nls *.txt                          # только .txt файлы\nls -ld /etc                       # информация о директории, не её содержимом\n\n# === СОЗДАНИЕ ===\nmkdir mydir                       # создать директорию\nmkdir -p parent/child/deep        # создать дерево директорий\ntouch file.txt                    # создать пустой файл / обновить время изменения\n\n# === КОПИРОВАНИЕ ===\ncp file.txt copy.txt              # скопировать файл\ncp -r dir/ newdir/                # скопировать директорию рекурсивно\ncp -p file.txt dest/              # сохранить права и временные метки\ncp -i file.txt dest/              # спрашивать перед перезаписью\ncp -u src/ dest/                  # только если источник новее\ncp file1 file2 file3 dest/        # несколько файлов в директорию\n\n# === ПЕРЕМЕЩЕНИЕ / ПЕРЕИМЕНОВАНИЕ ===\nmv file.txt newname.txt           # переименовать\nmv file.txt /var/tmp/             # переместить\nmv dir/ /var/tmp/dir/             # переместить директорию\nmv -i src dest                    # спрашивать перед перезаписью\n\n# === УДАЛЕНИЕ ===\nrm file.txt\nrm -i file.txt                    # спрашивать подтверждение\nrm -f file.txt                    # принудительно (без вопросов)\nrm -r directory/                  # удалить директорию рекурсивно\nrm -rf directory/                 # принудительно (ОСТОРОЖНО!)\nrmdir emptydir                    # удалить пустую директорию\n\n# === ПОИСК ФАЙЛОВ ===\nfind . -name "*.txt"              # найти .txt файлы\nfind /var/log -name "*.log" -type f\nfind . -type d -name "node_modules"  # директории\nfind . -size +100M                # файлы больше 100MB\nfind . -mtime -7                  # изменённые за последние 7 дней\nfind . -user root\nfind . -name "*.log" -delete      # найти и удалить\nfind . -name "*.txt" -exec chmod 644 {} \\;\n\n# Быстрый поиск по имени (индекс):\nlocate filename                   # нужен updatedb\n\n# === ПРОСМОТР ФАЙЛОВ ===\ncat file.txt                      # вывести содержимое\ncat -n file.txt                   # с номерами строк\nless file.txt                     # постраничный просмотр (q для выхода)\nmore file.txt                     # только вперёд\nhead file.txt                     # первые 10 строк\nhead -20 file.txt                 # первые 20 строк\ntail file.txt                     # последние 10 строк\ntail -f file.txt                  # следить за файлом в реальном времени\ntail -n +5 file.txt               # начиная с 5-й строки\n\n# === ССЫЛКИ ===\nln -s /path/to/target linkname    # символическая ссылка (symlink)\nln target hardlink                # жёсткая ссылка\nreadlink -f symlink               # показать цель symlink' },
        { type: 'tip', value: 'rm -rf без аргументов или с пробелом (rm -rf / home/user) — катастрофическое удаление. Всегда дважды проверяй путь. Используй trash-cli или перемещай в /tmp вместо удаления для важных файлов.' }
      ]
    },
    {
      id: 2,
      title: 'Текст (cat/grep/sed/awk/head/tail/wc)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Обработка текстовых потоков — суперсила Linux CLI. Инструменты объединяются через pipe (|) для создания мощных цепочек команд.' },
        { type: 'code', language: 'bash', value: '# === GREP — поиск по тексту ===\ngrep "pattern" file.txt           # найти строки с паттерном\ngrep -i "pattern" file.txt        # без учёта регистра\ngrep -r "pattern" ./src/          # рекурсивно в директории\ngrep -n "pattern" file.txt        # с номерами строк\ngrep -v "pattern" file.txt        # инвертировать (строки БЕЗ паттерна)\ngrep -c "pattern" file.txt        # количество совпадений\ngrep -l "pattern" *.txt           # только имена файлов\ngrep -w "word" file.txt           # только целые слова\ngrep -A 3 "pattern" file.txt      # + 3 строки ПОСЛЕ совпадения\ngrep -B 3 "pattern" file.txt      # + 3 строки ДО совпадения\ngrep -C 3 "pattern" file.txt      # + 3 строки ВОКРУГ совпадения\ngrep -E "error|warning" file.txt  # расширенные регулярные выражения\ngrep -P "\\\\d+" file.txt           # Perl-совместимые регулярки\ngrep --color "pattern" file.txt   # выделить совпадения цветом\n\n# Примеры:\ngrep -r "TODO" ./src --include="*.js"\ncat access.log | grep "ERROR" | grep -v "404"\n\n# === SED — stream editor ===\nsed \'s/old/new/\' file.txt         # заменить первое вхождение в каждой строке\nsed \'s/old/new/g\' file.txt        # заменить все вхождения\nsed \'s/old/new/gi\' file.txt       # без учёта регистра\nsed -i \'s/old/new/g\' file.txt     # in-place (изменить файл)\nsed -i.bak \'s/old/new/g\' file.txt # с резервной копией .bak\nsed \'5d\' file.txt                 # удалить 5-ю строку\nsed \'/pattern/d\' file.txt         # удалить строки с паттерном\nsed -n \'5,10p\' file.txt           # вывести строки 5-10\nsed -n \'/start/,/end/p\' file.txt  # между паттернами\nsed \'1i\\\\header line\' file.txt     # вставить строку перед 1-й\nsed \'$a\\\\footer line\' file.txt     # добавить строку в конец\nsed \'s/[[:space:]]*$//\' file.txt  # убрать пробелы в конце строк\n\n# === AWK — язык обработки текста ===\nawk \'{print $1}\' file.txt         # первый столбец (разделитель — пробел)\nawk \'{print $1, $3}\' file.txt     # 1-й и 3-й столбцы\nawk -F: \'{print $1}\' /etc/passwd  # разделитель :\nawk \'{print NR, $0}\' file.txt     # добавить номера строк\nawk \'NR==5\' file.txt              # только 5-я строка\nawk \'NR>=5 && NR<=10\' file.txt    # строки 5-10\nawk \'/pattern/\' file.txt          # строки с паттерном\nawk \'/pattern/{print $2}\' file.txt # 2-й столбец строк с паттерном\nawk \'{sum+=$1} END{print sum}\' f  # сумма первого столбца\nawk \'{count[$1]++} END{for(k in count) print k, count[k]}\' f\n\n# Примеры:\nps aux | awk \'{print $1, $11}\' | sort | uniq\nawk -F, \'{print $2}\' data.csv     # парсить CSV\n\n# === WC — подсчёт ===\nwc file.txt                       # строки, слова, байты\nwc -l file.txt                    # только строки\nwc -w file.txt                    # только слова\nwc -c file.txt                    # только байты\nwc -m file.txt                    # только символы\nls | wc -l                        # количество файлов\n\n# === SORT, UNIQ ===\nsort file.txt                     # сортировка\nsort -r file.txt                  # обратная сортировка\nsort -n file.txt                  # числовая сортировка\nsort -k2 file.txt                 # по 2-му столбцу\nsort -t: -k3 -n /etc/passwd       # разделитель : 3-й столбец числовой\nsort -u file.txt                  # уникальные строки\nuniq file.txt                     # удалить подряд идущие дубликаты (нужен sort)\nsort file.txt | uniq              # уникальные строки\nsort file.txt | uniq -c           # с количеством повторений\nsort file.txt | uniq -d           # только дубликаты\n\n# === CUT, PASTE, JOIN ===\ncut -d: -f1 /etc/passwd           # 1-е поле, разделитель :\ncut -c1-10 file.txt               # символы 1-10\ncut -d, -f2,4 data.csv            # поля 2 и 4 из CSV\npaste file1.txt file2.txt         # объединить файлы по столбцам\n\n# === TR — translate/delete символы ===\ncat file.txt | tr a-z A-Z         # в верхний регистр\ncat file.txt | tr -d "\\n"         # удалить переносы строк\ncat file.txt | tr -s " "          # схлопнуть множественные пробелы' },
        { type: 'note', value: 'Принцип UNIX: каждая программа делает одно, но хорошо. Объединяй через pipe: cat log | grep ERROR | awk \'{print $5}\' | sort | uniq -c | sort -rn — топ ошибок по частоте.' }
      ]
    },
    {
      id: 3,
      title: 'Процессы (ps/top/kill/bg/fg/nohup)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Управление процессами в Linux: просмотр запущенных процессов, отправка сигналов, управление фоновыми задачами.' },
        { type: 'code', language: 'bash', value: '# === PS — список процессов ===\nps                                # процессы текущего терминала\nps aux                            # все процессы (BSD стиль)\nps -ef                            # все процессы (System V стиль)\nps aux --sort=-%cpu               # сортировка по CPU\nps aux --sort=-%mem               # сортировка по памяти\nps aux | grep nginx               # найти процессы по имени\nps -p 1234                        # конкретный PID\nps --forest                       # дерево процессов\npstree                            # дерево процессов графически\n\n# === TOP / HTOP — мониторинг в реальном времени ===\ntop                               # интерактивный мониторинг\n# В top:\n# q — выход, k — kill по PID\n# P — сортировка по CPU, M — по памяти\n# 1 — показать все CPU ядра\n# H — показать потоки\n# u — фильтр по пользователю\n\nhtop                              # улучшенный top (нужно установить)\n\n# === KILL — сигналы процессам ===\nkill 1234                         # SIGTERM (15) — корректное завершение\nkill -9 1234                      # SIGKILL — немедленное завершение\nkill -HUP 1234                    # SIGHUP (1) — перезагрузить конфигурацию\nkill -STOP 1234                   # приостановить\nkill -CONT 1234                   # продолжить\n\nkillall nginx                     # kill по имени процесса\nkillall -9 python\npkill nginx                       # kill по паттерну имени\npkill -u username                 # kill все процессы пользователя\n\n# Список сигналов:\n# SIGTERM (15) — мягкое завершение, процесс может обработать\n# SIGKILL (9)  — принудительное, нельзя поймать/проигнорировать\n# SIGHUP  (1)  — обычно: reload config\n# SIGINT  (2)  — Ctrl+C\n# SIGSTOP (19) — приостановить\n# SIGCONT (18) — продолжить\n\n# === FIND PID ===\npidof nginx                       # PID по имени\npgrep nginx                       # то же самое\npgrep -u root nginx               # PID nginx запущенного от root\n\n# === ФОНОВЫЕ ЗАДАЧИ ===\ncommand &                         # запустить в фоне\njobs                              # список фоновых задач\nbg                                # перевести приостановленную задачу в фон\nbg %1                             # задача номер 1\nfg                                # перевести фоновую задачу на передний план\nfg %2                             # задача номер 2\n\n# Ctrl+Z — приостановить текущую задачу (SIGSTOP)\n# Затем: bg для фонового выполнения\n\n# === NOHUP — не завершать при выходе ===\nnohup command &                   # запустить, вывод в nohup.out\nnohup command > output.log 2>&1 & # явный файл вывода\n\n# === SCREEN / TMUX — многозадачный терминал ===\n# screen:\nscreen                            # новая сессия\nscreen -S mysession               # именованная сессия\nscreen -ls                        # список сессий\nscreen -r mysession               # подключиться к сессии\n# Ctrl+A D — отключиться (сессия продолжает работать)\n\n# === ПРИОРИТЕТЫ ===\nnice -n 10 command                # запустить с пониженным приоритетом (10)\nrenice -n 5 -p 1234               # изменить приоритет запущенного процесса\n# nice: от -20 (высокий приоритет) до 19 (низкий)\n# Только root может устанавливать отрицательный nice\n\n# === LSOF — открытые файлы/сокеты ===\nlsof -i :3000                     # что слушает порт 3000\nlsof -i tcp                       # все TCP соединения\nlsof -u username                  # файлы открытые пользователем\nlsof /var/log/app.log             # процессы использующие файл' },
        { type: 'tip', value: 'Используй kill -9 только как последнее средство. SIGKILL не даёт процессу закрыть файлы, соединения и освободить ресурсы. Сначала попробуй kill (SIGTERM) и подожди несколько секунд.' }
      ]
    },
    {
      id: 4,
      title: 'Права доступа (chmod/chown/su/sudo)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Linux использует модель прав Unix: владелец (owner), группа (group), остальные (others). Каждая категория имеет права: чтение (r=4), запись (w=2), выполнение (x=1).' },
        { type: 'code', language: 'bash', value: '# === ПРОСМОТР ПРАВ ===\nls -l file.txt\n# -rwxr-xr-- 1 alice devs 1024 Jan 1 12:00 file.txt\n# тип|owner|group|others\n# r=4, w=2, x=1\n# -  = файл, d = директория, l = symlink\n\n# === CHMOD — изменить права ===\n# Числовой (octal) режим:\nchmod 755 file.txt                # rwxr-xr-x (owner: все, group/others: r+x)\nchmod 644 file.txt                # rw-r--r-- (стандарт для файлов)\nchmod 600 .ssh/id_rsa             # rw------- (приватный ключ)\nchmod 700 .ssh                    # rwx------ (директория .ssh)\nchmod 777 file.txt                # rwxrwxrwx (все права всем — опасно!)\nchmod 000 file.txt                # ---------- (никаких прав)\n\n# Символьный режим:\nchmod u+x file.txt                # добавить execute для owner\nchmod g-w file.txt                # убрать write для group\nchmod o-r file.txt                # убрать read для others\nchmod a+r file.txt                # добавить read для всех (all)\nchmod u=rwx,g=rx,o=r file.txt    # задать явно\nchmod +x script.sh                # добавить execute всем (коротко)\n\n# Рекурсивно:\nchmod -R 755 /var/www/            # все файлы и директории\nchmod -R u+rX,g+rX,o-rwx /app/  # X = execute только если директория\n\n# === CHOWN — изменить владельца ===\nchown alice file.txt              # сменить владельца\nchown alice:devs file.txt         # сменить владельца и группу\nchown :devs file.txt              # только группу\nchown -R alice:devs /var/www/     # рекурсивно\n\n# === CHGRP — изменить группу ===\nchgrp devs file.txt\nchgrp -R devs /var/www/\n\n# === СПЕЦИАЛЬНЫЕ БИТЫ ===\n# SUID (4000) — выполнять от имени владельца файла:\nchmod 4755 /usr/bin/program       # s вместо x у owner\nls -l /usr/bin/sudo               # -rwsr-xr-x\n\n# SGID (2000) — выполнять от имени группы файла / наследовать группу:\nchmod 2755 /var/www/              # g+s на директорию: новые файлы → группа директории\n\n# Sticky bit (1000) — только владелец может удалить:\nchmod 1777 /tmp                   # rwxrwxrwt — стандарт для /tmp\n\n# === SU / SUDO ===\nsu                                # стать root (нужен пароль root)\nsu username                       # стать другим пользователем\nsu - username                     # с загрузкой окружения пользователя\nexit                              # вернуться к своему пользователю\n\nsudo command                      # выполнить команду от root\nsudo -u alice command             # выполнить от имени alice\nsudo -i                           # интерактивная оболочка root\nsudo -s                           # shell с правами root\nsudo !!                           # повторить предыдущую команду с sudo\n\n# Редактировать sudoers:\nvisudo                            # /etc/sudoers безопасно\n# Формат: user ALL=(ALL:ALL) ALL\n# alice ALL=(ALL) NOPASSWD: /usr/bin/apt\n\n# === UMASK — маска по умолчанию ===\numask                             # показать текущую маску\numask 022                         # файлы: 644, директории: 755\numask 027                         # файлы: 640, директории: 750\n# umask вычитается из 666 (файлы) и 777 (директории)\n\n# === ПОЛЬЗОВАТЕЛИ И ГРУППЫ ===\nwhoami                            # текущий пользователь\nid                                # UID, GID, группы\ngroups                            # группы текущего пользователя\ncat /etc/passwd                   # все пользователи\nuseradd -m -s /bin/bash alice     # создать пользователя\nusermod -aG sudo alice            # добавить в группу\npasswd alice                      # сменить пароль\ngroupadd devs                     # создать группу' },
        { type: 'note', value: 'Стандартные права: файлы 644 (rw-r--r--), директории 755 (rwxr-xr-x), скрипты 755, приватные ключи 600. Никогда не ставь 777 в production — это открывает запись для всех пользователей системы.' }
      ]
    },
    {
      id: 5,
      title: 'Сеть (curl/wget/ssh/scp/ping/netstat)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Сетевые утилиты Linux для диагностики, передачи данных и удалённого доступа.' },
        { type: 'code', language: 'bash', value: '# === CURL — HTTP запросы ===\ncurl https://api.example.com/users\ncurl -X POST https://api.example.com/users \\\n  -H "Content-Type: application/json" \\\n  -d \'{"name":"Alice"}\'\ncurl -X POST -d "user=alice&pass=123" https://example.com/login\ncurl -u user:password https://protected.com    # Basic Auth\ncurl -H "Authorization: Bearer token" https://api.com\ncurl -o output.json https://api.com/data       # сохранить в файл\ncurl -O https://example.com/file.zip           # файл с оригинальным именем\ncurl -L https://example.com                   # следовать редиректам\ncurl -I https://example.com                   # только заголовки (HEAD)\ncurl -v https://example.com                   # verbose (заголовки + тело)\ncurl -s https://example.com                   # silent (без прогресс-бара)\ncurl -k https://self-signed.example.com        # игнорировать TLS ошибки\ncurl --connect-timeout 5 https://example.com   # таймаут соединения\ncurl -x http://proxy:8080 https://example.com  # через прокси\n\n# Полезные комбинации:\ncurl -s https://api.com | python3 -m json.tool  # форматировать JSON\ncurl -w "%{http_code}" -o /dev/null -s https://example.com  # только статус код\n\n# === WGET — скачивание файлов ===\nwget https://example.com/file.zip\nwget -O myfile.zip https://example.com/file.zip  # другое имя\nwget -c https://example.com/bigfile.zip          # продолжить прерванную загрузку\nwget -r -np https://example.com/docs/            # рекурсивно (без родительских)\nwget -q https://example.com                      # тихий режим\nwget --limit-rate=1m https://example.com/file    # ограничить скорость\n\n# === SSH ===\nssh user@hostname\nssh user@hostname -p 2222          # нестандартный порт\nssh -i ~/.ssh/key.pem user@host    # конкретный ключ\nssh -L 8080:localhost:80 user@host # local port forward\nssh -R 8080:localhost:3000 user@host  # remote port forward\nssh -D 1080 user@host              # SOCKS proxy\nssh -N -f -L 5432:db:5432 user@jumphost  # фоновый туннель\n\n# Генерация ключей:\nssh-keygen -t ed25519 -C "email@example.com"\nssh-keygen -t rsa -b 4096 -C "email@example.com"\nssh-copy-id user@hostname          # скопировать публичный ключ\ncat ~/.ssh/id_ed25519.pub >> ~/.ssh/authorized_keys\n\n# ~/.ssh/config:\n# Host myserver\n#   HostName 192.168.1.100\n#   User alice\n#   IdentityFile ~/.ssh/id_ed25519\n#   Port 2222\n\n# === SCP — копирование через SSH ===\nscp file.txt user@host:/remote/path/\nscp user@host:/remote/file.txt .\nscp -r ./dir user@host:/path/      # рекурсивно\nscp -P 2222 file.txt user@host:/  # нестандартный порт\n\n# === PING ===\nping google.com                    # проверить доступность\nping -c 4 google.com               # 4 пакета\nping -i 0.5 google.com             # интервал 0.5 сек\ntraceroute google.com              # маршрут пакетов\nmtr google.com                     # traceroute + ping в реальном времени\n\n# === СЕТЕВЫЕ УТИЛИТЫ ===\nnetstat -tulnp                     # открытые порты и процессы (устаревший)\nss -tulnp                          # современная замена netstat\nss -tulnp | grep :3000             # кто слушает порт 3000\nlsof -i :3000                      # то же через lsof\nnmap -p 1-1000 hostname            # сканировать порты 1-1000\nnmap -sV hostname                  # определить версии сервисов\n\nip a                               # IP адреса интерфейсов (ip addr)\nip r                               # таблица маршрутизации (ip route)\nifconfig                           # устаревший ip a\nroute -n                           # таблица маршрутов\n\n# DNS:\nnslookup google.com                # DNS запрос\ndig google.com                     # подробный DNS запрос\ndig google.com A                   # A записи\ndig google.com MX                  # MX записи\ndig @8.8.8.8 google.com            # через конкретный DNS сервер\nhost google.com                    # краткий DNS запрос\n\ncat /etc/hosts                     # локальные DNS записи\ncat /etc/resolv.conf               # настройки DNS' },
        { type: 'tip', value: 'ssh -L 5432:database-server:5432 user@jumphost создаёт туннель к БД через jump-хост. После этого подключайся к localhost:5432 — трафик будет идти через SSH. Незаменимо при работе с БД в закрытой сети.' }
      ]
    },
    {
      id: 6,
      title: 'Архивы и диски (tar/zip/df/du/mount)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Архивирование и сжатие файлов, мониторинг дискового пространства и управление файловыми системами.' },
        { type: 'code', language: 'bash', value: '# === TAR — архивирование ===\n# Создание архива:\ntar -czf archive.tar.gz directory/     # создать .tar.gz (gzip)\ntar -cjf archive.tar.bz2 directory/   # создать .tar.bz2 (bzip2)\ntar -cJf archive.tar.xz directory/    # создать .tar.xz (xz, лучшее сжатие)\ntar -cf archive.tar directory/         # без сжатия\ntar -czf archive.tar.gz file1 file2    # несколько файлов\ntar -czf archive.tar.gz -C /var www    # сменить директорию перед архивацией\n--exclude=\'.git\' -czf arc.tar.gz dir/  # исключить .git\n\n# Распаковка:\ntar -xzf archive.tar.gz               # распаковать .tar.gz\ntar -xjf archive.tar.bz2              # распаковать .tar.bz2\ntar -xJf archive.tar.xz               # распаковать .tar.xz\ntar -xzf archive.tar.gz -C /target/   # в конкретную директорию\n\n# Просмотр:\ntar -tzf archive.tar.gz                # список файлов без распаковки\ntar -tzf archive.tar.gz | head -20\n\n# Ключи tar: c=create, x=extract, t=list, z=gzip, j=bzip2, J=xz, f=file, v=verbose\n\n# === ZIP ===\nzip archive.zip file1 file2\nzip -r archive.zip directory/          # рекурсивно\nzip -r archive.zip . -x \'*.git*\'       # исключить .git\nzip -e secret.zip file.txt             # с паролем\nunzip archive.zip                      # распаковать\nunzip archive.zip -d /target/          # в директорию\nunzip -l archive.zip                   # список файлов\nunzip -p archive.zip file.txt          # содержимое файла без распаковки\n\n# === GZIP / BZIP2 / XZ ===\ngzip file.txt                          # сжать (удаляет оригинал, создаёт file.txt.gz)\ngzip -k file.txt                       # -k сохранить оригинал\ngzip -d file.txt.gz                    # распаковать\ngunzip file.txt.gz                     # то же\nbzip2 file.txt                         # .bz2\nxz file.txt                            # .xz (лучшее сжатие, медленнее)\n\n# === DF — использование файловых систем ===\ndf -h                                  # в читаемом формате (K/M/G)\ndf -h /var                             # конкретная точка монтирования\ndf -i                                  # использование inodes\ndf -T                                  # с типом файловой системы\n\n# === DU — размер директорий ===\ndu -sh directory/                      # суммарный размер\ndu -sh *                               # размер всех в текущей директории\ndu -sh * | sort -rh                    # + сортировка по размеру\ndu -ah /var/log/                       # все файлы с размерами\ndu -h --max-depth=1 /var/             # только 1 уровень вглубь\ndu -sh /var/log/* 2>/dev/null | sort -rh | head -20  # топ 20 по размеру\n\n# === MOUNT — монтирование ===\nmount                                  # список смонтированных файловых систем\nmount -t ext4 /dev/sdb1 /mnt/disk     # смонтировать раздел\nmount -t nfs server:/share /mnt/nfs   # NFS\numount /mnt/disk                       # размонтировать\numount -l /mnt/disk                    # lazy unmount (когда занято)\n\n# /etc/fstab — автомонтирование при загрузке\nblkid                                  # UUID дисков\nlsblk                                  # блочные устройства\nfdisk -l                               # разделы дисков\nparted -l                              # разделы (GPT/MBR)\n\n# === УПРАВЛЕНИЕ ДИСКОВЫМ ПРОСТРАНСТВОМ ===\n# Найти большие файлы:\nfind / -size +100M -type f 2>/dev/null | sort\ndu -ah /var/log | sort -rh | head -20\n\n# Очистить логи:\njournalctl --vacuum-size=100M          # оставить только 100M логов\njournalctl --vacuum-time=7d            # логи старше 7 дней\ntruncate -s 0 /var/log/app.log         # очистить файл без удаления\n\n# Синхронизация:\nrsync -av src/ dest/                   # синхронизировать директории\nrsync -avz src/ user@host:/dest/       # по SSH\nrsync -av --delete src/ dest/          # с удалением лишнего в dest' },
        { type: 'note', value: 'Мнемоника tar: -czf = "create zip file", -xzf = "extract zip file". v (verbose) добавляй при отладке, в скриптах убирай для чистого вывода. rsync -av --delete безопаснее cp -r для синхронизации — показывает что делает и поддерживает продолжение.' }
      ]
    }
  ]
}
