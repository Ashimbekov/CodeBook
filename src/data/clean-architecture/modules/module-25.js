export default {
  id: 25,
  title: 'Практический проект: Clean Architecture',
  description: 'Полноценный проект: система управления задачами (Task Manager) с Clean Architecture, DDD, CQRS и тестами.',
  lessons: [
    {
      id: 1,
      title: 'Проектирование: требования и архитектура',
      type: 'theory',
      content: [
        { type: 'text', value: 'В этом модуле мы построим полноценное приложение Task Manager с применением всех изученных принципов: Clean Architecture, DDD, CQRS, Domain Events.' },
        { type: 'heading', value: 'Функциональные требования' },
        { type: 'list', value: [
          'Создание проектов с участниками',
          'Создание задач внутри проекта',
          'Назначение задач на участников',
          'Смена статуса: todo → in_progress → review → done',
          'Комментарии к задачам',
          'Уведомления при изменении задачи'
        ]},
        { type: 'heading', value: 'Bounded Contexts' },
        { type: 'list', value: [
          'Project Management — проекты и участники',
          'Task Tracking — задачи, статусы, комментарии',
          'Notification — уведомления (email, in-app)'
        ]},
        { type: 'heading', value: 'Структура проекта' },
        { type: 'code', language: 'java', value: 'task-manager/\n├── domain/\n│   ├── model/\n│   │   ├── project/\n│   │   │   ├── Project.java          # Aggregate Root\n│   │   │   ├── ProjectId.java        # Value Object\n│   │   │   └── Member.java           # Entity внутри агрегата\n│   │   └── task/\n│   │       ├── Task.java             # Aggregate Root\n│   │       ├── TaskId.java\n│   │       ├── TaskStatus.java\n│   │       └── Comment.java          # Entity внутри агрегата\n│   ├── valueobject/\n│   │   ├── UserId.java\n│   │   ├── Email.java\n│   │   └── Priority.java\n│   ├── event/\n│   │   ├── TaskCreatedEvent.java\n│   │   ├── TaskStatusChangedEvent.java\n│   │   └── CommentAddedEvent.java\n│   ├── repository/\n│   │   ├── ProjectRepository.java\n│   │   └── TaskRepository.java\n│   └── service/\n│       └── TaskAssignmentService.java # Domain Service\n├── application/\n│   ├── command/\n│   │   ├── CreateProjectUseCase.java\n│   │   ├── CreateTaskUseCase.java\n│   │   ├── ChangeTaskStatusUseCase.java\n│   │   └── AddCommentUseCase.java\n│   ├── query/\n│   │   ├── GetProjectDetailsHandler.java\n│   │   ├── GetTasksByProjectHandler.java\n│   │   └── GetTaskDetailsHandler.java\n│   └── dto/\n│       ├── ProjectDto.java\n│       └── TaskDto.java\n├── infrastructure/\n│   ├── persistence/\n│   │   ├── JpaProjectRepository.java\n│   │   ├── JpaTaskRepository.java\n│   │   └── entities/\n│   ├── notification/\n│   │   └── EmailNotificationAdapter.java\n│   └── config/\n│       └── AppConfig.java\n└── presentation/\n    ├── ProjectController.java\n    ├── TaskController.java\n    └── request/' },
        { type: 'tip', value: 'Это минимальный, но полноценный пример. В реальном проекте добавятся: аутентификация, пагинация, кеширование, мониторинг.' }
      ]
    },
    {
      id: 2,
      title: 'Доменный слой: агрегаты и бизнес-правила',
      type: 'theory',
      content: [
        { type: 'text', value: 'Реализуем два агрегата: Project (с участниками) и Task (со статусами и комментариями). Бизнес-правила инкапсулированы внутри агрегатов.' },
        { type: 'code', language: 'java', value: '// Aggregate Root: Project\npublic class Project extends AggregateRoot {\n    private ProjectId id;\n    private String name;\n    private String description;\n    private UserId ownerId;\n    private List<Member> members;\n    private ProjectStatus status;\n    \n    public static Project create(String name, String description, UserId ownerId) {\n        if (name == null || name.isBlank()) {\n            throw new InvalidProjectNameException();\n        }\n        Project project = new Project();\n        project.id = ProjectId.generate();\n        project.name = name;\n        project.description = description;\n        project.ownerId = ownerId;\n        project.members = new ArrayList<>();\n        project.members.add(new Member(ownerId, MemberRole.OWNER));\n        project.status = ProjectStatus.ACTIVE;\n        project.raise(new ProjectCreatedEvent(project.id, ownerId));\n        return project;\n    }\n    \n    public void addMember(UserId userId, MemberRole role) {\n        if (this.status != ProjectStatus.ACTIVE) {\n            throw new ProjectNotActiveException(this.id);\n        }\n        if (members.stream().anyMatch(m -> m.userId().equals(userId))) {\n            throw new MemberAlreadyExistsException(userId);\n        }\n        members.add(new Member(userId, role));\n    }\n    \n    public boolean isMember(UserId userId) {\n        return members.stream().anyMatch(m -> m.userId().equals(userId));\n    }\n    \n    public void archive() {\n        this.status = ProjectStatus.ARCHIVED;\n        raise(new ProjectArchivedEvent(this.id));\n    }\n}' },
        { type: 'code', language: 'java', value: '// Aggregate Root: Task\npublic class Task extends AggregateRoot {\n    private TaskId id;\n    private ProjectId projectId;\n    private String title;\n    private String description;\n    private TaskStatus status;\n    private Priority priority;\n    private UserId assigneeId;\n    private UserId createdBy;\n    private List<Comment> comments;\n    private Instant createdAt;\n    \n    // State Machine: допустимые переходы\n    private static final Map<TaskStatus, Set<TaskStatus>> TRANSITIONS = Map.of(\n        TaskStatus.TODO,        Set.of(TaskStatus.IN_PROGRESS, TaskStatus.CANCELLED),\n        TaskStatus.IN_PROGRESS, Set.of(TaskStatus.REVIEW, TaskStatus.TODO, TaskStatus.CANCELLED),\n        TaskStatus.REVIEW,      Set.of(TaskStatus.DONE, TaskStatus.IN_PROGRESS),\n        TaskStatus.DONE,        Set.of(TaskStatus.TODO), // можно вернуть\n        TaskStatus.CANCELLED,   Set.of(TaskStatus.TODO)  // можно восстановить\n    );\n    \n    public static Task create(\n        ProjectId projectId, String title, String description,\n        Priority priority, UserId createdBy\n    ) {\n        if (title == null || title.isBlank()) throw new InvalidTaskTitleException();\n        Task task = new Task();\n        task.id = TaskId.generate();\n        task.projectId = projectId;\n        task.title = title;\n        task.description = description;\n        task.status = TaskStatus.TODO;\n        task.priority = priority;\n        task.createdBy = createdBy;\n        task.comments = new ArrayList<>();\n        task.createdAt = Instant.now();\n        task.raise(new TaskCreatedEvent(task.id, projectId, createdBy));\n        return task;\n    }\n    \n    public void changeStatus(TaskStatus newStatus, UserId changedBy) {\n        Set<TaskStatus> allowed = TRANSITIONS.getOrDefault(this.status, Set.of());\n        if (!allowed.contains(newStatus)) {\n            throw new InvalidTaskTransitionException(this.status, newStatus);\n        }\n        TaskStatus oldStatus = this.status;\n        this.status = newStatus;\n        raise(new TaskStatusChangedEvent(this.id, oldStatus, newStatus, changedBy));\n    }\n    \n    public void assignTo(UserId assigneeId) {\n        this.assigneeId = assigneeId;\n        raise(new TaskAssignedEvent(this.id, assigneeId));\n    }\n    \n    public void addComment(String text, UserId authorId) {\n        if (text == null || text.isBlank()) throw new EmptyCommentException();\n        Comment comment = new Comment(CommentId.generate(), text, authorId, Instant.now());\n        this.comments.add(comment);\n        raise(new CommentAddedEvent(this.id, comment.id(), authorId));\n    }\n}' },
        { type: 'note', value: 'Task — основной агрегат с State Machine. Project — агрегат, управляющий участниками. Связь между ними — через ProjectId (ссылка по ID, не прямая ссылка).' }
      ]
    },
    {
      id: 3,
      title: 'Application-слой: Use Cases',
      type: 'theory',
      content: [
        { type: 'text', value: 'Use Cases координируют бизнес-процесс. Каждый Use Case — один бизнес-сценарий. Commands для записи, Queries для чтения.' },
        { type: 'code', language: 'java', value: '// Command: Создать задачу\npublic class CreateTaskUseCase {\n    private final TaskRepository taskRepo;\n    private final ProjectRepository projectRepo;\n    private final EventPublisher eventPublisher;\n    \n    public TaskDto execute(CreateTaskCommand cmd) {\n        // Проверка: проект существует и пользователь — участник\n        Project project = projectRepo.findById(new ProjectId(cmd.projectId()))\n            .orElseThrow(() -> new ProjectNotFoundException(cmd.projectId()));\n        \n        if (!project.isMember(new UserId(cmd.createdBy()))) {\n            throw new NotAProjectMemberException(cmd.createdBy(), cmd.projectId());\n        }\n        \n        // Создание задачи (бизнес-логика в Task.create())\n        Task task = Task.create(\n            project.id(),\n            cmd.title(),\n            cmd.description(),\n            Priority.valueOf(cmd.priority()),\n            new UserId(cmd.createdBy())\n        );\n        \n        // Назначить исполнителя (если указан)\n        if (cmd.assigneeId() != null) {\n            if (!project.isMember(new UserId(cmd.assigneeId()))) {\n                throw new NotAProjectMemberException(cmd.assigneeId(), cmd.projectId());\n            }\n            task.assignTo(new UserId(cmd.assigneeId()));\n        }\n        \n        taskRepo.save(task);\n        task.domainEvents().forEach(eventPublisher::publish);\n        task.clearDomainEvents();\n        \n        return TaskDto.from(task);\n    }\n}\n\n// Command: Сменить статус\npublic class ChangeTaskStatusUseCase {\n    private final TaskRepository taskRepo;\n    private final EventPublisher eventPublisher;\n    \n    public void execute(ChangeTaskStatusCommand cmd) {\n        Task task = taskRepo.findById(new TaskId(cmd.taskId()))\n            .orElseThrow(() -> new TaskNotFoundException(cmd.taskId()));\n        \n        task.changeStatus(\n            TaskStatus.valueOf(cmd.newStatus()),\n            new UserId(cmd.changedBy())\n        );\n        \n        taskRepo.save(task);\n        task.domainEvents().forEach(eventPublisher::publish);\n        task.clearDomainEvents();\n    }\n}' },
        { type: 'code', language: 'java', value: '// Query: Получить задачи проекта (Read Model)\npublic class GetTasksByProjectHandler {\n    private final JdbcTemplate jdbc; // прямой SQL для чтения!\n    \n    public List<TaskListItemDto> handle(GetTasksByProjectQuery query) {\n        return jdbc.query(\n            """\n            SELECT t.id, t.title, t.status, t.priority,\n                   u.name as assignee_name, t.created_at\n            FROM tasks t\n            LEFT JOIN users u ON t.assignee_id = u.id\n            WHERE t.project_id = ?\n            ORDER BY t.priority DESC, t.created_at DESC\n            LIMIT ? OFFSET ?\n            """,\n            (rs, row) -> new TaskListItemDto(\n                rs.getString("id"), rs.getString("title"),\n                rs.getString("status"), rs.getString("priority"),\n                rs.getString("assignee_name"), rs.getString("created_at")\n            ),\n            query.projectId(), query.pageSize(), query.offset()\n        );\n    }\n}' },
        { type: 'tip', value: 'Write-сторона (Commands) работает через агрегаты и репозитории. Read-сторона (Queries) может читать прямо из БД с JOIN — для чтения не нужны агрегаты!' }
      ]
    },
    {
      id: 4,
      title: 'Infrastructure и Presentation',
      type: 'theory',
      content: [
        { type: 'text', value: 'Infrastructure реализует интерфейсы Domain. Presentation принимает HTTP-запросы и вызывает Use Cases.' },
        { type: 'code', language: 'java', value: '// Infrastructure: Repository\npublic class JpaTaskRepository implements TaskRepository {\n    private final SpringDataTaskRepo jpaRepo;\n    private final TaskMapper mapper;\n    \n    @Override\n    public Optional<Task> findById(TaskId id) {\n        return jpaRepo.findById(id.value()).map(mapper::toDomain);\n    }\n    \n    @Override\n    public void save(Task task) {\n        jpaRepo.save(mapper.toJpa(task));\n    }\n    \n    @Override\n    public List<Task> findByProjectId(ProjectId projectId) {\n        return jpaRepo.findByProjectId(projectId.value())\n            .stream().map(mapper::toDomain).toList();\n    }\n}\n\n// Infrastructure: Notification\npublic class EmailNotificationAdapter implements NotificationSender {\n    private final JavaMailSender mailSender;\n    \n    @Override\n    public void sendTaskAssigned(UserId assignee, TaskId taskId) {\n        // отправка email о назначении задачи\n    }\n}\n\n// Event Handler: отправка уведомлений\n@Component\npublic class TaskEventNotificationHandler {\n    private final NotificationSender notifier;\n    private final UserRepository userRepo;\n    \n    @EventListener\n    public void onTaskAssigned(TaskAssignedEvent event) {\n        notifier.sendTaskAssigned(event.assigneeId(), event.taskId());\n    }\n    \n    @EventListener\n    public void onTaskStatusChanged(TaskStatusChangedEvent event) {\n        // уведомить создателя и назначенного\n    }\n}' },
        { type: 'code', language: 'java', value: '// Presentation: REST Controller\n@RestController\n@RequestMapping("/api/v1")\npublic class TaskController {\n    private final CreateTaskUseCase createTask;\n    private final ChangeTaskStatusUseCase changeStatus;\n    private final AddCommentUseCase addComment;\n    private final GetTasksByProjectHandler getTasksByProject;\n    \n    @PostMapping("/projects/{projectId}/tasks")\n    public ResponseEntity<TaskDto> create(\n        @PathVariable String projectId,\n        @Valid @RequestBody CreateTaskRequest req,\n        @AuthenticationPrincipal UserPrincipal user\n    ) {\n        CreateTaskCommand cmd = new CreateTaskCommand(\n            projectId, req.title(), req.description(),\n            req.priority(), user.getId(), req.assigneeId()\n        );\n        TaskDto result = createTask.execute(cmd);\n        return ResponseEntity.status(201).body(result);\n    }\n    \n    @PostMapping("/tasks/{taskId}/status")\n    public ResponseEntity<Void> changeStatus(\n        @PathVariable String taskId,\n        @RequestBody ChangeStatusRequest req,\n        @AuthenticationPrincipal UserPrincipal user\n    ) {\n        changeStatus.execute(new ChangeTaskStatusCommand(\n            taskId, req.newStatus(), user.getId()\n        ));\n        return ResponseEntity.noContent().build();\n    }\n    \n    @GetMapping("/projects/{projectId}/tasks")\n    public ResponseEntity<List<TaskListItemDto>> listTasks(\n        @PathVariable String projectId,\n        @RequestParam(defaultValue = "1") int page,\n        @RequestParam(defaultValue = "20") int size\n    ) {\n        List<TaskListItemDto> tasks = getTasksByProject.handle(\n            new GetTasksByProjectQuery(projectId, page, size)\n        );\n        return ResponseEntity.ok(tasks);\n    }\n}' },
        { type: 'note', value: 'Контроллер тонкий: извлекает данные из HTTP-запроса, создаёт Command/Query, вызывает Use Case, возвращает HTTP-ответ. Ноль бизнес-логики.' }
      ]
    },
    {
      id: 5,
      title: 'Тестирование проекта',
      type: 'theory',
      content: [
        { type: 'text', value: 'Полный набор тестов для нашего проекта: unit-тесты Domain, тесты Use Cases с фейками, Architecture Tests.' },
        { type: 'code', language: 'java', value: '// Unit-тесты Domain\nclass TaskTest {\n    @Test\n    void shouldCreateTask() {\n        Task task = Task.create(\n            ProjectId.generate(), "Implement login",\n            "JWT auth", Priority.HIGH, UserId.generate()\n        );\n        assertEquals(TaskStatus.TODO, task.status());\n        assertThat(task.domainEvents()).hasSize(1)\n            .first().isInstanceOf(TaskCreatedEvent.class);\n    }\n    \n    @Test\n    void shouldTransitionThroughStatuses() {\n        Task task = createTask();\n        UserId user = UserId.generate();\n        \n        task.changeStatus(TaskStatus.IN_PROGRESS, user);\n        assertEquals(TaskStatus.IN_PROGRESS, task.status());\n        \n        task.changeStatus(TaskStatus.REVIEW, user);\n        assertEquals(TaskStatus.REVIEW, task.status());\n        \n        task.changeStatus(TaskStatus.DONE, user);\n        assertEquals(TaskStatus.DONE, task.status());\n    }\n    \n    @Test\n    void shouldNotSkipStatuses() {\n        Task task = createTask(); // status = TODO\n        assertThrows(InvalidTaskTransitionException.class, () ->\n            task.changeStatus(TaskStatus.DONE, UserId.generate())\n        );\n    }\n    \n    @Test\n    void shouldAddComment() {\n        Task task = createTask();\n        task.addComment("Нужна помощь с дизайном", UserId.generate());\n        assertEquals(1, task.commentCount());\n    }\n    \n    @Test\n    void shouldRejectEmptyComment() {\n        Task task = createTask();\n        assertThrows(EmptyCommentException.class, () ->\n            task.addComment("", UserId.generate())\n        );\n    }\n}' },
        { type: 'code', language: 'java', value: '// Тесты Use Case с фейками\nclass CreateTaskUseCaseTest {\n    private InMemoryTaskRepository taskRepo;\n    private InMemoryProjectRepository projectRepo;\n    private FakeEventPublisher eventPublisher;\n    private CreateTaskUseCase useCase;\n    \n    @BeforeEach\n    void setUp() {\n        taskRepo = new InMemoryTaskRepository();\n        projectRepo = new InMemoryProjectRepository();\n        eventPublisher = new FakeEventPublisher();\n        useCase = new CreateTaskUseCase(taskRepo, projectRepo, eventPublisher);\n        \n        // Подготовка: создать проект с участником\n        Project project = Project.create("Test Project", "desc", new UserId(OWNER_ID));\n        projectRepo.save(project);\n    }\n    \n    @Test\n    void shouldCreateTaskInProject() {\n        CreateTaskCommand cmd = new CreateTaskCommand(\n            PROJECT_ID, "New Task", "Description", "HIGH", OWNER_ID, null\n        );\n        TaskDto result = useCase.execute(cmd);\n        \n        assertNotNull(result.id());\n        assertEquals("New Task", result.title());\n        assertEquals("TODO", result.status());\n        assertEquals(1, taskRepo.count());\n        assertEquals(1, eventPublisher.publishedEvents().size());\n    }\n    \n    @Test\n    void shouldRejectIfNotMember() {\n        CreateTaskCommand cmd = new CreateTaskCommand(\n            PROJECT_ID, "Task", "Desc", "LOW", "stranger-id", null\n        );\n        assertThrows(NotAProjectMemberException.class, () -> useCase.execute(cmd));\n        assertEquals(0, taskRepo.count());\n    }\n}\n\n// Architecture Test\n@ArchTest\nstatic final ArchRule domainIndependence =\n    noClasses().that().resideInAPackage("..domain..")\n        .should().dependOnClassesThat()\n        .resideInAnyPackage("..infrastructure..", "..presentation..", "org.springframework..");' },
        { type: 'tip', value: 'Unit-тесты Task: мгновенные, без зависимостей, проверяют ВСЕ бизнес-правила. Тесты Use Case: с фейками, проверяют координацию. Architecture Tests: автоматически проверяют правила зависимостей.' }
      ]
    },
    {
      id: 6,
      title: 'Composition Root и запуск',
      type: 'theory',
      content: [
        { type: 'text', value: 'Composition Root собирает всё вместе: Domain + Application + Infrastructure + Presentation. Здесь конкретные реализации привязываются к интерфейсам.' },
        { type: 'code', language: 'java', value: '// Spring Boot Composition Root\n@Configuration\npublic class AppConfig {\n    \n    // Repositories\n    @Bean\n    public TaskRepository taskRepository(SpringDataTaskRepo jpaRepo, TaskMapper mapper) {\n        return new JpaTaskRepository(jpaRepo, mapper);\n    }\n    \n    @Bean\n    public ProjectRepository projectRepository(SpringDataProjectRepo jpaRepo) {\n        return new JpaProjectRepository(jpaRepo, new ProjectMapper());\n    }\n    \n    // Use Cases\n    @Bean\n    public CreateTaskUseCase createTaskUseCase(\n        TaskRepository taskRepo,\n        ProjectRepository projectRepo,\n        EventPublisher eventPublisher\n    ) {\n        return new CreateTaskUseCase(taskRepo, projectRepo, eventPublisher);\n    }\n    \n    @Bean\n    public ChangeTaskStatusUseCase changeTaskStatusUseCase(\n        TaskRepository taskRepo,\n        EventPublisher eventPublisher\n    ) {\n        return new ChangeTaskStatusUseCase(taskRepo, eventPublisher);\n    }\n    \n    // Event Publisher\n    @Bean\n    public EventPublisher eventPublisher(ApplicationEventPublisher springPublisher) {\n        return new SpringEventPublisher(springPublisher);\n    }\n    \n    // Notification\n    @Bean\n    public NotificationSender notificationSender(JavaMailSender mailSender) {\n        return new EmailNotificationAdapter(mailSender);\n    }\n}' },
        { type: 'code', language: 'typescript', value: '// TypeScript: Pure DI Composition Root\nasync function main() {\n  const db = new Pool({ connectionString: process.env.DATABASE_URL });\n  const eventBus = new InMemoryEventBus();\n  \n  // Repositories\n  const taskRepo = new PgTaskRepository(db);\n  const projectRepo = new PgProjectRepository(db);\n  \n  // Use Cases\n  const createTask = new CreateTaskUseCase(taskRepo, projectRepo, eventBus);\n  const changeStatus = new ChangeTaskStatusUseCase(taskRepo, eventBus);\n  const addComment = new AddCommentUseCase(taskRepo, eventBus);\n  \n  // Query Handlers\n  const getTasksByProject = new GetTasksByProjectHandler(db);\n  \n  // Event Handlers\n  eventBus.on("TaskAssignedEvent", new NotifyOnTaskAssigned(new FakeEmailSender()));\n  \n  // Controllers\n  const taskController = new TaskController(createTask, changeStatus, addComment, getTasksByProject);\n  \n  // Express App\n  const app = express();\n  app.use(express.json());\n  app.post("/api/v1/projects/:id/tasks", taskController.create.bind(taskController));\n  app.post("/api/v1/tasks/:id/status", taskController.changeStatus.bind(taskController));\n  app.get("/api/v1/projects/:id/tasks", taskController.list.bind(taskController));\n  app.use(errorHandler);\n  \n  app.listen(3000, () => console.log("Task Manager API on port 3000"));\n}\n\nmain();' },
        { type: 'note', value: 'Composition Root — единственное место, знающее обо всех классах. Domain не знает о Spring/Express. Application не знает о JPA/PostgreSQL. Всё соединяется в одном месте.' }
      ]
    },
    {
      id: 7,
      title: 'Финальная практика: полный проект',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте полный модуль Task Manager: создание задачи, смена статуса, комментарии, уведомления.',
      requirements: [
        'Domain: Task Aggregate с State Machine (todo → in_progress → review → done)',
        'Domain: Value Objects (TaskId, Priority), Domain Events',
        'Application: CreateTaskUseCase, ChangeTaskStatusUseCase, AddCommentUseCase',
        'Query Handler: GetTasksByProjectHandler с пагинацией',
        'Infrastructure: InMemoryTaskRepository для тестов',
        'Presentation: TaskController с REST API',
        'Тесты: unit (Task), application (Use Cases), architecture'
      ],
      hint: 'Начните с Domain (Task, TaskStatus, events). Затем Application (Use Cases). Затем Infrastructure (фейки для тестов). Последним — Presentation (Controller). Тесты пишите параллельно.',
      expectedOutput: 'Полнофункциональный модуль: Domain с бизнес-логикой, Application с координацией, Infrastructure с репозиториями, Presentation с REST API. Тесты всех уровней.',
      solution: '// === DOMAIN ===\n\n// Value Objects\nclass TaskId {\n  constructor(readonly value: string) {}\n  static generate(): TaskId { return new TaskId(crypto.randomUUID()); }\n}\n\nenum TaskStatus { TODO = "TODO", IN_PROGRESS = "IN_PROGRESS", REVIEW = "REVIEW", DONE = "DONE" }\nenum Priority { LOW = "LOW", MEDIUM = "MEDIUM", HIGH = "HIGH" }\n\n// Aggregate\nclass Task {\n  private events: DomainEvent[] = [];\n  private comments: Comment[] = [];\n\n  private constructor(\n    public readonly id: TaskId,\n    public readonly projectId: string,\n    public title: string,\n    public description: string,\n    private _status: TaskStatus,\n    public priority: Priority,\n    private _assigneeId: string | null,\n    public readonly createdBy: string\n  ) {}\n\n  static create(projectId: string, title: string, desc: string, priority: Priority, createdBy: string): Task {\n    if (!title.trim()) throw new Error("Title is required");\n    const task = new Task(TaskId.generate(), projectId, title, desc, TaskStatus.TODO, priority, null, createdBy);\n    task.events.push({ type: "TaskCreated", taskId: task.id.value, projectId });\n    return task;\n  }\n\n  get status() { return this._status; }\n  get assigneeId() { return this._assigneeId; }\n\n  private static TRANSITIONS: Record<string, string[]> = {\n    TODO: ["IN_PROGRESS", "CANCELLED"],\n    IN_PROGRESS: ["REVIEW", "TODO"],\n    REVIEW: ["DONE", "IN_PROGRESS"],\n    DONE: ["TODO"],\n  };\n\n  changeStatus(newStatus: TaskStatus, changedBy: string): void {\n    const allowed = Task.TRANSITIONS[this._status] || [];\n    if (!allowed.includes(newStatus)) {\n      throw new Error(`Cannot transition from ${this._status} to ${newStatus}`);\n    }\n    const old = this._status;\n    this._status = newStatus;\n    this.events.push({ type: "TaskStatusChanged", taskId: this.id.value, from: old, to: newStatus, changedBy });\n  }\n\n  assignTo(userId: string): void {\n    this._assigneeId = userId;\n    this.events.push({ type: "TaskAssigned", taskId: this.id.value, assigneeId: userId });\n  }\n\n  addComment(text: string, authorId: string): void {\n    if (!text.trim()) throw new Error("Comment cannot be empty");\n    this.comments.push({ id: crypto.randomUUID(), text, authorId, createdAt: new Date() });\n    this.events.push({ type: "CommentAdded", taskId: this.id.value, authorId });\n  }\n\n  get commentCount() { return this.comments.length; }\n  pullEvents(): DomainEvent[] { const e = [...this.events]; this.events = []; return e; }\n}\n\n// Repository Interface\ninterface TaskRepository {\n  findById(id: TaskId): Promise<Task | null>;\n  save(task: Task): Promise<void>;\n  findByProjectId(projectId: string): Promise<Task[]>;\n}\n\n// === APPLICATION ===\nclass CreateTaskUseCase {\n  constructor(private taskRepo: TaskRepository, private projectRepo: ProjectRepository) {}\n  async execute(cmd: { projectId: string; title: string; description: string; priority: string; createdBy: string }) {\n    const project = await this.projectRepo.findById(cmd.projectId);\n    if (!project) throw new Error("Project not found");\n    if (!project.isMember(cmd.createdBy)) throw new Error("Not a member");\n    const task = Task.create(cmd.projectId, cmd.title, cmd.description, cmd.priority as Priority, cmd.createdBy);\n    await this.taskRepo.save(task);\n    return { id: task.id.value, title: task.title, status: task.status, priority: task.priority };\n  }\n}\n\nclass ChangeTaskStatusUseCase {\n  constructor(private taskRepo: TaskRepository) {}\n  async execute(cmd: { taskId: string; newStatus: string; changedBy: string }) {\n    const task = await this.taskRepo.findById(new TaskId(cmd.taskId));\n    if (!task) throw new Error("Task not found");\n    task.changeStatus(cmd.newStatus as TaskStatus, cmd.changedBy);\n    await this.taskRepo.save(task);\n  }\n}\n\n// === INFRASTRUCTURE ===\nclass InMemoryTaskRepository implements TaskRepository {\n  private tasks = new Map<string, Task>();\n  async findById(id: TaskId) { return this.tasks.get(id.value) || null; }\n  async save(task: Task) { this.tasks.set(task.id.value, task); }\n  async findByProjectId(projectId: string) {\n    return [...this.tasks.values()].filter(t => t.projectId === projectId);\n  }\n  count() { return this.tasks.size; }\n}\n\n// === TESTS ===\n// describe("Task", () => {\n//   it("should create with TODO status", ...)\n//   it("should transition TODO -> IN_PROGRESS -> REVIEW -> DONE", ...)\n//   it("should reject TODO -> DONE", ...)\n//   it("should add comment", ...)\n// });\n// describe("CreateTaskUseCase", () => {\n//   it("should create task in project", ...)\n//   it("should reject non-member", ...)\n// });',
      explanation: 'Полный проект Clean Architecture: Task (Domain) содержит бизнес-логику — State Machine, комментарии, события. CreateTaskUseCase (Application) координирует — проверяет проект, создаёт задачу, сохраняет. InMemoryTaskRepository (Infrastructure) — фейк для тестов. Controller (Presentation) — тонкий HTTP-адаптер. Зависимости: Presentation → Application → Domain ← Infrastructure. Domain не зависит ни от кого.'
    }
  ]
}
