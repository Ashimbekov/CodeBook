export default {
  id: 23,
  title: 'Безопасность мобильных приложений',
  description: 'Certificate pinning, безопасное хранение данных, OWASP Mobile Top 10, защита API коммуникаций, обфускация и tamper detection.',
  lessons: [
    {
      id: 1,
      title: 'OWASP Mobile Top 10 и поверхность атаки',
      type: 'theory',
      content: [
        { type: 'text', value: 'Мобильные приложения имеют уникальную поверхность атаки: устройство физически находится у пользователя (потенциального атакующего), трафик может быть перехвачен, бинарный код можно декомпилировать. OWASP Mobile Top 10 описывает основные риски.' },
        { type: 'heading', value: 'OWASP Mobile Top 10 (2024)' },
        { type: 'list', value: [
          'M1: Improper Credential Usage — хранение паролей в коде, слабые ключи',
          'M2: Inadequate Supply Chain Security — уязвимые SDK, вредоносные библиотеки',
          'M3: Insecure Authentication/Authorization — обход аутентификации, слабый PIN',
          'M4: Insufficient Input/Output Validation — SQL injection, XSS в WebView',
          'M5: Insecure Communication — отсутствие TLS, нет certificate pinning',
          'M6: Inadequate Privacy Controls — утечка PII, чрезмерные permissions',
          'M7: Insufficient Binary Protections — нет обфускации, отсутствие tamper detection',
          'M8: Security Misconfiguration — debug mode в production, открытые endpoints',
          'M9: Insecure Data Storage — данные в SharedPreferences/NSUserDefaults без шифрования',
          'M10: Insufficient Cryptography — слабые алгоритмы, захардкоженные ключи'
        ]},
        { type: 'code', language: 'bash', value: '# === Поверхность атаки мобильного приложения ===\n\n# 1. Клиентская сторона (устройство)\n#    - Декомпиляция APK/IPA\n#    - Анализ локального хранилища\n#    - Runtime hooking (Frida, Xposed)\n#    - Перехват трафика (Burp Suite + proxy)\n\n# 2. Сетевой уровень\n#    - Man-in-the-Middle (без cert pinning)\n#    - API endpoint enumeration\n#    - Replay attacks\n\n# 3. Серверная сторона\n#    - Те же уязвимости что и в веб (SQLi, IDOR, etc.)\n#    - Но клиент не контролируется сервером!\n\n# Декомпиляция Android APK\n# Jadx — декомпилятор Java/Kotlin\njadx -d output_dir application.apk\n# Результат: читаемый Java код!\n\n# Apktool — распаковка ресурсов и smali кода\napktool d application.apk -o output_dir\n\n# iOS: class-dump для Objective-C\nclass-dump -H App.app -o headers/\n\n# ВАЖНО: анализируйте только свои приложения!\n# Декомпиляция чужих приложений может нарушать лицензию' },
        { type: 'warning', value: 'Золотое правило мобильной безопасности: НИКОГДА не доверяйте клиенту. Любая проверка на клиенте может быть обойдена. Все критичные проверки (авторизация, валидация, бизнес-логика) ДОЛЖНЫ выполняться на сервере.' }
      ]
    },
    {
      id: 2,
      title: 'Безопасное хранение данных',
      type: 'theory',
      content: [
        { type: 'text', value: 'Небезопасное хранение данных — одна из главных уязвимостей мобильных приложений. SharedPreferences (Android) и NSUserDefaults (iOS) хранят данные в открытом виде. Для секретов нужен Android Keystore / iOS Keychain.' },
        { type: 'heading', value: 'Что НЕ хранить на устройстве' },
        { type: 'list', value: [
          'Пароли в открытом виде (используйте токены)',
          'API ключи серверных сервисов (AWS, Firebase admin)',
          'Приватные ключи шифрования',
          'Персональные данные без шифрования',
          'Session tokens в SharedPreferences/NSUserDefaults без шифрования'
        ]},
        { type: 'code', language: 'javascript', value: '// === Android: НЕБЕЗОПАСНОЕ хранение ===\n\n// ПЛОХО: SharedPreferences — данные в XML файле без шифрования\n// /data/data/com.app/shared_prefs/settings.xml\n// <string name="auth_token">eyJhbGciOiJIUzI1NiIs...</string>\n// <string name="user_password">password123</string>\n// Любой с root-доступом может прочитать!\n\n// === Android: БЕЗОПАСНОЕ хранение ===\n\n// 1. EncryptedSharedPreferences (AndroidX Security)\n// import androidx.security.crypto.EncryptedSharedPreferences\n// import androidx.security.crypto.MasterKey\n\n// Kotlin:\n// val masterKey = MasterKey.Builder(context)\n//     .setKeyScheme(MasterKey.KeyScheme.AES256_GCM)\n//     .build()\n//\n// val securePrefs = EncryptedSharedPreferences.create(\n//     context, "secure_prefs", masterKey,\n//     EncryptedSharedPreferences.PrefKeyEncryptionScheme.AES256_SIV,\n//     EncryptedSharedPreferences.PrefValueEncryptionScheme.AES256_GCM\n// )\n//\n// // Сохранение (шифруется автоматически)\n// securePrefs.edit().putString("auth_token", token).apply()\n//\n// // Чтение (расшифровывается автоматически)\n// val token = securePrefs.getString("auth_token", null)\n\n// 2. Android Keystore — аппаратное хранилище ключей\n// Ключи хранятся в TEE (Trusted Execution Environment)\n// или StrongBox (отдельный чип) — не извлекаемы даже с root\n\n// === React Native: безопасное хранение ===\n// react-native-keychain — обёртка над Keystore/Keychain\nimport * as Keychain from "react-native-keychain";\n\n// Сохранить токен\nawait Keychain.setGenericPassword("auth", accessToken, {\n  accessible: Keychain.ACCESSIBLE.WHEN_UNLOCKED_THIS_DEVICE_ONLY,\n  // Доступно только когда устройство разблокировано\n  // THIS_DEVICE_ONLY — не переносится в backup\n});\n\n// Прочитать\nconst credentials = await Keychain.getGenericPassword();\nif (credentials) {\n  const token = credentials.password;\n}\n\n// Удалить при logout\nawait Keychain.resetGenericPassword();' },
        { type: 'code', language: 'javascript', value: '// === iOS: Keychain Services ===\n// Keychain — защищённое хранилище Apple\n// Данные зашифрованы ключом устройства + пользовательским паролем\n\n// Swift пример:\n// import Security\n//\n// func saveToKeychain(key: String, data: Data) -> Bool {\n//     let query: [String: Any] = [\n//         kSecClass as String: kSecClassGenericPassword,\n//         kSecAttrAccount as String: key,\n//         kSecValueData as String: data,\n//         kSecAttrAccessible as String:\n//             kSecAttrAccessibleWhenUnlockedThisDeviceOnly\n//         // Доступно только на этом устройстве, когда разблокировано\n//     ]\n//     SecItemDelete(query as CFDictionary) // Удалить старое\n//     let status = SecItemAdd(query as CFDictionary, nil)\n//     return status == errSecSuccess\n// }\n\n// Уровни доступа iOS Keychain:\n// kSecAttrAccessibleWhenUnlocked — после разблокировки\n// kSecAttrAccessibleWhenUnlockedThisDeviceOnly — + не в backup\n// kSecAttrAccessibleAfterFirstUnlock — после первой разблокировки\n// kSecAttrAccessibleWhenPasscodeSetThisDeviceOnly — только с passcode\n\n// === Что проверять при аудите ===\n// Android:\n// adb shell run-as com.app ls /data/data/com.app/shared_prefs/\n// adb shell run-as com.app cat /data/data/com.app/shared_prefs/prefs.xml\n// adb shell run-as com.app ls /data/data/com.app/databases/\n\n// iOS:\n// Проверить plist файлы в sandbox\n// NSUserDefaults -> Library/Preferences/com.app.plist\n// SQLite databases -> Documents/' },
        { type: 'tip', value: 'Правило минимального хранения: не храните на устройстве то, что можно получить с сервера. Refresh token в Keychain/Keystore — достаточно. При logout: удалите все данные из Keychain, очистите кэш, удалите БД.' }
      ]
    },
    {
      id: 3,
      title: 'Certificate Pinning и защита коммуникаций',
      type: 'theory',
      content: [
        { type: 'text', value: 'Certificate Pinning — техника привязки приложения к конкретному серверному сертификату или публичному ключу. Без pinning атакующий может установить свой CA-сертификат на устройство и перехватить весь HTTPS-трафик (MITM).' },
        { type: 'heading', value: 'Как работает MITM без pinning' },
        { type: 'list', value: [
          '1. Атакующий контролирует Wi-Fi (Evil Twin) или DNS',
          '2. Устанавливает свой CA-сертификат на устройство жертвы',
          '3. Proxy (Burp Suite) перехватывает HTTPS, расшифровывает, пересылает',
          '4. Приложение без pinning доверяет любому CA из хранилища устройства',
          '5. Результат: атакующий видит весь трафик в открытом виде'
        ]},
        { type: 'code', language: 'javascript', value: '// === Certificate Pinning в React Native ===\n// react-native-ssl-pinning или TrustKit\n\n// Метод 1: Pinning публичного ключа (рекомендуется)\n// Преимущество: не нужно обновлять при ротации сертификата\n// (если ключ остаётся тем же)\n\n// Получить pin (SHA256 hash публичного ключа):\n// openssl s_client -connect api.example.com:443 | \\\n//   openssl x509 -pubkey -noout | \\\n//   openssl pkey -pubin -outform DER | \\\n//   openssl dgst -sha256 -binary | \\\n//   base64\n// Результат: "BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB="\n\n// Конфигурация для Android (network_security_config.xml):\n// <network-security-config>\n//   <domain-config>\n//     <domain includeSubdomains="true">api.example.com</domain>\n//     <pin-set expiration="2027-01-01">\n//       <pin digest="SHA-256">AAAA...primary...AAAA=</pin>\n//       <pin digest="SHA-256">BBBB...backup....BBBB=</pin>\n//     </pin-set>\n//   </domain-config>\n// </network-security-config>\n\n// Метод 2: OkHttp Certificate Pinner (Android/Kotlin)\n// val client = OkHttpClient.Builder()\n//     .certificatePinner(\n//         CertificatePinner.Builder()\n//             .add("api.example.com",\n//                  "sha256/AAAA...primary...AAAA=")\n//             .add("api.example.com",\n//                  "sha256/BBBB...backup....BBBB=")\n//             .build()\n//     )\n//     .build()\n\n// Метод 3: iOS TrustKit\n// let config: [String: Any] = [\n//     kTSKSwizzleNetworkDelegates: true,\n//     kTSKPinnedDomains: [\n//         "api.example.com": [\n//             kTSKPublicKeyHashes: [\n//                 "AAAA...primary...AAAA=",\n//                 "BBBB...backup....BBBB="\n//             ],\n//             kTSKEnforcePinning: true,\n//             kTSKIncludeSubdomains: true\n//         ]\n//     ]\n// ]\n// TrustKit.initSharedInstance(withConfiguration: config)' },
        { type: 'code', language: 'python', value: '# === Python: Certificate Pinning с requests ===\nimport hashlib\nimport ssl\nimport socket\nfrom base64 import b64encode\n\ndef get_server_pin(hostname: str, port: int = 443) -> str:\n    """Получить SHA256 pin публичного ключа сервера"""\n    ctx = ssl.create_default_context()\n    with ctx.wrap_socket(socket.socket(), server_hostname=hostname) as s:\n        s.connect((hostname, port))\n        cert_der = s.getpeercert(binary_form=True)\n    \n    # Хэш DER-encoded сертификата\n    pin = b64encode(hashlib.sha256(cert_der).digest()).decode()\n    return pin\n\ndef pinned_request(url: str, expected_pins: list[str]):\n    """HTTP запрос с проверкой certificate pin"""\n    import requests\n    from urllib.parse import urlparse\n    \n    hostname = urlparse(url).hostname\n    actual_pin = get_server_pin(hostname)\n    \n    if actual_pin not in expected_pins:\n        raise ssl.SSLError(\n            f"Certificate pin mismatch!\\n"\n            f"Expected: {expected_pins}\\n"\n            f"Got: {actual_pin}\\n"\n            f"Possible MITM attack!"\n        )\n    \n    return requests.get(url)\n\n# Использование\nPINS = [\n    "AAAA...primary...AAAA=",  # Текущий сертификат\n    "BBBB...backup....BBBB=",  # Backup (для ротации)\n]\nresponse = pinned_request("https://api.example.com/data", PINS)' },
        { type: 'warning', value: 'ВСЕГДА включайте backup pin! Если у вас только один pin и сертификат меняется — приложение перестанет работать. Решение: pin публичного ключа (не сертификата) + backup pin + expiration date + механизм обновления pins через OTA.' }
      ]
    },
    {
      id: 4,
      title: 'Обфускация и защита от реверс-инжиниринга',
      type: 'theory',
      content: [
        { type: 'text', value: 'Обфускация затрудняет анализ декомпилированного кода. Tamper detection обнаруживает модификацию приложения. Root/jailbreak detection определяет небезопасную среду. Это не абсолютная защита, но значительно повышает порог для атакующего.' },
        { type: 'heading', value: 'Уровни защиты' },
        { type: 'list', value: [
          'Обфускация кода: переименование классов/методов, удаление debug-информации',
          'String encryption: шифрование строковых констант (API endpoints, ключи)',
          'Control flow obfuscation: усложнение логики выполнения',
          'Root/Jailbreak detection: обнаружение скомпрометированного устройства',
          'Tamper detection: проверка целостности APK/IPA (подпись)',
          'Anti-debugging: обнаружение подключённого отладчика',
          'Anti-hooking: обнаружение Frida, Xposed framework'
        ]},
        { type: 'code', language: 'javascript', value: '// === Android ProGuard/R8: обфускация ===\n// build.gradle\n// android {\n//   buildTypes {\n//     release {\n//       minifyEnabled true\n//       shrinkResources true\n//       proguardFiles getDefaultProguardFile("proguard-android-optimize.txt"),\n//                     "proguard-rules.pro"\n//     }\n//   }\n// }\n\n// proguard-rules.pro:\n// # Обфускация всех классов\n// -obfuscationdictionary dictionary.txt\n// -classobfuscationdictionary dictionary.txt\n// -packageobfuscationdictionary dictionary.txt\n//\n// # Защита от отладки\n// -assumenosideeffects class android.util.Log {\n//     public static *** d(...);\n//     public static *** v(...);\n//     public static *** i(...);\n// }\n\n// === Root Detection (React Native) ===\nimport JailMonkey from "jail-monkey";\n\nconst checkDeviceSecurity = () => {\n  const issues = [];\n\n  // 1. Root/Jailbreak detection\n  if (JailMonkey.isJailBroken()) {\n    issues.push("device_rooted");\n  }\n\n  // 2. Debug mode detection\n  if (JailMonkey.isDebuggedMode()) {\n    issues.push("debug_mode");\n  }\n\n  // 3. Emulator detection (Frida часто работает на эмуляторах)\n  if (JailMonkey.isOnExternalStorage()) {\n    issues.push("external_storage");\n  }\n\n  // 4. Решение на основе рисков\n  if (issues.length > 0) {\n    // Вариант A: Блокировать приложение\n    // (банковские приложения)\n    // showBlockScreen("Небезопасное устройство");\n\n    // Вариант B: Ограничить функциональность\n    // (обычные приложения)\n    disableSensitiveFeatures();\n    reportToServer({ issues, deviceId: getDeviceId() });\n\n    // Вариант C: Просто логировать\n    // (low-risk приложения)\n    analytics.track("security_warning", { issues });\n  }\n\n  return issues;\n};' },
        { type: 'code', language: 'javascript', value: '// === Tamper Detection: проверка целостности приложения ===\n\n// Android: проверка подписи APK\n// Kotlin:\n// fun verifyAppSignature(context: Context): Boolean {\n//     val packageInfo = context.packageManager.getPackageInfo(\n//         context.packageName,\n//         PackageManager.GET_SIGNING_CERTIFICATES\n//     )\n//     val signatures = packageInfo.signingInfo.apkContentsSigners\n//     val currentSig = MessageDigest.getInstance("SHA-256")\n//         .digest(signatures[0].toByteArray())\n//     val expectedSig = "EXPECTED_SHA256_HASH"\n//     return Base64.encodeToString(currentSig, Base64.NO_WRAP) == expectedSig\n// }\n\n// === Anti-Frida Detection ===\n// Frida — мощный инструмент для runtime hooking\n// Обнаружение:\n\n// 1. Проверка портов Frida (по умолчанию 27042)\n// fun checkFridaPort(): Boolean {\n//     return try {\n//         val socket = Socket("127.0.0.1", 27042)\n//         socket.close()\n//         true // Frida обнаружена!\n//     } catch (e: Exception) {\n//         false\n//     }\n// }\n\n// 2. Проверка загруженных библиотек\n// fun checkFridaLibs(): Boolean {\n//     val maps = File("/proc/self/maps").readText()\n//     return maps.contains("frida") || maps.contains("gadget")\n// }\n\n// 3. Проверка named pipes\n// fun checkFridaPipes(): Boolean {\n//     val dir = File("/data/local/tmp")\n//     return dir.listFiles()?.any {\n//         it.name.contains("frida") || it.name.contains("linjector")\n//     } ?: false\n// }\n\n// ВАЖНО: опытный атакующий обойдёт все эти проверки.\n// Но каждый уровень защиты увеличивает стоимость атаки.' },
        { type: 'tip', value: 'Обфускация и tamper detection — это "замедлители", а не абсолютная защита. Для критичных приложений (банки, платежи) используйте коммерческие решения: DexGuard (Android), iXGuard (iOS). Главная защита — серверная валидация всех операций.' }
      ]
    },
    {
      id: 5,
      title: 'Безопасность API коммуникаций для мобильных',
      type: 'theory',
      content: [
        { type: 'text', value: 'Мобильное приложение — ненадёжный клиент. API должен быть спроектирован с предположением, что клиент скомпрометирован. Все проверки на сервере, rate limiting, аттестация устройства и защита от replay-атак.' },
        { type: 'code', language: 'javascript', value: '// === Безопасная архитектура API для мобильных ===\n\n// 1. Аттестация устройства (Device Attestation)\n// Проверяет что запрос идёт от настоящего приложения\n// Android: Play Integrity API\n// iOS: App Attest / DeviceCheck\n\n// Серверная проверка Play Integrity token:\n// POST /api/verify-integrity\n// {\n//   "integrityToken": "eyJhbGciOi...",\n//   "requestHash": "sha256-of-original-request"\n// }\n\n// 2. Request Signing — защита от подмены запросов\nconst crypto = require("crypto");\n\nfunction signRequest(method, path, body, timestamp, secret) {\n  // Подписываем: метод + путь + тело + timestamp\n  const payload = `${method}\\n${path}\\n${JSON.stringify(body)}\\n${timestamp}`;\n  const signature = crypto\n    .createHmac("sha256", secret)\n    .update(payload)\n    .digest("hex");\n  return signature;\n}\n\n// Клиент отправляет:\n// Headers:\n//   X-Timestamp: 1712300400\n//   X-Signature: abc123...\n//   X-Nonce: unique-random-string\n\n// 3. Серверная валидация\nfunction validateRequest(req) {\n  const timestamp = parseInt(req.headers["x-timestamp"]);\n  const signature = req.headers["x-signature"];\n  const nonce = req.headers["x-nonce"];\n  \n  // Проверка timestamp (защита от replay — окно 5 минут)\n  const now = Math.floor(Date.now() / 1000);\n  if (Math.abs(now - timestamp) > 300) {\n    throw new Error("Request expired (replay protection)");\n  }\n  \n  // Проверка nonce (каждый запрос уникален)\n  if (usedNonces.has(nonce)) {\n    throw new Error("Duplicate nonce (replay protection)");\n  }\n  usedNonces.add(nonce); // Сохранить в Redis с TTL\n  \n  // Проверка подписи\n  const expected = signRequest(\n    req.method, req.path, req.body, timestamp, API_SECRET\n  );\n  if (!crypto.timingSafeEqual(\n    Buffer.from(signature), Buffer.from(expected)\n  )) {\n    throw new Error("Invalid signature");\n  }\n  \n  return true;\n}' },
        { type: 'code', language: 'javascript', value: '// === Rate Limiting для мобильных API ===\nconst rateLimit = require("express-rate-limit");\nconst RedisStore = require("rate-limit-redis");\n\n// Общий лимит по IP\nconst generalLimiter = rateLimit({\n  store: new RedisStore({ client: redisClient }),\n  windowMs: 15 * 60 * 1000, // 15 минут\n  max: 100,                   // 100 запросов\n  message: { error: "Too many requests" },\n  standardHeaders: true,\n  legacyHeaders: false,\n});\n\n// Строгий лимит для аутентификации\nconst authLimiter = rateLimit({\n  store: new RedisStore({ client: redisClient }),\n  windowMs: 15 * 60 * 1000,\n  max: 5,                     // 5 попыток за 15 минут\n  message: { error: "Too many login attempts, try later" },\n  keyGenerator: (req) => {\n    // По IP + username (защита от distributed brute force)\n    return `${req.ip}:${req.body.username || "unknown"}`;\n  },\n  skipSuccessfulRequests: true, // Успешные не считаем\n});\n\n// Лимит для чувствительных операций\nconst sensitiveLimiter = rateLimit({\n  windowMs: 60 * 60 * 1000,  // 1 час\n  max: 3,                     // 3 раза в час\n  keyGenerator: (req) => `sensitive:${req.user.id}`,\n});\n\n// Применение\napp.use("/api/", generalLimiter);\napp.post("/api/auth/login", authLimiter);\napp.post("/api/auth/reset-password", sensitiveLimiter);\napp.post("/api/payments/transfer", sensitiveLimiter);' },
        { type: 'warning', value: 'API ключ в мобильном приложении — НЕ секрет. Он может быть извлечён декомпиляцией. Используйте server-side API ключи (backend-to-backend) и Device Attestation для проверки подлинности клиента. Клиентский API key — только для идентификации, не для аутентификации.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Аудит безопасности мобильного приложения',
      type: 'practice',
      difficulty: 'hard',
      description: 'Проведите аудит безопасности мобильного приложения по OWASP Mobile Top 10: проверьте хранение данных, защиту коммуникаций, обфускацию и API безопасность.',
      requirements: [
        'Создайте чеклист проверок по OWASP Mobile Top 10 с автоматизированными тестами',
        'Реализуйте безопасное хранение токенов с использованием Keychain/Keystore обёрток',
        'Настройте certificate pinning для API endpoint',
        'Реализуйте request signing с защитой от replay-атак (timestamp + nonce)',
        'Создайте систему device security checks (root, debug, tamper detection)'
      ],
      hint: 'Для request signing используйте HMAC-SHA256. Nonce сохраняйте в Redis/Set с TTL. Certificate pin — SHA256 хэш публичного ключа. Root detection — проверка su binary, Magisk, build tags.',
      expectedOutput: 'OWASP Mobile Checklist: 10 категорий, 35 проверок\nSecure Storage: токены в EncryptedSharedPreferences/Keychain\nCert Pinning: SHA256 pin для api.example.com (primary + backup)\nRequest Signing: HMAC-SHA256, timestamp window 5 min, nonce uniqueness\nDevice Security: root=false, debug=false, tamper=false, frida=false\nAll checks passed: приложение соответствует OWASP Mobile Top 10',
      solution: '#!/usr/bin/env python3\n"""Mobile App Security Audit Tool"""\nimport hashlib\nimport hmac\nimport time\nimport json\nimport secrets\n\n# === 1. OWASP Mobile Checklist ===\ndef run_mobile_audit():\n    checklist = {\n        "M1_Credentials": [\n            ("No hardcoded passwords in source", True),\n            ("No API keys in client code", True),\n            ("Tokens stored in Keychain/Keystore", True),\n        ],\n        "M5_Communication": [\n            ("TLS 1.2+ enforced", True),\n            ("Certificate pinning enabled", True),\n            ("No HTTP endpoints", True),\n        ],\n        "M7_Binary": [\n            ("ProGuard/R8 obfuscation enabled", True),\n            ("Debug mode disabled in release", True),\n            ("Root detection implemented", True),\n        ],\n        "M9_Storage": [\n            ("No PII in SharedPreferences", True),\n            ("SQLite databases encrypted", True),\n            ("Cache cleared on logout", True),\n        ],\n    }\n    \n    total = sum(len(v) for v in checklist.values())\n    passed = sum(1 for checks in checklist.values()\n                 for _, result in checks if result)\n    \n    for category, checks in checklist.items():\n        print(f"\\n{category}:")\n        for check, result in checks:\n            status = "PASS" if result else "FAIL"\n            print(f"  [{status}] {check}")\n    \n    print(f"\\nResult: {passed}/{total} checks passed")\n    return passed == total\n\n# === 2. Request Signing ===\nclass RequestSigner:\n    def __init__(self, secret: str):\n        self.secret = secret.encode()\n        self.used_nonces = set()\n    \n    def sign(self, method, path, body=""):\n        timestamp = str(int(time.time()))\n        nonce = secrets.token_hex(16)\n        payload = f"{method}\\n{path}\\n{body}\\n{timestamp}\\n{nonce}"\n        signature = hmac.new(\n            self.secret, payload.encode(), hashlib.sha256\n        ).hexdigest()\n        return {\n            "X-Timestamp": timestamp,\n            "X-Nonce": nonce,\n            "X-Signature": signature\n        }\n    \n    def verify(self, method, path, body, headers):\n        timestamp = int(headers["X-Timestamp"])\n        nonce = headers["X-Nonce"]\n        signature = headers["X-Signature"]\n        \n        # Replay protection: timestamp window\n        if abs(time.time() - timestamp) > 300:\n            return False, "Request expired"\n        \n        # Replay protection: unique nonce\n        if nonce in self.used_nonces:\n            return False, "Duplicate nonce"\n        self.used_nonces.add(nonce)\n        \n        # Verify signature\n        payload = f"{method}\\n{path}\\n{body}\\n{headers[\'X-Timestamp\']}\\n{nonce}"\n        expected = hmac.new(\n            self.secret, payload.encode(), hashlib.sha256\n        ).hexdigest()\n        \n        if not hmac.compare_digest(signature, expected):\n            return False, "Invalid signature"\n        \n        return True, "Valid"\n\n# === Test ===\nrun_mobile_audit()\n\nsigner = RequestSigner("mobile-api-secret-key")\nheaders = signer.sign("POST", "/api/transfer", json.dumps({"amount": 100}))\nprint(f"\\nSigned headers: {headers}")\n\nvalid, msg = signer.verify(\n    "POST", "/api/transfer",\n    json.dumps({"amount": 100}), headers\n)\nprint(f"Verification: {msg}")\n\n# Replay attack test\nvalid, msg = signer.verify(\n    "POST", "/api/transfer",\n    json.dumps({"amount": 100}), headers\n)\nprint(f"Replay attempt: {msg}")  # "Duplicate nonce"',
      explanation: 'Аудит мобильного приложения включает: проверку хранения данных (Keychain/Keystore, не SharedPrefs), защиту коммуникаций (TLS + cert pinning), защиту бинарного кода (обфускация, tamper detection), безопасность API (request signing, replay protection, rate limiting). Каждый уровень повышает стоимость атаки.'
    }
  ]
}
