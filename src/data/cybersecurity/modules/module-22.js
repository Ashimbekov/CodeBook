export default {
  id: 22,
  title: 'Логирование и мониторинг безопасности',
  description: 'SIEM системы, обнаружение угроз, аудит-логи, корреляция событий, настройка алертов и построение системы мониторинга безопасности.',
  lessons: [
    {
      id: 1,
      title: 'Основы логирования безопасности',
      type: 'theory',
      content: [
        { type: 'text', value: 'Логирование безопасности — фундамент для обнаружения, расследования и предотвращения атак. Без логов невозможно определить что произошло, когда и кем. OWASP включает "Insufficient Logging & Monitoring" в свой Top 10 — недостаточное логирование означает, что атаки остаются незамеченными месяцами.' },
        { type: 'heading', value: 'Что необходимо логировать' },
        { type: 'list', value: [
          'Аутентификация: успешные и неудачные попытки входа, logout, смена пароля',
          'Авторизация: отказы в доступе, изменения ролей, повышение привилегий',
          'Изменения данных: создание, обновление, удаление критичных записей',
          'Системные события: запуск/остановка сервисов, ошибки, изменения конфигурации',
          'Сетевые события: подключения, DNS запросы, firewall события',
          'Действия администраторов: все операции привилегированных пользователей'
        ]},
        { type: 'heading', value: 'Чего НЕ должно быть в логах' },
        { type: 'list', value: [
          'Пароли (даже хэшированные — в логах не место)',
          'Номера кредитных карт, SSN, персональные данные',
          'API ключи, токены, секреты',
          'Session ID в URL (используйте заголовки)',
          'Медицинские данные, биометрия без маскирования'
        ]},
        { type: 'code', language: 'python', value: '# Структурированное логирование безопасности\nimport logging\nimport json\nfrom datetime import datetime, timezone\n\nclass SecurityLogger:\n    def __init__(self, service_name: str):\n        self.logger = logging.getLogger(f"security.{service_name}")\n        self.logger.setLevel(logging.INFO)\n        handler = logging.StreamHandler()\n        handler.setFormatter(logging.Formatter(\'%(message)s\'))\n        self.logger.addHandler(handler)\n    \n    def _log(self, event_type: str, severity: str, details: dict):\n        """Структурированный лог в JSON формате"""\n        event = {\n            "timestamp": datetime.now(timezone.utc).isoformat(),\n            "event_type": event_type,\n            "severity": severity,\n            "service": "auth-service",\n            **details\n        }\n        self.logger.info(json.dumps(event))\n    \n    def auth_success(self, user_id: str, ip: str, method: str):\n        self._log("auth.success", "INFO", {\n            "user_id": user_id,\n            "source_ip": ip,\n            "auth_method": method\n        })\n    \n    def auth_failure(self, username: str, ip: str, reason: str):\n        self._log("auth.failure", "WARN", {\n            "username": username,  # НЕ user_id — пользователь не найден\n            "source_ip": ip,\n            "reason": reason  # "invalid_password", "user_not_found", "mfa_failed"\n        })\n    \n    def access_denied(self, user_id: str, resource: str, action: str):\n        self._log("authz.denied", "WARN", {\n            "user_id": user_id,\n            "resource": resource,\n            "action": action\n        })\n    \n    def suspicious_activity(self, user_id: str, detail: str, indicators: list):\n        self._log("threat.suspicious", "HIGH", {\n            "user_id": user_id,\n            "detail": detail,\n            "indicators": indicators\n        })\n\n# Использование\nsec_log = SecurityLogger("auth-service")\nsec_log.auth_failure("admin", "185.123.45.67", "invalid_password")\n# {"timestamp": "2026-04-05T10:30:00+00:00", "event_type": "auth.failure",\n#  "severity": "WARN", "username": "admin", "source_ip": "185.123.45.67",\n#  "reason": "invalid_password"}' },
        { type: 'warning', value: 'Маскируйте конфиденциальные данные в логах: email -> u***@example.com, IP -> 185.123.x.x (при необходимости), номер карты -> ****1234. GDPR и PCI DSS требуют защиты персональных данных даже в логах.' }
      ]
    },
    {
      id: 2,
      title: 'SIEM: Security Information and Event Management',
      type: 'theory',
      content: [
        { type: 'text', value: 'SIEM — централизованная платформа для сбора, агрегации, корреляции и анализа логов безопасности из множества источников. SIEM позволяет обнаруживать сложные атаки, которые невозможно увидеть по отдельным логам.' },
        { type: 'heading', value: 'Популярные SIEM решения' },
        { type: 'list', value: [
          'Splunk — лидер рынка, мощный поисковый движок (SPL), дорогой',
          'Elastic SIEM (ELK Stack) — open-source: Elasticsearch + Logstash + Kibana',
          'Wazuh — open-source SIEM + XDR, интеграция с ELK',
          'Microsoft Sentinel — облачный SIEM для Azure, KQL запросы',
          'Grafana Loki — легковесное решение для логов, интеграция с Grafana'
        ]},
        { type: 'code', language: 'yaml', value: '# === ELK Stack: docker-compose для SIEM ===\nversion: "3.8"\nservices:\n  elasticsearch:\n    image: docker.elastic.co/elasticsearch/elasticsearch:8.12.0\n    environment:\n      - discovery.type=single-node\n      - xpack.security.enabled=true\n      - ELASTIC_PASSWORD=changeme\n    ports:\n      - "9200:9200"\n    volumes:\n      - es-data:/usr/share/elasticsearch/data\n\n  kibana:\n    image: docker.elastic.co/kibana/kibana:8.12.0\n    environment:\n      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200\n      - ELASTICSEARCH_USERNAME=kibana_system\n      - ELASTICSEARCH_PASSWORD=changeme\n    ports:\n      - "5601:5601"\n    depends_on:\n      - elasticsearch\n\n  logstash:\n    image: docker.elastic.co/logstash/logstash:8.12.0\n    volumes:\n      - ./logstash/pipeline:/usr/share/logstash/pipeline\n    ports:\n      - "5044:5044"   # Beats input\n      - "5000:5000"   # TCP/UDP input\n    depends_on:\n      - elasticsearch\n\n  # Filebeat — агент для сбора логов с хостов\n  filebeat:\n    image: docker.elastic.co/beats/filebeat:8.12.0\n    volumes:\n      - ./filebeat.yml:/usr/share/filebeat/filebeat.yml:ro\n      - /var/log:/var/log:ro\n    depends_on:\n      - logstash\n\nvolumes:\n  es-data:' },
        { type: 'code', language: 'yaml', value: '# === filebeat.yml: сбор логов ===\nfilebeat.inputs:\n  - type: log\n    enabled: true\n    paths:\n      - /var/log/auth.log       # SSH, sudo логи\n      - /var/log/syslog          # Системные логи\n      - /var/log/nginx/access.log # Web-сервер\n      - /var/log/nginx/error.log\n    fields:\n      environment: production\n      service: web-server\n\n  - type: log\n    enabled: true\n    paths:\n      - /var/log/app/*.json     # Логи приложения\n    json.keys_under_root: true\n    json.add_error_key: true\n\noutput.logstash:\n  hosts: ["logstash:5044"]\n\n# Модули для автоматического парсинга\nfilebeat.modules:\n  - module: system\n    syslog:\n      enabled: true\n    auth:\n      enabled: true\n  - module: nginx\n    access:\n      enabled: true\n    error:\n      enabled: true' },
        { type: 'tip', value: 'Начните с ELK Stack — бесплатный и мощный. Filebeat собирает логи, Logstash парсит и обогащает, Elasticsearch хранит и индексирует, Kibana визуализирует. Для production добавьте Wazuh как SIEM надстройку над ELK.' }
      ]
    },
    {
      id: 3,
      title: 'Правила корреляции и обнаружение угроз',
      type: 'theory',
      content: [
        { type: 'text', value: 'Корреляция событий — объединение данных из разных источников для обнаружения сложных атак. Одно событие может быть безобидным, но паттерн из нескольких событий указывает на атаку. Sigma — универсальный формат правил обнаружения.' },
        { type: 'heading', value: 'Типичные корреляционные правила' },
        { type: 'list', value: [
          'Brute Force: >10 неудачных логинов за 5 минут с одного IP',
          'Account Takeover: успешный логин после серии неудачных + смена пароля',
          'Lateral Movement: логин с нового IP сразу после компрометации другого хоста',
          'Data Exfiltration: аномально большой объём исходящего трафика ночью',
          'Privilege Escalation: обычный пользователь выполняет admin-операции',
          'Impossible Travel: логин из двух стран с интервалом < 1 часа'
        ]},
        { type: 'code', language: 'yaml', value: '# === Sigma Rules: универсальный формат обнаружения ===\n# Sigma rules конвертируются в запросы для любого SIEM\n# (Splunk SPL, Elastic KQL, Microsoft KQL и др.)\n\n# Правило: Brute Force Detection\ntitle: Brute Force Login Attempt\nid: a1b2c3d4-5678-90ab-cdef-1234567890ab\nstatus: production\ndescription: Обнаружение попыток подбора пароля\nauthor: Security Team\ndate: 2026/04/05\ntags:\n  - attack.credential_access\n  - attack.t1110\nlogsource:\n  category: authentication\n  product: linux\ndetection:\n  selection:\n    EventType: authentication_failure\n  timeframe: 5m\n  condition: selection | count(source_ip) by username > 10\nfalsepositives:\n  - Забытый пароль пользователем\n  - Автоматизированные скрипты с устаревшим паролем\nlevel: high\n\n---\n# Правило: Impossible Travel\ntitle: Impossible Travel Detection\nstatus: production\ndescription: Логин из двух удалённых локаций за короткое время\ndetection:\n  selection:\n    EventType: authentication_success\n  condition: |\n    selection\n    | where geo_distance(prev_login_country, current_country) > 5000\n    | where time_diff(prev_login_time, current_time) < 3600\nlevel: critical' },
        { type: 'code', language: 'python', value: '# === Движок корреляции: обнаружение Brute Force ===\nfrom collections import defaultdict\nfrom datetime import datetime, timedelta, timezone\n\nclass CorrelationEngine:\n    def __init__(self):\n        self.failed_logins = defaultdict(list)  # {ip: [timestamps]}\n        self.alerts = []\n    \n    def process_event(self, event: dict):\n        """Обработка каждого события безопасности"""\n        if event["event_type"] == "auth.failure":\n            self._check_brute_force(event)\n        elif event["event_type"] == "auth.success":\n            self._check_impossible_travel(event)\n            self._check_post_brute_success(event)\n    \n    def _check_brute_force(self, event: dict):\n        ip = event["source_ip"]\n        now = datetime.now(timezone.utc)\n        \n        # Добавить timestamp неудачного логина\n        self.failed_logins[ip].append(now)\n        \n        # Оставить только события за последние 5 минут\n        cutoff = now - timedelta(minutes=5)\n        self.failed_logins[ip] = [\n            t for t in self.failed_logins[ip] if t > cutoff\n        ]\n        \n        # Порог: >10 неудачных попыток за 5 минут\n        if len(self.failed_logins[ip]) > 10:\n            self._create_alert(\n                severity="HIGH",\n                rule="brute_force",\n                description=f"Brute force с IP {ip}: "\n                           f"{len(self.failed_logins[ip])} попыток за 5 мин",\n                source_ip=ip,\n                action="block_ip"  # Автоматическое действие\n            )\n    \n    def _check_post_brute_success(self, event: dict):\n        """Успешный логин после brute force — возможная компрометация"""\n        ip = event["source_ip"]\n        if len(self.failed_logins.get(ip, [])) > 5:\n            self._create_alert(\n                severity="CRITICAL",\n                rule="brute_force_success",\n                description=f"Успешный логин с IP {ip} ПОСЛЕ brute force!",\n                source_ip=ip,\n                user_id=event.get("user_id"),\n                action="lock_account"\n            )\n    \n    def _create_alert(self, **kwargs):\n        alert = {"timestamp": datetime.now(timezone.utc).isoformat(), **kwargs}\n        self.alerts.append(alert)\n        print(f"ALERT [{kwargs[\'severity\']}]: {kwargs[\'description\']}")' },
        { type: 'tip', value: 'Sigma — универсальный язык правил обнаружения. Пишете правило один раз, конвертируете в формат вашего SIEM (sigmac для Splunk SPL, Elastic Query, Microsoft KQL). Репозиторий SigmaHQ содержит 3000+ готовых правил обнаружения.' }
      ]
    },
    {
      id: 4,
      title: 'Аудит-логи и compliance',
      type: 'theory',
      content: [
        { type: 'text', value: 'Аудит-логи — неизменяемые записи всех действий в системе, необходимые для compliance (GDPR, PCI DSS, SOC 2) и расследования инцидентов. Ключевое отличие от обычных логов: аудит-логи должны быть tamper-proof (защищены от подмены).' },
        { type: 'heading', value: 'Требования к аудит-логам' },
        { type: 'list', value: [
          'Неизменяемость: логи нельзя удалить или модифицировать (append-only)',
          'Полнота: все действия пользователей и администраторов зафиксированы',
          'Хранение: минимум 1 год (PCI DSS), 3 года (SOC 2), зависит от стандарта',
          'Доступ: только авторизованные аудиторы могут просматривать',
          'Целостность: хэши или цифровые подписи для проверки подлинности',
          'Время: синхронизация NTP, UTC timestamps'
        ]},
        { type: 'code', language: 'python', value: '# === Audit Log System с гарантией целостности ===\nimport hashlib\nimport json\nfrom datetime import datetime, timezone\n\nclass AuditLogger:\n    def __init__(self):\n        self.chain = []  # Цепочка аудит-записей (blockchain-подобная)\n        self.previous_hash = "0" * 64  # Генезис\n    \n    def log(self, actor: str, action: str, resource: str,\n            details: dict = None, result: str = "success"):\n        """Создать неизменяемую аудит-запись"""\n        entry = {\n            "sequence": len(self.chain) + 1,\n            "timestamp": datetime.now(timezone.utc).isoformat(),\n            "actor": actor,           # Кто выполнил действие\n            "action": action,         # Что было сделано\n            "resource": resource,     # Над чем\n            "details": details or {},\n            "result": result,         # success/failure/denied\n            "previous_hash": self.previous_hash\n        }\n        \n        # Хэш записи включает хэш предыдущей (цепочка)\n        entry_str = json.dumps(entry, sort_keys=True)\n        entry["hash"] = hashlib.sha256(entry_str.encode()).hexdigest()\n        \n        self.chain.append(entry)\n        self.previous_hash = entry["hash"]\n        \n        return entry\n    \n    def verify_integrity(self) -> bool:\n        """Проверить что цепочка не была изменена"""\n        for i, entry in enumerate(self.chain):\n            stored_hash = entry.pop("hash")\n            entry_str = json.dumps(entry, sort_keys=True)\n            computed_hash = hashlib.sha256(entry_str.encode()).hexdigest()\n            entry["hash"] = stored_hash\n            \n            if computed_hash != stored_hash:\n                print(f"INTEGRITY VIOLATION at entry #{entry[\'sequence\']}!")\n                return False\n            \n            if i > 0 and entry["previous_hash"] != self.chain[i-1]["hash"]:\n                print(f"CHAIN BROKEN at entry #{entry[\'sequence\']}!")\n                return False\n        \n        print(f"Audit chain verified: {len(self.chain)} entries, integrity OK")\n        return True\n\n# Использование\naudit = AuditLogger()\naudit.log("admin@company.com", "user.create", "/api/users",\n          details={"new_user": "john@company.com", "role": "developer"})\naudit.log("admin@company.com", "role.change", "/api/users/john",\n          details={"old_role": "developer", "new_role": "admin"})\naudit.log("john@company.com", "data.export", "/api/reports/users",\n          details={"records": 5000, "format": "csv"})\n\naudit.verify_integrity()  # Audit chain verified: 3 entries, integrity OK' },
        { type: 'code', language: 'bash', value: '# === Linux Audit Framework (auditd) ===\n# Встроенная система аудита Linux\n\n# Установка\nsudo apt install auditd audispd-plugins\n\n# Правила аудита: /etc/audit/rules.d/security.rules\n\n# Мониторинг изменений в /etc/passwd\n-w /etc/passwd -p wa -k identity_changes\n\n# Мониторинг изменений в /etc/shadow\n-w /etc/shadow -p wa -k identity_changes\n\n# Мониторинг SSH конфигурации\n-w /etc/ssh/sshd_config -p wa -k sshd_config\n\n# Мониторинг sudo использования\n-w /var/log/sudo.log -p wa -k sudo_log\n-w /etc/sudoers -p wa -k sudoers_changes\n\n# Мониторинг выполнения привилегированных команд\n-a always,exit -F arch=b64 -S execve -F euid=0 -k root_commands\n\n# Мониторинг подключений к сети\n-a always,exit -F arch=b64 -S connect -k network_connections\n\n# Применить правила\nsudo augenrules --load\n\n# Просмотр логов аудита\nsudo ausearch -k identity_changes --start today\nsudo aureport --auth --start today  # Отчёт по аутентификации\nsudo aureport --summary             # Общая сводка' },
        { type: 'warning', value: 'Отправляйте аудит-логи на удалённый сервер немедленно (real-time forwarding). Если атакующий получит root на сервере, он может удалить локальные логи. Удалённые логи на отдельном сервере с ограниченным доступом — единственная надёжная защита.' }
      ]
    },
    {
      id: 5,
      title: 'Мониторинг и дашборды безопасности',
      type: 'theory',
      content: [
        { type: 'text', value: 'Мониторинг безопасности — непрерывное наблюдение за системами для раннего обнаружения угроз. Дашборды визуализируют ключевые метрики: количество алертов, географию атак, подозрительные паттерны. SOC (Security Operations Center) использует дашборды 24/7.' },
        { type: 'heading', value: 'Ключевые метрики безопасности (KPI)' },
        { type: 'list', value: [
          'MTTD (Mean Time to Detect) — среднее время обнаружения инцидента',
          'MTTR (Mean Time to Respond) — среднее время реагирования',
          'Alert Volume — количество алертов за период (тренд)',
          'False Positive Rate — процент ложных срабатываний (цель: <5%)',
          'Coverage — процент систем покрытых мониторингом',
          'Failed Login Rate — динамика неудачных аутентификаций'
        ]},
        { type: 'code', language: 'python', value: '# === Prometheus метрики безопасности ===\nfrom prometheus_client import Counter, Histogram, Gauge, start_http_server\n\n# Счётчики событий безопасности\nauth_attempts = Counter(\n    "auth_attempts_total",\n    "Общее количество попыток аутентификации",\n    ["method", "result", "service"]\n)\n\nauth_failures_by_ip = Counter(\n    "auth_failures_by_ip_total",\n    "Неудачные попытки входа по IP",\n    ["source_ip"]\n)\n\nsecurity_alerts = Counter(\n    "security_alerts_total",\n    "Количество алертов безопасности",\n    ["severity", "rule_name"]\n)\n\n# Gauges для текущего состояния\nactive_sessions = Gauge(\n    "active_sessions_current",\n    "Текущее количество активных сессий"\n)\n\nblocked_ips = Gauge(\n    "blocked_ips_current",\n    "Количество заблокированных IP адресов"\n)\n\n# Гистограмма для времени реагирования\nincident_response_time = Histogram(\n    "incident_response_seconds",\n    "Время реагирования на инцидент",\n    buckets=[60, 300, 900, 1800, 3600, 7200, 14400, 28800, 86400]\n)\n\n# Использование в приложении\ndef login(username, password, ip):\n    result = authenticate(username, password)\n    if result.success:\n        auth_attempts.labels(\n            method="password", result="success", service="web"\n        ).inc()\n        active_sessions.inc()\n    else:\n        auth_attempts.labels(\n            method="password", result="failure", service="web"\n        ).inc()\n        auth_failures_by_ip.labels(source_ip=ip).inc()\n\n# Запуск Prometheus endpoint\nstart_http_server(8000)  # /metrics на порту 8000' },
        { type: 'code', language: 'yaml', value: '# === Grafana Dashboard: Security Overview ===\n# Alertmanager правила для Prometheus\n\n# prometheus-alerts.yml\ngroups:\n  - name: security_alerts\n    rules:\n      # Алерт: Brute Force\n      - alert: BruteForceDetected\n        expr: |\n          rate(auth_failures_by_ip_total[5m]) > 0.5\n        for: 2m\n        labels:\n          severity: high\n        annotations:\n          summary: "Brute force attack detected"\n          description: |\n            IP {{ $labels.source_ip }} has\n            {{ $value }} failed logins/sec in last 5 min\n\n      # Алерт: Аномальное количество алертов\n      - alert: SecurityAlertSpike\n        expr: |\n          rate(security_alerts_total[1h]) > 10\n        for: 5m\n        labels:\n          severity: critical\n        annotations:\n          summary: "Spike in security alerts"\n\n      # Алерт: Высокий false positive rate\n      - alert: HighFalsePositiveRate\n        expr: |\n          rate(security_alerts_total{result=\"false_positive\"}[24h])\n          /\n          rate(security_alerts_total[24h]) > 0.1\n        for: 1h\n        labels:\n          severity: warning\n        annotations:\n          summary: "False positive rate > 10%, tune detection rules"' },
        { type: 'tip', value: 'Настройте три уровня дашбордов: 1) Executive — высокоуровневые KPI для руководства, 2) SOC Operational — для аналитиков (live alerts, investigations), 3) Technical — для инженеров (детальные метрики систем). Используйте Grafana + Prometheus + Loki для полного стека мониторинга.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Настройка системы мониторинга',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создайте систему логирования безопасности с корреляционным движком, Sigma-совместимыми правилами обнаружения и аудит-логами с проверкой целостности.',
      requirements: [
        'Реализуйте структурированный security logger с JSON форматом и маскированием данных',
        'Создайте корреляционный движок для обнаружения brute force и impossible travel',
        'Напишите Sigma-совместимые правила для 3 типов угроз',
        'Реализуйте аудит-лог с цепочкой хэшей для проверки целостности',
        'Создайте docker-compose для ELK Stack или настройте Prometheus метрики безопасности'
      ],
      hint: 'Используйте defaultdict для агрегации событий по IP/user. Sigma правила — YAML файлы с полями detection, logsource, level. Для цепочки хэшей: каждая запись включает хэш предыдущей.',
      expectedOutput: 'SecurityLogger: JSON логи с маскированием PII данных\nCorrelation Engine: обнаружен brute force (15 попыток за 3 мин)\nCorrelation Engine: обнаружен impossible travel (Москва -> Нью-Йорк за 30 мин)\n3 Sigma правила: brute_force, privilege_escalation, data_exfiltration\nAudit chain: 10 записей, integrity verified\nDocker-compose: ELK Stack запущен, Kibana доступна на :5601',
      solution: '#!/usr/bin/env python3\n"""Security Monitoring System — полная реализация"""\nimport hashlib\nimport json\nfrom datetime import datetime, timedelta, timezone\nfrom collections import defaultdict\n\n# === 1. Security Logger с маскированием ===\nclass SecurityLogger:\n    MASKED_FIELDS = {"email", "phone", "credit_card", "ssn"}\n    \n    @staticmethod\n    def mask(value: str, field: str) -> str:\n        if field == "email" and "@" in value:\n            user, domain = value.split("@")\n            return f"{user[0]}***@{domain}"\n        if field == "credit_card":\n            return f"****{value[-4:]}"\n        return f"{value[:2]}***"\n    \n    def log(self, event_type, severity, **details):\n        # Маскирование PII\n        masked = {}\n        for k, v in details.items():\n            if k in self.MASKED_FIELDS and isinstance(v, str):\n                masked[k] = self.mask(v, k)\n            else:\n                masked[k] = v\n        \n        entry = {\n            "timestamp": datetime.now(timezone.utc).isoformat(),\n            "event_type": event_type,\n            "severity": severity,\n            **masked\n        }\n        print(json.dumps(entry, ensure_ascii=False))\n        return entry\n\n# === 2. Correlation Engine ===\nclass CorrelationEngine:\n    def __init__(self):\n        self.failed_logins = defaultdict(list)\n        self.user_locations = defaultdict(list)\n    \n    def check_brute_force(self, ip, timestamp):\n        self.failed_logins[ip].append(timestamp)\n        cutoff = timestamp - timedelta(minutes=5)\n        self.failed_logins[ip] = [\n            t for t in self.failed_logins[ip] if t > cutoff\n        ]\n        if len(self.failed_logins[ip]) > 10:\n            return {"alert": "brute_force", "ip": ip,\n                    "attempts": len(self.failed_logins[ip])}\n        return None\n    \n    def check_impossible_travel(self, user_id, country, timestamp):\n        self.user_locations[user_id].append((country, timestamp))\n        history = self.user_locations[user_id]\n        if len(history) >= 2:\n            prev_country, prev_time = history[-2]\n            if prev_country != country:\n                time_diff = (timestamp - prev_time).total_seconds()\n                if time_diff < 3600:  # Менее 1 часа\n                    return {"alert": "impossible_travel",\n                            "user": user_id,\n                            "from": prev_country, "to": country,\n                            "minutes": int(time_diff / 60)}\n        return None\n\n# === 3. Audit Logger с цепочкой хэшей ===\nclass AuditLogger:\n    def __init__(self):\n        self.chain = []\n        self.prev_hash = "0" * 64\n    \n    def log(self, actor, action, resource):\n        entry = {\n            "seq": len(self.chain) + 1,\n            "ts": datetime.now(timezone.utc).isoformat(),\n            "actor": actor, "action": action,\n            "resource": resource,\n            "prev_hash": self.prev_hash\n        }\n        raw = json.dumps(entry, sort_keys=True)\n        entry["hash"] = hashlib.sha256(raw.encode()).hexdigest()\n        self.chain.append(entry)\n        self.prev_hash = entry["hash"]\n    \n    def verify(self):\n        for i, entry in enumerate(self.chain):\n            h = entry.pop("hash")\n            raw = json.dumps(entry, sort_keys=True)\n            entry["hash"] = h\n            if hashlib.sha256(raw.encode()).hexdigest() != h:\n                return False\n        return True\n\n# === Тестирование ===\nlogger = SecurityLogger()\nlogger.log("auth.failure", "WARN", email="admin@company.com", source_ip="1.2.3.4")\n\nengine = CorrelationEngine()\nnow = datetime.now(timezone.utc)\nfor i in range(12):\n    result = engine.check_brute_force("1.2.3.4", now + timedelta(seconds=i*20))\n    if result:\n        print(f"ALERT: {result}")\n\nresult = engine.check_impossible_travel("user1", "RU", now)\nresult = engine.check_impossible_travel("user1", "US", now + timedelta(minutes=30))\nif result:\n    print(f"ALERT: {result}")\n\naudit = AuditLogger()\nfor i in range(10):\n    audit.log("admin", f"action_{i}", f"/resource/{i}")\nprint(f"Audit integrity: {audit.verify()}")',
      explanation: 'Система мониторинга безопасности состоит из: 1) структурированного логирования с маскированием PII, 2) корреляционного движка для обнаружения паттернов атак (brute force, impossible travel), 3) аудит-логов с цепочкой хэшей для гарантии целостности. В production добавляется SIEM (ELK/Splunk) для агрегации и визуализации.'
    }
  ]
}
