export default {
  id: 8,
  title: 'OWASP Top 10: CSRF, SSRF, XXE',
  description: 'Cross-Site Request Forgery, Server-Side Request Forgery, XML External Entities — векторы атак и методы защиты.',
  lessons: [
    {
      id: 1,
      title: 'CSRF: Cross-Site Request Forgery',
      type: 'theory',
      content: [
        { type: 'text', value: 'CSRF — атака, при которой злоумышленник заставляет браузер авторизованного пользователя выполнить нежелательное действие на доверенном сайте. Браузер автоматически отправляет cookies, поэтому запрос выглядит легитимным.' },
        { type: 'code', language: 'javascript', value: '// === Как работает CSRF ===\n\n// 1. Пользователь авторизован на bank.com (cookie сессии)\n// 2. Пользователь открывает вредоносную страницу evil.com\n// 3. evil.com содержит:\n\n// <form action="https://bank.com/transfer" method="POST" id="csrfForm">\n//   <input type="hidden" name="to" value="attacker_account">\n//   <input type="hidden" name="amount" value="10000">\n// </form>\n// <script>document.getElementById("csrfForm").submit();</script>\n\n// 4. Браузер автоматически отправит cookie bank.com!\n// 5. Bank.com видит валидную сессию и выполняет перевод\n\n// Ещё проще — через img тег (GET запрос):\n// <img src="https://bank.com/transfer?to=attacker&amount=10000">\n// (если банк использует GET для операций — грубая ошибка)' },
        { type: 'heading', value: 'Защита от CSRF' },
        { type: 'code', language: 'javascript', value: '// === Защита №1: CSRF Token ===\nconst crypto = require("crypto");\n\n// Генерация токена (сохраняем в сессии)\napp.use((req, res, next) => {\n  if (!req.session.csrfToken) {\n    req.session.csrfToken = crypto.randomBytes(32).toString("hex");\n  }\n  res.locals.csrfToken = req.session.csrfToken;\n  next();\n});\n\n// В форме: <input type="hidden" name="_csrf" value="{{csrfToken}}">\n\n// Проверка токена\napp.post("/transfer", (req, res) => {\n  if (req.body._csrf !== req.session.csrfToken) {\n    return res.status(403).json({ error: "Invalid CSRF token" });\n  }\n  // Выполнить действие\n});\n\n// === Защита №2: SameSite Cookie ===\n// Set-Cookie: session=abc123; SameSite=Lax; Secure; HttpOnly\n// Lax: cookie не отправляется при POST с другого сайта\n// Strict: cookie не отправляется при ЛЮБОМ переходе с другого сайта\n\n// === Защита №3: Custom Header ===\n// Для API: требовать кастомный заголовок\n// X-Requested-With: XMLHttpRequest\n// Браузер не позволяет отправить кастомный заголовок через form/img' },
        { type: 'tip', value: 'SameSite=Lax — минимальная защита (установлена по умолчанию в современных браузерах). Для критичных действий используйте CSRF токен + SameSite=Strict + проверку Origin/Referer заголовков.' }
      ]
    },
    {
      id: 2,
      title: 'SSRF: Server-Side Request Forgery',
      type: 'theory',
      content: [
        { type: 'text', value: 'SSRF — атака, при которой сервер выполняет HTTP запрос по URL, указанному атакующим. Позволяет: доступ к внутренним сервисам, чтение метаданных облака, сканирование внутренней сети.' },
        { type: 'heading', value: 'Примеры SSRF атак' },
        { type: 'code', language: 'python', value: '# === Уязвимый код ===\nfrom flask import Flask, request\nimport requests\n\napp = Flask(__name__)\n\n# УЯЗВИМО: сервер загружает URL от пользователя\n@app.route("/fetch")\ndef fetch_url():\n    url = request.args.get("url")\n    response = requests.get(url)  # ОПАСНО!\n    return response.text\n\n# Атака 1: Чтение внутренних сервисов\n# GET /fetch?url=http://localhost:6379/  (Redis)\n# GET /fetch?url=http://internal-admin:8080/  (внутренний сервис)\n\n# Атака 2: Чтение метаданных AWS (критично!)\n# GET /fetch?url=http://169.254.169.254/latest/meta-data/iam/security-credentials/\n# Возвращает AWS ключи доступа!\n\n# Атака 3: Чтение локальных файлов\n# GET /fetch?url=file:///etc/passwd\n\n# Атака 4: Сканирование внутренней сети\n# GET /fetch?url=http://10.0.0.1:22  (SSH?)\n# GET /fetch?url=http://10.0.0.2:3306  (MySQL?)' },
        { type: 'heading', value: 'Защита от SSRF' },
        { type: 'code', language: 'python', value: 'import ipaddress\nimport urllib.parse\nimport requests\n\n# Whitelist допустимых доменов\nALLOWED_DOMAINS = {"api.example.com", "cdn.example.com"}\n\n# Blacklist внутренних IP\nPRIVATE_NETWORKS = [\n    ipaddress.ip_network("10.0.0.0/8"),\n    ipaddress.ip_network("172.16.0.0/12"),\n    ipaddress.ip_network("192.168.0.0/16"),\n    ipaddress.ip_network("127.0.0.0/8"),\n    ipaddress.ip_network("169.254.0.0/16"),  # AWS metadata!\n    ipaddress.ip_network("::1/128"),\n]\n\ndef is_safe_url(url: str) -> bool:\n    """Проверка URL перед запросом"""\n    parsed = urllib.parse.urlparse(url)\n    \n    # Только HTTP/HTTPS\n    if parsed.scheme not in ("http", "https"):\n        return False\n    \n    # Whitelist доменов (если применимо)\n    if parsed.hostname not in ALLOWED_DOMAINS:\n        return False\n    \n    # Проверка IP (резолвим DNS)\n    import socket\n    try:\n        ip = socket.getaddrinfo(parsed.hostname, None)[0][4][0]\n        ip_obj = ipaddress.ip_address(ip)\n        for network in PRIVATE_NETWORKS:\n            if ip_obj in network:\n                return False  # Внутренний IP!\n    except socket.gaierror:\n        return False\n    \n    return True\n\n@app.route("/fetch")\ndef fetch_url_safe():\n    url = request.args.get("url")\n    if not is_safe_url(url):\n        return {"error": "URL не разрешён"}, 403\n    response = requests.get(url, timeout=5, allow_redirects=False)\n    return response.text' },
        { type: 'warning', value: 'Атака SSRF на AWS метаданные (169.254.169.254) — одна из самых критичных. Один запрос может раскрыть IAM credentials с полным доступом к облачной инфраструктуре. Capital One в 2019 потеряла 100 млн записей через SSRF.' }
      ]
    },
    {
      id: 3,
      title: 'XXE: XML External Entities',
      type: 'theory',
      content: [
        { type: 'text', value: 'XXE — атака через обработку XML с внешними сущностями (entities). Позволяет: чтение файлов сервера, SSRF через XML, отказ в обслуживании (Billion Laughs). Актуально для SOAP API, SVG, SAML, DocX/XLSX парсинга.' },
        { type: 'code', language: 'python', value: '# === XXE атака: чтение файлов ===\n\n# Вредоносный XML:\nmalicious_xml = """\n<?xml version="1.0" encoding="UTF-8"?>\n<!DOCTYPE foo [\n  <!ENTITY xxe SYSTEM "file:///etc/passwd">\n]>\n<user>\n  <name>&xxe;</name>\n</user>\n"""\n# &xxe; заменяется содержимым /etc/passwd!\n\n# === XXE SSRF ===\nmalicious_xml_ssrf = """\n<!DOCTYPE foo [\n  <!ENTITY xxe SYSTEM "http://169.254.169.254/latest/meta-data/">\n]>\n<data>&xxe;</data>\n"""\n\n# === Billion Laughs (DoS через XML) ===\nbillion_laughs = """\n<?xml version="1.0"?>\n<!DOCTYPE lolz [\n  <!ENTITY lol "lol">\n  <!ENTITY lol2 "&lol;&lol;&lol;&lol;&lol;&lol;&lol;&lol;&lol;&lol;">\n  <!ENTITY lol3 "&lol2;&lol2;&lol2;&lol2;&lol2;&lol2;&lol2;&lol2;&lol2;&lol2;">\n  <!ENTITY lol4 "&lol3;&lol3;&lol3;&lol3;&lol3;&lol3;&lol3;&lol3;&lol3;&lol3;">\n  <!-- ... экспоненциальный рост: 10^9 символов из 10 строк! -->\n]>\n<data>&lol4;</data>\n"""\n# Парсер попытается развернуть миллиарды символов -> out of memory!' },
        { type: 'heading', value: 'Защита от XXE' },
        { type: 'code', language: 'python', value: 'import xml.etree.ElementTree as ET\nfrom lxml import etree\nfrom defusedxml import ElementTree as defusedET\n\n# === УЯЗВИМО: стандартный парсер с DTD ===\n# tree = ET.parse("user_input.xml")  # Может обработать XXE!\n\n# === БЕЗОПАСНО: defusedxml (рекомендуется) ===\n# pip install defusedxml\ntree = defusedET.parse("user_input.xml")\n# Автоматически блокирует: external entities, DTD, entity expansion\n\n# === БЕЗОПАСНО: lxml с отключённым DTD ===\nparser = etree.XMLParser(\n    resolve_entities=False,  # Не разрешать entities\n    no_network=True,         # Запретить сетевые запросы\n    dtd_validation=False,    # Не валидировать DTD\n    load_dtd=False,          # Не загружать DTD\n)\ntree = etree.parse("user_input.xml", parser)\n\n# === Для Java (частая проблема) ===\n# DocumentBuilderFactory factory = DocumentBuilderFactory.newInstance();\n# factory.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true);\n# factory.setFeature("http://xml.org/sax/features/external-general-entities", false);\n# factory.setFeature("http://xml.org/sax/features/external-parameter-entities", false);\n\n# === Лучшее решение: используйте JSON вместо XML ===\n# JSON не поддерживает entities -> нет XXE по определению' },
        { type: 'tip', value: 'Лучшая защита от XXE — использовать JSON вместо XML. Если XML обязателен — используйте defusedxml (Python), отключите DTD processing (Java/.NET). Не забывайте про SVG, DOCX, XLSX — они тоже XML!' }
      ]
    },
    {
      id: 4,
      title: 'Clickjacking и защита',
      type: 'theory',
      content: [
        { type: 'text', value: 'Clickjacking — атака, при которой вредоносный сайт встраивает целевой сайт в невидимый iframe и обманом заставляет пользователя кликать на элементы целевого сайта.' },
        { type: 'code', language: 'javascript', value: '// === Clickjacking атака ===\n// Вредоносная страница evil.com:\n// <style>\n//   iframe {\n//     position: absolute;\n//     top: 0; left: 0;\n//     width: 100%; height: 100%;\n//     opacity: 0;          /* Полностью невидим! */\n//     z-index: 999;        /* Поверх всего */\n//   }\n//   .bait {\n//     position: absolute;\n//     top: 200px; left: 300px;\n//   }\n// </style>\n// <div class="bait">\n//   <h1>Нажмите чтобы выиграть iPhone!</h1>\n// </div>\n// <iframe src="https://bank.com/settings?action=delete-account"></iframe>\n// Пользователь думает что кликает на кнопку, а на самом деле\n// кликает на "Удалить аккаунт" в невидимом iframe!\n\n// === Защита №1: X-Frame-Options ===\n// Запретить встраивание в iframe\napp.use((req, res, next) => {\n  res.setHeader("X-Frame-Options", "DENY");  // Нигде нельзя\n  // или "SAMEORIGIN" — только с того же домена\n  next();\n});\n\n// === Защита №2: CSP frame-ancestors (современный способ) ===\n// Content-Security-Policy: frame-ancestors \'none\';\n// или: frame-ancestors \'self\' trusted-site.com;\n\n// === Защита №3: JavaScript frame-busting ===\n// (как дополнение, не основная защита)\nif (window.top !== window.self) {\n  window.top.location = window.self.location;\n}\n// Но атакующий может блокировать JS в iframe через sandbox!' },
        { type: 'tip', value: 'Используйте CSP frame-ancestors вместо X-Frame-Options — он более гибкий и поддерживает wildcards. Для максимальной совместимости устанавливайте оба заголовка.' }
      ]
    },
    {
      id: 5,
      title: 'HTTP Security Headers: полный набор',
      type: 'theory',
      content: [
        { type: 'text', value: 'HTTP Security Headers — набор HTTP заголовков, повышающих безопасность веб-приложения. Каждый заголовок защищает от определённого класса атак. Рекомендуется устанавливать все перечисленные заголовки.' },
        { type: 'code', language: 'javascript', value: '// === Полный набор security headers (Express.js) ===\n// Используйте helmet middleware: npm install helmet\nconst helmet = require("helmet");\napp.use(helmet());\n\n// Или вручную:\napp.use((req, res, next) => {\n  // 1. CSP — защита от XSS\n  // (настройте под ваше приложение)\n  \n  // 2. Strict-Transport-Security — принудительный HTTPS\n  res.setHeader("Strict-Transport-Security",\n    "max-age=31536000; includeSubDomains; preload");\n  \n  // 3. X-Content-Type-Options — запрет MIME sniffing\n  res.setHeader("X-Content-Type-Options", "nosniff");\n  \n  // 4. X-Frame-Options — защита от clickjacking\n  res.setHeader("X-Frame-Options", "DENY");\n  \n  // 5. Referrer-Policy — контроль Referer заголовка\n  res.setHeader("Referrer-Policy", "strict-origin-when-cross-origin");\n  \n  // 6. Permissions-Policy — ограничение API браузера\n  res.setHeader("Permissions-Policy",\n    "camera=(), microphone=(), geolocation=(), payment=()");\n  \n  // 7. X-XSS-Protection — устаревший, но для совместимости\n  res.setHeader("X-XSS-Protection", "0");\n  // Рекомендуется 0 (отключить) — встроенные фильтры браузера\n  // могут создать XSS уязвимости\n  \n  // 8. Cross-Origin-Opener-Policy\n  res.setHeader("Cross-Origin-Opener-Policy", "same-origin");\n  \n  // 9. Cross-Origin-Resource-Policy\n  res.setHeader("Cross-Origin-Resource-Policy", "same-origin");\n  \n  next();\n});' },
        { type: 'code', language: 'bash', value: '# Проверка security headers\ncurl -I https://example.com | grep -iE "^(strict|x-frame|x-content|content-security|referrer|permissions)"\n\n# Или используйте онлайн-инструменты:\n# securityheaders.com — оценка A-F\n# observatory.mozilla.org — подробный анализ\n\n# Nginx конфигурация:\n# add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;\n# add_header X-Content-Type-Options "nosniff" always;\n# add_header X-Frame-Options "DENY" always;\n# add_header Referrer-Policy "strict-origin-when-cross-origin" always;\n# add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;' },
        { type: 'tip', value: 'Проверяйте свои заголовки на securityheaders.com. Цель — оценка A+. Используйте helmet для Express.js или аналоги для вашего фреймворка — они устанавливают безопасные значения по умолчанию.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Защита от CSRF, SSRF и XXE',
      type: 'practice',
      difficulty: 'hard',
      description: 'Исправьте уязвимости CSRF, SSRF и XXE в веб-приложении и настройте полный набор security headers.',
      requirements: [
        'Добавьте CSRF токены к формам и настройте SameSite cookies',
        'Исправьте SSRF уязвимость в функции загрузки URL (whitelist + проверка IP)',
        'Защитите XML парсер от XXE атак',
        'Настройте все рекомендуемые HTTP security headers',
        'Проверьте защиту с помощью тестовых атак'
      ],
      hint: 'Используйте csurf middleware для CSRF, проверяйте IP адреса при SSRF, используйте defusedxml для XML, helmet для headers.',
      expectedOutput: 'CSRF: токен генерируется и проверяется, SameSite=Lax установлен\nSSRF: запросы к внутренним IP заблокированы (10.x, 127.x, 169.254.x)\nXXE: внешние entities отключены, DTD запрещены\nHeaders: HSTS, X-Frame-Options, CSP, X-Content-Type-Options установлены\nОценка securityheaders.com: A+',
      solution: 'const express = require("express");\nconst helmet = require("helmet");\nconst crypto = require("crypto");\nconst { URL } = require("url");\nconst dns = require("dns").promises;\nconst net = require("net");\n\nconst app = express();\n\n// === Security Headers (helmet) ===\napp.use(helmet());\napp.use(helmet.hsts({ maxAge: 31536000, includeSubDomains: true }));\n\n// === CSRF Protection ===\napp.use((req, res, next) => {\n  if (!req.session.csrfToken) {\n    req.session.csrfToken = crypto.randomBytes(32).toString("hex");\n  }\n  res.locals.csrfToken = req.session.csrfToken;\n  next();\n});\n\nconst validateCsrf = (req, res, next) => {\n  const token = req.body._csrf || req.headers["x-csrf-token"];\n  if (token !== req.session.csrfToken) {\n    return res.status(403).json({ error: "CSRF validation failed" });\n  }\n  next();\n};\n\napp.post("/transfer", validateCsrf, (req, res) => {\n  // Безопасная операция с CSRF защитой\n  res.json({ status: "ok" });\n});\n\n// === SSRF Protection ===\nconst PRIVATE_RANGES = [\n  [\"10.0.0.0\", \"10.255.255.255\"],\n  [\"172.16.0.0\", \"172.31.255.255\"],\n  [\"192.168.0.0\", \"192.168.255.255\"],\n  [\"127.0.0.0\", \"127.255.255.255\"],\n  [\"169.254.0.0\", \"169.254.255.255\"],\n];\n\nasync function isPrivateIP(hostname) {\n  const addresses = await dns.resolve4(hostname);\n  for (const addr of addresses) {\n    if (net.isIP(addr) && PRIVATE_RANGES.some(([start, end]) => {\n      const ip = addr.split(\".\").map(Number);\n      const s = start.split(\".\").map(Number);\n      const e = end.split(\".\").map(Number);\n      const ipNum = ip[0]*16777216+ip[1]*65536+ip[2]*256+ip[3];\n      return ipNum >= s[0]*16777216+s[1]*65536+s[2]*256+s[3] &&\n             ipNum <= e[0]*16777216+e[1]*65536+e[2]*256+e[3];\n    })) return true;\n  }\n  return false;\n}\n\napp.get("/fetch", async (req, res) => {\n  const url = new URL(req.query.url);\n  if (!["http:", "https:"].includes(url.protocol)) {\n    return res.status(403).json({ error: "Only HTTP(S) allowed" });\n  }\n  if (await isPrivateIP(url.hostname)) {\n    return res.status(403).json({ error: "Internal URLs blocked" });\n  }\n  // Безопасный запрос\n});',
      explanation: 'CSRF защита через токены и SameSite cookies. SSRF предотвращается проверкой IP адресов после DNS резолва и whitelist протоколов/доменов. XXE блокируется отключением DTD и external entities. Security headers обеспечивают дополнительный уровень защиты на уровне браузера.'
    }
  ]
}
