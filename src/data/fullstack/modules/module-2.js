export default {
  id: 2,
  title: 'Блог: Django + React + Docker',
  description: 'Создайте полноценную блог-платформу с системой аутентификации JWT, статьями, категориями, тегами и комментариями. Бэкенд на Django REST Framework, фронтенд на React с React Router, всё упаковано в Docker с nginx.',
  lessons: [
    {
      id: 201,
      title: 'Шаг 1: Django проект (startproject + startapp)',
      type: 'practice',
      difficulty: 'easy',
      description: 'Инициализируйте Django-проект и приложение blog. Настройте базовую конфигурацию: INSTALLED_APPS, база данных PostgreSQL, статика и медиафайлы.',
      requirements: [
        'Создать виртуальное окружение и установить зависимости',
        'django-admin startproject config . (в папке backend/)',
        'python manage.py startapp blog',
        'Настроить settings.py: DATABASES для PostgreSQL через переменные окружения',
        'Добавить django-environ для загрузки .env файла',
        'Настроить MEDIA_ROOT и STATIC_ROOT',
        'Убедиться что python manage.py runserver запускается без ошибок'
      ],
      expectedOutput: 'Django development server запущен на http://127.0.0.1:8000/. Страница /admin/ доступна. Суперпользователь создан через createsuperuser.',
      hint: 'pip install django djangorestframework django-environ psycopg2-binary Pillow djangorestframework-simplejwt django-cors-headers. В settings.py: import environ; env = environ.Env(); environ.Env.read_env()',
      solution: '# backend/requirements.txt\nDjango==4.2.7\ndjangorestframework==3.14.0\ndjango-environ==0.11.2\npsycopg2-binary==2.9.9\nPillow==10.1.0\ndjangorestframework-simplejwt==5.3.0\ndjango-cors-headers==4.3.1\ndjango-filter==23.3\n\n\n# backend/config/settings.py (ключевые настройки)\nimport environ\nfrom pathlib import Path\n\nBASE_DIR = Path(__file__).resolve().parent.parent\nenv = environ.Env(DEBUG=(bool, False))\nenviron.Env.read_env(BASE_DIR / ".env")\n\nSECRET_KEY = env("SECRET_KEY")\nDEBUG = env("DEBUG")\nALLOWED_HOSTS = env.list("ALLOWED_HOSTS", default=["*"])\n\nINSTALLED_APPS = [\n    "django.contrib.admin",\n    "django.contrib.auth",\n    "django.contrib.contenttypes",\n    "django.contrib.sessions",\n    "django.contrib.messages",\n    "django.contrib.staticfiles",\n    "rest_framework",\n    "rest_framework_simplejwt",\n    "corsheaders",\n    "django_filters",\n    "blog",\n]\n\nMIDDLEWARE = [\n    "corsheaders.middleware.CorsMiddleware",\n    "django.middleware.security.SecurityMiddleware",\n    # ... остальные middleware\n]\n\nDATABASES = {\n    "default": env.db("DATABASE_URL", default="sqlite:///db.sqlite3")\n}\n\nCORS_ALLOWED_ORIGINS = env.list("CORS_ALLOWED_ORIGINS", default=["http://localhost:5173"])\n\nMEDIA_URL = "/media/"\nMEDIA_ROOT = BASE_DIR / "media"\nSTATIC_URL = "/static/"\nSTATIC_ROOT = BASE_DIR / "staticfiles"\n\n\n# backend/.env\n# SECRET_KEY=your-secret-key-here\n# DEBUG=True\n# DATABASE_URL=postgresql://bloguser:blogpassword@localhost:5432/blogdb\n\n# Команды:\n# django-admin startproject config .\n# python manage.py startapp blog\n# python manage.py makemigrations\n# python manage.py migrate\n# python manage.py createsuperuser',
      explanation: 'django-environ позволяет загружать конфигурацию из .env файла — секреты не попадают в git. env.db() парсит DATABASE_URL строку в словарь настроек Django. Разделение config (настройки) и blog (бизнес-логика) следует принципу single responsibility.'
    },
    {
      id: 202,
      title: 'Шаг 2: Модели (Post, Category, Tag, Comment)',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создайте модели данных для блога: Post с slug и статусом публикации, Category, Tag (ManyToMany), Comment с вложенностью. Зарегистрируйте в admin.',
      requirements: [
        'Category: id, name, slug (unique), description',
        'Tag: id, name, slug (unique)',
        'Post: id, title, slug, content, excerpt, author (FK to User), category (FK), tags (M2M), status (draft/published), created_at, updated_at, cover_image',
        'Comment: id, post (FK), author (FK), content, created_at, parent (FK to self, nullable для вложенности)',
        'Автогенерация slug из title через pre_save сигнал или переопределение save()',
        'Зарегистрировать все модели в admin.py с list_display'
      ],
      expectedOutput: 'python manage.py makemigrations && migrate создаёт таблицы без ошибок. В Django Admin можно создать категорию, тег и пост.',
      hint: 'from django.utils.text import slugify. Для уникального slug: from django.utils.crypto import get_random_string; slug = slugify(title) + "-" + get_random_string(4). Auto slug в save(): if not self.slug: self.slug = slugify(self.title)',
      solution: '# blog/models.py\nfrom django.db import models\nfrom django.contrib.auth.models import User\nfrom django.utils.text import slugify\nimport uuid\n\nclass Category(models.Model):\n    name = models.CharField(max_length=100, unique=True)\n    slug = models.SlugField(max_length=100, unique=True, blank=True)\n    description = models.TextField(blank=True)\n\n    def save(self, *args, **kwargs):\n        if not self.slug:\n            self.slug = slugify(self.name)\n        super().save(*args, **kwargs)\n\n    def __str__(self):\n        return self.name\n\n    class Meta:\n        verbose_name_plural = "categories"\n\nclass Tag(models.Model):\n    name = models.CharField(max_length=50, unique=True)\n    slug = models.SlugField(max_length=50, unique=True, blank=True)\n\n    def save(self, *args, **kwargs):\n        if not self.slug:\n            self.slug = slugify(self.name)\n        super().save(*args, **kwargs)\n\n    def __str__(self):\n        return self.name\n\nclass Post(models.Model):\n    STATUS_DRAFT = "draft"\n    STATUS_PUBLISHED = "published"\n    STATUS_CHOICES = [(STATUS_DRAFT, "Черновик"), (STATUS_PUBLISHED, "Опубликован")]\n\n    title = models.CharField(max_length=300)\n    slug = models.SlugField(max_length=300, unique=True, blank=True)\n    content = models.TextField()\n    excerpt = models.TextField(max_length=500, blank=True)\n    author = models.ForeignKey(User, on_delete=models.CASCADE, related_name="posts")\n    category = models.ForeignKey(Category, on_delete=models.SET_NULL, null=True, blank=True, related_name="posts")\n    tags = models.ManyToManyField(Tag, blank=True, related_name="posts")\n    status = models.CharField(max_length=10, choices=STATUS_CHOICES, default=STATUS_DRAFT)\n    cover_image = models.ImageField(upload_to="covers/", blank=True, null=True)\n    created_at = models.DateTimeField(auto_now_add=True)\n    updated_at = models.DateTimeField(auto_now=True)\n\n    def save(self, *args, **kwargs):\n        if not self.slug:\n            base_slug = slugify(self.title)\n            slug = base_slug\n            counter = 1\n            while Post.objects.filter(slug=slug).exists():\n                slug = f"{base_slug}-{counter}"\n                counter += 1\n            self.slug = slug\n        super().save(*args, **kwargs)\n\n    def __str__(self):\n        return self.title\n\n    class Meta:\n        ordering = ["-created_at"]\n\nclass Comment(models.Model):\n    post = models.ForeignKey(Post, on_delete=models.CASCADE, related_name="comments")\n    author = models.ForeignKey(User, on_delete=models.CASCADE, related_name="comments")\n    content = models.TextField(max_length=1000)\n    parent = models.ForeignKey("self", on_delete=models.CASCADE, null=True, blank=True, related_name="replies")\n    created_at = models.DateTimeField(auto_now_add=True)\n\n    def __str__(self):\n        return f"Комментарий от {self.author} к {self.post}"\n\n    class Meta:\n        ordering = ["created_at"]\n\n\n# blog/admin.py\nfrom django.contrib import admin\nfrom .models import Post, Category, Tag, Comment\n\n@admin.register(Post)\nclass PostAdmin(admin.ModelAdmin):\n    list_display = ["title", "author", "category", "status", "created_at"]\n    list_filter = ["status", "category", "tags"]\n    search_fields = ["title", "content"]\n    prepopulated_fields = {"slug": ("title",)}\n\nadmin.site.register(Category)\nadmin.site.register(Tag)\nadmin.site.register(Comment)',
      explanation: 'Авто-генерация уникального slug с counter решает проблему дубликатов при одинаковых заголовках. ForeignKey с related_name="posts" позволяет обращаться category.posts.all() — reverse relation. Comment с FK на self (parent) реализует вложенные комментарии без дополнительных таблиц. ManyToManyField для тегов нормализует данные.'
    },
    {
      id: 203,
      title: 'Шаг 3: DRF API (serializers, viewsets, routers)',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создайте REST API с помощью Django REST Framework: сериализаторы для всех моделей, ViewSet-ы с фильтрацией и поиском, роутер для автоматической генерации URL.',
      requirements: [
        'PostSerializer: вложенный CategorySerializer и TagSerializer (read-only), поле author_name',
        'PostListSerializer: облегчённая версия без полного content (только excerpt)',
        'CommentSerializer с вложенными replies',
        'PostViewSet: list (только published), retrieve, create, update, destroy',
        'Фильтрация постов по category__slug и тегам',
        'Поиск по title и content (SearchFilter)',
        'DefaultRouter для автогенерации /api/posts/, /api/posts/{slug}/ и т.д.'
      ],
      expectedOutput: 'GET /api/posts/ возвращает список опубликованных постов. GET /api/posts/{slug}/ возвращает пост с комментариями. Работает поиск: GET /api/posts/?search=django',
      hint: 'Для lookup по slug вместо id: lookup_field = "slug" в ViewSet. В get_queryset(): return Post.objects.filter(status="published"). SerializerMethodField для author_name: return obj.author.get_full_name() or obj.author.username',
      solution: '# blog/serializers.py\nfrom rest_framework import serializers\nfrom django.contrib.auth.models import User\nfrom .models import Post, Category, Tag, Comment\n\nclass TagSerializer(serializers.ModelSerializer):\n    class Meta:\n        model = Tag\n        fields = ["id", "name", "slug"]\n\nclass CategorySerializer(serializers.ModelSerializer):\n    class Meta:\n        model = Category\n        fields = ["id", "name", "slug", "description"]\n\nclass AuthorSerializer(serializers.ModelSerializer):\n    class Meta:\n        model = User\n        fields = ["id", "username", "first_name", "last_name"]\n\nclass CommentSerializer(serializers.ModelSerializer):\n    author = AuthorSerializer(read_only=True)\n    replies = serializers.SerializerMethodField()\n\n    class Meta:\n        model = Comment\n        fields = ["id", "author", "content", "created_at", "parent", "replies"]\n        read_only_fields = ["author", "created_at"]\n\n    def get_replies(self, obj):\n        if obj.replies.exists():\n            return CommentSerializer(obj.replies.all(), many=True).data\n        return []\n\nclass PostListSerializer(serializers.ModelSerializer):\n    author = AuthorSerializer(read_only=True)\n    category = CategorySerializer(read_only=True)\n    tags = TagSerializer(many=True, read_only=True)\n    comments_count = serializers.IntegerField(source="comments.count", read_only=True)\n\n    class Meta:\n        model = Post\n        fields = ["id", "title", "slug", "excerpt", "author", "category", "tags",\n                  "status", "cover_image", "created_at", "comments_count"]\n\nclass PostSerializer(PostListSerializer):\n    comments = serializers.SerializerMethodField()\n\n    class Meta(PostListSerializer.Meta):\n        fields = PostListSerializer.Meta.fields + ["content", "comments"]\n\n    def get_comments(self, obj):\n        top_level = obj.comments.filter(parent=None)\n        return CommentSerializer(top_level, many=True).data\n\n\n# blog/views.py\nfrom rest_framework import viewsets, permissions, filters\nfrom rest_framework.decorators import action\nfrom rest_framework.response import Response\nfrom django_filters.rest_framework import DjangoFilterBackend\nfrom .models import Post, Category, Tag, Comment\nfrom .serializers import (PostSerializer, PostListSerializer,\n                          CategorySerializer, TagSerializer, CommentSerializer)\n\nclass PostViewSet(viewsets.ModelViewSet):\n    lookup_field = "slug"\n    filter_backends = [DjangoFilterBackend, filters.SearchFilter, filters.OrderingFilter]\n    filterset_fields = ["category__slug", "tags__slug", "status"]\n    search_fields = ["title", "content", "excerpt"]\n    ordering_fields = ["created_at", "updated_at"]\n\n    def get_queryset(self):\n        if self.request.user.is_authenticated and self.action in ["list"] and \\\n           self.request.query_params.get("mine"):\n            return Post.objects.filter(author=self.request.user)\n        return Post.objects.filter(status="published")\n\n    def get_serializer_class(self):\n        if self.action == "list":\n            return PostListSerializer\n        return PostSerializer\n\n    def get_permissions(self):\n        if self.action in ["create", "update", "partial_update", "destroy"]:\n            return [permissions.IsAuthenticated()]\n        return [permissions.AllowAny()]\n\n    def perform_create(self, serializer):\n        serializer.save(author=self.request.user)\n\n\n# blog/urls.py\nfrom rest_framework.routers import DefaultRouter\nfrom .views import PostViewSet\n\nrouter = DefaultRouter()\nrouter.register(r"posts", PostViewSet, basename="post")\n\nurlpatterns = router.urls',
      explanation: 'ViewSet объединяет list/create/retrieve/update/destroy в один класс — меньше кода, больше порядка. lookup_field="slug" делает URL читаемыми (/api/posts/django-basics/ вместо /api/posts/42/). SerializerMethodField для get_comments возвращает только top-level комментарии, а вложенные replies получаются рекурсивно. get_serializer_class() возвращает разные сериализаторы для list и detail — список постов не грузит полный контент.'
    },
    {
      id: 204,
      title: 'Шаг 4: Аутентификация (JWT, register/login)',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте JWT-аутентификацию с помощью djangorestframework-simplejwt. Создайте эндпоинты для регистрации, входа и получения данных текущего пользователя.',
      requirements: [
        'POST /api/auth/register/ — регистрация (username, email, password, password2)',
        'POST /api/auth/token/ — получение JWT токенов (access + refresh)',
        'POST /api/auth/token/refresh/ — обновление access токена',
        'GET /api/auth/me/ — данные текущего пользователя (requires auth)',
        'Валидация: пароли должны совпадать, username уникален',
        'Настроить REST_FRAMEWORK: DEFAULT_AUTHENTICATION_CLASSES с JWTAuthentication',
        'Access token живёт 15 минут, refresh — 7 дней'
      ],
      expectedOutput: 'POST /api/auth/register/ создаёт пользователя и возвращает токены. POST /api/auth/token/ с username/password возвращает {access, refresh}. GET /api/auth/me/ с заголовком Authorization: Bearer <token> возвращает данные юзера.',
      hint: 'SIMPLE_JWT settings: ACCESS_TOKEN_LIFETIME = timedelta(minutes=15), REFRESH_TOKEN_LIFETIME = timedelta(days=7). Для /me/ endpoint используйте RetrieveAPIView с permission_classes = [IsAuthenticated] и queryset = User.objects.all(). get_object() возвращает self.request.user.',
      solution: '# blog/auth_views.py\nfrom rest_framework import generics, permissions, status\nfrom rest_framework.response import Response\nfrom rest_framework_simplejwt.tokens import RefreshToken\nfrom django.contrib.auth.models import User\nfrom rest_framework import serializers\n\nclass RegisterSerializer(serializers.ModelSerializer):\n    password2 = serializers.CharField(write_only=True)\n\n    class Meta:\n        model = User\n        fields = ["username", "email", "password", "password2"]\n        extra_kwargs = {"password": {"write_only": True}}\n\n    def validate(self, data):\n        if data["password"] != data["password2"]:\n            raise serializers.ValidationError({"password": "Пароли не совпадают"})\n        if User.objects.filter(username=data["username"]).exists():\n            raise serializers.ValidationError({"username": "Имя пользователя уже занято"})\n        return data\n\n    def create(self, validated_data):\n        validated_data.pop("password2")\n        user = User.objects.create_user(**validated_data)\n        return user\n\nclass RegisterView(generics.CreateAPIView):\n    serializer_class = RegisterSerializer\n    permission_classes = [permissions.AllowAny]\n\n    def create(self, request, *args, **kwargs):\n        serializer = self.get_serializer(data=request.data)\n        serializer.is_valid(raise_exception=True)\n        user = serializer.save()\n        refresh = RefreshToken.for_user(user)\n        return Response({\n            "user": {"id": user.id, "username": user.username, "email": user.email},\n            "access": str(refresh.access_token),\n            "refresh": str(refresh),\n        }, status=status.HTTP_201_CREATED)\n\nclass MeView(generics.RetrieveAPIView):\n    permission_classes = [permissions.IsAuthenticated]\n\n    def retrieve(self, request, *args, **kwargs):\n        user = request.user\n        return Response({\n            "id": user.id,\n            "username": user.username,\n            "email": user.email,\n            "first_name": user.first_name,\n            "last_name": user.last_name,\n            "date_joined": user.date_joined,\n        })\n\n\n# config/settings.py (добавить)\nfrom datetime import timedelta\n\nREST_FRAMEWORK = {\n    "DEFAULT_AUTHENTICATION_CLASSES": (\n        "rest_framework_simplejwt.authentication.JWTAuthentication",\n    ),\n    "DEFAULT_PERMISSION_CLASSES": (\n        "rest_framework.permissions.IsAuthenticatedOrReadOnly",\n    ),\n}\n\nSIMPLE_JWT = {\n    "ACCESS_TOKEN_LIFETIME": timedelta(minutes=15),\n    "REFRESH_TOKEN_LIFETIME": timedelta(days=7),\n    "ROTATE_REFRESH_TOKENS": True,\n}\n\n\n# config/urls.py\nfrom django.urls import path, include\nfrom rest_framework_simplejwt.views import TokenObtainPairView, TokenRefreshView\nfrom blog.auth_views import RegisterView, MeView\n\nurlpatterns = [\n    path("api/auth/register/", RegisterView.as_view()),\n    path("api/auth/token/", TokenObtainPairView.as_view()),\n    path("api/auth/token/refresh/", TokenRefreshView.as_view()),\n    path("api/auth/me/", MeView.as_view()),\n    path("api/", include("blog.urls")),\n]',
      explanation: 'JWT (JSON Web Token) — stateless аутентификация: сервер не хранит сессии, токен содержит всю информацию. Access token короткоживущий (15 мин) — если украден, быстро протухает. Refresh token долгоживущий (7 дней) — используется только для получения нового access. ROTATE_REFRESH_TOKENS=True выдаёт новый refresh при каждом обновлении — старый токен инвалидируется.'
    },
    {
      id: 205,
      title: 'Шаг 5: React setup (Vite + React Router)',
      type: 'practice',
      difficulty: 'easy',
      description: 'Настройте React-приложение с маршрутизацией, глобальным состоянием для аутентификации и базовой структурой страниц.',
      requirements: [
        'Установить: react-router-dom, axios',
        'Создать структуру: pages/ (Home, PostDetail, CreatePost, Login, Register), components/',
        'Настроить React Router: маршруты для всех страниц',
        'AuthContext: хранить user, token, функции login/logout',
        'PrivateRoute компонент: редиректит на /login если не авторизован',
        'axios instance с baseURL и interceptor для добавления Authorization заголовка',
        'Навигационный компонент с ссылками и кнопкой выхода'
      ],
      expectedOutput: 'Переход по /login показывает форму входа. Переход по /create (без авторизации) редиректит на /login. После входа — редирект на главную страницу. Токен хранится в localStorage.',
      hint: 'AuthContext: const token = localStorage.getItem("token"). axios.interceptors.request.use: config.headers.Authorization = `Bearer ${token}`. PrivateRoute: if (!user) return <Navigate to="/login" />',
      solution: '// src/context/AuthContext.jsx\nimport { createContext, useContext, useState, useEffect } from "react";\nimport api from "../api/axios";\n\nconst AuthContext = createContext(null);\n\nexport function AuthProvider({ children }) {\n  const [user, setUser] = useState(null);\n  const [loading, setLoading] = useState(true);\n\n  useEffect(() => {\n    const token = localStorage.getItem("access_token");\n    if (token) {\n      api.get("/auth/me/")\n        .then((res) => setUser(res.data))\n        .catch(() => localStorage.removeItem("access_token"))\n        .finally(() => setLoading(false));\n    } else {\n      setLoading(false);\n    }\n  }, []);\n\n  const login = (tokens, userData) => {\n    localStorage.setItem("access_token", tokens.access);\n    localStorage.setItem("refresh_token", tokens.refresh);\n    setUser(userData);\n  };\n\n  const logout = () => {\n    localStorage.removeItem("access_token");\n    localStorage.removeItem("refresh_token");\n    setUser(null);\n  };\n\n  return (\n    <AuthContext.Provider value={{ user, loading, login, logout }}>\n      {children}\n    </AuthContext.Provider>\n  );\n}\n\nexport const useAuth = () => useContext(AuthContext);\n\n\n// src/api/axios.js\nimport axios from "axios";\n\nconst api = axios.create({\n  baseURL: "http://localhost:8000/api",\n  headers: { "Content-Type": "application/json" },\n});\n\napi.interceptors.request.use((config) => {\n  const token = localStorage.getItem("access_token");\n  if (token) config.headers.Authorization = `Bearer ${token}`;\n  return config;\n});\n\napi.interceptors.response.use(\n  (res) => res,\n  async (error) => {\n    if (error.response?.status === 401) {\n      const refresh = localStorage.getItem("refresh_token");\n      if (refresh) {\n        try {\n          const res = await axios.post("http://localhost:8000/api/auth/token/refresh/", { refresh });\n          localStorage.setItem("access_token", res.data.access);\n          error.config.headers.Authorization = `Bearer ${res.data.access}`;\n          return api(error.config);\n        } catch {\n          localStorage.removeItem("access_token");\n          localStorage.removeItem("refresh_token");\n          window.location.href = "/login";\n        }\n      }\n    }\n    return Promise.reject(error);\n  }\n);\n\nexport default api;\n\n\n// src/components/PrivateRoute.jsx\nimport { Navigate } from "react-router-dom";\nimport { useAuth } from "../context/AuthContext";\n\nexport default function PrivateRoute({ children }) {\n  const { user, loading } = useAuth();\n  if (loading) return <div>Загрузка...</div>;\n  return user ? children : <Navigate to="/login" replace />;\n}\n\n\n// src/App.jsx\nimport { BrowserRouter, Routes, Route } from "react-router-dom";\nimport { AuthProvider } from "./context/AuthContext";\nimport PrivateRoute from "./components/PrivateRoute";\nimport Navbar from "./components/Navbar";\nimport Home from "./pages/Home";\nimport PostDetail from "./pages/PostDetail";\nimport CreatePost from "./pages/CreatePost";\nimport Login from "./pages/Login";\nimport Register from "./pages/Register";\n\nexport default function App() {\n  return (\n    <AuthProvider>\n      <BrowserRouter>\n        <Navbar />\n        <Routes>\n          <Route path="/" element={<Home />} />\n          <Route path="/posts/:slug" element={<PostDetail />} />\n          <Route path="/login" element={<Login />} />\n          <Route path="/register" element={<Register />} />\n          <Route path="/create" element={<PrivateRoute><CreatePost /></PrivateRoute>} />\n        </Routes>\n      </BrowserRouter>\n    </AuthProvider>\n  );\n}',
      explanation: 'AuthContext — глобальное состояние аутентификации через React Context, исключает prop drilling. axios interceptor автоматически добавляет JWT-токен к каждому запросу и обновляет его при истечении (401 → refresh → повторный запрос). Разделение token refresh logic в interceptor делает компоненты чище — они не знают об истечении токенов.'
    },
    {
      id: 206,
      title: 'Шаг 6: Компоненты (PostList, PostDetail, CreatePost, Comments)',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создайте основные страницы блога: список постов с карточками, детальная страница поста с комментариями, форма создания нового поста.',
      requirements: [
        'Home: загружает и отображает список постов (PostCard компоненты)',
        'PostCard: title, excerpt, автор, дата, категория, теги, ссылка на /posts/:slug',
        'PostDetail: загружает пост по slug, отображает полный контент и комментарии',
        'CommentSection: список комментариев + форма добавления (только для авторизованных)',
        'CreatePost: форма с полями title, content, category, tags, status',
        'После создания — редирект на страницу поста'
      ],
      expectedOutput: 'Главная страница показывает список постов. Клик на пост открывает детальную страницу. Авторизованный пользователь может добавить комментарий. Форма создания поста работает.',
      hint: 'useParams() из react-router-dom для получения slug. useNavigate() для редиректа после создания. Для тегов — multiple select или input с запятыми.',
      solution: '// src/pages/Home.jsx\nimport { useState, useEffect } from "react";\nimport api from "../api/axios";\nimport PostCard from "../components/PostCard";\n\nexport default function Home() {\n  const [posts, setPosts] = useState([]);\n  const [loading, setLoading] = useState(true);\n\n  useEffect(() => {\n    api.get("/posts/").then((res) => {\n      setPosts(res.data.results || res.data);\n      setLoading(false);\n    });\n  }, []);\n\n  if (loading) return <div className="loading">Загрузка постов...</div>;\n\n  return (\n    <main className="container">\n      <h1>Последние статьи</h1>\n      <div className="posts-grid">\n        {posts.map((post) => <PostCard key={post.id} post={post} />)}\n      </div>\n    </main>\n  );\n}\n\n\n// src/components/PostCard.jsx\nimport { Link } from "react-router-dom";\n\nexport default function PostCard({ post }) {\n  return (\n    <article className="post-card">\n      {post.cover_image && <img src={post.cover_image} alt={post.title} />}\n      <div className="post-card-body">\n        {post.category && <span className="category">{post.category.name}</span>}\n        <h2><Link to={`/posts/${post.slug}`}>{post.title}</Link></h2>\n        <p className="excerpt">{post.excerpt}</p>\n        <div className="meta">\n          <span>{post.author.username}</span>\n          <span>{new Date(post.created_at).toLocaleDateString("ru-RU")}</span>\n          <span>{post.comments_count} комментариев</span>\n        </div>\n        <div className="tags">\n          {post.tags.map((tag) => (\n            <span key={tag.id} className="tag">#{tag.name}</span>\n          ))}\n        </div>\n      </div>\n    </article>\n  );\n}\n\n\n// src/pages/PostDetail.jsx\nimport { useState, useEffect } from "react";\nimport { useParams } from "react-router-dom";\nimport api from "../api/axios";\nimport CommentSection from "../components/CommentSection";\n\nexport default function PostDetail() {\n  const { slug } = useParams();\n  const [post, setPost] = useState(null);\n\n  useEffect(() => {\n    api.get(`/posts/${slug}/`).then((res) => setPost(res.data));\n  }, [slug]);\n\n  if (!post) return <div className="loading">Загрузка...</div>;\n\n  return (\n    <main className="container post-detail">\n      <h1>{post.title}</h1>\n      <div className="meta">\n        <span>Автор: {post.author.username}</span>\n        <span>{new Date(post.created_at).toLocaleDateString("ru-RU")}</span>\n      </div>\n      <div className="post-content" dangerouslySetInnerHTML={{ __html: post.content }} />\n      <CommentSection postSlug={slug} initialComments={post.comments} />\n    </main>\n  );\n}\n\n\n// src/components/CommentSection.jsx\nimport { useState } from "react";\nimport { useAuth } from "../context/AuthContext";\nimport api from "../api/axios";\n\nexport default function CommentSection({ postSlug, initialComments }) {\n  const [comments, setComments] = useState(initialComments || []);\n  const [text, setText] = useState("");\n  const { user } = useAuth();\n\n  const handleSubmit = async (e) => {\n    e.preventDefault();\n    if (!text.trim()) return;\n    const res = await api.post(`/posts/${postSlug}/comments/`, { content: text });\n    setComments((prev) => [...prev, res.data]);\n    setText("");\n  };\n\n  return (\n    <section className="comments">\n      <h3>Комментарии ({comments.length})</h3>\n      {comments.map((c) => (\n        <div key={c.id} className="comment">\n          <strong>{c.author.username}</strong>\n          <p>{c.content}</p>\n        </div>\n      ))}\n      {user ? (\n        <form onSubmit={handleSubmit}>\n          <textarea value={text} onChange={(e) => setText(e.target.value)}\n            placeholder="Ваш комментарий..." rows={3} />\n          <button type="submit">Отправить</button>\n        </form>\n      ) : (\n        <p>Войдите чтобы оставить комментарий.</p>\n      )}\n    </section>\n  );\n}',
      explanation: 'dangerouslySetInnerHTML используется для рендера HTML-контента поста — в production нужна дополнительная санитизация (DOMPurify). PostCard отображает только preview данные (excerpt), полный контент загружается только на странице детального просмотра — оптимизация трафика. CommentSection использует локальный state для мгновенного добавления комментариев без перезагрузки страницы.'
    },
    {
      id: 207,
      title: 'Шаг 7: Rich Text Editor (Markdown)',
      type: 'practice',
      difficulty: 'medium',
      description: 'Добавьте редактор для написания статей. Реализуйте поддержку Markdown: редактор с превью, конвертация Markdown в HTML при сохранении и отображении.',
      requirements: [
        'Установить @uiw/react-md-editor для редактирования',
        'Установить marked (или react-markdown) для рендера',
        'В форме CreatePost заменить textarea на MDEditor',
        'Добавить превью в режиме реального времени',
        'На бэкенде сохранять сырой Markdown в поле content',
        'На странице PostDetail рендерить Markdown в HTML через react-markdown',
        'Поддержка: заголовки, жирный, курсив, ссылки, код, списки'
      ],
      expectedOutput: 'В форме создания поста редактор показывает две панели: исходный Markdown слева и превью справа. На странице поста контент красиво отрендерен с форматированием.',
      hint: 'npm install @uiw/react-md-editor react-markdown. MDEditor: <MDEditor value={content} onChange={setContent} />. Рендер: <ReactMarkdown>{post.content}</ReactMarkdown>',
      solution: '// Установка:\n// npm install @uiw/react-md-editor react-markdown remark-gfm\n\n// src/pages/CreatePost.jsx\nimport { useState, useEffect } from "react";\nimport { useNavigate } from "react-router-dom";\nimport MDEditor from "@uiw/react-md-editor";\nimport api from "../api/axios";\n\nexport default function CreatePost() {\n  const navigate = useNavigate();\n  const [formData, setFormData] = useState({\n    title: "",\n    content: "# Заголовок\\n\\nНачните писать...",\n    excerpt: "",\n    status: "draft",\n    category: "",\n    tags: [],\n  });\n  const [categories, setCategories] = useState([]);\n  const [allTags, setAllTags] = useState([]);\n  const [error, setError] = useState(null);\n\n  useEffect(() => {\n    api.get("/categories/").then((r) => setCategories(r.data));\n    api.get("/tags/").then((r) => setAllTags(r.data));\n  }, []);\n\n  const handleSubmit = async (e) => {\n    e.preventDefault();\n    try {\n      const res = await api.post("/posts/", formData);\n      navigate(`/posts/${res.data.slug}`);\n    } catch (err) {\n      setError("Ошибка при создании поста");\n    }\n  };\n\n  return (\n    <main className="container">\n      <h1>Новая статья</h1>\n      {error && <div className="error">{error}</div>}\n      <form onSubmit={handleSubmit} className="create-form">\n        <input\n          type="text"\n          placeholder="Заголовок статьи"\n          value={formData.title}\n          onChange={(e) => setFormData({ ...formData, title: e.target.value })}\n          required\n        />\n        <input\n          type="text"\n          placeholder="Краткое описание (excerpt)"\n          value={formData.excerpt}\n          onChange={(e) => setFormData({ ...formData, excerpt: e.target.value })}\n        />\n        <div data-color-mode="light">\n          <MDEditor\n            value={formData.content}\n            onChange={(val) => setFormData({ ...formData, content: val || "" })}\n            height={400}\n          />\n        </div>\n        <select\n          value={formData.category}\n          onChange={(e) => setFormData({ ...formData, category: e.target.value })}\n        >\n          <option value="">Без категории</option>\n          {categories.map((c) => (\n            <option key={c.id} value={c.id}>{c.name}</option>\n          ))}\n        </select>\n        <select\n          value={formData.status}\n          onChange={(e) => setFormData({ ...formData, status: e.target.value })}\n        >\n          <option value="draft">Черновик</option>\n          <option value="published">Опубликовать</option>\n        </select>\n        <button type="submit">Сохранить</button>\n      </form>\n    </main>\n  );\n}\n\n\n// src/pages/PostDetail.jsx (рендер markdown)\nimport ReactMarkdown from "react-markdown";\nimport remarkGfm from "remark-gfm";\n\n// Заменить dangerouslySetInnerHTML на:\n<ReactMarkdown remarkPlugins={[remarkGfm]}>\n  {post.content}\n</ReactMarkdown>',
      explanation: 'Хранение сырого Markdown в БД — правильный подход: контент не зависит от рендерера, его легко редактировать и конвертировать. react-markdown рендерит Markdown безопасно (без XSS), в отличие от dangerouslySetInnerHTML. remark-gfm добавляет GitHub Flavored Markdown: таблицы, зачёркнутый текст, списки задач. MDEditor даёт split-view редактирование.'
    },
    {
      id: 208,
      title: 'Шаг 8: Пагинация и поиск',
      type: 'practice',
      difficulty: 'medium',
      description: 'Добавьте пагинацию для списка постов на бэкенде (PageNumberPagination) и компонент поиска на фронтенде с debounce.',
      requirements: [
        'Настроить PageNumberPagination: PAGE_SIZE = 10 в DRF settings',
        'Ответ списка: { count, next, previous, results }',
        'React: компонент Pagination с кнопками Previous/Next и номерами страниц',
        'Компонент SearchBar с input и debounce 300ms',
        'При поиске: GET /api/posts/?search=текст&page=1',
        'URL синхронизируется с поиском и страницей (useSearchParams)',
        'Показывать "Ничего не найдено" если results пуст'
      ],
      expectedOutput: 'Список постов показывает 10 штук, кнопки навигации работают. При вводе в поле поиска список фильтруется через 300ms. URL меняется: /?search=react&page=2',
      hint: 'useSearchParams из react-router-dom для синхронизации с URL. debounce через useEffect с setTimeout: const timer = setTimeout(() => setDebouncedSearch(search), 300); return () => clearTimeout(timer);',
      solution: '# config/settings.py (добавить)\nREST_FRAMEWORK = {\n    # ... остальные настройки ...\n    "DEFAULT_PAGINATION_CLASS": "rest_framework.pagination.PageNumberPagination",\n    "PAGE_SIZE": 10,\n}\n\n\n// src/hooks/useDebounce.js\nimport { useState, useEffect } from "react";\n\nexport function useDebounce(value, delay = 300) {\n  const [debounced, setDebounced] = useState(value);\n  useEffect(() => {\n    const timer = setTimeout(() => setDebounced(value), delay);\n    return () => clearTimeout(timer);\n  }, [value, delay]);\n  return debounced;\n}\n\n\n// src/pages/Home.jsx (с поиском и пагинацией)\nimport { useState, useEffect } from "react";\nimport { useSearchParams } from "react-router-dom";\nimport api from "../api/axios";\nimport PostCard from "../components/PostCard";\nimport Pagination from "../components/Pagination";\nimport SearchBar from "../components/SearchBar";\nimport { useDebounce } from "../hooks/useDebounce";\n\nexport default function Home() {\n  const [searchParams, setSearchParams] = useSearchParams();\n  const [posts, setPosts] = useState([]);\n  const [count, setCount] = useState(0);\n  const [loading, setLoading] = useState(true);\n\n  const page = parseInt(searchParams.get("page") || "1");\n  const search = searchParams.get("search") || "";\n  const debouncedSearch = useDebounce(search);\n\n  useEffect(() => {\n    setLoading(true);\n    const params = { page };\n    if (debouncedSearch) params.search = debouncedSearch;\n    api.get("/posts/", { params }).then((res) => {\n      setPosts(res.data.results);\n      setCount(res.data.count);\n      setLoading(false);\n    });\n  }, [page, debouncedSearch]);\n\n  const handleSearch = (value) => {\n    setSearchParams({ search: value, page: 1 });\n  };\n\n  const handlePage = (newPage) => {\n    setSearchParams({ search, page: newPage });\n  };\n\n  return (\n    <main className="container">\n      <SearchBar value={search} onChange={handleSearch} />\n      {loading ? <div>Загрузка...</div> : (\n        <>\n          {posts.length === 0 ? (\n            <p className="empty">Ничего не найдено</p>\n          ) : (\n            <div className="posts-grid">\n              {posts.map((post) => <PostCard key={post.id} post={post} />)}\n            </div>\n          )}\n          <Pagination\n            count={count}\n            pageSize={10}\n            currentPage={page}\n            onPageChange={handlePage}\n          />\n        </>\n      )}\n    </main>\n  );\n}',
      explanation: 'useSearchParams синхронизирует состояние страницы и поиска с URL — пользователь может поделиться ссылкой на конкретную страницу поиска. Debounce предотвращает лишние API-запросы при каждом нажатии клавиши. Серверная пагинация масштабируется — клиент получает только 10 постов, а не всю таблицу.'
    },
    {
      id: 209,
      title: 'Шаг 9: Docker (multi-container с nginx)',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создайте production-ready Docker окружение: Django + Gunicorn, React + nginx, PostgreSQL. Nginx выступает reverse proxy и раздаёт статику Django.',
      requirements: [
        'backend/Dockerfile: python:3.11-slim, pip install gunicorn, collectstatic',
        'frontend/Dockerfile: multi-stage (node + nginx)',
        'nginx/nginx.conf: upstream django, location /api → django, location /media → media files',
        'docker-compose.yml: 4 сервиса — db, django, react, nginx',
        'Django использует Gunicorn (не uvicorn) через CMD',
        'Автоматический запуск миграций через entrypoint.sh',
        'Volumes для media и static файлов (shared между django и nginx)'
      ],
      expectedOutput: 'docker-compose up --build запускает всё приложение. http://localhost открывает блог. API доступен через http://localhost/api/. Медиафайлы раздаются через nginx.',
      hint: 'entrypoint.sh: python manage.py migrate --no-input && python manage.py collectstatic --no-input && gunicorn config.wsgi:application --bind 0.0.0.0:8000. Shared volume: volumes: - static_volume:/app/staticfiles',
      solution: '# backend/Dockerfile\nFROM python:3.11-slim\nWORKDIR /app\nRUN apt-get update && apt-get install -y libpq-dev gcc && rm -rf /var/lib/apt/lists/*\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt gunicorn\nCOPY . .\nCOPY entrypoint.sh /entrypoint.sh\nRUN chmod +x /entrypoint.sh\nEXPOSE 8000\nENTRYPOINT ["/entrypoint.sh"]\n\n\n# backend/entrypoint.sh\n#!/bin/sh\npython manage.py migrate --no-input\npython manage.py collectstatic --no-input\ngunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 4\n\n\n# nginx/nginx.conf\nupstream django {\n    server django:8000;\n}\n\nserver {\n    listen 80;\n\n    location /api {\n        proxy_pass http://django;\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n    }\n\n    location /admin {\n        proxy_pass http://django;\n        proxy_set_header Host $host;\n    }\n\n    location /static {\n        alias /static;\n    }\n\n    location /media {\n        alias /media;\n    }\n\n    location / {\n        root /usr/share/nginx/html;\n        index index.html;\n        try_files $uri $uri/ /index.html;\n    }\n}\n\n\n# docker-compose.yml\nversion: "3.9"\n\nservices:\n  db:\n    image: postgres:15-alpine\n    environment:\n      POSTGRES_USER: bloguser\n      POSTGRES_PASSWORD: blogpassword\n      POSTGRES_DB: blogdb\n    volumes:\n      - postgres_data:/var/lib/postgresql/data\n    healthcheck:\n      test: ["CMD-SHELL", "pg_isready -U bloguser"]\n      interval: 5s\n      retries: 5\n\n  django:\n    build: ./backend\n    environment:\n      DATABASE_URL: postgresql://bloguser:blogpassword@db:5432/blogdb\n      SECRET_KEY: change-me-in-production\n      DEBUG: "False"\n    volumes:\n      - static_volume:/app/staticfiles\n      - media_volume:/app/media\n    depends_on:\n      db:\n        condition: service_healthy\n\n  frontend:\n    build:\n      context: ./frontend\n      dockerfile: Dockerfile\n    volumes:\n      - frontend_build:/usr/share/nginx/html\n\n  nginx:\n    image: nginx:alpine\n    ports:\n      - "80:80"\n    volumes:\n      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf\n      - static_volume:/static\n      - media_volume:/media\n      - frontend_build:/usr/share/nginx/html\n    depends_on:\n      - django\n      - frontend\n\nvolumes:\n  postgres_data:\n  static_volume:\n  media_volume:\n  frontend_build:',
      explanation: 'Gunicorn — production WSGI-сервер для Django (в отличие от development manage.py runserver). 4 workers обрабатывают запросы параллельно. Nginx как reverse proxy: TLS termination, gzip, кэширование статики — всё в одном месте. Shared volumes (static_volume, media_volume) позволяют nginx напрямую раздавать файлы без проксирования через Django.'
    },
    {
      id: 210,
      title: 'Шаг 10: CI/CD (GitHub Actions: test + build + deploy)',
      type: 'practice',
      difficulty: 'hard',
      description: 'Настройте автоматический pipeline через GitHub Actions: запуск тестов Django при push, сборка Docker образов и публикация на Docker Hub.',
      requirements: [
        'Создать .github/workflows/ci.yml',
        'Триггер: push и pull_request в ветку main',
        'Job 1 (test): запускает pytest с PostgreSQL в качестве service',
        'Job 2 (build): собирает Docker образы (зависит от test)',
        'Job 3 (push): пушит образы на Docker Hub (только при push в main)',
        'Секреты: DOCKER_USERNAME, DOCKER_PASSWORD в GitHub Secrets',
        'Badge в README: статус последнего CI запуска'
      ],
      expectedOutput: 'При push в main — GitHub Actions запускает пайплайн. Зелёный badge в README означает что тесты прошли и образы собраны. Docker Hub содержит теги: latest и SHA коммита.',
      hint: 'services: postgres: image: postgres:15 env: POSTGRES_DB/USER/PASSWORD, ports: ["5432:5432"]. Для Docker Hub: docker/login-action@v3 с secrets.DOCKER_USERNAME. Тег образа: ${{ github.sha }}',
      solution: '# .github/workflows/ci.yml\nname: CI/CD Pipeline\n\non:\n  push:\n    branches: [main]\n  pull_request:\n    branches: [main]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    services:\n      postgres:\n        image: postgres:15\n        env:\n          POSTGRES_DB: testdb\n          POSTGRES_USER: testuser\n          POSTGRES_PASSWORD: testpassword\n        ports:\n          - 5432:5432\n        options: >-\n          --health-cmd pg_isready\n          --health-interval 10s\n          --health-timeout 5s\n          --health-retries 5\n\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Set up Python\n        uses: actions/setup-python@v4\n        with:\n          python-version: "3.11"\n          cache: "pip"\n          cache-dependency-path: backend/requirements.txt\n\n      - name: Install dependencies\n        run: pip install -r backend/requirements.txt pytest pytest-django\n        working-directory: .\n\n      - name: Run tests\n        env:\n          DATABASE_URL: postgresql://testuser:testpassword@localhost:5432/testdb\n          SECRET_KEY: test-secret-key\n          DEBUG: "True"\n        run: pytest backend/\n\n  build:\n    needs: test\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Build backend image\n        run: docker build -t blog-backend ./backend\n\n      - name: Build frontend image\n        run: docker build -t blog-frontend ./frontend\n\n  push:\n    needs: build\n    runs-on: ubuntu-latest\n    if: github.ref == "refs/heads/main" && github.event_name == "push"\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Login to Docker Hub\n        uses: docker/login-action@v3\n        with:\n          username: ${{ secrets.DOCKER_USERNAME }}\n          password: ${{ secrets.DOCKER_PASSWORD }}\n\n      - name: Build and push backend\n        uses: docker/build-push-action@v5\n        with:\n          context: ./backend\n          push: true\n          tags: |\n            ${{ secrets.DOCKER_USERNAME }}/blog-backend:latest\n            ${{ secrets.DOCKER_USERNAME }}/blog-backend:${{ github.sha }}\n\n      - name: Build and push frontend\n        uses: docker/build-push-action@v5\n        with:\n          context: ./frontend\n          push: true\n          tags: |\n            ${{ secrets.DOCKER_USERNAME }}/blog-frontend:latest\n            ${{ secrets.DOCKER_USERNAME }}/blog-frontend:${{ github.sha }}',
      explanation: 'CI/CD pipeline автоматизирует рутину: каждый push запускает тесты и сборку, исключая "у меня работает". GitHub Actions services запускают PostgreSQL прямо в CI окружении. Тег с SHA коммита позволяет откатиться к любой версии. Разделение на jobs (test → build → push) создаёт fail-fast: если тесты упали, docker push не запустится.'
    }
  ]
};
