export default {
  id: 13,
  title: 'Практикум: Проектирование API',
  description: 'Практические задачи по проектированию REST API: URL, методы, статус-коды, запросы и ответы.',
  lessons: [
    {
      id: 1,
      title: 'Проектирование URL для блога',
      type: 'practice',
      difficulty: 'easy',
      description: 'Спроектируйте RESTful URL для блог-платформы с постами, комментариями и тегами.',
      requirements: [
        'Определите endpoints для CRUD операций с постами (/api/posts)',
        'Добавьте вложенные ресурсы: комментарии к посту (/api/posts/{id}/comments)',
        'Добавьте теги поста (/api/posts/{id}/tags)',
        'Для каждого endpoint укажите HTTP метод и описание',
        'Выведите таблицу всех endpoints (минимум 10)'
      ],
      expectedOutput: 'Метод  | URL                              | Описание\nGET    | /api/posts                       | Список постов\nPOST   | /api/posts                       | Создать пост\nGET    | /api/posts/1                     | Получить пост\nPUT    | /api/posts/1                     | Обновить пост\nDELETE | /api/posts/1                     | Удалить пост\nGET    | /api/posts/1/comments            | Комментарии поста\nPOST   | /api/posts/1/comments            | Добавить комментарий\nDELETE | /api/posts/1/comments/5          | Удалить комментарий\nGET    | /api/posts/1/tags                | Теги поста\nPOST   | /api/posts/1/tags                | Добавить тег\nGET    | /api/tags                        | Все теги',
      hint: 'Используйте вложенные ресурсы для связей: /posts/{id}/comments. Для тегов — и отдельный endpoint /tags, и связь через /posts/{id}/tags.',
      solution: 'public class Main {\n    public static void main(String[] args) {\n        String[][] endpoints = {\n            {"GET",    "/api/posts",                "Список постов"},\n            {"POST",   "/api/posts",                "Создать пост"},\n            {"GET",    "/api/posts/1",              "Получить пост"},\n            {"PUT",    "/api/posts/1",              "Обновить пост"},\n            {"DELETE", "/api/posts/1",              "Удалить пост"},\n            {"GET",    "/api/posts/1/comments",     "Комментарии поста"},\n            {"POST",   "/api/posts/1/comments",     "Добавить комментарий"},\n            {"DELETE", "/api/posts/1/comments/5",   "Удалить комментарий"},\n            {"GET",    "/api/posts/1/tags",          "Теги поста"},\n            {"POST",   "/api/posts/1/tags",          "Добавить тег"},\n            {"GET",    "/api/tags",                  "Все теги"},\n        };\n\n        System.out.printf("%-7s| %-33s| %s%n", "Метод", "URL", "Описание");\n        for (String[] ep : endpoints) {\n            System.out.printf("%-7s| %-33s| %s%n", ep[0], ep[1], ep[2]);\n        }\n    }\n}',
      explanation: 'RESTful URL строятся по принципу: существительные во множественном числе (/posts), вложенные ресурсы через слэш (/posts/1/comments). HTTP метод определяет действие: GET — чтение, POST — создание, PUT — обновление, DELETE — удаление. Это стандартный подход к проектированию API.'
    },
    {
      id: 2,
      title: 'CRUD для интернет-магазина',
      type: 'practice',
      difficulty: 'easy',
      description: 'Реализуйте полный CRUD API для товаров интернет-магазина с правильными статус-кодами.',
      requirements: [
        'POST /products — создание (201 Created)',
        'GET /products — список всех (200 OK)',
        'GET /products/{id} — один товар (200 OK или 404)',
        'PUT /products/{id} — обновление (200 OK или 404)',
        'DELETE /products/{id} — удаление (204 No Content или 404)',
        'Выведите результат каждой операции со статус-кодом'
      ],
      expectedOutput: 'POST /products {name: iPhone, price: 999} -> 201 Created {id: 1}\nPOST /products {name: MacBook, price: 1999} -> 201 Created {id: 2}\nGET /products -> 200 OK [2 товара]\nGET /products/1 -> 200 OK {id: 1, name: iPhone}\nGET /products/99 -> 404 Not Found\nPUT /products/1 {name: iPhone 15} -> 200 OK {id: 1, name: iPhone 15}\nPUT /products/99 -> 404 Not Found\nDELETE /products/1 -> 204 No Content\nDELETE /products/99 -> 404 Not Found\nGET /products -> 200 OK [1 товар]',
      hint: 'Используйте Map<Integer, Map<String, Object>> для хранения. Всегда проверяйте существование ресурса перед GET/PUT/DELETE.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<Integer, Map<String, Object>> products = new LinkedHashMap<>();\n    static int nextId = 1;\n\n    public static void main(String[] args) {\n        // POST\n        System.out.println("POST /products {name: iPhone, price: 999} -> "\n            + create("iPhone", 999));\n        System.out.println("POST /products {name: MacBook, price: 1999} -> "\n            + create("MacBook", 1999));\n\n        // GET list\n        System.out.println("GET /products -> 200 OK [" + products.size() + " товара]");\n\n        // GET by id\n        System.out.println("GET /products/1 -> " + getById(1));\n        System.out.println("GET /products/99 -> " + getById(99));\n\n        // PUT\n        System.out.println("PUT /products/1 {name: iPhone 15} -> " + update(1, "iPhone 15", 1099));\n        System.out.println("PUT /products/99 -> " + update(99, "X", 0));\n\n        // DELETE\n        System.out.println("DELETE /products/1 -> " + delete(1));\n        System.out.println("DELETE /products/99 -> " + delete(99));\n\n        // GET list after delete\n        System.out.println("GET /products -> 200 OK [" + products.size() + " товар]");\n    }\n\n    static String create(String name, int price) {\n        int id = nextId++;\n        Map<String, Object> p = new LinkedHashMap<>();\n        p.put("id", id);\n        p.put("name", name);\n        p.put("price", price);\n        products.put(id, p);\n        return "201 Created {id: " + id + "}";\n    }\n\n    static String getById(int id) {\n        Map<String, Object> p = products.get(id);\n        if (p == null) return "404 Not Found";\n        return "200 OK {id: " + p.get("id") + ", name: " + p.get("name") + "}";\n    }\n\n    static String update(int id, String name, int price) {\n        Map<String, Object> p = products.get(id);\n        if (p == null) return "404 Not Found";\n        p.put("name", name);\n        p.put("price", price);\n        return "200 OK {id: " + p.get("id") + ", name: " + p.get("name") + "}";\n    }\n\n    static String delete(int id) {\n        if (products.remove(id) == null) return "404 Not Found";\n        return "204 No Content";\n    }\n}',
      explanation: 'CRUD операции маппятся на HTTP методы: Create=POST, Read=GET, Update=PUT, Delete=DELETE. Статус-коды: 201 для создания, 200 для чтения/обновления, 204 для удаления, 404 если ресурс не найден. Это стандартный паттерн REST API.'
    },
    {
      id: 3,
      title: 'Пагинация и сортировка',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте endpoint GET /products с поддержкой пагинации, сортировки и фильтрации.',
      requirements: [
        'Создайте 20 товаров для тестирования',
        'Пагинация: page и size параметры (по умолчанию page=1, size=5)',
        'Сортировка: sort параметр (name, -name, price, -price)',
        'Фильтрация: category параметр',
        'Ответ содержит data, page, size, totalElements, totalPages'
      ],
      expectedOutput: '=== GET /products?page=1&size=5 ===\n{data: [Товар1, Товар2, Товар3, Товар4, Товар5], page: 1, size: 5, total: 20, pages: 4}\n\n=== GET /products?page=2&size=5&sort=price ===\n{data: [по цене 5 товаров], page: 2, size: 5, total: 20, pages: 4}\n\n=== GET /products?category=Электроника ===\n{data: [отфильтровано], total: 8}',
      hint: 'Используйте List.subList(from, to) для пагинации. Collections.sort() с Comparator для сортировки. Stream.filter() для фильтрации.',
      solution: 'import java.util.*;\nimport java.util.stream.*;\n\npublic class Main {\n    static List<Map<String, Object>> products = new ArrayList<>();\n    static {\n        String[] categories = {"Электроника", "Одежда", "Книги", "Еда"};\n        for (int i = 1; i <= 20; i++) {\n            Map<String, Object> p = new LinkedHashMap<>();\n            p.put("id", i);\n            p.put("name", "Товар " + i);\n            p.put("price", 100 + (i * 50));\n            p.put("category", categories[i % categories.length]);\n            products.add(p);\n        }\n    }\n\n    public static void main(String[] args) {\n        // Простая пагинация\n        System.out.println("=== GET /products?page=1&size=5 ===");\n        getProducts(1, 5, null, null);\n\n        // Сортировка по цене + пагинация\n        System.out.println("\\n=== GET /products?page=2&size=5&sort=price ===");\n        getProducts(2, 5, "price", null);\n\n        // Фильтрация по категории\n        System.out.println("\\n=== GET /products?category=Электроника ===");\n        getProducts(1, 5, null, "Электроника");\n    }\n\n    static void getProducts(int page, int size, String sort, String category) {\n        // Фильтрация\n        List<Map<String, Object>> filtered = products;\n        if (category != null) {\n            filtered = products.stream()\n                .filter(p -> category.equals(p.get("category")))\n                .collect(Collectors.toList());\n        }\n\n        // Сортировка\n        if (sort != null) {\n            boolean desc = sort.startsWith("-");\n            String field = desc ? sort.substring(1) : sort;\n            filtered = new ArrayList<>(filtered);\n            filtered.sort((a, b) -> {\n                Comparable va = (Comparable) a.get(field);\n                Comparable vb = (Comparable) b.get(field);\n                return desc ? vb.compareTo(va) : va.compareTo(vb);\n            });\n        }\n\n        // Пагинация\n        int total = filtered.size();\n        int totalPages = (int) Math.ceil((double) total / size);\n        int from = (page - 1) * size;\n        int to = Math.min(from + size, total);\n        List<Map<String, Object>> pageData = from < total\n            ? filtered.subList(from, to) : Collections.emptyList();\n\n        // Вывод\n        List<String> names = pageData.stream()\n            .map(p -> p.get("name") + "(" + p.get("price") + ")")\n            .collect(Collectors.toList());\n\n        System.out.println("{data: " + names\n            + ", page: " + page + ", size: " + size\n            + ", total: " + total + ", pages: " + totalPages + "}");\n    }\n}',
      explanation: 'Пагинация в REST API: page (номер страницы) и size (элементов на странице). Ответ содержит метаданные: totalElements, totalPages. Сортировка через sort=field (ASC) или sort=-field (DESC). Фильтрация через query параметры. Всё это стандартные паттерны для GET /collection endpoints.'
    },
    {
      id: 4,
      title: 'Обработка ошибок в заказах',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте создание заказа с комплексной валидацией и правильными кодами ошибок.',
      requirements: [
        'POST /orders принимает: userId, productId, quantity, shippingAddress',
        'Проверки: пользователь существует (404), товар существует (404), товар в наличии (409 Conflict), quantity <= stock',
        'При ошибке валидации — 422 со списком ошибок полей',
        'При успехе — 201 с созданным заказом',
        'Покажите 4 сценария: успех, пользователь не найден, товар закончился, ошибка валидации'
      ],
      expectedOutput: '=== Успешный заказ ===\nPOST /orders {userId:1, productId:1, qty:2, address: ул. Абая 150}\n201 Created: {orderId: 1, total: 1998}\n\n=== Пользователь не найден ===\nPOST /orders {userId:99, productId:1, qty:1}\n404 Not Found: Пользователь id=99 не найден\n\n=== Товар закончился ===\nPOST /orders {userId:1, productId:2, qty:5}\n409 Conflict: Недостаточно товара (в наличии: 0)\n\n=== Ошибка валидации ===\nPOST /orders {userId:null, productId:-1, qty:0}\n422: [userId: обязательно, productId: должен быть > 0, quantity: от 1 до 100]',
      hint: 'Проверяйте в правильном порядке: сначала валидация полей (422), потом существование ресурсов (404), потом бизнес-правила (409).',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<Integer, String> users = Map.of(1, "Алия", 2, "Бауыржан");\n    static Map<Integer, int[]> products = new HashMap<>(); // id -> [price, stock]\n    static {\n        products.put(1, new int[]{999, 10});\n        products.put(2, new int[]{1999, 0}); // нет в наличии\n    }\n    static int nextOrderId = 1;\n\n    public static void main(String[] args) {\n        System.out.println("=== Успешный заказ ===");\n        System.out.println("POST /orders {userId:1, productId:1, qty:2, address: ул. Абая 150}");\n        System.out.println(createOrder(1, 1, 2, "ул. Абая 150, Алматы"));\n\n        System.out.println("\\n=== Пользователь не найден ===");\n        System.out.println("POST /orders {userId:99, productId:1, qty:1}");\n        System.out.println(createOrder(99, 1, 1, "ул. Абая 150, Алматы"));\n\n        System.out.println("\\n=== Товар закончился ===");\n        System.out.println("POST /orders {userId:1, productId:2, qty:5}");\n        System.out.println(createOrder(1, 2, 5, "ул. Абая 150, Алматы"));\n\n        System.out.println("\\n=== Ошибка валидации ===");\n        System.out.println("POST /orders {userId:null, productId:-1, qty:0}");\n        System.out.println(createOrder(-1, -1, 0, ""));\n    }\n\n    static String createOrder(int userId, int productId, int qty, String address) {\n        // 1. Валидация полей\n        List<String> errors = new ArrayList<>();\n        if (userId <= 0) errors.add("userId: обязательно");\n        if (productId <= 0) errors.add("productId: должен быть > 0");\n        if (qty < 1 || qty > 100) errors.add("quantity: от 1 до 100");\n        if (address == null || address.length() < 10) errors.add("address: минимум 10 символов");\n\n        if (!errors.isEmpty()) {\n            return "422: " + errors;\n        }\n\n        // 2. Существование ресурсов\n        if (!users.containsKey(userId)) {\n            return "404 Not Found: Пользователь id=" + userId + " не найден";\n        }\n        if (!products.containsKey(productId)) {\n            return "404 Not Found: Товар id=" + productId + " не найден";\n        }\n\n        // 3. Бизнес-правила\n        int[] product = products.get(productId);\n        int stock = product[1];\n        if (stock < qty) {\n            return "409 Conflict: Недостаточно товара (в наличии: " + stock + ")";\n        }\n\n        // 4. Создание заказа\n        product[1] -= qty;\n        int total = product[0] * qty;\n        return "201 Created: {orderId: " + nextOrderId++ + ", total: " + total + "}";\n    }\n}',
      explanation: 'Порядок проверок при создании ресурса: 1) Валидация формата (422), 2) Существование связанных ресурсов (404), 3) Бизнес-правила (409 Conflict). Каждый тип ошибки имеет свой HTTP код. 422 — невалидные данные, 404 — ресурс не найден, 409 — конфликт состояния (нет на складе).'
    },
    {
      id: 5,
      title: 'Версионирование API',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте маршрутизатор API с поддержкой версий через URL и заголовки.',
      requirements: [
        'Поддержите 2 версии API: v1 и v2',
        'v1: GET /api/v1/users возвращает {id, name, email}',
        'v2: GET /api/v2/users возвращает {id, firstName, lastName, email, createdAt}',
        'Также поддержите версионирование через заголовок Accept: application/vnd.api.v2+json',
        'Покажите обработку запросов с разными версиями'
      ],
      expectedOutput: '=== URL версионирование ===\nGET /api/v1/users/1 -> {id: 1, name: Алия Серикова, email: aliya@mail.kz}\nGET /api/v2/users/1 -> {id: 1, firstName: Алия, lastName: Серикова, email: aliya@mail.kz, createdAt: 2024-01-15}\n\n=== Header версионирование ===\nGET /api/users/1 [Accept: application/vnd.api.v1+json]\n-> v1: {id: 1, name: Алия Серикова}\nGET /api/users/1 [Accept: application/vnd.api.v2+json]\n-> v2: {id: 1, firstName: Алия, lastName: Серикова}\n\n=== Без версии -> последняя (v2) ===\nGET /api/users/1 -> v2 (по умолчанию)',
      hint: 'Создайте два метода getUserV1() и getUserV2() с разными форматами ответа. Для header-версионирования парсите Accept заголовок.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<Integer, Map<String, String>> users = new HashMap<>();\n    static {\n        Map<String, String> user = new LinkedHashMap<>();\n        user.put("firstName", "Алия");\n        user.put("lastName", "Серикова");\n        user.put("email", "aliya@mail.kz");\n        user.put("createdAt", "2024-01-15");\n        users.put(1, user);\n    }\n\n    public static void main(String[] args) {\n        // URL версионирование\n        System.out.println("=== URL версионирование ===");\n        System.out.println("GET /api/v1/users/1 -> " + getUserV1(1));\n        System.out.println("GET /api/v2/users/1 -> " + getUserV2(1));\n\n        // Header версионирование\n        System.out.println("\\n=== Header версионирование ===");\n        System.out.println("GET /api/users/1 [Accept: application/vnd.api.v1+json]");\n        System.out.println("-> v1: " + routeByHeader(1, "application/vnd.api.v1+json"));\n        System.out.println("GET /api/users/1 [Accept: application/vnd.api.v2+json]");\n        System.out.println("-> v2: " + routeByHeader(1, "application/vnd.api.v2+json"));\n\n        // По умолчанию\n        System.out.println("\\n=== Без версии -> последняя (v2) ===");\n        System.out.println("GET /api/users/1 -> v2 (по умолчанию)");\n    }\n\n    static String getUserV1(int id) {\n        Map<String, String> u = users.get(id);\n        if (u == null) return "404 Not Found";\n        return "{id: " + id + ", name: " + u.get("firstName") + " "\n            + u.get("lastName") + ", email: " + u.get("email") + "}";\n    }\n\n    static String getUserV2(int id) {\n        Map<String, String> u = users.get(id);\n        if (u == null) return "404 Not Found";\n        return "{id: " + id + ", firstName: " + u.get("firstName")\n            + ", lastName: " + u.get("lastName")\n            + ", email: " + u.get("email")\n            + ", createdAt: " + u.get("createdAt") + "}";\n    }\n\n    static String routeByHeader(int id, String accept) {\n        if (accept.contains("v1")) return getUserV1(id);\n        if (accept.contains("v2")) return getUserV2(id);\n        return getUserV2(id); // по умолчанию последняя\n    }\n}',
      explanation: 'Версионирование API позволяет менять формат ответа без поломки клиентов. URL-версионирование (/v1/, /v2/) — самый простой и распространённый способ. Header-версионирование (Accept) — более "чистый" REST, но сложнее. В v2 мы разделили name на firstName/lastName — это breaking change, требующее новой версии.'
    },
    {
      id: 6,
      title: 'Вложенные ресурсы',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте API с вложенными ресурсами: пользователи, их заказы, товары в заказах.',
      requirements: [
        'GET /users — список пользователей',
        'GET /users/{id}/orders — заказы пользователя',
        'GET /users/{id}/orders/{orderId}/items — товары в заказе',
        'Каждый уровень вложенности проверяет существование родителя',
        'Если родительский ресурс не найден — 404'
      ],
      expectedOutput: 'GET /users -> [{id:1, name:Алия}, {id:2, name:Бауыржан}]\nGET /users/1/orders -> [{orderId:101, total:1998}, {orderId:102, total:999}]\nGET /users/1/orders/101/items -> [{product:iPhone, qty:2}, {product:Case, qty:1}]\nGET /users/99/orders -> 404: Пользователь 99 не найден\nGET /users/1/orders/999/items -> 404: Заказ 999 не найден',
      hint: 'Используйте вложенные Map: users -> orders -> items. На каждом уровне проверяйте существование родителя.',
      solution: 'import java.util.*;\n\npublic class Main {\n    // users: id -> name\n    static Map<Integer, String> users = Map.of(1, "Алия", 2, "Бауыржан");\n    // orders: userId -> list of orders\n    static Map<Integer, List<Map<String, Object>>> orders = new HashMap<>();\n    // items: orderId -> list of items\n    static Map<Integer, List<Map<String, Object>>> items = new HashMap<>();\n    static {\n        orders.put(1, List.of(\n            Map.of("orderId", 101, "total", 1998),\n            Map.of("orderId", 102, "total", 999)\n        ));\n        orders.put(2, List.of(\n            Map.of("orderId", 201, "total", 2499)\n        ));\n        items.put(101, List.of(\n            Map.of("product", "iPhone", "qty", 2),\n            Map.of("product", "Case", "qty", 1)\n        ));\n        items.put(102, List.of(\n            Map.of("product", "AirPods", "qty", 1)\n        ));\n    }\n\n    public static void main(String[] args) {\n        System.out.println("GET /users -> " + getUsers());\n        System.out.println("GET /users/1/orders -> " + getUserOrders(1));\n        System.out.println("GET /users/1/orders/101/items -> " + getOrderItems(1, 101));\n        System.out.println("GET /users/99/orders -> " + getUserOrders(99));\n        System.out.println("GET /users/1/orders/999/items -> " + getOrderItems(1, 999));\n    }\n\n    static String getUsers() {\n        List<String> result = new ArrayList<>();\n        users.forEach((id, name) ->\n            result.add("{id:" + id + ", name:" + name + "}"));\n        return result.toString();\n    }\n\n    static String getUserOrders(int userId) {\n        if (!users.containsKey(userId))\n            return "404: Пользователь " + userId + " не найден";\n        List<Map<String, Object>> userOrders = orders.getOrDefault(userId, List.of());\n        return userOrders.toString();\n    }\n\n    static String getOrderItems(int userId, int orderId) {\n        if (!users.containsKey(userId))\n            return "404: Пользователь " + userId + " не найден";\n        List<Map<String, Object>> userOrders = orders.getOrDefault(userId, List.of());\n        boolean orderExists = userOrders.stream()\n            .anyMatch(o -> (int) o.get("orderId") == orderId);\n        if (!orderExists)\n            return "404: Заказ " + orderId + " не найден";\n        return items.getOrDefault(orderId, List.of()).toString();\n    }\n}',
      explanation: 'Вложенные ресурсы (/users/1/orders/101/items) отражают иерархию данных. На каждом уровне проверяется существование родителя: нельзя получить заказы несуществующего пользователя. Глубина вложенности обычно не превышает 3 уровней — глубже стоит использовать плоские endpoints с query параметрами.'
    },
    {
      id: 7,
      title: 'Поиск и фильтрация',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте endpoint поиска товаров с множественными фильтрами, диапазонами цен и полнотекстовым поиском.',
      requirements: [
        'GET /products?q=phone — поиск по названию (содержит подстроку)',
        'GET /products?category=Electronics — фильтр по категории',
        'GET /products?minPrice=100&maxPrice=500 — диапазон цен',
        'GET /products?inStock=true — только в наличии',
        'Комбинация фильтров: ?q=phone&minPrice=500&inStock=true',
        'Показать количество найденных результатов'
      ],
      expectedOutput: '=== Поиск: q=Phone ===\nНайдено 2: [iPhone 15 (999), Phone Case (29)]\n\n=== Фильтр: category=Electronics ===\nНайдено 3: [iPhone 15 (999), MacBook (1999), AirPods (249)]\n\n=== Диапазон: minPrice=100, maxPrice=500 ===\nНайдено 2: [AirPods (249), Keyboard (149)]\n\n=== Комбинация: q=a, category=Electronics, minPrice=200 ===\nНайдено 2: [MacBook (1999), AirPods (249)]\n\n=== inStock=true ===\nНайдено 5 из 6',
      hint: 'Применяйте фильтры последовательно через stream().filter(). Каждый фильтр сужает выборку. Для поиска используйте toLowerCase().contains().',
      solution: 'import java.util.*;\nimport java.util.stream.*;\n\npublic class Main {\n    static List<Map<String, Object>> products = new ArrayList<>();\n    static {\n        products.add(product("iPhone 15", 999, "Electronics", true));\n        products.add(product("MacBook", 1999, "Electronics", true));\n        products.add(product("AirPods", 249, "Electronics", true));\n        products.add(product("Phone Case", 29, "Accessories", true));\n        products.add(product("Keyboard", 149, "Accessories", true));\n        products.add(product("Monitor", 799, "Electronics", false));\n    }\n\n    public static void main(String[] args) {\n        System.out.println("=== Поиск: q=Phone ===");\n        search("Phone", null, null, null, null);\n\n        System.out.println("\\n=== Фильтр: category=Electronics ===");\n        search(null, "Electronics", null, null, null);\n\n        System.out.println("\\n=== Диапазон: minPrice=100, maxPrice=500 ===");\n        search(null, null, 100, 500, null);\n\n        System.out.println("\\n=== Комбинация: q=a, category=Electronics, minPrice=200 ===");\n        search("a", "Electronics", 200, null, null);\n\n        System.out.println("\\n=== inStock=true ===");\n        search(null, null, null, null, true);\n    }\n\n    static void search(String q, String category,\n                       Integer minPrice, Integer maxPrice, Boolean inStock) {\n        Stream<Map<String, Object>> stream = products.stream();\n\n        if (q != null) {\n            String lower = q.toLowerCase();\n            stream = stream.filter(p ->\n                ((String) p.get("name")).toLowerCase().contains(lower));\n        }\n        if (category != null) {\n            stream = stream.filter(p -> category.equals(p.get("category")));\n        }\n        if (minPrice != null) {\n            stream = stream.filter(p -> (int) p.get("price") >= minPrice);\n        }\n        if (maxPrice != null) {\n            stream = stream.filter(p -> (int) p.get("price") <= maxPrice);\n        }\n        if (inStock != null) {\n            stream = stream.filter(p -> inStock.equals(p.get("inStock")));\n        }\n\n        List<Map<String, Object>> result = stream.collect(Collectors.toList());\n        String items = result.stream()\n            .map(p -> p.get("name") + " (" + p.get("price") + ")")\n            .collect(Collectors.joining(", "));\n\n        System.out.println("Найдено " + result.size()\n            + (inStock != null ? " из " + products.size() : "")\n            + ": [" + items + "]");\n    }\n\n    static Map<String, Object> product(String name, int price,\n                                       String category, boolean inStock) {\n        Map<String, Object> p = new LinkedHashMap<>();\n        p.put("name", name);\n        p.put("price", price);\n        p.put("category", category);\n        p.put("inStock", inStock);\n        return p;\n    }\n}',
      explanation: 'Поиск и фильтрация в REST API реализуются через query параметры GET запроса. Фильтры комбинируются (AND логика): каждый сужает выборку. q — полнотекстовый поиск (contains), category — точное совпадение, minPrice/maxPrice — диапазон. В реальных API фильтры валидируются и документируются в OpenAPI.'
    },
    {
      id: 8,
      title: 'Bulk операции',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте массовые (bulk) операции API: создание, обновление и удаление нескольких ресурсов за один запрос.',
      requirements: [
        'POST /products/bulk — создать несколько товаров за раз',
        'PATCH /products/bulk — обновить цены нескольких товаров',
        'DELETE /products/bulk — удалить несколько товаров по ID',
        'Каждая операция возвращает результат для каждого элемента (успех/ошибка)',
        'При частичном успехе — 207 Multi-Status'
      ],
      expectedOutput: '=== POST /products/bulk (создание 3 товаров) ===\n207 Multi-Status:\n  {id:1, name:iPhone} -> 201 Created\n  {id:2, name:MacBook} -> 201 Created\n  {name:} -> 422 Validation Error (name обязательно)\n\n=== PATCH /products/bulk (обновление цен) ===\n207 Multi-Status:\n  id=1: price 999 -> 899 (200 OK)\n  id=99: 404 Not Found\n\n=== DELETE /products/bulk [1, 2, 99] ===\n207 Multi-Status:\n  id=1: 204 Deleted\n  id=2: 204 Deleted\n  id=99: 404 Not Found',
      hint: 'Для каждого элемента в bulk запросе выполняйте операцию отдельно и собирайте результаты. 207 Multi-Status когда есть и успехи, и ошибки.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<Integer, Map<String, Object>> products = new LinkedHashMap<>();\n    static int nextId = 1;\n\n    public static void main(String[] args) {\n        // Bulk создание\n        System.out.println("=== POST /products/bulk (создание 3 товаров) ===");\n        String[][] items = {{"iPhone", "999"}, {"MacBook", "1999"}, {"", "500"}};\n        System.out.println("207 Multi-Status:");\n        for (String[] item : items) {\n            String name = item[0];\n            int price = Integer.parseInt(item[1]);\n            if (name.isEmpty()) {\n                System.out.println("  {name:} -> 422 Validation Error (name обязательно)");\n            } else {\n                int id = nextId++;\n                Map<String, Object> p = new LinkedHashMap<>();\n                p.put("name", name);\n                p.put("price", price);\n                products.put(id, p);\n                System.out.println("  {id:" + id + ", name:" + name + "} -> 201 Created");\n            }\n        }\n\n        // Bulk обновление\n        System.out.println("\\n=== PATCH /products/bulk (обновление цен) ===");\n        System.out.println("207 Multi-Status:");\n        int[][] updates = {{1, 899}, {99, 0}};\n        for (int[] upd : updates) {\n            int id = upd[0];\n            int newPrice = upd[1];\n            Map<String, Object> p = products.get(id);\n            if (p == null) {\n                System.out.println("  id=" + id + ": 404 Not Found");\n            } else {\n                int oldPrice = (int) p.get("price");\n                p.put("price", newPrice);\n                System.out.println("  id=" + id + ": price " + oldPrice\n                    + " -> " + newPrice + " (200 OK)");\n            }\n        }\n\n        // Bulk удаление\n        System.out.println("\\n=== DELETE /products/bulk [1, 2, 99] ===");\n        System.out.println("207 Multi-Status:");\n        int[] deleteIds = {1, 2, 99};\n        for (int id : deleteIds) {\n            if (products.remove(id) != null) {\n                System.out.println("  id=" + id + ": 204 Deleted");\n            } else {\n                System.out.println("  id=" + id + ": 404 Not Found");\n            }\n        }\n    }\n}',
      explanation: 'Bulk операции позволяют обрабатывать несколько ресурсов за один запрос — это снижает количество HTTP round-trips. 207 Multi-Status используется когда часть операций успешна, а часть нет. Каждый элемент результата содержит свой статус-код. Stripe и Google API поддерживают bulk/batch операции для производительности.'
    },
    {
      id: 9,
      title: 'Idempotency Key',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте механизм идемпотентности для POST запросов с использованием Idempotency-Key заголовка.',
      requirements: [
        'POST /payments принимает заголовок Idempotency-Key',
        'Первый запрос с ключом — выполняется и результат кэшируется',
        'Повторный запрос с тем же ключом — возвращает кэшированный результат',
        'Запрос без ключа — каждый раз создаёт новый ресурс',
        'Покажите защиту от дублирования платежей'
      ],
      expectedOutput: '=== POST /payments (key: pay_001) ===\n201 Created: {paymentId: 1, amount: 5000, status: SUCCESS}\n\n=== POST /payments (key: pay_001, повтор) ===\n200 OK (из кэша): {paymentId: 1, amount: 5000, status: SUCCESS}\nДубликат предотвращён!\n\n=== POST /payments (key: pay_002) ===\n201 Created: {paymentId: 2, amount: 3000, status: SUCCESS}\n\n=== POST /payments (без ключа) ===\n201 Created: {paymentId: 3}\n\n=== POST /payments (без ключа, повтор) ===\n201 Created: {paymentId: 4}\nОпасно! Без Idempotency-Key создано 2 платежа!',
      hint: 'Используйте Map<String, String> для кэша (key -> result). Перед выполнением проверяйте наличие ключа в кэше.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<String, String> idempotencyCache = new HashMap<>();\n    static int nextPaymentId = 1;\n\n    public static void main(String[] args) {\n        // Первый запрос с ключом\n        System.out.println("=== POST /payments (key: pay_001) ===");\n        System.out.println(createPayment("pay_001", 5000));\n\n        // Повторный запрос с тем же ключом\n        System.out.println("\\n=== POST /payments (key: pay_001, повтор) ===");\n        System.out.println(createPayment("pay_001", 5000));\n        System.out.println("Дубликат предотвращён!");\n\n        // Новый ключ\n        System.out.println("\\n=== POST /payments (key: pay_002) ===");\n        System.out.println(createPayment("pay_002", 3000));\n\n        // Без ключа\n        System.out.println("\\n=== POST /payments (без ключа) ===");\n        System.out.println(createPayment(null, 1000));\n\n        // Без ключа, повтор\n        System.out.println("\\n=== POST /payments (без ключа, повтор) ===");\n        System.out.println(createPayment(null, 1000));\n        System.out.println("Опасно! Без Idempotency-Key создано 2 платежа!");\n    }\n\n    static String createPayment(String idempotencyKey, int amount) {\n        // Проверяем кэш\n        if (idempotencyKey != null) {\n            String cached = idempotencyCache.get(idempotencyKey);\n            if (cached != null) {\n                return "200 OK (из кэша): " + cached;\n            }\n        }\n\n        // Выполняем операцию\n        int paymentId = nextPaymentId++;\n        String result = "{paymentId: " + paymentId\n            + ", amount: " + amount + ", status: SUCCESS}";\n\n        // Кэшируем результат\n        if (idempotencyKey != null) {\n            idempotencyCache.put(idempotencyKey, result);\n            return "201 Created: " + result;\n        }\n\n        return "201 Created: {paymentId: " + paymentId + "}";\n    }\n}',
      explanation: 'Idempotency-Key решает проблему дублирования при сетевых сбоях. Если клиент отправил POST и не получил ответ (timeout), он повторяет запрос с тем же ключом. Сервер: если ключ есть в кэше — возвращает сохранённый результат (не выполняя повторно). Stripe API требует Idempotency-Key для всех POST запросов. Ключи хранят 24 часа.'
    },
    {
      id: 10,
      title: 'API Gateway маршрутизатор',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте API Gateway, который маршрутизирует запросы к разным микросервисам, добавляя аутентификацию и rate limiting.',
      requirements: [
        '/api/users/* -> User Service (порт 8081)',
        '/api/products/* -> Product Service (порт 8082)',
        '/api/orders/* -> Order Service (порт 8083)',
        'Проверка API Key перед маршрутизацией',
        'Rate limiting: 10 запросов в минуту на API Key',
        'Логирование: метод, путь, сервис, время, статус'
      ],
      expectedOutput: '=== API Gateway ===\nGET /api/users/1 [key: sk_abc] -> User Service:8081 (200 OK, 15ms)\nPOST /api/orders [key: sk_abc] -> Order Service:8083 (201 Created, 45ms)\nGET /api/products [key: sk_abc] -> Product Service:8082 (200 OK, 12ms)\nGET /api/unknown [key: sk_abc] -> 404 (маршрут не найден)\nGET /api/users/1 [no key] -> 401 Unauthorized\n--- Rate limit test (11 запросов) ---\nЗапрос 10: 200 OK\nЗапрос 11: 429 Too Many Requests',
      hint: 'Создайте Map<String, String[]> routes (prefix -> [serviceName, port]). Для rate limiting используйте Map<String, Integer> counters.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<String, String[]> routes = Map.of(\n        "/api/users", new String[]{"User Service", "8081"},\n        "/api/products", new String[]{"Product Service", "8082"},\n        "/api/orders", new String[]{"Order Service", "8083"}\n    );\n    static Set<String> validKeys = Set.of("sk_abc", "sk_xyz");\n    static Map<String, Integer> rateLimits = new HashMap<>();\n    static final int RATE_LIMIT = 10;\n\n    public static void main(String[] args) {\n        System.out.println("=== API Gateway ===");\n        handleRequest("GET", "/api/users/1", "sk_abc");\n        handleRequest("POST", "/api/orders", "sk_abc");\n        handleRequest("GET", "/api/products", "sk_abc");\n        handleRequest("GET", "/api/unknown", "sk_abc");\n        handleRequest("GET", "/api/users/1", null);\n\n        // Rate limit test\n        System.out.println("--- Rate limit test (11 запросов) ---");\n        rateLimits.clear();\n        for (int i = 1; i <= 11; i++) {\n            String result = gateway("GET", "/api/users/1", "sk_test");\n            if (i >= 10) {\n                System.out.println("Запрос " + i + ": " + result);\n            }\n        }\n    }\n\n    static void handleRequest(String method, String path, String apiKey) {\n        String result = gateway(method, path, apiKey);\n        String keyInfo = apiKey != null ? "key: " + apiKey : "no key";\n        System.out.println(method + " " + path + " [" + keyInfo + "] -> " + result);\n    }\n\n    static String gateway(String method, String path, String apiKey) {\n        // 1. Аутентификация\n        if (apiKey == null || !validKeys.contains(apiKey)) {\n            return "401 Unauthorized";\n        }\n\n        // 2. Rate Limiting\n        int count = rateLimits.getOrDefault(apiKey, 0);\n        if (count >= RATE_LIMIT) {\n            return "429 Too Many Requests";\n        }\n        rateLimits.put(apiKey, count + 1);\n\n        // 3. Маршрутизация\n        String[] service = null;\n        for (Map.Entry<String, String[]> route : routes.entrySet()) {\n            if (path.startsWith(route.getKey())) {\n                service = route.getValue();\n                break;\n            }\n        }\n\n        if (service == null) {\n            return "404 (маршрут не найден)";\n        }\n\n        // 4. Проксирование\n        int latency = 10 + new Random(path.hashCode()).nextInt(40);\n        String status = method.equals("POST") ? "201 Created" : "200 OK";\n        return service[0] + ":" + service[1] + " (" + status + ", " + latency + "ms)";\n    }\n}',
      explanation: 'API Gateway — единая точка входа для всех клиентов. Он решает cross-cutting concerns: аутентификацию (проверка API Key), rate limiting (защита от злоупотреблений), маршрутизацию (направление запроса к нужному сервису), логирование. В продакшене используют Kong, AWS API Gateway, Spring Cloud Gateway. Каждый микросервис обрабатывает только свою бизнес-логику.'
    }
  ]
}
