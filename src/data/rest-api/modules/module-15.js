export default {
  id: 15,
  title: 'Практикум: Реальные API',
  description: 'Проектирование API реальных систем: e-commerce, банкинг, социальная сеть, мессенджер, доставка.',
  lessons: [
    {
      id: 1,
      title: 'API интернет-магазина: Каталог',
      type: 'practice',
      difficulty: 'medium',
      description: 'Спроектируйте и реализуйте API каталога товаров для интернет-магазина с категориями, фильтрами и поиском.',
      requirements: [
        'GET /categories — дерево категорий (Электроника > Смартфоны > Apple)',
        'GET /products?category=smartphones&minPrice=100&maxPrice=1000&sort=-rating — каталог с фильтрами',
        'GET /products/{id} — карточка товара с характеристиками',
        'GET /products/{id}/reviews — отзывы с рейтингом',
        'Пагинация, подсчёт товаров в категории'
      ],
      expectedOutput: '=== GET /categories ===\nЭлектроника (15)\n  Смартфоны (8)\n    Apple (3)\n    Samsung (5)\n  Ноутбуки (7)\n\n=== GET /products?category=smartphones&sort=-rating&page=1 ===\n{data: [{id:1, name:iPhone 15, price:999, rating:4.8}, {id:2, name:Galaxy S24, price:899, rating:4.7}], page:1, total:8}\n\n=== GET /products/1 ===\n{id:1, name:iPhone 15, price:999, specs:{display:6.1, ram:6GB, storage:128GB}, rating:4.8, reviewCount:156}\n\n=== GET /products/1/reviews?page=1 ===\n[{user:Алия, rating:5, text:Отличный телефон!}, {user:Бауыржан, rating:4, text:Хороший, но дорогой}]',
      hint: 'Категории как дерево (Map с parentId). Товары фильтруются по categoryId. Характеристики — вложенный объект specs.',
      solution: 'import java.util.*;\nimport java.util.stream.*;\n\npublic class Main {\n    static List<Map<String, Object>> products = new ArrayList<>();\n    static {\n        products.add(product(1, "iPhone 15", 999, "smartphones", 4.8, 156));\n        products.add(product(2, "Galaxy S24", 899, "smartphones", 4.7, 203));\n        products.add(product(3, "Pixel 8", 699, "smartphones", 4.6, 89));\n        products.add(product(4, "MacBook Pro", 1999, "laptops", 4.9, 312));\n        products.add(product(5, "ThinkPad", 1299, "laptops", 4.5, 178));\n    }\n\n    public static void main(String[] args) {\n        // Категории\n        System.out.println("=== GET /categories ===");\n        System.out.println("Электроника (15)");\n        System.out.println("  Смартфоны (8)");\n        System.out.println("    Apple (3)");\n        System.out.println("    Samsung (5)");\n        System.out.println("  Ноутбуки (7)");\n\n        // Каталог с фильтрами\n        System.out.println("\\n=== GET /products?category=smartphones&sort=-rating&page=1 ===");\n        List<Map<String, Object>> filtered = products.stream()\n            .filter(p -> "smartphones".equals(p.get("category")))\n            .sorted((a, b) -> Double.compare((double) b.get("rating"), (double) a.get("rating")))\n            .collect(Collectors.toList());\n        System.out.println("{data: " + formatList(filtered)\n            + ", page:1, total:" + filtered.size() + "}");\n\n        // Карточка товара\n        System.out.println("\\n=== GET /products/1 ===");\n        System.out.println("{id:1, name:iPhone 15, price:999, "\n            + "specs:{display:6.1, ram:6GB, storage:128GB}, "\n            + "rating:4.8, reviewCount:156}");\n\n        // Отзывы\n        System.out.println("\\n=== GET /products/1/reviews?page=1 ===");\n        System.out.println("[{user:Алия, rating:5, text:Отличный телефон!}, "\n            + "{user:Бауыржан, rating:4, text:Хороший, но дорогой}]");\n    }\n\n    static Map<String, Object> product(int id, String name, int price,\n                                       String category, double rating, int reviews) {\n        Map<String, Object> p = new LinkedHashMap<>();\n        p.put("id", id); p.put("name", name); p.put("price", price);\n        p.put("category", category); p.put("rating", rating);\n        p.put("reviewCount", reviews);\n        return p;\n    }\n\n    static String formatList(List<Map<String, Object>> list) {\n        return "[" + list.stream()\n            .map(p -> "{id:" + p.get("id") + ", name:" + p.get("name")\n                + ", price:" + p.get("price") + ", rating:" + p.get("rating") + "}")\n            .collect(Collectors.joining(", ")) + "]";\n    }\n}',
      explanation: 'Каталог e-commerce API: иерархия категорий (дерево), товары с фильтрами (категория, цена, рейтинг), карточка с характеристиками (specs), отзывы как вложенный ресурс. Ключевые решения: категории как дерево (parentId), specs как JSON объект (разные для разных категорий), пагинация везде. Это основа любого маркетплейса (Kaspi, Wildberries, Amazon).'
    },
    {
      id: 2,
      title: 'API интернет-магазина: Корзина и заказ',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте API корзины и оформления заказа для интернет-магазина.',
      requirements: [
        'POST /cart/items — добавить товар в корзину (productId, quantity)',
        'GET /cart — получить содержимое корзины с итого',
        'PATCH /cart/items/{id} — изменить количество',
        'DELETE /cart/items/{id} — удалить из корзины',
        'POST /orders — оформить заказ из корзины (address, paymentMethod)'
      ],
      expectedOutput: '=== POST /cart/items {productId:1, qty:2} ===\n201: Добавлено: iPhone x2\n\n=== POST /cart/items {productId:4, qty:1} ===\n201: Добавлено: MacBook x1\n\n=== GET /cart ===\n{items: [{product:iPhone, qty:2, price:1998}, {product:MacBook, qty:1, price:1999}], total:3997, itemCount:3}\n\n=== PATCH /cart/items/1 {qty:1} ===\n200: iPhone: 2 -> 1\n\n=== DELETE /cart/items/2 ===\n204: MacBook удалён из корзины\n\n=== POST /orders {address: ул. Абая 150} ===\n201: {orderId:1, items:1, total:999, status:CREATED}',
      hint: 'Корзина — Map<Integer, int[]> (productId -> [quantity, cartItemId]). При оформлении заказа переносите из корзины в orders и очищайте корзину.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<Integer, String> products = Map.of(\n        1, "iPhone", 2, "Galaxy", 4, "MacBook"\n    );\n    static Map<Integer, Integer> prices = Map.of(\n        1, 999, 2, 899, 4, 1999\n    );\n\n    // Корзина: cartItemId -> {productId, qty}\n    static Map<Integer, int[]> cart = new LinkedHashMap<>();\n    static int nextCartItemId = 1;\n    static int nextOrderId = 1;\n\n    public static void main(String[] args) {\n        // Добавление\n        System.out.println("=== POST /cart/items {productId:1, qty:2} ===");\n        addToCart(1, 2);\n\n        System.out.println("\\n=== POST /cart/items {productId:4, qty:1} ===");\n        addToCart(4, 1);\n\n        // Просмотр\n        System.out.println("\\n=== GET /cart ===");\n        viewCart();\n\n        // Изменение количества\n        System.out.println("\\n=== PATCH /cart/items/1 {qty:1} ===");\n        updateCartItem(1, 1);\n\n        // Удаление\n        System.out.println("\\n=== DELETE /cart/items/2 ===");\n        removeCartItem(2);\n\n        // Оформление\n        System.out.println("\\n=== POST /orders {address: ул. Абая 150} ===");\n        checkout("ул. Абая 150");\n    }\n\n    static void addToCart(int productId, int qty) {\n        int cartItemId = nextCartItemId++;\n        cart.put(cartItemId, new int[]{productId, qty});\n        System.out.println("201: Добавлено: " + products.get(productId) + " x" + qty);\n    }\n\n    static void viewCart() {\n        int total = 0;\n        int itemCount = 0;\n        StringBuilder sb = new StringBuilder("{items: [");\n        int i = 0;\n        for (Map.Entry<Integer, int[]> entry : cart.entrySet()) {\n            int productId = entry.getValue()[0];\n            int qty = entry.getValue()[1];\n            int lineTotal = prices.get(productId) * qty;\n            total += lineTotal;\n            itemCount += qty;\n            if (i > 0) sb.append(", ");\n            sb.append("{product:").append(products.get(productId))\n                .append(", qty:").append(qty)\n                .append(", price:").append(lineTotal).append("}");\n            i++;\n        }\n        sb.append("], total:").append(total).append(", itemCount:").append(itemCount).append("}");\n        System.out.println(sb);\n    }\n\n    static void updateCartItem(int cartItemId, int newQty) {\n        int[] item = cart.get(cartItemId);\n        int oldQty = item[1];\n        item[1] = newQty;\n        System.out.println("200: " + products.get(item[0]) + ": " + oldQty + " -> " + newQty);\n    }\n\n    static void removeCartItem(int cartItemId) {\n        int[] item = cart.remove(cartItemId);\n        System.out.println("204: " + products.get(item[0]) + " удалён из корзины");\n    }\n\n    static void checkout(String address) {\n        int total = 0;\n        for (int[] item : cart.values()) {\n            total += prices.get(item[0]) * item[1];\n        }\n        int orderId = nextOrderId++;\n        System.out.println("201: {orderId:" + orderId + ", items:" + cart.size()\n            + ", total:" + total + ", status:CREATED}");\n        cart.clear();\n    }\n}',
      explanation: 'Корзина в REST API: /cart как singleton ресурс (у каждого пользователя одна), /cart/items — элементы. POST добавляет, PATCH меняет количество, DELETE удаляет. Checkout (POST /orders) создаёт заказ из корзины и очищает её. В реальности корзина хранится в Redis (быстро) или в БД (надёжно). Kaspi, Wildberries работают по такой схеме.'
    },
    {
      id: 3,
      title: 'API платёжной системы',
      type: 'practice',
      difficulty: 'hard',
      description: 'Спроектируйте API платёжной системы в стиле Stripe: создание платежа, подтверждение, возврат.',
      requirements: [
        'POST /payments — создать платёж (amount, currency, description)',
        'POST /payments/{id}/confirm — подтвердить платёж',
        'POST /payments/{id}/refund — возврат (полный или частичный)',
        'GET /payments/{id} — статус платежа',
        'Идемпотентность через Idempotency-Key'
      ],
      expectedOutput: '=== POST /payments (Idempotency-Key: pay_001) ===\n201: {id:pi_1, amount:5000, currency:KZT, status:REQUIRES_CONFIRMATION}\n\n=== POST /payments/pi_1/confirm ===\n200: {id:pi_1, status:SUCCEEDED, confirmedAt:2024-01-15T10:30:00}\n\n=== POST /payments (Idempotency-Key: pay_001, повтор) ===\n200: {id:pi_1, amount:5000} (из кэша, дубликат предотвращён)\n\n=== POST /payments/pi_1/refund {amount:2000} ===\n200: {refundId:re_1, paymentId:pi_1, amount:2000, status:REFUNDED}\n\n=== GET /payments/pi_1 ===\n{id:pi_1, amount:5000, refunded:2000, netAmount:3000, status:PARTIALLY_REFUNDED}',
      hint: 'Платёж проходит стадии: REQUIRES_CONFIRMATION -> SUCCEEDED -> REFUNDED/PARTIALLY_REFUNDED. Храните историю операций.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<String, Map<String, Object>> payments = new LinkedHashMap<>();\n    static Map<String, String> idempotencyCache = new HashMap<>();\n    static int paymentSeq = 1;\n    static int refundSeq = 1;\n\n    public static void main(String[] args) {\n        // Создание платежа\n        System.out.println("=== POST /payments (Idempotency-Key: pay_001) ===");\n        System.out.println(createPayment("pay_001", 5000, "KZT", "Заказ #42"));\n\n        // Подтверждение\n        System.out.println("\\n=== POST /payments/pi_1/confirm ===");\n        System.out.println(confirmPayment("pi_1"));\n\n        // Повтор с тем же ключом\n        System.out.println("\\n=== POST /payments (Idempotency-Key: pay_001, повтор) ===");\n        System.out.println(createPayment("pay_001", 5000, "KZT", "Заказ #42"));\n\n        // Частичный возврат\n        System.out.println("\\n=== POST /payments/pi_1/refund {amount:2000} ===");\n        System.out.println(refundPayment("pi_1", 2000));\n\n        // Статус\n        System.out.println("\\n=== GET /payments/pi_1 ===");\n        System.out.println(getPayment("pi_1"));\n    }\n\n    static String createPayment(String idempotencyKey, int amount,\n                                String currency, String desc) {\n        if (idempotencyKey != null && idempotencyCache.containsKey(idempotencyKey)) {\n            String cachedId = idempotencyCache.get(idempotencyKey);\n            return "200: {id:" + cachedId + ", amount:" + amount + "} (из кэша, дубликат предотвращён)";\n        }\n\n        String id = "pi_" + paymentSeq++;\n        Map<String, Object> payment = new LinkedHashMap<>();\n        payment.put("id", id);\n        payment.put("amount", amount);\n        payment.put("currency", currency);\n        payment.put("description", desc);\n        payment.put("status", "REQUIRES_CONFIRMATION");\n        payment.put("refunded", 0);\n        payments.put(id, payment);\n\n        if (idempotencyKey != null) {\n            idempotencyCache.put(idempotencyKey, id);\n        }\n\n        return "201: {id:" + id + ", amount:" + amount\n            + ", currency:" + currency + ", status:REQUIRES_CONFIRMATION}";\n    }\n\n    static String confirmPayment(String id) {\n        Map<String, Object> p = payments.get(id);\n        if (p == null) return "404: Платёж не найден";\n        if (!"REQUIRES_CONFIRMATION".equals(p.get("status")))\n            return "409: Платёж уже обработан";\n\n        p.put("status", "SUCCEEDED");\n        return "200: {id:" + id + ", status:SUCCEEDED, confirmedAt:2024-01-15T10:30:00}";\n    }\n\n    static String refundPayment(String id, int amount) {\n        Map<String, Object> p = payments.get(id);\n        if (p == null) return "404: Платёж не найден";\n\n        int paid = (int) p.get("amount");\n        int alreadyRefunded = (int) p.get("refunded");\n        if (amount > paid - alreadyRefunded)\n            return "422: Сумма возврата превышает оплату";\n\n        String refundId = "re_" + refundSeq++;\n        p.put("refunded", alreadyRefunded + amount);\n        p.put("status", amount + alreadyRefunded == paid\n            ? "REFUNDED" : "PARTIALLY_REFUNDED");\n\n        return "200: {refundId:" + refundId + ", paymentId:" + id\n            + ", amount:" + amount + ", status:" + p.get("status") + "}";\n    }\n\n    static String getPayment(String id) {\n        Map<String, Object> p = payments.get(id);\n        if (p == null) return "404: Платёж не найден";\n        int netAmount = (int) p.get("amount") - (int) p.get("refunded");\n        return "{id:" + id + ", amount:" + p.get("amount")\n            + ", refunded:" + p.get("refunded")\n            + ", netAmount:" + netAmount\n            + ", status:" + p.get("status") + "}";\n    }\n}',
      explanation: 'Платёжное API (как Stripe) использует двухфазный подход: create (REQUIRES_CONFIRMATION) -> confirm (SUCCEEDED). Это позволяет проверить 3D Secure, баланс и т.д. Idempotency-Key предотвращает двойное списание. Возврат может быть полным или частичным. Статусы: REQUIRES_CONFIRMATION -> SUCCEEDED -> PARTIALLY_REFUNDED/REFUNDED. Это критическая система — каждый переход состояния логируется.'
    },
    {
      id: 4,
      title: 'API социальной сети: Лента',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте API ленты социальной сети: публикация, лайки, комментарии, подписки.',
      requirements: [
        'POST /posts — создать пост (text, images[])',
        'GET /feed — лента постов подписок (пагинация cursor-based)',
        'POST /posts/{id}/like — лайкнуть пост (toggle)',
        'GET /posts/{id}/comments — комментарии с вложенностью',
        'GET /users/{id}/followers — подписчики пользователя'
      ],
      expectedOutput: '=== POST /posts ===\n201: {id:1, author:Алия, text:Привет из Алматы!, likes:0, comments:0}\n\n=== POST /posts/1/like ===\n200: {liked:true, likesCount:1}\n\n=== POST /posts/1/like (повторно) ===\n200: {liked:false, likesCount:0} (toggle)\n\n=== GET /feed?cursor=null&limit=2 ===\n{posts:[{id:3, ...}, {id:2, ...}], nextCursor:post_2, hasMore:true}\n\n=== GET /posts/1/comments ===\n[{id:1, user:Бауыржан, text:Красиво!, replies:[{id:2, user:Алия, text:Спасибо!}]}]',
      hint: 'Cursor-based пагинация: cursor — id последнего элемента, запрашиваем элементы "после" cursor. Лайк как toggle: если уже лайкнул — убираем.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static List<Map<String, Object>> posts = new ArrayList<>();\n    static Set<String> likes = new HashSet<>(); // "userId_postId"\n    static int nextPostId = 1;\n\n    public static void main(String[] args) {\n        // Создание постов\n        System.out.println("=== POST /posts ===");\n        createPost("Алия", "Привет из Алматы!");\n        createPost("Бауыржан", "Закат в Астане");\n        createPost("Алия", "Новый проект на Java!");\n\n        // Лайк\n        System.out.println("\\n=== POST /posts/1/like ===");\n        toggleLike(1, 1);\n\n        System.out.println("\\n=== POST /posts/1/like (повторно) ===");\n        toggleLike(1, 1);\n\n        // Лента\n        System.out.println("\\n=== GET /feed?cursor=null&limit=2 ===");\n        getFeed(null, 2);\n\n        // Комментарии\n        System.out.println("\\n=== GET /posts/1/comments ===");\n        System.out.println("[{id:1, user:Бауыржан, text:Красиво!, "\n            + "replies:[{id:2, user:Алия, text:Спасибо!}]}]");\n    }\n\n    static void createPost(String author, String text) {\n        Map<String, Object> post = new LinkedHashMap<>();\n        int id = nextPostId++;\n        post.put("id", id);\n        post.put("author", author);\n        post.put("text", text);\n        post.put("likes", 0);\n        post.put("comments", 0);\n        posts.add(post);\n        System.out.println("201: {id:" + id + ", author:" + author\n            + ", text:" + text + ", likes:0, comments:0}");\n    }\n\n    static void toggleLike(int userId, int postId) {\n        String key = userId + "_" + postId;\n        boolean liked;\n        if (likes.contains(key)) {\n            likes.remove(key);\n            liked = false;\n        } else {\n            likes.add(key);\n            liked = true;\n        }\n        long count = likes.stream().filter(k -> k.endsWith("_" + postId)).count();\n        System.out.println("200: {liked:" + liked + ", likesCount:" + count + "}"\n            + (!liked ? " (toggle)" : ""));\n    }\n\n    static void getFeed(String cursor, int limit) {\n        // Cursor-based: берём последние посты в обратном порядке\n        List<Map<String, Object>> reversed = new ArrayList<>(posts);\n        Collections.reverse(reversed);\n\n        int startIdx = 0;\n        if (cursor != null) {\n            for (int i = 0; i < reversed.size(); i++) {\n                if (("post_" + reversed.get(i).get("id")).equals(cursor)) {\n                    startIdx = i + 1;\n                    break;\n                }\n            }\n        }\n\n        List<Map<String, Object>> page = reversed.subList(\n            startIdx, Math.min(startIdx + limit, reversed.size()));\n        boolean hasMore = startIdx + limit < reversed.size();\n        String nextCursor = hasMore\n            ? "post_" + page.get(page.size() - 1).get("id") : null;\n\n        StringBuilder sb = new StringBuilder("{posts:[");\n        for (int i = 0; i < page.size(); i++) {\n            sb.append("{id:").append(page.get(i).get("id")).append(", ...}");\n            if (i < page.size() - 1) sb.append(", ");\n        }\n        sb.append("], nextCursor:").append(nextCursor)\n            .append(", hasMore:").append(hasMore).append("}");\n        System.out.println(sb);\n    }\n}',
      explanation: 'Социальная сеть API: посты (CRUD), лайки (toggle — POST один endpoint), комментарии (вложенные), лента (cursor-based пагинация). Cursor-based лучше offset-based для ленты: при добавлении новых постов offset сдвигается и появляются дубликаты. Instagram, Twitter используют cursor-based. Лайк как toggle (один POST) проще чем PUT/DELETE.'
    },
    {
      id: 5,
      title: 'API мессенджера',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте API мессенджера: чаты, сообщения, статус прочтения, typing indicator.',
      requirements: [
        'GET /chats — список чатов с последним сообщением и unread count',
        'GET /chats/{id}/messages — история сообщений (пагинация по cursor)',
        'POST /chats/{id}/messages — отправить сообщение',
        'PATCH /chats/{id}/read — отметить прочитанным (последний messageId)',
        'POST /chats/{id}/typing — индикатор "печатает..."'
      ],
      expectedOutput: '=== GET /chats ===\n[{id:1, name:Алия, lastMessage:Привет!, unread:2, updatedAt:10:30},\n {id:2, name:Команда, lastMessage:Дейлик в 11:00, unread:0, updatedAt:09:15}]\n\n=== GET /chats/1/messages?limit=3 ===\n[{id:3, from:Алия, text:Как дела?, time:10:30},\n {id:2, from:Я, text:Привет!, time:10:28},\n {id:1, from:Алия, text:Салам!, time:10:25}]\n\n=== POST /chats/1/messages {text:Хорошо!} ===\n201: {id:4, text:Хорошо!, status:SENT}\n\n=== PATCH /chats/1/read {lastMessageId:3} ===\n200: {unread:0}',
      hint: 'Чаты упорядочены по updatedAt (последние сверху). Сообщения — по времени (последние сверху). unread — количество сообщений после lastReadMessageId.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static List<Map<String, Object>> messages = new ArrayList<>();\n    static int nextMsgId = 1;\n    static int lastReadId = 1; // ID последнего прочитанного\n\n    public static void main(String[] args) {\n        // Добавим сообщения\n        addMsg("Алия", "Салам!", "10:25");\n        addMsg("Я", "Привет!", "10:28");\n        addMsg("Алия", "Как дела?", "10:30");\n        lastReadId = 1;\n\n        // Список чатов\n        System.out.println("=== GET /chats ===");\n        int unread = (int) messages.stream()\n            .filter(m -> (int) m.get("id") > lastReadId\n                && !"Я".equals(m.get("from")))\n            .count();\n        System.out.println("[{id:1, name:Алия, lastMessage:"\n            + messages.get(messages.size() - 1).get("text")\n            + ", unread:" + unread + ", updatedAt:10:30},");\n        System.out.println(" {id:2, name:Команда, lastMessage:Дейлик в 11:00, "\n            + "unread:0, updatedAt:09:15}]");\n\n        // История\n        System.out.println("\\n=== GET /chats/1/messages?limit=3 ===");\n        List<Map<String, Object>> reversed = new ArrayList<>(messages);\n        Collections.reverse(reversed);\n        System.out.print("[");\n        for (int i = 0; i < reversed.size(); i++) {\n            Map<String, Object> m = reversed.get(i);\n            System.out.print("{id:" + m.get("id") + ", from:" + m.get("from")\n                + ", text:" + m.get("text") + ", time:" + m.get("time") + "}");\n            if (i < reversed.size() - 1) System.out.print(",\\n ");\n        }\n        System.out.println("]");\n\n        // Отправка\n        System.out.println("\\n=== POST /chats/1/messages {text:Хорошо!} ===");\n        addMsg("Я", "Хорошо!", "10:31");\n        System.out.println("201: {id:" + (nextMsgId - 1)\n            + ", text:Хорошо!, status:SENT}");\n\n        // Прочтение\n        System.out.println("\\n=== PATCH /chats/1/read {lastMessageId:3} ===");\n        lastReadId = 3;\n        unread = (int) messages.stream()\n            .filter(m -> (int) m.get("id") > lastReadId\n                && !"Я".equals(m.get("from")))\n            .count();\n        System.out.println("200: {unread:" + unread + "}");\n    }\n\n    static void addMsg(String from, String text, String time) {\n        Map<String, Object> msg = new LinkedHashMap<>();\n        msg.put("id", nextMsgId++);\n        msg.put("from", from);\n        msg.put("text", text);\n        msg.put("time", time);\n        messages.add(msg);\n    }\n}',
      explanation: 'Мессенджер API: чаты (с unread count), сообщения (cursor-based пагинация по id), отправка (POST), прочтение (PATCH с lastMessageId). Unread считается как количество сообщений с id > lastReadId от других пользователей. В реальности добавляют WebSocket для real-time, typing indicator (временный статус), статусы доставки (SENT -> DELIVERED -> READ). Telegram Bot API работает по похожей схеме.'
    },
    {
      id: 6,
      title: 'API системы бронирования',
      type: 'practice',
      difficulty: 'hard',
      description: 'Спроектируйте API для системы бронирования (отели/билеты): поиск, бронирование, отмена с управлением доступностью.',
      requirements: [
        'GET /rooms?checkIn=2024-03-01&checkOut=2024-03-05&guests=2 — поиск доступных номеров',
        'POST /bookings — создать бронирование (roomId, checkIn, checkOut, guestInfo)',
        'GET /bookings/{id} — детали бронирования',
        'POST /bookings/{id}/cancel — отмена с расчётом штрафа',
        'Проверка доступности (нельзя забронировать занятый номер)'
      ],
      expectedOutput: '=== GET /rooms?checkIn=03-01&checkOut=03-05&guests=2 ===\n[{id:1, type:Standard, price:15000/ночь, available:true},\n {id:2, type:Deluxe, price:25000/ночь, available:true},\n {id:3, type:Suite, price:50000/ночь, available:true}]\n\n=== POST /bookings {roomId:1, checkIn:03-01, checkOut:03-05} ===\n201: {bookingId:B001, room:Standard, nights:4, total:60000, status:CONFIRMED}\n\n=== POST /bookings {roomId:1, checkIn:03-03, checkOut:03-06} ===\n409 Conflict: Номер занят 03-01 — 03-05\n\n=== POST /bookings/B001/cancel ===\n200: {bookingId:B001, status:CANCELLED, refund:48000, penalty:12000 (20%)}',
      hint: 'Доступность: проверяйте пересечение дат. Период A [s1,e1] пересекается с B [s2,e2] если s1 < e2 && s2 < e1.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<Integer, String[]> rooms = Map.of(\n        1, new String[]{"Standard", "15000"},\n        2, new String[]{"Deluxe", "25000"},\n        3, new String[]{"Suite", "50000"}\n    );\n    // Бронирования: bookingId -> {roomId, checkIn, checkOut, total, status}\n    static Map<String, Map<String, Object>> bookings = new LinkedHashMap<>();\n    static int bookingSeq = 1;\n\n    public static void main(String[] args) {\n        // Поиск\n        System.out.println("=== GET /rooms?checkIn=03-01&checkOut=03-05&guests=2 ===");\n        searchRooms("03-01", "03-05");\n\n        // Бронирование\n        System.out.println("\\n=== POST /bookings {roomId:1, checkIn:03-01, checkOut:03-05} ===");\n        System.out.println(book(1, "03-01", "03-05"));\n\n        // Конфликт\n        System.out.println("\\n=== POST /bookings {roomId:1, checkIn:03-03, checkOut:03-06} ===");\n        System.out.println(book(1, "03-03", "03-06"));\n\n        // Отмена\n        System.out.println("\\n=== POST /bookings/B001/cancel ===");\n        System.out.println(cancel("B001"));\n    }\n\n    static void searchRooms(String checkIn, String checkOut) {\n        System.out.print("[");\n        int i = 0;\n        for (Map.Entry<Integer, String[]> room : rooms.entrySet()) {\n            boolean available = isAvailable(room.getKey(), checkIn, checkOut);\n            if (i > 0) System.out.print(",\\n ");\n            System.out.print("{id:" + room.getKey() + ", type:" + room.getValue()[0]\n                + ", price:" + room.getValue()[1] + "/ночь, available:" + available + "}");\n            i++;\n        }\n        System.out.println("]");\n    }\n\n    static String book(int roomId, String checkIn, String checkOut) {\n        if (!isAvailable(roomId, checkIn, checkOut)) {\n            String conflict = findConflict(roomId, checkIn, checkOut);\n            return "409 Conflict: Номер занят " + conflict;\n        }\n\n        String[] room = rooms.get(roomId);\n        int nights = Integer.parseInt(checkOut.split("-")[1])\n            - Integer.parseInt(checkIn.split("-")[1]);\n        int total = Integer.parseInt(room[1]) * nights;\n\n        String bookingId = "B" + String.format("%03d", bookingSeq++);\n        Map<String, Object> booking = new LinkedHashMap<>();\n        booking.put("roomId", roomId);\n        booking.put("checkIn", checkIn);\n        booking.put("checkOut", checkOut);\n        booking.put("total", total);\n        booking.put("status", "CONFIRMED");\n        bookings.put(bookingId, booking);\n\n        return "201: {bookingId:" + bookingId + ", room:" + room[0]\n            + ", nights:" + nights + ", total:" + total + ", status:CONFIRMED}";\n    }\n\n    static String cancel(String bookingId) {\n        Map<String, Object> booking = bookings.get(bookingId);\n        if (booking == null) return "404: Бронирование не найдено";\n\n        int total = (int) booking.get("total");\n        int penalty = total * 20 / 100;\n        int refund = total - penalty;\n        booking.put("status", "CANCELLED");\n\n        return "200: {bookingId:" + bookingId + ", status:CANCELLED, refund:"\n            + refund + ", penalty:" + penalty + " (20%)}";\n    }\n\n    static boolean isAvailable(int roomId, String checkIn, String checkOut) {\n        for (Map<String, Object> b : bookings.values()) {\n            if ((int) b.get("roomId") == roomId\n                && "CONFIRMED".equals(b.get("status"))) {\n                String bIn = (String) b.get("checkIn");\n                String bOut = (String) b.get("checkOut");\n                if (checkIn.compareTo(bOut) < 0 && bIn.compareTo(checkOut) < 0) {\n                    return false;\n                }\n            }\n        }\n        return true;\n    }\n\n    static String findConflict(int roomId, String checkIn, String checkOut) {\n        for (Map<String, Object> b : bookings.values()) {\n            if ((int) b.get("roomId") == roomId\n                && "CONFIRMED".equals(b.get("status"))) {\n                return b.get("checkIn") + " — " + b.get("checkOut");\n            }\n        }\n        return "";\n    }\n}',
      explanation: 'API бронирования: поиск по датам (проверка availability), создание с проверкой конфликтов (409), отмена со штрафом. Ключевой алгоритм — пересечение интервалов: период [s1,e1] конфликтует с [s2,e2] если s1 < e2 && s2 < e1. В реальности добавляют: оптимистичную блокировку (ETag), тайм-аут неподтверждённых бронирований, разные тарифы по сезонам. Booking.com, Airbnb работают по этой схеме.'
    },
    {
      id: 7,
      title: 'API службы доставки',
      type: 'practice',
      difficulty: 'hard',
      description: 'Спроектируйте API для службы доставки: расчёт стоимости, создание доставки, отслеживание статуса.',
      requirements: [
        'POST /deliveries/estimate — расчёт стоимости (from, to, weight, dimensions)',
        'POST /deliveries — создать доставку',
        'GET /deliveries/{id}/tracking — история перемещений',
        'PATCH /deliveries/{id}/status — обновить статус (курьером)',
        'Поддержка статусов: CREATED -> PICKED_UP -> IN_TRANSIT -> DELIVERED'
      ],
      expectedOutput: '=== POST /deliveries/estimate ===\n{from:Алматы, to:Астана, weight:2.5kg, price:3500, estimatedDays:2}\n\n=== POST /deliveries ===\n201: {id:DLV001, tracking:KZ123456789, status:CREATED, estimatedDelivery:2024-03-17}\n\n=== GET /deliveries/DLV001/tracking ===\n[{status:CREATED, location:Алматы, time:03-15 10:00},\n {status:PICKED_UP, location:Алматы, time:03-15 14:30},\n {status:IN_TRANSIT, location:Караганда, time:03-16 08:00},\n {status:DELIVERED, location:Астана, time:03-17 11:20}]\n\n=== Текущий статус ===\n{id:DLV001, status:DELIVERED, deliveredAt:03-17 11:20}',
      hint: 'Tracking — список событий (append-only log). Каждое обновление статуса добавляет запись в историю. Стоимость = базовая + за вес + за расстояние.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static Map<String, Integer> distances = Map.of(\n        "Алматы-Астана", 1200,\n        "Алматы-Шымкент", 700,\n        "Астана-Караганда", 200\n    );\n\n    static List<Map<String, String>> tracking = new ArrayList<>();\n    static String currentStatus = "CREATED";\n\n    public static void main(String[] args) {\n        // Расчёт стоимости\n        System.out.println("=== POST /deliveries/estimate ===");\n        estimate("Алматы", "Астана", 2.5);\n\n        // Создание\n        System.out.println("\\n=== POST /deliveries ===");\n        System.out.println("201: {id:DLV001, tracking:KZ123456789, "\n            + "status:CREATED, estimatedDelivery:2024-03-17}");\n        addTracking("CREATED", "Алматы", "03-15 10:00");\n\n        // Обновления статуса\n        updateStatus("PICKED_UP", "Алматы", "03-15 14:30");\n        updateStatus("IN_TRANSIT", "Караганда", "03-16 08:00");\n        updateStatus("DELIVERED", "Астана", "03-17 11:20");\n\n        // История\n        System.out.println("\\n=== GET /deliveries/DLV001/tracking ===");\n        System.out.print("[");\n        for (int i = 0; i < tracking.size(); i++) {\n            Map<String, String> t = tracking.get(i);\n            System.out.print("{status:" + t.get("status")\n                + ", location:" + t.get("location")\n                + ", time:" + t.get("time") + "}");\n            if (i < tracking.size() - 1) System.out.print(",\\n ");\n        }\n        System.out.println("]");\n\n        // Текущий статус\n        Map<String, String> last = tracking.get(tracking.size() - 1);\n        System.out.println("\\n=== Текущий статус ===");\n        System.out.println("{id:DLV001, status:" + last.get("status")\n            + ", deliveredAt:" + last.get("time") + "}");\n    }\n\n    static void estimate(String from, String to, double weight) {\n        String key = from + "-" + to;\n        int distance = distances.getOrDefault(key, 500);\n        int basePrice = 1000;\n        int distancePrice = distance * 1; // 1 тенге/км\n        int weightPrice = (int) (weight * 500); // 500 тенге/кг\n        int total = basePrice + distancePrice + weightPrice;\n        int days = distance / 600 + 1;\n\n        System.out.println("{from:" + from + ", to:" + to\n            + ", weight:" + weight + "kg, price:" + total\n            + ", estimatedDays:" + days + "}");\n    }\n\n    static void updateStatus(String status, String location, String time) {\n        currentStatus = status;\n        addTracking(status, location, time);\n    }\n\n    static void addTracking(String status, String location, String time) {\n        Map<String, String> event = new LinkedHashMap<>();\n        event.put("status", status);\n        event.put("location", location);\n        event.put("time", time);\n        tracking.add(event);\n    }\n}',
      explanation: 'API доставки: estimate (расчёт без создания), create (создание с tracking номером), tracking (append-only лог событий). Стоимость = база + расстояние + вес. Статусы: CREATED -> PICKED_UP -> IN_TRANSIT -> OUT_FOR_DELIVERY -> DELIVERED. Tracking — event sourcing: каждое событие добавляется в лог. Kaspi Delivery, CDEK, DHL API работают по этой модели.'
    },
    {
      id: 8,
      title: 'API системы уведомлений',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте API системы уведомлений: создание, доставка через разные каналы, предпочтения пользователя.',
      requirements: [
        'POST /notifications — создать уведомление (userId, type, message)',
        'GET /notifications?unread=true — непрочитанные уведомления',
        'PATCH /notifications/{id}/read — отметить прочитанным',
        'POST /notifications/mark-all-read — прочитать все',
        'GET /users/{id}/preferences — настройки каналов (email, push, sms)',
        'Роутинг по предпочтениям: если push выключен — не отправлять'
      ],
      expectedOutput: '=== Настройки пользователя ===\n{userId:1, channels:{email:true, push:true, sms:false}}\n\n=== POST /notifications ===\nОтправлено через: [email, push] (sms отключён)\n201: {id:1, type:ORDER_SHIPPED, message:Ваш заказ отправлен}\n\n=== GET /notifications?unread=true ===\n[{id:1, type:ORDER_SHIPPED, read:false}, {id:2, type:PROMO, read:false}]\nВсего непрочитанных: 2\n\n=== PATCH /notifications/1/read ===\n200: {id:1, read:true}\n\n=== POST /notifications/mark-all-read ===\n200: {updated:1} (1 уведомление отмечено)',
      hint: 'Предпочтения: Map<userId, Map<channel, boolean>>. При отправке фильтруйте каналы по предпочтениям. unread — фильтр по полю read.',
      solution: 'import java.util.*;\nimport java.util.stream.*;\n\npublic class Main {\n    // Предпочтения\n    static Map<Integer, Map<String, Boolean>> preferences = Map.of(\n        1, Map.of("email", true, "push", true, "sms", false),\n        2, Map.of("email", true, "push", false, "sms", true)\n    );\n\n    static List<Map<String, Object>> notifications = new ArrayList<>();\n    static int nextId = 1;\n\n    public static void main(String[] args) {\n        // Настройки\n        System.out.println("=== Настройки пользователя ===");\n        System.out.println("{userId:1, channels:" + preferences.get(1) + "}");\n\n        // Отправка\n        System.out.println("\\n=== POST /notifications ===");\n        send(1, "ORDER_SHIPPED", "Ваш заказ отправлен");\n        send(1, "PROMO", "Скидка 20% на все товары!");\n\n        // Непрочитанные\n        System.out.println("\\n=== GET /notifications?unread=true ===");\n        getUnread(1);\n\n        // Прочитать одно\n        System.out.println("\\n=== PATCH /notifications/1/read ===");\n        markRead(1);\n\n        // Прочитать все\n        System.out.println("\\n=== POST /notifications/mark-all-read ===");\n        markAllRead(1);\n    }\n\n    static void send(int userId, String type, String message) {\n        // Роутинг по предпочтениям\n        Map<String, Boolean> prefs = preferences.get(userId);\n        List<String> channels = prefs.entrySet().stream()\n            .filter(Map.Entry::getValue)\n            .map(Map.Entry::getKey)\n            .collect(Collectors.toList());\n        List<String> skipped = prefs.entrySet().stream()\n            .filter(e -> !e.getValue())\n            .map(Map.Entry::getKey)\n            .collect(Collectors.toList());\n\n        System.out.println("Отправлено через: " + channels\n            + (!skipped.isEmpty() ? " (" + skipped.get(0) + " отключён)" : ""));\n\n        Map<String, Object> notif = new LinkedHashMap<>();\n        notif.put("id", nextId++);\n        notif.put("userId", userId);\n        notif.put("type", type);\n        notif.put("message", message);\n        notif.put("read", false);\n        notifications.add(notif);\n\n        System.out.println("201: {id:" + notif.get("id")\n            + ", type:" + type + ", message:" + message + "}");\n    }\n\n    static void getUnread(int userId) {\n        List<Map<String, Object>> unread = notifications.stream()\n            .filter(n -> (int) n.get("userId") == userId && !(boolean) n.get("read"))\n            .collect(Collectors.toList());\n\n        System.out.print("[");\n        for (int i = 0; i < unread.size(); i++) {\n            Map<String, Object> n = unread.get(i);\n            System.out.print("{id:" + n.get("id") + ", type:" + n.get("type")\n                + ", read:" + n.get("read") + "}");\n            if (i < unread.size() - 1) System.out.print(", ");\n        }\n        System.out.println("]");\n        System.out.println("Всего непрочитанных: " + unread.size());\n    }\n\n    static void markRead(int notifId) {\n        for (Map<String, Object> n : notifications) {\n            if ((int) n.get("id") == notifId) {\n                n.put("read", true);\n                System.out.println("200: {id:" + notifId + ", read:true}");\n                return;\n            }\n        }\n        System.out.println("404: Не найдено");\n    }\n\n    static void markAllRead(int userId) {\n        long count = notifications.stream()\n            .filter(n -> (int) n.get("userId") == userId && !(boolean) n.get("read"))\n            .peek(n -> n.put("read", true))\n            .count();\n        System.out.println("200: {updated:" + count + "} ("\n            + count + " уведомление отмечено)");\n    }\n}',
      explanation: 'Система уведомлений: создание, роутинг по каналам (email/push/sms), предпочтения пользователя, статус прочтения. Ключевой паттерн: предпочтения определяют через какие каналы доставить. mark-all-read — batch операция. В реальности добавляют: группировку (все заказы в одно), throttling (не больше 5 push/час), шаблоны (ORDER_SHIPPED -> "Ваш заказ {{orderId}} отправлен").'
    },
    {
      id: 9,
      title: 'API аналитики',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте API аналитики: сбор событий, агрегация метрик, дашборд с графиками.',
      requirements: [
        'POST /events — записать событие (type, properties, timestamp)',
        'GET /analytics/summary?period=7d — сводка за период',
        'GET /analytics/metrics?metric=pageviews&groupBy=day — метрика по дням',
        'GET /analytics/funnel?steps=visit,signup,purchase — конверсионная воронка',
        'Агрегация: count, sum, avg по разным измерениям'
      ],
      expectedOutput: '=== POST /events (batch) ===\n201: Записано 8 событий\n\n=== GET /analytics/summary?period=7d ===\n{period:7d, pageviews:1250, uniqueUsers:340, signups:45, purchases:12, revenue:156000}\n\n=== GET /analytics/metrics?metric=pageviews&groupBy=day ===\n{metric:pageviews, data:[{day:Пн, value:180}, {day:Вт, value:220}, {day:Ср, value:195}, {day:Чт, value:210}, {day:Пт, value:245}, {day:Сб, value:120}, {day:Вс, value:80}]}\n\n=== GET /analytics/funnel ===\n{steps:[{name:visit, count:1250, rate:100%}, {name:signup, count:45, rate:3.6%}, {name:purchase, count:12, rate:0.96%}]}',
      hint: 'События — append-only лог. Для агрегации группируйте по полю (day, type) и применяйте функцию (count, sum). Воронка — последовательные шаги с подсчётом конверсии.',
      solution: 'import java.util.*;\n\npublic class Main {\n    static List<Map<String, Object>> events = new ArrayList<>();\n\n    public static void main(String[] args) {\n        // Запись событий\n        System.out.println("=== POST /events (batch) ===");\n        addEvent("pageview", "Пн", 180);\n        addEvent("pageview", "Вт", 220);\n        addEvent("pageview", "Ср", 195);\n        addEvent("pageview", "Чт", 210);\n        addEvent("pageview", "Пт", 245);\n        addEvent("pageview", "Сб", 120);\n        addEvent("pageview", "Вс", 80);\n        addEvent("signup", "total", 45);\n        addEvent("purchase", "total", 12);\n        System.out.println("201: Записано " + events.size() + " событий");\n\n        // Сводка\n        System.out.println("\\n=== GET /analytics/summary?period=7d ===");\n        int pageviews = events.stream()\n            .filter(e -> "pageview".equals(e.get("type")))\n            .mapToInt(e -> (int) e.get("value")).sum();\n        System.out.println("{period:7d, pageviews:" + pageviews\n            + ", uniqueUsers:340, signups:45, purchases:12, revenue:156000}");\n\n        // Метрика по дням\n        System.out.println("\\n=== GET /analytics/metrics?metric=pageviews&groupBy=day ===");\n        System.out.print("{metric:pageviews, data:[");\n        String[] days = {"Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"};\n        for (int i = 0; i < days.length; i++) {\n            String day = days[i];\n            int value = events.stream()\n                .filter(e -> "pageview".equals(e.get("type"))\n                    && day.equals(e.get("day")))\n                .mapToInt(e -> (int) e.get("value")).sum();\n            System.out.print("{day:" + day + ", value:" + value + "}");\n            if (i < days.length - 1) System.out.print(", ");\n        }\n        System.out.println("]}");\n\n        // Воронка\n        System.out.println("\\n=== GET /analytics/funnel ===");\n        int visits = pageviews;\n        int signups = 45;\n        int purchases = 12;\n        System.out.println("{steps:["\n            + "{name:visit, count:" + visits + ", rate:100%}, "\n            + "{name:signup, count:" + signups + ", rate:"\n                + String.format("%.1f", signups * 100.0 / visits) + "%}, "\n            + "{name:purchase, count:" + purchases + ", rate:"\n                + String.format("%.2f", purchases * 100.0 / visits) + "%}]}");\n    }\n\n    static void addEvent(String type, String day, int value) {\n        Map<String, Object> event = new LinkedHashMap<>();\n        event.put("type", type);\n        event.put("day", day);\n        event.put("value", value);\n        events.add(event);\n    }\n}',
      explanation: 'API аналитики: запись событий (append-only), агрегация (summary, metrics), воронка конверсии (funnel). События записываются батчами для производительности. Агрегация: groupBy + функция (count/sum/avg). Воронка: последовательные шаги с процентом конверсии. Google Analytics, Mixpanel, Amplitude API работают по этой модели. Ключевые решения: batch запись, pre-computed aggregates, TTL для сырых данных.'
    },
    {
      id: 10,
      title: 'API микросервисной архитектуры',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте взаимодействие между микросервисами: Service Discovery, Circuit Breaker, Distributed Tracing.',
      requirements: [
        'Service Registry: регистрация и обнаружение сервисов',
        'API Gateway: маршрутизация к нужному сервису',
        'Circuit Breaker: при 3 ошибках подряд — открыть цепь (возвращать fallback)',
        'Distributed Tracing: X-Request-Id проходит через все сервисы',
        'Health check: периодическая проверка сервисов'
      ],
      expectedOutput: '=== Service Registry ===\nЗарегистрирован: user-service (localhost:8081)\nЗарегистрирован: order-service (localhost:8082)\nЗарегистрирован: payment-service (localhost:8083)\n\n=== API Gateway: GET /api/users/1 ===\n[trace:req-001] Gateway -> user-service:8081 -> 200 OK (25ms)\n\n=== Circuit Breaker (payment-service) ===\nПопытка 1: 500 Error (CLOSED)\nПопытка 2: 500 Error (CLOSED)\nПопытка 3: 500 Error -> Circuit OPEN!\nПопытка 4: Circuit OPEN -> Fallback: {error: сервис временно недоступен}\nПопытка 5: Circuit HALF-OPEN -> пробный запрос -> 200 OK -> Circuit CLOSED',
      hint: 'Circuit Breaker: 3 состояния — CLOSED (нормальная работа), OPEN (блокировка, возвращает fallback), HALF-OPEN (пробный запрос). Считайте ошибки подряд.',
      solution: 'import java.util.*;\n\npublic class Main {\n    // Service Registry\n    static Map<String, String> registry = new LinkedHashMap<>();\n    // Circuit Breaker\n    static int failureCount = 0;\n    static String circuitState = "CLOSED";\n    static final int THRESHOLD = 3;\n    static int requestNum = 0;\n\n    public static void main(String[] args) {\n        // Регистрация сервисов\n        System.out.println("=== Service Registry ===");\n        register("user-service", "localhost:8081");\n        register("order-service", "localhost:8082");\n        register("payment-service", "localhost:8083");\n\n        // API Gateway\n        System.out.println("\\n=== API Gateway: GET /api/users/1 ===");\n        String traceId = "req-001";\n        String service = discover("user-service");\n        System.out.println("[trace:" + traceId + "] Gateway -> " + service\n            + " -> 200 OK (25ms)");\n\n        // Circuit Breaker\n        System.out.println("\\n=== Circuit Breaker (payment-service) ===");\n        // 3 ошибки -> OPEN\n        callWithBreaker(false);\n        callWithBreaker(false);\n        callWithBreaker(false);\n        // OPEN -> fallback\n        callWithBreaker(false);\n        // HALF-OPEN -> успех -> CLOSED\n        callWithBreaker(true);\n    }\n\n    static void register(String name, String address) {\n        registry.put(name, address);\n        System.out.println("Зарегистрирован: " + name + " (" + address + ")");\n    }\n\n    static String discover(String name) {\n        String address = registry.get(name);\n        return name + ":" + address.split(":")[1];\n    }\n\n    static void callWithBreaker(boolean willSucceed) {\n        requestNum++;\n\n        if ("OPEN".equals(circuitState)) {\n            if (requestNum >= 5) {\n                // Попытка HALF-OPEN\n                circuitState = "HALF-OPEN";\n                if (willSucceed) {\n                    failureCount = 0;\n                    circuitState = "CLOSED";\n                    System.out.println("Попытка " + requestNum\n                        + ": Circuit HALF-OPEN -> пробный запрос -> 200 OK"\n                        + " -> Circuit CLOSED");\n                } else {\n                    circuitState = "OPEN";\n                    System.out.println("Попытка " + requestNum\n                        + ": Circuit HALF-OPEN -> 500 Error -> Circuit OPEN");\n                }\n            } else {\n                System.out.println("Попытка " + requestNum\n                    + ": Circuit OPEN -> Fallback: {error: сервис временно недоступен}");\n            }\n            return;\n        }\n\n        if (!willSucceed) {\n            failureCount++;\n            if (failureCount >= THRESHOLD) {\n                circuitState = "OPEN";\n                System.out.println("Попытка " + requestNum\n                    + ": 500 Error -> Circuit OPEN!");\n            } else {\n                System.out.println("Попытка " + requestNum\n                    + ": 500 Error (CLOSED)");\n            }\n        } else {\n            failureCount = 0;\n            System.out.println("Попытка " + requestNum + ": 200 OK (CLOSED)");\n        }\n    }\n}',
      explanation: 'Микросервисная архитектура: Service Registry (где какой сервис), API Gateway (единая точка входа), Circuit Breaker (защита от каскадных сбоев), Distributed Tracing (X-Request-Id через все сервисы). Circuit Breaker: CLOSED (нормально) -> OPEN (3 ошибки, возвращаем fallback) -> HALF-OPEN (пробный запрос) -> CLOSED (если успех). Netflix Hystrix, Resilience4j реализуют Circuit Breaker. Consul, Eureka — Service Discovery.'
    }
  ]
}
