export default {
  id: 6,
  title: 'Пагинация и фильтрация',
  description: 'Offset и cursor пагинация, сортировка, поиск, фильтрация — как обрабатывать большие наборы данных в REST API.',
  lessons: [
    {
      id: 1,
      title: 'Offset пагинация',
      type: 'theory',
      content: [
        { type: 'text', value: 'Offset пагинация — самый простой способ разбить данные на страницы. Клиент указывает номер страницы (page) и размер (size). Сервер пропускает (offset) первые записи и возвращает нужную порцию.' },
        { type: 'heading', value: 'Параметры запроса' },
        { type: 'code', language: 'java', value: 'import java.util.*;\nimport java.util.stream.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        // Генерируем 50 пользователей\n        List<String> allUsers = new ArrayList<>();\n        for (int i = 1; i <= 50; i++) {\n            allUsers.add("User_" + i);\n        }\n\n        // GET /api/users?page=1&size=10\n        System.out.println("=== Страница 1 ===");\n        printPage(allUsers, 1, 10);\n\n        // GET /api/users?page=3&size=10\n        System.out.println("\\n=== Страница 3 ===");\n        printPage(allUsers, 3, 10);\n\n        // GET /api/users?page=5&size=10 (последняя)\n        System.out.println("\\n=== Страница 5 (последняя) ===");\n        printPage(allUsers, 5, 10);\n\n        // GET /api/users?page=6&size=10 (за пределами)\n        System.out.println("\\n=== Страница 6 (пустая) ===");\n        printPage(allUsers, 6, 10);\n    }\n\n    static void printPage(List<String> data, int page, int size) {\n        int totalElements = data.size();\n        int totalPages = (int) Math.ceil((double) totalElements / size);\n        int offset = (page - 1) * size;\n\n        List<String> pageData = data.stream()\n            .skip(offset)\n            .limit(size)\n            .collect(Collectors.toList());\n\n        System.out.println("GET /api/users?page=" + page + "&size=" + size);\n        System.out.println("Offset: " + offset);\n        System.out.println("Data: " + pageData);\n        System.out.println("Pagination: {page=" + page\n            + ", size=" + size\n            + ", totalElements=" + totalElements\n            + ", totalPages=" + totalPages\n            + ", hasNext=" + (page < totalPages)\n            + ", hasPrevious=" + (page > 1) + "}");\n    }\n}' },
        { type: 'heading', value: 'Плюсы и минусы' },
        { type: 'list', items: [
          'Плюсы: простота реализации, можно перейти на любую страницу, поддержка totalElements',
          'Минусы: пропуск/дублирование при вставке/удалении данных',
          'Минусы: медленный для больших offset (OFFSET 1000000 — тяжёлый SQL запрос)',
          'Подходит: админ-панели, таблицы с данными, где данные редко меняются'
        ]},
        { type: 'tip', value: 'GitHub API использует page/per_page: GET /repos?page=2&per_page=30. По умолчанию per_page=30, максимум 100.' }
      ]
    },
    {
      id: 2,
      title: 'Cursor пагинация',
      type: 'theory',
      content: [
        { type: 'text', value: 'Cursor пагинация использует указатель (cursor) на последний элемент предыдущей страницы. Нет проблемы с пропуском/дублированием, работает быстро на больших наборах данных.' },
        { type: 'heading', value: 'Как работает cursor' },
        { type: 'code', language: 'java', value: 'import java.util.*;\nimport java.util.stream.*;\n\npublic class Main {\n    static List<Map<String, Object>> allItems = new ArrayList<>();\n\n    public static void main(String[] args) {\n        // Генерируем данные\n        for (int i = 1; i <= 25; i++) {\n            Map<String, Object> item = new LinkedHashMap<>();\n            item.put("id", "item_" + String.format("%03d", i));\n            item.put("name", "Товар " + i);\n            allItems.add(item);\n        }\n\n        // Первый запрос: GET /api/items?limit=5\n        System.out.println("=== Запрос 1: limit=5 ===");\n        Map<String, Object> page1 = fetchPage(null, 5);\n        printResult(page1);\n\n        // Второй запрос: GET /api/items?limit=5&after=item_005\n        String cursor1 = (String) page1.get("nextCursor");\n        System.out.println("\\n=== Запрос 2: limit=5&after=" + cursor1 + " ===");\n        Map<String, Object> page2 = fetchPage(cursor1, 5);\n        printResult(page2);\n\n        // Третий запрос\n        String cursor2 = (String) page2.get("nextCursor");\n        System.out.println("\\n=== Запрос 3: limit=5&after=" + cursor2 + " ===");\n        Map<String, Object> page3 = fetchPage(cursor2, 5);\n        printResult(page3);\n    }\n\n    @SuppressWarnings("unchecked")\n    static Map<String, Object> fetchPage(String afterCursor, int limit) {\n        List<Map<String, Object>> result = new ArrayList<>();\n        boolean found = (afterCursor == null);\n\n        for (Map<String, Object> item : allItems) {\n            if (!found) {\n                if (item.get("id").equals(afterCursor)) found = true;\n                continue;\n            }\n            result.add(item);\n            if (result.size() >= limit) break;\n        }\n\n        Map<String, Object> response = new LinkedHashMap<>();\n        response.put("data", result);\n        response.put("hasMore", result.size() == limit);\n        response.put("nextCursor", result.isEmpty() ? null\n            : result.get(result.size() - 1).get("id"));\n        return response;\n    }\n\n    @SuppressWarnings("unchecked")\n    static void printResult(Map<String, Object> result) {\n        List<Map<String, Object>> data = (List<Map<String, Object>>) result.get("data");\n        System.out.println("Data: " + data.stream()\n            .map(m -> m.get("id").toString()).collect(Collectors.joining(", ")));\n        System.out.println("hasMore: " + result.get("hasMore"));\n        System.out.println("nextCursor: " + result.get("nextCursor"));\n    }\n}' },
        { type: 'heading', value: 'Cursor vs Offset' },
        { type: 'list', items: [
          'Cursor: быстрый, стабильный, не пропускает записи, но нельзя перейти на произвольную страницу',
          'Offset: произвольный доступ, totalPages, но медленный и нестабильный на больших данных',
          'Cursor идеален для: лент (Facebook, Twitter), чатов, логов, бесконечной прокрутки',
          'Offset идеален для: таблиц в админке, где нужен номер страницы'
        ]},
        { type: 'note', value: 'Stripe API использует cursor: GET /v1/charges?limit=10&starting_after=ch_123. Так же работает Twitter API. Это лучший выбор для API с большим объёмом данных.' }
      ]
    },
    {
      id: 3,
      title: 'Сортировка',
      type: 'theory',
      content: [
        { type: 'text', value: 'Сортировка через query параметры позволяет клиенту управлять порядком данных. Есть несколько паттернов: sort+order, sort с минусом, массив полей.' },
        { type: 'heading', value: 'Паттерны сортировки' },
        { type: 'code', language: 'java', value: 'import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Map<String, Object>> users = new ArrayList<>();\n        users.add(user(1, "Дана", 28, "2024-03-15"));\n        users.add(user(2, "Алия", 25, "2024-01-10"));\n        users.add(user(3, "Бауыржан", 30, "2024-02-20"));\n        users.add(user(4, "Кайрат", 22, "2024-04-01"));\n\n        // Паттерн 1: sort=field&order=asc|desc\n        System.out.println("=== sort=name&order=asc ===");\n        sort(users, "name", true);\n        users.forEach(u -> System.out.println("  " + u.get("name") + " (" + u.get("age") + ")"));\n\n        // Паттерн 2: sort=-field (минус = desc)\n        System.out.println("\\n=== sort=-age (по убыванию) ===");\n        sort(users, "age", false);\n        users.forEach(u -> System.out.println("  " + u.get("name") + " (" + u.get("age") + ")"));\n\n        // Паттерн 3: несколько полей sort=age,-name\n        System.out.println("\\n=== Множественная сортировка ===");\n        System.out.println("GET /api/users?sort=role,-createdAt");\n        System.out.println("Сначала по role (asc), затем по createdAt (desc)");\n\n        // Формат ответа GitHub API\n        System.out.println("\\n=== Примеры из реальных API ===");\n        System.out.println("GitHub:  ?sort=created&direction=desc");\n        System.out.println("Stripe:  ?created[gte]=1609459200 (фильтр по дате)");\n        System.out.println("Kaspi:   ?sort=amount&order=desc");\n    }\n\n    @SuppressWarnings("unchecked")\n    static void sort(List<Map<String, Object>> list, String field, boolean asc) {\n        list.sort((a, b) -> {\n            Comparable<Object> va = (Comparable<Object>) a.get(field);\n            Comparable<Object> vb = (Comparable<Object>) b.get(field);\n            int cmp = va.compareTo(vb);\n            return asc ? cmp : -cmp;\n        });\n    }\n\n    static Map<String, Object> user(int id, String name, int age, String created) {\n        Map<String, Object> u = new LinkedHashMap<>();\n        u.put("id", id); u.put("name", name);\n        u.put("age", age); u.put("createdAt", created);\n        return u;\n    }\n}' },
        { type: 'tip', value: 'Ограничивайте поля, по которым можно сортировать. Не позволяйте sort=passwordHash. Создайте whitelist допустимых полей и возвращайте 400 для неизвестных.' },
        { type: 'warning', value: 'Сортировка без индекса в БД = медленный запрос. Каждое поле, по которому разрешена сортировка, должно иметь индекс в базе данных.' }
      ]
    },
    {
      id: 4,
      title: 'Фильтрация и поиск',
      type: 'theory',
      content: [
        { type: 'text', value: 'Фильтрация позволяет клиенту получить только нужные данные. Поиск (search) — это полнотекстовый поиск по нескольким полям.' },
        { type: 'heading', value: 'Паттерны фильтрации' },
        { type: 'code', language: 'java', value: 'import java.util.*;\nimport java.util.stream.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Map<String, Object>> products = new ArrayList<>();\n        products.add(product(1, "iPhone 15", "phones", 599990, true));\n        products.add(product(2, "Samsung S24", "phones", 499990, true));\n        products.add(product(3, "MacBook Pro", "laptops", 1299990, true));\n        products.add(product(4, "Старый телефон", "phones", 50000, false));\n        products.add(product(5, "iPad Air", "tablets", 399990, true));\n\n        // Простая фильтрация: ?category=phones\n        System.out.println("=== ?category=phones ===");\n        filter(products, "category", "phones")\n            .forEach(p -> System.out.println("  " + p.get("name")));\n\n        // Диапазон: ?minPrice=100000&maxPrice=600000\n        System.out.println("\\n=== ?minPrice=100000&maxPrice=600000 ===");\n        products.stream()\n            .filter(p -> (int) p.get("price") >= 100000 && (int) p.get("price") <= 600000)\n            .forEach(p -> System.out.println("  " + p.get("name") + " - " + p.get("price")));\n\n        // Boolean фильтр: ?inStock=true\n        System.out.println("\\n=== ?inStock=true ===");\n        filter(products, "inStock", true)\n            .forEach(p -> System.out.println("  " + p.get("name")));\n\n        // Текстовый поиск: ?q=phone\n        System.out.println("\\n=== ?q=phone (поиск) ===");\n        products.stream()\n            .filter(p -> p.get("name").toString().toLowerCase().contains("phone")\n                || p.get("category").toString().toLowerCase().contains("phone"))\n            .forEach(p -> System.out.println("  " + p.get("name") + " [" + p.get("category") + "]"));\n\n        // Множественные значения: ?category=phones,tablets\n        System.out.println("\\n=== ?category=phones,tablets ===");\n        Set<String> categories = new HashSet<>(Arrays.asList("phones", "tablets"));\n        products.stream()\n            .filter(p -> categories.contains(p.get("category")))\n            .forEach(p -> System.out.println("  " + p.get("name") + " [" + p.get("category") + "]"));\n    }\n\n    static List<Map<String, Object>> filter(List<Map<String, Object>> data,\n                                             String field, Object value) {\n        return data.stream()\n            .filter(item -> value.equals(item.get(field)))\n            .collect(Collectors.toList());\n    }\n\n    static Map<String, Object> product(int id, String name, String cat, int price, boolean inStock) {\n        Map<String, Object> p = new LinkedHashMap<>();\n        p.put("id", id); p.put("name", name); p.put("category", cat);\n        p.put("price", price); p.put("inStock", inStock);\n        return p;\n    }\n}' },
        { type: 'heading', value: 'Комбинирование фильтров' },
        { type: 'text', value: 'GET /api/products?category=phones&minPrice=100000&sort=-price&page=1&size=10 — фильтрация + сортировка + пагинация в одном запросе. Это стандартный паттерн.' },
        { type: 'note', value: 'GitHub API фильтрация: GET /issues?state=open&labels=bug&assignee=octocat. Stripe API: GET /v1/charges?created[gte]=1609459200&status=succeeded. Оба поддерживают множественные фильтры.' }
      ]
    },
    {
      id: 5,
      title: 'Практика: Пагинация с фильтрацией',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте полноценную систему пагинации с offset и cursor подходами, фильтрацией и сортировкой.',
      requirements: [
        'Создайте набор из 30 продуктов с полями: id, name, category, price, rating',
        'Реализуйте offset пагинацию: page, size, totalElements, totalPages',
        'Реализуйте cursor пагинацию: limit, afterId, hasMore',
        'Добавьте фильтрацию по category и диапазону price',
        'Добавьте сортировку по price или rating',
        'Продемонстрируйте оба подхода'
      ],
      expectedOutput: '=== Offset: page=1, size=5, category=phones ===\nData: [iPhone 15, Samsung S24, Pixel 8, OnePlus 12, Xiaomi 14]\nPagination: {page=1, size=5, total=8, pages=2, hasNext=true}\n\n=== Cursor: limit=3, after=null, sort=-price ===\nData: [MacBook Pro (1299990), iPhone 15 Pro (899990), iPad Pro (799990)]\n{hasMore=true, nextCursor=5}',
      hint: 'Сначала примените фильтрацию, затем сортировку, затем пагинацию. Это стандартный порядок обработки: WHERE -> ORDER BY -> OFFSET/LIMIT.',
      solution: 'import java.util.*;\nimport java.util.stream.*;\n\npublic class Main {\n    static List<Map<String, Object>> products = new ArrayList<>();\n\n    public static void main(String[] args) {\n        initProducts();\n\n        // Offset пагинация с фильтрацией\n        System.out.println("=== Offset: page=1, size=5, category=phones ===");\n        Map<String, Object> result1 = offsetPaginate(\n            filterByCategory("phones"), 1, 5, "price", true);\n        printOffset(result1);\n\n        System.out.println("\\n=== Offset: page=2, size=5, category=phones ===");\n        Map<String, Object> result2 = offsetPaginate(\n            filterByCategory("phones"), 2, 5, "price", true);\n        printOffset(result2);\n\n        // Cursor пагинация с сортировкой\n        System.out.println("\\n=== Cursor: limit=3, sort=-price ===");\n        List<Map<String, Object>> sorted = new ArrayList<>(products);\n        sorted.sort((a, b) -> (int) b.get("price") - (int) a.get("price"));\n        Map<String, Object> cursor1 = cursorPaginate(sorted, null, 3);\n        printCursor(cursor1);\n\n        Integer next = (Integer) cursor1.get("nextCursor");\n        System.out.println("\\n=== Cursor: limit=3, after=" + next + " ===");\n        Map<String, Object> cursor2 = cursorPaginate(sorted, next, 3);\n        printCursor(cursor2);\n    }\n\n    static List<Map<String, Object>> filterByCategory(String category) {\n        return products.stream()\n            .filter(p -> category.equals(p.get("category")))\n            .collect(Collectors.toList());\n    }\n\n    @SuppressWarnings("unchecked")\n    static Map<String, Object> offsetPaginate(List<Map<String, Object>> data,\n                                               int page, int size, String sortBy, boolean asc) {\n        data.sort((a, b) -> {\n            int cmp = ((Comparable) a.get(sortBy)).compareTo(b.get(sortBy));\n            return asc ? cmp : -cmp;\n        });\n        int total = data.size();\n        int pages = (int) Math.ceil((double) total / size);\n        int offset = (page - 1) * size;\n        List<Map<String, Object>> pageData = data.stream()\n            .skip(offset).limit(size).collect(Collectors.toList());\n\n        Map<String, Object> result = new LinkedHashMap<>();\n        result.put("data", pageData);\n        result.put("page", page);\n        result.put("size", size);\n        result.put("total", total);\n        result.put("pages", pages);\n        result.put("hasNext", page < pages);\n        return result;\n    }\n\n    static Map<String, Object> cursorPaginate(List<Map<String, Object>> data,\n                                               Integer afterId, int limit) {\n        List<Map<String, Object>> result = new ArrayList<>();\n        boolean found = (afterId == null);\n        for (Map<String, Object> item : data) {\n            if (!found) {\n                if ((int) item.get("id") == afterId) found = true;\n                continue;\n            }\n            result.add(item);\n            if (result.size() >= limit) break;\n        }\n        Map<String, Object> response = new LinkedHashMap<>();\n        response.put("data", result);\n        response.put("hasMore", result.size() == limit);\n        response.put("nextCursor", result.isEmpty() ? null\n            : result.get(result.size() - 1).get("id"));\n        return response;\n    }\n\n    @SuppressWarnings("unchecked")\n    static void printOffset(Map<String, Object> result) {\n        List<Map<String, Object>> data = (List<Map<String, Object>>) result.get("data");\n        System.out.print("Data: [");\n        System.out.print(data.stream().map(p -> p.get("name") + "(" + p.get("price") + ")")\n            .collect(Collectors.joining(", ")));\n        System.out.println("]");\n        System.out.println("Pagination: {page=" + result.get("page")\n            + ", size=" + result.get("size") + ", total=" + result.get("total")\n            + ", pages=" + result.get("pages") + ", hasNext=" + result.get("hasNext") + "}");\n    }\n\n    @SuppressWarnings("unchecked")\n    static void printCursor(Map<String, Object> result) {\n        List<Map<String, Object>> data = (List<Map<String, Object>>) result.get("data");\n        System.out.print("Data: [");\n        System.out.print(data.stream().map(p -> p.get("name") + "(" + p.get("price") + ")")\n            .collect(Collectors.joining(", ")));\n        System.out.println("]");\n        System.out.println("{hasMore=" + result.get("hasMore")\n            + ", nextCursor=" + result.get("nextCursor") + "}");\n    }\n\n    static void initProducts() {\n        String[][] items = {\n            {"phones", "iPhone 15", "599990"}, {"phones", "Samsung S24", "499990"},\n            {"phones", "Pixel 8", "399990"}, {"phones", "OnePlus 12", "349990"},\n            {"phones", "Xiaomi 14", "299990"}, {"phones", "iPhone 15 Pro", "899990"},\n            {"phones", "Samsung S24 Ultra", "699990"}, {"phones", "Nothing Phone", "249990"},\n            {"laptops", "MacBook Pro", "1299990"}, {"laptops", "ThinkPad", "599990"},\n            {"laptops", "Dell XPS", "799990"}, {"tablets", "iPad Air", "399990"},\n            {"tablets", "iPad Pro", "799990"}, {"tablets", "Samsung Tab", "299990"}\n        };\n        for (int i = 0; i < items.length; i++) {\n            Map<String, Object> p = new LinkedHashMap<>();\n            p.put("id", i + 1); p.put("category", items[i][0]);\n            p.put("name", items[i][1]); p.put("price", Integer.parseInt(items[i][2]));\n            products.add(p);\n        }\n    }\n}',
      explanation: 'Offset пагинация проста: skip(offset).limit(size). Cursor пагинация ищет элемент с afterId и возвращает следующие limit элементов. Порядок обработки: фильтрация -> сортировка -> пагинация (как SQL: WHERE -> ORDER BY -> LIMIT). Cursor надёжнее при частых вставках, offset удобнее для UI с номерами страниц.'
    },
    {
      id: 6,
      title: 'Практика: Выбор полей (sparse fieldsets)',
      type: 'practice',
      difficulty: 'easy',
      description: 'Реализуйте возможность клиенту выбирать только нужные поля в ответе через параметр fields.',
      requirements: [
        'Создайте список пользователей с полями: id, name, email, phone, city, age, role',
        'Реализуйте фильтрацию полей: GET /api/users?fields=id,name,email',
        'Если fields не указан — возвращайте все поля',
        'Поле id всегда включено даже если не указано',
        'Продемонстрируйте с разными наборами полей'
      ],
      expectedOutput: '=== Все поля ===\n{id=1, name=Алия, email=aliya@mail.kz, phone=+7701, city=Алматы, age=25, role=USER}\n\n=== fields=name,email ===\n{id=1, name=Алия, email=aliya@mail.kz}\n{id=2, name=Бауыржан, email=baur@mail.kz}\n\n=== fields=name,city ===\n{id=1, name=Алия, city=Алматы}\n{id=2, name=Бауыржан, city=Астана}',
      hint: 'Разберите fields.split(",") и для каждого пользователя создайте новый Map, содержащий только запрошенные поля. Всегда добавляйте "id".',
      solution: 'import java.util.*;\nimport java.util.stream.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Map<String, Object>> users = new ArrayList<>();\n        users.add(user(1, "Алия", "aliya@mail.kz", "+7701", "Алматы", 25, "USER"));\n        users.add(user(2, "Бауыржан", "baur@mail.kz", "+7702", "Астана", 30, "ADMIN"));\n        users.add(user(3, "Дана", "dana@mail.kz", "+7705", "Шымкент", 22, "USER"));\n\n        // Все поля\n        System.out.println("=== Все поля ===");\n        selectFields(users, null).forEach(System.out::println);\n\n        // Только name и email\n        System.out.println("\\n=== fields=name,email ===");\n        selectFields(users, "name,email").forEach(System.out::println);\n\n        // Только name и city\n        System.out.println("\\n=== fields=name,city ===");\n        selectFields(users, "name,city").forEach(System.out::println);\n\n        // Только age и role\n        System.out.println("\\n=== fields=age,role ===");\n        selectFields(users, "age,role").forEach(System.out::println);\n    }\n\n    static List<Map<String, Object>> selectFields(List<Map<String, Object>> data, String fields) {\n        if (fields == null || fields.isEmpty()) return data;\n\n        Set<String> requested = new LinkedHashSet<>();\n        requested.add("id"); // id всегда включён\n        for (String f : fields.split(",")) {\n            requested.add(f.trim());\n        }\n\n        return data.stream().map(item -> {\n            Map<String, Object> filtered = new LinkedHashMap<>();\n            for (String field : requested) {\n                if (item.containsKey(field)) {\n                    filtered.put(field, item.get(field));\n                }\n            }\n            return filtered;\n        }).collect(Collectors.toList());\n    }\n\n    static Map<String, Object> user(int id, String name, String email,\n                                     String phone, String city, int age, String role) {\n        Map<String, Object> u = new LinkedHashMap<>();\n        u.put("id", id); u.put("name", name); u.put("email", email);\n        u.put("phone", phone); u.put("city", city);\n        u.put("age", age); u.put("role", role);\n        return u;\n    }\n}',
      explanation: 'Sparse fieldsets (выбор полей) — паттерн для оптимизации ответов. Клиент указывает только нужные поля, сервер не передаёт лишние данные. Это экономит трафик, особенно на мобильных устройствах. ID всегда включён, потому что без него клиент не сможет идентифицировать ресурс. Google API и JSON:API активно используют этот паттерн.'
    },
    {
      id: 7,
      title: 'Практика: Полный pipeline обработки',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте полный pipeline обработки GET запроса: парсинг параметров -> фильтрация -> сортировка -> пагинация -> выбор полей.',
      requirements: [
        'Парсинг URL: GET /api/products?category=phones&minPrice=100000&sort=-price&page=1&size=5&fields=id,name,price',
        'Фильтрация: по category и диапазону цен',
        'Сортировка: по указанному полю (минус = desc)',
        'Пагинация: offset с метаданными',
        'Выбор полей: только запрошенные',
        'Выведите итоговый ответ в формате JSON'
      ],
      expectedOutput: 'Request: GET /api/products?category=phones&minPrice=100000&sort=-price&page=1&size=3&fields=name,price\n\nResponse 200 OK:\n{\n  "data": [\n    {"id": 6, "name": "iPhone 15 Pro", "price": 899990},\n    {"id": 1, "name": "iPhone 15", "price": 599990},\n    {"id": 2, "name": "Samsung S24", "price": 499990}\n  ],\n  "pagination": {"page": 1, "size": 3, "total": 5, "hasNext": true}\n}',
      hint: 'Pipeline: parseParams(url) -> filter(data, params) -> sort(data, params) -> paginate(data, params) -> selectFields(data, params). Каждый шаг принимает данные и параметры, возвращает обработанные данные.',
      solution: 'import java.util.*;\nimport java.util.stream.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Map<String, Object>> products = initProducts();\n\n        String url = "/api/products?category=phones&minPrice=100000&sort=-price&page=1&size=3&fields=name,price";\n        System.out.println("Request: GET " + url);\n\n        // 1. Парсинг параметров\n        Map<String, String> params = parseParams(url);\n\n        // 2. Фильтрация\n        List<Map<String, Object>> filtered = products.stream()\n            .filter(p -> {\n                if (params.containsKey("category"))\n                    if (!params.get("category").equals(p.get("category"))) return false;\n                if (params.containsKey("minPrice"))\n                    if ((int) p.get("price") < Integer.parseInt(params.get("minPrice"))) return false;\n                if (params.containsKey("maxPrice"))\n                    if ((int) p.get("price") > Integer.parseInt(params.get("maxPrice"))) return false;\n                return true;\n            }).collect(Collectors.toList());\n\n        // 3. Сортировка\n        if (params.containsKey("sort")) {\n            String sortParam = params.get("sort");\n            boolean desc = sortParam.startsWith("-");\n            String field = desc ? sortParam.substring(1) : sortParam;\n            filtered.sort((a, b) -> {\n                @SuppressWarnings("unchecked")\n                int cmp = ((Comparable) a.get(field)).compareTo(b.get(field));\n                return desc ? -cmp : cmp;\n            });\n        }\n\n        // 4. Пагинация\n        int page = Integer.parseInt(params.getOrDefault("page", "1"));\n        int size = Integer.parseInt(params.getOrDefault("size", "10"));\n        int total = filtered.size();\n        int offset = (page - 1) * size;\n        List<Map<String, Object>> paged = filtered.stream()\n            .skip(offset).limit(size).collect(Collectors.toList());\n\n        // 5. Выбор полей\n        if (params.containsKey("fields")) {\n            Set<String> fields = new LinkedHashSet<>();\n            fields.add("id");\n            for (String f : params.get("fields").split(",")) fields.add(f.trim());\n            paged = paged.stream().map(item -> {\n                Map<String, Object> f = new LinkedHashMap<>();\n                for (String field : fields) {\n                    if (item.containsKey(field)) f.put(field, item.get(field));\n                }\n                return f;\n            }).collect(Collectors.toList());\n        }\n\n        // Формируем ответ\n        System.out.println("\\nResponse 200 OK:");\n        System.out.println("{");\n        System.out.println("  \\\"data\\\": [");\n        for (int i = 0; i < paged.size(); i++) {\n            Map<String, Object> p = paged.get(i);\n            StringBuilder sb = new StringBuilder("    {");\n            int j = 0;\n            for (Map.Entry<String, Object> e : p.entrySet()) {\n                if (j > 0) sb.append(", ");\n                sb.append("\\\"").append(e.getKey()).append("\\\": ");\n                if (e.getValue() instanceof String) sb.append("\\\"").append(e.getValue()).append("\\\"");\n                else sb.append(e.getValue());\n                j++;\n            }\n            sb.append("}").append(i < paged.size() - 1 ? "," : "");\n            System.out.println(sb);\n        }\n        System.out.println("  ],");\n        System.out.println("  \\\"pagination\\\": {\\\"page\\\": " + page\n            + ", \\\"size\\\": " + size + ", \\\"total\\\": " + total\n            + ", \\\"hasNext\\\": " + (page * size < total) + "}");\n        System.out.println("}");\n    }\n\n    static Map<String, String> parseParams(String url) {\n        Map<String, String> params = new LinkedHashMap<>();\n        int idx = url.indexOf(\'?\');\n        if (idx == -1) return params;\n        for (String pair : url.substring(idx + 1).split("&")) {\n            String[] kv = pair.split("=", 2);\n            params.put(kv[0], kv.length > 1 ? kv[1] : "");\n        }\n        return params;\n    }\n\n    static List<Map<String, Object>> initProducts() {\n        List<Map<String, Object>> list = new ArrayList<>();\n        String[][] items = {\n            {"1", "iPhone 15", "phones", "599990"},\n            {"2", "Samsung S24", "phones", "499990"},\n            {"3", "Pixel 8", "phones", "399990"},\n            {"4", "OnePlus 12", "phones", "349990"},\n            {"5", "Xiaomi 14", "phones", "299990"},\n            {"6", "iPhone 15 Pro", "phones", "899990"},\n            {"7", "MacBook Pro", "laptops", "1299990"},\n            {"8", "iPad Air", "tablets", "399990"},\n            {"9", "Nothing Phone", "phones", "79990"},\n            {"10", "Samsung Tab", "tablets", "249990"}\n        };\n        for (String[] item : items) {\n            Map<String, Object> p = new LinkedHashMap<>();\n            p.put("id", Integer.parseInt(item[0]));\n            p.put("name", item[1]);\n            p.put("category", item[2]);\n            p.put("price", Integer.parseInt(item[3]));\n            list.add(p);\n        }\n        return list;\n    }\n}',
      explanation: 'Полный pipeline обработки GET запроса: 1) Парсинг URL параметров, 2) Фильтрация (WHERE), 3) Сортировка (ORDER BY), 4) Пагинация (OFFSET/LIMIT), 5) Выбор полей (SELECT). Это точно соответствует SQL: SELECT fields FROM table WHERE filters ORDER BY sort LIMIT size OFFSET offset. Каждый шаг — отдельная функция, что позволяет гибко комбинировать их.'
    }
  ]
}
