export default {
  id: 14,
  title: 'Docker в CI/CD',
  description: 'Интеграция Docker в непрерывную интеграцию и доставку: GitHub Actions, GitLab CI, сборка и тестирование в контейнерах, канарейные деплои, rollback стратегии.',
  lessons: [
    {
      id: 1,
      title: 'Принципы CI/CD с Docker',
      type: 'theory',
      content: [
        { type: 'text', value: 'Docker в CI/CD решает проблему воспроизводимости: образ собранный в CI гарантированно идентичен тому что запустится в production. Pipeline: сборка образа -> тесты -> push в registry -> деплой.' },
        { type: 'code', language: 'bash', value: '# Типичный CI/CD pipeline с Docker:\n# 1. Trigger: push в main или tag\n# 2. Build: docker build -> собрать образ\n# 3. Test: docker run tests -> запустить тесты в контейнере\n# 4. Security: trivy scan -> проверить уязвимости\n# 5. Push: docker push -> загрузить в registry\n# 6. Deploy: обновить на серверах\n# 7. Notify: уведомить команду\n\n# Преимущества Docker в CI/CD:\n# - Одинаковое окружение: CI = production\n# - Изоляция: разные проекты не мешают\n# - Кэш: быстрее повторных сборок\n# - Воспроизводимость: образ с тегом = конкретная версия\n\n# Именование образов в CI:\nGIT_SHA=$(git rev-parse --short HEAD)\nBRANCH=$(git rev-parse --abbrev-ref HEAD | sed \'s/\\//-/g\')\nREGISTRY=ghcr.io/myorg/myapp\n\ndocker build -t ${REGISTRY}:${GIT_SHA} .\ndocker tag ${REGISTRY}:${GIT_SHA} ${REGISTRY}:${BRANCH}\ndocker tag ${REGISTRY}:${GIT_SHA} ${REGISTRY}:latest\n\ndocker push ${REGISTRY}:${GIT_SHA}\ndocker push ${REGISTRY}:${BRANCH}\ndocker push ${REGISTRY}:latest\n\n# Docker в Docker (DinD) — когда CI runner сам контейнер:\n# Проблема: нельзя запустить Docker внутри Docker просто так\n# Решения:\n# 1. socket mounting: -v /var/run/docker.sock:/var/run/docker.sock\n# 2. DinD образ: docker:dind (запускает Docker daemon)\n# 3. Kaniko: сборка без Docker daemon (для Kubernetes)' },
        { type: 'tip', value: 'Используй многоэтапный pipeline: build -> unit tests -> integration tests -> security scan -> push. Падение на любом этапе останавливает pipeline. Это предотвращает деплой сломанного или небезопасного кода.' }
      ]
    },
    {
      id: 2,
      title: 'GitHub Actions — CI/CD для Docker',
      type: 'theory',
      content: [
        { type: 'text', value: 'GitHub Actions — встроенная CI/CD система GitHub. Отличная интеграция с ghcr.io (GitHub Container Registry) и Docker Hub. Поддерживает кэширование слоёв между запусками.' },
        { type: 'code', language: 'yaml', value: '# .github/workflows/docker.yml:\nname: Build and Deploy\n\non:\n  push:\n    branches: [main]\n  pull_request:\n    branches: [main]\n\nenv:\n  REGISTRY: ghcr.io\n  IMAGE_NAME: ${{ github.repository }}\n\njobs:\n  build-and-push:\n    runs-on: ubuntu-latest\n    permissions:\n      contents: read\n      packages: write\n\n    steps:\n      - name: Checkout\n        uses: actions/checkout@v4\n\n      - name: Login to GitHub Container Registry\n        uses: docker/login-action@v3\n        with:\n          registry: ${{ env.REGISTRY }}\n          username: ${{ github.actor }}\n          password: ${{ secrets.GITHUB_TOKEN }}\n\n      - name: Setup Docker Buildx\n        uses: docker/setup-buildx-action@v3\n\n      - name: Extract metadata\n        id: meta\n        uses: docker/metadata-action@v5\n        with:\n          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}\n          tags: |\n            type=sha,prefix=sha-\n            type=ref,event=branch\n            type=semver,pattern={{version}}\n            type=raw,value=latest,enable={{is_default_branch}}\n\n      - name: Build and push\n        uses: docker/build-push-action@v5\n        with:\n          context: .\n          push: ${{ github.event_name != \'pull_request\' }}\n          tags: ${{ steps.meta.outputs.tags }}\n          labels: ${{ steps.meta.outputs.labels }}\n          cache-from: type=gha\n          cache-to: type=gha,mode=max' },
        { type: 'code', language: 'yaml', value: '# Добавить тестирование и сканирование:\n      - name: Run tests\n        run: |\n          docker build --target test -t myapp-test .\n          docker run --rm myapp-test pytest --tb=short\n\n      - name: Scan with Trivy\n        uses: aquasecurity/trivy-action@master\n        with:\n          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ github.sha }}\n          format: sarif\n          output: trivy-results.sarif\n          severity: CRITICAL,HIGH\n          exit-code: "1"\n\n      - name: Upload Trivy results\n        uses: github/codeql-action/upload-sarif@v2\n        if: always()\n        with:\n          sarif_file: trivy-results.sarif\n\n  deploy:\n    needs: build-and-push\n    runs-on: ubuntu-latest\n    if: github.ref == \'refs/heads/main\'\n    environment: production\n    \n    steps:\n      - name: Deploy to server\n        uses: appleboy/ssh-action@master\n        with:\n          host: ${{ secrets.SERVER_HOST }}\n          username: deploy\n          key: ${{ secrets.SSH_KEY }}\n          script: |\n            docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest\n            docker compose -f /app/docker-compose.yml up -d --no-deps app' }
      ]
    },
    {
      id: 3,
      title: 'GitLab CI — конфигурация с Docker',
      type: 'theory',
      content: [
        { type: 'text', value: 'GitLab CI использует .gitlab-ci.yml для конфигурации. GitLab runner может использовать Docker executor — каждая job запускается в контейнере. Встроенный GitLab Container Registry.' },
        { type: 'code', language: 'yaml', value: '# .gitlab-ci.yml:\nstages:\n  - build\n  - test\n  - scan\n  - push\n  - deploy\n\nvariables:\n  DOCKER_DRIVER: overlay2\n  DOCKER_TLS_CERTDIR: ""\n  IMAGE: $CI_REGISTRY_IMAGE:$CI_COMMIT_SHORT_SHA\n  IMAGE_LATEST: $CI_REGISTRY_IMAGE:latest\n\nbuild:\n  stage: build\n  image: docker:24\n  services:\n    - docker:24-dind\n  before_script:\n    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY\n  script:\n    - docker build --cache-from $IMAGE_LATEST -t $IMAGE -t $IMAGE_LATEST .\n    - docker push $IMAGE\n  cache:\n    paths:\n      - .docker_cache/\n\ntest:\n  stage: test\n  image: docker:24\n  services:\n    - docker:24-dind\n  script:\n    - docker pull $IMAGE\n    - docker run --rm $IMAGE pytest -v\n\nscan:\n  stage: scan\n  image:\n    name: aquasec/trivy:latest\n    entrypoint: [""]\n  script:\n    - trivy image --exit-code 1 --severity CRITICAL $IMAGE\n  allow_failure: false\n\npush-tags:\n  stage: push\n  image: docker:24\n  services:\n    - docker:24-dind\n  only:\n    - tags\n  script:\n    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY\n    - docker pull $IMAGE\n    - docker tag $IMAGE $CI_REGISTRY_IMAGE:$CI_COMMIT_TAG\n    - docker push $CI_REGISTRY_IMAGE:$CI_COMMIT_TAG\n\ndeploy-prod:\n  stage: deploy\n  environment: production\n  only:\n    - tags\n  script:\n    - ssh deploy@$PROD_SERVER "docker pull $IMAGE && docker compose up -d app"' }
      ]
    },
    {
      id: 4,
      title: 'Тестирование в Docker контейнерах',
      type: 'theory',
      content: [
        { type: 'text', value: 'Запуск тестов в контейнерах обеспечивает изоляцию и воспроизводимость. Integration tests требуют реальных зависимостей (PostgreSQL, Redis) — Docker Compose решает эту задачу.' },
        { type: 'code', language: 'bash', value: '# Unit тесты в контейнере:\ndocker build --target test -t myapp-test .\ndocker run --rm myapp-test pytest tests/unit/\n\n# Integration тесты с зависимостями:\n# docker-compose.test.yml:\n# services:\n#   app-test:\n#     build:\n#       target: test\n#     command: pytest tests/integration/\n#     depends_on:\n#       db:\n#         condition: service_healthy\n#     environment:\n#       DATABASE_URL: postgresql://postgres:test@db/testdb\n#\n#   db:\n#     image: postgres:15-alpine\n#     environment:\n#       POSTGRES_PASSWORD: test\n#       POSTGRES_DB: testdb\n#     healthcheck:\n#       test: ["CMD-SHELL", "pg_isready -U postgres"]\n#       interval: 5s\n#       retries: 5\n\ndocker compose -f docker-compose.test.yml up \\\n  --abort-on-container-exit \\\n  --exit-code-from app-test\n# --abort-on-container-exit: стоп всех при завершении любого\n# --exit-code-from: код возврата = код exit app-test\n\n# Cleanup после тестов:\ndocker compose -f docker-compose.test.yml down -v\n\n# Dockerfile multi-stage для тестов:\n# FROM python:3.11-alpine AS base\n# WORKDIR /app\n# COPY requirements.txt .\n# RUN pip install -r requirements.txt\n\n# FROM base AS test\n# COPY requirements-test.txt .\n# RUN pip install -r requirements-test.txt\n# COPY . .\n# CMD ["pytest"]\n\n# FROM base AS production\n# COPY . .\n# CMD ["python", "app.py"]' },
        { type: 'tip', value: 'Testcontainers — библиотека (Java, Python, Go, Node.js) для запуска реальных зависимостей в Docker прямо из кода тестов. Автоматически управляет lifecycle: запускает перед тестом, удаляет после. Нет нужды в отдельных Compose файлах.' }
      ]
    },
    {
      id: 5,
      title: 'Деплой стратегии с Docker',
      type: 'theory',
      content: [
        { type: 'text', value: 'Docker упрощает разные стратегии деплоя: rolling update (постепенная замена), blue-green (переключение трафика), canary (постепенный rollout). Rollback = переключить на предыдущий тег образа.' },
        { type: 'code', language: 'bash', value: '# ROLLING UPDATE на одном сервере:\n# 1. Pull новый образ:\ndocker pull registry.example.com/myapp:1.2.0\n\n# 2. Обновить Compose и перезапустить только нужный сервис:\ndocker compose up -d --no-deps app\n# --no-deps: не трогать зависимые сервисы (db, redis)\n\n# BLUE-GREEN ДЕПЛОЙ:\n# Blue = текущая версия, Green = новая версия\n\n# Запустить Green:\ndocker run -d --name app-green \\\n  -p 3001:3000 \\\n  registry.example.com/myapp:1.2.0\n\n# Переключить nginx:\nnginx -s reload  # nginx.conf указывает на порт 3001\n\n# Удалить Blue:\ndocker stop app-blue && docker rm app-blue\n\n# ROLLBACK — быстро и просто:\n# Docker сохраняет предыдущие образы в registry!\ndocker pull registry.example.com/myapp:1.1.0  # Предыдущая версия\ndocker compose up -d --no-deps app\n# Или через переменную:\nIMAGE_TAG=1.1.0 docker compose up -d\n\n# CANARY — постепенный rollout:\n# 10% трафика на v2, 90% на v1\n# nginx upstream с весами:\n# upstream app {\n#   server app-v1:3000 weight=9;\n#   server app-v2:3000 weight=1;\n# }\n\n# Скрипт деплоя с проверкой здоровья:\n# docker pull ${IMAGE}:${TAG}\n# docker compose up -d --no-deps app\n# sleep 10\n# if curl -sf http://localhost/health; then\n#   echo "Deploy successful"\n# else\n#   echo "Deploy failed, rolling back"\n#   docker compose up -d --no-deps app  # Старый тег из .env\n# fi' },
        { type: 'note', value: 'Для production используй оркестраторы (Kubernetes, Docker Swarm) которые автоматизируют rolling updates, health checks и rollback. Ручной деплой скриптами подходит для простых single-server приложений.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Настройка GitHub Actions pipeline',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создай полный CI/CD pipeline для Docker приложения в GitHub Actions.',
      requirements: [
        'Создай .github/workflows/docker.yml с этапами: build, test, scan, push',
        'Настрой кэширование слоёв Docker между запусками (type=gha)',
        'Добавь сканирование образа с Trivy (только CRITICAL должны блокировать)',
        'Настрой push только для main ветки, для PR — только build и test',
        'Добавь деплой на сервер через SSH при push тега (v*.*.*)  ',
        'Используй GitHub Secrets для паролей и ключей'
      ],
      hint: 'docker/login-action для login. docker/build-push-action для сборки с кэшем. aquasecurity/trivy-action для сканирования. on.push.tags для тегов. environment: production для защиты деплоя.',
      solution: '# .github/workflows/docker.yml:\n# name: CI/CD\n# on:\n#   push:\n#     branches: [main]\n#     tags: ["v*.*.*"]\n#   pull_request:\n#     branches: [main]\n#\n# env:\n#   REGISTRY: ghcr.io\n#   IMAGE_NAME: ${{ github.repository }}\n#\n# jobs:\n#   build-test-push:\n#     runs-on: ubuntu-latest\n#     permissions:\n#       contents: read\n#       packages: write\n#       security-events: write\n#\n#     steps:\n#       - uses: actions/checkout@v4\n#\n#       - uses: docker/setup-buildx-action@v3\n#\n#       - uses: docker/login-action@v3\n#         if: github.event_name != \'pull_request\'\n#         with:\n#           registry: ${{ env.REGISTRY }}\n#           username: ${{ github.actor }}\n#           password: ${{ secrets.GITHUB_TOKEN }}\n#\n#       - id: meta\n#         uses: docker/metadata-action@v5\n#         with:\n#           images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}\n#           tags: |\n#             type=sha\n#             type=ref,event=branch\n#             type=semver,pattern={{version}}\n#             type=raw,value=latest,enable=${{ github.ref == \'refs/heads/main\' }}\n#\n#       - name: Build\n#         uses: docker/build-push-action@v5\n#         with:\n#           push: false\n#           tags: ${{ steps.meta.outputs.tags }}\n#           load: true\n#           cache-from: type=gha\n#           cache-to: type=gha,mode=max\n#\n#       - name: Test\n#         run: docker run --rm ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.meta.outputs.version }} pytest\n#\n#       - name: Trivy scan\n#         uses: aquasecurity/trivy-action@master\n#         with:\n#           image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.meta.outputs.version }}\n#           severity: CRITICAL\n#           exit-code: "1"\n#\n#       - name: Push\n#         if: github.event_name != \'pull_request\'\n#         uses: docker/build-push-action@v5\n#         with:\n#           push: true\n#           tags: ${{ steps.meta.outputs.tags }}\n#           cache-from: type=gha\n#\n#   deploy:\n#     needs: build-test-push\n#     runs-on: ubuntu-latest\n#     if: startsWith(github.ref, \'refs/tags/v\')\n#     environment: production\n#     steps:\n#       - uses: appleboy/ssh-action@master\n#         with:\n#           host: ${{ secrets.PROD_HOST }}\n#           username: deploy\n#           key: ${{ secrets.SSH_PRIVATE_KEY }}\n#           script: |\n#             docker pull ghcr.io/${{ github.repository }}:${{ github.ref_name }}\n#             IMAGE_TAG=${{ github.ref_name }} docker compose up -d --no-deps app\n\n# Secrets в GitHub:\n# Settings -> Secrets -> Actions:\n# PROD_HOST: 1.2.3.4\n# SSH_PRIVATE_KEY: -----BEGIN RSA PRIVATE KEY...',
      explanation: 'Полный pipeline: checkout -> buildx (для кэша) -> login (только не PR) -> metadata (теги) -> build (load=true для локального тестирования) -> test -> trivy -> push (только не PR) -> deploy (только теги). Кэш type=gha сохраняет слои между runs. environment: production требует подтверждения для деплоя.'
    }
  ]
}
