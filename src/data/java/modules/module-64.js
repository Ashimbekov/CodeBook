export default {
  id: 64,
  title: 'Практикум: Регулярные выражения',
  description: 'Практические задачи по регулярным выражениям в Java: валидация форматов, извлечение данных, поиск и замена, разбор логов и токенизация',
  lessons: [
    {
      id: 1,
      title: 'Задача: Валидация форматов телефонных номеров',
      type: 'practice',
      difficulty: 'easy',
      description: 'Напиши валидатор телефонных номеров для нескольких форматов: +7XXXXXXXXXX, 8XXXXXXXXXX, +7 (XXX) XXX-XX-XX, 8 (XXX) XXX-XX-XX. Метод должен определять формат и нормализовать номер к единому виду +7XXXXXXXXXX.',
      requirements: [
        'isValidPhone(String phone) возвращает boolean',
        'normalizePhone(String phone) возвращает номер в формате +7XXXXXXXXXX или null',
        'Поддержать форматы: +79001234567, 89001234567, +7 (900) 123-45-67, 8 (900) 123-45-67',
        'Использовать Pattern.compile() для каждого формата',
        'В main протестировать с валидными и невалидными номерами'
      ],
      expectedOutput: '+79001234567 -> валидный -> +79001234567\n89001234567 -> валидный -> +79001234567\n+7 (900) 123-45-67 -> валидный -> +79001234567\n8 (900) 123-45-67 -> валидный -> +79001234567\n123456 -> невалидный\n+1 (800) 555-0199 -> невалидный',
      hint: 'Для нормализации: убери все нецифровые символы, затем замени ведущую "8" на "7", добавь "+". Pattern.compile("\\\\+7\\\\d{10}") для первого формата. Pattern.compile("[78]\\\\d{10}") для второго.',
      solution: 'import java.util.regex.Matcher;\nimport java.util.regex.Pattern;\n\npublic class PhoneValidator {\n\n    // Паттерны для разных форматов\n    private static final Pattern[] PHONE_PATTERNS = {\n        Pattern.compile("\\\\+7\\\\d{10}"),                          // +79001234567\n        Pattern.compile("8\\\\d{10}"),                              // 89001234567\n        Pattern.compile("\\\\+7\\\\s?\\\\(\\\\d{3}\\\\)\\\\s?\\\\d{3}-\\\\d{2}-\\\\d{2}"), // +7 (900) 123-45-67\n        Pattern.compile("8\\\\s?\\\\(\\\\d{3}\\\\)\\\\s?\\\\d{3}-\\\\d{2}-\\\\d{2}")   // 8 (900) 123-45-67\n    };\n\n    public static boolean isValidPhone(String phone) {\n        if (phone == null) return false;\n        String trimmed = phone.trim();\n        for (Pattern p : PHONE_PATTERNS) {\n            if (p.matcher(trimmed).matches()) return true;\n        }\n        return false;\n    }\n\n    public static String normalizePhone(String phone) {\n        if (!isValidPhone(phone)) return null;\n        // Убираем все нецифровые символы\n        String digits = phone.replaceAll("[^\\\\d]", "");\n        // Теперь digits = 11 цифр начинающихся с 7 или 8\n        if (digits.startsWith("8")) {\n            digits = "7" + digits.substring(1);\n        }\n        return "+" + digits;\n    }\n\n    public static void main(String[] args) {\n        String[] tests = {\n            "+79001234567",\n            "89001234567",\n            "+7 (900) 123-45-67",\n            "8 (900) 123-45-67",\n            "123456",\n            "+1 (800) 555-0199",\n            "7(900)1234567"\n        };\n\n        for (String phone : tests) {\n            boolean valid = isValidPhone(phone);\n            String normalized = normalizePhone(phone);\n            if (valid) {\n                System.out.println(phone + " -> валидный -> " + normalized);\n            } else {\n                System.out.println(phone + " -> невалидный");\n            }\n        }\n    }\n}',
      explanation: 'Pattern.compile() компилирует регулярное выражение — это дорогая операция. Сохраняй Pattern в static final поле для повторного использования. matcher(str).matches() проверяет что вся строка соответствует паттерну (в отличие от find() которая ищет вхождение). Экранирование: \\\\ в Java-строке = \\ в regex. Поэтому \\\\+ означает литеральный +, \\\\d — цифра, \\\\( — литеральная скобка. Нормализация через replaceAll("[^\\\\d]", "") удаляет всё кроме цифр — универсальный подход. [^\\\\d] = класс символов "не цифра". Такой подход проще чем отдельные паттерны для каждого формата: разделяй валидацию (проверяем формат) и нормализацию (приводим к единому виду).'
    },
    {
      id: 2,
      title: 'Задача: Извлечение email-адресов из текста',
      type: 'practice',
      difficulty: 'easy',
      description: 'Напиши метод extractEmails(String text) который находит все email-адреса в тексте. Используй Matcher.find() для итерации по всем вхождениям. Дополнительно: классифицировать email по доменам.',
      requirements: [
        'extractEmails(String text) возвращает List<String>',
        'Паттерн: [\\w._%+-]+@[\\w.-]+\\.[a-zA-Z]{2,}',
        'Дубликаты удалять (использовать LinkedHashSet)',
        'groupByDomain(List<String> emails) возвращает Map<String, List<String>>',
        'В main протестировать с текстом содержащим несколько email'
      ],
      expectedOutput: 'Найденные email (4 уникальных):\n  user@example.com\n  admin@company.org\n  test.user+tag@mail.ru\n  info@subdomain.example.com\nПо доменам:\n  example.com: [user@example.com, info@subdomain.example.com]\n  company.org: [admin@company.org]\n  mail.ru: [test.user+tag@mail.ru]',
      hint: 'Pattern p = Pattern.compile(pattern); Matcher m = p.matcher(text); while (m.find()) { emails.add(m.group()); }. m.group() возвращает текущее найденное совпадение. LinkedHashSet сохраняет порядок и убирает дубликаты. Для домена: email.substring(email.indexOf("@") + 1).',
      solution: 'import java.util.*;\nimport java.util.regex.*;\nimport java.util.stream.*;\n\npublic class EmailExtractor {\n\n    private static final Pattern EMAIL_PATTERN =\n        Pattern.compile("[\\\\w._%+\\\\-]+@[\\\\w.\\\\-]+\\\\.[a-zA-Z]{2,}");\n\n    public static List<String> extractEmails(String text) {\n        Set<String> found = new LinkedHashSet<>(); // сохраняет порядок, убирает дубликаты\n        if (text == null) return new ArrayList<>();\n\n        Matcher matcher = EMAIL_PATTERN.matcher(text);\n        while (matcher.find()) {\n            found.add(matcher.group().toLowerCase());\n        }\n        return new ArrayList<>(found);\n    }\n\n    public static Map<String, List<String>> groupByDomain(List<String> emails) {\n        Map<String, List<String>> byDomain = new LinkedHashMap<>();\n        for (String email : emails) {\n            String domain = email.substring(email.indexOf("@") + 1);\n            byDomain.computeIfAbsent(domain, k -> new ArrayList<>()).add(email);\n        }\n        return byDomain;\n    }\n\n    public static void main(String[] args) {\n        String text = "Свяжитесь с нами: user@example.com или admin@company.org.\\n" +\n            "Для тестирования: test.user+tag@mail.ru и дублирующий USER@EXAMPLE.COM.\\n" +\n            "Технический: info@subdomain.example.com\\n" +\n            "Не email: @nodomain, nodot@, incomplete@domain";\n\n        List<String> emails = extractEmails(text);\n        System.out.println("Найденные email (" + emails.size() + " уникальных):");\n        emails.forEach(e -> System.out.println("  " + e));\n\n        System.out.println("По доменам:");\n        groupByDomain(emails).forEach((domain, list) ->\n            System.out.println("  " + domain + ": " + list));\n    }\n}',
      explanation: 'Matcher.find() ищет следующее вхождение паттерна в строке — в отличие от matches() который требует полного совпадения. Цикл while (m.find()) итерирует по всем вхождениям. m.group() возвращает текст последнего найденного совпадения (без группировки = всё совпадение). toLowerCase() нормализует email — user@Example.COM = user@example.com. LinkedHashSet: Set убирает дубликаты, Linked сохраняет порядок первого появления. Паттерн email: [\\w._%+-]+ — один или более символов имени пользователя (буквы, цифры, точка, знак подчеркивания, процент, плюс, дефис). @ — буквальный символ. [\\w.-]+ — домен. \\.[a-zA-Z]{2,} — точка и TLD от 2 символов. Это упрощённый паттерн — строгая валидация email по RFC 5322 гораздо сложнее.'
    },
    {
      id: 3,
      title: 'Задача: Валидация надёжности пароля',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создай валидатор паролей который проверяет: минимальную длину, наличие букв верхнего и нижнего регистра, цифр, специальных символов. Возвращает уровень надёжности: WEAK, FAIR, STRONG, VERY_STRONG.',
      requirements: [
        'Enum PasswordStrength { WEAK, FAIR, STRONG, VERY_STRONG }',
        'checkPassword(String password) возвращает PasswordStrength',
        'Критерии: длина >= 8, есть буква в верхнем регистре, буква в нижнем, цифра, спецсимвол (!@#$%)',
        'WEAK: менее 3 критериев. FAIR: 3. STRONG: 4. VERY_STRONG: все 5',
        'getDetails(String password) возвращает список выполненных/невыполненных требований'
      ],
      expectedOutput: '"pass" -> WEAK\n  [✗] Минимум 8 символов (есть: 4)\n  [✗] Заглавная буква\n  [✓] Строчная буква\n  [✗] Цифра\n  [✗] Спецсимвол (!@#$%^&*)\n"Password1" -> FAIR\n"Password1!" -> STRONG\n"P@ssw0rd!2" -> VERY_STRONG',
      hint: 'Для каждого критерия свой Pattern: Pattern.compile("[A-Z]") для заглавных. pattern.matcher(password).find() проверяет наличие хотя бы одного совпадения. Счётчик выполненных критериев определяет уровень.',
      solution: 'import java.util.*;\nimport java.util.regex.*;\n\npublic class PasswordValidator {\n\n    enum PasswordStrength { WEAK, FAIR, STRONG, VERY_STRONG }\n\n    static class Requirement {\n        final String description;\n        final Pattern pattern;\n        final int minLength; // 0 если не проверка длины\n\n        Requirement(String desc, Pattern p) {\n            this.description = desc;\n            this.pattern = p;\n            this.minLength = 0;\n        }\n\n        Requirement(String desc, int minLen) {\n            this.description = desc;\n            this.pattern = null;\n            this.minLength = minLen;\n        }\n\n        boolean check(String password) {\n            if (pattern != null) return pattern.matcher(password).find();\n            return password.length() >= minLength;\n        }\n    }\n\n    private static final List<Requirement> REQUIREMENTS = Arrays.asList(\n        new Requirement("Минимум 8 символов", 8),\n        new Requirement("Заглавная буква", Pattern.compile("[A-Z]")),\n        new Requirement("Строчная буква", Pattern.compile("[a-z]")),\n        new Requirement("Цифра", Pattern.compile("[0-9]")),\n        new Requirement("Спецсимвол (!@#$%^&*)", Pattern.compile("[!@#$%^&*()_+\\\\-=\\\\[\\\\]{};:\\'\\\"\\\\\\\\|,.<>/?]"))\n    );\n\n    public static PasswordStrength checkPassword(String password) {\n        if (password == null) return PasswordStrength.WEAK;\n        int met = 0;\n        for (Requirement req : REQUIREMENTS) {\n            if (req.check(password)) met++;\n        }\n        if (met <= 2) return PasswordStrength.WEAK;\n        if (met == 3) return PasswordStrength.FAIR;\n        if (met == 4) return PasswordStrength.STRONG;\n        return PasswordStrength.VERY_STRONG;\n    }\n\n    public static void printDetails(String password) {\n        PasswordStrength strength = checkPassword(password);\n        System.out.println("\\\"" + password + "\\\" -> " + strength);\n        for (Requirement req : REQUIREMENTS) {\n            boolean ok = req.check(password);\n            String note = "";\n            if (req.minLength > 0 && !ok) {\n                note = " (есть: " + password.length() + ")";\n            }\n            System.out.println("  [" + (ok ? "✓" : "✗") + "] " + req.description + note);\n        }\n    }\n\n    public static void main(String[] args) {\n        String[] passwords = {"pass", "Password1", "Password1!", "P@ssw0rd!2", ""};\n        for (String pwd : passwords) {\n            printDetails(pwd);\n        }\n    }\n}',
      explanation: 'Валидация пароля через набор паттернов — расширяемый дизайн: добавить новое требование = добавить элемент в список. Pattern.compile("[A-Z]") — класс символов с диапазоном A-Z. find() — ищет хотя бы одно вхождение (достаточно одной заглавной). Enum для уровней надёжности: типобезопасно, читаемо, легко сравнивать. Класс Requirement объединяет описание и логику проверки — паттерн Command. Спецсимволы в regex требуют экранирования: [, ], (, ), - в классе символов. - внутри [] нужно либо ставить первым/последним, либо экранировать \\-. \\[ и \\] — литеральные квадратные скобки. Счётчик met определяет уровень — простая и понятная логика вместо сложных условий.'
    },
    {
      id: 4,
      title: 'Задача: Поиск дат в тексте',
      type: 'practice',
      difficulty: 'medium',
      description: 'Напиши парсер дат который находит все даты в тексте в форматах: dd.mm.yyyy, dd/mm/yyyy, dd-mm-yyyy, yyyy-mm-dd. Использует группы захвата для извлечения дня, месяца, года. Валидирует что дата реально существует.',
      requirements: [
        'findDates(String text) возвращает List<String> найденных дат',
        'Поддержать форматы: dd.mm.yyyy, dd/mm/yyyy, dd-mm-yyyy, yyyy-mm-dd',
        'Использовать группы захвата: (\\d{2})\\.(\\d{2})\\.(\\d{4})',
        'Метод isValidDate(int day, int month, int year) валидирует существование даты',
        'Нормализовать все найденные даты к формату dd.mm.yyyy'
      ],
      expectedOutput: 'Найдены даты:\n  "25.12.2023" -> 25.12.2023 (валидна)\n  "01/06/2024" -> 01.06.2024 (валидна)\n  "15-03-2022" -> 15.03.2022 (валидна)\n  "2024-07-04" -> 04.07.2024 (валидна)\n  "31.02.2023" -> (невалидна: нет 31 февраля)\nВсего валидных дат: 4',
      hint: 'Для группового захвата: Pattern.compile("(\\\\d{2})\\\\.(\\\\d{2})\\\\.(\\\\d{4})"). m.group(1) — день, m.group(2) — месяц, m.group(3) — год. Для yyyy-MM-dd: другой порядок групп. Валидация: массив дней в месяце, проверка высокосного года для февраля.',
      solution: 'import java.util.*;\nimport java.util.regex.*;\n\npublic class DateFinder {\n\n    // Форматы с группами: group(1)=день/год, group(2)=месяц, group(3)=год/день\n    private static final Object[][] FORMATS = {\n        // {pattern, dayGroup, monthGroup, yearGroup}\n        {Pattern.compile("\\\\b(\\\\d{2})\\\\.(\\\\d{2})\\\\.(\\\\d{4})\\\\b"), 1, 2, 3},\n        {Pattern.compile("\\\\b(\\\\d{2})/(\\\\d{2})/(\\\\d{4})\\\\b"), 1, 2, 3},\n        {Pattern.compile("\\\\b(\\\\d{2})-(\\\\d{2})-(\\\\d{4})\\\\b"), 1, 2, 3},\n        {Pattern.compile("\\\\b(\\\\d{4})-(\\\\d{2})-(\\\\d{2})\\\\b"), 3, 2, 1} // yyyy-mm-dd\n    };\n\n    private static final int[] DAYS_IN_MONTH = {0, 31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31};\n\n    public static boolean isLeapYear(int year) {\n        return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);\n    }\n\n    public static boolean isValidDate(int day, int month, int year) {\n        if (month < 1 || month > 12) return false;\n        if (day < 1) return false;\n        int maxDay = DAYS_IN_MONTH[month];\n        if (month == 2 && isLeapYear(year)) maxDay = 29;\n        return day <= maxDay;\n    }\n\n    static class FoundDate {\n        String original;\n        int day, month, year;\n        boolean valid;\n\n        FoundDate(String orig, int day, int month, int year) {\n            this.original = orig;\n            this.day = day;\n            this.month = month;\n            this.year = year;\n            this.valid = isValidDate(day, month, year);\n        }\n\n        String normalized() {\n            return String.format("%02d.%02d.%04d", day, month, year);\n        }\n    }\n\n    public static List<FoundDate> findDates(String text) {\n        List<FoundDate> dates = new ArrayList<>();\n        Set<String> seen = new HashSet<>();\n\n        for (Object[] format : FORMATS) {\n            Pattern p = (Pattern) format[0];\n            int dayG = (int) format[1];\n            int monG = (int) format[2];\n            int yrG  = (int) format[3];\n\n            Matcher m = p.matcher(text);\n            while (m.find()) {\n                String original = m.group();\n                if (seen.contains(original)) continue;\n                seen.add(original);\n\n                int day   = Integer.parseInt(m.group(dayG));\n                int month = Integer.parseInt(m.group(monG));\n                int year  = Integer.parseInt(m.group(yrG));\n\n                dates.add(new FoundDate(original, day, month, year));\n            }\n        }\n        return dates;\n    }\n\n    public static void main(String[] args) {\n        String text = "Дата рождения: 25.12.2023. Встреча: 01/06/2024.\\n" +\n            "Создан: 15-03-2022, обновлён: 2024-07-04.\\n" +\n            "Неверная дата: 31.02.2023. Ещё: 29.02.2024 (високосный год).\\n" +\n            "Не дата: 99.99.9999";\n\n        List<FoundDate> dates = findDates(text);\n        System.out.println("Найдены даты:");\n        int validCount = 0;\n        for (FoundDate d : dates) {\n            if (d.valid) {\n                System.out.println("  \\"" + d.original + "\\" -> " + d.normalized() + " (валидна)");\n                validCount++;\n            } else {\n                System.out.println("  \\"" + d.original + "\\" -> (невалидна)");\n            }\n        }\n        System.out.println("Всего валидных дат: " + validCount);\n    }\n}',
      explanation: 'Группы захвата (capturing groups) — мощная функция regex. (\\d{2}) — создаёт группу, m.group(1) получает первую группу. \\b — граница слова: предотвращает частичные совпадения (не захватывает "1234.56.789"). Массив FORMATS содержит паттерн и индексы групп — это позволяет одинаково обрабатывать разные форматы. yyyy-MM-dd формат имеет порядок год-месяц-день, поэтому индексы групп другие. Валидация дат: массив DAYS_IN_MONTH индексированный от 1. Высокосный год: делится на 4, но не на 100, ИЛИ делится на 400. 1900 год — не високосный, 2000 год — високосный. Регулярное выражение находит кандидатов, валидация отфильтровывает невозможные даты (31 февраля) — разделение ответственностей.'
    },
    {
      id: 5,
      title: 'Задача: Замена множественных пробелов на одинарный',
      type: 'practice',
      difficulty: 'easy',
      description: 'Напиши методы для нормализации пробелов в тексте: замена нескольких пробелов на один, удаление пробелов в начале/конце строк, нормализация переносов строк. Дополнительно: удаление пустых строк.',
      requirements: [
        'normalizeSpaces(String text) — заменяет 2+ пробелов на один',
        'normalizeLines(String text) — убирает пустые строки, нормализует переносы',
        'cleanText(String text) — полная нормализация: пробелы + строки + trim каждой строки',
        'Использовать replaceAll() с regex',
        'Вывести текст до и после нормализации с подсчётом удалённых символов'
      ],
      expectedOutput: 'До нормализации (89 символов):\n"  Привет   мир  !   \\n  \\nКак   дела?  \\n\\n\\n  Хорошо!  "\nПосле нормализации (31 символ):\n"Привет мир !\nКак дела?\nХорошо!"',
      hint: 'replaceAll("[ \\t]+", " ") — заменяет 2+ пробелов/табуляций на один. replaceAll("(?m)^\\\\s+|\\\\s+$", "") — убирает пробелы в начале/конце каждой строки (флаг (?m) — multiline). replaceAll("\\n{2,}", "\\n") — несколько переносов → один.',
      solution: 'public class TextNormalizer {\n\n    public static String normalizeSpaces(String text) {\n        if (text == null) return null;\n        // Заменяем 2+ пробелов/табуляций на один пробел\n        return text.replaceAll("[ \\t]+", " ");\n    }\n\n    public static String normalizeLines(String text) {\n        if (text == null) return null;\n        // Убираем пустые строки (2+ переносов → один)\n        String result = text.replaceAll("\\n{2,}", "\\n");\n        // Убираем строки только с пробелами\n        result = result.replaceAll("(?m)^[ \\t]+$", "");\n        result = result.replaceAll("\\n{2,}", "\\n");\n        return result;\n    }\n\n    public static String cleanText(String text) {\n        if (text == null) return null;\n        // 1. Убираем пробелы в начале/конце каждой строки\n        String result = text.replaceAll("(?m)^[ \\t]+|[ \\t]+$", "");\n        // 2. Нормализуем множественные пробелы\n        result = result.replaceAll("[ \\t]+", " ");\n        // 3. Убираем пустые строки\n        result = result.replaceAll("(\\n[ \\t]*){2,}", "\\n");\n        // 4. Убираем ведущий и хвостовой перенос\n        return result.trim();\n    }\n\n    public static void main(String[] args) {\n        String messy = "  Привет   мир  !   \\n  \\nКак   дела?  \\n\\n\\n  Хорошо!  ";\n\n        System.out.println("До нормализации (" + messy.length() + " символов):");\n        System.out.println("\\"" + messy + "\\"");\n\n        String clean = cleanText(messy);\n        System.out.println("После нормализации (" + clean.length() + " символов):");\n        System.out.println("\\"" + clean + "\\"");\n\n        System.out.println("Удалено символов: " + (messy.length() - clean.length()));\n\n        // Дополнительные тесты\n        System.out.println("\\n--- Тесты отдельных методов ---");\n        System.out.println(normalizeSpaces("один   два\\tтри    четыре"));\n        System.out.println(normalizeLines("строка1\\n\\n\\nстрока2\\n\\nстрока3"));\n    }\n}',
      explanation: 'replaceAll() принимает regex, replace() — литеральную строку. [ \\t]+ — класс символов содержащий пробел и \\t (табуляция), + = один или более. (?m) — флаг multiline: ^ и $ совпадают с началом/концом каждой строки, а не всей строки. Без (?m) ^ — только начало всего текста. \\n{2,} — два или более переноса строки. {2,} — квантификатор "от 2 до бесконечности". Порядок операций важен: сначала trim строк (убирает пустые строки состоящие только из пробелов), потом нормализация пробелов, потом схлопывание множественных переносов. text.trim() удаляет пробельные символы с начала и конца всей строки. В реальных приложениях нормализация текста важна перед поиском, сохранением в БД, сравнением строк.'
    },
    {
      id: 6,
      title: 'Задача: Извлечение содержимого HTML-тегов',
      type: 'practice',
      difficulty: 'medium',
      description: 'Напиши простой HTML-парсер на основе regex. Извлекай содержимое тегов: <p>текст</p>, <h1>заголовок</h1>, <a href="...">ссылка</a>. Умей удалять все теги для получения чистого текста.',
      requirements: [
        'extractTagContent(String html, String tag) возвращает List<String>',
        'extractLinks(String html) возвращает Map<String, String> (href -> текст)',
        'stripTags(String html) — удалить все HTML-теги',
        'Группы захвата для атрибутов: <a href="([^"]+)">([^<]+)</a>',
        'Обработать вложенные теги (игнорировать внутренние теги в содержимом)'
      ],
      expectedOutput: 'Параграфы:\n  "Первый абзац текста"\n  "Второй абзац"\nЗаголовки h1:\n  "Главный заголовок"\nСсылки:\n  https://example.com -> "Пример сайта"\n  https://java.com -> "Java"\nЧистый текст:\n"Главный заголовок Первый абзац текста Ссылка: Пример сайта Второй абзац"',
      hint: 'Pattern.compile("<tag>([^<]*)</tag>") — захватывает содержимое тега. [^<]* — любые символы кроме <. Для ссылок: Pattern.compile("<a[^>]*href=\\"([^\\"]+)\\"[^>]*>([^<]+)</a>"). stripTags: replaceAll("<[^>]+>", "").',
      solution: 'import java.util.*;\nimport java.util.regex.*;\n\npublic class HtmlParser {\n\n    public static List<String> extractTagContent(String html, String tag) {\n        List<String> results = new ArrayList<>();\n        // Паттерн: <tag ...>содержимое</tag> (не захватывает вложенные теги)\n        Pattern p = Pattern.compile(\n            "<" + tag + "(?:\\\\s[^>]*)?>([^<]*)</" + tag + ">",\n            Pattern.CASE_INSENSITIVE);\n        Matcher m = p.matcher(html);\n        while (m.find()) {\n            String content = m.group(1).trim();\n            if (!content.isEmpty()) results.add(content);\n        }\n        return results;\n    }\n\n    public static Map<String, String> extractLinks(String html) {\n        Map<String, String> links = new LinkedHashMap<>();\n        Pattern p = Pattern.compile(\n            "<a(?:\\\\s[^>]*)?href=\\"([^\\"]+)\\"[^>]*>([^<]+)</a>",\n            Pattern.CASE_INSENSITIVE);\n        Matcher m = p.matcher(html);\n        while (m.find()) {\n            String href = m.group(1).trim();\n            String text = m.group(2).trim();\n            links.put(href, text);\n        }\n        return links;\n    }\n\n    public static String stripTags(String html) {\n        if (html == null) return null;\n        // Удаляем все теги: <...>\n        String result = html.replaceAll("<[^>]+>", " ");\n        // Нормализуем пробелы\n        result = result.replaceAll("\\\\s+", " ").trim();\n        return result;\n    }\n\n    public static void main(String[] args) {\n        String html = "<html><body>\\n" +\n            "<h1>Главный заголовок</h1>\\n" +\n            "<p>Первый абзац текста</p>\\n" +\n            "<p>Ссылка: <a href=\\"https://example.com\\">Пример сайта</a></p>\\n" +\n            "<p>Второй абзац</p>\\n" +\n            "<a href=\\"https://java.com\\">Java</a>\\n" +\n            "</body></html>";\n\n        System.out.println("Параграфы:");\n        extractTagContent(html, "p").forEach(s -> System.out.println("  \\"" + s + "\\""));\n\n        System.out.println("Заголовки h1:");\n        extractTagContent(html, "h1").forEach(s -> System.out.println("  \\"" + s + "\\""));\n\n        System.out.println("Ссылки:");\n        extractLinks(html).forEach((href, text) ->\n            System.out.println("  " + href + " -> \\"" + text + "\\""));\n\n        System.out.println("Чистый текст:");\n        System.out.println("\\"" + stripTags(html) + "\\"");\n    }\n}',
      explanation: 'Regex для HTML — упрощённый подход, подходит для простых случаев. Для сложного HTML используй библиотеку jsoup. (?:\\s[^>]*)? — незахватывающая группа (?: не создаёт группу для m.group()). Позволяет опциональные атрибуты тега. [^<]* — жадный квантификатор для символов "не <" — захватывает до первого открывающего тега. [^"]+ — символы "не кавычка" для значений атрибутов. Pattern.CASE_INSENSITIVE — флаг для нечувствительного к регистру совпадения (html, HTML, Html — всё совпадает). m.group(0) — всё совпадение, m.group(1) — первая группа, m.group(2) — вторая. Метод stripTags с <[^>]+> удаляет всё от < до >. Предупреждение: regex не может корректно обработать вложенные теги одного типа (<p><p>текст</p></p>) — для этого нужны рекурсивные паттерны или настоящий парсер.'
    },
    {
      id: 7,
      title: 'Задача: Валидация IP-адреса',
      type: 'practice',
      difficulty: 'medium',
      description: 'Напиши валидатор IP-адресов для форматов IPv4 и IPv6. Для IPv4: каждый октет 0-255. Для IPv6: восемь групп по 4 hex-символа разделённых двоеточием. Реализуй также определение типа сети (частная/публичная).',
      requirements: [
        'isValidIPv4(String ip) — валидация IPv4 (четыре числа 0-255 разделённые точками)',
        'isValidIPv6(String ip) — валидация IPv6 (8 групп по 1-4 hex символа)',
        'getIPType(String ip) — определить тип: PRIVATE, LOOPBACK, PUBLIC, INVALID',
        'Частные сети: 10.x.x.x, 172.16-31.x.x, 192.168.x.x',
        'Loopback: 127.x.x.x'
      ],
      expectedOutput: 'IPv4 валидация:\n  192.168.1.1 -> VALID (PRIVATE)\n  10.0.0.1 -> VALID (PRIVATE)\n  127.0.0.1 -> VALID (LOOPBACK)\n  8.8.8.8 -> VALID (PUBLIC)\n  256.1.1.1 -> INVALID\n  192.168.1 -> INVALID\nIPv6 валидация:\n  2001:0db8:85a3:0000:0000:8a2e:0370:7334 -> VALID\n  ::1 -> INVALID (упрощённый парсер)\n  gggg::1 -> INVALID',
      hint: 'Для IPv4: split("\\\\.") даёт 4 части, каждую parseInt и проверяем 0<=n<=255. Для regex IPv4: "^((25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\\\.){3}(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$" — паттерн для одного октета.',
      solution: 'import java.util.regex.*;\n\npublic class IPValidator {\n\n    // Паттерн для одного октета: 0-255\n    private static final String OCTET =\n        "(25[0-5]|2[0-4][0-9]|1[0-9]{2}|[1-9][0-9]|[0-9])";\n    private static final Pattern IPV4_PATTERN =\n        Pattern.compile("^" + OCTET + "\\\\." + OCTET + "\\\\." + OCTET + "\\\\." + OCTET + "$");\n\n    // IPv6: 8 групп по 1-4 hex символа\n    private static final Pattern IPV6_PATTERN =\n        Pattern.compile("^([0-9a-fA-F]{1,4}:){7}[0-9a-fA-F]{1,4}$");\n\n    enum IPType { PRIVATE, LOOPBACK, PUBLIC, INVALID, IPV6 }\n\n    public static boolean isValidIPv4(String ip) {\n        if (ip == null) return false;\n        return IPV4_PATTERN.matcher(ip).matches();\n    }\n\n    public static boolean isValidIPv6(String ip) {\n        if (ip == null) return false;\n        return IPV6_PATTERN.matcher(ip).matches();\n    }\n\n    public static IPType getIPType(String ip) {\n        if (!isValidIPv4(ip)) {\n            if (isValidIPv6(ip)) return IPType.IPV6;\n            return IPType.INVALID;\n        }\n        String[] parts = ip.split("\\\\.");\n        int a = Integer.parseInt(parts[0]);\n        int b = Integer.parseInt(parts[1]);\n\n        if (a == 127) return IPType.LOOPBACK;\n        if (a == 10) return IPType.PRIVATE;\n        if (a == 172 && b >= 16 && b <= 31) return IPType.PRIVATE;\n        if (a == 192 && b == 168) return IPType.PRIVATE;\n        return IPType.PUBLIC;\n    }\n\n    public static void main(String[] args) {\n        System.out.println("IPv4 валидация:");\n        String[] ipv4tests = {"192.168.1.1", "10.0.0.1", "127.0.0.1",\n                              "8.8.8.8", "256.1.1.1", "192.168.1", "0.0.0.0", "255.255.255.255"};\n        for (String ip : ipv4tests) {\n            boolean valid = isValidIPv4(ip);\n            if (valid) {\n                System.out.println("  " + ip + " -> VALID (" + getIPType(ip) + ")");\n            } else {\n                System.out.println("  " + ip + " -> INVALID");\n            }\n        }\n\n        System.out.println("IPv6 валидация:");\n        String[] ipv6tests = {\n            "2001:0db8:85a3:0000:0000:8a2e:0370:7334",\n            "::1",\n            "gggg:0000:0000:0000:0000:0000:0000:0001",\n            "fe80:0000:0000:0000:0202:b3ff:fe1e:8329"\n        };\n        for (String ip : ipv6tests) {\n            System.out.println("  " + ip + " -> " + (isValidIPv6(ip) ? "VALID" : "INVALID"));\n        }\n    }\n}',
      explanation: 'Паттерн для одного октета IPv4 объясняется по частям: 25[0-5] — числа 250-255. 2[0-4][0-9] — числа 200-249. 1[0-9]{2} — числа 100-199. [1-9][0-9] — числа 10-99. [0-9] — числа 0-9. Использование переменной OCTET и конкатенации строк делает паттерн читаемым. ^ и $ — якоря начала и конца строки — обязательны для полного совпадения. Для IPv6: [0-9a-fA-F]{1,4} — от 1 до 4 hex-символов. :){7}...[0-9...]{1,4}$ — 7 групп с двоеточием плюс последняя. Определение типа сети через числовой анализ октетов — надёжнее чем regex для диапазонов. В реальных приложениях используй InetAddress.getByName(ip) из java.net — он правильно обрабатывает все форматы включая сокращённые IPv6 (::1).'
    },
    {
      id: 8,
      title: 'Задача: Парсинг записей лог-файла',
      type: 'practice',
      difficulty: 'hard',
      description: 'Напиши парсер лог-файлов формата Apache Combined Log Format. Строка лога: IP - - [дата] "МЕТОД URL HTTP/версия" код размер. Извлекай все поля, считай статистику.',
      requirements: [
        'Класс LogEntry: ip, date, method, url, httpVersion, statusCode, responseSize',
        'parseLogLine(String line) возвращает Optional<LogEntry>',
        'parseLogFile(List<String> lines) возвращает List<LogEntry>',
        'Статистика: подсчёт по кодам ответа, топ-5 URL, общий трафик',
        'Использовать именованные группы (?<name>...) для читаемости'
      ],
      expectedOutput: 'Распарсено записей: 4\nСтатистика по кодам:\n  200: 2 запросов\n  404: 1 запросов\n  302: 1 запросов\nТоп URL:\n  /index.html: 2\n  /favicon.ico: 1\n  /about: 1\nОбщий трафик: 12500 байт',
      hint: 'Pattern.compile("(?<ip>\\\\S+) - - \\\\[(?<date>[^\\\\]]+)\\\\] \\"(?<method>\\\\w+) (?<url>\\\\S+) HTTP/(?<version>[0-9.]+)\\" (?<code>\\\\d+) (?<size>\\\\d+)"). m.group("ip") для именованной группы.',
      solution: 'import java.util.*;\nimport java.util.regex.*;\nimport java.util.stream.*;\n\npublic class LogParser {\n\n    static class LogEntry {\n        final String ip;\n        final String date;\n        final String method;\n        final String url;\n        final String httpVersion;\n        final int statusCode;\n        final long responseSize;\n\n        LogEntry(String ip, String date, String method, String url,\n                 String httpVersion, int statusCode, long responseSize) {\n            this.ip = ip;\n            this.date = date;\n            this.method = method;\n            this.url = url;\n            this.httpVersion = httpVersion;\n            this.statusCode = statusCode;\n            this.responseSize = responseSize;\n        }\n\n        public String toString() {\n            return String.format("[%s] %s %s -> %d (%d bytes)",\n                ip, method, url, statusCode, responseSize);\n        }\n    }\n\n    // Apache Combined Log Format с именованными группами\n    private static final Pattern LOG_PATTERN = Pattern.compile(\n        "(?<ip>\\\\S+) \\\\S+ \\\\S+ " +\n        "\\\\[(?<date>[^\\\\]]+)\\\\] " +\n        "\\"(?<method>\\\\w+) (?<url>\\\\S+) HTTP/(?<version>[\\\\d.]+)\\" " +\n        "(?<code>\\\\d+) (?<size>\\\\d+)"\n    );\n\n    public static Optional<LogEntry> parseLogLine(String line) {\n        if (line == null || line.trim().isEmpty()) return Optional.empty();\n        Matcher m = LOG_PATTERN.matcher(line.trim());\n        if (!m.find()) return Optional.empty();\n\n        try {\n            return Optional.of(new LogEntry(\n                m.group("ip"),\n                m.group("date"),\n                m.group("method"),\n                m.group("url"),\n                m.group("version"),\n                Integer.parseInt(m.group("code")),\n                Long.parseLong(m.group("size"))\n            ));\n        } catch (NumberFormatException e) {\n            return Optional.empty();\n        }\n    }\n\n    public static List<LogEntry> parseLogFile(List<String> lines) {\n        return lines.stream()\n            .map(LogParser::parseLogLine)\n            .filter(Optional::isPresent)\n            .map(Optional::get)\n            .collect(Collectors.toList());\n    }\n\n    public static void printStats(List<LogEntry> entries) {\n        System.out.println("Распарсено записей: " + entries.size());\n\n        // Статистика по кодам\n        System.out.println("Статистика по кодам:");\n        entries.stream()\n            .collect(Collectors.groupingBy(e -> e.statusCode, Collectors.counting()))\n            .entrySet().stream()\n            .sorted(Map.Entry.comparingByKey())\n            .forEach(e -> System.out.println("  " + e.getKey() + ": " + e.getValue() + " запросов"));\n\n        // Топ URL\n        System.out.println("Топ URL:");\n        entries.stream()\n            .collect(Collectors.groupingBy(e -> e.url, Collectors.counting()))\n            .entrySet().stream()\n            .sorted(Map.Entry.<String, Long>comparingByValue().reversed())\n            .limit(5)\n            .forEach(e -> System.out.println("  " + e.getKey() + ": " + e.getValue()));\n\n        long totalTraffic = entries.stream().mapToLong(e -> e.responseSize).sum();\n        System.out.println("Общий трафик: " + totalTraffic + " байт");\n    }\n\n    public static void main(String[] args) {\n        List<String> logLines = Arrays.asList(\n            "192.168.1.1 - - [25/Dec/2023:10:00:00 +0600] \\"GET /index.html HTTP/1.1\\" 200 5000",\n            "10.0.0.1 - - [25/Dec/2023:10:01:00 +0600] \\"GET /favicon.ico HTTP/1.1\\" 200 500",\n            "172.16.0.1 - - [25/Dec/2023:10:02:00 +0600] \\"GET /notfound HTTP/1.1\\" 404 200",\n            "192.168.1.2 - - [25/Dec/2023:10:03:00 +0600] \\"GET /about HTTP/1.1\\" 302 300",\n            "192.168.1.1 - - [25/Dec/2023:10:04:00 +0600] \\"POST /index.html HTTP/1.1\\" 200 6500",\n            "INVALID LOG LINE"\n        );\n\n        List<LogEntry> entries = parseLogFile(logLines);\n        entries.forEach(System.out::println);\n        System.out.println();\n        printStats(entries);\n    }\n}',
      explanation: 'Именованные группы (?<name>...) — мощное улучшение читаемости. m.group("ip") вместо m.group(1) — самодокументирующийся код. \\S+ — один или более непробельных символов (для IP, метода, URL). [^\\]]+ — символы "не ]" — для даты в скобках [25/Dec/2023:...]. Конкатенация строк для сборки паттерна — читаемее чем одна длинная строка. Stream API для статистики: groupingBy + counting() — подсчёт по группам. Collectors.groupingBy(e -> e.statusCode, Collectors.counting()) — агрегация. map(Optional::get) безопасен после filter(Optional::isPresent) — все Optional гарантированно непустые. В реальных проектах: библиотека Logback/Log4j2 для написания логов, Elasticsearch/Grafana Loki для их анализа.'
    },
    {
      id: 9,
      title: 'Задача: Маскирование номеров кредитных карт',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй маскирование конфиденциальных данных в тексте: номера кредитных карт заменяй на **** **** **** XXXX (оставляй последние 4 цифры), CVV заменяй полностью, также маскируй СНИЛС и паспортные данные.',
      requirements: [
        'maskCreditCards(String text) — маскирует номера карт (16 цифр в форматах)',
        'Форматы карт: 1234567890123456, 1234 5678 9012 3456, 1234-5678-9012-3456',
        'Результат: **** **** **** XXXX (последние 4 цифры сохраняются)',
        'maskSensitiveData(String text) — маскирует все виды данных',
        'Не изменять уже замаскированные данные (содержащие *)'
      ],
      expectedOutput: 'Оригинал:\n"Карта: 1234 5678 9012 3456, CVV: 123, другая: 9876543210123456"\nПосле маскирования:\n"Карта: **** **** **** 3456, CVV: 123, другая: **** **** **** 3456"',
      hint: 'Для захвата карты и последних 4 цифр используй группу: Pattern.compile("\\\\b(\\\\d{4}[- ]?\\\\d{4}[- ]?\\\\d{4}[- ]?)(\\\\d{4})\\\\b"). Замена: matcher.appendReplacement(sb, "**** **** **** " + matcher.group(2)). Метод appendReplacement/appendTail для итерации и замены.',
      solution: 'import java.util.regex.*;\n\npublic class DataMasker {\n\n    // Паттерн кредитной карты: 16 цифр с разделителями или без\n    private static final Pattern CARD_PATTERN = Pattern.compile(\n        "\\\\b(\\\\d{4}[\\\\s\\\\-]?\\\\d{4}[\\\\s\\\\-]?\\\\d{4}[\\\\s\\\\-]?)(\\\\d{4})\\\\b"\n    );\n\n    // Паттерн email для маскирования\n    private static final Pattern EMAIL_PATTERN =\n        Pattern.compile("([\\\\w.]+)@([\\\\w.]+\\\\.[a-zA-Z]{2,})");\n\n    public static String maskCreditCards(String text) {\n        if (text == null) return null;\n        Matcher m = CARD_PATTERN.matcher(text);\n        StringBuffer sb = new StringBuffer();\n        while (m.find()) {\n            String lastFour = m.group(2);\n            m.appendReplacement(sb, "**** **** **** " + lastFour);\n        }\n        m.appendTail(sb);\n        return sb.toString();\n    }\n\n    public static String maskEmails(String text) {\n        if (text == null) return null;\n        Matcher m = EMAIL_PATTERN.matcher(text);\n        StringBuffer sb = new StringBuffer();\n        while (m.find()) {\n            String username = m.group(1);\n            String domain = m.group(2);\n            // Оставляем первые 2 символа имени\n            String maskedUser = username.length() > 2\n                ? username.substring(0, 2) + "***"\n                : "***";\n            m.appendReplacement(sb, Matcher.quoteReplacement(maskedUser + "@" + domain));\n        }\n        m.appendTail(sb);\n        return sb.toString();\n    }\n\n    public static String maskSensitiveData(String text) {\n        if (text == null) return null;\n        String result = maskCreditCards(text);\n        result = maskEmails(result);\n        return result;\n    }\n\n    public static void main(String[] args) {\n        String text = "Карта: 1234 5678 9012 3456, другая: 9876543210123456\\n" +\n            "Карта с дефисами: 1111-2222-3333-4444\\n" +\n            "Email: user.name@company.com и admin@test.org";\n\n        System.out.println("Оригинал:");\n        System.out.println("\\"" + text + "\\"");\n\n        System.out.println("После маскирования карт:");\n        System.out.println("\\"" + maskCreditCards(text) + "\\"");\n\n        System.out.println("После маскирования email:");\n        System.out.println("\\"" + maskEmails(text) + "\\"");\n\n        System.out.println("Полное маскирование:");\n        System.out.println("\\"" + maskSensitiveData(text) + "\\"");\n    }\n}',
      explanation: 'Маскирование чувствительных данных — обязательное требование при логировании (PCI DSS для карт, GDPR для персональных данных). appendReplacement(StringBuffer, replacement) — заменяет текущее совпадение. appendTail(sb) — добавляет оставшийся текст после последнего совпадения. Matcher.quoteReplacement() экранирует $ и \\ в строке замены — без этого $1 в строке замены будет интерпретировано как ссылка на группу. Паттерн [\\s\\-]? — опциональный пробел или дефис. Группы: (\\d{4}[...]{3 раза})(\\d{4}) — захватывает первые 12 и последние 4 цифры. group(2) — последние 4 цифры сохраняются. \\b — граница слова предотвращает ложные срабатывания в более длинных числах. StringBuffer (не StringBuilder) требуется для Matcher.appendReplacement — это API-ограничение Java.'
    },
    {
      id: 10,
      title: 'Задача: Токенизатор математических выражений',
      type: 'practice',
      difficulty: 'hard',
      description: 'Напиши токенизатор для математических выражений. Разбивает выражение на токены: числа (целые и дробные), операторы (+,-,*,/,^), скобки, переменные. Использует regex для определения типа каждого токена.',
      requirements: [
        'Enum TokenType { NUMBER, OPERATOR, PAREN_OPEN, PAREN_CLOSE, VARIABLE, WHITESPACE, UNKNOWN }',
        'Класс Token { TokenType type, String value }',
        'tokenize(String expression) возвращает List<Token> (без пробелов)',
        'Числа: целые (123) и дробные (3.14)',
        'Переменные: последовательность букв (x, abc, var1)',
        'Вывести токены с типами'
      ],
      expectedOutput: 'Выражение: "3.14 * x^2 + (b - 4*a*c) / 2"\nТокены:\n  NUMBER: "3.14"\n  OPERATOR: "*"\n  VARIABLE: "x"\n  OPERATOR: "^"\n  NUMBER: "2"\n  OPERATOR: "+"\n  PAREN_OPEN: "("\n  VARIABLE: "b"\n  OPERATOR: "-"\n  NUMBER: "4"\n  OPERATOR: "*"\n  VARIABLE: "a"\n  OPERATOR: "*"\n  VARIABLE: "c"\n  PAREN_CLOSE: ")"\n  OPERATOR: "/"\n  NUMBER: "2"',
      hint: 'Используй один Pattern с несколькими группами альтернаций: "([0-9]+\\.?[0-9]*)|([+\\-*/^])|([()])|([a-zA-Z][a-zA-Z0-9]*)|(\\s+)". Используй именованные группы или проверку m.group(i) != null для определения типа.',
      solution: 'import java.util.*;\nimport java.util.regex.*;\n\npublic class MathTokenizer {\n\n    enum TokenType {\n        NUMBER, OPERATOR, PAREN_OPEN, PAREN_CLOSE, VARIABLE, UNKNOWN\n    }\n\n    static class Token {\n        final TokenType type;\n        final String value;\n\n        Token(TokenType type, String value) {\n            this.type = type;\n            this.value = value;\n        }\n\n        public String toString() {\n            return String.format("  %s: \\"%s\\"", type, value);\n        }\n    }\n\n    // Паттерн с именованными группами для каждого типа токена\n    private static final Pattern TOKEN_PATTERN = Pattern.compile(\n        "(?<number>[0-9]+\\\\.?[0-9]*)" +   // целые и дробные числа\n        "|(?<operator>[+\\\\-*/^%])" +        // операторы\n        "|(?<open>\\\\()" +                   // открывающая скобка\n        "|(?<close>\\\\))" +                  // закрывающая скобка\n        "|(?<variable>[a-zA-Z][a-zA-Z0-9]*)" + // переменные/функции\n        "|(?<space>\\\\s+)" +                 // пробелы (пропускаем)\n        "|(?<unknown>.)"                    // всё остальное\n    );\n\n    public static List<Token> tokenize(String expression) {\n        List<Token> tokens = new ArrayList<>();\n        if (expression == null) return tokens;\n\n        Matcher m = TOKEN_PATTERN.matcher(expression);\n        while (m.find()) {\n            if (m.group("space") != null) continue; // пропускаем пробелы\n\n            if (m.group("number") != null) {\n                tokens.add(new Token(TokenType.NUMBER, m.group("number")));\n            } else if (m.group("operator") != null) {\n                tokens.add(new Token(TokenType.OPERATOR, m.group("operator")));\n            } else if (m.group("open") != null) {\n                tokens.add(new Token(TokenType.PAREN_OPEN, m.group("open")));\n            } else if (m.group("close") != null) {\n                tokens.add(new Token(TokenType.PAREN_CLOSE, m.group("close")));\n            } else if (m.group("variable") != null) {\n                tokens.add(new Token(TokenType.VARIABLE, m.group("variable")));\n            } else if (m.group("unknown") != null) {\n                tokens.add(new Token(TokenType.UNKNOWN, m.group("unknown")));\n            }\n        }\n        return tokens;\n    }\n\n    public static void main(String[] args) {\n        String[] expressions = {\n            "3.14 * x^2 + (b - 4*a*c) / 2",\n            "sin(x) + cos(y)",\n            "100 + 200.5 - 3.14"\n        };\n\n        for (String expr : expressions) {\n            System.out.println("Выражение: \\"" + expr + "\\"");\n            System.out.println("Токены:");\n            tokenize(expr).forEach(System.out::println);\n            System.out.println();\n        }\n    }\n}',
      explanation: 'Токенизация (лексический анализ) — первый шаг разбора любого языка. Паттерн с именованными группами и альтернациями (|) — элегантный способ разбить на токены за один проход. Важно: порядок альтернаций имеет значение. Числа должны идти до точки-оператора, иначе "3.14" распарсится как "3", ".", "14". [0-9]+\\.?[0-9]* — целое число, опциональная точка, опциональные цифры после. [a-zA-Z][a-zA-Z0-9]* — переменная начинается с буквы (не цифры). (?<name>...) — именованные группы; m.group("name") != null — проверка что эта альтернатива совпала. Пробелы (space) пропускаем — их не добавляем в список токенов. unknown . захватывает любой одиночный символ — "мусорное ведро" для неизвестных токенов. Это основа для создания калькуляторов, DSL-парсеров, препроцессоров.'
    }
  ]
}
