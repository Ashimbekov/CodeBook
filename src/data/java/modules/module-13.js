export default {
  id: 13,
  title: 'Основы ООП: Классы и объекты',
  description: 'Полное введение в объектно-ориентированное программирование: от понимания концепции до создания собственных классов с полями, методами, toString, equals, static и практическими проектами',
  lessons: [
    {
      id: 1,
      title: 'Что такое ООП и зачем оно нужно',
      type: 'theory',
      content: [
        { type: 'text', value: 'ООП (Объектно-ориентированное программирование) — это способ организации кода, при котором программа строится из объектов, подобных объектам реального мира. Каждый объект имеет свои свойства (данные) и поведение (действия).' },
        { type: 'heading', value: 'Аналогия с реальным миром' },
        { type: 'text', value: 'Посмотри вокруг. Ты видишь объекты: телефон, стол, человека, машину. У каждого объекта есть характеристики (цвет телефона, размер стола) и действия (телефон звонит, машина едет). ООП переносит эту идею в программирование. Вместо того чтобы писать длинную последовательность команд, мы создаём объекты и заставляем их взаимодействовать.' },
        { type: 'tip', value: 'Представь, что ты строишь город в Minecraft. Каждый житель — это объект с именем, здоровьем и инвентарём. Каждый дом — объект с координатами и материалом. Город — набор взаимодействующих объектов. Именно так работает ООП!' },
        { type: 'heading', value: 'Процедурное программирование vs ООП' },
        { type: 'text', value: 'До ООП программисты писали код процедурно — как пошаговую инструкцию сверху вниз. Это как рецепт: шаг 1, шаг 2, шаг 3... Для маленьких программ это работает, но когда код растёт до тысяч строк — становится хаос.' },
        { type: 'code', language: 'java', value: '// ===== ПРОЦЕДУРНЫЙ стиль =====\n// Данные разбросаны по переменным, функции отдельно\nString studentName1 = "Алия";\nint studentAge1 = 20;\ndouble studentGpa1 = 4.5;\n\nString studentName2 = "Берик";\nint studentAge2 = 21;\ndouble studentGpa2 = 3.8;\n\n// Нужна функция для каждого действия\nstatic void printStudent(String name, int age, double gpa) {\n    System.out.println(name + ", " + age + " лет, GPA: " + gpa);\n}\n\n// Если добавить поле "город" — нужно менять ВСЁ:\n// все переменные, все функции, все вызовы...' },
        { type: 'code', language: 'java', value: '// ===== ООП стиль =====\n// Данные и поведение объединены в одном месте\npublic class Student {\n    String name;\n    int age;\n    double gpa;\n\n    void printInfo() {\n        System.out.println(name + ", " + age + " лет, GPA: " + gpa);\n    }\n}\n\n// Создаём объекты\nStudent s1 = new Student();\ns1.name = "Алия";\ns1.age = 20;\ns1.gpa = 4.5;\n\nStudent s2 = new Student();\ns2.name = "Берик";\ns2.age = 21;\ns2.gpa = 3.8;\n\n// Каждый объект сам знает, как себя вывести\ns1.printInfo(); // Алия, 20 лет, GPA: 4.5\ns2.printInfo(); // Берик, 21 лет, GPA: 3.8\n\n// Добавить поле "город" — меняем только класс Student!' },
        { type: 'heading', value: 'Четыре главных принципа ООП' },
        { type: 'text', value: 'ООП стоит на четырёх столпах. Пока просто запомни их названия — каждый мы подробно разберём в будущих модулях:' },
        { type: 'list', items: [
          'Инкапсуляция — прячем внутреннюю кухню объекта. Ты нажимаешь кнопку на пульте — и телевизор включается. Тебе не нужно знать, как работают микросхемы внутри.',
          'Наследование — создаём новый класс на основе существующего. Электромобиль — это тоже Автомобиль, но с батареей вместо бензобака.',
          'Полиморфизм — один и тот же метод работает по-разному для разных объектов. Метод "издать звук" — кошка мяукает, собака лает, корова мычит.',
          'Абстракция — выделяем только важные характеристики, игнорируя мелочи. Для модели "Студент" нам важны имя и GPA, но не цвет носков.'
        ]},
        { type: 'code', language: 'java', value: '// Быстрый пример всех 4 принципов (просто для понимания)\n\n// 1. ИНКАПСУЛЯЦИЯ — поля скрыты, доступ через методы\npublic class BankAccount {\n    private double balance; // скрыто!\n\n    public void deposit(double amount) {\n        if (amount > 0) balance += amount; // проверка внутри\n    }\n}\n\n// 2. НАСЛЕДОВАНИЕ — Dog наследует от Animal\npublic class Animal {\n    String name;\n    void eat() { System.out.println(name + " ест"); }\n}\npublic class Dog extends Animal {\n    void bark() { System.out.println(name + " лает"); }\n}\n\n// 3. ПОЛИМОРФИЗМ — один метод, разное поведение\nAnimal a1 = new Cat();  // мяукает\nAnimal a2 = new Dog();  // лает\na1.makeSound(); // Мяу!\na2.makeSound(); // Гав!\n\n// 4. АБСТРАКЦИЯ — только важные детали\npublic abstract class Shape {\n    abstract double area(); // что именно внутри — не важно\n}' },
        { type: 'heading', value: 'Почему Java — это ООП-язык' },
        { type: 'text', value: 'В Java почти всё является объектом. Даже программа — это класс (public class Main). Строки — объекты (String), массивы — объекты. Java заставляет тебя думать объектами, и это одна из причин, почему она так популярна в крупных проектах.' },
        { type: 'note', value: 'Не пугайся, если 4 принципа кажутся сложными. Сейчас мы сосредоточимся на самом фундаменте — классах и объектах. Остальное придёт позже, шаг за шагом.' },
        { type: 'warning', value: 'Типичная ошибка новичка — пытаться сразу писать "правильный ООП-код". Не надо. Сначала научись создавать простые классы, а принципы дизайна придут с опытом.' }
      ]
    },
    {
      id: 2,
      title: 'Класс как чертёж',
      type: 'theory',
      content: [
        { type: 'text', value: 'Класс — это шаблон (чертёж, рецепт), который описывает, какими свойствами и действиями будут обладать объекты. Сам класс — это НЕ объект, а лишь описание. Объект — это конкретный экземпляр, созданный по этому шаблону.' },
        { type: 'heading', value: 'Аналогия: чертёж дома и реальный дом' },
        { type: 'text', value: 'Архитектор рисует чертёж дома. В чертеже указано: 3 комнаты, 2 этажа, красная крыша. Но чертёж — это не дом! Ты не можешь в нём жить. Зато по одному чертежу можно построить 10, 100, 1000 одинаковых домов. Каждый построенный дом — отдельный объект, со своим адресом, жильцами и мебелью.' },
        { type: 'tip', value: 'Ещё одна аналогия: класс — это как формочка для печенья, а объекты — сами печенья. Формочка одна, но печений можно наштамповать сколько угодно. И каждое печенье можно украсить по-разному (разные значения полей).' },
        { type: 'heading', value: 'Класс в коде' },
        { type: 'code', language: 'java', value: '// Класс = чертёж дома\npublic class House {\n    // Поля = характеристики дома (что ЕСТЬ у каждого дома)\n    String address;       // адрес\n    int floors;           // количество этажей\n    String color;         // цвет стен\n    double area;          // площадь в кв. метрах\n    boolean hasGarage;    // есть ли гараж\n\n    // Методы = действия дома (что дом УМЕЕТ)\n    void showInfo() {\n        System.out.println("Дом по адресу: " + address);\n        System.out.println("Этажей: " + floors + ", Цвет: " + color);\n        System.out.println("Площадь: " + area + " кв.м");\n        System.out.println("Гараж: " + (hasGarage ? "есть" : "нет"));\n    }\n}' },
        { type: 'heading', value: 'Из чего состоит класс' },
        { type: 'text', value: 'Любой класс состоит из двух основных частей:' },
        { type: 'list', items: [
          'Поля (fields) — переменные внутри класса. Описывают СОСТОЯНИЕ объекта. Пример: имя, возраст, цвет.',
          'Методы (methods) — функции внутри класса. Описывают ПОВЕДЕНИЕ объекта. Пример: бежать, есть, говорить.'
        ]},
        { type: 'code', language: 'java', value: '// Визуальная схема класса:\n//\n// ┌──────────────────────────┐\n// │       class Car          │\n// ├──────────────────────────┤\n// │ ПОЛЯ (состояние):        │\n// │   - brand: String        │\n// │   - color: String        │\n// │   - speed: int           │\n// │   - year: int            │\n// ├──────────────────────────┤\n// │ МЕТОДЫ (поведение):      │\n// │   + accelerate()         │\n// │   + brake()              │\n// │   + honk()               │\n// │   + showInfo()           │\n// └──────────────────────────┘\n\npublic class Car {\n    // Поля\n    String brand;\n    String color;\n    int speed;\n    int year;\n\n    // Методы\n    void accelerate() {\n        speed += 10;\n        System.out.println(brand + " разгоняется! Скорость: " + speed);\n    }\n\n    void brake() {\n        if (speed >= 10) {\n            speed -= 10;\n        }\n        System.out.println(brand + " тормозит. Скорость: " + speed);\n    }\n\n    void honk() {\n        System.out.println(brand + " сигналит: Бип-бип!");\n    }\n\n    void showInfo() {\n        System.out.println(brand + " " + year + " (" + color + "), скорость: " + speed);\n    }\n}' },
        { type: 'heading', value: 'Больше аналогий для понимания' },
        { type: 'code', language: 'java', value: '// Аналогия 1: Рецепт (класс) и блюдо (объект)\n// Рецепт борща → конкретная кастрюля борща\n\n// Аналогия 2: Анкета (класс) и заполненная анкета (объект)\n// Бланк анкеты: ФИО ___, Возраст ___, Город ___\n// Заполненная: ФИО Алия, Возраст 20, Город Астана\n\n// Аналогия 3: Форма для печенья (класс) и печенье (объект)\n// Форма звёздочки → конкретное печенье-звёздочка\n\n// В коде:\npublic class Anketa {  // бланк анкеты\n    String fio;        // поле для ФИО\n    int age;           // поле для возраста\n    String city;       // поле для города\n}\n\n// "Заполняем анкету" = создаём объект\nAnketa a = new Anketa();\na.fio = "Алия";\na.age = 20;\na.city = "Астана";' },
        { type: 'heading', value: 'Правила именования классов' },
        { type: 'list', items: [
          'Имя класса всегда начинается с ЗАГЛАВНОЙ буквы: Car, Person, BankAccount',
          'Используется CamelCase (каждое новое слово с заглавной): MyFirstClass, StudentGradeBook',
          'Имя файла ДОЛЖНО совпадать с именем public класса: класс Car → файл Car.java',
          'Имя класса — существительное (Car, Student), а не глагол (Run, Calculate)',
          'Имя должно быть понятным: не X, а Customer; не D, а Document'
        ]},
        { type: 'warning', value: 'Частая ошибка: путать класс и объект. Класс — это шаблон, он один. Объектов может быть много. Нельзя сказать "класс Алия" — Алия это объект класса Student.' },
        { type: 'note', value: 'В Java можно создать класс без методов (только поля) или без полей (только методы). Но обычно класс содержит и то, и другое — ведь объект должен иметь и состояние, и поведение.' }
      ]
    },
    {
      id: 3,
      title: 'Создание класса: поля, методы, модификаторы',
      type: 'theory',
      content: [
        { type: 'text', value: 'Теперь научимся создавать классы от начала до конца. Разберём каждую часть: объявление, поля, методы и модификаторы доступа.' },
        { type: 'heading', value: 'Синтаксис объявления класса' },
        { type: 'code', language: 'java', value: '// Полный синтаксис:\n[модификатор доступа] class ИмяКласса {\n    // поля\n    // методы\n}\n\n// Простейший класс:\npublic class Dog {\n    // пока пустой\n}' },
        { type: 'heading', value: 'Шаг 1: Определяем поля' },
        { type: 'text', value: 'Поля — это переменные, описывающие характеристики объекта. Подумай: какие данные нужно хранить?' },
        { type: 'code', language: 'java', value: 'public class Dog {\n    // Поля (свойства каждой собаки)\n    String name;        // кличка\n    String breed;       // порода\n    int age;            // возраст в годах\n    double weight;      // вес в кг\n    boolean isVaccinated; // привита ли\n    String ownerName;   // имя хозяина\n}' },
        { type: 'heading', value: 'Шаг 2: Добавляем методы' },
        { type: 'text', value: 'Методы — это действия, которые объект может выполнять. Подумай: что этот объект умеет делать?' },
        { type: 'code', language: 'java', value: 'public class Dog {\n    // Поля\n    String name;\n    String breed;\n    int age;\n    double weight;\n    boolean isVaccinated;\n\n    // Метод без параметров и без возврата\n    void bark() {\n        System.out.println(name + " говорит: Гав-гав!");\n    }\n\n    // Метод с параметром\n    void eat(String food) {\n        System.out.println(name + " ест " + food);\n        weight += 0.1; // вес немного увеличивается\n    }\n\n    // Метод, возвращающий значение\n    int ageInHumanYears() {\n        return age * 7;\n    }\n\n    // Метод с логикой\n    void vaccinate() {\n        if (!isVaccinated) {\n            isVaccinated = true;\n            System.out.println(name + " теперь привита!");\n        } else {\n            System.out.println(name + " уже была привита.");\n        }\n    }\n\n    // Метод вывода информации\n    void showInfo() {\n        System.out.println("=== Собака ===" );\n        System.out.println("Кличка: " + name);\n        System.out.println("Порода: " + breed);\n        System.out.println("Возраст: " + age + " лет (" + ageInHumanYears() + " по-человечески)");\n        System.out.println("Вес: " + weight + " кг");\n        System.out.println("Привита: " + (isVaccinated ? "да" : "нет"));\n    }\n}' },
        { type: 'heading', value: 'Модификаторы доступа' },
        { type: 'text', value: 'Модификаторы доступа контролируют, кто может видеть и использовать поля и методы. Это основа инкапсуляции.' },
        { type: 'code', language: 'java', value: 'public class BankAccount {\n    // public — доступно ВСЕМ, из любого класса\n    public String ownerName;\n\n    // private — доступно ТОЛЬКО внутри этого класса\n    private double balance;\n\n    // protected — доступно в этом классе и в классах-наследниках\n    protected String bankName;\n\n    // без модификатора (default/package-private) — доступно\n    // только внутри того же пакета\n    int accountNumber;\n\n    // public метод — можно вызвать откуда угодно\n    public void deposit(double amount) {\n        if (amount > 0) {\n            balance += amount; // внутри класса private поля доступны!\n        }\n    }\n\n    // public метод для чтения приватного поля\n    public double getBalance() {\n        return balance;\n    }\n}' },
        { type: 'heading', value: 'Таблица модификаторов доступа' },
        { type: 'code', language: 'java', value: '// ┌─────────────┬──────────┬─────────┬───────────┬───────────┐\n// │ Модификатор │ В классе │ Пакет   │ Наследник │ Все       │\n// ├─────────────┼──────────┼─────────┼───────────┼───────────┤\n// │ private     │    ✅    │   ❌    │    ❌     │    ❌     │\n// │ (default)   │    ✅    │   ✅    │    ❌     │    ❌     │\n// │ protected   │    ✅    │   ✅    │    ✅     │    ❌     │\n// │ public      │    ✅    │   ✅    │    ✅     │    ✅     │\n// └─────────────┴──────────┴─────────┴───────────┴───────────┘' },
        { type: 'heading', value: 'Полный пример класса' },
        { type: 'code', language: 'java', value: 'public class Smartphone {\n    // Поля\n    String brand;\n    String model;\n    int batteryLevel;     // 0–100%\n    boolean isScreenOn;\n    int storageUsed;      // использованная память в МБ\n    int totalStorage;     // общая память в МБ\n\n    // Метод включения экрана\n    void turnScreenOn() {\n        if (batteryLevel > 0) {\n            isScreenOn = true;\n            batteryLevel -= 1;\n            System.out.println(brand + " " + model + ": экран включён");\n        } else {\n            System.out.println("Батарея разряжена!");\n        }\n    }\n\n    // Метод выключения экрана\n    void turnScreenOff() {\n        isScreenOn = false;\n        System.out.println(brand + " " + model + ": экран выключен");\n    }\n\n    // Метод зарядки\n    void charge(int percent) {\n        batteryLevel = Math.min(100, batteryLevel + percent);\n        System.out.println("Заряд: " + batteryLevel + "%");\n    }\n\n    // Метод установки приложения\n    boolean installApp(String appName, int sizeMB) {\n        if (storageUsed + sizeMB <= totalStorage) {\n            storageUsed += sizeMB;\n            System.out.println("Установлено: " + appName + " (" + sizeMB + " МБ)");\n            return true;\n        } else {\n            System.out.println("Недостаточно памяти для " + appName);\n            return false;\n        }\n    }\n\n    // Метод проверки свободной памяти\n    int freeStorage() {\n        return totalStorage - storageUsed;\n    }\n\n    // Вывод информации\n    void showStatus() {\n        System.out.println("--- " + brand + " " + model + " ---");\n        System.out.println("Батарея: " + batteryLevel + "%");\n        System.out.println("Экран: " + (isScreenOn ? "вкл" : "выкл"));\n        System.out.println("Память: " + storageUsed + "/" + totalStorage + " МБ");\n        System.out.println("Свободно: " + freeStorage() + " МБ");\n    }\n}' },
        { type: 'tip', value: 'Совет: начинай создание класса с вопросов. 1) Что этот объект ИМЕЕТ? (→ поля) 2) Что этот объект ДЕЛАЕТ? (→ методы). Ответил — и пиши код.' },
        { type: 'warning', value: 'Пока мы используем поля без модификаторов (default access). В реальных проектах поля почти всегда private, а доступ к ним через getter/setter методы. Это мы подробно разберём в модуле про инкапсуляцию.' }
      ]
    },
    {
      id: 4,
      title: 'Создание объекта (new)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Класс — это чертёж, но чтобы реально работать с данными, нужно создать объект. Для этого в Java используется оператор new. Он выделяет память, создаёт объект и возвращает ссылку на него.' },
        { type: 'heading', value: 'Синтаксис создания объекта' },
        { type: 'code', language: 'java', value: '// Полный синтаксис:\nИмяКласса имяПеременной = new ИмяКласса();\n\n// Пошаговый разбор:\n//   Car     — тип переменной (какой класс)\n//   myCar   — имя переменной (ссылка на объект)\n//   =       — оператор присваивания\n//   new     — ключевое слово создания объекта\n//   Car()   — вызов конструктора\n\nCar myCar = new Car();' },
        { type: 'heading', value: 'Что происходит при вызове new' },
        { type: 'text', value: 'Когда ты пишешь new Car(), происходит следующее:' },
        { type: 'list', items: [
          'Шаг 1: Java выделяет память в области HEAP (куча) для нового объекта Car',
          'Шаг 2: Все поля объекта получают значения по умолчанию (0, null, false)',
          'Шаг 3: Вызывается конструктор (инициализация объекта)',
          'Шаг 4: Оператор new возвращает ССЫЛКУ (адрес в памяти) на созданный объект',
          'Шаг 5: Ссылка сохраняется в переменной myCar'
        ]},
        { type: 'code', language: 'java', value: '// Визуализация памяти:\n//\n// СТЕК (Stack)              КУЧА (Heap)\n// ┌──────────┐              ┌──────────────────┐\n// │ myCar ───┼──────────►   │ Объект Car       │\n// │ (ссылка) │              │ brand: null       │\n// └──────────┘              │ color: null       │\n//                           │ speed: 0          │\n//                           │ year: 0           │\n//                           └──────────────────┘\n//\n// Переменная myCar хранит НЕ сам объект,\n// а только адрес (ссылку) на него в куче.' },
        { type: 'heading', value: 'Полный пример: создаём и используем объект' },
        { type: 'code', language: 'java', value: 'public class Car {\n    String brand;\n    String color;\n    int speed;\n    int year;\n\n    void accelerate() {\n        speed += 10;\n        System.out.println(brand + " разгоняется! Скорость: " + speed);\n    }\n\n    void brake() {\n        speed = Math.max(0, speed - 10);\n        System.out.println(brand + " тормозит. Скорость: " + speed);\n    }\n\n    void honk() {\n        System.out.println(brand + " сигналит: Бип-бип!");\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        // 1. Создаём объект\n        Car myCar = new Car();\n\n        // 2. Устанавливаем значения полей через точку\n        myCar.brand = "Toyota";\n        myCar.color = "Красный";\n        myCar.speed = 0;\n        myCar.year = 2023;\n\n        // 3. Вызываем методы через точку\n        System.out.println("Марка: " + myCar.brand);  // Toyota\n        System.out.println("Цвет: " + myCar.color);   // Красный\n        myCar.honk();        // Toyota сигналит: Бип-бип!\n        myCar.accelerate();  // Toyota разгоняется! Скорость: 10\n        myCar.accelerate();  // Toyota разгоняется! Скорость: 20\n        myCar.brake();       // Toyota тормозит. Скорость: 10\n    }\n}' },
        { type: 'heading', value: 'Ссылки и null' },
        { type: 'text', value: 'Переменная объектного типа хранит не сам объект, а ссылку (адрес в памяти). Если переменная не указывает ни на какой объект, её значение — null.' },
        { type: 'code', language: 'java', value: '// Объявление переменной без создания объекта\nCar car1;          // car1 содержит мусор (неинициализирована)\nCar car2 = null;   // car2 явно указывает в "никуда"\n\n// Попытка использовать null — ОШИБКА!\n// car2.brand = "BMW"; // NullPointerException!\n\n// Правильно: сначала создаём объект\ncar2 = new Car();\ncar2.brand = "BMW"; // теперь работает!\n\n// Проверка на null\nif (car2 != null) {\n    System.out.println(car2.brand); // BMW\n}' },
        { type: 'heading', value: 'Две ссылки на один объект' },
        { type: 'code', language: 'java', value: 'Car original = new Car();\noriginal.brand = "Tesla";\noriginal.speed = 50;\n\n// copy НЕ создаёт новый объект!\n// Оба указывают на ОДИН и тот же объект в памяти\nCar copy = original;\n\ncopy.speed = 100;\n\n// Изменение через copy видно и через original!\nSystem.out.println(original.speed); // 100 (а не 50!)\nSystem.out.println(copy.speed);     // 100\n\n// Проверка: это один и тот же объект?\nSystem.out.println(original == copy); // true\n\n// Визуализация:\n// original ───►  ┌────────────┐\n//                │ Объект Car │\n//                │ brand: Tesla│\n// copy     ───►  │ speed: 100  │\n//                └────────────┘\n// Два указателя на один объект!' },
        { type: 'warning', value: 'NullPointerException — самая частая ошибка в Java! Она возникает при попытке вызвать метод или обратиться к полю через null-ссылку. Всегда проверяй, создан ли объект, прежде чем его использовать.' },
        { type: 'tip', value: 'Запомни формулу: ТипКласса имя = new ТипКласса(); Это как "создать новый объект типа X и сохранить ссылку на него в переменную". По одному классу можно создать сколько угодно объектов.' }
      ]
    },
    {
      id: 5,
      title: 'Поля объекта: состояние',
      type: 'theory',
      content: [
        { type: 'text', value: 'Поля (fields) — это переменные, объявленные внутри класса. Они описывают состояние объекта: какие данные он хранит. Каждый объект имеет свою собственную копию полей — изменение поля одного объекта не влияет на другой.' },
        { type: 'tip', value: 'Поля — это как графы в анкете. В бланке анкеты написано: "Имя: ___", "Возраст: ___". Бланк один, но каждый человек заполняет свою анкету разными данными. Так и в классе: поля одни, но у каждого объекта свои значения.' },
        { type: 'heading', value: 'Значения полей по умолчанию' },
        { type: 'text', value: 'Когда объект создаётся через new, все поля автоматически получают значения по умолчанию. Не нужно их инициализировать, если устраивают эти значения.' },
        { type: 'code', language: 'java', value: 'public class DefaultValues {\n    // Числовые типы — 0\n    byte b;       // 0\n    short s;      // 0\n    int i;        // 0\n    long l;       // 0L\n    float f;      // 0.0f\n    double d;     // 0.0\n\n    // Символ — пустой символ\n    char c;       // \'\\u0000\' (символ с кодом 0)\n\n    // Логический — false\n    boolean flag; // false\n\n    // Ссылочные типы — null\n    String text;  // null\n    int[] array;  // null\n    Object obj;   // null\n}\n\nDefaultValues dv = new DefaultValues();\nSystem.out.println(dv.i);    // 0\nSystem.out.println(dv.d);    // 0.0\nSystem.out.println(dv.flag); // false\nSystem.out.println(dv.text); // null' },
        { type: 'heading', value: 'Инициализация полей при объявлении' },
        { type: 'code', language: 'java', value: 'public class GameCharacter {\n    // Можно задать начальные значения прямо при объявлении\n    String name = "Безымянный герой";\n    int health = 100;       // полное здоровье\n    int maxHealth = 100;\n    int attack = 10;\n    int defense = 5;\n    int level = 1;\n    int experience = 0;\n    boolean isAlive = true;\n    String weapon = "Деревянный меч";\n\n    void showStats() {\n        System.out.println("=== " + name + " ===" );\n        System.out.println("HP: " + health + "/" + maxHealth);\n        System.out.println("Атака: " + attack + " | Защита: " + defense);\n        System.out.println("Уровень: " + level + " | Опыт: " + experience);\n        System.out.println("Оружие: " + weapon);\n    }\n}\n\nGameCharacter hero = new GameCharacter();\nhero.showStats();\n// === Безымянный герой ===\n// HP: 100/100\n// Атака: 10 | Защита: 5\n// Уровень: 1 | Опыт: 0\n// Оружие: Деревянный меч\n\n// Переопределяем имя и оружие\nhero.name = "Артас";\nhero.weapon = "Ледяная Скорбь";\nhero.attack = 50;\nhero.showStats();\n// === Артас ===\n// HP: 100/100\n// Атака: 50 | Защита: 5\n// ...' },
        { type: 'heading', value: 'Каждый объект имеет свои поля' },
        { type: 'code', language: 'java', value: 'public class Cat {\n    String name;\n    String color;\n    int age;\n    boolean isHungry = true; // все коты по умолчанию голодные :)\n\n    void feed() {\n        isHungry = false;\n        System.out.println(name + " покормлен!");\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Cat cat1 = new Cat();\n        cat1.name = "Мурзик";\n        cat1.color = "Рыжий";\n        cat1.age = 3;\n\n        Cat cat2 = new Cat();\n        cat2.name = "Барсик";\n        cat2.color = "Серый";\n        cat2.age = 5;\n\n        // Кормим только Мурзика\n        cat1.feed(); // Мурзик покормлен!\n\n        // Мурзик сыт, Барсик ещё голоден!\n        System.out.println(cat1.name + " голоден? " + cat1.isHungry); // false\n        System.out.println(cat2.name + " голоден? " + cat2.isHungry); // true\n        // Объекты независимы!\n    }\n}' },
        { type: 'heading', value: 'Поля с модификатором final' },
        { type: 'text', value: 'Если поле объявлено как final, его значение можно установить только один раз — при объявлении или в конструкторе. После этого изменить его нельзя.' },
        { type: 'code', language: 'java', value: 'public class Circle {\n    final double PI = 3.14159265;  // константа, нельзя изменить\n    double radius;\n\n    double area() {\n        return PI * radius * radius;\n    }\n}\n\nCircle c = new Circle();\nc.radius = 5;\nSystem.out.println(c.area()); // 78.5398...\n\n// c.PI = 3.0; // ОШИБКА КОМПИЛЯЦИИ! final поле нельзя изменить!' },
        { type: 'code', language: 'java', value: '// final поле можно инициализировать в конструкторе\npublic class Person {\n    final String id;     // уникальный ID, задаётся при создании\n    String name;\n    int age;\n\n    public Person(String id, String name, int age) {\n        this.id = id;    // OK — первое и единственное присваивание\n        this.name = name;\n        this.age = age;\n    }\n}\n\nPerson p = new Person("KZ-001", "Алия", 20);\nSystem.out.println(p.id);   // KZ-001\n// p.id = "KZ-002";         // ОШИБКА! final поле уже инициализировано' },
        { type: 'warning', value: 'Не путай final поле объекта с локальной переменной! Поля класса получают значения по умолчанию автоматически, а final поле обязательно должно быть инициализировано — при объявлении или в каждом конструкторе.' },
        { type: 'note', value: 'Хорошая практика: если значение поля не должно меняться после создания объекта (например, ID, дата рождения), объявляй его как final. Это защищает от случайных изменений.' }
      ]
    },
    {
      id: 6,
      title: 'Методы объекта: поведение',
      type: 'theory',
      content: [
        { type: 'text', value: 'Методы описывают, что объект умеет делать — его поведение. Метод может читать и изменять поля объекта, принимать параметры, выполнять вычисления и возвращать результат.' },
        { type: 'tip', value: 'Если поля — это "прилагательные" объекта (красный, большой, быстрый), то методы — это "глаголы" (ехать, есть, считать). Вместе они полностью описывают объект.' },
        { type: 'heading', value: 'Структура метода' },
        { type: 'code', language: 'java', value: '// Синтаксис метода:\n// [модификатор] возвращаемыйТип имяМетода(параметры) {\n//     тело метода\n//     return значение; // если есть возвращаемый тип\n// }\n\npublic class Example {\n\n    // void — метод ничего не возвращает\n    void sayHello() {\n        System.out.println("Привет!");\n    }\n\n    // int — метод возвращает целое число\n    int add(int a, int b) {\n        return a + b;\n    }\n\n    // String — метод возвращает строку\n    String greet(String name) {\n        return "Привет, " + name + "!";\n    }\n\n    // boolean — метод возвращает true/false\n    boolean isAdult(int age) {\n        return age >= 18;\n    }\n}' },
        { type: 'heading', value: 'Метод без параметров' },
        { type: 'code', language: 'java', value: 'public class Lamp {\n    boolean isOn = false;\n    int brightness = 0; // 0-100%\n\n    // Метод без параметров — включить\n    void turnOn() {\n        isOn = true;\n        brightness = 100;\n        System.out.println("Лампа включена на " + brightness + "%");\n    }\n\n    // Метод без параметров — выключить\n    void turnOff() {\n        isOn = false;\n        brightness = 0;\n        System.out.println("Лампа выключена");\n    }\n\n    // Метод без параметров — показать статус\n    void status() {\n        if (isOn) {\n            System.out.println("Лампа горит, яркость: " + brightness + "%");\n        } else {\n            System.out.println("Лампа выключена");\n        }\n    }\n}\n\nLamp lamp = new Lamp();\nlamp.status();  // Лампа выключена\nlamp.turnOn();  // Лампа включена на 100%\nlamp.status();  // Лампа горит, яркость: 100%\nlamp.turnOff(); // Лампа выключена' },
        { type: 'heading', value: 'Метод с параметрами' },
        { type: 'text', value: 'Параметры — это входные данные для метода. Они указываются в круглых скобках при объявлении метода.' },
        { type: 'code', language: 'java', value: 'public class Lamp {\n    boolean isOn = false;\n    int brightness = 0;\n\n    void turnOn() {\n        isOn = true;\n        brightness = 100;\n    }\n\n    // Метод с одним параметром\n    void setBrightness(int level) {\n        if (level >= 0 && level <= 100) {\n            brightness = level;\n            isOn = brightness > 0;\n            System.out.println("Яркость установлена: " + brightness + "%");\n        } else {\n            System.out.println("Ошибка: яркость должна быть 0–100!");\n        }\n    }\n\n    // Метод с несколькими параметрами\n    void configure(int level, boolean flash) {\n        setBrightness(level);\n        if (flash) {\n            System.out.println("Режим мигания включён!");\n        }\n    }\n}\n\nLamp lamp = new Lamp();\nlamp.setBrightness(75);     // Яркость установлена: 75%\nlamp.setBrightness(150);    // Ошибка: яркость должна быть 0–100!\nlamp.configure(50, true);   // Яркость установлена: 50%\n                            // Режим мигания включён!' },
        { type: 'heading', value: 'Метод с return' },
        { type: 'code', language: 'java', value: 'public class TemperatureConverter {\n    // Метод возвращает double — результат вычисления\n    double celsiusToFahrenheit(double celsius) {\n        return celsius * 9.0 / 5.0 + 32;\n    }\n\n    double fahrenheitToCelsius(double fahrenheit) {\n        return (fahrenheit - 32) * 5.0 / 9.0;\n    }\n\n    // Метод возвращает String — описание температуры\n    String describe(double celsius) {\n        if (celsius < 0) return "Мороз";\n        if (celsius < 10) return "Холодно";\n        if (celsius < 20) return "Прохладно";\n        if (celsius < 30) return "Тепло";\n        return "Жарко";\n    }\n}\n\nTemperatureConverter tc = new TemperatureConverter();\ndouble f = tc.celsiusToFahrenheit(36.6);\nSystem.out.println("36.6°C = " + f + "°F");  // 97.88°F\n\nString desc = tc.describe(25);\nSystem.out.println("25°C — " + desc);  // Тепло\n\n// Результат метода можно использовать прямо в выражении\nSystem.out.println("0°C = " + tc.celsiusToFahrenheit(0) + "°F"); // 32.0°F' },
        { type: 'heading', value: 'Методы, изменяющие состояние объекта' },
        { type: 'code', language: 'java', value: 'public class BankAccount {\n    String owner;\n    double balance = 0;\n    int transactionCount = 0;\n\n    void deposit(double amount) {\n        if (amount > 0) {\n            balance += amount;\n            transactionCount++;\n            System.out.println("+" + amount + " тг. Баланс: " + balance);\n        } else {\n            System.out.println("Сумма должна быть положительной!");\n        }\n    }\n\n    boolean withdraw(double amount) {\n        if (amount > 0 && amount <= balance) {\n            balance -= amount;\n            transactionCount++;\n            System.out.println("-" + amount + " тг. Баланс: " + balance);\n            return true;  // успешно\n        }\n        System.out.println("Невозможно снять " + amount + " тг!");\n        return false;  // неудача\n    }\n\n    void printStatement() {\n        System.out.println("=== Выписка ===" );\n        System.out.println("Владелец: " + owner);\n        System.out.println("Баланс: " + balance + " тг");\n        System.out.println("Транзакций: " + transactionCount);\n    }\n}\n\nBankAccount acc = new BankAccount();\nacc.owner = "Нурдаулет";\nacc.deposit(50000);            // +50000.0 тг. Баланс: 50000.0\nacc.deposit(25000);            // +25000.0 тг. Баланс: 75000.0\nboolean ok = acc.withdraw(10000); // -10000.0 тг. Баланс: 65000.0\nSystem.out.println("Успешно: " + ok); // true\nacc.withdraw(100000);          // Невозможно снять 100000.0 тг!\nacc.printStatement();\n// === Выписка ===\n// Владелец: Нурдаулет\n// Баланс: 65000.0 тг\n// Транзакций: 3' },
        { type: 'warning', value: 'Если метод объявлен с возвращаемым типом (int, String, boolean...), он ОБЯЗАН вернуть значение через return во всех ветках кода. Иначе код не скомпилируется.' },
        { type: 'note', value: 'Метод может вызывать другие методы того же объекта. Например, метод printStatement() мог бы вызвать внутри себя другой метод getFormattedBalance().' }
      ]
    },
    {
      id: 7,
      title: 'Методы с возвратом vs void',
      type: 'theory',
      content: [
        { type: 'text', value: 'Методы бывают двух основных видов: void (ничего не возвращают — просто делают действие) и с возвращаемым значением (return). Понимание разницы — ключ к правильному проектированию классов.' },
        { type: 'heading', value: 'void — метод-действие' },
        { type: 'text', value: 'void-метод выполняет действие, но не возвращает результат. Как кнопка "Пуск" — нажал и всё, ничего обратно не получаешь.' },
        { type: 'code', language: 'java', value: 'public class Printer {\n    String modelName;\n    int paperLeft = 500; // листов бумаги\n\n    // void — просто выполняет действие\n    void printDocument(String text) {\n        if (paperLeft > 0) {\n            System.out.println("Печатаю: " + text);\n            paperLeft--;\n        } else {\n            System.out.println("Нет бумаги!");\n        }\n    }\n\n    void loadPaper(int sheets) {\n        paperLeft += sheets;\n        System.out.println("Загружено " + sheets + " листов. Всего: " + paperLeft);\n    }\n\n    // void можно использовать для вывода\n    void showStatus() {\n        System.out.println("Принтер: " + modelName + ", бумаги: " + paperLeft);\n    }\n}\n\nPrinter p = new Printer();\np.modelName = "HP LaserJet";\np.printDocument("Отчёт.pdf"); // Печатаю: Отчёт.pdf\np.showStatus();               // Принтер: HP LaserJet, бумаги: 499\n\n// Нельзя сохранить результат void-метода!\n// String result = p.printDocument("test"); // ОШИБКА КОМПИЛЯЦИИ!' },
        { type: 'heading', value: 'return — метод-вычисление' },
        { type: 'text', value: 'Метод с return вычисляет и возвращает значение. Как калькулятор: вводишь числа, получаешь результат.' },
        { type: 'code', language: 'java', value: 'public class MathHelper {\n    // Возвращает int\n    int square(int n) {\n        return n * n;\n    }\n\n    // Возвращает double\n    double average(double a, double b, double c) {\n        return (a + b + c) / 3.0;\n    }\n\n    // Возвращает boolean\n    boolean isPrime(int n) {\n        if (n < 2) return false;\n        for (int i = 2; i <= Math.sqrt(n); i++) {\n            if (n % i == 0) return false;\n        }\n        return true;\n    }\n\n    // Возвращает String\n    String repeat(String text, int times) {\n        String result = "";\n        for (int i = 0; i < times; i++) {\n            result += text;\n        }\n        return result;\n    }\n}\n\nMathHelper math = new MathHelper();\n\n// Результат можно сохранить в переменную\nint sq = math.square(5);  // 25\nSystem.out.println("5² = " + sq);\n\n// Или использовать прямо в выражении\nSystem.out.println("Среднее: " + math.average(4, 5, 6)); // 5.0\n\n// Или в условии\nif (math.isPrime(17)) {\n    System.out.println("17 — простое число"); // выведет\n}\n\n// Или в другом вызове\nSystem.out.println(math.repeat("Ура! ", 3)); // Ура! Ура! Ура!' },
        { type: 'heading', value: 'Когда использовать void, а когда return?' },
        { type: 'code', language: 'java', value: '// ПРАВИЛО:\n// void  — когда метод ДЕЛАЕТ что-то (побочный эффект)\n// return — когда метод ВЫЧИСЛЯЕТ что-то (чистый результат)\n\npublic class ShoppingCart {\n    String[] items = new String[100];\n    double[] prices = new double[100];\n    int count = 0;\n\n    // void — ДОБАВЛЯЕТ товар (действие, побочный эффект)\n    void addItem(String item, double price) {\n        items[count] = item;\n        prices[count] = price;\n        count++;\n        System.out.println("Добавлено: " + item);\n    }\n\n    // void — УДАЛЯЕТ товар (действие)\n    void removeLastItem() {\n        if (count > 0) {\n            count--;\n            System.out.println("Удалено: " + items[count]);\n        }\n    }\n\n    // void — ВЫВОДИТ на экран (действие)\n    void printCart() {\n        System.out.println("=== Корзина ===" );\n        for (int i = 0; i < count; i++) {\n            System.out.println((i + 1) + ". " + items[i] + " — " + prices[i] + " тг");\n        }\n    }\n\n    // return double — ВЫЧИСЛЯЕТ общую сумму\n    double getTotal() {\n        double total = 0;\n        for (int i = 0; i < count; i++) {\n            total += prices[i];\n        }\n        return total;\n    }\n\n    // return int — ВЫЧИСЛЯЕТ количество товаров\n    int getItemCount() {\n        return count;\n    }\n\n    // return boolean — ПРОВЕРЯЕТ, пуста ли корзина\n    boolean isEmpty() {\n        return count == 0;\n    }\n\n    // return String — ВЫЧИСЛЯЕТ самый дорогой товар\n    String getMostExpensive() {\n        if (count == 0) return "Корзина пуста";\n        int maxIndex = 0;\n        for (int i = 1; i < count; i++) {\n            if (prices[i] > prices[maxIndex]) {\n                maxIndex = i;\n            }\n        }\n        return items[maxIndex] + " (" + prices[maxIndex] + " тг)";\n    }\n}' },
        { type: 'heading', value: 'Использование в связке' },
        { type: 'code', language: 'java', value: 'ShoppingCart cart = new ShoppingCart();\n\ncart.addItem("Ноутбук", 350000);   // void — действие\ncart.addItem("Мышка", 5000);        // void — действие\ncart.addItem("Клавиатура", 15000);  // void — действие\n\ncart.printCart();                    // void — действие вывода\n\n// Используем методы с return\nSystem.out.println("Товаров: " + cart.getItemCount());   // 3\nSystem.out.println("Итого: " + cart.getTotal() + " тг"); // 370000.0 тг\nSystem.out.println("Самое дорогое: " + cart.getMostExpensive());\n// Самое дорогое: Ноутбук (350000.0 тг)\n\nif (!cart.isEmpty()) {\n    double total = cart.getTotal();\n    System.out.println("Со скидкой 10%: " + (total * 0.9) + " тг");\n}' },
        { type: 'tip', value: 'Совет для новичков: если после вызова метода тебе нужно использовать результат (сохранить в переменную, передать в if, подставить в формулу) — используй return. Если метод просто что-то делает (печатает, изменяет состояние) — используй void.' },
        { type: 'warning', value: 'Частая ошибка: написать System.out.println() внутри метода вместо return. Метод, который выводит строку на экран, НЕ возвращает её! println и return — разные вещи.' }
      ]
    },
    {
      id: 8,
      title: 'Несколько объектов одного класса',
      type: 'theory',
      content: [
        { type: 'text', value: 'Главная сила классов — возможность создавать сколько угодно объектов по одному шаблону. Объекты можно хранить в массивах, списках, передавать в методы и заставлять взаимодействовать друг с другом.' },
        { type: 'heading', value: 'Несколько независимых объектов' },
        { type: 'code', language: 'java', value: 'public class Player {\n    String name;\n    int health = 100;\n    int score = 0;\n    String weapon = "Кулаки";\n\n    void takeDamage(int damage) {\n        health -= damage;\n        if (health < 0) health = 0;\n        System.out.println(name + " получил урон! HP: " + health);\n    }\n\n    void addScore(int points) {\n        score += points;\n        System.out.println(name + " +\" + points + " очков! Всего: " + score);\n    }\n\n    boolean isAlive() {\n        return health > 0;\n    }\n\n    void showInfo() {\n        System.out.println(name + " | HP: " + health +\n            " | Очки: " + score + " | Оружие: " + weapon);\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Player p1 = new Player();\n        p1.name = "Воин";\n        p1.weapon = "Меч";\n\n        Player p2 = new Player();\n        p2.name = "Маг";\n        p2.weapon = "Посох";\n\n        Player p3 = new Player();\n        p3.name = "Лучник";\n        p3.weapon = "Лук";\n\n        // Каждый объект живёт своей жизнью\n        p1.takeDamage(30);   // Воин получил урон! HP: 70\n        p2.addScore(100);    // Маг +100 очков! Всего: 100\n        p3.takeDamage(10);   // Лучник получил урон! HP: 90\n\n        p1.showInfo(); // Воин | HP: 70 | Очки: 0 | Оружие: Меч\n        p2.showInfo(); // Маг | HP: 100 | Очки: 100 | Оружие: Посох\n        p3.showInfo(); // Лучник | HP: 90 | Очки: 0 | Оружие: Лук\n    }\n}' },
        { type: 'heading', value: 'Массив объектов' },
        { type: 'code', language: 'java', value: 'public class Student {\n    String name;\n    double gpa;\n\n    void showInfo() {\n        System.out.println(name + " — GPA: " + gpa);\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        // Создаём массив из 5 студентов\n        Student[] students = new Student[5];\n\n        // ВАЖНО: массив создан, но объектов внутри ещё нет!\n        // students[0] == null, students[1] == null ...\n        // Нужно создать каждый объект отдельно:\n\n        students[0] = new Student();\n        students[0].name = "Алия";\n        students[0].gpa = 4.8;\n\n        students[1] = new Student();\n        students[1].name = "Берик";\n        students[1].gpa = 3.5;\n\n        students[2] = new Student();\n        students[2].name = "Гаухар";\n        students[2].gpa = 4.2;\n\n        students[3] = new Student();\n        students[3].name = "Данияр";\n        students[3].gpa = 3.9;\n\n        students[4] = new Student();\n        students[4].name = "Елена";\n        students[4].gpa = 4.6;\n\n        // Перебираем и выводим всех\n        System.out.println("=== Все студенты ===" );\n        for (Student s : students) {\n            s.showInfo();\n        }\n\n        // Находим студента с максимальным GPA\n        Student best = students[0];\n        for (int i = 1; i < students.length; i++) {\n            if (students[i].gpa > best.gpa) {\n                best = students[i];\n            }\n        }\n        System.out.println("\\nЛучший студент: " + best.name + " (GPA: " + best.gpa + ")");\n    }\n}' },
        { type: 'heading', value: 'ArrayList объектов (удобнее массива)' },
        { type: 'code', language: 'java', value: 'import java.util.ArrayList;\n\npublic class Contact {\n    String name;\n    String phone;\n    String email;\n\n    void show() {\n        System.out.println(name + " | " + phone + " | " + email);\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        // ArrayList — динамический список, не нужно знать размер заранее\n        ArrayList<Contact> contacts = new ArrayList<>();\n\n        Contact c1 = new Contact();\n        c1.name = "Алия";\n        c1.phone = "+7 777 111 22 33";\n        c1.email = "aliya@mail.kz";\n\n        Contact c2 = new Contact();\n        c2.name = "Берик";\n        c2.phone = "+7 701 444 55 66";\n        c2.email = "berik@mail.kz";\n\n        contacts.add(c1); // добавляем в список\n        contacts.add(c2);\n\n        // Выводим все контакты\n        System.out.println("=== Контакты (" + contacts.size() + ") ===" );\n        for (Contact c : contacts) {\n            c.show();\n        }\n\n        // Поиск по имени\n        String searchName = "Алия";\n        for (Contact c : contacts) {\n            if (c.name.equals(searchName)) {\n                System.out.println("Найден: " + c.phone);\n            }\n        }\n    }\n}' },
        { type: 'heading', value: 'Объекты взаимодействуют друг с другом' },
        { type: 'code', language: 'java', value: 'public class Player {\n    String name;\n    int health = 100;\n    int attackPower = 20;\n\n    // Объект атакует ДРУГОЙ объект!\n    void attack(Player target) {\n        System.out.println(name + " атакует " + target.name + "!");\n        target.takeDamage(attackPower);\n    }\n\n    void takeDamage(int damage) {\n        health -= damage;\n        if (health < 0) health = 0;\n        System.out.println(name + ": HP = " + health);\n        if (!isAlive()) {\n            System.out.println(name + " повержен!");\n        }\n    }\n\n    boolean isAlive() {\n        return health > 0;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Player knight = new Player();\n        knight.name = "Рыцарь";\n        knight.attackPower = 30;\n\n        Player dragon = new Player();\n        dragon.name = "Дракон";\n        dragon.health = 200;\n        dragon.attackPower = 50;\n\n        // Бой!\n        knight.attack(dragon); // Рыцарь атакует Дракона! Дракон: HP = 170\n        dragon.attack(knight); // Дракон атакует Рыцаря! Рыцарь: HP = 50\n        knight.attack(dragon); // Рыцарь атакует Дракона! Дракон: HP = 140\n        dragon.attack(knight); // Дракон атакует Рыцаря! Рыцарь: HP = 0\n                               // Рыцарь повержен!\n    }\n}' },
        { type: 'warning', value: 'Частая ошибка с массивами объектов: Student[] arr = new Student[10]; создаёт массив из 10 null-ячеек, а НЕ 10 объектов Student! Каждый объект нужно создавать через new отдельно: arr[0] = new Student();' },
        { type: 'tip', value: 'Когда объекты одного класса взаимодействуют — передаются как параметры методов. Метод attack(Player target) принимает другого игрока. Это основа ООП: объекты "общаются" через вызовы методов.' }
      ]
    },
    {
      id: 9,
      title: 'this — ссылка на текущий объект',
      type: 'theory',
      content: [
        { type: 'text', value: 'this — это специальное ключевое слово, которое ссылается на текущий объект. Тот самый объект, у которого был вызван метод. Это как слово "я" для объекта.' },
        { type: 'tip', value: 'Представь группу людей. Каждый может сказать "я" — и это будет ссылка на того, кто говорит. Если Алия говорит "я", это означает Алию. Если Берик — то Берика. Так и this: какой объект вызвал метод, тот и "this".' },
        { type: 'heading', value: 'Зачем нужен this?' },
        { type: 'text', value: 'Главная задача this — разрешать конфликты имён, когда параметр метода называется так же, как поле класса.' },
        { type: 'code', language: 'java', value: 'public class Person {\n    String name;\n    int age;\n\n    // БЕЗ this — ОШИБКА ЛОГИКИ!\n    void setNameBad(String name) {\n        name = name; // параметр = параметр (поле НЕ меняется!)\n        // Java думает: name — это параметр, и присваивает его самому себе\n    }\n\n    // С this — ПРАВИЛЬНО!\n    void setName(String name) {\n        this.name = name; // this.name = поле класса, name = параметр\n        // "МОЁ имя (this.name) теперь равно значению параметра (name)"\n    }\n\n    void setAge(int age) {\n        this.age = age; // this.age = поле, age = параметр\n    }\n}\n\nPerson p = new Person();\np.setNameBad("Алия");\nSystem.out.println(p.name); // null! Поле не было изменено!\n\np.setName("Алия");\nSystem.out.println(p.name); // Алия — теперь работает!' },
        { type: 'heading', value: 'this в конструкторе' },
        { type: 'code', language: 'java', value: 'public class Car {\n    String brand;\n    String color;\n    int year;\n    int speed;\n\n    // Параметры совпадают с полями — нужен this\n    public Car(String brand, String color, int year) {\n        this.brand = brand; // поле = параметр\n        this.color = color; // поле = параметр\n        this.year = year;   // поле = параметр\n        this.speed = 0;     // this необязателен здесь (нет конфликта)\n        // но можно писать для единообразия\n    }\n}\n\nCar car = new Car("Toyota", "Красный", 2023);\nSystem.out.println(car.brand); // Toyota' },
        { type: 'heading', value: 'this в обычных методах' },
        { type: 'code', language: 'java', value: 'public class Counter {\n    int value = 0;\n\n    void increment() {\n        this.value++; // this необязателен, но можно\n        // то же самое: value++;\n    }\n\n    // this можно использовать для вызова других методов\n    void incrementAndPrint() {\n        this.increment(); // вызываем свой метод\n        System.out.println("Значение: " + this.value);\n    }\n\n    // this для сравнения с другим объектом\n    boolean isGreaterThan(Counter other) {\n        return this.value > other.value;\n        // this.value — значение ЭТОГО счётчика\n        // other.value — значение ДРУГОГО счётчика\n    }\n}\n\nCounter c1 = new Counter();\nc1.value = 10;\n\nCounter c2 = new Counter();\nc2.value = 5;\n\nSystem.out.println(c1.isGreaterThan(c2)); // true\n// Когда вызван c1.isGreaterThan(c2):\n// this = c1, other = c2\n// this.value (10) > other.value (5) = true' },
        { type: 'heading', value: 'this для цепочки вызовов (method chaining)' },
        { type: 'code', language: 'java', value: 'public class MessageBuilder {\n    String message = "";\n\n    // Возвращаем this — можно вызывать методы цепочкой!\n    MessageBuilder add(String text) {\n        message += text;\n        return this; // возвращаем текущий объект\n    }\n\n    MessageBuilder addLine(String text) {\n        message += text + "\\n";\n        return this;\n    }\n\n    MessageBuilder addSeparator() {\n        message += "---\\n";\n        return this;\n    }\n\n    void print() {\n        System.out.println(message);\n    }\n}\n\n// Цепочка вызовов — каждый метод возвращает this\nnew MessageBuilder()\n    .addLine("Привет!")\n    .addSeparator()\n    .addLine("Это цепочка вызовов.")\n    .addLine("Каждый метод возвращает this.")\n    .addSeparator()\n    .add("Конец.")\n    .print();\n\n// Вывод:\n// Привет!\n// ---\n// Это цепочка вызовов.\n// Каждый метод возвращает this.\n// ---\n// Конец.' },
        { type: 'heading', value: 'Как работает this: визуализация' },
        { type: 'code', language: 'java', value: 'Person aliya = new Person();\naliya.setName("Алия");\n// Внутри setName:\n// this = aliya (объект, у которого вызван метод)\n// this.name = "Алия"  →  aliya.name = "Алия"\n\nPerson berik = new Person();\nberik.setName("Берик");\n// Внутри setName:\n// this = berik (другой объект!)\n// this.name = "Берик"  →  berik.name = "Берик"\n\n// this — это не конкретный объект.\n// Это "тот объект, который стоит перед точкой при вызове метода".' },
        { type: 'warning', value: 'Нельзя использовать this в static методах! Static методы принадлежат классу, а не объекту, поэтому у них нет "текущего объекта". О static — чуть позже в этом модуле.' },
        { type: 'note', value: 'В Java this необязателен, когда нет конфликта имён. Внутри метода можно писать просто name вместо this.name. Но многие разработчики предпочитают всегда писать this. для ясности — это вопрос стиля.' }
      ]
    },
    {
      id: 10,
      title: 'toString() — строковое представление объекта',
      type: 'theory',
      content: [
        { type: 'text', value: 'Метод toString() возвращает строковое представление объекта. Это один из самых полезных методов — он вызывается автоматически при печати объекта через println() или при конкатенации объекта со строкой.' },
        { type: 'heading', value: 'Проблема: что выводится без toString()?' },
        { type: 'code', language: 'java', value: 'public class Cat {\n    String name;\n    int age;\n}\n\nCat cat = new Cat();\ncat.name = "Мурзик";\ncat.age = 3;\n\nSystem.out.println(cat);\n// Вывод: Cat@7852e922\n// Это имя класса + @ + хеш-код в шестнадцатеричном формате\n// Абсолютно бесполезная информация!\n\nSystem.out.println("Мой кот: " + cat);\n// Мой кот: Cat@7852e922\n// Тоже бесполезно...' },
        { type: 'text', value: 'По умолчанию toString() (наследуется от класса Object) возвращает имя класса и адрес в памяти. Это не информативно — нужно переопределить метод.' },
        { type: 'heading', value: 'Добавляем toString()' },
        { type: 'code', language: 'java', value: 'public class Cat {\n    String name;\n    int age;\n    String color;\n\n    // @Override — аннотация, говорящая что мы переопределяем\n    // метод из родительского класса (Object)\n    @Override\n    public String toString() {\n        return "Cat{name=\'" + name + "\', age=" + age + ", color=\'" + color + "\'}";\n    }\n}\n\nCat cat = new Cat();\ncat.name = "Мурзик";\ncat.age = 3;\ncat.color = "Рыжий";\n\nSystem.out.println(cat);\n// Cat{name=\'Мурзик\', age=3, color=\'Рыжий\'}\n\n// toString() вызывается автоматически при:\n// 1. println(объект)\nSystem.out.println(cat); // автоматически вызывает cat.toString()\n\n// 2. Конкатенации со строкой\nString info = "Питомец: " + cat; // автоматически вызывает cat.toString()\nSystem.out.println(info);\n// Питомец: Cat{name=\'Мурзик\', age=3, color=\'Рыжий\'}\n\n// 3. Явный вызов\nString s = cat.toString();\nSystem.out.println(s);' },
        { type: 'heading', value: 'Разные стили toString()' },
        { type: 'code', language: 'java', value: 'public class Product {\n    String name;\n    double price;\n    int quantity;\n\n    // Стиль 1: Простое описание\n    @Override\n    public String toString() {\n        return name + " — " + price + " тг (кол-во: " + quantity + ")";\n    }\n    // "Ноутбук — 350000.0 тг (кол-во: 5)"\n}\n\npublic class Movie {\n    String title;\n    String director;\n    int year;\n    double rating;\n\n    // Стиль 2: Форматированный вывод\n    @Override\n    public String toString() {\n        return String.format("\"%s\" (%d) — реж. %s, рейтинг: %.1f/10",\n            title, year, director, rating);\n    }\n    // "\"Интерстеллар\" (2014) — реж. К. Нолан, рейтинг: 8.6/10"\n}\n\npublic class Coordinate {\n    double x;\n    double y;\n\n    // Стиль 3: Компактный\n    @Override\n    public String toString() {\n        return "(" + x + ", " + y + ")";\n    }\n    // "(3.5, 7.2)"\n}\n\npublic class Employee {\n    String name;\n    String department;\n    String position;\n    double salary;\n\n    // Стиль 4: Многострочный\n    @Override\n    public String toString() {\n        return "┌── Сотрудник ──────────\\n" +\n               "│ Имя: " + name + "\\n" +\n               "│ Отдел: " + department + "\\n" +\n               "│ Должность: " + position + "\\n" +\n               "│ Зарплата: " + salary + " тг\\n" +\n               "└──────────────────────";\n    }\n}' },
        { type: 'heading', value: 'toString() с массивом объектов' },
        { type: 'code', language: 'java', value: 'import java.util.ArrayList;\n\npublic class Book {\n    String title;\n    String author;\n    int year;\n\n    @Override\n    public String toString() {\n        return "\\"" + title + "\\" (" + author + ", " + year + ")";\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        ArrayList<Book> books = new ArrayList<>();\n\n        Book b1 = new Book();\n        b1.title = "Гарри Поттер";\n        b1.author = "Дж. Роулинг";\n        b1.year = 1997;\n        books.add(b1);\n\n        Book b2 = new Book();\n        b2.title = "Чистый код";\n        b2.author = "Р. Мартин";\n        b2.year = 2008;\n        books.add(b2);\n\n        // ArrayList сам вызывает toString() для каждого элемента!\n        System.out.println(books);\n        // ["Гарри Поттер" (Дж. Роулинг, 1997), "Чистый код" (Р. Мартин, 2008)]\n\n        // В цикле тоже работает\n        for (Book b : books) {\n            System.out.println("Книга: " + b); // toString() автоматически\n        }\n    }\n}' },
        { type: 'heading', value: 'Правила хорошего toString()' },
        { type: 'list', items: [
          'Включай ВСЕ важные поля объекта',
          'Используй @Override — это защита от опечаток (компилятор проверит)',
          'Метод должен быть public и возвращать String',
          'Не включай секретные данные (пароли, токены) в toString()',
          'Формат должен быть понятным и легко читаемым'
        ]},
        { type: 'warning', value: 'Не забудь @Override перед toString()! Без него, если ты случайно напишешь tostring() (с маленькой s), компилятор не скажет об ошибке — это будет просто новый метод, а не переопределение.' },
        { type: 'tip', value: 'В реальных проектах IDE (IntelliJ IDEA, Eclipse) умеют автоматически генерировать toString(). Но для обучения полезно писать его вручную, чтобы понимать механизм.' }
      ]
    },
    {
      id: 11,
      title: 'equals() и hashCode() — сравнение объектов',
      type: 'theory',
      content: [
        { type: 'text', value: 'В Java оператор == для объектов сравнивает не содержимое, а ссылки (адреса в памяти). Чтобы сравнивать объекты по содержимому, нужно переопределить метод equals().' },
        { type: 'heading', value: 'Проблема: == сравнивает ссылки, не содержимое' },
        { type: 'code', language: 'java', value: 'public class Point {\n    int x;\n    int y;\n}\n\nPoint p1 = new Point();\np1.x = 5;\np1.y = 10;\n\nPoint p2 = new Point();\np2.x = 5;\np2.y = 10;\n\n// Содержимое ОДИНАКОВОЕ, но...\nSystem.out.println(p1 == p2); // false!\n\n// Почему? Потому что == сравнивает ССЫЛКИ (адреса):\n// p1 → объект по адресу 0x001\n// p2 → объект по адресу 0x002\n// 0x001 != 0x002 → false\n\n// А если так?\nPoint p3 = p1; // p3 указывает на тот же объект, что и p1\nSystem.out.println(p1 == p3); // true! (одинаковый адрес)' },
        { type: 'tip', value: 'Аналогия: у тебя есть два одинаковых паспорта (одинаковые данные). Оператор == спрашивает: "Это ОДИН И ТОТ ЖЕ паспорт (один физический документ)?". Метод equals() спрашивает: "Данные в паспортах ОДИНАКОВЫЕ?"' },
        { type: 'heading', value: 'Переопределяем equals()' },
        { type: 'code', language: 'java', value: 'public class Point {\n    int x;\n    int y;\n\n    @Override\n    public boolean equals(Object obj) {\n        // 1. Если это тот же самый объект — true\n        if (this == obj) return true;\n\n        // 2. Если передали null или объект другого класса — false\n        if (obj == null || getClass() != obj.getClass()) return false;\n\n        // 3. Приводим тип и сравниваем поля\n        Point other = (Point) obj;\n        return this.x == other.x && this.y == other.y;\n    }\n\n    @Override\n    public String toString() {\n        return "(" + x + ", " + y + ")";\n    }\n}\n\nPoint p1 = new Point();\np1.x = 5;\np1.y = 10;\n\nPoint p2 = new Point();\np2.x = 5;\np2.y = 10;\n\nPoint p3 = new Point();\np3.x = 1;\np3.y = 2;\n\nSystem.out.println(p1.equals(p2)); // true — содержимое одинаковое!\nSystem.out.println(p1.equals(p3)); // false — содержимое разное\nSystem.out.println(p1 == p2);      // false — разные объекты в памяти' },
        { type: 'heading', value: 'Пошаговый разбор equals()' },
        { type: 'code', language: 'java', value: '@Override\npublic boolean equals(Object obj) {\n    // ШАГ 1: Проверка на идентичность\n    // Если obj — это тот же самый объект в памяти, сразу true\n    if (this == obj) return true;\n\n    // ШАГ 2: Проверка на null и тип\n    // Если obj == null — нельзя быть равным null\n    // Если obj другого класса — нельзя сравнивать Point с String\n    if (obj == null || getClass() != obj.getClass()) return false;\n\n    // ШАГ 3: Приведение типа\n    // Мы уже знаем, что obj — это Point, поэтому безопасно приводим\n    Point other = (Point) obj;\n\n    // ШАГ 4: Сравнение полей\n    // Два объекта равны, если ВСЕ значимые поля равны\n    return this.x == other.x && this.y == other.y;\n}' },
        { type: 'heading', value: 'hashCode() — обязательный спутник equals()' },
        { type: 'text', value: 'Если ты переопределяешь equals(), ОБЯЗАТЕЛЬНО переопределяй hashCode(). Это контракт Java: если два объекта equals() — у них должен быть одинаковый hashCode().' },
        { type: 'code', language: 'java', value: 'import java.util.Objects;\n\npublic class Student {\n    String name;\n    int age;\n    String group;\n\n    @Override\n    public boolean equals(Object obj) {\n        if (this == obj) return true;\n        if (obj == null || getClass() != obj.getClass()) return false;\n        Student other = (Student) obj;\n        return age == other.age &&\n               Objects.equals(name, other.name) &&\n               Objects.equals(group, other.group);\n    }\n\n    @Override\n    public int hashCode() {\n        return Objects.hash(name, age, group);\n    }\n\n    @Override\n    public String toString() {\n        return name + " (" + age + "), группа " + group;\n    }\n}\n\nStudent s1 = new Student();\ns1.name = "Алия";\ns1.age = 20;\ns1.group = "CS-101";\n\nStudent s2 = new Student();\ns2.name = "Алия";\ns2.age = 20;\ns2.group = "CS-101";\n\nSystem.out.println(s1.equals(s2));           // true\nSystem.out.println(s1.hashCode() == s2.hashCode()); // true\n\n// Для String полей используй Objects.equals() вместо ==\n// Objects.equals(null, null) вернёт true (безопасно)' },
        { type: 'heading', value: 'Полный пример с equals и hashCode' },
        { type: 'code', language: 'java', value: 'import java.util.Objects;\nimport java.util.ArrayList;\n\npublic class Product {\n    String name;\n    String category;\n    double price;\n\n    @Override\n    public boolean equals(Object obj) {\n        if (this == obj) return true;\n        if (obj == null || getClass() != obj.getClass()) return false;\n        Product other = (Product) obj;\n        return Double.compare(other.price, price) == 0 &&\n               Objects.equals(name, other.name) &&\n               Objects.equals(category, other.category);\n    }\n\n    @Override\n    public int hashCode() {\n        return Objects.hash(name, category, price);\n    }\n\n    @Override\n    public String toString() {\n        return name + " (" + category + ") — " + price + " тг";\n    }\n}\n\nProduct p1 = new Product();\np1.name = "iPhone 15";\np1.category = "Смартфоны";\np1.price = 450000;\n\nProduct p2 = new Product();\np2.name = "iPhone 15";\np2.category = "Смартфоны";\np2.price = 450000;\n\n// Теперь можно искать в списке!\nArrayList<Product> cart = new ArrayList<>();\ncart.add(p1);\n\n// contains использует equals() для поиска\nSystem.out.println(cart.contains(p2)); // true! (без equals было бы false)' },
        { type: 'heading', value: 'Контракт equals() и hashCode()' },
        { type: 'list', items: [
          'Рефлексивность: a.equals(a) всегда true',
          'Симметричность: если a.equals(b), то и b.equals(a)',
          'Транзитивность: если a.equals(b) и b.equals(c), то a.equals(c)',
          'Консистентность: многократный вызов a.equals(b) даёт один результат (если объекты не менялись)',
          'a.equals(null) всегда false',
          'Если a.equals(b) == true, то a.hashCode() == b.hashCode() (ОБЯЗАТЕЛЬНО!)'
        ]},
        { type: 'warning', value: 'Никогда не сравнивай объекты через == (кроме проверки на null). Всегда используй equals(). Это одна из самых частых ошибок начинающих Java-разработчиков.' },
        { type: 'note', value: 'Для сравнения String уже всегда используется equals(): "hello".equals(str). Для своих классов принцип такой же — переопредели equals() и hashCode().' }
      ]
    },
    {
      id: 12,
      title: 'Static поля и методы',
      type: 'theory',
      content: [
        { type: 'text', value: 'Ключевое слово static означает, что поле или метод принадлежит КЛАССУ, а не конкретному объекту. Static-члены общие для всех объектов — они существуют в единственном экземпляре.' },
        { type: 'tip', value: 'Аналогия: у каждого студента своё имя и GPA (обычные поля). Но название университета одно для всех студентов — это static поле. Если университет переименуют, это изменение видят ВСЕ студенты сразу.' },
        { type: 'heading', value: 'Static поле — общее для всех объектов' },
        { type: 'code', language: 'java', value: 'public class Student {\n    // Обычные поля — у каждого объекта свои\n    String name;\n    double gpa;\n\n    // Static поле — одно на весь класс, общее для всех\n    static String universityName = "КазНУ";\n    static int totalStudents = 0;  // счётчик всех студентов\n\n    public Student(String name, double gpa) {\n        this.name = name;\n        this.gpa = gpa;\n        totalStudents++; // при создании каждого студента — счётчик растёт\n    }\n\n    void showInfo() {\n        System.out.println(name + " (GPA: " + gpa + ") — " + universityName);\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        System.out.println("Студентов: " + Student.totalStudents); // 0\n\n        Student s1 = new Student("Алия", 4.5);\n        Student s2 = new Student("Берик", 3.8);\n        Student s3 = new Student("Гаухар", 4.2);\n\n        System.out.println("Студентов: " + Student.totalStudents); // 3\n\n        // Static поле общее — меняем один раз, видят все\n        Student.universityName = "Назарбаев Университет";\n        s1.showInfo(); // Алия (GPA: 4.5) — Назарбаев Университет\n        s2.showInfo(); // Берик (GPA: 3.8) — Назарбаев Университет\n        s3.showInfo(); // Гаухар (GPA: 4.2) — Назарбаев Университет\n    }\n}' },
        { type: 'heading', value: 'Визуализация: static vs обычные поля' },
        { type: 'code', language: 'java', value: '// Обычные поля — у КАЖДОГО объекта своя копия:\n//\n// Объект s1:        Объект s2:        Объект s3:\n// ┌──────────┐     ┌──────────┐     ┌──────────┐\n// │name: Алия│     │name:Берик│     │name:Гаухар│\n// │gpa: 4.5  │     │gpa: 3.8  │     │gpa: 4.2  │\n// └──────────┘     └──────────┘     └──────────┘\n//\n// Static поле — ОДНО на весь класс:\n//\n// Класс Student:\n// ┌────────────────────────────────┐\n// │ static universityName = "КазНУ"│  ← одно на всех\n// │ static totalStudents = 3       │  ← одно на всех\n// └────────────────────────────────┘\n//       ▲           ▲           ▲\n//       │           │           │\n//      s1          s2          s3   (все видят одно значение)' },
        { type: 'heading', value: 'Static метод — вызывается без объекта' },
        { type: 'code', language: 'java', value: 'public class MathUtils {\n    // Static метод — можно вызвать без создания объекта\n    static int max(int a, int b) {\n        return (a > b) ? a : b;\n    }\n\n    static int min(int a, int b) {\n        return (a < b) ? a : b;\n    }\n\n    static double average(int[] numbers) {\n        int sum = 0;\n        for (int n : numbers) {\n            sum += n;\n        }\n        return (double) sum / numbers.length;\n    }\n\n    static boolean isEven(int n) {\n        return n % 2 == 0;\n    }\n\n    static int factorial(int n) {\n        int result = 1;\n        for (int i = 2; i <= n; i++) {\n            result *= i;\n        }\n        return result;\n    }\n}\n\n// Вызываем через ИМЯ КЛАССА, а не через объект!\nint m = MathUtils.max(10, 20);        // 20\nint f = MathUtils.factorial(5);        // 120\ndouble avg = MathUtils.average(new int[]{4, 5, 3, 5, 4}); // 4.2\n\n// НЕ нужно создавать объект:\n// MathUtils utils = new MathUtils(); // так делать НЕ нужно\n// utils.max(10, 20);                 // работает, но нет смысла' },
        { type: 'heading', value: 'Знакомые static методы из Java' },
        { type: 'code', language: 'java', value: '// Ты уже давно используешь static методы!\n\n// Math — все методы static\nMath.random();        // случайное число\nMath.sqrt(16);        // 4.0\nMath.max(5, 10);      // 10\nMath.abs(-7);         // 7\n\n// Integer — static методы\nInteger.parseInt("123");     // 123\nInteger.MAX_VALUE;           // 2147483647 (static поле)\n\n// Arrays — все методы static\nArrays.sort(array);\nArrays.toString(array);\n\n// System.out.println — out это static поле класса System!\nSystem.out.println("Hello");  // System.out — static поле\n\n// main — тоже static!\npublic static void main(String[] args) {\n    // main — static, потому что вызывается ДО создания любых объектов\n}' },
        { type: 'heading', value: 'Static счётчик — популярный паттерн' },
        { type: 'code', language: 'java', value: 'public class Order {\n    // Static счётчик — автоматическая нумерация заказов\n    private static int nextId = 1;\n\n    int id;\n    String product;\n    int quantity;\n    double price;\n\n    public Order(String product, int quantity, double price) {\n        this.id = nextId;  // присваиваем текущий номер\n        nextId++;          // увеличиваем для следующего заказа\n        this.product = product;\n        this.quantity = quantity;\n        this.price = price;\n    }\n\n    double total() {\n        return quantity * price;\n    }\n\n    // Static метод — информация по всем заказам\n    static int getTotalOrders() {\n        return nextId - 1;\n    }\n\n    @Override\n    public String toString() {\n        return "Заказ #" + id + ": " + product +\n               " x" + quantity + " = " + total() + " тг";\n    }\n}\n\nOrder o1 = new Order("Ноутбук", 1, 350000);\nOrder o2 = new Order("Мышка", 2, 5000);\nOrder o3 = new Order("Монитор", 1, 120000);\n\nSystem.out.println(o1); // Заказ #1: Ноутбук x1 = 350000.0 тг\nSystem.out.println(o2); // Заказ #2: Мышка x2 = 10000.0 тг\nSystem.out.println(o3); // Заказ #3: Монитор x1 = 120000.0 тг\nSystem.out.println("Всего заказов: " + Order.getTotalOrders()); // 3' },
        { type: 'heading', value: 'Ограничения static методов' },
        { type: 'code', language: 'java', value: 'public class Example {\n    String name = "Привет";     // обычное поле\n    static int count = 0;       // static поле\n\n    // Static метод НЕ МОЖЕТ:\n    static void staticMethod() {\n        // count = 5;            // OK — static обращается к static\n        // name = "Нет";         // ОШИБКА! static не видит обычные поля\n        // this.name = "Нет";    // ОШИБКА! в static нет this\n        // regularMethod();      // ОШИБКА! static не может вызвать обычный метод\n    }\n\n    // Обычный метод МОЖЕТ всё:\n    void regularMethod() {\n        count = 5;              // OK — обычный видит static\n        name = "Да";            // OK — обычный видит обычные поля\n        staticMethod();         // OK — обычный может вызвать static\n    }\n}\n\n// ПРАВИЛО:\n// static → видит только static\n// обычный → видит и static, и обычные' },
        { type: 'warning', value: 'Не злоупотребляй static! Если сделать всё static — это превратится в процедурное программирование. Static нужен для утилит (Math, Arrays), констант и счётчиков. Основная логика должна быть в обычных методах объектов.' },
        { type: 'note', value: 'Static поля часто используют с модификатором final для констант: static final double PI = 3.14159; static final int MAX_SIZE = 100; Такие поля — это константы, которые нельзя изменить.' }
      ]
    },
    {
      id: 13,
      title: 'Практика: Класс Student',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создай полноценный класс Student с полями, методами, toString(), equals() и static счётчиком. Этот класс должен уметь хранить информацию о студенте, проверять успеваемость, и вести учёт общего количества студентов.',
      requirements: [
        'Создай класс Student с полями: name (String), age (int), gpa (double), group (String)',
        'Добавь static поле totalStudents (int) — счётчик всех созданных студентов',
        'В конструкторе инициализируй все поля и увеличивай totalStudents',
        'Добавь метод isExcellent() — возвращает true, если gpa >= 4.5',
        'Добавь метод isPassable() — возвращает true, если gpa >= 2.0',
        'Добавь метод getGrade() — возвращает строку: "Отлично" (>=4.5), "Хорошо" (>=3.5), "Удовл." (>=2.0), "Неуд." (<2.0)',
        'Переопредели toString() — вывод в формате: "Студент{name=Алия, age=20, gpa=4.5, group=CS-101}"',
        'Переопредели equals() и hashCode() — сравнение по name и group',
        'Добавь static метод getTotalStudents() — возвращает общее количество',
        'Создай минимум 3 объекта Student и продемонстрируй работу всех методов'
      ],
      expectedOutput: 'Студент{name=Алия, age=20, gpa=4.8, group=CS-101}\nОценка: Отлично\nОтличник: true\n\nСтудент{name=Берик, age=21, gpa=3.5, group=CS-101}\nОценка: Хорошо\nОтличник: false\n\nСтудент{name=Гаухар, age=19, gpa=1.8, group=CS-102}\nОценка: Неуд.\nПроходной: false\n\nВсего студентов: 3\nАлия == Берик? false\nАлия == Алия(копия)? true',
      hint: 'Начни с конструктора: public Student(String name, int age, double gpa, String group). Внутри используй this.name = name для каждого поля и totalStudents++ для счётчика. В equals() сравнивай name и group через Objects.equals().',
      solution: 'import java.util.Objects;\n\npublic class Student {\n    String name;\n    int age;\n    double gpa;\n    String group;\n    static int totalStudents = 0;\n\n    public Student(String name, int age, double gpa, String group) {\n        this.name = name;\n        this.age = age;\n        this.gpa = gpa;\n        this.group = group;\n        totalStudents++;\n    }\n\n    boolean isExcellent() {\n        return gpa >= 4.5;\n    }\n\n    boolean isPassable() {\n        return gpa >= 2.0;\n    }\n\n    String getGrade() {\n        if (gpa >= 4.5) return "Отлично";\n        if (gpa >= 3.5) return "Хорошо";\n        if (gpa >= 2.0) return "Удовл.";\n        return "Неуд.";\n    }\n\n    static int getTotalStudents() {\n        return totalStudents;\n    }\n\n    @Override\n    public String toString() {\n        return "Студент{name=" + name + ", age=" + age + ", gpa=" + gpa + ", group=" + group + "}";\n    }\n\n    @Override\n    public boolean equals(Object obj) {\n        if (this == obj) return true;\n        if (obj == null || getClass() != obj.getClass()) return false;\n        Student other = (Student) obj;\n        return Objects.equals(name, other.name) && Objects.equals(group, other.group);\n    }\n\n    @Override\n    public int hashCode() {\n        return Objects.hash(name, group);\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Student s1 = new Student("Алия", 20, 4.8, "CS-101");\n        Student s2 = new Student("Берик", 21, 3.5, "CS-101");\n        Student s3 = new Student("Гаухар", 19, 1.8, "CS-102");\n\n        System.out.println(s1);\n        System.out.println("Оценка: " + s1.getGrade());\n        System.out.println("Отличник: " + s1.isExcellent());\n        System.out.println();\n\n        System.out.println(s2);\n        System.out.println("Оценка: " + s2.getGrade());\n        System.out.println("Отличник: " + s2.isExcellent());\n        System.out.println();\n\n        System.out.println(s3);\n        System.out.println("Оценка: " + s3.getGrade());\n        System.out.println("Проходной: " + s3.isPassable());\n        System.out.println();\n\n        System.out.println("Всего студентов: " + Student.getTotalStudents());\n\n        Student s4 = new Student("Алия", 20, 4.8, "CS-101");\n        System.out.println("Алия == Берик? " + s1.equals(s2));\n        System.out.println("Алия == Алия(копия)? " + s1.equals(s4));\n    }\n}',
      explanation: 'Мы создали полноценный класс Student. Конструктор принимает все данные и увеличивает static счётчик. Методы isExcellent(), isPassable() и getGrade() анализируют GPA. toString() возвращает строковое представление. equals() сравнивает студентов по имени и группе (два студента с одинаковым именем в одной группе считаются одним человеком). hashCode() согласован с equals() через Objects.hash(). Static метод getTotalStudents() возвращает общий счётчик.'
    },
    {
      id: 14,
      title: 'Практика: Библиотека книг',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создай систему из двух классов: Book (книга) и Library (библиотека). Library хранит коллекцию книг и предоставляет методы для добавления, удаления, поиска, сортировки и статистики.',
      requirements: [
        'Класс Book: поля — title (String), author (String), year (int), pages (int), rating (double от 0 до 5)',
        'Book: конструктор со всеми полями, toString(), equals() по title и author',
        'Класс Library: поле name (String), массив или ArrayList книг',
        'Library: метод addBook(Book book) — добавить книгу',
        'Library: метод removeBook(Book book) — удалить книгу (через equals)',
        'Library: метод findByAuthor(String author) — вернуть все книги автора',
        'Library: метод findByTitle(String keyword) — поиск по части названия (contains)',
        'Library: метод getBookCount() — количество книг',
        'Library: метод getAverageRating() — средний рейтинг всех книг',
        'Library: метод getBestBook() — книга с наивысшим рейтингом',
        'Library: метод getBooksAfterYear(int year) — книги после указанного года',
        'Library: static поле totalLibraries — счётчик библиотек',
        'Продемонстрируй работу: создай библиотеку, добавь 5+ книг, выполни поиск и покажи статистику'
      ],
      expectedOutput: '=== Городская библиотека ===\nКниг: 5\n\nПоиск по автору "Толстой":\n"Война и мир" (Толстой, 1869) — 4.8★\n"Анна Каренина" (Толстой, 1877) — 4.6★\n\nПоиск "Гарри":\n"Гарри Поттер" (Роулинг, 1997) — 4.9★\n\nСредний рейтинг: 4.58\nЛучшая книга: "Гарри Поттер" (Роулинг, 1997) — 4.9★\n\nКниги после 1900:\n"Мастер и Маргарита" (Булгаков, 1967) — 4.7★\n"Гарри Поттер" (Роулинг, 1997) — 4.9★\n"1984" (Оруэлл, 1949) — 4.5★',
      hint: 'Используй ArrayList<Book> для хранения книг в Library. Для поиска по автору переберите список и сравните author через equals(). Для поиска по названию используйте title.toLowerCase().contains(keyword.toLowerCase()). Для лучшей книги переберите список и найдите максимальный rating.',
      solution: 'import java.util.ArrayList;\nimport java.util.Objects;\n\npublic class Book {\n    String title;\n    String author;\n    int year;\n    int pages;\n    double rating;\n\n    public Book(String title, String author, int year, int pages, double rating) {\n        this.title = title;\n        this.author = author;\n        this.year = year;\n        this.pages = pages;\n        this.rating = rating;\n    }\n\n    @Override\n    public String toString() {\n        return "\\"" + title + "\\" (" + author + ", " + year + ") — " + rating + "★";\n    }\n\n    @Override\n    public boolean equals(Object obj) {\n        if (this == obj) return true;\n        if (obj == null || getClass() != obj.getClass()) return false;\n        Book other = (Book) obj;\n        return Objects.equals(title, other.title) && Objects.equals(author, other.author);\n    }\n\n    @Override\n    public int hashCode() {\n        return Objects.hash(title, author);\n    }\n}\n\npublic class Library {\n    String name;\n    ArrayList<Book> books;\n    static int totalLibraries = 0;\n\n    public Library(String name) {\n        this.name = name;\n        this.books = new ArrayList<>();\n        totalLibraries++;\n    }\n\n    void addBook(Book book) {\n        books.add(book);\n    }\n\n    boolean removeBook(Book book) {\n        return books.remove(book);\n    }\n\n    ArrayList<Book> findByAuthor(String author) {\n        ArrayList<Book> result = new ArrayList<>();\n        for (Book b : books) {\n            if (b.author.equals(author)) {\n                result.add(b);\n            }\n        }\n        return result;\n    }\n\n    ArrayList<Book> findByTitle(String keyword) {\n        ArrayList<Book> result = new ArrayList<>();\n        for (Book b : books) {\n            if (b.title.toLowerCase().contains(keyword.toLowerCase())) {\n                result.add(b);\n            }\n        }\n        return result;\n    }\n\n    int getBookCount() {\n        return books.size();\n    }\n\n    double getAverageRating() {\n        if (books.isEmpty()) return 0;\n        double sum = 0;\n        for (Book b : books) {\n            sum += b.rating;\n        }\n        return sum / books.size();\n    }\n\n    Book getBestBook() {\n        if (books.isEmpty()) return null;\n        Book best = books.get(0);\n        for (int i = 1; i < books.size(); i++) {\n            if (books.get(i).rating > best.rating) {\n                best = books.get(i);\n            }\n        }\n        return best;\n    }\n\n    ArrayList<Book> getBooksAfterYear(int year) {\n        ArrayList<Book> result = new ArrayList<>();\n        for (Book b : books) {\n            if (b.year > year) {\n                result.add(b);\n            }\n        }\n        return result;\n    }\n\n    static int getTotalLibraries() {\n        return totalLibraries;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Library lib = new Library("Городская библиотека");\n\n        lib.addBook(new Book("Война и мир", "Толстой", 1869, 1225, 4.8));\n        lib.addBook(new Book("Анна Каренина", "Толстой", 1877, 864, 4.6));\n        lib.addBook(new Book("Мастер и Маргарита", "Булгаков", 1967, 480, 4.7));\n        lib.addBook(new Book("Гарри Поттер", "Роулинг", 1997, 332, 4.9));\n        lib.addBook(new Book("1984", "Оруэлл", 1949, 328, 4.5));\n\n        System.out.println("=== " + lib.name + " ===");\n        System.out.println("Книг: " + lib.getBookCount());\n\n        System.out.println("\\nПоиск по автору \\"Толстой\\":");\n        for (Book b : lib.findByAuthor("Толстой")) {\n            System.out.println(b);\n        }\n\n        System.out.println("\\nПоиск \\"Гарри\\":");\n        for (Book b : lib.findByTitle("Гарри")) {\n            System.out.println(b);\n        }\n\n        System.out.printf("\\nСредний рейтинг: %.2f\\n", lib.getAverageRating());\n        System.out.println("Лучшая книга: " + lib.getBestBook());\n\n        System.out.println("\\nКниги после 1900:");\n        for (Book b : lib.getBooksAfterYear(1900)) {\n            System.out.println(b);\n        }\n    }\n}',
      explanation: 'Мы создали два взаимосвязанных класса. Book — простой класс с данными, toString(), equals() и hashCode(). Library — класс-контейнер, который хранит ArrayList<Book> и предоставляет различные методы для работы с коллекцией: добавление, удаление, поиск по автору/названию, вычисление статистики. Метод removeBook() использует equals() для нахождения книги. findByTitle() использует contains() для поиска по части названия (регистронезависимо). getBestBook() перебирает все книги и находит максимальный рейтинг. Static поле totalLibraries считает созданные библиотеки. Это типичный паттерн в ООП: один класс (Book) представляет отдельную сущность, другой (Library) — коллекцию сущностей с бизнес-логикой.'
    }
  ]
}
