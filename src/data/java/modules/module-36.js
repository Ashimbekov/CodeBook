export default {
  id: 36,
  title: 'Многопоточность: основы',
  description: 'Потоки выполнения, Thread и Runnable, жизненный цикл, sleep/join, volatile, race condition, deadlock — полное введение в многопоточное программирование Java',
  lessons: [
    {
      id: 1,
      title: 'Зачем многопоточность?',
      type: 'theory',
      content: [
        { type: 'text', value: 'Многопоточность — одна из самых мощных и важных концепций в Java. Она позволяет программе выполнять несколько задач одновременно. Но прежде чем писать многопоточный код, нужно понять фундаментальные отличия между процессами и потоками, параллелизмом и конкурентностью.' },
        { type: 'heading', value: 'Процесс vs Поток' },
        { type: 'text', value: 'Процесс — это запущенная программа с собственной памятью. Поток — это единица выполнения внутри процесса. Один процесс может содержать множество потоков, и они разделяют общую память процесса.' },
        { type: 'code', language: 'text', value: '┌──────────────────────────────────────────────────────┐\n│                   ПРОЦЕСС (JVM)                      │\n│                                                      │\n│  ┌─────────────────────────────────────────────────┐  │\n│  │            ОБЩАЯ ПАМЯТЬ (Heap)                  │  │\n│  │   Объекты, массивы, статические переменные      │  │\n│  └─────────────────────────────────────────────────┘  │\n│                                                      │\n│  ┌──────────┐  ┌──────────┐  ┌──────────┐            │\n│  │ Поток-1  │  │ Поток-2  │  │ Поток-3  │            │\n│  │ (main)   │  │ (worker) │  │ (worker) │            │\n│  │          │  │          │  │          │            │\n│  │ Свой стек│  │ Свой стек│  │ Свой стек│            │\n│  │ Свой PC  │  │ Свой PC  │  │ Свой PC  │            │\n│  └──────────┘  └──────────┘  └──────────┘            │\n│                                                      │\n│  PC = Program Counter (счётчик команд)               │\n└──────────────────────────────────────────────────────┘\n\nСравнение:\n┌─────────────┬──────────────────────┬──────────────────────┐\n│             │      ПРОЦЕСС         │       ПОТОК          │\n├─────────────┼──────────────────────┼──────────────────────┤\n│ Память      │ Своя (изолированная) │ Общая (heap процесса)│\n│ Создание    │ Тяжёлое (мс)        │ Лёгкое (мкс)         │\n│ Общение     │ IPC (сложно)        │ Общие переменные     │\n│ Падение     │ Не влияет на других  │ Может убить процесс  │\n│ Пример      │ Chrome, IntelliJ     │ main, GC, HTTP-поток │\n└─────────────┴──────────────────────┴──────────────────────┘' },
        { type: 'heading', value: 'Параллелизм vs Конкурентность' },
        { type: 'text', value: 'Это два разных понятия, которые часто путают. Конкурентность (concurrency) — это когда несколько задач чередуются на одном ядре. Параллелизм (parallelism) — это когда задачи буквально выполняются одновременно на разных ядрах.' },
        { type: 'code', language: 'text', value: 'КОНКУРЕНТНОСТЬ (1 ядро, чередование):\n\nЯдро:  [──A──][──B──][──A──][──B──][──A──][──B──]\nВремя: ─────────────────────────────────────────▶\n\nЗадачи ЧЕРЕДУЮТСЯ. В каждый момент работает только одна.\nНо переключение настолько быстрое, что кажется одновременным.\n\nПАРАЛЛЕЛИЗМ (2 ядра, одновременно):\n\nЯдро 1: [──────────A──────────]\nЯдро 2: [──────────B──────────]\nВремя:  ─────────────────────▶\n\nЗадачи РЕАЛЬНО работают одновременно на разных ядрах.' },
        { type: 'heading', value: 'Реальные примеры многопоточности' },
        { type: 'text', value: 'Многопоточность окружает нас повсюду в реальных приложениях:' },
        { type: 'code', language: 'text', value: '🌐 БРАУЗЕР (Chrome):\n├── Поток рендеринга     → рисует страницу\n├── Поток JavaScript     → выполняет скрипты\n├── Поток сети           → качает картинки, CSS, JS\n├── Поток UI             → кнопки, скролл, клики\n└── Поток GPU            → ускорение графики\n\n☕ JAVA WEB-СЕРВЕР (Tomcat/Spring Boot):\n├── main поток           → запуск приложения\n├── HTTP поток-1         → обработка запроса от Клиента А\n├── HTTP поток-2         → обработка запроса от Клиента Б\n├── HTTP поток-3         → обработка запроса от Клиента В\n├── Scheduler поток      → периодические задачи (очистка кэша)\n└── GC поток             → сборка мусора\n\n🎮 ИГРА (Minecraft):\n├── Поток рендера        → 60 FPS отрисовка\n├── Поток логики         → физика, AI мобов\n├── Поток ввода          → клавиатура, мышь\n├── Поток звука          → фоновая музыка, эффекты\n└── Поток сети           → мультиплеер' },
        { type: 'code', language: 'java', value: '// Без многопоточности — сервер обслуживает 1 клиента за раз\npublic class SingleThreadServer {\n    public static void main(String[] args) {\n        // Клиент А ждёт 3 секунды обработки\n        handleRequest("Клиент А"); // 3 сек\n        // Клиент Б ждёт 3 сек + 3 сек обработки = 6 секунд!\n        handleRequest("Клиент Б"); // ещё 3 сек\n        // 1000 клиентов? 3000 секунд = 50 минут ожидания!\n    }\n}\n\n// С многопоточностью — каждый клиент в своём потоке\npublic class MultiThreadServer {\n    public static void main(String[] args) {\n        // Все клиенты обслуживаются одновременно!\n        new Thread(() -> handleRequest("Клиент А")).start();\n        new Thread(() -> handleRequest("Клиент Б")).start();\n        new Thread(() -> handleRequest("Клиент В")).start();\n        // Все получают ответ через ~3 секунды!\n    }\n}' },
        { type: 'tip', value: 'Представь ресторан. Однопоточный сервер — один повар, который готовит блюда строго по очереди. 10 заказов = 10 часов ожидания для последнего клиента. Многопоточный сервер — 10 поваров, каждый готовит свой заказ. Все клиенты получают еду примерно одновременно.' },
        { type: 'note', value: 'В Java любая программа уже многопоточная! Метод main() выполняется в главном потоке "main". Сборщик мусора (GC) работает в отдельном потоке. JIT-компилятор — тоже в своём потоке. Даже в простейшей программе "Hello World" работает несколько потоков.' }
      ]
    },
    {
      id: 2,
      title: 'Создание потока: Thread class',
      type: 'theory',
      content: [
        { type: 'text', value: 'Первый способ создать поток в Java — наследоваться от класса Thread. Класс Thread реализует интерфейс Runnable и представляет собой поток выполнения. Нужно переопределить метод run() — именно в нём описывается код, который будет выполняться в отдельном потоке.' },
        { type: 'heading', value: 'Простейший поток через extends Thread' },
        { type: 'code', language: 'java', value: '// Шаг 1: Наследуемся от Thread\nclass DownloaderThread extends Thread {\n    private String fileName;\n\n    DownloaderThread(String fileName) {\n        this.fileName = fileName;\n    }\n\n    @Override\n    public void run() {\n        // Этот код выполняется в НОВОМ потоке\n        System.out.println("[" + getName() + "] Начинаю загрузку: " + fileName);\n        try {\n            Thread.sleep(2000); // имитация загрузки\n        } catch (InterruptedException e) {\n            System.out.println("Загрузка прервана!");\n        }\n        System.out.println("[" + getName() + "] Загружен: " + fileName);\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        System.out.println("[" + Thread.currentThread().getName() + "] Запускаю загрузки");\n\n        DownloaderThread t1 = new DownloaderThread("photo.jpg");\n        DownloaderThread t2 = new DownloaderThread("video.mp4");\n        DownloaderThread t3 = new DownloaderThread("music.mp3");\n\n        t1.start(); // создаёт новый поток и вызывает run()\n        t2.start();\n        t3.start();\n\n        System.out.println("[main] Все загрузки запущены!");\n    }\n}\n// Вывод (порядок может отличаться):\n// [main] Запускаю загрузки\n// [main] Все загрузки запущены!\n// [Thread-0] Начинаю загрузку: photo.jpg\n// [Thread-1] Начинаю загрузку: video.mp4\n// [Thread-2] Начинаю загрузку: music.mp3\n// [Thread-0] Загружен: photo.jpg\n// [Thread-1] Загружен: video.mp4\n// [Thread-2] Загружен: music.mp3' },
        { type: 'heading', value: 'КРИТИЧЕСКИ ВАЖНО: run() vs start()' },
        { type: 'text', value: 'Это самая частая ошибка новичков. Метод start() создаёт новый поток и запускает в нём run(). Метод run() просто выполняет код в текущем потоке — никакой многопоточности не будет!' },
        { type: 'code', language: 'java', value: 'class MyThread extends Thread {\n    @Override\n    public void run() {\n        System.out.println("Работаю в потоке: " + Thread.currentThread().getName());\n    }\n}\n\nMyThread t = new MyThread();\n\n// НЕПРАВИЛЬНО — вызов run() напрямую\nt.run();\n// Выведет: "Работаю в потоке: main"\n// Никакого нового потока НЕ создаётся! Код выполняется в main.\n\n// ПРАВИЛЬНО — вызов start()\nt.start();\n// Выведет: "Работаю в потоке: Thread-0"\n// Создаётся НОВЫЙ поток, и run() вызывается в нём.' },
        { type: 'code', language: 'text', value: 'Что происходит при вызове start():\n\n  Ваш код          JVM                    ОС\n     │               │                     │\n     │── start() ──▶│                     │\n     │               │── запрос на ────────▶│\n     │               │   создание потока    │\n     │               │                     │── создаёт\n     │               │◀── поток создан ─────│   системный\n     │               │                     │   поток\n     │               │── вызывает run() ──▶│\n     │◀── возврат ───│   в НОВОМ потоке    │\n     │   из start()  │                     │\n     │   (сразу!)    │                     │\n\nВажно: start() возвращается НЕМЕДЛЕННО.\nrun() выполняется в ДРУГОМ потоке параллельно.' },
        { type: 'heading', value: 'Полезные методы Thread' },
        { type: 'code', language: 'java', value: 'Thread t = new Thread(() -> {\n    System.out.println("Работаю...");\n});\n\n// Имя потока\nt.setName("МойПоток");\nSystem.out.println(t.getName());    // МойПоток\n\n// ID потока (уникальный, присваивается JVM)\nSystem.out.println(t.getId());      // например 14\n\n// Приоритет (от 1 до 10, по умолчанию 5)\nSystem.out.println(t.getPriority()); // 5\n\n// Состояние\nSystem.out.println(t.getState());    // NEW\n\nt.start();\nSystem.out.println(t.isAlive());     // true (пока работает)\n\n// Текущий поток (статический метод)\nThread current = Thread.currentThread();\nSystem.out.println(current.getName()); // main' },
        { type: 'warning', value: 'Поток нельзя запустить дважды! Повторный вызов start() на уже запущенном или завершённом потоке бросает IllegalThreadStateException. Если нужно выполнить задачу ещё раз — создавай новый объект Thread.' },
        { type: 'note', value: 'Наследование от Thread — простой, но не лучший подход. Java не поддерживает множественное наследование, поэтому если класс уже extends другой класс, наследоваться от Thread не получится. Предпочтительный способ — Runnable (следующий урок).' }
      ]
    },
    {
      id: 3,
      title: 'Создание потока: Runnable',
      type: 'theory',
      content: [
        { type: 'text', value: 'Второй и предпочтительный способ создать поток — реализовать интерфейс Runnable. Это функциональный интерфейс с единственным методом run(). Runnable отделяет задачу (что делать) от механизма выполнения (Thread).' },
        { type: 'tip', value: 'Thread — это рабочий, а Runnable — это инструкция. Ты пишешь инструкцию отдельно, а потом даёшь её любому рабочему. Один рабочий может выполнить одну инструкцию, но одну инструкцию можно дать нескольким рабочим.' },
        { type: 'heading', value: 'Способ 1: Реализация интерфейса Runnable' },
        { type: 'code', language: 'java', value: '// Задача реализует Runnable\nclass DataProcessor implements Runnable {\n    private String dataSource;\n\n    DataProcessor(String dataSource) {\n        this.dataSource = dataSource;\n    }\n\n    @Override\n    public void run() {\n        System.out.println("Обработка данных из: " + dataSource);\n        // сложные вычисления...\n        for (int i = 0; i <= 100; i += 25) {\n            System.out.println(dataSource + " — прогресс: " + i + "%");\n            try { Thread.sleep(200); } catch (InterruptedException e) { break; }\n        }\n        System.out.println(dataSource + " — обработка завершена!");\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        // Создаём задачу\n        Runnable task = new DataProcessor("database.csv");\n\n        // Передаём задачу в Thread\n        Thread thread = new Thread(task);\n        thread.start();\n\n        // Одну задачу можно запустить в нескольких потоках!\n        Runnable sharedTask = new DataProcessor("users.json");\n        Thread t1 = new Thread(sharedTask);\n        Thread t2 = new Thread(sharedTask); // тот же Runnable, другой поток\n        t1.start();\n        t2.start();\n    }\n}' },
        { type: 'heading', value: 'Способ 2: Анонимный класс' },
        { type: 'code', language: 'java', value: '// Анонимная реализация Runnable\nThread thread = new Thread(new Runnable() {\n    @Override\n    public void run() {\n        System.out.println("Работаю в потоке: " + Thread.currentThread().getName());\n    }\n});\nthread.start();' },
        { type: 'heading', value: 'Способ 3: Лямбда-выражение (рекомендуется)' },
        { type: 'code', language: 'java', value: '// Runnable — функциональный интерфейс (1 абстрактный метод)\n// Поэтому можно использовать лямбду!\n\n// Однострочная задача\nThread t1 = new Thread(() -> System.out.println("Привет из потока!"));\nt1.start();\n\n// Многострочная задача\nThread t2 = new Thread(() -> {\n    String threadName = Thread.currentThread().getName();\n    for (int i = 1; i <= 5; i++) {\n        System.out.println("[" + threadName + "] Шаг " + i);\n        try { Thread.sleep(100); } catch (InterruptedException e) { break; }\n    }\n});\nt2.setName("Worker");\nt2.start();\n\n// Method reference (если метод уже существует)\nThread t3 = new Thread(Main::doHeavyWork);\nt3.start();\n\nstatic void doHeavyWork() {\n    System.out.println("Выполняю тяжёлую работу...");\n}' },
        { type: 'heading', value: 'Почему Runnable лучше, чем extends Thread?' },
        { type: 'code', language: 'text', value: '┌─────────────────────┬─────────────────────┬────────────────────────┐\n│    Критерий          │   extends Thread    │  implements Runnable   │\n├─────────────────────┼─────────────────────┼────────────────────────┤\n│ Наследование        │ Нельзя наследовать  │ Можно наследовать от   │\n│                     │ от другого класса   │ другого класса         │\n├─────────────────────┼─────────────────────┼────────────────────────┤\n│ Переиспользование   │ Задача привязана    │ Задачу можно запустить  │\n│                     │ к объекту Thread    │ в любом Thread/Pool    │\n├─────────────────────┼─────────────────────┼────────────────────────┤\n│ Принцип дизайна     │ Нарушает SRP        │ Разделяет задачу       │\n│                     │ (задача = поток)    │ и механизм запуска     │\n├─────────────────────┼─────────────────────┼────────────────────────┤\n│ Пулы потоков        │ Не работает         │ Работает с             │\n│                     │ с ExecutorService   │ ExecutorService        │\n├─────────────────────┼─────────────────────┼────────────────────────┤\n│ Лямбда              │ Невозможно          │ Поддерживает лямбды    │\n└─────────────────────┴─────────────────────┴────────────────────────┘' },
        { type: 'code', language: 'java', value: '// Проблема extends Thread — нельзя наследовать ещё от чего-то\nclass MyTask extends Thread { } // уже занято наследование\n// class MyTask extends Thread extends Animal { } // ОШИБКА!\n\n// Runnable решает проблему — можно наследовать И реализовать\nclass AnimalTask extends Animal implements Runnable {\n    @Override\n    public void run() {\n        // задача для потока\n    }\n}\n\n// Runnable работает с пулами потоков (ExecutorService)\nExecutorService pool = Executors.newFixedThreadPool(4);\npool.submit(() -> System.out.println("Запущено в пуле!"));\n// ExecutorService НЕ принимает Thread, только Runnable/Callable!' },
        { type: 'note', value: 'В реальном коде extends Thread используется крайне редко. В 95% случаев используют Runnable (или Callable для задач с результатом). Правило простое: если нужен поток — используй Runnable + Thread или пул потоков.' }
      ]
    },
    {
      id: 4,
      title: 'Жизненный цикл потока',
      type: 'theory',
      content: [
        { type: 'text', value: 'Каждый поток в Java проходит через определённые состояния (states) в течение своей жизни. Понимание жизненного цикла критически важно для отладки многопоточных программ. В Java у потока ровно 6 состояний, определённых в перечислении Thread.State.' },
        { type: 'heading', value: 'Диаграмма жизненного цикла' },
        { type: 'code', language: 'text', value: '                    ┌──────────────────────────────────────────────┐\n                    │           ЖИЗНЕННЫЙ ЦИКЛ ПОТОКА              │\n                    └──────────────────────────────────────────────┘\n\n                         new Thread()\n                              │\n                              ▼\n                        ┌───────────┐\n                        │    NEW    │  Поток создан, но не запущен\n                        └─────┬─────┘\n                              │ start()\n                              ▼\n                        ┌───────────┐\n              ┌────────▶│ RUNNABLE  │◀────────┐\n              │         └─┬───┬───┬─┘         │\n              │           │   │   │           │\n              │    synchronized  sleep(n)  wait()\n              │    блок занят │   join(n)  join()\n              │           │   │           │\n              │           ▼   ▼           ▼\n              │    ┌─────────┐ ┌────────┐ ┌─────────┐\n              │    │ BLOCKED │ │ TIMED  │ │ WAITING │\n              │    │         │ │WAITING │ │         │\n              │    └────┬────┘ └───┬────┘ └────┬────┘\n              │         │         │           │\n              │    монитор    timeout      notify()\n              │    свободен   истёк        notifyAll()\n              │         │         │           │\n              └─────────┴─────────┴───────────┘\n                              │\n                         run() завершён\n                         или исключение\n                              │\n                              ▼\n                       ┌────────────┐\n                       │ TERMINATED │  Поток завершён навсегда\n                       └────────────┘' },
        { type: 'heading', value: 'Подробно о каждом состоянии' },
        { type: 'code', language: 'text', value: '1. NEW (новый)\n   - Поток создан через new Thread(), но start() ещё не вызван\n   - Поток не потребляет CPU\n\n2. RUNNABLE (выполняемый)\n   - start() вызван, поток готов к работе\n   - Может реально работать на CPU или ожидать своей очереди в планировщике\n   - Внутри RUNNABLE два подсостояния: Ready и Running\n\n3. BLOCKED (заблокирован)\n   - Поток пытается войти в synchronized блок, но монитор занят\n   - Ждёт освобождения монитора другим потоком\n   - Автоматически перейдёт в RUNNABLE когда монитор освободится\n\n4. WAITING (ожидание без ограничения)\n   - Поток вызвал wait(), join() или LockSupport.park()\n   - Ждёт пока другой поток явно "разбудит" его\n   - Только notify()/notifyAll() вернёт из WAITING\n\n5. TIMED_WAITING (ожидание с таймаутом)\n   - Поток вызвал sleep(ms), join(ms), wait(ms)\n   - Ждёт заданное время, потом автоматически просыпается\n\n6. TERMINATED (завершён)\n   - Метод run() завершился (нормально или с исключением)\n   - Поток НЕЛЬЗЯ перезапустить — нужно создать новый' },
        { type: 'code', language: 'java', value: '// Наблюдаем за состояниями потока в реальном времени\npublic class ThreadLifecycleDemo {\n    public static void main(String[] args) throws InterruptedException {\n        Object lock = new Object();\n\n        Thread worker = new Thread(() -> {\n            try {\n                Thread.sleep(500); // → TIMED_WAITING\n            } catch (InterruptedException e) {\n                return;\n            }\n            synchronized (lock) { // может → BLOCKED если lock занят\n                // работа...\n            }\n        });\n\n        // Состояние: NEW\n        System.out.println("До start(): " + worker.getState()); // NEW\n\n        worker.start();\n\n        // Состояние: RUNNABLE (только стартовал)\n        System.out.println("После start(): " + worker.getState()); // RUNNABLE\n\n        Thread.sleep(100); // даём время вызвать sleep()\n        // Состояние: TIMED_WAITING (спит в sleep())\n        System.out.println("Во время sleep(): " + worker.getState()); // TIMED_WAITING\n\n        worker.join(); // ждём завершения\n\n        // Состояние: TERMINATED (run() завершён)\n        System.out.println("После join(): " + worker.getState()); // TERMINATED\n\n        // Попытка перезапуска:\n        try {\n            worker.start(); // IllegalThreadStateException!\n        } catch (IllegalThreadStateException e) {\n            System.out.println("Нельзя перезапустить: " + e.getMessage());\n        }\n    }\n}' },
        { type: 'heading', value: 'Мониторинг состояний в цикле' },
        { type: 'code', language: 'java', value: '// Полезная утилита для отладки\npublic class ThreadMonitor {\n    public static void main(String[] args) throws InterruptedException {\n        Thread worker = new Thread(() -> {\n            try {\n                Thread.sleep(1000);\n            } catch (InterruptedException e) {\n                Thread.currentThread().interrupt();\n            }\n        }, "Worker");\n\n        // Мониторим состояния\n        Thread monitor = new Thread(() -> {\n            Thread.State previousState = null;\n            while (worker.getState() != Thread.State.TERMINATED) {\n                Thread.State currentState = worker.getState();\n                if (currentState != previousState) {\n                    System.out.println("[Монитор] " + worker.getName()\n                        + " → " + currentState);\n                    previousState = currentState;\n                }\n            }\n            System.out.println("[Монитор] " + worker.getName() + " → TERMINATED");\n        }, "Monitor");\n\n        monitor.start();\n        Thread.sleep(50);\n        worker.start();\n        worker.join();\n        monitor.join();\n    }\n}\n// Вывод:\n// [Монитор] Worker → NEW\n// [Монитор] Worker → RUNNABLE\n// [Монитор] Worker → TIMED_WAITING\n// [Монитор] Worker → TERMINATED' },
        { type: 'tip', value: 'Запомни: поток никогда не возвращается из TERMINATED в другое состояние. Если нужно повторить задачу — создай новый Thread с тем же Runnable. Именно поэтому в реальных приложениях используют пулы потоков (ExecutorService) — они переиспользуют потоки.' }
      ]
    },
    {
      id: 5,
      title: 'sleep() — пауза потока',
      type: 'theory',
      content: [
        { type: 'text', value: 'Thread.sleep() — статический метод, который приостанавливает ТЕКУЩИЙ поток на указанное время. Поток переходит в состояние TIMED_WAITING и не потребляет CPU. После истечения времени поток снова становится RUNNABLE.' },
        { type: 'heading', value: 'Базовое использование sleep()' },
        { type: 'code', language: 'java', value: '// sleep принимает миллисекунды\npublic class SleepDemo {\n    public static void main(String[] args) {\n        System.out.println("Начало: " + System.currentTimeMillis());\n\n        try {\n            Thread.sleep(2000); // пауза 2 секунды (2000 мс)\n        } catch (InterruptedException e) {\n            // Кто-то вызвал interrupt() на этом потоке\n            System.out.println("Сон прерван!");\n            Thread.currentThread().interrupt(); // восстанавливаем флаг\n        }\n\n        System.out.println("Конец: " + System.currentTimeMillis());\n        // Разница ~2000 мс\n    }\n}' },
        { type: 'heading', value: 'TimeUnit — удобная альтернатива' },
        { type: 'code', language: 'java', value: 'import java.util.concurrent.TimeUnit;\n\n// Вместо вычисления миллисекунд используем TimeUnit\ntry {\n    Thread.sleep(5000);               // 5 секунд в мс — неочевидно\n    TimeUnit.SECONDS.sleep(5);        // 5 секунд — читаемо!\n\n    Thread.sleep(120000);             // 2 минуты? Надо считать...\n    TimeUnit.MINUTES.sleep(2);        // 2 минуты — понятно!\n\n    TimeUnit.MILLISECONDS.sleep(500); // 500 мс\n    TimeUnit.HOURS.sleep(1);          // 1 час\n} catch (InterruptedException e) {\n    Thread.currentThread().interrupt();\n}' },
        { type: 'heading', value: 'Что такое InterruptedException?' },
        { type: 'text', value: 'InterruptedException — это checked-исключение, которое бросается, если спящий поток был прерван вызовом interrupt(). Это механизм корректной остановки потоков в Java.' },
        { type: 'code', language: 'java', value: '// Поток спит, но его можно разбудить раньше\nThread sleeper = new Thread(() -> {\n    System.out.println("Ложусь спать на 10 секунд...");\n    try {\n        Thread.sleep(10_000); // 10 секунд\n        System.out.println("Проспал все 10 секунд"); // не выведется!\n    } catch (InterruptedException e) {\n        System.out.println("Меня разбудили раньше!");\n    }\n});\n\nsleeper.start();\nThread.sleep(2000); // ждём 2 секунды\nsleeper.interrupt(); // будим спящий поток!\n\n// Вывод:\n// Ложусь спать на 10 секунд...\n// (через 2 секунды)\n// Меня разбудили раньше!' },
        { type: 'heading', value: 'Временная шкала sleep()' },
        { type: 'code', language: 'text', value: 'Thread.sleep(3000):\n\nПоток:  [RUNNABLE]──▶[  TIMED_WAITING (3 сек)  ]──▶[RUNNABLE]\n                     │                          │\n                  sleep()                    timeout\n                  вызван                     истёк\nCPU:    [работает]   [НЕ потребляет CPU!]       [работает]\n\ninterrupt() во время sleep:\n\nПоток:  [RUNNABLE]──▶[  TIMED_WAIT... ]──X──▶[RUNNABLE]\n                     │                  │\n                  sleep()          interrupt()!\n                                  InterruptedException\n                                  бросается в catch блок' },
        { type: 'heading', value: 'Где используется sleep() на практике' },
        { type: 'code', language: 'java', value: '// 1. Polling — периодическая проверка состояния\nwhile (!task.isDone()) {\n    System.out.println("Задача ещё выполняется...");\n    Thread.sleep(1000); // проверяем раз в секунду\n}\n\n// 2. Rate limiting — ограничение скорости запросов\nfor (String url : urls) {\n    sendRequest(url);\n    Thread.sleep(100); // не более 10 запросов в секунду\n}\n\n// 3. Retry с задержкой — повтор при ошибке\nint attempts = 0;\nwhile (attempts < 3) {\n    try {\n        connectToServer();\n        break;\n    } catch (Exception e) {\n        attempts++;\n        System.out.println("Попытка " + attempts + " не удалась, жду...");\n        Thread.sleep(2000 * attempts); // 2с, 4с, 6с — увеличивающаяся задержка\n    }\n}\n\n// 4. Анимация / прогресс\nfor (int i = 0; i <= 100; i += 10) {\n    System.out.print("\\rЗагрузка: " + i + "%");\n    Thread.sleep(300);\n}\nSystem.out.println("\\rЗагрузка: 100% — Готово!");\n// \\r — возврат каретки, перезаписывает строку' },
        { type: 'warning', value: 'sleep() — статический метод! Он всегда усыпляет ТЕКУЩИЙ поток, а не тот, на котором вызван. Распространённая ошибка: t.sleep(1000) — усыпит НЕ поток t, а поток, в котором написан этот код!' },
        { type: 'note', value: 'sleep() не гарантирует точное время пробуждения. Поток проснётся не раньше указанного времени, но может проснуться немного позже — зависит от загрузки системы и планировщика ОС. Для точных таймингов используют ScheduledExecutorService.' }
      ]
    },
    {
      id: 6,
      title: 'join() — ожидание завершения потока',
      type: 'theory',
      content: [
        { type: 'text', value: 'Метод join() позволяет одному потоку дождаться завершения другого. Это фундаментальный механизм координации потоков. Когда вы вызываете t.join(), текущий поток блокируется до тех пор, пока поток t не завершится (перейдёт в TERMINATED).' },
        { type: 'heading', value: 'Базовый join()' },
        { type: 'code', language: 'java', value: 'public class JoinDemo {\n    public static void main(String[] args) throws InterruptedException {\n        Thread loader = new Thread(() -> {\n            System.out.println("Загрузка данных...");\n            try { Thread.sleep(2000); } catch (InterruptedException e) { return; }\n            System.out.println("Данные загружены!");\n        });\n\n        loader.start();\n\n        // Без join() — main не ждёт и сразу продолжает\n        // С join() — main блокируется пока loader не завершится\n        loader.join();\n\n        // Эта строка выполнится ТОЛЬКО после завершения loader\n        System.out.println("Обрабатываю загруженные данные...");\n    }\n}\n// Вывод (гарантированный порядок):\n// Загрузка данных...\n// Данные загружены!\n// Обрабатываю загруженные данные...' },
        { type: 'heading', value: 'Временная шкала: с join() и без' },
        { type: 'code', language: 'text', value: 'БЕЗ join() — main не ждёт:\n\nmain:   [start()]──[print("Обрабатываю")]──[END]\nloader: ──────────[загрузка 2 сек]──────[print("Загружено")]\n\nРезультат: "Обрабатываю" выведется ДО "Загружено" — неправильно!\n\nС join() — main ждёт завершения loader:\n\nmain:   [start()]──[join()...ЖДЁТ...ЖДЁТ...ЖДЁТ]──[print("Обрабатываю")]──[END]\nloader: ──────────[загрузка 2 сек]──────[print("Загружено")]──[TERMINATED]\n                                                              ↑\n                                                         main разблокирован!\n\nРезультат: "Обрабатываю" выведется ПОСЛЕ "Загружено" — правильно!' },
        { type: 'heading', value: 'join(timeout) — ожидание с лимитом' },
        { type: 'code', language: 'java', value: '// join(timeout) — ждём не более указанного времени\nThread slowTask = new Thread(() -> {\n    try { Thread.sleep(10_000); } catch (InterruptedException e) { return; }\n    System.out.println("Задача завершена");\n});\n\nslowTask.start();\n\n// Ждём максимум 3 секунды\nslowTask.join(3000);\n\nif (slowTask.isAlive()) {\n    System.out.println("Задача слишком долгая, не ждём!");\n    slowTask.interrupt(); // можно прервать\n} else {\n    System.out.println("Задача завершилась вовремя");\n}\n// Вывод: Задача слишком долгая, не ждём!' },
        { type: 'heading', value: 'Пример: параллельная загрузка → объединение результатов' },
        { type: 'code', language: 'java', value: 'import java.util.concurrent.atomic.AtomicReference;\n\npublic class ParallelLoadDemo {\n    public static void main(String[] args) throws InterruptedException {\n        // Контейнеры для результатов\n        AtomicReference<String> userData = new AtomicReference<>();\n        AtomicReference<String> orderData = new AtomicReference<>();\n        AtomicReference<String> productData = new AtomicReference<>();\n\n        // Три параллельные загрузки\n        Thread userThread = new Thread(() -> {\n            try { Thread.sleep(2000); } catch (InterruptedException e) { return; }\n            userData.set("Пользователь: Алина, ID: 42");\n            System.out.println("✓ Пользователь загружен (2 сек)");\n        });\n\n        Thread orderThread = new Thread(() -> {\n            try { Thread.sleep(3000); } catch (InterruptedException e) { return; }\n            orderData.set("Заказ #1001: MacBook Pro");\n            System.out.println("✓ Заказы загружены (3 сек)");\n        });\n\n        Thread productThread = new Thread(() -> {\n            try { Thread.sleep(1000); } catch (InterruptedException e) { return; }\n            productData.set("Товар: MacBook Pro, Цена: 2499$");\n            System.out.println("✓ Товары загружены (1 сек)");\n        });\n\n        long start = System.currentTimeMillis();\n\n        // Запускаем ВСЕ параллельно\n        userThread.start();\n        orderThread.start();\n        productThread.start();\n\n        // Ждём завершения ВСЕХ\n        userThread.join();\n        orderThread.join();\n        productThread.join();\n\n        long elapsed = System.currentTimeMillis() - start;\n\n        // Все данные готовы — собираем отчёт\n        System.out.println("\\n=== ОТЧЁТ ===\");\n        System.out.println(userData.get());\n        System.out.println(orderData.get());\n        System.out.println(productData.get());\n        System.out.println("Время: " + elapsed + " мс");\n        // ~3000 мс (а не 6000 мс последовательно!)\n    }\n}' },
        { type: 'code', language: 'text', value: 'Последовательно (без потоков):\nuser     [──2 сек──]\norder                 [──3 сек──]\nproduct                           [──1 сек──]\nИтого:   ──────────── 6 секунд ────────────▶\n\nПараллельно (с потоками + join):\nuser     [──2 сек──]\norder    [────3 сек────]\nproduct  [─1 сек─]\n         join() ждёт самый долгий\nИтого:   ───── 3 секунды ─────▶\n\nВыигрыш: 2x быстрее!' },
        { type: 'tip', value: 'join() — это как "точка сбора" (rendezvous point). Представь: три курьера разъехались по адресам за посылками. join() — это момент, когда вы ждёте возвращения всех трёх, прежде чем отправить посылки дальше.' },
        { type: 'warning', value: 'InterruptedException от join() нельзя игнорировать! Если текущий поток был прерван во время ожидания в join(), значит кто-то хочет его остановить. Минимум — восстановите флаг прерывания: Thread.currentThread().interrupt().' }
      ]
    },
    {
      id: 7,
      title: 'Приоритеты потоков',
      type: 'theory',
      content: [
        { type: 'text', value: 'Каждый поток в Java имеет приоритет — число от 1 до 10, определяющее "важность" потока для планировщика. Поток с более высоким приоритетом получает больше процессорного времени. Но это лишь рекомендация — планировщик ОС может её игнорировать.' },
        { type: 'heading', value: 'Константы приоритетов' },
        { type: 'code', language: 'java', value: '// Три предопределённые константы\nThread.MIN_PRIORITY  = 1;   // минимальный приоритет\nThread.NORM_PRIORITY = 5;   // нормальный (по умолчанию)\nThread.MAX_PRIORITY  = 10;  // максимальный приоритет\n\nThread t = new Thread(() -> {\n    System.out.println("Мой приоритет: " + Thread.currentThread().getPriority());\n});\n\nSystem.out.println("По умолчанию: " + t.getPriority()); // 5\n\nt.setPriority(Thread.MAX_PRIORITY); // 10\nSystem.out.println("После установки: " + t.getPriority()); // 10\n\nt.setPriority(Thread.MIN_PRIORITY); // 1\nSystem.out.println("Минимальный: " + t.getPriority()); // 1\n\n// Вне диапазона — исключение\ntry {\n    t.setPriority(11); // IllegalArgumentException!\n} catch (IllegalArgumentException e) {\n    System.out.println("Приоритет должен быть от 1 до 10");\n}' },
        { type: 'heading', value: 'Приоритеты на практике' },
        { type: 'code', language: 'java', value: '// Приоритеты — это РЕКОМЕНДАЦИЯ, а не гарантия!\npublic class PriorityDemo {\n    public static void main(String[] args) {\n        Runnable counter = () -> {\n            int count = 0;\n            long end = System.currentTimeMillis() + 1000; // 1 секунда\n            while (System.currentTimeMillis() < end) {\n                count++;\n            }\n            System.out.println(Thread.currentThread().getName()\n                + " (приоритет " + Thread.currentThread().getPriority()\n                + "): " + count + " итераций");\n        };\n\n        Thread low = new Thread(counter, "LOW");\n        Thread high = new Thread(counter, "HIGH");\n\n        low.setPriority(Thread.MIN_PRIORITY);   // 1\n        high.setPriority(Thread.MAX_PRIORITY);   // 10\n\n        low.start();\n        high.start();\n    }\n}\n// Возможный вывод:\n// HIGH (приоритет 10): 234567890 итераций\n// LOW  (приоритет 1):  198765432 итераций\n// HIGH выполнил БОЛЬШЕ итераций, но разница невелика!' },
        { type: 'heading', value: 'Как работает планировщик (scheduler)' },
        { type: 'code', language: 'text', value: 'Планировщик ОС решает, какой поток запустить:\n\n┌───────────────────────────────────────────────────┐\n│               ПЛАНИРОВЩИК (Scheduler)              │\n│                                                   │\n│  Очередь готовых потоков:                         │\n│  ┌─────────────────────────────────────────────┐  │\n│  │ [HIGH:10] [MED:5] [MED:5] [LOW:1] [LOW:1]  │  │\n│  └─────────────────────────────────────────────┘  │\n│                                                   │\n│  CPU Ядро 1: ←── HIGH:10 (получает больше)        │\n│  CPU Ядро 2: ←── MED:5   (получает нормально)     │\n│                                                   │\n│  LOW:1 тоже получит время, но реже                │\n│                                                   │\n│  ВАЖНО: Это только рекомендация!                  │\n│  ОС может распределять время по-своему.           │\n└───────────────────────────────────────────────────┘\n\nНа Windows приоритеты работают заметнее.\nНа Linux — почти не влияют (нужен nice/renice).' },
        { type: 'heading', value: 'Наследование приоритетов' },
        { type: 'code', language: 'java', value: '// Новый поток наследует приоритет родительского потока\npublic class InheritPriority {\n    public static void main(String[] args) {\n        // main поток имеет приоритет 5\n        Thread.currentThread().setPriority(8);\n\n        Thread child = new Thread(() -> {\n            System.out.println("Мой приоритет: "\n                + Thread.currentThread().getPriority());\n            // 8 — унаследовал от main!\n        });\n        child.start();\n    }\n}' },
        { type: 'warning', value: 'Никогда не полагайся на приоритеты для обеспечения корректности программы! Приоритеты — это подсказка планировщику, а не гарантия порядка выполнения. Для управления порядком используй join(), synchronized, Lock и другие механизмы синхронизации.' },
        { type: 'note', value: 'В реальных проектах приоритеты используются редко. Типичный случай — выделить важному потоку (обработка UI) высокий приоритет, а фоновой задаче (логирование, очистка) — низкий. Но даже это делается нечасто.' }
      ]
    },
    {
      id: 8,
      title: 'Daemon потоки',
      type: 'theory',
      content: [
        { type: 'text', value: 'В Java есть два типа потоков: пользовательские (user) и демон (daemon). Daemon-потоки — это фоновые потоки, которые автоматически завершаются, когда все пользовательские потоки закончили работу. JVM не ждёт завершения daemon-потоков — она просто их убивает.' },
        { type: 'heading', value: 'User vs Daemon потоки' },
        { type: 'code', language: 'text', value: '┌────────────────────────────────────────────────────────┐\n│           USER потоки          │     DAEMON потоки      │\n├────────────────────────────────┼────────────────────────┤\n│ JVM ждёт их завершения        │ JVM НЕ ждёт их         │\n│ Создаются по умолчанию        │ Нужен setDaemon(true)  │\n│ Важная бизнес-логика          │ Фоновые задачи         │\n│ main, обработчики запросов    │ GC, мониторинг, логи   │\n└────────────────────────────────┴────────────────────────┘\n\nПравило завершения JVM:\n\n  Все User-потоки завершены? ──▶ JVM завершается\n                                 (убивает все daemon-потоки)\n\nТаймлайн:\n\nmain (user):    [═══════════ работает ═══════════]──[END]\nworker (user):  [═══════════════ работает ═══════════════]──[END]\ngc (daemon):    [═══════ работает ═══════ работает ══]──✂── УБИТ!\nlogger (daemon):[═══════ работает ═══════ работает ══]──✂── УБИТ!\n                                                      ↑\n                                              все user-потоки\n                                              завершились → JVM exit' },
        { type: 'heading', value: 'Создание daemon-потока' },
        { type: 'code', language: 'java', value: 'public class DaemonDemo {\n    public static void main(String[] args) throws InterruptedException {\n        // Создаём daemon-поток\n        Thread daemon = new Thread(() -> {\n            while (true) { // бесконечный цикл!\n                System.out.println("[daemon] Мониторинг... "\n                    + System.currentTimeMillis());\n                try {\n                    Thread.sleep(500);\n                } catch (InterruptedException e) {\n                    break;\n                }\n            }\n            // Этот код может не выполниться — JVM убьёт поток!\n            System.out.println("[daemon] Завершаюсь корректно");\n        });\n\n        // ВАЖНО: setDaemon() вызывается ДО start()!\n        daemon.setDaemon(true);\n        daemon.start();\n\n        System.out.println("isDaemon: " + daemon.isDaemon()); // true\n\n        // main поток работает 2 секунды\n        Thread.sleep(2000);\n        System.out.println("[main] Завершаюсь...");\n\n        // main завершается → JVM убивает daemon → программа завершается\n        // daemon может выполнить ещё 0-1 итерацию и будет убит\n    }\n}\n// Вывод:\n// isDaemon: true\n// [daemon] Мониторинг... 1699900000000\n// [daemon] Мониторинг... 1699900000500\n// [daemon] Мониторинг... 1699900001000\n// [daemon] Мониторинг... 1699900001500\n// [main] Завершаюсь...\n// (программа завершается, daemon убит)' },
        { type: 'heading', value: 'Реальные примеры daemon-потоков' },
        { type: 'code', language: 'java', value: '// 1. Garbage Collector — самый известный daemon\n// GC работает в фоне, JVM не ждёт его завершения\n\n// 2. Автосохранение (как в текстовом редакторе)\nThread autoSave = new Thread(() -> {\n    while (true) {\n        saveDocument();\n        try { TimeUnit.MINUTES.sleep(1); } catch (InterruptedException e) { break; }\n    }\n});\nautoSave.setDaemon(true); // когда пользователь закроет — поток умрёт\nautoSave.start();\n\n// 3. Heartbeat / Health check\nThread heartbeat = new Thread(() -> {\n    while (true) {\n        sendHeartbeat(); // "я жив" сигнал на сервер\n        try { TimeUnit.SECONDS.sleep(30); } catch (InterruptedException e) { break; }\n    }\n});\nheartbeat.setDaemon(true);\nheartbeat.start();\n\n// 4. Мониторинг системных ресурсов\nThread monitor = new Thread(() -> {\n    while (true) {\n        Runtime rt = Runtime.getRuntime();\n        long usedMB = (rt.totalMemory() - rt.freeMemory()) / 1024 / 1024;\n        System.out.println("[Monitor] Память: " + usedMB + " MB");\n        try { TimeUnit.SECONDS.sleep(10); } catch (InterruptedException e) { break; }\n    }\n});\nmonitor.setDaemon(true);\nmonitor.start();' },
        { type: 'heading', value: 'Наследование daemon-статуса' },
        { type: 'code', language: 'java', value: '// Дочерний поток наследует daemon-статус от родительского\nThread parent = new Thread(() -> {\n    Thread child = new Thread(() -> {\n        System.out.println("Child isDaemon: "\n            + Thread.currentThread().isDaemon());\n    });\n    child.start(); // наследует daemon от parent\n});\nparent.setDaemon(true);\nparent.start();\n// Вывод: Child isDaemon: true' },
        { type: 'warning', value: 'setDaemon() нужно вызвать ДО start()! Вызов после start() бросит IllegalThreadStateException. Также будьте осторожны: daemon-потоки могут быть убиты в любой момент — не используйте их для записи в файлы или базу данных без flush/finalize.' },
        { type: 'tip', value: 'Простое правило: если поток выполняет важную работу (сохранение данных, обработка транзакции) — это user-поток. Если поток обслуживает другие потоки и может быть безопасно убит — это daemon.' }
      ]
    },
    {
      id: 9,
      title: 'Race condition — гонка потоков',
      type: 'theory',
      content: [
        { type: 'text', value: 'Race condition (состояние гонки) — это ошибка, которая возникает, когда два или более потоков одновременно читают и изменяют общие данные. Результат зависит от случайного порядка выполнения потоков. Это одна из самых коварных и сложных ошибок в программировании.' },
        { type: 'heading', value: 'Проблема: два потока инкрементируют счётчик' },
        { type: 'code', language: 'java', value: 'class SharedCounter {\n    int count = 0; // общая переменная — доступна всем потокам\n\n    void increment() {\n        count++; // ВЫГЛЯДИТ как одна операция, но это НЕ ТАК!\n    }\n}\n\npublic class RaceConditionDemo {\n    public static void main(String[] args) throws InterruptedException {\n        SharedCounter counter = new SharedCounter();\n\n        // 1000 потоков, каждый вызывает increment() один раз\n        Thread[] threads = new Thread[1000];\n        for (int i = 0; i < 1000; i++) {\n            threads[i] = new Thread(counter::increment);\n            threads[i].start();\n        }\n\n        // Ждём завершения всех\n        for (Thread t : threads) {\n            t.join();\n        }\n\n        System.out.println("Ожидаем: 1000");\n        System.out.println("Получили: " + counter.count);\n        // Получили: 987 (или 993, или 1000 — каждый раз по-разному!)\n    }\n}' },
        { type: 'heading', value: 'Почему count++ — НЕ атомарная операция' },
        { type: 'text', value: 'Операция count++ кажется одной командой, но на уровне процессора это ТРИ отдельных шага. Между этими шагами поток может быть прерван другим потоком.' },
        { type: 'code', language: 'text', value: 'count++ разбивается на 3 операции:\n\n  1. READ:  прочитать значение count из памяти в регистр CPU\n  2. ADD:   прибавить 1 к значению в регистре\n  3. WRITE: записать новое значение из регистра обратно в память\n\nВ однопоточной программе это безопасно:\n\n  Поток A:  [READ: 5] → [ADD: 6] → [WRITE: 6]\n  Результат: count = 6  ✓ Правильно' },
        { type: 'heading', value: 'Пошаговая визуализация гонки' },
        { type: 'code', language: 'text', value: 'Начальное значение: count = 5\nОжидаемый результат после двух increment(): count = 7\n\n═══ СЦЕНАРИЙ 1: Потоки выполняются последовательно (ПРАВИЛЬНО) ═══\n\nВремя │ Поток A              │ Поток B              │ count в памяти\n──────┼──────────────────────┼──────────────────────┼───────────────\n  t1  │ READ count → 5       │ (ждёт)               │ 5\n  t2  │ ADD 5+1 = 6          │ (ждёт)               │ 5\n  t3  │ WRITE 6 → count      │ (ждёт)               │ 6\n  t4  │ (готово)             │ READ count → 6       │ 6\n  t5  │                      │ ADD 6+1 = 7          │ 6\n  t6  │                      │ WRITE 7 → count      │ 7\n──────┴──────────────────────┴──────────────────────┴───────────────\nРезультат: count = 7  ✓ ПРАВИЛЬНО\n\n═══ СЦЕНАРИЙ 2: Потоки пересекаются (ГОНКА!) ═══\n\nВремя │ Поток A              │ Поток B              │ count в памяти\n──────┼──────────────────────┼──────────────────────┼───────────────\n  t1  │ READ count → 5       │                      │ 5\n  t2  │                      │ READ count → 5       │ 5  ← оба прочитали 5!\n  t3  │ ADD 5+1 = 6          │                      │ 5\n  t4  │                      │ ADD 5+1 = 6          │ 5\n  t5  │ WRITE 6 → count      │                      │ 6\n  t6  │                      │ WRITE 6 → count      │ 6  ← перезаписал!\n──────┴──────────────────────┴──────────────────────┴───────────────\nРезультат: count = 6  ✗ НЕПРАВИЛЬНО! Потеряли одно увеличение!\n\nПроблема: Поток B прочитал count ДО того, как A записал новое значение.\nОба прочитали 5, оба записали 6. Одно увеличение "потеряно".' },
        { type: 'heading', value: 'Более наглядный пример' },
        { type: 'code', language: 'java', value: '// Демонстрация гонки с подробным логированием\nclass DetailedCounter {\n    int count = 0;\n\n    void increment() {\n        int temp = count;     // READ — копируем в локальную переменную\n        // Именно здесь поток может быть прерван!\n        // Другой поток тоже прочитает старое значение!\n        temp = temp + 1;      // ADD — увеличиваем локальную копию\n        count = temp;         // WRITE — записываем обратно\n    }\n}\n\n// Запустим с малым числом потоков чтобы увидеть проблему\npublic class RaceDemo {\n    public static void main(String[] args) throws InterruptedException {\n        DetailedCounter counter = new DetailedCounter();\n\n        Thread t1 = new Thread(() -> {\n            for (int i = 0; i < 100_000; i++) {\n                counter.increment();\n            }\n        }, "Thread-A");\n\n        Thread t2 = new Thread(() -> {\n            for (int i = 0; i < 100_000; i++) {\n                counter.increment();\n            }\n        }, "Thread-B");\n\n        t1.start();\n        t2.start();\n        t1.join();\n        t2.join();\n\n        System.out.println("Ожидаем: 200000");\n        System.out.println("Получили: " + counter.count);\n        // Типичный результат: 156743, 178291, 192334 — каждый раз разный!\n    }\n}' },
        { type: 'heading', value: 'Аналогия: банковский счёт' },
        { type: 'code', language: 'text', value: 'Банковский счёт: баланс = 1000 руб.\nДва банкомата одновременно снимают 500 руб.\n\nПравильно (последовательно):\n  Банкомат A: читает 1000, снимает 500, записывает 500\n  Банкомат B: читает 500, снимает 500, записывает 0\n  Итог: 0 руб. ✓\n\nГонка (одновременно):\n  Банкомат A: читает 1000\n  Банкомат B: читает 1000  ← ещё не обновлено!\n  Банкомат A: записывает 500\n  Банкомат B: записывает 500  ← перезаписал!\n  Итог: 500 руб. ✗ Банк потерял 500 руб.!\n\nВ реальных банках это решается транзакциями и блокировками.\nВ Java — через synchronized, Lock, Atomic-классы.' },
        { type: 'warning', value: 'Race condition — это не ошибка, которая проявляется каждый раз. Она может не проявляться на вашем компьютере, но проявиться на продакшн-сервере под нагрузкой. Это делает её одной из самых опасных ошибок — тесты могут проходить, а на проде будут потери данных.' },
        { type: 'note', value: 'Решение race condition: synchronized (следующий модуль), volatile (для простых случаев), AtomicInteger/AtomicLong (для счётчиков), java.util.concurrent (профессиональные инструменты). В этом уроке мы только диагностируем проблему — решение впереди.' }
      ]
    },
    {
      id: 10,
      title: 'Volatile — видимость между потоками',
      type: 'theory',
      content: [
        { type: 'text', value: 'В Java каждый поток может кэшировать переменные в своём рабочей памяти (регистры CPU, кэш L1/L2). Из-за этого один поток может не видеть изменения, сделанные другим потоком. Ключевое слово volatile решает эту проблему, гарантируя видимость изменений.' },
        { type: 'heading', value: 'Проблема видимости без volatile' },
        { type: 'code', language: 'java', value: '// ПРОБЛЕМА: поток не видит изменение переменной\nclass StopFlag {\n    boolean stopped = false; // без volatile!\n}\n\npublic class VisibilityProblem {\n    public static void main(String[] args) throws InterruptedException {\n        StopFlag flag = new StopFlag();\n\n        Thread worker = new Thread(() -> {\n            System.out.println("Worker: запущен");\n            int count = 0;\n            // Этот цикл может НИКОГДА не завершиться!\n            while (!flag.stopped) {\n                count++;\n            }\n            System.out.println("Worker: остановлен после " + count + " итераций");\n        });\n\n        worker.start();\n        Thread.sleep(1000);\n\n        System.out.println("Main: устанавливаю stopped = true");\n        flag.stopped = true; // Worker может НЕ увидеть это изменение!\n\n        worker.join(3000);\n        if (worker.isAlive()) {\n            System.out.println("Worker ВСЁ ЕЩЁ работает! (не увидел изменение)");\n        }\n    }\n}' },
        { type: 'heading', value: 'Модель памяти Java (JMM)' },
        { type: 'code', language: 'text', value: 'БЕЗ volatile — каждый поток видит свою "копию":\n\n┌────────────────────┐      ┌────────────────────┐\n│     Поток A        │      │     Поток B        │\n│ ┌────────────────┐ │      │ ┌────────────────┐ │\n│ │ Локальный кэш  │ │      │ │ Локальный кэш  │ │\n│ │ stopped = false│ │      │ │ stopped = false│ │\n│ └───────┬────────┘ │      │ └───────┬────────┘ │\n└─────────┼──────────┘      └─────────┼──────────┘\n          │                           │\n          │    ┌──────────────────┐   │\n          └────│  ГЛАВНАЯ ПАМЯТЬ  │───┘\n               │ stopped = true   │\n               │ (обновлено A,    │\n               │  но B не видит!) │\n               └──────────────────┘\n\nA записал true в главную память, но B читает из СВОЕГО кэша!\n\nС volatile — принудительное чтение/запись из главной памяти:\n\n┌────────────────────┐      ┌────────────────────┐\n│     Поток A        │      │     Поток B        │\n│                    │      │                    │\n│ запись volatile ───┼──────┼──▶ чтение volatile │\n│ (flush в память)   │      │  (читает из памяти)│\n└────────────────────┘      └────────────────────┘\n               ┌──────────────────┐\n               │  ГЛАВНАЯ ПАМЯТЬ  │\n               │ stopped = true   │\n               │ ВСЕГДА актуально │\n               └──────────────────┘' },
        { type: 'heading', value: 'Решение с volatile' },
        { type: 'code', language: 'java', value: 'class StopFlag {\n    volatile boolean stopped = false; // volatile!\n}\n\npublic class VolatileSolution {\n    public static void main(String[] args) throws InterruptedException {\n        StopFlag flag = new StopFlag();\n\n        Thread worker = new Thread(() -> {\n            System.out.println("Worker: запущен");\n            long count = 0;\n            while (!flag.stopped) { // volatile гарантирует видимость!\n                count++;\n            }\n            System.out.println("Worker: остановлен после " + count + " итераций");\n        });\n\n        worker.start();\n        Thread.sleep(1000);\n\n        System.out.println("Main: устанавливаю stopped = true");\n        flag.stopped = true; // Worker УВИДИТ это изменение!\n\n        worker.join();\n        System.out.println("Worker завершён!");\n    }\n}\n// Теперь Worker гарантированно остановится!' },
        { type: 'heading', value: 'Happens-before — гарантия порядка' },
        { type: 'text', value: 'volatile устанавливает happens-before связь: запись volatile-переменной в одном потоке гарантированно видна при последующем чтении этой переменной в другом потоке. Более того, все записи ДО volatile-записи тоже становятся видимы.' },
        { type: 'code', language: 'java', value: '// happens-before пример\nclass Data {\n    int x = 0;           // обычная переменная\n    int y = 0;           // обычная переменная\n    volatile boolean ready = false; // volatile-флаг\n}\n\nData data = new Data();\n\n// Поток-писатель\nThread writer = new Thread(() -> {\n    data.x = 42;         // обычная запись\n    data.y = 100;        // обычная запись\n    data.ready = true;   // volatile запись → flush ВСЕГО в память\n});\n\n// Поток-читатель\nThread reader = new Thread(() -> {\n    while (!data.ready) { } // volatile чтение\n    // Если ready == true, то гарантированно:\n    System.out.println(data.x); // 42 (не 0!)\n    System.out.println(data.y); // 100 (не 0!)\n    // happens-before гарантирует: все записи ДО volatile\n    // видны ПОСЛЕ чтения volatile\n});' },
        { type: 'heading', value: 'Когда volatile достаточно, а когда нет' },
        { type: 'code', language: 'java', value: '// volatile ДОСТАТОЧНО, когда:\n// 1. Один поток ПИШЕТ, другие ЧИТАЮТ (флаг остановки)\nvolatile boolean running = true;\n\n// 2. Запись не зависит от текущего значения\nvolatile long timestamp; // один поток обновляет\ntimestamp = System.currentTimeMillis(); // не зависит от старого значения\n\n// volatile НЕ ДОСТАТОЧНО, когда:\n// 1. Операция "прочитать-изменить-записать" (count++)\nvolatile int count = 0;\ncount++; // НЕ АТОМАРНО даже с volatile!\n// volatile гарантирует видимость, но НЕ атомарность!\n\n// 2. Проверить-и-действовать\nvolatile boolean initialized = false;\nif (!initialized) {  // CHECK\n    initialize();     // ACT — между CHECK и ACT другой поток\n    initialized = true; // может тоже пройти CHECK!\n}\n\n// Для таких случаев нужен synchronized или AtomicInteger' },
        { type: 'warning', value: 'volatile НЕ заменяет synchronized! volatile гарантирует только видимость (visibility), но НЕ атомарность (atomicity). Операция count++ на volatile int всё ещё подвержена race condition. Для атомарных операций используй AtomicInteger или synchronized.' },
        { type: 'note', value: 'Мнемоника: volatile = "всегда свежее значение из памяти". Используй volatile для простых флагов (boolean running, boolean stopped, long timestamp), где один поток пишет, а другие читают. Для всего остального — synchronized или Atomic-классы.' }
      ]
    },
    {
      id: 11,
      title: 'Прерывание потоков — interrupt()',
      type: 'theory',
      content: [
        { type: 'text', value: 'В Java нет способа принудительно убить поток (метод stop() deprecated и опасен). Вместо этого используется кооперативный механизм прерывания: один поток просит другой остановиться, а тот проверяет запрос и корректно завершается.' },
        { type: 'heading', value: 'Как работает interrupt()' },
        { type: 'code', language: 'text', value: 'Механизм прерывания:\n\n  Поток A                         Поток B (worker)\n     │                               │\n     │── interrupt() ──────────────▶ │ (устанавливает флаг interrupted = true)\n     │                               │\n     │                               │ Два варианта:\n     │                               │\n     │                  ┌────────────┤\n     │                  │            │\n     │              Поток спит    Поток работает\n     │              (sleep/wait/  (цикл вычислений)\n     │               join)        │\n     │                  │            │\n     │          InterruptedException  isInterrupted()\n     │          бросается!           проверяет флаг\n     │                  │            │\n     │              catch блок    if (isInterrupted())\n     │              обрабатывает      завершается\n     │              прерывание\n     │                  │            │\n     │                  └────────────┤\n     │                               │\n     │                         Поток корректно\n     │                         завершает работу' },
        { type: 'heading', value: 'Прерывание спящего потока' },
        { type: 'code', language: 'java', value: '// Если поток спит (sleep/wait/join) — бросается InterruptedException\nThread worker = new Thread(() -> {\n    try {\n        System.out.println("Работаю 10 секунд...");\n        for (int i = 1; i <= 10; i++) {\n            System.out.println("Секунда " + i);\n            Thread.sleep(1000); // может быть прервано!\n        }\n        System.out.println("Работа завершена нормально");\n    } catch (InterruptedException e) {\n        // sleep() бросил исключение, потому что вызвали interrupt()\n        System.out.println("Работа прервана на полпути!");\n        // Хорошая практика — восстановить флаг:\n        Thread.currentThread().interrupt();\n    }\n});\n\nworker.start();\nThread.sleep(3000); // даём поработать 3 секунды\n\nSystem.out.println("Прерываю worker...");\nworker.interrupt(); // ПРОСИМ поток остановиться\n\nworker.join();\nSystem.out.println("Worker завершён");\n\n// Вывод:\n// Работаю 10 секунд...\n// Секунда 1\n// Секунда 2\n// Секунда 3\n// Прерываю worker...\n// Работа прервана на полпути!\n// Worker завершён' },
        { type: 'heading', value: 'Прерывание работающего потока (isInterrupted)' },
        { type: 'code', language: 'java', value: '// Если поток НЕ спит — нужно самому проверять флаг\nThread calculator = new Thread(() -> {\n    long sum = 0;\n    long i = 0;\n\n    // Проверяем isInterrupted() в каждой итерации\n    while (!Thread.currentThread().isInterrupted()) {\n        sum += i;\n        i++;\n        if (i % 100_000_000 == 0) {\n            System.out.println("Вычислено: " + i + ", сумма: " + sum);\n        }\n    }\n\n    System.out.println("Прервано! Итого: " + i + " итераций, сумма: " + sum);\n});\n\ncalculator.start();\nThread.sleep(2000); // даём поработать 2 секунды\n\ncalculator.interrupt(); // устанавливаем флаг\ncalculator.join();\n// Поток проверит флаг на следующей итерации цикла и завершится' },
        { type: 'heading', value: 'isInterrupted() vs Thread.interrupted()' },
        { type: 'code', language: 'java', value: '// isInterrupted() — проверяет флаг, НЕ сбрасывает его\nThread t = Thread.currentThread();\nt.interrupt();\nSystem.out.println(t.isInterrupted()); // true\nSystem.out.println(t.isInterrupted()); // true (флаг сохраняется)\n\n// Thread.interrupted() — статический, проверяет И СБРАСЫВАЕТ флаг\nThread.currentThread().interrupt();\nSystem.out.println(Thread.interrupted()); // true\nSystem.out.println(Thread.interrupted()); // false! (флаг сброшен)' },
        { type: 'heading', value: 'Правильная обработка прерывания' },
        { type: 'code', language: 'java', value: '// Паттерн: корректная остановка потока с очисткой ресурсов\nThread dbWorker = new Thread(() -> {\n    System.out.println("Подключаюсь к БД...");\n    // Имитация подключения\n    try {\n        while (!Thread.currentThread().isInterrupted()) {\n            // Основная работа\n            processNextRecord();\n            Thread.sleep(100); // пауза между обработками\n        }\n    } catch (InterruptedException e) {\n        System.out.println("Получен сигнал прерывания");\n        Thread.currentThread().interrupt(); // восстанавливаем флаг!\n    } finally {\n        // ВСЕГДА выполняется — закрываем ресурсы\n        System.out.println("Закрываю соединение с БД...");\n        // closeConnection();\n    }\n    System.out.println("Поток корректно завершён");\n});' },
        { type: 'warning', value: 'Никогда не используй Thread.stop() — он deprecated и опасен! stop() мгновенно убивает поток, не давая ему закрыть ресурсы, освободить блокировки или завершить транзакции. Это может повредить данные. Всегда используй interrupt() + проверку флага.' },
        { type: 'tip', value: 'Золотое правило: если поймал InterruptedException — либо (1) восстанови флаг через Thread.currentThread().interrupt(), либо (2) брось исключение выше (throws InterruptedException). НЕ глотай исключение молча — это скроет запрос на остановку.' }
      ]
    },
    {
      id: 12,
      title: 'ThreadLocal — своя копия для каждого потока',
      type: 'theory',
      content: [
        { type: 'text', value: 'ThreadLocal<T> — это специальный контейнер, который хранит отдельную копию значения для каждого потока. Каждый поток видит только свою копию и не может повлиять на значения других потоков. Это элегантное решение для данных, привязанных к потоку.' },
        { type: 'heading', value: 'Проблема без ThreadLocal' },
        { type: 'code', language: 'java', value: '// Проблема: общий SimpleDateFormat — НЕ потокобезопасный!\nimport java.text.SimpleDateFormat;\nimport java.util.Date;\n\nclass UnsafeFormatter {\n    // Один форматтер на все потоки — RACE CONDITION!\n    static SimpleDateFormat formatter = new SimpleDateFormat("dd-MM-yyyy HH:mm:ss");\n\n    static String formatDate(Date date) {\n        return formatter.format(date); // потоки мешают друг другу!\n    }\n}\n// При параллельном вызове: "25-13-2024 99:61:00" — мусор!' },
        { type: 'heading', value: 'Решение: ThreadLocal' },
        { type: 'code', language: 'java', value: 'import java.text.SimpleDateFormat;\nimport java.util.Date;\n\nclass SafeFormatter {\n    // Каждый поток получает СВОЙ экземпляр SimpleDateFormat\n    private static final ThreadLocal<SimpleDateFormat> formatter =\n        ThreadLocal.withInitial(\n            () -> new SimpleDateFormat("dd-MM-yyyy HH:mm:ss")\n        );\n\n    static String formatDate(Date date) {\n        return formatter.get().format(date); // безопасно!\n    }\n}\n\n// Каждый поток работает со своей копией\npublic class ThreadLocalDemo {\n    public static void main(String[] args) {\n        Runnable task = () -> {\n            String result = SafeFormatter.formatDate(new Date());\n            System.out.println(Thread.currentThread().getName() + ": " + result);\n        };\n\n        // 5 потоков — каждый использует свой форматтер\n        for (int i = 0; i < 5; i++) {\n            new Thread(task, "Worker-" + i).start();\n        }\n    }\n}' },
        { type: 'heading', value: 'Как работает ThreadLocal внутри' },
        { type: 'code', language: 'text', value: 'ThreadLocal хранит Map внутри каждого потока:\n\n┌──────────────────────┐\n│      Поток-1         │\n│ ┌──────────────────┐ │\n│ │ ThreadLocalMap    │ │\n│ │ key: formatter   │──── → SimpleDateFormat#1\n│ │ key: userId      │──── → "user-42"\n│ │ key: txId        │──── → "tx-001"\n│ └──────────────────┘ │\n└──────────────────────┘\n\n┌──────────────────────┐\n│      Поток-2         │\n│ ┌──────────────────┐ │\n│ │ ThreadLocalMap    │ │\n│ │ key: formatter   │──── → SimpleDateFormat#2  (другой объект!)\n│ │ key: userId      │──── → "user-99"\n│ │ key: txId        │──── → "tx-002"\n│ └──────────────────┘ │\n└──────────────────────┘\n\nКаждый поток имеет свою Map, хранящуюся внутри Thread.\nget() → находит Map текущего потока → возвращает значение по ключу.\nset() → находит Map текущего потока → записывает значение по ключу.' },
        { type: 'heading', value: 'Практический пример: User Session' },
        { type: 'code', language: 'java', value: '// На веб-сервере каждый запрос обрабатывается в своём потоке.\n// ThreadLocal идеально подходит для хранения контекста запроса.\n\nclass UserContext {\n    private static final ThreadLocal<String> currentUser = new ThreadLocal<>();\n    private static final ThreadLocal<String> requestId = new ThreadLocal<>();\n\n    static void setUser(String user) { currentUser.set(user); }\n    static String getUser() { return currentUser.get(); }\n\n    static void setRequestId(String id) { requestId.set(id); }\n    static String getRequestId() { return requestId.get(); }\n\n    static void clear() {\n        currentUser.remove(); // ОБЯЗАТЕЛЬНО очищать!\n        requestId.remove();\n    }\n}\n\n// Имитация обработки HTTP-запросов\npublic class WebServerSimulation {\n    public static void main(String[] args) {\n        // Каждый "запрос" в своём потоке\n        for (int i = 1; i <= 3; i++) {\n            final int requestNum = i;\n            new Thread(() -> {\n                try {\n                    // Фильтр устанавливает контекст\n                    UserContext.setUser("User-" + requestNum);\n                    UserContext.setRequestId("REQ-" + requestNum);\n\n                    // Бизнес-логика использует контекст\n                    processRequest();\n                    saveToDatabase();\n                } finally {\n                    UserContext.clear(); // ОБЯЗАТЕЛЬНО!\n                }\n            }, "HTTP-Thread-" + i).start();\n        }\n    }\n\n    static void processRequest() {\n        // Не нужно передавать user через параметры!\n        System.out.println("[" + UserContext.getRequestId() + "] "\n            + "Обработка для " + UserContext.getUser());\n    }\n\n    static void saveToDatabase() {\n        System.out.println("[" + UserContext.getRequestId() + "] "\n            + "Сохранение данных " + UserContext.getUser());\n    }\n}\n// Каждый поток видит СВОЕГО пользователя, не мешая другим!' },
        { type: 'heading', value: 'API ThreadLocal' },
        { type: 'code', language: 'java', value: '// Создание\nThreadLocal<String> tl = new ThreadLocal<>();       // начальное значение null\nThreadLocal<Integer> tl2 = ThreadLocal.withInitial(() -> 0); // начальное значение 0\n\n// Основные методы\ntl.set("значение");     // установить для текущего потока\nString val = tl.get();  // получить для текущего потока\ntl.remove();            // удалить для текущего потока\n\n// Пример: счётчик вызовов для каждого потока\nThreadLocal<Integer> callCount = ThreadLocal.withInitial(() -> 0);\n\nvoid processRequest() {\n    callCount.set(callCount.get() + 1);\n    System.out.println(Thread.currentThread().getName()\n        + " обработал " + callCount.get() + " запросов");\n}' },
        { type: 'warning', value: 'ОБЯЗАТЕЛЬНО вызывай remove() после использования ThreadLocal! Особенно в пулах потоков (ExecutorService), где потоки переиспользуются. Без remove() данные предыдущего запроса "утекут" в следующий — это серьёзная уязвимость и утечка памяти.' },
        { type: 'note', value: 'ThreadLocal широко используется в Spring Framework (хранение транзакций, SecurityContext), в логировании (MDC — Mapped Diagnostic Context для SLF4J/Logback), в JDBC (хранение соединений). Если вы работали со Spring — вы уже использовали ThreadLocal, даже не зная об этом.' }
      ]
    },
    {
      id: 13,
      title: 'Проблемы многопоточности',
      type: 'theory',
      content: [
        { type: 'text', value: 'Многопоточное программирование открывает дверь к целому классу уникальных проблем, которых не существует в однопоточном мире. Три главных врага: deadlock (взаимная блокировка), livelock (живая блокировка) и starvation (голодание). Понимание этих проблем — ключ к написанию надёжного кода.' },
        { type: 'heading', value: 'Deadlock — взаимная блокировка' },
        { type: 'text', value: 'Deadlock — ситуация, когда два (или более) потока навечно заблокированы, ожидая друг друга. Каждый держит ресурс, нужный другому, и никто не может продолжить работу.' },
        { type: 'code', language: 'text', value: '═══ ВИЗУАЛИЗАЦИЯ DEADLOCK ═══\n\n  Поток A                          Поток B\n     │                                │\n     │── захватил Lock-1 ──┐          │\n     │                     │          │── захватил Lock-2 ──┐\n     │                     │          │                     │\n     │── хочет Lock-2 ─────┼──────▶ [ЗАНЯТ Потоком B]      │\n     │   ЖДЁТ...           │          │                     │\n     │   ЖДЁТ...           │          │── хочет Lock-1 ─────┼──▶ [ЗАНЯТ Потоком A]\n     │   ЖДЁТ...           │          │   ЖДЁТ...           │\n     │   ЖДЁТ...           │          │   ЖДЁТ...           │\n     │   НАВЕЧНО!          │          │   НАВЕЧНО!          │\n     ▼                     │          ▼                     │\n  💀 DEADLOCK              │       💀 DEADLOCK              │\n                           │                               │\n            A держит Lock-1, ждёт Lock-2\n            B держит Lock-2, ждёт Lock-1\n            → Никто не может освободить свой Lock!\n\nАналогия: два человека в узком коридоре.\nКаждый хочет пройти, но никто не уступает.\nОба стоят навечно.' },
        { type: 'code', language: 'java', value: '// Классический deadlock\npublic class DeadlockDemo {\n    private static final Object lock1 = new Object();\n    private static final Object lock2 = new Object();\n\n    public static void main(String[] args) {\n        Thread threadA = new Thread(() -> {\n            synchronized (lock1) {\n                System.out.println("A: захватил lock1");\n                try { Thread.sleep(100); } catch (InterruptedException e) { return; }\n                System.out.println("A: пытаюсь захватить lock2...");\n                synchronized (lock2) {\n                    System.out.println("A: захватил lock2"); // никогда не выведется!\n                }\n            }\n        }, "Thread-A");\n\n        Thread threadB = new Thread(() -> {\n            synchronized (lock2) {\n                System.out.println("B: захватил lock2");\n                try { Thread.sleep(100); } catch (InterruptedException e) { return; }\n                System.out.println("B: пытаюсь захватить lock1...");\n                synchronized (lock1) {\n                    System.out.println("B: захватил lock1"); // никогда не выведется!\n                }\n            }\n        }, "Thread-B");\n\n        threadA.start();\n        threadB.start();\n\n        // Программа "зависнет" навсегда!\n        // Вывод:\n        // A: захватил lock1\n        // B: захватил lock2\n        // A: пытаюсь захватить lock2...\n        // B: пытаюсь захватить lock1...\n        // (тишина... оба ждут навечно)\n    }\n}' },
        { type: 'heading', value: 'Как предотвратить deadlock' },
        { type: 'code', language: 'java', value: '// Решение: всегда захватывать блокировки в ОДНОМ порядке!\n// Если все потоки захватывают lock1 → lock2, deadlock невозможен.\n\nThread threadA = new Thread(() -> {\n    synchronized (lock1) {        // сначала lock1\n        synchronized (lock2) {    // потом lock2\n            // работа\n        }\n    }\n});\n\nThread threadB = new Thread(() -> {\n    synchronized (lock1) {        // тоже сначала lock1!\n        synchronized (lock2) {    // потом lock2\n            // работа\n        }\n    }\n});\n// Теперь B ждёт lock1, а не захватывает lock2 первым → нет deadlock' },
        { type: 'heading', value: 'Livelock — живая блокировка' },
        { type: 'text', value: 'Livelock — потоки не заблокированы, они активно работают, но не продвигаются вперёд. Как два человека в коридоре, которые оба пытаются уступить друг другу и двигаются в одну сторону.' },
        { type: 'code', language: 'text', value: '═══ ВИЗУАЛИЗАЦИЯ LIVELOCK ═══\n\nДва человека в коридоре:\n\n Шаг 1:  →A   B←     (идут навстречу)\n Шаг 2:   A→ ←B      (оба уступают вправо)\n Шаг 3:  →A   B←     (снова блокируют друг друга)\n Шаг 4:   A→ ←B      (снова уступают)\n ...бесконечно!\n\nОтличие от deadlock:\n- Deadlock: потоки СПЯТ (не потребляют CPU)\n- Livelock: потоки РАБОТАЮТ (потребляют CPU), но бесполезно' },
        { type: 'code', language: 'java', value: '// Пример livelock: потоки бесконечно уступают друг другу\nclass Spoon {\n    String owner;\n    Spoon(String owner) { this.owner = owner; }\n    synchronized void setOwner(String owner) { this.owner = owner; }\n    synchronized String getOwner() { return owner; }\n}\n\nclass Diner {\n    String name;\n    boolean isHungry = true;\n\n    Diner(String name) { this.name = name; }\n\n    void eatWith(Spoon spoon, Diner partner) {\n        while (isHungry) {\n            if (!spoon.getOwner().equals(name)) {\n                continue; // ложка не у меня, жду\n            }\n            // Если партнёр тоже голоден — уступаю\n            if (partner.isHungry) {\n                System.out.println(name + ": \"Ешь первым, " + partner.name + "!\"");\n                spoon.setOwner(partner.name);\n                continue; // livelock! Оба постоянно уступают\n            }\n            // Ем\n            isHungry = false;\n            System.out.println(name + ": поел!");\n            spoon.setOwner(partner.name);\n        }\n    }\n}' },
        { type: 'heading', value: 'Starvation — голодание' },
        { type: 'text', value: 'Starvation — ситуация, когда поток технически может работать, но постоянно не получает доступ к ресурсу, потому что другие потоки с более высоким приоритетом или удачным таймингом забирают его первыми.' },
        { type: 'code', language: 'text', value: '═══ ВИЗУАЛИЗАЦИЯ STARVATION ═══\n\n  Ресурс (synchronized блок):\n  ┌─────────────────────────────────────────┐\n  │                                         │\n  │ Время: ══▶                              │\n  │                                         │\n  │ Поток A: [РАБОТАЕТ][РАБОТАЕТ][РАБОТАЕТ] │\n  │ Поток B: [РАБОТАЕТ][РАБОТАЕТ][РАБОТАЕТ] │\n  │ Поток C:  [ЖДЁТ...]  [ЖДЁТ...]  [ЖДЁТ] │ ← STARVATION!\n  │                                         │\n  │ A и B постоянно захватывают ресурс,     │\n  │ C никогда не получает свою очередь.     │\n  └─────────────────────────────────────────┘\n\nПричины:\n- Слишком высокий приоритет у других потоков\n- Невезение в планировщике\n- Слишком долгие synchronized блоки' },
        { type: 'heading', value: 'Сводная таблица проблем' },
        { type: 'code', language: 'text', value: '┌──────────────┬────────────────┬─────────────────┬───────────────────┐\n│              │   DEADLOCK     │    LIVELOCK      │   STARVATION      │\n├──────────────┼────────────────┼─────────────────┼───────────────────┤\n│ Потоки       │ Спят/ждут      │ Активны         │ Один/несколько    │\n│              │                │                 │ ждут              │\n├──────────────┼────────────────┼─────────────────┼───────────────────┤\n│ CPU          │ Не потребляет  │ Потребляет      │ Частично          │\n│              │                │ впустую         │                   │\n├──────────────┼────────────────┼─────────────────┼───────────────────┤\n│ Прогресс     │ Никто не       │ Все "работают"  │ Некоторые не      │\n│              │ продвигается   │ но без толку    │ продвигаются      │\n├──────────────┼────────────────┼─────────────────┼───────────────────┤\n│ Решение      │ Одинаковый     │ Случайная       │ Fair Lock,        │\n│              │ порядок        │ задержка        │ справедливый      │\n│              │ блокировок     │ (jitter)        │ планировщик       │\n├──────────────┼────────────────┼─────────────────┼───────────────────┤\n│ Аналогия     │ Два человека   │ Два человека    │ Очередь, где      │\n│              │ в коридоре,    │ уступают друг   │ кто-то постоянно  │\n│              │ оба стоят      │ другу бесконечно│ пропускает вперёд │\n└──────────────┴────────────────┴─────────────────┴───────────────────┘' },
        { type: 'warning', value: 'Deadlock — самая распространённая проблема. Её можно обнаружить через jstack (дамп потоков JVM), JVisualVM или IDE debugger. Если программа "зависла" и не отвечает — первое подозрение на deadlock.' },
        { type: 'tip', value: 'Четыре условия deadlock (все должны выполняться одновременно): (1) Mutual exclusion — ресурс нельзя разделить; (2) Hold and wait — поток держит ресурс и ждёт другой; (3) No preemption — ресурс нельзя отобрать; (4) Circular wait — циклическое ожидание. Убрав любое условие — вы предотвращаете deadlock. Обычно убирают (4) — упорядочивают захват блокировок.' }
      ]
    },
    {
      id: 14,
      title: 'Практика: Параллельный загрузчик',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создай программу, которая параллельно "скачивает" N файлов. Каждый файл скачивается в отдельном потоке. После завершения всех загрузок выведи отчёт: какой файл сколько времени скачивался и общее время.',
      requirements: [
        'Создай метод simulateDownload(String fileName, int sizeMB), который: выводит "Начало загрузки: <fileName> (<sizeMB> MB)", спит sizeMB * 300 мс (имитация), выводит "Загружен: <fileName>"',
        'Создай 5 потоков для файлов: "report.pdf" (2 MB), "photo.jpg" (5 MB), "data.csv" (1 MB), "video.mp4" (8 MB), "archive.zip" (3 MB)',
        'Каждому потоку задай имя "Loader-N" (N от 1 до 5)',
        'Запусти все потоки, дождись завершения ВСЕХ через join()',
        'Замерь общее время (System.currentTimeMillis) и выведи: "Все загрузки завершены за <время> мс" и "Последовательно заняло бы: <сумма> мс"'
      ],
      expectedOutput: 'Начало загрузки: report.pdf (2 MB)\nНачало загрузки: photo.jpg (5 MB)\nНачало загрузки: data.csv (1 MB)\nНачало загрузки: video.mp4 (8 MB)\nНачало загрузки: archive.zip (3 MB)\nЗагружен: data.csv\nЗагружен: report.pdf\nЗагружен: archive.zip\nЗагружен: photo.jpg\nЗагружен: video.mp4\nВсе загрузки завершены за ~2400 мс\nПоследовательно заняло бы: 5700 мс',
      hint: 'Общее время при параллельной загрузке определяется самым долгим файлом (video.mp4 = 8*300 = 2400 мс). Последовательное время = сумма всех: (2+5+1+8+3)*300 = 5700 мс. Для замера времени используй long start = System.currentTimeMillis() до запуска и long elapsed = System.currentTimeMillis() - start после всех join().',
      solution: 'public class ParallelDownloader {\n\n    static void simulateDownload(String fileName, int sizeMB) {\n        System.out.println("Начало загрузки: " + fileName + " (" + sizeMB + " MB)");\n        try {\n            Thread.sleep(sizeMB * 300L);\n        } catch (InterruptedException e) {\n            System.out.println("Загрузка прервана: " + fileName);\n            Thread.currentThread().interrupt();\n            return;\n        }\n        System.out.println("Загружен: " + fileName);\n    }\n\n    public static void main(String[] args) throws InterruptedException {\n        String[][] files = {\n            {"report.pdf", "2"},\n            {"photo.jpg", "5"},\n            {"data.csv", "1"},\n            {"video.mp4", "8"},\n            {"archive.zip", "3"}\n        };\n\n        Thread[] threads = new Thread[files.length];\n        int totalSequential = 0;\n\n        long start = System.currentTimeMillis();\n\n        for (int i = 0; i < files.length; i++) {\n            String name = files[i][0];\n            int size = Integer.parseInt(files[i][1]);\n            totalSequential += size * 300;\n\n            threads[i] = new Thread(\n                () -> simulateDownload(name, size),\n                "Loader-" + (i + 1)\n            );\n            threads[i].start();\n        }\n\n        for (Thread t : threads) {\n            t.join();\n        }\n\n        long elapsed = System.currentTimeMillis() - start;\n\n        System.out.println("Все загрузки завершены за " + elapsed + " мс");\n        System.out.println("Последовательно заняло бы: " + totalSequential + " мс");\n    }\n}',
      explanation: 'Все 5 потоков запускаются почти одновременно. Каждый "скачивает" свой файл независимо. Общее время определяется самым медленным файлом — video.mp4 (8*300=2400 мс). Параллельная загрузка экономит (5700-2400)=3300 мс по сравнению с последовательной. join() на каждом потоке гарантирует, что итоговое сообщение выведется только после завершения всех загрузок.'
    },
    {
      id: 15,
      title: 'Практика: Гонка потоков',
      type: 'practice',
      difficulty: 'medium',
      description: 'Продемонстрируй проблему race condition: два потока параллельно инкрементируют общий счётчик. Покажи, что результат непредсказуем без синхронизации, и исправь это с помощью volatile + проблема сохраняется, а затем с AtomicInteger.',
      requirements: [
        'Создай класс UnsafeCounter с полем int count = 0 и методом increment(), который делает count++',
        'Создай класс AtomicCounter с полем AtomicInteger count = new AtomicInteger(0) и методом increment(), который делает count.incrementAndGet()',
        'Для каждого счётчика запусти 2 потока, каждый вызывает increment() 100_000 раз',
        'Дождись завершения всех потоков через join()',
        'Выведи ожидаемый результат (200000) и фактический для обоих счётчиков',
        'Покажи, что UnsafeCounter даёт неправильный результат, а AtomicCounter — правильный'
      ],
      expectedOutput: '--- UnsafeCounter (без синхронизации) ---\nОжидаемый результат: 200000\nФактический результат: 173542\nРезультат НЕПРАВИЛЬНЫЙ! Потеряно инкрементов: 26458\n\n--- AtomicCounter (AtomicInteger) ---\nОжидаемый результат: 200000\nФактический результат: 200000\nРезультат ПРАВИЛЬНЫЙ!',
      hint: 'UnsafeCounter будет давать результат меньше 200000 (каждый запуск — разный). AtomicInteger.incrementAndGet() — атомарная операция, гарантирующая корректность. Импорт: java.util.concurrent.atomic.AtomicInteger. Число потерянных инкрементов = 200000 - фактический результат.',
      solution: 'import java.util.concurrent.atomic.AtomicInteger;\n\npublic class RaceConditionDemo {\n\n    static class UnsafeCounter {\n        int count = 0;\n        void increment() {\n            count++;\n        }\n    }\n\n    static class AtomicCounter {\n        AtomicInteger count = new AtomicInteger(0);\n        void increment() {\n            count.incrementAndGet();\n        }\n    }\n\n    static void testUnsafe() throws InterruptedException {\n        System.out.println("--- UnsafeCounter (без синхронизации) ---");\n        UnsafeCounter counter = new UnsafeCounter();\n        int iterations = 100_000;\n\n        Thread t1 = new Thread(() -> {\n            for (int i = 0; i < iterations; i++) counter.increment();\n        });\n        Thread t2 = new Thread(() -> {\n            for (int i = 0; i < iterations; i++) counter.increment();\n        });\n\n        t1.start();\n        t2.start();\n        t1.join();\n        t2.join();\n\n        int expected = iterations * 2;\n        System.out.println("Ожидаемый результат: " + expected);\n        System.out.println("Фактический результат: " + counter.count);\n        if (counter.count != expected) {\n            System.out.println("Результат НЕПРАВИЛЬНЫЙ! Потеряно инкрементов: "\n                + (expected - counter.count));\n        } else {\n            System.out.println("Результат правильный (повезло!)");\n        }\n    }\n\n    static void testAtomic() throws InterruptedException {\n        System.out.println("\\n--- AtomicCounter (AtomicInteger) ---");\n        AtomicCounter counter = new AtomicCounter();\n        int iterations = 100_000;\n\n        Thread t1 = new Thread(() -> {\n            for (int i = 0; i < iterations; i++) counter.increment();\n        });\n        Thread t2 = new Thread(() -> {\n            for (int i = 0; i < iterations; i++) counter.increment();\n        });\n\n        t1.start();\n        t2.start();\n        t1.join();\n        t2.join();\n\n        int expected = iterations * 2;\n        System.out.println("Ожидаемый результат: " + expected);\n        System.out.println("Фактический результат: " + counter.count.get());\n        if (counter.count.get() == expected) {\n            System.out.println("Результат ПРАВИЛЬНЫЙ!");\n        }\n    }\n\n    public static void main(String[] args) throws InterruptedException {\n        testUnsafe();\n        testAtomic();\n    }\n}',
      explanation: 'UnsafeCounter теряет инкременты из-за race condition: count++ — это три операции (read, add, write), между которыми другой поток может вмешаться. AtomicInteger.incrementAndGet() выполняет инкремент атомарно (как одну неделимую операцию) с помощью CAS (Compare-And-Swap) на уровне процессора. Volatile не помог бы здесь — он гарантирует видимость, но не атомарность операции read-modify-write. Это главный урок: для операций "прочитать-изменить-записать" нужны Atomic-классы или synchronized.'
    }
  ]
}
