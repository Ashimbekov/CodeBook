export default {
  id: 50,
  title: 'REST API на Spring Boot',
  description: 'Создадим полноценный REST API на Spring Boot: научимся обрабатывать HTTP-запросы, работать с JSON, path variables, статус-кодами и обрабатывать ошибки',
  lessons: [
    {
      id: 1, title: 'Что такое REST?', type: 'theory',
      content: [
        { type: 'text', value: 'REST (Representational State Transfer) — это архитектурный стиль для создания веб-сервисов. REST API — это набор правил о том, как серверы и клиенты должны общаться через HTTP.' },
        { type: 'tip', value: 'Представь ресторан. Есть меню (API документация), официант (HTTP), блюда (ресурсы). Ты делаешь заказ (запрос) — тебе приносят еду (ответ). REST — это правила этикета этого ресторана: как делать заказы, как официант должен отвечать.' },
        { type: 'heading', value: 'Принципы REST' },
        { type: 'list', items: [
          'Ресурсы — всё это ресурс: пользователи, продукты, заказы (/users, /products)',
          'HTTP методы — GET (получить), POST (создать), PUT (обновить), DELETE (удалить)',
          'Статус-коды — 200 OK, 201 Created, 404 Not Found, 500 Server Error',
          'Без состояния — каждый запрос независим, сервер не помнит предыдущие',
          'JSON — стандартный формат данных в REST API'
        ]},
        { type: 'heading', value: 'CRUD через HTTP методы' },
        { type: 'code', language: 'java', value: '// GET    /users          — получить всех пользователей\n// GET    /users/1        — получить пользователя с ID=1\n// POST   /users          — создать нового пользователя\n// PUT    /users/1        — обновить пользователя с ID=1\n// DELETE /users/1        — удалить пользователя с ID=1\n\n// Примеры запросов:\n// GET /users\n// Ответ 200 OK:\n// [\n//   {"id": 1, "name": "Иван", "email": "ivan@mail.ru"},\n//   {"id": 2, "name": "Мария", "email": "maria@mail.ru"}\n// ]\n\n// POST /users\n// Тело запроса: {"name": "Алексей", "email": "alex@mail.ru"}\n// Ответ 201 Created:\n// {"id": 3, "name": "Алексей", "email": "alex@mail.ru"}' },
        { type: 'note', value: 'REST — не протокол, а стиль. Нет строгого стандарта, но есть общепринятые соглашения. URL в нижнем регистре (/user-profiles а не /UserProfiles), существительные во множественном числе (/users а не /getUsers).' }
      ]
    },
    {
      id: 2, title: '@RestController и @GetMapping', type: 'theory',
      content: [
        { type: 'text', value: '@RestController — аннотация Spring, которая говорит что этот класс обрабатывает HTTP-запросы и автоматически конвертирует ответы в JSON. @GetMapping — обрабатывает GET-запросы.' },
        { type: 'code', language: 'java', value: 'package com.example.api;\n\nimport org.springframework.web.bind.annotation.*;\nimport java.util.List;\nimport java.util.ArrayList;\n\n@RestController\n@RequestMapping("/api/users")  // базовый путь для всех методов\npublic class UserController {\n\n    private List<String> users = new ArrayList<>();\n\n    // GET /api/users — получить всех пользователей\n    @GetMapping\n    public List<String> getAllUsers() {\n        return users; // Spring автоматически конвертирует в JSON\n    }\n\n    // GET /api/users/count — получить количество\n    @GetMapping("/count")\n    public int getUserCount() {\n        return users.size();\n    }\n\n    // GET /api/users/hello — простое текстовое сообщение\n    @GetMapping("/hello")\n    public String hello() {\n        return "Привет от Users API!";\n    }\n}\n\n// Результат:\n// GET http://localhost:8080/api/users       → [] (пустой массив JSON)\n// GET http://localhost:8080/api/users/count → 0\n// GET http://localhost:8080/api/users/hello → "Привет от Users API!"' },
        { type: 'heading', value: '@RequestMapping vs @GetMapping' },
        { type: 'code', language: 'java', value: '// @GetMapping("/path") — это сокращение от:\n// @RequestMapping(value = "/path", method = RequestMethod.GET)\n\n// Аналогично для других методов:\n// @PostMapping("/path")   = POST\n// @PutMapping("/path")    = PUT\n// @DeleteMapping("/path") = DELETE\n// @PatchMapping("/path")  = PATCH (частичное обновление)' },
        { type: 'heading', value: 'Возвращаем объекты' },
        { type: 'code', language: 'java', value: 'class UserResponse {\n    private int id;\n    private String name;\n    private String email;\n\n    public UserResponse(int id, String name, String email) {\n        this.id = id; this.name = name; this.email = email;\n    }\n    public int getId() { return id; }\n    public String getName() { return name; }\n    public String getEmail() { return email; }\n}\n\n@RestController\n@RequestMapping("/api")\npublic class DemoController {\n\n    @GetMapping("/user")\n    public UserResponse getUser() {\n        // Spring автоматически конвертирует объект в JSON!\n        return new UserResponse(1, "Иван", "ivan@mail.ru");\n    }\n\n    @GetMapping("/users")\n    public List<UserResponse> getUsers() {\n        List<UserResponse> users = new ArrayList<>();\n        users.add(new UserResponse(1, "Иван", "ivan@mail.ru"));\n        users.add(new UserResponse(2, "Мария", "maria@mail.ru"));\n        return users;\n    }\n}\n// GET /api/user → {"id":1,"name":"Иван","email":"ivan@mail.ru"}\n// GET /api/users → [{...},{...}]' },
        { type: 'tip', value: '@RestController = @Controller + @ResponseBody. @ResponseBody говорит Spring: "не ищи HTML шаблон, а верни объект как JSON в теле ответа". Jackson автоматически конвертирует Java-объекты в JSON.' }
      ]
    },
    {
      id: 3, title: '@PostMapping и тело запроса', type: 'theory',
      content: [
        { type: 'text', value: 'POST-запросы используются для создания новых ресурсов. Данные передаются в теле запроса (request body) в формате JSON. Аннотация @RequestBody указывает Spring распарсить тело запроса в Java-объект.' },
        { type: 'code', language: 'java', value: 'import org.springframework.web.bind.annotation.*;\nimport org.springframework.http.ResponseEntity;\nimport org.springframework.http.HttpStatus;\nimport java.util.*;\n\n// DTO для создания пользователя\nclass CreateUserRequest {\n    private String name;\n    private String email;\n    private int age;\n\n    // Конструктор без параметров нужен Jackson\n    public CreateUserRequest() {}\n\n    public String getName() { return name; }\n    public void setName(String name) { this.name = name; }\n    public String getEmail() { return email; }\n    public void setEmail(String email) { this.email = email; }\n    public int getAge() { return age; }\n    public void setAge(int age) { this.age = age; }\n}\n\nclass User {\n    private int id;\n    private String name;\n    private String email;\n    private int age;\n\n    public User(int id, String name, String email, int age) {\n        this.id = id; this.name = name;\n        this.email = email; this.age = age;\n    }\n    public int getId() { return id; }\n    public String getName() { return name; }\n    public String getEmail() { return email; }\n    public int getAge() { return age; }\n}\n\n@RestController\n@RequestMapping("/api/users")\npublic class UserController {\n    private Map<Integer, User> users = new HashMap<>();\n    private int nextId = 1;\n\n    @GetMapping\n    public Collection<User> getAll() {\n        return users.values();\n    }\n\n    // POST /api/users\n    // Тело: {"name":"Иван", "email":"ivan@mail.ru", "age":25}\n    @PostMapping\n    public ResponseEntity<User> createUser(@RequestBody CreateUserRequest request) {\n        // Создаём объект из тела запроса\n        User user = new User(nextId++, request.getName(),\n                             request.getEmail(), request.getAge());\n        users.put(user.getId(), user);\n\n        // Возвращаем 201 Created с телом ответа\n        return ResponseEntity.status(HttpStatus.CREATED).body(user);\n    }\n}' },
        { type: 'heading', value: 'Тестирование POST через curl' },
        { type: 'code', language: 'java', value: '// Тестирование через curl:\n// curl -X POST http://localhost:8080/api/users \\\n//      -H "Content-Type: application/json" \\\n//      -d \'{"name":"Иван","email":"ivan@mail.ru","age":25}\'\n\n// Или через Postman / IntelliJ HTTP Client (.http файл):\n// POST http://localhost:8080/api/users\n// Content-Type: application/json\n//\n// {"name":"Иван","email":"ivan@mail.ru","age":25}\n\n// Ответ 201 Created:\n// {"id":1,"name":"Иван","email":"ivan@mail.ru","age":25}' },
        { type: 'note', value: 'Всегда возвращай правильные статус-коды! POST для создания → 201 Created. GET для получения → 200 OK. DELETE → 204 No Content. Это важно для клиентов API.' }
      ]
    },
    {
      id: 4, title: 'Path Variables и Request Parameters', type: 'theory',
      content: [
        { type: 'text', value: 'Path Variables — переменные прямо в URL пути (/users/42). Request Parameters — параметры после ? в URL (/users?page=1&size=10). Spring легко извлекает их с помощью аннотаций.' },
        { type: 'heading', value: '@PathVariable — переменные в пути' },
        { type: 'code', language: 'java', value: 'import org.springframework.web.bind.annotation.*;\nimport org.springframework.http.ResponseEntity;\nimport java.util.*;\n\n@RestController\n@RequestMapping("/api/users")\npublic class UserController {\n    private Map<Integer, User> users = new HashMap<>();\n\n    // GET /api/users/1\n    // GET /api/users/42\n    @GetMapping("/{id}")\n    public ResponseEntity<User> getUserById(@PathVariable int id) {\n        User user = users.get(id);\n        if (user == null) {\n            return ResponseEntity.notFound().build(); // 404\n        }\n        return ResponseEntity.ok(user); // 200\n    }\n\n    // Несколько path variables:\n    // GET /api/departments/tech/users/5\n    @GetMapping("/departments/{dept}/users/{userId}")\n    public String getUserInDept(\n            @PathVariable String dept,\n            @PathVariable int userId) {\n        return "Пользователь " + userId + " в отделе " + dept;\n    }\n}' },
        { type: 'heading', value: '@RequestParam — параметры запроса' },
        { type: 'code', language: 'java', value: 'import org.springframework.web.bind.annotation.*;\nimport java.util.*;\n\n@RestController\n@RequestMapping("/api/users")\npublic class UserController {\n\n    // GET /api/users?name=Иван\n    // GET /api/users?name=Иван&minAge=18\n    @GetMapping("/search")\n    public List<User> searchUsers(\n            @RequestParam String name,\n            @RequestParam(required = false, defaultValue = "0") int minAge) {\n        // required=false — параметр необязателен\n        // defaultValue — значение по умолчанию\n        return users.values().stream()\n            .filter(u -> u.getName().contains(name) && u.getAge() >= minAge)\n            .collect(java.util.stream.Collectors.toList());\n    }\n\n    // GET /api/users?page=0&size=10&sort=name\n    @GetMapping\n    public List<User> getAll(\n            @RequestParam(defaultValue = "0") int page,\n            @RequestParam(defaultValue = "10") int size,\n            @RequestParam(defaultValue = "id") String sort) {\n        // Пагинация — в реальном Spring Data JPA это намного проще\n        return users.values().stream()\n            .skip((long) page * size)\n            .limit(size)\n            .collect(java.util.stream.Collectors.toList());\n    }\n}' },
        { type: 'tip', value: 'Path Variable для идентификаторов ресурсов: /users/42. Request Parameter для фильтрации, сортировки, пагинации: /users?page=2&sort=name. Это общепринятое соглашение в REST API.' }
      ]
    },
    {
      id: 5, title: 'HTTP Статус-коды', type: 'theory',
      content: [
        { type: 'text', value: 'HTTP статус-коды — это числа в ответе сервера, которые говорят клиенту что произошло. Правильные коды — признак хорошего API.' },
        { type: 'heading', value: 'Основные группы' },
        { type: 'list', items: [
          '2xx — Успех: 200 OK, 201 Created, 204 No Content',
          '3xx — Перенаправление: 301 Moved Permanently, 302 Found',
          '4xx — Ошибка клиента: 400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found',
          '5xx — Ошибка сервера: 500 Internal Server Error, 503 Service Unavailable'
        ]},
        { type: 'heading', value: 'ResponseEntity — полный контроль над ответом' },
        { type: 'code', language: 'java', value: 'import org.springframework.http.ResponseEntity;\nimport org.springframework.http.HttpStatus;\nimport org.springframework.web.bind.annotation.*;\n\n@RestController\n@RequestMapping("/api/products")\npublic class ProductController {\n\n    // 200 OK с телом\n    @GetMapping("/{id}")\n    public ResponseEntity<Product> getProduct(@PathVariable int id) {\n        Product product = findProduct(id);\n        if (product != null) {\n            return ResponseEntity.ok(product); // 200 OK\n        }\n        return ResponseEntity.notFound().build(); // 404 Not Found\n    }\n\n    // 201 Created с заголовком Location\n    @PostMapping\n    public ResponseEntity<Product> createProduct(@RequestBody Product product) {\n        Product created = save(product);\n        return ResponseEntity\n            .status(HttpStatus.CREATED)  // 201\n            .header("Location", "/api/products/" + created.getId())\n            .body(created);\n    }\n\n    // 204 No Content (успешное удаление без тела ответа)\n    @DeleteMapping("/{id}")\n    public ResponseEntity<Void> deleteProduct(@PathVariable int id) {\n        boolean deleted = delete(id);\n        if (deleted) {\n            return ResponseEntity.noContent().build(); // 204\n        }\n        return ResponseEntity.notFound().build(); // 404\n    }\n\n    // 400 Bad Request при некорректных данных\n    @PostMapping("/validate")\n    public ResponseEntity<String> validateAndCreate(@RequestBody Product p) {\n        if (p.getName() == null || p.getName().isBlank()) {\n            return ResponseEntity\n                .badRequest() // 400\n                .body("Ошибка: имя продукта не может быть пустым");\n        }\n        if (p.getPrice() < 0) {\n            return ResponseEntity.badRequest().body("Ошибка: цена не может быть отрицательной");\n        }\n        return ResponseEntity.status(201).body("Продукт создан!");\n    }\n\n    private Product findProduct(int id) { return null; } // заглушка\n    private Product save(Product p) { return p; } // заглушка\n    private boolean delete(int id) { return true; } // заглушка\n}' },
        { type: 'code', language: 'java', value: '// Удобные методы ResponseEntity:\nResponseEntity.ok(body)                    // 200 с телом\nResponseEntity.created(uri).body(body)     // 201 с Location\nResponseEntity.noContent().build()         // 204 без тела\nResponseEntity.badRequest().body(error)    // 400 с телом ошибки\nResponseEntity.notFound().build()          // 404 без тела\nResponseEntity.status(HttpStatus.X).body() // любой статус' }
      ]
    },
    {
      id: 6, title: 'PUT и DELETE методы', type: 'theory',
      content: [
        { type: 'text', value: 'PUT используется для полного обновления ресурса, PATCH — для частичного. DELETE — для удаления. Вместе GET, POST, PUT, DELETE образуют полный CRUD через HTTP.' },
        { type: 'code', language: 'java', value: 'import org.springframework.web.bind.annotation.*;\nimport org.springframework.http.ResponseEntity;\nimport java.util.*;\n\nclass Todo {\n    private int id;\n    private String title;\n    private boolean completed;\n\n    public Todo() {}\n    public Todo(int id, String title, boolean completed) {\n        this.id = id; this.title = title; this.completed = completed;\n    }\n    public int getId() { return id; }\n    public void setId(int id) { this.id = id; }\n    public String getTitle() { return title; }\n    public void setTitle(String title) { this.title = title; }\n    public boolean isCompleted() { return completed; }\n    public void setCompleted(boolean completed) { this.completed = completed; }\n}\n\n@RestController\n@RequestMapping("/api/todos")\npublic class TodoController {\n    private Map<Integer, Todo> todos = new HashMap<>();\n    private int nextId = 1;\n\n    @GetMapping\n    public Collection<Todo> getAll() { return todos.values(); }\n\n    @GetMapping("/{id}")\n    public ResponseEntity<Todo> getById(@PathVariable int id) {\n        Todo todo = todos.get(id);\n        return todo != null ? ResponseEntity.ok(todo) : ResponseEntity.notFound().build();\n    }\n\n    @PostMapping\n    public ResponseEntity<Todo> create(@RequestBody Todo todo) {\n        todo.setId(nextId++);\n        todos.put(todo.getId(), todo);\n        return ResponseEntity.status(201).body(todo);\n    }\n\n    // PUT — полное обновление\n    // PUT /api/todos/1\n    // Body: {"title":"Новый заголовок","completed":true}\n    @PutMapping("/{id}")\n    public ResponseEntity<Todo> update(\n            @PathVariable int id,\n            @RequestBody Todo updated) {\n        if (!todos.containsKey(id)) {\n            return ResponseEntity.notFound().build();\n        }\n        updated.setId(id);\n        todos.put(id, updated);\n        return ResponseEntity.ok(updated);\n    }\n\n    // PATCH — частичное обновление (только completed)\n    @PatchMapping("/{id}/complete")\n    public ResponseEntity<Todo> complete(@PathVariable int id) {\n        Todo todo = todos.get(id);\n        if (todo == null) return ResponseEntity.notFound().build();\n        todo.setCompleted(true);\n        return ResponseEntity.ok(todo);\n    }\n\n    // DELETE\n    @DeleteMapping("/{id}")\n    public ResponseEntity<Void> delete(@PathVariable int id) {\n        if (todos.remove(id) != null) {\n            return ResponseEntity.noContent().build(); // 204\n        }\n        return ResponseEntity.notFound().build();\n    }\n}' },
        { type: 'note', value: 'PUT заменяет весь ресурс целиком. Если клиент не передал какое-то поле — оно должно быть null/default. PATCH обновляет только переданные поля. В простых API часто используют только PUT.' }
      ]
    },
    {
      id: 7, title: 'Обработка исключений в REST', type: 'theory',
      content: [
        { type: 'text', value: 'Когда что-то идёт не так, API должен вернуть понятное сообщение об ошибке с правильным статус-кодом. Spring предоставляет мощные инструменты для централизованной обработки ошибок.' },
        { type: 'heading', value: 'Собственные исключения' },
        { type: 'code', language: 'java', value: '// Кастомное исключение\npublic class UserNotFoundException extends RuntimeException {\n    private int userId;\n\n    public UserNotFoundException(int userId) {\n        super("Пользователь с ID=" + userId + " не найден");\n        this.userId = userId;\n    }\n\n    public int getUserId() { return userId; }\n}' },
        { type: 'heading', value: '@ExceptionHandler и @RestControllerAdvice' },
        { type: 'code', language: 'java', value: 'import org.springframework.http.ResponseEntity;\nimport org.springframework.web.bind.annotation.*;\nimport java.time.LocalDateTime;\nimport java.util.*;\n\n// DTO для ответа с ошибкой\nclass ErrorResponse {\n    private int status;\n    private String message;\n    private String timestamp;\n\n    public ErrorResponse(int status, String message) {\n        this.status = status;\n        this.message = message;\n        this.timestamp = LocalDateTime.now().toString();\n    }\n    public int getStatus() { return status; }\n    public String getMessage() { return message; }\n    public String getTimestamp() { return timestamp; }\n}\n\n// Глобальный обработчик исключений для ВСЕХ контроллеров\n@RestControllerAdvice\npublic class GlobalExceptionHandler {\n\n    // Обрабатываем наше кастомное исключение\n    @ExceptionHandler(UserNotFoundException.class)\n    public ResponseEntity<ErrorResponse> handleUserNotFound(\n            UserNotFoundException e) {\n        ErrorResponse error = new ErrorResponse(404, e.getMessage());\n        return ResponseEntity.status(404).body(error);\n    }\n\n    // Обрабатываем ошибки валидации\n    @ExceptionHandler(IllegalArgumentException.class)\n    public ResponseEntity<ErrorResponse> handleBadRequest(\n            IllegalArgumentException e) {\n        ErrorResponse error = new ErrorResponse(400, e.getMessage());\n        return ResponseEntity.badRequest().body(error);\n    }\n\n    // Обрабатываем все остальные ошибки\n    @ExceptionHandler(Exception.class)\n    public ResponseEntity<ErrorResponse> handleGeneral(Exception e) {\n        ErrorResponse error = new ErrorResponse(500,\n            "Внутренняя ошибка сервера: " + e.getMessage());\n        return ResponseEntity.status(500).body(error);\n    }\n}' },
        { type: 'code', language: 'java', value: '// Теперь в контроллере просто бросаем исключение\n@RestController\n@RequestMapping("/api/users")\npublic class UserController {\n    private Map<Integer, User> users = new HashMap<>();\n\n    @GetMapping("/{id}")\n    public User getById(@PathVariable int id) {\n        User user = users.get(id);\n        if (user == null) {\n            throw new UserNotFoundException(id); // просто бросаем!\n            // GlobalExceptionHandler поймает и вернёт 404\n        }\n        return user;\n    }\n}\n\n// Ответ при ошибке:\n// HTTP 404 Not Found\n// {\n//   "status": 404,\n//   "message": "Пользователь с ID=99 не найден",\n//   "timestamp": "2024-01-15T10:30:00"\n// }' },
        { type: 'tip', value: '@RestControllerAdvice — лучше чем try-catch в каждом контроллере. Обработка ошибок в одном месте, консистентный формат ответов, легко поддерживать.' }
      ]
    },
    {
      id: 8, title: 'Практика: Полный REST API', type: 'practice', difficulty: 'hard',
      description: 'Создай полноценный REST API для управления списком книг. API должен поддерживать все CRUD операции, корректные статус-коды, обработку ошибок и поиск по параметрам.',
      requirements: [
        'Создай класс Book с полями: id, title, author, year, available (boolean)',
        'Создай BookNotFoundException extends RuntimeException',
        'Создай GlobalExceptionHandler с @RestControllerAdvice',
        'Реализуй GET /api/books — все книги, GET /api/books/{id} — по ID',
        'Реализуй POST /api/books — создать (201), PUT /api/books/{id} — обновить',
        'Реализуй DELETE /api/books/{id} — удалить (204)',
        'Добавь GET /api/books/search?author=Пушкин — поиск по автору'
      ],
      expectedOutput: 'POST /api/books → 201: {"id":1,"title":"Война и мир","author":"Толстой","year":1869,"available":true}\nGET /api/books → 200: [{"id":1,...},{"id":2,...}]\nGET /api/books/1 → 200: {"id":1,"title":"Война и мир",...}\nGET /api/books/99 → 404: {"status":404,"message":"Книга с ID=99 не найдена"}\nDELETE /api/books/1 → 204 (пустое тело)\nGET /api/books/search?author=Пушкин → 200: [{"id":2,...}]',
      hint: 'Используй Map<Integer, Book> для хранения. В GlobalExceptionHandler поймай BookNotFoundException → 404. В search используй @RequestParam String author и фильтруй по author.contains(). DELETE возвращает ResponseEntity.noContent().build() при успехе.',
      solution: 'import org.springframework.http.ResponseEntity;\nimport org.springframework.web.bind.annotation.*;\nimport java.util.*;\nimport java.util.stream.Collectors;\n\nclass Book {\n    private int id;\n    private String title;\n    private String author;\n    private int year;\n    private boolean available;\n\n    public Book() {}\n    public Book(int id, String title, String author, int year, boolean available) {\n        this.id = id; this.title = title; this.author = author;\n        this.year = year; this.available = available;\n    }\n    public int getId() { return id; }\n    public void setId(int id) { this.id = id; }\n    public String getTitle() { return title; }\n    public void setTitle(String t) { this.title = t; }\n    public String getAuthor() { return author; }\n    public void setAuthor(String a) { this.author = a; }\n    public int getYear() { return year; }\n    public void setYear(int y) { this.year = y; }\n    public boolean isAvailable() { return available; }\n    public void setAvailable(boolean a) { this.available = a; }\n    public String toString() {\n        return "Book{id=" + id + ", title=" + title + ", author=" + author + "}";\n    }\n}\n\nclass BookNotFoundException extends RuntimeException {\n    public BookNotFoundException(int id) {\n        super("Книга с ID=" + id + " не найдена");\n    }\n}\n\nclass ErrorResponse {\n    private int status;\n    private String message;\n    public ErrorResponse(int status, String message) {\n        this.status = status; this.message = message;\n    }\n    public int getStatus() { return status; }\n    public String getMessage() { return message; }\n}\n\n// В реальном Spring Boot это было бы в отдельном файле\n// @RestControllerAdvice\nclass GlobalExceptionHandler {\n    // @ExceptionHandler(BookNotFoundException.class)\n    public ResponseEntity<ErrorResponse> handleNotFound(BookNotFoundException e) {\n        return ResponseEntity.status(404).body(new ErrorResponse(404, e.getMessage()));\n    }\n    // @ExceptionHandler(Exception.class)\n    public ResponseEntity<ErrorResponse> handleGeneral(Exception e) {\n        return ResponseEntity.status(500).body(\n            new ErrorResponse(500, "Ошибка: " + e.getMessage()));\n    }\n}\n\n// @RestController\n// @RequestMapping("/api/books")\nclass BookController {\n    private Map<Integer, Book> books = new HashMap<>();\n    private int nextId = 1;\n\n    // GET /api/books\n    public Collection<Book> getAll() {\n        return books.values();\n    }\n\n    // GET /api/books/{id}\n    public ResponseEntity<Book> getById(int id) {\n        Book book = books.get(id);\n        if (book == null) throw new BookNotFoundException(id);\n        return ResponseEntity.ok(book);\n    }\n\n    // POST /api/books\n    public ResponseEntity<Book> create(Book book) {\n        book.setId(nextId++);\n        books.put(book.getId(), book);\n        return ResponseEntity.status(201).body(book);\n    }\n\n    // PUT /api/books/{id}\n    public ResponseEntity<Book> update(int id, Book updated) {\n        if (!books.containsKey(id)) throw new BookNotFoundException(id);\n        updated.setId(id);\n        books.put(id, updated);\n        return ResponseEntity.ok(updated);\n    }\n\n    // DELETE /api/books/{id}\n    public ResponseEntity<Void> delete(int id) {\n        if (books.remove(id) == null) throw new BookNotFoundException(id);\n        return ResponseEntity.noContent().build();\n    }\n\n    // GET /api/books/search?author=Пушкин\n    public List<Book> searchByAuthor(String author) {\n        return books.values().stream()\n            .filter(b -> b.getAuthor().contains(author))\n            .collect(Collectors.toList());\n    }\n}\n\n// Демонстрация без Spring (имитация запросов)\npublic class Main {\n    public static void main(String[] args) {\n        BookController controller = new BookController();\n        GlobalExceptionHandler handler = new GlobalExceptionHandler();\n\n        // POST\n        ResponseEntity<Book> created = controller.create(\n            new Book(0, "Война и мир", "Толстой", 1869, true));\n        System.out.println("POST /api/books → " + created.getStatusCode().value() +\n            ": " + created.getBody());\n\n        controller.create(new Book(0, "Евгений Онегин", "Пушкин", 1833, true));\n\n        // GET all\n        System.out.println("GET /api/books → 200: " + controller.getAll());\n\n        // GET by ID\n        System.out.println("GET /api/books/1 → " +\n            controller.getById(1).getStatusCode().value() + ": " +\n            controller.getById(1).getBody());\n\n        // GET not found\n        try {\n            controller.getById(99);\n        } catch (BookNotFoundException e) {\n            ResponseEntity<ErrorResponse> err = handler.handleNotFound(e);\n            System.out.println("GET /api/books/99 → " + err.getStatusCode().value() +\n                ": status=" + err.getBody().getStatus() +\n                ", message=" + err.getBody().getMessage());\n        }\n\n        // DELETE\n        ResponseEntity<Void> deleted = controller.delete(1);\n        System.out.println("DELETE /api/books/1 → " + deleted.getStatusCode().value());\n\n        // Search\n        List<Book> found = controller.searchByAuthor("Пушкин");\n        System.out.println("GET /api/books/search?author=Пушкин → 200: " + found);\n    }\n}',
      explanation: 'Полный REST API следует соглашениям: GET для чтения, POST для создания (201), PUT для обновления, DELETE для удаления (204), 404 когда ресурс не найден. GlobalExceptionHandler централизует обработку ошибок — все контроллеры просто бросают исключения, а Handler их перехватывает и формирует правильный ответ. В реальном Spring Boot приложении нужно раскомментировать аннотации @RestController, @RestControllerAdvice и @ExceptionHandler.'
    }
  ]
}
