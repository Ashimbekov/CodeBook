export default {
  id: 68,
  title: 'Практикум: Комбинированные задачи',
  description: 'Комплексные практические задачи, объединяющие несколько тем Java: потоки, многопоточность, дженерики, паттерны проектирования, обработка данных и файлов',
  lessons: [
    {
      id: 1,
      title: 'Задача: Частота слов из текста с использованием Stream API',
      type: 'practice',
      difficulty: 'medium',
      description: 'Используй Stream API для анализа текста: разбей на слова, подсчитай частоту каждого слова, выведи топ-10. Слова нормализуй (нижний регистр, без пунктуации). Используй Collectors.groupingBy и Collectors.counting.',
      requirements: [
        'Прочитать текст как строку, разбить на слова через split',
        'Нормализация: toLowerCase() и replaceAll("[^а-яёa-z0-9]", "")',
        'Использовать Stream.of() или Arrays.stream()',
        'Collectors.groupingBy(Function.identity(), Collectors.counting())',
        'Отсортировать по частоте и вывести топ-10'
      ],
      expectedOutput: 'Топ-10 слов:\n1. java         : 15\n2. класс        : 12\n3. метод        : 10\n4. объект       : 8\n5. интерфейс    : 7',
      hint: 'Arrays.stream(text.split("\\\\s+")).map(w -> w.toLowerCase().replaceAll("[^а-яёa-z0-9]", "")).filter(w -> !w.isEmpty()).collect(Collectors.groupingBy(Function.identity(), Collectors.counting())). Затем сортировка entrySet по value.',
      solution: 'import java.util.*;\nimport java.util.function.Function;\nimport java.util.stream.Collectors;\nimport java.util.stream.Stream;\n\npublic class WordFrequencyStream {\n\n    public static Map<String, Long> countWords(String text) {\n        return Arrays.stream(text.split("\\\\s+"))\n            .map(String::toLowerCase)\n            .map(w -> w.replaceAll("[^а-яёa-z0-9]", ""))\n            .filter(w -> !w.isEmpty())\n            .collect(Collectors.groupingBy(\n                Function.identity(),\n                Collectors.counting()\n            ));\n    }\n\n    public static List<Map.Entry<String, Long>> getTopWords(Map<String, Long> freq, int n) {\n        return freq.entrySet().stream()\n            .sorted(Map.Entry.<String, Long>comparingByValue(Comparator.reverseOrder())\n                .thenComparing(Map.Entry.comparingByKey()))\n            .limit(n)\n            .collect(Collectors.toList());\n    }\n\n    public static void main(String[] args) {\n        String text = "Java — объектно-ориентированный язык программирования. " +\n            "В Java всё является объектом. Класс в Java — основная единица. " +\n            "Каждый класс в Java имеет методы. Метод — это функция класса. " +\n            "Java поддерживает интерфейсы. Интерфейс в Java определяет контракт. " +\n            "Объект создаётся из класса. Метод объекта вызывается через точку. " +\n            "Java — платформонезависимый язык. Класс Java компилируется в байткод.";\n\n        Map<String, Long> freq = countWords(text);\n        List<Map.Entry<String, Long>> top = getTopWords(freq, 10);\n\n        System.out.println("Топ-10 слов:");\n        for (int i = 0; i < top.size(); i++) {\n            System.out.printf("%2d. %-15s: %d%n",\n                i + 1, top.get(i).getKey(), top.get(i).getValue());\n        }\n\n        System.out.println();\n        System.out.println("Всего уникальных слов: " + freq.size());\n        System.out.println("Всего слов: " + freq.values().stream().mapToLong(Long::longValue).sum());\n    }\n}',
      explanation: 'Задача демонстрирует мощь Stream API для обработки текста. Цепочка: split -> stream -> map(нормализация) -> filter(непустые) -> collect(группировка). Collectors.groupingBy(Function.identity(), Collectors.counting()) — двойной коллектор: внешний группирует по ключу, внутренний подсчитывает количество в каждой группе. Function.identity() — функция "вернуть сам аргумент", используется как ключ группировки. Сортировка Map по значению требует перехода через entrySet().stream() — прямой сортировки Map не существует. Дополнительный .sum() в конце показывает разницу между уникальными и общим числом слов. Этот паттерн применяется в поисковых системах, анализе логов, NLP.'
    },
    {
      id: 2,
      title: 'Задача: Потокобезопасный Singleton-логгер',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуй потокобезопасный логгер по паттерну Singleton с двойной проверкой блокировки (Double-Checked Locking). Логгер поддерживает уровни: DEBUG, INFO, WARN, ERROR. Хранит последние 100 записей в памяти. Вывод в формате "[ВРЕМЯ] [УРОВЕНЬ] сообщение".',
      requirements: [
        'Singleton с double-checked locking (volatile + synchronized)',
        'Enum LogLevel с уровнями DEBUG, INFO, WARN, ERROR',
        'Метод log(LogLevel level, String message) потокобезопасен',
        'Хранить последние 100 записей в LinkedList с ограничением размера',
        'Методы getLog() и clearLog()',
        'Установка минимального уровня логирования: setMinLevel(LogLevel)'
      ],
      expectedOutput: '[10:30:01] [INFO]  Приложение запущено\n[10:30:01] [DEBUG] Инициализация базы данных\n[10:30:02] [WARN]  Соединение медленное\n[10:30:02] [ERROR] Ошибка подключения: timeout\n[10:30:03] [INFO]  Повторное подключение...',
      hint: 'private static volatile Logger instance; в getInstance() — synchronized блок только при первой проверке null. LinkedList<String> с проверкой size >= 100 перед добавлением. DateTimeFormatter для временной метки.',
      solution: 'import java.time.LocalTime;\nimport java.time.format.DateTimeFormatter;\nimport java.util.LinkedList;\nimport java.util.List;\n\npublic class Logger {\n\n    public enum LogLevel {\n        DEBUG(0), INFO(1), WARN(2), ERROR(3);\n\n        final int priority;\n        LogLevel(int priority) { this.priority = priority; }\n    }\n\n    private static volatile Logger instance;\n    private final LinkedList<String> logBuffer = new LinkedList<>();\n    private static final int MAX_SIZE = 100;\n    private LogLevel minLevel = LogLevel.DEBUG;\n    private static final DateTimeFormatter TIME_FMT =\n        DateTimeFormatter.ofPattern("HH:mm:ss");\n\n    private Logger() {}\n\n    public static Logger getInstance() {\n        if (instance == null) {\n            synchronized (Logger.class) {\n                if (instance == null) {\n                    instance = new Logger();\n                }\n            }\n        }\n        return instance;\n    }\n\n    public synchronized void log(LogLevel level, String message) {\n        if (level.priority < minLevel.priority) return;\n        String timestamp = LocalTime.now().format(TIME_FMT);\n        String entry = String.format("[%s] [%-5s] %s", timestamp, level.name(), message);\n        System.out.println(entry);\n        if (logBuffer.size() >= MAX_SIZE) {\n            logBuffer.removeFirst();\n        }\n        logBuffer.addLast(entry);\n    }\n\n    public void setMinLevel(LogLevel level) { this.minLevel = level; }\n\n    public synchronized List<String> getLog() {\n        return new LinkedList<>(logBuffer);\n    }\n\n    public synchronized void clearLog() {\n        logBuffer.clear();\n    }\n\n    // Удобные методы\n    public void debug(String msg) { log(LogLevel.DEBUG, msg); }\n    public void info(String msg)  { log(LogLevel.INFO,  msg); }\n    public void warn(String msg)  { log(LogLevel.WARN,  msg); }\n    public void error(String msg) { log(LogLevel.ERROR, msg); }\n\n    public static void main(String[] args) throws InterruptedException {\n        Logger logger = Logger.getInstance();\n\n        // Проверка что singleton возвращает один объект\n        Logger logger2 = Logger.getInstance();\n        System.out.println("Один объект? " + (logger == logger2));\n        System.out.println();\n\n        logger.info("Приложение запущено");\n        logger.debug("Инициализация базы данных");\n        logger.warn("Соединение медленное");\n        logger.error("Ошибка подключения: timeout");\n        logger.info("Повторное подключение...");\n\n        System.out.println();\n        logger.setMinLevel(LogLevel.WARN);\n        logger.debug("Это не выведется (уровень ниже WARN)");\n        logger.warn("Это выведется");\n\n        System.out.println();\n        System.out.println("Записей в буфере: " + logger.getLog().size());\n    }\n}',
      explanation: 'Double-Checked Locking — классический паттерн для потокобезопасного Singleton с ленивой инициализацией. Первая проверка if (instance == null) без синхронизации — быстрый путь для уже созданного объекта. Вторая проверка внутри synchronized — защита от race condition при одновременном первом вызове несколькими потоками. volatile критично: без него JVM может кэшировать старое значение instance в потоке и не увидеть запись другого потока (проблема видимости памяти). synchronized на методах log/getLog/clearLog — защита от concurrent modification. LinkedList эффективен для очереди с ограниченным размером: removeFirst() O(1). Enum LogLevel с полем priority — идиоматичный способ сравнения уровней.'
    },
    {
      id: 3,
      title: 'Задача: Обобщённый стек с итератором',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй обобщённый (generic) стек Stack<T> с поддержкой интерфейса Iterable<T>. Стек должен поддерживать push, pop, peek, isEmpty, size. Итератор обходит стек от вершины к основанию. Реализуй toString() для удобного вывода.',
      requirements: [
        'Класс Stack<T> implements Iterable<T>',
        'Внутреннее хранение: связный список (вложенный класс Node<T>)',
        'Методы: push(T item), pop() throws EmptyStackException, peek(), isEmpty(), size()',
        'Итератор обходит от вершины к основанию',
        'toString() выводит элементы через разделитель',
        'В main() продемонстрировать все операции и foreach'
      ],
      expectedOutput: 'После push(1,2,3): [3, 2, 1]\npeek: 3\npop: 3\nСтек после pop: [2, 1]\nРазмер: 2\nОбход foreach: 2 1\nОшибка при pop пустого стека: Stack is empty',
      hint: 'Node<T> { T value; Node<T> next; }. push: новый Node становится top, next = старый top. pop: сохраняем top.value, top = top.next. Итератор: current = top, в hasNext() проверяем current != null, в next() возвращаем current.value и сдвигаем current.',
      solution: 'import java.util.EmptyStackException;\nimport java.util.Iterator;\nimport java.util.NoSuchElementException;\n\npublic class Stack<T> implements Iterable<T> {\n\n    private static class Node<T> {\n        T value;\n        Node<T> next;\n\n        Node(T value, Node<T> next) {\n            this.value = value;\n            this.next = next;\n        }\n    }\n\n    private Node<T> top = null;\n    private int size = 0;\n\n    public void push(T item) {\n        top = new Node<>(item, top);\n        size++;\n    }\n\n    public T pop() {\n        if (isEmpty()) throw new EmptyStackException();\n        T value = top.value;\n        top = top.next;\n        size--;\n        return value;\n    }\n\n    public T peek() {\n        if (isEmpty()) throw new EmptyStackException();\n        return top.value;\n    }\n\n    public boolean isEmpty() { return top == null; }\n    public int size()        { return size; }\n\n    @Override\n    public Iterator<T> iterator() {\n        return new Iterator<T>() {\n            private Node<T> current = top;\n\n            @Override\n            public boolean hasNext() { return current != null; }\n\n            @Override\n            public T next() {\n                if (!hasNext()) throw new NoSuchElementException();\n                T value = current.value;\n                current = current.next;\n                return value;\n            }\n        };\n    }\n\n    @Override\n    public String toString() {\n        if (isEmpty()) return "[]";\n        StringBuilder sb = new StringBuilder("[");\n        for (T item : this) {\n            sb.append(item).append(", ");\n        }\n        sb.setLength(sb.length() - 2);\n        sb.append("]");\n        return sb.toString();\n    }\n\n    public static void main(String[] args) {\n        Stack<Integer> stack = new Stack<>();\n        stack.push(1);\n        stack.push(2);\n        stack.push(3);\n        System.out.println("После push(1,2,3): " + stack);\n        System.out.println("peek: " + stack.peek());\n        System.out.println("pop: " + stack.pop());\n        System.out.println("Стек после pop: " + stack);\n        System.out.println("Размер: " + stack.size());\n\n        System.out.print("Обход foreach: ");\n        for (int x : stack) System.out.print(x + " ");\n        System.out.println();\n\n        // Тест строкового стека\n        Stack<String> strStack = new Stack<>();\n        strStack.push("hello");\n        strStack.push("world");\n        System.out.println("Стек строк: " + strStack);\n\n        // Тест исключения\n        Stack<Integer> empty = new Stack<>();\n        try {\n            empty.pop();\n        } catch (EmptyStackException e) {\n            System.out.println("Ошибка при pop пустого стека: Stack is empty");\n        }\n    }\n}',
      explanation: 'Generic Stack демонстрирует три важные концепции одновременно. Дженерики (generics): Stack<T> работает с любым типом без дублирования кода. Связный список как внутренняя структура: Node хранит значение и ссылку на следующий узел — push/pop O(1). Интерфейс Iterable<T>: реализация позволяет использовать стек в foreach-цикле. Вложенный анонимный класс итератора захватывает top из внешнего объекта. Ключевой инвариант стека: LIFO (Last In First Out) — pop возвращает элементы в порядке, обратном добавлению. EmptyStackException из java.util — стандартное исключение для пустого стека, не нужно создавать своё. toString() использует сам итератор — согласованность поведения.'
    },
    {
      id: 4,
      title: 'Задача: Конвертер CSV в JSON',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй конвертер CSV в JSON без использования сторонних библиотек. Первая строка CSV — заголовки столбцов. Каждая последующая строка — запись данных. Результат — JSON-массив объектов. Обработай кавычки (значения в кавычках могут содержать запятые).',
      requirements: [
        'Метод csvToJson(String csv) возвращает String с JSON',
        'Парсинг заголовков из первой строки',
        'Каждая строка данных — JSON-объект {ключ: значение}',
        'Экранирование спецсимволов в JSON: кавычки, обратный слеш, перенос строки',
        'Простая поддержка quoted fields (значения в кавычках)',
        'Форматированный JSON с отступами'
      ],
      expectedOutput: 'Входной CSV:\nname,age,city\nАлия,25,Алматы\nБекзат,30,Астана\nJSON результат:\n[\n  {\n    "name": "Алия",\n    "age": "25",\n    "city": "Алматы"\n  },\n  {\n    "name": "Бекзат",\n    "age": "30",\n    "city": "Астана"\n  }\n]',
      hint: 'Разбей CSV на строки через split("\\n"). Первая строка — headers. Для каждой последующей строки: создай JSON-объект с парами key: value. Метод parseLine разбирает с учётом кавычек. escapeJson экранирует " -> \\", \\ -> \\\\, \\n -> \\n.',
      solution: 'import java.util.ArrayList;\nimport java.util.Arrays;\nimport java.util.List;\n\npublic class CsvToJson {\n\n    public static String escapeJson(String value) {\n        if (value == null) return "";\n        return value\n            .replace("\\\\", "\\\\\\\\")\n            .replace("\"", "\\\\\"")\n            .replace("\\n", "\\\\n")\n            .replace("\\r", "\\\\r")\n            .replace("\\t", "\\\\t");\n    }\n\n    public static List<String> parseLine(String line) {\n        List<String> fields = new ArrayList<>();\n        boolean inQuotes = false;\n        StringBuilder current = new StringBuilder();\n\n        for (int i = 0; i < line.length(); i++) {\n            char c = line.charAt(i);\n            if (c == \'"\' ) {\n                if (inQuotes && i + 1 < line.length() && line.charAt(i + 1) == \'"\') {\n                    current.append(\'"\');\n                    i++; // Пропустить вторую кавычку (экранированная кавычка в CSV)\n                } else {\n                    inQuotes = !inQuotes;\n                }\n            } else if (c == \',\' && !inQuotes) {\n                fields.add(current.toString().trim());\n                current.setLength(0);\n            } else {\n                current.append(c);\n            }\n        }\n        fields.add(current.toString().trim());\n        return fields;\n    }\n\n    public static String csvToJson(String csv) {\n        String[] lines = csv.split("\\n");\n        if (lines.length == 0) return "[]";\n\n        List<String> headers = parseLine(lines[0]);\n        StringBuilder json = new StringBuilder("[\\n");\n\n        for (int i = 1; i < lines.length; i++) {\n            String line = lines[i].trim();\n            if (line.isEmpty()) continue;\n\n            List<String> fields = parseLine(line);\n            json.append("  {\\n");\n\n            for (int j = 0; j < headers.size(); j++) {\n                String key = escapeJson(headers.get(j));\n                String val = j < fields.size() ? escapeJson(fields.get(j)) : "";\n                json.append("    \\\"").append(key).append("\\\": \\\"").append(val).append("\\\"");\n                if (j < headers.size() - 1) json.append(",");\n                json.append("\\n");\n            }\n\n            json.append("  }");\n            if (i < lines.length - 1) json.append(",");\n            json.append("\\n");\n        }\n\n        json.append("]");\n        return json.toString();\n    }\n\n    public static void main(String[] args) {\n        String csv = "name,age,city\\n" +\n                     "Алия,25,Алматы\\n" +\n                     "Бекзат,30,Астана\\n" +\n                     "Нурдаулет,28,Шымкент";\n\n        System.out.println("Входной CSV:");\n        System.out.println(csv);\n        System.out.println();\n        System.out.println("JSON результат:");\n        System.out.println(csvToJson(csv));\n\n        System.out.println();\n        // Тест с кавычками в значениях\n        String csv2 = "name,description\\n" +\n                      "Книга,\\\"Война и мир\\\", Толстой\\n" +\n                      "Фильм,Матрица";\n        System.out.println("CSV с кавычками:");\n        System.out.println(csvToJson(csv2));\n    }\n}',
      explanation: 'Конвертер CSV-JSON без библиотек учит понимать форматы данных изнутри. Парсинг CSV сложнее чем кажется: запятые внутри кавычек не разделители, кавычки экранируются двойным повторением ("" внутри поля). Метод parseLine реализует конечный автомат: флаг inQuotes переключается при встрече кавычки. escapeJson — критически важен: без него спецсимволы сломают JSON. Порядок replace важен: сначала экранируем обратный слеш, иначе уже добавленные \\ будут экранированы повторно. В продакшене используют Jackson, Gson или org.json, но понимание ручной реализации помогает при отладке проблем с форматами. Этот паттерн часто встречается в ETL-пайплайнах для трансформации данных.'
    },
    {
      id: 5,
      title: 'Задача: Система событий (паттерн Observer)',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй EventBus — шину событий на основе паттерна Observer. Позволяет подписываться на события по типу (строке), отписываться и публиковать события. Несколько подписчиков могут слушать одно событие. Реализуй через Map<String, List<Consumer<Object>>>.',
      requirements: [
        'Класс EventBus с методами subscribe(String event, Consumer<Object> handler)',
        'Метод unsubscribe(String event, Consumer<Object> handler)',
        'Метод publish(String event, Object data) — вызывает всех подписчиков',
        'Несколько подписчиков на одно событие',
        'В main() продемонстрировать подписку, публикацию и отписку'
      ],
      expectedOutput: 'Подписчик 1 получил событие "user.login": Алия\nПодписчик 2 получил событие "user.login": Алия\nЛоггер: событие user.login для Алия\nПосле отписки подписчика 1:\nПодписчик 2 получил событие "user.login": Бекзат\nЛоггер: событие user.login для Бекзат',
      hint: 'Map<String, List<Consumer<Object>>> listeners. subscribe: getOrDefault(event, new ArrayList<>()).add(handler). publish: listeners.getOrDefault(event, Collections.emptyList()).forEach(h -> h.accept(data)). unsubscribe: listeners.get(event).remove(handler).',
      solution: 'import java.util.*;\nimport java.util.function.Consumer;\n\npublic class EventBus {\n\n    private final Map<String, List<Consumer<Object>>> listeners = new HashMap<>();\n\n    public void subscribe(String event, Consumer<Object> handler) {\n        listeners.computeIfAbsent(event, k -> new ArrayList<>()).add(handler);\n    }\n\n    public void unsubscribe(String event, Consumer<Object> handler) {\n        List<Consumer<Object>> handlers = listeners.get(event);\n        if (handlers != null) {\n            handlers.remove(handler);\n        }\n    }\n\n    public void publish(String event, Object data) {\n        List<Consumer<Object>> handlers = listeners.getOrDefault(event, Collections.emptyList());\n        // Копия списка на случай изменения во время итерации\n        new ArrayList<>(handlers).forEach(h -> h.accept(data));\n    }\n\n    public int subscriberCount(String event) {\n        return listeners.getOrDefault(event, Collections.emptyList()).size();\n    }\n\n    public static void main(String[] args) {\n        EventBus bus = new EventBus();\n\n        Consumer<Object> subscriber1 = data ->\n            System.out.println("Подписчик 1 получил событие \\"user.login\\": " + data);\n        Consumer<Object> subscriber2 = data ->\n            System.out.println("Подписчик 2 получил событие \\"user.login\\": " + data);\n        Consumer<Object> logger = data ->\n            System.out.println("Логгер: событие user.login для " + data);\n\n        bus.subscribe("user.login", subscriber1);\n        bus.subscribe("user.login", subscriber2);\n        bus.subscribe("user.login", logger);\n\n        System.out.println("Публикация события для Алия:");\n        bus.publish("user.login", "Алия");\n\n        System.out.println();\n        bus.unsubscribe("user.login", subscriber1);\n        System.out.println("После отписки подписчика 1 (осталось " +\n            bus.subscriberCount("user.login") + " подписчика):");\n        bus.publish("user.login", "Бекзат");\n\n        System.out.println();\n        // Событие без подписчиков — не выбрасывает исключение\n        bus.publish("user.logout", "Алия");\n        System.out.println("Публикация без подписчиков — тихо проигнорировано");\n    }\n}',
      explanation: 'EventBus (шина событий) — вариант Observer с дополнительным уровнем indirection: объекты общаются не напрямую, а через шину. Это снижает зависимость между компонентами. computeIfAbsent элегантно инициализирует список при первой подписке. getOrDefault при publish — защита от NPE при отсутствии подписчиков. Копирование списка перед итерацией (new ArrayList<>(handlers)) — защита от ConcurrentModificationException, если обработчик внутри отписывается. Consumer<Object> как тип обработчика — функциональный интерфейс, совместимый с лямбдами. Важный нюанс: unsubscribe работает через Object.equals(), что для лямбд означает — нужно хранить ссылку на ту же лямбду.'
    },
    {
      id: 6,
      title: 'Задача: LRU-кэш на HashMap и LinkedList',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуй LRU (Least Recently Used) кэш фиксированного размера. При добавлении нового элемента когда кэш полон — удаляется наименее недавно использованный элемент. Операции get и put должны работать за O(1).',
      requirements: [
        'Класс LRUCache<K, V> с параметром capacity',
        'get(K key) возвращает значение или null, обновляет порядок использования',
        'put(K key, V value) добавляет/обновляет, при переполнении удаляет LRU-элемент',
        'Использовать LinkedHashMap с accessOrder=true — встроенная поддержка LRU',
        'Альтернативно: HashMap + двусвязный список',
        'В main() продемонстрировать вытеснение элементов'
      ],
      expectedOutput: 'LRU кэш размером 3:\nput(1,"один") put(2,"два") put(3,"три")\nСостояние: {1=один, 2=два, 3=три}\nget(1) -> один (1 становится недавно использованным)\nput(4,"четыре") -> вытесняется 2 (давно не использовался)\nСостояние: {3=три, 1=один, 4=четыре}\nget(5) -> null (нет в кэше)',
      hint: 'LinkedHashMap(capacity, 0.75f, true) — третий параметр true означает accessOrder (порядок по доступу а не по вставке). Переопределить removeEldestEntry(eldest) чтобы возвращать size() > capacity. Тогда при put LinkedHashMap автоматически удалит самый старый элемент.',
      solution: 'import java.util.LinkedHashMap;\nimport java.util.Map;\n\npublic class LRUCache<K, V> {\n\n    private final int capacity;\n    private final LinkedHashMap<K, V> cache;\n\n    public LRUCache(int capacity) {\n        this.capacity = capacity;\n        // accessOrder=true: перемещает использованный элемент в конец\n        this.cache = new LinkedHashMap<K, V>(capacity, 0.75f, true) {\n            @Override\n            protected boolean removeEldestEntry(Map.Entry<K, V> eldest) {\n                return size() > capacity;\n            }\n        };\n    }\n\n    public V get(K key) {\n        return cache.getOrDefault(key, null);\n    }\n\n    public void put(K key, V value) {\n        cache.put(key, value);\n    }\n\n    public boolean containsKey(K key) {\n        return cache.containsKey(key);\n    }\n\n    public int size() {\n        return cache.size();\n    }\n\n    @Override\n    public String toString() {\n        return cache.toString();\n    }\n\n    public static void main(String[] args) {\n        System.out.println("LRU кэш размером 3:");\n        LRUCache<Integer, String> lru = new LRUCache<>(3);\n\n        lru.put(1, "один");\n        lru.put(2, "два");\n        lru.put(3, "три");\n        System.out.println("После put(1,2,3): " + lru);\n\n        System.out.println("get(1) -> " + lru.get(1) + " (1 становится недавно использованным)");\n        System.out.println("Состояние: " + lru);\n\n        lru.put(4, "четыре");\n        System.out.println("После put(4): вытеснен элемент 2 -> " + lru);\n\n        System.out.println("get(2) -> " + lru.get(2) + " (уже вытеснен)");\n        System.out.println("get(3) -> " + lru.get(3));\n\n        lru.put(5, "пять");\n        System.out.println("После put(5): " + lru);\n\n        System.out.println("get(5) -> " + lru.get(5));\n    }\n}',
      explanation: 'LRU-кэш — классическая задача на структуры данных, встречающаяся на собеседованиях. LinkedHashMap с accessOrder=true автоматически поддерживает порядок по доступу: при каждом get/put элемент перемещается в конец, самый "старый" (наименее недавно использованный) всегда в начале. Переопределение removeEldestEntry — официальный механизм LinkedHashMap для ограничения размера: вызывается после каждого put. size() > capacity — удалить если превысили лимит. Анонимный класс при создании LinkedHashMap — элегантный способ расширить поведение без создания отдельного файла. O(1) для get и put — благодаря хэш-таблице внутри LinkedHashMap. LRU-кэш используется в браузерах (кэш страниц), базах данных (буферный пул), CPU (кэш последних инструкций).'
    },
    {
      id: 7,
      title: 'Задача: Вычислитель выражений (калькулятор со стеком)',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуй вычислитель математических выражений в постфиксной нотации (Обратная польская нотация, ОПН). В ОПН операнды идут перед оператором: "3 4 +" = 7. Поддержи операции +, -, *, /. Дополнительно: реализуй конвертацию из инфиксной в постфиксную через алгоритм сортировочной станции.',
      requirements: [
        'Метод evaluateRPN(String expression) вычисляет выражение в ОПН',
        'Использовать Stack<Double> для вычисления',
        'Поддержка операций: +, -, *, /',
        'Обработка ошибок: деление на ноль, неверный формат',
        'В main() продемонстрировать несколько выражений'
      ],
      expectedOutput: '"3 4 +" = 7.0\n"10 2 /" = 5.0\n"5 1 2 + 4 * + 3 -" = 14.0\n"15 7 1 1 + - / 3 * 2 1 1 + + -" = 5.0\nОшибка: "1 0 /" -> Деление на ноль',
      hint: 'Разбей по пробелам. Если токен — число (Double.parseDouble), кидай в стек. Если оператор — вытащи два числа (b = pop, a = pop), применяй операцию, кидай результат обратно. В конце в стеке должно остаться одно число — результат.',
      solution: 'import java.util.Stack;\n\npublic class RPNCalculator {\n\n    public static double evaluateRPN(String expression) {\n        Stack<Double> stack = new Stack<>();\n        String[] tokens = expression.trim().split("\\\\s+");\n\n        for (String token : tokens) {\n            switch (token) {\n                case "+":\n                case "-":\n                case "*":\n                case "/": {\n                    if (stack.size() < 2) {\n                        throw new IllegalArgumentException(\n                            "Недостаточно операндов для оператора " + token);\n                    }\n                    double b = stack.pop();\n                    double a = stack.pop();\n                    switch (token) {\n                        case "+": stack.push(a + b); break;\n                        case "-": stack.push(a - b); break;\n                        case "*": stack.push(a * b); break;\n                        case "/":\n                            if (b == 0) throw new ArithmeticException("Деление на ноль");\n                            stack.push(a / b);\n                            break;\n                    }\n                    break;\n                }\n                default:\n                    try {\n                        stack.push(Double.parseDouble(token));\n                    } catch (NumberFormatException e) {\n                        throw new IllegalArgumentException("Неизвестный токен: " + token);\n                    }\n            }\n        }\n\n        if (stack.size() != 1) {\n            throw new IllegalArgumentException("Некорректное выражение: в стеке осталось " + stack.size() + " элементов");\n        }\n        return stack.pop();\n    }\n\n    public static void eval(String expr) {\n        try {\n            double result = evaluateRPN(expr);\n            System.out.printf("\\"%s\\" = %.1f%n", expr, result);\n        } catch (Exception e) {\n            System.out.printf("Ошибка: \\"%s\\" -> %s%n", expr, e.getMessage());\n        }\n    }\n\n    public static void main(String[] args) {\n        eval("3 4 +");\n        eval("10 2 /");\n        eval("5 1 2 + 4 * + 3 -");     // = 5 + (1+2)*4 - 3 = 14\n        eval("15 7 1 1 + - / 3 * 2 1 1 + + -"); // = 5\n        eval("2 3 * 4 5 * +");          // = 6 + 20 = 26\n        eval("1 0 /");                  // Деление на ноль\n        eval("1 2 3");                  // Некорректное: лишние операнды\n    }\n}',
      explanation: 'ОПН (Обратная Польская нотация) — элегантный способ представить выражения без скобок и без неоднозначности приоритетов. Алгоритм вычисления ОПН: стек. Число — в стек. Оператор — достать два числа, применить операцию, результат — в стек. В конце результат — единственный элемент стека. Почему pop порядок важен: b = pop(), a = pop() — b второй операнд, a первый. "5 3 -" = 5 - 3 = 2, не 3 - 5. Это частая ошибка. Обработка ошибок через исключения: деление на ноль, лишние операнды, неизвестные токены. ОПН используется в стековых машинах (JVM байткод!), HP-калькуляторах, Forth-языке. Алгоритм сортировочной станции (Дейкстры) конвертирует инфиксную запись в ОПН с учётом приоритетов.'
    },
    {
      id: 8,
      title: 'Задача: Мини-парсер аргументов командной строки',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуй парсер аргументов командной строки. Поддерживает флаги (--verbose, -v), параметры (--output file.txt), позиционные аргументы. Результат — объект ParsedArgs с доступом к флагам и параметрам.',
      requirements: [
        'Метод parse(String[] args) возвращает ParsedArgs',
        'Флаги: --flag или -f (без значения)',
        'Параметры: --key value или -k value',
        'Позиционные аргументы: слова без "-"',
        'Класс ParsedArgs: hasFlag, getParam, getPositional(index)',
        'В main() продемонстрировать парсинг разных комбинаций'
      ],
      expectedOutput: 'Аргументы: --verbose --output result.txt input.csv\nhasFlag("verbose"): true\nhasFlag("quiet"): false\ngetParam("output"): result.txt\ngetPositional(0): input.csv\nАргументы: -v -o result.txt -n 10 data.csv report.csv\nhasFlag("v"): true\ngetParam("o"): result.txt\ngetParam("n"): 10',
      hint: 'Итерируй по args. Если args[i] начинается с "--" — длинная форма. Если "-" — короткая. Если следующий args[i+1] не начинается с "-" — это значение параметра (i++). Иначе — это флаг. Остальное — позиционные аргументы.',
      solution: 'import java.util.*;\n\npublic class ArgParser {\n\n    public static class ParsedArgs {\n        private final Set<String> flags = new HashSet<>();\n        private final Map<String, String> params = new HashMap<>();\n        private final List<String> positional = new ArrayList<>();\n\n        public boolean hasFlag(String flag)      { return flags.contains(flag); }\n        public String getParam(String key)        { return params.get(key); }\n        public String getParam(String key, String def) { return params.getOrDefault(key, def); }\n        public String getPositional(int index)    {\n            return index < positional.size() ? positional.get(index) : null;\n        }\n        public List<String> getPositionals()      { return Collections.unmodifiableList(positional); }\n\n        @Override\n        public String toString() {\n            return "flags=" + flags + " params=" + params + " positional=" + positional;\n        }\n    }\n\n    public static ParsedArgs parse(String[] args) {\n        ParsedArgs result = new ParsedArgs();\n\n        for (int i = 0; i < args.length; i++) {\n            String arg = args[i];\n\n            if (arg.startsWith("--")) {\n                String key = arg.substring(2);\n                if (key.isEmpty()) continue; // "--" как разделитель\n                // Проверить: следующий аргумент — значение или ещё один флаг?\n                if (i + 1 < args.length && !args[i + 1].startsWith("-")) {\n                    result.params.put(key, args[++i]);\n                } else {\n                    result.flags.add(key);\n                }\n            } else if (arg.startsWith("-") && arg.length() > 1) {\n                String key = arg.substring(1);\n                if (i + 1 < args.length && !args[i + 1].startsWith("-")) {\n                    result.params.put(key, args[++i]);\n                } else {\n                    result.flags.add(key);\n                }\n            } else {\n                result.positional.add(arg);\n            }\n        }\n        return result;\n    }\n\n    public static void demo(String... args) {\n        System.out.println("Аргументы: " + String.join(" ", args));\n        ParsedArgs parsed = parse(args);\n        System.out.println("Результат: " + parsed);\n        System.out.println();\n    }\n\n    public static void main(String[] args) {\n        ParsedArgs p1 = parse(new String[]{"--verbose", "--output", "result.txt", "input.csv"});\n        System.out.println("--verbose --output result.txt input.csv");\n        System.out.println("hasFlag(verbose): " + p1.hasFlag("verbose"));\n        System.out.println("hasFlag(quiet): "   + p1.hasFlag("quiet"));\n        System.out.println("getParam(output): " + p1.getParam("output"));\n        System.out.println("getPositional(0): " + p1.getPositional(0));\n        System.out.println();\n\n        ParsedArgs p2 = parse(new String[]{"-v", "-o", "result.txt", "-n", "10", "data.csv"});\n        System.out.println("-v -o result.txt -n 10 data.csv");\n        System.out.println("hasFlag(v): "   + p2.hasFlag("v"));\n        System.out.println("getParam(o): "  + p2.getParam("o"));\n        System.out.println("getParam(n): "  + p2.getParam("n"));\n        System.out.println("positional: "   + p2.getPositionals());\n    }\n}',
      explanation: 'Парсер аргументов — базовый компонент любого CLI-инструмента. Задача учит аккуратной работе с массивами и несколькими типами входных данных. Ключевой момент: чтобы определить флаг от параметра со значением, нужно смотреть вперёд (lookahead): есть ли следующий аргумент и не начинается ли он с "-". i++ внутри условия — корректный способ "пропустить" следующий аргумент в цикле. Использование Set для флагов (быстрый lookup), Map для параметров, List для позиционных — правильный выбор структур данных. В продакшене используют Apache Commons CLI, picocli, JCommander — они обрабатывают сложные случаи (--key=value, bundled options -abc). Эта задача учит понимать, что делают такие библиотеки.'
    },
    {
      id: 9,
      title: 'Задача: Пайплайн обработки данных (filter-transform-aggregate)',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создай гибкий пайплайн обработки данных в функциональном стиле. Pipeline<T, R> поддерживает цепочку операций: filter, map, reduce/collect. Реализуй без использования Stream API — собственная реализация через список функций.',
      requirements: [
        'Класс Pipeline<T> с методами filter(Predicate<T>), map(Function<T,T>)',
        'Метод collect() возвращает List<T> с результатом',
        'Метод forEach(Consumer<T>) для конечной обработки',
        'Ленивое вычисление: операции применяются только при collect()',
        'В main() продемонстрировать на списке объектов Employee'
      ],
      expectedOutput: 'Сотрудники с зарплатой > 50000, отсортированные:\n1. Алия Сейткали - 75000\n2. Нурдаулет Ахмет - 80000\n3. Карим Жаксыбеков - 90000\nСредняя зарплата топ-сотрудников: 81666.67',
      hint: 'Хранить List<Function<List<T>, List<T>>> stages. filter добавляет stage: list -> list.stream().filter(p).collect(...). map аналогично. collect применяет все stages последовательно к исходным данным. Начальные данные можно передать при создании или в collect.',
      solution: 'import java.util.*;\nimport java.util.function.*;\nimport java.util.stream.Collectors;\n\npublic class DataPipeline {\n\n    static class Employee {\n        String name;\n        int salary;\n        String department;\n\n        Employee(String name, int salary, String department) {\n            this.name = name;\n            this.salary = salary;\n            this.department = department;\n        }\n\n        @Override\n        public String toString() {\n            return name + " - " + salary;\n        }\n    }\n\n    static class Pipeline<T> {\n        private final List<T> source;\n        private final List<Function<List<T>, List<T>>> stages = new ArrayList<>();\n\n        Pipeline(List<T> source) {\n            this.source = new ArrayList<>(source);\n        }\n\n        public Pipeline<T> filter(Predicate<T> predicate) {\n            stages.add(list -> list.stream().filter(predicate).collect(Collectors.toList()));\n            return this;\n        }\n\n        public Pipeline<T> map(UnaryOperator<T> mapper) {\n            stages.add(list -> list.stream().map(mapper).collect(Collectors.toList()));\n            return this;\n        }\n\n        public Pipeline<T> sorted(Comparator<T> comparator) {\n            stages.add(list -> list.stream().sorted(comparator).collect(Collectors.toList()));\n            return this;\n        }\n\n        public Pipeline<T> limit(int n) {\n            stages.add(list -> list.stream().limit(n).collect(Collectors.toList()));\n            return this;\n        }\n\n        public List<T> collect() {\n            List<T> result = new ArrayList<>(source);\n            for (Function<List<T>, List<T>> stage : stages) {\n                result = stage.apply(result);\n            }\n            return result;\n        }\n\n        public void forEach(Consumer<T> consumer) {\n            collect().forEach(consumer);\n        }\n    }\n\n    public static void main(String[] args) {\n        List<Employee> employees = Arrays.asList(\n            new Employee("Алия Сейткали",     75000, "IT"),\n            new Employee("Бекзат Нуров",       35000, "HR"),\n            new Employee("Нурдаулет Ахмет",    80000, "IT"),\n            new Employee("Гульнара Касымова",  45000, "Finance"),\n            new Employee("Карим Жаксыбеков",   90000, "IT"),\n            new Employee("Айгерим Бекова",     30000, "HR")\n        );\n\n        System.out.println("Сотрудники IT отдела с зарплатой > 50000:");\n        List<Employee> result = new Pipeline<>(employees)\n            .filter(e -> e.department.equals("IT"))\n            .filter(e -> e.salary > 50000)\n            .sorted(Comparator.comparingInt(e -> e.salary))\n            .collect();\n\n        for (int i = 0; i < result.size(); i++) {\n            System.out.println((i + 1) + ". " + result.get(i));\n        }\n\n        double avg = result.stream()\n            .mapToInt(e -> e.salary)\n            .average().orElse(0);\n        System.out.printf("Средняя зарплата: %.2f%n", avg);\n    }\n}',
      explanation: 'Пайплайн обработки данных — фундаментальный паттерн в ETL, потоковой обработке, реактивном программировании. Реализация через список функций List<Function<List<T>, List<T>>> — каждый этап трансформирует список. Ленивость (laziness): stages накапливаются, применяются только при collect() — позволяет описать "что сделать" отдельно от "когда делать". Method chaining (цепочка вызовов): return this позволяет писать .filter().map().sorted(). Это fluent API — стиль, популярный в Java 8+. На самом деле мы переизобрели упрощённый Stream API, что помогает понять, как устроен настоящий. Реальные пайплайны (Apache Beam, Spark) добавляют параллельность, оптимизацию и отказоустойчивость.'
    },
    {
      id: 10,
      title: 'Задача: Пакетный обработчик файлов с обработкой ошибок',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуй пакетный обработчик задач (batch processor) с обработкой ошибок. Принимает список "файлов" (имитированных строками), обрабатывает каждый, собирает результаты успехов и ошибок. Поддерживает retry для временных ошибок.',
      requirements: [
        'Интерфейс FileProcessor с методом process(String filename) throws Exception',
        'Класс BatchResult с полями: успехи, ошибки, пропущенные',
        'Метод processBatch(List<String> files, FileProcessor processor) возвращает BatchResult',
        'Retry: при временной ошибке (TransientException) повторить до 3 раз',
        'Продолжить обработку остальных файлов при ошибке одного',
        'Итоговый отчёт: сколько успешно, сколько ошибок, список проблемных файлов'
      ],
      expectedOutput: 'Обработка 6 файлов...\n[OK] data1.csv (150 записей)\n[OK] data2.csv (200 записей)\n[RETRY 1] data3.csv - временная ошибка, повтор...\n[OK] data3.csv (retry успешен, 180 записей)\n[ERROR] corrupt.csv - неверный формат\n[RETRY 1] slow.csv - ...\n[RETRY 2] slow.csv - ...\n[OK] slow.csv (retry успешен)\n[OK] data4.csv (120 записей)\n---\nУспешно: 5/6\nОшибки: 1/6\nПроблемные файлы: corrupt.csv',
      hint: 'TransientException extends Exception — временные ошибки (сеть, блокировка). Для retry: цикл for (int attempt = 1; attempt <= maxRetries; attempt++). Если TransientException и attempt < maxRetries — продолжить (continue внешний loop нельзя, нужен другой механизм). BatchResult содержит List<String> successes и Map<String, String> errors.',
      solution: 'import java.util.*;\n\npublic class BatchProcessor {\n\n    // Интерфейс обработчика\n    @FunctionalInterface\n    interface FileProcessor {\n        String process(String filename) throws Exception;\n    }\n\n    // Временная ошибка (можно повторить)\n    static class TransientException extends Exception {\n        TransientException(String msg) { super(msg); }\n    }\n\n    // Результат пакетной обработки\n    static class BatchResult {\n        List<String> successes = new ArrayList<>();\n        Map<String, String> errors = new LinkedHashMap<>();\n        int retryCount = 0;\n\n        void addSuccess(String file)                  { successes.add(file); }\n        void addError(String file, String error)       { errors.put(file, error); }\n\n        void printReport() {\n            int total = successes.size() + errors.size();\n            System.out.println("---");\n            System.out.printf("Успешно: %d/%d%n", successes.size(), total);\n            System.out.printf("Ошибки: %d/%d%n", errors.size(), total);\n            System.out.printf("Всего retry: %d%n", retryCount);\n            if (!errors.isEmpty()) {\n                System.out.println("Проблемные файлы:");\n                errors.forEach((file, err) ->\n                    System.out.println("  " + file + ": " + err));\n            }\n        }\n    }\n\n    private static final int MAX_RETRIES = 3;\n\n    public static BatchResult processBatch(List<String> files, FileProcessor processor) {\n        BatchResult result = new BatchResult();\n        System.out.println("Обработка " + files.size() + " файлов...");\n\n        for (String file : files) {\n            String lastError = null;\n            boolean success = false;\n\n            for (int attempt = 1; attempt <= MAX_RETRIES; attempt++) {\n                try {\n                    String output = processor.process(file);\n                    if (attempt > 1) {\n                        System.out.println("[OK] " + file + " (retry " + attempt + " успешен: " + output + ")");\n                    } else {\n                        System.out.println("[OK] " + file + " (" + output + ")");\n                    }\n                    result.addSuccess(file);\n                    success = true;\n                    break;\n                } catch (TransientException e) {\n                    result.retryCount++;\n                    lastError = e.getMessage();\n                    if (attempt < MAX_RETRIES) {\n                        System.out.println("[RETRY " + attempt + "] " + file + " - " + lastError + ", повтор...");\n                    } else {\n                        System.out.println("[FAIL] " + file + " - исчерпаны попытки: " + lastError);\n                    }\n                } catch (Exception e) {\n                    // Не временная ошибка — не повторяем\n                    System.out.println("[ERROR] " + file + " - " + e.getMessage());\n                    result.addError(file, e.getMessage());\n                    success = true; // Чтобы не добавить ошибку ещё раз\n                    break;\n                }\n            }\n\n            if (!success) {\n                result.addError(file, "Исчерпаны попытки: " + lastError);\n            }\n        }\n\n        result.printReport();\n        return result;\n    }\n\n    public static void main(String[] args) {\n        // Имитируем файлы: счётчик retry для slow.csv\n        Map<String, Integer> retryTracker = new HashMap<>();\n\n        FileProcessor processor = filename -> {\n            switch (filename) {\n                case "data1.csv": return "150 записей";\n                case "data2.csv": return "200 записей";\n                case "data3.csv": {\n                    int cnt = retryTracker.merge("data3", 1, Integer::sum);\n                    if (cnt < 2) throw new TransientException("временная ошибка IO");\n                    return "180 записей";\n                }\n                case "corrupt.csv":\n                    throw new IllegalArgumentException("неверный формат файла");\n                case "slow.csv": {\n                    int cnt = retryTracker.merge("slow", 1, Integer::sum);\n                    if (cnt < 3) throw new TransientException("сервер недоступен");\n                    return "100 записей";\n                }\n                case "data4.csv": return "120 записей";\n                default: throw new IllegalArgumentException("файл не найден");\n            }\n        };\n\n        processBatch(\n            Arrays.asList("data1.csv", "data2.csv", "data3.csv",\n                          "corrupt.csv", "slow.csv", "data4.csv"),\n            processor\n        );\n    }\n}',
      explanation: 'Пакетный обработчик с retry — реалистичный паттерн для работы с файлами, API, базами данных. Ключевое разделение: TransientException (временная — сеть, блокировка) vs постоянная ошибка (неверный формат). Для временных — retry, для постоянных — пропустить и продолжить. Логика retry: внутренний цикл от 1 до MAX_RETRIES, break при успехе, отслеживать lastError для финального отчёта. @FunctionalInterface позволяет передавать лямбду как processor. BatchResult как DTO агрегирует результаты. merge(key, 1, Integer::sum) в HashMap — счётчик retry для теста. В продакшене добавляют exponential backoff (следующая задача), логирование, метрики и, возможно, параллельную обработку через ExecutorService.'
    }
  ]
}
