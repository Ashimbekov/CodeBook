export default {
  id: 47,
  title: 'JDBC: работа с базами данных',
  description: 'Научимся подключаться к базам данных из Java, выполнять запросы SELECT/INSERT/UPDATE/DELETE и работать с транзакциями через JDBC',
  lessons: [
    {
      id: 1, title: 'Что такое JDBC?', type: 'theory',
      content: [
        { type: 'text', value: 'JDBC (Java Database Connectivity) — это стандартный API Java для работы с реляционными базами данных. JDBC позволяет Java-программам подключаться к любой базе данных: MySQL, PostgreSQL, SQLite, Oracle и другим.' },
        { type: 'tip', value: 'JDBC — это как универсальный переводчик между Java и любой базой данных. Ты пишешь один и тот же Java-код, а JDBC сам "переводит" команды для конкретной базы данных. Хочешь сменить MySQL на PostgreSQL — меняешь только настройки подключения, код остаётся прежним.' },
        { type: 'heading', value: 'Архитектура JDBC' },
        { type: 'list', items: [
          'Твой Java-код → JDBC API (стандартный интерфейс)',
          'JDBC API → JDBC Driver (специфичный для конкретной БД)',
          'JDBC Driver → База данных'
        ]},
        { type: 'heading', value: 'Добавление зависимостей' },
        { type: 'code', language: 'xml', value: '<!-- Maven: SQLite (встроенная БД, не нужно устанавливать) -->\n<dependency>\n    <groupId>org.xerial</groupId>\n    <artifactId>sqlite-jdbc</artifactId>\n    <version>3.43.0.0</version>\n</dependency>\n\n<!-- Maven: PostgreSQL -->\n<dependency>\n    <groupId>org.postgresql</groupId>\n    <artifactId>postgresql</artifactId>\n    <version>42.6.0</version>\n</dependency>\n\n<!-- Maven: MySQL -->\n<dependency>\n    <groupId>com.mysql</groupId>\n    <artifactId>mysql-connector-j</artifactId>\n    <version>8.1.0</version>\n</dependency>' },
        { type: 'heading', value: 'Основные классы JDBC' },
        { type: 'list', items: [
          'DriverManager — создаёт соединение с базой данных',
          'Connection — соединение с БД, через него выполняем запросы',
          'Statement — для выполнения SQL-запросов',
          'PreparedStatement — для параметризованных запросов (безопаснее)',
          'ResultSet — результат SELECT-запроса (курсор по строкам)'
        ]},
        { type: 'note', value: 'В учебных примерах мы используем SQLite — это база данных в одном файле, не требует отдельного сервера. Идеально для обучения и небольших проектов.' }
      ]
    },
    {
      id: 2, title: 'Подключение к базе данных', type: 'theory',
      content: [
        { type: 'text', value: 'Первый шаг в работе с JDBC — установить соединение с базой данных. Это делается через DriverManager.getConnection().' },
        { type: 'code', language: 'java', value: 'import java.sql.Connection;\nimport java.sql.DriverManager;\nimport java.sql.SQLException;\n\npublic class DatabaseDemo {\n    public static void main(String[] args) {\n        // URL подключения зависит от типа БД\n        String url = "jdbc:sqlite:myapp.db"; // SQLite файл\n\n        // try-with-resources автоматически закрывает соединение\n        try (Connection conn = DriverManager.getConnection(url)) {\n            System.out.println("Подключено к базе данных!");\n            System.out.println("Драйвер: " + conn.getMetaData().getDriverName());\n        } catch (SQLException e) {\n            System.err.println("Ошибка подключения: " + e.getMessage());\n        }\n    }\n}' },
        { type: 'heading', value: 'URL подключения для разных БД' },
        { type: 'code', language: 'java', value: '// SQLite (файл на диске)\nString sqliteUrl = "jdbc:sqlite:database.db";\n\n// SQLite в памяти (данные не сохраняются)\nString sqliteMemUrl = "jdbc:sqlite::memory:";\n\n// PostgreSQL\nString pgUrl = "jdbc:postgresql://localhost:5432/mydb";\n\n// MySQL\nString mysqlUrl = "jdbc:mysql://localhost:3306/mydb";\n\n// Для PostgreSQL и MySQL нужны логин и пароль:\nConnection conn = DriverManager.getConnection(\n    "jdbc:postgresql://localhost:5432/mydb",\n    "username",  // логин\n    "password"   // пароль\n);' },
        { type: 'heading', value: 'Создание таблицы' },
        { type: 'code', language: 'java', value: 'import java.sql.*;\n\npublic class DatabaseSetup {\n    public static void main(String[] args) throws SQLException {\n        String url = "jdbc:sqlite:school.db";\n\n        try (Connection conn = DriverManager.getConnection(url);\n             Statement stmt = conn.createStatement()) {\n\n            // Создаём таблицу студентов\n            stmt.execute(\n                "CREATE TABLE IF NOT EXISTS students (" +\n                "id INTEGER PRIMARY KEY AUTOINCREMENT," +\n                "name TEXT NOT NULL," +\n                "age INTEGER," +\n                "grade REAL" +\n                ")"\n            );\n\n            System.out.println("Таблица students создана!");\n        }\n    }\n}' },
        { type: 'tip', value: 'CREATE TABLE IF NOT EXISTS — безопасная команда: создаёт таблицу только если её нет. Запускай её при старте приложения — если таблица уже есть, ничего не сломается.' }
      ]
    },
    {
      id: 3, title: 'Statement и PreparedStatement', type: 'theory',
      content: [
        { type: 'text', value: 'Statement — это объект для выполнения SQL-запросов. PreparedStatement — его безопасная версия, использующая параметры вместо вставки значений прямо в строку запроса.' },
        { type: 'heading', value: 'Обычный Statement — небезопасно!' },
        { type: 'code', language: 'java', value: '// ОПАСНО! SQL-инъекция возможна!\nString userInput = "Иван\' OR \'1\'=\'1";\nString sql = "SELECT * FROM users WHERE name = \'" + userInput + "\'";\n// Итоговый SQL: SELECT * FROM users WHERE name = \'Иван\' OR \'1\'=\'1\'\n// Злоумышленник получит ВСЕ строки таблицы!\n\nStatement stmt = conn.createStatement();\nResultSet rs = stmt.executeQuery(sql); // НИКОГДА ТАК НЕ ДЕЛАЙ!' },
        { type: 'heading', value: 'PreparedStatement — правильный способ' },
        { type: 'code', language: 'java', value: 'import java.sql.*;\n\npublic class PreparedDemo {\n    public static void main(String[] args) throws SQLException {\n        String url = "jdbc:sqlite:school.db";\n\n        try (Connection conn = DriverManager.getConnection(url)) {\n            // ? — это плейсхолдеры для параметров\n            String sql = "INSERT INTO students (name, age, grade) VALUES (?, ?, ?)";\n\n            // Подготавливаем запрос один раз\n            try (PreparedStatement pstmt = conn.prepareStatement(sql)) {\n\n                // Устанавливаем параметры по индексу (с 1, не с 0!)\n                pstmt.setString(1, "Иван Иванов");  // ? -> "Иван Иванов"\n                pstmt.setInt(2, 20);                // ? -> 20\n                pstmt.setDouble(3, 4.5);            // ? -> 4.5\n\n                // Выполняем вставку\n                int rowsAffected = pstmt.executeUpdate();\n                System.out.println("Добавлено строк: " + rowsAffected);\n\n                // Повторное использование: меняем параметры\n                pstmt.setString(1, "Мария Петрова");\n                pstmt.setInt(2, 19);\n                pstmt.setDouble(3, 4.8);\n                pstmt.executeUpdate();\n\n                System.out.println("Студенты добавлены!");\n            }\n        }\n    }\n}' },
        { type: 'warning', value: 'Всегда используй PreparedStatement когда в запрос подставляются данные от пользователя! SQL-инъекции — одна из самых распространённых уязвимостей в веб-приложениях. PreparedStatement полностью защищает от этого.' },
        { type: 'heading', value: 'Методы установки параметров' },
        { type: 'list', items: [
          'setString(index, value) — для TEXT/VARCHAR',
          'setInt(index, value) — для INTEGER',
          'setDouble(index, value) — для DOUBLE/REAL',
          'setBoolean(index, value) — для BOOLEAN',
          'setNull(index, Types.VARCHAR) — для NULL значений',
          'setDate(index, date) — для дат'
        ]}
      ]
    },
    {
      id: 4, title: 'Выборка данных (SELECT)', type: 'theory',
      content: [
        { type: 'text', value: 'Для получения данных из базы используется executeQuery(). Он возвращает ResultSet — объект-курсор, который перемещается по строкам результата.' },
        { type: 'tip', value: 'ResultSet — как палец, который двигается по строкам таблицы. Изначально он перед первой строкой. Вызов next() перемещает его на следующую строку и возвращает true. Когда строки закончились — возвращает false.' },
        { type: 'code', language: 'java', value: 'import java.sql.*;\n\npublic class SelectDemo {\n    public static void main(String[] args) throws SQLException {\n        String url = "jdbc:sqlite:school.db";\n\n        try (Connection conn = DriverManager.getConnection(url)) {\n\n            // Получить всех студентов\n            String sql = "SELECT id, name, age, grade FROM students";\n\n            try (Statement stmt = conn.createStatement();\n                 ResultSet rs = stmt.executeQuery(sql)) {\n\n                System.out.println("ID | Имя           | Возраст | Оценка");\n                System.out.println("---+---------------+---------+-------");\n\n                while (rs.next()) { // двигаемся по строкам\n                    int id = rs.getInt("id");\n                    String name = rs.getString("name");\n                    int age = rs.getInt("age");\n                    double grade = rs.getDouble("grade");\n\n                    System.out.printf("%-2d | %-13s | %-7d | %.1f%n",\n                                      id, name, age, grade);\n                }\n            }\n        }\n    }\n}' },
        { type: 'heading', value: 'SELECT с параметрами' },
        { type: 'code', language: 'java', value: 'import java.sql.*;\n\npublic class SelectWithParams {\n    public static void main(String[] args) throws SQLException {\n        String url = "jdbc:sqlite:school.db";\n\n        try (Connection conn = DriverManager.getConnection(url)) {\n            // Найти студентов с оценкой выше порогового значения\n            String sql = "SELECT name, grade FROM students WHERE grade >= ? ORDER BY grade DESC";\n\n            try (PreparedStatement pstmt = conn.prepareStatement(sql)) {\n                pstmt.setDouble(1, 4.0); // оценка >= 4.0\n\n                try (ResultSet rs = pstmt.executeQuery()) {\n                    System.out.println("Хорошисты и отличники:");\n                    while (rs.next()) {\n                        System.out.printf("  %s - %.1f%n",\n                            rs.getString("name"),\n                            rs.getDouble("grade"));\n                    }\n                }\n            }\n        }\n    }\n}' },
        { type: 'heading', value: 'Получение одной строки' },
        { type: 'code', language: 'java', value: 'public static Student findById(Connection conn, int id) throws SQLException {\n    String sql = "SELECT * FROM students WHERE id = ?";\n    try (PreparedStatement pstmt = conn.prepareStatement(sql)) {\n        pstmt.setInt(1, id);\n        try (ResultSet rs = pstmt.executeQuery()) {\n            if (rs.next()) { // если нашли строку\n                Student s = new Student();\n                s.setId(rs.getInt("id"));\n                s.setName(rs.getString("name"));\n                s.setAge(rs.getInt("age"));\n                s.setGrade(rs.getDouble("grade"));\n                return s;\n            }\n            return null; // не найдено\n        }\n    }\n}' },
        { type: 'note', value: 'Всегда закрывай ResultSet, Statement и Connection! Незакрытые ресурсы — утечки памяти. Используй try-with-resources (try (Resource r = ...)) — Java закроет их автоматически.' }
      ]
    },
    {
      id: 5, title: 'Изменение данных (INSERT, UPDATE, DELETE)', type: 'theory',
      content: [
        { type: 'text', value: 'Для изменения данных в базе (INSERT, UPDATE, DELETE) используется executeUpdate(). В отличие от executeQuery(), он не возвращает ResultSet, а возвращает количество затронутых строк.' },
        { type: 'code', language: 'java', value: 'import java.sql.*;\n\npublic class CRUDDemo {\n    static String url = "jdbc:sqlite:school.db";\n\n    // INSERT — добавление новой записи\n    public static void addStudent(String name, int age, double grade) {\n        String sql = "INSERT INTO students (name, age, grade) VALUES (?, ?, ?)";\n        try (Connection conn = DriverManager.getConnection(url);\n             PreparedStatement pstmt = conn.prepareStatement(sql,\n                 Statement.RETURN_GENERATED_KEYS)) {\n\n            pstmt.setString(1, name);\n            pstmt.setInt(2, age);\n            pstmt.setDouble(3, grade);\n\n            int rows = pstmt.executeUpdate();\n\n            // Получить сгенерированный ID\n            try (ResultSet keys = pstmt.getGeneratedKeys()) {\n                if (keys.next()) {\n                    System.out.println("Добавлен студент с ID: " + keys.getInt(1));\n                }\n            }\n        } catch (SQLException e) {\n            System.err.println("Ошибка добавления: " + e.getMessage());\n        }\n    }\n\n    // UPDATE — обновление записи\n    public static void updateGrade(int studentId, double newGrade) {\n        String sql = "UPDATE students SET grade = ? WHERE id = ?";\n        try (Connection conn = DriverManager.getConnection(url);\n             PreparedStatement pstmt = conn.prepareStatement(sql)) {\n\n            pstmt.setDouble(1, newGrade);\n            pstmt.setInt(2, studentId);\n\n            int rows = pstmt.executeUpdate();\n            System.out.println("Обновлено строк: " + rows);\n        } catch (SQLException e) {\n            System.err.println("Ошибка обновления: " + e.getMessage());\n        }\n    }\n\n    // DELETE — удаление записи\n    public static void deleteStudent(int studentId) {\n        String sql = "DELETE FROM students WHERE id = ?";\n        try (Connection conn = DriverManager.getConnection(url);\n             PreparedStatement pstmt = conn.prepareStatement(sql)) {\n\n            pstmt.setInt(1, studentId);\n            int rows = pstmt.executeUpdate();\n\n            if (rows > 0) {\n                System.out.println("Студент ID=" + studentId + " удалён");\n            } else {\n                System.out.println("Студент не найден");\n            }\n        } catch (SQLException e) {\n            System.err.println("Ошибка удаления: " + e.getMessage());\n        }\n    }\n\n    public static void main(String[] args) throws SQLException {\n        // Создаём таблицу\n        try (Connection conn = DriverManager.getConnection(url);\n             Statement stmt = conn.createStatement()) {\n            stmt.execute("CREATE TABLE IF NOT EXISTS students " +\n                "(id INTEGER PRIMARY KEY AUTOINCREMENT, " +\n                "name TEXT, age INTEGER, grade REAL)");\n        }\n\n        addStudent("Иван", 20, 4.5);\n        addStudent("Мария", 19, 4.8);\n        updateGrade(1, 5.0);\n        deleteStudent(2);\n    }\n}' },
        { type: 'tip', value: 'Statement.RETURN_GENERATED_KEYS позволяет получить автоматически сгенерированный ID после INSERT. Это важно когда нужно сразу использовать новый ID.' }
      ]
    },
    {
      id: 6, title: 'ResultSet и маппинг в объекты', type: 'theory',
      content: [
        { type: 'text', value: 'В реальных проектах данные из ResultSet обычно преобразуют в Java-объекты. Это называется маппингом (mapping) и обычно выносится в отдельные методы.' },
        { type: 'code', language: 'java', value: 'import java.sql.*;\nimport java.util.ArrayList;\nimport java.util.List;\n\nclass Student {\n    private int id;\n    private String name;\n    private int age;\n    private double grade;\n\n    // Геттеры и сеттеры\n    public int getId() { return id; }\n    public void setId(int id) { this.id = id; }\n    public String getName() { return name; }\n    public void setName(String name) { this.name = name; }\n    public int getAge() { return age; }\n    public void setAge(int age) { this.age = age; }\n    public double getGrade() { return grade; }\n    public void setGrade(double grade) { this.grade = grade; }\n\n    @Override\n    public String toString() {\n        return String.format("Student{id=%d, name=%s, age=%d, grade=%.1f}",\n                             id, name, age, grade);\n    }\n}\n\nclass StudentDAO {\n    private String url;\n\n    public StudentDAO(String url) { this.url = url; }\n\n    // Маппинг одной строки ResultSet в объект Student\n    private Student mapRow(ResultSet rs) throws SQLException {\n        Student s = new Student();\n        s.setId(rs.getInt("id"));\n        s.setName(rs.getString("name"));\n        s.setAge(rs.getInt("age"));\n        s.setGrade(rs.getDouble("grade"));\n        return s;\n    }\n\n    public List<Student> findAll() throws SQLException {\n        List<Student> students = new ArrayList<>();\n        String sql = "SELECT * FROM students ORDER BY name";\n\n        try (Connection conn = DriverManager.getConnection(url);\n             Statement stmt = conn.createStatement();\n             ResultSet rs = stmt.executeQuery(sql)) {\n\n            while (rs.next()) {\n                students.add(mapRow(rs)); // используем маппинг\n            }\n        }\n        return students;\n    }\n\n    public Student findById(int id) throws SQLException {\n        String sql = "SELECT * FROM students WHERE id = ?";\n        try (Connection conn = DriverManager.getConnection(url);\n             PreparedStatement pstmt = conn.prepareStatement(sql)) {\n\n            pstmt.setInt(1, id);\n            try (ResultSet rs = pstmt.executeQuery()) {\n                if (rs.next()) return mapRow(rs);\n            }\n        }\n        return null;\n    }\n}' },
        { type: 'note', value: 'DAO (Data Access Object) — это паттерн проектирования для изоляции логики работы с базой данных. StudentDAO содержит все SQL-запросы для таблицы students. Остальной код приложения не знает о деталях SQL.' }
      ]
    },
    {
      id: 7, title: 'Транзакции', type: 'theory',
      content: [
        { type: 'text', value: 'Транзакция — это набор операций, которые выполняются как одно целое. Либо все операции выполняются успешно, либо все отменяются. Это называется атомарностью.' },
        { type: 'tip', value: 'Представь перевод денег: снять с одного счёта и положить на другой. Нельзя снять и не положить (деньги исчезнут), или положить и не снять (деньги появятся из воздуха). Транзакция гарантирует что оба действия либо выполнятся, либо оба отменятся.' },
        { type: 'code', language: 'java', value: 'import java.sql.*;\n\npublic class TransactionDemo {\n    static String url = "jdbc:sqlite:bank.db";\n\n    public static void transfer(int fromId, int toId, double amount) {\n        try (Connection conn = DriverManager.getConnection(url)) {\n\n            // Отключаем автоматические транзакции\n            conn.setAutoCommit(false);\n\n            try {\n                // Списание с счёта отправителя\n                String debit = "UPDATE accounts SET balance = balance - ? WHERE id = ? AND balance >= ?";\n                try (PreparedStatement pstmt = conn.prepareStatement(debit)) {\n                    pstmt.setDouble(1, amount);\n                    pstmt.setInt(2, fromId);\n                    pstmt.setDouble(3, amount);\n                    int rows = pstmt.executeUpdate();\n                    if (rows == 0) {\n                        throw new SQLException("Недостаточно средств!");\n                    }\n                }\n\n                // Зачисление на счёт получателя\n                String credit = "UPDATE accounts SET balance = balance + ? WHERE id = ?";\n                try (PreparedStatement pstmt = conn.prepareStatement(credit)) {\n                    pstmt.setDouble(1, amount);\n                    pstmt.setInt(2, toId);\n                    pstmt.executeUpdate();\n                }\n\n                // Всё хорошо — фиксируем транзакцию\n                conn.commit();\n                System.out.println("Перевод успешен: " + amount + " руб");\n\n            } catch (SQLException e) {\n                // Ошибка — откатываем все изменения\n                conn.rollback();\n                System.out.println("Перевод отменён: " + e.getMessage());\n            }\n\n        } catch (SQLException e) {\n            System.err.println("Ошибка подключения: " + e.getMessage());\n        }\n    }\n}' },
        { type: 'list', items: [
          'setAutoCommit(false) — отключить авто-фиксацию (по умолчанию каждый запрос — транзакция)',
          'commit() — зафиксировать все изменения',
          'rollback() — отменить все изменения транзакции',
          'Используй транзакции для операций которые должны быть атомарными'
        ]},
        { type: 'warning', value: 'По умолчанию JDBC работает в режиме autoCommit=true — каждый запрос автоматически фиксируется. Для транзакций всегда устанавливай autoCommit=false и явно вызывай commit() или rollback().' }
      ]
    },
    {
      id: 8, title: 'Практика: CRUD-приложение', type: 'practice', difficulty: 'hard',
      description: 'Создай полноценное CRUD-приложение для управления списком задач (To-Do List). Реализуй все операции: создание, чтение, обновление и удаление задач с использованием SQLite и PreparedStatement.',
      requirements: [
        'Создай таблицу tasks: id (auto), title TEXT, done INTEGER (0/1), created_at TEXT',
        'Реализуй метод addTask(String title) — добавляет задачу, возвращает ID',
        'Реализуй метод getAllTasks() — возвращает List<Task>',
        'Реализуй метод completeTask(int id) — помечает задачу выполненной',
        'Реализуй метод deleteTask(int id) — удаляет задачу',
        'В main: добавь 3 задачи, выведи список, выполни одну, удали другую, снова выведи список'
      ],
      expectedOutput: '--- Все задачи ---\n[1] [ ] Купить продукты\n[2] [ ] Сделать домашнюю работу\n[3] [ ] Позвонить маме\nВыполняем задачу 1...\nУдаляем задачу 3...\n--- Все задачи ---\n[1] [x] Купить продукты\n[2] [ ] Сделать домашнюю работу',
      hint: 'Создай класс Task с полями id, title, done. В getAllTasks() читай done как rs.getInt("done") и сравнивай с 1. Для completeTask используй UPDATE tasks SET done = 1 WHERE id = ?. Выводи [x] если done=true, [ ] если false.',
      solution: 'import java.sql.*;\nimport java.util.ArrayList;\nimport java.util.List;\n\nclass Task {\n    private int id;\n    private String title;\n    private boolean done;\n\n    public Task(int id, String title, boolean done) {\n        this.id = id; this.title = title; this.done = done;\n    }\n\n    public int getId() { return id; }\n    public String getTitle() { return title; }\n    public boolean isDone() { return done; }\n\n    @Override\n    public String toString() {\n        String status = done ? "[x]" : "[ ]";\n        return "[" + id + "] " + status + " " + title;\n    }\n}\n\nclass TodoApp {\n    private final String url = "jdbc:sqlite:todo.db";\n\n    public TodoApp() throws SQLException {\n        try (Connection conn = DriverManager.getConnection(url);\n             Statement stmt = conn.createStatement()) {\n            stmt.execute(\n                "CREATE TABLE IF NOT EXISTS tasks (" +\n                "id INTEGER PRIMARY KEY AUTOINCREMENT," +\n                "title TEXT NOT NULL," +\n                "done INTEGER DEFAULT 0" +\n                ")"\n            );\n        }\n    }\n\n    public int addTask(String title) throws SQLException {\n        String sql = "INSERT INTO tasks (title) VALUES (?)";\n        try (Connection conn = DriverManager.getConnection(url);\n             PreparedStatement pstmt = conn.prepareStatement(sql,\n                 Statement.RETURN_GENERATED_KEYS)) {\n            pstmt.setString(1, title);\n            pstmt.executeUpdate();\n            try (ResultSet keys = pstmt.getGeneratedKeys()) {\n                if (keys.next()) return keys.getInt(1);\n            }\n        }\n        return -1;\n    }\n\n    public List<Task> getAllTasks() throws SQLException {\n        List<Task> tasks = new ArrayList<>();\n        String sql = "SELECT * FROM tasks ORDER BY id";\n        try (Connection conn = DriverManager.getConnection(url);\n             Statement stmt = conn.createStatement();\n             ResultSet rs = stmt.executeQuery(sql)) {\n            while (rs.next()) {\n                tasks.add(new Task(\n                    rs.getInt("id"),\n                    rs.getString("title"),\n                    rs.getInt("done") == 1\n                ));\n            }\n        }\n        return tasks;\n    }\n\n    public void completeTask(int id) throws SQLException {\n        String sql = "UPDATE tasks SET done = 1 WHERE id = ?";\n        try (Connection conn = DriverManager.getConnection(url);\n             PreparedStatement pstmt = conn.prepareStatement(sql)) {\n            pstmt.setInt(1, id);\n            pstmt.executeUpdate();\n        }\n    }\n\n    public void deleteTask(int id) throws SQLException {\n        String sql = "DELETE FROM tasks WHERE id = ?";\n        try (Connection conn = DriverManager.getConnection(url);\n             PreparedStatement pstmt = conn.prepareStatement(sql)) {\n            pstmt.setInt(1, id);\n            pstmt.executeUpdate();\n        }\n    }\n\n    public void printTasks() throws SQLException {\n        System.out.println("--- Все задачи ---");\n        for (Task t : getAllTasks()) {\n            System.out.println(t);\n        }\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) throws SQLException {\n        TodoApp app = new TodoApp();\n\n        app.addTask("Купить продукты");\n        app.addTask("Сделать домашнюю работу");\n        app.addTask("Позвонить маме");\n\n        app.printTasks();\n\n        System.out.println("Выполняем задачу 1...");\n        app.completeTask(1);\n\n        System.out.println("Удаляем задачу 3...");\n        app.deleteTask(3);\n\n        app.printTasks();\n    }\n}',
      explanation: 'TodoApp инкапсулирует всю работу с базой данных. Каждый метод открывает новое соединение и закрывает его через try-with-resources. В реальных проектах используют Connection Pool (пул соединений) чтобы не открывать/закрывать соединение при каждом запросе.'
    }
  ]
}
