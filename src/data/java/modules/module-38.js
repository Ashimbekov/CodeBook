export default {
  id: 38,
  title: 'ExecutorService и пулы потоков',
  description: 'Как правильно управлять потоками с помощью ExecutorService, Callable, Future, CompletableFuture и паттернов пулов потоков в реальных серверных приложениях',
  lessons: [
    {
      id: 1,
      title: 'Зачем нужны пулы потоков',
      type: 'theory',
      content: [
        { type: 'text', value: 'Создавать новый поток на каждый входящий запрос — фатальная ошибка в серверных приложениях. Каждый Thread в Java занимает ~1 МБ стека, а создание потока — дорогостоящий системный вызов. При 10 000 одновременных запросов сервер съест 10 ГБ памяти только на стеки и упадёт с OutOfMemoryError.' },
        { type: 'tip', value: 'Представь ресторан. Вариант А: нанимать нового официанта для каждого гостя, а после обеда увольнять. Вариант Б: иметь команду из 10 постоянных официантов, каждый обслуживает несколько столиков. Вариант Б — это пул потоков. Официанты (потоки) уже наняты и обучены, они берут заказы (задачи) из очереди и переключаются между столиками.' },
        { type: 'heading', value: 'Проблема: Thread-per-request' },
        { type: 'code', language: 'java', value: '// АНТИПАТТЕРН: создаём поток на каждый запрос\nclass NaiveWebServer {\n    public void start(int port) throws IOException {\n        ServerSocket server = new ServerSocket(port);\n        System.out.println("Сервер запущен на порту " + port);\n\n        while (true) {\n            Socket client = server.accept();\n            // Новый поток для каждого клиента!\n            new Thread(() -> handleRequest(client)).start();\n        }\n        // Проблемы:\n        // 1. 10 000 клиентов = 10 000 потоков = ~10 ГБ памяти\n        // 2. Создание потока ~100 мкс на каждый запрос\n        // 3. Нет контроля нагрузки — сервер может упасть\n        // 4. Нет переиспользования — каждый поток одноразовый\n    }\n\n    private void handleRequest(Socket client) {\n        // обработка HTTP запроса\n    }\n}' },
        { type: 'heading', value: 'Решение: Thread Pool паттерн' },
        { type: 'code', language: 'java', value: '// ПРАВИЛЬНО: пул потоков с ограниченным количеством\nclass SmartWebServer {\n    private final ExecutorService threadPool;\n\n    public SmartWebServer(int maxThreads) {\n        // Создаём пул один раз при старте сервера\n        this.threadPool = Executors.newFixedThreadPool(maxThreads);\n    }\n\n    public void start(int port) throws IOException {\n        ServerSocket server = new ServerSocket(port);\n        System.out.println("Сервер запущен с пулом потоков");\n\n        while (true) {\n            Socket client = server.accept();\n            // Отправляем задачу в пул — поток переиспользуется!\n            threadPool.submit(() -> handleRequest(client));\n        }\n        // Теперь:\n        // 1. Максимум maxThreads потоков — контроль памяти\n        // 2. Потоки переиспользуются — нет затрат на создание\n        // 3. Лишние запросы ждут в очереди — сервер не падает\n    }\n\n    private void handleRequest(Socket client) {\n        // обработка HTTP запроса\n    }\n}' },
        { type: 'heading', value: 'Как это работает внутри' },
        { type: 'text', value: 'Thread Pool состоит из двух компонентов: очереди задач (BlockingQueue) и набора рабочих потоков (Worker Threads). Когда задача отправлена через submit(), она попадает в очередь. Свободный рабочий поток берёт задачу из очереди, выполняет её, и возвращается за следующей.' },
        { type: 'code', language: 'java', value: '// Упрощённая схема работы ThreadPool:\n//\n//  submit(task1) ──┐\n//  submit(task2) ──┤     ┌─────────────┐    ┌──────────────┐\n//  submit(task3) ──┼───▶ │ TaskQueue    │───▶│ Worker-1     │ ◄── выполняет task\n//  submit(task4) ──┤     │ [task3]      │    │ Worker-2     │ ◄── выполняет task\n//  submit(task5) ──┘     │ [task4]      │    │ Worker-3     │ ◄── ждёт задачу\n//                        │ [task5]      │    └──────────────┘\n//                        └─────────────┘\n//\n// Worker потоки живут всё время работы пула\n// Задачи в очереди ждут свободного Worker\'а' },
        { type: 'heading', value: 'Реальные цифры: почему Thread Pool быстрее' },
        { type: 'code', language: 'java', value: '// Бенчмарк: Thread vs ThreadPool\npublic class ThreadPoolBenchmark {\n    public static void main(String[] args) throws Exception {\n        int taskCount = 10_000;\n        Runnable task = () -> {\n            // лёгкая задача\n            double sum = 0;\n            for (int i = 0; i < 100; i++) sum += Math.sqrt(i);\n        };\n\n        // Вариант 1: новый Thread каждый раз\n        long start1 = System.currentTimeMillis();\n        Thread[] threads = new Thread[taskCount];\n        for (int i = 0; i < taskCount; i++) {\n            threads[i] = new Thread(task);\n            threads[i].start();\n        }\n        for (Thread t : threads) t.join();\n        long time1 = System.currentTimeMillis() - start1;\n\n        // Вариант 2: пул потоков\n        ExecutorService pool = Executors.newFixedThreadPool(\n            Runtime.getRuntime().availableProcessors()\n        );\n        long start2 = System.currentTimeMillis();\n        for (int i = 0; i < taskCount; i++) {\n            pool.submit(task);\n        }\n        pool.shutdown();\n        pool.awaitTermination(60, TimeUnit.SECONDS);\n        long time2 = System.currentTimeMillis() - start2;\n\n        System.out.println("10 000 новых потоков: " + time1 + " мс");\n        System.out.println("Пул потоков:          " + time2 + " мс");\n        // Типичный результат:\n        // 10 000 новых потоков: 2500 мс\n        // Пул потоков:          150 мс (в ~17 раз быстрее!)\n    }\n}' },
        { type: 'warning', value: 'Tomcat, Jetty, Netty, Spring Boot — все реальные Java веб-серверы используют пулы потоков. Tomcat по умолчанию создаёт пул из 200 потоков (maxThreads=200). Это значит максимум 200 одновременно обрабатываемых запросов. Остальные ждут в очереди.' }
      ]
    },
    {
      id: 2,
      title: 'ExecutorService — интерфейс и основы',
      type: 'theory',
      content: [
        { type: 'text', value: 'ExecutorService — центральный интерфейс из пакета java.util.concurrent для управления пулом потоков. Он предоставляет методы для отправки задач, получения результатов и управления жизненным циклом пула. Создаётся через фабрику Executors.' },
        { type: 'heading', value: 'Иерархия интерфейсов' },
        { type: 'code', language: 'java', value: '// Иерархия:\n//\n// Executor                    — базовый: execute(Runnable)\n//   └── ExecutorService       — расширенный: submit(), shutdown(), invokeAll()\n//         └── ScheduledExecutorService — с расписанием: schedule(), scheduleAtFixedRate()\n//\n// Фабрика создания:\n// Executors.newFixedThreadPool(n)      → ExecutorService\n// Executors.newCachedThreadPool()      → ExecutorService\n// Executors.newSingleThreadExecutor()  → ExecutorService\n// Executors.newScheduledThreadPool(n)  → ScheduledExecutorService' },
        { type: 'heading', value: 'Создание через Executors (фабрика)' },
        { type: 'code', language: 'java', value: 'import java.util.concurrent.*;\n\n// Executors — фабричный класс со статическими методами\n// Возвращает готовый к использованию пул потоков\n\n// Фиксированный пул из 4 потоков\nExecutorService pool = Executors.newFixedThreadPool(4);\n\n// Отправляем задачи — они распределяются по 4 потокам\nfor (int i = 1; i <= 10; i++) {\n    final int taskId = i;\n    pool.submit(() -> {\n        String thread = Thread.currentThread().getName();\n        System.out.println("Задача " + taskId + " → " + thread);\n        try { Thread.sleep(500); } catch (InterruptedException e) {}\n    });\n}\n\npool.shutdown();\npool.awaitTermination(30, TimeUnit.SECONDS);\n\n// Вывод (порядок может отличаться):\n// Задача 1 → pool-1-thread-1\n// Задача 2 → pool-1-thread-2\n// Задача 3 → pool-1-thread-3\n// Задача 4 → pool-1-thread-4\n// Задача 5 → pool-1-thread-1  ← поток-1 освободился и взял новую задачу\n// ...' },
        { type: 'heading', value: 'submit() vs execute() — ключевое различие' },
        { type: 'code', language: 'java', value: 'ExecutorService pool = Executors.newFixedThreadPool(2);\n\n// ━━━ execute() ━━━\n// Из интерфейса Executor\n// Принимает только Runnable\n// Не возвращает результат (void)\n// Исключения теряются!\npool.execute(() -> {\n    System.out.println("execute: fire-and-forget");\n    // если тут бросить исключение — оно пропадёт\n});\n\n// ━━━ submit(Runnable) ━━━\n// Из интерфейса ExecutorService\n// Возвращает Future<?> (результат null после завершения)\n// Исключения сохраняются в Future\nFuture<?> future1 = pool.submit(() -> {\n    System.out.println("submit Runnable");\n});\nfuture1.get(); // null, но если было исключение — оно вылетит тут\n\n// ━━━ submit(Callable<T>) ━━━\n// Принимает Callable — может вернуть результат\n// Возвращает Future<T>\nFuture<String> future2 = pool.submit(() -> {\n    return "Результат работы";\n});\nString result = future2.get(); // "Результат работы"\n\npool.shutdown();' },
        { type: 'heading', value: 'Обработка исключений: submit() vs execute()' },
        { type: 'code', language: 'java', value: 'ExecutorService pool = Executors.newFixedThreadPool(2);\n\n// execute() — исключение просто напечатается в stderr и поток умрёт\npool.execute(() -> {\n    throw new RuntimeException("Ошибка в execute!");\n    // Стектрейс выведется, но отловить его нельзя\n});\n\n// submit() — исключение "захвачено" в Future\nFuture<?> f = pool.submit(() -> {\n    throw new RuntimeException("Ошибка в submit!");\n});\n\ntry {\n    f.get(); // Тут вылетит ExecutionException\n} catch (ExecutionException e) {\n    System.out.println("Перехватили: " + e.getCause().getMessage());\n    // "Перехватили: Ошибка в submit!"\n}\n\npool.shutdown();' },
        { type: 'tip', value: 'Всегда используй submit() вместо execute(). Он даёт Future, через который можно отследить завершение задачи и перехватить исключения. execute() — подходит только если результат не важен и потеря ошибок допустима.' },
        { type: 'heading', value: 'Реальный пример: сервис обработки заказов' },
        { type: 'code', language: 'java', value: 'class OrderProcessor {\n    private final ExecutorService pool;\n\n    public OrderProcessor(int workers) {\n        this.pool = Executors.newFixedThreadPool(workers);\n    }\n\n    public Future<OrderResult> processAsync(Order order) {\n        return pool.submit(() -> {\n            // Валидация\n            validateOrder(order);\n            // Списание со склада\n            reserveInventory(order);\n            // Оплата\n            processPayment(order);\n            // Отправка уведомления\n            sendNotification(order);\n            return new OrderResult(order.getId(), "COMPLETED");\n        });\n    }\n\n    // Вызывающий код:\n    // Future<OrderResult> result = processor.processAsync(order);\n    // OrderResult r = result.get(5, TimeUnit.SECONDS);\n}' },
        { type: 'warning', value: 'Не создавай ExecutorService на каждый вызов метода! Пул создаётся один раз (обычно при старте приложения) и переиспользуется. В Spring Boot пул потоков часто создаётся как Bean и инжектится через @Autowired.' }
      ]
    },
    {
      id: 3,
      title: 'Типы пулов потоков',
      type: 'theory',
      content: [
        { type: 'text', value: 'Java предоставляет четыре стандартных типа пулов потоков через фабрику Executors. Каждый тип решает свою задачу. Неправильный выбор типа пула — частая ошибка, которая приводит к проблемам производительности или падению сервера.' },
        { type: 'heading', value: '1. newFixedThreadPool(n) — фиксированный пул' },
        { type: 'code', language: 'java', value: '// Пул из N потоков. Если все заняты — задачи ждут в очереди.\n// Очередь неограниченная (LinkedBlockingQueue)\nExecutorService fixed = Executors.newFixedThreadPool(4);\n\n// Внутри создаётся:\n// new ThreadPoolExecutor(\n//     4,                          // corePoolSize\n//     4,                          // maximumPoolSize (тоже 4 — фиксированно)\n//     0L, TimeUnit.MILLISECONDS,  // потоки не уничтожаются\n//     new LinkedBlockingQueue<>() // безлимитная очередь\n// );\n\n// Оптимальное N:\n// CPU-задачи (вычисления): N = количество ядер\n// I/O-задачи (сеть, БД):  N = ядра * 2..4 (потоки часто ждут ответа)\nint cores = Runtime.getRuntime().availableProcessors();\nExecutorService cpuPool = Executors.newFixedThreadPool(cores);        // для вычислений\nExecutorService ioPool  = Executors.newFixedThreadPool(cores * 2);   // для I/O\n\nSystem.out.println("CPU ядер: " + cores);\nSystem.out.println("CPU пул: " + cores + " потоков");\nSystem.out.println("I/O пул: " + (cores * 2) + " потоков");' },
        { type: 'heading', value: '2. newCachedThreadPool() — динамический пул' },
        { type: 'code', language: 'java', value: '// Создаёт потоки по мере необходимости.\n// Неиспользуемые потоки удаляются через 60 секунд.\n// Нет ограничения на максимум потоков!\nExecutorService cached = Executors.newCachedThreadPool();\n\n// Внутри создаётся:\n// new ThreadPoolExecutor(\n//     0,                           // corePoolSize = 0 (все потоки могут быть удалены)\n//     Integer.MAX_VALUE,           // maximumPoolSize = ~2 млрд (!)\n//     60L, TimeUnit.SECONDS,       // простаивающие потоки живут 60 сек\n//     new SynchronousQueue<>()     // задача сразу передаётся потоку\n// );\n\n// Хорошо: для множества коротких задач с переменной нагрузкой\nfor (int i = 0; i < 100; i++) {\n    cached.submit(() -> {\n        System.out.println(Thread.currentThread().getName());\n        Thread.sleep(50); // быстрая задача\n        return null;\n    });\n}\ncached.shutdown();\ncached.awaitTermination(10, TimeUnit.SECONDS);' },
        { type: 'warning', value: 'ОПАСНОСТЬ newCachedThreadPool(): если задачи приходят быстрее, чем выполняются — пул создаст тысячи потоков и съест всю память. Никогда не используй его для долгих задач или если поток задач непредсказуем (например, HTTP-запросы от клиентов).' },
        { type: 'heading', value: '3. newSingleThreadExecutor() — один поток' },
        { type: 'code', language: 'java', value: '// Один рабочий поток. Задачи выполняются строго по порядку (FIFO).\n// Если поток умирает от исключения — создаётся новый.\nExecutorService single = Executors.newSingleThreadExecutor();\n\n// Гарантирует последовательное выполнение\nsingle.submit(() -> System.out.println("1: " + Thread.currentThread().getName()));\nsingle.submit(() -> System.out.println("2: " + Thread.currentThread().getName()));\nsingle.submit(() -> System.out.println("3: " + Thread.currentThread().getName()));\n// Всегда:\n// 1: pool-1-thread-1\n// 2: pool-1-thread-1\n// 3: pool-1-thread-1\n\nsingle.shutdown();\n\n// Реальные use-cases:\n// - Запись логов в файл (порядок важен!)\n// - Обновление UI (JavaFX, Swing — один поток для GUI)\n// - Последовательная обработка событий\n// - Запись аудита — операции должны идти строго по порядку' },
        { type: 'heading', value: '4. newScheduledThreadPool(n) — с расписанием' },
        { type: 'code', language: 'java', value: 'ScheduledExecutorService scheduler = Executors.newScheduledThreadPool(3);\n\n// Одноразовый запуск через 5 секунд\nscheduler.schedule(\n    () -> System.out.println("Сработало через 5 секунд!"),\n    5, TimeUnit.SECONDS\n);\n\n// Периодическое выполнение каждые 10 секунд\nscheduler.scheduleAtFixedRate(\n    () -> System.out.println("Проверка здоровья сервиса: " + LocalTime.now()),\n    0,   // начать сразу\n    10,  // повторять каждые 10\n    TimeUnit.SECONDS\n);\n\n// Подробнее в уроке 9' },
        { type: 'heading', value: 'Сравнительная таблица' },
        { type: 'code', language: 'java', value: '// ┌────────────────────────┬──────────┬──────────┬─────────────────────────┐\n// │ Тип                    │ Потоков  │ Очередь  │ Когда использовать      │\n// ├────────────────────────┼──────────┼──────────┼─────────────────────────┤\n// │ newFixedThreadPool(n)  │ ровно n  │ безлимит │ Предсказуемая нагрузка, │\n// │                        │          │          │ веб-серверы, API        │\n// ├────────────────────────┼──────────┼──────────┼─────────────────────────┤\n// │ newCachedThreadPool()  │ 0..∞     │ нет      │ Много коротких задач,   │\n// │                        │          │          │ непредсказуемые всплески│\n// ├────────────────────────┼──────────┼──────────┼─────────────────────────┤\n// │ newSingleThreadExec()  │ ровно 1  │ безлимит │ Последовательные задачи,│\n// │                        │          │          │ логирование, аудит      │\n// ├────────────────────────┼──────────┼──────────┼─────────────────────────┤\n// │ newScheduledThreadPool │ n core   │ delay-q  │ Периодические задачи,   │\n// │                        │          │          │ health checks, cron     │\n// └────────────────────────┴──────────┴──────────┴─────────────────────────┘' },
        { type: 'heading', value: 'Реальный пример: микросервис с несколькими пулами' },
        { type: 'code', language: 'java', value: 'class PaymentService {\n    // Разные пулы для разных задач — изоляция нагрузки\n    private final ExecutorService httpPool;       // для HTTP запросов\n    private final ExecutorService dbPool;         // для запросов к БД\n    private final ExecutorService notifyPool;     // для уведомлений\n    private final ScheduledExecutorService cron;  // для периодических задач\n\n    public PaymentService() {\n        int cores = Runtime.getRuntime().availableProcessors();\n        this.httpPool   = Executors.newFixedThreadPool(cores * 2);  // I/O — побольше\n        this.dbPool     = Executors.newFixedThreadPool(10);         // ограничен пулом коннектов БД\n        this.notifyPool = Executors.newCachedThreadPool();          // всплески допустимы\n        this.cron       = Executors.newScheduledThreadPool(2);      // периодика\n\n        // Каждые 30 секунд — проверка зависших платежей\n        cron.scheduleAtFixedRate(\n            () -> checkStalledPayments(),\n            0, 30, TimeUnit.SECONDS\n        );\n    }\n\n    // Каждый тип задачи идёт в свой пул\n    public Future<PaymentResult> processPayment(Payment p) {\n        return httpPool.submit(() -> callPaymentGateway(p));\n    }\n\n    public Future<Void> saveToDb(Payment p) {\n        return dbPool.submit(() -> { repository.save(p); return null; });\n    }\n}' },
        { type: 'note', value: 'В production-коде часто используют ThreadPoolExecutor напрямую (а не через Executors) чтобы задать свою очередь, политику отклонения задач и другие параметры. Executors — удобный шорткат для типичных сценариев.' }
      ]
    },
    {
      id: 4,
      title: 'Future<T> — результат асинхронной операции',
      type: 'theory',
      content: [
        { type: 'text', value: 'Future<T> — это "обещание" (promise) будущего результата. Когда ты отправляешь задачу в пул через submit(), вызов возвращается мгновенно с объектом Future, через который позже можно получить результат, узнать статус или отменить задачу.' },
        { type: 'tip', value: 'Future — как квитанция из ремонтной мастерской. Ты сдаёшь ноутбук (отправляешь задачу) и получаешь квитанцию (Future). Можешь уйти по делам. Позже звонишь: "Готово?" (isDone()). Если готово — приходишь забирать (get()). Если устал ждать — "Отмените ремонт" (cancel()).' },
        { type: 'heading', value: 'Основные методы Future<T>' },
        { type: 'code', language: 'java', value: 'ExecutorService pool = Executors.newFixedThreadPool(2);\n\nFuture<String> future = pool.submit(() -> {\n    Thread.sleep(3000); // симулируем долгую работу\n    return "Данные загружены";\n});\n\n// ━━━ get() — блокирующее ожидание результата ━━━\nString result = future.get(); // ждёт пока задача не завершится\nSystem.out.println(result);   // "Данные загружены"\n\n// ━━━ get(timeout) — ожидание с таймаутом ━━━\ntry {\n    String data = future.get(2, TimeUnit.SECONDS);\n} catch (TimeoutException e) {\n    System.out.println("Не дождались за 2 секунды!");\n}\n\n// ━━━ isDone() — проверка без блокировки ━━━\nif (future.isDone()) {\n    System.out.println("Задача завершена");\n} else {\n    System.out.println("Ещё выполняется...");\n}\n\n// ━━━ cancel(mayInterruptIfRunning) — отмена задачи ━━━\nboolean cancelled = future.cancel(true); // true = прервать если выполняется\nSystem.out.println("Отменена: " + cancelled);\n\n// ━━━ isCancelled() — проверка отмены ━━━\nSystem.out.println("Была отменена: " + future.isCancelled());\n\npool.shutdown();' },
        { type: 'heading', value: 'Параллельная загрузка данных с Future' },
        { type: 'code', language: 'java', value: '// Реальный сценарий: страница профиля пользователя\n// Нужно загрузить данные из 3 разных сервисов\nclass ProfilePageLoader {\n    private final ExecutorService pool = Executors.newFixedThreadPool(3);\n\n    public ProfilePage loadProfile(long userId) throws Exception {\n        // Запускаем 3 запроса ПАРАЛЛЕЛЬНО\n        Future<User> userFuture = pool.submit(\n            () -> userService.getUser(userId)          // 200 мс\n        );\n        Future<List<Order>> ordersFuture = pool.submit(\n            () -> orderService.getOrders(userId)       // 300 мс\n        );\n        Future<List<Review>> reviewsFuture = pool.submit(\n            () -> reviewService.getReviews(userId)     // 250 мс\n        );\n\n        // Ждём все три результата\n        // Общее время ≈ max(200, 300, 250) = ~300 мс\n        // Без параллелизма было бы: 200 + 300 + 250 = 750 мс\n        User user = userFuture.get(5, TimeUnit.SECONDS);\n        List<Order> orders = ordersFuture.get(5, TimeUnit.SECONDS);\n        List<Review> reviews = reviewsFuture.get(5, TimeUnit.SECONDS);\n\n        return new ProfilePage(user, orders, reviews);\n    }\n}' },
        { type: 'heading', value: 'Обработка ошибок в Future' },
        { type: 'code', language: 'java', value: 'ExecutorService pool = Executors.newFixedThreadPool(2);\n\nFuture<Integer> future = pool.submit(() -> {\n    if (true) throw new IllegalStateException("БД недоступна!");\n    return 42;\n});\n\ntry {\n    Integer result = future.get();\n} catch (ExecutionException e) {\n    // Оригинальное исключение "обёрнуто" в ExecutionException\n    Throwable cause = e.getCause();\n    System.out.println("Тип ошибки: " + cause.getClass().getSimpleName());\n    System.out.println("Сообщение: " + cause.getMessage());\n    // Тип ошибки: IllegalStateException\n    // Сообщение: БД недоступна!\n} catch (InterruptedException e) {\n    // Текущий поток был прерван пока ждал результат\n    Thread.currentThread().interrupt();\n    System.out.println("Ожидание прервано!");\n}\n\npool.shutdown();' },
        { type: 'heading', value: 'Паттерн: polling (опрос) вместо блокировки' },
        { type: 'code', language: 'java', value: '// Иногда не хочется блокировать поток через get()\n// Можно периодически проверять isDone()\nFuture<byte[]> downloadFuture = pool.submit(() -> downloadLargeFile(url));\n\n// Показываем прогресс\nwhile (!downloadFuture.isDone()) {\n    System.out.print(".");\n    Thread.sleep(500); // проверяем каждые 0.5 сек\n}\n\nif (!downloadFuture.isCancelled()) {\n    byte[] data = downloadFuture.get(); // мгновенно — уже готово\n    System.out.println("\\nСкачано: " + data.length + " байт");\n}' },
        { type: 'warning', value: 'future.get() без таймаута может заблокировать поток навсегда, если задача зависнет. Всегда используй get(timeout, TimeUnit) в production-коде. Если таймаут истёк — решай: отменять задачу (cancel) или ждать дальше.' }
      ]
    },
    {
      id: 5,
      title: 'Callable<T> — задача с результатом',
      type: 'theory',
      content: [
        { type: 'text', value: 'Callable<T> — функциональный интерфейс, аналог Runnable, но с двумя важными отличиями: он может возвращать результат (тип T) и может бросать проверяемые исключения (checked exceptions). Callable используется вместе с ExecutorService.submit() для получения Future<T>.' },
        { type: 'heading', value: 'Callable vs Runnable: сравнение' },
        { type: 'code', language: 'java', value: '// ━━━ Runnable ━━━\n// void run() — ничего не возвращает, не бросает checked exceptions\nRunnable runnable = () -> {\n    System.out.println("Работаю...");\n    // return "результат";  ← НЕЛЬЗЯ — void метод\n    // throw new IOException();  ← НЕЛЬЗЯ — checked exception\n};\n\n// ━━━ Callable<T> ━━━\n// T call() throws Exception — возвращает результат, бросает что угодно\nCallable<String> callable = () -> {\n    // Может выполнять I/O операции с checked exceptions\n    byte[] data = Files.readAllBytes(Path.of("config.json"));\n    return new String(data); // МОЖНО — возвращает String\n};\n\n// Как использовать:\nExecutorService pool = Executors.newFixedThreadPool(2);\n\npool.execute(runnable);         // execute — только Runnable, результата нет\nFuture<?> f1 = pool.submit(runnable);      // submit — Runnable в Future, get() → null\nFuture<String> f2 = pool.submit(callable); // submit — Callable, get() → "String"\n\nSystem.out.println(f2.get()); // содержимое config.json\npool.shutdown();' },
        { type: 'heading', value: 'Реальный пример: параллельный расчёт статистики' },
        { type: 'code', language: 'java', value: 'class ReportGenerator {\n    private final ExecutorService pool = Executors.newFixedThreadPool(4);\n\n    public Report generate(LocalDate date) throws Exception {\n        // Каждый расчёт — отдельный Callable\n        Callable<BigDecimal> revenueTask = () -> {\n            // запрос к БД: SELECT SUM(amount) FROM orders WHERE date = ?\n            return orderRepository.calculateRevenue(date);\n        };\n\n        Callable<Integer> newUsersTask = () -> {\n            return userRepository.countNewUsers(date);\n        };\n\n        Callable<Map<String, Integer>> topProductsTask = () -> {\n            return productRepository.getTopProducts(date, 10);\n        };\n\n        Callable<Double> conversionTask = () -> {\n            int visits = analyticsService.getVisits(date);\n            int purchases = orderRepository.countOrders(date);\n            return (double) purchases / visits * 100;\n        };\n\n        // Запускаем все 4 параллельно\n        Future<BigDecimal> revenue = pool.submit(revenueTask);\n        Future<Integer> newUsers = pool.submit(newUsersTask);\n        Future<Map<String, Integer>> topProducts = pool.submit(topProductsTask);\n        Future<Double> conversion = pool.submit(conversionTask);\n\n        // Собираем результаты\n        return new Report(\n            revenue.get(10, TimeUnit.SECONDS),\n            newUsers.get(10, TimeUnit.SECONDS),\n            topProducts.get(10, TimeUnit.SECONDS),\n            conversion.get(10, TimeUnit.SECONDS)\n        );\n    }\n}' },
        { type: 'heading', value: 'invokeAll() — запустить все и дождаться' },
        { type: 'code', language: 'java', value: 'ExecutorService pool = Executors.newFixedThreadPool(3);\n\n// Список Callable задач\nList<Callable<String>> tasks = List.of(\n    () -> { Thread.sleep(1000); return "Сервер A: 200 OK"; },\n    () -> { Thread.sleep(500);  return "Сервер B: 200 OK"; },\n    () -> { Thread.sleep(800);  return "Сервер C: 200 OK"; }\n);\n\n// invokeAll блокирует пока ВСЕ задачи не завершатся\nList<Future<String>> futures = pool.invokeAll(tasks);\n\n// Все Future уже isDone() == true\nfor (Future<String> f : futures) {\n    System.out.println(f.get()); // мгновенно — уже готовы\n}\n// Вывод:\n// Сервер A: 200 OK\n// Сервер B: 200 OK\n// Сервер C: 200 OK\n\n// invokeAll с таймаутом — отменяет незавершённые задачи\nList<Future<String>> results = pool.invokeAll(tasks, 700, TimeUnit.MILLISECONDS);\n// Задача A (1000 мс) не успеет — будет отменена\nfor (Future<String> f : results) {\n    if (f.isCancelled()) {\n        System.out.println("Задача отменена по таймауту");\n    } else {\n        System.out.println(f.get());\n    }\n}\n\npool.shutdown();' },
        { type: 'heading', value: 'invokeAny() — вернуть первый результат' },
        { type: 'code', language: 'java', value: '// invokeAny возвращает результат САМОЙ БЫСТРОЙ задачи\n// Остальные задачи отменяются автоматически\n\nExecutorService pool = Executors.newFixedThreadPool(3);\n\nList<Callable<String>> searchTasks = List.of(\n    () -> {\n        Thread.sleep(3000);\n        return "Результат из кэша Redis (3 сек)";\n    },\n    () -> {\n        Thread.sleep(500);\n        return "Результат из кэша памяти (0.5 сек)"; // самый быстрый\n    },\n    () -> {\n        Thread.sleep(2000);\n        return "Результат из БД (2 сек)";\n    }\n);\n\n// Возвращает первый готовый результат\nString fastest = pool.invokeAny(searchTasks);\nSystem.out.println(fastest);\n// "Результат из кэша памяти (0.5 сек)"\n\npool.shutdown();\n\n// Реальные use-cases:\n// - Запрос к нескольким репликам БД — берём ответ от самой быстрой\n// - Поиск в нескольких источниках — первый нашёл, остальные не нужны\n// - Fallback стратегия — пробуем несколько вариантов параллельно' },
        { type: 'tip', value: 'invokeAll — когда нужны результаты ВСЕХ задач (генерация отчёта, batch-обработка). invokeAny — когда нужен ЛЮБОЙ один результат (поиск в нескольких источниках, failover). invokeAny быстрее — не ждёт остальных после первого успеха.' }
      ]
    },
    {
      id: 6,
      title: 'CompletableFuture: основы',
      type: 'theory',
      content: [
        { type: 'text', value: 'CompletableFuture (Java 8+) — это эволюция Future. Обычный Future умеет только get() — заблокировать поток и ждать. CompletableFuture позволяет строить цепочки асинхронных операций без блокировки: "когда результат будет готов — сделай с ним то-то, потом то-то".' },
        { type: 'tip', value: 'Future — как заказ в кафе: ты стоишь у стойки и ждёшь (get()). CompletableFuture — как доставка: заказал, указал адрес (thenApply), указал что делать когда привезут (thenAccept), и пошёл по делам. Еду привезут сами — ждать не нужно.' },
        { type: 'heading', value: 'Создание CompletableFuture' },
        { type: 'code', language: 'java', value: 'import java.util.concurrent.CompletableFuture;\n\n// ━━━ supplyAsync — запускает задачу в ForkJoinPool, возвращает результат ━━━\nCompletableFuture<String> future = CompletableFuture.supplyAsync(() -> {\n    // выполняется в отдельном потоке (ForkJoinPool.commonPool())\n    System.out.println("Поток: " + Thread.currentThread().getName());\n    return "Привет из async!";\n});\n\n// ━━━ runAsync — запускает задачу без результата (Runnable) ━━━\nCompletableFuture<Void> voidFuture = CompletableFuture.runAsync(() -> {\n    System.out.println("Фоновая задача без результата");\n});\n\n// ━━━ supplyAsync с кастомным пулом ━━━\nExecutorService myPool = Executors.newFixedThreadPool(4);\nCompletableFuture<Integer> customFuture = CompletableFuture.supplyAsync(\n    () -> heavyComputation(),\n    myPool  // задача выполнится в myPool, а не в ForkJoinPool\n);\n\n// ━━━ completedFuture — уже готовый результат ━━━\nCompletableFuture<String> ready = CompletableFuture.completedFuture("Уже готово!");\nSystem.out.println(ready.get()); // мгновенно: "Уже готово!"' },
        { type: 'heading', value: 'thenApply — преобразование результата' },
        { type: 'code', language: 'java', value: '// thenApply — как map() у Stream: преобразует результат\nCompletableFuture<String> nameFuture = CompletableFuture\n    .supplyAsync(() -> getUserFromDb(userId))    // User\n    .thenApply(user -> user.getFirstName())       // String (имя)\n    .thenApply(name -> "Привет, " + name + "!")   // String (приветствие)\n    .thenApply(String::toUpperCase);              // String (ПРИВЕТ, ИВАН!)\n\nString greeting = nameFuture.get();\nSystem.out.println(greeting); // "ПРИВЕТ, ИВАН!"\n\n// Цепочка thenApply — это пайплайн обработки данных:\n// [supplyAsync] → User → [thenApply] → "Иван" → [thenApply] → "Привет, Иван!" → [thenApply] → "ПРИВЕТ, ИВАН!"' },
        { type: 'heading', value: 'thenAccept — потребление результата (без возврата)' },
        { type: 'code', language: 'java', value: '// thenAccept — получает результат, но ничего не возвращает (Consumer)\nCompletableFuture<Void> pipeline = CompletableFuture\n    .supplyAsync(() -> fetchWeatherData("Москва"))\n    .thenAccept(weather -> {\n        System.out.println("Погода в Москве: " + weather.getTemp() + "°C");\n        System.out.println("Ветер: " + weather.getWind() + " м/с");\n    });\n\npipeline.get(); // ждём завершения\n// Погода в Москве: -5°C\n// Ветер: 3 м/с' },
        { type: 'heading', value: 'thenRun — действие после завершения (без доступа к результату)' },
        { type: 'code', language: 'java', value: '// thenRun — выполняет Runnable после завершения, не получает результат\nCompletableFuture\n    .supplyAsync(() -> sendEmail("user@example.com", "Отчёт"))\n    .thenRun(() -> {\n        System.out.println("Email отправлен! Обновляем статус...");\n        updateStatus("EMAIL_SENT");\n    });\n\n// thenApply vs thenAccept vs thenRun:\n// thenApply(T -> R)    — получает T, возвращает R     (Function)\n// thenAccept(T -> void) — получает T, ничего не возвращает (Consumer)\n// thenRun(() -> void)  — ничего не получает и не возвращает (Runnable)' },
        { type: 'heading', value: 'Реальный пример: обработка заказа' },
        { type: 'code', language: 'java', value: 'class OrderService {\n    private final ExecutorService pool = Executors.newFixedThreadPool(8);\n\n    public CompletableFuture<OrderResult> processOrder(Order order) {\n        return CompletableFuture\n            .supplyAsync(() -> {\n                // Шаг 1: Валидация заказа\n                validateOrder(order);\n                return order;\n            }, pool)\n            .thenApply(validOrder -> {\n                // Шаг 2: Расчёт стоимости с учётом скидок\n                BigDecimal total = calculateTotal(validOrder);\n                validOrder.setTotal(total);\n                return validOrder;\n            })\n            .thenApply(pricedOrder -> {\n                // Шаг 3: Резервирование товаров на складе\n                reserveItems(pricedOrder);\n                return pricedOrder;\n            })\n            .thenApply(reservedOrder -> {\n                // Шаг 4: Создание результата\n                return new OrderResult(reservedOrder.getId(),\n                    "CREATED", reservedOrder.getTotal());\n            });\n    }\n}\n\n// Использование:\nOrderService service = new OrderService();\nCompletableFuture<OrderResult> result = service.processOrder(newOrder);\n\n// Можем добавить действие по завершении\nresult.thenAccept(r -> {\n    System.out.println("Заказ " + r.getId() + " обработан!");\n    System.out.println("Сумма: " + r.getTotal());\n});' },
        { type: 'heading', value: 'Async-версии методов' },
        { type: 'code', language: 'java', value: '// Каждый метод имеет Async-версию, которая выполняется в другом потоке\nCompletableFuture<String> future = CompletableFuture\n    .supplyAsync(() -> "данные")\n    .thenApply(s -> s.toUpperCase())      // в том же потоке, что и предыдущий шаг\n    .thenApplyAsync(s -> s + "!!!")       // в новом потоке из ForkJoinPool\n    .thenApplyAsync(s -> s.trim(), pool); // в новом потоке из myPool\n\n// Правило:\n// thenApply     — выполняется в потоке предыдущего шага (быстрее)\n// thenApplyAsync — выполняется в отдельном потоке (когда шаг тяжёлый)' },
        { type: 'note', value: 'CompletableFuture по умолчанию использует ForkJoinPool.commonPool() — общий пул JVM. Для production-кода лучше указывать свой ExecutorService через второй аргумент supplyAsync/thenApplyAsync, чтобы контролировать количество потоков.' }
      ]
    },
    {
      id: 7,
      title: 'CompletableFuture: комбинирование',
      type: 'theory',
      content: [
        { type: 'text', value: 'Настоящая сила CompletableFuture — в комбинировании нескольких асинхронных операций. Можно объединить результаты двух задач, дождаться всех или любой из нескольких, и строить сложные пайплайны из параллельных и последовательных шагов.' },
        { type: 'heading', value: 'thenCombine — объединение двух результатов' },
        { type: 'code', language: 'java', value: '// thenCombine: запускает две задачи параллельно и объединяет результаты\nCompletableFuture<Double> priceFuture = CompletableFuture\n    .supplyAsync(() -> getProductPrice("iPhone 15"));     // 999.0\n\nCompletableFuture<Double> discountFuture = CompletableFuture\n    .supplyAsync(() -> getUserDiscount(currentUser));      // 0.15 (15%)\n\n// Когда ОБА готовы — вычисляем финальную цену\nCompletableFuture<Double> finalPrice = priceFuture.thenCombine(\n    discountFuture,\n    (price, discount) -> price * (1 - discount)  // 999.0 * 0.85 = 849.15\n);\n\nSystem.out.println("Цена: " + finalPrice.get() + "$"); // Цена: 849.15$\n\n// Временная диаграмма:\n// priceFuture:    ████████████████████ (200 мс)\n// discountFuture: ████████████ (120 мс)\n// thenCombine:                        █ (мгновенно, когда оба готовы)\n// Общее время: ~200 мс (не 200 + 120 = 320)' },
        { type: 'heading', value: 'thenCompose — цепочка зависимых async-операций' },
        { type: 'code', language: 'java', value: '// thenCompose — когда результат одной операции нужен для запуска следующей\n// (аналог flatMap у Stream)\n\n// Шаг 1: получаем userId по email (async)\nCompletableFuture<Long> userIdFuture = CompletableFuture\n    .supplyAsync(() -> findUserIdByEmail("ivan@mail.ru"));\n\n// Шаг 2: по userId загружаем заказы (async, зависит от шага 1)\n// НЕПРАВИЛЬНО с thenApply — получим Future<Future<List<Order>>>\n// CompletableFuture<CompletableFuture<List<Order>>> nested =\n//     userIdFuture.thenApply(id -> loadOrdersAsync(id)); // вложенный Future!\n\n// ПРАВИЛЬНО с thenCompose — "разворачивает" вложенный Future\nCompletableFuture<List<Order>> ordersFuture = userIdFuture\n    .thenCompose(userId -> loadOrdersAsync(userId));\n    // loadOrdersAsync возвращает CompletableFuture<List<Order>>\n\n// Можно продолжить цепочку\nCompletableFuture<BigDecimal> totalFuture = userIdFuture\n    .thenCompose(id -> loadOrdersAsync(id))                  // List<Order>\n    .thenApply(orders -> orders.stream()\n        .map(Order::getTotal)\n        .reduce(BigDecimal.ZERO, BigDecimal::add));           // BigDecimal\n\nSystem.out.println("Общая сумма заказов: " + totalFuture.get());' },
        { type: 'heading', value: 'allOf — дождаться завершения всех задач' },
        { type: 'code', language: 'java', value: '// allOf — создаёт Future который завершается когда ВСЕ задачи готовы\nCompletableFuture<String> task1 = CompletableFuture.supplyAsync(() -> {\n    sleep(1000); return "Данные из API 1";\n});\nCompletableFuture<String> task2 = CompletableFuture.supplyAsync(() -> {\n    sleep(800); return "Данные из API 2";\n});\nCompletableFuture<String> task3 = CompletableFuture.supplyAsync(() -> {\n    sleep(1200); return "Данные из API 3";\n});\n\n// allOf возвращает CompletableFuture<Void> — нет общего результата\nCompletableFuture<Void> allDone = CompletableFuture.allOf(task1, task2, task3);\n\n// Ждём все три\nallDone.get();\n\n// Теперь все Future гарантированно isDone — get() мгновенный\nSystem.out.println(task1.get());\nSystem.out.println(task2.get());\nSystem.out.println(task3.get());\n\n// Паттерн: собрать все результаты в список\nCompletableFuture<List<String>> allResults = CompletableFuture\n    .allOf(task1, task2, task3)\n    .thenApply(v -> List.of(task1.join(), task2.join(), task3.join()));\n\nList<String> results = allResults.get();\nresults.forEach(System.out::println);' },
        { type: 'heading', value: 'anyOf — первый завершившийся' },
        { type: 'code', language: 'java', value: '// anyOf — завершается когда ЛЮБАЯ из задач готова\nCompletableFuture<String> cacheRedis = CompletableFuture.supplyAsync(() -> {\n    sleep(100); return "Из Redis кэша";\n});\nCompletableFuture<String> cacheLocal = CompletableFuture.supplyAsync(() -> {\n    sleep(5); return "Из локального кэша"; // самый быстрый\n});\nCompletableFuture<String> database = CompletableFuture.supplyAsync(() -> {\n    sleep(500); return "Из базы данных";\n});\n\n// Возвращает CompletableFuture<Object> — тип Object потому что\n// задачи могут возвращать разные типы\nCompletableFuture<Object> fastest = CompletableFuture\n    .anyOf(cacheRedis, cacheLocal, database);\n\nSystem.out.println("Первый ответ: " + fastest.get());\n// "Первый ответ: Из локального кэша"' },
        { type: 'heading', value: 'Реальный пример: Dashboard с параллельными виджетами' },
        { type: 'code', language: 'java', value: 'class DashboardService {\n    public CompletableFuture<Dashboard> loadDashboard(long userId) {\n        // 5 виджетов загружаются параллельно\n        CompletableFuture<Stats> stats = CompletableFuture\n            .supplyAsync(() -> statsService.getStats(userId));\n\n        CompletableFuture<List<Notification>> notifications = CompletableFuture\n            .supplyAsync(() -> notificationService.getRecent(userId, 10));\n\n        CompletableFuture<ChartData> chart = CompletableFuture\n            .supplyAsync(() -> analyticsService.getChart(userId, "7d"));\n\n        CompletableFuture<List<Task>> tasks = CompletableFuture\n            .supplyAsync(() -> taskService.getActive(userId));\n\n        CompletableFuture<Weather> weather = CompletableFuture\n            .supplyAsync(() -> weatherService.getCurrent(userId));\n\n        // Ждём все 5 и собираем Dashboard\n        return CompletableFuture\n            .allOf(stats, notifications, chart, tasks, weather)\n            .thenApply(v -> new Dashboard(\n                stats.join(),\n                notifications.join(),\n                chart.join(),\n                tasks.join(),\n                weather.join()\n            ));\n    }\n    // Без параллелизма: 200+150+300+100+80 = 830 мс\n    // С параллелизмом: max(200,150,300,100,80) = ~300 мс\n}' },
        { type: 'tip', value: 'thenCombine — объединить результаты двух независимых задач. thenCompose — цепочка зависимых задач (результат первой нужен для запуска второй). allOf — дождаться всех. anyOf — дождаться первого. Это четыре ключевых инструмента комбинирования.' }
      ]
    },
    {
      id: 8,
      title: 'CompletableFuture: обработка ошибок',
      type: 'theory',
      content: [
        { type: 'text', value: 'В реальных приложениях асинхронные операции часто завершаются ошибкой: сервис недоступен, таймаут сети, невалидные данные. CompletableFuture предоставляет три метода для обработки ошибок в цепочках: exceptionally(), handle() и whenComplete().' },
        { type: 'heading', value: 'exceptionally — перехват ошибки и fallback-значение' },
        { type: 'code', language: 'java', value: '// exceptionally — вызывается ТОЛЬКО если произошла ошибка\n// Возвращает запасное (fallback) значение\nCompletableFuture<String> result = CompletableFuture\n    .supplyAsync(() -> {\n        // Симулируем ошибку сети\n        if (Math.random() > 0.5) {\n            throw new RuntimeException("Сервис недоступен!");\n        }\n        return "Данные из API";\n    })\n    .exceptionally(ex -> {\n        // ex — исключение, которое произошло\n        System.err.println("Ошибка: " + ex.getMessage());\n        return "Данные из кэша (fallback)"; // запасное значение\n    });\n\n// Результат всегда String — либо из API, либо из кэша\nSystem.out.println(result.get());\n\n// Реальный пример: цена с fallback\nCompletableFuture<Double> price = CompletableFuture\n    .supplyAsync(() -> fetchPriceFromExchange("AAPL"))   // может упасть\n    .exceptionally(ex -> {\n        log.warn("Exchange unavailable, using cached price", ex);\n        return getCachedPrice("AAPL\"); // fallback на кэш\n    });' },
        { type: 'heading', value: 'handle — обработка и результата, и ошибки' },
        { type: 'code', language: 'java', value: '// handle — вызывается ВСЕГДА: и при успехе, и при ошибке\n// Получает два параметра: (result, exception)\n// Один из них всегда null\nCompletableFuture<String> result = CompletableFuture\n    .supplyAsync(() -> {\n        return callExternalService();\n    })\n    .handle((data, ex) -> {\n        if (ex != null) {\n            // Произошла ошибка\n            System.err.println("Ошибка: " + ex.getMessage());\n            return "DEFAULT_VALUE";\n        }\n        // Успешный результат — трансформируем\n        return "Обработано: " + data;\n    });\n\n// handle удобен когда нужно преобразовать результат\n// И при этом обработать ошибку в одном месте\n\n// Реальный пример: HTTP ответ\nCompletableFuture<HttpResponse> response = CompletableFuture\n    .supplyAsync(() -> httpClient.send(request))\n    .handle((resp, ex) -> {\n        if (ex != null) {\n            return HttpResponse.error(500, "Service unavailable");\n        }\n        if (resp.getStatusCode() != 200) {\n            return HttpResponse.error(resp.getStatusCode(), resp.getBody());\n        }\n        return resp;\n    });' },
        { type: 'heading', value: 'whenComplete — побочный эффект (логирование, метрики)' },
        { type: 'code', language: 'java', value: '// whenComplete — вызывается ВСЕГДА, НО не меняет результат\n// Идеален для логирования и метрик\nCompletableFuture<String> result = CompletableFuture\n    .supplyAsync(() -> processPayment(order))\n    .whenComplete((data, ex) -> {\n        // Это побочный эффект — результат НЕ МЕНЯЕТСЯ\n        if (ex != null) {\n            log.error("Платёж не прошёл: orderId={}", order.getId(), ex);\n            metrics.increment("payment.failed");\n        } else {\n            log.info("Платёж успешен: orderId={}, result={}", order.getId(), data);\n            metrics.increment("payment.success");\n        }\n    });\n// result содержит оригинальный результат или оригинальное исключение\n// whenComplete НЕ заменяет результат, в отличие от handle\n\n// Сравнение:\n// exceptionally:  только ошибки → возвращает fallback значение\n// handle:         и успех, и ошибка → возвращает новое значение\n// whenComplete:   и успех, и ошибка → НЕ меняет результат (побочный эффект)' },
        { type: 'heading', value: 'Цепочка обработки ошибок' },
        { type: 'code', language: 'java', value: '// Ошибки пробрасываются по цепочке, как в try-catch\nCompletableFuture<String> pipeline = CompletableFuture\n    .supplyAsync(() -> {\n        // Шаг 1: может бросить ошибку\n        return fetchData("https://api.example.com/data");\n    })\n    .thenApply(data -> {\n        // Шаг 2: если шаг 1 упал — этот шаг ПРОПУСКАЕТСЯ\n        return parseJson(data);\n    })\n    .thenApply(json -> {\n        // Шаг 3: если шаг 1 или 2 упал — ПРОПУСКАЕТСЯ\n        return transform(json);\n    })\n    .exceptionally(ex -> {\n        // Перехватывает ошибку из ЛЮБОГО предыдущего шага\n        log.error("Pipeline failed", ex);\n        return "EMPTY_RESULT";\n    });\n\n// Аналог в синхронном коде:\n// try {\n//     data = fetchData(url);      // шаг 1\n//     json = parseJson(data);     // шаг 2\n//     result = transform(json);   // шаг 3\n// } catch (Exception ex) {\n//     result = "EMPTY_RESULT";    // exceptionally\n// }' },
        { type: 'heading', value: 'Реальный пример: устойчивый сервис с retry' },
        { type: 'code', language: 'java', value: 'class ResilientService {\n    private final ExecutorService pool = Executors.newFixedThreadPool(4);\n\n    // Метод с повторной попыткой при ошибке\n    public CompletableFuture<String> fetchWithRetry(String url, int maxRetries) {\n        CompletableFuture<String> future = CompletableFuture\n            .supplyAsync(() -> httpGet(url), pool);\n\n        // Добавляем retry логику\n        for (int i = 0; i < maxRetries; i++) {\n            future = future.exceptionally(ex -> {\n                System.out.println("Попытка повторная: " + ex.getMessage());\n                try { Thread.sleep(1000); } catch (InterruptedException e) {}\n                return httpGet(url); // повторяем запрос\n            });\n        }\n\n        return future.exceptionally(ex -> {\n            System.err.println("Все попытки исчерпаны: " + url);\n            return null;\n        });\n    }\n}\n\n// Использование:\nResilientService service = new ResilientService();\nCompletableFuture<String> data = service.fetchWithRetry(\n    "https://api.example.com/data", 3\n);\nSystem.out.println(data.get());' },
        { type: 'warning', value: 'exceptionally ловит все ошибки из цепочки выше — включая NullPointerException, ClassCastException и т.д. Будь осторожен: не маскируй баги ошибочным fallback-значением. Логируй все перехваченные ошибки.' }
      ]
    },
    {
      id: 9,
      title: 'ScheduledExecutorService — задачи по расписанию',
      type: 'theory',
      content: [
        { type: 'text', value: 'ScheduledExecutorService — расширение ExecutorService для задач, которые нужно выполнить с задержкой или периодически. Это правильная замена устаревших Timer/TimerTask. В реальных серверах используется для health checks, очистки кэша, отправки метрик и других периодических задач.' },
        { type: 'heading', value: 'Создание ScheduledExecutorService' },
        { type: 'code', language: 'java', value: 'import java.util.concurrent.*;\nimport java.time.LocalTime;\n\n// Пул из 3 потоков для задач по расписанию\nScheduledExecutorService scheduler = Executors.newScheduledThreadPool(3);\n\n// Почему 3, а не 1?\n// Если одна периодическая задача зависнет — остальные продолжат работать.\n// С 1 потоком — одна зависшая задача заблокирует все остальные.' },
        { type: 'heading', value: 'schedule() — одноразовая отложенная задача' },
        { type: 'code', language: 'java', value: 'ScheduledExecutorService scheduler = Executors.newScheduledThreadPool(2);\n\n// Выполнить ОДИН раз через 5 секунд (Runnable)\nscheduler.schedule(\n    () -> System.out.println("Отложено на 5 сек: " + LocalTime.now()),\n    5, TimeUnit.SECONDS\n);\n\n// Выполнить ОДИН раз через 3 секунды с результатом (Callable)\nScheduledFuture<String> future = scheduler.schedule(\n    () -> {\n        return "Результат отложенной задачи";\n    },\n    3, TimeUnit.SECONDS\n);\n\nString result = future.get(); // блокирует ~3 сек\nSystem.out.println(result);\n\n// Реальный use-case: отложенная отправка email\n// "Письмо будет отправлено через 30 минут. Можете отменить."\nScheduledFuture<?> emailTask = scheduler.schedule(\n    () -> emailService.send(email),\n    30, TimeUnit.MINUTES\n);\n// Пользователь нажал "Отменить":\nemailTask.cancel(false); // отмена — письмо не отправится' },
        { type: 'heading', value: 'scheduleAtFixedRate() — фиксированный интервал' },
        { type: 'code', language: 'java', value: 'ScheduledExecutorService scheduler = Executors.newScheduledThreadPool(2);\n\n// Выполнять каждые 10 секунд, начиная через 0 секунд\nScheduledFuture<?> healthCheck = scheduler.scheduleAtFixedRate(\n    () -> {\n        System.out.println("[" + LocalTime.now() + "] Health check: OK");\n        // проверка что сервис жив\n    },\n    0,   // initialDelay — начать сразу\n    10,  // period — каждые 10 секунд\n    TimeUnit.SECONDS\n);\n\n// Важно: если задача выполняется ДОЛЬШЕ периода —\n// следующий запуск начнётся СРАЗУ после завершения текущего\n// (задачи НЕ накапливаются, но интервал сдвигается)\n//\n// Пример: period = 10 сек, задача выполняется 15 сек\n// t=0   → запуск 1\n// t=15  → запуск 2 (сразу после завершения, а не в t=10)\n// t=30  → запуск 3\n\n// Остановить периодическую задачу:\nhealthCheck.cancel(false);\n// false = дождаться завершения текущего выполнения\n// true  = прервать текущее выполнение' },
        { type: 'heading', value: 'scheduleWithFixedDelay() — фиксированная пауза' },
        { type: 'code', language: 'java', value: 'ScheduledExecutorService scheduler = Executors.newScheduledThreadPool(2);\n\n// Пауза 5 секунд МЕЖДУ ЗАВЕРШЕНИЕМ одной задачи и НАЧАЛОМ следующей\nscheduler.scheduleWithFixedDelay(\n    () -> {\n        System.out.println("[" + LocalTime.now() + "] Очистка кэша...");\n        cacheService.evictExpired(); // может занять разное время\n    },\n    0,  // initialDelay\n    5,  // delay между завершением и следующим запуском\n    TimeUnit.SECONDS\n);\n\n// Разница с scheduleAtFixedRate:\n//\n// scheduleAtFixedRate(task, 0, 10, SECONDS) — "каждые 10 сек с момента СТАРТА":\n// t=0  ├──task(3с)──┤         t=10 ├──task(3с)──┤         t=20 ...\n//\n// scheduleWithFixedDelay(task, 0, 10, SECONDS) — "через 10 сек после ЗАВЕРШЕНИЯ":\n// t=0  ├──task(3с)──┤  ...10с...  t=13 ├──task(3с)──┤  ...10с...  t=26 ...\n//\n// FixedRate:  гарантирует частоту запуска\n// FixedDelay: гарантирует паузу между задачами' },
        { type: 'heading', value: 'Реальный пример: мониторинг сервера' },
        { type: 'code', language: 'java', value: 'class ServerMonitor {\n    private final ScheduledExecutorService scheduler;\n\n    public ServerMonitor() {\n        this.scheduler = Executors.newScheduledThreadPool(3);\n    }\n\n    public void startMonitoring() {\n        // 1. Метрики CPU/Memory — каждые 5 секунд\n        scheduler.scheduleAtFixedRate(() -> {\n            Runtime rt = Runtime.getRuntime();\n            long usedMb = (rt.totalMemory() - rt.freeMemory()) / 1_048_576;\n            long maxMb = rt.maxMemory() / 1_048_576;\n            System.out.printf("[Метрики] Память: %d/%d МБ, Потоков: %d%n",\n                usedMb, maxMb, Thread.activeCount());\n        }, 0, 5, TimeUnit.SECONDS);\n\n        // 2. Проверка внешних зависимостей — каждые 30 секунд\n        scheduler.scheduleWithFixedDelay(() -> {\n            boolean dbOk = checkDatabase();\n            boolean redisOk = checkRedis();\n            boolean apiOk = checkExternalApi();\n            if (!dbOk || !redisOk || !apiOk) {\n                alertService.send("Сервис деградирован!");\n            }\n        }, 0, 30, TimeUnit.SECONDS);\n\n        // 3. Очистка старых сессий — каждый час\n        scheduler.scheduleAtFixedRate(\n            () -> sessionManager.cleanExpired(),\n            1, 60, TimeUnit.MINUTES\n        );\n    }\n\n    public void stop() {\n        scheduler.shutdown();\n        try {\n            if (!scheduler.awaitTermination(10, TimeUnit.SECONDS)) {\n                scheduler.shutdownNow();\n            }\n        } catch (InterruptedException e) {\n            scheduler.shutdownNow();\n        }\n    }\n}' },
        { type: 'warning', value: 'Если задача в scheduleAtFixedRate/scheduleWithFixedDelay бросает исключение — она молча прекращает повторяться! Всегда оборачивай тело задачи в try-catch. Без этого одна ошибка остановит мониторинг, и ты об этом не узнаешь.' },
        { type: 'code', language: 'java', value: '// ПЛОХО — одна ошибка убьёт периодическую задачу навсегда\nscheduler.scheduleAtFixedRate(() -> {\n    processBatch(); // если бросит исключение — задача больше не запустится\n}, 0, 10, TimeUnit.SECONDS);\n\n// ПРАВИЛЬНО — ловим все исключения внутри\nscheduler.scheduleAtFixedRate(() -> {\n    try {\n        processBatch();\n    } catch (Exception e) {\n        log.error("Ошибка в периодической задаче", e);\n        // задача продолжит запускаться каждые 10 секунд\n    }\n}, 0, 10, TimeUnit.SECONDS);' }
      ]
    },
    {
      id: 10,
      title: 'Shutdown — корректное завершение пула',
      type: 'theory',
      content: [
        { type: 'text', value: 'Неправильное завершение пула потоков — частая ошибка. Если не вызвать shutdown(), JVM не завершится (потоки в пуле не демон-потоки). Если вызвать shutdownNow() — задачи могут оборваться на полуслове. Рассмотрим правильные стратегии завершения.' },
        { type: 'heading', value: 'shutdown() — мягкое завершение' },
        { type: 'code', language: 'java', value: 'ExecutorService pool = Executors.newFixedThreadPool(4);\n\n// Отправляем задачи\npool.submit(() -> { Thread.sleep(5000); return "Задача 1"; });\npool.submit(() -> { Thread.sleep(2000); return "Задача 2"; });\npool.submit(() -> { Thread.sleep(8000); return "Задача 3"; });\n\n// shutdown() — мягкое завершение:\n// 1. Перестаёт принимать НОВЫЕ задачи\n// 2. Ждёт завершения ВСЕХ уже запущенных и поставленных в очередь задач\n// 3. НЕ блокирует вызывающий поток!\npool.shutdown();\n\n// Эта строка выполнится сразу — shutdown() не блокирует\nSystem.out.println("shutdown() вызван");\n\n// Попытка отправить новую задачу после shutdown → RejectedExecutionException\ntry {\n    pool.submit(() -> System.out.println("Новая задача"));\n} catch (RejectedExecutionException e) {\n    System.out.println("Отклонена! Пул больше не принимает задачи");\n}\n\n// isShutdown — проверка что shutdown() был вызван\nSystem.out.println("Shutdown: " + pool.isShutdown());       // true\n// isTerminated — проверка что ВСЕ задачи завершились\nSystem.out.println("Terminated: " + pool.isTerminated());   // false (ещё работают)' },
        { type: 'heading', value: 'awaitTermination() — блокирующее ожидание' },
        { type: 'code', language: 'java', value: 'ExecutorService pool = Executors.newFixedThreadPool(4);\npool.submit(() -> { Thread.sleep(3000); return "done"; });\n\npool.shutdown(); // перестаём принимать новые задачи\n\n// awaitTermination блокирует текущий поток до:\n// - все задачи завершились, ИЛИ\n// - истёк таймаут, ИЛИ\n// - текущий поток прерван (InterruptedException)\nboolean finished = pool.awaitTermination(10, TimeUnit.SECONDS);\n\nif (finished) {\n    System.out.println("Все задачи завершились корректно");\n} else {\n    System.out.println("Таймаут! Не все задачи завершились за 10 сек");\n    // Принудительное завершение\n    pool.shutdownNow();\n}' },
        { type: 'heading', value: 'shutdownNow() — принудительное завершение' },
        { type: 'code', language: 'java', value: '// shutdownNow() — жёсткое завершение:\n// 1. Перестаёт принимать новые задачи\n// 2. Прерывает (interrupt) все выполняющиеся задачи\n// 3. Возвращает список задач, которые ожидали в очереди и не были запущены\nExecutorService pool = Executors.newFixedThreadPool(2);\n\npool.submit(() -> { Thread.sleep(10000); return "Долгая 1"; });\npool.submit(() -> { Thread.sleep(10000); return "Долгая 2"; });\npool.submit(() -> { return "В очереди 1"; }); // не успеет запуститься\npool.submit(() -> { return "В очереди 2"; }); // не успеет запуститься\n\nThread.sleep(100); // даём время запуститься первым двум\n\nList<Runnable> notStarted = pool.shutdownNow();\nSystem.out.println("Не запущенных задач: " + notStarted.size()); // 2\n\n// Важно: shutdownNow вызывает Thread.interrupt() для выполняющихся задач\n// Задача ДОЛЖНА проверять Thread.interrupted() или ловить InterruptedException\n// Если задача игнорирует interrupt — она продолжит работать!' },
        { type: 'heading', value: 'Правильный паттерн завершения (production)' },
        { type: 'code', language: 'java', value: '// Рекомендуемый паттерн из JavaDoc Oracle\npublic void shutdownGracefully(ExecutorService pool) {\n    pool.shutdown(); // Шаг 1: перестаём принимать новые задачи\n    try {\n        // Шаг 2: ждём завершения текущих задач (60 сек)\n        if (!pool.awaitTermination(60, TimeUnit.SECONDS)) {\n            // Шаг 3: принудительно прерываем если не завершились\n            pool.shutdownNow();\n            // Шаг 4: ждём ещё 60 сек после прерывания\n            if (!pool.awaitTermination(60, TimeUnit.SECONDS)) {\n                System.err.println("ОШИБКА: Пул потоков не завершился!");\n            }\n        }\n    } catch (InterruptedException e) {\n        // Текущий поток был прерван — принудительно завершаем пул\n        pool.shutdownNow();\n        // Восстанавливаем флаг прерывания\n        Thread.currentThread().interrupt();\n    }\n    System.out.println("Пул корректно завершён");\n}' },
        { type: 'heading', value: 'try-with-resources (Java 19+)' },
        { type: 'code', language: 'java', value: '// Java 19+: ExecutorService реализует AutoCloseable\n// close() = shutdown() + awaitTermination(Long.MAX_VALUE)\ntry (var pool = Executors.newFixedThreadPool(4)) {\n    pool.submit(() -> doTask1());\n    pool.submit(() -> doTask2());\n    pool.submit(() -> doTask3());\n} // автоматически: pool.shutdown() + ожидание завершения всех задач\n\nSystem.out.println("Все задачи завершены, пул закрыт");' },
        { type: 'heading', value: 'Реальный пример: Graceful Shutdown в веб-сервере' },
        { type: 'code', language: 'java', value: 'class Application {\n    private final ExecutorService httpPool;\n    private final ExecutorService asyncPool;\n    private final ScheduledExecutorService cronPool;\n\n    public Application() {\n        this.httpPool = Executors.newFixedThreadPool(200);\n        this.asyncPool = Executors.newFixedThreadPool(16);\n        this.cronPool = Executors.newScheduledThreadPool(4);\n\n        // JVM Shutdown Hook — вызывается при остановке приложения\n        // (Ctrl+C, kill, docker stop)\n        Runtime.getRuntime().addShutdownHook(new Thread(() -> {\n            System.out.println("Получен сигнал остановки...");\n\n            // 1. Перестаём принимать новые запросы\n            httpPool.shutdown();\n            cronPool.shutdown();\n            asyncPool.shutdown();\n\n            try {\n                // 2. Даём 30 сек на завершение текущих запросов\n                System.out.println("Ожидаем завершения текущих запросов...");\n                if (!httpPool.awaitTermination(30, TimeUnit.SECONDS)) {\n                    httpPool.shutdownNow();\n                }\n                asyncPool.awaitTermination(15, TimeUnit.SECONDS);\n                cronPool.awaitTermination(5, TimeUnit.SECONDS);\n            } catch (InterruptedException e) {\n                httpPool.shutdownNow();\n                asyncPool.shutdownNow();\n                cronPool.shutdownNow();\n            }\n\n            System.out.println("Сервер остановлен корректно");\n        }));\n    }\n}' },
        { type: 'warning', value: 'Никогда не забывай вызывать shutdown()! Без него JVM не завершится при выходе из main(), потому что потоки пула — не демон-потоки. Программа будет "висеть" вечно. В Spring Boot контейнер сам управляет жизненным циклом бинов ExecutorService.' }
      ]
    },
    {
      id: 11,
      title: 'Практика: Параллельный HTTP клиент',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создай систему параллельной загрузки данных с нескольких серверов, используя пул потоков и Future для сбора результатов.',
      requirements: [
        'Создай метод String fetchUrl(String url) — симулирует HTTP запрос: Thread.sleep(случайное время 500-2000 мс) и возвращает "Response from " + url + " [" + время + " мс]"',
        'Создай ExecutorService с пулом из 3 потоков',
        'Отправь 6 URL на параллельную загрузку через submit(Callable)',
        'URL: "api.users.com", "api.orders.com", "api.products.com", "api.reviews.com", "api.stats.com", "api.config.com"',
        'Собери все результаты через Future.get(3, TimeUnit.SECONDS) с обработкой TimeoutException',
        'Замерь и выведи общее время выполнения (должно быть значительно меньше суммы всех задач)',
        'Корректно завершай пул через shutdown()'
      ],
      expectedOutput: 'Параллельная загрузка 6 URL (пул: 3 потока)...\nResponse from api.users.com [1200 мс]\nResponse from api.orders.com [800 мс]\nResponse from api.products.com [1500 мс]\nResponse from api.reviews.com [600 мс]\nResponse from api.stats.com [1100 мс]\nResponse from api.config.com [900 мс]\nУспешно загружено: 6 из 6\nОбщее время: ~3100 мс (вместо ~6100 мс последовательно)',
      hint: 'Для случайного времени: int delay = 500 + new Random().nextInt(1501); Замер времени: long start = System.currentTimeMillis(); ... long elapsed = System.currentTimeMillis() - start; Создай List<Future<String>> и в цикле делай pool.submit(() -> fetchUrl(url)). Потом в другом цикле собирай результаты через f.get(3, TimeUnit.SECONDS), оборачивая в try-catch для TimeoutException.',
      solution: 'import java.util.concurrent.*;\nimport java.util.*;\n\npublic class Main {\n\n    static String fetchUrl(String url) throws InterruptedException {\n        int delay = 500 + new Random().nextInt(1501); // 500..2000 мс\n        Thread.sleep(delay);\n        return "Response from " + url + " [" + delay + " мс]";\n    }\n\n    public static void main(String[] args) throws Exception {\n        List<String> urls = List.of(\n            "api.users.com", "api.orders.com", "api.products.com",\n            "api.reviews.com", "api.stats.com", "api.config.com"\n        );\n\n        ExecutorService pool = Executors.newFixedThreadPool(3);\n        System.out.println("Параллельная загрузка " + urls.size() + " URL (пул: 3 потока)...");\n\n        long start = System.currentTimeMillis();\n\n        // Отправляем все задачи\n        List<Future<String>> futures = new ArrayList<>();\n        for (String url : urls) {\n            futures.add(pool.submit(() -> fetchUrl(url)));\n        }\n\n        // Собираем результаты\n        int success = 0;\n        for (Future<String> f : futures) {\n            try {\n                String result = f.get(3, TimeUnit.SECONDS);\n                System.out.println(result);\n                success++;\n            } catch (TimeoutException e) {\n                System.out.println("ТАЙМАУТ: запрос не завершился за 3 сек");\n                f.cancel(true);\n            } catch (ExecutionException e) {\n                System.out.println("ОШИБКА: " + e.getCause().getMessage());\n            }\n        }\n\n        long elapsed = System.currentTimeMillis() - start;\n        System.out.println("Успешно загружено: " + success + " из " + urls.size());\n        System.out.println("Общее время: " + elapsed + " мс");\n\n        pool.shutdown();\n        pool.awaitTermination(5, TimeUnit.SECONDS);\n    }\n}',
      explanation: 'Пул из 3 потоков обрабатывает 6 задач в два "захода": первые 3 URL загружаются параллельно, потом следующие 3. Без пула (последовательно) общее время было бы суммой всех задержек (~6000 мс). С пулом — примерно max(первая тройка) + max(вторая тройка) ≈ 2000 + 2000 = ~4000 мс. Это в 1.5 раза быстрее. С пулом из 6 потоков было бы ещё быстрее (~2000 мс), но мы сознательно ограничили пул для демонстрации очереди задач.'
    },
    {
      id: 12,
      title: 'Практика: Async Pipeline',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создай асинхронный пайплайн обработки данных с помощью CompletableFuture: загрузка → валидация → обработка → сохранение, с обработкой ошибок на каждом этапе.',
      requirements: [
        'Создай метод CompletableFuture<String> loadData(String source) — supplyAsync, sleep 1 сек, возвращает "RAW_DATA from " + source',
        'Создай метод String validate(String data) — если data содержит "error" (ignore case) бросает IllegalArgumentException("Невалидные данные"), иначе возвращает "VALIDATED: " + data',
        'Создай метод String process(String data) — sleep 500 мс, возвращает data.toUpperCase() + " [processed]"',
        'Создай метод String save(String data) — sleep 300 мс, возвращает "Saved: " + data + " → DB"',
        'Построй CompletableFuture цепочку: loadData → thenApply(validate) → thenApply(process) → thenApply(save)',
        'Добавь обработку ошибок через exceptionally с fallback-значением "PIPELINE FAILED: " + причина',
        'Добавь whenComplete для логирования результата/ошибки',
        'Запусти пайплайн для трёх источников: "server-1", "server-error-2", "server-3" параллельно',
        'Дождись всех через CompletableFuture.allOf() и выведи результаты'
      ],
      expectedOutput: '[LOG] Результат: Saved: VALIDATED: RAW_DATA FROM SERVER-1 [processed] → DB\n[LOG] Ошибка: java.lang.IllegalArgumentException: Невалидные данные\n[LOG] Результат: Saved: VALIDATED: RAW_DATA FROM SERVER-3 [processed] → DB\n--- Результаты ---\nserver-1:       Saved: VALIDATED: RAW_DATA FROM SERVER-1 [processed] → DB\nserver-error-2: PIPELINE FAILED: Невалидные данные\nserver-3:       Saved: VALIDATED: RAW_DATA FROM SERVER-3 [processed] → DB',
      hint: 'loadData возвращает CompletableFuture.supplyAsync(() -> { sleep(1000); return "RAW_DATA from " + source; }). Цепочка: loadData(source).thenApply(d -> validate(d)).thenApply(d -> process(d)).thenApply(d -> save(d)).exceptionally(ex -> "PIPELINE FAILED: " + ex.getCause().getMessage()).whenComplete((res, ex) -> { if (ex != null) log("Ошибка: " + ex); else log("Результат: " + res); }). Для "server-error-2" метод validate бросит исключение, потому что source содержит "error".',
      solution: 'import java.util.concurrent.*;\nimport java.util.*;\n\npublic class Main {\n\n    static CompletableFuture<String> loadData(String source) {\n        return CompletableFuture.supplyAsync(() -> {\n            try { Thread.sleep(1000); } catch (InterruptedException e) {}\n            return "RAW_DATA from " + source;\n        });\n    }\n\n    static String validate(String data) {\n        if (data.toLowerCase().contains("error")) {\n            throw new IllegalArgumentException("Невалидные данные");\n        }\n        return "VALIDATED: " + data;\n    }\n\n    static String process(String data) {\n        try { Thread.sleep(500); } catch (InterruptedException e) {}\n        return data.toUpperCase() + " [processed]";\n    }\n\n    static String save(String data) {\n        try { Thread.sleep(300); } catch (InterruptedException e) {}\n        return "Saved: " + data + " → DB";\n    }\n\n    static CompletableFuture<String> runPipeline(String source) {\n        return loadData(source)\n            .thenApply(data -> validate(data))\n            .thenApply(data -> process(data))\n            .thenApply(data -> save(data))\n            .exceptionally(ex -> "PIPELINE FAILED: " + ex.getCause().getMessage())\n            .whenComplete((result, ex) -> {\n                if (ex != null) {\n                    System.out.println("[LOG] Ошибка: " + ex);\n                } else {\n                    System.out.println("[LOG] Результат: " + result);\n                }\n            });\n    }\n\n    public static void main(String[] args) throws Exception {\n        List<String> sources = List.of("server-1", "server-error-2", "server-3");\n\n        Map<String, CompletableFuture<String>> pipelines = new LinkedHashMap<>();\n        for (String source : sources) {\n            pipelines.put(source, runPipeline(source));\n        }\n\n        CompletableFuture.allOf(\n            pipelines.values().toArray(new CompletableFuture[0])\n        ).get();\n\n        System.out.println("--- Результаты ---");\n        for (Map.Entry<String, CompletableFuture<String>> entry : pipelines.entrySet()) {\n            System.out.printf("%-15s %s%n", entry.getKey() + ":", entry.getValue().get());\n        }\n    }\n}',
      explanation: 'Этот пайплайн демонстрирует ключевые концепции CompletableFuture: 1) цепочка thenApply для последовательной обработки, 2) exceptionally для перехвата ошибок на любом этапе цепочки, 3) whenComplete для логирования, 4) allOf для параллельного запуска нескольких пайплайнов. Источник "server-error-2" содержит слово "error" — validate() бросит исключение, thenApply(process) и thenApply(save) будут пропущены, и сработает exceptionally() с fallback-значением. Остальные источники пройдут весь пайплайн успешно. Все три пайплайна работают параллельно — общее время ~1800 мс вместо ~5400 мс последовательно.'
    }
  ]
}
