export default {
  id: 17,
  title: 'Полиморфизм',
  description: 'Один интерфейс — много реализаций. Перегрузка и переопределение методов, динамическая диспетчеризация, паттерн Strategy',
  lessons: [
    {
      id: 1,
      title: 'Что такое полиморфизм?',
      type: 'theory',
      content: [
        { type: 'text', value: 'Полиморфизм (от греч. "poly" — много, "morphe" — форма) — это третий столп ООП (после инкапсуляции и наследования). Суть простая: один и тот же вызов может работать по-разному в зависимости от типа объекта. Одно имя — много форм поведения.' },
        { type: 'tip', value: 'Представь пульт от телевизора. На нём есть кнопка "Play". Теперь направь этот пульт на разные устройства:\n\n- Телевизор → начнёт показывать фильм\n- Музыкальный центр → начнёт играть музыку\n- DVD-плеер → начнёт крутить диск\n\nОдна кнопка "Play", но каждое устройство реагирует по-своему. Это и есть полиморфизм!' },
        { type: 'heading', value: 'Зачем нужен полиморфизм?' },
        { type: 'text', value: 'Без полиморфизма тебе пришлось бы писать отдельный код для каждого типа объекта. С полиморфизмом ты пишешь один код, который работает с любыми типами — существующими и будущими.' },
        { type: 'code', language: 'java', value: '// БЕЗ полиморфизма — кошмарный код:\nvoid makeSound(String animalType) {\n    if (animalType.equals("cat")) {\n        System.out.println("Мяу!");\n    } else if (animalType.equals("dog")) {\n        System.out.println("Гав!");\n    } else if (animalType.equals("cow")) {\n        System.out.println("Муу!");\n    }\n    // Добавляешь новое животное? Нужно лезть сюда и менять код!\n}\n\n// С ПОЛИМОРФИЗМОМ — чистый, расширяемый код:\nclass Animal {\n    String name;\n    Animal(String name) { this.name = name; }\n    void makeSound() { System.out.println("..."); }\n}\n\nclass Cat extends Animal {\n    Cat(String name) { super(name); }\n    @Override void makeSound() { System.out.println(name + ": Мяу!"); }\n}\n\nclass Dog extends Animal {\n    Dog(String name) { super(name); }\n    @Override void makeSound() { System.out.println(name + ": Гав!"); }\n}\n\nclass Cow extends Animal {\n    Cow(String name) { super(name); }\n    @Override void makeSound() { System.out.println(name + ": Муу!"); }\n}\n\n// Один код работает для ВСЕХ типов животных!\nAnimal[] animals = { new Cat("Мурзик"), new Dog("Рекс"), new Cow("Зорька") };\nfor (Animal a : animals) {\n    a.makeSound(); // Каждый кричит по-своему!\n}\n// Мурзик: Мяу!\n// Рекс: Гав!\n// Зорька: Муу!' },
        { type: 'heading', value: 'Два вида полиморфизма в Java' },
        { type: 'text', value: 'Java поддерживает два вида полиморфизма. Они отличаются тем, когда принимается решение, какой метод вызвать:' },
        { type: 'list', items: [
          'Полиморфизм времени компиляции (compile-time, статический) — перегрузка методов (method overloading). Компилятор решает, какой метод вызвать, по типам аргументов.',
          'Полиморфизм времени выполнения (runtime, динамический) — переопределение методов (method overriding). JVM решает, какой метод вызвать, по реальному типу объекта в момент выполнения.'
        ]},
        { type: 'code', language: 'java', value: '// Compile-time полиморфизм (перегрузка):\n// Компилятор видит типы аргументов и выбирает метод\nvoid print(int x)    { ... } // вызовется для print(42)\nvoid print(String s) { ... } // вызовется для print("Hello")\n\n// Runtime полиморфизм (переопределение):\n// JVM смотрит на реальный тип объекта\nAnimal a = new Cat("Мурзик"); // Переменная Animal, объект Cat\na.makeSound(); // JVM видит: реальный тип — Cat → вызывает Cat.makeSound()' },
        { type: 'note', value: 'Полиморфизм — это не просто теория. Это основа всего хорошего Java-кода. Spring Framework, коллекции, стримы, JDBC — всё построено на полиморфизме. Разобравшись в нём, ты поймёшь 80% архитектурных решений в Java.' }
      ]
    },
    {
      id: 2,
      title: 'Перегрузка методов (compile-time полиморфизм)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Перегрузка методов (method overloading) — это создание нескольких методов с одинаковым именем, но разными параметрами в одном классе. Java определяет, какой метод вызвать, ещё на этапе компиляции — по количеству и типам переданных аргументов.' },
        { type: 'tip', value: 'Перегрузка — как функция "отправить" в мессенджере:\n- отправить(текст) → отправляет сообщение\n- отправить(фото) → отправляет картинку\n- отправить(файл) → отправляет документ\n- отправить(текст, фото) → отправляет сообщение с картинкой\n\nОдно действие "отправить", но поведение зависит от того, что ты передал.' },
        { type: 'heading', value: 'Правила перегрузки методов' },
        { type: 'text', value: 'Чтобы перегрузка работала, методы должны отличаться параметрами:' },
        { type: 'list', items: [
          'Разное КОЛИЧЕСТВО параметров — print() и print(String text)',
          'Разные ТИПЫ параметров — print(int x) и print(String x)',
          'Разный ПОРЯДОК типов — print(int x, String s) и print(String s, int x)',
          'Возвращаемый тип НЕ ВЛИЯЕТ на перегрузку — нельзя иметь int calc() и double calc()',
          'Модификаторы доступа НЕ ВЛИЯЮТ на перегрузку',
          'Имена параметров НЕ ВЛИЯЮТ — print(int a) и print(int b) это один метод!'
        ]},
        { type: 'code', language: 'java', value: '// === Класс Calculator с перегруженными методами ===\npublic class Calculator {\n\n    // Сложение двух целых чисел\n    int add(int a, int b) {\n        System.out.println("add(int, int)");\n        return a + b;\n    }\n\n    // Сложение трёх целых чисел (другое кол-во параметров)\n    int add(int a, int b, int c) {\n        System.out.println("add(int, int, int)");\n        return a + b + c;\n    }\n\n    // Сложение двух дробных чисел (другие типы параметров)\n    double add(double a, double b) {\n        System.out.println("add(double, double)");\n        return a + b;\n    }\n\n    // Конкатенация строк (совсем другой тип)\n    String add(String a, String b) {\n        System.out.println("add(String, String)");\n        return a + b;\n    }\n}\n\nCalculator calc = new Calculator();\n\nSystem.out.println(calc.add(5, 3));         // add(int, int) → 8\nSystem.out.println(calc.add(5, 3, 2));      // add(int, int, int) → 10\nSystem.out.println(calc.add(2.5, 3.7));     // add(double, double) → 6.2\nSystem.out.println(calc.add("Привет", "!")); // add(String, String) → Привет!' },
        { type: 'heading', value: 'Перегрузка конструкторов' },
        { type: 'text', value: 'Конструкторы тоже можно перегружать. Это даёт разные способы создания объекта:' },
        { type: 'code', language: 'java', value: 'public class Pizza {\n    String size;\n    String crust;\n    String[] toppings;\n\n    // Конструктор 1: только размер (остальное по умолчанию)\n    public Pizza(String size) {\n        this(size, "Тонкое", new String[]{"Сыр"});\n    }\n\n    // Конструктор 2: размер и тесто\n    public Pizza(String size, String crust) {\n        this(size, crust, new String[]{"Сыр"});\n    }\n\n    // Конструктор 3: полная настройка\n    public Pizza(String size, String crust, String[] toppings) {\n        this.size = size;\n        this.crust = crust;\n        this.toppings = toppings;\n    }\n\n    void describe() {\n        System.out.print(size + " пицца, тесто: " + crust + ", начинка: ");\n        System.out.println(String.join(", ", toppings));\n    }\n}\n\n// Три способа создания пиццы:\nPizza p1 = new Pizza("Большая");\np1.describe(); // Большая пицца, тесто: Тонкое, начинка: Сыр\n\nPizza p2 = new Pizza("Средняя", "Толстое");\np2.describe(); // Средняя пицца, тесто: Толстое, начинка: Сыр\n\nPizza p3 = new Pizza("Маленькая", "Тонкое", new String[]{"Пепперони", "Грибы", "Оливки"});\np3.describe(); // Маленькая пицца, тесто: Тонкое, начинка: Пепперони, Грибы, Оливки' },
        { type: 'heading', value: 'Автоматическое расширение типов (type promotion)' },
        { type: 'text', value: 'Если Java не находит точного совпадения, она пытается расширить тип аргумента:' },
        { type: 'code', language: 'java', value: 'public class TypeDemo {\n    void show(int x)    { System.out.println("int: " + x); }\n    void show(double x) { System.out.println("double: " + x); }\n    void show(String x) { System.out.println("String: " + x); }\n}\n\nTypeDemo demo = new TypeDemo();\ndemo.show(42);      // int: 42 — точное совпадение\ndemo.show(3.14);    // double: 3.14 — точное совпадение\ndemo.show("Java");  // String: Java — точное совпадение\n\n// А что если передать byte или short?\nbyte b = 10;\ndemo.show(b);       // int: 10 — byte расширяется до int!\n\nchar c = \'A\';\ndemo.show(c);       // int: 65 — char расширяется до int!\n\nfloat f = 2.5f;\ndemo.show(f);       // double: 2.5 — float расширяется до double!' },
        { type: 'warning', value: 'Не злоупотребляй перегрузкой! Если у метода слишком много перегруженных версий с похожими типами, код становится запутанным. Используй перегрузку когда методы действительно делают одно и то же, но с разными входными данными.' },
        { type: 'note', value: 'Перегрузку называют compile-time полиморфизмом потому что компилятор знает типы аргументов и выбирает нужный метод ещё до запуска программы. Это НЕ настоящий полиморфизм в классическом понимании ООП, но Java включает его в эту категорию.' }
      ]
    },
    {
      id: 3,
      title: 'Переопределение методов (runtime полиморфизм)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Переопределение методов (method overriding) — это когда подкласс создаёт свою версию метода, унаследованного от родителя. Это настоящий, классический полиморфизм — какой метод вызовется, решается в момент выполнения программы (runtime).' },
        { type: 'tip', value: 'Переопределение — как фирменный рецепт. Бабушка готовит борщ (базовый метод). Мама переопределяет рецепт: добавляет фасоль. Ты переопределяешь рецепт мамы: делаешь вегетарианский. Название "приготовитьБорщ()" одинаковое, но каждый готовит по-своему.' },
        { type: 'heading', value: 'Правила переопределения (@Override)' },
        { type: 'list', items: [
          'Имя метода должно совпадать ТОЧНО',
          'Параметры (тип, количество, порядок) должны совпадать ТОЧНО',
          'Возвращаемый тип должен совпадать (или быть подтипом — ковариантность)',
          'Модификатор доступа НЕ может быть СТРОЖЕ (если у родителя public — нельзя сделать private)',
          'Нельзя переопределять static, final и private методы',
          '@Override — аннотация, которая просит компилятор проверить, что метод действительно переопределяет метод родителя'
        ]},
        { type: 'code', language: 'java', value: '// === Иерархия транспорта ===\npublic class Vehicle {\n    String name;\n\n    Vehicle(String name) { this.name = name; }\n\n    void start() {\n        System.out.println(name + ": Транспорт заводится");\n    }\n\n    void move() {\n        System.out.println(name + ": Транспорт движется");\n    }\n\n    void stop() {\n        System.out.println(name + ": Транспорт останавливается");\n    }\n\n    double fuelConsumption() {\n        return 0;\n    }\n}\n\npublic class Car extends Vehicle {\n    Car(String name) { super(name); }\n\n    @Override\n    void start() {\n        System.out.println(name + ": Поворот ключа... Двигатель урчит!");\n    }\n\n    @Override\n    void move() {\n        System.out.println(name + ": Едет по дороге со скоростью 80 км/ч");\n    }\n\n    @Override\n    double fuelConsumption() {\n        return 8.5; // 8.5 литров на 100 км\n    }\n}\n\npublic class Bicycle extends Vehicle {\n    Bicycle(String name) { super(name); }\n\n    @Override\n    void start() {\n        System.out.println(name + ": Запрыгиваем на седло!");\n    }\n\n    @Override\n    void move() {\n        System.out.println(name + ": Крутим педали, едем 20 км/ч");\n    }\n\n    @Override\n    double fuelConsumption() {\n        return 0; // Велосипед не расходует топливо!\n    }\n}\n\npublic class Airplane extends Vehicle {\n    Airplane(String name) { super(name); }\n\n    @Override\n    void start() {\n        System.out.println(name + ": Запуск двигателей! Выруливание на полосу...");\n    }\n\n    @Override\n    void move() {\n        System.out.println(name + ": Летит на высоте 10 000 м со скоростью 900 км/ч");\n    }\n\n    @Override\n    double fuelConsumption() {\n        return 2500; // 2500 литров на 100 км\n    }\n}\n\n// Полиморфизм в действии!\nVehicle[] fleet = {\n    new Car("Toyota Camry"),\n    new Bicycle("Trek"),\n    new Airplane("Boeing 737")\n};\n\nfor (Vehicle v : fleet) {\n    v.start();  // Каждый заводится по-своему\n    v.move();   // Каждый движется по-своему\n    System.out.println("Расход: " + v.fuelConsumption() + " л/100км");\n    v.stop();   // stop() не переопределён — используется родительский\n    System.out.println("---");\n}' },
        { type: 'heading', value: 'Зачем нужна аннотация @Override?' },
        { type: 'text', value: '@Override — это не просто декорация. Это защита от ошибок:' },
        { type: 'code', language: 'java', value: 'public class Dog extends Animal {\n\n    // БЕЗ @Override — опечатка останется незамеченной!\n    void makeSOund() { // Опечатка! s вместо S\n        System.out.println("Гав!");\n    }\n    // Java создаст НОВЫЙ метод makeSOund(), а не переопределит makeSound().\n    // Ошибку найдёшь только в runtime, когда собака будет молчать.\n\n    // С @Override — компилятор СРАЗУ увидит ошибку!\n    @Override\n    void makeSOund() { // ОШИБКА КОМПИЛЯЦИИ: метод makeSOund() не найден в Animal!\n        System.out.println("Гав!");\n    }\n}' },
        { type: 'heading', value: 'Переопределение с вызовом super' },
        { type: 'text', value: 'Иногда дочерний класс хочет не заменить метод родителя, а расширить его — добавить что-то к существующему поведению:' },
        { type: 'code', language: 'java', value: 'public class Logger {\n    void log(String message) {\n        System.out.println("[LOG] " + message);\n    }\n}\n\npublic class TimestampLogger extends Logger {\n    @Override\n    void log(String message) {\n        // Сначала добавляем время\n        System.out.print("[" + java.time.LocalTime.now() + "] ");\n        // Потом вызываем логирование родителя\n        super.log(message); // → [14:30:05] [LOG] Сообщение\n    }\n}\n\npublic class FileLogger extends TimestampLogger {\n    @Override\n    void log(String message) {\n        super.log(message);       // Вывод в консоль с временем\n        saveToFile(message);      // Дополнительно сохраняем в файл\n    }\n\n    private void saveToFile(String message) {\n        System.out.println("  → Сохранено в файл log.txt");\n    }\n}\n\nLogger logger = new FileLogger();\nlogger.log("Сервер запущен");\n// [14:30:05] [LOG] Сервер запущен\n//   → Сохранено в файл log.txt' },
        { type: 'warning', value: 'Не путай перегрузку (overloading) и переопределение (overriding)!\n- Перегрузка: ОДИН класс, РАЗНЫЕ параметры, compile-time\n- Переопределение: РАЗНЫЕ классы (родитель-потомок), ОДИНАКОВЫЕ параметры, runtime' }
      ]
    },
    {
      id: 4,
      title: 'Upcasting и динамическая диспетчеризация',
      type: 'theory',
      content: [
        { type: 'text', value: 'Upcasting (приведение вверх) — это когда объект подкласса сохраняется в переменную типа родительского класса. Это основа полиморфизма: мы работаем с объектом через "общий" тип, а Java сама определяет, какой метод вызвать.' },
        { type: 'tip', value: 'Upcasting — как бейджик "Сотрудник" на проходной. Ты можешь быть программистом, дизайнером или менеджером — но на бейджике написано просто "Сотрудник". Проходная (компилятор) видит только бейджик. Но когда тебя попросят работать, ты будешь делать СВОЮ работу (программировать, рисовать или управлять), а не абстрактную "работу сотрудника".' },
        { type: 'heading', value: 'Как работает upcasting' },
        { type: 'code', language: 'java', value: 'public class Animal {\n    String name;\n    Animal(String name) { this.name = name; }\n\n    void eat() {\n        System.out.println(name + " ест");\n    }\n\n    void makeSound() {\n        System.out.println(name + " издаёт звук");\n    }\n}\n\npublic class Cat extends Animal {\n    Cat(String name) { super(name); }\n\n    @Override\n    void makeSound() {\n        System.out.println(name + ": Мяу!");\n    }\n\n    // Свой метод, которого нет в Animal\n    void purr() {\n        System.out.println(name + " мурчит");\n    }\n}\n\n// === UPCASTING ===\nCat cat = new Cat("Мурзик");      // Тип переменной: Cat, тип объекта: Cat\nAnimal animal = cat;               // UPCASTING! Тип переменной: Animal, тип объекта: Cat\nAnimal animal2 = new Cat("Пушок"); // Тоже upcasting, сразу при создании\n\n// Что можно вызвать через переменную Animal?\nanimal.eat();       // OK — eat() есть в Animal\nanimal.makeSound(); // OK — makeSound() есть в Animal. Вызовется версия Cat!\n// animal.purr();   // ОШИБКА КОМПИЛЯЦИИ! purr() нет в Animal\n\n// Важно: хотя переменная типа Animal, вызывается метод Cat!\n// Потому что реальный объект — Cat\nanimal.makeSound(); // Мурзик: Мяу! (а не "издаёт звук")' },
        { type: 'heading', value: 'Динамическая диспетчеризация (Dynamic Dispatch)' },
        { type: 'text', value: 'Динамическая диспетчеризация — это механизм, благодаря которому Java определяет, какой именно метод вызвать, не при компиляции, а в момент выполнения (runtime). Java смотрит не на тип переменной, а на реальный тип объекта.' },
        { type: 'code', language: 'java', value: '// Как Java решает, какой метод вызвать?\n\npublic class Shape {\n    String name;\n    Shape(String name) { this.name = name; }\n\n    double area() {\n        return 0; // Базовая реализация\n    }\n\n    // Этот метод вызывает area() — какую версию?\n    String describe() {\n        return name + ": площадь = " + String.format("%.2f", area());\n    }\n}\n\npublic class Circle extends Shape {\n    double radius;\n    Circle(double r) { super("Круг"); this.radius = r; }\n\n    @Override\n    double area() {\n        return Math.PI * radius * radius;\n    }\n}\n\npublic class Square extends Shape {\n    double side;\n    Square(double s) { super("Квадрат"); this.side = s; }\n\n    @Override\n    double area() {\n        return side * side;\n    }\n}\n\n// describe() определён в Shape, но area() — ПОЛИМОРФНЫЙ!\nShape s1 = new Circle(5);   // Upcasting\nShape s2 = new Square(4);   // Upcasting\n\nSystem.out.println(s1.describe()); // Круг: площадь = 78.54\nSystem.out.println(s2.describe()); // Квадрат: площадь = 16.00\n\n// Как это работает пошагово:\n// 1. s1.describe() → Java видит: тип переменной Shape, но метод describe() не переопределён → вызывает Shape.describe()\n// 2. Внутри describe() вызывается area() → Java смотрит: реальный тип s1 — Circle → вызывает Circle.area()\n// 3. Возвращается Math.PI * 5 * 5 = 78.54\n// Это и есть динамическая диспетчеризация!' },
        { type: 'heading', value: 'Полиморфизм в массивах и методах' },
        { type: 'text', value: 'Главная сила upcasting: можно хранить разные типы в одном массиве и передавать в один метод:' },
        { type: 'code', language: 'java', value: '// Массив родительского типа хранит разные подтипы\nAnimal[] animals = new Animal[4];\nanimals[0] = new Cat("Мурзик");   // Upcasting!\nanimals[1] = new Dog("Шарик");    // Upcasting!\nanimals[2] = new Cat("Кися");     // Upcasting!\nanimals[3] = new Dog("Тузик");    // Upcasting!\n\n// Один метод работает с ЛЮБЫМ Animal\nvoid feedAll(Animal[] animals) {\n    for (Animal a : animals) {\n        a.eat();       // Полиморфизм!\n        a.makeSound(); // Каждый кричит по-своему!\n    }\n}\n\nfeedAll(animals);\n\n// Метод принимает один Animal — но работает с любым подтипом!\nvoid examine(Animal animal) {\n    System.out.println("Осмотр: " + animal.name);\n    animal.makeSound(); // Какой метод вызовется? Зависит от реального типа!\n}\n\nexamine(new Cat("Мурзик")); // Осмотр: Мурзик → Мурзик: Мяу!\nexamine(new Dog("Шарик"));  // Осмотр: Шарик → Шарик: Гав!' },
        { type: 'heading', value: 'Таблица виртуальных методов (vtable)' },
        { type: 'text', value: 'Под капотом JVM использует таблицу виртуальных методов (vtable). Каждый класс имеет свою таблицу, где для каждого метода указан адрес его реализации. При вызове метода JVM находит vtable реального объекта и вызывает нужный метод.' },
        { type: 'code', language: 'java', value: '// Визуализация vtable:\n//\n// Animal vtable:           Cat vtable:            Dog vtable:\n// eat()      → Animal.eat  eat()      → Animal.eat eat()      → Animal.eat\n// makeSound → Animal.makeS makeSound → Cat.makeS   makeSound → Dog.makeS\n//                          purr()    → Cat.purr    fetch()   → Dog.fetch\n\n// Когда JVM видит animal.makeSound():\n// 1. Берёт реальный тип объекта (Cat)\n// 2. Ищет makeSound() в vtable класса Cat\n// 3. Находит → Cat.makeSound()\n// 4. Вызывает его' },
        { type: 'note', value: 'final методы, static методы и private методы НЕ участвуют в динамической диспетчеризации — их вызов определяется при компиляции. Поэтому их нельзя переопределять полиморфно.' }
      ]
    },
    {
      id: 5,
      title: 'Полиморфизм через интерфейсы',
      type: 'theory',
      content: [
        { type: 'text', value: 'Полиморфизм работает не только с наследованием классов, но и с интерфейсами. Более того, в реальных проектах полиморфизм через интерфейсы используется ЧАЩЕ, чем через наследование классов. Интерфейс определяет контракт — "что должен уметь объект", а каждый класс реализует этот контракт по-своему.' },
        { type: 'tip', value: 'Интерфейс — как розетка. Стандартная розетка (интерфейс) говорит: "У меня два отверстия, дай мне штепсель". Ей всё равно, что ты подключаешь — чайник, телевизор или зарядку. Любое устройство, которое реализует "штепсель", подойдёт. Так же и интерфейс: любой класс, который реализует интерфейс, подходит.' },
        { type: 'heading', value: 'Базовый пример: интерфейс Speakable' },
        { type: 'code', language: 'java', value: '// Интерфейс определяет контракт\ninterface Speakable {\n    void speak(); // Каждый, кто реализует Speakable, обязан уметь "говорить"\n}\n\n// Разные классы реализуют интерфейс по-своему\nclass Dog implements Speakable {\n    String name;\n    Dog(String name) { this.name = name; }\n\n    @Override\n    public void speak() {\n        System.out.println(name + ": Гав-гав!");\n    }\n}\n\nclass Cat implements Speakable {\n    String name;\n    Cat(String name) { this.name = name; }\n\n    @Override\n    public void speak() {\n        System.out.println(name + ": Мяу!");\n    }\n}\n\nclass Parrot implements Speakable {\n    String name;\n    String phrase;\n    Parrot(String name, String phrase) {\n        this.name = name;\n        this.phrase = phrase;\n    }\n\n    @Override\n    public void speak() {\n        System.out.println(name + ": " + phrase + "!");\n    }\n}\n\nclass Robot implements Speakable {\n    @Override\n    public void speak() {\n        System.out.println("Робот: Бип-боп, я робот!");\n    }\n}\n\n// Полиморфизм: один список — разные типы!\nList<Speakable> speakers = new ArrayList<>();\nspeakers.add(new Dog("Рекс"));\nspeakers.add(new Cat("Мурзик"));\nspeakers.add(new Parrot("Кеша", "Кеша хороший"));\nspeakers.add(new Robot());\n\n// Один цикл — разное поведение!\nfor (Speakable s : speakers) {\n    s.speak();\n}\n// Рекс: Гав-гав!\n// Мурзик: Мяу!\n// Кеша: Кеша хороший!\n// Робот: Бип-боп, я робот!' },
        { type: 'heading', value: 'Полиморфизм через интерфейс в параметрах метода' },
        { type: 'text', value: 'Метод может принимать интерфейс как параметр — и работать с ЛЮБОЙ реализацией:' },
        { type: 'code', language: 'java', value: 'interface Drawable {\n    void draw();\n    String getColor();\n}\n\nclass CircleShape implements Drawable {\n    double radius;\n    String color;\n\n    CircleShape(double radius, String color) {\n        this.radius = radius;\n        this.color = color;\n    }\n\n    @Override\n    public void draw() {\n        System.out.println("Рисую круг радиусом " + radius + " цветом " + color);\n    }\n\n    @Override\n    public String getColor() { return color; }\n}\n\nclass RectangleShape implements Drawable {\n    double width, height;\n    String color;\n\n    RectangleShape(double w, double h, String color) {\n        this.width = w;\n        this.height = h;\n        this.color = color;\n    }\n\n    @Override\n    public void draw() {\n        System.out.println("Рисую прямоугольник " + width + "x" + height + " цветом " + color);\n    }\n\n    @Override\n    public String getColor() { return color; }\n}\n\n// Метод работает с ЛЮБОЙ реализацией Drawable!\nvoid renderAll(List<Drawable> shapes) {\n    for (Drawable d : shapes) {\n        d.draw(); // Полиморфный вызов!\n    }\n}\n\nList<Drawable> canvas = List.of(\n    new CircleShape(5, "Красный"),\n    new RectangleShape(10, 20, "Синий"),\n    new CircleShape(3, "Зелёный")\n);\n\nrenderAll(canvas);\n// Рисую круг радиусом 5.0 цветом Красный\n// Рисую прямоугольник 10.0x20.0 цветом Синий\n// Рисую круг радиусом 3.0 цветом Зелёный' },
        { type: 'heading', value: 'Преимущество интерфейсов: множественная реализация' },
        { type: 'text', value: 'В отличие от классов, один класс может реализовать НЕСКОЛЬКО интерфейсов:' },
        { type: 'code', language: 'java', value: 'interface Flyable {\n    void fly();\n}\n\ninterface Swimmable {\n    void swim();\n}\n\ninterface Speakable {\n    void speak();\n}\n\n// Утка умеет ВСЁ!\nclass Duck implements Flyable, Swimmable, Speakable {\n    String name;\n    Duck(String name) { this.name = name; }\n\n    @Override public void fly()   { System.out.println(name + " летит"); }\n    @Override public void swim()  { System.out.println(name + " плавает"); }\n    @Override public void speak() { System.out.println(name + ": Кря-кря!"); }\n}\n\n// Утку можно использовать через ЛЮБОЙ из её интерфейсов:\nDuck duck = new Duck("Дональд");\n\nFlyable flyer = duck;      // Утка как летающий объект\nSwimmable swimmer = duck;  // Утка как плавающий объект\nSpeakable speaker = duck;  // Утка как говорящий объект\n\nflyer.fly();       // Дональд летит\nswimmer.swim();    // Дональд плавает\nspeaker.speak();   // Дональд: Кря-кря!\n\n// Разные "грани" одного объекта — полиморфизм!' },
        { type: 'note', value: 'В реальных проектах чаще пишут так: "программируй на уровне интерфейса, а не реализации". То есть используй List вместо ArrayList, Map вместо HashMap. Это позволяет легко менять реализацию, не трогая остальной код.' }
      ]
    },
    {
      id: 6,
      title: 'instanceof и приведение типов',
      type: 'theory',
      content: [
        { type: 'text', value: 'Иногда при полиморфизме нужно узнать реальный тип объекта и обратиться к его специфическим методам. Для этого используется оператор instanceof и downcasting (приведение вниз).' },
        { type: 'tip', value: 'instanceof — как вопрос "Ты кто?". Допустим, ты достаёшь животное из корзины "Животные". Прежде чем погладить его (метод есть только у кошек), нужно спросить: "Ты кошка?" (instanceof Cat). Если да — можно смело гладить!' },
        { type: 'heading', value: 'Оператор instanceof' },
        { type: 'code', language: 'java', value: 'class Animal { String name; Animal(String n) { name = n; } }\nclass Cat extends Animal {\n    Cat(String n) { super(n); }\n    void purr() { System.out.println(name + " мурчит"); }\n}\nclass Dog extends Animal {\n    Dog(String n) { super(n); }\n    void fetch() { System.out.println(name + " приносит палку"); }\n}\n\n// instanceof проверяет: "Этот объект — экземпляр данного класса?"\nAnimal a1 = new Cat("Мурзик");\nAnimal a2 = new Dog("Шарик");\n\nSystem.out.println(a1 instanceof Cat);    // true\nSystem.out.println(a1 instanceof Dog);    // false\nSystem.out.println(a1 instanceof Animal); // true (Cat IS-A Animal!)\nSystem.out.println(a2 instanceof Dog);    // true\nSystem.out.println(a2 instanceof Cat);    // false\n\n// null instanceof ЧтоУгодно — всегда false\nAnimal a3 = null;\nSystem.out.println(a3 instanceof Cat);    // false (не NullPointerException!)' },
        { type: 'heading', value: 'Downcasting — приведение вниз' },
        { type: 'text', value: 'Downcasting — это обратная операция к upcasting: переменную родительского типа приводим к типу подкласса. Это опасная операция — если тип не совпадёт, получим ClassCastException!' },
        { type: 'code', language: 'java', value: '// === Безопасный downcasting ===\nAnimal animal = new Cat("Мурзик"); // Upcasting\n\n// Опасно! Без проверки\n// Dog dog = (Dog) animal; // ClassCastException! Это кошка, а не собака!\n\n// Безопасно! С проверкой instanceof\nif (animal instanceof Cat) {\n    Cat cat = (Cat) animal; // Безопасный downcasting\n    cat.purr();             // Мурзик мурчит\n}\n\n// === Обработка массива разных типов ===\nAnimal[] animals = {\n    new Cat("Мурзик"),\n    new Dog("Шарик"),\n    new Cat("Пушок"),\n    new Dog("Бобик")\n};\n\nfor (Animal a : animals) {\n    // Общие методы — полиморфно\n    System.out.println("Животное: " + a.name);\n\n    // Специфические методы — через instanceof + downcasting\n    if (a instanceof Cat) {\n        Cat cat = (Cat) a;\n        cat.purr(); // Только для кошек!\n    } else if (a instanceof Dog) {\n        Dog dog = (Dog) a;\n        dog.fetch(); // Только для собак!\n    }\n}' },
        { type: 'heading', value: 'Pattern Matching для instanceof (Java 16+)' },
        { type: 'text', value: 'Начиная с Java 16, можно совместить проверку и приведение в одной строке — это называется pattern matching:' },
        { type: 'code', language: 'java', value: '// Старый способ (до Java 16):\nif (animal instanceof Cat) {\n    Cat cat = (Cat) animal; // Нужно отдельно приводить\n    cat.purr();\n}\n\n// Новый способ (Java 16+): pattern matching\nif (animal instanceof Cat cat) { // Проверяет И приводит в одну строку!\n    cat.purr(); // cat уже типа Cat, можно использовать сразу\n}\n\n// Пример с switch (Java 21+):\nString describeAnimal(Animal a) {\n    return switch (a) {\n        case Cat cat   -> cat.name + " — кошка, мурчит";\n        case Dog dog   -> dog.name + " — собака, приносит палку";\n        case Animal an -> an.name + " — неизвестное животное";\n    };\n}' },
        { type: 'heading', value: 'Когда НЕ надо использовать instanceof' },
        { type: 'text', value: 'Частое использование instanceof — признак плохого дизайна. Если ты постоянно проверяешь тип, значит полиморфизм не используется правильно:' },
        { type: 'code', language: 'java', value: '// ПЛОХО: куча instanceof — "запах" плохого кода\nvoid process(Animal a) {\n    if (a instanceof Cat) {\n        System.out.println("Кормим рыбой");\n    } else if (a instanceof Dog) {\n        System.out.println("Кормим мясом");\n    } else if (a instanceof Parrot) {\n        System.out.println("Кормим зёрнами");\n    }\n    // Добавляешь новое животное? Нужно лезть сюда!\n}\n\n// ХОРОШО: используем полиморфизм\nclass Animal {\n    void eat() { /* базовая реализация */ }\n}\nclass Cat extends Animal {\n    @Override void eat() { System.out.println("Кормим рыбой"); }\n}\nclass Dog extends Animal {\n    @Override void eat() { System.out.println("Кормим мясом"); }\n}\n\nvoid process(Animal a) {\n    a.eat(); // Полиморфизм! Каждый ест по-своему, без instanceof\n}' },
        { type: 'warning', value: 'Используй instanceof только когда ДЕЙСТВИТЕЛЬНО нужен доступ к специфическим методам подкласса, которых нет в родительском классе. Если можно решить через переопределение — используй переопределение.' }
      ]
    },
    {
      id: 7,
      title: 'Полиморфные коллекции',
      type: 'theory',
      content: [
        { type: 'text', value: 'Одна из самых мощных возможностей полиморфизма — работа с коллекциями разнотипных объектов через общий интерфейс. Ты можешь хранить объекты разных классов в одном списке и обрабатывать их единообразно.' },
        { type: 'tip', value: 'Полиморфная коллекция — как коробка Lego. В коробке могут быть кирпичики, колёса, окна, двери — все разные. Но у каждого есть метод "соединить()" который работает по-своему. Ты берёшь каждую деталь из коробки и вызываешь "соединить()" — каждая знает, как ей присоединиться.' },
        { type: 'heading', value: 'Пример: Система фигур' },
        { type: 'code', language: 'java', value: 'import java.util.ArrayList;\nimport java.util.List;\n\n// Базовый класс\nabstract class Shape {\n    String color;\n\n    Shape(String color) { this.color = color; }\n\n    abstract double area();      // Каждая фигура ОБЯЗАНА посчитать площадь\n    abstract double perimeter(); // И периметр\n\n    @Override\n    public String toString() {\n        return String.format("%s %s (S=%.2f, P=%.2f)",\n            color, getClass().getSimpleName(), area(), perimeter());\n    }\n}\n\nclass Circle extends Shape {\n    double radius;\n    Circle(String color, double r) { super(color); this.radius = r; }\n\n    @Override double area()      { return Math.PI * radius * radius; }\n    @Override double perimeter() { return 2 * Math.PI * radius; }\n}\n\nclass Rectangle extends Shape {\n    double width, height;\n    Rectangle(String color, double w, double h) {\n        super(color); this.width = w; this.height = h;\n    }\n\n    @Override double area()      { return width * height; }\n    @Override double perimeter() { return 2 * (width + height); }\n}\n\nclass Triangle extends Shape {\n    double a, b, c;\n    Triangle(String color, double a, double b, double c) {\n        super(color); this.a = a; this.b = b; this.c = c;\n    }\n\n    @Override double area() {\n        double s = (a + b + c) / 2;\n        return Math.sqrt(s * (s - a) * (s - b) * (s - c));\n    }\n\n    @Override double perimeter() { return a + b + c; }\n}' },
        { type: 'heading', value: 'Единообразная обработка коллекции' },
        { type: 'code', language: 'java', value: '// Полиморфная коллекция — разные типы в одном списке!\nList<Shape> shapes = new ArrayList<>();\nshapes.add(new Circle("Красный", 5));\nshapes.add(new Rectangle("Синий", 4, 6));\nshapes.add(new Triangle("Зелёный", 3, 4, 5));\nshapes.add(new Circle("Жёлтый", 3));\nshapes.add(new Rectangle("Белый", 10, 2));\n\n// 1. Вывести все фигуры (полиморфный toString)\nSystem.out.println("=== Все фигуры ===");\nfor (Shape s : shapes) {\n    System.out.println(s); // toString() вызывает area() и perimeter() полиморфно!\n}\n\n// 2. Найти общую площадь\ndouble totalArea = 0;\nfor (Shape s : shapes) {\n    totalArea += s.area(); // Каждая фигура считает по-своему\n}\nSystem.out.println("\\nОбщая площадь: " + String.format("%.2f", totalArea));\n\n// 3. Найти фигуру с максимальной площадью\nShape maxShape = shapes.get(0);\nfor (Shape s : shapes) {\n    if (s.area() > maxShape.area()) {\n        maxShape = s;\n    }\n}\nSystem.out.println("Самая большая: " + maxShape);\n\n// 4. Отфильтровать только круги\nList<Shape> circles = new ArrayList<>();\nfor (Shape s : shapes) {\n    if (s instanceof Circle) {\n        circles.add(s);\n    }\n}\nSystem.out.println("\\nКруги: " + circles.size() + " шт.");' },
        { type: 'heading', value: 'Полиморфная сортировка' },
        { type: 'code', language: 'java', value: 'import java.util.Collections;\nimport java.util.Comparator;\n\n// Сортируем по площади — работает для ЛЮБЫХ фигур!\nCollections.sort(shapes, new Comparator<Shape>() {\n    @Override\n    public int compare(Shape s1, Shape s2) {\n        return Double.compare(s1.area(), s2.area()); // area() — полиморфный!\n    }\n});\n\n// Или с лямбдой (то же самое, короче):\nshapes.sort((s1, s2) -> Double.compare(s1.area(), s2.area()));\n\n// Или ещё короче:\nshapes.sort(Comparator.comparingDouble(Shape::area));\n\nSystem.out.println("\\n=== Отсортированы по площади ===");\nfor (Shape s : shapes) {\n    System.out.println(s);\n}' },
        { type: 'heading', value: 'Добавление нового типа — без изменения существующего кода!' },
        { type: 'text', value: 'Это главное преимущество полиморфных коллекций. Добавляешь новый класс — и он автоматически работает со всем существующим кодом:' },
        { type: 'code', language: 'java', value: '// Через месяц добавляем новую фигуру:\nclass Hexagon extends Shape {\n    double side;\n    Hexagon(String color, double side) { super(color); this.side = side; }\n\n    @Override double area()      { return 3 * Math.sqrt(3) / 2 * side * side; }\n    @Override double perimeter() { return 6 * side; }\n}\n\n// Просто добавляем в коллекцию — ВСЁ работает!\nshapes.add(new Hexagon("Фиолетовый", 4));\n\n// Вывод, сортировка, поиск максимума — всё без изменений!\n// Вот она, сила полиморфизма + коллекций.' },
        { type: 'note', value: 'Принцип Open/Closed (один из принципов SOLID): код должен быть открыт для расширения (добавление новых типов), но закрыт для изменения (существующий код не меняется). Полиморфные коллекции — отличный пример этого принципа.' }
      ]
    },
    {
      id: 8,
      title: 'Абстрактные методы и контракт',
      type: 'theory',
      content: [
        { type: 'text', value: 'Абстрактный метод — это метод без реализации, объявленный с ключевым словом abstract. Он задаёт контракт: "каждый подкласс ОБЯЗАН реализовать этот метод". Если подкласс не реализует абстрактный метод — код не скомпилируется.' },
        { type: 'tip', value: 'Абстрактный метод — как пункт в трудовом договоре. Когда ты устраиваешься на работу, в договоре написано: "Сотрудник обязан выполнять работу()". Но что конкретно ты делаешь — зависит от должности: программист пишет код, дизайнер рисует, менеджер управляет. Контракт один — реализация у каждого своя.' },
        { type: 'heading', value: 'Абстрактный класс vs обычный класс' },
        { type: 'code', language: 'java', value: '// ОБЫЧНЫЙ класс — можно создать объект, есть реализация\nclass Shape {\n    double area() {\n        return 0; // Какая площадь у "просто фигуры"? Бессмысленно!\n    }\n}\nShape s = new Shape(); // Можно создать. Но зачем? Фигура без формы...\n\n// АБСТРАКТНЫЙ класс — нельзя создать объект, есть контракт\nabstract class Shape {\n    abstract double area(); // НЕТ реализации! Подклассы ОБЯЗАНЫ реализовать\n}\n// Shape s = new Shape(); // ОШИБКА КОМПИЛЯЦИИ! Нельзя создать абстрактный объект' },
        { type: 'heading', value: 'Полный пример: система сотрудников' },
        { type: 'code', language: 'java', value: '// Абстрактный класс — базовый контракт для всех сотрудников\npublic abstract class Employee {\n    String name;\n    int id;\n\n    Employee(String name, int id) {\n        this.name = name;\n        this.id = id;\n    }\n\n    // АБСТРАКТНЫЕ методы — каждый тип сотрудника ОБЯЗАН реализовать\n    abstract double calculateSalary();  // Зарплата считается по-разному\n    abstract String getRole();          // У каждого своя должность\n\n    // ОБЫЧНЫЕ методы — общие для всех, можно не переопределять\n    void displayInfo() {\n        System.out.printf("[%d] %s (%s) — %.0f тг%n",\n            id, name, getRole(), calculateSalary());\n    }\n\n    boolean isHighPaid() {\n        return calculateSalary() > 500000; // Использует полиморфный метод!\n    }\n}\n\n// Конкретные реализации\npublic class Developer extends Employee {\n    double baseSalary;\n    int completedTasks;\n\n    Developer(String name, int id, double baseSalary, int completedTasks) {\n        super(name, id);\n        this.baseSalary = baseSalary;\n        this.completedTasks = completedTasks;\n    }\n\n    @Override\n    double calculateSalary() {\n        return baseSalary + completedTasks * 5000; // База + бонус за задачи\n    }\n\n    @Override\n    String getRole() { return "Разработчик"; }\n}\n\npublic class Manager extends Employee {\n    double baseSalary;\n    double teamBonus;\n\n    Manager(String name, int id, double baseSalary, double teamBonus) {\n        super(name, id);\n        this.baseSalary = baseSalary;\n        this.teamBonus = teamBonus;\n    }\n\n    @Override\n    double calculateSalary() {\n        return baseSalary + teamBonus; // База + бонус команды\n    }\n\n    @Override\n    String getRole() { return "Менеджер"; }\n}\n\npublic class Intern extends Employee {\n    double stipend;\n\n    Intern(String name, int id, double stipend) {\n        super(name, id);\n        this.stipend = stipend;\n    }\n\n    @Override\n    double calculateSalary() {\n        return stipend; // Стажёр получает только стипендию\n    }\n\n    @Override\n    String getRole() { return "Стажёр"; }\n}' },
        { type: 'heading', value: 'Использование контракта' },
        { type: 'code', language: 'java', value: '// Полиморфная работа с сотрудниками\nList<Employee> team = List.of(\n    new Developer("Нурдаулет", 1, 400000, 25),\n    new Manager("Айгерим", 2, 350000, 200000),\n    new Developer("Берик", 3, 350000, 15),\n    new Intern("Дана", 4, 100000)\n);\n\n// Выводим информацию — полиморфизм!\nSystem.out.println("=== Команда ===");\nfor (Employee e : team) {\n    e.displayInfo(); // getRole() и calculateSalary() — полиморфные!\n}\n// [1] Нурдаулет (Разработчик) — 525000 тг\n// [2] Айгерим (Менеджер) — 550000 тг\n// [3] Берик (Разработчик) — 425000 тг\n// [4] Дана (Стажёр) — 100000 тг\n\n// Общий фонд зарплаты\ndouble totalSalary = 0;\nfor (Employee e : team) {\n    totalSalary += e.calculateSalary();\n}\nSystem.out.println("\\nФонд зарплаты: " + totalSalary + " тг");\n\n// Кто получает много?\nSystem.out.println("\\nВысокооплачиваемые:");\nfor (Employee e : team) {\n    if (e.isHighPaid()) { // isHighPaid() использует calculateSalary() полиморфно!\n        System.out.println("  " + e.name + " — " + e.calculateSalary() + " тг");\n    }\n}' },
        { type: 'heading', value: 'Что будет, если не реализовать абстрактный метод?' },
        { type: 'code', language: 'java', value: '// Если подкласс НЕ реализует абстрактный метод — ошибка компиляции!\npublic class FreelancerBROKEN extends Employee {\n    FreelancerBROKEN(String name, int id) { super(name, id); }\n\n    @Override\n    String getRole() { return "Фрилансер"; }\n\n    // Забыли реализовать calculateSalary()!\n    // ОШИБКА: FreelancerBROKEN is not abstract and does not override\n    // abstract method calculateSalary() in Employee\n}\n\n// Два варианта исправить:\n// 1. Реализовать метод\npublic class Freelancer extends Employee {\n    double hourlyRate;\n    int hours;\n\n    Freelancer(String name, int id, double rate, int hours) {\n        super(name, id);\n        this.hourlyRate = rate;\n        this.hours = hours;\n    }\n\n    @Override double calculateSalary() { return hourlyRate * hours; }\n    @Override String getRole() { return "Фрилансер"; }\n}\n\n// 2. Сделать класс тоже абстрактным (передать обязанность дальше)\npublic abstract class PartTimeEmployee extends Employee {\n    PartTimeEmployee(String name, int id) { super(name, id); }\n    // calculateSalary() всё ещё абстрактный — реализуют подклассы\n}' },
        { type: 'note', value: 'Абстрактные методы — это гарантия полиморфизма. Они говорят: "Я не знаю как, но КАЖДЫЙ подкласс обязан это уметь." Это мощный инструмент проектирования, который компилятор проверяет за тебя.' }
      ]
    },
    {
      id: 9,
      title: 'Паттерн Strategy через полиморфизм',
      type: 'theory',
      content: [
        { type: 'text', value: 'Strategy (Стратегия) — это паттерн проектирования, который позволяет менять поведение объекта в runtime, подставляя разные реализации через интерфейс. Это один из самых популярных паттернов, и он полностью основан на полиморфизме.' },
        { type: 'tip', value: 'Strategy — как навигатор. Ты едешь из точки А в точку Б. Навигатор может предложить разные стратегии маршрута:\n- Быстрый маршрут (по магистрали)\n- Короткий маршрут (через дворы)\n- Без пробок (объездная)\n\nТы выбираешь стратегию — и навигатор строит маршрут. Стратегию можно менять прямо на ходу!' },
        { type: 'heading', value: 'Проблема без Strategy' },
        { type: 'code', language: 'java', value: '// БЕЗ Strategy — всё захардкожено:\nclass TextProcessor {\n    String formatText(String text, String format) {\n        if (format.equals("uppercase")) {\n            return text.toUpperCase();\n        } else if (format.equals("lowercase")) {\n            return text.toLowerCase();\n        } else if (format.equals("capitalize")) {\n            return Character.toUpperCase(text.charAt(0)) + text.substring(1);\n        }\n        // Добавляешь новый формат? Меняй этот класс!\n        // Нарушение Open/Closed Principle!\n        return text;\n    }\n}' },
        { type: 'heading', value: 'Решение: паттерн Strategy' },
        { type: 'code', language: 'java', value: '// Шаг 1: Определяем интерфейс стратегии\ninterface TextFormatter {\n    String format(String text);\n}\n\n// Шаг 2: Создаём конкретные стратегии\nclass UpperCaseFormatter implements TextFormatter {\n    @Override\n    public String format(String text) {\n        return text.toUpperCase();\n    }\n}\n\nclass LowerCaseFormatter implements TextFormatter {\n    @Override\n    public String format(String text) {\n        return text.toLowerCase();\n    }\n}\n\nclass CapitalizeFormatter implements TextFormatter {\n    @Override\n    public String format(String text) {\n        if (text == null || text.isEmpty()) return text;\n        return Character.toUpperCase(text.charAt(0)) + text.substring(1).toLowerCase();\n    }\n}\n\nclass LeetSpeakFormatter implements TextFormatter {\n    @Override\n    public String format(String text) {\n        return text.replace(\'a\', \'4\').replace(\'e\', \'3\')\n                   .replace(\'o\', \'0\').replace(\'s\', \'5\');\n    }\n}\n\n// Шаг 3: Класс, который ИСПОЛЬЗУЕТ стратегию\nclass TextProcessor {\n    private TextFormatter formatter; // Ссылка на стратегию (интерфейс!)\n\n    TextProcessor(TextFormatter formatter) {\n        this.formatter = formatter;\n    }\n\n    // Можно менять стратегию в runtime!\n    void setFormatter(TextFormatter formatter) {\n        this.formatter = formatter;\n    }\n\n    String process(String text) {\n        return formatter.format(text); // Полиморфный вызов!\n    }\n}\n\n// Использование:\nTextProcessor processor = new TextProcessor(new UpperCaseFormatter());\nSystem.out.println(processor.process("hello world")); // HELLO WORLD\n\nprocessor.setFormatter(new CapitalizeFormatter());\nSystem.out.println(processor.process("hello world")); // Hello world\n\nprocessor.setFormatter(new LeetSpeakFormatter());\nSystem.out.println(processor.process("hello world")); // h3ll0 w0rld' },
        { type: 'heading', value: 'Реальный пример: система скидок' },
        { type: 'code', language: 'java', value: '// Стратегия расчёта скидки\ninterface DiscountStrategy {\n    double calculateDiscount(double price);\n    String getDescription();\n}\n\nclass NoDiscount implements DiscountStrategy {\n    @Override\n    public double calculateDiscount(double price) { return 0; }\n\n    @Override\n    public String getDescription() { return "Без скидки"; }\n}\n\nclass PercentDiscount implements DiscountStrategy {\n    double percent;\n    PercentDiscount(double percent) { this.percent = percent; }\n\n    @Override\n    public double calculateDiscount(double price) {\n        return price * percent / 100;\n    }\n\n    @Override\n    public String getDescription() { return "Скидка " + percent + "%"; }\n}\n\nclass FixedDiscount implements DiscountStrategy {\n    double amount;\n    FixedDiscount(double amount) { this.amount = amount; }\n\n    @Override\n    public double calculateDiscount(double price) {\n        return Math.min(amount, price); // Не больше цены товара\n    }\n\n    @Override\n    public String getDescription() { return "Скидка " + amount + " тг"; }\n}\n\nclass BuyOneGetOneFree implements DiscountStrategy {\n    @Override\n    public double calculateDiscount(double price) {\n        return price / 2; // Второй бесплатно = 50% от пары\n    }\n\n    @Override\n    public String getDescription() { return "Второй товар бесплатно"; }\n}\n\n// Корзина использует стратегию\nclass ShoppingCart {\n    List<Double> prices = new ArrayList<>();\n    DiscountStrategy discount;\n\n    ShoppingCart(DiscountStrategy discount) {\n        this.discount = discount;\n    }\n\n    void addItem(double price) { prices.add(price); }\n\n    void setDiscount(DiscountStrategy discount) {\n        this.discount = discount; // Меняем стратегию!\n    }\n\n    double getTotal() {\n        double total = 0;\n        for (double p : prices) total += p;\n        double disc = discount.calculateDiscount(total);\n        return total - disc;\n    }\n\n    void printReceipt() {\n        double total = 0;\n        for (double p : prices) {\n            System.out.println("  Товар: " + p + " тг");\n            total += p;\n        }\n        double disc = discount.calculateDiscount(total);\n        System.out.println(discount.getDescription() + ": -" + disc + " тг");\n        System.out.println("ИТОГО: " + (total - disc) + " тг");\n    }\n}\n\n// Использование\nShoppingCart cart = new ShoppingCart(new NoDiscount());\ncart.addItem(5000);\ncart.addItem(3000);\ncart.addItem(2000);\n\ncart.printReceipt();\n// Без скидки: -0.0 тг\n// ИТОГО: 10000.0 тг\n\ncart.setDiscount(new PercentDiscount(15)); // Меняем стратегию!\ncart.printReceipt();\n// Скидка 15.0%: -1500.0 тг\n// ИТОГО: 8500.0 тг' },
        { type: 'note', value: 'Strategy — один из самых частых паттернов в Java. В стандартной библиотеке: Comparator (стратегия сортировки), Runnable (стратегия выполнения), EventListener (стратегия обработки событий). В Spring: различные Strategy-бины, которые подставляются через DI.' }
      ]
    },
    {
      id: 10,
      title: 'Реальные примеры полиморфизма в Java',
      type: 'theory',
      content: [
        { type: 'text', value: 'Полиморфизм — это не теоретическая концепция. Весь Java-мир построен на нём. Давай разберём примеры из стандартной библиотеки и реальных фреймворков, чтобы ты увидел: ты уже используешь полиморфизм каждый день!' },
        { type: 'heading', value: '1. Comparator — полиморфная сортировка' },
        { type: 'text', value: 'Collections.sort() не знает, как сортировать твои объекты. Ты передаёшь Comparator — и он полиморфно определяет порядок:' },
        { type: 'code', language: 'java', value: 'class Student {\n    String name;\n    double gpa;\n    int age;\n\n    Student(String name, double gpa, int age) {\n        this.name = name;\n        this.gpa = gpa;\n        this.age = age;\n    }\n\n    @Override\n    public String toString() {\n        return name + " (GPA: " + gpa + ", возраст: " + age + ")";\n    }\n}\n\nList<Student> students = new ArrayList<>(List.of(\n    new Student("Нурдаулет", 3.8, 22),\n    new Student("Айгерим", 3.9, 21),\n    new Student("Берик", 3.5, 23),\n    new Student("Дана", 4.0, 20)\n));\n\n// Стратегия 1: сортировка по GPA\nComparator<Student> byGpa = new Comparator<Student>() {\n    @Override\n    public int compare(Student s1, Student s2) {\n        return Double.compare(s2.gpa, s1.gpa); // По убыванию\n    }\n};\nstudents.sort(byGpa);\nSystem.out.println("По GPA: " + students);\n// [Дана (4.0), Айгерим (3.9), Нурдаулет (3.8), Берик (3.5)]\n\n// Стратегия 2: по возрасту (лямбда — краткая форма)\nstudents.sort((s1, s2) -> Integer.compare(s1.age, s2.age));\nSystem.out.println("По возрасту: " + students);\n\n// Стратегия 3: по имени\nstudents.sort(Comparator.comparing(s -> s.name));\nSystem.out.println("По имени: " + students);\n\n// Один метод sort() — разные стратегии сравнения. Полиморфизм!' },
        { type: 'heading', value: '2. JDBC — полиморфный доступ к базам данных' },
        { type: 'text', value: 'JDBC (Java Database Connectivity) — классический пример полиморфизма. Один интерфейс Connection работает с ЛЮБОЙ базой данных:' },
        { type: 'code', language: 'java', value: 'import java.sql.Connection;\nimport java.sql.DriverManager;\nimport java.sql.ResultSet;\nimport java.sql.Statement;\n\n// Один и тот же код работает с разными базами!\n// Connection — это ИНТЕРФЕЙС\n\n// PostgreSQL:\nConnection pgConn = DriverManager.getConnection(\n    "jdbc:postgresql://localhost/mydb", "user", "pass");\n\n// MySQL:\nConnection mysqlConn = DriverManager.getConnection(\n    "jdbc:mysql://localhost/mydb", "user", "pass");\n\n// SQLite:\nConnection sqliteConn = DriverManager.getConnection(\n    "jdbc:sqlite:mydb.db");\n\n// Один метод работает с ЛЮБОЙ базой данных!\nvoid executeQuery(Connection conn, String sql) {\n    Statement stmt = conn.createStatement();  // Полиморфный вызов!\n    ResultSet rs = stmt.executeQuery(sql);    // Полиморфный вызов!\n    while (rs.next()) {\n        System.out.println(rs.getString(1));\n    }\n}\n\n// Не важно, какая база — код один и тот же!\nexecuteQuery(pgConn, "SELECT name FROM users");\nexecuteQuery(mysqlConn, "SELECT name FROM users");\nexecuteQuery(sqliteConn, "SELECT name FROM users");' },
        { type: 'heading', value: '3. Коллекции — List, Set, Map' },
        { type: 'text', value: 'Когда ты пишешь List<String> вместо ArrayList<String>, ты используешь полиморфизм:' },
        { type: 'code', language: 'java', value: '// Интерфейс List — контракт. Реализация может быть любой:\nList<String> list1 = new ArrayList<>();  // Быстрый доступ по индексу\nList<String> list2 = new LinkedList<>(); // Быстрая вставка/удаление\n\n// Метод работает с ЛЮБОЙ реализацией List\nvoid printAll(List<String> items) {\n    for (String item : items) {\n        System.out.println(item);\n    }\n}\n\nprintAll(list1); // Работает!\nprintAll(list2); // Тоже работает!\n\n// Можно легко поменять реализацию:\n// List<String> list1 = new ArrayList<>();  // Было\n// List<String> list1 = new LinkedList<>(); // Стало\n// Весь остальной код работает без изменений!\n\n// Map — то же самое:\nMap<String, Integer> map1 = new HashMap<>();      // Быстрый, без порядка\nMap<String, Integer> map2 = new TreeMap<>();       // Отсортированный\nMap<String, Integer> map3 = new LinkedHashMap<>(); // Порядок вставки' },
        { type: 'heading', value: '4. Обработка событий (Event Listeners)' },
        { type: 'code', language: 'java', value: '// Интерфейс обработчика события\ninterface EventListener {\n    void onEvent(String event);\n}\n\n// Разные обработчики\nclass LoggingListener implements EventListener {\n    @Override\n    public void onEvent(String event) {\n        System.out.println("[LOG] Событие: " + event);\n    }\n}\n\nclass EmailListener implements EventListener {\n    @Override\n    public void onEvent(String event) {\n        System.out.println("[EMAIL] Отправка уведомления о: " + event);\n    }\n}\n\nclass MetricsListener implements EventListener {\n    @Override\n    public void onEvent(String event) {\n        System.out.println("[METRICS] Записываю метрику: " + event);\n    }\n}\n\n// Система событий\nclass EventManager {\n    List<EventListener> listeners = new ArrayList<>();\n\n    void subscribe(EventListener listener) {\n        listeners.add(listener);\n    }\n\n    void notify(String event) {\n        for (EventListener l : listeners) {\n            l.onEvent(event); // Полиморфный вызов!\n        }\n    }\n}\n\nEventManager manager = new EventManager();\nmanager.subscribe(new LoggingListener());\nmanager.subscribe(new EmailListener());\nmanager.subscribe(new MetricsListener());\n\nmanager.notify("Пользователь зарегистрирован");\n// [LOG] Событие: Пользователь зарегистрирован\n// [EMAIL] Отправка уведомления о: Пользователь зарегистрирован\n// [METRICS] Записываю метрику: Пользователь зарегистрирован' },
        { type: 'heading', value: '5. toString(), equals(), hashCode() — полиморфизм Object' },
        { type: 'code', language: 'java', value: '// Каждый класс в Java наследует от Object\n// System.out.println() вызывает toString() полиморфно!\n\nclass Product {\n    String name;\n    double price;\n\n    Product(String name, double price) {\n        this.name = name;\n        this.price = price;\n    }\n\n    @Override // Переопределяем метод Object!\n    public String toString() {\n        return name + " — " + price + " тг";\n    }\n}\n\nObject obj = new Product("Ноутбук", 350000);\nSystem.out.println(obj); // Ноутбук — 350000.0 тг\n// println() вызывает obj.toString() → полиморфно вызывается Product.toString()!' },
        { type: 'note', value: 'Полиморфизм — это не экзотика. Каждый раз, когда ты вызываешь toString(), сортируешь список, используешь коллекции или обрабатываешь события — ты используешь полиморфизм. Он буквально везде в Java.' }
      ]
    },
    {
      id: 11,
      title: 'Практика: Зоопарк',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создай иерархию животных зоопарка с полиморфными методами. Каждое животное умеет говорить и двигаться по-своему.',
      requirements: [
        'Абстрактный класс Animal: поля name и age, абстрактные методы speak() и move(), обычный метод info() который использует speak() и move()',
        'Класс Dog extends Animal: speak() возвращает "Гав-гав!", move() возвращает "бежит, виляя хвостом"',
        'Класс Cat extends Animal: speak() возвращает "Мяу!", move() возвращает "крадётся бесшумно"',
        'Класс Bird extends Animal: speak() возвращает "Чирик!", move() возвращает "летит, расправив крылья", уникальный метод fly(int meters)',
        'Создай List<Animal> из 5+ животных разных типов',
        'Выведи info() для каждого животного в цикле',
        'Найди самое старое животное',
        'Для Bird вызови fly() используя instanceof'
      ],
      expectedOutput: 'Рекс (3 года): говорит "Гав-гав!", бежит, виляя хвостом\nМурзик (5 лет): говорит "Мяу!", крадётся бесшумно\nКеша (2 года): говорит "Чирик!", летит, расправив крылья\nБобик (7 лет): говорит "Гав-гав!", бежит, виляя хвостом\nКися (4 года): говорит "Мяу!", крадётся бесшумно\nПопугай (1 год): говорит "Чирик!", летит, расправив крылья\n\nСамое старое: Бобик (7 лет)\n\nПтицы летают:\nКеша пролетел 100 метров!\nПопугай пролетел 50 метров!',
      hint: 'Используй абстрактный класс Animal с abstract методами speak() и move(). Метод info() определи в Animal — он будет вызывать speak() и move() полиморфно. Для возраста используй тернарный оператор для склонения слова "год/года/лет".',
      solution: 'import java.util.ArrayList;\nimport java.util.List;\n\npublic abstract class Animal {\n    String name;\n    int age;\n\n    Animal(String name, int age) {\n        this.name = name;\n        this.age = age;\n    }\n\n    abstract String speak();\n    abstract String move();\n\n    void info() {\n        String ageWord;\n        if (age % 10 == 1 && age != 11) ageWord = "год";\n        else if (age % 10 >= 2 && age % 10 <= 4 && (age < 12 || age > 14)) ageWord = "года";\n        else ageWord = "лет";\n        System.out.println(name + " (" + age + " " + ageWord + "): говорит \\\"" + speak() + "\\\", " + move());\n    }\n}\n\npublic class Dog extends Animal {\n    Dog(String name, int age) { super(name, age); }\n\n    @Override String speak() { return "Гав-гав!"; }\n    @Override String move()  { return "бежит, виляя хвостом"; }\n}\n\npublic class Cat extends Animal {\n    Cat(String name, int age) { super(name, age); }\n\n    @Override String speak() { return "Мяу!"; }\n    @Override String move()  { return "крадётся бесшумно"; }\n}\n\npublic class Bird extends Animal {\n    Bird(String name, int age) { super(name, age); }\n\n    @Override String speak() { return "Чирик!"; }\n    @Override String move()  { return "летит, расправив крылья"; }\n\n    void fly(int meters) {\n        System.out.println(name + " пролетел " + meters + " метров!");\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Animal> zoo = new ArrayList<>();\n        zoo.add(new Dog("Рекс", 3));\n        zoo.add(new Cat("Мурзик", 5));\n        zoo.add(new Bird("Кеша", 2));\n        zoo.add(new Dog("Бобик", 7));\n        zoo.add(new Cat("Кися", 4));\n        zoo.add(new Bird("Попугай", 1));\n\n        // Полиморфный вывод\n        for (Animal a : zoo) {\n            a.info();\n        }\n\n        // Самое старое\n        Animal oldest = zoo.get(0);\n        for (Animal a : zoo) {\n            if (a.age > oldest.age) oldest = a;\n        }\n        System.out.println("\\nСамое старое: " + oldest.name + " (" + oldest.age + " лет)");\n\n        // Птицы летают (instanceof)\n        System.out.println("\\nПтицы летают:");\n        for (Animal a : zoo) {\n            if (a instanceof Bird) {\n                Bird bird = (Bird) a;\n                bird.fly(a.name.equals("Кеша") ? 100 : 50);\n            }\n        }\n    }\n}',
      explanation: 'Этот пример показывает все ключевые аспекты полиморфизма:\n1. Абстрактный класс Animal задаёт контракт (speak, move)\n2. Каждый подкласс реализует контракт по-своему\n3. Метод info() в родителе вызывает speak() и move() полиморфно\n4. List<Animal> хранит разные типы — полиморфная коллекция\n5. instanceof + downcasting для вызова специфических методов (fly)'
    },
    {
      id: 12,
      title: 'Практика: Платёжная система',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создай систему обработки платежей с разными способами оплаты. Используй полиморфизм для единообразной обработки и паттерн Strategy для комиссий.',
      requirements: [
        'Абстрактный класс PaymentMethod: поля ownerName и balance, абстрактные методы validate(), processPayment(double amount), getMethodName()',
        'Класс CardPayment extends PaymentMethod: поле cardNumber (16 цифр), validate() проверяет длину карты и баланс, processPayment() снимает деньги + комиссия 2%',
        'Класс CashPayment extends PaymentMethod: validate() проверяет что сумма <= balance, processPayment() просто снимает деньги без комиссии',
        'Класс CryptoPayment extends PaymentMethod: поле walletAddress, validate() проверяет что адрес начинается с "0x", processPayment() снимает деньги + комиссия 1.5% + конвертация',
        'Класс PaymentProcessor: метод process(PaymentMethod method, double amount) — валидирует и обрабатывает платёж полиморфно',
        'Класс PaymentProcessor: метод processAll(List<PaymentMethod> methods, double[] amounts) — обрабатывает массив платежей',
        'Выведи итоговый отчёт: сколько успешных, сколько неудачных, общая сумма'
      ],
      expectedOutput: 'Обработка платежей:\n[Карта] Нурдаулет: 15000.00 тг + комиссия 300.00 тг = 15300.00 тг ✓\n[Наличные] Айгерим: 5000.00 тг (без комиссии) ✓\n[Крипто] Берик: 25000.00 тг + комиссия 375.00 тг = 25375.00 тг ✓\n[Карта] Дана: ОШИБКА — недостаточно средств\n[Крипто] Ержан: ОШИБКА — невалидный кошелёк\n\nИтого: 3 успешных, 2 неудачных, сумма: 45675.00 тг',
      hint: 'В validate() возвращай boolean. В processPayment() снимай деньги с баланса и выводи результат. PaymentProcessor не должен знать о конкретных типах — только вызывает validate() и processPayment() полиморфно.',
      solution: 'import java.util.ArrayList;\nimport java.util.List;\n\npublic abstract class PaymentMethod {\n    String ownerName;\n    double balance;\n\n    PaymentMethod(String ownerName, double balance) {\n        this.ownerName = ownerName;\n        this.balance = balance;\n    }\n\n    abstract boolean validate(double amount);\n    abstract String processPayment(double amount); // Возвращает описание\n    abstract String getMethodName();\n\n    String getErrorMessage(double amount) {\n        return "Ошибка валидации";\n    }\n}\n\npublic class CardPayment extends PaymentMethod {\n    String cardNumber;\n    static final double COMMISSION = 0.02; // 2%\n\n    CardPayment(String ownerName, double balance, String cardNumber) {\n        super(ownerName, balance);\n        this.cardNumber = cardNumber;\n    }\n\n    @Override\n    boolean validate(double amount) {\n        double total = amount + amount * COMMISSION;\n        return cardNumber.length() == 16 && balance >= total;\n    }\n\n    @Override\n    String processPayment(double amount) {\n        double commission = amount * COMMISSION;\n        double total = amount + commission;\n        balance -= total;\n        return String.format("%.2f тг + комиссия %.2f тг = %.2f тг", amount, commission, total);\n    }\n\n    @Override\n    String getMethodName() { return "Карта"; }\n\n    @Override\n    String getErrorMessage(double amount) {\n        if (cardNumber.length() != 16) return "невалидный номер карты";\n        return "недостаточно средств";\n    }\n}\n\npublic class CashPayment extends PaymentMethod {\n    CashPayment(String ownerName, double balance) {\n        super(ownerName, balance);\n    }\n\n    @Override\n    boolean validate(double amount) {\n        return amount > 0 && balance >= amount;\n    }\n\n    @Override\n    String processPayment(double amount) {\n        balance -= amount;\n        return String.format("%.2f тг (без комиссии)", amount);\n    }\n\n    @Override\n    String getMethodName() { return "Наличные"; }\n\n    @Override\n    String getErrorMessage(double amount) {\n        return "недостаточно наличных";\n    }\n}\n\npublic class CryptoPayment extends PaymentMethod {\n    String walletAddress;\n    static final double COMMISSION = 0.015; // 1.5%\n\n    CryptoPayment(String ownerName, double balance, String walletAddress) {\n        super(ownerName, balance);\n        this.walletAddress = walletAddress;\n    }\n\n    @Override\n    boolean validate(double amount) {\n        double total = amount + amount * COMMISSION;\n        return walletAddress.startsWith("0x") && balance >= total;\n    }\n\n    @Override\n    String processPayment(double amount) {\n        double commission = amount * COMMISSION;\n        double total = amount + commission;\n        balance -= total;\n        return String.format("%.2f тг + комиссия %.2f тг = %.2f тг", amount, commission, total);\n    }\n\n    @Override\n    String getMethodName() { return "Крипто"; }\n\n    @Override\n    String getErrorMessage(double amount) {\n        if (!walletAddress.startsWith("0x")) return "невалидный кошелёк";\n        return "недостаточно средств";\n    }\n}\n\npublic class PaymentProcessor {\n    int successful = 0;\n    int failed = 0;\n    double totalProcessed = 0;\n\n    boolean process(PaymentMethod method, double amount) {\n        System.out.print("[" + method.getMethodName() + "] " + method.ownerName + ": ");\n        if (method.validate(amount)) {\n            String result = method.processPayment(amount);\n            System.out.println(result + " \\u2713");\n            successful++;\n            totalProcessed += amount;\n            return true;\n        } else {\n            System.out.println("ОШИБКА — " + method.getErrorMessage(amount));\n            failed++;\n            return false;\n        }\n    }\n\n    void processAll(List<PaymentMethod> methods, double[] amounts) {\n        System.out.println("Обработка платежей:");\n        for (int i = 0; i < methods.size(); i++) {\n            process(methods.get(i), amounts[i]);\n        }\n        printReport();\n    }\n\n    void printReport() {\n        System.out.printf("\\nИтого: %d успешных, %d неудачных, сумма: %.2f тг%n",\n            successful, failed, totalProcessed);\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        List<PaymentMethod> methods = new ArrayList<>();\n        methods.add(new CardPayment("Нурдаулет", 50000, "1234567890123456"));\n        methods.add(new CashPayment("Айгерим", 10000));\n        methods.add(new CryptoPayment("Берик", 100000, "0xABCDEF1234"));\n        methods.add(new CardPayment("Дана", 1000, "1234567890123456"));\n        methods.add(new CryptoPayment("Ержан", 50000, "INVALID"));\n\n        double[] amounts = {15000, 5000, 25000, 15000, 10000};\n\n        PaymentProcessor processor = new PaymentProcessor();\n        processor.processAll(methods, amounts);\n    }\n}',
      explanation: 'Эта задача демонстрирует полиморфизм в реальном сценарии:\n1. Абстрактный класс PaymentMethod задаёт контракт: validate(), processPayment(), getMethodName()\n2. Каждый способ оплаты реализует контракт по-своему: карта берёт 2% комиссии, наличные — без комиссии, крипто — 1.5%\n3. PaymentProcessor НЕ ЗНАЕТ о конкретных типах — работает только через абстракцию PaymentMethod\n4. Добавить новый способ оплаты (например, QR-код) можно создав новый класс — без изменения PaymentProcessor!'
    }
  ]
}
