export default {
  id: 21,
  title: 'State',
  description: 'Паттерн State: конечные автоматы, изменение поведения объекта в зависимости от состояния',
  lessons: [
    {
      id: 1,
      title: 'Что такое State?',
      type: 'theory',
      content: [
        { type: 'text', value: 'State (Состояние) — поведенческий паттерн, который позволяет объекту менять своё поведение при изменении внутреннего состояния. Объект как будто меняет свой класс.' },
        { type: 'heading', value: 'Проблема без State' },
        { type: 'code', language: 'java', value: '// ❌ Без State — огромный switch в каждом методе\nclass Order {\n    String status; // "new", "paid", "shipped", "delivered"\n\n    void pay() {\n        if ("new".equals(status)) { status = "paid"; }\n        else if ("paid".equals(status)) { throw new Error("Уже оплачен"); }\n        else if ("shipped".equals(status)) { throw new Error("Уже отправлен"); }\n    }\n\n    void ship() {\n        if ("new".equals(status)) { throw new Error("Сначала оплатите"); }\n        else if ("paid".equals(status)) { status = "shipped"; }\n        // ... и так далее для каждого метода\n    }\n}' },
        { type: 'heading', value: 'Когда использовать?' },
        { type: 'list', items: [
          'Объект ведёт себя по-разному в зависимости от состояния',
          'Множество условных операторов (if/switch) проверяющих состояние',
          'Заказы (new→paid→shipped→delivered), документы, игровые персонажи',
          'Сетевые соединения, UI-компоненты, workflow-движки'
        ]},
        { type: 'note', value: 'State vs Strategy: оба заменяют поведение. Но State переключается автоматически (по внутренней логике), а Strategy устанавливается клиентом извне.' }
      ]
    },
    {
      id: 2,
      title: 'Реализация на Java: заказ',
      type: 'theory',
      content: [
        { type: 'code', language: 'java', value: '// Интерфейс состояния\ninterface OrderState {\n    void pay(OrderContext order);\n    void ship(OrderContext order);\n    void deliver(OrderContext order);\n    void cancel(OrderContext order);\n    String getStatus();\n}\n\n// Контекст\nclass OrderContext {\n    private OrderState state;\n\n    OrderContext() { this.state = new NewOrderState(); }\n\n    void setState(OrderState state) {\n        System.out.println("  Статус: " + this.state.getStatus() + " → " + state.getStatus());\n        this.state = state;\n    }\n\n    void pay() { state.pay(this); }\n    void ship() { state.ship(this); }\n    void deliver() { state.deliver(this); }\n    void cancel() { state.cancel(this); }\n    String getStatus() { return state.getStatus(); }\n}\n\nclass NewOrderState implements OrderState {\n    public void pay(OrderContext o) {\n        System.out.println("💳 Оплата принята");\n        o.setState(new PaidOrderState());\n    }\n    public void ship(OrderContext o) { System.out.println("❌ Сначала оплатите"); }\n    public void deliver(OrderContext o) { System.out.println("❌ Невозможно"); }\n    public void cancel(OrderContext o) {\n        System.out.println("🚫 Заказ отменён");\n        o.setState(new CancelledOrderState());\n    }\n    public String getStatus() { return "НОВЫЙ"; }\n}\n\nclass PaidOrderState implements OrderState {\n    public void pay(OrderContext o) { System.out.println("❌ Уже оплачен"); }\n    public void ship(OrderContext o) {\n        System.out.println("📦 Заказ отправлен");\n        o.setState(new ShippedOrderState());\n    }\n    public void deliver(OrderContext o) { System.out.println("❌ Сначала отправьте"); }\n    public void cancel(OrderContext o) {\n        System.out.println("💰 Возврат средств, заказ отменён");\n        o.setState(new CancelledOrderState());\n    }\n    public String getStatus() { return "ОПЛАЧЕН"; }\n}\n\nclass ShippedOrderState implements OrderState {\n    public void pay(OrderContext o) { System.out.println("❌ Уже оплачен"); }\n    public void ship(OrderContext o) { System.out.println("❌ Уже отправлен"); }\n    public void deliver(OrderContext o) {\n        System.out.println("✅ Заказ доставлен");\n        o.setState(new DeliveredOrderState());\n    }\n    public void cancel(OrderContext o) { System.out.println("❌ Нельзя отменить — в пути"); }\n    public String getStatus() { return "ОТПРАВЛЕН"; }\n}\n\nclass DeliveredOrderState implements OrderState {\n    public void pay(OrderContext o) { System.out.println("❌ Уже завершён"); }\n    public void ship(OrderContext o) { System.out.println("❌ Уже завершён"); }\n    public void deliver(OrderContext o) { System.out.println("❌ Уже доставлен"); }\n    public void cancel(OrderContext o) { System.out.println("❌ Нельзя отменить — доставлен"); }\n    public String getStatus() { return "ДОСТАВЛЕН"; }\n}\n\nclass CancelledOrderState implements OrderState {\n    public void pay(OrderContext o) { System.out.println("❌ Заказ отменён"); }\n    public void ship(OrderContext o) { System.out.println("❌ Заказ отменён"); }\n    public void deliver(OrderContext o) { System.out.println("❌ Заказ отменён"); }\n    public void cancel(OrderContext o) { System.out.println("❌ Уже отменён"); }\n    public String getStatus() { return "ОТМЕНЁН"; }\n}\n\n// Использование\nOrderContext order = new OrderContext();\norder.ship();    // ❌ Сначала оплатите\norder.pay();     // ✅ НОВЫЙ → ОПЛАЧЕН\norder.ship();    // ✅ ОПЛАЧЕН → ОТПРАВЛЕН\norder.cancel();  // ❌ Нельзя отменить — в пути\norder.deliver(); // ✅ ОТПРАВЛЕН → ДОСТАВЛЕН' },
        { type: 'tip', value: 'Каждое состояние — отдельный класс с собственной логикой. Добавить новое состояние (например, «Возврат») — создать один класс, не трогая остальные.' }
      ]
    },
    {
      id: 3,
      title: 'State на TypeScript: медиаплеер',
      type: 'theory',
      content: [
        { type: 'code', language: 'typescript', value: 'interface PlayerState {\n    play(player: MusicPlayer): void;\n    pause(player: MusicPlayer): void;\n    stop(player: MusicPlayer): void;\n    getName(): string;\n}\n\nclass StoppedState implements PlayerState {\n    play(player: MusicPlayer) {\n        console.log("▶️ Воспроизведение");\n        player.setState(new PlayingState());\n    }\n    pause(player: MusicPlayer) { console.log("⛔ Невозможно — остановлен"); }\n    stop(player: MusicPlayer) { console.log("⛔ Уже остановлен"); }\n    getName() { return "STOPPED"; }\n}\n\nclass PlayingState implements PlayerState {\n    play(player: MusicPlayer) { console.log("⛔ Уже воспроизводится"); }\n    pause(player: MusicPlayer) {\n        console.log("⏸️ Пауза");\n        player.setState(new PausedState());\n    }\n    stop(player: MusicPlayer) {\n        console.log("⏹️ Остановка");\n        player.setState(new StoppedState());\n    }\n    getName() { return "PLAYING"; }\n}\n\nclass PausedState implements PlayerState {\n    play(player: MusicPlayer) {\n        console.log("▶️ Продолжение воспроизведения");\n        player.setState(new PlayingState());\n    }\n    pause(player: MusicPlayer) { console.log("⛔ Уже на паузе"); }\n    stop(player: MusicPlayer) {\n        console.log("⏹️ Остановка");\n        player.setState(new StoppedState());\n    }\n    getName() { return "PAUSED"; }\n}\n\nclass MusicPlayer {\n    private state: PlayerState = new StoppedState();\n\n    setState(state: PlayerState) {\n        console.log(`  [${this.state.getName()} → ${state.getName()}]`);\n        this.state = state;\n    }\n\n    play() { this.state.play(this); }\n    pause() { this.state.pause(this); }\n    stop() { this.state.stop(this); }\n}\n\nconst player = new MusicPlayer();\nplayer.play();   // ▶️ STOPPED → PLAYING\nplayer.pause();  // ⏸️ PLAYING → PAUSED\nplayer.play();   // ▶️ PAUSED → PLAYING\nplayer.stop();   // ⏹️ PLAYING → STOPPED' },
        { type: 'note', value: 'State паттерн — реализация конечного автомата (FSM). Библиотеки XState (JS), Spring State Machine (Java) предоставляют мощные FSM-фреймворки.' }
      ]
    },
    {
      id: 4,
      title: 'Практика: торговый автомат',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте конечный автомат для торгового автомата с использованием State.',
      requirements: [
        'Состояния: Idle (ожидание), HasMoney (деньги внесены), Dispensing (выдача), SoldOut (нет товара)',
        'Действия: insertMoney(amount), selectProduct(name), dispense(), refund()',
        'При внесении денег → HasMoney. При выборе товара → проверка цены → Dispensing',
        'При выдаче → Idle (или SoldOut если товар кончился)',
        'refund() возвращает деньги и переходит в Idle'
      ],
      hint: 'Каждое состояние определяет, какие действия возможны. Например, в Idle нельзя выбрать товар — сначала внесите деньги.',
      expectedOutput: 'Вставлено: 100 тг (баланс: 100)\nВставлено: 50 тг (баланс: 150)\nВыбран: Кола (цена: 120)\nВыдача: Кола (сдача: 30 тг)\nАвтомат готов к следующему покупателю',
      solution: 'interface VendingState {\n    insertMoney(ctx: VendingMachine, amount: number): void;\n    selectProduct(ctx: VendingMachine, product: string): void;\n    dispense(ctx: VendingMachine): void;\n    refund(ctx: VendingMachine): void;\n}\n\nclass VendingMachine {\n    state: VendingState = new IdleState();\n    balance = 0;\n    products = new Map<string, { price: number; qty: number }>([\n        ["Кола", { price: 120, qty: 3 }],\n        ["Вода", { price: 80, qty: 5 }]\n    ]);\n    selectedProduct = "";\n\n    setState(s: VendingState) { this.state = s; }\n    insertMoney(a: number) { this.state.insertMoney(this, a); }\n    selectProduct(p: string) { this.state.selectProduct(this, p); }\n    dispense() { this.state.dispense(this); }\n    refund() { this.state.refund(this); }\n}\n\nclass IdleState implements VendingState {\n    insertMoney(ctx: VendingMachine, amount: number) {\n        ctx.balance += amount;\n        console.log(`Вставлено: ${amount} тг (баланс: ${ctx.balance})`);\n        ctx.setState(new HasMoneyState());\n    }\n    selectProduct(ctx: VendingMachine) { console.log("Сначала внесите деньги"); }\n    dispense(ctx: VendingMachine) { console.log("Сначала выберите товар"); }\n    refund(ctx: VendingMachine) { console.log("Нечего возвращать"); }\n}\n\nclass HasMoneyState implements VendingState {\n    insertMoney(ctx: VendingMachine, amount: number) {\n        ctx.balance += amount;\n        console.log(`Вставлено: ${amount} тг (баланс: ${ctx.balance})`);\n    }\n    selectProduct(ctx: VendingMachine, product: string) {\n        const item = ctx.products.get(product);\n        if (!item || item.qty === 0) { console.log("Товар отсутствует"); return; }\n        if (ctx.balance < item.price) { console.log(`Недостаточно: нужно ${item.price}, есть ${ctx.balance}`); return; }\n        ctx.selectedProduct = product;\n        console.log(`Выбран: ${product} (цена: ${item.price})`);\n        ctx.setState(new DispensingState());\n        ctx.dispense();\n    }\n    dispense(ctx: VendingMachine) { console.log("Сначала выберите товар"); }\n    refund(ctx: VendingMachine) {\n        console.log(`Возврат: ${ctx.balance} тг`);\n        ctx.balance = 0;\n        ctx.setState(new IdleState());\n    }\n}\n\nclass DispensingState implements VendingState {\n    insertMoney() { console.log("Подождите..."); }\n    selectProduct() { console.log("Подождите..."); }\n    dispense(ctx: VendingMachine) {\n        const item = ctx.products.get(ctx.selectedProduct)!;\n        item.qty--;\n        const change = ctx.balance - item.price;\n        console.log(`Выдача: ${ctx.selectedProduct} (сдача: ${change} тг)`);\n        ctx.balance = 0;\n        ctx.selectedProduct = "";\n        ctx.setState(new IdleState());\n        console.log("Автомат готов к следующему покупателю");\n    }\n    refund() { console.log("Подождите..."); }\n}\n\nconst vm = new VendingMachine();\nvm.insertMoney(100);\nvm.insertMoney(50);\nvm.selectProduct("Кола");',
      explanation: 'Каждое состояние определяет допустимые действия и переходы. IdleState принимает деньги, HasMoneyState — выбор товара, DispensingState — выдачу. Невозможные действия обрабатываются корректно в каждом состоянии.'
    },
    {
      id: 5,
      title: 'Практика: TCP-соединение на TypeScript',
      type: 'practice',
      difficulty: 'hard',
      description: 'Моделируйте конечный автомат TCP-соединения: CLOSED → LISTEN → ESTABLISHED → CLOSED.',
      requirements: [
        'Состояния: Closed, Listen, SynSent, Established, CloseWait',
        'Действия: open(), listen(), send(data), close()',
        'Логирование каждого перехода состояний',
        'send() работает только в Established',
        'close() из любого состояния переводит в Closed'
      ],
      hint: 'Каждое состояние — класс, реализующий интерфейс TCPState. Переходы: open() → SynSent → Established, listen() → Listen → Established.',
      expectedOutput: '[CLOSED] → open() → [SYN_SENT]\n[SYN_SENT] → ACK received → [ESTABLISHED]\n[ESTABLISHED] → send("Hello") → Данные отправлены\n[ESTABLISHED] → close() → [CLOSE_WAIT]\n[CLOSE_WAIT] → timeout → [CLOSED]',
      solution: 'interface TCPState {\n    open(ctx: TCPConnection): void;\n    listen(ctx: TCPConnection): void;\n    send(ctx: TCPConnection, data: string): void;\n    close(ctx: TCPConnection): void;\n    getName(): string;\n}\n\nclass TCPConnection {\n    private state: TCPState = new ClosedState();\n\n    setState(s: TCPState) {\n        console.log(`[${this.state.getName()}] → [${s.getName()}]`);\n        this.state = s;\n    }\n\n    open() { this.state.open(this); }\n    listen() { this.state.listen(this); }\n    send(data: string) { this.state.send(this, data); }\n    close() { this.state.close(this); }\n}\n\nclass ClosedState implements TCPState {\n    getName() { return "CLOSED"; }\n    open(ctx: TCPConnection) {\n        console.log("[CLOSED] → open() — отправка SYN");\n        ctx.setState(new SynSentState());\n    }\n    listen(ctx: TCPConnection) {\n        console.log("[CLOSED] → listen()");\n        ctx.setState(new ListenState());\n    }\n    send(ctx: TCPConnection) { console.log("❌ Соединение закрыто"); }\n    close(ctx: TCPConnection) { console.log("Уже закрыто"); }\n}\n\nclass SynSentState implements TCPState {\n    getName() { return "SYN_SENT"; }\n    open(ctx: TCPConnection) { console.log("Уже подключаемся"); }\n    listen(ctx: TCPConnection) { console.log("❌ Невозможно"); }\n    send(ctx: TCPConnection) { console.log("❌ Ещё не установлено"); }\n    close(ctx: TCPConnection) { ctx.setState(new ClosedState()); }\n}\n\nclass ListenState implements TCPState {\n    getName() { return "LISTEN"; }\n    open(ctx: TCPConnection) { console.log("❌ Слушаем, не подключаемся"); }\n    listen(ctx: TCPConnection) { console.log("Уже слушаем"); }\n    send(ctx: TCPConnection) {\n        console.log("[LISTEN] → SYN получен, отправка SYN+ACK");\n        ctx.setState(new EstablishedState());\n    }\n    close(ctx: TCPConnection) { ctx.setState(new ClosedState()); }\n}\n\nclass EstablishedState implements TCPState {\n    getName() { return "ESTABLISHED"; }\n    open(ctx: TCPConnection) { console.log("Уже установлено"); }\n    listen(ctx: TCPConnection) { console.log("❌ Невозможно"); }\n    send(ctx: TCPConnection, data: string) {\n        console.log(`[ESTABLISHED] → send("${data}") → Данные отправлены`);\n    }\n    close(ctx: TCPConnection) {\n        console.log("[ESTABLISHED] → close() — отправка FIN");\n        ctx.setState(new ClosedState());\n    }\n}\n\nconst conn = new TCPConnection();\nconn.send("test");  // ❌ Закрыто\nconn.open();        // CLOSED → SYN_SENT\nconn.close();       // SYN_SENT → CLOSED\nconn.open();        // CLOSED → SYN_SENT\n// Имитация ACK\nconn.setState(new EstablishedState());\nconn.send("Hello"); // Данные отправлены\nconn.close();       // ESTABLISHED → CLOSED',
      explanation: 'TCP-соединение — классический конечный автомат. Каждое состояние определяет допустимые операции. send() работает только в ESTABLISHED. close() доступен из любого состояния. Это точная модель TCP state machine.'
    },
    {
      id: 6,
      title: 'Практика: документный workflow',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте State для workflow документа: Draft → Review → Approved/Rejected → Published.',
      requirements: [
        'Состояния: Draft, UnderReview, Approved, Rejected, Published',
        'Действия: edit(), submitForReview(), approve(), reject(), publish()',
        'edit() доступен только в Draft и Rejected',
        'approve()/reject() — только в UnderReview',
        'publish() — только в Approved'
      ],
      hint: 'Rejected → edit() → Draft (документ возвращается на доработку). Approved → publish() → Published (финальное состояние).',
      expectedOutput: 'Документ: ЧЕРНОВИК\nedit() → Документ отредактирован\nsubmit() → ЧЕРНОВИК → НА РАССМОТРЕНИИ\nreject("Нужны правки") → НА РАССМОТРЕНИИ → ОТКЛОНЁН\nedit() → ОТКЛОНЁН → ЧЕРНОВИК\nsubmit() → ЧЕРНОВИК → НА РАССМОТРЕНИИ\napprove() → НА РАССМОТРЕНИИ → ОДОБРЕН\npublish() → ОДОБРЕН → ОПУБЛИКОВАН',
      solution: 'interface DocState {\n    edit(ctx: DocumentWF): void;\n    submit(ctx: DocumentWF): void;\n    approve(ctx: DocumentWF): void;\n    reject(ctx: DocumentWF, reason: string): void;\n    publish(ctx: DocumentWF): void;\n    name(): string;\n}\n\nclass DocumentWF {\n    state: DocState = new DraftState();\n    setState(s: DocState) {\n        console.log(`${this.state.name()} → ${s.name()}`);\n        this.state = s;\n    }\n    edit() { this.state.edit(this); }\n    submit() { this.state.submit(this); }\n    approve() { this.state.approve(this); }\n    reject(r: string) { this.state.reject(this, r); }\n    publish() { this.state.publish(this); }\n}\n\nconst no = (action: string, state: string) => console.log(`❌ ${action} недоступно в "${state}"`);\n\nclass DraftState implements DocState {\n    name() { return "ЧЕРНОВИК"; }\n    edit(ctx: DocumentWF) { console.log("edit() → Документ отредактирован"); }\n    submit(ctx: DocumentWF) { ctx.setState(new ReviewState()); }\n    approve() { no("approve", this.name()); }\n    reject() { no("reject", this.name()); }\n    publish() { no("publish", this.name()); }\n}\n\nclass ReviewState implements DocState {\n    name() { return "НА РАССМОТРЕНИИ"; }\n    edit() { no("edit", this.name()); }\n    submit() { no("submit", this.name()); }\n    approve(ctx: DocumentWF) { ctx.setState(new ApprovedState()); }\n    reject(ctx: DocumentWF, reason: string) {\n        console.log(`reject("${reason}")`);\n        ctx.setState(new RejectedState());\n    }\n    publish() { no("publish", this.name()); }\n}\n\nclass ApprovedState implements DocState {\n    name() { return "ОДОБРЕН"; }\n    edit() { no("edit", this.name()); }\n    submit() { no("submit", this.name()); }\n    approve() { no("approve", this.name()); }\n    reject() { no("reject", this.name()); }\n    publish(ctx: DocumentWF) { ctx.setState(new PublishedState()); }\n}\n\nclass RejectedState implements DocState {\n    name() { return "ОТКЛОНЁН"; }\n    edit(ctx: DocumentWF) { ctx.setState(new DraftState()); }\n    submit() { no("submit", this.name()); }\n    approve() { no("approve", this.name()); }\n    reject() { no("reject", this.name()); }\n    publish() { no("publish", this.name()); }\n}\n\nclass PublishedState implements DocState {\n    name() { return "ОПУБЛИКОВАН"; }\n    edit() { no("edit", this.name()); }\n    submit() { no("submit", this.name()); }\n    approve() { no("approve", this.name()); }\n    reject() { no("reject", this.name()); }\n    publish() { no("publish", this.name()); }\n}\n\nconst doc = new DocumentWF();\nconsole.log("Документ: " + doc.state.name());\ndoc.edit();\ndoc.submit();\ndoc.reject("Нужны правки");\ndoc.edit();\ndoc.submit();\ndoc.approve();\ndoc.publish();',
      explanation: 'Workflow документа — классический FSM. Rejected → edit() → Draft позволяет доработку. Каждое состояние чётко определяет допустимые переходы. Невозможные действия блокируются с понятным сообщением.'
    }
  ]
}
