export default {
  id: 3,
  title: 'Операторы',
  description: 'Арифметические, сравнения, логические, побитовые операторы, операторы присваивания и приоритет операций.',
  lessons: [
    {
      id: 1,
      title: 'Арифметические операторы',
      type: 'theory',
      content: [
        {
          type: 'heading',
          value: 'Математические операции в Python'
        },
        {
          type: 'text',
          value: 'Python поддерживает все стандартные арифметические операции. Важно помнить, что деление / всегда возвращает float, а // — целочисленное деление. Оператор ** используется для возведения в степень.'
        },
        {
          type: 'code',
          language: 'python',
          value: 'a = 17\nb = 5\n\nprint(a + b)   # 22  — сложение\nprint(a - b)   # 12  — вычитание\nprint(a * b)   # 85  — умножение\nprint(a / b)   # 3.4 — деление (всегда float)\nprint(a // b)  # 3   — целочисленное деление\nprint(a % b)   # 2   — остаток от деления (modulo)\nprint(a ** b)  # 1419857 — возведение в степень\n\n# Унарные операторы\nx = 10\nprint(-x)   # -10\nprint(+x)   # 10\nprint(-(-x)) # 10\n\n# Приоритет: ** > * / // % > + -\nprint(2 + 3 * 4)    # 14 (не 20!)\nprint(2 ** 3 ** 2)  # 512 (3**2=9, 2**9=512, правая ассоциативность)\nprint((2 + 3) * 4)  # 20 — скобки меняют приоритет'
        },
        {
          type: 'tip',
          value: 'Встроенная функция divmod(a, b) возвращает кортеж (частное, остаток) за один вызов: divmod(17, 5) → (3, 2). Полезно, когда нужны оба результата.'
        }
      ]
    },
    {
      id: 2,
      title: 'Операторы сравнения',
      type: 'theory',
      content: [
        {
          type: 'heading',
          value: 'Сравнение значений'
        },
        {
          type: 'text',
          value: 'Операторы сравнения возвращают True или False. В Python можно записывать цепочки сравнений: 1 < x < 10 означает "x больше 1 И меньше 10". Это элегантно и читаемо.'
        },
        {
          type: 'code',
          language: 'python',
          value: 'a, b = 5, 10\n\nprint(a == b)  # False — равно\nprint(a != b)  # True  — не равно\nprint(a < b)   # True  — меньше\nprint(a > b)   # False — больше\nprint(a <= b)  # True  — меньше или равно\nprint(a >= b)  # False — больше или равно\n\n# Цепочки сравнений\nx = 7\nprint(1 < x < 10)     # True\nprint(1 < x < 5)      # False\nprint(0 <= x <= 100)  # True\n\n# Сравнение строк (лексикографическое)\nprint("apple" < "banana")  # True\nprint("abc" == "abc")      # True\nprint("Z" < "a")           # True (заглавные < строчные)\n\n# Операторы is и is not — сравнение идентичности\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True  — одинаковые значения\nprint(a is b)   # False — разные объекты\nprint(a is c)   # True  — тот же объект\nprint(a is not b)  # True'
        },
        {
          type: 'warning',
          value: 'Не используйте is для сравнения чисел и строк. is проверяет идентичность объектов (один и тот же объект в памяти), а не равенство значений. Для значений используйте ==.'
        }
      ]
    },
    {
      id: 3,
      title: 'Логические операторы',
      type: 'theory',
      content: [
        {
          type: 'heading',
          value: 'and, or, not'
        },
        {
          type: 'text',
          value: 'Логические операторы and, or, not работают с булевыми значениями. Но в Python они применяют "короткое замыкание" (short-circuit evaluation) и возвращают одно из своих операндов, а не обязательно True/False.'
        },
        {
          type: 'code',
          language: 'python',
          value: '# Стандартные логические операции\nprint(True and True)   # True\nprint(True and False)  # False\nprint(False and True)  # False\nprint(False or True)   # True\nprint(not True)        # False\nprint(not False)       # True\n\n# Короткое замыкание\n# and: если первый False — второй не вычисляется\n# or:  если первый True  — второй не вычисляется\n\n# and возвращает первый "ложный" операнд или последний\nprint(1 and 2)     # 2 (оба истинны, возвращает последний)\nprint(0 and 2)     # 0 (первый ложный)\nprint("" and "hi") # "" (первый ложный)\n\n# or возвращает первый "истинный" операнд или последний\nprint(1 or 2)      # 1 (первый истинный)\nprint(0 or 2)      # 2 (первый ложный, второй истинный)\nprint(0 or "")     # "" (оба ложны, возвращает последний)\n\n# Практический пример\nname = ""\ndefault_name = name or "Гость"\nprint(default_name)  # Гость'
        }
      ]
    },
    {
      id: 4,
      title: 'Операторы присваивания',
      type: 'theory',
      content: [
        {
          type: 'heading',
          value: 'Составные операторы присваивания'
        },
        {
          type: 'text',
          value: 'Python поддерживает составные операторы присваивания, которые объединяют операцию и присваивание. Начиная с Python 3.8 есть "морж-оператор" := (walrus operator), который позволяет присваивать значение внутри выражения.'
        },
        {
          type: 'code',
          language: 'python',
          value: 'x = 10\n\nx += 5   # x = x + 5  → 15\nx -= 3   # x = x - 3  → 12\nx *= 2   # x = x * 2  → 24\nx /= 4   # x = x / 4  → 6.0\nx //= 2  # x = x // 2 → 3.0\nx **= 3  # x = x ** 3 → 27.0\nx %= 10  # x = x % 10 → 7.0\nprint(x)  # 7.0\n\n# Морж-оператор := (Python 3.8+)\nnumbers = [1, 2, 3, 4, 5, 6, 7, 8]\n\n# Без морж-оператора\ntotal = sum(numbers)\nif total > 20:\n    print(f"Сумма {total} больше 20")\n\n# С морж-оператором — вычисление и проверка в одном выражении\nif (total := sum(numbers)) > 20:\n    print(f"Сумма {total} больше 20")  # total уже доступна'
        },
        {
          type: 'tip',
          value: 'Морж-оператор := особенно полезен в while-циклах: while chunk := file.read(8192): process(chunk). Это элегантнее, чем читать в отдельной строке перед циклом.'
        }
      ]
    },
    {
      id: 5,
      title: 'Побитовые операторы',
      type: 'theory',
      content: [
        {
          type: 'heading',
          value: 'Операции над битами'
        },
        {
          type: 'text',
          value: 'Побитовые операторы работают с целыми числами на уровне отдельных битов. Они используются в алгоритмах, флагах, низкоуровневом программировании и оптимизациях. Важно понимать их для работы с разрешениями, масками и хэшами.'
        },
        {
          type: 'code',
          language: 'python',
          value: 'a = 0b1100  # 12 в двоичном\nb = 0b1010  # 10 в двоичном\n\n# Побитовое И (AND)\nprint(bin(a & b))   # 0b1000 = 8  (биты 1 только где оба 1)\n\n# Побитовое ИЛИ (OR)\nprint(bin(a | b))   # 0b1110 = 14 (биты 1 где хотя бы один 1)\n\n# Побитовое XOR (исключающее ИЛИ)\nprint(bin(a ^ b))   # 0b0110 = 6  (биты разные)\n\n# Побитовое НЕ (NOT)\nprint(~a)           # -13 (инвертирует все биты, включая знаковый)\n\n# Сдвиги\nprint(a << 2)  # 48 (умножение на 4 = 2^2)\nprint(a >> 1)  # 6  (деление на 2)\n\n# Практика: флаги разрешений\nREAD    = 0b001  # 1\nWRITE   = 0b010  # 2\nEXECUTE = 0b100  # 4\n\npermissions = READ | WRITE  # 3\nprint(bool(permissions & READ))     # True — есть право чтения\nprint(bool(permissions & EXECUTE))  # False — нет права выполнения'
        }
      ]
    },
    {
      id: 6,
      title: 'Практика: Конвертер единиц',
      type: 'practice',
      difficulty: 'beginner',
      description: 'Напишите конвертер температур. Создайте программу, которая конвертирует температуру из Цельсия в Фаренгейт, Кельвин и обратно.',
      requirements: [
        'Задайте температуру celsius = 100',
        'Вычислите Фаренгейт: F = C * 9/5 + 32',
        'Вычислите Кельвин: K = C + 273.15',
        'Выведите все три значения с 2 знаками после запятой',
        'Проверьте: 0°C = 32°F = 273.15K, 100°C = 212°F = 373.15K'
      ],
      expectedOutput: '100°C = 212.00°F = 373.15K',
      hint: 'Используйте порядок операций: умножение до сложения. Для вывода используйте f-строку с форматом :.2f.',
      solution: 'celsius = 100\nfahrenheit = celsius * 9 / 5 + 32\nkelvin = celsius + 273.15\n\nprint(f"{celsius}°C = {fahrenheit:.2f}°F = {kelvin:.2f}K")',
      explanation: 'Формула F = C * 9/5 + 32 использует деление /  которое возвращает float в Python 3. Приоритет операций гарантирует, что умножение выполняется раньше сложения. Оператор f-строки :.2f форматирует число с двумя знаками после запятой.'
    }
  ]
}
