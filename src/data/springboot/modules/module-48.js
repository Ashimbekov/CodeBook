export default {
  id: 48,
  title: 'CQRS и Event Sourcing',
  description: 'Паттерны CQRS и Event Sourcing: разделение чтения и записи, модели событий, event store, проекции и их реализация в Spring Boot.',
  lessons: [
    {
      id: 1,
      title: 'Паттерн CQRS',
      type: 'theory',
      content: [
        { type: `heading`, value: `Что такое CQRS?` },
        { type: `text`, value: `CQRS (Command Query Responsibility Segregation) — это паттерн, разделяющий операции чтения (Query) и записи (Command) в отдельные модели. Write Model оптимизирована для бизнес-логики и валидации. Read Model — для быстрых запросов и отображения.` },
        { type: `heading`, value: `Зачем разделять чтение и запись?` },
        { type: `list`, items: [
          'Разные требования: запись — consistency и валидация, чтение — скорость и удобный формат',
          'Масштабирование: чтение обычно в 10-100x чаще записи, можно масштабировать независимо',
          'Оптимизация: read model может быть денормализованной, со всеми нужными JOIN заранее',
          'Разные хранилища: write в PostgreSQL, read в Elasticsearch или MongoDB'
        ] },
        { type: `heading`, value: `Архитектура CQRS` },
        { type: `code`, language: `text`, value: `┌─────────────────────────────────────────────────┐\n│                    API Layer                     │\n│  POST /orders (Command)    GET /orders (Query)   │\n└────────┬───────────────────────────┬─────────────┘\n         │                           │\n    ┌────▼────┐                ┌─────▼────┐\n    │ Command │                │  Query   │\n    │ Handler │                │  Handler │\n    └────┬────┘                └─────┬────┘\n         │                           │\n    ┌────▼────┐   Events       ┌─────▼────┐\n    │ Write   │──────────────►│  Read    │\n    │ Model   │   (async)      │  Model   │\n    │ (PG)    │                │ (ES/Mongo)│\n    └─────────┘                └──────────┘` },
        { type: `heading`, value: `Команды и запросы` },
        { type: `code`, language: `java`, value: `// Commands — изменяют состояние\npublic sealed interface OrderCommand {\n    record CreateOrder(Long userId, List<OrderItem> items, \n                       String shippingAddress) implements OrderCommand {}\n    record CancelOrder(Long orderId, String reason) implements OrderCommand {}\n    record PayOrder(Long orderId, String paymentMethod) implements OrderCommand {}\n}\n\n// Queries — только чтение\npublic sealed interface OrderQuery {\n    record GetOrder(Long orderId) implements OrderQuery {}\n    record GetUserOrders(Long userId, int page, int size) implements OrderQuery {}\n    record SearchOrders(String query, OrderStatus status, \n                         int page, int size) implements OrderQuery {}\n}\n\n// Command Handler\n@Service\npublic class OrderCommandHandler {\n\n    private final OrderWriteRepository writeRepo;\n    private final ApplicationEventPublisher eventPublisher;\n\n    public Long handle(CreateOrder command) {\n        // Валидация и бизнес-логика\n        validateStock(command.items());\n        BigDecimal total = calculateTotal(command.items());\n\n        Order order = new Order();\n        order.setUserId(command.userId());\n        order.setTotalAmount(total);\n        order.setStatus(OrderStatus.NEW);\n        Order saved = writeRepo.save(order);\n\n        // Публикация события для обновления Read Model\n        eventPublisher.publishEvent(\n            new OrderCreatedEvent(saved.getId(), command.userId(), \n                total, command.items()));\n\n        return saved.getId();\n    }\n}\n\n// Query Handler\n@Service\npublic class OrderQueryHandler {\n\n    private final OrderReadRepository readRepo; // Elasticsearch\n\n    public OrderView handle(GetOrder query) {\n        return readRepo.findById(query.orderId())\n            .orElseThrow(() -> new OrderNotFoundException(query.orderId()));\n    }\n\n    public Page<OrderView> handle(SearchOrders query) {\n        return readRepo.search(query.query(), query.status(),\n            PageRequest.of(query.page(), query.size()));\n    }\n}` },
        { type: `note`, value: `CQRS не требует Event Sourcing. Можно использовать одну БД с разными моделями (Entity vs View). Но CQRS + Event Sourcing — мощная комбинация.` }
      ]
    },
    {
      id: 2,
      title: 'Read и Write модели',
      type: 'theory',
      content: [
        { type: `heading`, value: `Write Model — нормализованная` },
        { type: `code`, language: `java`, value: `// Write Model — строгая валидация, нормализованная структура\n@Entity\n@Table(name = \"orders\")\npublic class Order {\n    @Id @GeneratedValue\n    private Long id;\n    private Long userId;\n    @Enumerated(EnumType.STRING)\n    private OrderStatus status;\n    private BigDecimal totalAmount;\n    @OneToMany(cascade = CascadeType.ALL, orphanRemoval = true)\n    private List<OrderItem> items = new ArrayList<>();\n    @Embedded\n    private ShippingAddress shippingAddress;\n    @CreationTimestamp\n    private LocalDateTime createdAt;\n    private LocalDateTime paidAt;\n    private LocalDateTime shippedAt;\n}\n\n@Entity\npublic class OrderItem {\n    @Id @GeneratedValue\n    private Long id;\n    private Long productId;\n    private int quantity;\n    private BigDecimal priceAtTime;\n}\n\n// Write Repository — только CRUD\npublic interface OrderWriteRepository extends JpaRepository<Order, Long> {\n    // Минимум query методов — основная работа через Commands\n}` },
        { type: `heading`, value: `Read Model — денормализованная для быстрых запросов` },
        { type: `code`, language: `java`, value: `// Read Model — оптимизирована для отображения\n// Может храниться в Elasticsearch, MongoDB или отдельных таблицах\n\n@Document(indexName = \"orders\")\npublic class OrderView {\n    @Id\n    private String id;\n    private Long orderId;\n    private String userName;            // денормализовано из User\n    private String userEmail;           // денормализовано из User\n    private String status;\n    private BigDecimal totalAmount;\n    private List<OrderItemView> items;  // с названиями товаров!\n    private String shippingCity;\n    private String trackingNumber;\n    private LocalDateTime createdAt;\n    private LocalDateTime updatedAt;\n}\n\npublic class OrderItemView {\n    private Long productId;\n    private String productName;  // денормализовано из Product\n    private String productImage; // денормализовано из Product\n    private int quantity;\n    private BigDecimal price;\n    private BigDecimal subtotal; // предвычислено\n}\n\n// Read Repository — богатые query методы\npublic interface OrderReadRepository \n        extends ElasticsearchRepository<OrderView, String> {\n\n    Page<OrderView> findByUserName(String userName, Pageable pageable);\n    Page<OrderView> findByStatus(String status, Pageable pageable);\n    Page<OrderView> findByShippingCity(String city, Pageable pageable);\n\n    @Query(\"{\\\"bool\\\": {\\\"must\\\": [{\\\"match\\\": {\\\"items.productName\\\": \\\"?0\\\"}}]}}\")\n    Page<OrderView> searchByProductName(String productName, Pageable pageable);\n}` },
        { type: `heading`, value: `Синхронизация Read Model через события` },
        { type: `code`, language: `java`, value: `// Event Listener обновляет Read Model при изменении Write Model\n@Component\npublic class OrderViewProjection {\n\n    private final OrderReadRepository readRepo;\n    private final UserService userService;\n    private final ProductService productService;\n\n    @TransactionalEventListener\n    public void on(OrderCreatedEvent event) {\n        User user = userService.findById(event.userId());\n\n        OrderView view = new OrderView();\n        view.setOrderId(event.orderId());\n        view.setUserName(user.getName());\n        view.setUserEmail(user.getEmail());\n        view.setStatus(\"NEW\");\n        view.setTotalAmount(event.totalAmount());\n        view.setItems(event.items().stream()\n            .map(item -> {\n                Product product = productService.findById(item.productId());\n                return new OrderItemView(\n                    item.productId(), product.getName(), product.getImageUrl(),\n                    item.quantity(), item.price(),\n                    item.price().multiply(BigDecimal.valueOf(item.quantity())));\n            })\n            .toList());\n        view.setCreatedAt(event.createdAt());\n        readRepo.save(view);\n    }\n\n    @TransactionalEventListener\n    public void on(OrderStatusChangedEvent event) {\n        readRepo.findByOrderId(event.orderId()).ifPresent(view -> {\n            view.setStatus(event.newStatus().name());\n            view.setUpdatedAt(LocalDateTime.now());\n            if (event.trackingNumber() != null) {\n                view.setTrackingNumber(event.trackingNumber());\n            }\n            readRepo.save(view);\n        });\n    }\n}` },
        { type: `tip`, value: `Read Model денормализована: все нужные данные в одном документе. Нет JOIN-ов при чтении. Обновляется асинхронно через события. Eventual consistency — Read Model может отставать на миллисекунды.` }
      ]
    },
    {
      id: 3,
      title: 'Event Sourcing',
      type: 'theory',
      content: [
        { type: `heading`, value: `Что такое Event Sourcing?` },
        { type: `text`, value: `Event Sourcing — это паттерн, где состояние объекта определяется не текущими значениями полей, а последовательностью событий. Вместо UPDATE мы только INSERT события. Текущее состояние вычисляется путём воспроизведения всех событий.` },
        { type: `heading`, value: `Традиционный подход vs Event Sourcing` },
        { type: `code`, language: `text`, value: `Традиционный: UPDATE orders SET status=\\'PAID\\', paid_at=NOW() WHERE id=1\n  -> Потеряна информация о предыдущем состоянии\n\nEvent Sourcing:\n  Event 1: OrderCreated { orderId=1, userId=5, total=50000 }\n  Event 2: OrderConfirmed { orderId=1, confirmedBy=\"admin\" }\n  Event 3: PaymentReceived { orderId=1, amount=50000, method=\"card\" }\n  -> Полная история. Можно восстановить состояние на любой момент` },
        { type: `heading`, value: `Доменные события` },
        { type: `code`, language: `java`, value: `// Базовый класс события\npublic abstract class DomainEvent {\n    private final String eventId = UUID.randomUUID().toString();\n    private final LocalDateTime occurredAt = LocalDateTime.now();\n    private final String aggregateId;\n    private final int version;\n\n    protected DomainEvent(String aggregateId, int version) {\n        this.aggregateId = aggregateId;\n        this.version = version;\n    }\n}\n\n// Конкретные события\npublic class OrderCreated extends DomainEvent {\n    private final Long userId;\n    private final BigDecimal totalAmount;\n    private final List<OrderItemData> items;\n\n    public OrderCreated(String orderId, int version, Long userId,\n                        BigDecimal totalAmount, List<OrderItemData> items) {\n        super(orderId, version);\n        this.userId = userId;\n        this.totalAmount = totalAmount;\n        this.items = items;\n    }\n}\n\npublic class OrderPaid extends DomainEvent {\n    private final BigDecimal amount;\n    private final String paymentMethod;\n    private final String transactionId;\n\n    public OrderPaid(String orderId, int version, BigDecimal amount,\n                     String paymentMethod, String transactionId) {\n        super(orderId, version);\n        this.amount = amount;\n        this.paymentMethod = paymentMethod;\n        this.transactionId = transactionId;\n    }\n}\n\npublic class OrderShipped extends DomainEvent {\n    private final String trackingNumber;\n    private final String carrier;\n}\n\npublic class OrderCancelled extends DomainEvent {\n    private final String reason;\n    private final String cancelledBy;\n}` },
        { type: `heading`, value: `Aggregate — воспроизведение событий` },
        { type: `code`, language: `java`, value: `// Aggregate восстанавливает состояние из событий\npublic class OrderAggregate {\n    private String id;\n    private Long userId;\n    private OrderStatus status;\n    private BigDecimal totalAmount;\n    private List<OrderItemData> items;\n    private String trackingNumber;\n    private int version = 0;\n\n    private final List<DomainEvent> uncommittedEvents = new ArrayList<>();\n\n    // Восстановление из событий\n    public static OrderAggregate fromEvents(List<DomainEvent> events) {\n        OrderAggregate aggregate = new OrderAggregate();\n        events.forEach(aggregate::apply);\n        return aggregate;\n    }\n\n    // Команда: создать заказ\n    public void create(Long userId, BigDecimal total, List<OrderItemData> items) {\n        if (this.id != null) throw new IllegalStateException(\"Заказ уже создан\");\n        raiseEvent(new OrderCreated(generateId(), nextVersion(), \n            userId, total, items));\n    }\n\n    // Команда: оплатить\n    public void pay(BigDecimal amount, String method, String transactionId) {\n        if (status != OrderStatus.CONFIRMED)\n            throw new IllegalStateException(\"Оплата возможна только для CONFIRMED\");\n        if (amount.compareTo(totalAmount) < 0)\n            throw new IllegalArgumentException(\"Недостаточная сумма\");\n        raiseEvent(new OrderPaid(id, nextVersion(), amount, method, transactionId));\n    }\n\n    // Применение событий\n    private void apply(DomainEvent event) {\n        switch (event) {\n            case OrderCreated e -> {\n                this.id = e.getAggregateId();\n                this.userId = e.getUserId();\n                this.totalAmount = e.getTotalAmount();\n                this.items = e.getItems();\n                this.status = OrderStatus.NEW;\n            }\n            case OrderPaid e -> {\n                this.status = OrderStatus.PAID;\n            }\n            case OrderShipped e -> {\n                this.status = OrderStatus.SHIPPED;\n                this.trackingNumber = e.getTrackingNumber();\n            }\n            case OrderCancelled e -> {\n                this.status = OrderStatus.CANCELLED;\n            }\n            default -> throw new IllegalArgumentException(\n                \"Неизвестное событие: \" + event.getClass());\n        }\n        this.version = event.getVersion();\n    }\n\n    private void raiseEvent(DomainEvent event) {\n        apply(event);  // применяем к текущему состоянию\n        uncommittedEvents.add(event);  // запоминаем для сохранения\n    }\n}` },
        { type: `note`, value: `Event Sourcing даёт полный аудит бесплатно. Можно \"перемотать\" состояние на любой момент времени. Но это усложняет архитектуру — не используйте без необходимости.` }
      ]
    },
    {
      id: 4,
      title: 'Event Store',
      type: 'theory',
      content: [
        { type: `heading`, value: `Хранилище событий` },
        { type: `code`, language: `java`, value: `// Таблица для хранения событий\n@Entity\n@Table(name = \"event_store\", indexes = {\n    @Index(name = \"idx_aggregate\", columnList = \"aggregate_id, version\")\n})\npublic class StoredEvent {\n    @Id @GeneratedValue\n    private Long id;\n\n    @Column(name = \"aggregate_id\", nullable = false)\n    private String aggregateId;\n\n    @Column(name = \"aggregate_type\", nullable = false)\n    private String aggregateType;\n\n    @Column(nullable = false)\n    private int version;\n\n    @Column(name = \"event_type\", nullable = false)\n    private String eventType;\n\n    @Column(columnDefinition = \"TEXT\", nullable = false)\n    private String payload;  // JSON\n\n    @Column(name = \"occurred_at\", nullable = false)\n    private LocalDateTime occurredAt;\n\n    @Column(name = \"event_id\", unique = true)\n    private String eventId;\n}\n\npublic interface EventStoreRepository extends JpaRepository<StoredEvent, Long> {\n    List<StoredEvent> findByAggregateIdOrderByVersionAsc(String aggregateId);\n    Optional<StoredEvent> findTopByAggregateIdOrderByVersionDesc(String aggregateId);\n    List<StoredEvent> findByOccurredAtAfterOrderByOccurredAtAsc(LocalDateTime after);\n}` },
        { type: `heading`, value: `Event Store Service` },
        { type: `code`, language: `java`, value: `@Service\npublic class EventStoreService {\n\n    private final EventStoreRepository eventRepo;\n    private final ObjectMapper objectMapper;\n    private final ApplicationEventPublisher eventPublisher;\n\n    // Сохранение событий aggregate\n    @Transactional\n    public void saveEvents(String aggregateId, String aggregateType,\n                           List<DomainEvent> events, int expectedVersion) {\n        // Оптимистическая блокировка\n        Optional<StoredEvent> lastEvent = eventRepo\n            .findTopByAggregateIdOrderByVersionDesc(aggregateId);\n\n        int currentVersion = lastEvent.map(StoredEvent::getVersion).orElse(0);\n        if (currentVersion != expectedVersion) {\n            throw new ConcurrencyException(\n                \"Конфликт версий: ожидалась \" + expectedVersion + \n                \", текущая \" + currentVersion);\n        }\n\n        for (DomainEvent event : events) {\n            StoredEvent stored = new StoredEvent();\n            stored.setAggregateId(aggregateId);\n            stored.setAggregateType(aggregateType);\n            stored.setVersion(event.getVersion());\n            stored.setEventType(event.getClass().getSimpleName());\n            stored.setPayload(serialize(event));\n            stored.setOccurredAt(event.getOccurredAt());\n            stored.setEventId(event.getEventId());\n            eventRepo.save(stored);\n\n            // Публикуем для проекций\n            eventPublisher.publishEvent(event);\n        }\n    }\n\n    // Загрузка aggregate из событий\n    public OrderAggregate loadAggregate(String aggregateId) {\n        List<StoredEvent> storedEvents = eventRepo\n            .findByAggregateIdOrderByVersionAsc(aggregateId);\n\n        if (storedEvents.isEmpty()) {\n            throw new AggregateNotFoundException(aggregateId);\n        }\n\n        List<DomainEvent> events = storedEvents.stream()\n            .map(this::deserialize)\n            .toList();\n\n        return OrderAggregate.fromEvents(events);\n    }\n\n    private String serialize(DomainEvent event) {\n        try {\n            return objectMapper.writeValueAsString(event);\n        } catch (JsonProcessingException e) {\n            throw new RuntimeException(\"Ошибка сериализации\", e);\n        }\n    }\n\n    private DomainEvent deserialize(StoredEvent stored) {\n        try {\n            Class<?> eventClass = Class.forName(\n                \"com.example.event.\" + stored.getEventType());\n            return (DomainEvent) objectMapper.readValue(\n                stored.getPayload(), eventClass);\n        } catch (Exception e) {\n            throw new RuntimeException(\"Ошибка десериализации\", e);\n        }\n    }\n}` },
        { type: `heading`, value: `Command Handler с Event Store` },
        { type: `code`, language: `java`, value: `@Service\npublic class OrderCommandHandler {\n\n    private final EventStoreService eventStore;\n\n    public String handle(CreateOrder command) {\n        OrderAggregate aggregate = new OrderAggregate();\n        aggregate.create(command.userId(), command.total(), command.items());\n\n        eventStore.saveEvents(\n            aggregate.getId(), \"Order\",\n            aggregate.getUncommittedEvents(), 0);\n\n        return aggregate.getId();\n    }\n\n    public void handle(PayOrder command) {\n        OrderAggregate aggregate = eventStore.loadAggregate(\n            command.orderId());\n\n        aggregate.pay(command.amount(), command.method(), \n            command.transactionId());\n\n        eventStore.saveEvents(\n            aggregate.getId(), \"Order\",\n            aggregate.getUncommittedEvents(), \n            aggregate.getVersion());\n    }\n}` },
        { type: `warning`, value: `Event Store — это append-only хранилище. НИКОГДА не обновляйте и не удаляйте события! Это нарушит целостность данных и сделает восстановление невозможным.` }
      ]
    },
    {
      id: 5,
      title: 'Проекции',
      type: 'theory',
      content: [
        { type: `heading`, value: `Что такое проекции?` },
        { type: `text`, value: `Проекции — это read models, создаваемые путём обработки потока событий. Каждая проекция оптимизирована для конкретного use case: список заказов, статистика, поиск. Проекции можно пересоздать (rebuild) воспроизведя все события.` },
        { type: `code`, language: `java`, value: `// Проекция: список заказов пользователя\n@Component\npublic class UserOrdersProjection {\n\n    private final UserOrderViewRepository viewRepo;\n\n    @TransactionalEventListener\n    public void on(OrderCreated event) {\n        UserOrderView view = new UserOrderView();\n        view.setOrderId(event.getAggregateId());\n        view.setUserId(event.getUserId());\n        view.setTotalAmount(event.getTotalAmount());\n        view.setStatus(\"NEW\");\n        view.setItemCount(event.getItems().size());\n        view.setCreatedAt(event.getOccurredAt());\n        viewRepo.save(view);\n    }\n\n    @TransactionalEventListener\n    public void on(OrderPaid event) {\n        viewRepo.findByOrderId(event.getAggregateId()).ifPresent(view -> {\n            view.setStatus(\"PAID\");\n            view.setPaidAt(event.getOccurredAt());\n            viewRepo.save(view);\n        });\n    }\n\n    @TransactionalEventListener\n    public void on(OrderCancelled event) {\n        viewRepo.findByOrderId(event.getAggregateId()).ifPresent(view -> {\n            view.setStatus(\"CANCELLED\");\n            view.setCancelReason(event.getReason());\n            viewRepo.save(view);\n        });\n    }\n}\n\n// Проекция: статистика продаж\n@Component\npublic class SalesStatsProjection {\n\n    private final SalesStatsRepository statsRepo;\n\n    @TransactionalEventListener\n    public void on(OrderPaid event) {\n        String date = event.getOccurredAt().toLocalDate().toString();\n\n        SalesStats stats = statsRepo.findByDate(date)\n            .orElse(new SalesStats(date, 0, BigDecimal.ZERO));\n\n        stats.setOrderCount(stats.getOrderCount() + 1);\n        stats.setTotalRevenue(\n            stats.getTotalRevenue().add(event.getAmount()));\n        statsRepo.save(stats);\n    }\n}\n\n// Проекция: полнотекстовый поиск\n@Component\npublic class OrderSearchProjection {\n\n    private final ElasticsearchOperations esOps;\n\n    @TransactionalEventListener\n    public void on(OrderCreated event) {\n        OrderSearchDocument doc = new OrderSearchDocument();\n        doc.setId(event.getAggregateId());\n        doc.setProducts(event.getItems().stream()\n            .map(i -> productService.findById(i.productId()).getName())\n            .toList());\n        esOps.save(doc);\n    }\n}` },
        { type: `heading`, value: `Rebuild проекции` },
        { type: `code`, language: `java`, value: `// Пересоздание проекции из всех событий\n@Service\npublic class ProjectionRebuildService {\n\n    private final EventStoreRepository eventRepo;\n    private final List<Object> projections;\n\n    public void rebuild(String projectionName) {\n        log.info(\"Начинаем rebuild проекции: {}\", projectionName);\n\n        // Очищаем текущую проекцию\n        clearProjection(projectionName);\n\n        // Читаем все события в хронологическом порядке\n        List<StoredEvent> allEvents = eventRepo\n            .findAllByOrderByOccurredAtAsc();\n\n        int count = 0;\n        for (StoredEvent stored : allEvents) {\n            DomainEvent event = deserialize(stored);\n            // Применяем к нужной проекции\n            applyToProjection(projectionName, event);\n            count++;\n\n            if (count % 1000 == 0) {\n                log.info(\"Обработано {} событий\", count);\n            }\n        }\n\n        log.info(\"Rebuild завершён: {} событий обработано\", count);\n    }\n}\n\n// REST API для rebuild\n@PostMapping(\"/admin/projections/{name}/rebuild\")\n@PreAuthorize(\"hasRole(\\'ADMIN\\')\")\npublic ResponseEntity<String> rebuildProjection(@PathVariable String name) {\n    rebuildService.rebuild(name);\n    return ResponseEntity.ok(\"Проекция \" + name + \" пересоздана\");\n}` },
        { type: `heading`, value: `Snapshot для оптимизации` },
        { type: `code`, language: `java`, value: `// Для aggregate с тысячами событий — snapshot\n@Service\npublic class SnapshotService {\n\n    public OrderAggregate loadWithSnapshot(String aggregateId) {\n        // 1. Загрузить последний snapshot\n        Optional<Snapshot> snapshot = snapshotRepo\n            .findTopByAggregateIdOrderByVersionDesc(aggregateId);\n\n        OrderAggregate aggregate;\n        int fromVersion;\n\n        if (snapshot.isPresent()) {\n            aggregate = deserialize(snapshot.get().getData());\n            fromVersion = snapshot.get().getVersion();\n        } else {\n            aggregate = new OrderAggregate();\n            fromVersion = 0;\n        }\n\n        // 2. Применить события после snapshot\n        List<StoredEvent> newEvents = eventRepo\n            .findByAggregateIdAndVersionGreaterThan(aggregateId, fromVersion);\n\n        newEvents.stream()\n            .map(this::deserialize)\n            .forEach(aggregate::apply);\n\n        // 3. Создать новый snapshot если много событий\n        if (newEvents.size() > 100) {\n            createSnapshot(aggregate);\n        }\n\n        return aggregate;\n    }\n}` },
        { type: `tip`, value: `Проекции можно создавать по мере необходимости. Нужен новый отчёт? Создайте новую проекцию и rebuild из всех событий. Старые проекции не меняются.` }
      ]
    },
    {
      id: 6,
      title: 'Практика: CQRS для системы заказов',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте CQRS с Event Sourcing для системы заказов: раздельные Command/Query модели, Event Store, проекции.',
      requirements: [
        'Command: CreateOrder, PayOrder, CancelOrder — изменяют Write Model',
        'Query: GetOrder, GetUserOrders, GetSalesStats — читают из Read Model',
        'Event Store: таблица с событиями, append-only',
        'OrderAggregate: восстановление состояния из событий',
        'Проекция UserOrdersProjection: список заказов пользователя',
        'Проекция SalesStatsProjection: статистика продаж по дням',
        'REST API: POST /commands/create-order, GET /queries/orders/{id}',
        'Endpoint для rebuild проекции'
      ],
      hint: 'OrderAggregate.fromEvents() воспроизводит все события для восстановления текущего состояния. Event Store сохраняет события с оптимистической блокировкой по version. Проекции обновляются через @TransactionalEventListener.',
      expectedOutput: 'POST /api/commands/create-order:\n  Event Store: INSERT OrderCreated {orderId=1, total=50000}\n  UserOrdersProjection: INSERT {orderId=1, status="NEW"}\n  SalesStatsProjection: (ничего — не оплачен)\n\nPOST /api/commands/pay-order:\n  Event Store: INSERT OrderPaid {orderId=1, amount=50000}\n  UserOrdersProjection: UPDATE status="PAID"\n  SalesStatsProjection: UPDATE revenue += 50000\n\nGET /api/queries/orders/1:\n  Read Model: {orderId:1, status:"PAID", total:50000}\n\nGET /api/queries/stats/2026-04-05:\n  {date:"2026-04-05", orderCount:15, totalRevenue:750000}\n\nEvent Store (SELECT * FROM event_store WHERE aggregate_id=1):\n  v1: OrderCreated {total:50000}\n  v2: OrderPaid {amount:50000}\n  (полная история)',
      solution: '@Entity @Table(name = "event_store")\npublic class StoredEvent {\n    @Id @GeneratedValue private Long id;\n    private String aggregateId;\n    private int version;\n    private String eventType;\n    @Column(columnDefinition = "TEXT") private String payload;\n    private LocalDateTime occurredAt;\n}\n\npublic class OrderAggregate {\n    private String id; private OrderStatus status; private BigDecimal total;\n    private int version = 0;\n    private List<DomainEvent> uncommitted = new ArrayList<>();\n\n    public static OrderAggregate fromEvents(List<DomainEvent> events) {\n        OrderAggregate agg = new OrderAggregate();\n        events.forEach(agg::apply);\n        return agg;\n    }\n\n    public void create(Long userId, BigDecimal total) {\n        raiseEvent(new OrderCreated(UUID.randomUUID().toString(), ++version, userId, total));\n    }\n\n    public void pay(BigDecimal amount) {\n        if (status != OrderStatus.NEW) throw new IllegalStateException("Cant pay");\n        raiseEvent(new OrderPaid(id, ++version, amount));\n    }\n\n    private void apply(DomainEvent e) {\n        if (e instanceof OrderCreated c) { id = c.getAggregateId(); status = NEW; total = c.getTotal(); }\n        if (e instanceof OrderPaid p) { status = PAID; }\n        version = e.getVersion();\n    }\n    private void raiseEvent(DomainEvent e) { apply(e); uncommitted.add(e); }\n}\n\n@Service\npublic class OrderCommandHandler {\n    private final EventStoreService eventStore;\n\n    public String createOrder(CreateOrderCommand cmd) {\n        OrderAggregate agg = new OrderAggregate();\n        agg.create(cmd.userId(), cmd.total());\n        eventStore.saveEvents(agg.getId(), "Order", agg.getUncommitted(), 0);\n        return agg.getId();\n    }\n\n    public void payOrder(PayOrderCommand cmd) {\n        OrderAggregate agg = eventStore.loadAggregate(cmd.orderId());\n        agg.pay(cmd.amount());\n        eventStore.saveEvents(agg.getId(), "Order", agg.getUncommitted(), agg.getVersion()-1);\n    }\n}\n\n@Component\npublic class SalesStatsProjection {\n    @TransactionalEventListener\n    public void on(OrderPaid event) {\n        String date = event.getOccurredAt().toLocalDate().toString();\n        SalesStats stats = statsRepo.findByDate(date).orElse(new SalesStats(date, 0, ZERO));\n        stats.setOrderCount(stats.getOrderCount() + 1);\n        stats.setTotalRevenue(stats.getTotalRevenue().add(event.getAmount()));\n        statsRepo.save(stats);\n    }\n}',
      explanation: 'CQRS разделяет запись (Commands + Event Store) и чтение (Queries + Projections). Event Sourcing хранит все изменения как последовательность событий. Aggregate восстанавливает состояние воспроизводя события. Проекции создают оптимизированные Read Models для каждого use case.'
    }
  ]
}
