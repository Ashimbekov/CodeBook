export default {
  id: 34,
  title: 'Spring WebFlux: реактивное программирование',
  description: 'Реактивное программирование с Project Reactor: Mono, Flux, реактивные контроллеры, R2DBC для неблокирующего доступа к БД, WebClient и тестирование реактивных потоков.',
  lessons: [
    {
      id: 1,
      title: 'Основы Project Reactor: Mono и Flux',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Что такое реактивное программирование?' },
        { type: 'text', value: 'Реактивное программирование — это парадигма, основанная на асинхронных потоках данных. В отличие от императивного подхода, где вы блокируете поток выполнения при ожидании результата, реактивный подход позволяет обрабатывать данные по мере их поступления.' },
        { type: 'text', value: 'Spring WebFlux построен на Project Reactor — реализации спецификации Reactive Streams. Два ключевых типа: Mono (0 или 1 элемент) и Flux (0..N элементов).' },
        { type: 'heading', value: 'Mono — один элемент или ничего' },
        { type: 'code', language: 'java', value: '// Создание Mono\nMono<String> mono1 = Mono.just("Hello");\nMono<String> mono2 = Mono.empty();\nMono<String> mono3 = Mono.error(new RuntimeException("Ошибка"));\n\n// Трансформация\nMono<Integer> length = Mono.just("Hello")\n    .map(String::length);           // 5\n\nMono<String> upper = Mono.just("hello")\n    .flatMap(s -> Mono.just(s.toUpperCase()));  // "HELLO"\n\n// Обработка ошибок\nMono<String> safe = Mono.error(new RuntimeException())\n    .onErrorReturn("default")       // fallback значение\n    .onErrorResume(e -> Mono.just("recovered"));  // fallback Mono' },
        { type: 'heading', value: 'Flux — поток элементов' },
        { type: 'code', language: 'java', value: '// Создание Flux\nFlux<Integer> flux1 = Flux.just(1, 2, 3, 4, 5);\nFlux<Integer> flux2 = Flux.range(1, 10);       // 1..10\nFlux<Long> flux3 = Flux.interval(Duration.ofSeconds(1));  // каждую секунду\nFlux<String> flux4 = Flux.fromIterable(List.of("a", "b", "c"));\n\n// Операторы\nFlux.range(1, 10)\n    .filter(n -> n % 2 == 0)       // 2, 4, 6, 8, 10\n    .map(n -> n * n)                // 4, 16, 36, 64, 100\n    .take(3)                        // 4, 16, 36\n    .subscribe(System.out::println);\n\n// Комбинирование\nFlux<String> merged = Flux.merge(flux1.map(String::valueOf), flux4);\nFlux<String> concat = Flux.concat(flux4, Flux.just("d", "e"));' },
        { type: 'heading', value: 'Подписка и backpressure' },
        { type: 'text', value: 'Ничего не произойдёт пока вы не подпишетесь (subscribe). Reactive Streams поддерживают backpressure — подписчик контролирует скорость получения данных.' },
        { type: 'code', language: 'java', value: '// Подписка с обработкой\nFlux.range(1, 5)\n    .subscribe(\n        value -> System.out.println("Получено: " + value),\n        error -> System.err.println("Ошибка: " + error),\n        () -> System.out.println("Завершено!")\n    );\n\n// Backpressure — запрашиваем по 2 элемента\nFlux.range(1, 100)\n    .subscribe(new BaseSubscriber<Integer>() {\n        @Override\n        protected void hookOnSubscribe(Subscription sub) {\n            request(2);  // запросить первые 2\n        }\n        @Override\n        protected void hookOnNext(Integer value) {\n            System.out.println("Получено: " + value);\n            request(1);  // запросить следующий\n        }\n    });' },
        { type: 'tip', value: 'Mono и Flux — это \"ленивые\" (lazy) вычисления. Они описывают ЧТО нужно сделать, но ничего не выполняют пока нет подписчика.' },
        { type: 'note', value: 'Spring WebFlux автоматически подписывается на возвращаемые Mono/Flux из контроллеров, поэтому вам не нужно вызывать subscribe() вручную.' }
      ]
    },
    {
      id: 2,
      title: 'Реактивные контроллеры',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Настройка Spring WebFlux' },
        { type: 'text', value: 'Spring WebFlux — это альтернатива Spring MVC для построения реактивных веб-приложений. Он работает на неблокирующих серверах (Netty по умолчанию) и использует event loop вместо thread-per-request.' },
        { type: 'code', language: 'xml', value: '<!-- pom.xml -->\n<dependency>\n    <groupId>org.springframework.boot</groupId>\n    <artifactId>spring-boot-starter-webflux</artifactId>\n</dependency>\n<!-- Важно: НЕ подключайте spring-boot-starter-web вместе с webflux -->' },
        { type: 'heading', value: 'Аннотационные контроллеры' },
        { type: 'code', language: 'java', value: '@RestController\n@RequestMapping("/api/users")\npublic class UserController {\n\n    private final UserService userService;\n\n    public UserController(UserService userService) {\n        this.userService = userService;\n    }\n\n    @GetMapping\n    public Flux<UserDto> getAllUsers() {\n        return userService.findAll()\n            .map(UserDto::from);\n    }\n\n    @GetMapping("/{id}")\n    public Mono<ResponseEntity<UserDto>> getUser(@PathVariable Long id) {\n        return userService.findById(id)\n            .map(user -> ResponseEntity.ok(UserDto.from(user)))\n            .defaultIfEmpty(ResponseEntity.notFound().build());\n    }\n\n    @PostMapping\n    @ResponseStatus(HttpStatus.CREATED)\n    public Mono<UserDto> createUser(@Valid @RequestBody Mono<CreateUserRequest> request) {\n        return request\n            .flatMap(userService::create)\n            .map(UserDto::from);\n    }\n\n    @DeleteMapping("/{id}")\n    public Mono<Void> deleteUser(@PathVariable Long id) {\n        return userService.deleteById(id);\n    }\n}' },
        { type: 'heading', value: 'Функциональные роутеры (RouterFunction)' },
        { type: 'text', value: 'Альтернативный способ определения маршрутов — функциональный стиль с RouterFunction и HandlerFunction.' },
        { type: 'code', language: 'java', value: '@Configuration\npublic class UserRouter {\n\n    @Bean\n    public RouterFunction<ServerResponse> userRoutes(UserHandler handler) {\n        return RouterFunctions.route()\n            .path("/api/users", builder -> builder\n                .GET("", handler::getAllUsers)\n                .GET("/{id}", handler::getUser)\n                .POST("", handler::createUser)\n                .DELETE("/{id}", handler::deleteUser)\n            )\n            .build();\n    }\n}\n\n@Component\npublic class UserHandler {\n\n    private final UserService userService;\n\n    public UserHandler(UserService userService) {\n        this.userService = userService;\n    }\n\n    public Mono<ServerResponse> getAllUsers(ServerRequest request) {\n        return ServerResponse.ok()\n            .contentType(MediaType.APPLICATION_JSON)\n            .body(userService.findAll(), User.class);\n    }\n\n    public Mono<ServerResponse> getUser(ServerRequest request) {\n        Long id = Long.valueOf(request.pathVariable("id"));\n        return userService.findById(id)\n            .flatMap(user -> ServerResponse.ok().bodyValue(user))\n            .switchIfEmpty(ServerResponse.notFound().build());\n    }\n\n    public Mono<ServerResponse> createUser(ServerRequest request) {\n        return request.bodyToMono(CreateUserRequest.class)\n            .flatMap(userService::create)\n            .flatMap(user -> ServerResponse\n                .created(URI.create("/api/users/" + user.getId()))\n                .bodyValue(user));\n    }\n}' },
        { type: 'heading', value: 'Server-Sent Events (SSE)' },
        { type: 'code', language: 'java', value: '@GetMapping(value = "/stream", produces = MediaType.TEXT_EVENT_STREAM_VALUE)\npublic Flux<UserDto> streamUsers() {\n    return userService.findAll()\n        .delayElements(Duration.ofMillis(500));  // имитация потока\n}\n\n// SSE с данными в реальном времени\n@GetMapping(value = "/notifications", produces = MediaType.TEXT_EVENT_STREAM_VALUE)\npublic Flux<ServerSentEvent<Notification>> streamNotifications() {\n    return notificationService.getStream()\n        .map(n -> ServerSentEvent.<Notification>builder()\n            .id(n.getId().toString())\n            .event("notification")\n            .data(n)\n            .build());\n}' },
        { type: 'warning', value: 'Никогда не вызывайте блокирующие операции (block(), Thread.sleep()) внутри реактивных цепочек — это заблокирует event loop и убьёт производительность.' }
      ]
    },
    {
      id: 3,
      title: 'R2DBC: реактивный доступ к БД',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Что такое R2DBC?' },
        { type: 'text', value: 'R2DBC (Reactive Relational Database Connectivity) — это спецификация для неблокирующего доступа к реляционным базам данных. В отличие от JDBC, R2DBC полностью асинхронный и совместим с Reactive Streams.' },
        { type: 'code', language: 'xml', value: '<!-- pom.xml -->\n<dependency>\n    <groupId>org.springframework.boot</groupId>\n    <artifactId>spring-boot-starter-data-r2dbc</artifactId>\n</dependency>\n<dependency>\n    <groupId>org.postgresql</groupId>\n    <artifactId>r2dbc-postgresql</artifactId>\n    <scope>runtime</scope>\n</dependency>' },
        { type: 'code', language: 'yaml', value: '# application.yml\nspring:\n  r2dbc:\n    url: r2dbc:postgresql://localhost:5432/mydb\n    username: user\n    password: password' },
        { type: 'heading', value: 'Entity и Repository' },
        { type: 'code', language: 'java', value: '// R2DBC Entity — НЕ использует JPA аннотации!\n@Table("users")\npublic class User {\n    @Id\n    private Long id;\n    private String name;\n    private String email;\n    @CreatedDate\n    private LocalDateTime createdAt;\n    // конструктор, геттеры, сеттеры\n}\n\n// Реактивный репозиторий\npublic interface UserRepository extends ReactiveCrudRepository<User, Long> {\n\n    Flux<User> findByNameContaining(String name);\n\n    Mono<User> findByEmail(String email);\n\n    @Query("SELECT * FROM users WHERE created_at > :date ORDER BY created_at DESC")\n    Flux<User> findRecentUsers(@Param("date") LocalDateTime date);\n\n    @Query("SELECT COUNT(*) FROM users WHERE email = :email")\n    Mono<Long> countByEmail(@Param("email") String email);\n}\n\n// ReactiveSortingRepository для сортировки\npublic interface UserRepository extends ReactiveSortingRepository<User, Long> {\n    Flux<User> findAllBy(Pageable pageable);  // пагинация\n}' },
        { type: 'heading', value: 'Сервис с реактивными операциями' },
        { type: 'code', language: 'java', value: '@Service\npublic class UserService {\n\n    private final UserRepository userRepository;\n\n    public UserService(UserRepository userRepository) {\n        this.userRepository = userRepository;\n    }\n\n    public Mono<User> create(CreateUserRequest request) {\n        return userRepository.findByEmail(request.getEmail())\n            .hasElement()\n            .flatMap(exists -> {\n                if (exists) {\n                    return Mono.error(new EmailAlreadyExistsException(request.getEmail()));\n                }\n                User user = new User();\n                user.setName(request.getName());\n                user.setEmail(request.getEmail());\n                return userRepository.save(user);\n            });\n    }\n\n    public Mono<User> update(Long id, UpdateUserRequest request) {\n        return userRepository.findById(id)\n            .switchIfEmpty(Mono.error(new UserNotFoundException(id)))\n            .flatMap(user -> {\n                user.setName(request.getName());\n                return userRepository.save(user);\n            });\n    }\n\n    public Flux<User> findAll() {\n        return userRepository.findAll();\n    }\n\n    public Mono<Void> deleteById(Long id) {\n        return userRepository.findById(id)\n            .switchIfEmpty(Mono.error(new UserNotFoundException(id)))\n            .flatMap(userRepository::delete);\n    }\n}' },
        { type: 'heading', value: 'Миграции с Flyway' },
        { type: 'text', value: 'R2DBC не поддерживает автосоздание таблиц как JPA. Используйте Flyway для миграций.' },
        { type: 'code', language: 'sql', value: '-- V1__create_users.sql\nCREATE TABLE users (\n    id BIGSERIAL PRIMARY KEY,\n    name VARCHAR(255) NOT NULL,\n    email VARCHAR(255) NOT NULL UNIQUE,\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n\nCREATE INDEX idx_users_email ON users(email);' },
        { type: 'note', value: 'R2DBC не поддерживает связи (@OneToMany, @ManyToOne) как JPA. Связи нужно реализовывать вручную через отдельные запросы или использовать DatabaseClient.' }
      ]
    },
    {
      id: 4,
      title: 'WebClient: реактивный HTTP-клиент',
      type: 'theory',
      content: [
        { type: 'heading', value: 'WebClient вместо RestTemplate' },
        { type: 'text', value: 'WebClient — это реактивный HTTP-клиент Spring WebFlux. Он заменяет устаревший RestTemplate и поддерживает как блокирующий, так и неблокирующий режим работы.' },
        { type: 'code', language: 'java', value: '@Configuration\npublic class WebClientConfig {\n\n    @Bean\n    public WebClient webClient(WebClient.Builder builder) {\n        return builder\n            .baseUrl("https://api.example.com")\n            .defaultHeader(HttpHeaders.CONTENT_TYPE, MediaType.APPLICATION_JSON_VALUE)\n            .defaultHeader(HttpHeaders.ACCEPT, MediaType.APPLICATION_JSON_VALUE)\n            .filter(ExchangeFilterFunctions.basicAuthentication("user", "pass"))\n            .build();\n    }\n\n    // WebClient с таймаутами\n    @Bean\n    public WebClient webClientWithTimeout() {\n        HttpClient httpClient = HttpClient.create()\n            .responseTimeout(Duration.ofSeconds(5))\n            .option(ChannelOption.CONNECT_TIMEOUT_MILLIS, 3000);\n\n        return WebClient.builder()\n            .clientConnector(new ReactorClientHttpConnector(httpClient))\n            .build();\n    }\n}' },
        { type: 'heading', value: 'GET запросы' },
        { type: 'code', language: 'java', value: '@Service\npublic class ExternalApiService {\n\n    private final WebClient webClient;\n\n    public ExternalApiService(WebClient webClient) {\n        this.webClient = webClient;\n    }\n\n    // Получить один объект\n    public Mono<ProductDto> getProduct(Long id) {\n        return webClient.get()\n            .uri("/products/{id}", id)\n            .retrieve()\n            .bodyToMono(ProductDto.class);\n    }\n\n    // Получить список\n    public Flux<ProductDto> getAllProducts() {\n        return webClient.get()\n            .uri("/products")\n            .retrieve()\n            .bodyToFlux(ProductDto.class);\n    }\n\n    // С параметрами запроса\n    public Flux<ProductDto> searchProducts(String query, int page) {\n        return webClient.get()\n            .uri(uriBuilder -> uriBuilder\n                .path("/products/search")\n                .queryParam("q", query)\n                .queryParam("page", page)\n                .build())\n            .retrieve()\n            .bodyToFlux(ProductDto.class);\n    }\n}' },
        { type: 'heading', value: 'POST, PUT, DELETE запросы' },
        { type: 'code', language: 'java', value: '// POST с телом\npublic Mono<ProductDto> createProduct(CreateProductRequest request) {\n    return webClient.post()\n        .uri("/products")\n        .bodyValue(request)\n        .retrieve()\n        .bodyToMono(ProductDto.class);\n}\n\n// PUT\npublic Mono<ProductDto> updateProduct(Long id, UpdateProductRequest request) {\n    return webClient.put()\n        .uri("/products/{id}", id)\n        .bodyValue(request)\n        .retrieve()\n        .bodyToMono(ProductDto.class);\n}\n\n// DELETE\npublic Mono<Void> deleteProduct(Long id) {\n    return webClient.delete()\n        .uri("/products/{id}", id)\n        .retrieve()\n        .bodyToMono(Void.class);\n}' },
        { type: 'heading', value: 'Обработка ошибок' },
        { type: 'code', language: 'java', value: 'public Mono<ProductDto> getProductSafe(Long id) {\n    return webClient.get()\n        .uri("/products/{id}", id)\n        .retrieve()\n        .onStatus(HttpStatusCode::is4xxClientError, response -> {\n            if (response.statusCode() == HttpStatus.NOT_FOUND) {\n                return Mono.error(new ProductNotFoundException(id));\n            }\n            return response.bodyToMono(String.class)\n                .flatMap(body -> Mono.error(new ClientException(body)));\n        })\n        .onStatus(HttpStatusCode::is5xxServerError, response ->\n            Mono.error(new ExternalServiceException("Сервис недоступен")))\n        .bodyToMono(ProductDto.class)\n        .timeout(Duration.ofSeconds(5))\n        .retryWhen(Retry.backoff(3, Duration.ofSeconds(1))\n            .filter(e -> e instanceof ExternalServiceException));  // retry только 5xx\n}' },
        { type: 'heading', value: 'Exchange для полного контроля' },
        { type: 'code', language: 'java', value: 'public Mono<ProductDto> getProductWithHeaders(Long id) {\n    return webClient.get()\n        .uri("/products/{id}", id)\n        .exchangeToMono(response -> {\n            HttpHeaders headers = response.headers().asHttpHeaders();\n            String rateLimit = headers.getFirst("X-Rate-Limit-Remaining");\n            log.info("Rate limit remaining: {}", rateLimit);\n\n            if (response.statusCode().is2xxSuccessful()) {\n                return response.bodyToMono(ProductDto.class);\n            } else {\n                return response.createError();\n            }\n        });\n}' },
        { type: 'tip', value: 'WebClient можно использовать и в обычных Spring MVC приложениях. Он не требует WebFlux. Просто вызовите .block() для синхронного результата.' }
      ]
    },
    {
      id: 5,
      title: 'Тестирование реактивных потоков',
      type: 'theory',
      content: [
        { type: 'heading', value: 'StepVerifier — основной инструмент' },
        { type: 'text', value: 'StepVerifier из библиотеки reactor-test позволяет пошагово проверять реактивные потоки, включая элементы, ошибки и завершение.' },
        { type: 'code', language: 'xml', value: '<dependency>\n    <groupId>io.projectreactor</groupId>\n    <artifactId>reactor-test</artifactId>\n    <scope>test</scope>\n</dependency>' },
        { type: 'code', language: 'java', value: '@Test\nvoid testFlux() {\n    Flux<String> flux = Flux.just("A", "B", "C");\n\n    StepVerifier.create(flux)\n        .expectNext("A")\n        .expectNext("B")\n        .expectNext("C")\n        .verifyComplete();  // ожидаем завершение\n}\n\n@Test\nvoid testFluxCount() {\n    Flux<Integer> flux = Flux.range(1, 100);\n\n    StepVerifier.create(flux)\n        .expectNextCount(100)\n        .verifyComplete();\n}\n\n@Test\nvoid testMono() {\n    Mono<String> mono = Mono.just("Hello");\n\n    StepVerifier.create(mono)\n        .expectNext("Hello")\n        .verifyComplete();\n}\n\n@Test\nvoid testError() {\n    Mono<String> errorMono = Mono.error(new RuntimeException("Ошибка"));\n\n    StepVerifier.create(errorMono)\n        .expectError(RuntimeException.class)\n        .verify();\n}\n\n@Test\nvoid testErrorMessage() {\n    Mono<String> errorMono = Mono.error(new RuntimeException("not found"));\n\n    StepVerifier.create(errorMono)\n        .expectErrorMessage("not found")\n        .verify();\n}' },
        { type: 'heading', value: 'Тестирование сервисов' },
        { type: 'code', language: 'java', value: '@ExtendWith(MockitoExtension.class)\nclass UserServiceTest {\n\n    @Mock\n    UserRepository userRepository;\n\n    @InjectMocks\n    UserService userService;\n\n    @Test\n    void createUser_shouldSave() {\n        CreateUserRequest request = new CreateUserRequest("Иван", "ivan@mail.com");\n        User savedUser = new User(1L, "Иван", "ivan@mail.com", LocalDateTime.now());\n\n        when(userRepository.findByEmail("ivan@mail.com")).thenReturn(Mono.empty());\n        when(userRepository.save(any(User.class))).thenReturn(Mono.just(savedUser));\n\n        StepVerifier.create(userService.create(request))\n            .assertNext(user -> {\n                assertEquals("Иван", user.getName());\n                assertEquals("ivan@mail.com", user.getEmail());\n                assertNotNull(user.getId());\n            })\n            .verifyComplete();\n    }\n\n    @Test\n    void createUser_duplicateEmail_shouldFail() {\n        CreateUserRequest request = new CreateUserRequest("Иван", "existing@mail.com");\n        when(userRepository.findByEmail("existing@mail.com"))\n            .thenReturn(Mono.just(new User()));\n\n        StepVerifier.create(userService.create(request))\n            .expectError(EmailAlreadyExistsException.class)\n            .verify();\n    }\n}' },
        { type: 'heading', value: 'WebTestClient — тестирование контроллеров' },
        { type: 'code', language: 'java', value: '@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)\nclass UserControllerIT {\n\n    @Autowired\n    WebTestClient webTestClient;\n\n    @Test\n    void getAllUsers() {\n        webTestClient.get().uri("/api/users")\n            .exchange()\n            .expectStatus().isOk()\n            .expectBodyList(UserDto.class)\n            .hasSize(3);\n    }\n\n    @Test\n    void getUser_notFound() {\n        webTestClient.get().uri("/api/users/999")\n            .exchange()\n            .expectStatus().isNotFound();\n    }\n\n    @Test\n    void createUser() {\n        CreateUserRequest request = new CreateUserRequest("Тест", "test@mail.com");\n\n        webTestClient.post().uri("/api/users")\n            .contentType(MediaType.APPLICATION_JSON)\n            .bodyValue(request)\n            .exchange()\n            .expectStatus().isCreated()\n            .expectBody()\n            .jsonPath("$.name").isEqualTo("Тест")\n            .jsonPath("$.email").isEqualTo("test@mail.com")\n            .jsonPath("$.id").isNotEmpty();\n    }\n\n    @Test\n    void streamUsers_sse() {\n        webTestClient.get().uri("/api/users/stream")\n            .accept(MediaType.TEXT_EVENT_STREAM)\n            .exchange()\n            .expectStatus().isOk()\n            .returnResult(UserDto.class)\n            .getResponseBody()\n            .as(StepVerifier::create)\n            .expectNextCount(3)\n            .thenCancel()\n            .verify();\n    }\n}' },
        { type: 'heading', value: 'Тестирование WebClient с MockWebServer' },
        { type: 'code', language: 'java', value: 'class ExternalApiServiceTest {\n\n    private MockWebServer mockServer;\n    private ExternalApiService service;\n\n    @BeforeEach\n    void setUp() throws IOException {\n        mockServer = new MockWebServer();\n        mockServer.start();\n        WebClient webClient = WebClient.builder()\n            .baseUrl(mockServer.url("/").toString())\n            .build();\n        service = new ExternalApiService(webClient);\n    }\n\n    @AfterEach\n    void tearDown() throws IOException {\n        mockServer.shutdown();\n    }\n\n    @Test\n    void getProduct_success() {\n        mockServer.enqueue(new MockResponse()\n            .setBody("{\"id\":1,\"name\":\"Test\"}")\n            .setHeader("Content-Type", "application/json"));\n\n        StepVerifier.create(service.getProduct(1L))\n            .assertNext(p -> assertEquals("Test", p.getName()))\n            .verifyComplete();\n    }\n\n    @Test\n    void getProduct_notFound() {\n        mockServer.enqueue(new MockResponse().setResponseCode(404));\n\n        StepVerifier.create(service.getProduct(999L))\n            .expectError(ProductNotFoundException.class)\n            .verify();\n    }\n}' },
        { type: 'tip', value: 'StepVerifier.withVirtualTime() позволяет тестировать операции с задержками без реального ожидания: StepVerifier.withVirtualTime(() -> Flux.interval(Duration.ofHours(1)).take(3)).thenAwait(Duration.ofHours(3)).expectNextCount(3).verifyComplete()' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Реактивный CRUD API',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создайте реактивный REST API для управления задачами (Todo) с использованием Spring WebFlux и R2DBC.',
      requirements: [
        'Создать Entity Todo с полями: id, title, description, completed, createdAt',
        'ReactiveCrudRepository с методами findByCompleted(boolean) и findByTitleContaining(String)',
        'GET /api/todos — все задачи (Flux)',
        'GET /api/todos/{id} — одна задача (Mono) с 404 если не найдена',
        'POST /api/todos — создать задачу',
        'PUT /api/todos/{id}/complete — отметить как выполненную',
        'DELETE /api/todos/{id} — удалить задачу',
        'Написать StepVerifier тесты для сервиса'
      ],
      hint: 'Используйте switchIfEmpty(Mono.error(...)) для обработки 404. Контроллер должен возвращать Mono<ResponseEntity<...>> для контроля HTTP статуса.',
      expectedOutput: 'POST /api/todos {"title":"Изучить WebFlux","description":"Пройти модуль 34"}:\nHTTP 201 Created\n{"id":1,"title":"Изучить WebFlux","completed":false,"createdAt":"2026-04-05T10:00:00"}\n\nGET /api/todos:\n[{"id":1,"title":"Изучить WebFlux","completed":false}]\n\nPUT /api/todos/1/complete:\n{"id":1,"title":"Изучить WebFlux","completed":true}\n\nGET /api/todos/999:\nHTTP 404 Not Found',
      solution: '@Table("todos")\npublic class Todo {\n    @Id private Long id;\n    private String title;\n    private String description;\n    private boolean completed = false;\n    @CreatedDate private LocalDateTime createdAt;\n}\n\npublic interface TodoRepository extends ReactiveCrudRepository<Todo, Long> {\n    Flux<Todo> findByCompleted(boolean completed);\n    Flux<Todo> findByTitleContaining(String title);\n}\n\n@Service\npublic class TodoService {\n    private final TodoRepository repo;\n\n    public Mono<Todo> create(CreateTodoRequest req) {\n        Todo todo = new Todo();\n        todo.setTitle(req.getTitle());\n        todo.setDescription(req.getDescription());\n        return repo.save(todo);\n    }\n\n    public Mono<Todo> complete(Long id) {\n        return repo.findById(id)\n            .switchIfEmpty(Mono.error(new TodoNotFoundException(id)))\n            .flatMap(todo -> {\n                todo.setCompleted(true);\n                return repo.save(todo);\n            });\n    }\n}\n\n@RestController\n@RequestMapping("/api/todos")\npublic class TodoController {\n    private final TodoService service;\n\n    @GetMapping\n    public Flux<Todo> getAll() { return service.findAll(); }\n\n    @GetMapping("/{id}")\n    public Mono<ResponseEntity<Todo>> getById(@PathVariable Long id) {\n        return service.findById(id)\n            .map(ResponseEntity::ok)\n            .defaultIfEmpty(ResponseEntity.notFound().build());\n    }\n\n    @PostMapping @ResponseStatus(HttpStatus.CREATED)\n    public Mono<Todo> create(@RequestBody CreateTodoRequest req) {\n        return service.create(req);\n    }\n\n    @PutMapping("/{id}/complete")\n    public Mono<Todo> complete(@PathVariable Long id) {\n        return service.complete(id);\n    }\n}',
      explanation: 'Spring WebFlux + R2DBC обеспечивает полностью неблокирующий стек. ReactiveCrudRepository возвращает Mono/Flux вместо Optional/List. switchIfEmpty заменяет orElseThrow() из блокирующего мира.'
    }
  ]
}
