export default {
  id: 57,
  title: 'Практикум: Тестирование',
  description: 'Полное тестирование Spring Boot: контроллеры (@WebMvcTest), репозитории (@DataJpaTest), интеграционные тесты, MockMvc, TestContainers, REST Assured и тесты безопасности.',
  lessons: [
    {
      id: 1,
      title: 'Задача: @WebMvcTest — тесты контроллеров',
      type: 'practice',
      difficulty: 'easy',
      description: 'Напишите unit-тесты для REST-контроллера с @WebMvcTest и MockMvc.',
      requirements: [
        '@WebMvcTest(UserController.class) — загружает только веб-слой',
        '@MockBean UserService — мокаем зависимости',
        'Тест GET /api/users/{id} → 200 с JSON телом',
        'Тест GET /api/users/999 → 404',
        'Тест POST /api/users → 201 с валидным телом',
        'Тест POST /api/users → 422 с невалидным телом'
      ],
      expectedOutput: 'UserControllerTest:\n✓ getUserById_ReturnsUser (GET 200)\n✓ getUserById_NotFound_Returns404 (GET 404)\n✓ createUser_ValidInput_Returns201 (POST 201)\n✓ createUser_InvalidInput_Returns422 (POST 422)\n✓ getAllUsers_ReturnsList (GET 200)\n✓ deleteUser_Returns204 (DELETE 204)\n\n6 tests passed in 1.2s',
      hint: 'mockMvc.perform(get("/api/users/1")).andExpect(status().isOk()).andExpect(jsonPath("$.name").value("Иван")). Используйте when(service.findById(1L)).thenReturn(user).',
      solution: '@WebMvcTest(UserController.class)\nclass UserControllerTest {\n\n    @Autowired MockMvc mockMvc;\n    @Autowired ObjectMapper objectMapper;\n    @MockBean UserService userService;\n\n    @Test\n    void getUserById_ReturnsUser() throws Exception {\n        UserResponse user = new UserResponse(1L, "Иван", "ivan@mail.ru");\n        when(userService.findById(1L)).thenReturn(user);\n\n        mockMvc.perform(get("/api/users/1"))\n            .andExpect(status().isOk())\n            .andExpect(content().contentType(MediaType.APPLICATION_JSON))\n            .andExpect(jsonPath("$.id").value(1))\n            .andExpect(jsonPath("$.name").value("Иван"))\n            .andExpect(jsonPath("$.email").value("ivan@mail.ru"));\n    }\n\n    @Test\n    void getUserById_NotFound_Returns404() throws Exception {\n        when(userService.findById(999L))\n            .thenThrow(new ResourceNotFoundException("User", 999L));\n\n        mockMvc.perform(get("/api/users/999"))\n            .andExpect(status().isNotFound())\n            .andExpect(jsonPath("$.message").value("User with id 999 not found"));\n    }\n\n    @Test\n    void createUser_ValidInput_Returns201() throws Exception {\n        CreateUserRequest request = new CreateUserRequest("Иван", "ivan@mail.ru", "password123");\n        UserResponse response = new UserResponse(1L, "Иван", "ivan@mail.ru");\n        when(userService.create(any(CreateUserRequest.class))).thenReturn(response);\n\n        mockMvc.perform(post("/api/users")\n                .contentType(MediaType.APPLICATION_JSON)\n                .content(objectMapper.writeValueAsString(request)))\n            .andExpect(status().isCreated())\n            .andExpect(jsonPath("$.id").value(1))\n            .andExpect(jsonPath("$.name").value("Иван"));\n    }\n\n    @Test\n    void createUser_InvalidInput_Returns422() throws Exception {\n        CreateUserRequest request = new CreateUserRequest("", "invalid", "12");\n\n        mockMvc.perform(post("/api/users")\n                .contentType(MediaType.APPLICATION_JSON)\n                .content(objectMapper.writeValueAsString(request)))\n            .andExpect(status().isUnprocessableEntity())\n            .andExpect(jsonPath("$.errors").isArray())\n            .andExpect(jsonPath("$.errors.length()").value(greaterThan(0)));\n    }\n\n    @Test\n    void deleteUser_Returns204() throws Exception {\n        doNothing().when(userService).delete(1L);\n\n        mockMvc.perform(delete("/api/users/1"))\n            .andExpect(status().isNoContent());\n\n        verify(userService).delete(1L);\n    }\n}',
      explanation: '@WebMvcTest загружает только контроллеры, фильтры и @ControllerAdvice — без сервисов и репозиториев (быстро). @MockBean создаёт Mockito-мок и регистрирует его как Spring Bean. MockMvc отправляет HTTP-запросы без реального сервера. jsonPath проверяет JSON-ответ по XPath-подобному синтаксису.'
    },
    {
      id: 2,
      title: 'Задача: @DataJpaTest — тесты репозиториев',
      type: 'practice',
      difficulty: 'easy',
      description: 'Напишите тесты для репозиториев с @DataJpaTest и встроенной H2 базой данных.',
      requirements: [
        '@DataJpaTest — загружает только JPA-компоненты',
        'Тест кастомного @Query метода',
        'Тест пагинации и сортировки',
        'Тест каскадного сохранения',
        'TestEntityManager для подготовки тестовых данных',
        'Проверка генерации SQL (hibernate.show_sql=true)'
      ],
      expectedOutput: 'ProductRepositoryTest:\n✓ findByCategory_ReturnsFilteredProducts\n✓ findByPriceBetween_ReturnsCorrectRange\n✓ searchByName_CaseInsensitive\n✓ findTopExpensive_WithPagination\n✓ customQuery_CountByCategory\n✓ cascade_SaveProductWithTags\n\n6 tests passed in 0.8s (H2 in-memory)',
      hint: '@DataJpaTest по умолчанию использует H2. TestEntityManager.persistAndFlush() гарантирует что данные в БД. @Sql для предзагрузки данных.',
      solution: '@DataJpaTest\n@AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.NONE)\nclass ProductRepositoryTest {\n\n    @Autowired ProductRepository repository;\n    @Autowired TestEntityManager em;\n\n    @BeforeEach\n    void setUp() {\n        Product laptop = new Product("Laptop", "Electronics", new BigDecimal("999.99"), 10);\n        Product phone = new Product("Phone", "Electronics", new BigDecimal("599.99"), 25);\n        Product book = new Product("Spring in Action", "Books", new BigDecimal("49.99"), 100);\n        em.persistAndFlush(laptop);\n        em.persistAndFlush(phone);\n        em.persistAndFlush(book);\n    }\n\n    @Test\n    void findByCategory_ReturnsFilteredProducts() {\n        List<Product> electronics = repository.findByCategory("Electronics");\n\n        assertThat(electronics).hasSize(2);\n        assertThat(electronics).extracting(Product::getName)\n            .containsExactlyInAnyOrder("Laptop", "Phone");\n    }\n\n    @Test\n    void findByPriceBetween_ReturnsCorrectRange() {\n        List<Product> products = repository.findByPriceBetween(\n            new BigDecimal("100"), new BigDecimal("700"));\n\n        assertThat(products).hasSize(1);\n        assertThat(products.get(0).getName()).isEqualTo("Phone");\n    }\n\n    @Test\n    void searchByName_CaseInsensitive() {\n        List<Product> results = repository.findByNameContainingIgnoreCase("spring");\n\n        assertThat(results).hasSize(1);\n        assertThat(results.get(0).getName()).isEqualTo("Spring in Action");\n    }\n\n    @Test\n    void findTopExpensive_WithPagination() {\n        Page<Product> page = repository.findAll(\n            PageRequest.of(0, 2, Sort.by(Sort.Direction.DESC, "price")));\n\n        assertThat(page.getContent()).hasSize(2);\n        assertThat(page.getContent().get(0).getName()).isEqualTo("Laptop");\n        assertThat(page.getTotalElements()).isEqualTo(3);\n    }\n\n    @Test\n    void customQuery_CountByCategory() {\n        List<Object[]> counts = repository.countActiveByCategory();\n\n        assertThat(counts).hasSize(2);\n    }\n\n    @Test\n    void cascade_SaveProductWithTags() {\n        Product product = new Product("New Product", "Tech", BigDecimal.TEN, 5);\n        product.addTag(new Tag("java"));\n        product.addTag(new Tag("spring"));\n\n        Product saved = repository.save(product);\n        em.flush();\n        em.clear();\n\n        Product found = repository.findById(saved.getId()).orElseThrow();\n        assertThat(found.getTags()).hasSize(2);\n    }\n}',
      explanation: '@DataJpaTest настраивает JPA-слой: Entity, Repository, EntityManager. Каждый тест выполняется в транзакции и откатывается (@Transactional). TestEntityManager — утилита для подготовки данных (persist, find, flush). em.clear() очищает persistence context, заставляя следующий find обратиться к БД.'
    },
    {
      id: 3,
      title: 'Задача: @SpringBootTest — интеграционные тесты',
      type: 'practice',
      difficulty: 'medium',
      description: 'Напишите полные интеграционные тесты, поднимающие весь контекст приложения.',
      requirements: [
        '@SpringBootTest(webEnvironment = RANDOM_PORT)',
        'TestRestTemplate для HTTP-вызовов',
        'Тест полного цикла: создание → чтение → обновление → удаление',
        'Проверка сохранения в БД через репозиторий',
        'Проверка корректности HTTP-заголовков',
        'Тест с аутентификацией'
      ],
      expectedOutput: 'UserIntegrationTest:\n✓ fullCrudCycle — create, read, update, delete user\n✓ createUser_PersistsInDatabase\n✓ getUser_ReturnsCorrectHeaders\n✓ createDuplicate_Returns409\n✓ authenticatedEndpoint_WithToken\n\nApplication started on random port 54321\nAll 5 tests passed in 4.5s',
      hint: '@LocalServerPort получает случайный порт. TestRestTemplate автоматически использует этот порт. @DirtiesContext сбрасывает контекст между тестами.',
      solution: '@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)\nclass UserIntegrationTest {\n\n    @Autowired TestRestTemplate restTemplate;\n    @Autowired UserRepository userRepository;\n    @LocalServerPort int port;\n\n    @BeforeEach\n    void setUp() {\n        userRepository.deleteAll();\n    }\n\n    @Test\n    void fullCrudCycle() {\n        // Create\n        CreateUserRequest createReq = new CreateUserRequest("Иван", "ivan@mail.ru", "password123");\n        ResponseEntity<UserResponse> createResp = restTemplate.postForEntity(\n            "/api/users", createReq, UserResponse.class);\n\n        assertThat(createResp.getStatusCode()).isEqualTo(HttpStatus.CREATED);\n        Long userId = createResp.getBody().id();\n        assertThat(userId).isNotNull();\n\n        // Read\n        ResponseEntity<UserResponse> getResp = restTemplate.getForEntity(\n            "/api/users/" + userId, UserResponse.class);\n        assertThat(getResp.getStatusCode()).isEqualTo(HttpStatus.OK);\n        assertThat(getResp.getBody().name()).isEqualTo("Иван");\n\n        // Update\n        UpdateUserRequest updateReq = new UpdateUserRequest("Пётр", null, null);\n        restTemplate.put("/api/users/" + userId, updateReq);\n\n        UserResponse updated = restTemplate.getForObject(\n            "/api/users/" + userId, UserResponse.class);\n        assertThat(updated.name()).isEqualTo("Пётр");\n\n        // Delete\n        restTemplate.delete("/api/users/" + userId);\n        ResponseEntity<Void> deleteResp = restTemplate.getForEntity(\n            "/api/users/" + userId, Void.class);\n        assertThat(deleteResp.getStatusCode()).isEqualTo(HttpStatus.NOT_FOUND);\n    }\n\n    @Test\n    void createUser_PersistsInDatabase() {\n        CreateUserRequest request = new CreateUserRequest("Мария", "maria@mail.ru", "password123");\n        restTemplate.postForEntity("/api/users", request, UserResponse.class);\n\n        Optional<User> fromDb = userRepository.findByEmail("maria@mail.ru");\n        assertThat(fromDb).isPresent();\n        assertThat(fromDb.get().getName()).isEqualTo("Мария");\n    }\n\n    @Test\n    void createDuplicate_Returns409() {\n        CreateUserRequest request = new CreateUserRequest("Иван", "ivan@mail.ru", "password123");\n        restTemplate.postForEntity("/api/users", request, UserResponse.class);\n\n        ResponseEntity<ErrorResponse> duplicate = restTemplate.postForEntity(\n            "/api/users", request, ErrorResponse.class);\n        assertThat(duplicate.getStatusCode()).isEqualTo(HttpStatus.CONFLICT);\n    }\n\n    @Test\n    void getUser_ReturnsCorrectHeaders() {\n        CreateUserRequest request = new CreateUserRequest("Тест", "test@mail.ru", "password123");\n        ResponseEntity<UserResponse> resp = restTemplate.postForEntity(\n            "/api/users", request, UserResponse.class);\n\n        assertThat(resp.getHeaders().getContentType())\n            .isEqualTo(MediaType.APPLICATION_JSON);\n        assertThat(resp.getHeaders().getLocation()).isNotNull();\n    }\n}',
      explanation: '@SpringBootTest поднимает полный контекст приложения с реальным HTTP-сервером на случайном порту. TestRestTemplate отправляет реальные HTTP-запросы. Это тестирует всю цепочку: Controller → Service → Repository → DB. Медленнее unit-тестов, но проверяет реальную интеграцию компонентов.'
    },
    {
      id: 4,
      title: 'Задача: MockMvc — все HTTP-методы',
      type: 'practice',
      difficulty: 'medium',
      description: 'Протестируйте все HTTP-методы (POST, GET, PUT, PATCH, DELETE) через MockMvc.',
      requirements: [
        'POST с JSON телом и проверка Location заголовка',
        'GET с query параметрами и пагинацией',
        'PUT — полное обновление',
        'PATCH — частичное обновление',
        'DELETE — удаление',
        'Тестирование multipart/form-data (загрузка файлов)'
      ],
      expectedOutput: 'MockMvc HTTP Methods Test:\n✓ POST /api/products → 201 + Location header\n✓ GET /api/products?category=Electronics&page=0 → 200 + paginated\n✓ PUT /api/products/1 → 200 (full update)\n✓ PATCH /api/products/1 → 200 (partial update)\n✓ DELETE /api/products/1 → 204\n✓ POST /api/products/1/image → 201 (multipart upload)\n\n6 tests passed',
      hint: 'mockMvc.perform(multipart("/api/products/1/image").file(mockFile)). Для PATCH используйте MockMvcRequestBuilders.patch().',
      solution: '@WebMvcTest(ProductController.class)\nclass ProductControllerMockMvcTest {\n\n    @Autowired MockMvc mockMvc;\n    @Autowired ObjectMapper mapper;\n    @MockBean ProductService service;\n\n    @Test\n    void post_CreatesProduct_Returns201() throws Exception {\n        CreateProductRequest request = new CreateProductRequest("Laptop", "Electronics", BigDecimal.valueOf(999.99));\n        ProductResponse response = new ProductResponse(1L, "Laptop", "Electronics", BigDecimal.valueOf(999.99));\n        when(service.create(any())).thenReturn(response);\n\n        mockMvc.perform(post("/api/products")\n                .contentType(MediaType.APPLICATION_JSON)\n                .content(mapper.writeValueAsString(request)))\n            .andExpect(status().isCreated())\n            .andExpect(header().exists("Location"))\n            .andExpect(header().string("Location", containsString("/api/products/1")))\n            .andExpect(jsonPath("$.id").value(1));\n    }\n\n    @Test\n    void get_WithPagination_ReturnsList() throws Exception {\n        Page<ProductResponse> page = new PageImpl<>(List.of(\n            new ProductResponse(1L, "Laptop", "Electronics", BigDecimal.valueOf(999.99))\n        ), PageRequest.of(0, 10), 1);\n        when(service.findByCategory(eq("Electronics"), any(Pageable.class))).thenReturn(page);\n\n        mockMvc.perform(get("/api/products")\n                .param("category", "Electronics")\n                .param("page", "0")\n                .param("size", "10"))\n            .andExpect(status().isOk())\n            .andExpect(jsonPath("$.content").isArray())\n            .andExpect(jsonPath("$.content.length()").value(1))\n            .andExpect(jsonPath("$.totalElements").value(1));\n    }\n\n    @Test\n    void put_FullUpdate_Returns200() throws Exception {\n        UpdateProductRequest request = new UpdateProductRequest("Updated", "Tech", BigDecimal.valueOf(899.99));\n        ProductResponse response = new ProductResponse(1L, "Updated", "Tech", BigDecimal.valueOf(899.99));\n        when(service.update(eq(1L), any())).thenReturn(response);\n\n        mockMvc.perform(put("/api/products/1")\n                .contentType(MediaType.APPLICATION_JSON)\n                .content(mapper.writeValueAsString(request)))\n            .andExpect(status().isOk())\n            .andExpect(jsonPath("$.name").value("Updated"));\n    }\n\n    @Test\n    void patch_PartialUpdate_Returns200() throws Exception {\n        String patchJson = "{\\\"name\\\":\\\"New Name\\\"}";\n        ProductResponse response = new ProductResponse(1L, "New Name", "Electronics", BigDecimal.valueOf(999.99));\n        when(service.patch(eq(1L), any())).thenReturn(response);\n\n        mockMvc.perform(patch("/api/products/1")\n                .contentType(MediaType.APPLICATION_JSON)\n                .content(patchJson))\n            .andExpect(status().isOk())\n            .andExpect(jsonPath("$.name").value("New Name"));\n    }\n\n    @Test\n    void delete_Returns204() throws Exception {\n        mockMvc.perform(delete("/api/products/1"))\n            .andExpect(status().isNoContent());\n        verify(service).delete(1L);\n    }\n\n    @Test\n    void multipartUpload_Returns201() throws Exception {\n        MockMultipartFile file = new MockMultipartFile(\n            "file", "image.jpg", "image/jpeg", "fake-image-data".getBytes());\n\n        when(service.uploadImage(eq(1L), any())).thenReturn("image-url");\n\n        mockMvc.perform(multipart("/api/products/1/image").file(file))\n            .andExpect(status().isCreated())\n            .andExpect(jsonPath("$.imageUrl").value("image-url"));\n    }\n}',
      explanation: 'MockMvc тестирует контроллеры без запуска HTTP-сервера — быстрее TestRestTemplate. Каждый HTTP-метод имеет свой builder: get(), post(), put(), patch(), delete(), multipart(). jsonPath() проверяет JSON-ответ. header() проверяет HTTP-заголовки. verify() подтверждает что мок был вызван.'
    },
    {
      id: 5,
      title: 'Задача: @MockBean и мокирование сервисов',
      type: 'practice',
      difficulty: 'medium',
      description: 'Освойте продвинутое мокирование с Mockito: аргументы, цепочки, исключения.',
      requirements: [
        'when().thenReturn() — базовое мокирование',
        'when().thenThrow() — мокирование исключений',
        'ArgumentCaptor — захват аргументов вызова',
        'verify() — проверка вызовов (times, never, atLeast)',
        'doNothing().when() — для void методов',
        'Answer — динамический ответ на основе аргументов'
      ],
      expectedOutput: 'MockingTest:\n✓ basicMocking_ReturnsExpected\n✓ throwException_OnInvalidInput\n✓ captureArgument_VerifyServiceInput\n✓ verifyCallCount_ExactlyOnce\n✓ voidMethod_DoNothing\n✓ dynamicAnswer_BasedOnInput\n\n6 tests passed',
      hint: 'ArgumentCaptor<CreateUserRequest> captor = ArgumentCaptor.forClass(CreateUserRequest.class); verify(service).create(captor.capture()); assertThat(captor.getValue().name()).isEqualTo("Иван");',
      solution: '@WebMvcTest(UserController.class)\nclass MockingExamplesTest {\n\n    @Autowired MockMvc mockMvc;\n    @Autowired ObjectMapper mapper;\n    @MockBean UserService userService;\n    @MockBean EmailService emailService;\n\n    @Test\n    void basicMocking_ReturnsExpected() throws Exception {\n        UserResponse user = new UserResponse(1L, "Иван", "ivan@mail.ru");\n        when(userService.findById(1L)).thenReturn(user);\n\n        mockMvc.perform(get("/api/users/1"))\n            .andExpect(jsonPath("$.name").value("Иван"));\n    }\n\n    @Test\n    void throwException_OnInvalidInput() throws Exception {\n        when(userService.findById(999L))\n            .thenThrow(new ResourceNotFoundException("User", 999L));\n\n        mockMvc.perform(get("/api/users/999"))\n            .andExpect(status().isNotFound());\n    }\n\n    @Test\n    void captureArgument_VerifyServiceInput() throws Exception {\n        ArgumentCaptor<CreateUserRequest> captor =\n            ArgumentCaptor.forClass(CreateUserRequest.class);\n        UserResponse response = new UserResponse(1L, "Иван", "ivan@mail.ru");\n        when(userService.create(any())).thenReturn(response);\n\n        CreateUserRequest request = new CreateUserRequest("Иван", "ivan@mail.ru", "password123");\n        mockMvc.perform(post("/api/users")\n            .contentType(MediaType.APPLICATION_JSON)\n            .content(mapper.writeValueAsString(request)));\n\n        verify(userService).create(captor.capture());\n        assertThat(captor.getValue().name()).isEqualTo("Иван");\n        assertThat(captor.getValue().email()).isEqualTo("ivan@mail.ru");\n    }\n\n    @Test\n    void verifyCallCount() throws Exception {\n        when(userService.findById(anyLong())).thenReturn(\n            new UserResponse(1L, "Test", "test@mail.ru"));\n\n        mockMvc.perform(get("/api/users/1"));\n        mockMvc.perform(get("/api/users/2"));\n\n        verify(userService, times(2)).findById(anyLong());\n        verify(userService).findById(1L);\n        verify(userService).findById(2L);\n        verify(emailService, never()).sendEmail(any());\n    }\n\n    @Test\n    void voidMethod_DoNothing() throws Exception {\n        doNothing().when(userService).delete(1L);\n        doThrow(new ResourceNotFoundException("User", 999L))\n            .when(userService).delete(999L);\n\n        mockMvc.perform(delete("/api/users/1")).andExpect(status().isNoContent());\n        mockMvc.perform(delete("/api/users/999")).andExpect(status().isNotFound());\n    }\n\n    @Test\n    void dynamicAnswer_BasedOnInput() throws Exception {\n        when(userService.findById(anyLong())).thenAnswer(invocation -> {\n            Long id = invocation.getArgument(0);\n            if (id > 100) throw new ResourceNotFoundException("User", id);\n            return new UserResponse(id, "User" + id, "user" + id + "@mail.ru");\n        });\n\n        mockMvc.perform(get("/api/users/5"))\n            .andExpect(status().isOk())\n            .andExpect(jsonPath("$.name").value("User5"));\n\n        mockMvc.perform(get("/api/users/200"))\n            .andExpect(status().isNotFound());\n    }\n}',
      explanation: 'ArgumentCaptor захватывает аргументы переданные моку — позволяет проверить что контроллер правильно передал данные в сервис. verify() проверяет количество и аргументы вызовов. Answer позволяет создать динамическую логику мока — полезно когда результат зависит от входных данных.'
    },
    {
      id: 6,
      title: 'Задача: TestContainers для PostgreSQL',
      type: 'practice',
      difficulty: 'hard',
      description: 'Используйте TestContainers для запуска реального PostgreSQL в Docker-контейнере для тестов.',
      requirements: [
        'Подключить testcontainers и testcontainers-postgresql',
        '@Container для PostgreSQL контейнера',
        '@DynamicPropertySource для подключения к контейнеру',
        'Тест с реальным PostgreSQL (не H2)',
        'Общий контейнер для всех тестов (singleton pattern)',
        'Flyway миграции выполняются в тестовом контейнере'
      ],
      expectedOutput: 'PostgreSQL TestContainer starting...\nDocker: pulling postgres:16-alpine\nContainer started: localhost:55432\n\nProductRepositoryIT:\n✓ findByCategory_WithRealPostgreSQL\n✓ fullTextSearch_PostgresSpecific\n✓ nativeQuery_WindowFunction\n✓ flywayMigrations_Applied\n\n4 tests passed in 8.5s\nContainer stopped.',
      hint: '@Testcontainers + @Container автоматически управляют жизненным циклом контейнера. Используйте static container для переиспользования между тестами.',
      solution: '@SpringBootTest\n@Testcontainers\nclass ProductRepositoryIT {\n\n    @Container\n    static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:16-alpine")\n        .withDatabaseName("testdb")\n        .withUsername("test")\n        .withPassword("test");\n\n    @DynamicPropertySource\n    static void configureProperties(DynamicPropertyRegistry registry) {\n        registry.add("spring.datasource.url", postgres::getJdbcUrl);\n        registry.add("spring.datasource.username", postgres::getUsername);\n        registry.add("spring.datasource.password", postgres::getPassword);\n        registry.add("spring.jpa.hibernate.ddl-auto", () -> "create-drop");\n    }\n\n    @Autowired ProductRepository repository;\n\n    @BeforeEach\n    void setUp() {\n        repository.deleteAll();\n        repository.saveAll(List.of(\n            new Product("Laptop", "Electronics", new BigDecimal("999.99"), 10),\n            new Product("Phone", "Electronics", new BigDecimal("599.99"), 25),\n            new Product("Spring Boot Book", "Books", new BigDecimal("49.99"), 100)\n        ));\n    }\n\n    @Test\n    void findByCategory_WithRealPostgreSQL() {\n        List<Product> products = repository.findByCategory("Electronics");\n        assertThat(products).hasSize(2);\n    }\n\n    @Test\n    void nativeQuery_WorksWithPostgreSQL() {\n        // Native query специфичный для PostgreSQL\n        List<Object[]> results = repository.findWithRank();\n        assertThat(results).isNotEmpty();\n    }\n}\n\n// Singleton паттерн — один контейнер на все тесты\npublic abstract class AbstractIntegrationTest {\n\n    static final PostgreSQLContainer<?> POSTGRES;\n\n    static {\n        POSTGRES = new PostgreSQLContainer<>("postgres:16-alpine")\n            .withDatabaseName("testdb")\n            .withUsername("test")\n            .withPassword("test")\n            .withReuse(true);\n        POSTGRES.start();\n    }\n\n    @DynamicPropertySource\n    static void configureProperties(DynamicPropertyRegistry registry) {\n        registry.add("spring.datasource.url", POSTGRES::getJdbcUrl);\n        registry.add("spring.datasource.username", POSTGRES::getUsername);\n        registry.add("spring.datasource.password", POSTGRES::getPassword);\n    }\n}\n\n// Использование\n@SpringBootTest\nclass OrderServiceIT extends AbstractIntegrationTest {\n    // Использует тот же PostgreSQL контейнер\n}',
      explanation: 'TestContainers запускает реальный PostgreSQL в Docker — тесты работают с реальной СУБД, а не H2. @DynamicPropertySource подставляет URL контейнера в Spring properties. Singleton паттерн (static блок) переиспользует контейнер между тестами — запуск Docker-контейнера дорогой. withReuse(true) сохраняет контейнер между запусками тестов.'
    },
    {
      id: 7,
      title: 'Задача: Тестовые фикстуры и билдеры',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создайте удобные тестовые фикстуры с паттерном Builder для подготовки тестовых данных.',
      requirements: [
        'TestDataBuilder для User с дефолтными значениями',
        'TestDataBuilder для Product с цепочкой методов',
        'Фабрика тестовых данных: TestFixtures',
        '@Sql для загрузки данных из SQL-файлов',
        'Рандомизация тестовых данных (Faker/Instancio)',
        'Переиспользуемые тестовые сценарии'
      ],
      expectedOutput: 'User user = UserTestBuilder.aUser()\n    .withName("Тест")\n    .withEmail("test@mail.ru")\n    .withRole(ADMIN)\n    .build();\n\nProduct product = ProductTestBuilder.aProduct()\n    .withCategory("Electronics")\n    .withPrice(999.99)\n    .build();\n\nTestFixtures.createOrder(user, product, 2) → Order с 2 items',
      hint: 'Builder паттерн с дефолтными значениями: каждый with-метод опциональный. Без вызова используются дефолты. build() создаёт объект.',
      solution: 'public class UserTestBuilder {\n    private Long id = null;\n    private String name = "Test User";\n    private String email = "test@mail.ru";\n    private String password = "password123";\n    private Role role = Role.USER;\n    private boolean active = true;\n\n    public static UserTestBuilder aUser() {\n        return new UserTestBuilder();\n    }\n\n    public UserTestBuilder withId(Long id) { this.id = id; return this; }\n    public UserTestBuilder withName(String name) { this.name = name; return this; }\n    public UserTestBuilder withEmail(String email) { this.email = email; return this; }\n    public UserTestBuilder withPassword(String password) { this.password = password; return this; }\n    public UserTestBuilder withRole(Role role) { this.role = role; return this; }\n    public UserTestBuilder inactive() { this.active = false; return this; }\n\n    public User build() {\n        User user = new User();\n        user.setId(id);\n        user.setName(name);\n        user.setEmail(email);\n        user.setPassword(password);\n        user.setRole(role);\n        user.setActive(active);\n        return user;\n    }\n\n    public CreateUserRequest buildCreateRequest() {\n        return new CreateUserRequest(name, email, password);\n    }\n\n    public UserResponse buildResponse() {\n        return new UserResponse(id != null ? id : 1L, name, email);\n    }\n}\n\npublic class ProductTestBuilder {\n    private Long id = null;\n    private String name = "Test Product";\n    private String category = "General";\n    private BigDecimal price = new BigDecimal("99.99");\n    private int stock = 10;\n\n    public static ProductTestBuilder aProduct() { return new ProductTestBuilder(); }\n    public ProductTestBuilder withName(String name) { this.name = name; return this; }\n    public ProductTestBuilder withCategory(String cat) { this.category = cat; return this; }\n    public ProductTestBuilder withPrice(double price) { this.price = BigDecimal.valueOf(price); return this; }\n    public ProductTestBuilder withStock(int stock) { this.stock = stock; return this; }\n\n    public Product build() {\n        Product p = new Product();\n        p.setId(id); p.setName(name); p.setCategory(category);\n        p.setPrice(price); p.setStock(stock);\n        return p;\n    }\n}\n\n// Фабрика тестовых сценариев\npublic class TestFixtures {\n    public static User admin() {\n        return UserTestBuilder.aUser().withRole(Role.ADMIN).withName("Admin").build();\n    }\n    public static User regularUser() {\n        return UserTestBuilder.aUser().build();\n    }\n    public static Product laptop() {\n        return ProductTestBuilder.aProduct()\n            .withName("Laptop").withCategory("Electronics").withPrice(999.99).build();\n    }\n    public static Order createOrder(User user, Product product, int qty) {\n        Order order = new Order();\n        order.setCustomer(user);\n        order.addItem(new OrderItem(product, qty, product.getPrice()));\n        order.setTotalAmount(product.getPrice().multiply(BigDecimal.valueOf(qty)));\n        return order;\n    }\n}\n\n// Использование в тестах\n@Test\nvoid test() {\n    User user = TestFixtures.admin();\n    Product product = TestFixtures.laptop();\n    Order order = TestFixtures.createOrder(user, product, 2);\n    assertThat(order.getTotalAmount()).isEqualByComparingTo("1999.98");\n}',
      explanation: 'Test Builder паттерн: дефолтные значения позволяют указывать только важные для теста поля. Это делает тесты читаемыми: видно что именно тестируется. TestFixtures предоставляют готовые сценарии. buildCreateRequest() и buildResponse() создают DTO из того же билдера — DRY принцип.'
    },
    {
      id: 8,
      title: 'Задача: REST Assured тесты',
      type: 'practice',
      difficulty: 'hard',
      description: 'Используйте REST Assured для читаемых и выразительных API-тестов.',
      requirements: [
        'Подключить rest-assured и spring-mock-mvc',
        'Given-When-Then синтаксис',
        'Проверка JSON-схемы',
        'Тест цепочки запросов (создать → получить)',
        'Логирование запросов и ответов',
        'Параметризованные тесты'
      ],
      expectedOutput: 'REST Assured Tests:\n✓ givenUser_whenGetById_thenReturn200\n✓ givenInvalidInput_whenCreate_thenReturn422\n✓ givenProducts_whenFilter_thenReturnFiltered\n✓ createAndGet_ChainedRequests\n✓ validateJsonSchema\n\nRequest: GET /api/users/1\nResponse: 200 OK {"id":1,"name":"Иван"}\n\n5 tests passed',
      hint: 'RestAssuredMockMvc.given().contentType(JSON).body(request).when().post("/api/users").then().statusCode(201).body("name", equalTo("Иван")). Гораздо читаемее чем MockMvc.',
      solution: '@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)\nclass ProductApiRestAssuredTest {\n\n    @LocalServerPort int port;\n\n    @BeforeEach\n    void setUp() {\n        RestAssured.port = port;\n        RestAssured.basePath = "/api";\n        RestAssured.enableLoggingOfRequestAndResponseIfValidationFails();\n    }\n\n    @Test\n    void givenProduct_whenGetById_thenReturn200() {\n        // Сначала создаём продукт\n        Long productId = given()\n            .contentType(ContentType.JSON)\n            .body(Map.of("name", "Laptop", "category", "Electronics", "price", 999.99))\n        .when()\n            .post("/products")\n        .then()\n            .statusCode(201)\n            .extract().jsonPath().getLong("id");\n\n        // Затем получаем его\n        given()\n        .when()\n            .get("/products/{id}", productId)\n        .then()\n            .statusCode(200)\n            .body("name", equalTo("Laptop"))\n            .body("category", equalTo("Electronics"))\n            .body("price", equalTo(999.99f));\n    }\n\n    @Test\n    void givenInvalidInput_whenCreate_thenReturn422() {\n        given()\n            .contentType(ContentType.JSON)\n            .body(Map.of("name", "", "price", -1))\n        .when()\n            .post("/products")\n        .then()\n            .statusCode(422)\n            .body("errors", hasSize(greaterThan(0)))\n            .body("errors.field", hasItem("name"));\n    }\n\n    @Test\n    void givenProducts_whenFilterByCategory_thenReturnFiltered() {\n        given()\n            .param("category", "Electronics")\n            .param("page", 0)\n            .param("size", 10)\n        .when()\n            .get("/products")\n        .then()\n            .statusCode(200)\n            .body("content", everyItem(hasEntry("category", "Electronics")))\n            .body("totalElements", greaterThanOrEqualTo(0));\n    }\n\n    @Test\n    void validateJsonSchema() {\n        given()\n        .when()\n            .get("/products/1")\n        .then()\n            .statusCode(200)\n            .body(matchesJsonSchemaInClasspath("schemas/product-response.json"));\n    }\n\n    @ParameterizedTest\n    @ValueSource(strings = {"Electronics", "Books", "Clothing"})\n    void filterByCategory_AllCategories(String category) {\n        given()\n            .param("category", category)\n        .when()\n            .get("/products")\n        .then()\n            .statusCode(200)\n            .body("content.category", everyItem(equalTo(category)));\n    }\n}',
      explanation: 'REST Assured предоставляет BDD-стиль (Given-When-Then) для API-тестов. Более читаемый чем MockMvc. extract() позволяет извлекать данные из ответа для использования в следующих запросах. matchesJsonSchemaInClasspath проверяет структуру ответа по JSON Schema. Логирование при ошибках помогает отлаживать тесты.'
    },
    {
      id: 9,
      title: 'Задача: Тесты безопасности с @WithMockUser',
      type: 'practice',
      difficulty: 'hard',
      description: 'Тестируйте Spring Security: аутентификацию, авторизацию и роли.',
      requirements: [
        '@WithMockUser — тест от имени аутентифицированного пользователя',
        '@WithMockUser(roles = "ADMIN") — тест с ролью',
        'Тест без аутентификации → 401 Unauthorized',
        'Тест с недостаточными правами → 403 Forbidden',
        'Тест с JWT-токеном в Authorization заголовке',
        'Кастомная аннотация @WithMockAdmin'
      ],
      expectedOutput: 'SecurityTest:\n✓ anonymous_GetPublicEndpoint_Returns200\n✓ anonymous_GetProtectedEndpoint_Returns401\n✓ user_GetOwnProfile_Returns200\n✓ user_GetAdminEndpoint_Returns403\n✓ admin_GetAdminEndpoint_Returns200\n✓ withJwtToken_Returns200\n\n6 tests passed',
      hint: '@WithMockUser(username = "ivan", roles = {"USER"}) устанавливает SecurityContext. Для JWT тестов создайте реальный токен в тесте и передайте в Authorization header.',
      solution: '@WebMvcTest(UserController.class)\n@Import(SecurityConfig.class)\nclass SecurityTest {\n\n    @Autowired MockMvc mockMvc;\n    @MockBean UserService userService;\n    @MockBean JwtTokenProvider jwtProvider;\n\n    @Test\n    void anonymous_GetPublicEndpoint_Returns200() throws Exception {\n        mockMvc.perform(get("/api/public/health"))\n            .andExpect(status().isOk());\n    }\n\n    @Test\n    void anonymous_GetProtectedEndpoint_Returns401() throws Exception {\n        mockMvc.perform(get("/api/users/me"))\n            .andExpect(status().isUnauthorized());\n    }\n\n    @Test\n    @WithMockUser(username = "ivan@mail.ru", roles = {"USER"})\n    void user_GetOwnProfile_Returns200() throws Exception {\n        when(userService.findByEmail("ivan@mail.ru"))\n            .thenReturn(new UserResponse(1L, "Иван", "ivan@mail.ru"));\n\n        mockMvc.perform(get("/api/users/me"))\n            .andExpect(status().isOk())\n            .andExpect(jsonPath("$.name").value("Иван"));\n    }\n\n    @Test\n    @WithMockUser(username = "user@mail.ru", roles = {"USER"})\n    void user_GetAdminEndpoint_Returns403() throws Exception {\n        mockMvc.perform(get("/api/admin/users"))\n            .andExpect(status().isForbidden());\n    }\n\n    @Test\n    @WithMockUser(username = "admin@mail.ru", roles = {"ADMIN"})\n    void admin_GetAdminEndpoint_Returns200() throws Exception {\n        when(userService.findAll(any())).thenReturn(Page.empty());\n\n        mockMvc.perform(get("/api/admin/users"))\n            .andExpect(status().isOk());\n    }\n\n    @Test\n    void withJwtToken_Returns200() throws Exception {\n        String token = "valid.jwt.token";\n        when(jwtProvider.validateToken(token)).thenReturn(true);\n        when(jwtProvider.getUsername(token)).thenReturn("ivan@mail.ru");\n        when(userService.findByEmail("ivan@mail.ru"))\n            .thenReturn(new UserResponse(1L, "Иван", "ivan@mail.ru"));\n\n        mockMvc.perform(get("/api/users/me")\n                .header("Authorization", "Bearer " + token))\n            .andExpect(status().isOk());\n    }\n\n    // Кастомная аннотация\n    @Retention(RetentionPolicy.RUNTIME)\n    @WithMockUser(username = "admin@mail.ru", roles = {"ADMIN"})\n    @interface WithMockAdmin {}\n\n    @Test\n    @WithMockAdmin\n    void customAnnotation_AdminAccess() throws Exception {\n        when(userService.findAll(any())).thenReturn(Page.empty());\n        mockMvc.perform(get("/api/admin/users"))\n            .andExpect(status().isOk());\n    }\n}',
      explanation: '@WithMockUser устанавливает SecurityContext с указанным пользователем — не нужно реально аутентифицироваться. Без аннотации — запрос анонимный. roles={"USER"} → GrantedAuthority ROLE_USER. Кастомная аннотация @WithMockAdmin уменьшает дублирование. Для JWT-тестов мокаем JwtTokenProvider.'
    },
    {
      id: 10,
      title: 'Задача: Performance тест и конфигурация JMeter',
      type: 'practice',
      difficulty: 'hard',
      description: 'Настройте нагрузочное тестирование Spring Boot API с JMeter и встроенными средствами.',
      requirements: [
        'JMeter Test Plan конфигурация для REST API',
        'Сценарии: 100 concurrent users, ramp-up 10 секунд',
        'Тестирование GET endpoint: throughput, latency, error rate',
        'Тестирование POST endpoint: с JSON payload',
        'Spring Boot test: @RepeatedTest для baseline performance',
        'Gatling DSL альтернатива: проверка SLA (p95 < 200ms)'
      ],
      expectedOutput: 'JMeter Results:\n  Samples: 10000\n  Average: 45ms\n  Median: 32ms\n  90th percentile: 85ms\n  95th percentile: 120ms\n  99th percentile: 250ms\n  Throughput: 950 req/s\n  Error %: 0.1%\n\nSpring Boot baseline:\n  100 iterations of GET /api/products\n  Average: 15ms (without network overhead)\n  Max: 45ms',
      hint: 'Для Spring Boot baseline: измеряйте время в @RepeatedTest. Для JMeter: создайте .jmx файл. Для Gatling: Scala DSL с injection profile.',
      solution: '// JMeter Test Plan (описание конфигурации)\n// Файл: load-test.jmx\n//\n// Thread Group:\n//   Number of Threads: 100\n//   Ramp-Up Period: 10 seconds\n//   Loop Count: 100\n//\n// HTTP Request Defaults:\n//   Server: localhost\n//   Port: 8080\n//   Protocol: http\n//\n// HTTP Request - GET Products:\n//   Method: GET\n//   Path: /api/products?page=0&size=10\n//\n// HTTP Request - Create Product:\n//   Method: POST\n//   Path: /api/products\n//   Body: {"name":"Product ${__threadNum}","category":"Test","price":99.99}\n//   Content-Type: application/json\n//\n// Response Assertion: Status 200/201\n// Summary Report Listener\n// Aggregate Report Listener\n\n// Spring Boot baseline performance test\n@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)\nclass PerformanceBaselineTest {\n\n    @Autowired TestRestTemplate restTemplate;\n\n    @RepeatedTest(100)\n    void getProducts_ResponseTime() {\n        long start = System.nanoTime();\n        ResponseEntity<String> response = restTemplate.getForEntity(\n            "/api/products?page=0&size=10", String.class);\n        long duration = (System.nanoTime() - start) / 1_000_000; // ms\n\n        assertThat(response.getStatusCode()).isEqualTo(HttpStatus.OK);\n        assertThat(duration).isLessThan(500); // SLA: < 500ms\n    }\n\n    @Test\n    void concurrentRequests_NoErrors() throws Exception {\n        int threadCount = 50;\n        ExecutorService executor = Executors.newFixedThreadPool(threadCount);\n        CountDownLatch latch = new CountDownLatch(threadCount);\n        AtomicInteger errors = new AtomicInteger(0);\n        List<Long> responseTimes = Collections.synchronizedList(new ArrayList<>());\n\n        for (int i = 0; i < threadCount; i++) {\n            executor.submit(() -> {\n                try {\n                    long start = System.nanoTime();\n                    ResponseEntity<String> resp = restTemplate.getForEntity(\n                        "/api/products?page=0&size=10", String.class);\n                    long duration = (System.nanoTime() - start) / 1_000_000;\n                    responseTimes.add(duration);\n\n                    if (!resp.getStatusCode().is2xxSuccessful()) {\n                        errors.incrementAndGet();\n                    }\n                } catch (Exception e) {\n                    errors.incrementAndGet();\n                } finally {\n                    latch.countDown();\n                }\n            });\n        }\n\n        latch.await(30, TimeUnit.SECONDS);\n        executor.shutdown();\n\n        // Анализ результатов\n        Collections.sort(responseTimes);\n        long avg = (long) responseTimes.stream().mapToLong(Long::longValue).average().orElse(0);\n        long p95 = responseTimes.get((int) (responseTimes.size() * 0.95));\n        long max = responseTimes.get(responseTimes.size() - 1);\n\n        System.out.printf("Concurrent test: avg=%dms, p95=%dms, max=%dms, errors=%d%n",\n            avg, p95, max, errors.get());\n\n        assertThat(errors.get()).isZero();\n        assertThat(p95).isLessThan(1000); // SLA: p95 < 1s\n    }\n}',
      explanation: 'JMeter — стандартный инструмент нагрузочного тестирования. Thread Group определяет паттерн нагрузки (количество пользователей, ramp-up). Spring Boot @RepeatedTest даёт baseline без сетевого overhead. Concurrent test проверяет поведение под нагрузкой. P95 — 95-й перцентиль: 95% запросов выполняются быстрее этого времени.'
    }
  ]
}
