export default {
  id: 43,
  title: 'Resilience4j: отказоустойчивость',
  description: 'Построение отказоустойчивых приложений с Resilience4j: Circuit Breaker, Retry, Rate Limiter, Bulkhead, TimeLimiter и их интеграция со Spring Boot.',
  lessons: [
    {
      id: 1,
      title: 'Circuit Breaker',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Паттерн Circuit Breaker' },
        { type: 'text', value: 'Circuit Breaker предотвращает каскадные сбои в микросервисной архитектуре. Когда внешний сервис падает, Circuit Breaker \"размыкает\" цепь и возвращает fallback ответ вместо бесконечных попыток подключения.' },
        { type: 'heading', value: 'Состояния Circuit Breaker' },
        { type: 'list', items: [
          'CLOSED (замкнут) — нормальная работа, запросы проходят',
          'OPEN (разомкнут) — сервис недоступен, все запросы сразу получают fallback',
          'HALF_OPEN (полуоткрыт) — пропускает несколько пробных запросов для проверки'
        ] },
        { type: 'code', language: 'xml', value: '<dependency>\n    <groupId>io.github.resilience4j</groupId>\n    <artifactId>resilience4j-spring-boot3</artifactId>\n</dependency>\n<dependency>\n    <groupId>org.springframework.boot</groupId>\n    <artifactId>spring-boot-starter-aop</artifactId>\n</dependency>' },
        { type: 'code', language: 'yaml', value: '# application.yml\nresilience4j:\n  circuitbreaker:\n    instances:\n      paymentService:\n        sliding-window-type: COUNT_BASED\n        sliding-window-size: 10          # последние 10 вызовов\n        failure-rate-threshold: 50       # 50% ошибок -> OPEN\n        wait-duration-in-open-state: 30s # 30 сек в OPEN\n        permitted-number-of-calls-in-half-open-state: 3  # 3 пробных\n        minimum-number-of-calls: 5       # минимум 5 вызовов для оценки\n        slow-call-rate-threshold: 80     # 80% медленных -> OPEN\n        slow-call-duration-threshold: 2s # медленный = > 2 сек\n        record-exceptions:\n          - java.io.IOException\n          - java.util.concurrent.TimeoutException\n        ignore-exceptions:\n          - com.example.exception.BusinessException' },
        { type: 'heading', value: 'Использование с аннотацией' },
        { type: 'code', language: 'java', value: '@Service\npublic class PaymentService {\n\n    private final WebClient paymentClient;\n\n    @CircuitBreaker(name = \"paymentService\", fallbackMethod = \"paymentFallback\")\n    public PaymentResult processPayment(PaymentRequest request) {\n        return paymentClient.post()\n            .uri(\"/api/payments\")\n            .bodyValue(request)\n            .retrieve()\n            .bodyToMono(PaymentResult.class)\n            .block();\n    }\n\n    // Fallback метод — та же сигнатура + Exception параметр\n    private PaymentResult paymentFallback(PaymentRequest request, Exception ex) {\n        log.warn(\"Payment service недоступен: {}. Используем fallback.\", \n            ex.getMessage());\n        return new PaymentResult(\n            PaymentStatus.PENDING,\n            \"Платёж будет обработан позже\",\n            null);\n    }\n}\n\n// Fallback с разными типами исключений\n@CircuitBreaker(name = \"userService\", fallbackMethod = \"getUserFallback\")\npublic User getUser(Long id) {\n    return userClient.getUser(id);\n}\n\n// Для TimeoutException\nprivate User getUserFallback(Long id, TimeoutException ex) {\n    log.warn(\"Timeout при получении пользователя {}\", id);\n    return getCachedUser(id);  // вернуть из кеша\n}\n\n// Для всех остальных\nprivate User getUserFallback(Long id, Exception ex) {\n    log.error(\"Ошибка получения пользователя {}: {}\", id, ex.getMessage());\n    return User.unknown(id);  // заглушка\n}' },
        { type: 'heading', value: 'Мониторинг Circuit Breaker' },
        { type: 'code', language: 'java', value: '// Программный доступ к состоянию\n@Service\npublic class CircuitBreakerMonitor {\n\n    private final CircuitBreakerRegistry registry;\n\n    public Map<String, Object> getStatus(String name) {\n        CircuitBreaker cb = registry.circuitBreaker(name);\n        CircuitBreaker.Metrics metrics = cb.getMetrics();\n\n        return Map.of(\n            \"state\", cb.getState().name(),\n            \"failureRate\", metrics.getFailureRate(),\n            \"slowCallRate\", metrics.getSlowCallRate(),\n            \"numberOfBufferedCalls\", metrics.getNumberOfBufferedCalls(),\n            \"numberOfSuccessfulCalls\", metrics.getNumberOfSuccessfulCalls(),\n            \"numberOfFailedCalls\", metrics.getNumberOfFailedCalls()\n        );\n    }\n}\n\n// Actuator endpoint\n// GET /actuator/circuitbreakers\n// GET /actuator/circuitbreakerevents' },
        { type: 'note', value: 'Circuit Breaker важен для микросервисов: если один сервис упал, остальные не должны ждать timeout на каждый запрос. OPEN состояние мгновенно возвращает fallback.' }
      ]
    },
    {
      id: 2,
      title: 'Retry',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Автоматический повтор при ошибках' },
        { type: 'code', language: 'yaml', value: '# application.yml\nresilience4j:\n  retry:\n    instances:\n      emailService:\n        max-attempts: 3                    # 3 попытки\n        wait-duration: 1s                  # ждать 1 сек между попытками\n        enable-exponential-backoff: true   # exponential backoff\n        exponential-backoff-multiplier: 2  # 1s, 2s, 4s\n        retry-exceptions:\n          - java.io.IOException\n          - org.springframework.web.client.HttpServerErrorException\n        ignore-exceptions:\n          - com.example.exception.BusinessException' },
        { type: 'code', language: 'java', value: '@Service\npublic class EmailService {\n\n    @Retry(name = \"emailService\", fallbackMethod = \"emailFallback\")\n    public void sendEmail(String to, String subject, String body) {\n        log.info(\"Отправка email на {}\", to);\n        mailSender.send(createMessage(to, subject, body));\n    }\n\n    private void emailFallback(String to, String subject, String body, \n                                Exception ex) {\n        log.error(\"Не удалось отправить email на {} после всех попыток: {}\", \n            to, ex.getMessage());\n        // Сохранить в очередь для повторной попытки позже\n        failedEmailQueue.add(new FailedEmail(to, subject, body));\n    }\n}' },
        { type: 'heading', value: 'Программная конфигурация Retry' },
        { type: 'code', language: 'java', value: '@Configuration\npublic class RetryConfig {\n\n    @Bean\n    public Retry customRetry(RetryRegistry registry) {\n        RetryConfig config = RetryConfig.custom()\n            .maxAttempts(3)\n            .waitDuration(Duration.ofMillis(500))\n            .intervalFunction(IntervalFunction.ofExponentialBackoff(\n                500, 2.0))  // 500ms, 1s, 2s\n            .retryOnException(e -> e instanceof IOException)\n            .retryOnResult(response -> response == null)  // retry если null\n            .failAfterMaxAttempts(true)\n            .build();\n\n        return registry.retry(\"customRetry\", config);\n    }\n}\n\n// Использование без аннотаций\n@Service\npublic class ExternalApiService {\n\n    private final Retry retry;\n    private final WebClient webClient;\n\n    public DataResponse fetchData(String id) {\n        return Retry.decorateSupplier(retry, () -> \n            webClient.get()\n                .uri(\"/data/{id}\", id)\n                .retrieve()\n                .bodyToMono(DataResponse.class)\n                .block()\n        ).get();\n    }\n}' },
        { type: 'heading', value: 'Retry + Circuit Breaker вместе' },
        { type: 'code', language: 'java', value: '// Порядок важен! Retry оборачивает CircuitBreaker\n// Retry -> CircuitBreaker -> вызов метода\n// Retry повторяет, если CircuitBreaker выбросил CallNotPermittedException\n\n@CircuitBreaker(name = \"externalApi\")\n@Retry(name = \"externalApi\")\npublic ExternalData callExternalApi(String id) {\n    return externalClient.getData(id);\n}\n\n// Порядок выполнения:\n// 1. Retry перехватывает вызов\n// 2. CircuitBreaker проверяет состояние\n// 3. Если OPEN — CircuitBreaker бросает CallNotPermittedException\n// 4. Retry НЕ повторяет CallNotPermittedException (нет смысла)\n// 5. Если CLOSED — вызов проходит, при ошибке Retry повторяет' },
        { type: 'tip', value: 'Используйте exponential backoff вместо фиксированных интервалов. Это предотвращает \"thundering herd\" — когда все retry попадают одновременно на восстановившийся сервис.' }
      ]
    },
    {
      id: 3,
      title: 'Rate Limiter',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Ограничение частоты запросов' },
        { type: 'code', language: 'yaml', value: '# application.yml\nresilience4j:\n  ratelimiter:\n    instances:\n      api:\n        limit-for-period: 100          # 100 запросов\n        limit-refresh-period: 1m       # за 1 минуту\n        timeout-duration: 5s           # ждать до 5 сек если лимит исчерпан\n      loginApi:\n        limit-for-period: 5            # 5 попыток\n        limit-refresh-period: 1m       # в минуту\n        timeout-duration: 0s           # не ждать — сразу ошибка' },
        { type: 'code', language: 'java', value: '@RestController\n@RequestMapping(\"/api\")\npublic class ApiController {\n\n    @GetMapping(\"/products\")\n    @RateLimiter(name = \"api\", fallbackMethod = \"rateLimitFallback\")\n    public List<Product> getProducts() {\n        return productService.findAll();\n    }\n\n    @PostMapping(\"/login\")\n    @RateLimiter(name = \"loginApi\", fallbackMethod = \"loginRateLimitFallback\")\n    public AuthResponse login(@RequestBody LoginRequest request) {\n        return authService.login(request);\n    }\n\n    private List<Product> rateLimitFallback(Exception ex) {\n        throw new ResponseStatusException(HttpStatus.TOO_MANY_REQUESTS,\n            \"Слишком много запросов. Попробуйте через минуту.\");\n    }\n\n    private AuthResponse loginRateLimitFallback(LoginRequest req, Exception ex) {\n        throw new ResponseStatusException(HttpStatus.TOO_MANY_REQUESTS,\n            \"Слишком много попыток входа. Подождите 1 минуту.\");\n    }\n}' },
        { type: 'heading', value: 'Rate Limiter per-user' },
        { type: 'code', language: 'java', value: '// Динамический Rate Limiter для каждого пользователя\n@Service\npublic class UserRateLimiterService {\n\n    private final RateLimiterRegistry registry;\n\n    public <T> T executeWithRateLimit(String userId, Supplier<T> supplier) {\n        RateLimiter limiter = registry.rateLimiter(\"user-\" + userId,\n            RateLimiterConfig.custom()\n                .limitForPeriod(50)\n                .limitRefreshPeriod(Duration.ofMinutes(1))\n                .timeoutDuration(Duration.ZERO)\n                .build());\n\n        try {\n            return RateLimiter.decorateSupplier(limiter, supplier).get();\n        } catch (RequestNotPermitted e) {\n            throw new RateLimitExceededException(\n                \"Пользователь \" + userId + \": лимит запросов исчерпан\");\n        }\n    }\n}\n\n// В контроллере\n@GetMapping(\"/data\")\npublic DataResponse getData(@AuthenticationPrincipal UserDetails user) {\n    return rateLimiterService.executeWithRateLimit(\n        user.getUsername(),\n        () -> dataService.getData());\n}' },
        { type: 'note', value: 'Rate Limiter Resilience4j работает in-memory (на одном инстансе). Для распределённого rate limiting используйте Redis (Redisson, bucket4j с Redis backend).' }
      ]
    },
    {
      id: 4,
      title: 'Bulkhead и TimeLimiter',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Bulkhead — изоляция ресурсов' },
        { type: 'text', value: 'Bulkhead ограничивает количество одновременных вызовов к определённому сервису. Это предотвращает ситуацию, когда один медленный сервис занимает все потоки.' },
        { type: 'code', language: 'yaml', value: '# application.yml\nresilience4j:\n  bulkhead:\n    instances:\n      paymentService:\n        max-concurrent-calls: 10         # макс. 10 одновременных\n        max-wait-duration: 5s            # ждать до 5 сек\n  thread-pool-bulkhead:\n    instances:\n      reportService:\n        max-thread-pool-size: 5          # 5 потоков\n        core-thread-pool-size: 3         # базовые 3 потока\n        queue-capacity: 20               # очередь на 20\n        keep-alive-duration: 10s' },
        { type: 'code', language: 'java', value: '// Semaphore Bulkhead — ограничение concurrent calls\n@Service\npublic class PaymentService {\n\n    @Bulkhead(name = \"paymentService\", fallbackMethod = \"bulkheadFallback\")\n    public PaymentResult processPayment(PaymentRequest request) {\n        return paymentGateway.process(request);\n    }\n\n    private PaymentResult bulkheadFallback(PaymentRequest request, \n                                            BulkheadFullException ex) {\n        log.warn(\"Bulkhead полон. Платёж поставлен в очередь.\");\n        paymentQueue.add(request);\n        return new PaymentResult(PaymentStatus.QUEUED);\n    }\n}\n\n// ThreadPool Bulkhead — изолированный пул потоков\n@Service\npublic class ReportService {\n\n    @Bulkhead(name = \"reportService\", type = Bulkhead.Type.THREADPOOL)\n    public CompletableFuture<Report> generateReport(ReportRequest request) {\n        return CompletableFuture.supplyAsync(() -> {\n            // Тяжёлая генерация отчёта в отдельном пуле потоков\n            return reportGenerator.generate(request);\n        });\n    }\n}' },
        { type: 'heading', value: 'TimeLimiter — ограничение времени' },
        { type: 'code', language: 'yaml', value: '# application.yml\nresilience4j:\n  timelimiter:\n    instances:\n      externalApi:\n        timeout-duration: 3s             # макс. 3 секунды\n        cancel-running-future: true      # отменить если timeout' },
        { type: 'code', language: 'java', value: '@Service\npublic class ExternalApiService {\n\n    @TimeLimiter(name = \"externalApi\", fallbackMethod = \"timeoutFallback\")\n    public CompletableFuture<ExternalData> getData(String id) {\n        return CompletableFuture.supplyAsync(() -> \n            externalClient.fetchData(id));\n    }\n\n    private CompletableFuture<ExternalData> timeoutFallback(\n            String id, TimeoutException ex) {\n        log.warn(\"Timeout при получении данных для {}\", id);\n        return CompletableFuture.completedFuture(\n            ExternalData.cached(id));  // вернуть кешированные\n    }\n}' },
        { type: 'heading', value: 'Комбинирование всех паттернов' },
        { type: 'code', language: 'java', value: '// Порядок выполнения (снаружи внутрь):\n// Retry -> CircuitBreaker -> RateLimiter -> TimeLimiter -> Bulkhead -> метод\n\n@Service\npublic class ResilientExternalService {\n\n    @Retry(name = \"externalApi\")\n    @CircuitBreaker(name = \"externalApi\", fallbackMethod = \"fallback\")\n    @RateLimiter(name = \"externalApi\")\n    @Bulkhead(name = \"externalApi\")\n    @TimeLimiter(name = \"externalApi\")\n    public CompletableFuture<ExternalData> callExternalApi(String id) {\n        return CompletableFuture.supplyAsync(() -> \n            webClient.get()\n                .uri(\"/api/data/{id}\", id)\n                .retrieve()\n                .bodyToMono(ExternalData.class)\n                .block());\n    }\n\n    private CompletableFuture<ExternalData> fallback(String id, Exception ex) {\n        log.error(\"Все механизмы resilience исчерпаны для {}: {}\", \n            id, ex.getClass().getSimpleName());\n        return CompletableFuture.completedFuture(\n            ExternalData.unavailable(id));\n    }\n}' },
        { type: 'warning', value: 'Порядок аннотаций важен! Resilience4j применяет их в определённом порядке. @Retry должен быть снаружи (первым), @CircuitBreaker — вторым. Обратный порядок может привести к бесконечным повторам.' }
      ]
    },
    {
      id: 5,
      title: 'Мониторинг и события',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Actuator интеграция' },
        { type: 'code', language: 'yaml', value: '# application.yml\nmanagement:\n  endpoints:\n    web:\n      exposure:\n        include: health, circuitbreakers, circuitbreakerevents, ratelimiters, retries, bulkheads\n  health:\n    circuitbreakers:\n      enabled: true\n    ratelimiters:\n      enabled: true\n\n  endpoint:\n    health:\n      show-details: always' },
        { type: 'code', language: 'json', value: '// GET /actuator/health\n{\n  "status": "UP",\n  "components": {\n    "circuitBreakers": {\n      "status": "UP",\n      "details": {\n        "paymentService": {\n          "status": "UP",\n          "details": {\n            "state": "CLOSED",\n            "failureRate": "-1.0%",\n            "slowCallRate": "-1.0%",\n            "bufferedCalls": 5,\n            "failedCalls": 0\n          }\n        }\n      }\n    }\n  }\n}\n\n// GET /actuator/circuitbreakerevents\n{\n  "circuitBreakerEvents": [\n    {\n      "circuitBreakerName": "paymentService",\n      "type": "SUCCESS",\n      "creationTime": "2026-04-05T10:00:00",\n      "duration": "PT0.045S"\n    },\n    {\n      "circuitBreakerName": "paymentService",\n      "type": "ERROR",\n      "creationTime": "2026-04-05T10:01:00",\n      "errorMessage": "Connection refused"\n    }\n  ]\n}' },
        { type: 'heading', value: 'Метрики в Prometheus/Grafana' },
        { type: 'code', language: 'java', value: '// Resilience4j автоматически публикует метрики в Micrometer\n// Доступно через /actuator/prometheus:\n\n// resilience4j_circuitbreaker_state{name=\"paymentService\"} 0.0  (CLOSED)\n// resilience4j_circuitbreaker_calls_total{name=\"paymentService\",kind=\"successful\"} 150\n// resilience4j_circuitbreaker_calls_total{name=\"paymentService\",kind=\"failed\"} 3\n// resilience4j_circuitbreaker_failure_rate{name=\"paymentService\"} 2.0\n// resilience4j_retry_calls_total{name=\"emailService\",kind=\"successful_with_retry\"} 5\n// resilience4j_ratelimiter_available_permissions{name=\"api\"} 85' },
        { type: 'heading', value: 'Event Listeners' },
        { type: 'code', language: 'java', value: '@Component\npublic class ResilienceEventListener {\n\n    private final CircuitBreakerRegistry cbRegistry;\n    private final RetryRegistry retryRegistry;\n\n    @PostConstruct\n    public void registerListeners() {\n        // Circuit Breaker events\n        cbRegistry.circuitBreaker(\"paymentService\")\n            .getEventPublisher()\n            .onStateTransition(event -> {\n                log.warn(\"CircuitBreaker {} перешёл из {} в {}\",\n                    event.getCircuitBreakerName(),\n                    event.getStateTransition().getFromState(),\n                    event.getStateTransition().getToState());\n\n                if (event.getStateTransition().getToState() == \n                        CircuitBreaker.State.OPEN) {\n                    alertService.sendAlert(\n                        \"CircuitBreaker OPEN: paymentService\");\n                }\n            })\n            .onError(event -> \n                log.error(\"CB Error: {} - {}\", \n                    event.getCircuitBreakerName(), \n                    event.getThrowable().getMessage()));\n\n        // Retry events\n        retryRegistry.retry(\"emailService\")\n            .getEventPublisher()\n            .onRetry(event -> \n                log.info(\"Retry {} попытка {} для emailService\",\n                    event.getNumberOfRetryAttempts(),\n                    event.getName()));\n    }\n}' },
        { type: 'heading', value: 'Health Indicator' },
        { type: 'code', language: 'java', value: '// Кастомный Health Indicator на базе Circuit Breaker\n@Component\npublic class PaymentHealthIndicator implements HealthIndicator {\n\n    private final CircuitBreakerRegistry registry;\n\n    @Override\n    public Health health() {\n        CircuitBreaker cb = registry.circuitBreaker(\"paymentService\");\n\n        return switch (cb.getState()) {\n            case CLOSED -> Health.up()\n                .withDetail(\"state\", \"CLOSED\")\n                .withDetail(\"failureRate\", cb.getMetrics().getFailureRate())\n                .build();\n            case HALF_OPEN -> Health.status(\"DEGRADED\")\n                .withDetail(\"state\", \"HALF_OPEN\")\n                .build();\n            case OPEN -> Health.down()\n                .withDetail(\"state\", \"OPEN\")\n                .withDetail(\"failureRate\", cb.getMetrics().getFailureRate())\n                .build();\n            default -> Health.unknown().build();\n        };\n    }\n}' },
        { type: 'tip', value: 'Настройте алерты в Grafana на переход CircuitBreaker в OPEN состояние. Это первый индикатор проблем с внешним сервисом.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Отказоустойчивый сервис',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создайте отказоустойчивый сервис заказов с Circuit Breaker для платёжного шлюза, Retry для email и Rate Limiter для API.',
      requirements: [
        'Circuit Breaker для PaymentService (50% failure -> OPEN, 30s wait, 3 probe)',
        'Fallback для платежей: статус PENDING, сохранить в очередь',
        'Retry для EmailService (3 попытки, exponential backoff)',
        'Rate Limiter для API (100 req/min)',
        'Bulkhead для ReportService (max 5 concurrent)',
        'Actuator endpoints для мониторинга состояния',
        'Event Listener для алертов при Circuit Breaker OPEN',
        'Тесты: проверить что fallback вызывается при недоступности сервиса'
      ],
      hint: 'Для тестов используйте WireMock для эмуляции недоступного сервиса. Настройте sliding-window-size маленьким (5) для быстрого перехода в OPEN.',
      expectedOutput: 'Нормальная работа:\n  POST /api/orders -> PaymentService -> SUCCESS\n  CircuitBreaker state: CLOSED\n\nПлатёжный сервис упал:\n  POST /api/orders (1-5): IOException\n  CircuitBreaker: 5/5 failed = 100% -> OPEN\n  POST /api/orders (6): fallback -> PaymentResult{status=PENDING}\n  Alert: "CircuitBreaker OPEN: paymentService"\n\nЧерез 30 секунд:\n  CircuitBreaker: HALF_OPEN\n  POST /api/orders: пробный запрос -> SUCCESS\n  CircuitBreaker: CLOSED\n\nRate Limiter:\n  Запросы 1-100: OK\n  Запрос 101: HTTP 429 Too Many Requests\n\nGET /actuator/health:\n  circuitBreakers.paymentService: UP (CLOSED)',
      solution: '@Service\npublic class OrderService {\n    private final PaymentService paymentService;\n    private final EmailService emailService;\n\n    @Transactional\n    public Order createOrder(CreateOrderRequest req) {\n        Order order = orderRepo.save(toOrder(req));\n        PaymentResult payment = paymentService.processPayment(\n            new PaymentRequest(order.getId(), order.getTotalAmount()));\n\n        order.setPaymentStatus(payment.getStatus());\n        if (payment.getStatus() == PaymentStatus.PENDING) {\n            pendingPaymentQueue.add(order.getId());\n        }\n        emailService.sendOrderConfirmation(order);\n        return orderRepo.save(order);\n    }\n}\n\n@Service\npublic class PaymentService {\n    @CircuitBreaker(name = "paymentService", fallbackMethod = "fallback")\n    public PaymentResult processPayment(PaymentRequest req) {\n        return paymentClient.post().uri("/pay").bodyValue(req)\n            .retrieve().bodyToMono(PaymentResult.class).block();\n    }\n\n    private PaymentResult fallback(PaymentRequest req, Exception ex) {\n        log.warn("Payment fallback: {}", ex.getMessage());\n        return new PaymentResult(PaymentStatus.PENDING, "Будет обработано позже");\n    }\n}\n\n@Service\npublic class EmailService {\n    @Retry(name = "emailService", fallbackMethod = "emailFallback")\n    public void sendOrderConfirmation(Order order) {\n        mailSender.send(createMessage(order));\n    }\n\n    private void emailFallback(Order order, Exception ex) {\n        log.error("Email не отправлен: {}", ex.getMessage());\n        failedEmailRepo.save(new FailedEmail(order.getUserEmail(), order.getId()));\n    }\n}\n\n// Тест\n@SpringBootTest\nclass PaymentServiceTest {\n    @Autowired PaymentService paymentService;\n\n    @Test\n    void circuitBreaker_shouldOpenAfterFailures() {\n        // Эмулируем 5 ошибок\n        for (int i = 0; i < 5; i++) {\n            paymentService.processPayment(new PaymentRequest(1L, BigDecimal.TEN));\n        }\n        // 6-й вызов — fallback без реального вызова\n        PaymentResult result = paymentService.processPayment(new PaymentRequest(2L, BigDecimal.TEN));\n        assertEquals(PaymentStatus.PENDING, result.getStatus());\n    }\n}',
      explanation: 'Circuit Breaker защищает от каскадных сбоев — при недоступности платёжного сервиса заказы сохраняются со статусом PENDING для позднейшей обработки. Retry обеспечивает отправку email при временных проблемах. Rate Limiter защищает API от перегрузки.'
    }
  ]
}
