export default {
  id: 36,
  title: 'Spring AOP: аспектно-ориентированное программирование',
  description: 'Аспектно-ориентированное программирование в Spring: концепции AOP, создание аспектов, pointcut expressions, кастомные аннотации, практическое применение для логирования и метрик.',
  lessons: [
    {
      id: 1,
      title: 'Концепции AOP',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Что такое AOP?' },
        { type: 'text', value: 'AOP (Aspect-Oriented Programming) — это парадигма, позволяющая отделить сквозную функциональность (cross-cutting concerns) от бизнес-логики. Типичные примеры: логирование, безопасность, транзакции, кеширование, метрики.' },
        { type: 'text', value: 'Без AOP код логирования, проверки прав и измерения времени рассеян по всем методам сервиса. С AOP — эта логика определяется один раз в отдельном классе (Aspect) и автоматически применяется к нужным методам.' },
        { type: 'heading', value: 'Ключевые понятия' },
        { type: 'list', items: [
          'Aspect — модуль сквозной функциональности (класс с @Aspect)',
          'Join Point — точка выполнения программы (вызов метода, выброс исключения)',
          'Advice — действие аспекта (Before, After, Around)',
          'Pointcut — выражение, определяющее к каким Join Point применяется Advice',
          'Weaving — процесс внедрения аспекта в код (compile-time, load-time, runtime)'
        ] },
        { type: 'code', language: 'xml', value: '<dependency>\n    <groupId>org.springframework.boot</groupId>\n    <artifactId>spring-boot-starter-aop</artifactId>\n</dependency>' },
        { type: 'heading', value: 'Как работает Spring AOP' },
        { type: 'text', value: 'Spring AOP использует proxy-based подход: создаёт прокси-объект вокруг целевого bean. Когда вы вызываете метод bean, вызов проходит через прокси, который выполняет Advice до/после/вокруг метода.' },
        { type: 'code', language: 'java', value: '// Без AOP — сквозная логика в каждом методе\n@Service\npublic class OrderService {\n    public Order createOrder(CreateOrderRequest req) {\n        long start = System.currentTimeMillis();  // метрики\n        log.info("createOrder called with: {}", req);  // логирование\n        securityCheck();  // безопасность\n        \n        Order order = doBusinessLogic(req);  // бизнес-логика\n        \n        log.info("createOrder completed in {}ms", \n            System.currentTimeMillis() - start);  // метрики\n        return order;\n    }\n}\n\n// С AOP — чистая бизнес-логика\n@Service\npublic class OrderService {\n    public Order createOrder(CreateOrderRequest req) {\n        return doBusinessLogic(req);  // только бизнес-логика!\n    }\n}\n\n// Аспекты обрабатывают остальное автоматически\n@Aspect @Component\npublic class LoggingAspect { /* логирование */ }\n\n@Aspect @Component\npublic class MetricsAspect { /* метрики */ }' },
        { type: 'note', value: 'Spring AOP работает только с Spring-managed beans и только с public методами. Вызовы внутри одного класса (this.method()) не проходят через прокси и не перехватываются аспектами.' }
      ]
    },
    {
      id: 2,
      title: '@Aspect, @Before, @After, @Around',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Типы Advice' },
        { type: 'code', language: 'java', value: '@Aspect\n@Component\npublic class LoggingAspect {\n\n    private static final Logger log = LoggerFactory.getLogger(LoggingAspect.class);\n\n    // @Before — выполняется ДО метода\n    @Before("execution(* com.example.service.*.*(..))")\n    public void logBefore(JoinPoint joinPoint) {\n        log.info("Вызов: {}.{}({})",\n            joinPoint.getTarget().getClass().getSimpleName(),\n            joinPoint.getSignature().getName(),\n            Arrays.toString(joinPoint.getArgs()));\n    }\n\n    // @AfterReturning — выполняется ПОСЛЕ успешного возврата\n    @AfterReturning(pointcut = "execution(* com.example.service.*.*(..))",\n                     returning = "result")\n    public void logAfterReturning(JoinPoint joinPoint, Object result) {\n        log.info("Результат {}: {}", \n            joinPoint.getSignature().getName(), result);\n    }\n\n    // @AfterThrowing — выполняется при выбросе исключения\n    @AfterThrowing(pointcut = "execution(* com.example.service.*.*(..))",\n                    throwing = "ex")\n    public void logAfterThrowing(JoinPoint joinPoint, Exception ex) {\n        log.error("Ошибка в {}: {}", \n            joinPoint.getSignature().getName(), ex.getMessage());\n    }\n\n    // @After — выполняется ВСЕГДА (finally)\n    @After("execution(* com.example.service.*.*(..))")\n    public void logAfter(JoinPoint joinPoint) {\n        log.debug("Завершён: {}", joinPoint.getSignature().getName());\n    }\n}' },
        { type: 'heading', value: '@Around — полный контроль' },
        { type: 'text', value: '@Around — самый мощный тип Advice. Он оборачивает вызов метода и может контролировать: выполнять ли метод, изменять аргументы, изменять результат, обрабатывать исключения.' },
        { type: 'code', language: 'java', value: '@Aspect\n@Component\npublic class PerformanceAspect {\n\n    private static final Logger log = LoggerFactory.getLogger(PerformanceAspect.class);\n\n    @Around("execution(* com.example.service.*.*(..))")\n    public Object measureTime(ProceedingJoinPoint joinPoint) throws Throwable {\n        long start = System.currentTimeMillis();\n        String methodName = joinPoint.getSignature().getName();\n\n        try {\n            Object result = joinPoint.proceed();  // вызвать оригинальный метод\n            long duration = System.currentTimeMillis() - start;\n            log.info("{} выполнен за {}ms", methodName, duration);\n            \n            if (duration > 1000) {\n                log.warn("SLOW METHOD: {} took {}ms", methodName, duration);\n            }\n            return result;\n        } catch (Exception e) {\n            long duration = System.currentTimeMillis() - start;\n            log.error("{} завершился ошибкой за {}ms: {}", \n                methodName, duration, e.getMessage());\n            throw e;  // обязательно пробросить!\n        }\n    }\n}\n\n// @Around с изменением аргументов\n@Around("execution(* com.example.service.UserService.create(..))")\npublic Object sanitizeInput(ProceedingJoinPoint joinPoint) throws Throwable {\n    Object[] args = joinPoint.getArgs();\n    if (args[0] instanceof CreateUserRequest req) {\n        req.setEmail(req.getEmail().toLowerCase().trim());\n        req.setName(req.getName().trim());\n    }\n    return joinPoint.proceed(args);  // передать изменённые аргументы\n}' },
        { type: 'warning', value: 'В @Around обязательно вызывайте joinPoint.proceed() и возвращайте результат. Если забыть — метод не выполнится. Если не вернуть результат — вызывающий код получит null.' }
      ]
    },
    {
      id: 3,
      title: 'Pointcut Expressions',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Синтаксис Pointcut' },
        { type: 'text', value: 'Pointcut выражения определяют, к каким методам применяется Advice. Spring AOP поддерживает execution, within, @annotation, @within, args, bean и другие указатели.' },
        { type: 'code', language: 'java', value: '// execution — основной указатель\n// Формат: execution(модификатор? возврат пакет.класс.метод(параметры) throws?)\n\n// Все методы всех классов в пакете service\n@Before("execution(* com.example.service.*.*(..))")\n\n// Только public методы\n@Before("execution(public * com.example.service.*.*(..))")\n\n// Методы возвращающие String\n@Before("execution(String com.example.service.*.*(..))")\n\n// Методы начинающиеся с find\n@Before("execution(* com.example.service.*.find*(..))")\n\n// Методы с одним параметром типа Long\n@Before("execution(* com.example.service.*.*(Long))")\n\n// Методы без параметров\n@Before("execution(* com.example.service.*.*(  ))")\n\n// Все подпакеты (..)\n@Before("execution(* com.example.service..*.*(..))")\n\n// Конкретный метод\n@Before("execution(* com.example.service.UserService.findById(Long))")' },
        { type: 'heading', value: 'Другие указатели' },
        { type: 'code', language: 'java', value: '// within — все методы класса/пакета\n@Before("within(com.example.service.UserService)")\n@Before("within(com.example.service..*)")  // все в пакете\n\n// @annotation — методы с определённой аннотацией\n@Before("@annotation(com.example.annotation.Loggable)")\n@Before("@annotation(org.springframework.transaction.annotation.Transactional)")\n\n// @within — все методы классов с аннотацией\n@Before("@within(org.springframework.stereotype.Service)")\n\n// bean — конкретный bean по имени\n@Before("bean(userService)")\n@Before("bean(*Service)")  // все бины, заканчивающиеся на Service\n\n// args — по типу аргументов\n@Before("args(Long, ..)")  // первый аргумент Long\n@Before("args(com.example.dto.CreateUserRequest)")\n\n// target — по типу целевого объекта\n@Before("target(com.example.service.UserService)")' },
        { type: 'heading', value: 'Комбинирование Pointcut' },
        { type: 'code', language: 'java', value: '@Aspect\n@Component\npublic class AuditAspect {\n\n    // Именованные Pointcut\n    @Pointcut("execution(* com.example.service.*.*(..))")\n    public void serviceMethods() {}\n\n    @Pointcut("@annotation(com.example.annotation.Auditable)")\n    public void auditableMethods() {}\n\n    @Pointcut("execution(* com.example.controller.*.*(..))")\n    public void controllerMethods() {}\n\n    // Комбинирование: AND (&&)\n    @Before("serviceMethods() && auditableMethods()")\n    public void auditServiceCall(JoinPoint jp) {\n        log.info("Audit: {}", jp.getSignature());\n    }\n\n    // Комбинирование: OR (||)\n    @Around("serviceMethods() || controllerMethods()")\n    public Object measureAll(ProceedingJoinPoint jp) throws Throwable {\n        return measure(jp);\n    }\n\n    // Комбинирование: NOT (!)\n    @Before("serviceMethods() && !auditableMethods()")\n    public void nonAuditableOnly(JoinPoint jp) {\n        log.debug("Non-auditable call: {}", jp.getSignature());\n    }\n}' },
        { type: 'tip', value: 'Используйте именованные @Pointcut для переиспользования. Определите их в отдельном классе CommonPointcuts и ссылайтесь через полное имя: @Before("com.example.aop.CommonPointcuts.serviceMethods()")' }
      ]
    },
    {
      id: 4,
      title: 'Кастомные аннотации с AOP',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Создание кастомных аннотаций' },
        { type: 'text', value: 'Кастомные аннотации в сочетании с AOP — мощный инструмент. Вы создаёте аннотацию, а AOP-аспект определяет её поведение. Это делает код декларативным и чистым.' },
        { type: 'code', language: 'java', value: '// Аннотация для логирования\n@Target(ElementType.METHOD)\n@Retention(RetentionPolicy.RUNTIME)\npublic @interface Loggable {\n    String value() default "";\n    boolean logArgs() default true;\n    boolean logResult() default true;\n}\n\n// Аспект для @Loggable\n@Aspect\n@Component\npublic class LoggableAspect {\n\n    @Around("@annotation(loggable)")\n    public Object logMethod(ProceedingJoinPoint joinPoint, \n                            Loggable loggable) throws Throwable {\n        String methodName = loggable.value().isEmpty() \n            ? joinPoint.getSignature().getName() \n            : loggable.value();\n\n        if (loggable.logArgs()) {\n            log.info("-> {} args: {}", methodName, \n                Arrays.toString(joinPoint.getArgs()));\n        } else {\n            log.info("-> {}", methodName);\n        }\n\n        Object result = joinPoint.proceed();\n\n        if (loggable.logResult()) {\n            log.info("<- {} result: {}", methodName, result);\n        }\n        return result;\n    }\n}\n\n// Использование\n@Service\npublic class OrderService {\n    @Loggable("Создание заказа")\n    public Order createOrder(CreateOrderRequest req) {\n        // бизнес-логика\n    }\n\n    @Loggable(logResult = false)  // не логировать результат\n    public void deleteOrder(Long id) { }\n}' },
        { type: 'heading', value: '@RateLimit — ограничение частоты' },
        { type: 'code', language: 'java', value: '@Target(ElementType.METHOD)\n@Retention(RetentionPolicy.RUNTIME)\npublic @interface RateLimit {\n    int maxRequests() default 10;\n    int windowSeconds() default 60;\n}\n\n@Aspect\n@Component\npublic class RateLimitAspect {\n\n    private final Map<String, Deque<Instant>> requestLog = \n        new ConcurrentHashMap<>();\n\n    @Around("@annotation(rateLimit)")\n    public Object checkRateLimit(ProceedingJoinPoint joinPoint,\n                                  RateLimit rateLimit) throws Throwable {\n        String key = getClientKey(joinPoint);\n        Deque<Instant> timestamps = requestLog\n            .computeIfAbsent(key, k -> new ConcurrentLinkedDeque<>());\n\n        Instant cutoff = Instant.now()\n            .minus(rateLimit.windowSeconds(), ChronoUnit.SECONDS);\n\n        // Удалить старые записи\n        while (!timestamps.isEmpty() && timestamps.peekFirst().isBefore(cutoff)) {\n            timestamps.pollFirst();\n        }\n\n        if (timestamps.size() >= rateLimit.maxRequests()) {\n            throw new RateLimitExceededException(\n                "Превышен лимит: " + rateLimit.maxRequests() + \n                " запросов за " + rateLimit.windowSeconds() + " секунд");\n        }\n\n        timestamps.addLast(Instant.now());\n        return joinPoint.proceed();\n    }\n}\n\n// Использование\n@PostMapping("/login")\n@RateLimit(maxRequests = 5, windowSeconds = 60)\npublic AuthResponse login(@RequestBody LoginRequest req) { }' },
        { type: 'heading', value: '@Retry — повтор при ошибке' },
        { type: 'code', language: 'java', value: '@Target(ElementType.METHOD)\n@Retention(RetentionPolicy.RUNTIME)\npublic @interface Retryable {\n    int maxAttempts() default 3;\n    long delayMs() default 1000;\n    Class<? extends Exception>[] retryFor() default {Exception.class};\n}\n\n@Aspect\n@Component\npublic class RetryAspect {\n\n    @Around("@annotation(retryable)")\n    public Object retry(ProceedingJoinPoint joinPoint,\n                        Retryable retryable) throws Throwable {\n        int attempts = 0;\n        Exception lastException = null;\n\n        while (attempts < retryable.maxAttempts()) {\n            try {\n                return joinPoint.proceed();\n            } catch (Exception e) {\n                lastException = e;\n                boolean shouldRetry = Arrays.stream(retryable.retryFor())\n                    .anyMatch(cls -> cls.isInstance(e));\n                if (!shouldRetry) throw e;\n\n                attempts++;\n                log.warn("Попытка {}/{} для {} не удалась: {}",\n                    attempts, retryable.maxAttempts(),\n                    joinPoint.getSignature().getName(), e.getMessage());\n\n                if (attempts < retryable.maxAttempts()) {\n                    Thread.sleep(retryable.delayMs() * attempts);  // exponential backoff\n                }\n            }\n        }\n        throw lastException;\n    }\n}\n\n// Использование\n@Retryable(maxAttempts = 3, delayMs = 500, retryFor = HttpTimeoutException.class)\npublic ExternalData callExternalApi(String id) {\n    return webClient.get().uri("/data/{id}", id).retrieve().bodyToMono(ExternalData.class).block();\n}' },
        { type: 'note', value: 'Кастомные аннотации делают код самодокументируемым: @Loggable, @RateLimit, @Retryable — сразу понятно что делает метод, без чтения реализации аспекта.' }
      ]
    },
    {
      id: 5,
      title: 'Практическое AOP: логирование и метрики',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Полноценный Logging Aspect' },
        { type: 'code', language: 'java', value: '@Aspect\n@Component\n@Order(1)  // порядок выполнения аспектов\npublic class RequestLoggingAspect {\n\n    private static final Logger log = LoggerFactory.getLogger(RequestLoggingAspect.class);\n\n    @Around("@within(org.springframework.web.bind.annotation.RestController)")\n    public Object logRequest(ProceedingJoinPoint joinPoint) throws Throwable {\n        HttpServletRequest request = getCurrentRequest();\n        String traceId = MDC.get("traceId");\n\n        log.info("[{}] {} {} | method: {}.{} | args: {}",\n            traceId,\n            request != null ? request.getMethod() : "N/A",\n            request != null ? request.getRequestURI() : "N/A",\n            joinPoint.getTarget().getClass().getSimpleName(),\n            joinPoint.getSignature().getName(),\n            sanitizeArgs(joinPoint.getArgs()));\n\n        long start = System.nanoTime();\n        try {\n            Object result = joinPoint.proceed();\n            long durationMs = (System.nanoTime() - start) / 1_000_000;\n\n            log.info("[{}] {} {} completed in {}ms | status: OK",\n                traceId,\n                request != null ? request.getMethod() : "N/A",\n                request != null ? request.getRequestURI() : "N/A",\n                durationMs);\n            return result;\n        } catch (Exception e) {\n            long durationMs = (System.nanoTime() - start) / 1_000_000;\n            log.error("[{}] {} {} failed in {}ms | error: {}",\n                traceId,\n                request != null ? request.getMethod() : "N/A",\n                request != null ? request.getRequestURI() : "N/A",\n                durationMs, e.getMessage());\n            throw e;\n        }\n    }\n\n    // Скрываем пароли и токены из логов\n    private String sanitizeArgs(Object[] args) {\n        return Arrays.stream(args)\n            .map(arg -> {\n                if (arg instanceof LoginRequest) return "[CREDENTIALS]";\n                return String.valueOf(arg);\n            })\n            .collect(Collectors.joining(", "));\n    }\n\n    private HttpServletRequest getCurrentRequest() {\n        RequestAttributes attrs = RequestContextHolder.getRequestAttributes();\n        if (attrs instanceof ServletRequestAttributes) {\n            return ((ServletRequestAttributes) attrs).getRequest();\n        }\n        return null;\n    }\n}' },
        { type: 'heading', value: 'Metrics Aspect с Micrometer' },
        { type: 'code', language: 'java', value: '@Aspect\n@Component\n@Order(2)\npublic class MetricsAspect {\n\n    private final MeterRegistry meterRegistry;\n\n    public MetricsAspect(MeterRegistry meterRegistry) {\n        this.meterRegistry = meterRegistry;\n    }\n\n    @Around("execution(* com.example.service.*.*(..))")\n    public Object recordMetrics(ProceedingJoinPoint joinPoint) throws Throwable {\n        String className = joinPoint.getTarget().getClass().getSimpleName();\n        String methodName = joinPoint.getSignature().getName();\n        Timer.Sample sample = Timer.start(meterRegistry);\n\n        try {\n            Object result = joinPoint.proceed();\n            sample.stop(Timer.builder("service.method.duration")\n                .tag("class", className)\n                .tag("method", methodName)\n                .tag("status", "success")\n                .register(meterRegistry));\n\n            meterRegistry.counter("service.method.calls",\n                "class", className,\n                "method", methodName,\n                "status", "success").increment();\n\n            return result;\n        } catch (Exception e) {\n            sample.stop(Timer.builder("service.method.duration")\n                .tag("class", className)\n                .tag("method", methodName)\n                .tag("status", "error")\n                .register(meterRegistry));\n\n            meterRegistry.counter("service.method.calls",\n                "class", className,\n                "method", methodName,\n                "status", "error").increment();\n\n            throw e;\n        }\n    }\n}' },
        { type: 'heading', value: 'Audit Aspect — аудит действий' },
        { type: 'code', language: 'java', value: '@Target(ElementType.METHOD)\n@Retention(RetentionPolicy.RUNTIME)\npublic @interface Auditable {\n    String action();\n    String entity();\n}\n\n@Aspect\n@Component\npublic class AuditAspect {\n\n    private final AuditLogRepository auditRepo;\n\n    @AfterReturning(pointcut = "@annotation(auditable)", returning = "result")\n    public void audit(JoinPoint joinPoint, Auditable auditable, Object result) {\n        String userId = SecurityContextHolder.getContext()\n            .getAuthentication().getName();\n\n        AuditLog log = new AuditLog();\n        log.setUserId(userId);\n        log.setAction(auditable.action());\n        log.setEntity(auditable.entity());\n        log.setEntityId(extractId(result));\n        log.setTimestamp(LocalDateTime.now());\n        log.setDetails(joinPoint.getSignature().toString());\n\n        auditRepo.save(log);\n    }\n}\n\n// Использование\n@Auditable(action = "CREATE", entity = "Order")\npublic Order createOrder(CreateOrderRequest req) { }\n\n@Auditable(action = "DELETE", entity = "Product")\npublic void deleteProduct(Long id) { }' },
        { type: 'heading', value: 'Порядок выполнения аспектов' },
        { type: 'text', value: 'Когда несколько аспектов применяются к одному методу, порядок определяется аннотацией @Order. Меньшее значение = более высокий приоритет (выполняется первым для @Before, последним для @After).' },
        { type: 'code', language: 'java', value: '@Aspect @Component @Order(1)\npublic class SecurityAspect { }     // первый\n\n@Aspect @Component @Order(2)\npublic class LoggingAspect { }      // второй\n\n@Aspect @Component @Order(3)\npublic class MetricsAspect { }      // третий\n\n// Порядок вызова:\n// SecurityAspect.before()\n//   LoggingAspect.before()\n//     MetricsAspect.before()\n//       targetMethod()\n//     MetricsAspect.after()\n//   LoggingAspect.after()\n// SecurityAspect.after()' },
        { type: 'tip', value: 'Используйте @Order для предсказуемого порядка: Security -> Logging -> Metrics -> Business. Это гарантирует что аудит записывает только авторизованные действия.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Создание аспектов для приложения',
      type: 'practice',
      difficulty: 'medium',
      description: 'Создайте набор AOP-аспектов: кастомную аннотацию @Timed для измерения времени выполнения, аспект для автоматического логирования методов сервисов и аспект для проверки прав доступа.',
      requirements: [
        'Аннотация @Timed с параметром metricName — измеряет время выполнения метода',
        'TimedAspect: @Around для @Timed, логирует время и записывает в MeterRegistry',
        'ServiceLoggingAspect: @Before/@AfterReturning для всех методов в пакете service',
        'Аннотация @RequiresRole(value = "ADMIN") — проверяет роль текущего пользователя',
        'RoleCheckAspect: @Before для @RequiresRole, бросает AccessDeniedException если роли нет',
        'Написать юнит-тесты для каждого аспекта'
      ],
      hint: 'Для тестирования AOP используйте @SpringBootTest с реальным ApplicationContext — Spring создаст прокси. Получите bean из контекста и вызовите метод.',
      expectedOutput: '@Timed("order.create")\npublic Order createOrder(req) { ... }\n\nЛоги:\nINFO  -> OrderService.createOrder([CreateOrderRequest{...}])\nINFO  <- OrderService.createOrder completed in 45ms\nINFO  Metric: order.create = 45ms\n\n@RequiresRole("ADMIN")\npublic void deleteUser(id) { ... }\n\nС ролью USER:\nAccessDeniedException: Требуется роль ADMIN\n\nС ролью ADMIN:\nINFO  -> AdminService.deleteUser([5])\nINFO  <- AdminService.deleteUser completed',
      solution: '@Target(ElementType.METHOD)\n@Retention(RetentionPolicy.RUNTIME)\npublic @interface Timed {\n    String value() default "";\n}\n\n@Target(ElementType.METHOD)\n@Retention(RetentionPolicy.RUNTIME)\npublic @interface RequiresRole {\n    String value();\n}\n\n@Aspect @Component\npublic class TimedAspect {\n    private final MeterRegistry registry;\n\n    @Around("@annotation(timed)")\n    public Object measureTime(ProceedingJoinPoint jp, Timed timed) throws Throwable {\n        String metric = timed.value().isEmpty() \n            ? jp.getSignature().getName() : timed.value();\n        Timer.Sample sample = Timer.start(registry);\n        try {\n            Object result = jp.proceed();\n            sample.stop(registry.timer(metric, "status", "success"));\n            log.info("Metric: {} = {}ms", metric, sample);\n            return result;\n        } catch (Exception e) {\n            sample.stop(registry.timer(metric, "status", "error"));\n            throw e;\n        }\n    }\n}\n\n@Aspect @Component\npublic class RoleCheckAspect {\n    @Before("@annotation(requiresRole)")\n    public void checkRole(RequiresRole requiresRole) {\n        Authentication auth = SecurityContextHolder.getContext().getAuthentication();\n        boolean hasRole = auth.getAuthorities().stream()\n            .anyMatch(a -> a.getAuthority().equals("ROLE_" + requiresRole.value()));\n        if (!hasRole) {\n            throw new AccessDeniedException("Требуется роль " + requiresRole.value());\n        }\n    }\n}\n\n// Тест\n@SpringBootTest\nclass TimedAspectTest {\n    @Autowired OrderService orderService;\n    @Autowired MeterRegistry registry;\n\n    @Test\n    void timedAnnotation_shouldRecordMetric() {\n        orderService.createOrder(new CreateOrderRequest(...));\n        Timer timer = registry.find("order.create").timer();\n        assertNotNull(timer);\n        assertEquals(1, timer.count());\n    }\n}',
      explanation: 'Кастомные аннотации с AOP позволяют добавлять поведение декларативно. @Around даёт полный контроль — можно измерить время, изменить результат или заблокировать выполнение. SecurityContextHolder даёт доступ к текущему пользователю в любом аспекте.'
    }
  ]
}
