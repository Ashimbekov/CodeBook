export default {
  id: 41,
  title: 'OAuth2 и SSO',
  description: 'OAuth2 и Single Sign-On в Spring Boot: OAuth2 flows, настройка OAuth2 клиента и Resource Server, интеграция с Keycloak, социальная авторизация, управление токенами.',
  lessons: [
    {
      id: 1,
      title: 'OAuth2 Flows',
      type: 'theory',
      content: [
        { type: `heading`, value: `Что такое OAuth2?` },
        { type: `text`, value: `OAuth2 — это протокол авторизации, позволяющий приложениям получать ограниченный доступ к ресурсам пользователя без передачи пароля. Ключевые роли: Resource Owner (пользователь), Client (приложение), Authorization Server (выдаёт токены), Resource Server (защищённый API).` },
        { type: `heading`, value: `Authorization Code Flow (рекомендуемый)` },
        { type: `text`, value: `Самый безопасный flow для веб-приложений с серверной частью. Пользователь перенаправляется на Authorization Server, авторизуется, получает code, который обменивается на token на сервере.` },
        { type: `code`, language: `text`, value: `1. Client -> Authorization Server: /authorize?response_type=code&client_id=X&redirect_uri=Y&scope=read\n2. Пользователь авторизуется на странице Authorization Server\n3. Authorization Server -> Client: redirect_uri?code=AUTHORIZATION_CODE\n4. Client -> Authorization Server: /token (code + client_secret)\n5. Authorization Server -> Client: { access_token, refresh_token, expires_in }\n6. Client -> Resource Server: GET /api/data (Authorization: Bearer access_token)` },
        { type: `heading`, value: `Authorization Code + PKCE (для SPA и мобильных)` },
        { type: `text`, value: `PKCE (Proof Key for Code Exchange) добавляет дополнительную защиту для публичных клиентов (SPA, мобильные приложения), которые не могут безопасно хранить client_secret.` },
        { type: `code`, language: `text`, value: `1. Client генерирует code_verifier (случайная строка) и code_challenge = SHA256(code_verifier)\n2. Client -> Authorization Server: /authorize?response_type=code&code_challenge=X&code_challenge_method=S256\n3. Пользователь авторизуется\n4. Authorization Server -> Client: redirect_uri?code=CODE\n5. Client -> Authorization Server: /token (code + code_verifier)\n6. Authorization Server проверяет: SHA256(code_verifier) == code_challenge\n7. Authorization Server -> Client: { access_token }` },
        { type: `heading`, value: `Client Credentials Flow (для сервисов)` },
        { type: `text`, value: `Для M2M (machine-to-machine) коммуникации, когда нет пользователя. Сервис аутентифицируется своими credentials и получает токен.` },
        { type: `code`, language: `text`, value: `1. Service -> Authorization Server: /token (client_id + client_secret + grant_type=client_credentials)\n2. Authorization Server -> Service: { access_token }\n3. Service -> Resource Server: GET /api/data (Authorization: Bearer access_token)` },
        { type: `heading`, value: `Refresh Token Flow` },
        { type: `code`, language: `text`, value: `// Когда access_token истёк:\n1. Client -> Authorization Server: /token (refresh_token + grant_type=refresh_token)\n2. Authorization Server -> Client: { new_access_token, new_refresh_token }` },
        { type: `note`, value: `Resource Owner Password Flow (логин/пароль через API) и Implicit Flow считаются устаревшими и небезопасными. Используйте Authorization Code + PKCE.` }
      ]
    },
    {
      id: 2,
      title: 'Spring Security OAuth2 Client',
      type: 'theory',
      content: [
        { type: `heading`, value: `Настройка OAuth2 Client` },
        { type: `code`, language: `xml`, value: `<dependency>\n    <groupId>org.springframework.boot</groupId>\n    <artifactId>spring-boot-starter-oauth2-client</artifactId>\n</dependency>` },
        { type: `code`, language: `yaml`, value: `# application.yml\nspring:\n  security:\n    oauth2:\n      client:\n        registration:\n          keycloak:\n            client-id: my-app\n            client-secret: secret\n            scope: openid, profile, email\n            authorization-grant-type: authorization_code\n            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"\n          google:\n            client-id: google-client-id\n            client-secret: google-secret\n            scope: openid, profile, email\n          github:\n            client-id: github-client-id\n            client-secret: github-secret\n            scope: user:email\n        provider:\n          keycloak:\n            issuer-uri: http://localhost:8180/realms/my-realm\n            # Автоматически настраивает все endpoints` },
        { type: `heading`, value: `Security Configuration` },
        { type: `code`, language: `java`, value: `@Configuration\n@EnableWebSecurity\npublic class SecurityConfig {\n\n    @Bean\n    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {\n        return http\n            .authorizeHttpRequests(auth -> auth\n                .requestMatchers(\"/\", \"/public/**\").permitAll()\n                .requestMatchers(\"/admin/**\").hasRole(\"ADMIN\")\n                .anyRequest().authenticated()\n            )\n            .oauth2Login(oauth2 -> oauth2\n                .loginPage(\"/login\")\n                .defaultSuccessUrl(\"/dashboard\")\n                .failureUrl(\"/login?error\")\n                .userInfoEndpoint(userInfo -> userInfo\n                    .userService(customOAuth2UserService())\n                )\n            )\n            .oauth2Client(Customizer.withDefaults())\n            .logout(logout -> logout\n                .logoutSuccessUrl(\"/\")\n                .deleteCookies(\"JSESSIONID\")\n            )\n            .build();\n    }\n\n    @Bean\n    public OAuth2UserService<OAuth2UserRequest, OAuth2User> customOAuth2UserService() {\n        return new CustomOAuth2UserService();\n    }\n}` },
        { type: `heading`, value: `Кастомный OAuth2UserService` },
        { type: `code`, language: `java`, value: `@Service\npublic class CustomOAuth2UserService implements OAuth2UserService<OAuth2UserRequest, OAuth2User> {\n\n    private final UserRepository userRepository;\n    private final DefaultOAuth2UserService delegate = new DefaultOAuth2UserService();\n\n    @Override\n    public OAuth2User loadUser(OAuth2UserRequest userRequest) {\n        OAuth2User oAuth2User = delegate.loadUser(userRequest);\n        String provider = userRequest.getClientRegistration().getRegistrationId();\n        String providerId = oAuth2User.getAttribute(\"sub\");\n        String email = oAuth2User.getAttribute(\"email\");\n        String name = oAuth2User.getAttribute(\"name\");\n\n        // Создать или обновить пользователя в БД\n        User user = userRepository.findByProviderAndProviderId(provider, providerId)\n            .orElseGet(() -> {\n                User newUser = new User();\n                newUser.setProvider(provider);\n                newUser.setProviderId(providerId);\n                newUser.setEmail(email);\n                newUser.setName(name);\n                newUser.setRole(\"USER\");\n                return userRepository.save(newUser);\n            });\n\n        return new CustomOAuth2User(oAuth2User, user);\n    }\n}\n\npublic class CustomOAuth2User implements OAuth2User {\n    private final OAuth2User delegate;\n    private final User localUser;\n\n    @Override\n    public Collection<? extends GrantedAuthority> getAuthorities() {\n        return List.of(new SimpleGrantedAuthority(\"ROLE_\" + localUser.getRole()));\n    }\n\n    @Override\n    public String getName() { return localUser.getName(); }\n\n    @Override\n    public Map<String, Object> getAttributes() {\n        return delegate.getAttributes();\n    }\n\n    public Long getUserId() { return localUser.getId(); }\n}` },
        { type: `tip`, value: `Spring Boot автоматически генерирует login page с кнопками для каждого зарегистрированного провайдера. Для кастомной страницы используйте .loginPage("/custom-login").` }
      ]
    },
    {
      id: 3,
      title: 'OAuth2 Resource Server',
      type: 'theory',
      content: [
        { type: `heading`, value: `Настройка Resource Server` },
        { type: `text`, value: `Resource Server — это API, защищённый OAuth2 токенами. Он валидирует JWT access_token и извлекает из него права доступа.` },
        { type: `code`, language: `xml`, value: `<dependency>\n    <groupId>org.springframework.boot</groupId>\n    <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>\n</dependency>` },
        { type: `code`, language: `yaml`, value: `# application.yml\nspring:\n  security:\n    oauth2:\n      resourceserver:\n        jwt:\n          issuer-uri: http://localhost:8180/realms/my-realm\n          # Автоматически загружает JWK Set для валидации\n          # jwk-set-uri: http://localhost:8180/realms/my-realm/protocol/openid-connect/certs` },
        { type: `heading`, value: `Security Configuration для Resource Server` },
        { type: `code`, language: `java`, value: `@Configuration\n@EnableWebSecurity\n@EnableMethodSecurity\npublic class ResourceServerConfig {\n\n    @Bean\n    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {\n        return http\n            .authorizeHttpRequests(auth -> auth\n                .requestMatchers(\"/api/public/**\").permitAll()\n                .requestMatchers(\"/api/admin/**\").hasRole(\"ADMIN\")\n                .requestMatchers(\"/api/**\").authenticated()\n            )\n            .oauth2ResourceServer(oauth2 -> oauth2\n                .jwt(jwt -> jwt\n                    .jwtAuthenticationConverter(jwtAuthConverter())\n                )\n            )\n            .sessionManagement(session -> \n                session.sessionCreationPolicy(SessionCreationPolicy.STATELESS))\n            .csrf(csrf -> csrf.disable())\n            .build();\n    }\n\n    // Конвертер ролей из JWT\n    @Bean\n    public JwtAuthenticationConverter jwtAuthConverter() {\n        JwtGrantedAuthoritiesConverter converter = \n            new JwtGrantedAuthoritiesConverter();\n        converter.setAuthoritiesClaimName(\"realm_access.roles\");\n        converter.setAuthorityPrefix(\"ROLE_\");\n\n        JwtAuthenticationConverter jwtConverter = new JwtAuthenticationConverter();\n        jwtConverter.setJwtGrantedAuthoritiesConverter(jwt -> {\n            // Извлекаем роли из Keycloak JWT\n            Map<String, Object> realmAccess = jwt.getClaim(\"realm_access\");\n            if (realmAccess == null) return List.of();\n\n            @SuppressWarnings(\"unchecked\")\n            List<String> roles = (List<String>) realmAccess.get(\"roles\");\n            return roles.stream()\n                .map(role -> new SimpleGrantedAuthority(\"ROLE_\" + role.toUpperCase()))\n                .collect(Collectors.toList());\n        });\n\n        return jwtConverter;\n    }\n}` },
        { type: `heading`, value: `Использование в контроллерах` },
        { type: `code`, language: `java`, value: `@RestController\n@RequestMapping(\"/api\")\npublic class ApiController {\n\n    // Доступ к данным JWT\n    @GetMapping(\"/me\")\n    public Map<String, Object> me(@AuthenticationPrincipal Jwt jwt) {\n        return Map.of(\n            \"sub\", jwt.getSubject(),\n            \"email\", jwt.getClaim(\"email\"),\n            \"name\", jwt.getClaim(\"preferred_username\"),\n            \"roles\", jwt.getClaim(\"realm_access\")\n        );\n    }\n\n    @GetMapping(\"/users\")\n    @PreAuthorize(\"hasRole(\\'ADMIN\\')\")\n    public List<User> allUsers() {\n        return userService.findAll();\n    }\n\n    @GetMapping(\"/users/{id}\")\n    @PreAuthorize(\"hasRole(\\'ADMIN\\') or #id == authentication.token.claims[\\'sub\\']\")\n    public User getUser(@PathVariable String id) {\n        return userService.findById(id);\n    }\n\n    // Scope-based авторизация\n    @GetMapping(\"/reports\")\n    @PreAuthorize(\"hasAuthority(\\'SCOPE_reports:read\\')\")\n    public List<Report> getReports() {\n        return reportService.findAll();\n    }\n}` },
        { type: `note`, value: `Resource Server не хранит сессии (STATELESS). Каждый запрос должен содержать валидный JWT токен в заголовке Authorization: Bearer <token>.` }
      ]
    },
    {
      id: 4,
      title: 'Keycloak: Identity Provider',
      type: 'theory',
      content: [
        { type: `heading`, value: `Что такое Keycloak?` },
        { type: `text`, value: `Keycloak — это open-source Identity and Access Management (IAM) сервер. Он предоставляет готовый Authorization Server с поддержкой OAuth2, OpenID Connect, SAML, SSO, социальной авторизации и управления пользователями.` },
        { type: `heading`, value: `Запуск Keycloak в Docker` },
        { type: `code`, language: `yaml`, value: `# docker-compose.yml\nservices:\n  keycloak:\n    image: quay.io/keycloak/keycloak:24.0\n    command: start-dev\n    environment:\n      KEYCLOAK_ADMIN: admin\n      KEYCLOAK_ADMIN_PASSWORD: admin\n      KC_DB: postgres\n      KC_DB_URL: jdbc:postgresql://postgres:5432/keycloak\n      KC_DB_USERNAME: keycloak\n      KC_DB_PASSWORD: password\n    ports:\n      - "8180:8080"\n    depends_on:\n      - postgres\n\n  postgres:\n    image: postgres:16\n    environment:\n      POSTGRES_DB: keycloak\n      POSTGRES_USER: keycloak\n      POSTGRES_PASSWORD: password` },
        { type: `heading`, value: `Настройка Realm и Client в Keycloak` },
        { type: `code`, language: `text`, value: `1. Создать Realm: "my-app-realm"\n2. Создать Client:\n   - Client ID: spring-boot-app\n   - Client Authentication: ON (confidential)\n   - Valid Redirect URIs: http://localhost:8080/*\n   - Web Origins: http://localhost:8080\n3. Создать Roles: ADMIN, USER\n4. Создать Users: admin (роль ADMIN), user1 (роль USER)\n5. Копировать Client Secret из вкладки Credentials` },
        { type: `heading`, value: `Spring Boot + Keycloak` },
        { type: `code`, language: `yaml`, value: `# application.yml\nspring:\n  security:\n    oauth2:\n      client:\n        registration:\n          keycloak:\n            client-id: spring-boot-app\n            client-secret: copy-from-keycloak\n            scope: openid, profile, email\n            authorization-grant-type: authorization_code\n        provider:\n          keycloak:\n            issuer-uri: http://localhost:8180/realms/my-app-realm\n      resourceserver:\n        jwt:\n          issuer-uri: http://localhost:8180/realms/my-app-realm` },
        { type: `heading`, value: `Keycloak Admin API` },
        { type: `code`, language: `java`, value: `// Программное управление пользователями через Keycloak Admin Client\n// <dependency>\n//   <groupId>org.keycloak</groupId>\n//   <artifactId>keycloak-admin-client</artifactId>\n// </dependency>\n\n@Service\npublic class KeycloakAdminService {\n\n    private final Keycloak keycloak;\n\n    public KeycloakAdminService() {\n        this.keycloak = KeycloakBuilder.builder()\n            .serverUrl(\"http://localhost:8180\")\n            .realm(\"master\")\n            .clientId(\"admin-cli\")\n            .username(\"admin\")\n            .password(\"admin\")\n            .build();\n    }\n\n    public void createUser(String username, String email, String password) {\n        UserRepresentation user = new UserRepresentation();\n        user.setUsername(username);\n        user.setEmail(email);\n        user.setEnabled(true);\n        user.setEmailVerified(true);\n\n        CredentialRepresentation credential = new CredentialRepresentation();\n        credential.setType(CredentialRepresentation.PASSWORD);\n        credential.setValue(password);\n        credential.setTemporary(false);\n        user.setCredentials(List.of(credential));\n\n        Response response = keycloak.realm(\"my-app-realm\")\n            .users().create(user);\n\n        if (response.getStatus() == 201) {\n            String userId = CreatedResponseUtil.getCreatedId(response);\n            assignRole(userId, \"USER\");\n        }\n    }\n\n    private void assignRole(String userId, String roleName) {\n        RoleRepresentation role = keycloak.realm(\"my-app-realm\")\n            .roles().get(roleName).toRepresentation();\n\n        keycloak.realm(\"my-app-realm\")\n            .users().get(userId)\n            .roles().realmLevel()\n            .add(List.of(role));\n    }\n}` },
        { type: `tip`, value: `Keycloak предоставляет готовые страницы логина, регистрации, восстановления пароля и управления аккаунтом. Их можно кастомизировать через темы.` }
      ]
    },
    {
      id: 5,
      title: 'Social Login и Token Management',
      type: 'theory',
      content: [
        { type: `heading`, value: `Social Login: Google, GitHub, Facebook` },
        { type: `code`, language: `yaml`, value: `# application.yml — Social Login\nspring:\n  security:\n    oauth2:\n      client:\n        registration:\n          google:\n            client-id: ${GOOGLE_CLIENT_ID}\n            client-secret: ${GOOGLE_CLIENT_SECRET}\n            scope: openid, profile, email\n          github:\n            client-id: ${GITHUB_CLIENT_ID}\n            client-secret: ${GITHUB_CLIENT_SECRET}\n            scope: user:email, read:user\n          facebook:\n            client-id: ${FACEBOOK_CLIENT_ID}\n            client-secret: ${FACEBOOK_CLIENT_SECRET}\n            scope: email, public_profile` },
        { type: `heading`, value: `Обработка Social Login` },
        { type: `code`, language: `java`, value: `@Service\npublic class SocialLoginService implements OAuth2UserService<OAuth2UserRequest, OAuth2User> {\n\n    private final UserRepository userRepo;\n\n    @Override\n    public OAuth2User loadUser(OAuth2UserRequest request) {\n        DefaultOAuth2UserService delegate = new DefaultOAuth2UserService();\n        OAuth2User oAuth2User = delegate.loadUser(request);\n\n        String provider = request.getClientRegistration().getRegistrationId();\n        String userNameAttribute = request.getClientRegistration()\n            .getProviderDetails().getUserInfoEndpoint()\n            .getUserNameAttributeName();\n\n        // Извлечение данных в зависимости от провайдера\n        String email;\n        String name;\n        String avatarUrl;\n\n        switch (provider) {\n            case \"google\" -> {\n                email = oAuth2User.getAttribute(\"email\");\n                name = oAuth2User.getAttribute(\"name\");\n                avatarUrl = oAuth2User.getAttribute(\"picture\");\n            }\n            case \"github\" -> {\n                email = oAuth2User.getAttribute(\"email\");\n                name = oAuth2User.getAttribute(\"login\");\n                avatarUrl = oAuth2User.getAttribute(\"avatar_url\");\n            }\n            default -> throw new OAuth2AuthenticationException(\n                \"Неподдерживаемый провайдер: \" + provider);\n        }\n\n        User user = userRepo.findByEmail(email)\n            .map(existing -> {\n                existing.setName(name);\n                existing.setAvatarUrl(avatarUrl);\n                return userRepo.save(existing);\n            })\n            .orElseGet(() -> userRepo.save(\n                new User(name, email, avatarUrl, provider)));\n\n        return new CustomOAuth2User(oAuth2User, user);\n    }\n}` },
        { type: `heading`, value: `Token Management` },
        { type: `code`, language: `java`, value: `// Хранение и обновление токенов\n@Service\npublic class TokenService {\n\n    private final OAuth2AuthorizedClientService clientService;\n\n    // Получить access token текущего пользователя\n    public String getAccessToken(Authentication authentication) {\n        OAuth2AuthenticationToken oauthToken = \n            (OAuth2AuthenticationToken) authentication;\n\n        OAuth2AuthorizedClient client = clientService.loadAuthorizedClient(\n            oauthToken.getAuthorizedClientRegistrationId(),\n            oauthToken.getName());\n\n        return client.getAccessToken().getTokenValue();\n    }\n\n    // Проверить не истёк ли токен\n    public boolean isTokenExpired(OAuth2AuthorizedClient client) {\n        OAuth2AccessToken token = client.getAccessToken();\n        return token.getExpiresAt() != null && \n               token.getExpiresAt().isBefore(Instant.now());\n    }\n}\n\n// M2M коммуникация с Client Credentials\n@Configuration\npublic class M2MClientConfig {\n\n    @Bean\n    public WebClient webClient(\n            OAuth2AuthorizedClientManager clientManager) {\n        ServletOAuth2AuthorizedClientExchangeFilterFunction oauth2 =\n            new ServletOAuth2AuthorizedClientExchangeFilterFunction(clientManager);\n        oauth2.setDefaultClientRegistrationId(\"service-client\");\n\n        return WebClient.builder()\n            .apply(oauth2.oauth2Configuration())\n            .build();\n    }\n\n    @Bean\n    public OAuth2AuthorizedClientManager clientManager(\n            ClientRegistrationRepository registrations,\n            OAuth2AuthorizedClientRepository clients) {\n        OAuth2AuthorizedClientProvider provider =\n            OAuth2AuthorizedClientProviderBuilder.builder()\n                .clientCredentials()\n                .refreshToken()\n                .build();\n\n        DefaultOAuth2AuthorizedClientManager manager =\n            new DefaultOAuth2AuthorizedClientManager(registrations, clients);\n        manager.setAuthorizedClientProvider(provider);\n        return manager;\n    }\n}` },
        { type: `heading`, value: `Logout с OAuth2` },
        { type: `code`, language: `java`, value: `// Logout с OAuth2 — уведомить Authorization Server\n@Bean\npublic SecurityFilterChain filterChain(HttpSecurity http) throws Exception {\n    return http\n        .oauth2Login(Customizer.withDefaults())\n        .logout(logout -> logout\n            .logoutSuccessHandler(oidcLogoutHandler())\n        )\n        .build();\n}\n\nprivate OidcClientInitiatedLogoutSuccessHandler oidcLogoutHandler() {\n    OidcClientInitiatedLogoutSuccessHandler handler =\n        new OidcClientInitiatedLogoutSuccessHandler(clientRegistrationRepo);\n    handler.setPostLogoutRedirectUri(\"{baseUrl}/\");\n    return handler;\n    // Перенаправляет на Keycloak logout endpoint, затем обратно\n}` },
        { type: `note`, value: `SSO (Single Sign-On) достигается через общий Authorization Server (Keycloak). Пользователь логинится один раз и получает доступ ко всем приложениям realm.` }
      ]
    },
    {
      id: 6,
      title: 'Практика: OAuth2 защита API',
      type: 'practice',
      difficulty: 'hard',
      description: 'Настройте OAuth2 Resource Server с JWT токенами и Social Login через Google.',
      requirements: [
        'Resource Server с JWT валидацией (issuer-uri)',
        'JwtAuthenticationConverter для извлечения ролей из JWT',
        'GET /api/me — данные текущего пользователя из JWT',
        'GET /api/admin — только ROLE_ADMIN',
        '@PreAuthorize на уровне методов',
        'Social Login через Google OAuth2',
        'CustomOAuth2UserService — сохранение пользователя в БД при первом входе',
        'M2M коммуникация через Client Credentials'
      ],
      hint: 'Для тестирования используйте MockMvc с SecurityMockMvcRequestPostProcessors.jwt() для имитации JWT токена.',
      expectedOutput: 'GET /api/me (с валидным JWT):\n{\n  "sub": "user-uuid",\n  "email": "ivan@gmail.com",\n  "name": "Иван",\n  "roles": ["USER"]\n}\n\nGET /api/me (без токена):\nHTTP 401 Unauthorized\n\nGET /api/admin (роль USER):\nHTTP 403 Forbidden\n\nGET /api/admin (роль ADMIN):\nHTTP 200 OK\n\nGET /login -> перенаправление на Google OAuth2\nПосле авторизации -> /dashboard\nUser создан в БД: {name: "Иван", email: "ivan@gmail.com", provider: "google"}',
      solution: `@Configuration @EnableWebSecurity @EnableMethodSecurity\npublic class SecurityConfig {\n\n    @Bean\n    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {\n        return http\n            .authorizeHttpRequests(auth -> auth\n                .requestMatchers("/api/public/**").permitAll()\n                .requestMatchers("/api/admin/**").hasRole("ADMIN")\n                .anyRequest().authenticated())\n            .oauth2ResourceServer(oauth2 -> oauth2\n                .jwt(jwt -> jwt.jwtAuthenticationConverter(jwtConverter())))\n            .oauth2Login(oauth2 -> oauth2\n                .userInfoEndpoint(ui -> ui.userService(socialLoginService)))\n            .sessionManagement(s -> s.sessionCreationPolicy(STATELESS))\n            .build();\n    }\n\n    private JwtAuthenticationConverter jwtConverter() {\n        JwtAuthenticationConverter converter = new JwtAuthenticationConverter();\n        converter.setJwtGrantedAuthoritiesConverter(jwt -> {\n            Map<String, Object> realmAccess = jwt.getClaim("realm_access");\n            List<String> roles = (List<String>) realmAccess.get("roles");\n            return roles.stream()\n                .map(r -> new SimpleGrantedAuthority("ROLE_" + r.toUpperCase()))\n                .collect(Collectors.toList());\n        });\n        return converter;\n    }\n}\n\n@RestController @RequestMapping("/api")\npublic class ApiController {\n    @GetMapping("/me")\n    public Map<String, Object> me(@AuthenticationPrincipal Jwt jwt) {\n        return Map.of("sub", jwt.getSubject(), "email", jwt.getClaim("email"));\n    }\n\n    @GetMapping("/admin") @PreAuthorize("hasRole(\\'ADMIN\\')")\n    public String admin() { return "Admin area"; }\n}\n\n// Тест\n@WebMvcTest(ApiController.class)\nclass ApiControllerTest {\n    @Autowired MockMvc mvc;\n\n    @Test\n    void me_withJwt() throws Exception {\n        mvc.perform(get("/api/me").with(jwt()\n                .jwt(j -> j.subject("user-1").claim("email", "test@mail.com"))\n                .authorities(new SimpleGrantedAuthority("ROLE_USER"))))\n            .andExpect(status().isOk())\n            .andExpect(jsonPath("$.sub").value("user-1"));\n    }\n}`,
      explanation: 'Resource Server валидирует JWT при каждом запросе через JWK endpoint. JwtAuthenticationConverter извлекает роли из структуры токена (Keycloak хранит их в realm_access.roles). SecurityMockMvcRequestPostProcessors.jwt() позволяет тестировать без реального Authorization Server.'
    }
  ]
}
