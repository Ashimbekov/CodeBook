export default {
  id: 38,
  title: 'gRPC с Spring Boot',
  description: 'Интеграция gRPC с Spring Boot: основы gRPC и Protocol Buffers, определение сервисов, реализация серверной и клиентской части, streaming, обработка ошибок.',
  lessons: [
    {
      id: 1,
      title: 'Основы gRPC и Protocol Buffers',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Что такое gRPC?' },
        { type: 'text', value: 'gRPC — это высокопроизводительный RPC-фреймворк от Google. Он использует HTTP/2 для транспорта и Protocol Buffers (protobuf) для сериализации. gRPC значительно быстрее REST/JSON для межсервисного взаимодействия.' },
        { type: 'heading', value: 'REST vs gRPC' },
        { type: 'list', items: [
          'REST: текстовый JSON, HTTP/1.1, один запрос-ответ, человекочитаемый',
          'gRPC: бинарный protobuf (в 5-10x компактнее), HTTP/2, поддержка streaming, строго типизированный контракт',
          'gRPC идеален для межсервисной коммуникации, REST — для публичных API'
        ] },
        { type: 'heading', value: 'Protocol Buffers — определение контракта' },
        { type: 'code', language: 'protobuf', value: '// src/main/proto/user.proto\nsyntax = "proto3";\n\npackage com.example.grpc;\n\noption java_multiple_files = true;\noption java_package = "com.example.grpc.user";\n\n// Определение сервиса\nservice UserService {\n    // Unary — один запрос, один ответ\n    rpc GetUser (GetUserRequest) returns (UserResponse);\n    rpc CreateUser (CreateUserRequest) returns (UserResponse);\n    rpc DeleteUser (DeleteUserRequest) returns (Empty);\n    \n    // Server streaming — один запрос, поток ответов\n    rpc ListUsers (ListUsersRequest) returns (stream UserResponse);\n    \n    // Client streaming — поток запросов, один ответ\n    rpc ImportUsers (stream CreateUserRequest) returns (ImportResult);\n    \n    // Bidirectional streaming — поток в обе стороны\n    rpc Chat (stream ChatMessage) returns (stream ChatMessage);\n}\n\n// Сообщения\nmessage GetUserRequest {\n    int64 id = 1;\n}\n\nmessage CreateUserRequest {\n    string name = 1;\n    string email = 2;\n    int32 age = 3;\n}\n\nmessage DeleteUserRequest {\n    int64 id = 1;\n}\n\nmessage UserResponse {\n    int64 id = 1;\n    string name = 2;\n    string email = 3;\n    int32 age = 4;\n    string created_at = 5;\n}\n\nmessage ListUsersRequest {\n    int32 page = 1;\n    int32 size = 2;\n}\n\nmessage ImportResult {\n    int32 imported_count = 1;\n    int32 failed_count = 2;\n}\n\nmessage ChatMessage {\n    string sender = 1;\n    string text = 2;\n    string timestamp = 3;\n}\n\nmessage Empty {}' },
        { type: 'heading', value: 'Подключение к проекту' },
        { type: 'code', language: 'xml', value: '<!-- pom.xml -->\n<dependency>\n    <groupId>net.devh</groupId>\n    <artifactId>grpc-spring-boot-starter</artifactId>\n    <version>3.1.0.RELEASE</version>\n</dependency>\n\n<build>\n    <extensions>\n        <extension>\n            <groupId>kr.motd.maven</groupId>\n            <artifactId>os-maven-plugin</artifactId>\n            <version>1.7.1</version>\n        </extension>\n    </extensions>\n    <plugins>\n        <plugin>\n            <groupId>org.xolstice.maven.plugins</groupId>\n            <artifactId>protobuf-maven-plugin</artifactId>\n            <version>0.6.1</version>\n            <configuration>\n                <protocArtifact>com.google.protobuf:protoc:3.25.1:exe:${os.detected.classifier}</protocArtifact>\n                <pluginId>grpc-java</pluginId>\n                <pluginArtifact>io.grpc:protoc-gen-grpc-java:1.61.0:exe:${os.detected.classifier}</pluginArtifact>\n            </configuration>\n            <executions>\n                <execution>\n                    <goals>\n                        <goal>compile</goal>\n                        <goal>compile-custom</goal>\n                    </goals>\n                </execution>\n            </executions>\n        </plugin>\n    </plugins>\n</build>' },
        { type: 'tip', value: 'Protocol Buffers генерирует Java-классы из .proto файлов при компиляции. Запустите mvn compile для генерации. Сгенерированные классы появятся в target/generated-sources/protobuf/.' }
      ]
    },
    {
      id: 2,
      title: 'gRPC Server в Spring Boot',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Реализация gRPC сервиса' },
        { type: 'code', language: 'java', value: '@GrpcService\npublic class UserGrpcService extends UserServiceGrpc.UserServiceImplBase {\n\n    private final UserService userService;\n\n    public UserGrpcService(UserService userService) {\n        this.userService = userService;\n    }\n\n    // Unary — GetUser\n    @Override\n    public void getUser(GetUserRequest request, \n                        StreamObserver<UserResponse> responseObserver) {\n        User user = userService.findById(request.getId())\n            .orElseThrow(() -> Status.NOT_FOUND\n                .withDescription("Пользователь " + request.getId() + " не найден")\n                .asRuntimeException());\n\n        UserResponse response = UserResponse.newBuilder()\n            .setId(user.getId())\n            .setName(user.getName())\n            .setEmail(user.getEmail())\n            .setAge(user.getAge())\n            .setCreatedAt(user.getCreatedAt().toString())\n            .build();\n\n        responseObserver.onNext(response);\n        responseObserver.onCompleted();\n    }\n\n    // Unary — CreateUser\n    @Override\n    public void createUser(CreateUserRequest request,\n                           StreamObserver<UserResponse> responseObserver) {\n        User user = userService.create(\n            new CreateUserDto(request.getName(), request.getEmail(), request.getAge())\n        );\n\n        responseObserver.onNext(toResponse(user));\n        responseObserver.onCompleted();\n    }\n\n    // Unary — DeleteUser\n    @Override\n    public void deleteUser(DeleteUserRequest request,\n                           StreamObserver<Empty> responseObserver) {\n        userService.deleteById(request.getId());\n        responseObserver.onNext(Empty.getDefaultInstance());\n        responseObserver.onCompleted();\n    }\n\n    private UserResponse toResponse(User user) {\n        return UserResponse.newBuilder()\n            .setId(user.getId())\n            .setName(user.getName())\n            .setEmail(user.getEmail())\n            .setAge(user.getAge())\n            .build();\n    }\n}' },
        { type: 'heading', value: 'Server Streaming' },
        { type: 'code', language: 'java', value: '// Server Streaming — ListUsers\n@Override\npublic void listUsers(ListUsersRequest request,\n                      StreamObserver<UserResponse> responseObserver) {\n    Page<User> page = userService.findAll(\n        PageRequest.of(request.getPage(), request.getSize()));\n\n    // Отправляем каждого пользователя по одному\n    for (User user : page.getContent()) {\n        responseObserver.onNext(toResponse(user));\n    }\n\n    responseObserver.onCompleted();  // завершаем поток\n}' },
        { type: 'heading', value: 'Client Streaming' },
        { type: 'code', language: 'java', value: '// Client Streaming — ImportUsers\n@Override\npublic StreamObserver<CreateUserRequest> importUsers(\n        StreamObserver<ImportResult> responseObserver) {\n\n    return new StreamObserver<CreateUserRequest>() {\n        int imported = 0;\n        int failed = 0;\n\n        @Override\n        public void onNext(CreateUserRequest request) {\n            try {\n                userService.create(new CreateUserDto(\n                    request.getName(), request.getEmail(), request.getAge()));\n                imported++;\n            } catch (Exception e) {\n                failed++;\n                log.warn("Ошибка импорта: {}", e.getMessage());\n            }\n        }\n\n        @Override\n        public void onError(Throwable t) {\n            log.error("Ошибка при импорте", t);\n        }\n\n        @Override\n        public void onCompleted() {\n            ImportResult result = ImportResult.newBuilder()\n                .setImportedCount(imported)\n                .setFailedCount(failed)\n                .build();\n            responseObserver.onNext(result);\n            responseObserver.onCompleted();\n        }\n    };\n}' },
        { type: 'code', language: 'yaml', value: '# application.yml\ngrpc:\n  server:\n    port: 9090\n    security:\n      enabled: false  # для разработки' },
        { type: 'note', value: 'StreamObserver — это callback-интерфейс gRPC. onNext() отправляет данные, onCompleted() завершает поток, onError() сигнализирует об ошибке.' }
      ]
    },
    {
      id: 3,
      title: 'gRPC Client в Spring Boot',
      type: 'theory',
      content: [
        { type: 'heading', value: 'Настройка gRPC клиента' },
        { type: 'code', language: 'yaml', value: '# application.yml — клиентское приложение\ngrpc:\n  client:\n    user-service:\n      address: static://localhost:9090\n      negotiationType: plaintext  # без TLS для разработки\n    order-service:\n      address: dns:///order-service:9090\n      negotiationType: tls' },
        { type: 'heading', value: 'Blocking (синхронный) клиент' },
        { type: 'code', language: 'java', value: '@Service\npublic class UserGrpcClient {\n\n    private final UserServiceGrpc.UserServiceBlockingStub blockingStub;\n\n    public UserGrpcClient(\n            @GrpcClient("user-service") \n            UserServiceGrpc.UserServiceBlockingStub blockingStub) {\n        this.blockingStub = blockingStub;\n    }\n\n    // Unary вызов\n    public UserResponse getUser(Long id) {\n        GetUserRequest request = GetUserRequest.newBuilder()\n            .setId(id)\n            .build();\n        return blockingStub.getUser(request);\n    }\n\n    // Создание пользователя\n    public UserResponse createUser(String name, String email, int age) {\n        CreateUserRequest request = CreateUserRequest.newBuilder()\n            .setName(name)\n            .setEmail(email)\n            .setAge(age)\n            .build();\n        return blockingStub.createUser(request);\n    }\n\n    // Server Streaming — получение списка\n    public List<UserResponse> listUsers(int page, int size) {\n        ListUsersRequest request = ListUsersRequest.newBuilder()\n            .setPage(page)\n            .setSize(size)\n            .build();\n\n        List<UserResponse> users = new ArrayList<>();\n        Iterator<UserResponse> iterator = blockingStub.listUsers(request);\n        iterator.forEachRemaining(users::add);\n        return users;\n    }\n}' },
        { type: 'heading', value: 'Async (асинхронный) клиент' },
        { type: 'code', language: 'java', value: '@Service\npublic class UserGrpcAsyncClient {\n\n    private final UserServiceGrpc.UserServiceStub asyncStub;\n\n    public UserGrpcAsyncClient(\n            @GrpcClient("user-service")\n            UserServiceGrpc.UserServiceStub asyncStub) {\n        this.asyncStub = asyncStub;\n    }\n\n    // Асинхронный вызов\n    public CompletableFuture<UserResponse> getUserAsync(Long id) {\n        CompletableFuture<UserResponse> future = new CompletableFuture<>();\n\n        asyncStub.getUser(\n            GetUserRequest.newBuilder().setId(id).build(),\n            new StreamObserver<UserResponse>() {\n                @Override\n                public void onNext(UserResponse response) {\n                    future.complete(response);\n                }\n\n                @Override\n                public void onError(Throwable t) {\n                    future.completeExceptionally(t);\n                }\n\n                @Override\n                public void onCompleted() { }\n            });\n\n        return future;\n    }\n\n    // Client Streaming — массовый импорт\n    public CompletableFuture<ImportResult> importUsers(List<CreateUserRequest> users) {\n        CompletableFuture<ImportResult> future = new CompletableFuture<>();\n\n        StreamObserver<CreateUserRequest> requestObserver = asyncStub.importUsers(\n            new StreamObserver<ImportResult>() {\n                @Override\n                public void onNext(ImportResult result) {\n                    future.complete(result);\n                }\n\n                @Override\n                public void onError(Throwable t) {\n                    future.completeExceptionally(t);\n                }\n\n                @Override\n                public void onCompleted() { }\n            });\n\n        // Отправляем данные\n        for (CreateUserRequest user : users) {\n            requestObserver.onNext(user);\n        }\n        requestObserver.onCompleted();\n\n        return future;\n    }\n}' },
        { type: 'heading', value: 'Interceptors — перехватчики' },
        { type: 'code', language: 'java', value: '// Client Interceptor — добавление метаданных (токен)\n@GrpcGlobalClientInterceptor\npublic class AuthClientInterceptor implements ClientInterceptor {\n\n    @Override\n    public <ReqT, RespT> ClientCall<ReqT, RespT> interceptCall(\n            MethodDescriptor<ReqT, RespT> method,\n            CallOptions callOptions,\n            Channel next) {\n        return new ForwardingClientCall.SimpleForwardingClientCall<>(\n                next.newCall(method, callOptions)) {\n            @Override\n            public void start(Listener<RespT> listener, Metadata headers) {\n                headers.put(\n                    Metadata.Key.of("Authorization", Metadata.ASCII_STRING_MARSHALLER),\n                    "Bearer " + getToken());\n                super.start(listener, headers);\n            }\n        };\n    }\n}' },
        { type: 'tip', value: 'Используйте BlockingStub для простых синхронных вызовов, Stub для асинхронных, и FutureStub для вызовов с ListenableFuture.' }
      ]
    },
    {
      id: 4,
      title: 'Streaming в gRPC',
      type: 'theory',
      content: [
        { type: 'heading', value: '4 типа вызовов gRPC' },
        { type: 'list', items: [
          'Unary: один запрос -> один ответ (как REST)',
          'Server Streaming: один запрос -> поток ответов',
          'Client Streaming: поток запросов -> один ответ',
          'Bidirectional Streaming: поток запросов <-> поток ответов'
        ] },
        { type: 'heading', value: 'Bidirectional Streaming — чат' },
        { type: 'code', language: 'protobuf', value: '// chat.proto\nservice ChatService {\n    rpc Chat (stream ChatMessage) returns (stream ChatMessage);\n}\n\nmessage ChatMessage {\n    string user = 1;\n    string room = 2;\n    string text = 3;\n    int64 timestamp = 4;\n}' },
        { type: 'code', language: 'java', value: '// Серверная реализация чата\n@GrpcService\npublic class ChatGrpcService extends ChatServiceGrpc.ChatServiceImplBase {\n\n    // Хранение подключённых клиентов по комнатам\n    private final Map<String, Set<StreamObserver<ChatMessage>>> rooms = \n        new ConcurrentHashMap<>();\n\n    @Override\n    public StreamObserver<ChatMessage> chat(\n            StreamObserver<ChatMessage> responseObserver) {\n\n        return new StreamObserver<ChatMessage>() {\n            private String currentRoom;\n\n            @Override\n            public void onNext(ChatMessage message) {\n                currentRoom = message.getRoom();\n                \n                // Добавляем пользователя в комнату\n                rooms.computeIfAbsent(currentRoom, k -> \n                    ConcurrentHashMap.newKeySet()).add(responseObserver);\n\n                // Рассылаем сообщение всем в комнате\n                ChatMessage broadcastMsg = ChatMessage.newBuilder()\n                    .setUser(message.getUser())\n                    .setRoom(message.getRoom())\n                    .setText(message.getText())\n                    .setTimestamp(System.currentTimeMillis())\n                    .build();\n\n                Set<StreamObserver<ChatMessage>> observers = \n                    rooms.get(currentRoom);\n                if (observers != null) {\n                    for (StreamObserver<ChatMessage> observer : observers) {\n                        try {\n                            observer.onNext(broadcastMsg);\n                        } catch (Exception e) {\n                            observers.remove(observer);\n                        }\n                    }\n                }\n            }\n\n            @Override\n            public void onError(Throwable t) {\n                removeFromRoom();\n                log.error("Ошибка чата: {}", t.getMessage());\n            }\n\n            @Override\n            public void onCompleted() {\n                removeFromRoom();\n                responseObserver.onCompleted();\n            }\n\n            private void removeFromRoom() {\n                if (currentRoom != null) {\n                    Set<StreamObserver<ChatMessage>> observers = \n                        rooms.get(currentRoom);\n                    if (observers != null) {\n                        observers.remove(responseObserver);\n                    }\n                }\n            }\n        };\n    }\n}' },
        { type: 'heading', value: 'Server Streaming — мониторинг метрик' },
        { type: 'code', language: 'java', value: '// Стриминг метрик каждую секунду\n@Override\npublic void streamMetrics(MetricsRequest request,\n                          StreamObserver<MetricData> responseObserver) {\n    ScheduledExecutorService scheduler = Executors.newSingleThreadScheduledExecutor();\n\n    scheduler.scheduleAtFixedRate(() -> {\n        try {\n            MetricData data = MetricData.newBuilder()\n                .setCpuUsage(getCpuUsage())\n                .setMemoryUsage(getMemoryUsage())\n                .setActiveConnections(getActiveConnections())\n                .setTimestamp(System.currentTimeMillis())\n                .build();\n\n            responseObserver.onNext(data);\n        } catch (Exception e) {\n            responseObserver.onError(e);\n            scheduler.shutdown();\n        }\n    }, 0, 1, TimeUnit.SECONDS);\n\n    // Остановка при отключении клиента\n    Context.current().addListener(context -> {\n        scheduler.shutdown();\n        log.info("Клиент отключился от метрик");\n    }, Runnable::run);\n}' },
        { type: 'note', value: 'HTTP/2 позволяет gRPC мультиплексировать множество потоков по одному TCP соединению. Это делает streaming эффективным и не блокирует другие вызовы.' }
      ]
    },
    {
      id: 5,
      title: 'Обработка ошибок в gRPC',
      type: 'theory',
      content: [
        { type: 'heading', value: 'gRPC Status Codes' },
        { type: 'text', value: 'gRPC использует собственные статус-коды (не HTTP): OK, NOT_FOUND, INVALID_ARGUMENT, PERMISSION_DENIED, INTERNAL, UNAVAILABLE и другие.' },
        { type: 'code', language: 'java', value: '// Серверная обработка ошибок\n@GrpcService\npublic class UserGrpcService extends UserServiceGrpc.UserServiceImplBase {\n\n    @Override\n    public void getUser(GetUserRequest request,\n                        StreamObserver<UserResponse> responseObserver) {\n        try {\n            // NOT_FOUND\n            User user = userService.findById(request.getId())\n                .orElseThrow(() -> Status.NOT_FOUND\n                    .withDescription("Пользователь " + request.getId() + " не найден")\n                    .asRuntimeException());\n\n            responseObserver.onNext(toResponse(user));\n            responseObserver.onCompleted();\n        } catch (StatusRuntimeException e) {\n            responseObserver.onError(e);  // пробрасываем gRPC ошибку\n        } catch (Exception e) {\n            responseObserver.onError(\n                Status.INTERNAL\n                    .withDescription("Внутренняя ошибка: " + e.getMessage())\n                    .withCause(e)\n                    .asRuntimeException());\n        }\n    }\n\n    @Override\n    public void createUser(CreateUserRequest request,\n                           StreamObserver<UserResponse> responseObserver) {\n        // INVALID_ARGUMENT\n        if (request.getName().isBlank()) {\n            responseObserver.onError(\n                Status.INVALID_ARGUMENT\n                    .withDescription("Имя не может быть пустым")\n                    .asRuntimeException());\n            return;\n        }\n\n        // ALREADY_EXISTS\n        if (userService.existsByEmail(request.getEmail())) {\n            responseObserver.onError(\n                Status.ALREADY_EXISTS\n                    .withDescription("Email уже зарегистрирован")\n                    .asRuntimeException());\n            return;\n        }\n\n        User user = userService.create(toDto(request));\n        responseObserver.onNext(toResponse(user));\n        responseObserver.onCompleted();\n    }\n}' },
        { type: 'heading', value: 'Расширенные ошибки с метаданными' },
        { type: 'code', language: 'java', value: '// Ошибка с дополнительными деталями\nimport com.google.rpc.ErrorInfo;\nimport io.grpc.protobuf.StatusProto;\n\npublic StatusRuntimeException validationError(String field, String message) {\n    com.google.rpc.Status status = com.google.rpc.Status.newBuilder()\n        .setCode(Code.INVALID_ARGUMENT.getNumber())\n        .setMessage("Ошибка валидации")\n        .addDetails(Any.pack(\n            ErrorInfo.newBuilder()\n                .setReason("VALIDATION_ERROR")\n                .setDomain("user-service")\n                .putMetadata("field", field)\n                .putMetadata("message", message)\n                .build()))\n        .build();\n\n    return StatusProto.toStatusRuntimeException(status);\n}' },
        { type: 'heading', value: 'Global Exception Handler (Interceptor)' },
        { type: 'code', language: 'java', value: '@GrpcGlobalServerInterceptor\npublic class ErrorHandlingInterceptor implements ServerInterceptor {\n\n    @Override\n    public <ReqT, RespT> ServerCall.Listener<ReqT> interceptCall(\n            ServerCall<ReqT, RespT> call,\n            Metadata headers,\n            ServerCallHandler<ReqT, RespT> next) {\n\n        ServerCall.Listener<ReqT> listener = next.startCall(call, headers);\n\n        return new ForwardingServerCallListener.SimpleForwardingServerCallListener<>(listener) {\n            @Override\n            public void onHalfClose() {\n                try {\n                    super.onHalfClose();\n                } catch (ValidationException e) {\n                    call.close(Status.INVALID_ARGUMENT\n                        .withDescription(e.getMessage()), new Metadata());\n                } catch (NotFoundException e) {\n                    call.close(Status.NOT_FOUND\n                        .withDescription(e.getMessage()), new Metadata());\n                } catch (AccessDeniedException e) {\n                    call.close(Status.PERMISSION_DENIED\n                        .withDescription(e.getMessage()), new Metadata());\n                } catch (Exception e) {\n                    log.error("gRPC Error", e);\n                    call.close(Status.INTERNAL\n                        .withDescription("Внутренняя ошибка сервера"), \n                        new Metadata());\n                }\n            }\n        };\n    }\n}' },
        { type: 'heading', value: 'Обработка ошибок на клиенте' },
        { type: 'code', language: 'java', value: '// Клиентская обработка ошибок\npublic UserResponse getUser(Long id) {\n    try {\n        return blockingStub.getUser(\n            GetUserRequest.newBuilder().setId(id).build());\n    } catch (StatusRuntimeException e) {\n        switch (e.getStatus().getCode()) {\n            case NOT_FOUND:\n                throw new UserNotFoundException(id);\n            case UNAVAILABLE:\n                throw new ServiceUnavailableException("User service недоступен");\n            case DEADLINE_EXCEEDED:\n                throw new TimeoutException("Таймаут запроса к User service");\n            default:\n                throw new ExternalServiceException(\n                    "gRPC ошибка: " + e.getStatus().getDescription());\n        }\n    }\n}\n\n// Установка deadline (timeout)\nUserResponse response = blockingStub\n    .withDeadlineAfter(5, TimeUnit.SECONDS)\n    .getUser(request);' },
        { type: 'warning', value: 'Всегда устанавливайте deadline для gRPC вызовов. Без deadline вызов может висеть бесконечно при проблемах с сетью.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: gRPC сервис для продуктов',
      type: 'practice',
      difficulty: 'hard',
      description: 'Создайте gRPC сервис для каталога продуктов с CRUD операциями и server streaming.',
      requirements: [
        'Определить product.proto: ProductService с GetProduct, CreateProduct, ListProducts (stream), SearchProducts',
        'Реализовать gRPC Server с @GrpcService',
        'GetProduct возвращает NOT_FOUND если продукт не существует',
        'CreateProduct валидирует данные (INVALID_ARGUMENT)',
        'ListProducts — server streaming всех продуктов',
        'SearchProducts — фильтрация по имени и категории',
        'gRPC Client для вызова из другого сервиса',
        'Написать интеграционные тесты с grpc-testing'
      ],
      hint: 'Используйте GrpcCleanupRule и InProcessServerBuilder для тестов — сервер создаётся в памяти, без реального порта.',
      expectedOutput: 'gRPC Server запущен на порту 9090.\n\ngrpcurl -plaintext localhost:9090 ProductService/GetProduct {"id": 1}:\n{"id":"1","name":"Ноутбук HP","price":45000,"category":"Электроника"}\n\ngrpcurl GetProduct {"id": 999}:\nERROR: NOT_FOUND: Продукт 999 не найден\n\ngrpcurl ListProducts {}:\n{"id":"1","name":"Ноутбук HP"}\n{"id":"2","name":"Телефон Samsung"}\n{"id":"3","name":"Наушники Sony"}\n(stream завершён)\n\ngrpcurl CreateProduct {"name":"","price":-1}:\nERROR: INVALID_ARGUMENT: Название не может быть пустым',
      solution: '// product.proto\nsyntax = "proto3";\nservice ProductService {\n    rpc GetProduct(GetProductRequest) returns (ProductResponse);\n    rpc CreateProduct(CreateProductRequest) returns (ProductResponse);\n    rpc ListProducts(Empty) returns (stream ProductResponse);\n}\n\n// Server\n@GrpcService\npublic class ProductGrpcService extends ProductServiceGrpc.ProductServiceImplBase {\n    private final ProductRepository repo;\n\n    @Override\n    public void getProduct(GetProductRequest req, StreamObserver<ProductResponse> observer) {\n        Product product = repo.findById(req.getId())\n            .orElseThrow(() -> Status.NOT_FOUND\n                .withDescription("Продукт " + req.getId() + " не найден")\n                .asRuntimeException());\n        observer.onNext(toProto(product));\n        observer.onCompleted();\n    }\n\n    @Override\n    public void createProduct(CreateProductRequest req, StreamObserver<ProductResponse> observer) {\n        if (req.getName().isBlank()) {\n            observer.onError(Status.INVALID_ARGUMENT\n                .withDescription("Название не может быть пустым").asRuntimeException());\n            return;\n        }\n        Product product = repo.save(fromProto(req));\n        observer.onNext(toProto(product));\n        observer.onCompleted();\n    }\n\n    @Override\n    public void listProducts(Empty req, StreamObserver<ProductResponse> observer) {\n        repo.findAll().forEach(p -> observer.onNext(toProto(p)));\n        observer.onCompleted();\n    }\n}\n\n// Тест\n@SpringBootTest(properties = "grpc.server.inProcessName=test")\nclass ProductGrpcServiceTest {\n    @GrpcClient("inProcess")\n    ProductServiceBlockingStub stub;\n\n    @Test\n    void getProduct_notFound() {\n        StatusRuntimeException ex = assertThrows(StatusRuntimeException.class,\n            () -> stub.getProduct(GetProductRequest.newBuilder().setId(999).build()));\n        assertEquals(Status.NOT_FOUND.getCode(), ex.getStatus().getCode());\n    }\n}',
      explanation: 'gRPC использует protobuf для строгой типизации контракта. StreamObserver — ключевой API для отправки ответов. Status.NOT_FOUND аналогичен HTTP 404. Server Streaming позволяет отправлять большие наборы данных без загрузки всего в память.'
    }
  ]
}
