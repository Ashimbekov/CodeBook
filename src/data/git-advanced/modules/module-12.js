export default {
  id: 12,
  title: 'Практикум: Реальные сценарии',
  description: 'Практические задачи на Java: симуляция реальных Git-сценариев — разрешение конфликтов, rebase workflow, release management, hotfix, gitflow и другие.',
  lessons: [
    {
      id: 1,
      title: 'Resolve Merge Conflict',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте систему разрешения merge-конфликтов: парсинг конфликтных маркеров, выбор стратегии (ours/theirs/manual) и применение решения.',
      requirements: [
        'Класс ConflictBlock с полями oursContent, theirsContent, lineNumber',
        'Метод parseConflicts(fileContent) — находит и парсит конфликтные блоки',
        'Метод resolve(strategy) — разрешает конфликт (OURS, THEIRS, BOTH)',
        'Метод resolveAll(conflicts, strategy) — массовое разрешение',
        'Продемонстрировать парсинг файла с конфликтами и разрешение разными стратегиями'
      ],
      expectedOutput: 'File with conflicts:\n  public class Config {\n  <<<<<<< HEAD\n      private int timeout = 5000;\n  =======\n      private int timeout = 3000;\n  >>>>>>> feature/optimization\n  <<<<<<< HEAD\n      private String url = "https://api.prod.com";\n  =======\n      private String url = "https://api.staging.com";\n  >>>>>>> feature/optimization\n  }\n\nFound 2 conflicts\n\nResolving with OURS strategy:\n  Conflict 1: kept "    private int timeout = 5000;"\n  Conflict 2: kept "    private String url = \\"https://api.prod.com\\";"\n\nResolved file:\n  public class Config {\n      private int timeout = 5000;\n      private String url = "https://api.prod.com";\n  }',
      hint: 'Парсите файл построчно. Когда встречаете <<<<<<< — начало конфликта (ours). ======= — разделитель (начало theirs). >>>>>>> — конец конфликта. OURS берёт верхнюю часть, THEIRS — нижнюю.',
      solution: 'import java.util.*;\n\npublic class Main {\n\n    static class ConflictBlock {\n        String oursContent;\n        String theirsContent;\n        int lineNumber;\n\n        ConflictBlock(String oursContent, String theirsContent, int lineNumber) {\n            this.oursContent = oursContent;\n            this.theirsContent = theirsContent;\n            this.lineNumber = lineNumber;\n        }\n    }\n\n    static List<ConflictBlock> parseConflicts(String fileContent) {\n        List<ConflictBlock> conflicts = new ArrayList<>();\n        String[] lines = fileContent.split("\\n");\n        int i = 0;\n        while (i < lines.length) {\n            if (lines[i].startsWith("<<<<<<<")) {\n                int startLine = i;\n                StringBuilder ours = new StringBuilder();\n                i++;\n                while (i < lines.length && !lines[i].startsWith("=======")) {\n                    ours.append(lines[i]);\n                    i++;\n                }\n                i++; // skip =======\n                StringBuilder theirs = new StringBuilder();\n                while (i < lines.length && !lines[i].startsWith(">>>>>>>")) {\n                    theirs.append(lines[i]);\n                    i++;\n                }\n                conflicts.add(new ConflictBlock(ours.toString(), theirs.toString(), startLine));\n            }\n            i++;\n        }\n        return conflicts;\n    }\n\n    static String resolveFile(String fileContent, String strategy) {\n        String[] lines = fileContent.split("\\n");\n        StringBuilder result = new StringBuilder();\n        int i = 0;\n        int conflictNum = 0;\n\n        while (i < lines.length) {\n            if (lines[i].startsWith("<<<<<<<")) {\n                conflictNum++;\n                StringBuilder ours = new StringBuilder();\n                i++;\n                while (!lines[i].startsWith("=======")) {\n                    ours.append(lines[i]);\n                    i++;\n                }\n                i++; // skip =======\n                StringBuilder theirs = new StringBuilder();\n                while (!lines[i].startsWith(">>>>>>>")) {\n                    theirs.append(lines[i]);\n                    i++;\n                }\n                i++; // skip >>>>>>>\n\n                String resolved;\n                switch (strategy) {\n                    case "THEIRS": resolved = theirs.toString(); break;\n                    case "BOTH": resolved = ours.toString() + "\\n" + theirs.toString(); break;\n                    default: resolved = ours.toString(); break;\n                }\n                System.out.println("  Conflict " + conflictNum + ": kept \\"" + resolved.trim() + "\\"");\n                result.append(resolved).append("\\n");\n            } else {\n                result.append(lines[i]).append("\\n");\n                i++;\n            }\n        }\n        return result.toString().trim();\n    }\n\n    public static void main(String[] args) {\n        String fileWithConflicts =\n            "public class Config {\\n" +\n            "<<<<<<< HEAD\\n" +\n            "    private int timeout = 5000;\\n" +\n            "=======\\n" +\n            "    private int timeout = 3000;\\n" +\n            ">>>>>>> feature/optimization\\n" +\n            "<<<<<<< HEAD\\n" +\n            "    private String url = \\"https://api.prod.com\\";\\n" +\n            "=======\\n" +\n            "    private String url = \\"https://api.staging.com\\";\\n" +\n            ">>>>>>> feature/optimization\\n" +\n            "}";\n\n        System.out.println("File with conflicts:");\n        for (String line : fileWithConflicts.split("\\n")) {\n            System.out.println("  " + line);\n        }\n\n        List<ConflictBlock> conflicts = parseConflicts(fileWithConflicts);\n        System.out.println("\\nFound " + conflicts.size() + " conflicts");\n\n        System.out.println("\\nResolving with OURS strategy:");\n        String resolved = resolveFile(fileWithConflicts, "OURS");\n\n        System.out.println("\\nResolved file:");\n        for (String line : resolved.split("\\n")) {\n            System.out.println("  " + line);\n        }\n    }\n}',
      explanation: 'Git вставляет конфликтные маркеры (<<<<<<<, =======, >>>>>>>) когда не может автоматически слить изменения. Стратегия OURS берёт версию текущей ветки, THEIRS — входящей ветки. В реальных проектах чаще требуется ручное разрешение — комбинация обеих версий. IDE (IntelliJ, VS Code) предоставляют визуальные инструменты для разрешения конфликтов.'
    },
    {
      id: 2,
      title: 'Rebase Workflow',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте полный rebase workflow: перенос коммитов ветки на новую базу с обнаружением конфликтов и continue/abort.',
      requirements: [
        'Класс RebaseWorkflow с состояниями IN_PROGRESS, CONFLICT, COMPLETED, ABORTED',
        'Метод startRebase(featureBranch, targetBranch) — начинает rebase',
        'Поэтапное применение коммитов с проверкой конфликтов',
        'Методы continueRebase() и abortRebase()',
        'Продемонстрировать успешный rebase и rebase с конфликтом'
      ],
      expectedOutput: 'Main branch: [m1, m2, m3]\nFeature branch: [m1, m2, f1, f2, f3]\n\n=== Rebase feature onto main ===\nRebasing f1 "feat: add search" onto m3... OK\nRebasing f2 "feat: add filters" onto m3... OK\nRebasing f3 "feat: add sorting" onto m3... OK\nRebase completed!\n\nFeature branch after rebase: [m1, m2, m3, f1\', f2\', f3\']\n\n=== Rebase with conflict ===\nRebasing c1 "update config" onto m3... CONFLICT!\nRebase paused. Resolve conflict and run continue.\n\nResolving conflict...\nContinuing rebase...\nRebasing c2 "update readme" onto m3... OK\nRebase completed!',
      hint: 'Rebase берёт коммиты feature-ветки (от точки ответвления) и применяет их один за одним поверх target-ветки. Если коммит конфликтует — rebase останавливается. После разрешения — continue.',
      solution: 'import java.util.*;\n\npublic class Main {\n\n    static class Commit {\n        String hash;\n        String message;\n        boolean causesConflict;\n\n        Commit(String hash, String message) {\n            this(hash, message, false);\n        }\n\n        Commit(String hash, String message, boolean causesConflict) {\n            this.hash = hash;\n            this.message = message;\n            this.causesConflict = causesConflict;\n        }\n\n        @Override\n        public String toString() {\n            return hash;\n        }\n    }\n\n    enum RebaseState { IDLE, IN_PROGRESS, CONFLICT, COMPLETED, ABORTED }\n\n    static class RebaseWorkflow {\n        private RebaseState state = RebaseState.IDLE;\n        private List<Commit> baseCommits;\n        private List<Commit> toRebase;\n        private List<Commit> result;\n        private int currentIndex;\n\n        void startRebase(List<Commit> featureCommits, List<Commit> mainCommits, int divergePoint) {\n            state = RebaseState.IN_PROGRESS;\n            baseCommits = new ArrayList<>(mainCommits);\n            toRebase = new ArrayList<>(featureCommits.subList(divergePoint, featureCommits.size()));\n            result = new ArrayList<>(mainCommits);\n            currentIndex = 0;\n            applyNext();\n        }\n\n        private void applyNext() {\n            while (currentIndex < toRebase.size()) {\n                Commit c = toRebase.get(currentIndex);\n                System.out.print("Rebasing " + c.hash + " \\"" + c.message + "\\" onto " +\n                    baseCommits.get(baseCommits.size() - 1).hash + "... ");\n\n                if (c.causesConflict) {\n                    System.out.println("CONFLICT!");\n                    System.out.println("Rebase paused. Resolve conflict and run continue.");\n                    state = RebaseState.CONFLICT;\n                    return;\n                }\n\n                System.out.println("OK");\n                result.add(new Commit(c.hash + "\\'", c.message));\n                currentIndex++;\n            }\n            state = RebaseState.COMPLETED;\n            System.out.println("Rebase completed!");\n        }\n\n        void continueRebase() {\n            if (state != RebaseState.CONFLICT) {\n                System.out.println("ERROR: No rebase in progress");\n                return;\n            }\n            System.out.println("Continuing rebase...");\n            Commit c = toRebase.get(currentIndex);\n            result.add(new Commit(c.hash + "\\'", c.message));\n            currentIndex++;\n            state = RebaseState.IN_PROGRESS;\n            applyNext();\n        }\n\n        void abortRebase() {\n            System.out.println("Aborting rebase. Restoring original state.");\n            state = RebaseState.ABORTED;\n            result = null;\n        }\n\n        List<Commit> getResult() {\n            return result;\n        }\n    }\n\n    public static void main(String[] args) {\n        // Main branch\n        Commit m1 = new Commit("m1", "initial");\n        Commit m2 = new Commit("m2", "add models");\n        Commit m3 = new Commit("m3", "add services");\n        List<Commit> mainBranch = List.of(m1, m2, m3);\n\n        // Feature branch (diverged after m2)\n        Commit f1 = new Commit("f1", "feat: add search");\n        Commit f2 = new Commit("f2", "feat: add filters");\n        Commit f3 = new Commit("f3", "feat: add sorting");\n        List<Commit> featureBranch = new ArrayList<>(List.of(m1, m2, f1, f2, f3));\n\n        System.out.println("Main branch: " + mainBranch);\n        System.out.println("Feature branch: " + featureBranch);\n\n        // Successful rebase\n        System.out.println("\\n=== Rebase feature onto main ===");\n        RebaseWorkflow rebase = new RebaseWorkflow();\n        rebase.startRebase(featureBranch, mainBranch, 2);\n\n        System.out.println("\\nFeature branch after rebase: " + rebase.getResult());\n\n        // Rebase with conflict\n        System.out.println("\\n=== Rebase with conflict ===");\n        Commit c1 = new Commit("c1", "update config", true);\n        Commit c2 = new Commit("c2", "update readme");\n        List<Commit> conflictBranch = new ArrayList<>(List.of(m1, m2, c1, c2));\n\n        RebaseWorkflow rebase2 = new RebaseWorkflow();\n        rebase2.startRebase(conflictBranch, mainBranch, 2);\n\n        System.out.println("\\nResolving conflict...");\n        rebase2.continueRebase();\n    }\n}',
      explanation: 'Rebase переносит коммиты ветки на новую базу. В отличие от merge, который создаёт merge-commit, rebase создаёт линейную историю. Процесс: 1) найти точку расхождения, 2) применить каждый коммит поверх target-ветки. Если возникает конфликт — rebase останавливается, разработчик разрешает конфликт, затем git rebase --continue. Можно отменить через git rebase --abort.'
    },
    {
      id: 3,
      title: 'Cherry-pick Backport',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте backport hotfix-ов: cherry-pick конкретных коммитов из main в несколько release-веток с отслеживанием статуса.',
      requirements: [
        'Класс ReleaseBranch с версией и списком коммитов',
        'Класс BackportManager с методом backport(commit, targetBranches)',
        'Отслеживание статуса: SUCCESS, CONFLICT, SKIPPED (уже применён)',
        'Метод getBackportStatus() — отчёт по всем backport-ам',
        'Продемонстрировать backport hotfix-а в несколько release-веток'
      ],
      expectedOutput: 'Main branch commits:\n  h1: fix(security): patch XSS vulnerability\n  h2: fix(payment): prevent double charge\n\nRelease branches:\n  release/2.1 (3 commits)\n  release/2.0 (5 commits)\n  release/1.9 (8 commits)\n\n=== Backporting h1 to all releases ===\n  release/2.1: SUCCESS (applied fix(security): patch XSS vulnerability)\n  release/2.0: SUCCESS (applied fix(security): patch XSS vulnerability)\n  release/1.9: CONFLICT (manual resolution needed)\n\n=== Backporting h2 to release/2.1 ===\n  release/2.1: SUCCESS (applied fix(payment): prevent double charge)\n\nBackport status report:\n  h1 → release/2.1: SUCCESS\n  h1 → release/2.0: SUCCESS\n  h1 → release/1.9: CONFLICT\n  h2 → release/2.1: SUCCESS',
      hint: 'Каждый backport — это cherry-pick. Создайте Map<String, Map<String, String>> для отслеживания статуса: commitHash → (branchName → status). Конфликт можно симулировать на основе количества коммитов разницы.',
      solution: 'import java.util.*;\n\npublic class Main {\n\n    static class Commit {\n        String hash;\n        String message;\n\n        Commit(String hash, String message) {\n            this.hash = hash;\n            this.message = message;\n        }\n    }\n\n    static class ReleaseBranch {\n        String name;\n        int commitCount;\n        List<String> appliedHotfixes = new ArrayList<>();\n\n        ReleaseBranch(String name, int commitCount) {\n            this.name = name;\n            this.commitCount = commitCount;\n        }\n    }\n\n    static class BackportManager {\n        private final Map<String, Map<String, String>> status = new LinkedHashMap<>();\n\n        String backport(Commit commit, ReleaseBranch branch) {\n            status.computeIfAbsent(commit.hash, k -> new LinkedHashMap<>());\n\n            // Already applied?\n            if (branch.appliedHotfixes.contains(commit.hash)) {\n                String result = "SKIPPED";\n                status.get(commit.hash).put(branch.name, result);\n                System.out.println("  " + branch.name + ": SKIPPED (already applied)");\n                return result;\n            }\n\n            // Simulate: older branches with more divergence may conflict\n            boolean hasConflict = branch.commitCount > 7;\n\n            if (hasConflict) {\n                String result = "CONFLICT";\n                status.get(commit.hash).put(branch.name, result);\n                System.out.println("  " + branch.name + ": CONFLICT (manual resolution needed)");\n                return result;\n            }\n\n            branch.appliedHotfixes.add(commit.hash);\n            String result = "SUCCESS";\n            status.get(commit.hash).put(branch.name, result);\n            System.out.println("  " + branch.name + ": SUCCESS (applied " + commit.message + ")");\n            return result;\n        }\n\n        void backportToAll(Commit commit, List<ReleaseBranch> branches) {\n            System.out.println("\\n=== Backporting " + commit.hash + " to all releases ===");\n            for (ReleaseBranch branch : branches) {\n                backport(commit, branch);\n            }\n        }\n\n        void backportTo(Commit commit, ReleaseBranch branch) {\n            System.out.println("\\n=== Backporting " + commit.hash + " to " + branch.name + " ===");\n            backport(commit, branch);\n        }\n\n        void printReport() {\n            System.out.println("\\nBackport status report:");\n            for (var commitEntry : status.entrySet()) {\n                for (var branchEntry : commitEntry.getValue().entrySet()) {\n                    System.out.println("  " + commitEntry.getKey() + " → " +\n                        branchEntry.getKey() + ": " + branchEntry.getValue());\n                }\n            }\n        }\n    }\n\n    public static void main(String[] args) {\n        // Hotfix commits on main\n        Commit h1 = new Commit("h1", "fix(security): patch XSS vulnerability");\n        Commit h2 = new Commit("h2", "fix(payment): prevent double charge");\n\n        System.out.println("Main branch commits:");\n        System.out.println("  h1: " + h1.message);\n        System.out.println("  h2: " + h2.message);\n\n        // Release branches\n        ReleaseBranch r21 = new ReleaseBranch("release/2.1", 3);\n        ReleaseBranch r20 = new ReleaseBranch("release/2.0", 5);\n        ReleaseBranch r19 = new ReleaseBranch("release/1.9", 8);\n        List<ReleaseBranch> releases = List.of(r21, r20, r19);\n\n        System.out.println("\\nRelease branches:");\n        for (ReleaseBranch r : releases) {\n            System.out.println("  " + r.name + " (" + r.commitCount + " commits)");\n        }\n\n        // Backport h1 to all\n        BackportManager manager = new BackportManager();\n        manager.backportToAll(h1, releases);\n\n        // Backport h2 only to 2.1\n        manager.backportTo(h2, r21);\n\n        // Report\n        manager.printReport();\n    }\n}',
      explanation: 'Backport — перенос hotfix-ов из main в поддерживаемые release-ветки через cherry-pick. Чем старше release-ветка, тем больше вероятность конфликтов из-за накопленных различий. В крупных проектах (Linux kernel, Chrome) backport — рутинная задача: критические фиксы применяются ко всем поддерживаемым версиям.'
    },
    {
      id: 4,
      title: 'Release Branch Manager',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте менеджер release-веток: создание release из main, feature freeze, добавление только bugfix-ов, финализация и создание тега.',
      requirements: [
        'Класс ReleasePipeline с состояниями DEVELOPMENT, FEATURE_FREEZE, RC, RELEASED',
        'Метод createRelease(version) — создаёт release-ветку от main',
        'Метод addCommit(type, message) — добавляет коммит (блокирует feat в freeze)',
        'Метод promoteToRC() и release() — продвижение по стадиям',
        'Метод getChangelog() — генерирует CHANGELOG из коммитов'
      ],
      expectedOutput: 'Creating release/2.4.0 from main...\nStatus: DEVELOPMENT\n\nAdding feat: add search → OK\nAdding fix: null pointer in user → OK\n\nFeature freeze!\nStatus: FEATURE_FREEZE\n\nAdding feat: add filters → BLOCKED (feature freeze)\nAdding fix: search pagination → OK\n\nPromoting to RC...\nStatus: RC (release/2.4.0-rc.1)\n\nAdding fix: typo in error message → OK\n\nReleasing v2.4.0...\nStatus: RELEASED\nTag: v2.4.0\n\nChangelog v2.4.0:\n  ## Features\n  - add search\n  ## Bug Fixes\n  - null pointer in user\n  - search pagination\n  - typo in error message',
      hint: 'Используйте enum для состояний и switch для контроля, какие типы коммитов разрешены в каждом состоянии: DEVELOPMENT — всё, FEATURE_FREEZE — только fix, RC — только fix.',
      solution: 'import java.util.*;\n\npublic class Main {\n\n    enum ReleaseState { DEVELOPMENT, FEATURE_FREEZE, RC, RELEASED }\n\n    static class ReleaseCommit {\n        String type;\n        String message;\n\n        ReleaseCommit(String type, String message) {\n            this.type = type;\n            this.message = message;\n        }\n    }\n\n    static class ReleasePipeline {\n        String version;\n        ReleaseState state;\n        List<ReleaseCommit> commits = new ArrayList<>();\n        int rcNumber = 0;\n\n        void createRelease(String version) {\n            this.version = version;\n            this.state = ReleaseState.DEVELOPMENT;\n            System.out.println("Creating release/" + version + " from main...");\n            System.out.println("Status: " + state);\n        }\n\n        boolean addCommit(String type, String message) {\n            System.out.print("Adding " + type + ": " + message + " → ");\n\n            if (state == ReleaseState.RELEASED) {\n                System.out.println("BLOCKED (already released)");\n                return false;\n            }\n\n            if (state != ReleaseState.DEVELOPMENT && type.equals("feat")) {\n                System.out.println("BLOCKED (feature freeze)");\n                return false;\n            }\n\n            commits.add(new ReleaseCommit(type, message));\n            System.out.println("OK");\n            return true;\n        }\n\n        void featureFreeze() {\n            System.out.println("\\nFeature freeze!");\n            state = ReleaseState.FEATURE_FREEZE;\n            System.out.println("Status: " + state);\n        }\n\n        void promoteToRC() {\n            System.out.println("\\nPromoting to RC...");\n            state = ReleaseState.RC;\n            rcNumber++;\n            System.out.println("Status: " + state + " (release/" + version + "-rc." + rcNumber + ")");\n        }\n\n        void release() {\n            System.out.println("\\nReleasing v" + version + "...");\n            state = ReleaseState.RELEASED;\n            System.out.println("Status: " + state);\n            System.out.println("Tag: v" + version);\n        }\n\n        void printChangelog() {\n            System.out.println("\\nChangelog v" + version + ":");\n\n            List<String> features = new ArrayList<>();\n            List<String> fixes = new ArrayList<>();\n\n            for (ReleaseCommit c : commits) {\n                if (c.type.equals("feat")) features.add(c.message);\n                else if (c.type.equals("fix")) fixes.add(c.message);\n            }\n\n            if (!features.isEmpty()) {\n                System.out.println("  ## Features");\n                features.forEach(f -> System.out.println("  - " + f));\n            }\n            if (!fixes.isEmpty()) {\n                System.out.println("  ## Bug Fixes");\n                fixes.forEach(f -> System.out.println("  - " + f));\n            }\n        }\n    }\n\n    public static void main(String[] args) {\n        ReleasePipeline pipeline = new ReleasePipeline();\n\n        pipeline.createRelease("2.4.0");\n        System.out.println();\n\n        pipeline.addCommit("feat", "add search");\n        pipeline.addCommit("fix", "null pointer in user");\n\n        pipeline.featureFreeze();\n        System.out.println();\n\n        pipeline.addCommit("feat", "add filters");\n        pipeline.addCommit("fix", "search pagination");\n\n        pipeline.promoteToRC();\n        System.out.println();\n\n        pipeline.addCommit("fix", "typo in error message");\n\n        pipeline.release();\n        pipeline.printChangelog();\n    }\n}',
      explanation: 'Release management — поэтапный процесс: Development (все изменения) → Feature Freeze (только баг-фиксы) → Release Candidate (финальное тестирование) → Released (тег, деплой). Этот workflow используется в крупных проектах (Spring, IntelliJ) для контроля качества релизов. Conventional commits позволяют автоматически генерировать CHANGELOG.'
    },
    {
      id: 5,
      title: 'Hotfix Workflow',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте полный hotfix workflow: создание hotfix-ветки от production-тега, исправление, merge в main и release-ветки.',
      requirements: [
        'Класс HotfixWorkflow с методами createHotfix(), applyFix(), finalize()',
        'Создание ветки от тега (production version)',
        'Merge hotfix обратно в main И в текущую release-ветку',
        'Автоматическое создание patch-тега (v1.2.3 → v1.2.4)',
        'Продемонстрировать полный цикл hotfix'
      ],
      expectedOutput: 'Current state:\n  Production: v2.3.1\n  Main branch: [m1, m2, m3, m4, m5]\n  Release/2.4: [m1, m2, m3, r1, r2]\n\n=== Starting hotfix ===\nCreated branch: hotfix/2.3.2 from tag v2.3.1\n\nApplying fix: fix(auth): patch token expiry bypass\nCommit added to hotfix/2.3.2\n\n=== Finalizing hotfix ===\nMerging hotfix/2.3.2 → main... OK\nMerging hotfix/2.3.2 → release/2.4... OK\nCreated tag: v2.3.2\nDeleted branch: hotfix/2.3.2\n\nFinal state:\n  Production: v2.3.2\n  Main branch: [m1, m2, m3, m4, m5, hotfix-merge]\n  Release/2.4: [m1, m2, m3, r1, r2, hotfix-merge]',
      hint: 'Hotfix workflow (Gitflow): 1) branch от тега, 2) fix, 3) merge в main, 4) merge в release (если есть), 5) тег, 6) удалить ветку.',
      solution: 'import java.util.*;\n\npublic class Main {\n\n    static class Branch {\n        String name;\n        List<String> commits;\n\n        Branch(String name, String... commits) {\n            this.name = name;\n            this.commits = new ArrayList<>(Arrays.asList(commits));\n        }\n    }\n\n    static class HotfixWorkflow {\n        String productionTag;\n        Branch mainBranch;\n        Branch releaseBranch;\n        Branch hotfixBranch;\n        String fixMessage;\n\n        HotfixWorkflow(String productionTag, Branch mainBranch, Branch releaseBranch) {\n            this.productionTag = productionTag;\n            this.mainBranch = mainBranch;\n            this.releaseBranch = releaseBranch;\n        }\n\n        void createHotfix() {\n            String[] parts = productionTag.substring(1).split("\\\\.");\n            int patch = Integer.parseInt(parts[2]) + 1;\n            String hotfixVersion = parts[0] + "." + parts[1] + "." + patch;\n\n            hotfixBranch = new Branch("hotfix/" + hotfixVersion);\n            System.out.println("\\n=== Starting hotfix ===");\n            System.out.println("Created branch: " + hotfixBranch.name + " from tag " + productionTag);\n        }\n\n        void applyFix(String message) {\n            this.fixMessage = message;\n            System.out.println("\\nApplying fix: " + message);\n            hotfixBranch.commits.add("hotfix-fix");\n            System.out.println("Commit added to " + hotfixBranch.name);\n        }\n\n        void finalize() {\n            System.out.println("\\n=== Finalizing hotfix ===");\n\n            // Merge to main\n            mainBranch.commits.add("hotfix-merge");\n            System.out.println("Merging " + hotfixBranch.name + " → main... OK");\n\n            // Merge to release if exists\n            if (releaseBranch != null) {\n                releaseBranch.commits.add("hotfix-merge");\n                System.out.println("Merging " + hotfixBranch.name + " → " + releaseBranch.name + "... OK");\n            }\n\n            // Create new tag\n            String newTag = hotfixBranch.name.replace("hotfix/", "v");\n            productionTag = newTag;\n            System.out.println("Created tag: " + newTag);\n\n            // Delete hotfix branch\n            System.out.println("Deleted branch: " + hotfixBranch.name);\n            hotfixBranch = null;\n        }\n\n        void printState(String label) {\n            System.out.println(label);\n            System.out.println("  Production: " + productionTag);\n            System.out.println("  Main branch: " + mainBranch.commits);\n            if (releaseBranch != null) {\n                System.out.println("  " + capitalize(releaseBranch.name) + ": " + releaseBranch.commits);\n            }\n        }\n\n        private String capitalize(String s) {\n            return s.substring(0, 1).toUpperCase() + s.substring(1);\n        }\n    }\n\n    public static void main(String[] args) {\n        Branch main = new Branch("main", "m1", "m2", "m3", "m4", "m5");\n        Branch release = new Branch("release/2.4", "m1", "m2", "m3", "r1", "r2");\n\n        HotfixWorkflow hotfix = new HotfixWorkflow("v2.3.1", main, release);\n\n        hotfix.printState("Current state:");\n        hotfix.createHotfix();\n        hotfix.applyFix("fix(auth): patch token expiry bypass");\n        hotfix.finalize();\n\n        System.out.println();\n        hotfix.printState("Final state:");\n    }\n}',
      explanation: 'Hotfix workflow (часть Gitflow): критический баг в production → создаём ветку от тега → фиксим → merge обратно в main и release-ветку → новый тег → удаляем ветку. Важно: hotfix мержится в ОБЕ ветки (main и release), чтобы фикс не потерялся. Автоматическое создание patch-версии (v2.3.1 → v2.3.2) следует SemVer.'
    },
    {
      id: 6,
      title: 'Gitflow Simulator',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте полную симуляцию Gitflow: main, develop, feature/*, release/*, hotfix/* ветки с правилами merge.',
      requirements: [
        'Класс GitflowSimulator с ветками main, develop',
        'Методы startFeature(), finishFeature() — создание/merge feature-веток',
        'Методы startRelease(), finishRelease() — release workflow',
        'Валидация: feature только от develop, release только от develop, merge feature в develop',
        'Продемонстрировать полный цикл: feature → develop → release → main'
      ],
      expectedOutput: 'Initializing Gitflow...\n  Created: main (initial commit)\n  Created: develop (from main)\n\nStarting feature: user-auth\n  Created: feature/user-auth (from develop)\n  Adding: feat(auth): implement JWT\n  Adding: test(auth): add unit tests\n\nFinishing feature: user-auth\n  Merged feature/user-auth → develop\n  Deleted: feature/user-auth\n\nStarting feature: payment\n  Created: feature/payment (from develop)\n  Adding: feat(payment): add Stripe\n\nFinishing feature: payment\n  Merged feature/payment → develop\n  Deleted: feature/payment\n\nStarting release: 1.0.0\n  Created: release/1.0.0 (from develop)\n  Adding: fix: update version numbers\n\nFinishing release: 1.0.0\n  Merged release/1.0.0 → main\n  Merged release/1.0.0 → develop\n  Tagged: v1.0.0\n  Deleted: release/1.0.0\n\nBranch state:\n  main: [init, release-1.0.0-merge] (tag: v1.0.0)\n  develop: [init, user-auth-merge, payment-merge, release-1.0.0-merge]',
      hint: 'Gitflow правила: feature/* → от develop → merge в develop. release/* → от develop → merge в main И develop. hotfix/* → от main → merge в main И develop. Используйте Map<String, List<String>> для хранения веток.',
      solution: 'import java.util.*;\n\npublic class Main {\n\n    static class GitflowSimulator {\n        private final Map<String, List<String>> branches = new LinkedHashMap<>();\n        private final List<String> tags = new ArrayList<>();\n\n        void init() {\n            System.out.println("Initializing Gitflow...");\n            branches.put("main", new ArrayList<>(List.of("init")));\n            System.out.println("  Created: main (initial commit)");\n            branches.put("develop", new ArrayList<>(List.of("init")));\n            System.out.println("  Created: develop (from main)");\n        }\n\n        void startFeature(String name) {\n            System.out.println("\\nStarting feature: " + name);\n            String branchName = "feature/" + name;\n            branches.put(branchName, new ArrayList<>(branches.get("develop")));\n            System.out.println("  Created: " + branchName + " (from develop)");\n        }\n\n        void addCommit(String branch, String message) {\n            branches.get(branch).add(message);\n            System.out.println("  Adding: " + message);\n        }\n\n        void finishFeature(String name) {\n            System.out.println("\\nFinishing feature: " + name);\n            String branchName = "feature/" + name;\n            branches.get("develop").add(name + "-merge");\n            System.out.println("  Merged " + branchName + " → develop");\n            branches.remove(branchName);\n            System.out.println("  Deleted: " + branchName);\n        }\n\n        void startRelease(String version) {\n            System.out.println("\\nStarting release: " + version);\n            String branchName = "release/" + version;\n            branches.put(branchName, new ArrayList<>(branches.get("develop")));\n            System.out.println("  Created: " + branchName + " (from develop)");\n        }\n\n        void finishRelease(String version) {\n            System.out.println("\\nFinishing release: " + version);\n            String branchName = "release/" + version;\n\n            branches.get("main").add("release-" + version + "-merge");\n            System.out.println("  Merged " + branchName + " → main");\n\n            branches.get("develop").add("release-" + version + "-merge");\n            System.out.println("  Merged " + branchName + " → develop");\n\n            String tag = "v" + version;\n            tags.add(tag);\n            System.out.println("  Tagged: " + tag);\n\n            branches.remove(branchName);\n            System.out.println("  Deleted: " + branchName);\n        }\n\n        void printState() {\n            System.out.println("\\nBranch state:");\n            String latestTag = tags.isEmpty() ? "" : " (tag: " + tags.get(tags.size() - 1) + ")";\n            System.out.println("  main: " + branches.get("main") + latestTag);\n            System.out.println("  develop: " + branches.get("develop"));\n            for (var entry : branches.entrySet()) {\n                if (!entry.getKey().equals("main") && !entry.getKey().equals("develop")) {\n                    System.out.println("  " + entry.getKey() + ": " + entry.getValue());\n                }\n            }\n        }\n    }\n\n    public static void main(String[] args) {\n        GitflowSimulator git = new GitflowSimulator();\n        git.init();\n\n        // Feature 1\n        git.startFeature("user-auth");\n        git.addCommit("feature/user-auth", "feat(auth): implement JWT");\n        git.addCommit("feature/user-auth", "test(auth): add unit tests");\n        git.finishFeature("user-auth");\n\n        // Feature 2\n        git.startFeature("payment");\n        git.addCommit("feature/payment", "feat(payment): add Stripe");\n        git.finishFeature("payment");\n\n        // Release\n        git.startRelease("1.0.0");\n        git.addCommit("release/1.0.0", "fix: update version numbers");\n        git.finishRelease("1.0.0");\n\n        git.printState();\n    }\n}',
      explanation: 'Gitflow — структурированный workflow с пятью типами веток: main (production), develop (интеграция), feature/* (новые фичи), release/* (подготовка релиза), hotfix/* (экстренные фиксы). Каждый тип ветки имеет строгие правила: откуда создавать, куда мержить. Это обеспечивает стабильность production (main) при активной разработке в develop.'
    },
    {
      id: 7,
      title: 'Commit Message Analyzer',
      type: 'practice',
      difficulty: 'easy',
      description: 'Реализуйте анализатор commit messages: парсинг Conventional Commits, статистика по типам, определение следующей SemVer версии.',
      requirements: [
        'Метод parseCommit(message) — разбирает сообщение на type, scope, description, isBreaking',
        'Метод analyzeHistory(commits) — статистика: количество по типам',
        'Метод suggestVersion(currentVersion, commits) — предлагает следующую версию на основе коммитов',
        'Обработка невалидных сообщений',
        'Продемонстрировать анализ набора коммитов'
      ],
      expectedOutput: 'Parsing commits:\n  "feat(auth): add OAuth2" → type=feat, scope=auth, desc="add OAuth2"\n  "fix(payment): prevent double charge" → type=fix, scope=payment, desc="prevent double charge"\n  "feat(api)!: change response format" → type=feat, scope=api, desc="change response format", BREAKING!\n  "docs: update README" → type=docs, scope=none, desc="update README"\n  "invalid message" → INVALID FORMAT\n\nStatistics:\n  feat: 2\n  fix: 1\n  docs: 1\n  invalid: 1\n  breaking changes: 1\n\nCurrent version: v1.2.3\nSuggested next version: v2.0.0 (reason: breaking change)',
      hint: 'Используйте regex: "^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\\(([a-z0-9-]+)\\))?(!)?: (.+)$". Группы: 1=type, 3=scope, 4=breaking, 5=description.',
      solution: 'import java.util.*;\nimport java.util.regex.Matcher;\nimport java.util.regex.Pattern;\n\npublic class Main {\n\n    static class ParsedCommit {\n        String type;\n        String scope;\n        String description;\n        boolean isBreaking;\n        boolean isValid;\n\n        ParsedCommit(boolean isValid) {\n            this.isValid = isValid;\n        }\n    }\n\n    static final Pattern COMMIT_PATTERN = Pattern.compile(\n        "^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)" +\n        "(\\\\(([a-z0-9-]+)\\\\))?(!)?: (.+)$"\n    );\n\n    static ParsedCommit parseCommit(String message) {\n        Matcher m = COMMIT_PATTERN.matcher(message);\n        if (!m.matches()) {\n            ParsedCommit invalid = new ParsedCommit(false);\n            return invalid;\n        }\n\n        ParsedCommit parsed = new ParsedCommit(true);\n        parsed.type = m.group(1);\n        parsed.scope = m.group(3);\n        parsed.description = m.group(5);\n        parsed.isBreaking = m.group(4) != null;\n        return parsed;\n    }\n\n    static String suggestVersion(String currentVersion, List<ParsedCommit> commits) {\n        String ver = currentVersion.startsWith("v") ? currentVersion.substring(1) : currentVersion;\n        String[] parts = ver.split("\\\\.");\n        int major = Integer.parseInt(parts[0]);\n        int minor = Integer.parseInt(parts[1]);\n        int patch = Integer.parseInt(parts[2]);\n\n        boolean hasBreaking = false;\n        boolean hasFeat = false;\n        boolean hasFix = false;\n        String reason = "no changes";\n\n        for (ParsedCommit c : commits) {\n            if (!c.isValid) continue;\n            if (c.isBreaking) hasBreaking = true;\n            if (c.type.equals("feat")) hasFeat = true;\n            if (c.type.equals("fix")) hasFix = true;\n        }\n\n        if (hasBreaking) {\n            major++; minor = 0; patch = 0;\n            reason = "breaking change";\n        } else if (hasFeat) {\n            minor++; patch = 0;\n            reason = "new feature";\n        } else if (hasFix) {\n            patch++;\n            reason = "bug fix";\n        }\n\n        return "v" + major + "." + minor + "." + patch + " (reason: " + reason + ")";\n    }\n\n    public static void main(String[] args) {\n        String[] messages = {\n            "feat(auth): add OAuth2",\n            "fix(payment): prevent double charge",\n            "feat(api)!: change response format",\n            "docs: update README",\n            "invalid message"\n        };\n\n        System.out.println("Parsing commits:");\n        Map<String, Integer> stats = new LinkedHashMap<>();\n        int breakingCount = 0;\n        int invalidCount = 0;\n        List<ParsedCommit> allCommits = new ArrayList<>();\n\n        for (String msg : messages) {\n            ParsedCommit parsed = parseCommit(msg);\n            allCommits.add(parsed);\n            System.out.print("  \\"" + msg + "\\" → ");\n\n            if (!parsed.isValid) {\n                System.out.println("INVALID FORMAT");\n                invalidCount++;\n                continue;\n            }\n\n            stats.merge(parsed.type, 1, Integer::sum);\n            StringBuilder sb = new StringBuilder();\n            sb.append("type=").append(parsed.type);\n            sb.append(", scope=").append(parsed.scope != null ? parsed.scope : "none");\n            sb.append(", desc=\\"").append(parsed.description).append("\\"");\n            if (parsed.isBreaking) {\n                sb.append(", BREAKING!");\n                breakingCount++;\n            }\n            System.out.println(sb);\n        }\n\n        System.out.println("\\nStatistics:");\n        for (var entry : stats.entrySet()) {\n            System.out.println("  " + entry.getKey() + ": " + entry.getValue());\n        }\n        if (invalidCount > 0) System.out.println("  invalid: " + invalidCount);\n        System.out.println("  breaking changes: " + breakingCount);\n\n        System.out.println("\\nCurrent version: v1.2.3");\n        System.out.println("Suggested next version: " + suggestVersion("v1.2.3", allCommits));\n    }\n}',
      explanation: 'Conventional Commits + SemVer = автоматизированное версионирование. Regex парсит сообщение коммита на составляющие. Анализ типов определяет следующую версию: breaking change → MAJOR, feat → MINOR, fix → PATCH. Инструменты типа semantic-release делают это автоматически в CI/CD pipeline.'
    },
    {
      id: 8,
      title: 'Git Workflow Validator',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте валидатор Git workflow: проверка именования веток, commit messages, правил merge и branch protection.',
      requirements: [
        'Класс WorkflowRule — интерфейс для правила валидации',
        'Класс BranchNameRule — валидация имени ветки (feature/, fix/, hotfix/)',
        'Класс CommitMessageRule — валидация Conventional Commits',
        'Класс MergeRule — проверка, разрешён ли merge (например, feature → main запрещён)',
        'Класс WorkflowValidator — выполняет все правила и выводит отчёт'
      ],
      expectedOutput: 'Validating workflow...\n\nBranch name validation:\n  "feature/auth" → PASS\n  "fix/login-bug" → PASS\n  "my-branch" → FAIL (must start with feature/, fix/, hotfix/, release/)\n  "feature/AUTH" → FAIL (must be lowercase)\n\nCommit message validation:\n  "feat(auth): add login" → PASS\n  "fixed stuff" → FAIL (invalid conventional commit format)\n  "feat: a]" → PASS\n\nMerge rules validation:\n  feature/auth → develop: PASS\n  feature/auth → main: FAIL (feature branches must merge to develop)\n  release/1.0 → main: PASS\n  hotfix/fix → main: PASS\n\nReport: 7 passed, 3 failed',
      hint: 'Каждый Rule имеет метод validate() возвращающий boolean и getMessage(). Валидатор собирает все правила и выполняет их последовательно.',
      solution: 'import java.util.*;\nimport java.util.regex.Pattern;\n\npublic class Main {\n\n    interface WorkflowRule {\n        boolean validate(String input);\n        String getMessage();\n    }\n\n    static class BranchNameRule implements WorkflowRule {\n        private static final Pattern PATTERN = Pattern.compile(\n            "^(feature|fix|hotfix|release|develop|main)/[a-z0-9-]+$|^(main|develop)$"\n        );\n        private String lastInput;\n\n        @Override\n        public boolean validate(String input) {\n            lastInput = input;\n            return PATTERN.matcher(input).matches();\n        }\n\n        @Override\n        public String getMessage() {\n            if (!lastInput.equals(lastInput.toLowerCase())) {\n                return "must be lowercase";\n            }\n            return "must start with feature/, fix/, hotfix/, release/";\n        }\n    }\n\n    static class CommitMessageRule implements WorkflowRule {\n        private static final Pattern PATTERN = Pattern.compile(\n            "^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)" +\n            "(\\\\([a-z0-9-]+\\\\))?(!)?: .+$"\n        );\n\n        @Override\n        public boolean validate(String input) {\n            return PATTERN.matcher(input).matches();\n        }\n\n        @Override\n        public String getMessage() {\n            return "invalid conventional commit format";\n        }\n    }\n\n    static class MergeRule implements WorkflowRule {\n        private static final Map<String, List<String>> ALLOWED_TARGETS = Map.of(\n            "feature", List.of("develop"),\n            "fix", List.of("develop"),\n            "hotfix", List.of("main", "develop"),\n            "release", List.of("main", "develop")\n        );\n        private String source, target;\n\n        boolean validate(String source, String target) {\n            this.source = source;\n            this.target = target;\n            String sourceType = source.contains("/") ? source.split("/")[0] : source;\n            List<String> allowed = ALLOWED_TARGETS.getOrDefault(sourceType, List.of());\n            return allowed.contains(target);\n        }\n\n        @Override\n        public boolean validate(String input) {\n            return false;\n        }\n\n        @Override\n        public String getMessage() {\n            String sourceType = source.split("/")[0];\n            return sourceType + " branches must merge to " +\n                ALLOWED_TARGETS.getOrDefault(sourceType, List.of("develop"));\n        }\n    }\n\n    public static void main(String[] args) {\n        System.out.println("Validating workflow...\\n");\n        int passed = 0, failed = 0;\n\n        // Branch name validation\n        System.out.println("Branch name validation:");\n        BranchNameRule branchRule = new BranchNameRule();\n        String[] branches = {"feature/auth", "fix/login-bug", "my-branch", "feature/AUTH"};\n        for (String branch : branches) {\n            boolean valid = branchRule.validate(branch);\n            System.out.println("  \\"" + branch + "\\" → " +\n                (valid ? "PASS" : "FAIL (" + branchRule.getMessage() + ")"));\n            if (valid) passed++; else failed++;\n        }\n\n        // Commit message validation\n        System.out.println("\\nCommit message validation:");\n        CommitMessageRule commitRule = new CommitMessageRule();\n        String[] commits = {"feat(auth): add login", "fixed stuff", "feat: a]"};\n        for (String commit : commits) {\n            boolean valid = commitRule.validate(commit);\n            System.out.println("  \\"" + commit + "\\" → " +\n                (valid ? "PASS" : "FAIL (" + commitRule.getMessage() + ")"));\n            if (valid) passed++; else failed++;\n        }\n\n        // Merge rules validation\n        System.out.println("\\nMerge rules validation:");\n        MergeRule mergeRule = new MergeRule();\n        String[][] merges = {\n            {"feature/auth", "develop"},\n            {"feature/auth", "main"},\n            {"release/1.0", "main"},\n            {"hotfix/fix", "main"}\n        };\n        for (String[] merge : merges) {\n            boolean valid = mergeRule.validate(merge[0], merge[1]);\n            System.out.println("  " + merge[0] + " → " + merge[1] + ": " +\n                (valid ? "PASS" : "FAIL (" + mergeRule.getMessage() + ")"));\n            if (valid) passed++; else failed++;\n        }\n\n        System.out.println("\\nReport: " + passed + " passed, " + failed + " failed");\n    }\n}',
      explanation: 'Workflow validator — это автоматическая проверка соблюдения Git-конвенций команды. В реальных проектах подобные проверки реализуются через Git hooks (pre-commit, commit-msg), CI/CD pipeline и GitHub/GitLab protected branches. Единые правила: именование веток, формат коммитов, направления merge — обеспечивают порядок в проекте.'
    },
    {
      id: 9,
      title: 'Git Blame Analyzer',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте аналог git blame: определение автора каждой строки файла, статистика по контрибьюторам и поиск самых часто изменяемых строк.',
      requirements: [
        'Класс BlameLine с полями lineNumber, content, author, commitHash, date',
        'Класс BlameResult — результат blame для файла',
        'Метод blame(fileHistory) — анализирует историю изменений файла',
        'Метод getAuthorStats() — процент строк каждого автора',
        'Метод getHotspots() — наиболее часто изменяемые строки (code churn)'
      ],
      expectedOutput: 'File: UserService.java\n\nBlame:\n  Line 1: (alice, abc111) public class UserService {\n  Line 2: (alice, abc111)     private UserRepo repo;\n  Line 3: (bob,   abc222)     private Logger log;\n  Line 4: (alice, abc111)     public User getUser(int id) {\n  Line 5: (bob,   abc222)         log.info("getUser: " + id);\n  Line 6: (carol, abc333)         return repo.findById(id).orElseThrow();\n  Line 7: (alice, abc111)     }\n  Line 8: (alice, abc111) }\n\nAuthor statistics:\n  alice: 5 lines (62.5%)\n  bob: 2 lines (25.0%)\n  carol: 1 lines (12.5%)\n\nHotspots (most changed lines):\n  Line 6: 3 changes (by alice, bob, carol)\n  Line 5: 2 changes (by alice, bob)',
      hint: 'Blame: для каждой строки запоминайте автора последнего изменения. Hotspots: считайте количество изменений каждой строки через все коммиты.',
      solution: 'import java.util.*;\nimport java.util.stream.Collectors;\n\npublic class Main {\n\n    static class FileVersion {\n        String commitHash;\n        String author;\n        List<String> lines;\n\n        FileVersion(String commitHash, String author, List<String> lines) {\n            this.commitHash = commitHash;\n            this.author = author;\n            this.lines = lines;\n        }\n    }\n\n    static class BlameLine {\n        int lineNumber;\n        String content;\n        String author;\n        String commitHash;\n\n        BlameLine(int lineNumber, String content, String author, String commitHash) {\n            this.lineNumber = lineNumber;\n            this.content = content;\n            this.author = author;\n            this.commitHash = commitHash;\n        }\n    }\n\n    static class BlameResult {\n        List<BlameLine> lines = new ArrayList<>();\n        Map<Integer, List<String>> changeHistory = new LinkedHashMap<>();\n\n        void addLine(BlameLine line) {\n            lines.add(line);\n        }\n\n        void recordChange(int lineNumber, String author) {\n            changeHistory.computeIfAbsent(lineNumber, k -> new ArrayList<>()).add(author);\n        }\n\n        Map<String, Integer> getAuthorStats() {\n            Map<String, Integer> stats = new LinkedHashMap<>();\n            for (BlameLine line : lines) {\n                stats.merge(line.author, 1, Integer::sum);\n            }\n            return stats;\n        }\n\n        List<Map.Entry<Integer, List<String>>> getHotspots() {\n            return changeHistory.entrySet().stream()\n                .filter(e -> e.getValue().size() > 1)\n                .sorted((a, b) -> b.getValue().size() - a.getValue().size())\n                .collect(Collectors.toList());\n        }\n    }\n\n    static BlameResult blame(List<FileVersion> history) {\n        BlameResult result = new BlameResult();\n        Map<Integer, BlameLine> currentBlame = new LinkedHashMap<>();\n\n        for (FileVersion version : history) {\n            for (int i = 0; i < version.lines.size(); i++) {\n                int lineNum = i + 1;\n                String content = version.lines.get(i);\n\n                if (!currentBlame.containsKey(lineNum) ||\n                    !currentBlame.get(lineNum).content.equals(content)) {\n                    currentBlame.put(lineNum,\n                        new BlameLine(lineNum, content, version.author, version.commitHash));\n                    result.recordChange(lineNum, version.author);\n                }\n            }\n        }\n\n        for (var entry : currentBlame.entrySet()) {\n            result.addLine(entry.getValue());\n        }\n        return result;\n    }\n\n    public static void main(String[] args) {\n        List<FileVersion> history = List.of(\n            new FileVersion("abc111", "alice", List.of(\n                "public class UserService {",\n                "    private UserRepo repo;",\n                "    public User getUser(int id) {",\n                "        return repo.findById(id);",\n                "    }",\n                "}"\n            )),\n            new FileVersion("abc222", "bob", List.of(\n                "public class UserService {",\n                "    private UserRepo repo;",\n                "    private Logger log;",\n                "    public User getUser(int id) {",\n                "        log.info(\\"getUser: \\" + id);",\n                "        return repo.findById(id);",\n                "    }",\n                "}"\n            )),\n            new FileVersion("abc333", "carol", List.of(\n                "public class UserService {",\n                "    private UserRepo repo;",\n                "    private Logger log;",\n                "    public User getUser(int id) {",\n                "        log.info(\\"getUser: \\" + id);",\n                "        return repo.findById(id).orElseThrow();",\n                "    }",\n                "}"\n            ))\n        );\n\n        System.out.println("File: UserService.java\\n");\n\n        BlameResult result = blame(history);\n\n        System.out.println("Blame:");\n        for (BlameLine line : result.lines) {\n            System.out.printf("  Line %d: (%-5s, %s) %s%n",\n                line.lineNumber, line.author, line.commitHash, line.content);\n        }\n\n        System.out.println("\\nAuthor statistics:");\n        Map<String, Integer> stats = result.getAuthorStats();\n        int total = stats.values().stream().mapToInt(Integer::intValue).sum();\n        for (var entry : stats.entrySet()) {\n            double pct = (entry.getValue() * 100.0) / total;\n            System.out.printf("  %s: %d lines (%.1f%%)%n",\n                entry.getKey(), entry.getValue(), pct);\n        }\n\n        System.out.println("\\nHotspots (most changed lines):");\n        for (var hotspot : result.getHotspots()) {\n            System.out.println("  Line " + hotspot.getKey() + ": " +\n                hotspot.getValue().size() + " changes (by " +\n                String.join(", ", hotspot.getValue()) + ")");\n        }\n    }\n}',
      explanation: 'Git blame показывает, кто и когда последний раз изменил каждую строку файла. Это помогает найти автора кода для вопросов, определить причину бага (кто менял?), и выявить hotspots — строки, которые часто меняются (признак нестабильного кода). В IDE (IntelliJ) blame встроен: правый клик → Annotate with Git Blame.'
    },
    {
      id: 10,
      title: 'CI/CD Pipeline Simulator',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте симулятор CI/CD pipeline, привязанный к Git-событиям: push, PR, merge. Pipeline выполняет stages (build, test, deploy) с проверкой статуса.',
      requirements: [
        'Класс Pipeline с stages: BUILD, TEST, LINT, SECURITY_SCAN, DEPLOY',
        'Класс PipelineRun — выполнение pipeline с результатом каждого stage',
        'Триггеры: PUSH (build+test), PULL_REQUEST (all except deploy), MERGE_TO_MAIN (all)',
        'Параллельные stage (LINT и SECURITY_SCAN одновременно)',
        'Блокировка следующего stage при падении предыдущего',
        'Продемонстрировать успешный и провальный pipeline'
      ],
      expectedOutput: 'Pipeline triggered by: PUSH to feature/auth\n  [1/4] BUILD........... PASSED (5s)\n  [2/4] TEST............ PASSED (15s)\n  [3/4] LINT............ PASSED (3s)\n  [3/4] SECURITY_SCAN... PASSED (8s)\n  Result: SUCCESS (31s)\n\nPipeline triggered by: PULL_REQUEST #42\n  [1/4] BUILD........... PASSED (5s)\n  [2/4] TEST............ FAILED (12s)\n    Error: UserServiceTest.testLogin() - assertion failed\n  [3/4] LINT............ SKIPPED\n  [3/4] SECURITY_SCAN... SKIPPED\n  Result: FAILED (17s)\n\nPipeline triggered by: MERGE to main\n  [1/5] BUILD........... PASSED (5s)\n  [2/5] TEST............ PASSED (15s)\n  [3/5] LINT............ PASSED (3s)\n  [3/5] SECURITY_SCAN... PASSED (8s)\n  [4/5] DEPLOY.......... PASSED (20s)\n  Result: SUCCESS (51s)',
      hint: 'Каждый Stage имеет name, duration, shouldPass (для симуляции). Pipeline проходит stages последовательно (кроме параллельных), останавливаясь при ошибке. Trigger определяет набор stages.',
      solution: 'import java.util.*;\n\npublic class Main {\n\n    enum StageResult { PASSED, FAILED, SKIPPED }\n\n    static class Stage {\n        String name;\n        int durationSec;\n        boolean shouldPass;\n        String errorMessage;\n        int order;\n        boolean parallel;\n\n        Stage(String name, int durationSec, boolean shouldPass, int order, boolean parallel) {\n            this.name = name;\n            this.durationSec = durationSec;\n            this.shouldPass = shouldPass;\n            this.order = order;\n            this.parallel = parallel;\n        }\n\n        Stage withError(String msg) {\n            this.errorMessage = msg;\n            return this;\n        }\n    }\n\n    static class StageRun {\n        String name;\n        StageResult result;\n        int durationSec;\n        String error;\n    }\n\n    static class Pipeline {\n        String trigger;\n        List<Stage> stages;\n        List<StageRun> runs = new ArrayList<>();\n        int totalStages;\n\n        Pipeline(String trigger, List<Stage> stages) {\n            this.trigger = trigger;\n            this.stages = stages;\n            this.totalStages = (int) stages.stream().map(s -> s.order).distinct().count();\n        }\n\n        void execute() {\n            System.out.println("Pipeline triggered by: " + trigger);\n            boolean failed = false;\n            int totalTime = 0;\n\n            Map<Integer, List<Stage>> byOrder = new LinkedHashMap<>();\n            for (Stage s : stages) {\n                byOrder.computeIfAbsent(s.order, k -> new ArrayList<>()).add(s);\n            }\n\n            for (var entry : byOrder.entrySet()) {\n                List<Stage> group = entry.getValue();\n                int maxGroupTime = 0;\n\n                for (Stage stage : group) {\n                    StageRun run = new StageRun();\n                    run.name = stage.name;\n\n                    String orderStr = "[" + stage.order + "/" + totalStages + "]";\n                    String nameStr = String.format("%-15s", stage.name + "...");\n                    System.out.print("  " + orderStr + " " + nameStr + " ");\n\n                    if (failed) {\n                        run.result = StageResult.SKIPPED;\n                        run.durationSec = 0;\n                        System.out.println("SKIPPED");\n                    } else if (!stage.shouldPass) {\n                        run.result = StageResult.FAILED;\n                        run.durationSec = stage.durationSec;\n                        run.error = stage.errorMessage;\n                        System.out.println("FAILED (" + stage.durationSec + "s)");\n                        if (run.error != null) {\n                            System.out.println("    Error: " + run.error);\n                        }\n                        failed = true;\n                    } else {\n                        run.result = StageResult.PASSED;\n                        run.durationSec = stage.durationSec;\n                        System.out.println("PASSED (" + stage.durationSec + "s)");\n                    }\n\n                    runs.add(run);\n                    maxGroupTime = Math.max(maxGroupTime, run.durationSec);\n                }\n\n                if (group.size() > 1 && !failed) {\n                    totalTime += maxGroupTime;\n                } else {\n                    totalTime += maxGroupTime;\n                }\n            }\n\n            String result = failed ? "FAILED" : "SUCCESS";\n            System.out.println("  Result: " + result + " (" + totalTime + "s)");\n        }\n    }\n\n    static List<Stage> getPushStages() {\n        return List.of(\n            new Stage("BUILD", 5, true, 1, false),\n            new Stage("TEST", 15, true, 2, false),\n            new Stage("LINT", 3, true, 3, true),\n            new Stage("SECURITY_SCAN", 8, true, 3, true)\n        );\n    }\n\n    static List<Stage> getPRStages(boolean testPass) {\n        List<Stage> stages = new ArrayList<>();\n        stages.add(new Stage("BUILD", 5, true, 1, false));\n        Stage test = new Stage("TEST", 12, testPass, 2, false);\n        if (!testPass) test.withError("UserServiceTest.testLogin() - assertion failed");\n        stages.add(test);\n        stages.add(new Stage("LINT", 3, true, 3, true));\n        stages.add(new Stage("SECURITY_SCAN", 8, true, 3, true));\n        return stages;\n    }\n\n    static List<Stage> getMergeStages() {\n        return List.of(\n            new Stage("BUILD", 5, true, 1, false),\n            new Stage("TEST", 15, true, 2, false),\n            new Stage("LINT", 3, true, 3, true),\n            new Stage("SECURITY_SCAN", 8, true, 3, true),\n            new Stage("DEPLOY", 20, true, 4, false)\n        );\n    }\n\n    public static void main(String[] args) {\n        // Push pipeline (success)\n        Pipeline pushPipeline = new Pipeline("PUSH to feature/auth", getPushStages());\n        pushPipeline.execute();\n\n        // PR pipeline (test fails)\n        System.out.println();\n        Pipeline prPipeline = new Pipeline("PULL_REQUEST #42", getPRStages(false));\n        prPipeline.execute();\n\n        // Merge pipeline (success)\n        System.out.println();\n        Pipeline mergePipeline = new Pipeline("MERGE to main", getMergeStages());\n        mergePipeline.execute();\n    }\n}',
      explanation: 'CI/CD pipeline привязан к Git-событиям: push запускает build+test, PR — полный набор проверок (без deploy), merge в main — всё включая deploy. Stages выполняются последовательно, некоторые параллельно (LINT и SECURITY_SCAN). При падении stage — последующие пропускаются (SKIPPED). В GitHub Actions, GitLab CI, Jenkins — этот принцип реализован через YAML-конфигурации.'
    }
  ]
}
