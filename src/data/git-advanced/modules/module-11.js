export default {
  id: 11,
  title: 'Практикум: Git задачи',
  description: 'Практические задачи на Java: симуляция Git-концепций — граф коммитов, merge алгоритм, детектор конфликтов, менеджер веток, diff алгоритм и другие.',
  lessons: [
    {
      id: 1,
      title: 'Граф коммитов',
      type: 'practice',
      difficulty: 'easy',
      description: 'Реализуйте структуру данных для графа коммитов Git: создание коммитов, связь parent-child, обход истории и поиск общего предка.',
      requirements: [
        'Класс Commit с полями hash, message, timestamp, parent',
        'Класс CommitGraph с методом addCommit() — создаёт коммит с ссылкой на parent',
        'Метод getHistory(commit) — возвращает историю от коммита до корня',
        'Метод findCommonAncestor(commit1, commit2) — ищет общего предка двух коммитов',
        'Продемонстрировать граф с ветвлением и поиск общего предка'
      ],
      expectedOutput: 'Commit history (main):\n  c5: feat: add logging\n  c4: feat: add validation\n  c3: refactor: extract service\n  c2: feat: add user model\n  c1: initial commit\n\nCommit history (feature):\n  c7: feat: add caching\n  c6: feat: add search\n  c3: refactor: extract service\n  c2: feat: add user model\n  c1: initial commit\n\nCommon ancestor of c5 and c7: c3 "refactor: extract service"',
      hint: 'Для поиска общего предка: соберите всех предков первого коммита в Set, затем пройдите по предкам второго коммита — первый найденный в Set и есть общий предок.',
      solution: 'import java.util.*;\n\npublic class Main {\n\n    static class Commit {\n        String hash;\n        String message;\n        Commit parent;\n\n        Commit(String hash, String message, Commit parent) {\n            this.hash = hash;\n            this.message = message;\n            this.parent = parent;\n        }\n\n        @Override\n        public String toString() {\n            return hash + ": " + message;\n        }\n    }\n\n    static class CommitGraph {\n        private final Map<String, Commit> commits = new LinkedHashMap<>();\n\n        Commit addCommit(String hash, String message, Commit parent) {\n            Commit commit = new Commit(hash, message, parent);\n            commits.put(hash, commit);\n            return commit;\n        }\n\n        List<Commit> getHistory(Commit commit) {\n            List<Commit> history = new ArrayList<>();\n            Commit current = commit;\n            while (current != null) {\n                history.add(current);\n                current = current.parent;\n            }\n            return history;\n        }\n\n        Commit findCommonAncestor(Commit a, Commit b) {\n            Set<String> ancestors = new HashSet<>();\n            Commit current = a;\n            while (current != null) {\n                ancestors.add(current.hash);\n                current = current.parent;\n            }\n            current = b;\n            while (current != null) {\n                if (ancestors.contains(current.hash)) {\n                    return current;\n                }\n                current = current.parent;\n            }\n            return null;\n        }\n    }\n\n    public static void main(String[] args) {\n        CommitGraph graph = new CommitGraph();\n\n        // Main branch\n        Commit c1 = graph.addCommit("c1", "initial commit", null);\n        Commit c2 = graph.addCommit("c2", "feat: add user model", c1);\n        Commit c3 = graph.addCommit("c3", "refactor: extract service", c2);\n        Commit c4 = graph.addCommit("c4", "feat: add validation", c3);\n        Commit c5 = graph.addCommit("c5", "feat: add logging", c4);\n\n        // Feature branch (diverges from c3)\n        Commit c6 = graph.addCommit("c6", "feat: add search", c3);\n        Commit c7 = graph.addCommit("c7", "feat: add caching", c6);\n\n        // Print history\n        System.out.println("Commit history (main):");\n        for (Commit c : graph.getHistory(c5)) {\n            System.out.println("  " + c);\n        }\n\n        System.out.println("\\nCommit history (feature):");\n        for (Commit c : graph.getHistory(c7)) {\n            System.out.println("  " + c);\n        }\n\n        // Find common ancestor\n        Commit ancestor = graph.findCommonAncestor(c5, c7);\n        System.out.println("\\nCommon ancestor of c5 and c7: " + ancestor);\n    }\n}',
      explanation: 'Граф коммитов Git — направленный ациклический граф (DAG), где каждый коммит ссылается на родителя. Обход истории — это traversal по parent-ссылкам. Поиск общего предка (merge-base) — ключевая операция для merge и rebase: Git находит точку расхождения двух веток.'
    },
    {
      id: 2,
      title: 'Алгоритм Merge',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте трёхстороннее слияние (three-way merge): базовая версия, ветка A, ветка B. Определите автоматически сливаемые изменения и конфликты.',
      requirements: [
        'Метод threeWayMerge(base, branchA, branchB) — принимает три версии файла (как список строк)',
        'Если строка изменена только в одной ветке — берём изменение',
        'Если строка изменена в обеих ветках одинаково — берём любую',
        'Если строка изменена в обеих ветках по-разному — конфликт',
        'Вывести результат слияния с маркерами конфликтов'
      ],
      expectedOutput: 'Base version:\n  line 1: Hello World\n  line 2: foo = 1\n  line 3: bar = 2\n  line 4: end\n\nBranch A:\n  line 1: Hello World\n  line 2: foo = 10\n  line 3: bar = 2\n  line 4: end!\n\nBranch B:\n  line 1: Hi World\n  line 2: foo = 1\n  line 3: bar = 20\n  line 4: end!!\n\nMerge result:\n  line 1: Hi World (from B)\n  line 2: foo = 10 (from A)\n  line 3: bar = 20 (from B)\n  line 4: CONFLICT\n    <<<<<<< branch-A\n    end!\n    =======\n    end!!\n    >>>>>>> branch-B\n\nConflicts found: 1',
      hint: 'Сравнивайте построчно: если base[i] == A[i] и base[i] != B[i], берём B. Если base[i] != A[i] и base[i] == B[i], берём A. Если A[i] != B[i] и обе != base[i], это конфликт.',
      solution: 'import java.util.ArrayList;\nimport java.util.List;\n\npublic class Main {\n\n    static class MergeResult {\n        List<String> lines = new ArrayList<>();\n        int conflicts = 0;\n    }\n\n    static MergeResult threeWayMerge(List<String> base, List<String> branchA, List<String> branchB) {\n        MergeResult result = new MergeResult();\n        int maxLines = Math.max(base.size(), Math.max(branchA.size(), branchB.size()));\n\n        for (int i = 0; i < maxLines; i++) {\n            String baseLine = i < base.size() ? base.get(i) : "";\n            String lineA = i < branchA.size() ? branchA.get(i) : "";\n            String lineB = i < branchB.size() ? branchB.get(i) : "";\n\n            boolean aChanged = !baseLine.equals(lineA);\n            boolean bChanged = !baseLine.equals(lineB);\n\n            if (!aChanged && !bChanged) {\n                // No changes\n                result.lines.add("  line " + (i + 1) + ": " + baseLine + " (unchanged)");\n            } else if (aChanged && !bChanged) {\n                // Only A changed\n                result.lines.add("  line " + (i + 1) + ": " + lineA + " (from A)");\n            } else if (!aChanged && bChanged) {\n                // Only B changed\n                result.lines.add("  line " + (i + 1) + ": " + lineB + " (from B)");\n            } else if (lineA.equals(lineB)) {\n                // Both changed the same way\n                result.lines.add("  line " + (i + 1) + ": " + lineA + " (both same)");\n            } else {\n                // Conflict!\n                result.conflicts++;\n                result.lines.add("  line " + (i + 1) + ": CONFLICT");\n                result.lines.add("    <<<<<<< branch-A");\n                result.lines.add("    " + lineA);\n                result.lines.add("    =======");\n                result.lines.add("    " + lineB);\n                result.lines.add("    >>>>>>> branch-B");\n            }\n        }\n        return result;\n    }\n\n    public static void main(String[] args) {\n        List<String> base = List.of("Hello World", "foo = 1", "bar = 2", "end");\n        List<String> branchA = List.of("Hello World", "foo = 10", "bar = 2", "end!");\n        List<String> branchB = List.of("Hi World", "foo = 1", "bar = 20", "end!!");\n\n        System.out.println("Base version:");\n        for (int i = 0; i < base.size(); i++) {\n            System.out.println("  line " + (i + 1) + ": " + base.get(i));\n        }\n\n        System.out.println("\\nBranch A:");\n        for (int i = 0; i < branchA.size(); i++) {\n            System.out.println("  line " + (i + 1) + ": " + branchA.get(i));\n        }\n\n        System.out.println("\\nBranch B:");\n        for (int i = 0; i < branchB.size(); i++) {\n            System.out.println("  line " + (i + 1) + ": " + branchB.get(i));\n        }\n\n        MergeResult result = threeWayMerge(base, branchA, branchB);\n\n        System.out.println("\\nMerge result:");\n        for (String line : result.lines) {\n            System.out.println(line);\n        }\n        System.out.println("\\nConflicts found: " + result.conflicts);\n    }\n}',
      explanation: 'Three-way merge — основной алгоритм Git. Он сравнивает три версии: общий предок (base), ветка A и ветка B. Если изменение только в одной ветке — автоматически принимается. Если в обеих — проверяется, одинаковые ли изменения. Разные изменения одной строки — конфликт, который требует ручного разрешения.'
    },
    {
      id: 3,
      title: 'Детектор конфликтов',
      type: 'practice',
      difficulty: 'easy',
      description: 'Реализуйте детектор потенциальных конфликтов: анализирует изменения двух веток и определяет, будет ли конфликт при merge.',
      requirements: [
        'Класс FileChange с полями filename и changedLines (набор номеров строк)',
        'Класс Branch с именем и списком FileChange',
        'Метод detectConflicts(branchA, branchB) — находит файлы, где обе ветки изменили одни и те же строки',
        'Вывести список конфликтных файлов и строк',
        'Продемонстрировать с несколькими файлами: некоторые с конфликтами, некоторые без'
      ],
      expectedOutput: 'Branch "feature/auth" changes:\n  UserService.java: lines [12, 15, 20]\n  AuthController.java: lines [5, 8, 10]\n  Config.java: lines [3]\n\nBranch "feature/payment" changes:\n  UserService.java: lines [15, 25, 30]\n  PaymentService.java: lines [10, 20]\n  Config.java: lines [7]\n\nConflict analysis:\n  UserService.java: CONFLICT on lines [15]\n  AuthController.java: OK (no overlap)\n  PaymentService.java: OK (only in feature/payment)\n  Config.java: OK (no overlap, lines 3 vs 7)\n\nTotal: 1 conflict(s) in 1 file(s)',
      hint: 'Для каждого файла, изменённого в обеих ветках, найдите пересечение множеств изменённых строк (Set.retainAll). Непустое пересечение = конфликт.',
      solution: 'import java.util.*;\n\npublic class Main {\n\n    static class FileChange {\n        String filename;\n        Set<Integer> changedLines;\n\n        FileChange(String filename, Integer... lines) {\n            this.filename = filename;\n            this.changedLines = new TreeSet<>(Arrays.asList(lines));\n        }\n    }\n\n    static class Branch {\n        String name;\n        List<FileChange> changes;\n\n        Branch(String name, FileChange... changes) {\n            this.name = name;\n            this.changes = List.of(changes);\n        }\n\n        Map<String, Set<Integer>> getChangeMap() {\n            Map<String, Set<Integer>> map = new LinkedHashMap<>();\n            for (FileChange fc : changes) {\n                map.put(fc.filename, fc.changedLines);\n            }\n            return map;\n        }\n    }\n\n    static class ConflictResult {\n        String filename;\n        Set<Integer> conflictLines;\n        String status;\n\n        ConflictResult(String filename, Set<Integer> conflictLines, String status) {\n            this.filename = filename;\n            this.conflictLines = conflictLines;\n            this.status = status;\n        }\n    }\n\n    static List<ConflictResult> detectConflicts(Branch branchA, Branch branchB) {\n        Map<String, Set<Integer>> changesA = branchA.getChangeMap();\n        Map<String, Set<Integer>> changesB = branchB.getChangeMap();\n\n        Set<String> allFiles = new LinkedHashSet<>();\n        allFiles.addAll(changesA.keySet());\n        allFiles.addAll(changesB.keySet());\n\n        List<ConflictResult> results = new ArrayList<>();\n        for (String file : allFiles) {\n            Set<Integer> linesA = changesA.getOrDefault(file, Set.of());\n            Set<Integer> linesB = changesB.getOrDefault(file, Set.of());\n\n            if (linesA.isEmpty()) {\n                results.add(new ConflictResult(file, Set.of(), "OK (only in " + branchB.name + ")"));\n            } else if (linesB.isEmpty()) {\n                results.add(new ConflictResult(file, Set.of(), "OK (no overlap)"));\n            } else {\n                Set<Integer> overlap = new TreeSet<>(linesA);\n                overlap.retainAll(linesB);\n                if (overlap.isEmpty()) {\n                    results.add(new ConflictResult(file, Set.of(),\n                        "OK (no overlap, lines " + linesA + " vs " + linesB + ")"));\n                } else {\n                    results.add(new ConflictResult(file, overlap,\n                        "CONFLICT on lines " + overlap));\n                }\n            }\n        }\n        return results;\n    }\n\n    public static void main(String[] args) {\n        Branch auth = new Branch("feature/auth",\n            new FileChange("UserService.java", 12, 15, 20),\n            new FileChange("AuthController.java", 5, 8, 10),\n            new FileChange("Config.java", 3)\n        );\n\n        Branch payment = new Branch("feature/payment",\n            new FileChange("UserService.java", 15, 25, 30),\n            new FileChange("PaymentService.java", 10, 20),\n            new FileChange("Config.java", 7)\n        );\n\n        System.out.println("Branch \\"" + auth.name + "\\" changes:");\n        for (FileChange fc : auth.changes) {\n            System.out.println("  " + fc.filename + ": lines " + new ArrayList<>(fc.changedLines));\n        }\n\n        System.out.println("\\nBranch \\"" + payment.name + "\\" changes:");\n        for (FileChange fc : payment.changes) {\n            System.out.println("  " + fc.filename + ": lines " + new ArrayList<>(fc.changedLines));\n        }\n\n        List<ConflictResult> results = detectConflicts(auth, payment);\n\n        System.out.println("\\nConflict analysis:");\n        int conflictFiles = 0;\n        int totalConflicts = 0;\n        for (ConflictResult r : results) {\n            System.out.println("  " + r.filename + ": " + r.status);\n            if (!r.conflictLines.isEmpty()) {\n                conflictFiles++;\n                totalConflicts += r.conflictLines.size();\n            }\n        }\n        System.out.println("\\nTotal: " + totalConflicts + " conflict(s) in " + conflictFiles + " file(s)");\n    }\n}',
      explanation: 'Детектор конфликтов анализирует, какие строки изменили разные ветки. Если обе ветки изменили одну и ту же строку в одном файле — будет конфликт при merge. Это пересечение множеств (set intersection). В реальном Git анализ сложнее — учитываются добавления/удаления строк, но принцип тот же.'
    },
    {
      id: 4,
      title: 'Менеджер веток',
      type: 'practice',
      difficulty: 'easy',
      description: 'Реализуйте менеджер веток Git: создание, переключение, удаление, вывод списка веток и текущей ветки (HEAD).',
      requirements: [
        'Класс BranchManager с методами createBranch(), checkout(), deleteBranch(), listBranches()',
        'Поле HEAD, указывающее на текущую ветку',
        'Каждая ветка указывает на коммит (хеш)',
        'Защита: нельзя удалить текущую ветку',
        'Защита: нельзя создать ветку с существующим именем'
      ],
      expectedOutput: 'Creating branch "main" at commit abc1234\nCreating branch "feature/auth" at commit abc1234\nCreating branch "feature/payment" at commit abc1234\n\nBranches:\n  * main (HEAD) → abc1234\n    feature/auth → abc1234\n    feature/payment → abc1234\n\nCheckout to "feature/auth"\nAdvancing "feature/auth" to def5678\n\nBranches:\n    main → abc1234\n  * feature/auth (HEAD) → def5678\n    feature/payment → abc1234\n\nERROR: Cannot delete current branch "feature/auth"\nCheckout to "main"\nDeleting branch "feature/auth"\n\nBranches:\n  * main (HEAD) → abc1234\n    feature/payment → abc1234',
      hint: 'Используйте Map<String, String> (branchName → commitHash) для хранения веток и String head для текущей ветки.',
      solution: 'import java.util.*;\n\npublic class Main {\n\n    static class BranchManager {\n        private final Map<String, String> branches = new LinkedHashMap<>();\n        private String head;\n\n        void createBranch(String name, String commitHash) {\n            if (branches.containsKey(name)) {\n                System.out.println("ERROR: Branch \\"" + name + "\\" already exists");\n                return;\n            }\n            branches.put(name, commitHash);\n            System.out.println("Creating branch \\"" + name + "\\" at commit " + commitHash);\n            if (head == null) {\n                head = name;\n            }\n        }\n\n        void checkout(String name) {\n            if (!branches.containsKey(name)) {\n                System.out.println("ERROR: Branch \\"" + name + "\\" not found");\n                return;\n            }\n            head = name;\n            System.out.println("Checkout to \\"" + name + "\\"");\n        }\n\n        void deleteBranch(String name) {\n            if (!branches.containsKey(name)) {\n                System.out.println("ERROR: Branch \\"" + name + "\\" not found");\n                return;\n            }\n            if (name.equals(head)) {\n                System.out.println("ERROR: Cannot delete current branch \\"" + name + "\\"");\n                return;\n            }\n            branches.remove(name);\n            System.out.println("Deleting branch \\"" + name + "\\"");\n        }\n\n        void advanceBranch(String name, String commitHash) {\n            if (!branches.containsKey(name)) {\n                System.out.println("ERROR: Branch \\"" + name + "\\" not found");\n                return;\n            }\n            branches.put(name, commitHash);\n            System.out.println("Advancing \\"" + name + "\\" to " + commitHash);\n        }\n\n        void listBranches() {\n            System.out.println("\\nBranches:");\n            for (var entry : branches.entrySet()) {\n                String prefix = entry.getKey().equals(head) ? "  * " : "    ";\n                String suffix = entry.getKey().equals(head) ? " (HEAD)" : "";\n                System.out.println(prefix + entry.getKey() + suffix + " → " + entry.getValue());\n            }\n        }\n    }\n\n    public static void main(String[] args) {\n        BranchManager manager = new BranchManager();\n\n        manager.createBranch("main", "abc1234");\n        manager.createBranch("feature/auth", "abc1234");\n        manager.createBranch("feature/payment", "abc1234");\n        manager.listBranches();\n\n        System.out.println();\n        manager.checkout("feature/auth");\n        manager.advanceBranch("feature/auth", "def5678");\n        manager.listBranches();\n\n        System.out.println();\n        manager.deleteBranch("feature/auth");\n        manager.checkout("main");\n        manager.deleteBranch("feature/auth");\n        manager.listBranches();\n    }\n}',
      explanation: 'В Git ветки — это просто файлы с хешем коммита (refs/heads/branchname). HEAD — указатель на текущую ветку. Создание ветки = создание файла с хешем. Checkout = изменение HEAD. Удаление = удаление файла. Это одна из причин, почему Git такой быстрый — ветки практически бесплатны.'
    },
    {
      id: 5,
      title: 'Diff алгоритм',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте упрощённый diff алгоритм: сравнение двух версий файла и вывод добавленных/удалённых строк в формате unified diff.',
      requirements: [
        'Метод computeDiff(oldLines, newLines) — вычисляет разницу между двумя версиями',
        'Используйте алгоритм LCS (Longest Common Subsequence) для нахождения общих строк',
        'Вывод в стиле unified diff: "+" для добавленных, "-" для удалённых, " " для неизменённых',
        'Подсчитать количество добавлений и удалений'
      ],
      expectedOutput: 'Old version:\n  public class UserService {\n      private UserRepo repo;\n      public User getUser(int id) {\n          return repo.findById(id);\n      }\n  }\n\nNew version:\n  public class UserService {\n      private final UserRepo repo;\n      private final Logger log;\n      public Optional<User> getUser(int id) {\n          log.info("Getting user: " + id);\n          return repo.findById(id);\n      }\n  }\n\nDiff:\n   public class UserService {\n  -    private UserRepo repo;\n  +    private final UserRepo repo;\n  +    private final Logger log;\n  -    public User getUser(int id) {\n  +    public Optional<User> getUser(int id) {\n  +        log.info("Getting user: " + id);\n           return repo.findById(id);\n       }\n   }\n\nStats: 4 insertions(+), 2 deletions(-)',
      hint: 'LCS (Longest Common Subsequence): постройте таблицу dp[i][j] = длина LCS для old[0..i] и new[0..j]. Затем пройдите обратно для восстановления diff. Упрощение: для построчного diff можно использовать простое сравнение.',
      solution: 'import java.util.*;\n\npublic class Main {\n\n    static class DiffLine {\n        char type; // \' \', \'+\', \'-\'\n        String content;\n\n        DiffLine(char type, String content) {\n            this.type = type;\n            this.content = content;\n        }\n    }\n\n    static List<DiffLine> computeDiff(List<String> oldLines, List<String> newLines) {\n        int m = oldLines.size();\n        int n = newLines.size();\n\n        // Build LCS table\n        int[][] dp = new int[m + 1][n + 1];\n        for (int i = 1; i <= m; i++) {\n            for (int j = 1; j <= n; j++) {\n                if (oldLines.get(i - 1).equals(newLines.get(j - 1))) {\n                    dp[i][j] = dp[i - 1][j - 1] + 1;\n                } else {\n                    dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]);\n                }\n            }\n        }\n\n        // Backtrack to build diff\n        List<DiffLine> diff = new ArrayList<>();\n        int i = m, j = n;\n        while (i > 0 || j > 0) {\n            if (i > 0 && j > 0 && oldLines.get(i - 1).equals(newLines.get(j - 1))) {\n                diff.add(new DiffLine(\' \', oldLines.get(i - 1)));\n                i--;\n                j--;\n            } else if (j > 0 && (i == 0 || dp[i][j - 1] >= dp[i - 1][j])) {\n                diff.add(new DiffLine(\'+\', newLines.get(j - 1)));\n                j--;\n            } else {\n                diff.add(new DiffLine(\'-\', oldLines.get(i - 1)));\n                i--;\n            }\n        }\n        Collections.reverse(diff);\n        return diff;\n    }\n\n    public static void main(String[] args) {\n        List<String> oldVersion = List.of(\n            "public class UserService {",\n            "    private UserRepo repo;",\n            "    public User getUser(int id) {",\n            "        return repo.findById(id);",\n            "    }",\n            "}"\n        );\n\n        List<String> newVersion = List.of(\n            "public class UserService {",\n            "    private final UserRepo repo;",\n            "    private final Logger log;",\n            "    public Optional<User> getUser(int id) {",\n            "        log.info(\\"Getting user: \\" + id);",\n            "        return repo.findById(id);",\n            "    }",\n            "}"\n        );\n\n        System.out.println("Old version:");\n        for (String line : oldVersion) System.out.println("  " + line);\n\n        System.out.println("\\nNew version:");\n        for (String line : newVersion) System.out.println("  " + line);\n\n        List<DiffLine> diff = computeDiff(oldVersion, newVersion);\n\n        System.out.println("\\nDiff:");\n        int insertions = 0, deletions = 0;\n        for (DiffLine d : diff) {\n            System.out.println("  " + d.type + " " + d.content);\n            if (d.type == \'+\') insertions++;\n            if (d.type == \'-\') deletions++;\n        }\n        System.out.println("\\nStats: " + insertions + " insertions(+), " + deletions + " deletions(-)");\n    }\n}',
      explanation: 'Diff алгоритм Git основан на LCS (Longest Common Subsequence). Строится таблица динамического программирования, где dp[i][j] — длина наибольшей общей подпоследовательности old[0..i] и new[0..j]. Обратный проход по таблице восстанавливает diff: совпадающие строки — context, остальные — additions/deletions.'
    },
    {
      id: 6,
      title: 'Stash Stack',
      type: 'practice',
      difficulty: 'easy',
      description: 'Реализуйте стек stash-записей Git: сохранение, применение, удаление и просмотр отложенных изменений.',
      requirements: [
        'Класс StashEntry с полями message, changedFiles и timestamp',
        'Класс GitStash реализующий стек (LIFO) stash-записей',
        'Методы push(message, files), pop(), apply(index), drop(index), list()',
        'pop() — применяет и удаляет верхний stash',
        'apply() — применяет без удаления'
      ],
      expectedOutput: 'git stash push "WIP: auth feature"\n  Saved: stash@{0}: WIP: auth feature [AuthService.java, LoginTest.java]\n\ngit stash push "WIP: payment bugfix"\n  Saved: stash@{0}: WIP: payment bugfix [PaymentService.java]\n\ngit stash list:\n  stash@{0}: WIP: payment bugfix [PaymentService.java]\n  stash@{1}: WIP: auth feature [AuthService.java, LoginTest.java]\n\ngit stash pop:\n  Applied and removed: WIP: payment bugfix [PaymentService.java]\n\ngit stash list:\n  stash@{0}: WIP: auth feature [AuthService.java, LoginTest.java]\n\ngit stash apply 0:\n  Applied (kept in stash): WIP: auth feature [AuthService.java, LoginTest.java]\n\ngit stash drop 0:\n  Dropped: WIP: auth feature\n\ngit stash list:\n  (empty)',
      hint: 'Stash — это Stack (LIFO). Используйте Deque<StashEntry> или List<StashEntry>, где push добавляет в начало, pop удаляет из начала.',
      solution: 'import java.util.*;\n\npublic class Main {\n\n    static class StashEntry {\n        String message;\n        List<String> changedFiles;\n\n        StashEntry(String message, List<String> changedFiles) {\n            this.message = message;\n            this.changedFiles = changedFiles;\n        }\n\n        @Override\n        public String toString() {\n            return message + " " + changedFiles;\n        }\n    }\n\n    static class GitStash {\n        private final Deque<StashEntry> stack = new ArrayDeque<>();\n\n        void push(String message, String... files) {\n            System.out.println("git stash push \\"" + message + "\\"");\n            StashEntry entry = new StashEntry(message, List.of(files));\n            stack.push(entry);\n            System.out.println("  Saved: stash@{0}: " + entry);\n        }\n\n        void pop() {\n            System.out.println("git stash pop:");\n            if (stack.isEmpty()) {\n                System.out.println("  ERROR: No stash entries");\n                return;\n            }\n            StashEntry entry = stack.pop();\n            System.out.println("  Applied and removed: " + entry);\n        }\n\n        void apply(int index) {\n            System.out.println("git stash apply " + index + ":");\n            List<StashEntry> list = new ArrayList<>(stack);\n            if (index >= list.size()) {\n                System.out.println("  ERROR: stash@{" + index + "} does not exist");\n                return;\n            }\n            StashEntry entry = list.get(index);\n            System.out.println("  Applied (kept in stash): " + entry);\n        }\n\n        void drop(int index) {\n            System.out.println("git stash drop " + index + ":");\n            List<StashEntry> list = new ArrayList<>(stack);\n            if (index >= list.size()) {\n                System.out.println("  ERROR: stash@{" + index + "} does not exist");\n                return;\n            }\n            StashEntry entry = list.remove(index);\n            stack.clear();\n            list.forEach(stack::addLast);\n            System.out.println("  Dropped: " + entry.message);\n        }\n\n        void list() {\n            System.out.println("git stash list:");\n            if (stack.isEmpty()) {\n                System.out.println("  (empty)");\n                return;\n            }\n            int i = 0;\n            for (StashEntry entry : stack) {\n                System.out.println("  stash@{" + i + "}: " + entry);\n                i++;\n            }\n        }\n    }\n\n    public static void main(String[] args) {\n        GitStash stash = new GitStash();\n\n        stash.push("WIP: auth feature", "AuthService.java", "LoginTest.java");\n        System.out.println();\n        stash.push("WIP: payment bugfix", "PaymentService.java");\n\n        System.out.println();\n        stash.list();\n\n        System.out.println();\n        stash.pop();\n\n        System.out.println();\n        stash.list();\n\n        System.out.println();\n        stash.apply(0);\n\n        System.out.println();\n        stash.drop(0);\n\n        System.out.println();\n        stash.list();\n    }\n}',
      explanation: 'Git stash — это стек (LIFO). push сохраняет изменения и очищает рабочую директорию. pop применяет верхний stash и удаляет его. apply — применяет без удаления (полезно если нужно применить в несколько мест). drop — удаляет без применения. Нумерация stash@{0} — самый свежий, stash@{1} — следующий и т.д.'
    },
    {
      id: 7,
      title: 'Reflog Tracker',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте систему reflog: запись всех действий с HEAD, восстановление удалённых коммитов и веток.',
      requirements: [
        'Класс ReflogEntry с полями action, fromHash, toHash, message, timestamp',
        'Класс Reflog с методами record(), getLog(), findCommit()',
        'Автоматическая запись при commit, checkout, reset, branch delete',
        'Метод recover(index) — восстанавливает HEAD на состояние из reflog',
        'Продемонстрировать восстановление после reset --hard'
      ],
      expectedOutput: 'commit: initial commit → abc1111\ncommit: add user model → abc2222\ncommit: add auth → abc3333\ncheckout: main → feature/test\ncommit: test feature → abc4444\ncheckout: feature/test → main\nreset --hard: HEAD to abc1111\n\nReflog:\n  HEAD@{0}: reset: moving to abc1111\n  HEAD@{1}: checkout: moving from feature/test to main\n  HEAD@{2}: commit: test feature\n  HEAD@{3}: checkout: moving from main to feature/test\n  HEAD@{4}: commit: add auth\n  HEAD@{5}: commit: add user model\n  HEAD@{6}: commit (initial): initial commit\n\nCurrent HEAD: abc1111\nRecovering to HEAD@{4} (abc3333)...\nHEAD restored to: abc3333',
      hint: 'Reflog — это List<ReflogEntry> в обратном порядке (последнее действие = HEAD@{0}). При каждом действии (commit, checkout, reset) добавляйте запись. Восстановление = установить HEAD на хеш из записи.',
      solution: 'import java.util.*;\n\npublic class Main {\n\n    static class ReflogEntry {\n        String action;\n        String fromHash;\n        String toHash;\n        String message;\n\n        ReflogEntry(String action, String fromHash, String toHash, String message) {\n            this.action = action;\n            this.fromHash = fromHash;\n            this.toHash = toHash;\n            this.message = message;\n        }\n    }\n\n    static class GitSimulator {\n        private String head;\n        private String currentBranch = "main";\n        private final Map<String, String> branches = new LinkedHashMap<>();\n        private final List<ReflogEntry> reflog = new ArrayList<>();\n\n        void commit(String hash, String message) {\n            String oldHead = head;\n            head = hash;\n            branches.put(currentBranch, hash);\n            String action = oldHead == null ? "commit (initial)" : "commit";\n            reflog.add(0, new ReflogEntry(action, oldHead, hash, message));\n            System.out.println("commit: " + message + " → " + hash);\n        }\n\n        void checkout(String branch) {\n            String oldBranch = currentBranch;\n            currentBranch = branch;\n            String oldHead = head;\n            head = branches.getOrDefault(branch, head);\n            if (!branches.containsKey(branch)) {\n                branches.put(branch, head);\n            }\n            reflog.add(0, new ReflogEntry("checkout",\n                oldHead, head, "moving from " + oldBranch + " to " + branch));\n            System.out.println("checkout: " + oldBranch + " → " + branch);\n        }\n\n        void resetHard(String targetHash) {\n            String oldHead = head;\n            head = targetHash;\n            branches.put(currentBranch, targetHash);\n            reflog.add(0, new ReflogEntry("reset",\n                oldHead, targetHash, "moving to " + targetHash));\n            System.out.println("reset --hard: HEAD to " + targetHash);\n        }\n\n        void printReflog() {\n            System.out.println("\\nReflog:");\n            for (int i = 0; i < reflog.size(); i++) {\n                ReflogEntry e = reflog.get(i);\n                System.out.println("  HEAD@{" + i + "}: " + e.action + ": " + e.message);\n            }\n        }\n\n        void recover(int index) {\n            if (index >= reflog.size()) {\n                System.out.println("ERROR: HEAD@{" + index + "} does not exist");\n                return;\n            }\n            ReflogEntry entry = reflog.get(index);\n            System.out.println("Recovering to HEAD@{" + index + "} (" + entry.toHash + ")...");\n            head = entry.toHash;\n            branches.put(currentBranch, head);\n            System.out.println("HEAD restored to: " + head);\n        }\n\n        String getHead() {\n            return head;\n        }\n    }\n\n    public static void main(String[] args) {\n        GitSimulator git = new GitSimulator();\n\n        git.commit("abc1111", "initial commit");\n        git.commit("abc2222", "add user model");\n        git.commit("abc3333", "add auth");\n\n        git.checkout("feature/test");\n        git.commit("abc4444", "test feature");\n\n        git.checkout("main");\n        git.resetHard("abc1111");\n\n        git.printReflog();\n\n        System.out.println("\\nCurrent HEAD: " + git.getHead());\n        git.recover(4);\n    }\n}',
      explanation: 'Reflog (reference log) записывает каждое перемещение HEAD: коммиты, checkout, reset, rebase. Это локальная история действий, которая позволяет восстановить "потерянные" коммиты. Даже после reset --hard коммиты физически остаются в .git/objects — reflog хранит ссылки на них. Reflog — главная страховка разработчика.'
    },
    {
      id: 8,
      title: 'Cherry-pick Selector',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте механизм cherry-pick: выборочное применение коммитов из одной ветки в другую с обнаружением конфликтов.',
      requirements: [
        'Класс Commit с полями hash, message, changes (Map<String, String> — файл → содержимое)',
        'Класс Branch с именем, списком коммитов и текущим состоянием файлов',
        'Метод cherryPick(commit) — применяет изменения коммита к текущей ветке',
        'Обнаружение конфликтов: если файл уже отличается',
        'Продемонстрировать cherry-pick нескольких коммитов'
      ],
      expectedOutput: 'Feature branch commits:\n  f1: feat: add search (files: [SearchService.java])\n  f2: fix: search pagination (files: [SearchService.java])\n  f3: feat: add filters (files: [FilterService.java])\n\nCherry-picking f1 to main...\n  Applied: feat: add search\n  New file: SearchService.java\n\nCherry-picking f3 to main...\n  Applied: feat: add filters\n  New file: FilterService.java\n\nMain branch state:\n  Files: {UserService.java=v1, SearchService.java=search_v1, FilterService.java=filter_v1}\n  History: [init, cherry-pick: feat: add search, cherry-pick: feat: add filters]',
      hint: 'Cherry-pick применяет diff одного коммита к текущей ветке. Если файл из коммита уже существует с другим содержимым — возможен конфликт. Для простоты: если файл не существует — добавляем, если существует с тем же содержимым — пропускаем, иначе — конфликт.',
      solution: 'import java.util.*;\n\npublic class Main {\n\n    static class Commit {\n        String hash;\n        String message;\n        Map<String, String> changes; // filename → content\n\n        Commit(String hash, String message, Map<String, String> changes) {\n            this.hash = hash;\n            this.message = message;\n            this.changes = changes;\n        }\n    }\n\n    static class Branch {\n        String name;\n        Map<String, String> files = new LinkedHashMap<>();\n        List<String> history = new ArrayList<>();\n\n        Branch(String name) {\n            this.name = name;\n        }\n\n        void commit(Commit c) {\n            files.putAll(c.changes);\n            history.add(c.message);\n        }\n\n        boolean cherryPick(Commit c) {\n            System.out.println("Cherry-picking " + c.hash + " to " + name + "...");\n\n            for (var entry : c.changes.entrySet()) {\n                String file = entry.getKey();\n                String content = entry.getValue();\n\n                if (!files.containsKey(file)) {\n                    System.out.println("  New file: " + file);\n                } else if (files.get(file).equals(content)) {\n                    System.out.println("  Skipped (same content): " + file);\n                    continue;\n                } else {\n                    System.out.println("  Updated: " + file);\n                }\n                files.put(file, content);\n            }\n\n            history.add("cherry-pick: " + c.message);\n            System.out.println("  Applied: " + c.message);\n            return true;\n        }\n\n        void printState() {\n            System.out.println(name + " branch state:");\n            System.out.println("  Files: " + files);\n            System.out.println("  History: " + history);\n        }\n    }\n\n    public static void main(String[] args) {\n        // Create feature branch commits\n        Commit f1 = new Commit("f1", "feat: add search",\n            Map.of("SearchService.java", "search_v1"));\n        Commit f2 = new Commit("f2", "fix: search pagination",\n            Map.of("SearchService.java", "search_v2"));\n        Commit f3 = new Commit("f3", "feat: add filters",\n            Map.of("FilterService.java", "filter_v1"));\n\n        System.out.println("Feature branch commits:");\n        System.out.println("  f1: " + f1.message + " (files: " + new ArrayList<>(f1.changes.keySet()) + ")");\n        System.out.println("  f2: " + f2.message + " (files: " + new ArrayList<>(f2.changes.keySet()) + ")");\n        System.out.println("  f3: " + f3.message + " (files: " + new ArrayList<>(f3.changes.keySet()) + ")");\n\n        // Main branch\n        Branch main = new Branch("main");\n        main.commit(new Commit("m1", "init", Map.of("UserService.java", "v1")));\n\n        // Cherry-pick f1 and f3 (skip f2)\n        System.out.println();\n        main.cherryPick(f1);\n        System.out.println();\n        main.cherryPick(f3);\n\n        System.out.println();\n        main.printState();\n    }\n}',
      explanation: 'Cherry-pick в Git применяет diff конкретного коммита к текущей ветке, создавая новый коммит с тем же изменением, но другим хешем (другой parent). Это позволяет выборочно переносить изменения между ветками без полного merge. Часто используется для backport-а hotfix-ов из main в release-ветки.'
    },
    {
      id: 9,
      title: 'Tag Manager',
      type: 'practice',
      difficulty: 'easy',
      description: 'Реализуйте систему управления тегами Git с поддержкой semantic versioning: создание тегов, вывод списка, определение следующей версии по типу изменений.',
      requirements: [
        'Класс Tag с полями name, commitHash, message, isAnnotated',
        'Класс TagManager с методами createTag(), listTags(), deleteTag()',
        'Метод getNextVersion(currentVersion, changeType) — определяет следующую SemVer версию',
        'changeType: MAJOR, MINOR, PATCH',
        'Вывести список тегов, отсортированных по версии'
      ],
      expectedOutput: 'Creating tags:\n  Created: v1.0.0 at commit abc1111 (annotated: "Release 1.0.0")\n  Created: v1.1.0 at commit abc2222 (annotated: "Release 1.1.0")\n  Created: v1.1.1 at commit abc3333 (annotated: "Hotfix 1.1.1")\n  Created: v1.2.0 at commit abc4444 (annotated: "Release 1.2.0")\n\nAll tags:\n  v1.0.0 → abc1111 "Release 1.0.0"\n  v1.1.0 → abc2222 "Release 1.1.0"\n  v1.1.1 → abc3333 "Hotfix 1.1.1"\n  v1.2.0 → abc4444 "Release 1.2.0"\n\nNext versions from v1.2.0:\n  PATCH → v1.2.1\n  MINOR → v1.3.0\n  MAJOR → v2.0.0\n\nLatest version: v1.2.0',
      hint: 'Парсите версию через split("."): major.minor.patch. PATCH → patch++, MINOR → minor++, patch=0, MAJOR → major++, minor=0, patch=0.',
      solution: 'import java.util.*;\n\npublic class Main {\n\n    static class Tag {\n        String name;\n        String commitHash;\n        String message;\n\n        Tag(String name, String commitHash, String message) {\n            this.name = name;\n            this.commitHash = commitHash;\n            this.message = message;\n        }\n\n        int[] parseVersion() {\n            String ver = name.startsWith("v") ? name.substring(1) : name;\n            String[] parts = ver.split("\\\\.");\n            return new int[]{\n                Integer.parseInt(parts[0]),\n                Integer.parseInt(parts[1]),\n                Integer.parseInt(parts[2])\n            };\n        }\n    }\n\n    static class TagManager {\n        private final List<Tag> tags = new ArrayList<>();\n\n        void createTag(String name, String commitHash, String message) {\n            tags.add(new Tag(name, commitHash, message));\n            System.out.println("  Created: " + name + " at commit " + commitHash +\n                " (annotated: \\"" + message + "\\")");\n        }\n\n        void listTags() {\n            System.out.println("\\nAll tags:");\n            List<Tag> sorted = new ArrayList<>(tags);\n            sorted.sort((a, b) -> {\n                int[] va = a.parseVersion();\n                int[] vb = b.parseVersion();\n                for (int i = 0; i < 3; i++) {\n                    if (va[i] != vb[i]) return va[i] - vb[i];\n                }\n                return 0;\n            });\n            for (Tag tag : sorted) {\n                System.out.println("  " + tag.name + " → " + tag.commitHash +\n                    " \\"" + tag.message + "\\"");\n            }\n        }\n\n        String getNextVersion(String current, String changeType) {\n            String ver = current.startsWith("v") ? current.substring(1) : current;\n            String[] parts = ver.split("\\\\.");\n            int major = Integer.parseInt(parts[0]);\n            int minor = Integer.parseInt(parts[1]);\n            int patch = Integer.parseInt(parts[2]);\n\n            switch (changeType) {\n                case "MAJOR": major++; minor = 0; patch = 0; break;\n                case "MINOR": minor++; patch = 0; break;\n                case "PATCH": patch++; break;\n            }\n            return "v" + major + "." + minor + "." + patch;\n        }\n\n        String getLatestVersion() {\n            return tags.stream()\n                .max((a, b) -> {\n                    int[] va = a.parseVersion();\n                    int[] vb = b.parseVersion();\n                    for (int i = 0; i < 3; i++) {\n                        if (va[i] != vb[i]) return va[i] - vb[i];\n                    }\n                    return 0;\n                })\n                .map(t -> t.name)\n                .orElse("v0.0.0");\n        }\n    }\n\n    public static void main(String[] args) {\n        TagManager manager = new TagManager();\n\n        System.out.println("Creating tags:");\n        manager.createTag("v1.0.0", "abc1111", "Release 1.0.0");\n        manager.createTag("v1.1.0", "abc2222", "Release 1.1.0");\n        manager.createTag("v1.1.1", "abc3333", "Hotfix 1.1.1");\n        manager.createTag("v1.2.0", "abc4444", "Release 1.2.0");\n\n        manager.listTags();\n\n        System.out.println("\\nNext versions from v1.2.0:");\n        System.out.println("  PATCH → " + manager.getNextVersion("v1.2.0", "PATCH"));\n        System.out.println("  MINOR → " + manager.getNextVersion("v1.2.0", "MINOR"));\n        System.out.println("  MAJOR → " + manager.getNextVersion("v1.2.0", "MAJOR"));\n\n        System.out.println("\\nLatest version: " + manager.getLatestVersion());\n    }\n}',
      explanation: 'Semantic Versioning (MAJOR.MINOR.PATCH) в сочетании с git tags позволяет чётко версионировать релизы. MAJOR — несовместимые изменения, MINOR — новая функциональность, PATCH — исправления. Автоматическое определение следующей версии на основе типа изменений упрощает release workflow.'
    },
    {
      id: 10,
      title: 'Interactive Rebase Simulator',
      type: 'practice',
      difficulty: 'medium',
      description: 'Реализуйте симулятор interactive rebase: pick, squash, reword, drop, reorder коммитов перед push.',
      requirements: [
        'Класс Commit с полями hash, message',
        'Класс InteractiveRebase с методами pick(), squash(), reword(), drop(), fixup()',
        'Метод execute(commits, actions) — применяет действия к списку коммитов',
        'squash — объединяет коммит с предыдущим (сообщения объединяются)',
        'fixup — объединяет коммит с предыдущим (сообщение предыдущего сохраняется)',
        'Продемонстрировать rebase с разными действиями'
      ],
      expectedOutput: 'Original commits:\n  abc111 feat: add user model\n  abc222 fix: typo in user model\n  abc333 feat: add validation\n  abc444 WIP: temp debug code\n  abc555 feat: add user service\n\nRebase plan:\n  pick   abc111 feat: add user model\n  fixup  abc222 fix: typo in user model\n  pick   abc333 feat: add validation\n  drop   abc444 WIP: temp debug code\n  reword abc555 feat: add complete user service\n\nResult after rebase:\n  abc111 feat: add user model\n  abc333 feat: add validation\n  abc555 feat: add complete user service\n\nCommits: 5 → 3 (squashed 1, dropped 1)',
      hint: 'Пройдите по списку действий: pick — оставить как есть, squash/fixup — объединить с предыдущим, drop — пропустить, reword — изменить message. Результат — новый список коммитов.',
      solution: 'import java.util.*;\n\npublic class Main {\n\n    static class Commit {\n        String hash;\n        String message;\n\n        Commit(String hash, String message) {\n            this.hash = hash;\n            this.message = message;\n        }\n\n        Commit copy() {\n            return new Commit(hash, message);\n        }\n    }\n\n    static class RebaseAction {\n        String type; // pick, squash, fixup, drop, reword\n        String hash;\n        String newMessage; // for reword\n\n        RebaseAction(String type, String hash) {\n            this.type = type;\n            this.hash = hash;\n        }\n\n        RebaseAction(String type, String hash, String newMessage) {\n            this.type = type;\n            this.hash = hash;\n            this.newMessage = newMessage;\n        }\n    }\n\n    static List<Commit> interactiveRebase(List<Commit> commits, List<RebaseAction> actions) {\n        Map<String, Commit> commitMap = new LinkedHashMap<>();\n        for (Commit c : commits) commitMap.put(c.hash, c);\n\n        List<Commit> result = new ArrayList<>();\n        int squashed = 0, dropped = 0;\n\n        System.out.println("Rebase plan:");\n        for (RebaseAction action : actions) {\n            Commit c = commitMap.get(action.hash);\n            String displayMsg = action.newMessage != null ? action.newMessage : c.message;\n            System.out.printf("  %-6s %s %s%n", action.type, c.hash, displayMsg);\n\n            switch (action.type) {\n                case "pick":\n                    result.add(c.copy());\n                    break;\n                case "squash":\n                    if (!result.isEmpty()) {\n                        Commit prev = result.get(result.size() - 1);\n                        prev.message = prev.message + "\\n" + c.message;\n                        squashed++;\n                    }\n                    break;\n                case "fixup":\n                    // Merge with previous, keep previous message\n                    squashed++;\n                    break;\n                case "drop":\n                    dropped++;\n                    break;\n                case "reword":\n                    Commit reworded = c.copy();\n                    reworded.message = action.newMessage;\n                    result.add(reworded);\n                    break;\n            }\n        }\n\n        System.out.println("\\nResult after rebase:");\n        for (Commit c : result) {\n            System.out.println("  " + c.hash + " " + c.message);\n        }\n        System.out.println("\\nCommits: " + commits.size() + " → " + result.size() +\n            " (squashed " + squashed + ", dropped " + dropped + ")");\n\n        return result;\n    }\n\n    public static void main(String[] args) {\n        List<Commit> commits = List.of(\n            new Commit("abc111", "feat: add user model"),\n            new Commit("abc222", "fix: typo in user model"),\n            new Commit("abc333", "feat: add validation"),\n            new Commit("abc444", "WIP: temp debug code"),\n            new Commit("abc555", "feat: add user service")\n        );\n\n        System.out.println("Original commits:");\n        for (Commit c : commits) {\n            System.out.println("  " + c.hash + " " + c.message);\n        }\n        System.out.println();\n\n        List<RebaseAction> actions = List.of(\n            new RebaseAction("pick", "abc111"),\n            new RebaseAction("fixup", "abc222"),\n            new RebaseAction("pick", "abc333"),\n            new RebaseAction("drop", "abc444"),\n            new RebaseAction("reword", "abc555", "feat: add complete user service")\n        );\n\n        interactiveRebase(commits, actions);\n    }\n}',
      explanation: 'Interactive rebase (git rebase -i) — мощный инструмент для очистки истории перед push. pick — оставить, squash — объединить с предыдущим (сообщения склеиваются), fixup — объединить (сохранить сообщение предыдущего), drop — удалить, reword — изменить сообщение. Позволяет превратить 10 "WIP" коммитов в 3 чистых атомарных коммита.'
    }
  ]
}
