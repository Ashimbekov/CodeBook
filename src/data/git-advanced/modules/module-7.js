export default {
  id: 7,
  title: 'Git Hooks',
  description: 'Автоматизация с Git hooks: pre-commit, commit-msg, pre-push, server-side hooks, Husky и lint-staged для Java-проектов.',
  lessons: [
    {
      id: 1,
      title: 'Введение в Git Hooks',
      type: 'theory',
      content: [
        { type: 'text', value: 'Git hooks — скрипты, автоматически запускающиеся при определённых событиях Git. Они позволяют автоматизировать проверки кода, форматирование, валидацию коммитов и многое другое. Hooks — первая линия защиты качества кода.' },
        { type: 'heading', value: 'Типы хуков' },
        { type: 'code', language: 'bash', value: '# Хуки хранятся в .git/hooks/\nls .git/hooks/\n# applypatch-msg.sample\n# commit-msg.sample\n# pre-commit.sample\n# pre-push.sample\n# pre-rebase.sample\n# prepare-commit-msg.sample\n# ...\n\n# Два типа хуков:\n\n# ═══ КЛИЕНТСКИЕ (на машине разработчика) ═══\n# pre-commit        — перед созданием коммита\n# prepare-commit-msg — шаблон сообщения коммита\n# commit-msg        — валидация сообщения коммита\n# post-commit       — после создания коммита (нотификации)\n# pre-push          — перед push (тесты)\n# pre-rebase        — перед rebase\n# post-checkout     — после checkout/switch\n# post-merge        — после merge\n\n# ═══ СЕРВЕРНЫЕ (на Git-сервере) ═══\n# pre-receive       — перед принятием push\n# update            — проверка каждой ветки при push\n# post-receive      — после принятия push (деплой, CI)\n\n# Создание хука:\n# 1. Создать файл без .sample\n# 2. Сделать исполняемым (chmod +x)\n# 3. Exit code 0 = OK, != 0 = отменить операцию\n\n# Пример минимального pre-commit:\ncat > .git/hooks/pre-commit << \'EOF\'\n#!/bin/bash\necho "Running pre-commit checks..."\nexit 0  # 0 = разрешить коммит\nEOF\nchmod +x .git/hooks/pre-commit' },
        { type: 'tip', value: 'Клиентские хуки НЕ копируются при git clone. Для распространения в команде используйте core.hooksPath, Husky или pre-commit framework.' }
      ]
    },
    {
      id: 2,
      title: 'Pre-commit hook для Java',
      type: 'theory',
      content: [
        { type: 'text', value: 'Pre-commit hook запускается ПЕРЕД созданием коммита. Если хук вернёт ненулевой exit code — коммит отменяется. Идеально для: проверки форматирования, линтеры, поиск секретов, компиляция.' },
        { type: 'heading', value: 'Pre-commit hook для Java/Maven' },
        { type: 'code', language: 'bash', value: '#!/bin/bash\n# .git/hooks/pre-commit\nset -e\n\necho "========================================"\necho "Running pre-commit checks..."\necho "========================================"\n\n# 1. Проверка форматирования (Spotless/Checkstyle)\necho ">> Checking code format..."\nmvn spotless:check -q 2>/dev/null\nif [ $? -ne 0 ]; then\n    echo "ERROR: Code not formatted!"\n    echo "Run: mvn spotless:apply"\n    exit 1\nfi\n\n# 2. Компиляция\necho ">> Compiling..."\nmvn compile -q 2>/dev/null\nif [ $? -ne 0 ]; then\n    echo "ERROR: Compilation failed!"\n    exit 1\nfi\n\n# 3. Проверка на секреты в staged файлах\necho ">> Checking for secrets..."\nPATTERNS="password\\s*=\\s*[\\\"\\x27]|api[_-]?key\\s*=\\s*[\\\"\\x27]|secret\\s*=\\s*[\\\"\\x27]"\nif git diff --cached --diff-filter=ACM | grep -iE "$PATTERNS" > /dev/null 2>&1; then\n    echo "ERROR: Possible secrets found in staged files!"\n    echo "Review your changes: git diff --cached"\n    exit 1\nfi\n\n# 4. Проверка размера файлов (> 5MB)\necho ">> Checking file sizes..."\nMAX_SIZE=5242880\nfor file in $(git diff --cached --name-only --diff-filter=ACM); do\n    if [ -f "$file" ]; then\n        size=$(wc -c < "$file")\n        if [ "$size" -gt "$MAX_SIZE" ]; then\n            echo "ERROR: $file is too large ($size bytes > $MAX_SIZE)"\n            exit 1\n        fi\n    fi\ndone\n\n# 5. Запрет коммита в main (защита от случайности)\nBRANCH=$(git rev-parse --abbrev-ref HEAD)\nif [ "$BRANCH" = "main" ] || [ "$BRANCH" = "master" ]; then\n    echo "ERROR: Direct commits to $BRANCH are not allowed!"\n    echo "Create a feature branch: git checkout -b feature/my-feature"\n    exit 1\nfi\n\necho "All pre-commit checks passed!"' },
        { type: 'heading', value: 'Быстрый pre-commit (только staged файлы)' },
        { type: 'code', language: 'bash', value: '#!/bin/bash\n# Оптимизированный pre-commit: проверяет ТОЛЬКО staged файлы\nset -e\n\n# Получаем список staged Java-файлов\nSTAGED_JAVA=$(git diff --cached --name-only --diff-filter=ACM | grep "\\.java$" || true)\n\nif [ -z "$STAGED_JAVA" ]; then\n    echo "No Java files staged, skipping checks."\n    exit 0\nfi\n\necho "Checking $(echo "$STAGED_JAVA" | wc -l) Java files..."\n\n# Проверка TODO/FIXME (предупреждение, не блокирует)\nfor file in $STAGED_JAVA; do\n    TODOS=$(git diff --cached -- "$file" | grep "^+" | grep -i "TODO\\|FIXME" || true)\n    if [ -n "$TODOS" ]; then\n        echo "WARNING: TODO/FIXME found in $file"\n    fi\ndone\n\n# Проверка System.out.println (не должен быть в коммите)\nfor file in $STAGED_JAVA; do\n    if git diff --cached -- "$file" | grep "^+" | grep -q "System\\.out\\.print"; then\n        echo "ERROR: System.out.println found in $file"\n        echo "Use a logger (SLF4J) instead!"\n        exit 1\n    fi\ndone\n\n# Быстрая компиляция\nmvn compile -q -pl . 2>/dev/null || {\n    echo "ERROR: Compilation failed!"\n    exit 1\n}\n\necho "Pre-commit checks passed!"' },
        { type: 'note', value: 'Pre-commit hook должен быть БЫСТРЫМ (< 10 секунд). Длинные проверки (полные тесты, Sonar) лучше выносить в pre-push или CI. Разработчик не должен ждать 5 минут при каждом коммите.' }
      ]
    },
    {
      id: 3,
      title: 'Commit-msg hook: Conventional Commits',
      type: 'theory',
      content: [
        { type: 'text', value: 'Commit-msg hook валидирует сообщение коммита. Обеспечивает единый формат в команде, что критично для автоматической генерации CHANGELOG, semantic versioning и читаемости истории.' },
        { type: 'heading', value: 'Валидация Conventional Commits' },
        { type: 'code', language: 'bash', value: '#!/bin/bash\n# .git/hooks/commit-msg\n\nCOMMIT_MSG_FILE=$1\nCOMMIT_MSG=$(cat "$COMMIT_MSG_FILE")\nFIRST_LINE=$(head -1 "$COMMIT_MSG_FILE")\n\n# Conventional Commits format:\n# type(scope): description\n# Допустимые типы:\n# feat, fix, docs, style, refactor, perf, test, build, ci, chore, revert\n\nPATTERN="^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\\([a-z0-9-]+\\))?(!)?: .{1,72}$"\n\n# Проверяем первую строку\nif ! echo "$FIRST_LINE" | grep -qE "$PATTERN"; then\n    echo ""\n    echo "ERROR: Invalid commit message format!"\n    echo ""\n    echo "Your message:"\n    echo "  $FIRST_LINE"\n    echo ""\n    echo "Expected format:"\n    echo "  type(scope): description"\n    echo ""\n    echo "Examples:"\n    echo "  feat(auth): add JWT token refresh"\n    echo "  fix(payment): prevent double charge"\n    echo "  refactor(user): extract UserMapper class"\n    echo "  test(order): add integration tests"\n    echo "  docs(api): update Swagger documentation"\n    echo "  chore(deps): upgrade Spring Boot to 3.2"\n    echo ""\n    echo "Types: feat, fix, docs, style, refactor, perf, test, build, ci, chore, revert"\n    echo "Breaking change: add ! before : (e.g., feat(api)!: change response format)"\n    echo ""\n    exit 1\nfi\n\n# Проверка длины первой строки (max 72 символа)\nLENGTH=${#FIRST_LINE}\nif [ "$LENGTH" -gt 72 ]; then\n    echo "ERROR: First line too long ($LENGTH chars, max 72)"\n    exit 1\nfi\n\n# Проверка: не начинается с заглавной после :\nDESC=$(echo "$FIRST_LINE" | sed \'s/^[^:]*: //\')\nif echo "$DESC" | grep -q "^[A-Z]"; then\n    echo "WARNING: Description should start with lowercase: $DESC"\n    # Не блокируем, только предупреждаем\nfi\n\nexit 0' },
        { type: 'heading', value: 'Шаблон сообщения (prepare-commit-msg)' },
        { type: 'code', language: 'bash', value: '#!/bin/bash\n# .git/hooks/prepare-commit-msg\n# Автоматически добавляет номер Jira-тикета из имени ветки\n\nCOMMIT_MSG_FILE=$1\nCOMMIT_SOURCE=$2\n\n# Только для новых коммитов (не merge, amend и т.д.)\nif [ "$COMMIT_SOURCE" = "merge" ] || [ "$COMMIT_SOURCE" = "squash" ]; then\n    exit 0\nfi\n\n# Извлекаем номер тикета из имени ветки\n# feature/JIRA-123-user-auth → JIRA-123\nBRANCH=$(git rev-parse --abbrev-ref HEAD)\nTICKET=$(echo "$BRANCH" | grep -oE "[A-Z]+-[0-9]+" | head -1)\n\nif [ -n "$TICKET" ]; then\n    # Добавляем тикет в конец сообщения, если его ещё нет\n    if ! grep -q "$TICKET" "$COMMIT_MSG_FILE"; then\n        # Дописываем в начало\n        CURRENT=$(cat "$COMMIT_MSG_FILE")\n        echo "$CURRENT [$TICKET]" > "$COMMIT_MSG_FILE"\n    fi\nfi\n\n# Пример:\n# Ветка: feature/JIRA-456-payment-api\n# git commit -m "feat(payment): add Stripe integration"\n# Результат: "feat(payment): add Stripe integration [JIRA-456]"\n\n# Альтернатива: шаблон коммита\ngit config commit.template .gitmessage\n# .gitmessage:\n# type(scope): description\n#\n# Body (what and why, not how)\n#\n# Refs: JIRA-XXX' },
        { type: 'tip', value: 'Commit-msg hook — обязательный minimum для командной работы. Без единого формата коммитов git log превращается в хаос: "fix", "update", "asdfgh". С conventional commits — автоматический CHANGELOG и понятная история.' }
      ]
    },
    {
      id: 4,
      title: 'Pre-push hook и серверные хуки',
      type: 'theory',
      content: [
        { type: 'text', value: 'Pre-push hook запускается перед отправкой кода на сервер. Идеален для запуска тестов. Серверные хуки (pre-receive, update) — защита на стороне сервера, которую нельзя обойти.' },
        { type: 'heading', value: 'Pre-push hook' },
        { type: 'code', language: 'bash', value: '#!/bin/bash\n# .git/hooks/pre-push\n# Запускает тесты перед push\n\nset -e\n\nREMOTE="$1"\nURL="$2"\n\necho "Running pre-push checks before pushing to $REMOTE..."\n\n# 1. Запуск unit-тестов\necho ">> Running tests..."\nmvn test -q 2>/dev/null\nif [ $? -ne 0 ]; then\n    echo "ERROR: Tests failed! Fix tests before pushing."\n    exit 1\nfi\n\n# 2. Проверка покрытия (JaCoCo)\necho ">> Checking code coverage..."\nmvn jacoco:check -q 2>/dev/null\nif [ $? -ne 0 ]; then\n    echo "WARNING: Code coverage below threshold"\n    # Не блокируем, только предупреждаем\nfi\n\n# 3. Защита от push в main/master\nwhile read local_ref local_sha remote_ref remote_sha; do\n    if echo "$remote_ref" | grep -qE "refs/heads/(main|master)$"; then\n        echo "ERROR: Direct push to main/master is not allowed!"\n        echo "Create a PR instead."\n        exit 1\n    fi\ndone\n\n# 4. Проверка на большие файлы в новых коммитах\nwhile read local_ref local_sha remote_ref remote_sha; do\n    if [ "$remote_sha" = "0000000000000000000000000000000000000000" ]; then\n        RANGE="$local_sha"\n    else\n        RANGE="$remote_sha..$local_sha"\n    fi\n    \n    LARGE_FILES=$(git diff --name-only --diff-filter=ACM "$RANGE" | while read f; do\n        if [ -f "$f" ]; then\n            size=$(wc -c < "$f")\n            if [ "$size" -gt 10485760 ]; then  # 10MB\n                echo "$f ($size bytes)"\n            fi\n        fi\n    done)\n    \n    if [ -n "$LARGE_FILES" ]; then\n        echo "ERROR: Large files detected:"\n        echo "$LARGE_FILES"\n        echo "Use Git LFS for large files."\n        exit 1\n    fi\ndone\n\necho "Pre-push checks passed! Pushing..."' },
        { type: 'heading', value: 'Серверные хуки' },
        { type: 'code', language: 'bash', value: '# Серверные хуки нельзя обойти (в отличие от клиентских --no-verify)\n\n# GitHub: нет прямых server hooks, но есть:\n# - Branch Protection Rules (UI)\n# - GitHub Actions (CI/CD)\n# - Required Status Checks\n\n# GitLab: Push Rules (Settings → Repository → Push Rules)\n# - Reject unsigned commits\n# - Commit message regex: ^(feat|fix|docs)\\(\\w+\\): .+$\n# - Branch name regex: ^(feature|fix|hotfix)/.*$\n# - Maximum file size\n# - Prevent secrets\n\n# Self-hosted Git (Gitea, Gitolite):\n# pre-receive hook на сервере:\n#!/bin/bash\n# /path/to/repo.git/hooks/pre-receive\nwhile read oldrev newrev refname; do\n    # Проверка commit messages\n    for commit in $(git rev-list $oldrev..$newrev); do\n        msg=$(git log --format=%s -1 $commit)\n        if ! echo "$msg" | grep -qE "^(feat|fix|docs|refactor|test|chore)"; then\n            echo "ERROR: Invalid commit message: $msg"\n            echo "Use Conventional Commits format"\n            exit 1\n        fi\n    done\n    \n    # Защита main от force push\n    if echo "$refname" | grep -q "refs/heads/main"; then\n        FORCED=$(git merge-base --is-ancestor $oldrev $newrev 2>/dev/null; echo $?)\n        if [ "$FORCED" -ne 0 ]; then\n            echo "ERROR: Force push to main is not allowed!"\n            exit 1\n        fi\n    fi\ndone' },
        { type: 'warning', value: 'Клиентские хуки можно обойти: git commit --no-verify. Для жёсткого enforcement используйте серверные хуки или CI/CD проверки. Клиентские хуки — это помощь разработчику, а не защита.' }
      ]
    },
    {
      id: 5,
      title: 'Распространение хуков в команде',
      type: 'theory',
      content: [
        { type: 'text', value: 'Хуки из .git/hooks/ не попадают в репозиторий. Для распространения в команде есть несколько подходов: core.hooksPath, Husky (Node.js), Maven plugins и скрипты установки.' },
        { type: 'heading', value: 'core.hooksPath' },
        { type: 'code', language: 'bash', value: '# Самый простой способ: хранить хуки в репозитории\n\n# 1. Создаём директорию для хуков\nmkdir -p .githooks\n\n# 2. Помещаем хуки туда\ncp .git/hooks/pre-commit .githooks/pre-commit\ncp .git/hooks/commit-msg .githooks/commit-msg\nchmod +x .githooks/*\n\n# 3. Указываем Git использовать эту директорию\ngit config core.hooksPath .githooks\n\n# 4. Для всей команды: добавить в Makefile/README\n# Makefile:\n# setup:\n# \tgit config core.hooksPath .githooks\n# \techo "Git hooks configured!"\n\n# Или автоматически через Maven (pom.xml):\n# <plugin>\n#   <groupId>org.codehaus.mojo</groupId>\n#   <artifactId>exec-maven-plugin</artifactId>\n#   <executions>\n#     <execution>\n#       <id>git-hooks</id>\n#       <phase>initialize</phase>\n#       <goals><goal>exec</goal></goals>\n#       <configuration>\n#         <executable>git</executable>\n#         <arguments>\n#           <argument>config</argument>\n#           <argument>core.hooksPath</argument>\n#           <argument>.githooks</argument>\n#         </arguments>\n#       </configuration>\n#     </execution>\n#   </executions>\n# </plugin>' },
        { type: 'heading', value: 'Maven Git Hooks Plugins' },
        { type: 'code', language: 'bash', value: '# git-build-hook Maven Plugin — автоматическая установка хуков\n\n# pom.xml:\n# <plugin>\n#     <groupId>com.rudikershaw.gitbuildhook</groupId>\n#     <artifactId>git-build-hook-maven-plugin</artifactId>\n#     <version>3.4.1</version>\n#     <configuration>\n#         <gitConfig>\n#             <core.hooksPath>.githooks</core.hooksPath>\n#         </gitConfig>\n#     </configuration>\n#     <executions>\n#         <execution>\n#             <goals><goal>configure</goal></goals>\n#         </execution>\n#     </executions>\n# </plugin>\n\n# При mvn compile автоматически настроит core.hooksPath\n\n# Альтернатива: maven-git-code-format (Spotless + pre-commit)\n# <plugin>\n#     <groupId>com.cosium.code</groupId>\n#     <artifactId>git-code-format-maven-plugin</artifactId>\n#     <version>5.3</version>\n#     <executions>\n#         <execution>\n#             <goals>\n#                 <goal>install-hooks</goal>\n#             </goals>\n#         </execution>\n#     </executions>\n# </plugin>\n\n# При mvn verify: устанавливает pre-commit hook\n# который автоматически форматирует код перед коммитом\n\n# Структура проекта:\n# my-project/\n# ├── .githooks/\n# │   ├── pre-commit\n# │   ├── commit-msg\n# │   └── pre-push\n# ├── pom.xml (настроен core.hooksPath)\n# └── src/' },
        { type: 'tip', value: 'Для Java-проектов лучший подход: .githooks/ + Maven plugin (git-build-hook). При первом mvn compile хуки автоматически настраиваются. Разработчику не нужно ничего делать вручную.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Git Hook Manager',
      type: 'practice',
      difficulty: 'medium',
      description: 'Напишите Java-программу, которая симулирует систему Git hooks: регистрация хуков, валидация commit message по Conventional Commits, проверка staged файлов на секреты и System.out.println.',
      requirements: [
        'Интерфейс GitHook с методом execute(context) возвращающим boolean',
        'Класс CommitMsgHook — валидирует message по Conventional Commits',
        'Класс PreCommitHook — проверяет файлы на секреты и System.out.println',
        'Класс HookManager — регистрирует и запускает хуки',
        'Протестировать: валидные/невалидные commit messages, файлы с секретами'
      ],
      expectedOutput: 'Running pre-commit hooks...\n  [PreCommitHook] Checking 3 files...\n  [PreCommitHook] FAIL: Secret found in application.properties: password=secret123\n  Pre-commit: FAILED\n\nRunning pre-commit hooks...\n  [PreCommitHook] Checking 2 files...\n  [PreCommitHook] WARNING: System.out.println found in UserService.java\n  [PreCommitHook] All checks passed\n  Pre-commit: PASSED\n\nRunning commit-msg hooks...\n  [CommitMsgHook] Validating: "fix bug"\n  [CommitMsgHook] FAIL: Invalid format. Expected: type(scope): description\n  Commit-msg: FAILED\n\nRunning commit-msg hooks...\n  [CommitMsgHook] Validating: "feat(auth): add JWT authentication"\n  [CommitMsgHook] PASSED\n  Commit-msg: PASSED',
      hint: 'Используйте regex для валидации Conventional Commits: "^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\\\\([a-z0-9-]+\\\\))?(!)?: .{1,72}$". Для поиска секретов проверяйте наличие password=, api_key= и т.д.',
      solution: 'import java.util.*;\nimport java.util.regex.Pattern;\n\npublic class Main {\n\n    interface GitHook {\n        boolean execute(Map<String, Object> context);\n        String getName();\n    }\n\n    static class CommitMsgHook implements GitHook {\n        private static final Pattern CONVENTIONAL_COMMIT = Pattern.compile(\n            "^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)" +\n            "(\\\\([a-z0-9-]+\\\\))?(!)?: .{1,72}$"\n        );\n\n        @Override\n        public String getName() { return "CommitMsgHook"; }\n\n        @Override\n        public boolean execute(Map<String, Object> context) {\n            String message = (String) context.get("commitMessage");\n            System.out.println("  [" + getName() + "] Validating: \\"" + message + "\\"");\n\n            if (!CONVENTIONAL_COMMIT.matcher(message).matches()) {\n                System.out.println("  [" + getName() + "] FAIL: Invalid format. " +\n                    "Expected: type(scope): description");\n                return false;\n            }\n\n            if (message.length() > 72) {\n                System.out.println("  [" + getName() + "] FAIL: Message too long (" +\n                    message.length() + " > 72)");\n                return false;\n            }\n\n            System.out.println("  [" + getName() + "] PASSED");\n            return true;\n        }\n    }\n\n    static class PreCommitHook implements GitHook {\n        private static final List<String> SECRET_PATTERNS = List.of(\n            "password=", "api_key=", "secret=", "token=", "AWS_SECRET"\n        );\n\n        @Override\n        public String getName() { return "PreCommitHook"; }\n\n        @Override\n        public boolean execute(Map<String, Object> context) {\n            @SuppressWarnings("unchecked")\n            Map<String, String> files = (Map<String, String>) context.get("stagedFiles");\n            System.out.println("  [" + getName() + "] Checking " + files.size() + " files...");\n\n            // Check for secrets\n            for (var entry : files.entrySet()) {\n                String filename = entry.getKey();\n                String content = entry.getValue();\n                for (String pattern : SECRET_PATTERNS) {\n                    if (content.toLowerCase().contains(pattern.toLowerCase())) {\n                        System.out.println("  [" + getName() + "] FAIL: Secret found in " +\n                            filename + ": " + findLine(content, pattern));\n                        return false;\n                    }\n                }\n            }\n\n            // Check for System.out.println (warning only)\n            for (var entry : files.entrySet()) {\n                if (entry.getKey().endsWith(".java") &&\n                    entry.getValue().contains("System.out.println")) {\n                    System.out.println("  [" + getName() +\n                        "] WARNING: System.out.println found in " + entry.getKey());\n                }\n            }\n\n            System.out.println("  [" + getName() + "] All checks passed");\n            return true;\n        }\n\n        private String findLine(String content, String pattern) {\n            for (String line : content.split("\\n")) {\n                if (line.toLowerCase().contains(pattern.toLowerCase())) {\n                    return line.trim();\n                }\n            }\n            return pattern;\n        }\n    }\n\n    static class HookManager {\n        private final Map<String, List<GitHook>> hooks = new LinkedHashMap<>();\n\n        void register(String event, GitHook hook) {\n            hooks.computeIfAbsent(event, k -> new ArrayList<>()).add(hook);\n        }\n\n        boolean run(String event, Map<String, Object> context) {\n            List<GitHook> eventHooks = hooks.getOrDefault(event, List.of());\n            if (eventHooks.isEmpty()) return true;\n\n            System.out.println("Running " + event + " hooks...");\n            for (GitHook hook : eventHooks) {\n                if (!hook.execute(context)) {\n                    System.out.println("  " + capitalize(event) + ": FAILED");\n                    return false;\n                }\n            }\n            System.out.println("  " + capitalize(event) + ": PASSED");\n            return true;\n        }\n\n        private String capitalize(String s) {\n            return s.substring(0, 1).toUpperCase() + s.substring(1);\n        }\n    }\n\n    public static void main(String[] args) {\n        HookManager manager = new HookManager();\n        manager.register("pre-commit", new PreCommitHook());\n        manager.register("commit-msg", new CommitMsgHook());\n\n        // Test 1: File with secrets\n        Map<String, Object> ctx1 = new HashMap<>();\n        ctx1.put("stagedFiles", Map.of(\n            "application.properties", "spring.datasource.password=secret123\\nserver.port=8080",\n            "UserService.java", "public class UserService {}",\n            "README.md", "# Project"\n        ));\n        manager.run("pre-commit", ctx1);\n\n        // Test 2: File with System.out.println (warning only)\n        System.out.println();\n        Map<String, Object> ctx2 = new HashMap<>();\n        ctx2.put("stagedFiles", Map.of(\n            "UserService.java", "public class UserService {\\n  System.out.println(\\"debug\\");\\n}",\n            "OrderService.java", "public class OrderService {}"\n        ));\n        manager.run("pre-commit", ctx2);\n\n        // Test 3: Invalid commit message\n        System.out.println();\n        Map<String, Object> ctx3 = new HashMap<>();\n        ctx3.put("commitMessage", "fix bug");\n        manager.run("commit-msg", ctx3);\n\n        // Test 4: Valid commit message\n        System.out.println();\n        Map<String, Object> ctx4 = new HashMap<>();\n        ctx4.put("commitMessage", "feat(auth): add JWT authentication");\n        manager.run("commit-msg", ctx4);\n    }\n}',
      explanation: 'Git hooks работают как event-driven система: каждый этап Git (commit, push, merge) генерирует событие, и зарегистрированные хуки выполняются. Если хук возвращает false (exit code != 0) — операция отменяется. CommitMsgHook использует regex для валидации Conventional Commits. PreCommitHook проверяет содержимое staged-файлов на секреты и debug-код. В реальном Git хуки — это исполняемые скрипты (bash, python, node), но принцип тот же.'
    }
  ]
}
