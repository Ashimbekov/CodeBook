export default {
  id: 9,
  title: 'Submodules и Monorepo',
  description: 'Управление зависимостями и многомодульными проектами: git submodule, git subtree, monorepo инструменты (Nx, Turborepo), преимущества и недостатки.',
  lessons: [
    {
      id: 1,
      title: 'Git Submodules: основы',
      type: 'theory',
      content: [
        { type: 'text', value: 'Git submodule — механизм включения одного Git-репозитория внутрь другого. Submodule — это ссылка на конкретный коммит внешнего репозитория. Используется для общих библиотек, shared-конфигураций и переиспользования кода между проектами.' },
        { type: 'heading', value: 'Добавление submodule' },
        { type: 'code', language: 'bash', value: '# Добавить submodule\ngit submodule add https://github.com/team/shared-lib.git libs/shared-lib\n\n# Что произошло:\n# 1. Клонирован репозиторий shared-lib в libs/shared-lib/\n# 2. Создан файл .gitmodules:\ncat .gitmodules\n# [submodule "libs/shared-lib"]\n#     path = libs/shared-lib\n#     url = https://github.com/team/shared-lib.git\n\n# 3. В index добавлена запись (gitlink — ссылка на коммит)\ngit diff --cached\n# +Subproject commit abc1234567890...\n\n# Коммитим добавление submodule\ngit add .gitmodules libs/shared-lib\ngit commit -m "feat: add shared-lib submodule"\n\n# Структура проекта:\n# my-project/\n# ├── .gitmodules          ← конфигурация submodules\n# ├── libs/\n# │   └── shared-lib/      ← submodule (отдельный .git)\n# │       ├── src/\n# │       └── pom.xml\n# ├── src/\n# └── pom.xml\n\n# Submodule с конкретной веткой:\ngit submodule add -b main https://github.com/team/config.git config/' },
        { type: 'heading', value: 'Клонирование проекта с submodules' },
        { type: 'code', language: 'bash', value: '# Клонирование без submodules (директории пустые!)\ngit clone https://github.com/team/my-project.git\nls libs/shared-lib/\n# (пусто)\n\n# Инициализация и загрузка submodules:\ngit submodule init\ngit submodule update\n\n# Или одной командой:\ngit submodule update --init\n\n# Рекурсивно (submodules внутри submodules):\ngit submodule update --init --recursive\n\n# Лучший вариант — клонировать сразу с submodules:\ngit clone --recurse-submodules https://github.com/team/my-project.git\n\n# Посмотреть состояние submodules:\ngit submodule status\n# abc1234 libs/shared-lib (v2.1.0)\n# def5678 config (heads/main)\n\n# Символы перед хешем:\n#  (пробел) — submodule на нужном коммите\n# -         — submodule не инициализирован\n# +         — submodule на другом коммите (не совпадает с index)' },
        { type: 'warning', value: 'Submodule — это ссылка на КОНКРЕТНЫЙ коммит, не на ветку. После git submodule update вы в detached HEAD. Для работы с кодом submodule нужно перейти на ветку: cd libs/shared-lib && git checkout main.' }
      ]
    },
    {
      id: 2,
      title: 'Обновление и работа с Submodules',
      type: 'theory',
      content: [
        { type: 'text', value: 'Submodules требуют явного обновления — git pull в родительском проекте НЕ обновляет submodules автоматически. Нужно отдельно обновлять submodule и коммитить новую ссылку в родительском проекте.' },
        { type: 'heading', value: 'Обновление submodule' },
        { type: 'code', language: 'bash', value: '# Обновить submodule до последнего коммита на remote\ngit submodule update --remote libs/shared-lib\n# Подтягивает latest с ветки, указанной в .gitmodules\n\n# Обновить все submodules:\ngit submodule update --remote\n\n# После обновления — закоммитить новую ссылку\ngit add libs/shared-lib\ngit commit -m "chore: update shared-lib to latest"\n\n# Обновить до конкретного тега/коммита:\ncd libs/shared-lib\ngit fetch\ngit checkout v2.2.0\ncd ../..\ngit add libs/shared-lib\ngit commit -m "chore: update shared-lib to v2.2.0"\n\n# Работа с кодом submodule:\ncd libs/shared-lib\ngit checkout main         # переходим на ветку (из detached HEAD)\ngit pull                  # подтягиваем изменения\n# ... вносим изменения ...\ngit add .\ngit commit -m "fix: update validation"\ngit push                  # пушим в shared-lib\ncd ../..\ngit add libs/shared-lib   # обновляем ссылку в родителе\ngit commit -m "chore: update shared-lib"' },
        { type: 'heading', value: 'Настройка и автоматизация' },
        { type: 'code', language: 'bash', value: '# Автоматический update при pull:\ngit config submodule.recurse true\n# Теперь git pull также обновляет submodules\n\n# Или при каждом pull:\ngit pull --recurse-submodules\n\n# Diff с учётом submodules:\ngit diff --submodule\n# Submodule libs/shared-lib abc1234..def5678:\n#   > fix: update validation\n#   > feat: add phone validation\n\n# Подробный diff:\ngit diff --submodule=diff\n# Покажет полный diff внутри submodule\n\n# Полезные alias\'ы:\ngit config alias.sdiff "diff --submodule=diff"\ngit config alias.spull "pull --recurse-submodules"\ngit config alias.supdate "submodule update --remote --merge"\n\n# foreach — выполнить команду во всех submodules:\ngit submodule foreach "git checkout main && git pull"\ngit submodule foreach "git status"\ngit submodule foreach "mvn clean install -q"' },
        { type: 'tip', value: 'Настройте git config submodule.recurse true глобально. Без этого после каждого pull/checkout нужно помнить про submodule update, иначе submodules будут рассинхронизированы.' }
      ]
    },
    {
      id: 3,
      title: 'Git Subtree: альтернатива submodules',
      type: 'theory',
      content: [
        { type: 'text', value: 'Git subtree — альтернативный подход к включению внешних репозиториев. В отличие от submodule, subtree копирует код в основной репозиторий. Не требует специальных команд при clone и проще для командной работы.' },
        { type: 'heading', value: 'Git subtree add и pull' },
        { type: 'code', language: 'bash', value: '# Добавить subtree (внешний репозиторий как поддиректорию)\ngit subtree add --prefix=libs/shared-lib \\\n    https://github.com/team/shared-lib.git main --squash\n\n# --prefix=libs/shared-lib  — куда поместить код\n# main                      — ветка внешнего репо\n# --squash                  — сжать всю историю в один коммит\n\n# Результат: код shared-lib скопирован в libs/shared-lib/\n# НО это обычные файлы, не submodule!\n# Нет .gitmodules, нет отдельного .git\n\nls libs/shared-lib/\n# src/  pom.xml  README.md\n\n# Клонирование проекта — всё работает сразу:\ngit clone https://github.com/team/my-project.git\n# Все файлы на месте, никаких дополнительных команд!\n\n# Обновить subtree (подтянуть изменения из внешнего репо):\ngit subtree pull --prefix=libs/shared-lib \\\n    https://github.com/team/shared-lib.git main --squash\n\n# Удобнее добавить remote:\ngit remote add shared-lib https://github.com/team/shared-lib.git\ngit subtree pull --prefix=libs/shared-lib shared-lib main --squash' },
        { type: 'heading', value: 'Subtree push и split' },
        { type: 'code', language: 'bash', value: '# Push изменений обратно во внешний репо\n# Если вы изменили файлы в libs/shared-lib/ и хотите\n# отправить изменения в оригинальный shared-lib репо:\n\ngit subtree push --prefix=libs/shared-lib \\\n    shared-lib main\n\n# Subtree split — извлечь историю поддиректории в отдельную ветку\ngit subtree split --prefix=libs/shared-lib -b shared-lib-branch\n# Создаёт ветку shared-lib-branch с историей только libs/shared-lib/\n\n# ═══ Submodule vs Subtree ═══\n#\n# Submodule:\n# + Чёткое разделение (отдельный .git)\n# + Можно зафиксировать точную версию\n# + Не дублирует историю\n# - Требует git submodule init/update\n# - Detached HEAD при update\n# - Сложнее для новичков\n#\n# Subtree:\n# + Работает сразу при clone (обычные файлы)\n# + Проще workflow\n# + Нет detached HEAD\n# - Смешивает историю с основным проектом\n# - Нет привязки к конкретной версии\n# - Merge при pull может быть сложным\n#\n# Когда что:\n# Submodule → большие общие библиотеки, строгий version control\n# Subtree → маленькие shared-утилиты, простой workflow' },
        { type: 'note', value: 'Subtree проще в повседневной работе: git clone — и всё работает. Но submodule даёт больше контроля над версиями. Для Java-проектов чаще используют Maven/Gradle зависимости вместо submodule/subtree.' }
      ]
    },
    {
      id: 4,
      title: 'Monorepo: концепция и инструменты',
      type: 'theory',
      content: [
        { type: 'text', value: 'Monorepo — один репозиторий для множества проектов/сервисов. Используется в Google, Meta, Microsoft. Обеспечивает атомарные изменения, единые стандарты и упрощает переиспользование кода. Для эффективной работы нужны специальные инструменты.' },
        { type: 'heading', value: 'Monorepo vs Multirepo' },
        { type: 'code', language: 'bash', value: '# ═══ Multirepo (polyrepo) ═══\n# Каждый сервис — отдельный репозиторий\n# team/\n# ├── user-service/     (.git)\n# ├── payment-service/  (.git)\n# ├── order-service/    (.git)\n# ├── shared-lib/       (.git)\n# └── frontend/         (.git)\n\n# ═══ Monorepo ═══\n# Все сервисы в одном репозитории\n# team-platform/  (.git)\n# ├── services/\n# │   ├── user-service/\n# │   ├── payment-service/\n# │   └── order-service/\n# ├── libs/\n# │   ├── shared-models/\n# │   ├── common-utils/\n# │   └── test-helpers/\n# ├── apps/\n# │   └── frontend/\n# ├── tools/\n# │   └── scripts/\n# └── pom.xml (parent POM)\n\n# Преимущества monorepo:\n# + Атомарные изменения (изменить API + все клиенты = 1 PR)\n# + Единые стандарты (один .editorconfig, один CI)\n# + Легко переиспользовать код (без версионирования)\n# + Один git log для всей истории\n# + Рефакторинг через весь проект\n\n# Недостатки:\n# - Большой размер репозитория\n# - Медленный clone/pull/status\n# - Сложная CI/CD (что тестировать при изменении?)\n# - Права доступа (все видят весь код)\n# - Нужны специальные инструменты' },
        { type: 'heading', value: 'Git Sparse Checkout для monorepo' },
        { type: 'code', language: 'bash', value: '# Sparse checkout — клонировать только нужные директории\n# Решает проблему "monorepo слишком большой"\n\n# Клонирование без файлов:\ngit clone --filter=blob:none --sparse \\\n    https://github.com/team/platform.git\ncd platform\n\n# Подключаем только нужные директории:\ngit sparse-checkout set services/user-service libs/shared-models\n\n# Теперь на диске только:\n# platform/\n# ├── services/\n# │   └── user-service/   ← наш сервис\n# └── libs/\n#     └── shared-models/  ← общие модели\n\n# Добавить ещё директорию:\ngit sparse-checkout add services/payment-service\n\n# Посмотреть, что подключено:\ngit sparse-checkout list\n# services/user-service\n# services/payment-service\n# libs/shared-models\n\n# Отключить sparse checkout (скачать всё):\ngit sparse-checkout disable\n\n# Partial clone + sparse checkout = быстрый старт\n# Вместо клонирования 10 GB монорепо,\n# скачиваем только 200 MB нужного сервиса' },
        { type: 'tip', value: 'Для Java monorepo используйте Maven multi-module project (parent POM) + git sparse-checkout. Разработчик клонирует только свой сервис и shared-библиотеки, а не весь монорепо.' }
      ]
    },
    {
      id: 5,
      title: 'Инструменты Monorepo: Nx, Turborepo, Bazel',
      type: 'theory',
      content: [
        { type: 'text', value: 'Специализированные инструменты решают главную проблему monorepo: "что пересобирать и тестировать при изменении?". Они анализируют граф зависимостей, кешируют результаты и запускают только affected задачи.' },
        { type: 'heading', value: 'Инструменты и их возможности' },
        { type: 'code', language: 'bash', value: '# ═══ Nx (nrwl.io) ═══\n# Изначально для Angular/React, теперь поддерживает Java/любые проекты\n\n# Граф зависимостей:\nnpx nx graph\n# Показывает визуально, какие проекты зависят друг от друга\n\n# Affected — запустить тесты ТОЛЬКО для изменённых проектов:\nnpx nx affected:test\n# Анализирует git diff и запускает тесты только для:\n# 1. Изменённых проектов\n# 2. Проектов, которые зависят от изменённых\n\n# Кеширование — не пересобирать то, что не изменилось:\nnpx nx build user-service\n# [cached] — если исходники не менялись, берёт из кеша\n\n# ═══ Turborepo (turbo.build) ═══\n# Быстрый, простой, от Vercel\n\n# turbo.json:\n# {\n#   "pipeline": {\n#     "build": { "dependsOn": ["^build"], "outputs": ["target/**"] },\n#     "test": { "dependsOn": ["build"] },\n#     "lint": {}\n#   }\n# }\n\nturbo run build          # сборка с кешированием\nturbo run test --filter=user-service  # тесты одного сервиса\nturbo run build test lint --parallel  # параллельный запуск\n\n# ═══ Bazel (bazel.build) ═══\n# Используется в Google. Поддерживает Java, C++, Python и др.\n# Максимальная производительность, но сложная настройка\n\nbazel build //services/user-service:all\nbazel test //services/user-service:all\nbazel query "rdeps(//..., //libs/shared-models)"  # кто зависит' },
        { type: 'heading', value: 'Maven/Gradle для Java monorepo' },
        { type: 'code', language: 'bash', value: '# ═══ Maven Multi-Module ═══\n# Самый распространённый вариант для Java monorepo\n\n# Родительский pom.xml:\n# <modules>\n#   <module>services/user-service</module>\n#   <module>services/payment-service</module>\n#   <module>libs/shared-models</module>\n# </modules>\n\n# Сборка всего:\nmvn clean install\n\n# Сборка одного модуля с зависимостями:\nmvn clean install -pl services/user-service -am\n# -pl = project list (какие модули)\n# -am = also make (собрать зависимости)\n\n# Тесты только изменённых модулей (с плагином):\nmvn test -pl services/user-service\n\n# ═══ Gradle Composite Build ═══\n# settings.gradle.kts:\n# include("services:user-service")\n# include("services:payment-service")\n# include("libs:shared-models")\n\n# Сборка:\n./gradlew :services:user-service:build\n./gradlew :services:user-service:test\n\n# Build cache (аналог Nx/Turborepo):\n./gradlew build --build-cache\n# Не пересобирает модули, если исходники не менялись\n\n# CI/CD для monorepo — определяем affected:\n# git diff --name-only main...HEAD | grep "services/user-service"\n# Если есть изменения → запускаем тесты user-service\n# Если нет → пропускаем' },
        { type: 'note', value: 'Для Java-проектов Maven multi-module — стандартный и проверенный выбор. Nx/Turborepo больше популярны в JS/TS экосистеме. Bazel — для очень больших проектов (1000+ разработчиков). Gradle с build cache — хороший компромисс.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Менеджер зависимостей',
      type: 'practice',
      difficulty: 'medium',
      description: 'Напишите Java-программу, которая симулирует monorepo менеджер зависимостей: граф зависимостей между модулями, определение affected модулей и порядок сборки.',
      requirements: [
        'Класс Module с полями name, dependencies (список зависимостей)',
        'Класс DependencyGraph — хранит модули и их зависимости',
        'Метод addModule(name, dependencies) — добавляет модуль',
        'Метод getAffected(changedModules) — возвращает все affected модули (прямые и транзитивные)',
        'Метод getBuildOrder() — топологическая сортировка (порядок сборки)',
        'Продемонстрировать: при изменении shared-models определить, какие сервисы нужно пересобрать'
      ],
      expectedOutput: 'Monorepo modules:\n  shared-models: []\n  common-utils: [shared-models]\n  user-service: [shared-models, common-utils]\n  payment-service: [shared-models]\n  order-service: [user-service, payment-service]\n  api-gateway: [user-service, order-service]\n\nBuild order (topological sort):\n  1. shared-models\n  2. common-utils\n  3. user-service\n  4. payment-service\n  5. order-service\n  6. api-gateway\n\nChanged: [shared-models]\nAffected modules: [shared-models, common-utils, user-service, payment-service, order-service, api-gateway]\n\nChanged: [payment-service]\nAffected modules: [payment-service, order-service, api-gateway]\n\nChanged: [api-gateway]\nAffected modules: [api-gateway]',
      hint: 'Для getAffected используйте обратный граф (reverse dependencies): если A зависит от B, то в обратном графе B → A. Обход в ширину (BFS) от изменённых модулей по обратному графу даёт все affected. Для топологической сортировки используйте алгоритм Кана (BFS с подсчётом входящих рёбер).',
      solution: 'import java.util.*;\n\npublic class Main {\n\n    static class DependencyGraph {\n        private final Map<String, List<String>> dependencies = new LinkedHashMap<>();\n        private final Map<String, List<String>> reverseDeps = new LinkedHashMap<>();\n\n        void addModule(String name, String... deps) {\n            dependencies.put(name, new ArrayList<>(Arrays.asList(deps)));\n            reverseDeps.putIfAbsent(name, new ArrayList<>());\n            for (String dep : deps) {\n                reverseDeps.computeIfAbsent(dep, k -> new ArrayList<>()).add(name);\n            }\n        }\n\n        // Получить все affected модули (прямые + транзитивные зависимости)\n        List<String> getAffected(List<String> changed) {\n            Set<String> affected = new LinkedHashSet<>();\n            Queue<String> queue = new LinkedList<>(changed);\n\n            while (!queue.isEmpty()) {\n                String module = queue.poll();\n                if (affected.add(module)) {\n                    List<String> dependents = reverseDeps.getOrDefault(module, List.of());\n                    queue.addAll(dependents);\n                }\n            }\n            return new ArrayList<>(affected);\n        }\n\n        // Топологическая сортировка (порядок сборки)\n        List<String> getBuildOrder() {\n            Map<String, Integer> inDegree = new LinkedHashMap<>();\n            for (String module : dependencies.keySet()) {\n                inDegree.put(module, 0);\n            }\n            for (var entry : dependencies.entrySet()) {\n                for (String dep : entry.getValue()) {\n                    inDegree.merge(dep, 0, Integer::sum);\n                }\n                inDegree.merge(entry.getKey(), entry.getValue().size(), Integer::sum);\n            }\n\n            Queue<String> queue = new LinkedList<>();\n            for (var entry : inDegree.entrySet()) {\n                if (entry.getValue() == 0) {\n                    queue.add(entry.getKey());\n                }\n            }\n\n            List<String> order = new ArrayList<>();\n            while (!queue.isEmpty()) {\n                String module = queue.poll();\n                order.add(module);\n                for (String dependent : reverseDeps.getOrDefault(module, List.of())) {\n                    inDegree.merge(dependent, -1, Integer::sum);\n                    if (inDegree.get(dependent) == 0) {\n                        queue.add(dependent);\n                    }\n                }\n            }\n            return order;\n        }\n\n        void printModules() {\n            System.out.println("Monorepo modules:");\n            for (var entry : dependencies.entrySet()) {\n                System.out.println("  " + entry.getKey() + ": " + entry.getValue());\n            }\n        }\n    }\n\n    public static void main(String[] args) {\n        DependencyGraph graph = new DependencyGraph();\n        graph.addModule("shared-models");\n        graph.addModule("common-utils", "shared-models");\n        graph.addModule("user-service", "shared-models", "common-utils");\n        graph.addModule("payment-service", "shared-models");\n        graph.addModule("order-service", "user-service", "payment-service");\n        graph.addModule("api-gateway", "user-service", "order-service");\n\n        graph.printModules();\n\n        // Build order\n        System.out.println("\\nBuild order (topological sort):");\n        List<String> order = graph.getBuildOrder();\n        for (int i = 0; i < order.size(); i++) {\n            System.out.println("  " + (i + 1) + ". " + order.get(i));\n        }\n\n        // Affected: shared-models changed\n        System.out.println("\\nChanged: [shared-models]");\n        System.out.println("Affected modules: " + graph.getAffected(List.of("shared-models")));\n\n        // Affected: payment-service changed\n        System.out.println("\\nChanged: [payment-service]");\n        System.out.println("Affected modules: " + graph.getAffected(List.of("payment-service")));\n\n        // Affected: api-gateway changed (leaf node)\n        System.out.println("\\nChanged: [api-gateway]");\n        System.out.println("Affected modules: " + graph.getAffected(List.of("api-gateway")));\n    }\n}',
      explanation: 'Monorepo инструменты (Nx, Turborepo, Bazel) решают ключевую задачу: определить, какие модули affected при изменении. Для этого строится граф зависимостей и обратный граф (reverse dependencies). BFS по обратному графу от изменённых модулей находит все транзитивно зависимые модули. Топологическая сортировка (алгоритм Кана) определяет правильный порядок сборки: сначала модули без зависимостей, затем те, чьи зависимости уже собраны.'
    }
  ]
}
