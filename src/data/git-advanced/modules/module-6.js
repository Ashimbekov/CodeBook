export default {
  id: 6,
  title: 'Merge конфликты',
  description: 'Стратегии разрешения merge-конфликтов: 3-way merge, merge strategies, инструменты, автоматизация и предотвращение конфликтов.',
  lessons: [
    {
      id: 1,
      title: 'Как возникают конфликты',
      type: 'theory',
      content: [
        { type: 'text', value: 'Конфликт возникает когда Git не может автоматически объединить изменения. Обычно это происходит когда два разработчика изменили одну и ту же строку файла, или один удалил файл, а другой изменил его.' },
        { type: 'heading', value: 'Типы конфликтов' },
        { type: 'code', language: 'bash', value: '# 1. CONTENT CONFLICT — одна и та же строка изменена\n# main:    port = 3000\n# feature: port = 8080\n# Результат:\n# <<<<<<< HEAD\n# port = 3000\n# =======\n# port = 8080\n# >>>>>>> feature\n\n# 2. DELETE/MODIFY — файл удалён в одной ветке, изменён в другой\n# main:    удалил UserService.java\n# feature: изменил UserService.java\n# Git: CONFLICT (modify/delete): UserService.java deleted in HEAD\n\n# 3. RENAME/RENAME — файл переименован по-разному\n# main:    User.java → Person.java\n# feature: User.java → Customer.java\n# Git: CONFLICT (rename/rename)\n\n# 4. ADD/ADD — оба создали файл с одним именем\n# main:    создал config.yaml (версия 1)\n# feature: создал config.yaml (версия 2)\n# Git: CONFLICT (add/add)\n\n# Что Git НЕ считает конфликтом:\n# ✅ Изменения в РАЗНЫХ файлах — автоматический merge\n# ✅ Изменения в РАЗНЫХ частях одного файла — автоматический merge\n# ✅ Один добавил строки, другой удалил другие строки — OK' },
        { type: 'heading', value: 'Анатомия конфликтного файла' },
        { type: 'code', language: 'bash', value: '# Маркеры конфликта в файле:\n\n# <<<<<<< HEAD\n# Код из ТЕКУЩЕЙ ветки (куда мержим)\npublic class UserService {\n    private final UserRepository userRepo;\n    \n    public User findById(Long id) {\n        return userRepo.findById(id).orElseThrow();\n    }\n# =======\n# Код из ВХОДЯЩЕЙ ветки (что мержим)\npublic class UserService {\n    private final UserRepository userRepository;\n    \n    public User findById(Long id) {\n        return userRepository.findById(id)\n            .orElseThrow(() -> new UserNotFoundException(id));\n    }\n# >>>>>>> feature/user-improvements\n\n# Разрешение: выбираем лучший вариант или комбинируем:\npublic class UserService {\n    private final UserRepository userRepository;\n    \n    public User findById(Long id) {\n        return userRepository.findById(id)\n            .orElseThrow(() -> new UserNotFoundException(id));\n    }\n\n# Удаляем ВСЕ маркеры: <<<<<<<, =======, >>>>>>>\n\ngit add src/UserService.java\ngit merge --continue\n# или: git commit' },
        { type: 'tip', value: 'В IntelliJ IDEA: правой кнопкой → Git → Resolve Conflicts. Покажет 3-панельный вид: ваши изменения (слева), общий предок (центр), входящие изменения (справа). Удобнее чем текстовый редактор.' }
      ]
    },
    {
      id: 2,
      title: '3-way merge и merge strategies',
      type: 'theory',
      content: [
        { type: 'text', value: 'Git использует 3-way merge: сравнивает три версии файла — общий предок (base), текущая ветка (ours) и входящая ветка (theirs). Это позволяет понять КТО что изменил и автоматически объединить непересекающиеся изменения.' },
        { type: 'heading', value: '3-way merge принцип' },
        { type: 'code', language: 'bash', value: '# 3-way merge:\n#\n# BASE (общий предок)     OURS (наша ветка)     THEIRS (их ветка)\n# ─────────────────────   ──────────────────    ──────────────────\n# port = 3000              port = 3000           port = 8080\n# host = localhost         host = 0.0.0.0        host = localhost\n# debug = false            debug = false         debug = true\n#\n# Результат автоматического merge:\n# port = 8080       ← theirs изменили (base→theirs), ours не менял\n# host = 0.0.0.0    ← ours изменили (base→ours), theirs не менял\n# debug = true      ← theirs изменили, ours не менял\n#\n# Конфликт ТОЛЬКО если ОБА изменили ОДНУ строку:\n# BASE: port = 3000\n# OURS: port = 8080\n# THEIRS: port = 9090\n# → CONFLICT!\n\n# Посмотреть 3 версии конфликтного файла:\ngit show :1:file.java    # BASE (общий предок)\ngit show :2:file.java    # OURS (текущая ветка)\ngit show :3:file.java    # THEIRS (входящая ветка)\n\n# Включить показ BASE в маркерах конфликта:\ngit config merge.conflictStyle diff3\n# Теперь конфликт выглядит так:\n# <<<<<<< HEAD\n# port = 8080\n# ||||||| merged common ancestors\n# port = 3000\n# =======\n# port = 9090\n# >>>>>>> feature' },
        { type: 'heading', value: 'Merge стратегии' },
        { type: 'code', language: 'bash', value: '# Git поддерживает несколько merge стратегий:\n\n# 1. ort (по умолчанию, заменил recursive)\n# Стандартный 3-way merge, умный и быстрый\ngit merge feature\n\n# 2. ours — при конфликте всегда берём НАШУ версию\ngit merge -s ours feature\n# Полностью игнорирует изменения из feature\n# Полезно: "пометить фичу как смерженную" без реальных изменений\n\n# 3. octopus — merge нескольких веток одновременно\ngit merge feature1 feature2 feature3\n# Используется для объединения многих независимых веток\n\n# Стратегические ОПЦИИ (не путать со стратегиями):\n# -X ours — при конфликте предпочтительна НАША версия\ngit merge -X ours feature\n# Автоматический merge, но при конфликте — берёт ours\n\n# -X theirs — при конфликте предпочтительна ИХ версия\ngit merge -X theirs feature\n\n# Важно: -s ours vs -X ours\n# -s ours  — ПОЛНОСТЬЮ игнорирует theirs (даже неконфликтующие)\n# -X ours  — merge нормально, только конфликты решает в пользу ours\n\n# То же для rebase:\ngit rebase -X ours main\ngit rebase -X theirs main' },
        { type: 'note', value: 'Рекомендуется включить diff3: git config --global merge.conflictStyle diff3. Видеть BASE-версию помогает понять, что каждая сторона изменила, и принять правильное решение.' }
      ]
    },
    {
      id: 3,
      title: 'Инструменты разрешения конфликтов',
      type: 'theory',
      content: [
        { type: 'text', value: 'Разрешение конфликтов вручную в текстовом редакторе — не единственный вариант. Существуют мощные визуальные инструменты: mergetool, IDE-интеграции, и специализированные утилиты.' },
        { type: 'heading', value: 'git mergetool' },
        { type: 'code', language: 'bash', value: '# Настройка mergetool:\n\n# VS Code как mergetool:\ngit config --global merge.tool vscode\ngit config --global mergetool.vscode.cmd \'code --wait $MERGED\'\n\n# IntelliJ IDEA как mergetool:\ngit config --global merge.tool intellij\ngit config --global mergetool.intellij.cmd \\\n  \'idea merge $LOCAL $REMOTE $BASE $MERGED\'\n\n# vimdiff (терминал):\ngit config --global merge.tool vimdiff\n\n# Использование:\ngit merge feature\n# CONFLICT in UserService.java\ngit mergetool\n# Откроется визуальный инструмент\n\n# После разрешения:\ngit add .\ngit merge --continue\n\n# Отключить создание .orig файлов:\ngit config --global mergetool.keepBackup false\n\n# IntelliJ IDEA встроенный merge:\n# 1. Открыть проект в IDEA\n# 2. Git → Resolve Conflicts\n# 3. Трёхпанельный вид:\n#    LEFT:   ваша версия (ours)\n#    CENTER: результат (то что будет закоммичено)\n#    RIGHT:  входящая версия (theirs)\n# 4. Кнопки: Accept Left, Accept Right, Accept Both\n# 5. Ручное редактирование центральной панели' },
        { type: 'heading', value: 'Продвинутые приёмы' },
        { type: 'code', language: 'bash', value: '# Посмотреть конфликтующие файлы:\ngit diff --name-only --diff-filter=U\n# Только файлы с конфликтами\n\n# Принять ВСЮ версию одной стороны для файла:\ngit checkout --ours src/UserService.java     # наша версия\ngit checkout --theirs src/UserService.java   # их версия\ngit add src/UserService.java\n\n# Для ВСЕХ конфликтных файлов:\ngit checkout --theirs .\ngit add .\n\n# Показать что изменилось с каждой стороны:\ngit log --merge --left-right -p\n# < коммиты из нашей ветки\n# > коммиты из их ветки\n\n# Отменить merge (вернуться к чистому состоянию):\ngit merge --abort\n# или после частичного разрешения:\ngit reset --hard HEAD\n\n# Проверить оставшиеся маркеры конфликтов:\ngrep -r "<<<<<<" src/\ngrep -r "=======" src/\ngrep -r ">>>>>>" src/\n\n# Автоматизация проверки (pre-commit hook):\n#!/bin/bash\nif grep -rn "<<<<<<\\|======\\|>>>>>>" --include="*.java" src/; then\n    echo "ERROR: Merge conflict markers found!"\n    exit 1\nfi' },
        { type: 'tip', value: 'IntelliJ IDEA — лучший инструмент для разрешения конфликтов в Java. Он понимает структуру кода, подсвечивает синтаксис и позволяет принимать изменения по частям (chunk по chunk).' }
      ]
    },
    {
      id: 4,
      title: 'Предотвращение конфликтов',
      type: 'theory',
      content: [
        { type: 'text', value: 'Лучший конфликт — тот, который не произошёл. Правильная организация кода, маленькие PR, частая синхронизация и коммуникация в команде значительно снижают количество конфликтов.' },
        { type: 'heading', value: 'Практики предотвращения конфликтов' },
        { type: 'code', language: 'bash', value: '# 1. ЧАСТАЯ СИНХРОНИЗАЦИЯ (главное правило!)\n# Каждое утро:\ngit fetch origin main\ngit rebase origin/main\n# Чем реже синхронизируешься — тем больше конфликтов\n\n# 2. МАЛЕНЬКИЕ PR (< 400 строк)\n# Большой PR = много изменённых файлов = высокий шанс конфликта\n# Маленький PR = меньше пересечений с коллегами\n\n# 3. ПРАВИЛЬНАЯ АРХИТЕКТУРА\n# Каждый разработчик работает в своём модуле/пакете:\n# src/\n#   user/         ← Разработчик А\n#     UserService.java\n#     UserController.java\n#   order/        ← Разработчик Б\n#     OrderService.java\n#     OrderController.java\n#   payment/      ← Разработчик В\n#     PaymentService.java\n\n# 4. ИЗБЕГАЙТЕ "МАГИЧЕСКИХ" ФАЙЛОВ\n# Файлы, которые все часто меняют:\n# - application.yml  → разделить на модульные конфиги\n# - Constants.java   → разделить по доменам\n# - SecurityConfig   → модульная конфигурация\n# - pom.xml          → модульный Maven проект\n\n# 5. CODE OWNERSHIP (CODEOWNERS)\n# .github/CODEOWNERS:\n# src/user/     @dev-team-user\n# src/payment/  @dev-team-payment\n# src/order/    @dev-team-order' },
        { type: 'heading', value: 'Автоматическое форматирование' },
        { type: 'code', language: 'bash', value: '# Частая причина "ложных" конфликтов:\n# Разные стили форматирования в команде\n# Dev A: использует табы, ставит { на новой строке\n# Dev B: использует пробелы, ставит { на той же строке\n\n# Решение: единый форматировщик + pre-commit hook\n\n# Java: google-java-format или Spotless\n# pom.xml:\n# <plugin>\n#   <groupId>com.diffplug.spotless</groupId>\n#   <artifactId>spotless-maven-plugin</artifactId>\n#   <configuration>\n#     <java>\n#       <googleJavaFormat/>\n#     </java>\n#   </configuration>\n# </plugin>\n\n# Pre-commit hook:\n#!/bin/bash\nmvn spotless:check\nif [ $? -ne 0 ]; then\n    echo "Code not formatted! Run: mvn spotless:apply"\n    exit 1\nfi\n\n# .editorconfig (единые настройки для всех IDE):\n# root = true\n# [*.java]\n# indent_style = space\n# indent_size = 4\n# end_of_line = lf\n# charset = utf-8\n# trim_trailing_whitespace = true\n# insert_final_newline = true\n\n# Git: игнорировать whitespace при merge:\ngit merge -X ignore-space-change feature\ngit merge -X ignore-all-space feature' },
        { type: 'note', value: 'В команде Kaspi.kz конфликты минимизируют через: микросервисную архитектуру (каждый сервис — отдельный репо), CODEOWNERS, обязательное форматирование через Spotless, и правило "PR < 400 строк".' }
      ]
    },
    {
      id: 5,
      title: 'Merge конфликты при rebase',
      type: 'theory',
      content: [
        { type: 'text', value: 'Конфликты при rebase отличаются от конфликтов при merge: они могут возникнуть на КАЖДОМ пересоздаваемом коммите. Это может быть утомительно, но есть стратегии для упрощения.' },
        { type: 'heading', value: 'Конфликты при rebase vs merge' },
        { type: 'code', language: 'bash', value: '# При MERGE: один конфликт\n# Git сравнивает финальные состояния двух веток\n# Решаешь один раз → готово\n\n# При REBASE: конфликт на КАЖДЫЙ коммит\n# feature: E───F───G───H (4 коммита)\n# rebase на main: каждый коммит может дать конфликт\n\ngit rebase main\n# Applying E → CONFLICT!\ngit add .\ngit rebase --continue\n# Applying F → CONFLICT!\ngit add .\ngit rebase --continue\n# Applying G → OK\n# Applying H → CONFLICT!\ngit add .\ngit rebase --continue\n# Готово!\n\n# Если устал от конфликтов:\ngit rebase --abort\n# Вернётся к состоянию до rebase\n\n# Стратегия: squash перед rebase\n# 4 коммита → 1 → rebase → максимум 1 конфликт\ngit rebase -i HEAD~4\n# squash всё в один коммит\ngit rebase main\n# Один конфликт вместо четырёх!' },
        { type: 'heading', value: 'rerere для повторяющихся конфликтов' },
        { type: 'code', language: 'bash', value: '# Включи rerere СЕЙЧАС:\ngit config --global rerere.enabled true\n\n# rerere запоминает решения конфликтов:\n\n# День 1: rebase feature на main → конфликт → решаешь\n# rerere запоминает решение\n\n# День 2: ещё раз rebase feature на main\n# Тот же конфликт → rerere решает АВТОМАТИЧЕСКИ!\n# Resolved \'UserService.java\' using previous resolution.\n\n# Сценарий: еженедельный rebase\ngit fetch origin main\ngit rebase origin/main\n# Первый раз: 5 конфликтов, решаешь вручную\n# Второй раз: 3 из 5 решены rerere автоматически\n# Третий раз: все 5 решены автоматически (если те же конфликты)\n\n# Тренировка rerere (записать решения для тестовых merge):\ngit config rerere.autoUpdate true\n# Автоматически git add файлы решённые rerere\n\n# Посмотреть записанные решения:\nls .git/rr-cache/\n\n# Забыть решение (если решили неправильно):\ngit rerere forget src/UserService.java\n\n# Диагностика: показать что rerere сделал\ngit rerere diff\ngit rerere status' },
        { type: 'tip', value: 'Комбинация squash + rerere = минимум боли при rebase. Squash уменьшает количество конфликтов, rerere автоматически решает повторяющиеся. Это экономит часы в неделю.' }
      ]
    },
    {
      id: 6,
      title: 'Сложные сценарии конфликтов',
      type: 'theory',
      content: [
        { type: 'text', value: 'В реальных проектах бывают сложные конфликты: при рефакторинге, миграциях, обновлении зависимостей, и изменениях конфигурации. Рассмотрим стратегии для каждого случая.' },
        { type: 'heading', value: 'Конфликты при рефакторинге' },
        { type: 'code', language: 'bash', value: '# Сценарий: Коллега рефакторил UserService\n# (переименовал класс, переместил пакет)\n# Вы: изменили логику в UserService\n\n# Git видит: файл удалён + создан новый → modify/delete конфликт\n\n# Стратегия:\n# 1. Посмотрите что коллега сделал:\ngit log --follow --diff-filter=R -- src/\n# Показывает переименования файлов\n\ngit diff main...feature --stat\n# Краткая статистика изменений\n\n# 2. Примите рефакторинг, применитие свои изменения:\ngit checkout --theirs .         # принять их структуру\ngit add .\n# Затем вручную применить свои логические изменения\n# в новом файле/пакете\n\n# Сценарий: Конфликт в pom.xml / build.gradle\n# Оба добавили зависимости в одно место\n# Решение: обычно нужно оставить ОБЕ зависимости\n\n# Сценарий: Конфликт в application.yml\n# Решение: разделить на модульные конфиги\n# application.yml\n# application-user.yml\n# application-payment.yml\n# spring.profiles.include: user, payment' },
        { type: 'heading', value: 'Чеклист разрешения конфликтов' },
        { type: 'code', language: 'bash', value: '# Шаг за шагом:\n\n# 1. Оценить масштаб:\ngit diff --name-only --diff-filter=U\n# Сколько файлов конфликтуют?\n\n# 2. Для каждого файла:\n# a) Понять ЧТО изменила каждая сторона:\ngit log --merge -p -- src/UserService.java\n\n# b) Понять ЗАЧЕМ (читаем commit message):\ngit log --merge --format="%h %s" -- src/UserService.java\n\n# c) Решить конфликт:\n#    - Одна сторона права → checkout --ours/--theirs\n#    - Оба правы → комбинируем вручную\n#    - Нужна помощь → зовём автора второго изменения\n\n# 3. Проверить после разрешения:\nmvn compile                    # компилируется?\nmvn test                       # тесты проходят?\ngrep -r "<<<<<<" src/          # не осталось маркеров?\n\n# 4. Закоммитить:\ngit add .\ngit merge --continue\n# или: git rebase --continue\n\n# ПРАВИЛА:\n# ✅ Всегда запускай тесты после разрешения конфликтов\n# ✅ Если не уверен — спроси коллегу\n# ✅ Используй IDE для сложных конфликтов\n# ❌ Не принимай вслепую --ours/--theirs для кода\n# ❌ Не коммить с маркерами конфликтов' },
        { type: 'warning', value: 'Самая частая ошибка: принять --ours или --theirs не разбираясь. Это может потерять чужие изменения или ваши. Всегда разбирайтесь в конфликте, даже если это занимает время.' }
      ]
    },
    {
      id: 7,
      title: 'Практика: Conflict Resolver',
      type: 'practice',
      difficulty: 'hard',
      description: 'Напишите Java-программу, которая симулирует 3-way merge: принимает три версии файла (base, ours, theirs), находит конфликты и пытается автоматически разрешить неконфликтующие изменения.',
      requirements: [
        'Метод threeWayMerge(List<String> base, List<String> ours, List<String> theirs) возвращает результат merge',
        'Автоматически мержит неконфликтующие изменения (изменение только в одной стороне)',
        'Помечает конфликты маркерами <<<<<<< / ======= / >>>>>>>',
        'Если строка изменена в base→ours, но не в base→theirs — берём ours',
        'Если строка изменена в обоих — CONFLICT',
        'Протестировать на примере конфигурационного файла'
      ],
      expectedOutput: 'Base:\n  port=3000\n  host=localhost\n  debug=false\n  timeout=30\n\nOurs:\n  port=3000\n  host=0.0.0.0\n  debug=false\n  timeout=30\n\nTheirs:\n  port=8080\n  host=localhost\n  debug=true\n  timeout=30\n\nMerge result:\n  <<<<<<< OURS\n  port=3000\n  =======\n  port=8080\n  >>>>>>> THEIRS\n  host=0.0.0.0\n  debug=true\n  timeout=30\n\nConflicts: 1, Auto-merged: 2',
      hint: 'Сравнивайте построчно: для каждого индекса проверьте base[i] vs ours[i] vs theirs[i]. Если base==ours но base!=theirs — берём theirs. Если base==theirs но base!=ours — берём ours. Если ours!=base && theirs!=base && ours!=theirs — конфликт.',
      solution: 'import java.util.ArrayList;\nimport java.util.List;\n\npublic class Main {\n\n    static class MergeResult {\n        List<String> lines = new ArrayList<>();\n        int conflicts = 0;\n        int autoMerged = 0;\n    }\n\n    static MergeResult threeWayMerge(List<String> base, List<String> ours, List<String> theirs) {\n        MergeResult result = new MergeResult();\n        int maxLen = Math.max(base.size(), Math.max(ours.size(), theirs.size()));\n\n        for (int i = 0; i < maxLen; i++) {\n            String baseLine = i < base.size() ? base.get(i) : null;\n            String oursLine = i < ours.size() ? ours.get(i) : null;\n            String theirsLine = i < theirs.size() ? theirs.get(i) : null;\n\n            boolean oursChanged = !equals(baseLine, oursLine);\n            boolean theirsChanged = !equals(baseLine, theirsLine);\n\n            if (!oursChanged && !theirsChanged) {\n                // Никто не менял — берём base\n                if (baseLine != null) result.lines.add(baseLine);\n            } else if (oursChanged && !theirsChanged) {\n                // Только мы изменили — берём ours\n                if (oursLine != null) result.lines.add(oursLine);\n                result.autoMerged++;\n            } else if (!oursChanged && theirsChanged) {\n                // Только они изменили — берём theirs\n                if (theirsLine != null) result.lines.add(theirsLine);\n                result.autoMerged++;\n            } else if (equals(oursLine, theirsLine)) {\n                // Оба изменили ОДИНАКОВО — нет конфликта\n                if (oursLine != null) result.lines.add(oursLine);\n            } else {\n                // Оба изменили ПО-РАЗНОМУ — КОНФЛИКТ!\n                result.lines.add("<<<<<<< OURS");\n                if (oursLine != null) result.lines.add(oursLine);\n                result.lines.add("=======");\n                if (theirsLine != null) result.lines.add(theirsLine);\n                result.lines.add(">>>>>>> THEIRS");\n                result.conflicts++;\n            }\n        }\n\n        return result;\n    }\n\n    static boolean equals(String a, String b) {\n        if (a == null && b == null) return true;\n        if (a == null || b == null) return false;\n        return a.equals(b);\n    }\n\n    static void printLines(String label, List<String> lines) {\n        System.out.println(label + ":");\n        for (String line : lines) {\n            System.out.println("  " + line);\n        }\n    }\n\n    public static void main(String[] args) {\n        List<String> base = List.of(\n            "port=3000",\n            "host=localhost",\n            "debug=false",\n            "timeout=30"\n        );\n\n        List<String> ours = List.of(\n            "port=3000",\n            "host=0.0.0.0",\n            "debug=false",\n            "timeout=30"\n        );\n\n        List<String> theirs = List.of(\n            "port=8080",\n            "host=localhost",\n            "debug=true",\n            "timeout=30"\n        );\n\n        printLines("Base", base);\n        System.out.println();\n        printLines("Ours", ours);\n        System.out.println();\n        printLines("Theirs", theirs);\n\n        MergeResult result = threeWayMerge(base, ours, theirs);\n\n        System.out.println();\n        printLines("Merge result", result.lines);\n\n        System.out.println();\n        System.out.println("Conflicts: " + result.conflicts +\n            ", Auto-merged: " + result.autoMerged);\n    }\n}',
      explanation: '3-way merge — основа работы Git. Для каждой строки Git сравнивает три версии: base (общий предок), ours (наша ветка), theirs (их ветка). Если изменилась только одна сторона — берём её изменение (auto-merge). Если обе стороны изменили по-разному — конфликт. Если обе изменили одинаково — не конфликт. Этот алгоритм работает построчно, что является упрощением: реальный Git использует более сложный diff-алгоритм (patience diff, histogram diff).'
    }
  ]
}
