export default {
  id: 2,
  title: 'Branching стратегии',
  description: 'Стратегии ветвления для команд: Git Flow, GitHub Flow, Trunk-Based Development, GitLab Flow. Как выбрать стратегию для своего проекта.',
  lessons: [
    {
      id: 1,
      title: 'Git Flow',
      type: 'theory',
      content: [
        { type: 'text', value: 'Git Flow — классическая стратегия ветвления, предложенная Винсентом Дриссеном в 2010 году. Используется в проектах с чёткими релизными циклами: банковские системы, enterprise-приложения, мобильные приложения с релизами в App Store.' },
        { type: 'heading', value: 'Основные ветки Git Flow' },
        { type: 'code', language: 'bash', value: '# Git Flow определяет 5 типов веток:\n#\n# ДОЛГОЖИВУЩИЕ ВЕТКИ:\n# main (master)  — стабильный production-код, каждый коммит = релиз\n# develop        — интеграционная ветка, сюда сливаются все фичи\n#\n# КОРОТКОЖИВУЩИЕ ВЕТКИ:\n# feature/*      — новые фичи (от develop → в develop)\n# release/*      — подготовка релиза (от develop → в main + develop)\n# hotfix/*       — срочные исправления (от main → в main + develop)\n\n# Визуально:\n# main:    ──●─────────────────●───────●──────\n#            │                 ↑       ↑\n# release:  │           ──●──●┘       │\n#            │           ↑             │\n# develop:  ──●──●──●──●──●──●──●──●──│──●──\n#               ↑  ↑           ↑      │\n# feature:  ──●──┘  │       ──●┘      │\n# feature:       ──●┘                 │\n# hotfix:                         ──●─┘' },
        { type: 'heading', value: 'Git Flow: команды' },
        { type: 'code', language: 'bash', value: '# Инициализация Git Flow\ngit flow init\n# Или вручную:\ngit checkout -b develop main\n\n# === FEATURE ===\n# Начать фичу (от develop)\ngit checkout develop\ngit checkout -b feature/user-auth\n\n# Работа над фичей...\ngit commit -m "feat(auth): добавил JWT токены"\ngit commit -m "feat(auth): добавил refresh token"\ngit commit -m "test(auth): тесты для авторизации"\n\n# Завершить фичу (merge в develop)\ngit checkout develop\ngit merge --no-ff feature/user-auth\n# --no-ff создаёт merge-коммит (видно в истории что была фича)\ngit branch -d feature/user-auth\ngit push origin develop\n\n# === RELEASE ===\n# Подготовка релиза\ngit checkout develop\ngit checkout -b release/1.2.0\n\n# Только баг-фиксы и подготовка к релизу!\ngit commit -m "fix: исправил баг в авторизации"\ngit commit -m "chore: обновил версию до 1.2.0"\n\n# Завершить релиз\ngit checkout main\ngit merge --no-ff release/1.2.0\ngit tag -a v1.2.0 -m "Релиз 1.2.0"\ngit checkout develop\ngit merge --no-ff release/1.2.0\ngit branch -d release/1.2.0\n\n# === HOTFIX ===\n# Срочное исправление в production\ngit checkout main\ngit checkout -b hotfix/critical-bug\ngit commit -m "fix: критический баг в оплате"\ngit checkout main\ngit merge --no-ff hotfix/critical-bug\ngit tag -a v1.2.1 -m "Hotfix 1.2.1"\ngit checkout develop\ngit merge --no-ff hotfix/critical-bug\ngit branch -d hotfix/critical-bug' },
        { type: 'note', value: 'Git Flow хорошо подходит для проектов с запланированными релизами (раз в 2-4 недели). Банки, страховые компании, enterprise. Избыточен для стартапов и проектов с CI/CD.' }
      ]
    },
    {
      id: 2,
      title: 'GitHub Flow',
      type: 'theory',
      content: [
        { type: 'text', value: 'GitHub Flow — упрощённая стратегия: одна main ветка + короткоживущие feature-ветки. Идеальна для CI/CD, SaaS-продуктов и команд, которые деплоят несколько раз в день. Используется в GitHub, Shopify, и большинстве стартапов.' },
        { type: 'heading', value: 'Принципы GitHub Flow' },
        { type: 'code', language: 'bash', value: '# GitHub Flow — 6 шагов:\n#\n# 1. Ветка main ВСЕГДА deployable\n# 2. Создай ветку от main для новой работы\n# 3. Регулярно коммить и пуш в свою ветку\n# 4. Открой Pull Request для обсуждения и ревью\n# 5. После approve и прохождения CI — merge в main\n# 6. Деплой сразу после merge (автоматически)\n\n# Визуально:\n# main:    ──●──●──●──●──●──●──●──●──●──\n#              ↑     ↑     ↑\n# feature: ──●─┘  ──●┘  ──●┘\n#   (живёт 1-3 дня)\n\n# Практика:\n# 1. Создать ветку\ngit checkout main\ngit pull origin main\ngit checkout -b feature/payment-api\n\n# 2. Работа (маленькие коммиты)\ngit commit -m "feat(payment): добавил PaymentService"\ngit commit -m "feat(payment): интеграция с Stripe"\ngit commit -m "test(payment): unit-тесты"\ngit push -u origin feature/payment-api\n\n# 3. Открыть PR\ngh pr create --title "feat: Payment API интеграция" \\\n  --body "## Что сделано\\n- PaymentService\\n- Stripe интеграция\\n- Unit тесты"\n\n# 4. После review и CI\ngh pr merge --squash  # или --merge, --rebase\n\n# 5. Удалить ветку\ngit checkout main\ngit pull origin main\ngit branch -d feature/payment-api' },
        { type: 'heading', value: 'GitHub Flow: лучшие практики' },
        { type: 'code', language: 'bash', value: '# 1. Именование веток\n# feature/JIRA-123-user-auth      — фича с номером задачи\n# fix/JIRA-456-login-error         — баг-фикс\n# refactor/payment-service         — рефакторинг\n# docs/api-documentation           — документация\n\n# 2. Conventional Commits\n# feat(scope): описание            — новая фича\n# fix(scope): описание             — баг-фикс\n# refactor(scope): описание        — рефакторинг\n# test(scope): описание            — тесты\n# docs(scope): описание            — документация\n# chore(scope): описание           — инфраструктура\n# perf(scope): описание            — производительность\n\n# 3. Branch Protection Rules (GitHub Settings)\n# ✅ Require PR before merging\n# ✅ Require 1-2 approvals\n# ✅ Require status checks (CI/CD)\n# ✅ Require conversation resolution\n# ✅ Require linear history (no merge commits)\n# ❌ Allow force pushes\n# ❌ Allow deletions\n\n# 4. Настройка squash merge по умолчанию\n# Settings → Pull Requests:\n# ✅ Allow squash merging (по умолчанию)\n# ☐ Allow merge commits\n# ☐ Allow rebase merging\n# ✅ Automatically delete head branches' },
        { type: 'tip', value: 'GitHub Flow — выбор 90% команд. Он прост, понятен и хорошо интегрируется с CI/CD. Правило: ветка живёт 1-3 дня, PR < 400 строк, merge в main = деплой.' }
      ]
    },
    {
      id: 3,
      title: 'Trunk-Based Development',
      type: 'theory',
      content: [
        { type: 'text', value: 'Trunk-Based Development (TBD) — стратегия, при которой все разработчики коммитят в одну ветку (trunk/main). Feature flags скрывают незавершённую функциональность. Используется в Google, Meta, Netflix. Требует зрелой CI/CD культуры.' },
        { type: 'heading', value: 'Как работает TBD' },
        { type: 'code', language: 'bash', value: '# Trunk-Based Development:\n# - Все работают в main (trunk)\n# - Ветки живут максимум 1-2 дня (если используются)\n# - Feature flags скрывают незавершённый код\n# - CI запускается на каждый коммит\n# - Релизы из main или release branches\n\n# Вариант 1: Прямые коммиты в main\ngit checkout main\ngit pull\n# ... пишем код ...\ngit commit -m "feat: добавил метод расчёта скидки"\ngit push origin main\n\n# Вариант 2: Очень короткие ветки (< 1 день)\ngit checkout -b feat/discount-calc\n# ... 2-3 коммита за 2-4 часа ...\ngit push origin feat/discount-calc\ngh pr create --title "feat: discount calculation"\n# Ревью + merge в тот же день\n\n# Feature flags в Java (Spring Boot):\n# @ConditionalOnProperty(name = "feature.new-payment", havingValue = "true")\n# @Component\n# public class NewPaymentService implements PaymentService { ... }\n\n# application.yml:\n# feature:\n#   new-payment: false        # выключена в production\n#   new-payment: true         # включена для тестирования\n\n# Или через Feature Flag Service (LaunchDarkly, Unleash):\n# if (featureFlags.isEnabled("new-payment", userId)) {\n#     return newPaymentService.process(order);\n# } else {\n#     return oldPaymentService.process(order);\n# }' },
        { type: 'heading', value: 'TBD: требования и trade-offs' },
        { type: 'code', language: 'bash', value: '# Требования для Trunk-Based Development:\n# ✅ Отличное покрытие тестами (> 80%)\n# ✅ Быстрый CI pipeline (< 10 минут)\n# ✅ Feature flags инфраструктура\n# ✅ Культура мелких коммитов\n# ✅ Автоматический деплой\n# ✅ Мониторинг и быстрый rollback\n\n# Преимущества:\n# + Нет долгоживущих веток → нет сложных merge-конфликтов\n# + Интеграция изменений каждый день → раннее обнаружение проблем\n# + Простая модель → легко понять\n# + Continuous Deployment ready\n\n# Недостатки:\n# - Требует зрелую инженерную культуру\n# - Feature flags усложняют код\n# - Нужно убирать старые флаги (техдолг)\n# - Сложнее для junior-разработчиков\n\n# Компании использующие TBD:\n# Google     — монорепо, 25000+ разработчиков\n# Meta       — trunk + feature flags\n# Netflix    — continuous deployment\n# LinkedIn   — trunk-based с feature flags\n# Kaspi.kz   — микросервисы, CI/CD' },
        { type: 'warning', value: 'TBD не означает "коммитим без ревью". Даже Google использует code review (через Gerrit). Разница — в скорости: PR/MR ревьюится и мержится в тот же день, а не висит неделю.' }
      ]
    },
    {
      id: 4,
      title: 'GitLab Flow и Release Branches',
      type: 'theory',
      content: [
        { type: 'text', value: 'GitLab Flow — компромисс между Git Flow и GitHub Flow. Добавляет environment branches (staging, production) или release branches. Полезен когда нужен контроль над тем, какой код попадает в конкретное окружение.' },
        { type: 'heading', value: 'GitLab Flow с environment branches' },
        { type: 'code', language: 'bash', value: '# GitLab Flow: main → staging → production\n#\n# main       — основная разработка (merge от feature)\n# staging    — тестовое окружение (merge от main)\n# production — боевое окружение (merge от staging)\n\n# Workflow:\n# 1. Feature branch → merge в main\ngit checkout main && git pull\ngit checkout -b feature/user-profile\ngit commit -m "feat(profile): страница профиля"\ngit push -u origin feature/user-profile\n# Merge Request → review → merge в main\n\n# 2. Деплой на staging\ngit checkout staging\ngit merge main\ngit push origin staging\n# CI/CD деплоит на staging-сервер\n# QA тестирует\n\n# 3. Деплой на production\ngit checkout production\ngit merge staging\ngit push origin production\n# CI/CD деплоит на production\n\n# Hotfix:\ngit checkout main\ngit checkout -b hotfix/payment-bug\ngit commit -m "fix(payment): исправил двойное списание"\n# Merge в main → cherry-pick в staging → cherry-pick в production\ngit checkout production\ngit cherry-pick <hotfix-commit-hash>\ngit push origin production' },
        { type: 'heading', value: 'Release Branches для версионных продуктов' },
        { type: 'code', language: 'bash', value: '# Для продуктов с несколькими поддерживаемыми версиями:\n# Java SDK, Spring Framework, библиотеки\n\n# main        — следующая версия (3.x)\n# release/2.x — поддержка 2.x\n# release/1.x — поддержка 1.x (только security fixes)\n\n# Создание release branch\ngit checkout main\ngit checkout -b release/2.0\ngit tag -a v2.0.0 -m "Release 2.0.0"\ngit push origin release/2.0 --tags\n\n# Bug fix для release/2.x\ngit checkout release/2.0\ngit checkout -b fix/security-patch\ngit commit -m "fix(security): обновил зависимости"\ngit checkout release/2.0\ngit merge fix/security-patch\ngit tag -a v2.0.1 -m "Release 2.0.1"\n# Если фикс нужен и в main:\ngit checkout main\ngit cherry-pick <commit-hash>\n\n# Semantic Versioning (SemVer):\n# MAJOR.MINOR.PATCH\n# 2.1.3\n# MAJOR — breaking changes (несовместимые изменения API)\n# MINOR — новые фичи (обратно совместимые)\n# PATCH — баг-фиксы (обратно совместимые)\n\n# Примеры:\n# 1.0.0 → 1.0.1  — баг-фикс\n# 1.0.1 → 1.1.0  — новая фича\n# 1.1.0 → 2.0.0  — breaking change' },
        { type: 'tip', value: 'Выбор стратегии зависит от контекста: стартап с 3 разработчиками — GitHub Flow. Банк с 50 разработчиками и квартальными релизами — Git Flow. Зрелый SaaS с continuous deployment — Trunk-Based.' }
      ]
    },
    {
      id: 5,
      title: 'Conventional Commits и Semantic Versioning',
      type: 'theory',
      content: [
        { type: 'text', value: 'Conventional Commits — стандарт именования коммитов. В связке с Semantic Versioning позволяет автоматически генерировать CHANGELOG, определять версию релиза и создавать release notes.' },
        { type: 'heading', value: 'Формат Conventional Commits' },
        { type: 'code', language: 'bash', value: '# Формат: <type>(<scope>): <description>\n#\n# type     — тип изменения\n# scope    — область (опционально)\n# description — краткое описание\n\n# Типы:\n# feat     — новая функциональность (→ MINOR в SemVer)\n# fix      — исправление бага (→ PATCH в SemVer)\n# docs     — документация\n# style    — форматирование, пробелы, точки с запятой\n# refactor — рефакторинг без изменения поведения\n# perf     — улучшение производительности\n# test     — добавление/исправление тестов\n# build    — система сборки (Maven, Gradle)\n# ci       — CI конфигурация (GitHub Actions, Jenkins)\n# chore    — обслуживание (обновление зависимостей)\n# revert   — откат предыдущего коммита\n\n# Примеры:\ngit commit -m "feat(auth): добавил JWT авторизацию"\ngit commit -m "fix(payment): исправил двойное списание"\ngit commit -m "refactor(user): выделил UserMapper в отдельный класс"\ngit commit -m "test(order): добавил интеграционные тесты для OrderService"\ngit commit -m "docs(api): обновил Swagger документацию"\ngit commit -m "perf(search): оптимизировал поисковый запрос с индексом"\ngit commit -m "build(deps): обновил Spring Boot до 3.2.0"\ngit commit -m "ci: добавил SonarQube анализ в pipeline"\n\n# Breaking change (→ MAJOR в SemVer):\ngit commit -m "feat(api)!: изменил формат ответа /users endpoint"\n# Или в теле:\ngit commit -m "feat(api): новый формат ответа" -m "BREAKING CHANGE: поле name заменено на firstName + lastName"' },
        { type: 'heading', value: 'Автоматизация версионирования' },
        { type: 'code', language: 'bash', value: '# Инструменты для автоматизации:\n\n# 1. standard-version / release-please (Node.js)\nnpx standard-version\n# Анализирует коммиты → определяет версию → обновляет CHANGELOG\n\n# 2. Maven: versions-maven-plugin\n# pom.xml:\n# <version>1.2.3-SNAPSHOT</version>\nmvn versions:set -DnewVersion=1.3.0\n\n# 3. Gradle:\n# gradle.properties:\n# version=1.2.3\n# build.gradle:\n# version = project.version\n\n# 4. git-cliff — CHANGELOG генератор\ngit cliff --output CHANGELOG.md\n# Генерирует красивый CHANGELOG из conventional commits\n\n# Пример автоматического CHANGELOG:\n# ## [1.3.0] - 2024-01-15\n# ### Features\n# - **auth**: добавил JWT авторизацию (abc1234)\n# - **payment**: интеграция с Stripe (def5678)\n# ### Bug Fixes\n# - **order**: исправил расчёт скидки (ghi9012)\n# ### Breaking Changes\n# - **api**: изменён формат ответа /users (jkl3456)\n\n# GitHub Release автоматизация:\n# .github/workflows/release.yml\n# Триггер: push tag v*\n# Шаги: build → test → create GitHub Release с CHANGELOG' },
        { type: 'note', value: 'Conventional Commits + SemVer + CI/CD = полная автоматизация релизов. Разработчик только пишет правильные commit messages, а pipeline сам определяет версию, генерирует CHANGELOG и создаёт релиз.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Выбор стратегии ветвления',
      type: 'practice',
      difficulty: 'medium',
      description: 'Напишите Java-программу, которая по параметрам проекта (размер команды, частота деплоев, тип продукта) рекомендует стратегию ветвления и генерирует список команд для настройки.',
      requirements: [
        'Enum BranchStrategy с вариантами: GIT_FLOW, GITHUB_FLOW, TRUNK_BASED, GITLAB_FLOW',
        'Класс ProjectProfile: teamSize, deploysPerWeek, hasCI, hasFeatureFlags, productType',
        'Метод recommend(ProjectProfile) возвращает BranchStrategy с обоснованием',
        'Метод generateSetupCommands(BranchStrategy) — список git-команд для настройки',
        'Протестировать на 4 профилях: стартап, банк, SaaS-продукт, open-source библиотека'
      ],
      expectedOutput: 'Project: Стартап (3 devs, 10 deploys/week)\n  Strategy: GITHUB_FLOW\n  Reason: Маленькая команда с частыми деплоями\n  Commands:\n    git checkout -b main\n    git push -u origin main\n\nProject: Банковская система (50 devs, 1 deploy/month)\n  Strategy: GIT_FLOW\n  Reason: Большая команда с редкими контролируемыми релизами\n  Commands:\n    git checkout -b develop main\n    git push -u origin develop\n\nProject: SaaS-платформа (20 devs, 50 deploys/week)\n  Strategy: TRUNK_BASED\n  Reason: CI/CD + feature flags = continuous deployment\n  Commands:\n    git checkout main\n\nProject: Open-source библиотека\n  Strategy: GITLAB_FLOW\n  Reason: Нужны release branches для поддержки нескольких версий',
      hint: 'Используйте if/else или switch по приоритетам: частота деплоев > 20/неделю → TBD, размер команды > 30 и редкие релизы → Git Flow, open-source с версиями → GitLab Flow, иначе → GitHub Flow.',
      solution: 'import java.util.ArrayList;\nimport java.util.List;\n\npublic class Main {\n\n    enum BranchStrategy {\n        GIT_FLOW("Git Flow"),\n        GITHUB_FLOW("GitHub Flow"),\n        TRUNK_BASED("Trunk-Based Development"),\n        GITLAB_FLOW("GitLab Flow");\n\n        final String displayName;\n        BranchStrategy(String name) { this.displayName = name; }\n    }\n\n    enum ProductType {\n        STARTUP, ENTERPRISE, SAAS, OPEN_SOURCE, MOBILE\n    }\n\n    static class ProjectProfile {\n        String name;\n        int teamSize;\n        int deploysPerWeek;\n        boolean hasCI;\n        boolean hasFeatureFlags;\n        ProductType productType;\n\n        ProjectProfile(String name, int teamSize, int deploysPerWeek,\n                       boolean hasCI, boolean hasFeatureFlags, ProductType type) {\n            this.name = name;\n            this.teamSize = teamSize;\n            this.deploysPerWeek = deploysPerWeek;\n            this.hasCI = hasCI;\n            this.hasFeatureFlags = hasFeatureFlags;\n            this.productType = type;\n        }\n    }\n\n    static class Recommendation {\n        BranchStrategy strategy;\n        String reason;\n        List<String> commands;\n\n        Recommendation(BranchStrategy s, String r, List<String> c) {\n            this.strategy = s;\n            this.reason = r;\n            this.commands = c;\n        }\n    }\n\n    static Recommendation recommend(ProjectProfile profile) {\n        // Trunk-Based: частые деплои + CI + feature flags\n        if (profile.deploysPerWeek >= 20 && profile.hasCI && profile.hasFeatureFlags) {\n            return new Recommendation(\n                BranchStrategy.TRUNK_BASED,\n                "CI/CD + feature flags = continuous deployment",\n                List.of("git checkout main",\n                         "# Configure feature flags service",\n                         "# Setup CI pipeline for every commit")\n            );\n        }\n\n        // Git Flow: большая команда + редкие релизы + enterprise\n        if (profile.teamSize >= 20 && profile.deploysPerWeek <= 4\n                && profile.productType == ProductType.ENTERPRISE) {\n            return new Recommendation(\n                BranchStrategy.GIT_FLOW,\n                "Большая команда с редкими контролируемыми релизами",\n                List.of("git checkout -b develop main",\n                         "git push -u origin develop",\n                         "# Branch protection: main + develop",\n                         "# Setup release process")\n            );\n        }\n\n        // GitLab Flow: open-source или версионные продукты\n        if (profile.productType == ProductType.OPEN_SOURCE) {\n            return new Recommendation(\n                BranchStrategy.GITLAB_FLOW,\n                "Нужны release branches для поддержки нескольких версий",\n                List.of("git checkout main",\n                         "git checkout -b release/1.0 main",\n                         "# Setup version tags: v1.0.0, v1.0.1",\n                         "# Configure backport workflow")\n            );\n        }\n\n        // GitHub Flow: по умолчанию — простой и эффективный\n        return new Recommendation(\n            BranchStrategy.GITHUB_FLOW,\n            "Маленькая команда с частыми деплоями",\n            List.of("git checkout -b main",\n                     "git push -u origin main",\n                     "# Setup branch protection rules",\n                     "# Configure CI/CD pipeline")\n        );\n    }\n\n    public static void main(String[] args) {\n        ProjectProfile[] projects = {\n            new ProjectProfile("Стартап (3 devs, 10 deploys/week)",\n                3, 10, true, false, ProductType.STARTUP),\n            new ProjectProfile("Банковская система (50 devs, 1 deploy/month)",\n                50, 1, true, false, ProductType.ENTERPRISE),\n            new ProjectProfile("SaaS-платформа (20 devs, 50 deploys/week)",\n                20, 50, true, true, ProductType.SAAS),\n            new ProjectProfile("Open-source библиотека",\n                10, 2, true, false, ProductType.OPEN_SOURCE)\n        };\n\n        for (ProjectProfile project : projects) {\n            Recommendation rec = recommend(project);\n            System.out.println("Project: " + project.name);\n            System.out.println("  Strategy: " + rec.strategy);\n            System.out.println("  Reason: " + rec.reason);\n            System.out.println("  Commands:");\n            for (String cmd : rec.commands) {\n                System.out.println("    " + cmd);\n            }\n            System.out.println();\n        }\n    }\n}',
      explanation: 'Выбор branching-стратегии зависит от нескольких факторов: размер команды, частота деплоев, наличие CI/CD и feature flags. Стартапы обычно выбирают GitHub Flow за простоту. Enterprise (банки, телеком) — Git Flow для контролируемых релизов. Зрелые SaaS-продукты с continuous deployment — Trunk-Based. Open-source проекты с несколькими поддерживаемыми версиями — GitLab Flow с release branches.'
    }
  ]
}
