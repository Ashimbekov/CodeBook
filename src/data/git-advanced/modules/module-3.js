export default {
  id: 3,
  title: 'Rebase',
  description: 'Interactive rebase: squash, fixup, reword, edit, drop. Rebase vs merge. Перезапись истории и golden rule of rebasing.',
  lessons: [
    {
      id: 1,
      title: 'Rebase: основы',
      type: 'theory',
      content: [
        { type: 'text', value: 'git rebase перемещает (пересоздаёт) коммиты одной ветки на вершину другой. В отличие от merge, rebase создаёт линейную историю без merge-коммитов. Это мощный инструмент для чистой, читаемой истории проекта.' },
        { type: 'heading', value: 'Merge vs Rebase' },
        { type: 'code', language: 'bash', value: '# Исходная ситуация:\n# main:    A───B───C───D\n#               \\\n# feature:       E───F\n\n# ═══ git merge main (из feature) ═══\n# main:    A───B───C───D\n#               \\       \\\n# feature:       E───F───M  (merge commit M)\n#\n# Плюсы: полная история, безопасно\n# Минусы: нелинейная история, "спагетти" в git log\n\n# ═══ git rebase main (из feature) ═══\n# main:    A───B───C───D\n#                       \\\n# feature:               E\'───F\'  (коммиты ПЕРЕСОЗДАНЫ)\n#\n# Плюсы: чистая линейная история\n# Минусы: переписывает историю (новые хеши)\n\n# Что происходит при rebase:\n# 1. Git находит общего предка (B)\n# 2. Сохраняет diff каждого коммита feature (E, F)\n# 3. Перемещает HEAD feature на вершину main (D)\n# 4. Применяет каждый diff заново → новые коммиты E\', F\'\n# E\' и F\' имеют НОВЫЕ хеши (другой parent)' },
        { type: 'heading', value: 'Практический workflow с rebase' },
        { type: 'code', language: 'bash', value: '# Типичный рабочий день Java-разработчика:\n\n# Утро: синхронизируемся с main\ngit checkout feature/user-service\ngit fetch origin\ngit rebase origin/main\n# Если конфликты:\n#   1. Исправить конфликтующие файлы\n#   2. git add <файлы>\n#   3. git rebase --continue\n# Или отменить: git rebase --abort\n\n# Вечер: пушим обновлённую ветку\ngit push --force-with-lease origin feature/user-service\n# --force-with-lease: безопасный force push\n# Откажет, если кто-то тоже пушил в эту ветку\n\n# ВАЖНО: --force-with-lease вместо --force\n# --force         — перезапишет ВСЕГДА (может потерять чужие коммиты)\n# --force-with-lease — перезапишет только если remote не изменился\n\n# Алиас для удобства\ngit config --global alias.fpush "push --force-with-lease"\ngit fpush origin feature/user-service\n\n# Перед merge в main: rebase + squash\ngit rebase -i origin/main\n# Объединяем WIP коммиты в один осмысленный' },
        { type: 'warning', value: 'ЗОЛОТОЕ ПРАВИЛО: никогда не делай rebase публичных веток (main, develop). Rebase переписывает историю — если кто-то работал от старых коммитов, его работа сломается. Rebase только свои feature-ветки.' }
      ]
    },
    {
      id: 2,
      title: 'Interactive Rebase: squash и fixup',
      type: 'theory',
      content: [
        { type: 'text', value: 'Interactive rebase (git rebase -i) — самый мощный инструмент для работы с историей. Позволяет объединять, переупорядочивать, редактировать и удалять коммиты. Используется перед merge в main для создания чистой истории.' },
        { type: 'heading', value: 'Squash — объединение коммитов' },
        { type: 'code', language: 'bash', value: '# Перед merge в main: у нас 5 "грязных" коммитов\ngit log --oneline\n# abc1234 fix: опечатка\n# def5678 WIP: доделываю\n# ghi9012 feat: добавил валидацию\n# jkl3456 WIP: начал валидацию\n# mno7890 feat: создал UserService\n\n# Объединяем в 1-2 осмысленных коммита\ngit rebase -i HEAD~5\n\n# Откроется редактор:\n# pick mno7890 feat: создал UserService\n# pick jkl3456 WIP: начал валидацию\n# pick ghi9012 feat: добавил валидацию\n# pick def5678 WIP: доделываю\n# pick abc1234 fix: опечатка\n\n# Меняем на:\n# pick mno7890 feat: создал UserService\n# squash jkl3456 WIP: начал валидацию\n# squash ghi9012 feat: добавил валидацию\n# squash def5678 WIP: доделываю\n# squash abc1234 fix: опечатка\n\n# squash: объединить с предыдущим коммитом, СОХРАНИТЬ сообщение\n# Откроется редактор для нового сообщения. Пишем:\n# feat(user): создал UserService с валидацией\n\n# Результат: 5 коммитов → 1 чистый коммит\ngit log --oneline\n# xyz9999 feat(user): создал UserService с валидацией' },
        { type: 'heading', value: 'Fixup — squash без сообщения' },
        { type: 'code', language: 'bash', value: '# fixup = squash, но УДАЛЯЕТ сообщение коммита\n# Идеально для мелких исправлений\n\n# Сценарий: сделал коммит, потом нашёл опечатку\ngit commit -m "feat(order): добавил расчёт скидок"\n# ... нашли опечатку ...\ngit commit -m "fix: опечатка в OrderService"\n\ngit rebase -i HEAD~2\n# pick abc1234 feat(order): добавил расчёт скидок\n# fixup def5678 fix: опечатка в OrderService\n# ↑ fixup удалит сообщение "fix: опечатка" и объединит с предыдущим\n\n# Ещё быстрее: git commit --fixup\ngit commit --fixup abc1234\n# Создаёт коммит "fixup! feat(order): добавил расчёт скидок"\n\n# Автоматический rebase с --autosquash\ngit rebase -i --autosquash HEAD~3\n# Git сам расставит fixup-коммиты под нужные коммиты!\n\n# Настроить autosquash по умолчанию:\ngit config --global rebase.autoSquash true\n# Теперь git rebase -i автоматически использует --autosquash' },
        { type: 'tip', value: 'Workflow: делай коммиты часто (каждые 30 минут), а перед PR — squash в 1-3 осмысленных коммита. Так ты не потеряешь работу и при этом история будет чистой.' }
      ]
    },
    {
      id: 3,
      title: 'Interactive Rebase: reword, edit, drop',
      type: 'theory',
      content: [
        { type: 'text', value: 'Помимо squash и fixup, interactive rebase позволяет: reword (изменить сообщение), edit (остановиться для редактирования), drop (удалить коммит), и переупорядочить коммиты.' },
        { type: 'heading', value: 'Reword — изменение сообщения коммита' },
        { type: 'code', language: 'bash', value: '# Исправить сообщение ЛЮБОГО коммита (не только последнего)\n\n# Для последнего коммита:\ngit commit --amend -m "feat(auth): добавил JWT авторизацию"\n\n# Для любого коммита в истории:\ngit rebase -i HEAD~4\n# pick abc1234 feat: авторизация\n# pick def5678 добавил тесты\n# pick ghi9012 fix: баг\n# pick jkl3456 рефакторинг\n\n# Меняем pick на reword для нужных:\n# reword abc1234 feat: авторизация\n# pick def5678 добавил тесты\n# reword ghi9012 fix: баг\n# pick jkl3456 рефакторинг\n\n# Git остановится на каждом reword и предложит изменить сообщение\n# Пишем правильные conventional commits:\n# feat(auth): добавил JWT авторизацию с refresh token\n# fix(auth): исправил expired token handling' },
        { type: 'heading', value: 'Edit — остановка для редактирования' },
        { type: 'code', language: 'bash', value: '# edit останавливает rebase на коммите для любых изменений\n\ngit rebase -i HEAD~3\n# pick abc1234 feat: UserService + OrderService\n# pick def5678 test: тесты\n# pick ghi9012 docs: документация\n\n# Меняем:\n# edit abc1234 feat: UserService + OrderService\n# pick def5678 test: тесты\n# pick ghi9012 docs: документация\n\n# Git остановится после abc1234. Мы можем:\n\n# Вариант 1: Разделить коммит на два\ngit reset HEAD~1   # "откатить" коммит, но сохранить файлы\ngit add src/UserService.java\ngit commit -m "feat(user): создал UserService"\ngit add src/OrderService.java\ngit commit -m "feat(order): создал OrderService"\ngit rebase --continue\n\n# Вариант 2: Добавить забытый файл\ngit add src/UserServiceTest.java\ngit commit --amend --no-edit\ngit rebase --continue\n\n# Вариант 3: Изменить код\n# Правим файлы...\ngit add -A\ngit commit --amend\ngit rebase --continue' },
        { type: 'heading', value: 'Drop и переупорядочивание' },
        { type: 'code', language: 'bash', value: '# Drop — удаление коммита из истории\ngit rebase -i HEAD~4\n# pick abc1234 feat: UserService\n# drop def5678 WIP: debug output     ← УДАЛИТЬ\n# pick ghi9012 feat: OrderService\n# pick jkl3456 test: тесты\n\n# Или просто удалите строку — эффект тот же\n\n# Переупорядочивание — перетаскиваем строки\n# До:\n# pick abc1234 test: тесты для UserService\n# pick def5678 feat: UserService\n# pick ghi9012 feat: OrderService\n# pick jkl3456 test: тесты для OrderService\n\n# После (логичнее: фича → тест):\n# pick def5678 feat: UserService\n# pick abc1234 test: тесты для UserService\n# pick ghi9012 feat: OrderService\n# pick jkl3456 test: тесты для OrderService\n\n# ОСТОРОЖНО: если коммиты зависят друг от друга,\n# переупорядочивание может вызвать конфликты!' },
        { type: 'warning', value: 'При drop коммит удаляется из истории, но его можно восстановить через reflog в течение 30 дней. Всегда проверяйте: git log --oneline после rebase, чтобы убедиться что история правильная.' }
      ]
    },
    {
      id: 4,
      title: 'Rebase onto и advanced rebase',
      type: 'theory',
      content: [
        { type: 'text', value: 'git rebase --onto позволяет пересадить часть ветки на другую базу. Это мощный инструмент для реорганизации веток, когда обычный rebase не справляется.' },
        { type: 'heading', value: 'Rebase --onto' },
        { type: 'code', language: 'bash', value: '# Сценарий: feature-B создана от feature-A, но feature-A ещё не смержена\n# main:      A───B───C\n#                 \\\n# feature-A:      D───E\n#                      \\\n# feature-B:            F───G\n\n# feature-A отклонили/изменили. Нужно пересадить feature-B на main\n\n# git rebase --onto <новая-база> <старая-база> <ветка>\ngit rebase --onto main feature-A feature-B\n\n# Результат:\n# main:      A───B───C\n#                     \\\n# feature-B:           F\'───G\'\n# (feature-A осталась без изменений)\n\n# Другой сценарий: удалить коммиты из середины ветки\n# feature: A───B───C───D───E───F\n# Хотим убрать C и D\n\ngit rebase --onto B D feature\n# Результат: A───B───E\'───F\'\n# (C и D удалены из истории)\n\n# Ещё сценарий: "пересадить" на другую ветку\n# release/1.0:  A───B───C\n# release/2.0:  A───B───D───E\n#                        \\\n# fix:                    F───G\n\n# Перенести fix с release/2.0 на release/1.0:\ngit rebase --onto release/1.0 release/2.0 fix\n# Результат:\n# release/1.0:  A───B───C\n#                        \\\n# fix:                    F\'───G\'' },
        { type: 'heading', value: 'Rebase: опции и трюки' },
        { type: 'code', language: 'bash', value: '# Полезные опции rebase:\n\n# --keep-empty: сохранить пустые коммиты\ngit rebase --keep-empty main\n\n# --committer-date-is-author-date: сохранить оригинальную дату\ngit rebase --committer-date-is-author-date main\n\n# --exec: выполнить команду после каждого коммита\ngit rebase -i --exec "mvn test" HEAD~5\n# Запустит mvn test после каждого из 5 коммитов\n# Если тест упадёт — rebase остановится\n\n# Полезные алиасы:\ngit config --global alias.ri "rebase -i"\ngit config --global alias.rc "rebase --continue"\ngit config --global alias.ra "rebase --abort"\ngit config --global alias.squash "rebase -i --autosquash"\n\n# Workflow: rebase перед PR\n# 1. Fetch последний main\ngit fetch origin main\n# 2. Interactive rebase: squash WIP коммиты\ngit rebase -i origin/main\n# 3. Force push обновлённую ветку\ngit push --force-with-lease\n# 4. PR с чистой историей' },
        { type: 'tip', value: 'git rebase --exec "mvn test" — отличный способ убедиться, что каждый коммит в истории проходит тесты. Если CI проверяет только последний коммит, rebase --exec проверит каждый.' }
      ]
    },
    {
      id: 5,
      title: 'Rebase: конфликты и проблемы',
      type: 'theory',
      content: [
        { type: 'text', value: 'При rebase конфликты могут возникнуть на каждом пересоздаваемом коммите. В отличие от merge (один раз разрешил — готово), rebase может требовать разрешения конфликтов несколько раз. Но есть инструменты для упрощения.' },
        { type: 'heading', value: 'Разрешение конфликтов при rebase' },
        { type: 'code', language: 'bash', value: '# Конфликт при rebase:\ngit rebase main\n# CONFLICT (content): Merge conflict in src/UserService.java\n# error: could not apply abc1234... feat: обновил UserService\n\n# 1. Смотрим что конфликтует:\ngit status\n# both modified: src/UserService.java\n\n# 2. Открываем файл и видим маркеры:\n# <<<<<<< HEAD (main)\n#     private final UserRepository userRepo;\n# =======\n#     private final UserRepository repository;\n# >>>>>>> abc1234 (feature)\n\n# 3. Решаем конфликт (выбираем правильный вариант):\n#     private final UserRepository userRepository;\n\n# 4. Продолжаем rebase:\ngit add src/UserService.java\ngit rebase --continue\n# Может быть ещё конфликт на следующем коммите...\n\n# Или отменяем всё:\ngit rebase --abort\n# Вернёт ветку в состояние до rebase\n\n# Пропустить проблемный коммит:\ngit rebase --skip\n# ОСТОРОЖНО: коммит будет потерян!' },
        { type: 'heading', value: 'rerere — запоминание решений конфликтов' },
        { type: 'code', language: 'bash', value: '# rerere = "reuse recorded resolution"\n# Git запоминает как вы решили конфликт и применяет решение автоматически\n\n# Включить rerere:\ngit config --global rerere.enabled true\n\n# Как это работает:\n# 1. Первый rebase: конфликт в UserService.java\n#    Вы разрешаете конфликт вручную\n#    Git запоминает: "конфликт X → решение Y"\n\n# 2. Следующий rebase с тем же конфликтом:\n#    Git автоматически применяет запомненное решение!\n#    Resolved \'src/UserService.java\' using previous resolution.\n\n# Управление rerere:\ngit rerere status     # Показать файлы с запомненными решениями\ngit rerere diff       # Показать что rerere изменил\ngit rerere forget src/UserService.java  # Забыть решение для файла\ngit rerere gc         # Очистить старые записи\n\n# Хранятся в .git/rr-cache/\nls .git/rr-cache/\n\n# rerere НЕЗАМЕНИМ при частых rebase:\n# Каждый раз при rebase origin/main — те же конфликты\n# С rerere — решаете один раз, дальше автоматически' },
        { type: 'tip', value: 'Включите rerere глобально: git config --global rerere.enabled true. Это сэкономит часы при частых rebase. Особенно полезно в проектах с долгоживущими feature-ветками.' }
      ]
    },
    {
      id: 6,
      title: 'Когда использовать rebase, а когда merge',
      type: 'theory',
      content: [
        { type: 'text', value: 'Rebase vs Merge — один из самых спорных вопросов в Git. Нет "правильного" ответа — зависит от ситуации. Но есть чёткие guidelines, которых придерживаются профессиональные команды.' },
        { type: 'heading', value: 'Rebase vs Merge: guidelines' },
        { type: 'code', language: 'bash', value: '# ═══ ИСПОЛЬЗУЙ REBASE когда: ═══\n\n# 1. Синхронизируешь feature-ветку с main\ngit checkout feature/my-work\ngit rebase origin/main\n# Линейная история, легко читать\n\n# 2. Squash перед merge в main\ngit rebase -i origin/main\n# Объединяешь WIP-коммиты\n\n# 3. Один работаешь над веткой\n# Нет риска сломать чужую работу\n\n# 4. Хочешь чистую историю\n# git log --oneline красивый и понятный\n\n# ═══ ИСПОЛЬЗУЙ MERGE когда: ═══\n\n# 1. Вливаешь feature в main/develop\ngit checkout main\ngit merge --no-ff feature/user-auth\n# --no-ff создаёт merge-коммит → видно границы фичи\n\n# 2. Несколько человек работают над одной веткой\n# Rebase переписывает хеши → конфликты у коллег\n\n# 3. Нужно сохранить точную историю\n# Аудит, compliance, forensics\n\n# 4. Main/develop ветки\n# НИКОГДА не rebase публичные ветки\n\n# ═══ ПОПУЛЯРНЫЕ СТРАТЕГИИ В КОМАНДАХ: ═══\n\n# Kaspi, EPAM, большинство:\n# feature: rebase на main для синхронизации\n# PR merge: squash merge (GitHub) или merge --no-ff\n\n# Google:\n# Squash + merge (один коммит на фичу в trunk)' },
        { type: 'heading', value: 'Настройка стратегии merge в GitHub/GitLab' },
        { type: 'code', language: 'bash', value: '# GitHub: Settings → Pull Requests\n\n# 1. Squash and merge (рекомендуется для большинства)\n# Все коммиты PR объединяются в один\n# feat(auth): JWT авторизация (#42)\n# Чистая история main: 1 коммит = 1 фича/PR\n\n# 2. Create a merge commit (merge --no-ff)\n# Сохраняет все коммиты + merge commit\n# Полезно когда каждый коммит осмысленный\n\n# 3. Rebase and merge\n# Коммиты пересоздаются на main без merge commit\n# Линейная история с отдельными коммитами\n\n# GitLab: Settings → Merge Requests\n# Merge commit\n# Merge commit with semi-linear history\n# Fast-forward merge (только если ветка up-to-date с main)\n\n# Рекомендация для Java-проектов:\n# ✅ Squash and merge — для feature/fix PR\n# ✅ Merge commit — для release branches\n# ✅ Rebase locally — для синхронизации feature с main\n# ❌ Force push в main/develop — никогда\n\n# Настройка по умолчанию:\n# GitHub CLI:\ngh repo edit --default-branch-merge-method squash' },
        { type: 'note', value: 'В большинстве команд: rebase для синхронизации своей ветки, squash merge для PR в main. Это даёт чистую историю main (1 коммит = 1 фича) и возможность подробной работы в feature-ветке.' }
      ]
    },
    {
      id: 7,
      title: 'Практика: Interactive Rebase',
      type: 'practice',
      difficulty: 'medium',
      description: 'Напишите Java-программу, которая симулирует interactive rebase: принимает список коммитов и действия (pick, squash, fixup, reword, drop, reorder) и показывает результат.',
      requirements: [
        'Класс Commit с полями: hash, message, author, timestamp',
        'Enum RebaseAction: PICK, SQUASH, FIXUP, REWORD, DROP',
        'Класс RebaseInstruction: action + commit + (optional) newMessage',
        'Метод executeRebase(List<RebaseInstruction>) — применяет действия и возвращает результат',
        'Squash объединяет сообщения, fixup отбрасывает сообщение, drop удаляет коммит',
        'Продемонстрировать на примере: 5 коммитов → 2 после squash, fixup и drop'
      ],
      expectedOutput: 'Original commits:\n  abc1234 feat: создал UserService\n  def5678 WIP: начал валидацию\n  ghi9012 feat: добавил валидацию\n  jkl3456 fix: опечатка\n  mno7890 debug: временный лог\n\nRebase plan:\n  pick    abc1234 feat: создал UserService\n  squash  def5678 WIP: начал валидацию\n  squash  ghi9012 feat: добавил валидацию\n  fixup   jkl3456 fix: опечатка\n  drop    mno7890 debug: временный лог\n\nResult after rebase:\n  xyz0001 feat: создал UserService | WIP: начал валидацию | feat: добавил валидацию\n\nCommits: 5 -> 1',
      hint: 'Итерируйте по инструкциям: pick — добавляет в результат, squash — объединяет message с предыдущим, fixup — объединяет без message, drop — пропускает. Храните "текущий" коммит для объединения.',
      solution: 'import java.util.ArrayList;\nimport java.util.List;\n\npublic class Main {\n\n    enum RebaseAction {\n        PICK, SQUASH, FIXUP, REWORD, DROP\n    }\n\n    static class Commit {\n        String hash;\n        String message;\n\n        Commit(String hash, String message) {\n            this.hash = hash;\n            this.message = message;\n        }\n\n        Commit copy(String newHash) {\n            return new Commit(newHash, this.message);\n        }\n\n        @Override\n        public String toString() {\n            return hash + " " + message;\n        }\n    }\n\n    static class RebaseInstruction {\n        RebaseAction action;\n        Commit commit;\n        String newMessage; // для REWORD\n\n        RebaseInstruction(RebaseAction action, Commit commit) {\n            this.action = action;\n            this.commit = commit;\n        }\n\n        RebaseInstruction(RebaseAction action, Commit commit, String newMessage) {\n            this.action = action;\n            this.commit = commit;\n            this.newMessage = newMessage;\n        }\n    }\n\n    static List<Commit> executeRebase(List<RebaseInstruction> instructions) {\n        List<Commit> result = new ArrayList<>();\n        Commit current = null;\n        int counter = 1;\n\n        for (RebaseInstruction instr : instructions) {\n            switch (instr.action) {\n                case PICK:\n                    if (current != null) {\n                        result.add(current);\n                    }\n                    current = new Commit(\n                        String.format("xyz%04d", counter++),\n                        instr.commit.message\n                    );\n                    break;\n\n                case SQUASH:\n                    if (current != null) {\n                        current.message += " | " + instr.commit.message;\n                    }\n                    break;\n\n                case FIXUP:\n                    // Объединяем, но НЕ добавляем сообщение\n                    // (коммит сливается с предыдущим молча)\n                    break;\n\n                case REWORD:\n                    if (current != null) {\n                        result.add(current);\n                    }\n                    current = new Commit(\n                        String.format("xyz%04d", counter++),\n                        instr.newMessage != null ? instr.newMessage : instr.commit.message\n                    );\n                    break;\n\n                case DROP:\n                    // Коммит просто пропускается\n                    break;\n            }\n        }\n\n        if (current != null) {\n            result.add(current);\n        }\n\n        return result;\n    }\n\n    public static void main(String[] args) {\n        // Исходные коммиты\n        List<Commit> commits = List.of(\n            new Commit("abc1234", "feat: создал UserService"),\n            new Commit("def5678", "WIP: начал валидацию"),\n            new Commit("ghi9012", "feat: добавил валидацию"),\n            new Commit("jkl3456", "fix: опечатка"),\n            new Commit("mno7890", "debug: временный лог")\n        );\n\n        System.out.println("Original commits:");\n        for (Commit c : commits) {\n            System.out.println("  " + c);\n        }\n\n        // Rebase план\n        List<RebaseInstruction> plan = List.of(\n            new RebaseInstruction(RebaseAction.PICK, commits.get(0)),\n            new RebaseInstruction(RebaseAction.SQUASH, commits.get(1)),\n            new RebaseInstruction(RebaseAction.SQUASH, commits.get(2)),\n            new RebaseInstruction(RebaseAction.FIXUP, commits.get(3)),\n            new RebaseInstruction(RebaseAction.DROP, commits.get(4))\n        );\n\n        System.out.println("\\nRebase plan:");\n        for (RebaseInstruction instr : plan) {\n            System.out.printf("  %-7s %s%n", instr.action.name().toLowerCase(), instr.commit);\n        }\n\n        // Выполняем rebase\n        List<Commit> result = executeRebase(plan);\n\n        System.out.println("\\nResult after rebase:");\n        for (Commit c : result) {\n            System.out.println("  " + c);\n        }\n\n        System.out.println("\\nCommits: " + commits.size() + " -> " + result.size());\n    }\n}',
      explanation: 'Interactive rebase обрабатывает коммиты последовательно: pick создаёт новый коммит (с новым хешем), squash объединяет сообщение с предыдущим pick-коммитом, fixup тоже объединяет но отбрасывает сообщение, drop полностью пропускает коммит. Ключевое: все коммиты после rebase получают НОВЫЕ хеши, потому что у них изменился parent. Именно поэтому нужен force push после rebase.'
    }
  ]
}
