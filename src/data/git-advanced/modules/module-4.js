export default {
  id: 4,
  title: 'Cherry-pick и Revert',
  description: 'Выборочный перенос коммитов (cherry-pick), безопасная отмена изменений (revert), и восстановление после ошибок.',
  lessons: [
    {
      id: 1,
      title: 'Cherry-pick: основы',
      type: 'theory',
      content: [
        { type: 'text', value: 'git cherry-pick копирует конкретный коммит из одной ветки в другую. В отличие от merge (переносит ВСЕ коммиты), cherry-pick позволяет выбрать ОДИН конкретный коммит. Незаменим для hotfix-ов, backport-ов и точечных переносов.' },
        { type: 'heading', value: 'Базовый cherry-pick' },
        { type: 'code', language: 'bash', value: '# Сценарий: в feature-ветке есть баг-фикс, нужный в main\n# main:    A───B───C\n#               \\\n# feature:       D───E(fix)───F───G\n# Хотим коммит E перенести в main\n\n# 1. Находим хеш нужного коммита\ngit log --oneline feature\n# ghi9012 feat: новый функционал\n# def5678 feat: ещё код\n# abc1234 fix: критический баг в UserService  ← нужен этот\n# xyz7890 feat: начал фичу\n\n# 2. Переключаемся на main\ngit checkout main\n\n# 3. Cherry-pick коммит\ngit cherry-pick abc1234\n# Создаётся НОВЫЙ коммит abc9999 с тем же содержимым\n# Но с ДРУГИМ хешем (другой parent)\n\n# Результат:\n# main:    A───B───C───E\'(fix)\n#               \\\n# feature:       D───E(fix)───F───G\n\n# Cherry-pick нескольких коммитов:\ngit cherry-pick abc1234 def5678\n\n# Диапазон коммитов (исключая первый):\ngit cherry-pick abc1234..ghi9012\n\n# Диапазон (включая первый):\ngit cherry-pick abc1234^..ghi9012' },
        { type: 'heading', value: 'Cherry-pick: опции' },
        { type: 'code', language: 'bash', value: '# Без автоматического коммита (для редактирования):\ngit cherry-pick --no-commit abc1234\n# Изменения добавлены в stage, но не закоммичены\n# Можно изменить, добавить файлы и закоммитить вручную\ngit commit -m "fix: перенесённый фикс с доработкой"\n\n# Изменить сообщение коммита:\ngit cherry-pick --edit abc1234\n# Откроется редактор для нового сообщения\n\n# Добавить информацию о source:\ngit cherry-pick -x abc1234\n# Добавит в сообщение:\n# (cherry picked from commit abc1234...)\n\n# Сохранить оригинального автора:\ngit cherry-pick abc1234\n# По умолчанию: author = оригинальный, committer = вы\n# --signoff: добавить Signed-off-by\ngit cherry-pick --signoff abc1234\n\n# При конфликте:\ngit cherry-pick abc1234\n# CONFLICT (content): Merge conflict in UserService.java\n# Разрешаем конфликт:\ngit add UserService.java\ngit cherry-pick --continue\n\n# Или отменяем:\ngit cherry-pick --abort' },
        { type: 'tip', value: 'Cherry-pick с -x добавляет ссылку на оригинальный коммит — полезно для отслеживания backport-ов. В enterprise-проектах (банки, телеком) это обязательная практика.' }
      ]
    },
    {
      id: 2,
      title: 'Cherry-pick: сценарии использования',
      type: 'theory',
      content: [
        { type: 'text', value: 'Cherry-pick используется в реальных проектах для hotfix-ов, backport-ов в release-ветки, переноса конкретных фикcов между микросервисами и спасения работы из удалённых веток.' },
        { type: 'heading', value: 'Hotfix в production' },
        { type: 'code', language: 'bash', value: '# Сценарий: критический баг в production\n# develop:    A───B───C───D───E (разработка продолжается)\n# main/prod:  A───B (текущий релиз)\n\n# 1. Разработчик фиксит баг в develop\ngit checkout develop\ngit commit -m "fix(payment): исправил двойное списание"\n# Коммит: fix123\n\n# 2. Переносим фикс в production\ngit checkout main\ngit cherry-pick fix123\ngit tag -a v1.0.1 -m "Hotfix: двойное списание"\ngit push origin main --tags\n\n# Или через hotfix-ветку (Git Flow):\ngit checkout main\ngit checkout -b hotfix/double-charge\ngit cherry-pick fix123\ngit checkout main\ngit merge --no-ff hotfix/double-charge\ngit tag -a v1.0.1 -m "Hotfix: двойное списание"' },
        { type: 'heading', value: 'Backport в release branches' },
        { type: 'code', language: 'bash', value: '# Сценарий: поддерживаем несколько версий (как Spring Framework)\n# main:        v3.x (последняя)\n# release/2.x: v2.x (поддерживается)\n# release/1.x: v1.x (только security)\n\n# Security fix в main:\ngit checkout main\ngit commit -m "fix(security): CVE-2024-1234 SQL injection"\n# Коммит: sec456\n\n# Backport в release/2.x:\ngit checkout release/2.x\ngit cherry-pick -x sec456\n# Сообщение: fix(security): CVE-2024-1234 SQL injection\n#            (cherry picked from commit sec456)\ngit push origin release/2.x\n\n# Backport в release/1.x:\ngit checkout release/1.x\ngit cherry-pick -x sec456\n# Может быть конфликт — код отличается\n# Разрешаем и продолжаем\ngit push origin release/1.x\n\n# Автоматизация с GitHub Labels:\n# PR label "backport/2.x" → GitHub Action автоматически\n# cherry-pick в release/2.x\n\n# Kaspi, Halyk и другие банки:\n# Один фикс → cherry-pick в production + staging + dev\n# Трекинг: Jira тикет привязан к коммитам во всех ветках' },
        { type: 'warning', value: 'Cherry-pick создаёт ДУБЛИКАТ коммита (с другим хешем). Если потом мержить ветки — будет два одинаковых изменения. Git обычно справляется, но возможны конфликты.' }
      ]
    },
    {
      id: 3,
      title: 'Git Revert',
      type: 'theory',
      content: [
        { type: 'text', value: 'git revert создаёт НОВЫЙ коммит, который отменяет изменения указанного коммита. В отличие от reset (переписывает историю), revert безопасен для публичных веток — он не меняет существующие коммиты.' },
        { type: 'heading', value: 'Revert vs Reset' },
        { type: 'code', language: 'bash', value: '# Исходная история:\n# A───B───C───D (HEAD/main)\n# Хотим отменить коммит C\n\n# ═══ git revert C ═══\n# A───B───C───D───C\' (revert of C)\n# Создаёт НОВЫЙ коммит C\' с обратными изменениями\n# История НЕ переписывается — безопасно для main\n\n# ═══ git reset --hard B ═══\n# A───B (HEAD/main)\n# C и D УДАЛЕНЫ из истории\n# ОПАСНО для публичных веток!\n\n# Когда использовать:\n# revert → публичные ветки (main, develop), командная работа\n# reset  → локальные ветки, личные эксперименты\n\n# Базовый revert:\ngit revert abc1234\n# Откроется редактор для сообщения:\n# Revert "feat: добавил функцию X"\n# This reverts commit abc1234...\n\n# Revert без автокоммита:\ngit revert --no-commit abc1234\n# Изменения в stage, можно доработать перед коммитом\n\n# Revert нескольких коммитов:\ngit revert abc1234 def5678 ghi9012\n\n# Revert диапазона (от старого к новому):\ngit revert abc1234..ghi9012\n# Ревертит каждый коммит отдельным revert-коммитом' },
        { type: 'heading', value: 'Revert merge-коммита' },
        { type: 'code', language: 'bash', value: '# Revert merge-коммита — особый случай\n# Merge-коммит имеет ДВА parent-а\n\ngit log --oneline\n# abc1234 Merge branch \'feature/payment\' into main\n# ...\n\n# При revert нужно указать какой parent оставить:\ngit revert -m 1 abc1234\n# -m 1: оставить первый parent (main)\n# -m 2: оставить второй parent (feature)\n\n# -m 1 чаще всего: "отменить фичу, вернуться к main"\n# Это отменит ВСЕ изменения, внесённые feature-веткой\n\n# ВАЖНО: после revert merge, повторный merge feature НЕ СРАБОТАЕТ!\n# Git считает что эти коммиты уже были в main (и были ревертнуты)\n\n# Решение: "revert the revert"\n# 1. Revert merge → main без фичи\ngit revert -m 1 <merge-commit>\n# 2. Исправляем фичу в feature-ветке\n# 3. Revert the revert → возвращаем изменения\ngit revert <revert-commit>\n# 4. Merge исправленную фичу\ngit merge feature/payment\n\n# Или проще: после revert создать новую ветку\ngit checkout -b feature/payment-v2 feature/payment\n# Внести исправления и создать новый PR' },
        { type: 'warning', value: 'Revert merge-коммита создаёт "ловушку": если потом мержить ту же ветку — изменения не появятся. Нужно сделать "revert of revert" или создать новую ветку. Это один из самых частых источников путаницы в Git.' }
      ]
    },
    {
      id: 4,
      title: 'Восстановление после ошибок',
      type: 'theory',
      content: [
        { type: 'text', value: 'Git почти никогда не удаляет данные. Даже после reset --hard, удаления веток и ребейза — данные можно восстановить через reflog, fsck и другие инструменты. Главное — знать куда смотреть.' },
        { type: 'heading', value: 'Сценарии восстановления' },
        { type: 'code', language: 'bash', value: '# === Случайный reset --hard ===\n# Ой! Удалил 3 коммита:\ngit reset --hard HEAD~3\n\n# Восстановление через reflog:\ngit reflog\n# abc1234 HEAD@{0}: reset: moving to HEAD~3\n# def5678 HEAD@{1}: commit: feat: важная фича  ← сюда!\ngit reset --hard def5678\n# Или создать ветку:\ngit branch recovery def5678\n\n# === Удалённая ветка ===\ngit branch -D feature/important\n# Восстановление:\ngit reflog | grep "feature/important"\n# abc1234 HEAD@{5}: checkout: moving from main to feature/important\ngit checkout -b feature/important abc1234\n\n# === Потерянный коммит после rebase ===\ngit rebase -i HEAD~5  # squash всё, потом поняли что зря\ngit reflog\n# Находим коммит ДО rebase:\n# xyz7890 HEAD@{6}: rebase (start): checkout HEAD~5\ngit reset --hard HEAD@{7}  # состояние до rebase\n\n# === Случайный git checkout -- файл ===\ngit checkout -- important-file.java\n# Файл перезаписан версией из index.\n# Если файл был в stage:\ngit checkout HEAD -- important-file.java\n# Если нет — данные потеряны (не были в Git)' },
        { type: 'heading', value: 'git fsck и lost-found' },
        { type: 'code', language: 'bash', value: '# git fsck — проверка целостности + поиск "сирот"\ngit fsck --lost-found\n# dangling commit abc1234...\n# dangling blob def5678...\n\n# Посмотреть dangling коммиты:\ngit fsck --lost-found --no-reflogs\nls .git/lost-found/commit/\n\n# Проверить содержимое:\ngit show abc1234\ngit log --oneline abc1234\n\n# Восстановить:\ngit cherry-pick abc1234\n# или:\ngit branch recovered abc1234\n\n# === Когда данные НЕЛЬЗЯ восстановить ===\n# 1. Незакоммиченные, не-staged изменения после checkout/reset\n# 2. Файлы после git clean -f\n# 3. После git gc --prune=now (если прошло 30+ дней)\n# 4. git stash drop без запоминания хеша\n\n# Правила безопасности:\n# ✅ Коммить рано и часто (даже WIP)\n# ✅ Stash перед опасными операциями\n# ✅ Используй --force-with-lease вместо --force\n# ✅ Проверяй reflog перед паникой\n# ✅ git status / git stash list перед checkout' },
        { type: 'tip', value: 'Если не уверены — создайте backup-ветку перед опасной операцией: git branch backup/before-rebase. Это занимает 0 секунд и может спасти часы работы.' }
      ]
    },
    {
      id: 5,
      title: 'Git Restore и Switch',
      type: 'theory',
      content: [
        { type: 'text', value: 'Git 2.23 ввёл git restore и git switch — более понятные альтернативы перегруженному git checkout. restore — для файлов, switch — для веток. Это разделение делает Git менее запутанным.' },
        { type: 'heading', value: 'git switch — переключение веток' },
        { type: 'code', language: 'bash', value: '# Раньше (git checkout — делает всё):\ngit checkout main                      # переключить ветку\ngit checkout -b feature/new            # создать и переключить\ngit checkout -- file.java              # восстановить файл\ngit checkout abc1234                   # detached HEAD\n# Слишком много ответственностей!\n\n# Теперь (git switch — только ветки):\ngit switch main                        # переключить ветку\ngit switch -c feature/new              # создать и переключить (-c = create)\ngit switch -c feature/new origin/main  # создать от другой ветки\ngit switch -                           # предыдущая ветка\ngit switch --detach abc1234            # явный detached HEAD\n\n# Безопасность: switch откажется если есть несохранённые изменения\ngit switch main\n# error: Your local changes would be overwritten\n# Решение:\ngit stash && git switch main\n# или: git switch -f main  (force — потеряет изменения!)' },
        { type: 'heading', value: 'git restore — восстановление файлов' },
        { type: 'code', language: 'bash', value: '# git restore — только для файлов\n\n# Отменить изменения в working directory:\ngit restore file.java\n# Эквивалент: git checkout -- file.java\n\n# Убрать файл из staging (unstage):\ngit restore --staged file.java\n# Эквивалент: git reset HEAD file.java\n\n# Восстановить из конкретного коммита:\ngit restore --source=abc1234 file.java\n# Эквивалент: git checkout abc1234 -- file.java\n\n# Восстановить и в stage и в working directory:\ngit restore --source=HEAD~3 --staged --worktree file.java\n\n# Восстановить всё:\ngit restore .\n# Отменяет все изменения в working directory\n\n# Unstage всё:\ngit restore --staged .\n\n# Сравнение команд:\n# Старый способ:                    Новый способ:\n# git checkout -- file.java    →    git restore file.java\n# git reset HEAD file.java     →    git restore --staged file.java\n# git checkout abc -- file     →    git restore --source=abc file\n# git checkout branch          →    git switch branch\n# git checkout -b new          →    git switch -c new' },
        { type: 'note', value: 'git switch и git restore — рекомендуемые команды начиная с Git 2.23. Они безопаснее и понятнее чем перегруженный checkout. Старый checkout по-прежнему работает, но в новых проектах лучше использовать switch/restore.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Cherry-pick менеджер',
      type: 'practice',
      difficulty: 'medium',
      description: 'Напишите Java-программу, которая симулирует cherry-pick и revert операции на графе коммитов. Программа должна уметь переносить коммиты между ветками и создавать revert-коммиты.',
      requirements: [
        'Класс Branch с именем и списком коммитов',
        'Метод cherryPick(Branch target, String commitHash) — копирует коммит в целевую ветку',
        'Метод revert(Branch branch, String commitHash) — создаёт revert-коммит',
        'Cherry-pick должен создавать НОВЫЙ коммит с новым хешем но тем же содержимым',
        'Revert должен создавать коммит "Revert: <original message>"',
        'Продемонстрировать сценарий: hotfix cherry-pick из develop в main + revert'
      ],
      expectedOutput: 'Branch: develop\n  d001 feat: UserService\n  d002 feat: OrderService\n  d003 fix: критический баг оплаты\n  d004 feat: NotificationService\n\nBranch: main\n  m001 release: v1.0.0\n  m002 docs: обновил README\n\nCherry-pick d003 -> main...\n\nBranch: main (after cherry-pick)\n  m001 release: v1.0.0\n  m002 docs: обновил README\n  cp01 fix: критический баг оплаты\n\nRevert cp01 in main...\n\nBranch: main (after revert)\n  m001 release: v1.0.0\n  m002 docs: обновил README\n  cp01 fix: критический баг оплаты\n  rv01 Revert: fix: критический баг оплаты',
      hint: 'Cherry-pick: найдите коммит по хешу в source-ветке, создайте новый коммит с уникальным хешем (cp01, cp02...) и тем же message. Revert: создайте коммит с "Revert: " + original message.',
      solution: 'import java.util.ArrayList;\nimport java.util.List;\n\npublic class Main {\n\n    static class Commit {\n        String hash;\n        String message;\n        String originalHash; // для cherry-pick: ссылка на оригинал\n\n        Commit(String hash, String message) {\n            this.hash = hash;\n            this.message = message;\n        }\n\n        @Override\n        public String toString() {\n            return hash + " " + message;\n        }\n    }\n\n    static class Branch {\n        String name;\n        List<Commit> commits = new ArrayList<>();\n\n        Branch(String name) {\n            this.name = name;\n        }\n\n        void addCommit(String hash, String message) {\n            commits.add(new Commit(hash, message));\n        }\n\n        Commit findCommit(String hash) {\n            return commits.stream()\n                .filter(c -> c.hash.equals(hash))\n                .findFirst()\n                .orElse(null);\n        }\n\n        void print() {\n            System.out.println("Branch: " + name);\n            for (Commit c : commits) {\n                System.out.println("  " + c);\n            }\n        }\n    }\n\n    static int cpCounter = 1;\n    static int rvCounter = 1;\n\n    static Commit cherryPick(Branch source, Branch target, String commitHash) {\n        Commit original = source.findCommit(commitHash);\n        if (original == null) {\n            System.out.println("Commit " + commitHash + " not found!");\n            return null;\n        }\n\n        String newHash = String.format("cp%02d", cpCounter++);\n        Commit picked = new Commit(newHash, original.message);\n        picked.originalHash = original.hash;\n        target.commits.add(picked);\n\n        System.out.println("\\nCherry-pick " + commitHash + " -> " + target.name + "...");\n        return picked;\n    }\n\n    static Commit revert(Branch branch, String commitHash) {\n        Commit original = branch.findCommit(commitHash);\n        if (original == null) {\n            System.out.println("Commit " + commitHash + " not found!");\n            return null;\n        }\n\n        String newHash = String.format("rv%02d", rvCounter++);\n        Commit revertCommit = new Commit(newHash, "Revert: " + original.message);\n        branch.commits.add(revertCommit);\n\n        System.out.println("\\nRevert " + commitHash + " in " + branch.name + "...");\n        return revertCommit;\n    }\n\n    public static void main(String[] args) {\n        // Создаём ветки\n        Branch develop = new Branch("develop");\n        develop.addCommit("d001", "feat: UserService");\n        develop.addCommit("d002", "feat: OrderService");\n        develop.addCommit("d003", "fix: критический баг оплаты");\n        develop.addCommit("d004", "feat: NotificationService");\n\n        Branch mainBranch = new Branch("main");\n        mainBranch.addCommit("m001", "release: v1.0.0");\n        mainBranch.addCommit("m002", "docs: обновил README");\n\n        develop.print();\n        System.out.println();\n        mainBranch.print();\n\n        // Cherry-pick hotfix из develop в main\n        cherryPick(develop, mainBranch, "d003");\n        System.out.println();\n        mainBranch.print();\n\n        // Revert cherry-picked коммита\n        revert(mainBranch, "cp01");\n        System.out.println();\n        mainBranch.print();\n    }\n}',
      explanation: 'Cherry-pick создаёт КОПИЮ коммита с новым хешем в целевой ветке. Оригинальный коммит остаётся в source-ветке. Revert не удаляет коммит, а создаёт НОВЫЙ коммит с обратными изменениями. Это безопасно для публичных веток, потому что история не переписывается. В реальном Git cherry-pick вычисляет diff оригинального коммита и применяет его к текущей ветке, что может вызвать конфликты.'
    }
  ]
}
