export default {
  id: 8,
  title: 'Git Bisect',
  description: 'Бинарный поиск багов с git bisect: start, good/bad, skip, автоматизация с run, практические примеры поиска регрессий.',
  lessons: [
    {
      id: 1,
      title: 'Введение в Git Bisect',
      type: 'theory',
      content: [
        { type: 'text', value: 'Git bisect — мощный инструмент для поиска коммита, который привнёс баг. Он использует бинарный поиск по истории коммитов: вместо проверки каждого коммита последовательно, bisect делит диапазон пополам и проверяет средний коммит. Это сокращает поиск с O(n) до O(log n).' },
        { type: 'heading', value: 'Принцип работы' },
        { type: 'code', language: 'bash', value: '# Представим 1000 коммитов между "работает" и "сломалось"\n# Линейный поиск: до 1000 проверок\n# Бинарный поиск (bisect): ~10 проверок (log2(1000) ≈ 10)\n\n# Алгоритм:\n# 1. Указываем "плохой" коммит (с багом) — обычно HEAD\n# 2. Указываем "хороший" коммит (без бага) — последняя рабочая версия\n# 3. Git выбирает средний коммит между ними\n# 4. Мы проверяем: есть баг? → bad / нет бага? → good\n# 5. Git сужает диапазон вдвое и выбирает следующий\n# 6. Повторяем, пока не найдём первый "плохой" коммит\n\n# Визуализация:\n# good ─── ? ─── ? ─── ? ─── ? ─── ? ─── bad\n#                      ↑\n#                  проверяем (середина)\n#\n# Если middle = bad:\n# good ─── ? ─── bad ─── ─── ─── ─── ─── bad\n#           ↑\n#       проверяем\n#\n# Если middle = good:\n# good ─── good ─── ─── ? ─── ─── ─── ─── bad\n#                        ↑\n#                    проверяем' },
        { type: 'heading', value: 'Когда использовать bisect' },
        { type: 'code', language: 'bash', value: '# Типичные сценарии для git bisect:\n\n# 1. Регрессия: "Раньше работало, теперь нет"\n#    Тесты проходили неделю назад, сейчас падают\n\n# 2. Баг неизвестного происхождения:\n#    "В какой момент сломался экспорт PDF?"\n\n# 3. Проблемы производительности:\n#    "Когда API стал отвечать за 5 секунд вместо 200ms?"\n\n# 4. Визуальные регрессии:\n#    "Когда кнопка стала красной вместо синей?"\n\n# 5. Анализ:\n#    "В каком коммите был добавлен этот файл/функция?"\n#    (Хотя для этого лучше git log --follow)\n\n# НЕ подходит, когда:\n# - Баг существовал всегда (нет "good" коммита)\n# - Нет способа быстро проверить наличие бага\n# - История нелинейная с множеством merge-коммитов' },
        { type: 'tip', value: 'Git bisect особенно эффективен в больших проектах с сотнями коммитов между релизами. Вместо "кто-то когда-то что-то сломал" вы за 10 шагов находите конкретный коммит и конкретного автора.' }
      ]
    },
    {
      id: 2,
      title: 'git bisect start, good, bad',
      type: 'theory',
      content: [
        { type: 'text', value: 'Базовый workflow git bisect: запускаем сессию, отмечаем хороший и плохой коммиты, затем Git автоматически переключает нас на средний коммит для проверки. Мы отвечаем good/bad, и Git сужает диапазон.' },
        { type: 'heading', value: 'Базовый workflow' },
        { type: 'code', language: 'bash', value: '# Шаг 1: Начинаем bisect\ngit bisect start\n\n# Шаг 2: Отмечаем текущий коммит как "плохой" (есть баг)\ngit bisect bad\n# Или указываем конкретный коммит:\ngit bisect bad HEAD\ngit bisect bad abc1234\n\n# Шаг 3: Отмечаем коммит, где бага НЕ было\n# Это может быть тег релиза, дата или хеш\ngit bisect good v2.0.0\ngit bisect good abc5678\n\n# Git выводит:\n# Bisecting: 50 revisions left to test after this (roughly 6 steps)\n# [hash] commit message\n\n# Шаг 4: Проверяем текущий коммит\n# Запускаем тесты / проверяем руками\nmvn test -pl module-auth\n# или открываем приложение и проверяем\n\n# Шаг 5: Отмечаем результат\ngit bisect good   # Баг отсутствует\n# или\ngit bisect bad    # Баг присутствует\n\n# Git выбирает следующий коммит:\n# Bisecting: 25 revisions left to test after this (roughly 5 steps)\n\n# Повторяем шаги 4-5 пока Git не найдёт виновника:\n# abc1234 is the first bad commit\n# commit abc1234\n# Author: developer@team.com\n# Date: Mon Jan 15 14:30:00 2024\n#\n#     refactor(payment): change decimal precision\n\n# Шаг 6: Завершаем bisect (ОБЯЗАТЕЛЬНО!)\ngit bisect reset\n# Возвращает на исходную ветку' },
        { type: 'heading', value: 'Сокращённый запуск' },
        { type: 'code', language: 'bash', value: '# Можно указать good и bad сразу при start:\ngit bisect start HEAD v2.0.0\n# Эквивалентно:\n#   git bisect start\n#   git bisect bad HEAD\n#   git bisect good v2.0.0\n\n# Ещё короче — с указанием пути для ускорения:\ngit bisect start HEAD v2.0.0 -- src/main/java/com/app/payment/\n# Git будет проверять только коммиты, затрагивающие payment/\n\n# Просмотр текущего состояния bisect:\ngit bisect log\n# git bisect start\n# # status: waiting for both good and bad commits\n# git bisect bad abc1234\n# git bisect good def5678\n# git bisect good ghi9012\n# git bisect bad jkl3456\n\n# Визуализация оставшихся коммитов:\ngit bisect visualize\n# или\ngit bisect view\n# Открывает gitk с оставшимися коммитами' },
        { type: 'warning', value: 'Всегда завершайте bisect командой git bisect reset! Иначе вы останетесь в detached HEAD состоянии, и Git не даст вам работать нормально. Если забыли — git bisect reset вернёт вас на исходную ветку.' }
      ]
    },
    {
      id: 3,
      title: 'git bisect skip',
      type: 'theory',
      content: [
        { type: 'text', value: 'Иногда при bisect текущий коммит невозможно проверить: не компилируется, нет нужных данных, сломан по другой причине. Команда git bisect skip пропускает такой коммит, и Git выбирает соседний для проверки.' },
        { type: 'heading', value: 'Использование skip' },
        { type: 'code', language: 'bash', value: '# Начинаем bisect\ngit bisect start HEAD v2.0.0\n\n# Git переключил на коммит, но он не компилируется\nmvn compile\n# BUILD FAILURE\n# Невозможно определить, есть ли баг\n\n# Пропускаем этот коммит\ngit bisect skip\n# Bisecting: 24 revisions left to test after this (roughly 5 steps)\n# [другой хеш] другой коммит\n\n# Можно пропустить диапазон коммитов\ngit bisect skip abc1234..def5678\n\n# Можно пропустить текущий и указать коммит\ngit bisect skip HEAD\n\n# ВАЖНО: skip может привести к неточному результату\n# Если слишком много коммитов пропущено, Git предупредит:\n# "There are only skip\'ped commits left to test.\n#  The first bad commit could be any of:\n#  abc1234\n#  def5678\n#  ghi9012\n#  We cannot bisect more!"' },
        { type: 'heading', value: 'Стратегии при проблемных коммитах' },
        { type: 'code', language: 'bash', value: '# Частая проблема: merge-коммиты в bisect\n# Merge-коммит может быть "сломан" из-за конфликтов\n# Решение: использовать --first-parent\ngit bisect start --first-parent HEAD v2.0.0\n# Проверяет только коммиты на основной линии (без merge)\n\n# Проблема: коммит не компилируется\n# Стратегия 1: пропустить\ngit bisect skip\n\n# Стратегия 2: ручная починка для проверки\n# (не коммитить, просто проверить)\nmvn compile -DskipTests\n# Если удалось — проверяем баг, отмечаем good/bad\n# Если нет — skip\n\n# Проблема: нужно применить hotfix для компиляции\n# Используем stash:\ngit stash\n# Правим код для компиляции\nvim src/Main.java\nmvn compile\n# Проверяем баг\ngit bisect good  # или bad\ngit stash pop    # Git переключит коммит, stash автоматически уйдёт\n\n# Лучшая стратегия: replay bisect с логом\ngit bisect log > bisect.log\n# Отредактировать лог (убрать skip\'нутые)\nvim bisect.log\n# Проиграть заново:\ngit bisect replay bisect.log' },
        { type: 'tip', value: 'Если skip используется слишком часто, это сигнал о проблемах: коммиты не атомарны (не каждый коммит компилируется). В здоровом проекте каждый коммит должен оставлять проект в рабочем состоянии.' }
      ]
    },
    {
      id: 4,
      title: 'git bisect run: автоматизация',
      type: 'theory',
      content: [
        { type: 'text', value: 'git bisect run — самая мощная фича bisect. Вместо ручной проверки каждого коммита, вы даёте bisect скрипт (тест), и он автоматически проходит весь бинарный поиск без вашего участия.' },
        { type: 'heading', value: 'Автоматический bisect со скриптом' },
        { type: 'code', language: 'bash', value: '# Формат: git bisect run <команда>\n# Exit code 0 = good (нет бага)\n# Exit code 1-124, 126-127 = bad (есть баг)\n# Exit code 125 = skip (невозможно проверить)\n\n# Пример 1: Поиск коммита, ломающего тест\ngit bisect start HEAD v2.0.0\ngit bisect run mvn test -pl payment-service -Dtest=PaymentTest\n# Git автоматически прогонит все шаги!\n# Результат: abc1234 is the first bad commit\ngit bisect reset\n\n# Пример 2: Поиск коммита, где Spring Boot не стартует\ngit bisect start HEAD v2.0.0\ngit bisect run bash -c "mvn spring-boot:run & sleep 15 && curl -f http://localhost:8080/health; kill %1"\n\n# Пример 3: Проверка наличия строки в файле\n# "Когда пропала валидация email?"\ngit bisect start HEAD v1.0.0\ngit bisect run grep -q "validateEmail" src/main/java/com/app/UserService.java\n# grep возвращает 0 если найдено (good), 1 если нет (bad)\ngit bisect reset\n\n# Пример 4: Проверка размера бандла\ngit bisect start HEAD v2.0.0\ngit bisect run bash -c "mvn package -q && [ $(stat -f%z target/app.jar) -lt 52428800 ]"\n# Ищем коммит, где JAR стал больше 50MB\ngit bisect reset' },
        { type: 'heading', value: 'Скрипт для bisect run' },
        { type: 'code', language: 'bash', value: '#!/bin/bash\n# bisect-test.sh — скрипт для git bisect run\nset -e\n\n# 1. Попробуем скомпилировать\nmvn compile -q 2>/dev/null\nif [ $? -ne 0 ]; then\n    # Не компилируется — skip (exit 125)\n    exit 125\nfi\n\n# 2. Запускаем конкретный тест\nmvn test -pl payment-service \\\n    -Dtest="PaymentServiceTest#testCalculateTotal" \\\n    -q 2>/dev/null\n\n# Exit code автоматически передаётся:\n# 0 = тест прошёл = good\n# 1 = тест упал = bad\n\n# Использование:\n# chmod +x bisect-test.sh\n# git bisect start HEAD v2.0.0\n# git bisect run ./bisect-test.sh\n# git bisect reset\n\n# ───────────────────────────────────────\n\n# Альтернативный скрипт: проверка производительности\n#!/bin/bash\n# bisect-perf.sh\nmvn compile -q 2>/dev/null || exit 125\n\n# Запускаем бенчмарк\nRESULT=$(mvn exec:java -Dexec.mainClass="com.app.Benchmark" -q 2>&1 | tail -1)\nTIME_MS=$(echo "$RESULT" | grep -oE "[0-9]+")\n\nif [ "$TIME_MS" -gt 1000 ]; then\n    echo "Too slow: ${TIME_MS}ms > 1000ms"\n    exit 1  # bad\nelse\n    echo "Fast enough: ${TIME_MS}ms"\n    exit 0  # good\nfi' },
        { type: 'tip', value: 'git bisect run — идеальный инструмент для регрессий. Написали тест, который ловит баг? Запустите bisect run с этим тестом, и Git сам найдёт виновника. Весь процесс занимает минуты вместо часов ручного поиска.' }
      ]
    },
    {
      id: 5,
      title: 'Практика: Реализация бинарного поиска',
      type: 'practice',
      difficulty: 'medium',
      description: 'Напишите Java-программу, которая симулирует работу git bisect: бинарный поиск по массиву коммитов для нахождения первого "плохого" коммита.',
      requirements: [
        'Класс Commit с полями hash, message, isBuggy',
        'Класс GitBisect с методами start(), markGood(), markBad(), skip(), getCurrentCommit(), getResult()',
        'Бинарный поиск: bisect выбирает средний коммит между good и bad',
        'Метод autoRun(predicate) — аналог git bisect run',
        'Продемонстрировать ручной и автоматический режим'
      ],
      expectedOutput: 'History: 16 commits\n\n=== Manual bisect ===\nBisect started: 16 commits to check (~4 steps)\nStep 1: Checking commit #8 "add caching"\n  → Marking as GOOD\nStep 2: Checking commit #12 "change decimal precision"\n  → Marking as BAD\nStep 3: Checking commit #10 "refactor payment"\n  → Marking as GOOD\nStep 4: Checking commit #11 "update exchange rates"\n  → Marking as BAD\nFound! First bad commit: #11 "update exchange rates"\n\n=== Auto bisect (bisect run) ===\nAuto bisect: checking commit #8 "add caching" → GOOD\nAuto bisect: checking commit #12 "change decimal precision" → BAD\nAuto bisect: checking commit #10 "refactor payment" → GOOD\nAuto bisect: checking commit #11 "update exchange rates" → BAD\nAuto run found: #11 "update exchange rates"',
      hint: 'Бинарный поиск: low = индекс good коммита, high = индекс bad коммита. Средний: mid = (low + high) / 2. Если mid — good, двигаем low = mid. Если bad — двигаем high = mid. Продолжаем пока high - low > 1.',
      solution: 'import java.util.ArrayList;\nimport java.util.List;\nimport java.util.function.Predicate;\n\npublic class Main {\n\n    static class Commit {\n        int number;\n        String hash;\n        String message;\n        boolean isBuggy;\n\n        Commit(int number, String hash, String message, boolean isBuggy) {\n            this.number = number;\n            this.hash = hash;\n            this.message = message;\n            this.isBuggy = isBuggy;\n        }\n\n        @Override\n        public String toString() {\n            return "#" + number + " \\"" + message + "\\"";\n        }\n    }\n\n    static class GitBisect {\n        private List<Commit> commits;\n        private int low;   // good boundary\n        private int high;  // bad boundary\n        private int current;\n        private int steps;\n        private boolean finished;\n        private Commit result;\n\n        void start(List<Commit> commits, int goodIndex, int badIndex) {\n            this.commits = commits;\n            this.low = goodIndex;\n            this.high = badIndex;\n            this.steps = 0;\n            this.finished = false;\n            this.result = null;\n            int total = high - low;\n            int estimatedSteps = (int) Math.ceil(Math.log(total) / Math.log(2));\n            System.out.println("Bisect started: " + total + " commits to check (~" + estimatedSteps + " steps)");\n            selectMiddle();\n        }\n\n        private void selectMiddle() {\n            current = (low + high) / 2;\n        }\n\n        Commit getCurrentCommit() {\n            return commits.get(current);\n        }\n\n        void markGood() {\n            steps++;\n            System.out.println("Step " + steps + ": Checking commit " + getCurrentCommit());\n            System.out.println("  → Marking as GOOD");\n            low = current;\n            checkFinished();\n        }\n\n        void markBad() {\n            steps++;\n            System.out.println("Step " + steps + ": Checking commit " + getCurrentCommit());\n            System.out.println("  → Marking as BAD");\n            high = current;\n            checkFinished();\n        }\n\n        private void checkFinished() {\n            if (high - low <= 1) {\n                finished = true;\n                result = commits.get(high);\n                System.out.println("Found! First bad commit: " + result);\n            } else {\n                selectMiddle();\n            }\n        }\n\n        boolean isFinished() {\n            return finished;\n        }\n\n        Commit getResult() {\n            return result;\n        }\n\n        // Аналог git bisect run\n        Commit autoRun(List<Commit> commits, int goodIndex, int badIndex, Predicate<Commit> isBuggy) {\n            int lo = goodIndex;\n            int hi = badIndex;\n\n            while (hi - lo > 1) {\n                int mid = (lo + hi) / 2;\n                Commit c = commits.get(mid);\n                boolean buggy = isBuggy.test(c);\n                System.out.println("Auto bisect: checking commit " + c + " → " + (buggy ? "BAD" : "GOOD"));\n                if (buggy) {\n                    hi = mid;\n                } else {\n                    lo = mid;\n                }\n            }\n            return commits.get(hi);\n        }\n    }\n\n    public static void main(String[] args) {\n        // Создаём историю коммитов (баг появляется с коммита #11)\n        List<Commit> history = new ArrayList<>();\n        String[] messages = {\n            "initial commit", "add user model", "add auth service",\n            "add REST controller", "add validation", "add tests",\n            "add logging", "add caching", "optimize queries",\n            "refactor payment", "update exchange rates", "change decimal precision",\n            "add monitoring", "update dependencies", "add docs", "release prep"\n        };\n        for (int i = 0; i < 16; i++) {\n            String hash = String.format("%07x", (i + 1) * 1111);\n            history.add(new Commit(i + 1, hash, messages[i], i >= 10)); // bug from #11 (index 10)\n        }\n        System.out.println("History: " + history.size() + " commits");\n\n        // === Ручной bisect ===\n        System.out.println("\\n=== Manual bisect ===");\n        GitBisect bisect = new GitBisect();\n        bisect.start(history, 0, 15); // good=#1, bad=#16\n\n        while (!bisect.isFinished()) {\n            Commit c = bisect.getCurrentCommit();\n            if (c.isBuggy) {\n                bisect.markBad();\n            } else {\n                bisect.markGood();\n            }\n        }\n\n        // === Автоматический bisect run ===\n        System.out.println("\\n=== Auto bisect (bisect run) ===");\n        Commit found = bisect.autoRun(history, 0, 15, c -> c.isBuggy);\n        System.out.println("Auto run found: " + found);\n    }\n}',
      explanation: 'Git bisect использует бинарный поиск: задаём границы (good/bad) и делим диапазон пополам на каждом шаге. Для 1000 коммитов нужно ~10 проверок. Команда git bisect run автоматизирует процесс — передаём скрипт (тест), и Git сам прогоняет бинарный поиск без участия человека. В нашей симуляции Predicate<Commit> играет роль такого скрипта.'
    }
  ]
}
