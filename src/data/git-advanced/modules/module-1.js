export default {
  id: 1,
  title: 'Git под капотом',
  description: 'Внутреннее устройство Git: объекты (blob, tree, commit, tag), хеши SHA-1, refs, reflog, packfiles и garbage collection.',
  lessons: [
    {
      id: 1,
      title: 'Объектная модель Git',
      type: 'theory',
      content: [
        { type: 'text', value: 'Git — это по сути content-addressable файловая система. Каждый объект хранится под ключом SHA-1 хеша от его содержимого. Понимание внутреннего устройства Git — ключ к продвинутому использованию: rebase, cherry-pick, восстановление данных.' },
        { type: 'heading', value: 'Четыре типа объектов Git' },
        { type: 'code', language: 'bash', value: '# Git хранит данные как 4 типа объектов:\n\n# 1. BLOB — содержимое файла (без имени, без метаданных)\n# Хранит только сырые данные файла\necho "Hello World" | git hash-object --stdin\n# aaf4c61ddcc5e8a2dabede0f3b482cd9aea9434d\n\n# 2. TREE — директория (список blob-ов и вложенных tree)\n# Как ls: содержит имена файлов, права доступа и ссылки на blob/tree\ngit ls-tree HEAD\n# 100644 blob abc123  README.md\n# 040000 tree def456  src/\n\n# 3. COMMIT — снимок проекта\n# Содержит: tree (корневая директория), parent (предыдущий коммит),\n# author, committer, message\ngit cat-file -p HEAD\n# tree 4b825dc642cb6eb9a060e54bf899d3e23...\n# parent 9a5f8e3b7c1d2f4e6a8b0c3d5e7f9a1b...\n# author Nurdaulet <email> 1700000000 +0600\n# committer Nurdaulet <email> 1700000000 +0600\n#\n# feat: добавил авторизацию\n\n# 4. TAG (annotated) — ссылка на коммит с метаданными\ngit tag -a v1.0 -m "Релиз 1.0"\ngit cat-file -p v1.0\n# object abc123...\n# type commit\n# tag v1.0\n# tagger Nurdaulet <email>' },
        { type: 'heading', value: 'Структура .git директории' },
        { type: 'code', language: 'bash', value: '# Вся магия Git — в директории .git/\nls -la .git/\n\n# objects/     — все объекты (blob, tree, commit, tag)\n# refs/        — ветки и теги (ссылки на коммиты)\n#   heads/     — локальные ветки (refs/heads/main)\n#   remotes/   — удалённые ветки (refs/remotes/origin/main)\n#   tags/      — теги\n# HEAD         — текущая ветка (ref: refs/heads/main)\n# index        — staging area (подготовленные к коммиту файлы)\n# config       — конфигурация репозитория\n# hooks/       — git hooks (скрипты автоматизации)\n# logs/        — reflog (история перемещений HEAD)\n\n# Посмотреть на что указывает HEAD\ncat .git/HEAD\n# ref: refs/heads/main\n\n# Посмотреть хеш текущего коммита\ncat .git/refs/heads/main\n# 9a5f8e3b7c1d2f4e6a8b0c3d5e7f9a1b3c5d7e9f' },
        { type: 'tip', value: 'Каждый коммит хранит ссылку на полный снимок проекта (tree), а не дифф. Git оптимизирует хранение через packfiles — если файл не изменился, blob остаётся тем же.' }
      ]
    },
    {
      id: 2,
      title: 'SHA-1 хеши и адресация',
      type: 'theory',
      content: [
        { type: 'text', value: 'Каждый объект в Git идентифицируется SHA-1 хешем (40 символов hex). Этот хеш вычисляется от типа объекта, размера и содержимого. Одинаковое содержимое всегда даёт одинаковый хеш — это основа дедупликации и целостности.' },
        { type: 'heading', value: 'Работа с хешами' },
        { type: 'code', language: 'bash', value: '# Вычислить хеш файла без добавления в репозиторий\necho "Hello" | git hash-object --stdin\n# ce013625030ba8dba906f756967f9e9ca394464a\n\n# Записать объект в .git/objects\necho "Hello" | git hash-object -w --stdin\n\n# Прочитать объект по хешу\ngit cat-file -t ce0136    # тип: blob\ngit cat-file -p ce0136    # содержимое: Hello\ngit cat-file -s ce0136    # размер: 6 байт\n\n# Как Git вычисляет хеш:\n# SHA-1("blob 6\\0Hello\\n")\n# Формат: "<тип> <размер>\\0<содержимое>"\n\n# Проверка в Python:\n# import hashlib\n# content = "Hello\\n"\n# header = f"blob {len(content)}\\0"\n# hashlib.sha1((header + content).encode()).hexdigest()\n# ce013625030ba8dba906f756967f9e9ca394464a' },
        { type: 'heading', value: 'Сокращённые хеши и ревизии' },
        { type: 'code', language: 'bash', value: '# Сокращённый хеш — первые 7+ символов (уникальных в рамках репо)\ngit log --oneline\n# abc1234 feat: авторизация\n# def5678 fix: исправил баг\n\n# Разные способы обратиться к коммиту:\ngit show abc1234                    # по хешу\ngit show HEAD                       # текущий коммит\ngit show HEAD~1                     # предыдущий коммит (parent)\ngit show HEAD~3                     # 3 коммита назад\ngit show HEAD^                      # первый parent (== HEAD~1)\ngit show HEAD^2                     # второй parent (при merge)\ngit show main                       # по имени ветки\ngit show v1.0                       # по тегу\ngit show main@{yesterday}           # коммит на вчера\ngit show main@{2}                   # 2-я предыдущая позиция main\ngit show HEAD@{2024-01-15}          # HEAD на дату\n\n# Диапазоны коммитов:\ngit log main..feature               # коммиты в feature, которых нет в main\ngit log main...feature              # коммиты уникальные для каждой ветки\ngit log --left-right main...feature # с маркерами < (main) > (feature)' },
        { type: 'warning', value: 'Git переходит с SHA-1 на SHA-256 (более безопасный хеш). Пока SHA-1 используется повсеместно, но в будущем Git будет поддерживать оба формата.' }
      ]
    },
    {
      id: 3,
      title: 'Refs, HEAD и reflog',
      type: 'theory',
      content: [
        { type: 'text', value: 'Refs (ссылки) — человеко-читаемые имена для хешей коммитов. Ветки и теги — это просто файлы с 40-символьным хешем. HEAD — указатель на текущую ветку. Reflog — журнал всех перемещений HEAD.' },
        { type: 'heading', value: 'Refs и ветки' },
        { type: 'code', language: 'bash', value: '# Ветка — это просто файл с хешем коммита\ncat .git/refs/heads/main\n# 9a5f8e3b7c1d2f4e6a8b0c3d5e7f9a1b3c5d7e9f\n\n# Создать ветку = создать файл с хешем\ngit branch feature/auth\ncat .git/refs/heads/feature/auth\n# тот же хеш, что и main (ветка создаётся от текущего коммита)\n\n# HEAD — указатель на текущую ветку\ncat .git/HEAD\n# ref: refs/heads/main\n\n# Detached HEAD — HEAD указывает на коммит, а не ветку\ngit checkout abc1234\ncat .git/HEAD\n# abc1234...  (прямой хеш, не ref)\n\n# Вернуться к ветке\ngit checkout main\n\n# Packed refs — оптимизация для репозиториев с множеством веток\ncat .git/packed-refs\n# abc1234... refs/remotes/origin/main\n# def5678... refs/tags/v1.0\n\n# Все refs\ngit show-ref\ngit for-each-ref --format="%(refname:short) %(objectname:short)" refs/heads/' },
        { type: 'heading', value: 'Reflog — страховка разработчика' },
        { type: 'code', language: 'bash', value: '# Reflog записывает КАЖДОЕ перемещение HEAD\ngit reflog\n# abc1234 HEAD@{0}: commit: feat: авторизация\n# def5678 HEAD@{1}: checkout: moving from feature to main\n# ghi9012 HEAD@{2}: commit: fix: баг в логине\n# jkl3456 HEAD@{3}: rebase: feat: обновил API\n\n# Reflog для конкретной ветки\ngit reflog show main\ngit reflog show feature/auth\n\n# ВОССТАНОВЛЕНИЕ после ошибки:\n# Случайно сделал hard reset?\ngit reset --hard HEAD~3\n# Ой! Вернуть обратно:\ngit reflog\n# Находим нужный коммит:\ngit reset --hard HEAD@{1}\n\n# Удалил ветку?\ngit branch -D feature/important\n# Восстановить:\ngit reflog | grep feature\ngit checkout -b feature/important abc1234\n\n# Reflog хранится 90 дней (по умолчанию)\ngit config gc.reflogExpire          # 90 days\ngit config gc.reflogExpireUnreachable # 30 days\n\n# ВАЖНО: reflog — ТОЛЬКО локальный. Его нет на remote.' },
        { type: 'tip', value: 'Reflog — главная страховка Git. Практически любое действие можно отменить через reflog в течение 90 дней. Даже после reset --hard, rebase или удаления ветки коммиты физически не удаляются, пока не пройдёт garbage collection.' }
      ]
    },
    {
      id: 4,
      title: 'Index (staging area) и три дерева',
      type: 'theory',
      content: [
        { type: 'text', value: 'Git работает с тремя "деревьями": Working Directory (рабочая директория), Index/Staging Area (подготовленные изменения) и HEAD (последний коммит). Понимание этих трёх состояний — ключ к пониманию reset, checkout и restore.' },
        { type: 'heading', value: 'Три дерева Git' },
        { type: 'code', language: 'bash', value: '# Три дерева Git:\n#\n# Working Directory    →    Index (Stage)    →    HEAD (Commit)\n#   (файлы на диске)     (git add)              (git commit)\n#\n# Просмотр каждого дерева:\n\n# 1. Working Directory — файлы на диске\nls -la src/\n\n# 2. Index (Staging Area) — что попадёт в следующий коммит\ngit ls-files --stage\n# 100644 abc123 0\tsrc/Main.java\n# 100644 def456 0\tsrc/Utils.java\n\n# 3. HEAD — последний коммит\ngit ls-tree -r HEAD\n# 100644 blob abc123  src/Main.java\n\n# Сравнения между деревьями:\ngit diff                # Working Dir vs Index\ngit diff --cached       # Index vs HEAD (что будет закоммичено)\ngit diff HEAD           # Working Dir vs HEAD\n\n# Пример workflow:\necho "new code" > file.java\n# Working Dir: изменён | Index: старый | HEAD: старый\n\ngit add file.java\n# Working Dir: изменён | Index: новый  | HEAD: старый\n\ngit commit -m "update"\n# Working Dir: изменён | Index: новый  | HEAD: новый' },
        { type: 'heading', value: 'git reset и три уровня' },
        { type: 'code', language: 'bash', value: '# git reset перемещает HEAD и опционально обновляет Index/Working Dir\n\n# --soft: перемещает ТОЛЬКО HEAD\ngit reset --soft HEAD~1\n# HEAD: откатился | Index: без изменений | Working Dir: без изменений\n# Коммит "отменён", но изменения остались в stage\n\n# --mixed (по умолчанию): перемещает HEAD + обновляет Index\ngit reset HEAD~1\n# HEAD: откатился | Index: откатился | Working Dir: без изменений\n# Коммит "отменён", изменения в Working Dir (unstaged)\n\n# --hard: перемещает HEAD + обновляет Index + Working Dir\ngit reset --hard HEAD~1\n# HEAD: откатился | Index: откатился | Working Dir: откатился\n# ВСЁ удалено! (но reflog спасёт)\n\n# Практические сценарии:\n# "Хочу переделать последний коммит"\ngit reset --soft HEAD~1\n# Правим код, добавляем, коммитим заново\n\n# "Хочу убрать файл из stage"\ngit reset HEAD file.java\n# или: git restore --staged file.java\n\n# "Хочу полностью откатить всё до коммита X"\ngit reset --hard abc1234' },
        { type: 'warning', value: 'git reset --hard удаляет все незакоммиченные изменения БЕЗ ВОЗМОЖНОСТИ восстановления (reflog не хранит working directory). Всегда коммитьте или стешьте перед hard reset.' }
      ]
    },
    {
      id: 5,
      title: 'Packfiles и Garbage Collection',
      type: 'theory',
      content: [
        { type: 'text', value: 'Git оптимизирует хранение через packfiles (сжатие объектов) и garbage collection (удаление недостижимых объектов). Понимание этих механизмов помогает при работе с большими репозиториями и восстановлении данных.' },
        { type: 'heading', value: 'Loose objects vs Packfiles' },
        { type: 'code', language: 'bash', value: '# Изначально каждый объект — отдельный файл (loose object)\nfind .git/objects -type f | head -5\n# .git/objects/ab/c1234...  (первые 2 символа хеша = директория)\n# .git/objects/de/f5678...\n\n# При git push/gc/repack объекты упаковываются в packfile\n# Packfile использует delta-компрессию:\n# Если файл изменился на 1 строку — хранится только дельта\n\n# Посмотреть packfiles\nls .git/objects/pack/\n# pack-abc123.idx    — индекс (быстрый поиск)\n# pack-abc123.pack   — данные (сжатые объекты)\n\n# Сколько объектов в репозитории\ngit count-objects -v\n# count: 42           — loose objects\n# size: 168           — размер loose objects (KB)\n# in-pack: 1337       — объектов в packfiles\n# packs: 1            — количество packfiles\n# size-pack: 4096     — размер packfiles (KB)\n\n# Ручная упаковка\ngit repack -a -d      # упаковать все, удалить старые\ngit prune             # удалить недостижимые loose objects\n\n# Проверить содержимое packfile\ngit verify-pack -v .git/objects/pack/pack-*.idx | head -20' },
        { type: 'heading', value: 'Garbage Collection' },
        { type: 'code', language: 'bash', value: '# git gc — автоматическая оптимизация репозитория\ngit gc\n# 1. Упаковывает loose objects в packfiles\n# 2. Упаковывает refs (packed-refs)\n# 3. Удаляет недостижимые объекты старше gc.pruneExpire (2 недели)\n# 4. Удаляет устаревшие записи reflog\n\n# Автоматический gc запускается при:\n# - Количество loose objects > gc.auto (6700)\n# - Количество packfiles > gc.autoPackLimit (50)\ngit config gc.auto           # 6700\ngit config gc.autoPackLimit  # 50\n\n# Агрессивная оптимизация (для старых/больших репо)\ngit gc --aggressive --prune=now\n\n# Проверка целостности репозитория\ngit fsck\n# Покажет dangling objects (недостижимые коммиты, blob-ы)\ngit fsck --unreachable\n\n# Восстановление dangling commit\ngit fsck --lost-found\nls .git/lost-found/commit/\ngit show <dangling-commit-hash>\n\n# Важно для Java-проектов: .gitignore для target/build\n# Если случайно закоммитили target/ — удалить из истории:\ngit filter-branch --tree-filter "rm -rf target" HEAD\n# Или современный вариант:\ngit filter-repo --path target/ --invert-paths' },
        { type: 'tip', value: 'Для Java-проектов: всегда добавляйте target/, build/, *.class, *.jar в .gitignore ДО первого коммита. Если большие файлы уже в истории — используйте git filter-repo или BFG Repo Cleaner для очистки.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Исследуем Git-объекты',
      type: 'practice',
      difficulty: 'medium',
      description: 'Напишите Java-программу, которая симулирует объектную модель Git: создание blob, tree и commit объектов с вычислением SHA-1 хешей.',
      requirements: [
        'Класс GitObject с полями type, content и методом computeHash()',
        'Класс Blob — хранит содержимое файла и вычисляет SHA-1 по формату Git: "blob <size>\\0<content>"',
        'Класс TreeEntry — имя файла + хеш blob-а',
        'Класс Commit — tree hash, parent hash, author, message',
        'Метод computeHash() для каждого типа объекта',
        'Продемонстрировать что одинаковое содержимое даёт одинаковый хеш'
      ],
      expectedOutput: 'Blob "Hello World": 557db03de997c86a4a028e1ebd3a1ceb225be238\nBlob "Hello World" (duplicate): 557db03de997c86a4a028e1ebd3a1ceb225be238\nHashes equal: true\nBlob "README content": 45b2a960f4fad0e0fe4a5427040f87a0c68fba12\nTree hash: computed\nCommit hash: computed\nCommit info:\n  tree: <tree-hash>\n  author: Nurdaulet\n  message: feat: initial commit',
      hint: 'Используйте java.security.MessageDigest для вычисления SHA-1. Формат Git-объекта: "<type> <size>\\0<content>". Для blob: "blob 11\\0Hello World".',
      solution: 'import java.security.MessageDigest;\nimport java.nio.charset.StandardCharsets;\nimport java.util.ArrayList;\nimport java.util.List;\n\npublic class Main {\n\n    // Базовый Git-объект\n    static String computeGitHash(String type, String content) {\n        try {\n            // Формат Git: "<type> <size>\\0<content>"\n            String header = type + " " + content.length() + "\\0";\n            byte[] fullContent = (header + content).getBytes(StandardCharsets.UTF_8);\n\n            MessageDigest md = MessageDigest.getInstance("SHA-1");\n            byte[] hash = md.digest(fullContent);\n\n            // Конвертируем в hex строку\n            StringBuilder hex = new StringBuilder();\n            for (byte b : hash) {\n                hex.append(String.format("%02x", b));\n            }\n            return hex.toString();\n        } catch (Exception e) {\n            throw new RuntimeException(e);\n        }\n    }\n\n    // Blob — содержимое файла\n    static class Blob {\n        String content;\n        String hash;\n\n        Blob(String content) {\n            this.content = content;\n            this.hash = computeGitHash("blob", content);\n        }\n    }\n\n    // Tree Entry — запись в дереве\n    static class TreeEntry {\n        String mode;\n        String name;\n        String hash;\n\n        TreeEntry(String mode, String name, String hash) {\n            this.mode = mode;\n            this.name = name;\n            this.hash = hash;\n        }\n\n        String serialize() {\n            return mode + " " + name + "\\0" + hash;\n        }\n    }\n\n    // Tree — директория\n    static class Tree {\n        List<TreeEntry> entries = new ArrayList<>();\n        String hash;\n\n        void addEntry(String mode, String name, String blobHash) {\n            entries.add(new TreeEntry(mode, name, blobHash));\n        }\n\n        String computeHash() {\n            StringBuilder sb = new StringBuilder();\n            for (TreeEntry entry : entries) {\n                sb.append(entry.serialize()).append("\\n");\n            }\n            this.hash = computeGitHash("tree", sb.toString());\n            return this.hash;\n        }\n    }\n\n    // Commit — снимок\n    static class Commit {\n        String treeHash;\n        String parentHash;\n        String author;\n        String message;\n        String hash;\n\n        Commit(String treeHash, String parentHash, String author, String message) {\n            this.treeHash = treeHash;\n            this.parentHash = parentHash;\n            this.author = author;\n            this.message = message;\n        }\n\n        String computeHash() {\n            StringBuilder sb = new StringBuilder();\n            sb.append("tree ").append(treeHash).append("\\n");\n            if (parentHash != null) {\n                sb.append("parent ").append(parentHash).append("\\n");\n            }\n            sb.append("author ").append(author).append("\\n");\n            sb.append("\\n");\n            sb.append(message).append("\\n");\n            this.hash = computeGitHash("commit", sb.toString());\n            return this.hash;\n        }\n    }\n\n    public static void main(String[] args) {\n        // 1. Создаём blob-ы\n        Blob blob1 = new Blob("Hello World");\n        System.out.println("Blob \\"Hello World\\": " + blob1.hash);\n\n        Blob blob2 = new Blob("Hello World");\n        System.out.println("Blob \\"Hello World\\" (duplicate): " + blob2.hash);\n        System.out.println("Hashes equal: " + blob1.hash.equals(blob2.hash));\n\n        Blob readmeBlob = new Blob("README content");\n        System.out.println("Blob \\"README content\\": " + readmeBlob.hash);\n\n        // 2. Создаём tree\n        Tree tree = new Tree();\n        tree.addEntry("100644", "Main.java", blob1.hash);\n        tree.addEntry("100644", "README.md", readmeBlob.hash);\n        tree.computeHash();\n        System.out.println("Tree hash: computed");\n\n        // 3. Создаём commit\n        Commit commit = new Commit(tree.hash, null, "Nurdaulet", "feat: initial commit");\n        commit.computeHash();\n        System.out.println("Commit hash: computed");\n        System.out.println("Commit info:");\n        System.out.println("  tree: " + tree.hash.substring(0, 12) + "...");\n        System.out.println("  author: " + commit.author);\n        System.out.println("  message: " + commit.message);\n    }\n}',
      explanation: 'Git использует SHA-1 для адресации объектов. Ключевое свойство: одинаковое содержимое всегда даёт одинаковый хеш. Это позволяет дедуплицировать данные — если два файла одинаковы, Git хранит только один blob. Формат хеширования: тип + пробел + размер + нулевой байт + содержимое. Blob не содержит имя файла — имя хранится в tree. Commit ссылается на tree (состояние всего проекта) и parent (предыдущий коммит), образуя цепочку — историю.'
    }
  ]
}
