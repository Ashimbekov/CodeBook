export default {
  id: 25,
  title: 'Практика: E-commerce микросервисы',
  description: 'Комплексный практический проект: проектирование и реализация e-commerce платформы на микросервисной архитектуре с полным стеком технологий.',
  lessons: [
    {
      id: 1,
      title: 'Архитектура проекта',
      type: 'theory',
      content: [
        { type: 'text', value: 'В этом модуле мы спроектируем и реализуем e-commerce платформу из 6 микросервисов. Используем все паттерны и технологии из курса: REST, gRPC, Kafka, Saga, CQRS, Docker, Kubernetes.' },
        { type: 'heading', value: 'Микросервисы проекта' },
        { type: 'code', language: 'bash', value: '# E-commerce Platform Architecture\n#\n# [Mobile/Web] -> [API Gateway (Spring Cloud Gateway)]\n#                      |\n#        ┌──────────────┼──────────────────────┐\n#        |              |              |        |\n# [User Service] [Catalog Service] [Order Service] [Payment Service]\n#   PostgreSQL     PostgreSQL +       PostgreSQL      PostgreSQL\n#                  Elasticsearch\n#        |              |              |        |\n#        └──────────────┼──────────────────────┘\n#                       |\n#              [Apache Kafka] (Events)\n#                       |\n#           ┌───────────┼──────────┐\n#           |                      |\n#    [Notification Service]  [Analytics Service]\n#         Redis                 ClickHouse\n#\n# Инфраструктура:\n# - Docker + Docker Compose (development)\n# - Kubernetes + Helm (production)\n# - Prometheus + Grafana (мониторинг)\n# - ELK Stack (логирование)\n# - Jaeger (distributed tracing)\n# - Keycloak (аутентификация)' },
        { type: 'list', value: [
          'User Service: регистрация, авторизация, профиль, адреса',
          'Catalog Service: товары, категории, поиск, фильтрация',
          'Order Service: корзина, заказы, статусы, Saga orchestrator',
          'Payment Service: обработка оплат, возвраты, история',
          'Notification Service: email, SMS, push уведомления',
          'Analytics Service: статистика продаж, отчёты, дашборды'
        ] },
        { type: 'tip', value: 'Начните с User Service и Catalog Service — они самые простые. Потом Order Service с Saga. Payment и Notification — подписчики событий. Analytics — проекция из всех событий.' }
      ]
    },
    {
      id: 2,
      title: 'User Service и аутентификация',
      type: 'theory',
      content: [
        { type: 'text', value: 'User Service управляет пользователями и интегрируется с Keycloak для OAuth2/JWT аутентификации. REST API для CRUD пользователей, публикация событий в Kafka.' },
        { type: 'code', language: 'java', value: '// User Service — REST API\n@RestController\n@RequestMapping("/api/v1/users")\npublic class UserController {\n\n    @PostMapping("/register")\n    @ResponseStatus(HttpStatus.CREATED)\n    public UserResponse register(@Valid @RequestBody RegisterRequest request) {\n        return userService.register(request);\n    }\n\n    @GetMapping("/{id}")\n    public UserResponse getUser(@PathVariable UUID id) {\n        return userService.findById(id);\n    }\n\n    @PutMapping("/{id}/profile")\n    public UserResponse updateProfile(\n            @PathVariable UUID id,\n            @Valid @RequestBody UpdateProfileRequest request,\n            @AuthenticationPrincipal Jwt jwt) {\n        // Только свой профиль\n        if (!jwt.getSubject().equals(id.toString())) {\n            throw new ForbiddenException("Нельзя редактировать чужой профиль");\n        }\n        return userService.updateProfile(id, request);\n    }\n}\n\n@Service\npublic class UserService {\n    private final UserRepository userRepository;\n    private final KafkaTemplate<String, Object> kafkaTemplate;\n    private final PasswordEncoder passwordEncoder;\n\n    @Transactional\n    public UserResponse register(RegisterRequest request) {\n        if (userRepository.existsByEmail(request.email())) {\n            throw new EmailAlreadyExistsException(request.email());\n        }\n\n        User user = User.builder()\n            .name(request.name())\n            .email(request.email())\n            .password(passwordEncoder.encode(request.password()))\n            .role(UserRole.CUSTOMER)\n            .build();\n\n        userRepository.save(user);\n\n        // Событие для других сервисов\n        kafkaTemplate.send("user-events", user.getId().toString(),\n            new UserRegisteredEvent(user.getId(), user.getName(),\n                user.getEmail(), Instant.now()));\n\n        return UserResponse.from(user);\n    }\n}' },
        { type: 'code', language: 'yaml', value: '# docker-compose.yml — User Service\nuser-service:\n  build: ./user-service\n  ports: ["8081:8080"]\n  environment:\n    SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/users_db\n    SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092\n    SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI: http://keycloak:8180/realms/shop\n  depends_on:\n    postgres:\n      condition: service_healthy\n    kafka:\n      condition: service_healthy' }
      ]
    },
    {
      id: 3,
      title: 'Catalog Service и поиск',
      type: 'theory',
      content: [
        { type: 'text', value: 'Catalog Service хранит товары в PostgreSQL и синхронизирует в Elasticsearch для полнотекстового поиска. CQRS: запись в PostgreSQL, чтение из Elasticsearch.' },
        { type: 'code', language: 'java', value: '// Catalog Service — CQRS для товаров\n\n// Write Model (PostgreSQL)\n@Entity\n@Table(name = "products")\npublic class Product {\n    @Id\n    @GeneratedValue(strategy = GenerationType.UUID)\n    private UUID id;\n    private String name;\n    private String description;\n    private BigDecimal price;\n    private String category;\n    private int stock;\n    private boolean active;\n}\n\n// Read Model (Elasticsearch)\n@Document(indexName = "products")\npublic class ProductDocument {\n    @Id\n    private String id;\n    private String name;\n    private String description;\n    private double price;\n    private String category;\n    private boolean inStock; // stock > 0\n}\n\n// Command: создание товара\n@PostMapping\n@ResponseStatus(HttpStatus.CREATED)\npublic ProductResponse createProduct(@Valid @RequestBody CreateProductRequest request) {\n    Product product = productService.create(request);\n    return ProductResponse.from(product);\n}\n\n// Query: поиск через Elasticsearch\n@GetMapping("/search")\npublic Page<ProductDocument> search(\n        @RequestParam String q,\n        @RequestParam(required = false) String category,\n        @RequestParam(required = false) BigDecimal minPrice,\n        @RequestParam(required = false) BigDecimal maxPrice,\n        @RequestParam(defaultValue = "0") int page,\n        @RequestParam(defaultValue = "20") int size) {\n    return productSearchService.search(q, category, minPrice, maxPrice, page, size);\n}\n\n// Projection: PostgreSQL -> Elasticsearch\n@TransactionalEventListener\npublic void onProductCreated(ProductCreatedEvent event) {\n    ProductDocument doc = ProductDocument.from(event);\n    elasticsearchRepository.save(doc);\n}\n\n// Поиск в Elasticsearch\n@Service\npublic class ProductSearchService {\n    public Page<ProductDocument> search(String query, String category,\n            BigDecimal minPrice, BigDecimal maxPrice, int page, int size) {\n        var criteria = new Criteria("name").matches(query)\n            .or("description").matches(query);\n\n        if (category != null) criteria = criteria.and("category").is(category);\n        if (minPrice != null) criteria = criteria.and("price").greaterThanEqual(minPrice);\n        if (maxPrice != null) criteria = criteria.and("price").lessThanEqual(maxPrice);\n\n        var searchQuery = new CriteriaQuery(criteria)\n            .setPageable(PageRequest.of(page, size));\n\n        return elasticsearchOperations.search(searchQuery, ProductDocument.class);\n    }\n}' },
        { type: 'note', value: 'Elasticsearch синхронизируется через domain events. При создании/обновлении товара событие обновляет Elasticsearch. Поиск по Elasticsearch значительно быстрее чем LIKE в PostgreSQL.' }
      ]
    },
    {
      id: 4,
      title: 'Order Service и Saga',
      type: 'theory',
      content: [
        { type: 'text', value: 'Order Service — самый сложный сервис. Управляет жизненным циклом заказа через Orchestration Saga: создание -> резервирование товара -> оплата -> подтверждение.' },
        { type: 'code', language: 'java', value: '// Order Service — Saga Orchestrator\n@Service\n@Slf4j\npublic class CreateOrderSaga {\n    private final OrderRepository orderRepository;\n    private final CatalogServiceClient catalogClient;\n    private final PaymentServiceClient paymentClient;\n    private final KafkaTemplate<String, Object> kafka;\n\n    @Transactional\n    public OrderResponse execute(CreateOrderRequest request, UUID userId) {\n        // Шаг 1: Создать заказ\n        Order order = Order.builder()\n            .customerId(userId)\n            .status(OrderStatus.PENDING)\n            .items(request.items().stream()\n                .map(i -> OrderItem.builder()\n                    .productId(i.productId())\n                    .quantity(i.quantity())\n                    .price(i.price())\n                    .build())\n                .toList())\n            .build();\n        order.calculateTotal();\n        orderRepository.save(order);\n        log.info("Saga step 1: Order {} created", order.getId());\n\n        try {\n            // Шаг 2: Зарезервировать товар\n            catalogClient.reserveItems(order.getId(),\n                order.getItems().stream()\n                    .map(i -> new ReserveRequest(i.getProductId(), i.getQuantity()))\n                    .toList());\n            log.info("Saga step 2: Items reserved");\n\n            // Шаг 3: Обработать оплату\n            paymentClient.processPayment(\n                new PaymentRequest(order.getId(), userId, order.getTotalAmount()));\n            log.info("Saga step 3: Payment processed");\n\n            // Шаг 4: Подтвердить заказ\n            order.confirm();\n            orderRepository.save(order);\n\n            kafka.send("order-events", order.getId().toString(),\n                new OrderConfirmedEvent(order.getId(), userId, order.getTotalAmount()));\n\n            log.info("Saga completed: Order {} confirmed", order.getId());\n            return OrderResponse.from(order);\n\n        } catch (InsufficientStockException e) {\n            log.warn("Saga compensation: insufficient stock");\n            order.cancel("Товар недоступен: " + e.getMessage());\n            orderRepository.save(order);\n            throw new OrderCreationFailedException("Товар недоступен", e);\n\n        } catch (PaymentFailedException e) {\n            log.warn("Saga compensation: payment failed");\n            // Компенсация: отмена резерва\n            catalogClient.cancelReservation(order.getId());\n            order.cancel("Оплата не прошла: " + e.getMessage());\n            orderRepository.save(order);\n            throw new OrderCreationFailedException("Оплата не прошла", e);\n        }\n    }\n}' },
        { type: 'warning', value: 'В production добавьте: таймауты для каждого шага Saga, retry для временных ошибок, сохранение состояния Saga в БД для recovery, Dead Letter Queue для неудачных компенсаций.' }
      ]
    },
    {
      id: 5,
      title: 'Notification и Analytics',
      type: 'theory',
      content: [
        { type: 'text', value: 'Notification Service подписан на события из Kafka и отправляет уведомления. Analytics Service создаёт проекции из всех событий для дашбордов и отчётов.' },
        { type: 'code', language: 'java', value: '// Notification Service — подписка на события\n@Service\n@Slf4j\npublic class NotificationEventHandler {\n\n    private final EmailService emailService;\n    private final UserServiceClient userClient;\n\n    @KafkaListener(topics = "order-events", groupId = "notification-service")\n    public void handleOrderEvent(OrderEvent event) {\n        switch (event) {\n            case OrderConfirmedEvent e -> {\n                UserResponse user = userClient.getUser(e.customerId());\n                emailService.send(user.email(),\n                    "Заказ подтверждён",\n                    String.format("Ваш заказ #%s на сумму %s подтверждён!",\n                        e.orderId(), e.totalAmount()));\n            }\n            case OrderCancelledEvent e -> {\n                UserResponse user = userClient.getUser(e.customerId());\n                emailService.send(user.email(),\n                    "Заказ отменён",\n                    String.format("Ваш заказ #%s отменён: %s",\n                        e.orderId(), e.reason()));\n            }\n            default -> log.debug("Unhandled event: {}", event.getClass());\n        }\n    }\n\n    @KafkaListener(topics = "payment-events", groupId = "notification-service")\n    public void handlePaymentEvent(PaymentEvent event) {\n        if (event instanceof PaymentProcessedEvent e) {\n            // Отправка чека\n            emailService.send(e.customerEmail(),\n                "Оплата получена", "Чек: " + e.receiptUrl());\n        }\n    }\n}\n\n// Analytics Service — агрегация из событий\n@Service\npublic class AnalyticsProjection {\n\n    private final AnalyticsRepository analyticsRepo;\n\n    @KafkaListener(topics = {"order-events", "payment-events", "user-events"},\n                   groupId = "analytics-service")\n    public void handleEvent(Object event) {\n        if (event instanceof OrderConfirmedEvent e) {\n            analyticsRepo.incrementDailyOrders(LocalDate.now());\n            analyticsRepo.addDailyRevenue(LocalDate.now(), e.totalAmount());\n        } else if (event instanceof UserRegisteredEvent e) {\n            analyticsRepo.incrementDailyRegistrations(LocalDate.now());\n        }\n    }\n\n    // API для дашборда\n    public DashboardStats getDashboardStats(LocalDate from, LocalDate to) {\n        return new DashboardStats(\n            analyticsRepo.getTotalOrders(from, to),\n            analyticsRepo.getTotalRevenue(from, to),\n            analyticsRepo.getNewUsers(from, to),\n            analyticsRepo.getTopProducts(from, to, 10)\n        );\n    }\n}' },
        { type: 'tip', value: 'Notification Service должен быть idempotent: повторная обработка события не должна отправить email дважды. Используйте таблицу processed_events или идемпотентность email-провайдера.' }
      ]
    },
    {
      id: 6,
      title: 'Docker Compose: полный стек',
      type: 'theory',
      content: [
        { type: 'text', value: 'Docker Compose объединяет все микросервисы и инфраструктуру для локальной разработки. Одна команда docker-compose up запускает весь стек.' },
        { type: 'code', language: 'yaml', value: '# docker-compose.yml — полный E-commerce стек\nservices:\n  # === Инфраструктура ===\n  postgres:\n    image: postgres:16-alpine\n    environment:\n      POSTGRES_USER: admin\n      POSTGRES_PASSWORD: admin\n    ports: ["5432:5432"]\n    volumes:\n      - ./init-databases.sql:/docker-entrypoint-initdb.d/init.sql\n    healthcheck:\n      test: pg_isready -U admin\n      interval: 5s\n      timeout: 3s\n\n  kafka:\n    image: confluentinc/cp-kafka:7.5.0\n    ports: ["9092:9092"]\n    environment:\n      KAFKA_NODE_ID: 1\n      KAFKA_PROCESS_ROLES: broker,controller\n      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093\n      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092\n      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093\n      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER\n      CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qk\n\n  elasticsearch:\n    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0\n    environment:\n      - discovery.type=single-node\n      - xpack.security.enabled=false\n    ports: ["9200:9200"]\n\n  redis:\n    image: redis:7-alpine\n    ports: ["6379:6379"]\n\n  keycloak:\n    image: quay.io/keycloak/keycloak:23.0\n    command: start-dev\n    environment:\n      KEYCLOAK_ADMIN: admin\n      KEYCLOAK_ADMIN_PASSWORD: admin\n    ports: ["8180:8080"]\n\n  # === Микросервисы ===\n  api-gateway:\n    build: ./api-gateway\n    ports: ["8080:8080"]\n    depends_on: [keycloak]\n\n  user-service:\n    build: ./user-service\n    environment:\n      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/users_db\n      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092\n    depends_on: [postgres, kafka]\n\n  catalog-service:\n    build: ./catalog-service\n    environment:\n      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/catalog_db\n      SPRING_ELASTICSEARCH_URIS: http://elasticsearch:9200\n      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092\n    depends_on: [postgres, kafka, elasticsearch]\n\n  order-service:\n    build: ./order-service\n    environment:\n      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/orders_db\n      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092\n    depends_on: [postgres, kafka]\n\n  payment-service:\n    build: ./payment-service\n    environment:\n      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/payments_db\n      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092\n    depends_on: [postgres, kafka]\n\n  notification-service:\n    build: ./notification-service\n    environment:\n      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092\n      SPRING_DATA_REDIS_HOST: redis\n    depends_on: [kafka, redis]\n\n  # === Observability ===\n  prometheus:\n    image: prom/prometheus:v2.48.0\n    ports: ["9090:9090"]\n    volumes: [./prometheus.yml:/etc/prometheus/prometheus.yml]\n\n  grafana:\n    image: grafana/grafana:10.2.0\n    ports: ["3000:3000"]' },
        { type: 'note', value: 'Для первого запуска: docker-compose up --build. Это может занять 5-10 минут для сборки всех образов. Далее: docker-compose up (использует кэш). Для пересборки одного сервиса: docker-compose up --build order-service.' }
      ]
    },
    {
      id: 7,
      title: 'Практика: Собираем всё вместе',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте полный e-commerce flow: регистрация -> поиск товаров -> создание заказа -> оплата -> уведомление.',
      requirements: [
        'Создайте Docker Compose с 6 микросервисами и инфраструктурой',
        'Реализуйте полный flow: регистрация пользователя через Keycloak',
        'Каталог товаров с поиском через Elasticsearch',
        'Создание заказа через Saga (Order -> Catalog reserve -> Payment)',
        'Отправка уведомлений при подтверждении заказа (Kafka -> Notification)',
        'Мониторинг: Prometheus дашборд с метриками всех сервисов',
        'Distributed Tracing: полный trace запроса через Jaeger'
      ],
      hint: 'Начните с docker-compose up инфраструктуры. Потом по одному добавляйте сервисы. Тестируйте каждый сервис отдельно через curl. Потом полный flow. Используйте Postman Collection для автоматизации тестирования.',
      expectedOutput: '1. docker-compose up — все 6 сервисов и инфраструктура запущены.\n2. POST /api/v1/users/register -> user created, JWT получен.\n3. GET /api/v1/products/search?q=laptop -> 5 товаров из Elasticsearch.\n4. POST /api/v1/orders -> Saga: reserve OK -> payment OK -> order CONFIRMED.\n5. Kafka: OrderConfirmedEvent -> Notification Service -> email отправлен.\n6. Prometheus: все сервисы мониторятся, 0 ошибок.\n7. Jaeger: trace показывает полный путь через 4 сервиса (150ms total).\n8. Grafana дашборд: 10 заказов/мин, 0% errors, p99=200ms.',
      solution: '# Запуск\ndocker-compose up --build -d\n\n# Тестирование полного flow\n\n# 1. Регистрация\ncurl -X POST http://localhost:8080/api/v1/users/register \\\n  -H "Content-Type: application/json" \\\n  -d \'{"name":"John","email":"john@test.com","password":"pass123"}\'\n\n# 2. Получение JWT\ncurl -X POST http://localhost:8180/realms/shop/protocol/openid-connect/token \\\n  -d "client_id=shop-app&username=john@test.com&password=pass123&grant_type=password"\n\n# 3. Поиск товаров\ncurl http://localhost:8080/api/v1/products/search?q=laptop \\\n  -H "Authorization: Bearer $TOKEN"\n\n# 4. Создание заказа\ncurl -X POST http://localhost:8080/api/v1/orders \\\n  -H "Authorization: Bearer $TOKEN" \\\n  -H "Content-Type: application/json" \\\n  -d \'{"items":[{"productId":"prod-1","quantity":1,"price":999.99}]}\'\n\n# 5. Проверка статуса\ncurl http://localhost:8080/api/v1/orders/$ORDER_ID \\\n  -H "Authorization: Bearer $TOKEN"\n# {"id":"...","status":"CONFIRMED","totalAmount":999.99}\n\n# 6. Мониторинг\n# Prometheus: http://localhost:9090\n# Grafana:    http://localhost:3000\n# Jaeger:     http://localhost:16686',
      explanation: 'Этот проект объединяет все темы курса: микросервисная декомпозиция (DDD), синхронное (REST) и асинхронное (Kafka) взаимодействие, Saga для распределённых транзакций, CQRS для поиска, OAuth2/JWT для безопасности, Docker для контейнеризации, Prometheus/Grafana для мониторинга, Jaeger для трейсинга. Это фундамент для production-ready микросервисной системы.'
    }
  ]
}
