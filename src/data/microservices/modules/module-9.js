export default {
  id: 9,
  title: 'Паттерн Saga',
  description: 'Управление распределёнными транзакциями через Saga: choreography vs orchestration, компенсирующие транзакции, обработка ошибок и откат.',
  lessons: [
    {
      id: 1,
      title: 'Проблема распределённых транзакций',
      type: 'theory',
      content: [
        { type: 'text', value: 'В монолите транзакция ACID охватывает все операции: создание заказа, списание денег, резервирование товара — одна транзакция. В микросервисах каждый сервис имеет свою БД — ACID невозможен через несколько БД. Saga решает эту проблему.' },
        { type: 'code', language: 'bash', value: '# Монолит: одна транзакция\n# BEGIN TRANSACTION;\n#   INSERT INTO orders (...);\n#   UPDATE inventory SET stock = stock - 1;\n#   INSERT INTO payments (...);\n# COMMIT; -- Всё или ничего (ACID)\n\n# Микросервисы: каждый сервис — своя БД\n# Order Service -> orders_db: INSERT INTO orders\n# Inventory Service -> inventory_db: UPDATE stock\n# Payment Service -> payments_db: INSERT INTO payments\n# Нет единой транзакции! Что если Payment упал?\n\n# Saga — последовательность локальных транзакций\n# Каждый шаг: локальная транзакция + публикация события\n# При ошибке: компенсирующие транзакции (откат)\n\n# Создание заказа — Saga:\n# 1. Order Service: создать заказ (PENDING)\n# 2. Inventory Service: зарезервировать товар\n# 3. Payment Service: списать деньги\n# 4. Order Service: подтвердить заказ (CONFIRMED)\n#\n# Если шаг 3 упал:\n# C2. Inventory Service: ОТМЕНИТЬ резерв (компенсация)\n# C1. Order Service: отменить заказ (CANCELLED)' },
        { type: 'warning', value: 'Saga обеспечивает eventual consistency, НЕ ACID. Между шагами данные временно несогласованы. Заказ может быть PENDING пока оплата обрабатывается. UI должен показывать это состояние пользователю.' }
      ]
    },
    {
      id: 2,
      title: 'Choreography Saga',
      type: 'theory',
      content: [
        { type: 'text', value: 'Choreography Saga — децентрализованный подход. Сервисы общаются через события. Каждый сервис слушает события и реагирует на них. Нет центрального координатора.' },
        { type: 'code', language: 'java', value: '// Choreography Saga: Создание заказа\n\n// Шаг 1: Order Service создаёт заказ и публикует событие\n@Service\npublic class OrderService {\n    public Order createOrder(CreateOrderRequest request) {\n        Order order = orderRepository.save(\n            new Order(request.customerId(), request.items(), OrderStatus.PENDING));\n        // Публикуем событие\n        eventPublisher.publish(new OrderCreatedEvent(\n            order.getId(), order.getCustomerId(),\n            order.getItems(), order.getTotalAmount()));\n        return order;\n    }\n}\n\n// Шаг 2: Inventory Service слушает OrderCreated\n@Service\npublic class InventoryEventHandler {\n    @KafkaListener(topics = "order-events", groupId = "inventory-service")\n    public void handleOrderCreated(OrderCreatedEvent event) {\n        try {\n            inventoryService.reserveItems(event.items());\n            eventPublisher.publish(new InventoryReservedEvent(\n                event.orderId(), event.items()));\n        } catch (InsufficientStockException e) {\n            // Компенсация: отправляем событие об ошибке\n            eventPublisher.publish(new InventoryReservationFailedEvent(\n                event.orderId(), e.getMessage()));\n        }\n    }\n}\n\n// Шаг 3: Payment Service слушает InventoryReserved\n@Service\npublic class PaymentEventHandler {\n    @KafkaListener(topics = "inventory-events", groupId = "payment-service")\n    public void handleInventoryReserved(InventoryReservedEvent event) {\n        try {\n            paymentService.processPayment(event.orderId());\n            eventPublisher.publish(new PaymentProcessedEvent(event.orderId()));\n        } catch (PaymentFailedException e) {\n            eventPublisher.publish(new PaymentFailedEvent(\n                event.orderId(), e.getMessage()));\n        }\n    }\n}\n\n// Компенсация: Order Service слушает ошибки\n@Service\npublic class OrderCompensationHandler {\n    @KafkaListener(topics = {"inventory-events", "payment-events"},\n                   groupId = "order-service")\n    public void handleFailure(Object event) {\n        if (event instanceof InventoryReservationFailedEvent e) {\n            orderService.cancelOrder(e.orderId(), "Товар недоступен");\n        } else if (event instanceof PaymentFailedEvent e) {\n            orderService.cancelOrder(e.orderId(), "Оплата не прошла");\n            eventPublisher.publish(new OrderCancelledEvent(e.orderId()));\n            // Inventory Service слушает OrderCancelled -> отменяет резерв\n        }\n    }\n}' },
        { type: 'list', value: [
          'Плюсы: простота, слабая связанность, каждый сервис автономен',
          'Минусы: сложно отследить весь flow, риск циклических зависимостей',
          'Подходит для: простых Saga с 2-4 шагами'
        ] }
      ]
    },
    {
      id: 3,
      title: 'Orchestration Saga',
      type: 'theory',
      content: [
        { type: 'text', value: 'Orchestration Saga — централизованный подход. Saga Orchestrator (координатор) управляет всеми шагами, вызывает сервисы в нужном порядке и обрабатывает компенсации при ошибках.' },
        { type: 'code', language: 'java', value: '// Orchestration Saga: Создание заказа\n\n@Service\npublic class CreateOrderSaga {\n    private final OrderService orderService;\n    private final InventoryServiceClient inventoryClient;\n    private final PaymentServiceClient paymentClient;\n\n    @Transactional\n    public Order execute(CreateOrderRequest request) {\n        // Шаг 1: Создать заказ\n        Order order = orderService.create(request);\n        log.info("Saga шаг 1: Заказ {} создан", order.getId());\n\n        try {\n            // Шаг 2: Зарезервировать товар\n            inventoryClient.reserveItems(\n                order.getId(), request.items());\n            log.info("Saga шаг 2: Товар зарезервирован");\n\n            // Шаг 3: Обработать оплату\n            paymentClient.processPayment(\n                order.getId(), order.getTotalAmount());\n            log.info("Saga шаг 3: Оплата обработана");\n\n            // Шаг 4: Подтвердить заказ\n            orderService.confirm(order.getId());\n            log.info("Saga завершена: заказ подтверждён");\n\n            return order;\n        } catch (InventoryException e) {\n            // Компенсация шага 1\n            log.warn("Saga компенсация: товар недоступен");\n            orderService.cancel(order.getId(), e.getMessage());\n            throw new SagaFailedException("Товар недоступен", e);\n        } catch (PaymentException e) {\n            // Компенсация шагов 2 и 1\n            log.warn("Saga компенсация: оплата не прошла");\n            inventoryClient.cancelReservation(order.getId());\n            orderService.cancel(order.getId(), e.getMessage());\n            throw new SagaFailedException("Оплата не прошла", e);\n        }\n    }\n}\n\n// Более продвинутый подход: Saga как State Machine\npublic enum CreateOrderSagaState {\n    STARTED,\n    ORDER_CREATED,\n    INVENTORY_RESERVED,\n    PAYMENT_PROCESSED,\n    COMPLETED,\n    COMPENSATING_INVENTORY,\n    COMPENSATING_ORDER,\n    FAILED\n}\n\n@Entity\npublic class SagaInstance {\n    @Id\n    private UUID id;\n    private String sagaType;\n    private String currentState;\n    private String payload;  // JSON данные saga\n    private Instant createdAt;\n    private Instant updatedAt;\n}' },
        { type: 'list', value: [
          'Плюсы: чёткий flow, легко отслеживать, централизованная обработка ошибок',
          'Минусы: orchestrator — single point of failure, усиливает связанность',
          'Подходит для: сложных Saga с 5+ шагами, сложной логикой компенсации'
        ] }
      ]
    },
    {
      id: 4,
      title: 'Компенсирующие транзакции',
      type: 'theory',
      content: [
        { type: 'text', value: 'Компенсирующая транзакция — это обратная операция для отмены эффекта уже выполненного шага Saga. Не все операции можно отменить — некоторые только компенсировать.' },
        { type: 'code', language: 'java', value: '// Компенсирующие транзакции для каждого шага\n\npublic interface SagaStep<T> {\n    void execute(T context);          // Прямое действие\n    void compensate(T context);       // Компенсация\n    String getName();                  // Имя шага для логирования\n}\n\n// Шаг: Резервирование товара\npublic class ReserveInventoryStep implements SagaStep<OrderSagaContext> {\n    @Override\n    public void execute(OrderSagaContext ctx) {\n        ReservationResponse response = inventoryClient.reserve(\n            ctx.getOrderId(), ctx.getItems());\n        ctx.setReservationId(response.reservationId());\n    }\n\n    @Override\n    public void compensate(OrderSagaContext ctx) {\n        // Компенсация: отмена резервации\n        inventoryClient.cancelReservation(ctx.getReservationId());\n    }\n\n    @Override\n    public String getName() { return "ReserveInventory"; }\n}\n\n// Шаг: Оплата\npublic class ProcessPaymentStep implements SagaStep<OrderSagaContext> {\n    @Override\n    public void execute(OrderSagaContext ctx) {\n        PaymentResponse response = paymentClient.charge(\n            ctx.getCustomerId(), ctx.getTotalAmount());\n        ctx.setPaymentId(response.paymentId());\n    }\n\n    @Override\n    public void compensate(OrderSagaContext ctx) {\n        // Компенсация: возврат денег\n        paymentClient.refund(ctx.getPaymentId());\n    }\n\n    @Override\n    public String getName() { return "ProcessPayment"; }\n}\n\n// Saga Executor — выполняет шаги с компенсацией\npublic class SagaExecutor<T> {\n    private final List<SagaStep<T>> steps;\n\n    public void execute(T context) {\n        List<SagaStep<T>> completedSteps = new ArrayList<>();\n\n        for (SagaStep<T> step : steps) {\n            try {\n                log.info("Executing step: {}", step.getName());\n                step.execute(context);\n                completedSteps.add(step);\n            } catch (Exception e) {\n                log.error("Step {} failed: {}", step.getName(), e.getMessage());\n                // Компенсация в обратном порядке!\n                Collections.reverse(completedSteps);\n                for (SagaStep<T> completed : completedSteps) {\n                    try {\n                        log.info("Compensating step: {}", completed.getName());\n                        completed.compensate(context);\n                    } catch (Exception ce) {\n                        log.error("Compensation failed for {}: {}",\n                            completed.getName(), ce.getMessage());\n                        // Записать в Dead Letter для ручной обработки\n                    }\n                }\n                throw new SagaFailedException(step.getName(), e);\n            }\n        }\n    }\n}' },
        { type: 'warning', value: 'Компенсация может упасть тоже! Нужен механизм retry для компенсаций и Dead Letter Queue для случаев когда компенсация невозможна (требует ручного вмешательства).' }
      ]
    },
    {
      id: 5,
      title: 'Saga с Axon Framework',
      type: 'theory',
      content: [
        { type: 'text', value: 'Axon Framework — Java-фреймворк для реализации CQRS и Event Sourcing с встроенной поддержкой Saga. Упрощает создание orchestration-based Saga с автоматическим управлением состоянием.' },
        { type: 'code', language: 'java', value: '// Axon Framework Saga\n@Saga\npublic class CreateOrderSaga {\n\n    private transient CommandGateway commandGateway;\n\n    @SagaEventHandler(associationProperty = "orderId")\n    @StartSaga\n    public void on(OrderCreatedEvent event) {\n        // Saga начинается при создании заказа\n        // Отправляем команду на резервирование\n        commandGateway.send(new ReserveInventoryCommand(\n            event.getOrderId(), event.getItems()))\n            .exceptionally(e -> {\n                // Компенсация при ошибке\n                commandGateway.send(\n                    new CancelOrderCommand(event.getOrderId(), e.getMessage()));\n                return null;\n            });\n    }\n\n    @SagaEventHandler(associationProperty = "orderId")\n    public void on(InventoryReservedEvent event) {\n        // Товар зарезервирован -> обрабатываем оплату\n        commandGateway.send(new ProcessPaymentCommand(\n            event.getOrderId(), event.getTotalAmount()))\n            .exceptionally(e -> {\n                // Компенсация: отмена резерва\n                commandGateway.send(\n                    new CancelInventoryReservationCommand(event.getOrderId()));\n                commandGateway.send(\n                    new CancelOrderCommand(event.getOrderId(), e.getMessage()));\n                return null;\n            });\n    }\n\n    @SagaEventHandler(associationProperty = "orderId")\n    public void on(PaymentProcessedEvent event) {\n        // Оплата прошла -> подтверждаем заказ\n        commandGateway.send(new ConfirmOrderCommand(event.getOrderId()));\n    }\n\n    @SagaEventHandler(associationProperty = "orderId")\n    @EndSaga\n    public void on(OrderConfirmedEvent event) {\n        log.info("Saga завершена: заказ {} подтверждён", event.getOrderId());\n        // Saga автоматически завершается\n    }\n\n    @SagaEventHandler(associationProperty = "orderId")\n    @EndSaga\n    public void on(OrderCancelledEvent event) {\n        log.info("Saga завершена: заказ {} отменён", event.getOrderId());\n    }\n}' },
        { type: 'tip', value: 'Axon Framework сохраняет состояние Saga в БД. При рестарте приложения Saga продолжит с того места, где остановилась. Это важно для долгих Saga (например, ожидание подтверждения оплаты).' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Реализация Saga',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте Saga для создания заказа с компенсирующими транзакциями (choreography и orchestration).',
      requirements: [
        'Реализуйте Choreography Saga через Kafka события',
        'Реализуйте Orchestration Saga с SagaExecutor',
        'Определите компенсирующие транзакции для каждого шага',
        'Обработайте сценарий: оплата не прошла -> откат резерва и заказа',
        'Сохраняйте состояние Saga в БД для recovery',
        'Добавьте таймаут: если шаг не завершился за 30 сек -> компенсация'
      ],
      hint: 'Для choreography используйте Kafka топики с чёткими событиями. Для orchestration создайте SagaStep интерфейс. Состояние Saga сохраняйте в отдельной таблице saga_instances.',
      expectedOutput: 'Choreography:\n1. OrderCreated -> published to order-events\n2. InventoryReserved -> published to inventory-events\n3. PaymentFailed -> published to payment-events\n4. OrderCancelled + InventoryReservationCancelled (компенсация)\n\nOrchestration:\n1. CreateOrder step: OK\n2. ReserveInventory step: OK\n3. ProcessPayment step: FAILED\n4. Compensating ReserveInventory: OK\n5. Compensating CreateOrder: OK\nSaga status: FAILED',
      solution: '// Orchestration Saga\npublic class CreateOrderSagaOrchestrator {\n    private final List<SagaStep<OrderSagaContext>> steps = List.of(\n        new CreateOrderStep(orderService),\n        new ReserveInventoryStep(inventoryClient),\n        new ProcessPaymentStep(paymentClient),\n        new ConfirmOrderStep(orderService)\n    );\n\n    public Order execute(CreateOrderRequest request) {\n        OrderSagaContext context = new OrderSagaContext(request);\n        SagaInstance saga = saveSaga("CreateOrder", "STARTED");\n        List<SagaStep<OrderSagaContext>> completed = new ArrayList<>();\n\n        for (SagaStep<OrderSagaContext> step : steps) {\n            try {\n                step.execute(context);\n                completed.add(step);\n                updateSaga(saga, step.getName() + "_COMPLETED");\n            } catch (Exception e) {\n                updateSaga(saga, "COMPENSATING");\n                Collections.reverse(completed);\n                for (SagaStep<OrderSagaContext> s : completed) {\n                    try {\n                        s.compensate(context);\n                    } catch (Exception ce) {\n                        log.error("Compensation failed: {}", s.getName());\n                        deadLetterQueue.send(saga.getId(), s.getName());\n                    }\n                }\n                updateSaga(saga, "FAILED");\n                throw new SagaFailedException(e);\n            }\n        }\n        updateSaga(saga, "COMPLETED");\n        return context.getOrder();\n    }\n}',
      explanation: 'Saga обеспечивает consistency между микросервисами через последовательность локальных транзакций. Choreography — через события (простой flow). Orchestration — через координатор (сложный flow). Компенсации выполняются в обратном порядке. Состояние Saga сохраняется для recovery при сбоях.'
    }
  ]
}
