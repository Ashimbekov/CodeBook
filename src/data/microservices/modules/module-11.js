export default {
  id: 11,
  title: 'Event Sourcing',
  description: 'Event Sourcing: хранение истории событий вместо текущего состояния, Event Store, проекции, снимки (snapshots), replay событий.',
  lessons: [
    {
      id: 1,
      title: 'Что такое Event Sourcing',
      type: 'theory',
      content: [
        { type: 'text', value: 'Event Sourcing — паттерн, при котором состояние сохраняется как последовательность событий, а не как текущий снимок. Вместо UPDATE в таблице — INSERT нового события. Текущее состояние вычисляется из всех событий.' },
        { type: 'code', language: 'bash', value: '# Традиционный подход (State Sourcing):\n# UPDATE orders SET status = "CONFIRMED" WHERE id = 123;\n# Мы потеряли историю: не знаем когда и кем создан, менялась ли цена\n\n# Event Sourcing:\n# events таблица:\n# | event_id | aggregate_id | type              | data                    | timestamp           |\n# | 1        | order-123    | OrderCreated      | {customer: "John", ...} | 2026-01-01 10:00:00 |\n# | 2        | order-123    | ItemAdded          | {product: "Laptop"}     | 2026-01-01 10:01:00 |\n# | 3        | order-123    | ItemAdded          | {product: "Mouse"}      | 2026-01-01 10:02:00 |\n# | 4        | order-123    | OrderConfirmed     | {confirmedBy: "admin"}  | 2026-01-01 10:05:00 |\n# | 5        | order-123    | PaymentReceived    | {amount: 1500}          | 2026-01-01 10:10:00 |\n# | 6        | order-123    | OrderShipped       | {tracking: "TR123"}     | 2026-01-02 09:00:00 |\n\n# Текущее состояние = replay всех событий:\n# apply(OrderCreated)   -> status=CREATED, customer=John\n# apply(ItemAdded)      -> items=[Laptop]\n# apply(ItemAdded)      -> items=[Laptop, Mouse]\n# apply(OrderConfirmed) -> status=CONFIRMED\n# apply(PaymentReceived) -> paid=true\n# apply(OrderShipped)   -> status=SHIPPED, tracking=TR123\n\n# Преимущества:\n# - Полная история (audit log бесплатно)\n# - Можно восстановить состояние на любой момент времени\n# - Debug: "что случилось с заказом?" — смотри события\n# - Replay: пересчитать всё с нуля (например, после bugfix)' },
        { type: 'tip', value: 'Event Sourcing как бухгалтерская книга: не стираем записи, а добавляем новые. Баланс = сумма всех транзакций. Ошибка? Новая запись "Коррекция", а не стирание старой.' }
      ]
    },
    {
      id: 2,
      title: 'Event Store',
      type: 'theory',
      content: [
        { type: 'text', value: 'Event Store — хранилище событий. Может быть специализированным (EventStoreDB) или обычной БД с append-only таблицей. Ключевые операции: append (добавить событие), read (прочитать все события агрегата).' },
        { type: 'code', language: 'java', value: '// Event Store на PostgreSQL\n@Entity\n@Table(name = "events")\npublic class StoredEvent {\n    @Id\n    @GeneratedValue(strategy = GenerationType.IDENTITY)\n    private Long sequenceNumber;\n\n    private String aggregateId;\n    private String aggregateType;\n    private String eventType;\n\n    @Column(columnDefinition = "jsonb")\n    private String payload;\n\n    private int version;  // Optimistic locking\n    private Instant createdAt;\n}\n\n@Repository\npublic interface EventStoreRepository extends JpaRepository<StoredEvent, Long> {\n\n    // Все события агрегата в порядке\n    List<StoredEvent> findByAggregateIdOrderByVersionAsc(String aggregateId);\n\n    // Проверка версии для optimistic locking\n    boolean existsByAggregateIdAndVersion(String aggregateId, int version);\n}\n\n// Event Store сервис\n@Service\npublic class EventStore {\n    private final EventStoreRepository repository;\n    private final ObjectMapper objectMapper;\n\n    // Сохранение события\n    public void append(String aggregateId, String aggregateType,\n                       DomainEvent event, int expectedVersion) {\n        // Optimistic concurrency check\n        if (repository.existsByAggregateIdAndVersion(aggregateId, expectedVersion)) {\n            throw new ConcurrencyException(\n                "Aggregate " + aggregateId + " version conflict");\n        }\n\n        StoredEvent stored = new StoredEvent();\n        stored.setAggregateId(aggregateId);\n        stored.setAggregateType(aggregateType);\n        stored.setEventType(event.getClass().getSimpleName());\n        stored.setPayload(objectMapper.writeValueAsString(event));\n        stored.setVersion(expectedVersion + 1);\n        stored.setCreatedAt(Instant.now());\n\n        repository.save(stored);\n    }\n\n    // Загрузка всех событий агрегата\n    public List<DomainEvent> loadEvents(String aggregateId) {\n        return repository.findByAggregateIdOrderByVersionAsc(aggregateId)\n            .stream()\n            .map(this::deserialize)\n            .toList();\n    }\n}' },
        { type: 'warning', value: 'Events НИКОГДА не удаляются и не изменяются! Это immutable log. Если нужно "отменить" событие — создайте новое компенсирующее событие (OrderCancelled вместо удаления OrderCreated).' }
      ]
    },
    {
      id: 3,
      title: 'Агрегат с Event Sourcing',
      type: 'theory',
      content: [
        { type: 'text', value: 'Event-sourced агрегат восстанавливает своё состояние из событий. Метод apply() применяет событие к состоянию. При создании — агрегат загружается из Event Store, replay всех событий.' },
        { type: 'code', language: 'java', value: '// Event-Sourced Aggregate\npublic class Order {\n    private UUID id;\n    private UUID customerId;\n    private OrderStatus status;\n    private List<OrderItem> items = new ArrayList<>();\n    private BigDecimal totalAmount = BigDecimal.ZERO;\n    private int version = 0;\n    private List<DomainEvent> uncommittedEvents = new ArrayList<>();\n\n    // Восстановление из событий\n    public static Order fromEvents(List<DomainEvent> events) {\n        Order order = new Order();\n        events.forEach(order::apply);\n        return order;\n    }\n\n    // Команда: создать заказ\n    public static Order create(UUID customerId) {\n        Order order = new Order();\n        order.raiseEvent(new OrderCreatedEvent(\n            UUID.randomUUID(), customerId, Instant.now()));\n        return order;\n    }\n\n    // Команда: добавить товар\n    public void addItem(UUID productId, String name, BigDecimal price, int qty) {\n        if (status != OrderStatus.CREATED) {\n            throw new IllegalStateException("Нельзя добавить товар в " + status);\n        }\n        raiseEvent(new ItemAddedEvent(id, productId, name, price, qty));\n    }\n\n    // Команда: подтвердить заказ\n    public void confirm() {\n        if (items.isEmpty()) throw new IllegalStateException("Заказ пуст");\n        if (status != OrderStatus.CREATED)\n            throw new IllegalStateException("Неверный статус");\n        raiseEvent(new OrderConfirmedEvent(id, Instant.now()));\n    }\n\n    // Apply — применение событий к состоянию\n    private void apply(DomainEvent event) {\n        switch (event) {\n            case OrderCreatedEvent e -> {\n                this.id = e.orderId();\n                this.customerId = e.customerId();\n                this.status = OrderStatus.CREATED;\n            }\n            case ItemAddedEvent e -> {\n                this.items.add(new OrderItem(\n                    e.productId(), e.name(), e.price(), e.quantity()));\n                this.totalAmount = totalAmount.add(\n                    e.price().multiply(BigDecimal.valueOf(e.quantity())));\n            }\n            case OrderConfirmedEvent e -> {\n                this.status = OrderStatus.CONFIRMED;\n            }\n            default -> throw new IllegalArgumentException("Unknown: " + event);\n        }\n        this.version++;\n    }\n\n    private void raiseEvent(DomainEvent event) {\n        apply(event);  // Применяем к текущему состоянию\n        uncommittedEvents.add(event);  // Запоминаем для сохранения\n    }\n\n    public List<DomainEvent> getUncommittedEvents() {\n        return Collections.unmodifiableList(uncommittedEvents);\n    }\n}' },
        { type: 'note', value: 'Два типа методов: raiseEvent() — генерирует новое событие (для команд), apply() — применяет событие к состоянию (для восстановления). Бизнес-правила проверяются в командах ДО генерации события.' }
      ]
    },
    {
      id: 4,
      title: 'Проекции (Read Models)',
      type: 'theory',
      content: [
        { type: 'text', value: 'Проекция — Read Model, построенная из событий. Каждая проекция оптимизирована для конкретного запроса. Можно иметь множество проекций из одного потока событий.' },
        { type: 'code', language: 'java', value: '// Проекция 1: OrderSummaryProjection (для списка заказов)\n@Component\npublic class OrderSummaryProjection {\n    private final OrderSummaryRepository repository;\n\n    @EventHandler\n    public void on(OrderCreatedEvent event) {\n        repository.save(new OrderSummary(\n            event.orderId().toString(),\n            event.customerId().toString(),\n            "CREATED", BigDecimal.ZERO, 0, event.createdAt()));\n    }\n\n    @EventHandler\n    public void on(ItemAddedEvent event) {\n        repository.findById(event.orderId().toString()).ifPresent(summary -> {\n            summary.setItemCount(summary.getItemCount() + event.quantity());\n            summary.setTotalAmount(summary.getTotalAmount()\n                .add(event.price().multiply(BigDecimal.valueOf(event.quantity()))));\n            repository.save(summary);\n        });\n    }\n\n    @EventHandler\n    public void on(OrderConfirmedEvent event) {\n        repository.findById(event.orderId().toString()).ifPresent(summary -> {\n            summary.setStatus("CONFIRMED");\n            repository.save(summary);\n        });\n    }\n}\n\n// Проекция 2: CustomerOrderStatsProjection (аналитика)\n@Component\npublic class CustomerOrderStatsProjection {\n    private final StatsRepository statsRepository;\n\n    @EventHandler\n    public void on(OrderConfirmedEvent event) {\n        statsRepository.incrementOrderCount(event.customerId());\n    }\n\n    @EventHandler\n    public void on(PaymentReceivedEvent event) {\n        statsRepository.addRevenue(event.customerId(), event.amount());\n    }\n}\n\n// Проекция 3: Elasticsearch для полнотекстового поиска\n// Проекция 4: Redis кэш для горячих данных\n// Проекция 5: Отчёты для бизнеса\n\n// Пересборка проекции:\n// 1. Очистить Read Model\n// 2. Прочитать ВСЕ события из Event Store\n// 3. Применить каждое событие к проекции\n// Это позволяет менять структуру Read Model без миграций!' },
        { type: 'tip', value: 'Пересборка проекции — мощная возможность. Нашли баг в проекции? Исправили код, пересобрали — все данные корректны. Нужна новая аналитика? Создали новую проекцию и применили ко всей истории.' }
      ]
    },
    {
      id: 5,
      title: 'Snapshots и оптимизация',
      type: 'theory',
      content: [
        { type: 'text', value: 'Snapshot (снимок) — периодическое сохранение текущего состояния агрегата. Позволяет восстановить агрегат не из всех событий, а от последнего снимка. Необходим для агрегатов с большим количеством событий.' },
        { type: 'code', language: 'java', value: '// Snapshot — периодический снимок состояния\n@Entity\n@Table(name = "snapshots")\npublic class Snapshot {\n    @Id\n    private String aggregateId;\n    private String aggregateType;\n    @Column(columnDefinition = "jsonb")\n    private String state;       // Сериализованное состояние\n    private int version;         // Версия на момент снимка\n    private Instant createdAt;\n}\n\n// Оптимизированная загрузка агрегата\n@Service\npublic class OrderRepository {\n    private final EventStore eventStore;\n    private final SnapshotStore snapshotStore;\n\n    public Order load(String orderId) {\n        // 1. Ищем последний snapshot\n        Optional<Snapshot> snapshot = snapshotStore.findLatest(orderId);\n\n        Order order;\n        int fromVersion;\n\n        if (snapshot.isPresent()) {\n            // Восстанавливаем из snapshot\n            order = deserialize(snapshot.get().getState());\n            fromVersion = snapshot.get().getVersion();\n        } else {\n            order = new Order();\n            fromVersion = 0;\n        }\n\n        // 2. Дочитываем события ПОСЛЕ snapshot\n        List<DomainEvent> events = eventStore.loadEventsAfterVersion(\n            orderId, fromVersion);\n        events.forEach(order::apply);\n\n        return order;\n    }\n\n    public void save(Order order) {\n        // Сохраняем новые события\n        for (DomainEvent event : order.getUncommittedEvents()) {\n            eventStore.append(order.getId().toString(), "Order\",\n                event, order.getVersion());\n        }\n\n        // Создаём snapshot каждые 100 событий\n        if (order.getVersion() % 100 == 0) {\n            snapshotStore.save(new Snapshot(\n                order.getId().toString(), \"Order\",\n                serialize(order), order.getVersion()));\n        }\n    }\n}\n\n// Без snapshot: загрузка заказа с 10000 событиями = 10000 apply()\n// С snapshot каждые 100: максимум 100 apply() после snapshot\n// Ускорение: ~100x' },
        { type: 'warning', value: 'При изменении структуры агрегата старые snapshots могут стать невалидными. Решения: версионирование snapshots, миграция, или пересоздание snapshots из событий.' }
      ]
    },
    {
      id: 6,
      title: 'Практика: Event Sourcing заказов',
      type: 'practice',
      difficulty: 'hard',
      description: 'Реализуйте Event Sourcing для Order агрегата с Event Store, проекциями и snapshots.',
      requirements: [
        'Создайте Event Store на PostgreSQL (таблица events с jsonb)',
        'Реализуйте Order агрегат с методами create, addItem, confirm, cancel',
        'Реализуйте replay: восстановление состояния из событий',
        'Создайте две проекции: OrderSummary и OrderStats',
        'Добавьте Snapshot каждые 50 событий',
        'Реализуйте команду пересборки проекций из всех событий'
      ],
      hint: 'Используйте jsonb в PostgreSQL для хранения событий. Для replay загружайте события по aggregateId. Snapshot сохраняйте в отдельную таблицу. Пересборка: TRUNCATE read table, SELECT * FROM events ORDER BY sequence_number.',
      expectedOutput: 'Event Store: 6 событий для order-123.\nReplay: Order.fromEvents() -> status=CONFIRMED, items=2, total=1500.\nOrderSummary проекция: {id: order-123, status: CONFIRMED, total: 1500}.\nOrderStats проекция: {customer-456: totalOrders=1, totalSpent=1500}.\nSnapshot: создан на версии 50, загрузка из snapshot + 6 новых событий.\nПересборка проекций: все Read Models обновлены из 100 событий.',
      solution: '// Event Store\n@Service\npublic class PostgresEventStore {\n    private final JdbcTemplate jdbc;\n\n    public void append(String aggregateId, DomainEvent event, int version) {\n        jdbc.update(\n            "INSERT INTO events (aggregate_id, event_type, payload, version, created_at) VALUES (?, ?, ?::jsonb, ?, now())",\n            aggregateId, event.getClass().getSimpleName(),\n            toJson(event), version);\n    }\n\n    public List<DomainEvent> load(String aggregateId) {\n        return jdbc.query(\n            "SELECT * FROM events WHERE aggregate_id = ? ORDER BY version",\n            (rs, i) -> deserialize(rs.getString("event_type"), rs.getString("payload")),\n            aggregateId);\n    }\n}\n\n// Order Aggregate\npublic class Order {\n    // ... (как в теории)\n    public static Order fromEvents(List<DomainEvent> events) {\n        Order order = new Order();\n        events.forEach(order::apply);\n        return order;\n    }\n}\n\n// Snapshot\npublic void save(Order order) {\n    eventStore.append(order.getId(), events, order.getVersion());\n    if (order.getVersion() % 50 == 0) {\n        snapshotStore.save(order.getId(), serialize(order), order.getVersion());\n    }\n}\n\n// Пересборка проекций\npublic void rebuildProjections() {\n    orderSummaryRepository.deleteAll();\n    eventStore.loadAll().forEach(event -> projection.handle(event));\n}',
      explanation: 'Event Sourcing хранит полную историю изменений. Текущее состояние = replay всех событий. Проекции создают оптимизированные Read Models из событий. Snapshots ускоряют загрузку агрегатов с большой историей. Пересборка проекций позволяет исправить баги или создать новые модели из исторических данных.'
    }
  ]
}
